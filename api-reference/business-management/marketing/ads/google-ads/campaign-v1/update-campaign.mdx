---
title: "Update Campaign"
description: "Updates the fields of an existing campaign."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates the fields of an existing campaign.
    Only fields included in updateMask are modified.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-GOOGLE-ADS">Manage google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/_serverless/pa-google/v1/campaigns/{campaign.id}
    ```

    ## Path Params

    <ParamField path="campaign.id" type="string" required>

      Unique campaign ID.

    </ParamField>

    ## Body Params

    <ParamField body="campaign" type="Campaign" required>

      The campaign containing updated field values. The id and accountId fields are required.

      See the [Campaign Object](/api-reference/business-management/marketing/ads/google-ads/campaign-v1/campaign-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="campaign" type="Campaign">

      The updated campaign.

      See the [Campaign Object](/api-reference/business-management/marketing/ads/google-ads/campaign-v1/campaign-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CAMPAIGN_NOT_FOUND` | Couldn't find the campaign. |
    | `400` | `INVALID_ARGUMENT` | `RESTRICTED_LOCATION` |  |
    | `400` | `INVALID_ARGUMENT` | `CAMPAIGN_DAILY_BUDGET_TOO_HIGH` |  |
    | `400` | `INVALID_ARGUMENT` | `INVALID_ASSET_GROUP_ASSETS` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_IMAGE_ASSETS` |  |
    | `400` | `INVALID_ARGUMENT` | `SEARCH_THEME_POLICY_VIOLATION` |  |
    | `400` | `INVALID_ARGUMENT` | `INVALID_IMAGE_FORMAT` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_ASSET` |  |
    | `400` | `INVALID_ARGUMENT` | `POLICY_VIOLATION` |  |
    | `428` | `FAILED_PRECONDITION` | `ACCOUNT_NOT_ENABLED` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_CAMPAIGN_NAME` |  |
    | `400` | `INVALID_ARGUMENT` | `NOT_ENOUGH_ASSETS` |  |
    | `400` | `INVALID_ARGUMENT` | `GOOGLE_ADS_API_ERROR` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/_serverless/pa-google/v1/campaigns/c9d8e7f6-a5b4-3210-fedc-ba9876543210' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "campaign": {
            "id": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
            "name": "Summer Bakery Promotion",
            "budget": {
              "amountMicros": "20000000"
            }
          }
        }'
      ```

      ```json Response
      {
        "campaign": {
          "id": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
          "accountId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "campaignType": "SMART",
          "status": "LIVE",
          "name": "Summer Bakery Promotion",
          "budget": {
            "amountMicros": "20000000",
            "resourceName": "customers/1234567890/campaignBudgets/9876543210"
          },
          "locations": [
            {
              "location": { "geoTargetConstant": "geoTargetConstants/1023191" },
              "displayName": "New York, New York"
            }
          ],
          "smartCampaign": {
            "keywordThemes": [
              { "freeFormKeywordTheme": "bakery", "displayName": "bakery" }
            ],
            "businessName": "Sunrise Bakery",
            "url": "https://example.com",
            "languageCode": "en"
          },
          "reportingKey": "spring-bakery-promotion-2024-3-15-c9d8e7f6",
          "resourceName": "customers/1234567890/campaigns/9876543210",
          "createdDate": "2024-03-15T10:30:00.000Z",
          "updatedDate": "2024-04-01T09:00:00.000Z",
          "actionDate": "2024-03-20T08:00:00.000Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates the fields of an existing campaign.
    Only fields included in updateMask are modified.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-GOOGLE-ADS">Manage google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { campaigns } from '@wix/promote-google-ads';

    campaigns.updateCampaign(_id, campaign): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Unique campaign ID.

      **Read-only.**

    </ParamField>

    <ParamField body="campaign" type="UpdateCampaign" required>

      The campaign containing updated field values. The id and accountId fields are required.

      <Expandable title="campaign properties">

        <ParamField body="_id" type="string">

          Unique campaign ID.

          **Read-only.**

        </ParamField>

        <ParamField body="accountId" type="string" required>

          Wix internal account ID associated with this campaign.

        </ParamField>

        <ParamField body="campaignType" type="string">

          Type of the campaign, either Smart or Performance Max.

          **Supported values:**

          - `SMART` — Simplified auto-managed campaign. Google handles bidding and ad delivery based on keyword themes and geo targets.
          Creating new campaigns of this type is deprecated. Support for it will be removed in a future release, so use
          PERFORMANCE_MAX or PERFORMANCE_MAX_LEADS instead. Existing campaigns of this type aren't affected.
          - `PERFORMANCE_MAX` — Goal-based campaign that runs across all Google channels (Search, Display, YouTube, etc.) using asset groups.
          - `PERFORMANCE_MAX_LEADS` — Performance Max variant optimized for lead generation, such as phone calls and form fills.

        </ParamField>

        <ParamField body="status" type="string">

          Current campaign status, synced from Google Ads on each read.

          **Read-only.**

          **Supported values:**

          - `LIVE` — Campaign is currently serving ads, depending on budget information.
          - `PAUSED` — Campaign has been paused by the user.
          - `ENDED` — Campaign has been ended.
          - `IN_REVIEW` — The campaign was created but its budget hasn't been charged yet.
          - `ERROR` — Error state.
          - `DISAPPROVED` — Campaign is not approved.
          - `NOT_SERVING` — Campaign is currently not serving due to budget limitations.
          - `DRAFT` — Campaign is a draft.
          - `LEARNING` — Campaign is in the learning phase. Currently supported only for Google Ads Performance Max leads campaigns.

        </ParamField>

        <ParamField body="resourceName" type="string">

          Google Ads resource name for this campaign, e.g. customers/&#123;customerId&#125;/campaigns/&#123;campaignId&#125;.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Display name of the campaign.

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the campaign was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the campaign was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="actionDate" type="datetime">

          Date and time of the most recent lifecycle action (pause, resume, launch, etc.).

          **Read-only.**

        </ParamField>

        <ParamField body="budget" type="CampaignBudget">

          Daily budget for the campaign.

          <Expandable title="budget properties">

            <ParamField body="resourceName" type="string">

              Google Ads resource name for the campaign budget.

              **Read-only.**

            </ParamField>

            <ParamField body="amountMicros" type="string">

              Daily budget amount in micros using the account's local currency.
              1,000,000 micros equals 1 currency unit (e.g. 6,400,000 = $6.40).
              Google caps monthly spend at 30.4 times this amount.
              Minimum is approximately $6.40 USD for standard subscribers; maximum is approximately $127.60 USD standard, or up to $1,500 for high-spend accounts.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="locations" type="LocationCriterion[]">

          Geographic locations targeted by this campaign.

          Constraints: `maxItems 1000`.

          <Expandable title="locations properties">

            <ParamField body="resourceName" type="string">

              Google Ads campaign criterion resource name.
              Format: customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ParamField>

            <ParamField body="location" type="Location">

              The location being targeted.

              _Nested structure omitted for brevity — see the `Location` object._

            </ParamField>

            <ParamField body="displayName" type="string">

              Human-readable location name shown in the UI (e.g. "New York, New York").

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="adSchedule" type="WeeklyAdSchedule">

          Weekly ad schedule defining the days and hours when ads can run.

          <Expandable title="adSchedule properties">

            <ParamField body="adShowAnytime" type="boolean">

              Whether ads show at any time during the day. When `true`, the daily schedules below are ignored.

            </ParamField>

            <ParamField body="sunday" type="DailyAdSchedule">

              Sunday ad show range.

              _Nested structure omitted for brevity — see the `DailyAdSchedule` object._

            </ParamField>

            <ParamField body="monday" type="DailyAdSchedule">

              Monday ad show range.

              _Nested structure omitted for brevity — see the `DailyAdSchedule` object._

            </ParamField>

            <ParamField body="tuesday" type="DailyAdSchedule">

              Tuesday ad show range.

              _Nested structure omitted for brevity — see the `DailyAdSchedule` object._

            </ParamField>

            <ParamField body="wednesday" type="DailyAdSchedule">

              Wednesday ad show range.

              _Nested structure omitted for brevity — see the `DailyAdSchedule` object._

            </ParamField>

            <ParamField body="thursday" type="DailyAdSchedule">

              Thursday ad show range.

              _Nested structure omitted for brevity — see the `DailyAdSchedule` object._

            </ParamField>

            <ParamField body="friday" type="DailyAdSchedule">

              Friday ad show range.

              _Nested structure omitted for brevity — see the `DailyAdSchedule` object._

            </ParamField>

            <ParamField body="saturday" type="DailyAdSchedule">

              Saturday ad show range.

              _Nested structure omitted for brevity — see the `DailyAdSchedule` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="reportingKey" type="string">

          Value used as the utm_campaign parameter for conversion tracking.

          **Read-only.**

        </ParamField>

        <ParamField body="campaignBrief" type="CampaignBrief">

          The brief used to generate this campaign.
          Only present on campaigns created via the GetCampaignSuggestions endpoint of SuggestionsService.

          <Expandable title="campaignBrief properties">

            <ParamField body="_id" type="string">

              Campaign brief ID (Wix internal)

              **Read-only.**

            </ParamField>

            <ParamField body="platformType" type="string">

              The type of platform the campaign will be created on (Google/FB)

              **Read-only.**

              **Supported values:**

              - `GOOGLE`
              - `FACEBOOK`

            </ParamField>

            <ParamField body="goal" type="string">

              Goal of the campaign

            </ParamField>

            <ParamField body="audience" type="string">

              Audience the campaign should target

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="campaign" type="campaign">

          campaign

          <Expandable title="campaign properties">

            <ParamField body="smartCampaign" type="SmartCampaign">

              Smart Campaign details. Present when campaignType is SMART.
              Creating new Smart Campaigns is deprecated. Support for it will be removed in a future release, so use
              PERFORMANCE_MAX or PERFORMANCE_MAX_LEADS instead. Existing Smart Campaigns aren't affected.

              _Nested structure omitted for brevity — see the `SmartCampaign` object._

            </ParamField>

            <ParamField body="performanceMaxCampaign" type="PerformanceMaxCampaign">

              Performance Max campaign details. Present when campaignType is PERFORMANCE_MAX or PERFORMANCE_MAX_LEADS.

              _Nested structure omitted for brevity — see the `PerformanceMaxCampaign` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Campaign`.

    See the [Campaign Object](/api-reference/business-management/marketing/ads/google-ads/campaign-v1/campaign-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CAMPAIGN_NOT_FOUND` | Couldn't find the campaign. |
    | `400` | `INVALID_ARGUMENT` | `RESTRICTED_LOCATION` |  |
    | `400` | `INVALID_ARGUMENT` | `CAMPAIGN_DAILY_BUDGET_TOO_HIGH` |  |
    | `400` | `INVALID_ARGUMENT` | `INVALID_ASSET_GROUP_ASSETS` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_IMAGE_ASSETS` |  |
    | `400` | `INVALID_ARGUMENT` | `SEARCH_THEME_POLICY_VIOLATION` |  |
    | `400` | `INVALID_ARGUMENT` | `INVALID_IMAGE_FORMAT` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_ASSET` |  |
    | `400` | `INVALID_ARGUMENT` | `POLICY_VIOLATION` |  |
    | `428` | `FAILED_PRECONDITION` | `ACCOUNT_NOT_ENABLED` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_CAMPAIGN_NAME` |  |
    | `400` | `INVALID_ARGUMENT` | `NOT_ENOUGH_ASSETS` |  |
    | `400` | `INVALID_ARGUMENT` | `GOOGLE_ADS_API_ERROR` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Campaign Updated | `wix.promote_paid_ads.google_ads.v1.campaign_updated` | Triggered when a campaign field is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-campaign" id="ex-sdk-update-campaign-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-campaign" id="ex-sdk-update-campaign-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-campaign-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-campaign-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a campaign's name and daily budget</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-campaign-0"><span className="wix-picker-name">Update a campaign's name and daily budget</span></label>
    <label htmlFor="ex-sdk-update-campaign-1"><span className="wix-picker-name">updateCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a campaign's name and daily budget
    import { campaigns } from '@wix/promote-google-ads';

    const response = await campaigns.updateCampaign({
      id: 'c9d8e7f6-a5b4-3210-fedc-ba9876543210',
      name: 'Summer Bakery Promotion',
      budget: {
        amountMicros: '20000000',
      },
    });

    /* Promise resolves to:
     * {
     *   "_id": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
     *   "campaignType": "SMART",
     *   "status": "LIVE",
     *   "name": "Summer Bakery Promotion",
     *   "budget": { "amountMicros": "20000000" },
     *   "reportingKey": "spring-bakery-promotion-2024-3-15-c9d8e7f6",
     *   "_createdDate": "2024-03-15T10:30:00.000Z",
     *   "_updatedDate": "2024-04-01T09:00:00.000Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCampaign (self-hosted)
    import { createClient } from '@wix/sdk';
    import { campaigns } from '@wix/promote-google-ads';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { campaigns },
      // Include the auth strategy and host as relevant
    });

    async function updateCampaign(_id,campaign) {
      const response = await myWixClient.campaigns.updateCampaign(_id,campaign);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
