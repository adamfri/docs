---
title: "Remove Reply"
description: "Removes reply from a review."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    Removes reply from a review.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REVIEWS.MANAGE-REVIEWS">Manage Reviews</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/reviews/v1/reviews/{reviewId}/reply
    ```

    ## Path Params

    <ParamField path="reviewId" type="string" required>

      ID of review to remove the reply for.

    </ParamField>

    ## Response Object

    <ResponseField name="review" type="Review">

      Updated review.

      See the [Review Object](/api-reference/crm/community/feedback-moderation/reviews/reviews/review-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Review Updated | `wix.reviews.v1.review_updated` | Triggered when a review is updated. |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes reply from a review.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REVIEWS.MANAGE-REVIEWS">Manage Reviews</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { reviews } from '@wix/reviews';

    reviews.removeReply(reviewId): Promise<RemoveReplyResponse>
    ```

    ## Method Parameters

    <ParamField body="reviewId" type="string" required>

      ID of review to remove the reply for.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<RemoveReplyResponse>`.

    <ResponseField name="review" type="Review">

      Updated review.

      See the [Review Object](/api-reference/crm/community/feedback-moderation/reviews/reviews/review-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Review Updated | `wix.reviews.v1.review_updated` | Triggered when a review is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-reply" id="ex-sdk-remove-reply-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-reply" id="ex-sdk-remove-reply-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-remove-reply" id="ex-sdk-remove-reply-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-reply-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-reply-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">removeReply</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeReply (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeReply (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-reply-0"><span className="wix-picker-name">removeReply</span></label>
    <label htmlFor="ex-sdk-remove-reply-1"><span className="wix-picker-name">removeReply (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-remove-reply-2"><span className="wix-picker-name">removeReply (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript removeReply
    import { reviews } from '@wix/reviews';

    async function removeReply(reviewId) {
      const response = await reviews.removeReply(reviewId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeReply (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reviews } from '@wix/reviews';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reviews },
      // Include the auth strategy and host as relevant
    });

    async function removeReply(reviewId) {
      const response = await myWixClient.reviews.removeReply(reviewId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeReply (with elevated permissions)
    import { reviews } from '@wix/reviews';
    import { auth } from '@wix/essentials';

    async function myRemoveReplyMethod(reviewId) {
      const elevatedRemoveReply = auth.elevate(reviews.removeReply);
      const response = await elevatedRemoveReply(reviewId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
