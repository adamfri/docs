---
title: "Create Reservation"
description: "Creates a new reservation."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new reservation.

    `Create Reservation` accepts and requires different fields depending on the `status` and `source` specified, and the permissions of the [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) you're authenticated as.

    ### Status and source

    If a `status` is not specified, it will be set to:
    * `RESERVED` if manual approval is not required for confirmation
    * `REQUESTED` if manual approval is required for confirmation.

    A reservation created with any `source` other than `WALK_IN` requires the `reservation.reservee.phone` and `reservation.reservee.firstName` fields.
    Attempting to create a reservation without these fields results in an error.

    ### Permissions

    The fields `status`, `source`, `reservation.details.tableIds`, `reservation.details.tables.ids`, `reservation.details.endDate`, `ignoreReservationLocationConflicts`, `ignoreTableCombinationConflicts`, and `ignoreConflicts` should only be specified if you're authenticated as an [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) with the `MANAGE RESERVATIONS (FULL)` permission scope.
    Specifying these fields without the required permissions results in an application error.

    If `source` is not specified, its value is set depending on the permissions of the permissions of the [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) you're authenticated as.
    If the identity has the `MANAGE RESERVATIONS (FULL)` permission scope the `source` is set to `UNDEFINED`. Otherwise, the `source` is set to `ONLINE`.

    <Note>

    `Create Reservation` requires all details of the reservation upfront. The process of creating a reservation can be broken up using `Create Held Reservation`. `Create Held Reservation` creates a temporary reservation that expires automatically unless it is completed with the addition of more details using `Reserve Reservation`.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-RESERVATIONS-BASIC">Manage Reservations (Basic)</span>
    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-RESERVATIONS-FULL">Manage Reservations (Full)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/table-reservations/reservations/v1/reservations
    ```

    ## Body Params

    <ParamField body="reservation" type="Reservation" required>

      Reservation details.

      See the [Reservation Object](/api-reference/business-solutions/restaurants/reservations/reservations/reservation-object) for this object's fields.

    </ParamField>

    <ParamField body="force" type="boolean" deprecated>

      If creation is forced.

      **Deprecated.** If creation is forced. Use `ignore_table_combination_conflicts` instead. Removal: 2023-11-01.

    </ParamField>

    <ParamField body="ignoreTableCombinationConflicts" type="string[]" deprecated>

      Ignore table combination conflicts of the types specified in the array. This ensures that the reservation is created even if the given conflicts would normally prevent it.

      Possible values:
      * `"RESERVED"`: One or more of the chosen tables are already reserved.
      * `"TOO_BIG"`: The party is too big for the selected table.
      * `"TOO_SMALL"`: The party is too small for the selected table.
      * `"OFFLINE_ONLY"`: The restaurant does not allow online reservations.

      **Deprecated.** Use `ignore_conflicts` instead. Removal: 2025-12-05. Constraints: `maxItems 10`.

      **Supported values:**

      - `RESERVED` — One or more of the chosen tables are already reserved.
      - `TOO_BIG` — The party is too big for the selected table.
      - `TOO_SMALL` — The party is too small for the selected table.
      - `OFFLINE_ONLY` — The restaurant does not allow online reservations.
      - `RESERVED_FOR_EXPERIENCE` — The table is reserved for an active experience.
      - `TABLE_NOT_IN_AREA` — The table is not in the requested area.

    </ParamField>

    <ParamField body="ignoreReservationLocationConflicts" type="string[]" deprecated>

      Ignored reservation location conflicts of the types specified in the array. This ensures that the reservation is created even if the given conflicts would normally prevent it.

      Possible values:
      * `"PARTY_PACING"`: The restaurant cannot accommodate a party of the given size according to party pacing settings.
      * `"SEAT_PACING"`: The required number of seats are unavailable according to seat pacing settings.

      **Deprecated.** Use `ignore_conflicts` instead. Removal: 2025-12-05. Constraints: `maxItems 10`.

      **Supported values:**

      - `PARTY_PACING` — The restaurant cannot accommodate a party of the given size according to party pacing settings.
      - `SEAT_PACING` — The required number of seats are unavailable according to seat pacing settings.

    </ParamField>

    <ParamField body="ignoreConflicts" type="IgnoreConflicts">

      Conflicts to ignore.

      <Expandable title="ignoreConflicts properties">

        <ParamField body="ignoreConflictsType" type="string">

          Types of conflicts to ignore.

          **Supported values:**

          - `STANDARD`
          - `EXPERIENCE`

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="ignoreConflictsOptions" type="ignoreConflictsOptions">

          ignoreConflictsOptions

          <Expandable title="ignoreConflictsOptions properties">

            <ParamField body="standardOptions" type="StandardOptions">

              Conflicts to ignore for standard reservations.

              _Nested structure omitted for brevity — see the `StandardOptions` object._

            </ParamField>

            <ParamField body="experienceOptions" type="ExperienceOptions">

              Conflicts to ignore for experiences.

              _Nested structure omitted for brevity — see the `ExperienceOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="reservation" type="Reservation">

      Reservation.

      See the [Reservation Object](/api-reference/business-solutions/restaurants/reservations/reservations/reservation-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `TIME_NOT_AVAILABLE` | The requested time slot isn't available due to conflicts. |
    | `400` | `INVALID_ARGUMENT` | `RESERVATION_VIOLATION` | Reservation data violates validation rules. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reservation Created | `wix.table_reservations.v1.reservation_created` | Triggered when a reservation is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/table-reservations/reservations/v1/reservations' \
          -H 'Authorization: <AUTH>'
          --data-binary '{
           "reservation": {
               "details": {
                   "reservationLocationId": "28bca9ee-e5b9-4081-9f45-a6dded8593a1",
                   "tables": {
                      "ids": ["c215fb2a-0eb9-43d9-b7a7-8957e24c66e7"]
                   },
                   "startDate": "2023-07-22T12:30:00Z",
                   "endDate": "2023-07-22T14:00:00Z",
                   "partySize": 2
               },
               "reservee": {
                   "firstName": "John",
                   "lastName": "Doe",
                   "email": "john.doe@mail.com",
                   "phone": "+38055325555",
                   "marketingConsent": false
               },
               "status": "RESERVED"
           }
         }'
      ```

      ```json Response
      {
          "reservation": {
              "id": "09b9ed74-ce92-4102-82dd-24c1c4941c3c",
              "status": "RESERVED",
              "details": {
                  "reservationLocationId": "28bca9ee-e5b9-4081-9f45-a6dded8593a1",
                  "tables": {
                      "ids": ["c215fb2a-0eb9-43d9-b7a7-8957e24c66e7"]
                   },
                  "startDate": "2023-07-22T12:30:00Z",
                  "endDate": "2023-07-22T14:00:00Z",
                  "partySize": 2
              },
              "reservee": {
                  "firstName": "John",
                  "lastName": "Doe",
                  "email": "john.doe@mail.com",
                  "phone": "+38055325555",
                  "marketingConsent": false,
                  "contactId": "91c03323-c8bb-41e1-967c-bbf182e99be4"
              },
              "reservedBy": {
                  "contactId": "0274f02a-2170-452f-bfe8-452aacc70d0a"
              },
              "createdDate": "2023-04-06T14:52:13.915Z",
              "updatedDate": "2023-04-06T14:52:13.915Z",
              "revision": "1"
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new reservation.

    `Create Reservation` accepts and requires different fields depending on the `status` and `source` specified, and the permissions of the [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) you're authenticated as.

    ### Status and source

    If a `status` is not specified, it will be set to:
    * `RESERVED` if manual approval is not required for confirmation
    * `REQUESTED` if manual approval is required for confirmation.

    A reservation created with any `source` other than `WALK_IN` requires the `reservation.reservee.phone` and `reservation.reservee.firstName` fields.
    Attempting to create a reservation without these fields results in an error.

    ### Permissions

    The fields `status`, `source`, `reservation.details.tableIds`, `reservation.details.tables.ids`, `reservation.details.endDate`, `ignoreReservationLocationConflicts`, `ignoreTableCombinationConflicts`, and `ignoreConflicts` should only be specified if you're authenticated as an [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) with the `MANAGE RESERVATIONS (FULL)` permission scope.
    Specifying these fields without the required permissions results in an application error.

    If `source` is not specified, its value is set depending on the permissions of the permissions of the [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) you're authenticated as.
    If the identity has the `MANAGE RESERVATIONS (FULL)` permission scope the `source` is set to `UNDEFINED`. Otherwise, the `source` is set to `ONLINE`.

    <Note>

    `Create Reservation` requires all details of the reservation upfront. The process of creating a reservation can be broken up using `Create Held Reservation`. `Create Held Reservation` creates a temporary reservation that expires automatically unless it is completed with the addition of more details using `Reserve Reservation`.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-RESERVATIONS-BASIC">Manage Reservations (Basic)</span>
    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-RESERVATIONS-FULL">Manage Reservations (Full)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { reservations } from '@wix/table-reservations';

    reservations.createReservation(reservation, options): Promise<Reservation>
    ```

    ## Method Parameters

    <ParamField body="reservation" type="Reservation" required>

      Reservation details.

      See the [Reservation Object](/api-reference/business-solutions/restaurants/reservations/reservations/reservation-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="CreateReservationOptions">

      Options for creating the reservation.

      <Expandable title="options properties">

        <ParamField body="force" type="boolean" deprecated>

          If creation is forced.

          **Deprecated.** If creation is forced. Use `ignore_table_combination_conflicts` instead. Removal: 2023-11-01.

        </ParamField>

        <ParamField body="ignoreTableCombinationConflicts" type="string[]" deprecated>

          Ignore table combination conflicts of the types specified in the array. This ensures that the reservation is created even if the given conflicts would normally prevent it.

          Possible values:
          * `"RESERVED"`: One or more of the chosen tables are already reserved.
          * `"TOO_BIG"`: The party is too big for the selected table.
          * `"TOO_SMALL"`: The party is too small for the selected table.
          * `"OFFLINE_ONLY"`: The restaurant does not allow online reservations.

          **Deprecated.** Use `ignore_conflicts` instead. Removal: 2025-12-05. Constraints: `maxItems 10`.

          **Supported values:**

          - `RESERVED` — One or more of the chosen tables are already reserved.
          - `TOO_BIG` — The party is too big for the selected table.
          - `TOO_SMALL` — The party is too small for the selected table.
          - `OFFLINE_ONLY` — The restaurant does not allow online reservations.
          - `RESERVED_FOR_EXPERIENCE` — The table is reserved for an active experience.
          - `TABLE_NOT_IN_AREA` — The table is not in the requested area.

        </ParamField>

        <ParamField body="ignoreReservationLocationConflicts" type="string[]" deprecated>

          Ignored reservation location conflicts of the types specified in the array. This ensures that the reservation is created even if the given conflicts would normally prevent it.

          Possible values:
          * `"PARTY_PACING"`: The restaurant cannot accommodate a party of the given size according to party pacing settings.
          * `"SEAT_PACING"`: The required number of seats are unavailable according to seat pacing settings.

          **Deprecated.** Use `ignore_conflicts` instead. Removal: 2025-12-05. Constraints: `maxItems 10`.

          **Supported values:**

          - `PARTY_PACING` — The restaurant cannot accommodate a party of the given size according to party pacing settings.
          - `SEAT_PACING` — The required number of seats are unavailable according to seat pacing settings.

        </ParamField>

        <ParamField body="ignoreConflicts" type="IgnoreConflicts">

          Conflicts to ignore.

          <Expandable title="ignoreConflicts properties">

            <ParamField body="ignoreConflictsType" type="string">

              Types of conflicts to ignore.

              **Supported values:**

              - `STANDARD`
              - `EXPERIENCE`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="ignoreConflictsOptions" type="ignoreConflictsOptions">

              ignoreConflictsOptions

              _Nested structure omitted for brevity — see the `ignoreConflictsOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Reservation>`.

    See the [Reservation Object](/api-reference/business-solutions/restaurants/reservations/reservations/reservation-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `TIME_NOT_AVAILABLE` | The requested time slot isn't available due to conflicts. |
    | `400` | `INVALID_ARGUMENT` | `RESERVATION_VIOLATION` | Reservation data violates validation rules. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reservation Created | `wix.table_reservations.v1.reservation_created` | Triggered when a reservation is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-reservation" id="ex-sdk-create-reservation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-reservation" id="ex-sdk-create-reservation-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-reservation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-reservation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createReservation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-reservation-0"><span className="wix-picker-name">createReservation</span></label>
    <label htmlFor="ex-sdk-create-reservation-1"><span className="wix-picker-name">createReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createReservation
    import { reservations } from '@wix/table-reservations';

    async function createReservation(reservation,options) {
      const response = await reservations.createReservation(reservation,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createReservation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reservations } from '@wix/table-reservations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reservations },
      // Include the auth strategy and host as relevant
    });

    async function createReservation(reservation,options) {
      const response = await myWixClient.reservations.createReservation(reservation,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
