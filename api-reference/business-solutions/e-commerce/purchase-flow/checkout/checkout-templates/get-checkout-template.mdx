---
title: "Get Checkout Template"
description: "Retrieves a checkout template."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a checkout template.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/ecom/v1/checkout-templates/{checkoutTemplateId}
    ```

    ## Path Params

    <ParamField path="checkoutTemplateId" type="string" required>

      ID of the checkout template to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="checkoutTemplate" type="CheckoutTemplate">

      Retrieved checkout template.

      See the [Checkout Template Object](/api-reference/business-solutions/e-commerce/purchase-flow/checkout/checkout-templates/checkout-template-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/ecom/v1/checkout-templates/d447475f-34ed-4be5-b199-b120c6938c39' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {
        "checkoutTemplate": {
          "id": "d447475f-34ed-4be5-b199-b120c6938c39",
          "status": "ACTIVE",
          "customization": {
            "webClient": {
              "disabledContinueShoppingButton": true,
              "disabledPolicyAgreementCheckbox": false
            },
            "lockedGiftCard": false,
            "lockedCoupon": true,
            "disabledManualPayment": true
          },
          "lineItems": [
            {
              "quantity": 2,
              "catalogReference": {
                "catalogItemId": "8df1afe4-90d1-162d-2d42-b8308125c193",
                "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                "options": {
                  "options": {
                    "Size": "Medium"
                  },
                  "variantId": "00000000-0000-0000-0000-000000000000"
                }
              }
            }
          ],
          "customLineItems": [
           {
             "quantity": 1,
             "price": "5",
             "descriptionLines": [],
             "productName": {
                "original": "My Custom Item"
             },
             "itemType": {
                "preset": "PHYSICAL"
             },
             "paymentOption": "FULL_PAYMENT_ONLINE"
           }
          ],
          "couponCode": "SUMMER_FLASH_SALE"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a checkout template.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { checkoutTemplates } from '@wix/ecom';

    checkoutTemplates.getCheckoutTemplate(checkoutTemplateId): Promise<CheckoutTemplate>
    ```

    ## Method Parameters

    <ParamField body="checkoutTemplateId" type="string" required>

      ID of the checkout template to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<CheckoutTemplate>`.

    See the [Checkout Template Object](/api-reference/business-solutions/e-commerce/purchase-flow/checkout/checkout-templates/checkout-template-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-checkout-template" id="ex-sdk-get-checkout-template-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-checkout-template" id="ex-sdk-get-checkout-template-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-checkout-template" id="ex-sdk-get-checkout-template-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-checkout-template-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-checkout-template-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getCheckoutTemplate</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCheckoutTemplate (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCheckoutTemplate (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-checkout-template-0"><span className="wix-picker-name">getCheckoutTemplate</span></label>
    <label htmlFor="ex-sdk-get-checkout-template-1"><span className="wix-picker-name">getCheckoutTemplate (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-checkout-template-2"><span className="wix-picker-name">getCheckoutTemplate (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getCheckoutTemplate
    import { checkoutTemplates } from '@wix/ecom';

    async function getCheckoutTemplate(checkoutTemplateId) {
      const response = await checkoutTemplates.getCheckoutTemplate(checkoutTemplateId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCheckoutTemplate (self-hosted)
    import { createClient } from '@wix/sdk';
    import { checkoutTemplates } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { checkoutTemplates },
      // Include the auth strategy and host as relevant
    });

    async function getCheckoutTemplate(checkoutTemplateId) {
      const response = await myWixClient.checkoutTemplates.getCheckoutTemplate(checkoutTemplateId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCheckoutTemplate (with elevated permissions)
    import { checkoutTemplates } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myGetCheckoutTemplateMethod(checkoutTemplateId) {
      const elevatedGetCheckoutTemplate = auth.elevate(checkoutTemplates.getCheckoutTemplate);
      const response = await elevatedGetCheckoutTemplate(checkoutTemplateId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
