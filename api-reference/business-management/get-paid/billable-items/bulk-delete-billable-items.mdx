---
title: "Bulk Delete Billable Items"
description: "Delete multiple billable items."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Delete multiple billable items.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BILLABLE_ITEMS.MANAGE-BILLABLE-ITEMS">Manage Billable Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/billable-items/v1/bulk/billable-items/delete
    ```

    ## Body Params

    <ParamField body="billableItemIds" type="string[]" required>

      Billable item IDs to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkBillableItemResult[]">

      Billable items delete operation results including metadata.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Billable item's delete operation metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata regarding the bulk delete operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Billable Item Deleted | `wix.billable_items.v1.billable_item_deleted` | Triggered when a billable item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes multiple Billable Items

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/billable-items/v1/bulk/billable-items/delete' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
        "billableItemIds": [
          "8046df3c-7575-4098-a5ab-c91ad8f33c47",
          "9157ef4d-8686-5109-b967-d92be8f44c58"
        ]
      }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
              "success": true
            }
          },
          {
            "itemMetadata": {
              "id": "9157ef4d-8686-5109-b967-d92be8f44c58",
              "success": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Delete multiple billable items.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BILLABLE_ITEMS.MANAGE-BILLABLE-ITEMS">Manage Billable Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { billableItems } from '@wix/get-paid';

    billableItems.bulkDeleteBillableItems(billableItemIds): Promise<BulkDeleteBillableItemsResponse>
    ```

    ## Method Parameters

    <ParamField body="billableItemIds" type="string[]" required>

      Billable item IDs to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Returns

    Returns `Promise<BulkDeleteBillableItemsResponse>`.

    <ResponseField name="results" type="BulkBillableItemResult[]">

      Billable items delete operation results including metadata.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Billable item's delete operation metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata regarding the bulk delete operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Billable Item Deleted | `wix.billable_items.v1.billable_item_deleted` | Triggered when a billable item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-billable-items" id="ex-sdk-bulk-delete-billable-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-billable-items" id="ex-sdk-bulk-delete-billable-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-billable-items" id="ex-sdk-bulk-delete-billable-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-billable-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-billable-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteBillableItems</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteBillableItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteBillableItems (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-billable-items-0"><span className="wix-picker-name">bulkDeleteBillableItems</span></label>
    <label htmlFor="ex-sdk-bulk-delete-billable-items-1"><span className="wix-picker-name">bulkDeleteBillableItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-billable-items-2"><span className="wix-picker-name">bulkDeleteBillableItems (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteBillableItems
    import { billableItems } from '@wix/get-paid';

    async function bulkDeleteBillableItems(billableItemIds) {
      const response = await billableItems.bulkDeleteBillableItems(billableItemIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteBillableItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { billableItems } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { billableItems },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteBillableItems(billableItemIds) {
      const response = await myWixClient.billableItems.bulkDeleteBillableItems(billableItemIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteBillableItems (with elevated permissions)
    import { billableItems } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteBillableItemsMethod(billableItemIds) {
      const elevatedBulkDeleteBillableItems = auth.elevate(billableItems.bulkDeleteBillableItems);
      const response = await elevatedBulkDeleteBillableItems(billableItemIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
