---
title: "Get Account For Current Site"
description: "Retrieves the Google Ads account linked to the current Wix site."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the Google Ads account linked to the current Wix site.
    Returns an empty response (not an error) when no account exists for the site.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-GOOGLE-ADS">View google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/_serverless/pa-google/v1/accounts/current-site
    ```

    ## Response Object

    <ResponseField name="account" type="Account">

      The Google Ads account linked to the current site. Empty when no account exists.

      See the [Google Account Object](/api-reference/business-management/marketing/ads/google-ads/google-account-v1/google-account-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/_serverless/pa-google/v1/accounts/current-site' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "account": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "googleAccountId": "123-456-7890",
          "currency": "USD",
          "accountStatus": "ACTIVE",
          "currentBudget": {
            "value": "50.00",
            "currency": "USD"
          },
          "spentBudget": {
            "value": "12.50",
            "currency": "USD"
          },
          "paymentStatus": {
            "status": "PAID"
          },
          "createdDate": "2024-03-15T10:30:00.000Z",
          "updatedDate": "2024-03-16T09:00:00.000Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the Google Ads account linked to the current Wix site.
    Returns an empty response (not an error) when no account exists for the site.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-GOOGLE-ADS">View google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { accounts } from '@wix/promote-google-ads';

    accounts.getAccountForCurrentSite(): Promise
    ```

    ## Returns

    <ResponseField name="account" type="Account">

      The Google Ads account linked to the current site. Empty when no account exists.

      See the [Google Account Object](/api-reference/business-management/marketing/ads/google-ads/google-account-v1/google-account-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-account-for-current-site" id="ex-sdk-get-account-for-current-site-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-account-for-current-site" id="ex-sdk-get-account-for-current-site-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-account-for-current-site-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-account-for-current-site-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get the Google Ads account for the current site</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getAccountForCurrentSite (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-account-for-current-site-0"><span className="wix-picker-name">Get the Google Ads account for the current site</span></label>
    <label htmlFor="ex-sdk-get-account-for-current-site-1"><span className="wix-picker-name">getAccountForCurrentSite (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get the Google Ads account for the current site
    import { accounts } from '@wix/promote-google-ads';

    const response = await accounts.getAccountForCurrentSite();

    /* Promise resolves to:
     * {
     *   "_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
     *   "googleAccountId": "123-456-7890",
     *   "currency": "USD",
     *   "accountStatus": "ACTIVE",
     *   "currentBudget": { "value": "50.00", "currency": "USD" },
     *   "spentBudget": { "value": "12.50", "currency": "USD" },
     *   "paymentStatus": "PAID",
     *   "_createdDate": "2024-03-15T10:30:00.000Z",
     *   "_updatedDate": "2024-03-16T09:00:00.000Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getAccountForCurrentSite (self-hosted)
    import { createClient } from '@wix/sdk';
    import { accounts } from '@wix/promote-google-ads';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { accounts },
      // Include the auth strategy and host as relevant
    });

    async function getAccountForCurrentSite() {
      const response = await myWixClient.accounts.getAccountForCurrentSite();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
