---
title: "Activation Object"
description: "The Activation object in the Wix Activation API."
icon: "cube"
---

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Activation ID

      Constraints: `format GUID`.

    </ResponseField>

    <ResponseField name="automation" type="Automation">

      Activation automation

      <Expandable title="automation properties">

        <ResponseField name="id" type="string">

          Automation ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number. This increments by 1 whenever the automation is updated. Specify the current revision number whenever updating an existing automation.

          **Read-only.**

        </ResponseField>

        <ResponseField name="createdBy" type="AuditInfo">

          Who created the automation.

          **Read-only.** Constraints: `immutable`.

          <Expandable title="One of the following">

            <ResponseField name="id" type="id">

              id

              <Expandable title="id properties">

                <ResponseField name="userId" type="string">

                  [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

                  You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="createdDate" type="datetime">

          When the automation was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updatedBy" type="AuditInfo">

          Who last updated the automation.

          **Read-only.**

          <Expandable title="One of the following">

            <ResponseField name="id" type="id">

              id

              <Expandable title="id properties">

                <ResponseField name="userId" type="string">

                  [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

                  You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="updatedDate" type="datetime">

          When the automation was last updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Automation name as displayed on the user's site.

          Constraints: `minLength 1`, `maxLength 500`.

        </ResponseField>

        <ResponseField name="description" type="string">

          Automation description.

        </ResponseField>

        <ResponseField name="configuration" type="AutomationConfiguration">

          Automation configuration.

          <Expandable title="configuration properties">

            <ResponseField name="status" type="string">

              Status of the automation on the site.

              **Supported values:**

              - `ACTIVE` — Active. Active automations can be triggered.
              - `INACTIVE` — Inactive. Inactive automations cannot be triggered.

            </ResponseField>

            <ResponseField name="trigger" type="Trigger">

              Trigger configuration.

              <Expandable title="trigger properties">

                <ResponseField name="appId" type="string">

                  ID of the app that defines the trigger.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="triggerKey" type="string">

                  Trigger key.

                  Learn about [setting up a trigger](https://dev.wix.com/docs/rest/business-management/automations/triggers/add-a-trigger-to-your-app#step-1--set-up-the-trigger).

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

                <ResponseField name="filters" type="Filter[]">

                  Schema field filters. All filter conditions must be met for the automation to run.

                  Learn more about setting up [automation filters](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#filters).

                  Constraints: `maxItems 5`.

                  _Nested structure omitted for brevity — see the `Filter[]` object._

                </ResponseField>

                <ResponseField name="scheduledEventOffset" type="FutureDateActivationOffset">

                  Automation offset. You can schedule automations to run before the trigger occurs.

                  Learn more about [scheduled events](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#scheduled-events).

                  _Nested structure omitted for brevity — see the `FutureDateActivationOffset` object._

                </ResponseField>

                <ResponseField name="overrideSchema" type="object">

                  Trigger schema override. When specified, this replaces the trigger schema.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rootActionIds" type="string[]">

              Root action IDs. A root action is the first action that runs after the trigger occurs. Root actions run in parallel.

              > **Note**: You can currently only specify 1 root action.

              Constraints: `format GUID`, `maxItems 20`.

            </ResponseField>

            <ResponseField name="actions" type="object">

              Actions the automation can execute, as `key:value` pairs. For the key, specify the action ID. The value must be an object structured as described below.

              Constraints: `format map`.

              <Expandable title="actions properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="Action">

                  _Nested structure omitted for brevity — see the `Action` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="origin" type="string">

          How the automation was added to the user's site.

          Constraints: `immutable`.

          **Supported values:**

          - `USER` — Created by a [Wix user](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-user).
          - `APPLICATION` — Created by a [Wix app](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-app) for a particular site.
          - `PREINSTALLED` — [Preinstalled automation](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/automations/pre-installed-automations/about-pre-installed-automations).

        </ResponseField>

        <ResponseField name="settings" type="AutomationSettings">

          Automation settings. Applied only for pre-installed and application automations. Not applied to User origin automations

          <Expandable title="settings properties">

            <ResponseField name="hidden" type="boolean">

              Whether the automation is hidden from users.

              Default: `false`

            </ResponseField>

            <ResponseField name="readonly" type="boolean">

              Whether the automation is read-only.

              When `true`, site owners can't modify the automation or any of its actions. When `false`, users can configure specific actions as skippable or read-only using `settings.actionSettings`.

              Default: `false`.

              > **Note**: Setting `readOnly` to `true` overrides `settings.actionSettings`.

            </ResponseField>

            <ResponseField name="disableDelete" type="boolean">

              Whether to disable the option to delete the automation from the site.

              Default: `false`.

            </ResponseField>

            <ResponseField name="disableStatusChange" type="boolean">

              Whether to disable the option to change the automation's `configuration.status`.

              Default: `false`.

            </ResponseField>

            <ResponseField name="actionSettings" type="ActionSettings">

              Automation action settings.

              <Expandable title="actionSettings properties">

                <ResponseField name="permanentActionIds" type="string[]">

                  IDs of actions whose `skipActionExpression` can't be modified.

                  An action's `skipActionExpression` determines whether the action is skipped. Including an action's ID in this array makes its `skipActionExpression` permanent, so it can't be changed.

                  When empty, the `skipActionExpression` of all actions can be edited.

                  Constraints: `format GUID`, `maxItems 30`.

                </ResponseField>

                <ResponseField name="readonlyActionIds" type="string[]">

                  IDs of read-only actions. Read-only actions cannot be modified.

                  When empty and the automation's `settings.readonly` is `false`, all actions are modifiable.

                  Constraints: `format GUID`, `maxItems 30`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="draftInfo" type="DraftInfo">

          When the automation is a draft, the draft details.

          **Read-only.**

          <Expandable title="draftInfo properties">

            <ResponseField name="originalAutomationId" type="string">

              ID of the original automation.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="archived" type="boolean">

          Whether the automation is archived.

          To archive an automation, set this to `true`. To restore an archived automation, set this to `false`.

        </ResponseField>

        <ResponseField name="autoArchivePolicy" type="AutoArchivePolicy">

          Auto archive policy

          <Expandable title="autoArchivePolicy properties">

            <ResponseField name="archiveDate" type="datetime">

              Date when to archive the automation
              If this date in the past, nothing will happen (automation will not go into archived state)
              If this date in the future, on this date the automation will be updated with archived = true and configuration.status = INACTIVE
              After this date the automation may be unarchived and archived again, this date will have no influence

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="originInfo" type="originInfo">

          originInfo

          <Expandable title="originInfo properties">

            <ResponseField name="applicationInfo" type="ApplicationOrigin">

              When the origin of the automation is `APPLICATION`, the details of the app that created it.

              <Expandable title="applicationInfo properties">

                <ResponseField name="appId" type="string">

                  [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

                  You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="preinstalledInfo" type="PreinstalledOrigin">

              When the origin of the automation is `PREINSTALLED`, the details of the preinstalled automation.

              <Expandable title="preinstalledInfo properties">

                <ResponseField name="appId" type="string">

                  ID of the app that added the [preinstalled automation](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/automations/pre-installed-automations/about-pre-installed-automations).

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="override" type="boolean">

                  Whether the automation is an override.

                  When a user modifies the preinstalled automation installed on their site, a site-specific version of the automation is created that overrides the automation that was originally installed. This override automation is assigned a **new** automation ID — it does not reuse the ID of the original preinstalled automation.

                  > **Note**: Once an override exists, [Get Automation](https://dev.wix.com/docs/api-reference/business-management/automations/automations/automations-v2/get-automation) on the *original* preinstalled automation's ID returns `404 Not Found`. Use [Query Automations](https://dev.wix.com/docs/api-reference/business-management/automations/automations/automations-v2/query-automations) (for example, by `configuration.trigger.triggerKey`) to find the current automation, or retrieve `automationId` from the activation log.
                  >
                  > An override automation can no longer be updated by the app that originally installed it.
                  >
                  > To revert to the original preinstalled version, the user must delete the override by calling the Delete Automation method. Calling Delete Automation on an override automation removes the override and restores the preinstalled automation that was previously installed, once again accessible via its original ID.

                  Default: `false`.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="templateInfo" type="TemplateOrigin">

              When the origin of the automation is `TEMPLATE`, the details of the template automation.

              <Expandable title="templateInfo properties">

                <ResponseField name="appId" type="string">

                  ID of the app that added the template automation.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="activationInitiationSourceInfo" type="ActivationInitiationSourceInfo">

      Activation Initiation Source information

      <Expandable title="activationInitiationSourceInfo properties">

        <ResponseField name="activationInitiationSource" type="string">

          The source type of this activation.

          **Supported values:**

          - `TRIGGER_PROVIDER_EVENT` — Activation triggered by a provider event (e.g., reportEvent, bulkReportEvent).
          The automation runs in response to a business event. Uses published automations only.
          - `MANUAL_ACTIVATION` — Activation triggered by manual execution (e.g., testAutomation, ReRunAutomation).
          Explicitly initiated by user or API caller with explicit payload.
          - `RERUN_ACTIVATION` — Activation triggered by re-running a previous activation.

        </ResponseField>

        <ResponseField name="initiatorInfo" type="InitiatorInfo">

          Information about the user who initiated the activation.

          <Expandable title="initiatorInfo properties">

            <ResponseField name="initiatorUserId" type="string">

              The user ID of the initiator.

            </ResponseField>

            <ResponseField name="wixEmployee" type="boolean">

              Whether the initiating user is a Wix employee.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="initiationSourceInfo" type="initiationSourceInfo">

          initiationSourceInfo

          <Expandable title="initiationSourceInfo properties">

            <ResponseField name="manualActivationInfo" type="ManualActivationInfo">

              Details for MANUAL_ACTIVATION source.

              <Expandable title="manualActivationInfo properties">

                <ResponseField name="testMode" type="boolean">

                  Whether this activation is running in test mode.

                  In test mode:
                  - Draft automations can be executed
                  - Certain actions may be skipped (e.g., delays, rate limits)
                  - Used primarily for testing automations before publication

                  Default: false (standard production mode)

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rerunActivationInfo" type="RerunActivationInfo">

              Details for RERUN_ACTIVATION source.

              <Expandable title="rerunActivationInfo properties">

                <ResponseField name="originalActivationId" type="string">

                  The activation ID of the original activation being rerun.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="skipDelays" type="boolean">

                  When true, delay actions will be skipped during the rerun.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Activation ID

      Constraints: `format GUID`.

    </ResponseField>

    <ResponseField name="automation" type="Automation">

      Activation automation

      <Expandable title="automation properties">

        <ResponseField name="_id" type="string">

          Automation ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number. This increments by 1 whenever the automation is updated. Specify the current revision number whenever updating an existing automation.

          **Read-only.**

        </ResponseField>

        <ResponseField name="createdBy" type="AuditInfo">

          Who created the automation.

          **Read-only.** Constraints: `immutable`.

          <Expandable title="One of the following">

            <ResponseField name="id" type="id">

              id

              <Expandable title="id properties">

                <ResponseField name="userId" type="string">

                  [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

                  You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="_createdDate" type="datetime">

          When the automation was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updatedBy" type="AuditInfo">

          Who last updated the automation.

          **Read-only.**

          <Expandable title="One of the following">

            <ResponseField name="id" type="id">

              id

              <Expandable title="id properties">

                <ResponseField name="userId" type="string">

                  [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

                  You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="_updatedDate" type="datetime">

          When the automation was last updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Automation name as displayed on the user's site.

          Constraints: `minLength 1`, `maxLength 500`.

        </ResponseField>

        <ResponseField name="description" type="string">

          Automation description.

        </ResponseField>

        <ResponseField name="configuration" type="AutomationConfiguration">

          Automation configuration.

          <Expandable title="configuration properties">

            <ResponseField name="status" type="string">

              Status of the automation on the site.

              **Supported values:**

              - `ACTIVE` — Active. Active automations can be triggered.
              - `INACTIVE` — Inactive. Inactive automations cannot be triggered.

            </ResponseField>

            <ResponseField name="trigger" type="Trigger">

              Trigger configuration.

              <Expandable title="trigger properties">

                <ResponseField name="appId" type="string">

                  ID of the app that defines the trigger.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="triggerKey" type="string">

                  Trigger key.

                  Learn about [setting up a trigger](https://dev.wix.com/docs/rest/business-management/automations/triggers/add-a-trigger-to-your-app#step-1--set-up-the-trigger).

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

                <ResponseField name="filters" type="Filter[]">

                  Schema field filters. All filter conditions must be met for the automation to run.

                  Learn more about setting up [automation filters](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#filters).

                  Constraints: `maxItems 5`.

                  _Nested structure omitted for brevity — see the `Filter[]` object._

                </ResponseField>

                <ResponseField name="scheduledEventOffset" type="FutureDateActivationOffset">

                  Automation offset. You can schedule automations to run before the trigger occurs.

                  Learn more about [scheduled events](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#scheduled-events).

                  _Nested structure omitted for brevity — see the `FutureDateActivationOffset` object._

                </ResponseField>

                <ResponseField name="overrideSchema" type="object">

                  Trigger schema override. When specified, this replaces the trigger schema.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rootActionIds" type="string[]">

              Root action IDs. A root action is the first action that runs after the trigger occurs. Root actions run in parallel.

              > **Note**: You can currently only specify 1 root action.

              Constraints: `format GUID`, `maxItems 20`.

            </ResponseField>

            <ResponseField name="actions" type="object">

              Actions the automation can execute, as `key:value` pairs. For the key, specify the action ID. The value must be an object structured as described below.

              Constraints: `format map`.

              <Expandable title="actions properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="Action">

                  _Nested structure omitted for brevity — see the `Action` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="origin" type="string">

          How the automation was added to the user's site.

          Constraints: `immutable`.

          **Supported values:**

          - `USER` — Created by a [Wix user](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-user).
          - `APPLICATION` — Created by a [Wix app](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-app) for a particular site.
          - `PREINSTALLED` — [Preinstalled automation](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/automations/pre-installed-automations/about-pre-installed-automations).

        </ResponseField>

        <ResponseField name="settings" type="AutomationSettings">

          Automation settings. Applied only for pre-installed and application automations. Not applied to User origin automations

          <Expandable title="settings properties">

            <ResponseField name="hidden" type="boolean">

              Whether the automation is hidden from users.

              Default: `false`

            </ResponseField>

            <ResponseField name="readonly" type="boolean">

              Whether the automation is read-only.

              When `true`, site owners can't modify the automation or any of its actions. When `false`, users can configure specific actions as skippable or read-only using `settings.actionSettings`.

              Default: `false`.

              > **Note**: Setting `readOnly` to `true` overrides `settings.actionSettings`.

            </ResponseField>

            <ResponseField name="disableDelete" type="boolean">

              Whether to disable the option to delete the automation from the site.

              Default: `false`.

            </ResponseField>

            <ResponseField name="disableStatusChange" type="boolean">

              Whether to disable the option to change the automation's `configuration.status`.

              Default: `false`.

            </ResponseField>

            <ResponseField name="actionSettings" type="ActionSettings">

              Automation action settings.

              <Expandable title="actionSettings properties">

                <ResponseField name="permanentActionIds" type="string[]">

                  IDs of actions whose `skipActionExpression` can't be modified.

                  An action's `skipActionExpression` determines whether the action is skipped. Including an action's ID in this array makes its `skipActionExpression` permanent, so it can't be changed.

                  When empty, the `skipActionExpression` of all actions can be edited.

                  Constraints: `format GUID`, `maxItems 30`.

                </ResponseField>

                <ResponseField name="readonlyActionIds" type="string[]">

                  IDs of read-only actions. Read-only actions cannot be modified.

                  When empty and the automation's `settings.readonly` is `false`, all actions are modifiable.

                  Constraints: `format GUID`, `maxItems 30`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="draftInfo" type="DraftInfo">

          When the automation is a draft, the draft details.

          **Read-only.**

          <Expandable title="draftInfo properties">

            <ResponseField name="originalAutomationId" type="string">

              ID of the original automation.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="archived" type="boolean">

          Whether the automation is archived.

          To archive an automation, set this to `true`. To restore an archived automation, set this to `false`.

        </ResponseField>

        <ResponseField name="autoArchivePolicy" type="AutoArchivePolicy">

          Auto archive policy

          <Expandable title="autoArchivePolicy properties">

            <ResponseField name="archiveDate" type="datetime">

              Date when to archive the automation
              If this date in the past, nothing will happen (automation will not go into archived state)
              If this date in the future, on this date the automation will be updated with archived = true and configuration.status = INACTIVE
              After this date the automation may be unarchived and archived again, this date will have no influence

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="originInfo" type="originInfo">

          originInfo

          <Expandable title="originInfo properties">

            <ResponseField name="applicationInfo" type="ApplicationOrigin">

              When the origin of the automation is `APPLICATION`, the details of the app that created it.

              <Expandable title="applicationInfo properties">

                <ResponseField name="appId" type="string">

                  [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

                  You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="preinstalledInfo" type="PreinstalledOrigin">

              When the origin of the automation is `PREINSTALLED`, the details of the preinstalled automation.

              <Expandable title="preinstalledInfo properties">

                <ResponseField name="appId" type="string">

                  ID of the app that added the [preinstalled automation](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/automations/pre-installed-automations/about-pre-installed-automations).

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="override" type="boolean">

                  Whether the automation is an override.

                  When a user modifies the preinstalled automation installed on their site, a site-specific version of the automation is created that overrides the automation that was originally installed. This override automation is assigned a **new** automation ID — it does not reuse the ID of the original preinstalled automation.

                  > **Note**: Once an override exists, [Get Automation](https://dev.wix.com/docs/api-reference/business-management/automations/automations/automations-v2/get-automation) on the *original* preinstalled automation's ID returns `404 Not Found`. Use [Query Automations](https://dev.wix.com/docs/api-reference/business-management/automations/automations/automations-v2/query-automations) (for example, by `configuration.trigger.triggerKey`) to find the current automation, or retrieve `automationId` from the activation log.
                  >
                  > An override automation can no longer be updated by the app that originally installed it.
                  >
                  > To revert to the original preinstalled version, the user must delete the override by calling the Delete Automation method. Calling Delete Automation on an override automation removes the override and restores the preinstalled automation that was previously installed, once again accessible via its original ID.

                  Default: `false`.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="templateInfo" type="TemplateOrigin">

              When the origin of the automation is `TEMPLATE`, the details of the template automation.

              <Expandable title="templateInfo properties">

                <ResponseField name="appId" type="string">

                  ID of the app that added the template automation.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="activationInitiationSourceInfo" type="ActivationInitiationSourceInfo">

      Activation Initiation Source information

      <Expandable title="activationInitiationSourceInfo properties">

        <ResponseField name="activationInitiationSource" type="string">

          The source type of this activation.

          **Supported values:**

          - `TRIGGER_PROVIDER_EVENT` — Activation triggered by a provider event (e.g., reportEvent, bulkReportEvent).
          The automation runs in response to a business event. Uses published automations only.
          - `MANUAL_ACTIVATION` — Activation triggered by manual execution (e.g., testAutomation, ReRunAutomation).
          Explicitly initiated by user or API caller with explicit payload.
          - `RERUN_ACTIVATION` — Activation triggered by re-running a previous activation.

        </ResponseField>

        <ResponseField name="initiatorInfo" type="InitiatorInfo">

          Information about the user who initiated the activation.

          <Expandable title="initiatorInfo properties">

            <ResponseField name="initiatorUserId" type="string">

              The user ID of the initiator.

            </ResponseField>

            <ResponseField name="wixEmployee" type="boolean">

              Whether the initiating user is a Wix employee.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="initiationSourceInfo" type="initiationSourceInfo">

          initiationSourceInfo

          <Expandable title="initiationSourceInfo properties">

            <ResponseField name="manualActivationInfo" type="ManualActivationInfo">

              Details for MANUAL_ACTIVATION source.

              <Expandable title="manualActivationInfo properties">

                <ResponseField name="testMode" type="boolean">

                  Whether this activation is running in test mode.

                  In test mode:
                  - Draft automations can be executed
                  - Certain actions may be skipped (e.g., delays, rate limits)
                  - Used primarily for testing automations before publication

                  Default: false (standard production mode)

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rerunActivationInfo" type="RerunActivationInfo">

              Details for RERUN_ACTIVATION source.

              <Expandable title="rerunActivationInfo properties">

                <ResponseField name="originalActivationId" type="string">

                  The activation ID of the original activation being rerun.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="skipDelays" type="boolean">

                  When true, delay actions will be skipped during the rerun.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
