---
title: "onCampaignRejectedEvent"
description: "Triggered when a campaign is identified as abusive and rejected without sending emails to recipients."
icon: "bolt"
---

Triggered when a campaign is identified as abusive and rejected without sending emails to recipients.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.email_marketing.v1.campaign_campaign_rejected_event` |
| Package | `@wix/email-marketing` |
| Permissions | `Manage Stores`, `Access Verticals by Automations`, `Set Up Automations`, `Read Email Marketing`, `Manage Email Marketing` |

## Examples

<CodeGroup>

  ```javascript onCampaignRejectedEvent
  import { campaigns } from '@wix/email-marketing';

  campaigns.onCampaignRejectedEvent((event) => {
      // handle your event here
  });
  ```

  ```javascript onCampaignRejectedEvent (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { campaigns } from '@wix/email-marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        campaigns,
      },
  });

  wixClient.campaigns.onCampaignRejectedEvent((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
