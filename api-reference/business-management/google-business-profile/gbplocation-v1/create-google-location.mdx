---
title: "Create Google Location"
description: "Creates a Business Profile listing in Google and records it for the site."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a Business Profile listing in Google and records it for the site.

    Use this to add a location the Google account doesn't have yet. To record a
    listing that already exists, call Create Location instead.

    Specify at least one of `storefrontAddress` or `serviceArea`. If neither is set,
    the call fails with `INVALID_LOCATION_PAYLOAD`.

    Both can be set together when `serviceArea.businessType` is
    `CUSTOMER_AND_BUSINESS_LOCATION`, which is how a business that has premises and
    also travels to customers is represented.

    A Google account must be connected first. See the
    [Connection API](https://dev.wix.com/docs/api-reference/business-management/google-business-profile/connection-v1).

    Set `validateOnly` to check the details without creating anything: Google
    runs the same validation and returns the same errors, but no listing is
    created and none is recorded for the site. On success the response has no
    `location` — that absence is what "valid" looks like.

    Errors:
    - `CONNECTION_NOT_FOUND`: no Google account is connected. Google isn't called.
    - `TOKEN_RESOLUTION_FAILED`: the stored credentials couldn't be used. Google
    isn't called and nothing is created.
    - `GOOGLE_LOCATION_VALIDATION_FAILED`: Google rejected the details, for
    example an invalid phone number or address, or a listing that needs a
    postcard PIN. The specific reasons are in the error payload, and they're
    worth surfacing: they tell the site owner what to correct. Nothing is
    created.
    - `GOOGLE_API_CALL_FAILED`: the call to Google failed for any other reason.
    Google's status and response are in the error payload. Nothing is created.
    - `LOCATION_DB_PERSIST_FAILED`: Google created the listing but it couldn't be
    recorded for the site. The new Google location ID is in the error payload,
    so keep it: the listing exists in Google and can be recorded with Create
    Location, or removed with Delete Google Location.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/gbp/v1/locations/google
    ```

    ## Body Params

    <ParamField body="accountId" type="string" required>

      GBP account id (numeric, no "accounts/" prefix). The server forms the
      Google resource name as "accounts/&#123;account_id&#125;".

      Constraints: `minLength 1`, `maxLength 255`.

    </ParamField>

    <ParamField body="location" type="GoogleLocation" required>

      The Google Location to create. The `name` field is ignored on input
      and populated by Google on response.

      <Expandable title="location properties">

        <ParamField body="name" type="string">

          Google resource name, e.g. "locations/12345678901234567890".
          Empty on create request; populated on response.

          Constraints: `maxLength 200`.

        </ParamField>

        <ParamField body="title" type="string" required>

          Business name shown on the listing (Google's "title").

          Constraints: `minLength 1`, `maxLength 300`.

        </ParamField>

        <ParamField body="languageCode" type="string" required>

          BCP-47 language tag, e.g. "en-US". Required by Google, immutable.

          Constraints: `format LANGUAGE_TAG`.

        </ParamField>

        <ParamField body="primaryCategory" type="Category" required>

          Primary business category. Required by Google.

          <Expandable title="primaryCategory properties">

            <ParamField body="id" type="string" required>

              Google's category identifier, such as `categories/gcid:restaurant`.

              The `categories/` prefix is optional when you send it. A bare `gcid:...` is
              accepted and prefixed for you.

              Constraints: `minLength 1`, `maxLength 200`.

            </ParamField>

            <ParamField body="displayName" type="string">

              Display name of the category. Returned by Google, and ignored if you send
              it.

              **Read-only.** Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="additionalCategories" type="Category[]">

          Additional categories. Google caps at 9.

          Constraints: `maxItems 9`.

          <Expandable title="additionalCategories properties">

            <ParamField body="id" type="string">

              Google's category identifier, such as `categories/gcid:restaurant`.

              The `categories/` prefix is optional when you send it. A bare `gcid:...` is
              accepted and prefixed for you.

              Constraints: `minLength 1`, `maxLength 200`.

            </ParamField>

            <ParamField body="displayName" type="string">

              Display name of the category. Returned by Google, and ignored if you send
              it.

              **Read-only.** Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="profile" type="Profile">

          Optional. Google asks for a description for every category except lodging
          but doesn't enforce it, so neither does this API. Both an absent profile and
          one with an empty description are accepted.

          <Expandable title="profile properties">

            <ParamField body="description" type="string">

              Description of the location in the site owner's own words. Optional, and an
              empty value clears it on the listing. Google allows up to 750 characters.

              Constraints: `maxLength 750`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="storefrontAddress" type="PostalAddress">

          Storefront postal address. Required for a business that serves customers at
          its premises, and allowed alongside `serviceArea` when that field's
          `businessType` is `CUSTOMER_AND_BUSINESS_LOCATION`.

          <Expandable title="storefrontAddress properties">

            <ParamField body="countryCode" type="string">

              ISO 3166-1 alpha-2 country code (e.g. "US", "GB").

              Constraints: `format COUNTRY`.

            </ParamField>

            <ParamField body="postalCode" type="string">

              Postal/ZIP code.

              Constraints: `maxLength 20`.

            </ParamField>

            <ParamField body="administrativeArea" type="string">

              Administrative area (state, province, region).

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="locality" type="string">

              City or locality.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="addressLines" type="string[]">

              Street address lines. Up to 5 lines.

              Constraints: `maxLength 255`, `maxItems 5`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="serviceArea" type="ServiceArea">

          Service-area definition (for businesses that serve customers at the
          customer's location, exclusively or in addition to their storefront).

          <Expandable title="serviceArea properties">

            <ParamField body="businessType" type="string">

              Type of service-area business.

              **Supported values:**

              - `CUSTOMER_LOCATION_ONLY`
              - `CUSTOMER_AND_BUSINESS_LOCATION`

            </ParamField>

            <ParamField body="places" type="PlaceInfo[]">

              Places served. Google caps at 20.

              Constraints: `maxItems 20`.

              _Nested structure omitted for brevity — see the `PlaceInfo[]` object._

            </ParamField>

            <ParamField body="regionCode" type="string">

              ISO 3166-1 alpha-2 country code. Required by Google for
              CUSTOMER_LOCATION_ONLY businesses; ignored otherwise.

              Constraints: `format COUNTRY`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="primaryPhone" type="string">

          Primary business phone.

          Constraints: `maxLength 50`.

        </ParamField>

        <ParamField body="additionalPhones" type="string[]">

          Additional phones. Google caps at 2.

          Constraints: `maxLength 50`, `maxItems 2`.

        </ParamField>

        <ParamField body="websiteUri" type="string">

          Business website.

          Clear it by masking `websiteUri` on Update Google Location and sending
          `null`. Sending an empty string is rejected.

        </ParamField>

        <ParamField body="storeCode" type="string">

          Merchant-defined store code (free-form identifier shown only to the merchant).

          Constraints: `maxLength 64`.

        </ParamField>

        <ParamField body="regularHours" type="BusinessHours[]">

          Regular weekly business hours.

          Constraints: `maxItems 14`.

          <Expandable title="regularHours properties">

            <ParamField body="openDay" type="string">

              Day of the week when this open/close window starts.

              **Supported values:**

              - `MONDAY`
              - `TUESDAY`
              - `WEDNESDAY`
              - `THURSDAY`
              - `FRIDAY`
              - `SATURDAY`
              - `SUNDAY`

            </ParamField>

            <ParamField body="openTime" type="TimeOfDay">

              Wall-clock time the business opens on `openDay`.

              _Nested structure omitted for brevity — see the `TimeOfDay` object._

            </ParamField>

            <ParamField body="closeDay" type="string">

              Day of the week when this open/close window ends.

              **Supported values:**

              - `MONDAY`
              - `TUESDAY`
              - `WEDNESDAY`
              - `THURSDAY`
              - `FRIDAY`
              - `SATURDAY`
              - `SUNDAY`

            </ParamField>

            <ParamField body="closeTime" type="TimeOfDay">

              Wall-clock time the business closes on `closeDay`.

              _Nested structure omitted for brevity — see the `TimeOfDay` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="latLng" type="LatLng">

          Geographic coordinates of the storefront.

          <Expandable title="latLng properties">

            <ParamField body="latitude" type="number">

              Latitude in degrees. Must be in [-90, 90].

            </ParamField>

            <ParamField body="longitude" type="number">

              Longitude in degrees. Must be in [-180, 180].

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="openInfo" type="OpenInfo">

          Open/closed state of the listing.

          <Expandable title="openInfo properties">

            <ParamField body="status" type="string">

              Current listing status.

              **Supported values:**

              - `OPEN`
              - `CLOSED_PERMANENTLY`
              - `CLOSED_TEMPORARILY`

            </ParamField>

            <ParamField body="canReopen" type="boolean">

              Whether the listing can be reopened (only relevant for CLOSED_PERMANENTLY).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="metadata" type="Metadata">

          Read-only signals Google reports about the listing.

          Only populated on the read paths (Query Google Locations, Get Google
          Location) and on the Update Google Location response. Search Google
          Locations reads Google's catalog rather than the merchant's own listing,
          so it leaves this unset — don't read a default there as a "no".

          **Read-only.**

          <Expandable title="metadata properties">

            <ParamField body="canOperateLodgingData" type="boolean">

              Whether this location can carry lodging (hotel) data — that is, whether
              Get/Update Google Location Lodging are usable for it. Google derives it
              from the primary category, so it flips when the category changes.

            </ParamField>

            <ParamField body="mapsUri" type="string">

              Link to the location's page on Google Maps. Google returns it only once
              the listing is on Maps, so it stays empty for a listing that is still
              unverified or suspended.

              Constraints: `format WEB_URL`, `maxLength 1000`.

            </ParamField>

            <ParamField body="canHaveFoodMenus" type="boolean">

              Whether this location is eligible for food menus. Callers must check
              this flag before using Update Location Food Menus.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="validateOnly" type="boolean">

      Dry run. When true the payload is sent to Google's
      accounts.locations.create with its `validateOnly` query param set, so
      Google runs its full validation (categories, address, phone, hours,
      pin-drop requirements) and creates nothing.

      A dry run reports failures exactly as a real create does — the same
      GOOGLE_LOCATION_VALIDATION_FAILED reasons, the same INVALID_LOCATION_PAYLOAD
      for the storefront_address / service_area combinations this service checks
      itself — which is the point: a caller can pre-flight a form before
      committing.

      On success nothing is created at Google, nothing is persisted here, no
      GBPLocation.CREATED event is emitted, and the response's `location` is
      UNSET (Google returns no resource name for a validate-only call). Callers
      must treat an absent `location` as "validation passed", not as a failure.

    </ParamField>

    ## Response Object

    <ResponseField name="location" type="GBPLocation">

      The created location, with `id` set to the ID Google assigned and `accountId`
      carried through from the request.

      UNSET when the request had `validate_only` set: Google validated the payload
      and created nothing, so there is no location to return.

      See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `GOOGLE_API_CALL_FAILED` |  |
    | `400` | `INVALID_ARGUMENT` | `GOOGLE_LOCATION_VALIDATION_FAILED` |  |
    | `400` | `INVALID_ARGUMENT` | `INVALID_LOCATION_PAYLOAD` |  |
    | `500` | `INTERNAL` | `LOCATION_DB_PERSIST_FAILED` |  |
    | `428` | `FAILED_PRECONDITION` | `CONNECTION_NOT_FOUND` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/gbp/v1/locations/google' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "accountId": "104312867792031877552",
        "location": {
          "title": "Sunset Coffee Roasters",
          "languageCode": "en-US",
          "primaryCategory": {
            "id": "categories/gcid:coffee_shop"
          },
          "storefrontAddress": {
            "countryCode": "US",
            "postalCode": "94110",
            "administrativeArea": "CA",
            "locality": "San Francisco",
            "addressLines": ["2436 Mission St"]
          },
          "primaryPhone": "+1 415-555-0129",
          "websiteUri": "https://www.sunsetcoffeeroasters.com",
          "regularHours": [
            {
              "openDay": "MONDAY",
              "openTime": { "hours": 8, "minutes": 0 },
              "closeDay": "MONDAY",
              "closeTime": { "hours": 17, "minutes": 0 }
            }
          ]
        }
      }'
      ```

      ```json Response
      {
        "location": {
          "id": "6489021573118254907",
          "accountId": "104312867792031877552",
          "createdDate": "2026-08-27T09:24:02.114Z",
          "updatedDate": "2026-08-27T09:24:02.114Z",
          "revision": "1",
          "googleLocationId": "6489021573118254907"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a Business Profile listing in Google and records it for the site.

    Use this to add a location the Google account doesn't have yet. To record a
    listing that already exists, call Create Location instead.

    Specify at least one of `storefrontAddress` or `serviceArea`. If neither is set,
    the call fails with `INVALID_LOCATION_PAYLOAD`.

    Both can be set together when `serviceArea.businessType` is
    `CUSTOMER_AND_BUSINESS_LOCATION`, which is how a business that has premises and
    also travels to customers is represented.

    A Google account must be connected first. See the
    [Connection API](https://dev.wix.com/docs/api-reference/business-management/google-business-profile/connection-v1).

    Set `validateOnly` to check the details without creating anything: Google
    runs the same validation and returns the same errors, but no listing is
    created and none is recorded for the site. On success the response has no
    `location` — that absence is what "valid" looks like.

    Errors:
    - `CONNECTION_NOT_FOUND`: no Google account is connected. Google isn't called.
    - `TOKEN_RESOLUTION_FAILED`: the stored credentials couldn't be used. Google
    isn't called and nothing is created.
    - `GOOGLE_LOCATION_VALIDATION_FAILED`: Google rejected the details, for
    example an invalid phone number or address, or a listing that needs a
    postcard PIN. The specific reasons are in the error payload, and they're
    worth surfacing: they tell the site owner what to correct. Nothing is
    created.
    - `GOOGLE_API_CALL_FAILED`: the call to Google failed for any other reason.
    Google's status and response are in the error payload. Nothing is created.
    - `LOCATION_DB_PERSIST_FAILED`: Google created the listing but it couldn't be
    recorded for the site. The new Google location ID is in the error payload,
    so keep it: the listing exists in Google and can be recorded with Create
    Location, or removed with Delete Google Location.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/gbp';

    locations.createGoogleLocation(accountId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="accountId" type="string" required>

      GBP account id (numeric, no "accounts/" prefix). The server forms the
      Google resource name as "accounts/&#123;account_id&#125;".

      Constraints: `minLength 1`, `maxLength 255`.

    </ParamField>

    <ParamField body="options" type="CreateGoogleLocationOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="location" type="GoogleLocation" required>

          The Google Location to create. The `name` field is ignored on input
          and populated by Google on response.

          <Expandable title="location properties">

            <ParamField body="name" type="string">

              Google resource name, e.g. "locations/12345678901234567890".
              Empty on create request; populated on response.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="title" type="string" required>

              Business name shown on the listing (Google's "title").

              Constraints: `minLength 1`, `maxLength 300`.

            </ParamField>

            <ParamField body="languageCode" type="string" required>

              BCP-47 language tag, e.g. "en-US". Required by Google, immutable.

              Constraints: `format LANGUAGE_TAG`.

            </ParamField>

            <ParamField body="primaryCategory" type="Category" required>

              Primary business category. Required by Google.

              _Nested structure omitted for brevity — see the `Category` object._

            </ParamField>

            <ParamField body="additionalCategories" type="Category[]">

              Additional categories. Google caps at 9.

              Constraints: `maxItems 9`.

              _Nested structure omitted for brevity — see the `Category[]` object._

            </ParamField>

            <ParamField body="profile" type="Profile">

              Optional. Google asks for a description for every category except lodging
              but doesn't enforce it, so neither does this API. Both an absent profile and
              one with an empty description are accepted.

              _Nested structure omitted for brevity — see the `Profile` object._

            </ParamField>

            <ParamField body="storefrontAddress" type="PostalAddress">

              Storefront postal address. Required for a business that serves customers at
              its premises, and allowed alongside `serviceArea` when that field's
              `businessType` is `CUSTOMER_AND_BUSINESS_LOCATION`.

              _Nested structure omitted for brevity — see the `PostalAddress` object._

            </ParamField>

            <ParamField body="serviceArea" type="ServiceArea">

              Service-area definition (for businesses that serve customers at the
              customer's location, exclusively or in addition to their storefront).

              _Nested structure omitted for brevity — see the `ServiceArea` object._

            </ParamField>

            <ParamField body="primaryPhone" type="string">

              Primary business phone.

              Constraints: `maxLength 50`.

            </ParamField>

            <ParamField body="additionalPhones" type="string[]">

              Additional phones. Google caps at 2.

              Constraints: `maxLength 50`, `maxItems 2`.

            </ParamField>

            <ParamField body="websiteUri" type="string">

              Business website.

              Clear it by masking `websiteUri` on Update Google Location and sending
              `null`. Sending an empty string is rejected.

            </ParamField>

            <ParamField body="storeCode" type="string">

              Merchant-defined store code (free-form identifier shown only to the merchant).

              Constraints: `maxLength 64`.

            </ParamField>

            <ParamField body="regularHours" type="BusinessHours[]">

              Regular weekly business hours.

              Constraints: `maxItems 14`.

              _Nested structure omitted for brevity — see the `BusinessHours[]` object._

            </ParamField>

            <ParamField body="latLng" type="LatLng">

              Geographic coordinates of the storefront.

              _Nested structure omitted for brevity — see the `LatLng` object._

            </ParamField>

            <ParamField body="openInfo" type="OpenInfo">

              Open/closed state of the listing.

              _Nested structure omitted for brevity — see the `OpenInfo` object._

            </ParamField>

            <ParamField body="metadata" type="Metadata">

              Read-only signals Google reports about the listing.

              Only populated on the read paths (Query Google Locations, Get Google
              Location) and on the Update Google Location response. Search Google
              Locations reads Google's catalog rather than the merchant's own listing,
              so it leaves this unset — don't read a default there as a "no".

              **Read-only.**

              _Nested structure omitted for brevity — see the `Metadata` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="validateOnly" type="boolean">

          Dry run. When true the payload is sent to Google's
          accounts.locations.create with its `validateOnly` query param set, so
          Google runs its full validation (categories, address, phone, hours,
          pin-drop requirements) and creates nothing.

          A dry run reports failures exactly as a real create does — the same
          GOOGLE_LOCATION_VALIDATION_FAILED reasons, the same INVALID_LOCATION_PAYLOAD
          for the storefront_address / service_area combinations this service checks
          itself — which is the point: a caller can pre-flight a form before
          committing.

          On success nothing is created at Google, nothing is persisted here, no
          GBPLocation.CREATED event is emitted, and the response's `location` is
          UNSET (Google returns no resource name for a validate-only call). Callers
          must treat an absent `location` as "validation passed", not as a failure.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="location" type="GBPLocation">

      The created location, with `id` set to the ID Google assigned and `accountId`
      carried through from the request.

      UNSET when the request had `validate_only` set: Google validated the payload
      and created nothing, so there is no location to return.

      See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `GOOGLE_API_CALL_FAILED` |  |
    | `400` | `INVALID_ARGUMENT` | `GOOGLE_LOCATION_VALIDATION_FAILED` |  |
    | `400` | `INVALID_ARGUMENT` | `INVALID_LOCATION_PAYLOAD` |  |
    | `500` | `INTERNAL` | `LOCATION_DB_PERSIST_FAILED` |  |
    | `428` | `FAILED_PRECONDITION` | `CONNECTION_NOT_FOUND` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Gbplocation Created | `wix.gbp.location.v1.gbplocation_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-google-location" id="ex-sdk-create-google-location-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-google-location" id="ex-sdk-create-google-location-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-google-location-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-google-location-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a new location on Google</span><span className="wix-picker-desc">Create a Google Business Profile listing and record it for the site</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createGoogleLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-google-location-0"><span className="wix-picker-name">Create a new location on Google</span><span className="wix-picker-desc">Create a Google Business Profile listing and record it for the site</span></label>
    <label htmlFor="ex-sdk-create-google-location-1"><span className="wix-picker-name">createGoogleLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a new location on Google
    import { locations } from "@wix/gbp";

    async function createGoogleLocation() {
      const response = await locations.createGoogleLocation(
        "104312867792031877552",
        {
          location: {
            title: "Sunset Coffee Roasters",
            languageCode: "en-US",
            primaryCategory: { _id: "categories/gcid:coffee_shop" },
            storefrontAddress: {
              countryCode: "US",
              postalCode: "94110",
              administrativeArea: "CA",
              locality: "San Francisco",
              addressLines: ["2436 Mission St"],
            },
            primaryPhone: "+1 415-555-0129",
            websiteUri: "https://www.sunsetcoffeeroasters.com",
            regularHours: [
              {
                openDay: "MONDAY",
                openTime: { hours: 8, minutes: 0 },
                closeDay: "MONDAY",
                closeTime: { hours: 17, minutes: 0 },
              },
            ],
          },
        },
      );

      console.log(response.location);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createGoogleLocation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/gbp';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function createGoogleLocation(accountId,options) {
      const response = await myWixClient.locations.createGoogleLocation(accountId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
