---
title: "Create Experience"
description: "Creates an experience."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates an experience.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-LOCATIONS">Manage Reservation Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/table-reservations/experiences/v1/experiences
    ```

    ## Body Params

    <ParamField body="experience" type="Experience" required>

      Experience to create.

      See the [Experience Object](/api-reference/business-solutions/restaurants/reservations/experiences/experience-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="experience" type="Experience">

      Created experience.

      See the [Experience Object](/api-reference/business-solutions/restaurants/reservations/experiences/experience-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Experience Created | `wix.table_reservations.v1.experience_created` | Triggered when an experience is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST "https://www.wixapis.com/table-reservations/experiences/v1/experiences" \
        -H "Authorization: <AUTH>" \
        -H "Content-Type: application/json" \
        --data '
      {
        "experience": {
          "reservationLocationId": "4d9a2bd1-1c6b-4a3c-9c1d-9b0a9f3c1f11",
          "configuration": {
            "displayInfo": {
              "name": "Weekend brunch"
            },
            "onlineReservations": {
              "partySize": { "min": 1, "max": 8 },
              "minimumReservationNotice": { "unit": "HOURS", "number": 2 },
              "maximumReservationNotice": { "unit": "DAYS", "number": 30 },
              "approval": { "mode": "AUTOMATIC" },
              "maxGuests": { "number": 80 }
            },
            "reservationForm": {
              "customFieldDefinitions": [
                { "name": "Allergies" },
                { "name": "Occasion" }
              ],
              "privacyPolicy": { "enabled": true, "url": { "url": "https://example.com/privacy" } },
              "termsAndConditions": { "enabled": true, "url": { "url": "https://example.com/terms" } }
            },
            "paymentPolicy": { "paymentPolicyType": "FREE" }
          },
          "tags": {
            "publicTags": { "tagIds": ["brunch", "weekend"] }
          }
        }
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "experience": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "reservationLocationId": "4d9a2bd1-1c6b-4a3c-9c1d-9b0a9f3c1f11",
          "revision": "1",
          "createdDate": "2024-06-01T10:00:00.000Z",
          "updatedDate": "2024-06-01T10:00:00.000Z",
          "configuration": {
            "displayInfo": {
              "name": "Weekend brunch"
            },
            "onlineReservations": {
              "partySize": { "min": 1, "max": 8 },
              "minimumReservationNotice": { "unit": "HOURS", "number": 2 },
              "maximumReservationNotice": { "unit": "DAYS", "number": 30 },
              "approval": { "mode": "AUTOMATIC" },
              "maxGuests": { "number": 80 }
            },
            "reservationForm": {
              "customFieldDefinitions": [
                { "id": "f1a2b3c4-d5e6-7890-abcd-ef1234567891", "name": "Allergies", "required": false },
                { "id": "f1a2b3c4-d5e6-7890-abcd-ef1234567892", "name": "Occasion", "required": false }
              ],
              "privacyPolicy": { "enabled": true, "url": { "url": "https://example.com/privacy" } },
              "termsAndConditions": { "enabled": true, "url": { "url": "https://example.com/terms" } }
            },
            "paymentPolicy": { "paymentPolicyType": "FREE" }
          },
          "tags": {
            "publicTags": { "tagIds": ["brunch", "weekend"] }
          }
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates an experience.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-LOCATIONS">Manage Reservation Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { experiences } from '@wix/table-reservations';

    experiences.createExperience(experience): Promise
    ```

    ## Method Parameters

    <ParamField body="experience" type="Experience" required>

      Experience to create.

      See the [Experience Object](/api-reference/business-solutions/restaurants/reservations/experiences/experience-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Experience`.

    See the [Experience Object](/api-reference/business-solutions/restaurants/reservations/experiences/experience-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Experience Created | `wix.table_reservations.v1.experience_created` | Triggered when an experience is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-experience" id="ex-sdk-create-experience-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-experience" id="ex-sdk-create-experience-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-experience" id="ex-sdk-create-experience-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-experience-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-experience-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createExperience</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createExperience (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createExperience (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-experience-0"><span className="wix-picker-name">createExperience</span></label>
    <label htmlFor="ex-sdk-create-experience-1"><span className="wix-picker-name">createExperience (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-experience-2"><span className="wix-picker-name">createExperience (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createExperience
    import { experiences } from '@wix/table-reservations';

    async function createExperience(experience) {
      const response = await experiences.createExperience(experience);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createExperience (self-hosted)
    import { createClient } from '@wix/sdk';
    import { experiences } from '@wix/table-reservations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { experiences },
      // Include the auth strategy and host as relevant
    });

    async function createExperience(experience) {
      const response = await myWixClient.experiences.createExperience(experience);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createExperience (with elevated permissions)
    import { experiences } from '@wix/table-reservations';
    import { auth } from '@wix/essentials';

    async function myCreateExperienceMethod(experience) {
      const elevatedCreateExperience = auth.elevate(experiences.createExperience);
      const response = await elevatedCreateExperience(experience);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
