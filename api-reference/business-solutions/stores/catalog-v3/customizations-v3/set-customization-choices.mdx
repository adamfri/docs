---
title: "Set Customization Choices"
description: "Sets a customization's choices."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets a customization's choices. Any and all existing choices will be overridden.

    <Note>

    A choice cannot be overridden if it is assigned to one or more products.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CUSTOMIZATION_WRITE">Customization write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/customizations/{customizationId}/set-choices
    ```

    ## Path Params

    <ParamField path="customizationId" type="string" required>

      Customization ID.

    </ParamField>

    ## Body Params

    <ParamField body="choices" type="Choice[]" required>

      Choices to set.

      Constraints: `minItems 1`, `maxItems 1000`.

      <Expandable title="choices properties">

        <ParamField body="id" type="string">

          Choice ID.

          **Read-only.** Constraints: `format GUID`.

        </ParamField>

        <ParamField body="choiceType" type="string">

          Choice type.

          > **Notes:**
          > + For `customizationRenderType: SWATCH_CHOICES`, the supported `choiceType` value is: `ONE_COLOR`.
          > + For a `customizationRenderType` of `TEXT_CHOICES`, the supported `choiceType` value is: `CHOICE_TEXT`.

          Constraints: `immutable`.

          **Supported values:**

          - `CHOICE_TEXT` — For a `customizationRenderType` of `TEXT_CHOICES`.
          - `ONE_COLOR` — For `customizationRenderType: SWATCH_CHOICES`.

        </ParamField>

        <ParamField body="key" type="string">

          A read-only identifier generated from the choice name.

          Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Choice name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ParamField>

        <ParamField body="defaultAddedPrice" type="string">

          Default amount added to a product's price when this customization is assigned to a modifier.

        </ParamField>

        <ParamField body="displayImage" type="Image">

          Optional image displayed alongside a `ONE_COLOR` choice in the storefront.
          Only valid for `ONE_COLOR` choices; not populated for other choice types.

          <Expandable title="displayImage properties">

            <ParamField body="id" type="string">

              WixMedia image ID.

              Constraints: `maxLength 400`.

            </ParamField>

            <ParamField body="url" type="string">

              Image URL.

              Constraints: `format WEB_URL`.

            </ParamField>

            <ParamField body="height" type="integer">

              Original image height.

              **Read-only.**

            </ParamField>

            <ParamField body="width" type="integer">

              Original image width.

              **Read-only.**

            </ParamField>

            <ParamField body="altText" type="string">

              Image alt text.

            </ParamField>

            <ParamField body="filename" type="string">

              Image filename.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="primaryChoiceIds" type="string[]">

          IDs of the primary choices this choice is grouped under.

          A choice that has `primaryChoiceIds` is a "linked" choice; a choice without `primaryChoiceIds` is a "primary" choice.
          A linked choice can reference up to 4 primary choices and appears nested under each of them on the storefront,
          rather than in the top-level choice list.

          Only one level of grouping is supported: a primary choice cannot itself be a linked choice.

          **Developer Preview.** Constraints: `format GUID`, `maxItems 4`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Supported values: `ASSIGNED_PRODUCTS_COUNT`

      Constraints: `maxItems 100`.

      **Supported values:**

      - `ASSIGNED_PRODUCTS_COUNT` — Assigned products count.
      - `CHOICE_DISPLAY_IMAGE` — Returns display images for choices.

    </ParamField>

    ## Response Object

    <ResponseField name="customization" type="Customization">

      Updated customization.

      See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `DOESNT_SUPPORT_CHOICES` | `FREE_TEXT` render type doesn't support choices. Use `SWATCH_CHOICES` or `TEXT_CHOICES` render type instead. |
    | `428` | `FAILED_PRECONDITION` | `CHOICES_LIMIT_EXCEEDED` | Number of choices exceeded the limit of 100 per customization. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CHOICE_TYPE` | Choice type doesn't match the customization type. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CHOICE_VALUE` | Choice value doesn't match the choice type. For `SWATCH_CHOICES`, use `ONE_COLOR` choice type. For `TEXT_CHOICES`, use `CHOICE_TEXT` choice type. |
    | `400` | `INVALID_ARGUMENT` | `NOT_ALLOWED_DELETE_CHOICE_ASSIGNED_TO_PRODUCT` | Choice is assigned to at least 1 product. Remove the choice from all products before deleting. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_CHOICE_NAME` | Duplicate choice name found. Each choice name must be unique within the customization. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_SELF_REFERENCE` | Choice references itself in `primaryChoiceIds`. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_NOT_FOUND` | `primaryChoiceIds` contains an ID that doesn't match any existing choice in the customization. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_HIERARCHY_VIOLATION` | A choice that is a primary of other choices can't also be a linked choice. Only a single level of grouping is supported. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Customization Updated | `wix.stores.catalog.v3.customization_updated` | Triggered when a customization is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Sets given choices to an existing customization instead of their current ones

    <CodeGroup>

      ```bash Request
      curl -X POST \
       'https://www.wixapis.com/stores/v3/customizations/4c7fd83b-f17b-4e2f-a0ce-4758201fd464/set-choices' \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -d '{
        "choices": [
          {
            "choiceType": "ONE_COLOR",
            "name": "White",
            "colorCode": "#FFFFFF"
          },
          {
            "choiceType": "ONE_COLOR",
            "name": "Red",
            "colorCode": "#FF0000"
          }
        ]
       }'
      ```

      ```json Response
      {
          "customization": {
              "id": "6f13ad1c-c49e-4279-b49c-35f8b6c05eaf",
              "revision": "2",
              "createdDate": "2024-06-27T12:32:48.120Z",
              "updatedDate": "2024-07-03T08:36:53.175Z",
              "key": "Color Scheme",
              "name": "Color Scheme",
              "customizationType": "MODIFIER",
              "customizationRenderType": "SWATCH_CHOICES",
              "choicesSettings": {
                  "choices": [{
                      "id": "0045afd2-d037-43a8-b2e6-60a015fc75cd",
                      "choiceType": "ONE_COLOR",
                      "key": "White",
                      "name": "White",
                      "colorCode": "#FFFFFF"
                  }, {
                      "id": "c0a1be19-4f1a-4843-9e1f-8befd0602ad9",
                      "choiceType": "ONE_COLOR",
                      "key": "Red",
                      "name": "Red",
                      "colorCode": "#FF0000"
                  }]
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets a customization's choices. Any and all existing choices will be overridden.

    <Note>

    A choice cannot be overridden if it is assigned to one or more products.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CUSTOMIZATION_WRITE">Customization write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customizationsV3 } from '@wix/stores';

    customizationsV3.setCustomizationChoices(customizationId, choices, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="customizationId" type="string" required>

      Customization ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="choices" type="SetCustomizationChoices[]" required>

      Choices to set.

      Constraints: `minItems 1`, `maxItems 1000`.

      <Expandable title="choices properties">

        <ParamField body="_id" type="string">

          Choice ID.

          **Read-only.** Constraints: `format GUID`.

        </ParamField>

        <ParamField body="choiceType" type="string">

          Choice type.

          > **Notes:**
          > + For `customizationRenderType: SWATCH_CHOICES`, the supported `choiceType` value is: `ONE_COLOR`.
          > + For a `customizationRenderType` of `TEXT_CHOICES`, the supported `choiceType` value is: `CHOICE_TEXT`.

          Constraints: `immutable`.

          **Supported values:**

          - `CHOICE_TEXT` — For a `customizationRenderType` of `TEXT_CHOICES`.
          - `ONE_COLOR` — For `customizationRenderType: SWATCH_CHOICES`.

        </ParamField>

        <ParamField body="key" type="string">

          A read-only identifier generated from the choice name.

          Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Choice name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ParamField>

        <ParamField body="defaultAddedPrice" type="string">

          Default amount added to a product's price when this customization is assigned to a modifier.

        </ParamField>

        <ParamField body="displayImage" type="string">

          Optional image displayed alongside a `ONE_COLOR` choice in the storefront.
          Only valid for `ONE_COLOR` choices; not populated for other choice types.

        </ParamField>

        <ParamField body="primaryChoiceIds" type="string[]">

          IDs of the primary choices this choice is grouped under.

          A choice that has `primaryChoiceIds` is a "linked" choice; a choice without `primaryChoiceIds` is a "primary" choice.
          A linked choice can reference up to 4 primary choices and appears nested under each of them on the storefront,
          rather than in the top-level choice list.

          Only one level of grouping is supported: a primary choice cannot itself be a linked choice.

          **Developer Preview.** Constraints: `format GUID`, `maxItems 4`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="SetCustomizationChoicesOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Supported values: `ASSIGNED_PRODUCTS_COUNT`

          Constraints: `maxItems 100`.

          **Supported values:**

          - `ASSIGNED_PRODUCTS_COUNT` — Assigned products count.
          - `CHOICE_DISPLAY_IMAGE` — Returns display images for choices.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="customization" type="Customization">

      Updated customization.

      See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `DOESNT_SUPPORT_CHOICES` | `FREE_TEXT` render type doesn't support choices. Use `SWATCH_CHOICES` or `TEXT_CHOICES` render type instead. |
    | `428` | `FAILED_PRECONDITION` | `CHOICES_LIMIT_EXCEEDED` | Number of choices exceeded the limit of 100 per customization. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CHOICE_TYPE` | Choice type doesn't match the customization type. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CHOICE_VALUE` | Choice value doesn't match the choice type. For `SWATCH_CHOICES`, use `ONE_COLOR` choice type. For `TEXT_CHOICES`, use `CHOICE_TEXT` choice type. |
    | `400` | `INVALID_ARGUMENT` | `NOT_ALLOWED_DELETE_CHOICE_ASSIGNED_TO_PRODUCT` | Choice is assigned to at least 1 product. Remove the choice from all products before deleting. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_CHOICE_NAME` | Duplicate choice name found. Each choice name must be unique within the customization. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_SELF_REFERENCE` | Choice references itself in `primaryChoiceIds`. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_NOT_FOUND` | `primaryChoiceIds` contains an ID that doesn't match any existing choice in the customization. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_HIERARCHY_VIOLATION` | A choice that is a primary of other choices can't also be a linked choice. Only a single level of grouping is supported. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Customization Updated | `wix.stores.catalog.v3.customization_updated` | Triggered when a customization is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-set-customization-choices" id="ex-sdk-set-customization-choices-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-set-customization-choices" id="ex-sdk-set-customization-choices-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-set-customization-choices-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-set-customization-choices-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Set choices for a customization</span><span className="wix-picker-desc">Replace all choices in an existing customization with new ones</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setCustomizationChoices (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-set-customization-choices-0"><span className="wix-picker-name">Set choices for a customization</span><span className="wix-picker-desc">Replace all choices in an existing customization with new ones</span></label>
    <label htmlFor="ex-sdk-set-customization-choices-1"><span className="wix-picker-name">setCustomizationChoices (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Set choices for a customization
    import { customizationsV3 } from "@wix/stores";

    const customizationId = "4c7fd83b-f17b-4e2f-a0ce-4758201fd464";

    const choices = [
      {
        choiceType: customizationsV3.ChoiceType.ONE_COLOR,
        name: "White",
        colorCode: "#FFFFFF"
      },
      {
        choiceType: customizationsV3.ChoiceType.ONE_COLOR,
        name: "Red",
        colorCode: "#FF0000"
      }
    ];

    async function setCustomizationChoices() {
      const response = await customizationsV3.setCustomizationChoices(customizationId, choices);
    }

    /* Promise resolves to:
     * {
     *   "_id": "6f13ad1c-c49e-4279-b49c-35f8b6c05eaf",
     *   "revision": "2",
     *   "name": "Color Scheme",
     *   "customizationType": "MODIFIER",
     *   "customizationRenderType": "SWATCH_CHOICES",
     *   "choicesSettings": {
     *     "choices": [
     *       {
     *         "id": "0045afd2-d037-43a8-b2e6-60a015fc75cd",
     *         "choiceType": "ONE_COLOR",
     *         "name": "White",
     *         "colorCode": "#FFFFFF"
     *       },
     *       {
     *         "id": "c0a1be19-4f1a-4843-9e1f-8befd0602ad9",
     *         "choiceType": "ONE_COLOR",
     *         "name": "Red",
     *         "colorCode": "#FF0000"
     *       }
     *     ]
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setCustomizationChoices (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customizationsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customizationsV3 },
      // Include the auth strategy and host as relevant
    });

    async function setCustomizationChoices(customizationId,choices,options) {
      const response = await myWixClient.customizationsV3.setCustomizationChoices(customizationId,choices,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
