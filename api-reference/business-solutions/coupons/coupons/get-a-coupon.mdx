---
title: "Get A Coupon"
description: "Retrieves a coupon by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a coupon by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-COUPONS.MANAGE-COUPONS">Manage Coupons</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/stores/v2/coupons/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the coupon to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="coupon" type="Coupon">

      Retrieved coupon.

      See the [Coupon Object](/api-reference/business-solutions/coupons/coupons/coupon-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Get coupon by ID.

    <CodeGroup>

      ```bash Request
      curl -X GET \
         'https://www.wixapis.com/stores/v2/coupons/abeb638b-f9f4-4bb8-8fe7-2319504df6d9' \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "coupon": {
          "id": "abeb638b-f9f4-4bb8-8fe7-2319504df6d9",
          "specification": {
            "name": "Shark",
            "code": "Shark",
            "startTime": "1719390501000",
            "limitedToOneItem": false,
            "active": true,
            "scope": {
              "namespace": "events",
              "group": {
                "name": "ticket"
              }
            },
            "moneyOffAmount": 10,
            "type": "MoneyOff"
          },
          "dateCreated": "1719390501000",
          "appId": "d655aab0-fd9e-44d8-9020-20700f0047c3"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a coupon by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-COUPONS.MANAGE-COUPONS">Manage Coupons</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { coupons } from '@wix/marketing';

    coupons.getCoupon(_id): Promise<Coupon>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the coupon to retrieve.

    </ParamField>

    ## Returns

    Returns `Promise<Coupon>`.

    See the [Coupon Object](/api-reference/business-solutions/coupons/coupons/coupon-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-a-coupon" id="ex-sdk-get-a-coupon-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-a-coupon" id="ex-sdk-get-a-coupon-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-a-coupon" id="ex-sdk-get-a-coupon-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-a-coupon-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-a-coupon-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getCoupon</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCoupon (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCoupon (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-a-coupon-0"><span className="wix-picker-name">getCoupon</span></label>
    <label htmlFor="ex-sdk-get-a-coupon-1"><span className="wix-picker-name">getCoupon (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-a-coupon-2"><span className="wix-picker-name">getCoupon (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getCoupon
    import { coupons } from '@wix/marketing';

    async function getCoupon(_id) {
      const response = await coupons.getCoupon(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCoupon (self-hosted)
    import { createClient } from '@wix/sdk';
    import { coupons } from '@wix/marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { coupons },
      // Include the auth strategy and host as relevant
    });

    async function getCoupon(_id) {
      const response = await myWixClient.coupons.getCoupon(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCoupon (with elevated permissions)
    import { coupons } from '@wix/marketing';
    import { auth } from '@wix/essentials';

    async function myGetCouponMethod(_id) {
      const elevatedGetCoupon = auth.elevate(coupons.getCoupon);
      const response = await elevatedGetCoupon(_id);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
