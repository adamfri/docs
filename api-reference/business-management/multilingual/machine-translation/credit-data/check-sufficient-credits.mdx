---
title: "Check Sufficient Credits"
description: "Checks whether a site has enough credits to translate the specified number of words."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks whether a site has enough credits to translate the specified number of words.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MULTILINGUAL.MANAGE_TRANSLATIONS">Wix Multilingual</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/translation-credits/v1/credit/is-eligible
    ```

    ## Body Params

    <ParamField body="wordCount" type="integer" required>

      Number of words to translate.

      Constraints: `min 0`.

    </ParamField>

    ## Response Object

    <ResponseField name="sufficientCredits" type="boolean">

      Whether the site has enough credits to translate the specified number of words.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `FEATURE_NOT_FOUND` | Couldn't find the feature. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/translation-credits/v1/credit/is-eligible' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "wordCount": 100
       }'
      ```

      ```json Response
      {
        "sufficientCredits": true
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks whether a site has enough credits to translate the specified number of words.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MULTILINGUAL.MANAGE_TRANSLATIONS">Wix Multilingual</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { translationCredits } from '@wix/multilingual';

    translationCredits.checkSufficientCredits(wordCount): Promise<CheckSufficientCreditsResponse>
    ```

    ## Method Parameters

    <ParamField body="wordCount" type="integer" required>

      Number of words to translate.

      Constraints: `min 0`.

    </ParamField>

    ## Returns

    Returns `Promise<CheckSufficientCreditsResponse>`.

    <ResponseField name="sufficientCredits" type="boolean">

      Whether the site has enough credits to translate the specified number of words.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `FEATURE_NOT_FOUND` | Couldn't find the feature. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-check-sufficient-credits" id="ex-sdk-check-sufficient-credits-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-check-sufficient-credits" id="ex-sdk-check-sufficient-credits-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-check-sufficient-credits" id="ex-sdk-check-sufficient-credits-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-check-sufficient-credits-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-check-sufficient-credits-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">checkSufficientCredits</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkSufficientCredits (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkSufficientCredits (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-check-sufficient-credits-0"><span className="wix-picker-name">checkSufficientCredits</span></label>
    <label htmlFor="ex-sdk-check-sufficient-credits-1"><span className="wix-picker-name">checkSufficientCredits (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-check-sufficient-credits-2"><span className="wix-picker-name">checkSufficientCredits (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript checkSufficientCredits
    import { translationCredits } from '@wix/multilingual';

    async function checkSufficientCredits(wordCount) {
      const response = await translationCredits.checkSufficientCredits(wordCount);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkSufficientCredits (self-hosted)
    import { createClient } from '@wix/sdk';
    import { translationCredits } from '@wix/multilingual';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { translationCredits },
      // Include the auth strategy and host as relevant
    });

    async function checkSufficientCredits(wordCount) {
      const response = await myWixClient.translationCredits.checkSufficientCredits(wordCount);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkSufficientCredits (with elevated permissions)
    import { translationCredits } from '@wix/multilingual';
    import { auth } from '@wix/essentials';

    async function myCheckSufficientCreditsMethod(wordCount) {
      const elevatedCheckSufficientCredits = auth.elevate(translationCredits.checkSufficientCredits);
      const response = await elevatedCheckSufficientCredits(wordCount);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
