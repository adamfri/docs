---
title: "List Secret Info"
description: "Retrieves a list of secrets."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of secrets.

    <Note>

    This method doesn't return the secret's value for security reasons. To retrieve the value, call [Get Secret Value](https://dev.wix.com/docs/rest/business-management/secrets/get-secret-value).

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.VELO.MANAGE_SECRETS">Manage Secrets</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/secrets-service/api/v1/secrets
    ```

    ## Response Object

    <ResponseField name="secrets" type="Secret[]">

      A list of secrets with encrypted values.

      See the [Secret Object](/api-reference/business-management/secrets/secret-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET 'https://www.wixapis.com/_api/cloud-secrets-vault-server/api/v1/secrets' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "secrets": [
          {
            "id": "a560ff70-3e9b-41b6-b66d-cf3434085f7d",
            "name": "secret_key",
            "description": "AWS secret access key",
            "createdDate": "2024-09-12T12:32:15.000Z",
            "updatedDate": "2024-09-12T12:32:15.000Z"
          }
        ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of secrets.

    <Note>

    This method doesn't return the secret's value for security reasons. To retrieve the value, call [Get Secret Value](https://dev.wix.com/docs/rest/business-management/secrets/get-secret-value).

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.VELO.MANAGE_SECRETS">Manage Secrets</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { secrets } from '@wix/secrets';

    secrets.listSecretInfo(): Promise<ListSecretInfoResponse>
    ```

    ## Returns

    Returns `Promise<ListSecretInfoResponse>`.

    <ResponseField name="secrets" type="Secret[]">

      A list of secrets with encrypted values.

      See the [Secret Object](/api-reference/business-management/secrets/secret-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-secret-info" id="ex-sdk-list-secret-info-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-secret-info" id="ex-sdk-list-secret-info-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-secret-info" id="ex-sdk-list-secret-info-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-secret-info-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-secret-info-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listSecretInfo</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listSecretInfo (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listSecretInfo (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-secret-info-0"><span className="wix-picker-name">listSecretInfo</span></label>
    <label htmlFor="ex-sdk-list-secret-info-1"><span className="wix-picker-name">listSecretInfo (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-secret-info-2"><span className="wix-picker-name">listSecretInfo (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listSecretInfo
    import { secrets } from '@wix/secrets';

    async function listSecretInfo() {
      const response = await secrets.listSecretInfo();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listSecretInfo (self-hosted)
    import { createClient } from '@wix/sdk';
    import { secrets } from '@wix/secrets';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { secrets },
      // Include the auth strategy and host as relevant
    });

    async function listSecretInfo() {
      const response = await myWixClient.secrets.listSecretInfo();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listSecretInfo (with elevated permissions)
    import { secrets } from '@wix/secrets';
    import { auth } from '@wix/essentials';

    async function myListSecretInfoMethod() {
      const elevatedListSecretInfo = auth.elevate(secrets.listSecretInfo);
      const response = await elevatedListSecretInfo();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
