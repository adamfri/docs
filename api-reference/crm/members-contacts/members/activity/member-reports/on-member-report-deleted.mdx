---
title: "onMemberReportDeleted"
description: "Triggered when a report is deleted."
icon: "bolt"
---

Triggered when a report is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.members.v1.member_report_deleted` |
| Package | `@wix/members` |
| Permissions | `Manage Members` |

## Examples

<CodeGroup>

  ```javascript onMemberReportDeleted
  import { memberReport } from '@wix/members';

  memberReport.onMemberReportDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onMemberReportDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { memberReport } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        memberReport,
      },
  });

  wixClient.memberReport.onMemberReportDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
