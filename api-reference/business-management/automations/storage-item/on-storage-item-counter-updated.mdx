---
title: "onStorageItemCounterUpdated"
description: "The onStorageItemCounterUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.automations.storage.v1.storage_item_storage_item_counter_updated` |
| Package | `@wix/automations` |
| Permissions | `Manage Stores`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onStorageItemCounterUpdated
  import { storageItem } from '@wix/automations';

  storageItem.onStorageItemCounterUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onStorageItemCounterUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { storageItem } from '@wix/automations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        storageItem,
      },
  });

  wixClient.storageItem.onStorageItemCounterUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
