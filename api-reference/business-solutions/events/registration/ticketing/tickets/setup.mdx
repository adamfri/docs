---
title: "Setup"
description: "To use the Tickets API, install the @wix/events package using npm or Yarn: or Then import { tickets } from @wix/events:"
icon: "file-lines"
---

<Note>This article applies to the JavaScript SDK only.</Note>

To use the Tickets API, install the `@wix/events` package using npm or Yarn:
  ```bash
npm install @wix/events
```

or

```bash
yarn add @wix/events
```

Then import `{ tickets }` from `@wix/events`:

```js
import { tickets } from '@wix/events'
```
