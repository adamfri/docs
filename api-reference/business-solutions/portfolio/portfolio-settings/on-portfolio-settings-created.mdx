---
title: "onPortfolioSettingsCreated"
description: "Triggered when portfolio settings are created."
icon: "bolt"
---

Triggered when portfolio settings are created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.portfolio_app.v1.portfolio_settings_created` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onPortfolioSettingsCreated
  import { portfolioSettings } from '@wix/portfolio';

  portfolioSettings.onPortfolioSettingsCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPortfolioSettingsCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { portfolioSettings } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        portfolioSettings,
      },
  });

  wixClient.portfolioSettings.onPortfolioSettingsCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
