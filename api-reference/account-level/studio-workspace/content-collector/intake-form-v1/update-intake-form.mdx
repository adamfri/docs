---
title: "Update Intake Form"
description: "Updates an intake form."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an intake form.

    Each time the intake form is updated, `revision` increments by 1. The current
    `revision` must be passed when updating the intake form. This ensures you're
    working with the latest intake form and prevents unintended overwrites.

    Only the fields included in `fieldMask.paths` are updated. To share the form
    with a client, add their email address to `clientEmails`. Adding the first
    email stamps `publishDate`; removing all emails clears it.

    To update multiple intake forms in a single request, call [Bulk Update Intake Forms](https://dev.wix.com/docs/api-reference/account-level/partners/content-collector/intake-form-v1/bulk-update-intake-forms).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.CONTENT-COLLECTOR">SCOPE.PARTNERS.CONTENT-COLLECTOR</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/partners/content-collector/v1/intake-forms/{intakeForm.id}
    ```

    ## Path Params

    <ParamField path="intakeForm.id" type="string" required>

      Intake form ID.

    </ParamField>

    ## Body Params

    <ParamField body="intakeForm" type="IntakeForm" required>

      IntakeForm to be updated, may be partial.

      See the [Intake Form Object](/api-reference/account-level/studio-workspace/content-collector/intake-form-v1/intake-form-object) for this object's fields.

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Constraints: `maxItems 5`.

      **Supported values:**

      - `CONTEXT`
      - `SOURCE_MATERIALS`
      - `INTRO_DATA`

    </ParamField>

    ## Response Object

    <ResponseField name="intakeForm" type="IntakeForm">

      Updated IntakeForm.

      See the [Intake Form Object](/api-reference/account-level/studio-workspace/content-collector/intake-form-v1/intake-form-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Shares the form with a client by adding a client email

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/intake-forms/v1/intake-forms/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "intakeForm": {
            "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
            "revision": "1",
            "clientEmails": ["jane@bloombakery.com"]
          }
        }'
      ```

      ```json Response
      {
        "intakeForm": {
          "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
          "revision": "2",
          "createdDate": "2024-03-18T17:22:10.299Z",
          "updatedDate": "2024-03-19T09:14:02.118Z",
          "clientName": "Jane Cohen",
          "project": {
            "name": "Bloom Bakery Website",
            "industry": "FOOD_AND_BEVERAGE"
          },
          "clientEmails": ["jane@bloombakery.com"],
          "userId": "f1a2b3c4-d5e6-4789-9abc-1234567890ab",
          "accountId": "a9b8c7d6-e5f4-4321-8765-0fedcba98765",
          "publishDate": "2024-03-19T09:14:02.118Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an intake form.

    Each time the intake form is updated, `revision` increments by 1. The current
    `revision` must be passed when updating the intake form. This ensures you're
    working with the latest intake form and prevents unintended overwrites.

    Only the fields included in `fieldMask.paths` are updated. To share the form
    with a client, add their email address to `clientEmails`. Adding the first
    email stamps `publishDate`; removing all emails clears it.

    To update multiple intake forms in a single request, call [Bulk Update Intake Forms](https://dev.wix.com/docs/api-reference/account-level/partners/content-collector/intake-form-v1/bulk-update-intake-forms).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.CONTENT-COLLECTOR">SCOPE.PARTNERS.CONTENT-COLLECTOR</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contentCollectorIntakeForms } from '@wix/content-collector-intake-forms';

    contentCollectorIntakeForms.updateIntakeForm(_id, intakeForm, options): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Intake form ID.

      **Read-only.**

    </ParamField>

    <ParamField body="intakeForm" type="UpdateIntakeForm" required>

      IntakeForm to be updated, may be partial.

      <Expandable title="intakeForm properties">

        <ParamField body="_id" type="string">

          Intake form ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the intake form is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the intake form.

          Ignored when creating an intake form.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the IntakeForm was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the IntakeForm was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="context" type="string">

          AI-generated background context for the intake form, stored as free-form
          text. Used as input when generating the questionnaire; stored and returned
          as-is and not otherwise interpreted by this service. Returned only when
          `CONTEXT` is included in the request's `fields`.

        </ParamField>

        <ParamField body="clientName" type="string">

          Client name.

        </ParamField>

        <ParamField body="project" type="Project">

          Project information.

          <Expandable title="project properties">

            <ParamField body="name" type="string">

              Project name.

            </ParamField>

            <ParamField body="industry" type="string">

              Industry vertical.

              **Supported values:**

              - `BEAUTY_AND_WELLNESS`
              - `CREATIVE`
              - `EDUCATION`
              - `FIELD_AND_CONSTRUCTION`
              - `FITNESS`
              - `HEALTH`
              - `FOOD_AND_BEVERAGE`
              - `NON_PROFIT_AND_COMMUNITIES`
              - `REAL_ESTATE`
              - `SCIENCE_AND_TECHNOLOGY`
              - `TRAVEL_AND_TOURISM`
              - `CONSULTING_AND_MARKETING`
              - `LEGAL_AND_FINANCE`
              - `FASHION_AND_APPAREL`
              - `ENTERTAINMENT`
              - `RESTAURANT`
              - `TECHNOLOGY`
              - `WELLNESS`
              - `CONSULTING`
              - `HEALTHCARE`
              - `OTHER`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="userId" type="string">

          Owner user ID.

          **Read-only.**

        </ParamField>

        <ParamField body="sourceMaterials" type="SourceMaterials">

          Source materials used as input for AI generation.

          <Expandable title="sourceMaterials properties">

            <ParamField body="projectMaterials" type="string">

              User-provided project description used as input for AI generation.

            </ParamField>

            <ParamField body="filesMarkdown" type="string">

              Markdown rendering of the files uploaded by the user, used as input for
              AI generation.

            </ParamField>

            <ParamField body="existingQuestionnaire" type="string">

              Existing questionnaire provided by the user, used as input for AI generation.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="clientFormInteraction" type="ClientFormInteraction">

          The client's interaction with the form: when they last edited it, the
          submission ID, and when they finally submitted. Populated asynchronously from
          Wix Forms events — fields may be empty until a submission event is processed.
          The form is considered submitted only when `submission_date` is set.

          **Read-only.**

          <Expandable title="clientFormInteraction properties">

            <ParamField body="submissionDate" type="datetime">

              Date and time the client finally submitted the form. When set, the form is considered submitted.

              **Read-only.**

            </ParamField>

            <ParamField body="lastEditDate" type="datetime">

              Date and time the client last edited the form.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="accountId" type="string">

          Account ID.

          **Read-only.**

        </ParamField>

        <ParamField body="teamMemberIds" type="string[]">

          IDs of the team members to notify about the form. Listed team members are
          notified through Wix Automations when the client submits the form or adds a
          note to it.

          Constraints: `format GUID`, `maxItems 50`.

        </ParamField>

        <ParamField body="clientEmails" type="string[]">

          Email addresses of the clients the form is shared with. Adding the first
          email shares the form and stamps `publishDate`; removing all emails clears
          `publishDate` (see `publishDate`). A client can retrieve the form through
          `GetClientIntakeForm` only if their email is listed here.

          Constraints: `format EMAIL`, `maxItems 20`.

        </ParamField>

        <ParamField body="customFields" type="object">

          Free-form custom data stored verbatim with the intake form. Accepts any JSON
          structure, isn't interpreted or validated by the service, and is returned
          as-is. Use it to attach your own structured data to an intake form.

        </ParamField>

        <ParamField body="publishDate" type="datetime">

          Date and time the form was first shared with clients (the first time client emails were added).
          Cleared if all client emails are removed; re-stamped if client emails are added again.

          **Read-only.**

        </ParamField>

        <ParamField body="introData" type="IntroData">

          Intro-screen content shown to the client before they sign in (title and
          message). Stored on the form so the anonymous intro stage can render it
          without fetching the questionnaire. Returned only when `INTRO_DATA` is
          included in the request's `fields`.

          <Expandable title="introData properties">

            <ParamField body="title" type="string">

              Welcome headline shown at the top of the intro screen. Unset means the
              partner never customized it (the client app falls back to its default
              headline); an empty string means the partner explicitly cleared it.

            </ParamField>

            <ParamField body="message" type="string">

              Welcome message body shown on the intro screen, stored as rich-text HTML.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateIntakeFormOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Constraints: `maxItems 5`.

          **Supported values:**

          - `CONTEXT`
          - `SOURCE_MATERIALS`
          - `INTRO_DATA`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `IntakeForm`.

    See the [Intake Form Object](/api-reference/account-level/studio-workspace/content-collector/intake-form-v1/intake-form-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Intake Form Updated | `wix.partners.content_collector.v1.intake_form_updated` | Triggered when an intake form is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-intake-form" id="ex-sdk-update-intake-form-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-intake-form" id="ex-sdk-update-intake-form-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-intake-form-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-intake-form-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update an intake form</span><span className="wix-picker-desc">Shares the form with a client by adding a client email</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateIntakeForm (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-intake-form-0"><span className="wix-picker-name">Update an intake form</span><span className="wix-picker-desc">Shares the form with a client by adding a client email</span></label>
    <label htmlFor="ex-sdk-update-intake-form-1"><span className="wix-picker-name">updateIntakeForm (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update an intake form
    import { contentCollectorIntakeForms } from "@wix/content-collector-intake-forms";

    const intakeFormId = "8046df3c-7575-4098-a5ab-c91ad8f33c47";

    const intakeForm = {
      revision: "1",
      clientEmails: ["jane@bloombakery.com"],
    };

    async function updateIntakeForm() {
      const response = await contentCollectorIntakeForms.updateIntakeForm(
        intakeFormId,
        intakeForm
      );
    }

    /* Promise resolves to:
     * {
     *   "_id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
     *   "revision": "2",
     *   "_createdDate": "2024-03-18T17:22:10.299Z",
     *   "_updatedDate": "2024-03-19T09:14:02.118Z",
     *   "clientName": "Jane Cohen",
     *   "project": {
     *     "name": "Bloom Bakery Website",
     *     "industry": "FOOD_AND_BEVERAGE"
     *   },
     *   "clientEmails": ["jane@bloombakery.com"],
     *   "userId": "f1a2b3c4-d5e6-4789-9abc-1234567890ab",
     *   "accountId": "a9b8c7d6-e5f4-4321-8765-0fedcba98765",
     *   "publishDate": "2024-03-19T09:14:02.118Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateIntakeForm (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contentCollectorIntakeForms } from '@wix/content-collector-intake-forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contentCollectorIntakeForms },
      // Include the auth strategy and host as relevant
    });

    async function updateIntakeForm(_id,intakeForm,options) {
      const response = await myWixClient.contentCollectorIntakeForms.updateIntakeForm(_id,intakeForm,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
