---
title: "onReservationCreated"
description: "Triggered when a held reservation is created."
icon: "bolt"
---

Triggered when a held reservation is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.table_reservations.v1.reservation_created` |
| Package | `@wix/table-reservations` |
| Permissions | `Manage Reservations (Medium)`, `Manage Reservations (Full)`, `Manage Restaurants - all permissions`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onReservationCreated
  import { reservations } from '@wix/table-reservations';

  reservations.onReservationCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReservationCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reservations } from '@wix/table-reservations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reservations,
      },
  });

  wixClient.reservations.onReservationCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
