---
title: "onScheduleCancelled"
description: "Triggered when a schedule is canceled."
icon: "bolt"
---

Triggered when a schedule is canceled.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.calendar.v3.schedule_cancelled` |
| Package | `@wix/calendar` |
| Permissions | `Read Calendar - Including PI`, `Manage Calendars`, `Manage Bookings`, `Read Bookings - Including Participants`, `Manage Bookings - all permissions`, `Read bookings calendar - including participants` |

## Examples

<CodeGroup>

  ```javascript onScheduleCancelled
  import { schedules } from '@wix/calendar';

  schedules.onScheduleCancelled((event) => {
      // handle your event here
  });
  ```

  ```javascript onScheduleCancelled (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { schedules } from '@wix/calendar';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        schedules,
      },
  });

  wixClient.schedules.onScheduleCancelled((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
