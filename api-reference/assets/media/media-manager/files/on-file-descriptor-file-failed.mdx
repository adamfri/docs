---
title: "onFileDescriptorFileFailed"
description: "Triggered when a file fails during essential post-upload processing."
icon: "bolt"
---

Triggered when a file fails during essential post-upload processing.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.media.site_media.v1.file_descriptor_file_failed` |
| Package | `@wix/media` |
| Permissions | `Manage Bookings Services and Settings`, `Manage Events`, `Manage Challenges`, `Read Media Manager`, `Manage Media Manager`, `Manage Portfolio`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onFileDescriptorFileFailed
  import { files } from '@wix/media';

  files.onFileDescriptorFileFailed((event) => {
      // handle your event here
  });
  ```

  ```javascript onFileDescriptorFileFailed (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { files } from '@wix/media';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        files,
      },
  });

  wixClient.files.onFileDescriptorFileFailed((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
