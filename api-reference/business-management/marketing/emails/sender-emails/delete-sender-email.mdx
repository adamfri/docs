---
title: "Delete Sender Email"
description: "Deletes a sender email."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a sender email.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/sender-emails/v1/sender-emails/{senderEmailId}
    ```

    ## Path Params

    <ParamField path="senderEmailId" type="string" required>

      ID of the sender email to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteSenderEmailResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Email Deleted | `wix.promote.v1.sender_email_deleted` | Triggered when a sender email is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE 'https://www.wixapis.com/sender-emails/v1/sender-emails/30057014-8ecc-4a1b-9c53-bcc7dd033bb5' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a sender email.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderEmails } from '@wix/sender-emails';

    senderEmails.deleteSenderEmail(senderEmailId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="senderEmailId" type="string" required>

      ID of the sender email to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Email Deleted | `wix.promote.v1.sender_email_deleted` | Triggered when a sender email is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-sender-email" id="ex-sdk-delete-sender-email-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-sender-email" id="ex-sdk-delete-sender-email-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-sender-email" id="ex-sdk-delete-sender-email-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-sender-email-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-sender-email-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteSenderEmail</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSenderEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSenderEmail (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-sender-email-0"><span className="wix-picker-name">deleteSenderEmail</span></label>
    <label htmlFor="ex-sdk-delete-sender-email-1"><span className="wix-picker-name">deleteSenderEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-sender-email-2"><span className="wix-picker-name">deleteSenderEmail (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteSenderEmail
    import { senderEmails } from '@wix/sender-emails';

    async function deleteSenderEmail(senderEmailId) {
      const response = await senderEmails.deleteSenderEmail(senderEmailId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSenderEmail (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderEmails } from '@wix/sender-emails';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderEmails },
      // Include the auth strategy and host as relevant
    });

    async function deleteSenderEmail(senderEmailId) {
      const response = await myWixClient.senderEmails.deleteSenderEmail(senderEmailId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSenderEmail (with elevated permissions)
    import { senderEmails } from '@wix/sender-emails';
    import { auth } from '@wix/essentials';

    async function myDeleteSenderEmailMethod(senderEmailId) {
      const elevatedDeleteSenderEmail = auth.elevate(senderEmails.deleteSenderEmail);
      const response = await elevatedDeleteSenderEmail(senderEmailId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
