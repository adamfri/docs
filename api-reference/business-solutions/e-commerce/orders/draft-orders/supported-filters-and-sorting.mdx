---
title: "Supported Filters and Sorting"
description: "The following table shows field support for filters and sorting for the DraftOrder object: | Field | Supported Filters | Sortable | |----------------|---------…"
icon: "file-lines"
---

<Note>This article applies to the REST API only.</Note>

The following table shows field support for filters and sorting
for the `DraftOrder` object:

| Field          | Supported Filters                                                               | Sortable |
|----------------|---------------------------------------------------------------------------------|----------|
| `status`       | `eq()`, `ne()`, `exists()`, `in()`, `hasSome()`                                 | Sortable |
| `orderId`      | `eq()`, `ne()`, `exists()`, `in()`, `hasSome()`, `startsWith()`                 |          |
| `_createdDate` | `eq()`, `ne()`, `exists()`, `in()`, `hasSome()`, `lt()`, `le()`, `gt()`, `ge()` | Sortable |
| `_updatedDate` | `eq()`, `ne()`, `exists()`, `in()`, `hasSome()`, `lt()`, `le()`, `gt()`, `ge()` | Sortable |

__Related content:__
[API Query Language](fallback::https://dev.wix.com/docs/rest/articles/get-started/api-query-language),
[Query Draft Orders](fallback::https://dev.wix.com/docs/rest/business-solutions/e-commerce/orders/draft-orders/query-draft-orders)
