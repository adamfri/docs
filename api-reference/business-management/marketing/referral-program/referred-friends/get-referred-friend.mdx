---
title: "Get Referred Friend"
description: "Retrieves a referred friend by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referred friend by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/referral_friends/v1/referred-friends/{referredFriendId}
    ```

    ## Path Params

    <ParamField path="referredFriendId" type="string" required>

      ID of the referred friend to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="referredFriend" type="ReferredFriend">

      Retrieved referred friend.

      See the [Referred Friend Object](/api-reference/business-management/marketing/referral-program/referred-friends/referred-friend-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/referral_friends/v1/referred-friends/3857d039-b129-4393-b983-947078b6a007' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
          "referredFriend": {
              "id": "3857d039-b129-4393-b983-947078b6a007",
              "contactId": "661f5bb3-0723-4aa4-87d7-a21ebd1d0fa3",
              "referringCustomerId": "07a3306c-b311-466e-92dd-c635c4145d1c",
              "status": "ACTIONS_COMPLETED",
              "revision": "2",
              "createdDate": "2024-08-12T08:12:17.813Z",
              "updatedDate": "2024-08-12T08:13:27.708Z"
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referred friend by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { friends } from '@wix/referral';

    friends.getReferredFriend(referredFriendId): Promise<ReferredFriend>
    ```

    ## Method Parameters

    <ParamField body="referredFriendId" type="string" required>

      ID of the referred friend to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<ReferredFriend>`.

    See the [Referred Friend Object](/api-reference/business-management/marketing/referral-program/referred-friends/referred-friend-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-referred-friend" id="ex-sdk-get-referred-friend-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-referred-friend" id="ex-sdk-get-referred-friend-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-referred-friend" id="ex-sdk-get-referred-friend-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-referred-friend-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-referred-friend-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getReferredFriend</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferredFriend (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferredFriend (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-referred-friend-0"><span className="wix-picker-name">getReferredFriend</span></label>
    <label htmlFor="ex-sdk-get-referred-friend-1"><span className="wix-picker-name">getReferredFriend (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-referred-friend-2"><span className="wix-picker-name">getReferredFriend (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getReferredFriend
    import { friends } from '@wix/referral';

    async function getReferredFriend(referredFriendId) {
      const response = await friends.getReferredFriend(referredFriendId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferredFriend (self-hosted)
    import { createClient } from '@wix/sdk';
    import { friends } from '@wix/referral';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { friends },
      // Include the auth strategy and host as relevant
    });

    async function getReferredFriend(referredFriendId) {
      const response = await myWixClient.friends.getReferredFriend(referredFriendId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferredFriend (with elevated permissions)
    import { friends } from '@wix/referral';
    import { auth } from '@wix/essentials';

    async function myGetReferredFriendMethod(referredFriendId) {
      const elevatedGetReferredFriend = auth.elevate(friends.getReferredFriend);
      const response = await elevatedGetReferredFriend(referredFriendId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
