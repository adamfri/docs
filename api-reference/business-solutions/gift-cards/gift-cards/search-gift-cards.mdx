---
title: "Search Gift Cards"
description: "Retrieves a list of gift cards that match the provided search options, paging, and filtering."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of gift cards that match the provided search options, paging, and filtering.

    Learn about working with [Search, Query and List Methods](https://dev.wix.com/docs/rest/articles/get-started/search-query-and-list-methods).

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `balance` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$exists` |
    | `code` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `codeSuffix` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `expirationDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `initialValue` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$exists` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/gift-cards/v1/gift-cards/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch" required>

      Search options with text matching, filtering, and aggregation capabilities.
      Supports searching by gift card code and code suffix.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations | Faceted search: refers to a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket
              - `SCALAR` — A single-value metric aggregation - e.g. min, max, sum, avg
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined

              **Supported values:**

              - `OR` — Any of the search terms must be present
              - `AND` — All search terms must be present

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Flag if should use auto fuzzy search (allowing typos by a managed proximity algorithm)

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. e.g. `"2023-12-20T10:52:34.795Z"`

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="giftCards" type="GiftCard[]">

      Retrieved gift cards matching the search criteria.

      See the [Gift Card Object](/api-reference/business-solutions/gift-cards/gift-cards/gift-card-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging information for navigating through large result sets.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.
          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data providing insights about the search results.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request)

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket
              - `SCALAR` — A single-value metric aggregation - e.g. min, max, sum, avg
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches for existing gift cards filtered by `expirationDate`, sorted by `createdDate` in descending order, with a limit of 2

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/gift-cards/v1/gift-cards/search' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "search": {
            "filter": {
              "expirationDate": {
                "$gte": "2024-10-01T14:52:02.588Z"
              }
            },
            "sort": [
              {
                "fieldName": "createdDate",
                "order":"DESC"
              }
            ],
            "cursorPaging": {
              "limit": 2
            }
          }
        }'
      ```

      ```json Response
      {
        "giftCards": [
          {
            "id": "544d4606-8066-4836-af47-89bbd1be869f",
            "code": "************AREW",
            "initialValue": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "balance": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "currency": "USD",
            "expirationDate": "2026-05-25T00:00:00Z",
            "createdDate": "2024-09-25T08:00:05.715Z",
            "updatedDate": "2024-09-25T08:00:05.752Z",
            "disabledDate": null,
            "notificationInfo": {
              "recipient": {
                "email": "johndoe@gmail.com",
                "name": "John Doe"
              },
              "notificationDate": null,
              "personalizedMessage": null,
              "sender": {
                "email": "janedoe@gmail.com",
                "name": "Jane Doe"
              }
            },
            "source": "ORDER",
            "orderInfo": {
              "orderId": "84ab1c54-c7a5-4835-98ee-f3688819da24",
              "orderNumber": "10054"
            }
          },
          {
            "id": "4923b8be-fdcb-4f35-ad69-2e6508cba5b6",
            "code": "************5F8C",
            "initialValue": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "balance": {
              "amount": "0",
              "formattedAmount": "$0.00"
            },
            "currency": "USD",
            "expirationDate": "2026-05-24T00:00:00Z",
            "createdDate": "2024-09-24T12:34:34.603Z",
            "updatedDate": "2024-09-26T06:43:05.641Z",
            "disabledDate": null,
            "notificationInfo": {
              "recipient": {
                "email": "janedoe@gmail.com",
                "name": "Jane Doe"
              },
              "notificationDate": null,
              "personalizedMessage": null,
              "sender": {
                "email": "johndoe@gmail.com",
                "name": "John Doe"
              }
            },
            "source": "ORDER",
            "orderInfo": {
              "orderId": "43b199c1-4afb-4e10-89e7-7418e62cddc6",
              "orderNumber": "10051"
            }
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {
            "next": "2747ab883aacade0b7b3a94475d50a3e0e1815be.",
            "prev": null
          },
          "hasNext": true
        },
        "aggregationData": null
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of gift cards that match the provided search options, paging, and filtering.

    Learn about working with [Search, Query and List Methods](https://dev.wix.com/docs/rest/articles/get-started/search-query-and-list-methods).

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `balance` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$exists` |
    | `code` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `codeSuffix` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `expirationDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `initialValue` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$exists` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { giftVouchers } from '@wix/gift-vouchers';

    giftVouchers.searchGiftCards(search): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search options with text matching, filtering, and aggregation capabilities.
      Supports searching by gift card code and code suffix.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations | Faceted search: refers to a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket
              - `SCALAR` — A single-value metric aggregation - e.g. min, max, sum, avg
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined

              **Supported values:**

              - `OR` — Any of the search terms must be present
              - `AND` — All search terms must be present

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Flag if should use auto fuzzy search (allowing typos by a managed proximity algorithm)

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. e.g. `"2023-12-20T10:52:34.795Z"`

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="giftCards" type="GiftCard[]">

      Retrieved gift cards matching the search criteria.

      See the [Gift Card Object](/api-reference/business-solutions/gift-cards/gift-cards/gift-card-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging information for navigating through large result sets.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.
          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data providing insights about the search results.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request)

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket
              - `SCALAR` — A single-value metric aggregation - e.g. min, max, sum, avg
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-gift-cards" id="ex-sdk-search-gift-cards-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-gift-cards" id="ex-sdk-search-gift-cards-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-gift-cards-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-gift-cards-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search gift cards by code</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchGiftCards (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-gift-cards-0"><span className="wix-picker-name">Search gift cards by code</span></label>
    <label htmlFor="ex-sdk-search-gift-cards-1"><span className="wix-picker-name">searchGiftCards (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Search gift cards by code
    import { giftVouchers } from "@wix/gift-vouchers";

    const options = {
      search: { expression: "HAPPY" },
      cursorPaging: { limit: 10 },
    };

    async function searchGiftCards() {
      const response = await giftVouchers.searchGiftCards(options);
      return response;
    }

    /* Promise resolves to:
     * {
     *   giftCards: [
     *     {
     *       id: "1d752091-e722-4847-8e3d-9d3d88d34706",
     *       code: "************HDAY",
     *       initialValue: { amount: "100", formattedAmount: "$100.00" },
     *       balance: { amount: "100", formattedAmount: "$100.00" },
     *       currency: "USD",
     *       source: "MANUAL",
     *     },
     *   ],
     *   pagingMetadata: { count: 1, cursors: {}, hasNext: false },
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchGiftCards (self-hosted)
    import { createClient } from '@wix/sdk';
    import { giftVouchers } from '@wix/gift-vouchers';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { giftVouchers },
      // Include the auth strategy and host as relevant
    });

    async function searchGiftCards(search) {
      const response = await myWixClient.giftVouchers.searchGiftCards(search);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
