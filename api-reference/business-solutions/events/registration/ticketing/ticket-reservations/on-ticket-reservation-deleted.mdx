---
title: "onTicketReservationDeleted"
description: "Triggered when a ticket reservation is deleted."
icon: "bolt"
---

Triggered when a ticket reservation is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v1.ticket_reservation_deleted` |
| Package | `@wix/events` |
| Permissions | `Manage Events - all permissions`, `Manage Events`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onTicketReservationDeleted
  import { ticketReservations } from '@wix/events';

  ticketReservations.onTicketReservationDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onTicketReservationDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ticketReservations } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        ticketReservations,
      },
  });

  wixClient.ticketReservations.onTicketReservationDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
