---
title: "List Eligible Memberships"
description: "Deprecated."
icon: "upload"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deprecated. Use `ListExistingMemberships` + `ListPurchasablePlans` for read, and `EnhanceAndValidateSelection` for selection validation.

    ## Endpoint

    ```http
    POST /v1/list-eligible-memberships
    ```

    ## Body Params

    <ParamField body="lineItems" type="LineItem[]" required>

      The line items for which to list eligible memberships.

      Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="lineItems properties">

        <ParamField body="id" type="string">

          Line item ID.

          Constraints: `minLength 1`, `maxLength 100`.

        </ParamField>

        <ParamField body="catalogReference" type="CatalogReference" required>

          Catalog and item reference info.

          <Expandable title="catalogReference properties">

            <ParamField body="catalogItemId" type="string">

              ID of the item within the catalog it belongs to.

              Constraints: `minLength 1`, `maxLength 36`.

            </ParamField>

            <ParamField body="appId" type="string" required>

              ID of the app providing the catalog.

              You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

              For items from Wix catalogs, the following values always apply:
              + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
              + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
              + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

              Constraints: `minLength 1`.

            </ParamField>

            <ParamField body="options" type="object">

              Additional item details in `key:value` pairs.

              Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

              For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
              or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="serviceProperties" type="ServiceProperties">

          Properties of the service. When relevant, contains information such as date and number of participants.

          <Expandable title="serviceProperties properties">

            <ParamField body="scheduledDate" type="datetime">

              Date and time the service is to be provided, in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.
              For example, the start time of a class.

            </ParamField>

            <ParamField body="numberOfParticipants" type="integer">

              The number of people participating in the service. For example, the number of people attending a class or the number of people per hotel room.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="rootCatalogItemId" type="string">

          Root catalog item ID.

          The value will usually be the same as `catalogReference.catalogItemId`.
          In cases when these are not the same, this field will return the actual ID of the item in the catalog.
          For example, for Wix bookings, the value of `catalogReference.catalogItemId` is the booking ID, but `rootCatalogItemId` is set to the service ID.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="memberId" type="string" required>

      Member ID.

      Do not retrieve this from the request context. In some cases the caller is not a member, but a user who is using the membership on behalf of a member.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="selectedMemberships" type="SelectedMemberships">

      The selected payment memberships and which line items they apply to.

      When not provided, your implementation is expected to return the default selection.
      When provided, your implementation is expected to validate and return it.

      <Expandable title="selectedMemberships properties">

        <ParamField body="memberships" type="SelectedMembership[]">

          Selected memberships.

          Constraints: `maxItems 300`.

          <Expandable title="memberships properties">

            <ParamField body="id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ParamField>

            <ParamField body="lineItemIds" type="string[]">

              IDs of the line items this membership applies to.

              Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="eligibleMemberships" type="Membership[]">

      List of memberships that are eligible for the given member and line items.

      Constraints: `maxItems 300`.

      <Expandable title="eligibleMemberships properties">

        <ResponseField name="id" type="string">

          Membership ID.

          Constraints: `minLength 1`, `maxLength 200`.

        </ResponseField>

        <ResponseField name="name" type="MembershipName">

          Membership name.

          <Expandable title="name properties">

            <ResponseField name="original" type="string">

              Membership name.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="translated" type="string">

              Translated membership name. Defaults to `original` when not provided.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]" deprecated>

          Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

          Populate this only in the legacy `ListEligibleMemberships` flow, which still requires it and rejects a
          membership that omits it. Leave it empty everywhere else - `ListMembershipsByIds` receives no line items
          at all, so there is nothing to report there.

          **Deprecated.** Use `line_item_id` instead. Removal: 2026-08-30. Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

        </ResponseField>

        <ResponseField name="credits" type="MembershipPaymentCredits">

          Membership's payment credits (balance information).

          <Expandable title="credits properties">

            <ResponseField name="total" type="integer">

              Membership's initial value.

              Constraints: `min 1`.

            </ResponseField>

            <ResponseField name="remaining" type="integer">

              Membership's remaining value.

              Constraints: `min 0`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="expirationDate" type="datetime">

          Membership expiration date.

        </ResponseField>

        <ResponseField name="additionalData" type="object">

          Deprecated. Use `offering_description` for human-readable provider data.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="invalidMemberships" type="InvalidMembership[]">

      List of memberships owned by the member, but cannot be used due to the reason provided.

      Constraints: `maxItems 300`.

      <Expandable title="invalidMemberships properties">

        <ResponseField name="membership" type="Membership">

          The membership that is invalid and cannot be used.

          <Expandable title="membership properties">

            <ResponseField name="id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="name" type="MembershipName">

              Membership name.

              _Nested structure omitted for brevity — see the `MembershipName` object._

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]" deprecated>

              Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

              Populate this only in the legacy `ListEligibleMemberships` flow, which still requires it and rejects a
              membership that omits it. Leave it empty everywhere else - `ListMembershipsByIds` receives no line items
              at all, so there is nothing to report there.

              **Deprecated.** Use `line_item_id` instead. Removal: 2026-08-30. Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="credits" type="MembershipPaymentCredits">

              Membership's payment credits (balance information).

              _Nested structure omitted for brevity — see the `MembershipPaymentCredits` object._

            </ResponseField>

            <ResponseField name="expirationDate" type="datetime">

              Membership expiration date.

            </ResponseField>

            <ResponseField name="additionalData" type="object">

              Deprecated. Use `offering_description` for human-readable provider data.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="reason" type="string">

          Reason why this membership is invalid.

          Constraints: `minLength 1`, `maxLength 200`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="selectedMemberships" type="SelectedMembership[]">

      List of selected memberships and which line items they apply to.

      Constraints: `maxItems 300`.

      <Expandable title="selectedMemberships properties">

        <ResponseField name="id" type="string">

          Membership ID.

          Constraints: `minLength 1`, `maxLength 200`.

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          IDs of the line items this membership applies to.

          Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SELECTION` | The provided membership selection is invalid |
    | `428` | `FAILED_PRECONDITION` | `INSUFFICIENT_BALANCE` | The selected memberships do not have enough balance to cover the line items. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Get list of all member's memberships, that are applicable for given line item.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://provider.example.com/v1/list-eligible-memberships' \
        -H 'user-agent: Wix' \
        -H 'accept-encoding: gzip, deflate' \
        -H 'content-type: text/plain; charset=utf-8' \
        --data-binary '{
      	  "memberId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
      	  "lineItems": [
      	    {
      	      "id": "00000000-0000-0000-0000-000000000001",
      	      "catalogReference": {
      	        "catalogItemId": "68f71a63-d406-49bd-942f-b70865e72d99",
      	        "appId": "13d21c63-b5ec-5912-8397-c3a5ddb27a97"
      	      },
      	      "rootCatalogItemId": "8e02a329-893c-46d4-9d52-c45aed5ecf32"
      	    }
      	  ]
        }'
      ```

      ```json Response
      {
        "eligibleMemberships": [
          {
            "id": "7c1ff545-95f8-3391-a110-9670f906190c/9bd72a2f-b7fa-4594-8c91-e362d5d279f0",
            "name": {
              "original": "Silver membership"
            },
            "lineItemIds": [
              "00000000-0000-0000-0000-000000000001"
            ],
            "additionalData": {
              "benefitKey": "9bd72a2f-b7fa-4594-8c91-e362d5d279f0",
              "poolId": "7c1ff545-95f8-3391-a110-9670f906190c",
              "programId": "85c193b0-72ad-4eb6-9184-f49c7ba325b4"
            }
          },
          {
            "id": "1709a8ba-aa1d-3921-ab42-24f1fa21bacb/aee2c110-9644-4e8c-84dc-27bf42d45d27",
            "name": {
              "original": "Free first session"
            },
            "lineItemIds": [
              "00000000-0000-0000-0000-000000000001"
            ],
            "credits": {
              "total": 1,
              "remaining": 1
            },
            "additionalData": {
              "benefitKey": "aee2c110-9644-4e8c-84dc-27bf42d45d27",
              "poolId": "1709a8ba-aa1d-3921-ab42-24f1fa21bacb",
              "programId": "805c2719-00a2-4e67-b294-d58b8ed3b1ac"
            }
          }
        ],
        "invalidMemberships": [],
        "selectedMemberships": [
          {
            "id": "7c1ff545-95f8-3391-a110-9670f906190c/9bd72a2f-b7fa-4594-8c91-e362d5d279f0",
            "lineItemIds": [
              "00000000-0000-0000-0000-000000000001"
            ]
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deprecated. Use `ListExistingMemberships` + `ListPurchasablePlans` for read, and `EnhanceAndValidateSelection` for selection validation.

    ## Method Declaration

    ```typescript
    import { memberships } from '@wix/ecom';

    memberships.listEligibleMemberships(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="ListEligibleMembershipsRequest">

      request

      <Expandable title="request properties">

        <ParamField body="lineItems" type="LineItem[]">

          The line items for which to list eligible memberships.

          Constraints: `minItems 1`, `maxItems 300`.

          <Expandable title="lineItems properties">

            <ParamField body="_id" type="string">

              Line item ID.

              Constraints: `minLength 1`, `maxLength 100`.

            </ParamField>

            <ParamField body="catalogReference" type="CatalogReference">

              Catalog and item reference info.

              _Nested structure omitted for brevity — see the `CatalogReference` object._

            </ParamField>

            <ParamField body="serviceProperties" type="ServiceProperties">

              Properties of the service. When relevant, contains information such as date and number of participants.

              _Nested structure omitted for brevity — see the `ServiceProperties` object._

            </ParamField>

            <ParamField body="rootCatalogItemId" type="string">

              Root catalog item ID.

              The value will usually be the same as `catalogReference.catalogItemId`.
              In cases when these are not the same, this field will return the actual ID of the item in the catalog.
              For example, for Wix bookings, the value of `catalogReference.catalogItemId` is the booking ID, but `rootCatalogItemId` is set to the service ID.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="memberId" type="string">

          Member ID.

          Do not retrieve this from the request context. In some cases the caller is not a member, but a user who is using the membership on behalf of a member.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="selectedMemberships" type="SelectedMemberships">

          The selected payment memberships and which line items they apply to.

          When not provided, your implementation is expected to return the default selection.
          When provided, your implementation is expected to validate and return it.

          <Expandable title="selectedMemberships properties">

            <ParamField body="memberships" type="SelectedMembership[]">

              Selected memberships.

              Constraints: `maxItems 300`.

              _Nested structure omitted for brevity — see the `SelectedMembership[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="eligibleMemberships" type="Membership[]">

      List of memberships that are eligible for the given member and line items.

      Constraints: `maxItems 300`.

      <Expandable title="eligibleMemberships properties">

        <ResponseField name="_id" type="string">

          Membership ID.

          Constraints: `minLength 1`, `maxLength 200`.

        </ResponseField>

        <ResponseField name="name" type="MembershipName">

          Membership name.

          <Expandable title="name properties">

            <ResponseField name="original" type="string">

              Membership name.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="translated" type="string">

              Translated membership name. Defaults to `original` when not provided.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]" deprecated>

          Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

          Populate this only in the legacy `ListEligibleMemberships` flow, which still requires it and rejects a
          membership that omits it. Leave it empty everywhere else - `ListMembershipsByIds` receives no line items
          at all, so there is nothing to report there.

          **Deprecated.** Use `line_item_id` instead. Removal: 2026-08-30. Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

        </ResponseField>

        <ResponseField name="credits" type="MembershipPaymentCredits">

          Membership's payment credits (balance information).

          <Expandable title="credits properties">

            <ResponseField name="total" type="integer">

              Membership's initial value.

              Constraints: `min 1`.

            </ResponseField>

            <ResponseField name="remaining" type="integer">

              Membership's remaining value.

              Constraints: `min 0`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="expirationDate" type="datetime">

          Membership expiration date.

        </ResponseField>

        <ResponseField name="additionalData" type="object">

          Deprecated. Use `offering_description` for human-readable provider data.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="invalidMemberships" type="InvalidMembership[]">

      List of memberships owned by the member, but cannot be used due to the reason provided.

      Constraints: `maxItems 300`.

      <Expandable title="invalidMemberships properties">

        <ResponseField name="membership" type="Membership">

          The membership that is invalid and cannot be used.

          <Expandable title="membership properties">

            <ResponseField name="_id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="name" type="MembershipName">

              Membership name.

              _Nested structure omitted for brevity — see the `MembershipName` object._

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]" deprecated>

              Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

              Populate this only in the legacy `ListEligibleMemberships` flow, which still requires it and rejects a
              membership that omits it. Leave it empty everywhere else - `ListMembershipsByIds` receives no line items
              at all, so there is nothing to report there.

              **Deprecated.** Use `line_item_id` instead. Removal: 2026-08-30. Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="credits" type="MembershipPaymentCredits">

              Membership's payment credits (balance information).

              _Nested structure omitted for brevity — see the `MembershipPaymentCredits` object._

            </ResponseField>

            <ResponseField name="expirationDate" type="datetime">

              Membership expiration date.

            </ResponseField>

            <ResponseField name="additionalData" type="object">

              Deprecated. Use `offering_description` for human-readable provider data.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="reason" type="string">

          Reason why this membership is invalid.

          Constraints: `minLength 1`, `maxLength 200`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="selectedMemberships" type="SelectedMembership[]">

      List of selected memberships and which line items they apply to.

      Constraints: `maxItems 300`.

      <Expandable title="selectedMemberships properties">

        <ResponseField name="_id" type="string">

          Membership ID.

          Constraints: `minLength 1`, `maxLength 200`.

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          IDs of the line items this membership applies to.

          Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SELECTION` | The provided membership selection is invalid |
    | `428` | `FAILED_PRECONDITION` | `INSUFFICIENT_BALANCE` | The selected memberships do not have enough balance to cover the line items. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-eligible-memberships" id="ex-sdk-list-eligible-memberships-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-eligible-memberships" id="ex-sdk-list-eligible-memberships-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-eligible-memberships" id="ex-sdk-list-eligible-memberships-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-eligible-memberships-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-eligible-memberships-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listEligibleMemberships</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listEligibleMemberships (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listEligibleMemberships (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-eligible-memberships-0"><span className="wix-picker-name">listEligibleMemberships</span></label>
    <label htmlFor="ex-sdk-list-eligible-memberships-1"><span className="wix-picker-name">listEligibleMemberships (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-eligible-memberships-2"><span className="wix-picker-name">listEligibleMemberships (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listEligibleMemberships
    import { memberships } from '@wix/ecom/service-plugins';

    async function listEligibleMemberships(request,metadata) {
      const response = await memberships.listEligibleMemberships(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listEligibleMemberships (self-hosted)
    import { createClient } from '@wix/sdk';
    import { memberships } from '@wix/ecom/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { memberships },
      // Include the auth strategy and host as relevant
    });

    async function listEligibleMemberships(request,metadata) {
      const response = await myWixClient.memberships.listEligibleMemberships(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listEligibleMemberships (with elevated permissions)
    import { memberships } from '@wix/ecom/service-plugins';
    import { auth } from '@wix/essentials';

    async function myListEligibleMembershipsMethod(request,metadata) {
      const elevatedListEligibleMemberships = auth.elevate(memberships.listEligibleMemberships);
      const response = await elevatedListEligibleMemberships(request,metadata);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
