---
title: "Mark Sender Details As Default"
description: "Marks a sender as default."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Marks a sender as default.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/sender-details/v1/sender-details/{senderDetailsId}/mark-as-default
    ```

    ## Path Params

    <ParamField path="senderDetailsId" type="string" required>

      ID of the sender to mark as default.

    </ParamField>

    ## Response Object

    Returns `MarkSenderDetailsAsDefaultResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Details Updated | `wix.promote.v1.sender_details_updated` | Triggered when sender details are updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/sender-details/v1/sender-details/a6351a69-f4cb-411c-9853-26634468b03d/mark-as-default' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Marks a sender as default.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderDetails } from '@wix/sender-details';

    senderDetails.markSenderDetailsAsDefault(senderDetailsId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="senderDetailsId" type="string" required>

      ID of the sender to mark as default.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Details Updated | `wix.promote.v1.sender_details_updated` | Triggered when sender details are updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-mark-sender-details-as-default" id="ex-sdk-mark-sender-details-as-default-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-mark-sender-details-as-default" id="ex-sdk-mark-sender-details-as-default-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-mark-sender-details-as-default" id="ex-sdk-mark-sender-details-as-default-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-mark-sender-details-as-default-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-mark-sender-details-as-default-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">markSenderDetailsAsDefault</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markSenderDetailsAsDefault (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markSenderDetailsAsDefault (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-mark-sender-details-as-default-0"><span className="wix-picker-name">markSenderDetailsAsDefault</span></label>
    <label htmlFor="ex-sdk-mark-sender-details-as-default-1"><span className="wix-picker-name">markSenderDetailsAsDefault (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-mark-sender-details-as-default-2"><span className="wix-picker-name">markSenderDetailsAsDefault (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript markSenderDetailsAsDefault
    import { senderDetails } from '@wix/sender-details';

    async function markSenderDetailsAsDefault(senderDetailsId) {
      const response = await senderDetails.markSenderDetailsAsDefault(senderDetailsId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markSenderDetailsAsDefault (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderDetails } from '@wix/sender-details';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderDetails },
      // Include the auth strategy and host as relevant
    });

    async function markSenderDetailsAsDefault(senderDetailsId) {
      const response = await myWixClient.senderDetails.markSenderDetailsAsDefault(senderDetailsId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markSenderDetailsAsDefault (with elevated permissions)
    import { senderDetails } from '@wix/sender-details';
    import { auth } from '@wix/essentials';

    async function myMarkSenderDetailsAsDefaultMethod(senderDetailsId) {
      const elevatedMarkSenderDetailsAsDefault = auth.elevate(senderDetails.markSenderDetailsAsDefault);
      const response = await elevatedMarkSenderDetailsAsDefault(senderDetailsId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
