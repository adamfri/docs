---
title: "Get Tier"
description: "Retrieves a specified loyalty tier."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a specified loyalty tier.

    To retrieve a list of all of a site's tiers, use List Tiers.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.READ-LOYALTY">Read Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/loyalty-tiers/v1/tiers/{tierId}
    ```

    ## Path Params

    <ParamField path="tierId" type="string" required>

      ID of the tier to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="tier" type="Tier">

      Retrieved loyalty tier.

      See the [Tier Object](/api-reference/crm/loyalty-program/loyalty-program-management/tiers/tier-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/loyalty-tiers/v1/tiers/acddc0e0-5e27-4a1b-a52d-b3785ac258cb' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "id": "acddc0e0-5e27-4a1b-a52d-b3785ac258cb",
        "tierDefinition": {
          "icon": {
            "id": "",
            "url": "shapes/39dce0a5d1ce498f95526b1390eaf585.svg",
            "height": 0,
            "width": 0
          },
          "name": "Gold",
          "description": "Earn 2x points and get special discount"
        },
        "requiredPoints": 200,
        "revision": "1",
        "createdDate": "2024-02-15T14:22:02.539Z",
        "updatedDate": "2024-04-17T11:34:25.274Z"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a specified loyalty tier.

    To retrieve a list of all of a site's tiers, use List Tiers.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.READ-LOYALTY">Read Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { tiers } from '@wix/loyalty';

    tiers.getTier(tierId): Promise<Tier>
    ```

    ## Method Parameters

    <ParamField body="tierId" type="string" required>

      ID of the tier to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Tier>`.

    See the [Tier Object](/api-reference/crm/loyalty-program/loyalty-program-management/tiers/tier-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-tier" id="ex-sdk-get-tier-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-tier" id="ex-sdk-get-tier-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-tier-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-tier-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get tier</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-tier-0"><span className="wix-picker-name">Get tier</span></label>
    <label htmlFor="ex-sdk-get-tier-1"><span className="wix-picker-name">getTier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get tier
    import { tiers } from "@wix/loyalty";

    /* Sample tierId value:
     *
     * {
     *     "tierId": "f882b4b5-44c4-42b4-a380-9662b55bbe71"
     * }
     */ 

    async function getTier(tierId) {
      try {
        const result = await tiers.getTier(tierId);
        return result;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     *
     * {
     *   "_createdDate": "2024-06-10T07:07:35.686Z",
     *   "_id": "f882b4b5-44c4-42b4-a380-9662b55bbe71",
     *   "_updatedDate": "2024-06-10T07:07:35.686Z",
     *   "requiredPoints": 40,
     *   "revision": "1",
     *   "tierDefinition": {
     *     "name": "Super API Tier",
     *     "description": "Tiers created from API"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTier (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tiers } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tiers },
      // Include the auth strategy and host as relevant
    });

    async function getTier(tierId) {
      const response = await myWixClient.tiers.getTier(tierId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
