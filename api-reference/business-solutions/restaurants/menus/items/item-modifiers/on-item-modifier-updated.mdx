---
title: "onItemModifierUpdated"
description: "The onItemModifierUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_modifier_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemModifierUpdated
  import { itemModifiers } from '@wix/restaurants';

  itemModifiers.onItemModifierUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemModifierUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemModifiers } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemModifiers,
      },
  });

  wixClient.itemModifiers.onItemModifierUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
