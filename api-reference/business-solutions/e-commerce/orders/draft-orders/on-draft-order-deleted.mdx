---
title: "onDraftOrderDeleted"
description: "Triggered when a draft order is deleted."
icon: "bolt"
---

Triggered when a draft order is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.draft_order_deleted` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions` |

## Examples

<CodeGroup>

  ```javascript onDraftOrderDeleted
  import { draftOrders } from '@wix/ecom';

  draftOrders.onDraftOrderDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onDraftOrderDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { draftOrders } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        draftOrders,
      },
  });

  wixClient.draftOrders.onDraftOrderDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
