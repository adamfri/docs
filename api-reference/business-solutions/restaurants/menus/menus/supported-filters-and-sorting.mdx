---
title: "Supported Filters and Sorting"
description: "The following table shows field support for filters and sorting for the menu object: | Field | Supported Filters | Sortable | |-----------------|--------------…"
icon: "file-lines"
---

<Note>This article applies to the REST API only.</Note>

The following table shows field support for filters and sorting for the menu object:

| Field           | Supported Filters                          | Sortable |
|-----------------|--------------------------------------------|----------|
| `id`            | `$eq`, `$ne`, `$in`, `$nin`                |          |
| `createdDate`   | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sortable |
| `updatedDate`   | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sortable |
| `name`          | `$eq`, `$ne`, `$in`, `$nin`, `$startsWith` |          |
| `visible`       | `$eq`, `$ne`                               |
| `description`   | `$eq`, `$ne`, `$in`, `$nin`, `$startsWith` |          |
| `urlQueryParam` | `$eq`, `$ne`, `$in`, `$nin`, `$startsWith` |          |
| `sectionIds`    | `$hasSome`                                 |          |

**Related content:**
[API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language),
[Query Menus](https://dev.wix.com/docs/rest/business-solutions/restaurants/menus/menu/query-menus).
