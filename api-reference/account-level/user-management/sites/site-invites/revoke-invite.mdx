---
title: "Revoke Invite"
description: "Revokes a pending site contributor invite."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Revokes a pending site contributor invite.
    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header. API keys are currently available to selected beta users only.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-IDENTITY.MANAGE-CONTRIBUTORS">Manage Contributors</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/invites/site-invite/{inviteId}/revoke
    ```

    ## Path Params

    <ParamField path="inviteId" type="string" required>

      Invite ID.

    </ParamField>

    ## Response Object

    Returns `RevokeSiteInviteResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/invites/site-invite/0092ff87-6028-41cb-92b5-980d5474abcf/revoke' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Revokes a pending site contributor invite.
    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header. API keys are currently available to selected beta users only.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-IDENTITY.MANAGE-CONTRIBUTORS">Manage Contributors</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { siteInvites } from '@wix/user-management';

    siteInvites.revokeInvite(inviteId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="inviteId" type="string" required>

      Invite ID.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-revoke-invite" id="ex-sdk-revoke-invite-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-revoke-invite" id="ex-sdk-revoke-invite-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-revoke-invite-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-revoke-invite-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Revoke a pending site invite with an API key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">revokeInvite (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-revoke-invite-0"><span className="wix-picker-name">Revoke a pending site invite with an API key</span></label>
    <label htmlFor="ex-sdk-revoke-invite-1"><span className="wix-picker-name">revokeInvite (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Revoke a pending site invite with an API key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { siteInvites } from "@wix/user-management";

    const wixClient = createClient({
     modules: { siteInvites },
     auth: ApiKeyStrategy({
        siteId: "MY-SITE-ID",
        apiKey: "MY-API-KEY",
      }),
    });

    async function revokeInvite(inviteId) {
      const response = await siteInvites.revokeInvite(inviteId);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript revokeInvite (self-hosted)
    import { createClient } from '@wix/sdk';
    import { siteInvites } from '@wix/user-management';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { siteInvites },
      // Include the auth strategy and host as relevant
    });

    async function revokeInvite(inviteId) {
      const response = await myWixClient.siteInvites.revokeInvite(inviteId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
