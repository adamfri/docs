---
title: "onReviewRequestDeleted"
description: "Triggered when a review request is deleted."
icon: "bolt"
---

Triggered when a review request is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reviews.v2.review_request_deleted` |
| Package | `@wix/reviews` |
| Permissions | `Manage Review Requests` |

## Examples

<CodeGroup>

  ```javascript onReviewRequestDeleted
  import { reviewRequests } from '@wix/reviews';

  reviewRequests.onReviewRequestDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onReviewRequestDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reviewRequests } from '@wix/reviews';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reviewRequests,
      },
  });

  wixClient.reviewRequests.onReviewRequestDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
