---
title: "onGuestDeleted"
description: "Triggered when a guest is deleted."
icon: "bolt"
---

Triggered when a guest is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.guests.v1.guest_deleted` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onGuestDeleted
  import { guests } from '@wix/events';

  guests.onGuestDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onGuestDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { guests } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        guests,
      },
  });

  wixClient.guests.onGuestDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
