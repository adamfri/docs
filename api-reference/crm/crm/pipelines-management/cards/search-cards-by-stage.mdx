---
title: "Search Cards By Stage"
description: "Retrieves a list of cards by the pipeline's stage, given the provided free-text search expression, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of cards by the pipeline's stage, given the provided free-text search expression, filtering, and sorting.

    By default, cards are sorted by `updatedDate` in descending order.

    For a detailed list of supported filters, sorting options, and search capabilities, see [Cards: Supported Filters, Sorting, and Search](https://dev.wix.com/docs/api-reference/crm/crm/pipelines-management/cards/supported-filters-sorting-and-search).

    To learn more about working with _Search_ methods, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.READ-PIPELINES">Read Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/crm/pipelines/v1/cards/search-by-stage
    ```

    ## Body Params

    <ParamField body="pipelineId" type="string" required>

      ID of the pipeline in which to look for cards.

    </ParamField>

    <ParamField body="stageIds" type="string[]" required>

      Stage IDs to filter by.

      Constraints: `format GUID`, `minItems 1`, `maxItems 35`.

    </ParamField>

    <ParamField body="search" type="CursorSearch">

      Search options. `search.cursorPaging.cursor` is not supported in this request and is ignored.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use auto fuzzy search (allowing typos by a managed proximity algorithm).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="nameFormat" type="string">

      Specifies how contact names should be formatted and ordered when filtering, sorting, or searching cards by contact names.
      By default, names are ordered by first name followed by last name.
      When set to `"BY_LAST_NAME"`, names are ordered by last name followed by first name.

      **Supported values:**

      - `BY_FIRST_NAME` — Names are ordered by first name followed by last name.
      - `BY_LAST_NAME` — Names are ordered by last name followed by first name.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkSearchCardsResult[]">

      List of card results matching the filter and search.

      Constraints: `minItems 1`, `maxItems 30`.

      <Expandable title="results properties">

        <ResponseField name="pipelineId" type="string">

          ID of the pipeline containing the cards.

        </ResponseField>

        <ResponseField name="stageId" type="string">

          ID of the stage containing the cards.

        </ResponseField>

        <ResponseField name="cards" type="Card[]">

          List of cards.

          See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

        </ResponseField>

        <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

          Paging metadata.

          <Expandable title="pagingMetadata properties">

            <ResponseField name="count" type="integer">

              Number of items returned in current page.

            </ResponseField>

            <ResponseField name="cursors" type="Cursors">

              Cursor strings that point to the next page, previous page, or both.

              _Nested structure omitted for brevity — see the `Cursors` object._

            </ResponseField>

            <ResponseField name="hasNext" type="boolean">

              Whether there are more pages to retrieve following the current page.

              + `true`: Another page of results can be retrieved.
              + `false`: This is the last page.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="aggregationData" type="AggregationData">

          Aggregation data.

          <Expandable title="aggregationData properties">

            <ResponseField name="results" type="AggregationResults[]">

              key = aggregation name (as derived from search request).

              Constraints: `maxItems 10000`.

              _Nested structure omitted for brevity — see the `AggregationResults[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches cards within specific pipeline stages.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/crm/pipelines/v1/cards/search-by-stage' \
        -H 'Authorization:<AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "pipelineId": "f9e8d7c6-b5a4-3210-fedc-ba9876543210",
          "stageIds": [
            "01234567-89ab-cdef-0123-456789abcdef",
            "89abcdef-0123-4567-89ab-cdef01234567"
          ],
          "search": {
            "expression": "enterprise software",
            "filter": {
              "value.value": {
                "$gte": "100000.00"
              },
              "assigneeId": {
                "$exists": true
              }
            },
            "sort": [
              {
                "fieldName": "value.value",
                "order": "DESC"
              }
            ],
            "paging": {
              "limit": 25,
              "offset": 0
            }
          },
          "nameFormat": "BY_FIRST_NAME"
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "pipelineId": "f9e8d7c6-b5a4-3210-fedc-ba9876543210",
            "stageId": "01234567-89ab-cdef-0123-456789abcdef",
            "cards": [
              {
                "id": "a1b2c3d4-e5f6-7890-1234-567890abcdef",
                "revision": "2",
                "createdDate": "2024-01-15T14:30:00.000Z",
                "updatedDate": "2024-01-18T09:15:30.000Z",
                "stageUpdatedDate": "2024-01-17T16:45:00.000Z",
                "pipelineId": "f9e8d7c6-b5a4-3210-fedc-ba9876543210",
                "stageId": "01234567-89ab-cdef-0123-456789abcdef",
                "name": "Enterprise Software Deal",
                "description": "Large enterprise client interested in our software solution with premium features.",
                "priority": "P1_URGENT",
                "assigneeId": "bcdefa01-2345-6789-abcd-ef0123456789",
                "dueDate": "2024-02-15T17:00:00.000Z",
                "value": {
                  "value": "175000.00",
                  "currency": "USD"
                },
                "outcome": "NONE",
                "mainContact": {
                  "contactId": "1a2b3c4d-5e6f-7890-1234-567890abcdef",
                  "firstName": "Sarah",
                  "lastName": "Johnson",
                  "email": "sarah.johnson@techcorp.com",
                  "phone": "+18005551234",
                  "fullName": "Sarah Johnson",
                  "displayName": "Sarah Johnson"
                }
              }
            ],
            "pagingMetadata": {
              "count": 1,
              "offset": 0,
              "total": 1,
              "tooManyToCount": false,
              "cursors": {
                "next": null,
                "prev": null
              }
            },
            "aggregationData": {
              "results": [
                {
                  "name": "total_value",
                  "type": "SCALAR",
                  "scalar": {
                    "type": "SUM",
                    "value": 175000.0
                  }
                }
              ]
            }
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of cards by the pipeline's stage, given the provided free-text search expression, filtering, and sorting.

    By default, cards are sorted by `updatedDate` in descending order.

    For a detailed list of supported filters, sorting options, and search capabilities, see [Cards: Supported Filters, Sorting, and Search](https://dev.wix.com/docs/api-reference/crm/crm/pipelines-management/cards/supported-filters-sorting-and-search).

    To learn more about working with _Search_ methods, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.READ-PIPELINES">Read Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { cards } from '@wix/crm';

    cards.searchCardsByStage(pipelineId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="pipelineId" type="string" required>

      ID of the pipeline in which to look for cards.

    </ParamField>

    <ParamField body="options" type="SearchCardsByStageOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="stageIds" type="string[]" required>

          Stage IDs to filter by.

          Constraints: `format GUID`, `minItems 1`, `maxItems 35`.

        </ParamField>

        <ParamField body="search" type="CursorSearch">

          Search options. `search.cursorPaging.cursor` is not supported in this request and is ignored.

          <Expandable title="search properties">

            <ParamField body="filter" type="object">

              Filter object.

              Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              List of sort objects.

              Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

              Constraints: `maxItems 10`.

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="aggregations" type="Aggregation[]">

              Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

              Constraints: `maxItems 10`.

              _Nested structure omitted for brevity — see the `Aggregation[]` object._

            </ParamField>

            <ParamField body="search" type="SearchDetails">

              Free text to match in searchable fields.

              _Nested structure omitted for brevity — see the `SearchDetails` object._

            </ParamField>

            <ParamField body="timeZone" type="string">

              UTC offset or IANA time zone. Valid values are
              ISO 8601 UTC offsets, such as +02:00 or -06:00,
              and IANA time zone IDs, such as Europe/Rome.

              Affects all filters and aggregations returned values.
              You may override this behavior in a specific filter by providing
              timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="pagingMethod" type="pagingMethod">

              pagingMethod

              _Nested structure omitted for brevity — see the `pagingMethod` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="nameFormat" type="string">

          Specifies how contact names should be formatted and ordered when filtering, sorting, or searching cards by contact names.
          By default, names are ordered by first name followed by last name.
          When set to `"BY_LAST_NAME"`, names are ordered by last name followed by first name.

          **Supported values:**

          - `BY_FIRST_NAME` — Names are ordered by first name followed by last name.
          - `BY_LAST_NAME` — Names are ordered by last name followed by first name.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkSearchCardsResult[]">

      List of card results matching the filter and search.

      Constraints: `minItems 1`, `maxItems 30`.

      <Expandable title="results properties">

        <ResponseField name="pipelineId" type="string">

          ID of the pipeline containing the cards.

        </ResponseField>

        <ResponseField name="stageId" type="string">

          ID of the stage containing the cards.

        </ResponseField>

        <ResponseField name="cards" type="Card[]">

          List of cards.

          See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

        </ResponseField>

        <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

          Paging metadata.

          <Expandable title="pagingMetadata properties">

            <ResponseField name="count" type="integer">

              Number of items returned in current page.

            </ResponseField>

            <ResponseField name="cursors" type="Cursors">

              Cursor strings that point to the next page, previous page, or both.

              _Nested structure omitted for brevity — see the `Cursors` object._

            </ResponseField>

            <ResponseField name="hasNext" type="boolean">

              Whether there are more pages to retrieve following the current page.

              + `true`: Another page of results can be retrieved.
              + `false`: This is the last page.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="aggregationData" type="AggregationData">

          Aggregation data.

          <Expandable title="aggregationData properties">

            <ResponseField name="results" type="AggregationResults[]">

              key = aggregation name (as derived from search request).

              Constraints: `maxItems 10000`.

              _Nested structure omitted for brevity — see the `AggregationResults[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-cards-by-stage" id="ex-sdk-search-cards-by-stage-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-cards-by-stage" id="ex-sdk-search-cards-by-stage-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-search-cards-by-stage" id="ex-sdk-search-cards-by-stage-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-cards-by-stage-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-cards-by-stage-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">searchCardsByStage</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchCardsByStage (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchCardsByStage (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-cards-by-stage-0"><span className="wix-picker-name">searchCardsByStage</span></label>
    <label htmlFor="ex-sdk-search-cards-by-stage-1"><span className="wix-picker-name">searchCardsByStage (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-search-cards-by-stage-2"><span className="wix-picker-name">searchCardsByStage (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript searchCardsByStage
    import { cards } from '@wix/crm';

    async function searchCardsByStage(pipelineId,options) {
      const response = await cards.searchCardsByStage(pipelineId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchCardsByStage (self-hosted)
    import { createClient } from '@wix/sdk';
    import { cards } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { cards },
      // Include the auth strategy and host as relevant
    });

    async function searchCardsByStage(pipelineId,options) {
      const response = await myWixClient.cards.searchCardsByStage(pipelineId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchCardsByStage (with elevated permissions)
    import { cards } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function mySearchCardsByStageMethod(pipelineId,options) {
      const elevatedSearchCardsByStage = auth.elevate(cards.searchCardsByStage);
      const response = await elevatedSearchCardsByStage(pipelineId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
