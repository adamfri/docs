---
title: "Create Report"
description: "Creates a report."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a report.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.MANAGE-REPORTS">Manage Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/reports/v2/reports
    ```

    ## Body Params

    <ParamField body="report" type="Report" required>

      Report details.

      See the [Report Object](/api-reference/crm/community/feedback-moderation/reports-v2/report-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="report" type="Report">

      Created report.

      See the [Report Object](/api-reference/crm/community/feedback-moderation/reports-v2/report-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Report Created | `wix.reports.v2.report_created` | Triggered when a report is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \ https://www.wixapis.com/reports/v2/reports \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "report": {
                "entityName": "comment",
                "entityId": "50353fbc-b265-4f03-888f-a53aa272758d"
                "reason": {
                  "reasonType": "DRUGS",
                  "description": "This person promotes drug usage."
                }
            }
        }'
      ```

      ```json Response
      {
        "report": {
          "id": "dd7cfd02-1837-4fb8-bc6f-86316f0984a9",
          "entityName": "comment",
          "entityId": "50353fbc-b265-4f03-888f-a53aa272758d",
          "identity": {
            "identityType": "MEMBER",
            "memberId": "141a3e01-da55-4b3a-a44a-2f194bfc8897"
          },
          "reason": {
            "reasonType": "DRUGS",
            "description": "This person promotes drug usage."
          },
          "revision": "1",
          "createdDate": "2021-10-26T17:22:10.299Z",
          "updatedDate": "2021-10-26T17:22:10.299Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a report.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.MANAGE-REPORTS">Manage Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { reports } from '@wix/reports';

    reports.createReport(report): Promise<Report>
    ```

    ## Method Parameters

    <ParamField body="report" type="Report" required>

      Report details.

      See the [Report Object](/api-reference/crm/community/feedback-moderation/reports-v2/report-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Report>`.

    See the [Report Object](/api-reference/crm/community/feedback-moderation/reports-v2/report-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Report Created | `wix.reports.v2.report_created` | Triggered when a report is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-report" id="ex-sdk-create-report-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-report" id="ex-sdk-create-report-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-report" id="ex-sdk-create-report-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-report-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-report-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createReport</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createReport (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createReport (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-report-0"><span className="wix-picker-name">createReport</span></label>
    <label htmlFor="ex-sdk-create-report-1"><span className="wix-picker-name">createReport (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-report-2"><span className="wix-picker-name">createReport (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createReport
    import { reports } from '@wix/reports';

    async function createReport(report) {
      const response = await reports.createReport(report);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createReport (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reports } from '@wix/reports';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reports },
      // Include the auth strategy and host as relevant
    });

    async function createReport(report) {
      const response = await myWixClient.reports.createReport(report);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createReport (with elevated permissions)
    import { reports } from '@wix/reports';
    import { auth } from '@wix/essentials';

    async function myCreateReportMethod(report) {
      const elevatedCreateReport = auth.elevate(reports.createReport);
      const response = await elevatedCreateReport(report);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
