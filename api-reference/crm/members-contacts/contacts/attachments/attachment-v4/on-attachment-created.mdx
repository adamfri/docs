---
title: "onAttachmentCreated"
description: "The onAttachmentCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.contacts.v4.attachment_created` |
| Package | `@wix/crm` |
| Permissions | `Manage Events`, `Manage Contact Attachments` |

## Examples

<CodeGroup>

  ```javascript onAttachmentCreated
  import { attachments } from '@wix/crm';

  attachments.onAttachmentCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onAttachmentCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { attachments } from '@wix/crm';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        attachments,
      },
  });

  wixClient.attachments.onAttachmentCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
