---
title: "Increment Inventory"
description: "Adds a set number of items to inventory."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds a set number of items to inventory.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v2/inventoryItems/increment
    ```

    ## Body Params

    <ParamField body="incrementData" type="IncrementData[]">

      Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="incrementData properties">

        <ParamField body="variantId" type="string">

          Variant ID.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="incrementBy" type="integer">

          Number to increment inventory by.

          Constraints: `min 1`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    Returns `IncrementInventoryResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST https://www.wixapis.com/stores/v2/inventoryItems/increment \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>' \
         --data-binary '
             {
               "incrementData": [
                 {
                   "productId": "f007074f-d7a8-a68b-e6c1-7d7c3de021ff",
                   "variantId": "00000000-0000-0001-0005-9ccb19c4e6f0",
                   "incrementBy": 2
                 }
               ]
             }'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds a set number of items to inventory.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { inventory } from '@wix/stores';

    inventory.incrementInventory(incrementData): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="incrementData" type="IncrementData[]" required>

      Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="incrementData properties">

        <ParamField body="variantId" type="string">

          Variant ID.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="incrementBy" type="integer">

          Number to increment inventory by.

          Constraints: `min 1`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-increment-inventory" id="ex-sdk-increment-inventory-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-increment-inventory" id="ex-sdk-increment-inventory-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-increment-inventory" id="ex-sdk-increment-inventory-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-increment-inventory-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-increment-inventory-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Increment inventory</span><span className="wix-picker-desc">Increase inventory quantity for a product variant</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Increment the inventory of a product's first variant</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">incrementInventory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-increment-inventory-0"><span className="wix-picker-name">Increment inventory</span><span className="wix-picker-desc">Increase inventory quantity for a product variant</span></label>
    <label htmlFor="ex-sdk-increment-inventory-1"><span className="wix-picker-name">Increment the inventory of a product's first variant</span></label>
    <label htmlFor="ex-sdk-increment-inventory-2"><span className="wix-picker-name">incrementInventory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Increment inventory
    import { inventory } from "@wix/stores";

    const incrementData = [
      {
        productId: "c43d36c5-c35a-91ec-1c33-d28efc70d5a6",
        variantId: "00000000-0000-0000-0000-000000000000",
        incrementBy: 2
      }
    ];

    async function incrementInventory() {
      const response = await inventory.incrementInventory(incrementData);
      return response;
    }

    const result = await inventory.incrementInventory(incrementData);
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Increment the inventory of a product's first variant
    /**************************************
     * Backend code - inventory.web.js/ts *
     **************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { inventory, products } from '@wix/stores';

    export const incrementInventory = webMethod(Permissions.Anyone, (incrementData) => {
      return inventory.incrementInventory(incrementData);
    });

    export const getProductVariants = webMethod(Permissions.Anyone, (_id, options) => {
      return products.getProductVariants(_id, options);
    });

    /**************
     * Page code *
     **************/

    import { incrementInventory, getProductVariants } from 'backend/inventory.web';

    async function incrementHandler() {

      const productId = "3fb6a3c8-988b-8755-04bd-ks75ae0b18ea"
      let variants = await getProductVariants(productId);

      incrementInventory(
        [{
          variantId: variants[0]._id,
          productId: productId,
          incrementBy: 1
        }])
        .then(() => {
          console.log("Inventory incremented successfully")
        })
        .catch(error => {
          // Inventory increment failed
          console.error(error);
        })
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript incrementInventory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { inventory } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { inventory },
      // Include the auth strategy and host as relevant
    });

    async function incrementInventory(incrementData) {
      const response = await myWixClient.inventory.incrementInventory(incrementData);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
