---
title: "List Event Categories"
description: "Retrieves a list of categories that are not in the HIDDEN state."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of categories that are not in the `HIDDEN` state.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-EVENTS">Read Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/events/v1/categories/{eventId}
    ```

    ## Path Params

    <ParamField path="eventId" type="string" required>

      Event ID.

    </ParamField>

    ## Response Object

    <ResponseField name="categories" type="Category[]">

      A list of categories.

      See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET 'https://www.wixapis.com/events/v1/categories/e8e784a9-8f2b-47ed-9c42-238c0922da7e' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "categories": [
          {
            "id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a",
            "name": "workshop",
            "states": [
              "MANUAL"
            ],
            "createdDate": "2022-12-13T11:03:19.174Z"
          },
          {
            "id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88",
            "name": "technology",
            "states": [
              "MANUAL"
            ],
            "createdDate": "2023-07-12T11:07:35.776Z"
          }
        ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of categories that are not in the `HIDDEN` state.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-EVENTS">Read Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/events';

    categories.listEventCategories(eventId): Promise<ListEventCategoriesResponse>
    ```

    ## Method Parameters

    <ParamField body="eventId" type="string" required>

      Event ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<ListEventCategoriesResponse>`.

    <ResponseField name="categories" type="Category[]">

      A list of categories.

      See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-event-categories" id="ex-sdk-list-event-categories-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-event-categories" id="ex-sdk-list-event-categories-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-event-categories-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-event-categories-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List event categories</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listEventCategories (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-event-categories-0"><span className="wix-picker-name">List event categories</span></label>
    <label htmlFor="ex-sdk-list-event-categories-1"><span className="wix-picker-name">listEventCategories (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List event categories
    import { categories } from '@wix/events';

    // Sample eventId value: "4e5e4adb-9778-4171-a9bb-44e27834ac89"

    export async function myListEventCategoriesFunction(eventId) {
      try {
        const listedCategories = await categories.listEventCategories(eventId);
        console.log('Event categories: ', listedCategories);
        return listedCategories;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     *  {
     *    "categories": [
     *     {
     *       "name": "workshop",
     *       "states": [
     *         "MANUAL"
     *       ],
     *       "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a",
     *       "_createdDate": "2022-12-13T11:03:19.174Z"
     *     },
     *     {
     *       "name": "technology",
     *       "states": [
     *         "MANUAL"
     *       ],
     *       "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88",
     *       "_createdDate": "2023-07-12T11:07:35.776Z"
     *     }
     *   ]
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listEventCategories (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function listEventCategories(eventId) {
      const response = await myWixClient.categories.listEventCategories(eventId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
