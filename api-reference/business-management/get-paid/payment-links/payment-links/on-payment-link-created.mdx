---
title: "onPaymentLinkCreated"
description: "Triggered when a payment link is created."
icon: "bolt"
---

Triggered when a payment link is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.paymentlinks.v1.payment_link_created` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Paylinks` |

## Examples

<CodeGroup>

  ```javascript onPaymentLinkCreated
  import { paymentLinks } from '@wix/get-paid';

  paymentLinks.onPaymentLinkCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPaymentLinkCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { paymentLinks } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        paymentLinks,
      },
  });

  wixClient.paymentLinks.onPaymentLinkCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
