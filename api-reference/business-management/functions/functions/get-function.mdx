---
title: "Get Function"
description: "Retrieves a function."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a function.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.VIEW-FORM">View Forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/functions/v1/functions/{functionId}
    ```

    ## Path Params

    <ParamField path="functionId" type="string" required>

      ID of the function to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="function" type="Function">

      The retrieved function.

      See the [Function Object](/api-reference/business-management/functions/functions/function-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        https://www.wixapis.com/functions/v1/functions/3099fa19-a7cc-4905-990f-480181918459 \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
          "function": {
              "id": "3099fa19-a7cc-4905-990f-480181918459",
              "revision": "1",
              "createdDate": "2024-01-01T01:01:01.1Z",
              "updatedDate": "2024-01-01T01:01:01.1Z",
              "functionExtensionId": "d1a528f3-ee9b-4a58-82af-7c61ca127e3a",
              "functionName": "Frequent buyer perk",
              "activationStatus": "DRAFT",
              "appId": "553c79f3-5625-4f38-b14b-ef7c0d1e87df",
              "functionExtensionName": "Loyalty rewards",
              "externalIds": []
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a function.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.VIEW-FORM">View Forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { functions } from '@wix/functions';

    functions.getFunction(functionId): Promise<_Function>
    ```

    ## Method Parameters

    <ParamField body="functionId" type="string" required>

      ID of the function to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<_Function>`.

    See the [Function Object](/api-reference/business-management/functions/functions/function-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-function" id="ex-sdk-get-function-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-function" id="ex-sdk-get-function-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-function" id="ex-sdk-get-function-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-function-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-function-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getFunction</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getFunction (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getFunction (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-function-0"><span className="wix-picker-name">getFunction</span></label>
    <label htmlFor="ex-sdk-get-function-1"><span className="wix-picker-name">getFunction (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-function-2"><span className="wix-picker-name">getFunction (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getFunction
    import { functions } from '@wix/functions';

    async function getFunction(functionId) {
      const response = await functions.getFunction(functionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getFunction (self-hosted)
    import { createClient } from '@wix/sdk';
    import { functions } from '@wix/functions';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { functions },
      // Include the auth strategy and host as relevant
    });

    async function getFunction(functionId) {
      const response = await myWixClient.functions.getFunction(functionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getFunction (with elevated permissions)
    import { functions } from '@wix/functions';
    import { auth } from '@wix/essentials';

    async function myGetFunctionMethod(functionId) {
      const elevatedGetFunction = auth.elevate(functions.getFunction);
      const response = await elevatedGetFunction(functionId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
