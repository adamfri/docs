---
title: "Bulk Delete Rules"
description: "Bulk deletes rules."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Bulk deletes rules.

    To delete a single rule, use [Delete Rule](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/service-fees/rules/delete-rule).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/service-fees/v1/bulk/rules/delete
    ```

    ## Query Params

    <ParamField query="ruleIds" type="string[]" required>

      IDs of the rules to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkRuleResult[]">

      Information about the deleted rules.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata of the rule.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="rule" type="Rule">

          The created rule.

          See the [Rule Object](/api-reference/business-solutions/restaurants/online-orders/service-fees/rules/rule-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk Delete Rule metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rule Deleted | `wix.service.fees.v1.rule_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE https://www.wixapis.com/service-fees-rules/v1/rules/delete \
      -H 'Content-Type: application/json;charset=UTF-8' \
      -H 'Authorization: <AUTH>' \
      --data-raw '{
        "rule_ids": [
          "8f6dc28a-a915-446b-ae75-d201f9f462a9"
        ]'
      }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "8f6dc28a-a915-446b-ae75-d201f9f462a9",
              "originalIndex": 0,
              "success": true,
              "error": null
            },
            "rule": null
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 1,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Bulk deletes rules.

    To delete a single rule, use [Delete Rule](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/service-fees/rules/delete-rule).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { serviceFees } from '@wix/restaurants';

    serviceFees.bulkDeleteRules(ruleIds): Promise
    ```

    ## Method Parameters

    <ParamField body="ruleIds" type="string[]" required>

      IDs of the rules to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkRuleResult[]">

      Information about the deleted rules.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata of the rule.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="rule" type="Rule">

          The created rule.

          See the [Rule Object](/api-reference/business-solutions/restaurants/online-orders/service-fees/rules/rule-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk Delete Rule metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rule Deleted | `wix.service.fees.v1.rule_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-rules" id="ex-sdk-bulk-delete-rules-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-rules" id="ex-sdk-bulk-delete-rules-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-rules" id="ex-sdk-bulk-delete-rules-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-rules-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-rules-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteRules</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteRules (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteRules (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-rules-0"><span className="wix-picker-name">bulkDeleteRules</span></label>
    <label htmlFor="ex-sdk-bulk-delete-rules-1"><span className="wix-picker-name">bulkDeleteRules (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-rules-2"><span className="wix-picker-name">bulkDeleteRules (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteRules
    import { serviceFees } from '@wix/restaurants';

    async function bulkDeleteRules(ruleIds) {
      const response = await serviceFees.bulkDeleteRules(ruleIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteRules (self-hosted)
    import { createClient } from '@wix/sdk';
    import { serviceFees } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { serviceFees },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteRules(ruleIds) {
      const response = await myWixClient.serviceFees.bulkDeleteRules(ruleIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteRules (with elevated permissions)
    import { serviceFees } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteRulesMethod(ruleIds) {
      const elevatedBulkDeleteRules = auth.elevate(serviceFees.bulkDeleteRules);
      const response = await elevatedBulkDeleteRules(ruleIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
