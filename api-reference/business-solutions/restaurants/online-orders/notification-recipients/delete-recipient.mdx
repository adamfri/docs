---
title: "Delete Recipient"
description: "Deletes a recipient."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a recipient.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/restaurants/recipient/v1/recipients/{recipientId}
    ```

    ## Path Params

    <ParamField path="recipientId" type="string" required>

      ID of the recipient to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteRecipientResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Recipient Deleted | `wix.restaurants.recipient.v1.recipient_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE \
        'https://www.wixapis.com/restaurants/v1/recipients/12345678-1234-1234-1234-123456789012' \
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a recipient.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { recipients } from '@wix/restaurants';

    recipients.deleteRecipient(recipientId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="recipientId" type="string" required>

      ID of the recipient to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Recipient Deleted | `wix.restaurants.recipient.v1.recipient_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-recipient" id="ex-sdk-delete-recipient-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-recipient" id="ex-sdk-delete-recipient-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-recipient" id="ex-sdk-delete-recipient-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-recipient-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-recipient-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteRecipient</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteRecipient (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteRecipient (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-recipient-0"><span className="wix-picker-name">deleteRecipient</span></label>
    <label htmlFor="ex-sdk-delete-recipient-1"><span className="wix-picker-name">deleteRecipient (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-recipient-2"><span className="wix-picker-name">deleteRecipient (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteRecipient
    import { recipients } from '@wix/restaurants';

    async function deleteRecipient(recipientId) {
      const response = await recipients.deleteRecipient(recipientId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteRecipient (self-hosted)
    import { createClient } from '@wix/sdk';
    import { recipients } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { recipients },
      // Include the auth strategy and host as relevant
    });

    async function deleteRecipient(recipientId) {
      const response = await myWixClient.recipients.deleteRecipient(recipientId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteRecipient (with elevated permissions)
    import { recipients } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myDeleteRecipientMethod(recipientId) {
      const elevatedDeleteRecipient = auth.elevate(recipients.deleteRecipient);
      const response = await elevatedDeleteRecipient(recipientId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
