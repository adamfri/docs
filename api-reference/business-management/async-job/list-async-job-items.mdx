---
title: "List Async Job Items"
description: "Retrieves a list of job items."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of job items.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ASYNC_JOB.READ">READ ASYNC JOBS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/async-jobs/v1/async-jobs/{jobId}/items
    ```

    ## Path Params

    <ParamField path="jobId" type="string" required>

      Job ID.

    </ParamField>

    ## Query Params

    <ParamField query="paging" type="CursorPaging">

      Pagination options.

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Maximum number of items to return in the results.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="statusFilter" type="string">

      async job item status filter.

      **Supported values:**

      - `ALL` — Return all items.
      - `FAILED_ONLY` — Return only failed items.
      - `SUCCESSFUL_ONLY` — Return only successful items.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="AsyncJobItem[]">

      Job items.

      <Expandable title="results properties">

        <ResponseField name="id" type="string">

          Job item ID.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="data" type="object">

          Job item data, as passed by the originating service.

        </ResponseField>

        <ResponseField name="entityId" type="string">

          ID of the entity being processed, as passed by the originating service.

        </ResponseField>

        <ResponseField name="success" type="boolean">

          Whether the job item was processed successfully.

          **Read-only.**

        </ResponseField>

        <ResponseField name="error" type="ApplicationError">

          Error that prevented the item from being processed, if relevant.

          Constraints: `immutable`.

          <Expandable title="error properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
          'https://www.wixapis.com/async-jobs/v1/async-jobs/076e0505-6944-4645-a248-80b197ee9cfd/items?limit=10' \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "results": [
          {
            "id": "365b44a2-61b2-42a0-87cf-272e7c42cc6e",
            "data": {},
            "entityId": "entity1",
            "success": true
          },
          {
            "id": "6fe127a3-9846-480f-8d9c-dc1e00623eaa",
            "data": {},
            "entityId": "entity2",
            "success": true
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of job items.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ASYNC_JOB.READ">READ ASYNC JOBS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { asyncJobs } from '@wix/async-jobs';

    asyncJobs.listAsyncJobItems(jobId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="jobId" type="string" required>

      Job ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="ListAsyncJobItemsOptions">

      Field options.

      <Expandable title="options properties">

        <ParamField body="paging" type="CursorPaging">

          Pagination options.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return in the results.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="statusFilter" type="string">

          async job item status filter.

          **Supported values:**

          - `ALL` — Return all items.
          - `FAILED_ONLY` — Return only failed items.
          - `SUCCESSFUL_ONLY` — Return only successful items.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="AsyncJobItem[]">

      Job items.

      <Expandable title="results properties">

        <ResponseField name="_id" type="string">

          Job item ID.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="data" type="object">

          Job item data, as passed by the originating service.

        </ResponseField>

        <ResponseField name="entityId" type="string">

          ID of the entity being processed, as passed by the originating service.

        </ResponseField>

        <ResponseField name="success" type="boolean">

          Whether the job item was processed successfully.

          **Read-only.**

        </ResponseField>

        <ResponseField name="error" type="ApplicationError">

          Error that prevented the item from being processed, if relevant.

          Constraints: `immutable`.

          <Expandable title="error properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-async-job-items" id="ex-sdk-list-async-job-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-async-job-items" id="ex-sdk-list-async-job-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-async-job-items" id="ex-sdk-list-async-job-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-async-job-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-async-job-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listAsyncJobItems</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listAsyncJobItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listAsyncJobItems (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-async-job-items-0"><span className="wix-picker-name">listAsyncJobItems</span></label>
    <label htmlFor="ex-sdk-list-async-job-items-1"><span className="wix-picker-name">listAsyncJobItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-async-job-items-2"><span className="wix-picker-name">listAsyncJobItems (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listAsyncJobItems
    import { asyncJobs } from '@wix/async-jobs';

    async function listAsyncJobItems(jobId,options) {
      const response = await asyncJobs.listAsyncJobItems(jobId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listAsyncJobItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { asyncJobs } from '@wix/async-jobs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { asyncJobs },
      // Include the auth strategy and host as relevant
    });

    async function listAsyncJobItems(jobId,options) {
      const response = await myWixClient.asyncJobs.listAsyncJobItems(jobId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listAsyncJobItems (with elevated permissions)
    import { asyncJobs } from '@wix/async-jobs';
    import { auth } from '@wix/essentials';

    async function myListAsyncJobItemsMethod(jobId,options) {
      const elevatedListAsyncJobItems = auth.elevate(asyncJobs.listAsyncJobItems);
      const response = await elevatedListAsyncJobItems(jobId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
