---
title: "Verify Email"
description: "Verifies the sender's email using a verification code sent to the user's email address upon update."
icon: "upload"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Verifies the sender's email using a verification code sent to the user's email address upon update.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/email-marketing/v1/sender-details/verify-email
    ```

    ## Body Params

    <ParamField body="verificationCode" type="string" required>

      Verification code.

    </ParamField>

    ## Response Object

    Returns `VerifySenderEmailResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_VERIFICATION_CODE` | Verification code does not match with the one sent via email. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/email-marketing/v1/sender-details/verify-email' \
      --data-binary '{
                       "verificationCode": "abcde"
                     }' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Verifies the sender's email using a verification code sent to the user's email address upon update.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderDetails } from '@wix/email-marketing';

    senderDetails.verifyEmail(verificationCode): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="verificationCode" type="string" required>

      Verification code.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_VERIFICATION_CODE` | Verification code does not match with the one sent via email. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Details Updated | `wix.email_marketing.v1.sender_details_updated` | An event that triggers after a user has verified changes to their sender details. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-verify-email" id="ex-sdk-verify-email-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-verify-email" id="ex-sdk-verify-email-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-verify-email" id="ex-sdk-verify-email-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-verify-email-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-verify-email-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Verifies an updated sender address with a verification code</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Verifies an updated sender address with a verification code (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">verifyEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-verify-email-0"><span className="wix-picker-name">Verifies an updated sender address with a verification code</span></label>
    <label htmlFor="ex-sdk-verify-email-1"><span className="wix-picker-name">Verifies an updated sender address with a verification code (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-verify-email-2"><span className="wix-picker-name">verifyEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Verifies an updated sender address with a verification code
    import { senderDetails } from '@wix/email-marketing';

    // Sample verificationCode: "Sw4e7"

    export async function myVerifyEmailFunction(verificationCode) {
      try {
        const result = await senderDetails.verifyEmail(verificationCode);

        console.log('Success! Your email address has been verified')
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise returns void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Verifies an updated sender address with a verification code (with elevated permissions)
    import { senderDetails } from '@wix/email-marketing';
    import { auth } from '@wix/essentials';

    const elevatedVerifyEmail = auth.elevate(senderDetails.verifyEmail);

    // Sample verificationCode: "Sw4e7"

    export async function myVerifyEmailFunction(verificationCode) {
      try {
        const result = await elevatedVerifyEmail(verificationCode);

        console.log('Success! Your email address has been verified')
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise returns void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript verifyEmail (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderDetails } from '@wix/email-marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderDetails },
      // Include the auth strategy and host as relevant
    });

    async function verifyEmail(verificationCode) {
      const response = await myWixClient.senderDetails.verifyEmail(verificationCode);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
