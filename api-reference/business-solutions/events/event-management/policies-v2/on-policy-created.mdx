---
title: "onPolicyCreated"
description: "Triggered when a policy is created."
icon: "bolt"
---

Triggered when a policy is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v2.policy_created` |
| Package | `@wix/events` |
| Permissions | `Manage Events - all permissions`, `Manage Events`, `Read Policies`, `Manage Policies` |

## Examples

<CodeGroup>

  ```javascript onPolicyCreated
  import { policies } from '@wix/events';

  policies.onPolicyCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPolicyCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { policies } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        policies,
      },
  });

  wixClient.policies.onPolicyCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
