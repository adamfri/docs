---
title: "onItemVariantDeleted"
description: "The onItemVariantDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_variant_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemVariantDeleted
  import { itemVariants } from '@wix/restaurants';

  itemVariants.onItemVariantDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemVariantDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemVariants } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemVariants,
      },
  });

  wixClient.itemVariants.onItemVariantDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
