---
title: "onBookingRescheduled"
description: "Triggered when a booking is rescheduled."
icon: "bolt"
---

Triggered when a booking is rescheduled.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.v2.booking_rescheduled` |
| Package | `@wix/bookings` |
| Permissions | `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions`, `Read bookings calendar - including participants` |

## Examples

<CodeGroup>

  ```javascript onBookingRescheduled
  import { bookings } from '@wix/bookings';

  bookings.onBookingRescheduled((event) => {
      // handle your event here
  });
  ```

  ```javascript onBookingRescheduled (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bookings } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bookings,
      },
  });

  wixClient.bookings.onBookingRescheduled((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
