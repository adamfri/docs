---
title: "Validate Submission"
description: "Note: The Form Submission service plugin only works with the Wix Forms app."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Form Submission service plugin only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.

    </Note>
    <br />
    Validates a submission. <br />
    Validates a site visitor's form submission and returns any validation violations. <br />
    Site visitors can see the validation violations on their forms. For example, invalid fields are highlighted in red.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORMS.MANAGE-SUBMISSIONS">Manage Submissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST /v4/submissions/validate
    ```

    ## Body Params

    <ParamField body="submission" type="FormSubmission" required>

      Submission to validate.

      <Expandable title="submission properties">

        <ParamField body="id" type="string">

          Submission ID.

          **Read-only.**

        </ParamField>

        <ParamField body="formId" type="string">

          ID of the form which the submission belongs to.

          Constraints: `format GUID`, `immutable`.

        </ParamField>

        <ParamField body="namespace" type="string">

          The app which the form submissions belong to. For example, the namespace for the Wix Forms app is `wix.form_app.form`. Call `Get Submission` to retrieve the namespace.

          **Read-only.** Constraints: `minLength 10`, `maxLength 50`.

        </ParamField>

        <ParamField body="status" type="string">

          Status of the submission.

          The status a submission is created with depends on the form's namespace and on whether the form collects payment, so read it from the response rather than assuming.

          A submission for a form that collects payment starts as `PAYMENT_WAITING`. Otherwise, most namespaces record the submission immediately as `CONFIRMED`. Namespaces that need to complete another step first, such as taking payment for a pricing plan, create the submission as `PENDING` and confirm it later with Confirm Submission.

          A `PENDING` submission isn't recorded yet, and is deleted automatically if it isn't confirmed in time.

          **Supported values:**

          - `PENDING` — A submission is created, but has not yet been recorded in the Wix Forms collection.
          - `CONFIRMED` — A submission is recorded in the Wix Forms collection.
          - `PAYMENT_WAITING` — A form submission requiring payment is created.
          - `PAYMENT_CANCELED` — An order of a form submission is canceled.

        </ParamField>

        <ParamField body="submissions" type="object">

          Values the visitor submitted.

          Each key is the `target` of a field in the form schema, which is the field's stable storage key rather than its ID or label. Only keys matching a field target are accepted.

          Each value's shape follows that field's `inputType`: `STRING` fields submit a string, `NUMBER` a number, `BOOLEAN` a boolean, and `ARRAY` an array of the selected option values. Date and time fields are strings.

          3 input types submit structured values: `ADDRESS` submits an address object, `PAYMENT` an array holding 1 product object per item bought, and `SCHEDULING` an appointment object.

          For the full mapping, see [About Submission Values](https://dev.wix.com/docs/api-reference/crm/forms/form-submissions/about-submission-values).

          Constraints: `format map`.

          <Expandable title="submissions properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="any" />

          </Expandable>

        </ParamField>

        <ParamField body="createdDate" type="datetime">

          Date and time the form submission was created.

        </ParamField>

        <ParamField body="updatedDate" type="datetime">

          Date and time the form submission was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer">

          Revision number, which increments by 1 each time the form submission is updated. To prevent conflicting changes, the existing revision must be used when updating a form submission.

          **Read-only.**

        </ParamField>

        <ParamField body="submitter" type="Submitter">

          ID of the visitor that submitted the form.

          **Read-only.**

          <Expandable title="One of the following">

            <ParamField body="submitter" type="submitter">

              submitter

              _Nested structure omitted for brevity — see the `submitter` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="seen" type="boolean">

          Whether a site owner marked a submission as "seen".

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Data extension object that holds users' and apps' fields.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="orderDetails" type="OrderDetails">

          Order details. &lt;br>
          &lt;b>Note&lt;/b>: This object is only applicable when submitting a form in the Wix Payments app.

          <Expandable title="orderDetails properties">

            <ParamField body="orderId" type="string">

              ID of the order related to submission (only applicable if a form has payments).

              **Read-only.**

            </ParamField>

            <ParamField body="number" type="integer">

              Order number.

              **Read-only.**

            </ParamField>

            <ParamField body="currency" type="string">

              Currency.

              **Read-only.**

            </ParamField>

            <ParamField body="itemSubtotal" type="string">

              Item subtotal.

              **Read-only.** Constraints: `>= 0.00`.

            </ParamField>

            <ParamField body="checkoutId" type="string">

              ID of the checkout related to submission (only applicable if a form has payments).

              **Read-only.** Constraints: `format GUID`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="contactId" type="string">

          Contact ID of a site visitor who created the submission.

        </ParamField>

        <ParamField body="accessRestriction" type="string">

          Authorized viewer ID.

        </ParamField>

        <ParamField body="tags" type="PublicTags">

          Tag IDs collections associated with the current entity.

          <Expandable title="tags properties">

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="appointmentDetails" type="AppointmentDetails">

          Appointment details.
          This object is only applicable when submitting a form that creates a booking appointment.

          <Expandable title="appointmentDetails properties">

            <ParamField body="staffMemberId" type="string">

              ID of the staff member assigned to the appointment.

            </ParamField>

            <ParamField body="staffMemberName" type="string">

              Name of the staff member assigned to the appointment.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="actionType" type="string" required>

      Whether to create or update the submission.

      **Supported values:**

      - `UNKNOWN_ACTION` — Unknown action type.
      - `UPDATE` — Update submission.
      - `CREATE` — Create submission.

    </ParamField>

    ## Response Object

    <ResponseField name="errors" type="SubmissionValidationError[]">

      List of validation errors. &lt;br>
      If there are no validation errors, returns an empty array.

      Constraints: `maxItems 100`.

      <Expandable title="errors properties">

        <ResponseField name="errorPath" type="string">

          Path indicating the source of the error, such as `form.fields.target`.

          Constraints: `maxLength 500`.

        </ResponseField>

        <ResponseField name="params" type="object">

          Additional error parameters.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'http://www.wixapis.com/form-submission/v4/submissions/validate' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "submission": {
                "id": "e62e3011-55cf-4de3-a497-e097b52d86b7",
                "formId": "e62e3011-55cf-4de3-a497-e097b52d86b8",
                "namespace": "wix.form_app.form",
                "status": "PENDING",
                "submissions": {
                  "first_name": "J0hn",
                  "last_name": "Doe",
                  "fiend_list": ["Tom", "Sally", "Bob"]
                },
                "submitter": {
                  "memberId": "4b42fb49-8c77-4128-859c-8247756f68ef"
                },
                "seen": false,
                "revision": "1",
                "createdDate": "2019-10-30T17:22:10.299Z",
                "updatedDate": "2019-10-30T17:22:10.299Z"
              },
              "actionType": "CREATE"
            }'
      ```

      ```json Response
      {
        "errors": [
          {
            "errorPath": "first_name",
            "params": {},
            "errorType": "TYPE_ERROR"
          },
          {
            "errorPath": "fiend_list/2",
            "params": {},
            "customErrorMessage": "Bob is not a friend"
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Form Submission service plugin only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.

    </Note>
    <br />
    Validates a submission. <br />
    Validates a site visitor's form submission and returns any validation violations. <br />
    Site visitors can see the validation violations on their forms. For example, invalid fields are highlighted in red.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORMS.MANAGE-SUBMISSIONS">Manage Submissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { formSubmissions } from '@wix/forms';

    formSubmissions.validateSubmission(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="ValidateSubmissionRequest">

      request

      <Expandable title="request properties">

        <ParamField body="submission" type="FormSubmission">

          Submission to validate.

          <Expandable title="submission properties">

            <ParamField body="_id" type="string">

              Submission ID.

              **Read-only.**

            </ParamField>

            <ParamField body="formId" type="string">

              ID of the form which the submission belongs to.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="namespace" type="string">

              The app which the form submissions belong to. For example, the namespace for the Wix Forms app is `wix.form_app.form`. Call `Get Submission` to retrieve the namespace.

              **Read-only.** Constraints: `minLength 10`, `maxLength 50`.

            </ParamField>

            <ParamField body="status" type="string">

              Status of the submission.

              The status a submission is created with depends on the form's namespace and on whether the form collects payment, so read it from the response rather than assuming.

              A submission for a form that collects payment starts as `PAYMENT_WAITING`. Otherwise, most namespaces record the submission immediately as `CONFIRMED`. Namespaces that need to complete another step first, such as taking payment for a pricing plan, create the submission as `PENDING` and confirm it later with Confirm Submission.

              A `PENDING` submission isn't recorded yet, and is deleted automatically if it isn't confirmed in time.

              **Supported values:**

              - `PENDING` — A submission is created, but has not yet been recorded in the Wix Forms collection.
              - `CONFIRMED` — A submission is recorded in the Wix Forms collection.
              - `PAYMENT_WAITING` — A form submission requiring payment is created.
              - `PAYMENT_CANCELED` — An order of a form submission is canceled.

            </ParamField>

            <ParamField body="submissions" type="object">

              Values the visitor submitted.

              Each key is the `target` of a field in the form schema, which is the field's stable storage key rather than its ID or label. Only keys matching a field target are accepted.

              Each value's shape follows that field's `inputType`: `STRING` fields submit a string, `NUMBER` a number, `BOOLEAN` a boolean, and `ARRAY` an array of the selected option values. Date and time fields are strings.

              3 input types submit structured values: `ADDRESS` submits an address object, `PAYMENT` an array holding 1 product object per item bought, and `SCHEDULING` an appointment object.

              For the full mapping, see [About Submission Values](https://dev.wix.com/docs/api-reference/crm/forms/form-submissions/about-submission-values).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

            <ParamField body="_createdDate" type="datetime">

              Date and time the form submission was created.

            </ParamField>

            <ParamField body="_updatedDate" type="datetime">

              Date and time the form submission was updated.

              **Read-only.**

            </ParamField>

            <ParamField body="revision" type="integer">

              Revision number, which increments by 1 each time the form submission is updated. To prevent conflicting changes, the existing revision must be used when updating a form submission.

              **Read-only.**

            </ParamField>

            <ParamField body="submitter" type="Submitter">

              ID of the visitor that submitted the form.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Submitter` object._

            </ParamField>

            <ParamField body="seen" type="boolean">

              Whether a site owner marked a submission as "seen".

            </ParamField>

            <ParamField body="extendedFields" type="ExtendedFields">

              Data extension object that holds users' and apps' fields.

              _Nested structure omitted for brevity — see the `ExtendedFields` object._

            </ParamField>

            <ParamField body="orderDetails" type="OrderDetails">

              Order details. &lt;br>
              &lt;b>Note&lt;/b>: This object is only applicable when submitting a form in the Wix Payments app.

              _Nested structure omitted for brevity — see the `OrderDetails` object._

            </ParamField>

            <ParamField body="contactId" type="string">

              Contact ID of a site visitor who created the submission.

            </ParamField>

            <ParamField body="accessRestriction" type="string">

              Authorized viewer ID.

            </ParamField>

            <ParamField body="tags" type="PublicTags">

              Tag IDs collections associated with the current entity.

              _Nested structure omitted for brevity — see the `PublicTags` object._

            </ParamField>

            <ParamField body="appointmentDetails" type="AppointmentDetails">

              Appointment details.
              This object is only applicable when submitting a form that creates a booking appointment.

              _Nested structure omitted for brevity — see the `AppointmentDetails` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="actionType" type="string">

          Whether to create or update the submission.

          **Supported values:**

          - `UNKNOWN_ACTION` — Unknown action type.
          - `UPDATE` — Update submission.
          - `CREATE` — Create submission.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="actions" type="actions">

          actions

          <Expandable title="actions properties">

            <ParamField body="updateOptions" type="UpdateOptions">

              Data for updating an existing submission.

              _Nested structure omitted for brevity — see the `UpdateOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="errors" type="SubmissionValidationError[]">

      List of validation errors. &lt;br>
      If there are no validation errors, returns an empty array.

      Constraints: `maxItems 100`.

      <Expandable title="errors properties">

        <ResponseField name="errorPath" type="string">

          Path indicating the source of the error, such as `form.fields.target`.

          Constraints: `maxLength 500`.

        </ResponseField>

        <ResponseField name="params" type="object">

          Additional error parameters.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-validate-submission" id="ex-sdk-validate-submission-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-validate-submission" id="ex-sdk-validate-submission-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-validate-submission-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-validate-submission-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Example of an errors return value @description:</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">validateSubmission (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-validate-submission-0"><span className="wix-picker-name">Example of an errors return value @description:</span></label>
    <label htmlFor="ex-sdk-validate-submission-1"><span className="wix-picker-name">validateSubmission (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Example of an errors return value @description:
    import { formSubmissions } from '@wix/forms/service-plugins';

    formSubmissions.provideHandlers({

      validateSubmission: async ( payload ) => {
        const {request, metadata} = payload;
            // Use the `request` and `metadata` received from Wix and
            // apply custom logic.
          return {
            // Return your response exactly as documented to integrate with Wix.
            // Return value example:
            errors: [
              {
                errorPath: "first_name",
                params: {},
                errorType: "TYPE_ERROR"
              },
              {
                errorPath: "fiend_list/2",
                params: {},
                custom_error_message: "Bob is not a friend."
              }
            ]
          }
        }
    });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript validateSubmission (self-hosted)
    import { createClient } from '@wix/sdk';
    import { formSubmissions } from '@wix/forms/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { formSubmissions },
      // Include the auth strategy and host as relevant
    });

    async function validateSubmission(request,metadata) {
      const response = await myWixClient.formSubmissions.validateSubmission(request,metadata);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
