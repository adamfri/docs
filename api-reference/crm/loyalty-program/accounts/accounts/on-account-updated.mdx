---
title: "onAccountUpdated"
description: "Triggered when a loyalty account is updated, including when points are adjusted, earned, or redeemed."
icon: "bolt"
---

Triggered when a loyalty account is updated, including when points are adjusted, earned, or redeemed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.v1.account_updated` |
| Package | `@wix/loyalty` |
| Permissions | `Read Loyalty`, `Manage Loyalty` |

## Examples

<CodeGroup>

  ```javascript onAccountUpdated
  import { accounts } from '@wix/loyalty';

  accounts.onAccountUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onAccountUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { accounts } from '@wix/loyalty';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        accounts,
      },
  });

  wixClient.accounts.onAccountUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
