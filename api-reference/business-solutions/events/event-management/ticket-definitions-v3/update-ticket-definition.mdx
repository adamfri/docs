---
title: "Update Ticket Definition"
description: "Updates a ticket definition."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a ticket definition.

    Each time the ticket definition is updated, `revision` increments by 1. The existing `revision` must be included when updating the ticket definition. This ensures you're working with the latest ticket definition and prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-TICKET-DEF">Manage Ticket Definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/events/v3/ticket-definitions/{ticketDefinition.id}
    ```

    ## Path Params

    <ParamField path="ticketDefinition.id" type="string" required>

      Ticket definition ID.

    </ParamField>

    ## Body Params

    <ParamField body="ticketDefinition" type="TicketDefinition" required>

      Ticket definition to update.

      See the [Ticket Definition Object](/api-reference/business-solutions/events/event-management/ticket-definitions-v3/ticket-definition-object) for this object's fields.

    </ParamField>

    <ParamField body="fields" type="string[]">

      Predefined sets of fields to return.

      Constraints: `maxItems 5`.

      **Supported values:**

      - `SALES_DETAILS` — Returns `salesDetails` in the response.
      - `EVENT_DETAILS` — Returns `eventDetails` in the response.
      - `SEATING_DETAILS` — Returns `seatingDetails` with available seats in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="ticketDefinition" type="TicketDefinition">

      The updated ticket definition.

      See the [Ticket Definition Object](/api-reference/business-solutions/events/event-management/ticket-definitions-v3/ticket-definition-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `INVALID_SALE_PERIOD` | The `salePeriod.startDate` must be before the `salePeriod.endDate`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ticket Definition Updated | `wix.events.v3.ticket_definition_updated` | Triggered when a ticket definition is updated. |
    | Ticket Definition Sale Period Updated | `wix.events.v3.ticket_definition_sale_period_updated` | Triggered when a sale period is updated. |
    | Ticket Definition Sale Started | `wix.events.v3.ticket_definition_sale_started` | Triggered when a ticket sale starts. Sales details aren't included in the response. |
    | Ticket Definition Sale Ended | `wix.events.v3.ticket_definition_sale_ended` | Triggered when a ticket sale ends. Sales details aren't included in the response. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH 'https://www.wixapis.com/events/v3/ticket-definitions/aaf51c30-a61c-473e-96be-a42dfa5064c0' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "ticketDefinition": {
          "id": "aaf51c30-a61c-473e-96be-a42dfa5064c0",
          "eventId": "9d720f99-1b5a-4141-9877-d32985391e18",
          "revision": "1",
          "name": "Leather Workshop",
          "pricingMethod": {
            "fixedPrice": {
              "value": "200.00",
              "currency": "USD"
            }
          },
          "feeType": "FEE_INCLUDED",
          "salePeriod": {
            "startDate": "2023-06-08T10:00:00Z"
          }
        }
      }'
      ```

      ```json Response
      {
        "ticketDefinition": {
          "id": "aaf51c30-a61c-473e-96be-a42dfa5064c0",
          "eventId": "9d720f99-1b5a-4141-9877-d32985391e18",
          "revision": "2",
          "createdDate": "2023-05-25T11:39:26.108Z",
          "updatedDate": "2023-05-25T13:44:58.775Z",
          "name": "Leather Workshop",
          "description": "Step into the world of fine craftsmanship and explore the artistry of handcrafted leather goods at our captivating Leather Workshop. Join us for a hands-on experience that delves into the intricate process of transforming raw leather into exquisite creations. Immerse yourself in a creative atmosphere where seasoned leather artisans will guide you through the journey of mastering age-old techniques and unlocking your inner artisan.",
          "policyText": "This is your event ticket. Ticket holders must present their tickets on entry. You can either print your ticket or present this digital version. You can find all the details about this event on our website. If you have any questions, issues or wish to get a refund, contact the event host. If you can’t attend the event, please get in touch. Looking forward to seeing you there!",
          "hidden": false,
          "limited": true,
          "initialLimit": 3,
          "actualLimit": 3,
          "pricingMethod": {
            "fixedPrice": {
              "value": "200.00",
              "currency": "USD"
            },
            "pricingType": "STANDARD",
            "free": false
          },
          "feeType": "FEE_INCLUDED",
          "salePeriod": {
            "startDate": "2023-06-08T10:00:00Z",
            "endDate": "2023-06-30T00:00:00Z",
            "displayNotOnSale": false
          },
          "saleStatus": "SALE_STARTED",
          "salesDetails": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a ticket definition.

    Each time the ticket definition is updated, `revision` increments by 1. The existing `revision` must be included when updating the ticket definition. This ensures you're working with the latest ticket definition and prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-TICKET-DEF">Manage Ticket Definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { ticketDefinitionsV2 } from '@wix/events';

    ticketDefinitionsV2.updateTicketDefinition(_id, ticketDefinition, options): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Ticket definition ID.

      **Read-only.**

    </ParamField>

    <ParamField body="ticketDefinition" type="UpdateTicketDefinition" required>

      Ticket definition to update.

      <Expandable title="ticketDefinition properties">

        <ParamField body="_id" type="string">

          Ticket definition ID.

          **Read-only.**

        </ParamField>

        <ParamField body="eventId" type="string">

          Event ID to which the ticket definition belongs.

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the ticket definition is updated. To prevent conflicting changes, the existing `revision` must be used when updating a ticket definition.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the ticket definition was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the ticket definition was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Ticket definition name.

        </ParamField>

        <ParamField body="description" type="string">

          Ticket definition description.

        </ParamField>

        <ParamField body="policyText" type="string">

          Ticket definition policy.

        </ParamField>

        <ParamField body="hidden" type="boolean">

          Whether this ticket definition is hidden from site visitors and can't be purchased.

        </ParamField>

        <ParamField body="limited" type="boolean">

          Whether the ticket has a limited maximum quantity.

          **Read-only.**

        </ParamField>

        <ParamField body="initialLimit" type="integer">

          The maximum number of tickets that can be sold for the event when first defining the event. If a seating map is defined after you created a ticket definition, this property is ignored and `actualLimit` is used instead. To create unlimited tickets, skip this field in the request.

        </ParamField>

        <ParamField body="actualLimit" type="integer">

          The maximum number of tickets that can be sold for the event after adding a seating map to the event. If no seating map is defined, this property is the same as `initialLimit`.

          **Read-only.**

        </ParamField>

        <ParamField body="pricingMethod" type="PricingMethod">

          Ticket pricing method.

          <Expandable title="pricingMethod properties">

            <ParamField body="pricingType" type="string">

              Ticket price type.

              **Read-only.**

              **Supported values:**

              - `STANDARD` — All money goes to a seller. Applies to all ticket pricing methods except for `guestPrice`.
              - `DONATION` — All collected money is a donation. This pricing type is automatically assigned when you select the `guestPrice` pricing method.

            </ParamField>

            <ParamField body="free" type="boolean">

              Whether the ticket is free. To create a free ticket, enter `"0"` in `pricingMethod.fixedPrice.value`.

              **Read-only.**

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="price" type="price">

              price

              _Nested structure omitted for brevity — see the `price` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="feeType" type="string">

          Type of ticket service fee to collect.

          **Supported values:**

          - `FEE_INCLUDED` — The fee is deducted from the ticket price for a seller. &lt;br>&lt;br> For example, if you're selling tickets for $10, then a ticket service fee of $0.25 will be deducted from the price and you'll get $9.75.
          - `FEE_ADDED_AT_CHECKOUT` — The fee is shown in addition to the ticket price at checkout and a guest pays the fee. &lt;br>&lt;br> For example, if you sell tickets for $10, a customer will see a ticket service fee of $0.25 and will pay $10.25 in total.
          - `NO_FEE` — Ticket service fee isn't collected. Available only for free tickets and legacy Wix users.

        </ParamField>

        <ParamField body="salePeriod" type="SalePeriod">

          Ticket sale period.

          <Expandable title="salePeriod properties">

            <ParamField body="startDate" type="datetime">

              Date and time the ticket sale starts. Rounded down to the nearest minute.

            </ParamField>

            <ParamField body="endDate" type="datetime">

              Date and time the ticket sale ends. Rounded down to the nearest minute.

            </ParamField>

            <ParamField body="displayNotOnSale" type="boolean">

              Whether to display the ticket if it's not available to buy.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="saleStatus" type="string">

          Ticket sale status.

          **Read-only.**

          **Supported values:**

          - `SALE_SCHEDULED` — Tickets aren't on sale yet.
          - `SALE_STARTED` — Tickets are on sale.
          - `SALE_ENDED` — Tickets are no longer on sale.

        </ParamField>

        <ParamField body="salesDetails" type="SalesDetails">

          Ticket sales information. &lt;br> &lt;br>
          **Note:** This field is only returned when `SALES_DETAILS` is specified in `field` in the request.

          **Read-only.**

          <Expandable title="salesDetails properties">

            <ParamField body="unsoldCount" type="integer">

              Number of tickets that haven't been purchased yet. The field is `null` if the ticket quantity is unlimited.

              **Read-only.**

            </ParamField>

            <ParamField body="soldCount" type="integer">

              Number of purchased tickets.

              **Read-only.**

            </ParamField>

            <ParamField body="reservedCount" type="integer">

              Number of reserved tickets.

              **Read-only.**

            </ParamField>

            <ParamField body="soldOut" type="boolean">

              Whether the tickets are sold out.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="limitPerCheckout" type="integer">

          Number of tickets that can be purchased per checkout. &lt;br> &lt;br>
          **Note:** If the `actualLimit` or `salesDetails.unsoldCount` field value is smaller than `limitPerCheckout`, then it overrides this field.

          **Read-only.**

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Data extensions.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="eventDetails" type="EventDetails">

          Information about the even the ticket is for. &lt;br>
          **Note:** This field is only returned when `EVENT_DETAILS` is specified in `field` in the request.

          **Read-only.**

          <Expandable title="eventDetails properties">

            <ParamField body="title" type="string">

              Event title.

              **Read-only.**

            </ParamField>

            <ParamField body="shortDescription" type="string">

              Short description of the event.

              **Read-only.**

            </ParamField>

            <ParamField body="location" type="Location">

              Event location.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Location` object._

            </ParamField>

            <ParamField body="dateAndTimeSettings" type="DateAndTimeSettings">

              Event date and time settings.

              **Read-only.**

              _Nested structure omitted for brevity — see the `DateAndTimeSettings` object._

            </ParamField>

            <ParamField body="eventPageUrl" type="string">

              Event page URL components.

              **Read-only.**

            </ParamField>

            <ParamField body="status" type="string">

              Event status.

              **Read-only.**

              **Supported values:**

              - `UPCOMING` — Event is published and scheduled to start.
              - `STARTED` — Event has started.
              - `ENDED` — Event has ended.
              - `CANCELED` — Event is canceled.
              - `DRAFT` — Event is not public.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="seatingDetails" type="SeatingDetails">

          Seating information including available seats and areas for this ticket definition. &lt;br>
          **Note:** This field is only returned when `SEATING_DETAILS` is specified in `field` in the request, and when the event has a [seating plan](https://support.wix.com/en/article/wix-events-creating-a-seating-map).

          **Read-only.**

          <Expandable title="seatingDetails properties">

            <ParamField body="places" type="AvailablePlace[]">

              List of available places.

              Constraints: `maxItems 50000`.

              _Nested structure omitted for brevity — see the `AvailablePlace[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateTicketDefinitionOptions">

      Optional fields.

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Predefined sets of fields to return.

          Constraints: `maxItems 5`.

          **Supported values:**

          - `SALES_DETAILS` — Returns `salesDetails` in the response.
          - `EVENT_DETAILS` — Returns `eventDetails` in the response.
          - `SEATING_DETAILS` — Returns `seatingDetails` with available seats in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `TicketDefinition`.

    See the [Ticket Definition Object](/api-reference/business-solutions/events/event-management/ticket-definitions-v3/ticket-definition-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `INVALID_SALE_PERIOD` | The `salePeriod.startDate` must be before the `salePeriod.endDate`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ticket Definition Updated | `wix.events.v3.ticket_definition_updated` | Triggered when a ticket definition is updated. |
    | Ticket Definition Sale Period Updated | `wix.events.v3.ticket_definition_sale_period_updated` | Triggered when a sale period is updated. |
    | Ticket Definition Sale Started | `wix.events.v3.ticket_definition_sale_started` | Triggered when a ticket sale starts. Sales details aren't included in the response. |
    | Ticket Definition Sale Ended | `wix.events.v3.ticket_definition_sale_ended` | Triggered when a ticket sale ends. Sales details aren't included in the response. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-ticket-definition" id="ex-sdk-update-ticket-definition-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-ticket-definition" id="ex-sdk-update-ticket-definition-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-ticket-definition-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-ticket-definition-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a ticket definition</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateTicketDefinition (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-ticket-definition-0"><span className="wix-picker-name">Update a ticket definition</span></label>
    <label htmlFor="ex-sdk-update-ticket-definition-1"><span className="wix-picker-name">updateTicketDefinition (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a ticket definition
    import { ticketDefinitionsV2 } from "@wix/events";

    const ticketDefinitionId = "7fa0314b-1300-4b0f-8fbe-1bd86fbcb6de";

    async function updateTicketDefinition() {
      const response = await ticketDefinitionsV2.updateTicketDefinition(
        ticketDefinitionId,
        {
          revision: "1",
          name: "VIP Admission",
        }
      );
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateTicketDefinition (self-hosted)
    import { createClient } from '@wix/sdk';
    import { ticketDefinitionsV2 } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { ticketDefinitionsV2 },
      // Include the auth strategy and host as relevant
    });

    async function updateTicketDefinition(_id,ticketDefinition,options) {
      const response = await myWixClient.ticketDefinitionsV2.updateTicketDefinition(_id,ticketDefinition,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
