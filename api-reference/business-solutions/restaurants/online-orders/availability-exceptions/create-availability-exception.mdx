---
title: "Create Availability Exception"
description: "Creates a new availability exception."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new availability exception.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants-availability-exceptions/v1/availability-exceptions
    ```

    ## Body Params

    <ParamField body="availabilityException" type="AvailabilityException" required>

      Availability exception details.

      See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="availabilityException" type="AvailabilityException">

      The created availability exception.

      See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Availability Exception Created | `wix.restaurants.v1.availability_exception_created` | Triggered when an availability exception is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/restaurants-availability-exceptions/v1/availability-exceptions' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "availabilityException": {
            "available": false,
            "name": "Passover",
            "affectedFulfillmentMethods": {
              "affectedMethods": "ALL_FULFILLMENT_METHODS"
            },
            "operationId": "48605ac5-53fc-49b4-96b2-87f7636f5ce0",
        }'
      ```

      ```json Response
      {
        "availabilityException": {
            "id": "63eb0bf7-96ea-4034-822b-7dd9a24197e9",
            "revision": "1",
            "createdDate": "2024-07-09T10:17:08.865Z",
            "updatedDate": "2024-07-09T10:17:08.865Z",
            "startTime": "2024-07-08T21:00:00.587Z",
            "endTime": "2024-07-09T20:59:59.587Z",
            "available": false,
            "name": "Passover",
            "affectedFulfillmentMethods": {
              "affectedMethods": "ALL_FULFILLMENT_METHODS"
            },
            "operationId": "48605ac5-53fc-49b4-96b2-87f7636f5ce0"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new availability exception.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { availabilityExceptions } from '@wix/restaurants';

    availabilityExceptions.createAvailabilityException(availabilityException): Promise<AvailabilityException>
    ```

    ## Method Parameters

    <ParamField body="availabilityException" type="AvailabilityException" required>

      Availability exception details.

      See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<AvailabilityException>`.

    See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Availability Exception Created | `wix.restaurants.v1.availability_exception_created` | Triggered when an availability exception is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-availability-exception" id="ex-sdk-create-availability-exception-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-availability-exception" id="ex-sdk-create-availability-exception-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-availability-exception" id="ex-sdk-create-availability-exception-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-availability-exception-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-availability-exception-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createAvailabilityException</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createAvailabilityException (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createAvailabilityException (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-availability-exception-0"><span className="wix-picker-name">createAvailabilityException</span></label>
    <label htmlFor="ex-sdk-create-availability-exception-1"><span className="wix-picker-name">createAvailabilityException (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-availability-exception-2"><span className="wix-picker-name">createAvailabilityException (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createAvailabilityException
    import { availabilityExceptions } from '@wix/restaurants';

    async function createAvailabilityException(availabilityException) {
      const response = await availabilityExceptions.createAvailabilityException(availabilityException);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createAvailabilityException (self-hosted)
    import { createClient } from '@wix/sdk';
    import { availabilityExceptions } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { availabilityExceptions },
      // Include the auth strategy and host as relevant
    });

    async function createAvailabilityException(availabilityException) {
      const response = await myWixClient.availabilityExceptions.createAvailabilityException(availabilityException);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createAvailabilityException (with elevated permissions)
    import { availabilityExceptions } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myCreateAvailabilityExceptionMethod(availabilityException) {
      const elevatedCreateAvailabilityException = auth.elevate(availabilityExceptions.createAvailabilityException);
      const response = await elevatedCreateAvailabilityException(availabilityException);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
