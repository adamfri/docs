---
title: "Cancel Reservation"
description: "Cancels ticket reservation and returns tickets to stock."
icon: "trash"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Cancels ticket reservation and returns tickets to stock.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EVENTS.EVENTS-CHECKOUT">Events Checkout</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/events/v1/checkout/reservations/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Reservation ID.

    </ParamField>

    ## Query Params

    <ParamField query="eventId" type="string" required>

      Event ID to which the reservation belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    Returns `CancelReservationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE 'https://www.wixapis.com/events/v1/checkout/reservations/1fe76e20-efa0-4330-be44-2a8b2ca3c208?eventId=fd56076c-38f4-4705-b9eb-ec322b85c745' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Cancels ticket reservation and returns tickets to stock.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EVENTS.EVENTS-CHECKOUT">Events Checkout</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { orders } from '@wix/events';

    orders.cancelReservation(_id, eventId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Reservation ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="eventId" type="string" required>

      Event ID to which the reservation belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-cancel-reservation" id="ex-sdk-cancel-reservation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-cancel-reservation" id="ex-sdk-cancel-reservation-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-cancel-reservation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-cancel-reservation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">cancelReservation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">cancelReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-cancel-reservation-0"><span className="wix-picker-name">cancelReservation</span></label>
    <label htmlFor="ex-sdk-cancel-reservation-1"><span className="wix-picker-name">cancelReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript cancelReservation
    import { orders } from '@wix/events';

    async function cancelReservation(_id,eventId) {
      const response = await orders.cancelReservation(_id,eventId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript cancelReservation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { orders } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { orders },
      // Include the auth strategy and host as relevant
    });

    async function cancelReservation(_id,eventId) {
      const response = await myWixClient.orders.cancelReservation(_id,eventId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
