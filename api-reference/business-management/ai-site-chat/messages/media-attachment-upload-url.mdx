---
title: "Media Attachment Upload Url"
description: "Allows site visitor to create upload url to use for uploading a file."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Allows site visitor to create upload url to use for uploading a file.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INNOVATION_LAB.USER">Innovation Lab User</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/innovation/widget/v1/messages/files/generate-upload-url
    ```

    ## Query Params

    <ParamField query="mimeType" type="string" required>

      File type e.g. "image/png".

      Constraints: `maxLength 100`.

    </ParamField>

    <ParamField query="fileName" type="string">

      File name

    </ParamField>

    <ParamField query="parentFolderId" type="string" />

    <ParamField query="filePath" type="string" />

    ## Response Object

    <ResponseField name="uploadUrl" type="string">

      Constraints: `format WEB_URL`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Creates an upload URL the site visitor can use to upload a file to attach to a message.

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/innovation/widget/v1/messages/files/generate-upload-url?mimeType=image%2Fpng&fileName=receipt.png' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "uploadUrl": "https://upload.wixmp.com/upload/2f6c8d1a-4b7e-4a3c-9f21-6d0e5c8b1a44"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Allows site visitor to create upload url to use for uploading a file.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INNOVATION_LAB.USER">Innovation Lab User</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { messages } from '@wix/ai-site-chat';

    messages.mediaAttachmentUploadUrl(mimeType, options): Promise
    ```

    ## Method Parameters

    <ParamField body="mimeType" type="string" required>

      File type e.g. "image/png".

      Constraints: `maxLength 100`.

    </ParamField>

    <ParamField body="options" type="MediaAttachmentUploadUrlOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fileName" type="string">

          File name

        </ParamField>

        <ParamField body="parentFolderId" type="string" />

        <ParamField body="filePath" type="string" />

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="uploadUrl" type="string">

      Constraints: `format WEB_URL`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-media-attachment-upload-url" id="ex-sdk-media-attachment-upload-url-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-media-attachment-upload-url" id="ex-sdk-media-attachment-upload-url-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-media-attachment-upload-url-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-media-attachment-upload-url-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Generate an upload URL for a media attachment</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">mediaAttachmentUploadUrl (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-media-attachment-upload-url-0"><span className="wix-picker-name">Generate an upload URL for a media attachment</span></label>
    <label htmlFor="ex-sdk-media-attachment-upload-url-1"><span className="wix-picker-name">mediaAttachmentUploadUrl (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Generate an upload URL for a media attachment
    import { messages } from "@wix/ai-site-chat";

    const mimeType = "image/png";

    const options = {
      fileName: "receipt.png",
    };

    async function mediaAttachmentUploadUrl() {
      const response = await messages.mediaAttachmentUploadUrl(mimeType, options);

      return response.uploadUrl;
    }

    /* Promise resolves to:
     * {
     *   "uploadUrl": "https://upload.wixmp.com/upload/2f6c8d1a-4b7e-4a3c-9f21-6d0e5c8b1a44"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript mediaAttachmentUploadUrl (self-hosted)
    import { createClient } from '@wix/sdk';
    import { messages } from '@wix/ai-site-chat';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { messages },
      // Include the auth strategy and host as relevant
    });

    async function mediaAttachmentUploadUrl(mimeType,options) {
      const response = await myWixClient.messages.mediaAttachmentUploadUrl(mimeType,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
