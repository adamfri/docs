---
title: "onReferredFriendCreated"
description: "Triggered when a referred friend is created."
icon: "bolt"
---

Triggered when a referred friend is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.referral.v1.referred_friend_created` |
| Package | `@wix/referral` |

## Examples

<CodeGroup>

  ```javascript onReferredFriendCreated
  import { friends } from '@wix/referral';

  friends.onReferredFriendCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReferredFriendCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { friends } from '@wix/referral';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        friends,
      },
  });

  wixClient.friends.onReferredFriendCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
