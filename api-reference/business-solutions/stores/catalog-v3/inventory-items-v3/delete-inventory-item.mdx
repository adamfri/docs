---
title: "Delete Inventory Item"
description: "Deletes an inventory item."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an inventory item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INVENTORY_ITEM_WRITE">Inventory write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/stores/v3/inventory-items/{inventoryItemId}
    ```

    ## Path Params

    <ParamField path="inventoryItemId" type="string" required>

      Inventory item ID.

    </ParamField>

    ## Response Object

    Returns `DeleteInventoryItemResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Inventory Item Deleted | `wix.stores.catalog.v3.inventory_item_deleted` | Triggered when an inventory item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Delete inventory item associated with product, variant, and location.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/stores/v3/inventory-items/5c8c00a8-799b-4c67-8e43-71e2a4fee5d2' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an inventory item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INVENTORY_ITEM_WRITE">Inventory write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { inventoryItemsV3 } from '@wix/stores';

    inventoryItemsV3.deleteInventoryItem(inventoryItemId): Promise
    ```

    ## Method Parameters

    <ParamField body="inventoryItemId" type="string" required>

      Inventory item ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteInventoryItemResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Inventory Item Deleted | `wix.stores.catalog.v3.inventory_item_deleted` | Triggered when an inventory item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-inventory-item" id="ex-sdk-delete-inventory-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-inventory-item" id="ex-sdk-delete-inventory-item-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-inventory-item" id="ex-sdk-delete-inventory-item-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-inventory-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-inventory-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteInventoryItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteInventoryItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteInventoryItem (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-inventory-item-0"><span className="wix-picker-name">deleteInventoryItem</span></label>
    <label htmlFor="ex-sdk-delete-inventory-item-1"><span className="wix-picker-name">deleteInventoryItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-inventory-item-2"><span className="wix-picker-name">deleteInventoryItem (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteInventoryItem
    import { inventoryItemsV3 } from '@wix/stores';

    async function deleteInventoryItem(inventoryItemId) {
      const response = await inventoryItemsV3.deleteInventoryItem(inventoryItemId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteInventoryItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { inventoryItemsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { inventoryItemsV3 },
      // Include the auth strategy and host as relevant
    });

    async function deleteInventoryItem(inventoryItemId) {
      const response = await myWixClient.inventoryItemsV3.deleteInventoryItem(inventoryItemId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteInventoryItem (with elevated permissions)
    import { inventoryItemsV3 } from '@wix/stores';
    import { auth } from '@wix/essentials';

    async function myDeleteInventoryItemMethod(inventoryItemId) {
      const elevatedDeleteInventoryItem = auth.elevate(inventoryItemsV3.deleteInventoryItem);
      const response = await elevatedDeleteInventoryItem(inventoryItemId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
