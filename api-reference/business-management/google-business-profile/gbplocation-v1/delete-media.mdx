---
title: "Delete Media"
description: "Deletes a media item from a location."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a media item from a location.

    Specify the media key from List Media. Deleting an item that's already gone
    succeeds, so it's safe to retry.

    Deletions draw on the same budget of about 10 edits per minute per Business
    Profile as every other write to it, including Create Media, Update Google
    Location and Update Location Attributes. Removing a set of images in quick
    succession returns 429, so pace the profile as a whole.

    To remove several photos at once, call Bulk Create Media Uploads with removal
    items instead. That queues them and applies them gradually.

    Errors:
    - `NOT_FOUND`: the location isn't recorded for this site. Google isn't called.
    - `GOOGLE_API_CALL_FAILED`: the call to Google failed. Google's status and
    response are in the error payload.

    A malformed media key, or one belonging to a different location, also comes
    back as `GOOGLE_API_CALL_FAILED` rather than `NOT_FOUND`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/gbp/v1/locations/{locationId}/media/{mediaKey}
    ```

    ## Path Params

    <ParamField path="locationId" type="string" required>

      Location ID. This is Google's location identifier.

    </ParamField>

    <ParamField path="mediaKey" type="string" required>

      Google's opaque media key.

    </ParamField>

    ## Response Object

    Returns `DeleteMediaResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `GOOGLE_API_CALL_FAILED` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/gbp/v1/locations/8034569321457853640/media/AF1QipP5tWn2XcV7bRjL9mKqEuDgA3sYhTf6oZmNiC1B' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a media item from a location.

    Specify the media key from List Media. Deleting an item that's already gone
    succeeds, so it's safe to retry.

    Deletions draw on the same budget of about 10 edits per minute per Business
    Profile as every other write to it, including Create Media, Update Google
    Location and Update Location Attributes. Removing a set of images in quick
    succession returns 429, so pace the profile as a whole.

    To remove several photos at once, call Bulk Create Media Uploads with removal
    items instead. That queues them and applies them gradually.

    Errors:
    - `NOT_FOUND`: the location isn't recorded for this site. Google isn't called.
    - `GOOGLE_API_CALL_FAILED`: the call to Google failed. Google's status and
    response are in the error payload.

    A malformed media key, or one belonging to a different location, also comes
    back as `GOOGLE_API_CALL_FAILED` rather than `NOT_FOUND`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/gbp';

    locations.deleteMedia(identifiers): Promise
    ```

    ## Method Parameters

    <ParamField body="identifiers" type="DeleteMediaIdentifiers" required>

      identifiers

      <Expandable title="identifiers properties">

        <ParamField body="locationId" type="string" required>

          Location ID. This is Google's location identifier.

          Constraints: `minLength 1`, `maxLength 255`.

        </ParamField>

        <ParamField body="mediaKey" type="string" required>

          Google's opaque media key.

          Constraints: `maxLength 255`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `DeleteMediaResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `GOOGLE_API_CALL_FAILED` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-media" id="ex-sdk-delete-media-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-media" id="ex-sdk-delete-media-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-media-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-media-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a media item from a listing</span><span className="wix-picker-desc">Remove one photo or video from the Google Business Profile listing</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMedia (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-media-0"><span className="wix-picker-name">Delete a media item from a listing</span><span className="wix-picker-desc">Remove one photo or video from the Google Business Profile listing</span></label>
    <label htmlFor="ex-sdk-delete-media-1"><span className="wix-picker-name">deleteMedia (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a media item from a listing
    import { locations } from "@wix/gbp";

    async function deleteMedia() {
      await locations.deleteMedia({
        locationId: "8034569321457853640",
        mediaKey: "AF1QipP5tWn2XcV7bRjL9mKqEuDgA3sYhTf6oZmNiC1B",
      });

      console.log("Media deleted");
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMedia (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/gbp';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function deleteMedia(identifiers) {
      const response = await myWixClient.locations.deleteMedia(identifiers);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
