---
title: "Notify"
description: "Sends notifications based on the template and dynamic values provided."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sends notifications based on the template and dynamic values provided.

    <Note>

    An app can call this method up to 100,000 times per month for each site.

    </Note>

    When you create a [notification template](https://dev.wix.com/docs/rest/business-management/notifications/notifications/creating-a-notification-template), you are provided with a notification template ID. Call the Notify method with this ID as `notificationTemplateID` to trigger notifications based on that template.

    If the notification template contains placeholders for dynamic values, provide those values as key-value pairs in the `dynamicValues` array. The values you specify are incorporated in the notifications sent out.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-NOTIFICATIONS.MANAGE-NOTIFICATIONS">Manage Notifications</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/notifications/v3/notify
    ```

    ## Body Params

    <ParamField body="notificationTemplateId" type="string" required>

      Notification template ID. A notification template specifies the text and recipients for notifications.
      To obtain a notification template ID, create a notification template in your [app's dashboard](https://dev.wix.com/docs/build-apps/develop-your-app/app-dashboard-setup/about-the-app-dashboard).

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="dynamicValues" type="object">

      Each key is a placeholder name you specify when creating a notification template.
      The value is an object containing the text to replace the placeholder in the notifications.

      Constraints: `format map`.

      <Expandable title="dynamicValues properties">

        <ParamField body="key" type="string" />

        <ParamField body="value" type="DynamicValue">

          <Expandable title="One of the following">

            <ParamField body="ofType" type="ofType">

              ofType

              _Nested structure omitted for brevity — see the `ofType` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="notificationBatchId" type="string">

      ID of the batch of notifications triggered by one request.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      https://www.wixapis.com/notifications/v3/notify \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "notificationTemplateId": "2664799d-49ad-417c-aab0-aba0c39a3951",
        "dynamicValues": {
          "recipientName": {
            "text": "Danny"
          },
          "companyName": {
            "text": "WIX"
          },
          "age": {
            "text": "34"
          }
        }
      }'
      ```

      ```json Response
      {
        "notificationBatchId": "1ed7fdd1-e99a-4ddd-8295-037459cdddca"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sends notifications based on the template and dynamic values provided.

    <Note>

    An app can call this method up to 100,000 times per month for each site.

    </Note>

    When you create a [notification template](https://dev.wix.com/docs/rest/business-management/notifications/notifications/creating-a-notification-template), you are provided with a notification template ID. Call the Notify method with this ID as `notificationTemplateID` to trigger notifications based on that template.

    If the notification template contains placeholders for dynamic values, provide those values as key-value pairs in the `dynamicValues` array. The values you specify are incorporated in the notifications sent out.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-NOTIFICATIONS.MANAGE-NOTIFICATIONS">Manage Notifications</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { notificationsV3 } from '@wix/notifications';

    notificationsV3.notify(notificationTemplateId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="notificationTemplateId" type="string" required>

      Notification template ID. A notification template specifies the text and recipients for notifications.
      To obtain a notification template ID, create a notification template in your [app's dashboard](https://dev.wix.com/docs/build-apps/develop-your-app/app-dashboard-setup/about-the-app-dashboard).

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="NotifyOptions">

      options

      <Expandable title="options properties">

        <ParamField body="dynamicValues" type="object">

          Each key is a placeholder name you specify when creating a notification template.
          The value is an object containing the text to replace the placeholder in the notifications.

          Constraints: `format map`.

          <Expandable title="dynamicValues properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="DynamicValue">

              _Nested structure omitted for brevity — see the `DynamicValue` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="notificationBatchId" type="string">

      ID of the batch of notifications triggered by one request.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-notify" id="ex-sdk-notify-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-notify" id="ex-sdk-notify-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-notify" id="ex-sdk-notify-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-notify-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-notify-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Send notifications with dynamic values</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Send notifications without dynamic values</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">notify (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-notify-0"><span className="wix-picker-name">Send notifications with dynamic values</span></label>
    <label htmlFor="ex-sdk-notify-1"><span className="wix-picker-name">Send notifications without dynamic values</span></label>
    <label htmlFor="ex-sdk-notify-2"><span className="wix-picker-name">notify (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Send notifications with dynamic values
    import { notificationsV3 } from "@wix/notifications";

    async function notify() {
      const response = await notificationsV3.notify({
        notificationTemplateId: "2664799d-49ad-417c-aab0-aba0c39a3951",
        dynamicValues: {
          recipientName: { text: "Danny" },
          companyName: { text: "WIX" },
          age: { text: "34" },
        },
      });
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Send notifications without dynamic values
    import { notificationsV3 } from "@wix/notifications";

    async function notify() {
      const response = await notificationsV3.notify({
        notificationTemplateId: "2664799d-49ad-417c-aab0-aba0c39a3951",
      });
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript notify (self-hosted)
    import { createClient } from '@wix/sdk';
    import { notificationsV3 } from '@wix/notifications';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { notificationsV3 },
      // Include the auth strategy and host as relevant
    });

    async function notify(notificationTemplateId,options) {
      const response = await myWixClient.notificationsV3.notify(notificationTemplateId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
