---
title: "Check Benefit Eligibility By Filter"
description: "Checks benefit eligibility across the pools that match the filter."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks benefit eligibility across the pools that match the filter.

    The filter selects pools (not individual benefits), using the same fields supported by Query Pools. For field support, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

    To be redeemed, a benefit's pool must be active and the associated balance must be greater than the cost of the benefit.

    Each benefit is checked independently. For example, if there is a benefit with a price of 3 credits and a benefit with a price of 4 credits, and there are 5 available credits, both benefits would be returned as eligible, but they can't both be redeemed.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/pools/check-benefit-eligibility-by-filter
    ```

    ## Body Params

    <ParamField body="poolFilter" type="PoolFilter" deprecated>

      Defines filtering and sorting criteria used to select pools when checking eligibility.

      The filter selects pools using the same fields supported by Query Pools. For field support, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

      When omitted, eligibility is checked across all pools in the namespace that match the provided `beneficiary`. Combined with `beneficiary` and `namespace`, the filter further narrows which pools are checked.

      Deprecated: use `pool_selector` instead. Can't be combined with `pool_selector`.

      **Deprecated.** Defines filtering and sorting criteria used to select pools when checking eligibility.

      The filter selects pools using the same fields supported by Query Pools. For field support, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

      When omitted, eligibility is checked across all pools in the namespace that match the provided `beneficiary`. Combined with `beneficiary` and `namespace`, the filter further narrows which pools are checked.

      Deprecated: use `pool_selector` instead. Can't be combined with `pool_selector`. Removal: 2026-08-01.

      <Expandable title="poolFilter properties">

        <ParamField body="filter" type="object">

          Filter object. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more information.

          For supported filterable and sortable fields, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sorting to apply to the matched pools. Determines the order of the returned results, and which results are kept when more than 100 are produced.

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order. Use `ASC` for ascending order or `DESC` for descending order.

              Default: `ASC`

              **Supported values:**

              - `ASC` — Ascending sort order.
              - `DESC` — Descending sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="namespace" type="string" required>

      Namespace for your app or site's benefit programs. Namespaces allow you to distinguish between entities that you created and entities that other apps created.

      Constraints: `minLength 1`, `maxLength 20`.

    </ParamField>

    <ParamField body="itemReference" type="ItemReference" required>

      Information about the item to check.

      <Expandable title="itemReference properties">

        <ParamField body="externalId" type="string" required>

          External ID of the item.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="category" type="string">

          Item category.

          Constraints: `maxLength 20`.

        </ParamField>

        <ParamField body="providerAppId" type="string" required>

          ID of the app providing the item.

          Constraints: `format GUID`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="count" type="integer" required>

      Number of items to check.

      Ensures the balance is high enough to redeem this many items.

      Constraints: `min 1`.

    </ParamField>

    <ParamField body="targetDate" type="datetime">

      Date that the item will be provided to the beneficiary.

      This parameter should be used to manage the potential logistics of providing the item.

    </ParamField>

    <ParamField body="beneficiary" type="IdentificationData">

      Pool beneficiary. When provided, only the beneficiary's pools that also match `pool_filter` are checked.

      <Expandable title="One of the following">

        <ParamField body="id" type="id">

          id

          <Expandable title="id properties">

            <ParamField body="anonymousVisitorId" type="string">

              ID of a site visitor that hasn't logged in to the site.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="memberId" type="string">

              ID of a site member.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="wixUserId" type="string">

              ID of a Wix user.

              Constraints: `format GUID`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="additionalData" type="object">

      Additional information.

    </ParamField>

    <ParamField body="poolSelector" type="PoolSelector">

      Predefined attributes used to select the pools to check eligibility in.

      When omitted, eligibility is checked across all pools in the namespace that match the provided `beneficiary`. Combined with `beneficiary` and `namespace`, the selector further narrows which pools are checked.

      Can't be combined with `pool_filter`.

      <Expandable title="poolSelector properties">

        <ParamField body="poolStatuses" type="string[]">

          Only pools in these statuses are checked. When empty, pools in all statuses are checked.

          Constraints: `maxItems 10`.

          **Supported values:**

          - `ACTIVE` — Pool is active.
          - `PAUSED` — Pool is paused. It can be resumed.
          - `ENDED` — Pool is ended. It can't be resumed.
          - `PROVISIONING` — Pool provisioning is in progress. This status exists for a short time during processing.
          - `RENEWING` — Pool renewing is in progress. This status exists for a short time during processing.
          - `PENDING` — Pool is pending

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="EligibilityCheckResult[]">

      Results of the eligibility check for the benefits in the pools that matched the filter. A pool that contains multiple benefits for the requested item produces one result per benefit.

      Empty when no pools match the filter. Capped at 100 results. When more than 100 results are produced, only the first 100, in the order specified by `poolFilter.sort`, are returned.

      Constraints: `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="benefitResults" type="BenefitResult[]">

          Eligibility status.

          Constraints: `maxItems 10`.

          <Expandable title="benefitResults properties">

            <ResponseField name="type" type="string">

              Eligibility status.

              **Supported values:**

              - `ELIGIBLE_BENEFIT` — Item is eligible to be redeemed.
              - `NOT_ENOUGH_BALANCE` — Balance is lower than the cost of redeeming the items.
              - `POOL_NOT_ACTIVE` — Pool isn't active.
              - `BENEFIT_NOT_FOUND` — Invalid benefit details provided.
              - `POOL_NOT_FOUND` — Invalid pool details provided.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_ITEM_REFERENCE` | The `itemReference` contains invalid values. |
    | `400` | `INVALID_ARGUMENT` | `POOL_FILTER_AND_POOL_SELECTOR_PROVIDED` | Both `poolFilter` and `poolSelector` are provided. Provide at most one of them. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request POST https://www.wixapis.com/benefit-programs/v1/pools/check-benefit-eligibility-by-filter \
        -H "Authorization: <AUTH>" \
        -H "Content-Type: application/json" \
        --data '{
          "pool_filter": {
            "filter": {
              "$and": [
                {
                  "programId": {
                    "$eq": "8faed6d2-dfdf-44f1-8a71-bcc19d2fbdde"
                  }
                },
                {
                  "status": {
                    "$eq": "ACTIVE"
                  }
                }
              ]
            },
            "sort": [
              {
                "fieldName": "createdDate",
                "order": "DESC"
              }
            ]
          },
          "item_reference": {
            "external_id": "57273802-b873-495c-8eba-b8f054452e6c",
            "provider_app_id": "00000000-0000-0000-0000-000000000000"
          },
          "count": 3,
          "beneficiary": {
            "identity_type": "MEMBER",
            "member_id": "00000000-0000-0000-0000-000000000000"
          },
          "namespace": "benefit_programs_app"
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "results": [
          {
            "benefit_results": [
              {
                "type": "ELIGIBLE_BENEFIT",
                "eligible_benefit_options": {
                  "pool_id": "4a4de95d-867a-4089-98a8-83af77e38eca",
                  "benefit_key": "001",
                  "item_reference": {
                    "external_id": "57273802-b873-495c-8eba-b8f054452e6c",
                    "category": "",
                    "provider_app_id": "00000000-0000-0000-0000-000000000000"
                  },
                  "price": "5"
                }
              }
            ]
          },
          {
            "benefit_results": [
              {
                "type": "NOT_ENOUGH_BALANCE",
                "not_enough_balance_options": {
                  "pool_id": "e189a369-bb35-42c2-9a49-27c9a0a205d9",
                  "item_reference": {
                    "external_id": "57273802-b873-495c-8eba-b8f054452e6c",
                    "category": "",
                    "provider_app_id": "00000000-0000-0000-0000-000000000000"
                  },
                  "available_balance": "10",
                  "requested_balance": "15"
                }
              }
            ]
          }
        ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks benefit eligibility across the pools that match the filter.

    The filter selects pools (not individual benefits), using the same fields supported by Query Pools. For field support, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

    To be redeemed, a benefit's pool must be active and the associated balance must be greater than the cost of the benefit.

    Each benefit is checked independently. For example, if there is a benefit with a price of 3 credits and a benefit with a price of 4 credits, and there are 5 available credits, both benefits would be returned as eligible, but they can't both be redeemed.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { pools } from '@wix/benefit-programs';

    pools.checkBenefitEligibilityByFilter(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="CheckBenefitEligibilityByFilterOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="poolFilter" type="PoolFilter" deprecated>

          Defines filtering and sorting criteria used to select pools when checking eligibility.

          The filter selects pools using the same fields supported by Query Pools. For field support, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

          When omitted, eligibility is checked across all pools in the namespace that match the provided `beneficiary`. Combined with `beneficiary` and `namespace`, the filter further narrows which pools are checked.

          Deprecated: use `pool_selector` instead. Can't be combined with `pool_selector`.

          **Deprecated.** Defines filtering and sorting criteria used to select pools when checking eligibility.

          The filter selects pools using the same fields supported by Query Pools. For field support, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

          When omitted, eligibility is checked across all pools in the namespace that match the provided `beneficiary`. Combined with `beneficiary` and `namespace`, the filter further narrows which pools are checked.

          Deprecated: use `pool_selector` instead. Can't be combined with `pool_selector`. Removal: 2026-08-01.

          <Expandable title="poolFilter properties">

            <ParamField body="filter" type="object">

              Filter object. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more information.

              For supported filterable and sortable fields, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pools/filtering-and-sorting).

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sorting to apply to the matched pools. Determines the order of the returned results, and which results are kept when more than 100 are produced.

              Constraints: `maxItems 5`.

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="namespace" type="string" required>

          Namespace for your app or site's benefit programs. Namespaces allow you to distinguish between entities that you created and entities that other apps created.

          Constraints: `minLength 1`, `maxLength 20`.

        </ParamField>

        <ParamField body="itemReference" type="ItemReference" required>

          Information about the item to check.

          <Expandable title="itemReference properties">

            <ParamField body="externalId" type="string" required>

              External ID of the item.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="category" type="string">

              Item category.

              Constraints: `maxLength 20`.

            </ParamField>

            <ParamField body="providerAppId" type="string" required>

              ID of the app providing the item.

              Constraints: `format GUID`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="count" type="integer" required>

          Number of items to check.

          Ensures the balance is high enough to redeem this many items.

          Constraints: `min 1`.

        </ParamField>

        <ParamField body="targetDate" type="datetime">

          Date that the item will be provided to the beneficiary.

          This parameter should be used to manage the potential logistics of providing the item.

        </ParamField>

        <ParamField body="beneficiary" type="IdentificationData">

          Pool beneficiary. When provided, only the beneficiary's pools that also match `pool_filter` are checked.

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="additionalData" type="object">

          Additional information.

        </ParamField>

        <ParamField body="poolSelector" type="PoolSelector">

          Predefined attributes used to select the pools to check eligibility in.

          When omitted, eligibility is checked across all pools in the namespace that match the provided `beneficiary`. Combined with `beneficiary` and `namespace`, the selector further narrows which pools are checked.

          Can't be combined with `pool_filter`.

          <Expandable title="poolSelector properties">

            <ParamField body="poolStatuses" type="string[]">

              Only pools in these statuses are checked. When empty, pools in all statuses are checked.

              Constraints: `maxItems 10`.

              **Supported values:**

              - `ACTIVE` — Pool is active.
              - `PAUSED` — Pool is paused. It can be resumed.
              - `ENDED` — Pool is ended. It can't be resumed.
              - `PROVISIONING` — Pool provisioning is in progress. This status exists for a short time during processing.
              - `RENEWING` — Pool renewing is in progress. This status exists for a short time during processing.
              - `PENDING` — Pool is pending

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="EligibilityCheckResult[]">

      Results of the eligibility check for the benefits in the pools that matched the filter. A pool that contains multiple benefits for the requested item produces one result per benefit.

      Empty when no pools match the filter. Capped at 100 results. When more than 100 results are produced, only the first 100, in the order specified by `poolFilter.sort`, are returned.

      Constraints: `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="benefitResults" type="BenefitResult[]">

          Eligibility status.

          Constraints: `maxItems 10`.

          <Expandable title="benefitResults properties">

            <ResponseField name="type" type="string">

              Eligibility status.

              **Supported values:**

              - `ELIGIBLE_BENEFIT` — Item is eligible to be redeemed.
              - `NOT_ENOUGH_BALANCE` — Balance is lower than the cost of redeeming the items.
              - `POOL_NOT_ACTIVE` — Pool isn't active.
              - `BENEFIT_NOT_FOUND` — Invalid benefit details provided.
              - `POOL_NOT_FOUND` — Invalid pool details provided.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_ITEM_REFERENCE` | The `itemReference` contains invalid values. |
    | `400` | `INVALID_ARGUMENT` | `POOL_FILTER_AND_POOL_SELECTOR_PROVIDED` | Both `poolFilter` and `poolSelector` are provided. Provide at most one of them. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-check-benefit-eligibility-by-filter" id="ex-sdk-check-benefit-eligibility-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-check-benefit-eligibility-by-filter" id="ex-sdk-check-benefit-eligibility-by-filter-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-check-benefit-eligibility-by-filter" id="ex-sdk-check-benefit-eligibility-by-filter-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-check-benefit-eligibility-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-check-benefit-eligibility-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">checkBenefitEligibilityByFilter</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkBenefitEligibilityByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkBenefitEligibilityByFilter (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-check-benefit-eligibility-by-filter-0"><span className="wix-picker-name">checkBenefitEligibilityByFilter</span></label>
    <label htmlFor="ex-sdk-check-benefit-eligibility-by-filter-1"><span className="wix-picker-name">checkBenefitEligibilityByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-check-benefit-eligibility-by-filter-2"><span className="wix-picker-name">checkBenefitEligibilityByFilter (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript checkBenefitEligibilityByFilter
    import { pools } from '@wix/benefit-programs';

    async function checkBenefitEligibilityByFilter(options) {
      const response = await pools.checkBenefitEligibilityByFilter(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkBenefitEligibilityByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { pools } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { pools },
      // Include the auth strategy and host as relevant
    });

    async function checkBenefitEligibilityByFilter(options) {
      const response = await myWixClient.pools.checkBenefitEligibilityByFilter(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkBenefitEligibilityByFilter (with elevated permissions)
    import { pools } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myCheckBenefitEligibilityByFilterMethod(options) {
      const elevatedCheckBenefitEligibilityByFilter = auth.elevate(pools.checkBenefitEligibilityByFilter);
      const response = await elevatedCheckBenefitEligibilityByFilter(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
