---
title: "onItemLabelDeleted"
description: "Triggered when an item label is deleted."
icon: "bolt"
---

Triggered when an item label is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_label_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemLabelDeleted
  import { itemLabels } from '@wix/restaurants';

  itemLabels.onItemLabelDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemLabelDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemLabels } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemLabels,
      },
  });

  wixClient.itemLabels.onItemLabelDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
