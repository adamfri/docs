---
title: "Campaign Object"
description: "A Google Ads campaign managed through the Wix Promote interface."
icon: "cube"
---

A Google Ads campaign managed through the Wix Promote interface.
A campaign can be either a Smart Campaign or a Performance Max campaign, determined by the campaignType field.
Creating new Smart Campaigns is deprecated. Support for it will be removed in a future release, so use
PERFORMANCE_MAX or PERFORMANCE_MAX_LEADS instead. Existing Smart Campaigns aren't affected.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Unique campaign ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="accountId" type="string">

      Wix internal account ID associated with this campaign.

    </ResponseField>

    <ResponseField name="campaignType" type="string">

      Type of the campaign, either Smart or Performance Max.

      **Supported values:**

      - `SMART` — Simplified auto-managed campaign. Google handles bidding and ad delivery based on keyword themes and geo targets.
      Creating new campaigns of this type is deprecated. Support for it will be removed in a future release, so use
      PERFORMANCE_MAX or PERFORMANCE_MAX_LEADS instead. Existing campaigns of this type aren't affected.
      - `PERFORMANCE_MAX` — Goal-based campaign that runs across all Google channels (Search, Display, YouTube, etc.) using asset groups.
      - `PERFORMANCE_MAX_LEADS` — Performance Max variant optimized for lead generation, such as phone calls and form fills.

    </ResponseField>

    <ResponseField name="status" type="string">

      Current campaign status, synced from Google Ads on each read.

      **Read-only.**

      **Supported values:**

      - `LIVE` — Campaign is currently serving ads, depending on budget information.
      - `PAUSED` — Campaign has been paused by the user.
      - `ENDED` — Campaign has been ended.
      - `IN_REVIEW` — The campaign was created but its budget hasn't been charged yet.
      - `ERROR` — Error state.
      - `DISAPPROVED` — Campaign is not approved.
      - `NOT_SERVING` — Campaign is currently not serving due to budget limitations.
      - `DRAFT` — Campaign is a draft.
      - `LEARNING` — Campaign is in the learning phase. Currently supported only for Google Ads Performance Max leads campaigns.

    </ResponseField>

    <ResponseField name="resourceName" type="string">

      Google Ads resource name for this campaign, e.g. customers/&#123;customerId&#125;/campaigns/&#123;campaignId&#125;.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Display name of the campaign.

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the campaign was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time the campaign was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="actionDate" type="datetime">

      Date and time of the most recent lifecycle action (pause, resume, launch, etc.).

      **Read-only.**

    </ResponseField>

    <ResponseField name="budget" type="CampaignBudget">

      Daily budget for the campaign.

      <Expandable title="budget properties">

        <ResponseField name="resourceName" type="string">

          Google Ads resource name for the campaign budget.

          **Read-only.**

        </ResponseField>

        <ResponseField name="amountMicros" type="string">

          Daily budget amount in micros using the account's local currency.
          1,000,000 micros equals 1 currency unit (e.g. 6,400,000 = $6.40).
          Google caps monthly spend at 30.4 times this amount.
          Minimum is approximately $6.40 USD for standard subscribers; maximum is approximately $127.60 USD standard, or up to $1,500 for high-spend accounts.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="locations" type="LocationCriterion[]">

      Geographic locations targeted by this campaign.

      Constraints: `maxItems 1000`.

      <Expandable title="locations properties">

        <ResponseField name="resourceName" type="string">

          Google Ads campaign criterion resource name.
          Format: customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

          **Read-only.**

        </ResponseField>

        <ResponseField name="location" type="Location">

          The location being targeted.

          <Expandable title="One of the following">

            <ResponseField name="location" type="location">

              location

              <Expandable title="location properties">

                <ResponseField name="geoTargetConstant" type="string">

                  Google geo target constant resource name (e.g. geoTargetConstants/1023191).

                </ResponseField>

                <ResponseField name="proximity" type="ProximityAddress">

                  Proximity criterion defining a circular area by geo point and radius.

                  _Nested structure omitted for brevity — see the `ProximityAddress` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="displayName" type="string">

          Human-readable location name shown in the UI (e.g. "New York, New York").

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="adSchedule" type="WeeklyAdSchedule">

      Weekly ad schedule defining the days and hours when ads can run.

      <Expandable title="adSchedule properties">

        <ResponseField name="adShowAnytime" type="boolean">

          Whether ads show at any time during the day. When `true`, the daily schedules below are ignored.

        </ResponseField>

        <ResponseField name="sunday" type="DailyAdSchedule">

          Sunday ad show range.

          <Expandable title="sunday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="monday" type="DailyAdSchedule">

          Monday ad show range.

          <Expandable title="monday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tuesday" type="DailyAdSchedule">

          Tuesday ad show range.

          <Expandable title="tuesday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="wednesday" type="DailyAdSchedule">

          Wednesday ad show range.

          <Expandable title="wednesday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="thursday" type="DailyAdSchedule">

          Thursday ad show range.

          <Expandable title="thursday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="friday" type="DailyAdSchedule">

          Friday ad show range.

          <Expandable title="friday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="saturday" type="DailyAdSchedule">

          Saturday ad show range.

          <Expandable title="saturday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="reportingKey" type="string">

      Value used as the utm_campaign parameter for conversion tracking.

      **Read-only.**

    </ResponseField>

    <ResponseField name="campaignBrief" type="CampaignBrief">

      The brief used to generate this campaign.
      Only present on campaigns created via the GetCampaignSuggestions endpoint of SuggestionsService.

      <Expandable title="campaignBrief properties">

        <ResponseField name="id" type="string">

          Campaign brief ID (Wix internal)

          **Read-only.**

        </ResponseField>

        <ResponseField name="platformType" type="string">

          The type of platform the campaign will be created on (Google/FB)

          **Read-only.**

          **Supported values:**

          - `GOOGLE`
          - `FACEBOOK`

        </ResponseField>

        <ResponseField name="goal" type="string">

          Goal of the campaign

        </ResponseField>

        <ResponseField name="audience" type="string">

          Audience the campaign should target

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "campaign": {
          "id": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
          "accountId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "campaignType": "SMART",
          "status": "LIVE",
          "name": "Spring Bakery Promotion",
          "budget": {
            "amountMicros": "15000000",
            "resourceName": "customers/1234567890/campaignBudgets/9876543210"
          },
          "locations": [
            {
              "location": { "geoTargetConstant": "geoTargetConstants/1023191" },
              "displayName": "New York, New York",
              "resourceName": "customers/1234567890/campaignCriteria/9876543210~111"
            }
          ],
          "smartCampaign": {
            "keywordThemes": [
              { "freeFormKeywordTheme": "bakery", "displayName": "bakery" },
              { "freeFormKeywordTheme": "fresh bread", "displayName": "fresh bread" }
            ],
            "businessName": "Sunrise Bakery",
            "url": "https://example.com",
            "languageCode": "en",
            "adGroups": []
          },
          "reportingKey": "spring-bakery-promotion-2024-3-15-c9d8e7f6",
          "resourceName": "customers/1234567890/campaigns/9876543210",
          "createdDate": "2024-03-15T10:30:00.000Z",
          "updatedDate": "2024-03-20T08:00:00.000Z",
          "actionDate": "2024-03-20T08:00:00.000Z"
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Unique campaign ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="accountId" type="string">

      Wix internal account ID associated with this campaign.

    </ResponseField>

    <ResponseField name="campaignType" type="string">

      Type of the campaign, either Smart or Performance Max.

      **Supported values:**

      - `SMART` — Simplified auto-managed campaign. Google handles bidding and ad delivery based on keyword themes and geo targets.
      Creating new campaigns of this type is deprecated. Support for it will be removed in a future release, so use
      PERFORMANCE_MAX or PERFORMANCE_MAX_LEADS instead. Existing campaigns of this type aren't affected.
      - `PERFORMANCE_MAX` — Goal-based campaign that runs across all Google channels (Search, Display, YouTube, etc.) using asset groups.
      - `PERFORMANCE_MAX_LEADS` — Performance Max variant optimized for lead generation, such as phone calls and form fills.

    </ResponseField>

    <ResponseField name="status" type="string">

      Current campaign status, synced from Google Ads on each read.

      **Read-only.**

      **Supported values:**

      - `LIVE` — Campaign is currently serving ads, depending on budget information.
      - `PAUSED` — Campaign has been paused by the user.
      - `ENDED` — Campaign has been ended.
      - `IN_REVIEW` — The campaign was created but its budget hasn't been charged yet.
      - `ERROR` — Error state.
      - `DISAPPROVED` — Campaign is not approved.
      - `NOT_SERVING` — Campaign is currently not serving due to budget limitations.
      - `DRAFT` — Campaign is a draft.
      - `LEARNING` — Campaign is in the learning phase. Currently supported only for Google Ads Performance Max leads campaigns.

    </ResponseField>

    <ResponseField name="resourceName" type="string">

      Google Ads resource name for this campaign, e.g. customers/&#123;customerId&#125;/campaigns/&#123;campaignId&#125;.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Display name of the campaign.

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the campaign was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time the campaign was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="actionDate" type="datetime">

      Date and time of the most recent lifecycle action (pause, resume, launch, etc.).

      **Read-only.**

    </ResponseField>

    <ResponseField name="budget" type="CampaignBudget">

      Daily budget for the campaign.

      <Expandable title="budget properties">

        <ResponseField name="resourceName" type="string">

          Google Ads resource name for the campaign budget.

          **Read-only.**

        </ResponseField>

        <ResponseField name="amountMicros" type="string">

          Daily budget amount in micros using the account's local currency.
          1,000,000 micros equals 1 currency unit (e.g. 6,400,000 = $6.40).
          Google caps monthly spend at 30.4 times this amount.
          Minimum is approximately $6.40 USD for standard subscribers; maximum is approximately $127.60 USD standard, or up to $1,500 for high-spend accounts.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="locations" type="LocationCriterion[]">

      Geographic locations targeted by this campaign.

      Constraints: `maxItems 1000`.

      <Expandable title="locations properties">

        <ResponseField name="resourceName" type="string">

          Google Ads campaign criterion resource name.
          Format: customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

          **Read-only.**

        </ResponseField>

        <ResponseField name="location" type="Location">

          The location being targeted.

          <Expandable title="One of the following">

            <ResponseField name="location" type="location">

              location

              <Expandable title="location properties">

                <ResponseField name="geoTargetConstant" type="string">

                  Google geo target constant resource name (e.g. geoTargetConstants/1023191).

                </ResponseField>

                <ResponseField name="proximity" type="ProximityAddress">

                  Proximity criterion defining a circular area by geo point and radius.

                  _Nested structure omitted for brevity — see the `ProximityAddress` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="displayName" type="string">

          Human-readable location name shown in the UI (e.g. "New York, New York").

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="adSchedule" type="WeeklyAdSchedule">

      Weekly ad schedule defining the days and hours when ads can run.

      <Expandable title="adSchedule properties">

        <ResponseField name="adShowAnytime" type="boolean">

          Whether ads show at any time during the day. When `true`, the daily schedules below are ignored.

        </ResponseField>

        <ResponseField name="sunday" type="DailyAdSchedule">

          Sunday ad show range.

          <Expandable title="sunday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="monday" type="DailyAdSchedule">

          Monday ad show range.

          <Expandable title="monday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tuesday" type="DailyAdSchedule">

          Tuesday ad show range.

          <Expandable title="tuesday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="wednesday" type="DailyAdSchedule">

          Wednesday ad show range.

          <Expandable title="wednesday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="thursday" type="DailyAdSchedule">

          Thursday ad show range.

          <Expandable title="thursday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="friday" type="DailyAdSchedule">

          Friday ad show range.

          <Expandable title="friday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="saturday" type="DailyAdSchedule">

          Saturday ad show range.

          <Expandable title="saturday properties">

            <ResponseField name="resourceName" type="string">

              Campaign criterion resource names have the form:
              customers/&#123;customerId&#125;/campaignCriteria/&#123;campaignId&#125;~&#123;criterionId&#125;

              **Read-only.**

            </ResponseField>

            <ResponseField name="startTime" type="AdTime">

              The start time of showing an ad in a specific day.

              <Expandable title="startTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="endTime" type="AdTime">

              Stop showing the ad at this time in a specific day.

              <Expandable title="endTime properties">

                <ResponseField name="hour" type="integer">

                  Starting hour in 24 hour time. This field must be between 0 and 23, inclusive.

                </ResponseField>

                <ResponseField name="minute" type="string">

                  Minutes after the start hour at which this schedule starts.

                  **Supported values:**

                  - `ZERO` — Zero minutes past the hour.
                  - `FIFTEEN` — Fifteen minutes past the hour.
                  - `THIRTY` — Thirty minutes past the hour.
                  - `FORTY_FIVE` — Forty-five minutes past the hour.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="reportingKey" type="string">

      Value used as the utm_campaign parameter for conversion tracking.

      **Read-only.**

    </ResponseField>

    <ResponseField name="campaignBrief" type="CampaignBrief">

      The brief used to generate this campaign.
      Only present on campaigns created via the GetCampaignSuggestions endpoint of SuggestionsService.

      <Expandable title="campaignBrief properties">

        <ResponseField name="_id" type="string">

          Campaign brief ID (Wix internal)

          **Read-only.**

        </ResponseField>

        <ResponseField name="platformType" type="string">

          The type of platform the campaign will be created on (Google/FB)

          **Read-only.**

          **Supported values:**

          - `GOOGLE`
          - `FACEBOOK`

        </ResponseField>

        <ResponseField name="goal" type="string">

          Goal of the campaign

        </ResponseField>

        <ResponseField name="audience" type="string">

          Audience the campaign should target

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
