---
title: "onAvailabilityExceptionCreated"
description: "Triggered when an availability exception is created."
icon: "bolt"
---

Triggered when an availability exception is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.availability_exception_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onAvailabilityExceptionCreated
  import { availabilityExceptions } from '@wix/restaurants';

  availabilityExceptions.onAvailabilityExceptionCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onAvailabilityExceptionCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { availabilityExceptions } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        availabilityExceptions,
      },
  });

  wixClient.availabilityExceptions.onAvailabilityExceptionCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
