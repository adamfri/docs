---
title: "onSectionDeleted"
description: "The onSectionDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.section_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onSectionDeleted
  import { sections } from '@wix/restaurants';

  sections.onSectionDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onSectionDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { sections } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        sections,
      },
  });

  wixClient.sections.onSectionDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
