---
title: "onResourceTypeUpdated"
description: "Triggered when a resource type is updated."
icon: "bolt"
---

Triggered when a resource type is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.resources.v2.resource_type_updated` |
| Package | `@wix/bookings` |

## Examples

<CodeGroup>

  ```javascript onResourceTypeUpdated
  import { resourceTypes } from '@wix/bookings';

  resourceTypes.onResourceTypeUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onResourceTypeUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { resourceTypes } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        resourceTypes,
      },
  });

  wixClient.resourceTypes.onResourceTypeUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
