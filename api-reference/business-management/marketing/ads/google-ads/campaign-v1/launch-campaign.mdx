---
title: "Launch Campaign"
description: "Activates a paused campaign, changing its status to enabled and beginning ad delivery."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Activates a paused campaign, changing its status to enabled and beginning ad delivery.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-GOOGLE-ADS">Manage google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_serverless/pa-google/v1/campaigns/{campaignId}/launch
    ```

    ## Path Params

    <ParamField path="campaignId" type="string" required>

      ID of the campaign to launch.

    </ParamField>

    ## Response Object

    <ResponseField name="campaign" type="Campaign">

      The launched campaign.

      See the [Campaign Object](/api-reference/business-management/marketing/ads/google-ads/campaign-v1/campaign-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CAMPAIGN_NOT_FOUND` | Couldn't find the campaign. |
    | `428` | `FAILED_PRECONDITION` | `MAXIMUM_NUMBER_OF_CAMPAIGNS_REACHED` |  |
    | `428` | `FAILED_PRECONDITION` | `CUSTOM_CHARGES_SUBSCRIPTION_EXPIRED` |  |
    | `428` | `FAILED_PRECONDITION` | `CUSTOM_CHARGES_SUBSCRIPTION_AUTO_RENEWAL_OFF` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/_serverless/pa-google/v1/campaigns/c9d8e7f6-a5b4-3210-fedc-ba9876543210/launch' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{}'
      ```

      ```json Response
      {
        "campaign": {
          "id": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
          "accountId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "campaignType": "SMART",
          "status": "LIVE",
          "name": "Spring Bakery Promotion",
          "budget": { "amountMicros": "15000000" },
          "reportingKey": "spring-bakery-promotion-2024-3-15-c9d8e7f6",
          "resourceName": "customers/1234567890/campaigns/9876543210",
          "createdDate": "2024-03-15T10:30:00.000Z",
          "updatedDate": "2024-03-20T08:00:00.000Z",
          "actionDate": "2024-03-20T08:00:00.000Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Activates a paused campaign, changing its status to enabled and beginning ad delivery.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-GOOGLE-ADS">Manage google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { campaigns } from '@wix/promote-google-ads';

    campaigns.launchCampaign(campaignId): Promise
    ```

    ## Method Parameters

    <ParamField body="campaignId" type="string" required>

      ID of the campaign to launch.

    </ParamField>

    ## Returns

    <ResponseField name="campaign" type="Campaign">

      The launched campaign.

      See the [Campaign Object](/api-reference/business-management/marketing/ads/google-ads/campaign-v1/campaign-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CAMPAIGN_NOT_FOUND` | Couldn't find the campaign. |
    | `428` | `FAILED_PRECONDITION` | `MAXIMUM_NUMBER_OF_CAMPAIGNS_REACHED` |  |
    | `428` | `FAILED_PRECONDITION` | `CUSTOM_CHARGES_SUBSCRIPTION_EXPIRED` |  |
    | `428` | `FAILED_PRECONDITION` | `CUSTOM_CHARGES_SUBSCRIPTION_AUTO_RENEWAL_OFF` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Campaign Launched | `wix.promote_paid_ads.google_ads.v1.campaign_launched` | Triggered when a campaign is activated via LaunchCampaign. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-launch-campaign" id="ex-sdk-launch-campaign-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-launch-campaign" id="ex-sdk-launch-campaign-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-launch-campaign-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-launch-campaign-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Launch a campaign for the first time</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">launchCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-launch-campaign-0"><span className="wix-picker-name">Launch a campaign for the first time</span></label>
    <label htmlFor="ex-sdk-launch-campaign-1"><span className="wix-picker-name">launchCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Launch a campaign for the first time
    import { campaigns } from '@wix/promote-google-ads';

    const response = await campaigns.launchCampaign('c9d8e7f6-a5b4-3210-fedc-ba9876543210');

    /* Promise resolves to:
     * {
     *   "campaign": {
     *     "id": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
     *     "status": "LIVE",
     *     "actionDate": "2024-03-20T08:00:00.000Z"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript launchCampaign (self-hosted)
    import { createClient } from '@wix/sdk';
    import { campaigns } from '@wix/promote-google-ads';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { campaigns },
      // Include the auth strategy and host as relevant
    });

    async function launchCampaign(campaignId) {
      const response = await myWixClient.campaigns.launchCampaign(campaignId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
