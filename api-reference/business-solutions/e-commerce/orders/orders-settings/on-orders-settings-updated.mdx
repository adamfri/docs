---
title: "onOrdersSettingsUpdated"
description: "Triggered when orders settings are updated."
icon: "bolt"
---

Triggered when orders settings are updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.orders_settings_updated` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Read eCommerce - all read permissions`, `Read Orders`, `Read Stores - all read permissions`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onOrdersSettingsUpdated
  import { ordersSettings } from '@wix/ecom';

  ordersSettings.onOrdersSettingsUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrdersSettingsUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ordersSettings } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        ordersSettings,
      },
  });

  wixClient.ordersSettings.onOrdersSettingsUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
