---
title: "onPostUnliked"
description: "An existing post was unliked."
icon: "bolt"
---

An existing post was unliked.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_unliked` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostUnliked
  import { posts } from '@wix/forum';

  posts.onPostUnliked((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostUnliked (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostUnliked((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
