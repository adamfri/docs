---
title: "Get Folder"
description: "Retrieves a folder."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a folder.

    If a folder ID is provided, returns the specified folder. If no folder ID is provided, returns the root folder.
    The root folder contains a `folders` property with all of a site's folders.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.DATA-COLLECTIONS-MANAGE">Manage Data Collections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/wix-data/v1/folders
    ```

    ## Query Params

    <ParamField query="folderId" type="string">

      Folder ID.

      Omit this property to retrieve the root folder.

    </ParamField>

    ## Response Object

    <ResponseField name="folder" type="Folder">

      Retrieved folder.

      See the [Folder Object](/api-reference/business-solutions/cms/collection-management/folders/folder-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `FOLDER_NOT_FOUND` | Couldn't find the folder. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://wixapis.com/autocms/v1/folders  \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
          "folder": {
              "name": "",
              "collectionReferences": [
                  {
                      "collectionName": "Items"
                  },
                  {
                      "collectionName": "RestaurantMenu"
                  }
              ],
              "folders": [
                  {
                      "id": "593c732f-d781-4457-b4a9-7ad73ddf4b1a",
                      "name": "Favorite Collections",
                      "description": "",
                      "collectionReferences": [],
                      "folders": []
                  }
              ]
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a folder.

    If a folder ID is provided, returns the specified folder. If no folder ID is provided, returns the root folder.
    The root folder contains a `folders` property with all of a site's folders.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.DATA-COLLECTIONS-MANAGE">Manage Data Collections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { folders } from '@wix/autocms-folders-service';

    folders.getFolder(options): Promise<Folder>
    ```

    ## Method Parameters

    <ParamField body="options" type="GetFolderOptions">

      options

      <Expandable title="options properties">

        <ParamField body="folderId" type="string">

          Folder ID.

          Omit this property to retrieve the root folder.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Folder>`.

    See the [Folder Object](/api-reference/business-solutions/cms/collection-management/folders/folder-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `FOLDER_NOT_FOUND` | Couldn't find the folder. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-folder" id="ex-sdk-get-folder-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-folder" id="ex-sdk-get-folder-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-folder" id="ex-sdk-get-folder-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-folder-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-folder-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getFolder</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getFolder (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getFolder (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-folder-0"><span className="wix-picker-name">getFolder</span></label>
    <label htmlFor="ex-sdk-get-folder-1"><span className="wix-picker-name">getFolder (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-folder-2"><span className="wix-picker-name">getFolder (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getFolder
    import { folders } from '@wix/autocms-folders-service';

    async function getFolder(options) {
      const response = await folders.getFolder(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getFolder (self-hosted)
    import { createClient } from '@wix/sdk';
    import { folders } from '@wix/autocms-folders-service';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { folders },
      // Include the auth strategy and host as relevant
    });

    async function getFolder(options) {
      const response = await myWixClient.folders.getFolder(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getFolder (with elevated permissions)
    import { folders } from '@wix/autocms-folders-service';
    import { auth } from '@wix/essentials';

    async function myGetFolderMethod(options) {
      const elevatedGetFolder = auth.elevate(folders.getFolder);
      const response = await elevatedGetFolder(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
