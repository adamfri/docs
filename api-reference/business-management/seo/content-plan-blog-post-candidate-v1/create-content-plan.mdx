---
title: "Create Content Plan"
description: "Generates the blog post briefs for a content plan flow."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Generates the blog post briefs for a content plan flow.

    A content plan flow stops at `KEYWORD_RESEARCH` and waits. Call this
    method to generate the briefs and let the flow continue to `SUCCESS`,
    then poll Get Content Plan Flow to follow it. A flow that's only polled
    never completes.

    Called on a flow that already reached `SUCCESS`, this method regenerates
    the plan under a separate content plan flow. The response carries the new
    `contentPlanFlowId`, and you poll that ID rather than the original. The
    original flow keeps its own candidates.

    Check `success` in the response as well as the HTTP status. A request
    that omits `contentPlanFlowId` reports every failure that way, with
    `success` set to `false` and the reason in `message` rather than an
    error. A generation failure reports that way even when you do send the
    ID.

    Call this method once for a flow and wait for it to return. Concurrent
    calls for the same site can produce duplicate content plans.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/promote/seo/v1/create-content-plan
    ```

    ## Body Params

    <ParamField body="contentPlanFlowId" type="string">

      ID of the content plan flow to generate the blog post briefs for.

      Omit to use the site's most recent eligible flow.
      Do not remove the fallback — existing callers depend on it.

    </ParamField>

    ## Response Object

    <ResponseField name="success" type="boolean">

      Whether the blog post briefs were generated.

      Check this field as well as the HTTP status. Some failures return `false`
      here rather than an error.

    </ResponseField>

    <ResponseField name="message" type="string">

      Reason the generation failed.

      Returned only when `success` is `false`.

    </ResponseField>

    <ResponseField name="contentPlanFlowId" type="string">

      ID of the content plan flow the briefs were generated under.

      When this method regenerates a plan for a flow that already succeeded,
      this is a new ID. Poll this ID rather than the one you sent.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `FLOW_NOT_READY_FOR_CONTENT_PLAN` | The flow's status is neither `SUCCESS` nor `KEYWORD_RESEARCH`, so a content plan can't be generated from it. |
    | `404` | `NOT_FOUND` | `CONTENT_PLAN_FLOW_NOT_FOUND` | Couldn't find the content plan flow. |
    | `428` | `FAILED_PRECONDITION` | `CONTENT_PLAN_FLOW_CANCELED` | The flow was canceled while generation was running, so no content plan was produced. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Releases a flow parked at KEYWORD_RESEARCH. Check the success field as well as the HTTP status, and poll the returned contentPlanFlowId.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/seo-content-plan-service/v1/create-content-plan' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "contentPlanFlowId": "d290f1ee-6c54-4b01-90e6-d701748f0851"
      }'
      ```

      ```json Response
      {
        "success": true,
        "contentPlanFlowId": "d290f1ee-6c54-4b01-90e6-d701748f0851"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Generates the blog post briefs for a content plan flow.

    A content plan flow stops at `KEYWORD_RESEARCH` and waits. Call this
    method to generate the briefs and let the flow continue to `SUCCESS`,
    then poll Get Content Plan Flow to follow it. A flow that's only polled
    never completes.

    Called on a flow that already reached `SUCCESS`, this method regenerates
    the plan under a separate content plan flow. The response carries the new
    `contentPlanFlowId`, and you poll that ID rather than the original. The
    original flow keeps its own candidates.

    Check `success` in the response as well as the HTTP status. A request
    that omits `contentPlanFlowId` reports every failure that way, with
    `success` set to `false` and the reason in `message` rather than an
    error. A generation failure reports that way even when you do send the
    ID.

    Call this method once for a flow and wait for it to return. Concurrent
    calls for the same site can produce duplicate content plans.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { blogPostCandidates } from '@wix/seo';

    blogPostCandidates.createContentPlan(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="CreateContentPlanOptions">

      options

      <Expandable title="options properties">

        <ParamField body="contentPlanFlowId" type="string">

          ID of the content plan flow to generate the blog post briefs for.

          Omit to use the site's most recent eligible flow.
          Do not remove the fallback — existing callers depend on it.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="success" type="boolean">

      Whether the blog post briefs were generated.

      Check this field as well as the HTTP status. Some failures return `false`
      here rather than an error.

    </ResponseField>

    <ResponseField name="message" type="string">

      Reason the generation failed.

      Returned only when `success` is `false`.

    </ResponseField>

    <ResponseField name="contentPlanFlowId" type="string">

      ID of the content plan flow the briefs were generated under.

      When this method regenerates a plan for a flow that already succeeded,
      this is a new ID. Poll this ID rather than the one you sent.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `FLOW_NOT_READY_FOR_CONTENT_PLAN` | The flow's status is neither `SUCCESS` nor `KEYWORD_RESEARCH`, so a content plan can't be generated from it. |
    | `404` | `NOT_FOUND` | `CONTENT_PLAN_FLOW_NOT_FOUND` | Couldn't find the content plan flow. |
    | `428` | `FAILED_PRECONDITION` | `CONTENT_PLAN_FLOW_CANCELED` | The flow was canceled while generation was running, so no content plan was produced. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-content-plan" id="ex-sdk-create-content-plan-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-content-plan" id="ex-sdk-create-content-plan-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-content-plan" id="ex-sdk-create-content-plan-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-content-plan-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-content-plan-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createContentPlan</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createContentPlan (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createContentPlan (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-content-plan-0"><span className="wix-picker-name">createContentPlan</span></label>
    <label htmlFor="ex-sdk-create-content-plan-1"><span className="wix-picker-name">createContentPlan (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-content-plan-2"><span className="wix-picker-name">createContentPlan (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createContentPlan
    import { blogPostCandidates } from '@wix/seo';

    async function createContentPlan(options) {
      const response = await blogPostCandidates.createContentPlan(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createContentPlan (self-hosted)
    import { createClient } from '@wix/sdk';
    import { blogPostCandidates } from '@wix/seo';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { blogPostCandidates },
      // Include the auth strategy and host as relevant
    });

    async function createContentPlan(options) {
      const response = await myWixClient.blogPostCandidates.createContentPlan(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createContentPlan (with elevated permissions)
    import { blogPostCandidates } from '@wix/seo';
    import { auth } from '@wix/essentials';

    async function myCreateContentPlanMethod(options) {
      const elevatedCreateContentPlan = auth.elevate(blogPostCandidates.createContentPlan);
      const response = await elevatedCreateContentPlan(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
