---
title: "Media Upload"
description: "One queued photo change on a Google Business Profile location: either an addition or a removal."
icon: "book-open"
---

One queued photo change on a Google Business Profile location: either an
addition or a removal.

Photo changes are queued and applied gradually rather than immediately,
because Google accepts only about 10 edits per minute per Business Profile.

Anything you can read here is either still waiting to be applied or has
failed. A change that succeeded leaves nothing behind, so an empty list means
the queue finished and everything worked. See `state`.

## SDK package

```bash
npm install @wix/gbp
```

Import the `mediaUploads` namespace from `@wix/gbp`.

## Methods

<CardGroup cols={2}>

  <Card title="Bulk Create Media Uploads" href="/api-reference/business-management/google-business-profile/media-upload-v1/bulk-create-media-uploads">
    POST Queues photo changes for one location and returns without calling Google.
  </Card>
  <Card title="List Media Uploads" href="/api-reference/business-management/google-business-profile/media-upload-v1/list-media-uploads">
    GET Retrieves the photo changes still queued or failed for a location.
  </Card>
  <Card title="Dismiss Media Upload Failures" href="/api-reference/business-management/google-business-profile/media-upload-v1/dismiss-media-upload-failures">
    POST Clears a location's failed photo changes, leaving queued ones alone.
  </Card>

</CardGroup>
