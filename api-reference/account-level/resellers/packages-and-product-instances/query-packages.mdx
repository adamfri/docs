---
title: "Query Packages"
description: "Retrieves a list of packages, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of packages, given the provided paging, filtering, and sorting.

    You must pass the ID of the Wix account that the packages belong to in the
    header of the call. The call returns packages that belong to the parent
    account, regardless if you pass the ID of a parent or sub-account. In case
    you want to retrieve only packages that belong to a sub-account, you must
    pass the sub-account ID as `filter` in the `query` object like this:

    ```json
    {
    "query": {
    "filter": {
    "accountId": {
    "$eq": "<SUB_ACCOUNT_ID>"
    }
    }
    }
    }
    ```

    Query Packages runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `100`

    By default `pagingMetadata.cursors` are returned, unless you specifically pass
    `query.paging`.

    The maximum for `cursorPaging.limit` is `100`.

    If you pass a cursor token that you have received in a previous Query Package response,
    passing additional sort or filter information results in an invalid cursor
    error. Since the received cursor token already holds all the needed information
    for sort and filter. Changing the sort and filter properties during the span of
    a cursor query isn't supported. But you can provide a different limit.

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language),
    [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination),
    and [Field Projection](https://dev.wix.com/api/rest/getting-started/field-projection).

    <Note>

    This call requires an account level API key and cannot be
    authenticated with the standard authorization header.

    </Note>

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `accountId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `externalId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances` |  | None |
    | `productInstances.instanceId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.siteId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.catalogProductId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.status` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.failure` | `$exists` | None |
    | `productInstances.failure.code` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.failure.message` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.billingInfo` | `$exists` | None |
    | `productInstances.billingInfo.type` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.billingInfo.cycleDuration` | `$exists` | None |
    | `productInstances.billingInfo.cycleDuration.unit` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.billingInfo.cycleDuration.count` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | None |
    | `productInstances.createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | None |
    | `productInstances.updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | None |
    | `productInstances.countryCode` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="premium.reseller-package.read">premium.reseller-package.read</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/resellers/v1/packages/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2" required>

      Query options. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="packages" type="Package[]">

      Retrieved packages.

      Max: 1000 packages

      Constraints: `minItems 0`, `maxItems 1000`.

      See the [Packages Object](/api-reference/account-level/resellers/packages-and-product-instances/packages-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'www.wixapis.com/resellers/v1/packages/query' \
      -H 'wix-account-id: <ACCOUNT_ID>' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "query": {
              "filter": {
                  "productInstances.status": {
                      "$hasAll": ["Enabled"]
                  }
              },
              "fields": ["id"],
              "fieldsets": ["id"]
          }
      }'
      ```

      ```json Response
      {
              "metadata": {
                  "count": 1,
                  "cursors": {}
              },
              "packages": [
                  {
                      "id": "ea1b8a6c-c57a-48e8-8db8-655269ad13c2",
                      "accountId": "<ACCOUNT_ID>",
                      "externalId": "some-external-id",
                      "productInstances": [
                          {
                              "instanceId": "b83e5be9-763e-4e1f-bd30-763ca0f7f3e5",
                              "siteId": "918aa943-ab4f-40bc-88c3-8dfd02fae7cd",
                              "catalogProductId": "00ba7923-2655-402a-852d-2b2100568bcf",
                              "status": "ENABLED",
                              "paymentInfo": {
                                  "type": "RECURRING",
                                  "cycleDuration": {
                                      "unit": "YEAR",
                                      "count": 1
                                  }
                              },
                              "createdDate": "2021-12-02T09:16:32.890Z",
                              "updatedDate": "2021-12-02T09:16:32.890Z"
                          },
                          {
                              "instanceId": "db59c0b3-81a0-469f-8f17-9c2a8b6ed3ad",
                              "siteId": "918aa943-ab4f-40bc-88c3-8dfd02fae7cd",
                              "catalogProductId": "040639ae-277e-4a73-a4d2-1b6801b92f45",
                              "status": "CANCELED",
                              "paymentInfo": {
                                  "type": "RECURRING",
                                  "cycleDuration": {
                                      "unit": "YEAR",
                                      "count": 1
                                  }
                              },
                              "createdDate": "2021-12-02T09:16:32.890Z",
                              "updatedDate": "2021-12-02T09:16:32.890Z"
                          },
                          {
                              "instanceId": "0080aebd-117b-47e9-93e1-1b1c0b461ed2",
                              "catalogProductId": "53cfc9f8-197a-4e12-b570-ad9ed0cb4e2c",
                              "status": "FAILED",
                              "failure": {
                                  "code": "DELIVERY_TIMEOUT",
                                  "message": "delivery of product has reached it's deadline"
                              },
                              "paymentInfo": {
                                  "type": "RECURRING",
                                  "cycleDuration": {
                                      "unit": "MONTH",
                                      "count": 1
                                  }
                              },
                              "createdDate": "2021-12-02T09:16:32.893Z",
                              "updatedDate": "2021-12-02T09:16:32.893Z"
                          }
                      ],
                      "createdDate": "2021-12-02T09:16:33.142Z",
                      "updatedDate": "2021-12-02T10:28:00.767Z"
                  }
              ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of packages, given the provided paging, filtering, and sorting.

    You must pass the ID of the Wix account that the packages belong to in the
    header of the call. The call returns packages that belong to the parent
    account, regardless if you pass the ID of a parent or sub-account. In case
    you want to retrieve only packages that belong to a sub-account, you must
    pass the sub-account ID as `filter` in the `query` object like this:

    ```json
    {
    "query": {
    "filter": {
    "accountId": {
    "$eq": "<SUB_ACCOUNT_ID>"
    }
    }
    }
    }
    ```

    Query Packages runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `100`

    By default `pagingMetadata.cursors` are returned, unless you specifically pass
    `query.paging`.

    The maximum for `cursorPaging.limit` is `100`.

    If you pass a cursor token that you have received in a previous Query Package response,
    passing additional sort or filter information results in an invalid cursor
    error. Since the received cursor token already holds all the needed information
    for sort and filter. Changing the sort and filter properties during the span of
    a cursor query isn't supported. But you can provide a different limit.

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language),
    [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination),
    and [Field Projection](https://dev.wix.com/api/rest/getting-started/field-projection).

    <Note>

    This call requires an account level API key and cannot be
    authenticated with the standard authorization header.

    </Note>

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `accountId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `externalId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances` |  | None |
    | `productInstances.instanceId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.siteId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.catalogProductId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.status` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.failure` | `$exists` | None |
    | `productInstances.failure.code` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.failure.message` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `productInstances.billingInfo` | `$exists` | None |
    | `productInstances.billingInfo.type` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.billingInfo.cycleDuration` | `$exists` | None |
    | `productInstances.billingInfo.cycleDuration.unit` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `productInstances.billingInfo.cycleDuration.count` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | None |
    | `productInstances._createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | None |
    | `productInstances._updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | None |
    | `productInstances.countryCode` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="premium.reseller-package.read">premium.reseller-package.read</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { packages } from '@wix/packages';

    packages.queryPackages(query): PackagesQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="PackagesQuery" required>

      Query options. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `PackagesQueryBuilder`.

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="packages" type="Package[]">

      Retrieved packages.

      Max: 1000 packages

      Constraints: `minItems 0`, `maxItems 1000`.

      See the [Packages Object](/api-reference/account-level/resellers/packages-and-product-instances/packages-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-packages" id="ex-sdk-query-packages-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-packages" id="ex-sdk-query-packages-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-packages-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-packages-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query packages with an API key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPackages (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-packages-0"><span className="wix-picker-name">Query packages with an API key</span></label>
    <label htmlFor="ex-sdk-query-packages-1"><span className="wix-picker-name">queryPackages (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query packages with an API key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { packages } from "@wix/packages";

    const wixClient = createClient({
     modules: { packages },
     auth: ApiKeyStrategy({
        apiKey: "MY-API-KEY",
      }),
    });

    async function queryPackages() {
      const { items } = await packages.queryPackages().find();
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPackages (self-hosted)
    import { createClient } from '@wix/sdk';
    import { packages } from '@wix/packages';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { packages },
      // Include the auth strategy and host as relevant
    });

    async function queryPackages(query) {
      const response = await myWixClient.packages.queryPackages(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
