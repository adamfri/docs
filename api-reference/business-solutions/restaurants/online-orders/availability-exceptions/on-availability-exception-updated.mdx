---
title: "onAvailabilityExceptionUpdated"
description: "Triggered when an availability exception is updated."
icon: "bolt"
---

Triggered when an availability exception is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.availability_exception_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onAvailabilityExceptionUpdated
  import { availabilityExceptions } from '@wix/restaurants';

  availabilityExceptions.onAvailabilityExceptionUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onAvailabilityExceptionUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { availabilityExceptions } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        availabilityExceptions,
      },
  });

  wixClient.availabilityExceptions.onAvailabilityExceptionUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
