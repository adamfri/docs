---
title: "onQuestionEntryCreated"
description: "Triggered when a question entry is created."
icon: "bolt"
---

Triggered when a question entry is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.faq.question_entry.v2.question_entry_created` |
| Package | `@wix/faq` |
| Permissions | `Manage FAQ`, `Read FAQ` |

## Examples

<CodeGroup>

  ```javascript onQuestionEntryCreated
  import { questionEntry } from '@wix/faq';

  questionEntry.onQuestionEntryCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onQuestionEntryCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { questionEntry } from '@wix/faq';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        questionEntry,
      },
  });

  wixClient.questionEntry.onQuestionEntryCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
