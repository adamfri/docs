---
title: "Referral Reward Object"
description: "The Referral Reward object in the Wix Referral Reward API."
icon: "cube"
---

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Referral reward ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the referral reward is updated.
      To prevent conflicting changes, the current revision must be passed when updating the referral reward.

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the referral reward was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time the referral reward was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="rewardType" type="string">

      Type of reward given.

      **Supported values:**

      - `COUPON` — Loyalty coupon is given.
      - `LOYALTY_POINTS` — Loyalty points are awarded.
      - `NOTHING` — No reward is given.

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "referralReward": {
          "id": "c8c732cf-2ca2-4025-9b1c-3be02f099cdb",
          "revision": "2",
          "createdDate": "2024-07-08T11:22:00.728Z",
          "updatedDate": "2024-07-08T11:27:57.197Z",
          "rewardType": "COUPON",
          "rewardedReferringCustomerId": "4fb41de4-da9c-4818-b54f-82c955ecc866",
          "coupon": {
            "id": "43fa238a-b464-4a5b-9079-5682a5a1eab7",
            "code": "6QW1ASGTZ0Q4",
            "status": "APPLIED",
            "couponSpecification": {
              "name": "Your reward",
              "discountType": "FIXED_AMOUNT",
              "limitedToOneItem": true,
              "appliesToSubscriptions": null,
              "discountedCycleCount": null,
              "fixedAmountOptions": {
                "amount": 10
              },
              "scope": {
                "namespace": "stores",
                "group": null
              }
            }
          }
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Referral reward ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the referral reward is updated.
      To prevent conflicting changes, the current revision must be passed when updating the referral reward.

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the referral reward was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time the referral reward was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="rewardType" type="string">

      Type of reward given.

      **Supported values:**

      - `COUPON` — Loyalty coupon is given.
      - `LOYALTY_POINTS` — Loyalty points are awarded.
      - `NOTHING` — No reward is given.

    </ResponseField>

  </Tab>

</Tabs>
