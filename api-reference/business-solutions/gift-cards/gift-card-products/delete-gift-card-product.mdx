---
title: "Delete Gift Card Product"
description: "Deletes a gift card product."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a gift card product. Gift cards that were already purchased from the deleted product remain valid.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/gift-cards/v1/gift-card-products/{giftCardProductId}
    ```

    ## Path Params

    <ParamField path="giftCardProductId" type="string" required>

      ID of the gift card product to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteGiftCardProductResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Gift Card Product Deleted | `wix.gift_cards.v1.gift_card_product_deleted` | Triggered when a gift card product is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a gift card product by ID.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/gift-cards/v1/gift-card-products/9041ce44-2efe-4d07-a3ae-b7084be31339' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a gift card product. Gift cards that were already purchased from the deleted product remain valid.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { giftVoucherProducts } from '@wix/gift-vouchers';

    giftVoucherProducts.deleteGiftCardProduct(giftCardProductId): Promise
    ```

    ## Method Parameters

    <ParamField body="giftCardProductId" type="string" required>

      ID of the gift card product to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteGiftCardProductResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Gift Card Product Deleted | `wix.gift_cards.v1.gift_card_product_deleted` | Triggered when a gift card product is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-gift-card-product" id="ex-sdk-delete-gift-card-product-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-gift-card-product" id="ex-sdk-delete-gift-card-product-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-gift-card-product" id="ex-sdk-delete-gift-card-product-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-gift-card-product-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-gift-card-product-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteGiftCardProduct</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteGiftCardProduct (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteGiftCardProduct (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-gift-card-product-0"><span className="wix-picker-name">deleteGiftCardProduct</span></label>
    <label htmlFor="ex-sdk-delete-gift-card-product-1"><span className="wix-picker-name">deleteGiftCardProduct (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-gift-card-product-2"><span className="wix-picker-name">deleteGiftCardProduct (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteGiftCardProduct
    import { giftVoucherProducts } from '@wix/gift-vouchers';

    async function deleteGiftCardProduct(giftCardProductId) {
      const response = await giftVoucherProducts.deleteGiftCardProduct(giftCardProductId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteGiftCardProduct (self-hosted)
    import { createClient } from '@wix/sdk';
    import { giftVoucherProducts } from '@wix/gift-vouchers';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { giftVoucherProducts },
      // Include the auth strategy and host as relevant
    });

    async function deleteGiftCardProduct(giftCardProductId) {
      const response = await myWixClient.giftVoucherProducts.deleteGiftCardProduct(giftCardProductId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteGiftCardProduct (with elevated permissions)
    import { giftVoucherProducts } from '@wix/gift-vouchers';
    import { auth } from '@wix/essentials';

    async function myDeleteGiftCardProductMethod(giftCardProductId) {
      const elevatedDeleteGiftCardProduct = auth.elevate(giftVoucherProducts.deleteGiftCardProduct);
      const response = await elevatedDeleteGiftCardProduct(giftCardProductId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
