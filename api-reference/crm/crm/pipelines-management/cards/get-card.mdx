---
title: "Get Card"
description: "Retrieves a card by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a card by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.READ-PIPELINES">Read Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/crm/pipelines/v1/cards/{cardId}
    ```

    ## Path Params

    <ParamField path="cardId" type="string" required>

      Card ID to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="card" type="Card">

      Retrieved card.

      See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a card by ID.

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/crm/pipelines/v1/cards/a1b2c3d4-e5f6-7890-1234-567890abcdef' \
        -H 'Authorization:<AUTH>'
      ```

      ```json Response
      {
        "card": {
          "id": "a1b2c3d4-e5f6-7890-1234-567890abcdef",
          "revision": "3",
          "createdDate": "2024-01-15T14:30:00.000Z",
          "updatedDate": "2024-01-18T09:15:30.000Z",
          "stageUpdatedDate": "2024-01-17T16:45:00.000Z",
          "pipelineId": "f9e8d7c6-b5a4-3210-fedc-ba9876543210",
          "stageId": "89abcdef-0123-4567-89ab-cdef01234567",
          "name": "Enterprise Software Deal",
          "description": "Large enterprise client interested in our software solution. Had demo call on Jan 16th - very interested in premium features.",
          "priority": "P1_URGENT",
          "assigneeId": "bcdefa01-2345-6789-abcd-ef0123456789",
          "dueDate": "2024-02-15T17:00:00.000Z",
          "staleDate": "2024-02-20T17:00:00.000Z",
          "value": {
            "value": "175000.00",
            "currency": "USD"
          },
          "outcome": "NONE",
          "mainContact": {
            "contactId": "1a2b3c4d-5e6f-7890-1234-567890abcdef",
            "firstName": "Sarah",
            "lastName": "Johnson",
            "email": "sarah.johnson@techcorp.com",
            "phone": "+18005551234",
            "fullName": "Sarah Johnson",
            "displayName": "Sarah Johnson"
          },
          "additionalContacts": [
            {
              "contactId": "ef012345-6789-abcd-ef01-23456789abcd",
              "firstName": "Michael",
              "lastName": "Chen",
              "email": "michael.chen@techcorp.com",
              "phone": "+18005551234",
              "fullName": "Michael Chen",
              "displayName": "Michael Chen"
            }
          ],
          "linkedEntities": [
            {
              "entityId": "2b3a4c5d-6e7f-8901-2345-67890abcdef1",
              "referencedEntity": "cc552162-24a4-45e0-9695-230c4931ef40_inquiryServicesPipeline",
              "linkedDate": "2024-01-15T14:30:00.000Z"
            }
          ],
          "stageUpdateSource": {
            "userId": "98765432-10fe-dcba-9876-543210fedcba",
            "appId": "2404cf16-fc8f-4002-98b7-863dbabe374f"
          },
          "extendedFields": {
            "customFields": {
              "leadSource": "Website Form",
              "companySize": "500-1000 employees"
            }
          },
          "tags": {
            "privateTags": {
              "tagIds": ["tag123", "tag456"]
            }
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a card by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.READ-PIPELINES">Read Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { cards } from '@wix/crm';

    cards.getCard(cardId): Promise
    ```

    ## Method Parameters

    <ParamField body="cardId" type="string" required>

      Card ID to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Card`.

    See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-card" id="ex-sdk-get-card-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-card" id="ex-sdk-get-card-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-card" id="ex-sdk-get-card-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-card-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-card-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getCard</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCard (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-card-0"><span className="wix-picker-name">getCard</span></label>
    <label htmlFor="ex-sdk-get-card-1"><span className="wix-picker-name">getCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-card-2"><span className="wix-picker-name">getCard (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getCard
    import { cards } from '@wix/crm';

    async function getCard(cardId) {
      const response = await cards.getCard(cardId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCard (self-hosted)
    import { createClient } from '@wix/sdk';
    import { cards } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { cards },
      // Include the auth strategy and host as relevant
    });

    async function getCard(cardId) {
      const response = await myWixClient.cards.getCard(cardId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCard (with elevated permissions)
    import { cards } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myGetCardMethod(cardId) {
      const elevatedGetCard = auth.elevate(cards.getCard);
      const response = await elevatedGetCard(cardId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
