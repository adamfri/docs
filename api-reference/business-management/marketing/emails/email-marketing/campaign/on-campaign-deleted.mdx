---
title: "onCampaignDeleted"
description: "Triggered when a campaign is deleted."
icon: "bolt"
---

Triggered when a campaign is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.email_marketing.v1.campaign_deleted` |
| Package | `@wix/email-marketing` |
| Permissions | `Manage Stores`, `Access Verticals by Automations`, `Set Up Automations`, `Read Email Marketing`, `Manage Email Marketing` |

## Examples

<CodeGroup>

  ```javascript onCampaignDeleted
  import { campaigns } from '@wix/email-marketing';

  campaigns.onCampaignDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onCampaignDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { campaigns } from '@wix/email-marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        campaigns,
      },
  });

  wixClient.campaigns.onCampaignDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
