---
title: "onSectionUpdated"
description: "Triggered when a section is updated."
icon: "bolt"
---

Triggered when a section is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.section_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onSectionUpdated
  import { sections } from '@wix/restaurants';

  sections.onSectionUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onSectionUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { sections } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        sections,
      },
  });

  wixClient.sections.onSectionUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
