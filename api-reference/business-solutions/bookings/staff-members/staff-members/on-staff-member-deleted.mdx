---
title: "onStaffMemberDeleted"
description: "Triggered when a staff member is deleted."
icon: "bolt"
---

Triggered when a staff member is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.staff.v1.staff_member_deleted` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onStaffMemberDeleted
  import { staffMembers } from '@wix/bookings';

  staffMembers.onStaffMemberDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onStaffMemberDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { staffMembers } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        staffMembers,
      },
  });

  wixClient.staffMembers.onStaffMemberDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
