---
title: "onRichContentUpdated"
description: "Triggered when rich content is updated."
icon: "bolt"
---

Triggered when rich content is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v1.rich_content_updated` |
| Package | `@wix/events` |
| Permissions | `Manage Stores`, `Read Events - all read permissions`, `Manage Bookings Services and Settings`, `Manage Events - all permissions`, `Manage Events`, `Read Events`, `Manage Challenges`, `Manage Portfolio`, `Manage Blog`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Manage Guest List` |

## Examples

<CodeGroup>

  ```javascript onRichContentUpdated
  import { ricos } from '@wix/events';

  ricos.onRichContentUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRichContentUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ricos } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        ricos,
      },
  });

  wixClient.ricos.onRichContentUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
