---
title: "onDraftCreated"
description: "Triggered when a draft post is created."
icon: "bolt"
---

Triggered when a draft post is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v3.draft_created` |
| Package | `@wix/blog` |
| Permissions | `Manage Blog`, `Read Draft Blog Posts`, `Read Blog` |

## Examples

<CodeGroup>

  ```javascript onDraftCreated
  import { draftPosts } from '@wix/blog';

  draftPosts.onDraftCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onDraftCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { draftPosts } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        draftPosts,
      },
  });

  wixClient.draftPosts.onDraftCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
