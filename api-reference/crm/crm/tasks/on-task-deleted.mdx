---
title: "onTaskDeleted"
description: "Triggered when a task is deleted."
icon: "bolt"
---

Triggered when a task is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.crm.tasks.v2.task_deleted` |
| Package | `@wix/crm` |
| Permissions | `Read Tasks`, `Manage Tasks` |

## Examples

<CodeGroup>

  ```javascript onTaskDeleted
  import { tasks } from '@wix/crm';

  tasks.onTaskDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onTaskDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { tasks } from '@wix/crm';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        tasks,
      },
  });

  wixClient.tasks.onTaskDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
