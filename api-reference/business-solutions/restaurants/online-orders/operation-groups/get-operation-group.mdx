---
title: "Get Operation Group"
description: "Retrieves a OperationGroup."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a OperationGroup.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/restaurants/v1/operation-groups/{operationGroupId}
    ```

    ## Path Params

    <ParamField path="operationGroupId" type="string" required>

      ID of the OperationGroup to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="operationGroup" type="OperationGroup">

      The requested OperationGroup.

      See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://www.wixapis.com/restaurants/v1/operation-groups/48605ac5-53fc-49b4-96b2-87f7636f5ce0 \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "availabilityException": {
            "id": "48605ac5-53fc-49b4-96b2-87f7636f5ce0",
            "revision": "1",
            "createdDate": "2024-07-09T10:17:08.865Z",
            "updatedDate": "2024-07-09T10:17:08.865Z",
            "name": "Ordering page 1"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a OperationGroup.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { operationGroups } from '@wix/restaurants';

    operationGroups.getOperationGroup(operationGroupId): Promise<OperationGroup>
    ```

    ## Method Parameters

    <ParamField body="operationGroupId" type="string" required>

      ID of the OperationGroup to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<OperationGroup>`.

    See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-operation-group" id="ex-sdk-get-operation-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-operation-group" id="ex-sdk-get-operation-group-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-operation-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-operation-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getOperationGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getOperationGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-operation-group-0"><span className="wix-picker-name">getOperationGroup</span></label>
    <label htmlFor="ex-sdk-get-operation-group-1"><span className="wix-picker-name">getOperationGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getOperationGroup
    // @title Get Operation Group
    import { operationGroups } from "@wix/restaurants";

    async function getOperationGroup() {
      const response = await operationGroups.getOperationGroup("48605ac5-53fc-49b4-96b2-87f7636f5ce0");

      /* Promise resolves to:
       * {
       *   "operationGroup": {
       *     "id": "48605ac5-53fc-49b4-96b2-87f7636f5ce0",
       *     "name": "Ordering page 1"
       *   }
       * }
       */
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getOperationGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { operationGroups } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { operationGroups },
      // Include the auth strategy and host as relevant
    });

    async function getOperationGroup(operationGroupId) {
      const response = await myWixClient.operationGroups.getOperationGroup(operationGroupId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
