---
title: "onOrderStartDateChanged"
description: "Triggered when an order's startDate is changed."
icon: "bolt"
---

Triggered when an order's `startDate` is changed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.v2.order_start_date_changed` |
| Package | `@wix/pricing-plans` |
| Permissions | `Manage Orders`, `Read Orders`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onOrderStartDateChanged
  import { orders } from '@wix/pricing-plans';

  orders.onOrderStartDateChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderStartDateChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderStartDateChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
