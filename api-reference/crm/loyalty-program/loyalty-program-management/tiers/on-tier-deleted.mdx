---
title: "onTierDeleted"
description: "Triggered when a tier is deleted."
icon: "bolt"
---

Triggered when a tier is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.v1.tier_deleted` |
| Package | `@wix/loyalty` |

## Examples

<CodeGroup>

  ```javascript onTierDeleted
  import { tiers } from '@wix/loyalty';

  tiers.onTierDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onTierDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { tiers } from '@wix/loyalty';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        tiers,
      },
  });

  wixClient.tiers.onTierDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
