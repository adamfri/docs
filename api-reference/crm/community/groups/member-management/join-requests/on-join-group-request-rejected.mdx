---
title: "onJoinGroupRequestRejected"
description: "Triggered when a join group request is rejected."
icon: "bolt"
---

Triggered when a join group request is rejected.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.join_group_request_rejected` |
| Package | `@wix/groups` |
| Permissions | `Manage Social Groups` |

## Examples

<CodeGroup>

  ```javascript onJoinGroupRequestRejected
  import { joinGroupRequests } from '@wix/groups';

  joinGroupRequests.onJoinGroupRequestRejected((event) => {
      // handle your event here
  });
  ```

  ```javascript onJoinGroupRequestRejected (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { joinGroupRequests } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        joinGroupRequests,
      },
  });

  wixClient.joinGroupRequests.onJoinGroupRequestRejected((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
