---
title: "onCategoryDeleted"
description: "Triggered when a category is deleted."
icon: "bolt"
---

Triggered when a category is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.faq.category.v2.category_deleted` |
| Package | `@wix/faq` |
| Permissions | `Manage FAQ`, `Read FAQ` |

## Examples

<CodeGroup>

  ```javascript onCategoryDeleted
  import { category } from '@wix/faq';

  category.onCategoryDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { category } from '@wix/faq';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        category,
      },
  });

  wixClient.category.onCategoryDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
