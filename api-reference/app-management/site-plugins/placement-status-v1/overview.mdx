---
title: "Placement Status"
description: "The PlacementStatus object represents the status of your app's site plugins, indicating whether they are currently placed in a slot on the user's site."
icon: "book-open"
---

The `PlacementStatus` object represents the status of your app's site plugins, indicating whether they are currently placed in a slot on the user's site.

## SDK package

```bash
npm install @wix/site-plugins
```

Import the `plugins` namespace from `@wix/site-plugins`.

## Methods

<CardGroup cols={2}>

  <Card title="Get Placement Status" href="/api-reference/app-management/site-plugins/placement-status-v1/get-placement-status">
    GET Get the placement statuses of your app's site plugins on the user's site.
  </Card>

</CardGroup>
