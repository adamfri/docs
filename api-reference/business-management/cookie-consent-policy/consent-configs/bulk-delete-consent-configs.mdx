---
title: "Bulk Delete Consent Configs"
description: "Deletes multiple consent configs."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple consent configs.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bu/legal/v1/bulk/consent-configs/delete
    ```

    ## Body Params

    <ParamField body="consentConfigIds" type="string[]" required>

      Consent config IDs to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkConsentConfigResult[]">

      Consent configs deleted by bulk action.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Individual item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Deleted | `wix.bu.legal.v1.consent_config_deleted` | Triggered when a consent config is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Delete multiple consent configs in a single operation

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/consent/consent-config/v1/bulk/consent-configs/delete' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "consentConfigIds": [
            "334afbf6-5c8b-4b43-9e9e-dd28664ca28f",
            "445bfcg7-6d9c-5c54-0f0f-ee39775db39g"
          ]
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "334afbf6-5c8b-4b43-9e9e-dd28664ca28f",
              "originalIndex": 0,
              "success": true
            }
          },
          {
            "itemMetadata": {
              "id": "445bfcg7-6d9c-5c54-0f0f-ee39775db39g",
              "originalIndex": 1,
              "success": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalCount": 2,
          "successCount": 2,
          "failureCount": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple consent configs.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { consentConfig } from '@wix/consent-policy';

    consentConfig.bulkDeleteConsentConfigs(consentConfigIds): Promise
    ```

    ## Method Parameters

    <ParamField body="consentConfigIds" type="string[]" required>

      Consent config IDs to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkConsentConfigResult[]">

      Consent configs deleted by bulk action.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Individual item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Deleted | `wix.bu.legal.v1.consent_config_deleted` | Triggered when a consent config is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-consent-configs" id="ex-sdk-bulk-delete-consent-configs-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-consent-configs" id="ex-sdk-bulk-delete-consent-configs-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-consent-configs" id="ex-sdk-bulk-delete-consent-configs-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-consent-configs-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-consent-configs-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteConsentConfigs</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteConsentConfigs (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteConsentConfigs (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-consent-configs-0"><span className="wix-picker-name">bulkDeleteConsentConfigs</span></label>
    <label htmlFor="ex-sdk-bulk-delete-consent-configs-1"><span className="wix-picker-name">bulkDeleteConsentConfigs (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-consent-configs-2"><span className="wix-picker-name">bulkDeleteConsentConfigs (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteConsentConfigs
    import { consentConfig } from '@wix/consent-policy';

    async function bulkDeleteConsentConfigs(consentConfigIds) {
      const response = await consentConfig.bulkDeleteConsentConfigs(consentConfigIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteConsentConfigs (self-hosted)
    import { createClient } from '@wix/sdk';
    import { consentConfig } from '@wix/consent-policy';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { consentConfig },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteConsentConfigs(consentConfigIds) {
      const response = await myWixClient.consentConfig.bulkDeleteConsentConfigs(consentConfigIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteConsentConfigs (with elevated permissions)
    import { consentConfig } from '@wix/consent-policy';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteConsentConfigsMethod(consentConfigIds) {
      const elevatedBulkDeleteConsentConfigs = auth.elevate(consentConfig.bulkDeleteConsentConfigs);
      const response = await elevatedBulkDeleteConsentConfigs(consentConfigIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
