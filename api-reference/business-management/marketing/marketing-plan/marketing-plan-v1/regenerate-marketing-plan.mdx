---
title: "Regenerate Marketing Plan"
description: "Regenerates the upcoming activities of the existing marketing plan."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Regenerates the upcoming activities of the existing marketing plan. Activities whose date is
    already in the past are preserved.

    Generation is asynchronous. The response immediately returns a `status` of `GENERATING`.
    Poll `GetSocialMarketingPlan` until `status` is `ACTIVE` (ready) or `FAILED`.
    A plan must already exist. If the plan is already generating, this method returns a
    `FAILED_PRECONDITION` error ("plan already generating"). Poll `GetSocialMarketingPlan` until
    the status is no longer `GENERATING` before calling this method. To include fresh SEO keyword
    research and blog activities, use `RegenerateMarketingPlanWithKeywordResearch` instead.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-plan/regenerate-marketing-plan
    ```

    ## Response Object

    <ResponseField name="status" type="string">

      Current plan status. Returns `GENERATING` immediately; poll `GetSocialMarketingPlan` until `ACTIVE` or `FAILED`.

      **Supported values:**

      - `NEVER_CREATED` — No plan has been generated for this site yet.
      - `GENERATING` — The plan is currently being generated. Poll `GetSocialMarketingPlan` until status changes.
      - `ACTIVE` — The plan is ready to use and activities are available.
      - `INACTIVE` — The plan was automatically deactivated after a long period without engagement. An `INACTIVE` plan returns no activities. Calling `GetSocialMarketingPlan` after a long idle period can itself trigger this transition. Generating or regenerating reactivates the plan.
      - `FAILED` — Generation failed. Call a generate or regenerate method to try again.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Regenerate the existing social marketing plan for the site. Regeneration runs asynchronously, so the response returns the current plan status.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-plan/regenerate-marketing-plan' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{}'
      ```

      ```json Response
      {
        "status": "GENERATING"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Regenerates the upcoming activities of the existing marketing plan. Activities whose date is
    already in the past are preserved.

    Generation is asynchronous. The response immediately returns a `status` of `GENERATING`.
    Poll `GetSocialMarketingPlan` until `status` is `ACTIVE` (ready) or `FAILED`.
    A plan must already exist. If the plan is already generating, this method returns a
    `FAILED_PRECONDITION` error ("plan already generating"). Poll `GetSocialMarketingPlan` until
    the status is no longer `GENERATING` before calling this method. To include fresh SEO keyword
    research and blog activities, use `RegenerateMarketingPlanWithKeywordResearch` instead.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingPlan } from '@wix/promote-growth-tools-marketing-plan';

    marketingPlan.regenerateMarketingPlan(): Promise
    ```

    ## Returns

    <ResponseField name="status" type="string">

      Current plan status. Returns `GENERATING` immediately; poll `GetSocialMarketingPlan` until `ACTIVE` or `FAILED`.

      **Supported values:**

      - `NEVER_CREATED` — No plan has been generated for this site yet.
      - `GENERATING` — The plan is currently being generated. Poll `GetSocialMarketingPlan` until status changes.
      - `ACTIVE` — The plan is ready to use and activities are available.
      - `INACTIVE` — The plan was automatically deactivated after a long period without engagement. An `INACTIVE` plan returns no activities. Calling `GetSocialMarketingPlan` after a long idle period can itself trigger this transition. Generating or regenerating reactivates the plan.
      - `FAILED` — Generation failed. Call a generate or regenerate method to try again.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-regenerate-marketing-plan" id="ex-sdk-regenerate-marketing-plan-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-regenerate-marketing-plan" id="ex-sdk-regenerate-marketing-plan-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-regenerate-marketing-plan-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-regenerate-marketing-plan-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Regenerate a marketing plan</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">regenerateMarketingPlan (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-regenerate-marketing-plan-0"><span className="wix-picker-name">Regenerate a marketing plan</span></label>
    <label htmlFor="ex-sdk-regenerate-marketing-plan-1"><span className="wix-picker-name">regenerateMarketingPlan (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Regenerate a marketing plan
    import { marketingPlan } from "@wix/promote-growth-tools-marketing-plan";

    async function regenerateMarketingPlan() {
      const response = await marketingPlan.regenerateMarketingPlan();
    }

    /* Promise resolves to:
     * {
     *   "status": "GENERATING"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript regenerateMarketingPlan (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingPlan } from '@wix/promote-growth-tools-marketing-plan';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingPlan },
      // Include the auth strategy and host as relevant
    });

    async function regenerateMarketingPlan() {
      const response = await myWixClient.marketingPlan.regenerateMarketingPlan();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
