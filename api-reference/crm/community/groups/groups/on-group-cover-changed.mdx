---
title: "onGroupCoverChanged"
description: "Triggered when a group's logo is changed."
icon: "bolt"
---

Triggered when a group's logo is changed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_group_cover_changed` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupCoverChanged
  import { groups } from '@wix/groups';

  groups.onGroupCoverChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupCoverChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { groups } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        groups,
      },
  });

  wixClient.groups.onGroupCoverChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
