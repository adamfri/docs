---
title: "onProductCollectionChanged"
description: "Triggered when a collection is changed."
icon: "bolt"
---

Triggered when a collection is changed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `com.wix.ecommerce.catalog.api.v1.CollectionChanged` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onProductCollectionChanged
  import { products } from '@wix/stores';

  products.onProductCollectionChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onProductCollectionChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { products } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        products,
      },
  });

  wixClient.products.onProductCollectionChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
