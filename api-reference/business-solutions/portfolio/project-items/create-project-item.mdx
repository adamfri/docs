---
title: "Create Project Item"
description: "Creates a project item."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a project item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/portfolio/v1/items
    ```

    ## Body Params

    <ParamField body="item" type="Item" required>

      Project item to create.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="item" type="Item">

      Newly created project item.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Created | `wix.portfolio.project_items.v1.project_item_created` | Triggered when a project item is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/portfolio/v1/Items' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "item": {
          "projectId": "123e4567-e89b-12d3-a456-426614174000",
          "sortOrder": 1.0,
          "title": "Lonely",
          "description": "This is the Lonely song image cover.",
          "image": {
            "imageInfo": {
              "id": "23ff-66b77-888aaf-9c45227e"
            }
          },
          "link": {
            "text": "Listen to the song",
            "url": "https://lonely-song.com",
            "target": "_blank"
          }
        }
      }'
      ```

      ```json Response
      {
        "item": {
          "id": "456e7890-e89b-12d3-a456-426614174000",
          "projectId": "123e4567-e89b-12d3-a456-426614174000",
          "title": "Lonely",
          "description": "This is the Lonely song image cover.",
          "sortOrder": 1.0,
          "type": "IMAGE",
          "image": {
            "imageInfo": {
              "id": "23ff-66b77-888aaf-9c45227e",
            }
          },
          "createdDate": "2024-07-31T12:00:00Z",
          "updatedDate": "2024-07-31T12:00:00Z",
          "link": {
              "text": "Listen to the song",
              "url": "https://lonely-song.com",
              "target": "_blank"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a project item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { projectItems } from '@wix/portfolio';

    projectItems.createProjectItem(item): Promise<Item>
    ```

    ## Method Parameters

    <ParamField body="item" type="Item" required>

      Project item to create.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Item>`.

    See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Created | `wix.portfolio.project_items.v1.project_item_created` | Triggered when a project item is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-project-item" id="ex-sdk-create-project-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-project-item" id="ex-sdk-create-project-item-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-project-item" id="ex-sdk-create-project-item-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-project-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-project-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createProjectItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createProjectItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createProjectItem (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-project-item-0"><span className="wix-picker-name">createProjectItem</span></label>
    <label htmlFor="ex-sdk-create-project-item-1"><span className="wix-picker-name">createProjectItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-project-item-2"><span className="wix-picker-name">createProjectItem (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createProjectItem
    import { projectItems } from '@wix/portfolio';

    async function createProjectItem(item) {
      const response = await projectItems.createProjectItem(item);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createProjectItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { projectItems } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { projectItems },
      // Include the auth strategy and host as relevant
    });

    async function createProjectItem(item) {
      const response = await myWixClient.projectItems.createProjectItem(item);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createProjectItem (with elevated permissions)
    import { projectItems } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myCreateProjectItemMethod(item) {
      const elevatedCreateProjectItem = auth.elevate(projectItems.createProjectItem);
      const response = await elevatedCreateProjectItem(item);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
