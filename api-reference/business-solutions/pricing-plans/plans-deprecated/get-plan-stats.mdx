---
title: "Get Plan Stats"
description: "Gets statistics about the pricing plans."
icon: "download"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Gets statistics about the pricing plans. Currently provides only the total number of pricing plans, including archived plans.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAIDPLANS.READ-PLANS">Read Pricing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/pricing-plans/v2/plans/stats
    ```

    ## Response Object

    <ResponseField name="totalPlans" type="integer">

      Total number of plans created, including active plans (both public and hidden) and archived plans.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PRICING_PLANS_NOT_INSTALLED` | The user does not have the Wix Pricing Plans app installed on the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
        https://www.wixapis.com/pricing-plans/v2/plans/stats
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "totalPlans": 80
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Gets statistics about the pricing plans. Currently provides only the total number of pricing plans, including archived plans.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAIDPLANS.READ-PLANS">Read Pricing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { plans } from '@wix/pricing-plans';

    plans.getPlanStats(): Promise<GetPlanStatsResponse>
    ```

    ## Returns

    Returns `Promise<GetPlanStatsResponse>`.

    <ResponseField name="totalPlans" type="integer">

      Total number of plans created, including active plans (both public and hidden) and archived plans.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PRICING_PLANS_NOT_INSTALLED` | The user does not have the Wix Pricing Plans app installed on the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-plan-stats" id="ex-sdk-get-plan-stats-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-plan-stats" id="ex-sdk-get-plan-stats-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-plan-stats" id="ex-sdk-get-plan-stats-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-plan-stats-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-plan-stats-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get number of plans</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get number of plans (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPlanStats (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-plan-stats-0"><span className="wix-picker-name">Get number of plans</span></label>
    <label htmlFor="ex-sdk-get-plan-stats-1"><span className="wix-picker-name">Get number of plans (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-get-plan-stats-2"><span className="wix-picker-name">getPlanStats (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get number of plans
    import { plans } from '@wix/pricing-plans';

    export async function myGetPlanStatsFunction() {
      try {
        const planStats = plans.getPlanStats();

        return planStats;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "totalPlans": 8     
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get number of plans (with elevated permissions)
    import { plans } from '@wix/pricing-plans';
    import { auth } from '@wix/essentials';

    const elevatedGetPlanStats = auth.elevate(plans.getPlanStats);

    export async function myGetPlanStatsFunction() {
      try {
        const planStats = elevatedGetPlanStats();

        return planStats;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "totalPlans": 8     
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPlanStats (self-hosted)
    import { createClient } from '@wix/sdk';
    import { plans } from '@wix/pricing-plans';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { plans },
      // Include the auth strategy and host as relevant
    });

    async function getPlanStats() {
      const response = await myWixClient.plans.getPlanStats();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
