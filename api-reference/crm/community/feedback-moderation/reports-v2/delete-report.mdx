---
title: "Delete Report"
description: "Deletes a report and removes it from the report list in the dashboard."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a report and removes it from the report list in the dashboard.

    Site members and visitors can only delete their own reports.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.MANAGE-REPORTS">Manage Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/reports/v2/reports/{reportId}
    ```

    ## Path Params

    <ParamField path="reportId" type="string" required>

      ID of the report to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteReportResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Report Deleted | `wix.reports.v2.report_deleted` | Triggered when a report is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \ https://www.wixapis.com/reports/v2/reports/dd7cfd02-1837-4fb8-bc6f-86316f0984a9 \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a report and removes it from the report list in the dashboard.

    Site members and visitors can only delete their own reports.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.MANAGE-REPORTS">Manage Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { reports } from '@wix/reports';

    reports.deleteReport(reportId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="reportId" type="string" required>

      ID of the report to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Report Deleted | `wix.reports.v2.report_deleted` | Triggered when a report is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-report" id="ex-sdk-delete-report-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-report" id="ex-sdk-delete-report-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-report" id="ex-sdk-delete-report-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-report-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-report-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteReport</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReport (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReport (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-report-0"><span className="wix-picker-name">deleteReport</span></label>
    <label htmlFor="ex-sdk-delete-report-1"><span className="wix-picker-name">deleteReport (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-report-2"><span className="wix-picker-name">deleteReport (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteReport
    import { reports } from '@wix/reports';

    async function deleteReport(reportId) {
      const response = await reports.deleteReport(reportId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReport (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reports } from '@wix/reports';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reports },
      // Include the auth strategy and host as relevant
    });

    async function deleteReport(reportId) {
      const response = await myWixClient.reports.deleteReport(reportId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReport (with elevated permissions)
    import { reports } from '@wix/reports';
    import { auth } from '@wix/essentials';

    async function myDeleteReportMethod(reportId) {
      const elevatedDeleteReport = auth.elevate(reports.deleteReport);
      const response = await elevatedDeleteReport(reportId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
