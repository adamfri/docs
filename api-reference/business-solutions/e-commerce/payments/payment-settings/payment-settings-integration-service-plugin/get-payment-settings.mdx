---
title: "Get Payment Settings"
description: "This method retrieves payment settings from your app."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    This method retrieves payment settings from your app.

    Wix calls this method during the payment process. For example, when a customer inserts credit card details and places an order.
    This method retrieves the payment settings to apply, and Wix passes on the settings to the payment provider.

    ## Endpoint

    ```http
    POST /v1/payment-settings
    ```

    ## Body Params

    <ParamField body="order" type="Order">

      Order.

      <Expandable title="order properties">

        <ParamField body="id" type="string">

          Order ID.

          **Read-only.**

        </ParamField>

        <ParamField body="number" type="number">

          Order number displayed in the site owner's dashboard.

          Auto-generated for orders created through the regular order flow. When importing an order, you can provide the original number to preserve it. See [Set Order Number Counter](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/orders/set-order-number-counter) to control numbering of orders created after a migration.

          **Read-only.**

        </ParamField>

        <ParamField body="createdDate" type="datetime">

          Date and time the order was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

          **Read-only.**

        </ParamField>

        <ParamField body="updatedDate" type="datetime">

          Date and time the order was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

          **Read-only.**

        </ParamField>

        <ParamField body="lineItems" type="OrderLineItem[]">

          Order line items.

          **Read-only.** Constraints: `minItems 1`, `maxItems 300`.

          <Expandable title="lineItems properties">

            <ParamField body="id" type="string">

              Line item ID.

              Constraints: `immutable`.

            </ParamField>

            <ParamField body="productName" type="ProductName">

              Item name.

              + Stores - `product.name`
              + Bookings - `service.info.name`
              + Events - `ticket.name`

              _Nested structure omitted for brevity — see the `ProductName` object._

            </ParamField>

            <ParamField body="catalogReference" type="CatalogReference">

              References to the line item's origin catalog. Empty for custom line items.

              _Nested structure omitted for brevity — see the `CatalogReference` object._

            </ParamField>

            <ParamField body="quantity" type="integer">

              Line item quantity.

              Constraints: `min 1`, `max 100000`.

            </ParamField>

            <ParamField body="totalDiscount" type="Price">

              Total discount for this line item's entire quantity.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="descriptionLines" type="DescriptionLine[]">

              Line item description lines. Used for display purposes for the cart, checkout and order.

              Constraints: `minItems 0`, `maxItems 20`.

              _Nested structure omitted for brevity — see the `DescriptionLine[]` object._

            </ParamField>

            <ParamField body="image" type="Image">

              Line item image.

              _Nested structure omitted for brevity — see the `Image` object._

            </ParamField>

            <ParamField body="physicalProperties" type="PhysicalProperties">

              Physical properties of the item. When relevant, contains information such as SKU and item weight.

              _Nested structure omitted for brevity — see the `PhysicalProperties` object._

            </ParamField>

            <ParamField body="itemType" type="ItemType">

              Item type. Either a preset type or custom.

              _Nested structure omitted for brevity — see the `ItemType` object._

            </ParamField>

            <ParamField body="fulfillerId" type="string">

              Fulfiller ID. Field is empty when the line item is self-fulfilled.
              To get fulfillment information, pass the order ID to [List Fulfillments For Single Order](https://dev.wix.com/docs/rest/api-reference/wix-e-commerce/order-fulfillments/list-fulfillments-for-single-order).

            </ParamField>

            <ParamField body="refundQuantity" type="integer">

              Number of items that were refunded.

            </ParamField>

            <ParamField body="restockQuantity" type="integer">

              Number of items restocked.

              For a per-location breakdown, see `restockLocations`.

            </ParamField>

            <ParamField body="price" type="Price">

              Line item price after line item discounts for display purposes.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="priceBeforeDiscounts" type="Price">

              Line item price before line item discounts for display purposes. Defaults to `price` when not provided.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="totalPriceBeforeTax" type="Price">

              Total price after discounts, and before tax.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="totalPriceAfterTax" type="Price">

              Total price after all discounts and tax.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="paymentOption" type="string">

              Type of selected payment option for current item.

              Default: `FULL_PAYMENT_ONLINE`

              **Supported values:**

              - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
              - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
              - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
              - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
              - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
              - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

            </ParamField>

            <ParamField body="taxDetails" type="ItemTaxFullDetails" deprecated>

              Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024.
              Tax details for this line item.

              **Deprecated.** Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024.
              Tax details for this line item. Use `tax_info` instead. Removal: 2024-09-30.

              _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

            </ParamField>

            <ParamField body="taxInfo" type="LineItemTaxInfo">

              Represents all the relevant tax details for a specific line item.

              _Nested structure omitted for brevity — see the `LineItemTaxInfo` object._

            </ParamField>

            <ParamField body="digitalFile" type="DigitalFile">

              Digital file identifier, relevant only for items with type DIGITAL.

              _Nested structure omitted for brevity — see the `DigitalFile` object._

            </ParamField>

            <ParamField body="subscriptionInfo" type="SubscriptionInfo">

              Subscription info.

              _Nested structure omitted for brevity — see the `SubscriptionInfo` object._

            </ParamField>

            <ParamField body="priceDescription" type="PriceDescription">

              Additional description for the price. For example, when price is 0 but additional details about the actual price are needed - "Starts at $67".

              _Nested structure omitted for brevity — see the `PriceDescription` object._

            </ParamField>

            <ParamField body="depositAmount" type="Price">

              Item's price amount to be charged during checkout. Relevant for items with a `paymentOption` value of `"DEPOSIT_ONLINE"`.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="locations" type="LocationAndQuantity[]">

              Line item locations.

              The location's total quantity must not exceed the line item quantity.

              **Developer Preview.** Constraints: `minItems 0`, `maxItems 5`.

              _Nested structure omitted for brevity — see the `LocationAndQuantity[]` object._

            </ParamField>

            <ParamField body="lineItemPrice" type="Price">

              Total price **after** catalog discounts and line item discounts.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="taxableAddress" type="TaxableAddress">

              Address used for tax calculation.

              _Nested structure omitted for brevity — see the `TaxableAddress` object._

            </ParamField>

            <ParamField body="priceUndetermined" type="boolean">

              Whether the price is not yet defined, and will be updated after the order is created.

              Default: `false`

            </ParamField>

            <ParamField body="fixedQuantity" type="boolean">

              Whether the line item quantity is fixed and cannot be changed.

              Default: `false`

              **Developer Preview.**

            </ParamField>

            <ParamField body="decimalQuantity" type="DecimalQuantity">

              Decimal-quantity metadata. Set when the line represents a
              non-integer purchase (e.g. 2.75 m² of fake grass).

              When set, the line item must have `fixedQuantity` = true and be set with `quantity` = 1.
              The decimal value lives in `decimalQuantity.value`.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `DecimalQuantity` object._

            </ParamField>

            <ParamField body="extendedFields" type="ExtendedFields">

              Custom extended fields for the line item object.

              [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `ExtendedFields` object._

            </ParamField>

            <ParamField body="modifierGroups" type="ModifierGroup[]">

              Modifier groups that were added to the item.

              **Read-only.** **Developer Preview.** Constraints: `maxItems 10`.

              _Nested structure omitted for brevity — see the `ModifierGroup[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="buyerInfo" type="BuyerInfo">

          Buyer information.

          <Expandable title="buyerInfo properties">

            <ParamField body="contactId" type="string">

              Contact ID. Automatically created if one doesn't exist. For more information, see [Contacts API](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts/contact-v4/introduction).

            </ParamField>

            <ParamField body="email" type="string">

              Buyer's email address.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paymentStatus" type="string">

          Order payment status.

          **Supported values:**

          - `UNSPECIFIED`
          - `NOT_PAID` — `NOT_PAID` can apply to an order made online, but not yet paid. In such cases `order.status` will be `INITIALIZED`.
          This status also applies when an offline order needs to be manually marked as paid. In such cases `order.status` will be `APPROVED`.
          - `PAID` — All required payments associated with this order are paid.
          - `PARTIALLY_REFUNDED` — Order partially refunded, but the refunded amount is less than the order's total price. See `order.balanceSummary` for more details.
          - `FULLY_REFUNDED` — Order fully refunded. Refund amount equals total price. See `order.balanceSummary` for more details.
          - `PENDING` — All payments pending.

          This can happen with two-step payments, when a payment requires manual review, or when a payment is in progress and will be concluded shortly.
          Learn more about [pending orders](https://support.wix.com/en/article/pending-orders).
          - `PARTIALLY_PAID` — At least one payment received and approved, but it covers less than the order's total price. See `order.balanceSummary` for more details.
          - `PENDING_MERCHANT` — Payment received, but not yet confirmed by the payment provider.

          In most cases, when a payment provider is holding payment it's because setup hasn't been successfully completed by the merchant/site owner.
          To solve this, the merchant/site owner should log in to the payment provider's dashboard and make sure their account is set up correctly, or contact their support for further assistance.
          - `CANCELED` — One or more payments canceled.
          - `DECLINED` — One or more payments declined.

        </ParamField>

        <ParamField body="fulfillmentStatus" type="string">

          Order fulfillment status.

          **Read-only.**

          **Supported values:**

          - `NOT_FULFILLED` — None of the order items are fulfilled or the order was manually marked as unfulfilled.
          - `FULFILLED` — All of the order items are fulfilled or the order was manually marked as fulfilled.
          Orders without shipping info are fulfilled automatically.
          - `PARTIALLY_FULFILLED` — Some, but not all, of the order items are fulfilled.

        </ParamField>

        <ParamField body="buyerLanguage" type="string">

          Language for communication with the buyer. Defaults to the site language.
          For a site that supports multiple languages, this is the language the buyer selected.

        </ParamField>

        <ParamField body="weightUnit" type="string">

          Weight measurement unit - defaults to site's weight unit.

          **Supported values:**

          - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
          - `KG` — Kilograms.
          - `LB` — Pounds.

        </ParamField>

        <ParamField body="currency" type="string">

          Currency used for the pricing of this order in [ISO-4217](https://en.wikipedia.org/wiki/ISO_4217#List_of_ISO_4217_currency_codes) format.

          Default: The site's currency.

        </ParamField>

        <ParamField body="currencyConversionDetails" type="CurrencyConversionDetails">

          Currency conversion details. For use with multi-currency sites.

          **Developer Preview.**

          <Expandable title="currencyConversionDetails properties">

            <ParamField body="originalCurrency" type="string">

              Currency used for the pricing of this order, in [ISO-4217](https://en.wikipedia.org/wiki/ISO_4217#List_of_ISO_4217_currency_codes) format.

              This currency is used to calculate the pricing before conversion to the buyer's payment currency.

              **Read-only.**

            </ParamField>

            <ParamField body="conversionRate" type="string">

              The rate used for converting the original currency to the currency used for payment.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="taxIncludedInPrices" type="boolean">

          Whether tax is included in line item prices.

          Constraints: `immutable`.

        </ParamField>

        <ParamField body="siteLanguage" type="string">

          Site language in which original values are shown.

          **Read-only.**

        </ParamField>

        <ParamField body="priceSummary" type="PriceSummary">

          Order price summary.

          **Read-only.**

          <Expandable title="priceSummary properties">

            <ParamField body="subtotal" type="Price">

              Subtotal of all line items, before discounts and before tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="shipping" type="Price">

              Total shipping price, before discounts and before tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="tax" type="Price">

              Total tax applied to the order.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="discount" type="Price">

              Total discount amount applied to the order.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="total" type="Price">

              Order's total price after discounts and tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="totalAdditionalFees" type="Price">

              Total additional fees before tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="billingInfo" type="AddressWithContact">

          Billing address and contact details.

          <Expandable title="billingInfo properties">

            <ParamField body="address" type="Address">

              Address.

              _Nested structure omitted for brevity — see the `Address` object._

            </ParamField>

            <ParamField body="contactDetails" type="FullAddressContactDetails">

              Contact details.

              _Nested structure omitted for brevity — see the `FullAddressContactDetails` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="shippingInfo" type="ShippingInformation">

          Shipping info and selected shipping option details.

          <Expandable title="shippingInfo properties">

            <ParamField body="carrierId" type="string">

              App Def Id of external provider which was a source of shipping info

            </ParamField>

            <ParamField body="code" type="string">

              Unique code (or ID) of selected shipping option. For example, `"usps_std_overnight"`.

            </ParamField>

            <ParamField body="title" type="string">

              Shipping option title.
              For example, `"USPS Standard Overnight Delivery"`, `"Standard"` or `"First-Class Package International"`.

              Constraints: `maxLength 250`.

            </ParamField>

            <ParamField body="logistics" type="DeliveryLogistics">

              Shipping logistics.

              _Nested structure omitted for brevity — see the `DeliveryLogistics` object._

            </ParamField>

            <ParamField body="cost" type="ShippingPrice">

              Shipping costs.

              _Nested structure omitted for brevity — see the `ShippingPrice` object._

            </ParamField>

            <ParamField body="region" type="ShippingRegion">

              Shipping region.

              _Nested structure omitted for brevity — see the `ShippingRegion` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="buyerNote" type="string">

          [Buyer note](https://support.wix.com/en/article/wix-stores-viewing-buyer-notes) left by the customer.

        </ParamField>

        <ParamField body="status" type="string">

          Order status.

          **Supported values:**

          - `INITIALIZED` — Order created, but not yet approved or canceled.
          - `APPROVED` — Order approved.

          This happens when either an online payment is received **or** when `priceSummary.total` is `0` (a zero-total order).
          Offline orders (cash payment) are automatically approved.
          - `CANCELED` — Order canceled by the merchant or buyer.
          - `PENDING` — Order is waiting for the buyer to complete checkout.
          - `REJECTED` — Order rejected.

          This happens when pending payments fail.

        </ParamField>

        <ParamField body="archived" type="boolean">

          Whether the order is archived. Archived orders are hidden from the main orders view in the dashboard, but remain retrievable through the API.

          To archive or unarchive an order, call [Update Order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/orders/update-order) and set `archived` to `true` or `false`.

          Default: `false`

        </ParamField>

        <ParamField body="taxSummary" type="TaxSummary" deprecated>

          Tax summary.
          Deprecated. Use `taxInfo` instead.
          This field will be removed on September 30, 2024.

          **Deprecated.** Tax summary.
          Deprecated. Use `taxInfo` instead.
          This field will be removed on September 30, 2024. Use `tax_info` instead. Removal: 2024-09-30.

          <Expandable title="taxSummary properties">

            <ParamField body="totalTax" type="Price">

              Total tax.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="taxInfo" type="OrderTaxInfo">

          Tax information.

          <Expandable title="taxInfo properties">

            <ParamField body="totalTax" type="Price">

              Calculated tax, added from line items.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="taxBreakdown" type="OrderTaxBreakdown[]">

              The summary of the tax breakdown for all the line items. It will hold for each tax name, the aggregated tax amount paid for it and the tax rate.

              Constraints: `minItems 0`, `maxItems 50`.

              _Nested structure omitted for brevity — see the `OrderTaxBreakdown[]` object._

            </ParamField>

            <ParamField body="taxExempt" type="boolean">

              Whether the order is exempt from tax calculations.

              Default: `false`

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="appliedDiscounts" type="AppliedDiscount[]">

          Applied discounts.

          Constraints: `maxItems 320`.

          <Expandable title="appliedDiscounts properties">

            <ParamField body="discountType" type="string">

              Type of discount.

              **Supported values:**

              - `GLOBAL` — Discount applies to the entire order.
              - `SPECIFIC_ITEMS` — Discount applies to specific items.
              - `SHIPPING` — Discount applies to shipping. For example, free shipping.

            </ParamField>

            <ParamField body="lineItemIds" type="string[]" deprecated>

              __Deprecated.__ Use `lineItemDiscounts` instead.

              IDs of line items the discount applies to.

              **Deprecated.** __Deprecated.__ Use `lineItemDiscounts` instead.

              IDs of line items the discount applies to. Use `line_item_discounts` instead. Removal: 2024-10-30. Constraints: `format GUID`, `minItems 0`, `maxItems 300`.

            </ParamField>

            <ParamField body="id" type="string">

              Discount ID.

            </ParamField>

            <ParamField body="lineItemDiscounts" type="LineItemDiscount[]">

              Line items the discount applies to, including the discount amount for each.

              **Developer Preview.** Constraints: `maxItems 300`.

              _Nested structure omitted for brevity — see the `LineItemDiscount[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="activities" type="Activity[]">

          Order activities.

          **Read-only.** Constraints: `maxItems 1000`.

          <Expandable title="activities properties">

            <ParamField body="id" type="string">

              Activity ID.

              **Read-only.**

            </ParamField>

            <ParamField body="authorEmail" type="string">

              Activity author's email.

              **Read-only.**

            </ParamField>

            <ParamField body="createdDate" type="datetime">

              Activity creation date and time.

              **Read-only.**

            </ParamField>

            <ParamField body="type" type="string" deprecated>

              Activity type.

              **Deprecated.** Activity type. Use `activity_type` instead. Removal: 2026-03-01.

              **Supported values:**

              - `ORDER_REFUNDED` — Order was refunded, either partially or fully.
              - `ORDER_PLACED` — New order was created and placed.
              - `ORDER_PAID` — Order payment was completed and confirmed.
              - `ORDER_FULFILLED` — Order items were marked as fulfilled (shipped or ready for pickup).
              - `ORDER_NOT_FULFILLED` — Order fulfillment status was changed to not fulfilled.
              - `ORDER_CANCELED` — Order was canceled by merchant or customer.
              - `DOWNLOAD_LINK_SENT` — Download link was sent for digital items in the order.
              - `TRACKING_NUMBER_ADDED` — Shipping tracking number was added to the order.
              - `TRACKING_NUMBER_EDITED` — Existing shipping tracking number was modified.
              - `TRACKING_LINK_ADDED` — Tracking link for shipment was added to the order.
              - `SHIPPING_CONFIRMATION_EMAIL_SENT` — Email confirming shipment was sent to the customer.
              - `INVOICE_ADDED` — Invoice document was added to the order.
              - `INVOICE_REMOVED` — Invoice was removed from the order.
              - `INVOICE_SENT` — Invoice was sent to the customer via email.
              - `FULFILLER_EMAIL_SENT` — Notification email was sent to the order fulfiller.
              - `SHIPPING_ADDRESS_EDITED` — Order's shipping address was modified.
              - `EMAIL_EDITED` — Order's contact email address was changed.
              - `PICKUP_READY_EMAIL_SENT` — Email notification for pickup readiness was sent.
              - `CUSTOM_ACTIVITY` — Custom activity created by a third-party application.
              - `MERCHANT_COMMENT` — Comment added to the order by a merchant.
              - `ORDER_PARTIALLY_PAID` — Partial payment was received for the order.
              - `DRAFT_ORDER_CHANGES_APPLIED` — Changes were applied to a draft order.
              - `SAVED_PAYMENT_METHOD` — Payment method was saved for future use.
              - `AUTHORIZED_PAYMENT_CREATED` — Payment was authorized but not yet captured.
              - `AUTHORIZED_PAYMENT_CAPTURED` — Previously authorized payment was captured.
              - `AUTHORIZED_PAYMENT_VOIDED` — Previously authorized payment was voided.
              - `REFUND_INITIATED` — Refund process was initiated.
              - `PAYMENT_REFUNDED` — Payment was successfully refunded.
              - `PAYMENT_REFUND_FAILED` — Attempt to refund payment failed.
              - `REFUNDED_AS_STORE_CREDIT` — Refund was issued as store credit.
              - `PAYMENT_PENDING` — Payment is awaiting processing or confirmation.
              - `PAYMENT_CANCELED` — Payment was canceled before completion.
              - `PAYMENT_DECLINED` — Payment was declined by payment provider.
              - `ORDER_PENDING` — Order is awaiting approval or processing.
              - `ORDER_REJECTED` — Order was rejected by merchant.
              - `RECEIPT_CREATED` — Receipt was generated for a payment.
              - `RECEIPT_SENT` — Receipt was sent to the customer.
              - `CHARGEBACK_CREATED` — Chargeback was created for one of the order's payments.
              - `CHARGEBACK_REVERSED` — Chargeback was resolved in merchant's favor.

            </ParamField>

            <ParamField body="activityType" type="string">

              Activity type.

              **Supported values:**

              - `ORDER_PLACED` — New order was created and placed.
              - `ORDER_PAID` — Order payment was completed and confirmed.
              - `ORDER_FULFILLED` — Order items were marked as fulfilled (shipped or ready for pickup).
              - `ORDER_NOT_FULFILLED` — Order fulfillment status was changed to not fulfilled.
              - `ORDER_CANCELED` — Order was canceled by merchant or customer.
              - `DOWNLOAD_LINK_SENT` — Download link was sent for digital items in the order.
              - `TRACKING_NUMBER_ADDED` — Shipping tracking number was added to the order.
              - `TRACKING_NUMBER_EDITED` — Existing shipping tracking number was modified.
              - `TRACKING_LINK_ADDED` — Tracking link for shipment was added to the order.
              - `SHIPPING_CONFIRMATION_EMAIL_SENT` — Email confirming shipment was sent to the customer.
              - `INVOICE_ADDED` — Invoice document was added to the order.
              - `INVOICE_REMOVED` — Invoice was removed from the order.
              - `INVOICE_SENT` — Invoice was sent to the customer via email.
              - `FULFILLER_EMAIL_SENT` — Notification email was sent to the order fulfiller.
              - `SHIPPING_ADDRESS_EDITED` — Order's shipping address was modified.
              - `EMAIL_EDITED` — Order's contact email address was changed.
              - `PICKUP_READY_EMAIL_SENT` — Email notification for pickup readiness was sent.
              - `MERCHANT_COMMENT` — Comment added to the order by a merchant.
              - `ORDER_PARTIALLY_PAID` — Partial payment was received for the order.
              - `DRAFT_ORDER_CHANGES_APPLIED` — Changes were applied to a draft order.
              - `SAVED_PAYMENT_METHOD` — Payment method was saved for future use.
              - `AUTHORIZED_PAYMENT_CREATED` — Payment was authorized but not yet captured.
              - `AUTHORIZED_PAYMENT_CAPTURED` — Previously authorized payment was captured.
              - `AUTHORIZED_PAYMENT_VOIDED` — Previously authorized payment was voided.
              - `REFUND_INITIATED` — Refund process was initiated.
              - `PAYMENT_REFUNDED` — Payment was successfully refunded.
              - `PAYMENT_REFUND_FAILED` — Attempt to refund payment failed.
              - `REFUNDED_AS_STORE_CREDIT` — Refund was issued as store credit.
              - `PAYMENT_PENDING` — Payment is awaiting processing or confirmation.
              - `PAYMENT_CANCELED` — Payment was canceled before completion.
              - `PAYMENT_DECLINED` — Payment was declined by payment provider.
              - `ORDER_PENDING` — Order is awaiting approval or processing.
              - `ORDER_REJECTED` — Order was rejected by merchant.
              - `RECEIPT_CREATED` — Receipt was generated for a payment.
              - `RECEIPT_SENT` — Receipt was sent to the customer.
              - `CHARGEBACK_CREATED` — Chargeback was created for one of the order's payments.
              - `CHARGEBACK_REVERSED` — Chargeback was resolved in merchant's favor.
              - `ORDER_REFUNDED` — Order was refunded, either partially or fully.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="attributionSource" type="string">

          Order attribution source.

          **Supported values:**

          - `UNSPECIFIED`
          - `FACEBOOK_ADS`

        </ParamField>

        <ParamField body="createdBy" type="CreatedBy">

          ID of the order's initiator.

          **Read-only.**

          <Expandable title="One of the following">

            <ParamField body="string" type="string">

              string

              _Nested structure omitted for brevity — see the `string` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="channelInfo" type="ChannelInfo">

          Information about the sales channel that submitted this order.

          <Expandable title="channelInfo properties">

            <ParamField body="type" type="string">

              Sales channel that submitted the order.

              When creating an order via the API to record an external order, use the channel type that matches the original source. If no matching type exists, use `OTHER_PLATFORM`.

              **Supported values:**

              - `UNSPECIFIED` — Unspecified sales channel. This value is not supported.
              - `WEB` — A web client.
              - `POS` — [Point of sale solutions](https://support.wix.com/en/wix-mobile-pos-2196395).
              - `EBAY` — [eBay shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-ebay-shop).
              - `AMAZON` — [Amazon shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-amazon-shop).
              - `OTHER_PLATFORM` — Other sales platform.
              - `WIX_APP_STORE` — [Wix Owner app](https://support.wix.com/article/wix-owner-app-an-overview).
              - `WIX_INVOICES` — Wix Invoices app in [your dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Select%20Site&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Finvoices/settings/general-settings)
              - `BACKOFFICE_MERCHANT` — Wix merchant backoffice.
              - `WISH` — Wish sales channel.
              - `CLASS_PASS` — [ClassPass sales channel](https://support.wix.com/en/article/wix-bookings-letting-clients-book-your-services-with-classpass).
              - `GLOBAL_E` — Global-E sales channel.
              - `FACEBOOK` — [Facebook shop](https://support.wix.com/en/article/wix-stores-changes-to-facebook-shops).
              - `ETSY` — [Etsy sales channel](https://support.wix.com/en/article/wix-stores-request-adding-etsy-as-a-sales-channel).
              - `TIKTOK` — [TikTok sales channel](https://support.wix.com/en/article/wix-stores-request-adding-tiktok-as-a-sales-channel).
              - `FAIRE_COM` — [Faire marketplace integration](https://support.wix.com/en/article/wix-stores-creating-a-faire-store-using-the-faire-integration-app).
              - `PAYPAL_AGENTIC_CHECKOUT` — PayPal Agentic Checkout sales channel.
              - `STRIPE_AGENTIC_CHECKOUT` — Stripe Agentic Checkout sales channel.

            </ParamField>

            <ParamField body="externalOrderId" type="string">

              Reference to an order ID from an external system. Relevant when recording orders from external platforms.

            </ParamField>

            <ParamField body="externalOrderUrl" type="string">

              URL to the order in the external system. Relevant when recording orders from external platforms.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="seenByAHuman" type="boolean">

          Whether a human has seen the order. Set when an order is clicked on in the dashboard.

        </ParamField>

        <ParamField body="checkoutId" type="string">

          Checkout ID.

        </ParamField>

        <ParamField body="customFields" type="CustomField[]">

          Custom fields.

          <Expandable title="customFields properties">

            <ParamField body="value" type="any">

              Custom field value.

            </ParamField>

            <ParamField body="title" type="string">

              Custom field title.

              Constraints: `minLength 1`, `maxLength 500`.

            </ParamField>

            <ParamField body="translatedTitle" type="string">

              Translated custom field title.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="balanceSummary" type="BalanceSummary">

          Balance summary.

          **Read-only.**

          <Expandable title="balanceSummary properties">

            <ParamField body="balance" type="Balance">

              Current amount left to pay.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Balance` object._

            </ParamField>

            <ParamField body="paid" type="Price">

              Sum of all approved and successful payments.

              The value includes payments that have subsequently been fully or partially refunded.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="refunded" type="Price">

              Sum of all successfully refunded payments.

              **Read-only.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="authorized" type="Price">

              Sum of all authorized payments.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="pendingRefund" type="Price">

              Sum of all pending refund transactions.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="pending" type="Price">

              Sum of all pending transactions.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="chargeback" type="Price">

              Sum of all transaction chargebacks.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="chargebackReversal" type="Price">

              Sum of all chargeback reversals.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="platformFees" type="Price">

              Total platform fees actually charged across all payments.
              This reflects amounts collected, which may differ from the expected fees in `order.platformFeeSummary`.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="totalMinusPlatformFees" type="Price">

              Order total after deducting platform fees. Represents the net amount payable to the merchant.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="cashRoundingAdjustment" type="CashRounding">

              The total adjustment applied to the cash payment due to rounding.

              Positive values indicate the total was rounded up; negative values indicate it was rounded down.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `CashRounding` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="additionalFees" type="AdditionalFee[]">

          Additional fees applied to the order.

          Constraints: `maxItems 100`.

          <Expandable title="additionalFees properties">

            <ParamField body="code" type="string">

              Additional fee's unique code for future processing.

            </ParamField>

            <ParamField body="name" type="string">

              Name of additional fee.

              Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

            <ParamField body="price" type="Price">

              Additional fee's price.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="taxDetails" type="ItemTaxFullDetails" deprecated>

              Tax details.

              **Deprecated.** Tax details. Use `tax_info` instead. Removal: 2026-03-30.

              _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

            </ParamField>

            <ParamField body="taxInfo" type="LineItemTaxInfo">

              Represents all the relevant tax details for additional fee.

              _Nested structure omitted for brevity — see the `LineItemTaxInfo` object._

            </ParamField>

            <ParamField body="providerAppId" type="string">

              SPI implementer's `appId`.

            </ParamField>

            <ParamField body="priceBeforeTax" type="Price">

              Additional fee's price before tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="priceAfterTax" type="Price">

              Additional fee's price after tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="id" type="string">

              Additional fee's id.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="lineItemIds" type="string[]">

              Optional - Line items associated with this additional fee.
              If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="source" type="string">

              Specifies the entity that added the additional fee.

              **Developer Preview.**

              **Supported values:**

              - `SERVICE_PLUGIN` — The additional fee was added by an additional fee service plugin.
              - `ITEM` — The additional fee was added to the item by a catalog or custom line item.
              - `MANUAL` — The additional fee was added manually on request.
              - `SHIPPING` — The additional fee was added by the shipping provider.
              - `PLATFORM` — The additional fee was added by the Wix eCommerce platform.

            </ParamField>

            <ParamField body="taxableAddress" type="TaxableAddress">

              Address type tax was calculated against for this fee.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `TaxableAddress` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom field data for the order object.

          [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="purchaseFlowId" type="string">

          Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

        </ParamField>

        <ParamField body="recipientInfo" type="AddressWithContact">

          Final recipient's address and contact details.

          This field represents who will ultimately receive the order. It may differ from `shippingInfo.logistics.shippingDestination` when:
          + The chosen shipping option is a pickup point or store pickup, where `shippingDestination` contains the pickup location.
          + No shipping option is selected.

          <Expandable title="recipientInfo properties">

            <ParamField body="address" type="Address">

              Address.

              _Nested structure omitted for brevity — see the `Address` object._

            </ParamField>

            <ParamField body="contactDetails" type="FullAddressContactDetails">

              Contact details.

              _Nested structure omitted for brevity — see the `FullAddressContactDetails` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Order tags.

          [Tags](https://dev.wix.com/docs/rest/business-management/tags/introduction) are labels attached to entities, allowing for flexible categorization and data management.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="purchasedDate" type="datetime">

          Date and time the order was originally purchased in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.
          Used for migration from external systems.

        </ParamField>

        <ParamField body="businessLocation" type="Location">

          Order location.

          **Developer Preview.**

          <Expandable title="businessLocation properties">

            <ParamField body="id" type="string">

              Location ID.
              Learn more about the [Wix Locations API](https://dev.wix.com/docs/rest/business-management/locations/introduction).

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="name" type="string">

              Location name.

              **Read-only.** Constraints: `minLength 1`, `maxLength 500`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="payAfterFreeTrial" type="PriceSummary">

          Pay after free trial price summary. Price summary that should be paid for subscriptions after free trial period.

          **Read-only.** **Developer Preview.**

          <Expandable title="payAfterFreeTrial properties">

            <ParamField body="subtotal" type="Price">

              Subtotal of all line items, before discounts and before tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="shipping" type="Price">

              Total shipping price, before discounts and before tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="tax" type="Price">

              Total tax applied to the order.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="discount" type="Price">

              Total discount amount applied to the order.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="total" type="Price">

              Order's total price after discounts and tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="totalAdditionalFees" type="Price">

              Total additional fees before tax.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="platformFeeSummary" type="PlatformFeeSummary">

          Summary of platform fees for this order, including totals by charge type and a breakdown of individual fees.

          **Developer Preview.**

          <Expandable title="platformFeeSummary properties">

            <ParamField body="total" type="Price">

              Total sum of all platform fees.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="totalPassOn" type="Price">

              Total amount of platform fees with `PASS_ON` charge type.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="totalAbsorbed" type="Price">

              Total amount of platform fees with `ABSORBED` charge type.

              _Nested structure omitted for brevity — see the `Price` object._

            </ParamField>

            <ParamField body="fees" type="PlatformFee[]">

              Specific information about each platform fee.

              Constraints: `maxItems 300`.

              _Nested structure omitted for brevity — see the `PlatformFee[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="imported" type="boolean">

          Whether the order was imported from an external system using the Order Import API.

          When `true`, the order originated outside of Wix (for example, during a migration from another
          eCommerce platform) rather than being placed through the Wix checkout flow or created manually.
          Imported orders trigger the dedicated `OrderImported` and `ImportedOrderDeleted` domain events.

          Set automatically at import time and cannot be changed afterwards.

          Default: `false`

          **Read-only.** **Developer Preview.** Constraints: `immutable`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="paymentSettings" type="PaymentSettings">

      Retrieved payment settings.

      <Expandable title="paymentSettings properties">

        <ResponseField name="requires3dSecure" type="boolean">

          Whether to apply [3D Secure](https://support.wix.com/en/article/about-3d-secure-3ds-payments-with-third-party-payment-providers) during the payment process.

          > __Note:__
          > + Not all payment providers offer this feature in their Wix integration.
          > + If the service plugin call fails, the value set in the [extension configuration](https://dev.wix.com/docs/rest/api-reference/wix-e-commerce/payment-settings-integration-spi/introduction#configuration) for `fallbackValueForRequires3dSecure` will be used.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    The data payload will include the following object as an encoded JWT. For the purposes of this example, we show the request and response objects decoded.

    <CodeGroup>

      ```bash Request
      curl -X POST https://provider.example.com/v1/payment-settings \
        -H 'user-agent: Wix' \
        -H 'accept-encoding: gzip, deflate' \
        -H 'content-type: text/plain; charset=utf-8' \
        -d '{
              "order": {
               "activities": [
                {
                 "createdDate": "2023-09-10T09:03:06.546Z",
                 "type": "ORDER_PLACED"
                }
               ],
               "additionalFees": [
               ],
               "archived": false,
               "balanceSummary": {
                "balance": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                },
                "paid": {
                 "amount": "0",
                 "formattedAmount": "$0.00"
                },
                "refunded": {
                 "amount": "0",
                 "formattedAmount": "$0.00"
                }
               },
               "billingInfo": {
                "address": {
                 "addressLine": "Aahar Indian Cuisine",
                 "city": "Farmington Hills",
                 "country": "US",
                 "countryFullname": "United States",
                 "postalCode": "48335-3123",
                 "subdivision": "US-MI",
                 "subdivisionFullname": "Michigan"
                },
                "contactDetails": {
                 "firstName": "aa",
                 "lastName": "aa",
                 "phone": "012345678"
                }
               },
               "buyerInfo": {
                "email": "ba1a18ca30bf6289323070a751094c881d57c8948ca94b8a8d1dfa126d2cb9ab",
                "visitorId": "cbc62c47-5aed-4949-8195-f7e6d02211ec"
               },
               "buyerLanguage": "en",
               "channelInfo": {
                "type": "WEB"
               },
               "checkoutId": "d28158fd-ebcd-4911-b1d5-964aa99ebba9",
               "createdBy": {
                "visitorId": "cbc62c47-5aed-4949-8195-f7e6d02211ec"
               },
               "createdDate": "2023-09-10T09:03:06.546Z",
               "currency": "USD",
               "id": "3005ef53-df05-4ab8-84c8-b942884a7ee1",
               "lineItems": [
                {
                 "catalogReference": {
                  "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                  "catalogItemId": "1b2b47bf-c162-dafc-f68a-c37eddcc208f",
                  "options": {
                   "customTextFields": {},
                   "variantId": "00000000-0000-0000-0000-000000000000"
                  }
                 },
                 "id": "00000000-0000-0000-0000-000000000001",
                 "image": {
                  "height": 1980,
                  "id": "c837a6_fdb039110b784858933d038f14f78b1c~mv2.jpg",
                  "url": "https://static.wixstatic.com/media/c837a6_fdb039110b784858933d038f14f78b1c~mv2.jpg/v1/fit/w_1796,h_1980,q_90/file.jpg",
                  "width": 1796
                 },
                 "itemType": {
                  "preset": "PHYSICAL"
                 },
                 "physicalProperties": {
                  "shippable": true,
                  "sku": "0007"
                 },
                 "price": {
                  "amount": "15.00",
                  "formattedAmount": "$15.00"
                 },
                 "priceBeforeDiscounts": {
                  "amount": "15.00",
                  "formattedAmount": "$15.00"
                 },
                 "productName": {
                  "original": "I'm a product",
                  "translated": "I'm a product"
                 },
                 "quantity": 1,
                 "refundQuantity": 0,
                 "taxDetails": {
                  "taxRate": "0",
                  "taxableAmount": {
                   "amount": "15.00",
                   "formattedAmount": "$15.00"
                  },
                  "totalTax": {
                   "amount": "0.0",
                   "formattedAmount": "$0.00"
                  }
                 },
                 "totalDiscount": {
                  "amount": "0",
                  "formattedAmount": "$0.00"
                 },
                 "totalPriceAfterTax": {
                  "amount": "15.00",
                  "formattedAmount": "$15.00"
                 },
                 "totalPriceBeforeTax": {
                  "amount": "15.00",
                  "formattedAmount": "$15.00"
                 }
                }
               ],
               "payNow": {
                "discount": {
                 "amount": "0",
                 "formattedAmount": "$0.00"
                },
                "shipping": {
                 "amount": "5.0",
                 "formattedAmount": "$5.00"
                },
                "subtotal": {
                 "amount": "15.00",
                 "formattedAmount": "$15.00"
                },
                "tax": {
                 "amount": "0.0",
                 "formattedAmount": "$0.00"
                },
                "total": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                },
                "totalAdditionalFees": {
                 "amount": "0",
                 "formattedAmount": "$0.00"
                },
                "totalPrice": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                },
                "totalWithGiftCard": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                },
                "totalWithoutGiftCard": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                }
               },
               "paymentStatus": "NOT_PAID",
               "priceSummary": {
                "discount": {
                 "amount": "0",
                 "formattedAmount": "$0.00"
                },
                "shipping": {
                 "amount": "5.0",
                 "formattedAmount": "$5.00"
                },
                "subtotal": {
                 "amount": "15.00",
                 "formattedAmount": "$15.00"
                },
                "tax": {
                 "amount": "0.0",
                 "formattedAmount": "$0.00"
                },
                "total": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                },
                "totalAdditionalFees": {
                 "amount": "0",
                 "formattedAmount": "$0.00"
                },
                "totalPrice": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                },
                "totalWithGiftCard": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                },
                "totalWithoutGiftCard": {
                 "amount": "20.00",
                 "formattedAmount": "$20.00"
                }
               },
               "seenByAHuman": false,
               "shippingInfo": {
                "carrierId": "c8a08776-c095-4dec-8553-8f9698d86adc",
                "code": "bae3f4de-d759-fccb-6dff-9b49d5d5f13f",
                "cost": {
                 "discount": {
                  "amount": "0",
                  "formattedAmount": "$0.00"
                 },
                 "price": {
                  "amount": "5",
                  "formattedAmount": "$5.00"
                 },
                 "taxDetails": {
                  "taxRate": "0",
                  "totalTax": {
                   "amount": "0.0",
                   "formattedAmount": "$0.00"
                  }
                 },
                 "totalPriceAfterTax": {
                  "amount": "5.0",
                  "formattedAmount": "$5.00"
                 },
                 "totalPriceBeforeTax": {
                  "amount": "5",
                  "formattedAmount": "$5.00"
                 }
                },
                "logistics": {
                 "deliveryTime": "",
                 "shippingDestination": {
                  "address": {
                   "addressLine": "Aahar Indian Cuisine",
                   "city": "Farmington Hills",
                   "country": "US",
                   "countryFullname": "United States",
                   "postalCode": "48335-3123",
                   "subdivision": "US-MI",
                   "subdivisionFullname": "Michigan"
                  },
                  "contactDetails": {
                   "firstName": "aa",
                   "lastName": "aa",
                   "phone": "012345678"
                  }
                 }
                },
                "region": {
                 "name": "Region 3"
                },
                "title": "Standard Shipping"
               },
               "taxSummary": {
                "totalTax": {
                 "amount": "0.0",
                 "formattedAmount": "$0.00"
                }
               },
               "updatedDate": "2023-09-10T09:03:06.790Z",
               "weightUnit": "KG"
              }
             }'
      ```

      ```json Response
      {
        "paymentSettings": {
          "requires3dSecure": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    This method retrieves payment settings from your app.

    Wix calls this method during the payment process. For example, when a customer inserts credit card details and places an order.
    This method retrieves the payment settings to apply, and Wix passes on the settings to the payment provider.

    ## Method Declaration

    ```typescript
    import { paymentSettings } from '@wix/ecom';

    paymentSettings.getPaymentSettings(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="GetPaymentSettingsRequest">

      request

      <Expandable title="request properties">

        <ParamField body="order" type="Order">

          Order.

          <Expandable title="order properties">

            <ParamField body="_id" type="string">

              Order ID.

              **Read-only.**

            </ParamField>

            <ParamField body="number" type="number">

              Order number displayed in the site owner's dashboard.

              Auto-generated for orders created through the regular order flow. When importing an order, you can provide the original number to preserve it. See [Set Order Number Counter](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/orders/set-order-number-counter) to control numbering of orders created after a migration.

              **Read-only.**

            </ParamField>

            <ParamField body="_createdDate" type="datetime">

              Date and time the order was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

              **Read-only.**

            </ParamField>

            <ParamField body="_updatedDate" type="datetime">

              Date and time the order was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

              **Read-only.**

            </ParamField>

            <ParamField body="lineItems" type="OrderLineItem[]">

              Order line items.

              **Read-only.** Constraints: `minItems 1`, `maxItems 300`.

              _Nested structure omitted for brevity — see the `OrderLineItem[]` object._

            </ParamField>

            <ParamField body="buyerInfo" type="BuyerInfo">

              Buyer information.

              _Nested structure omitted for brevity — see the `BuyerInfo` object._

            </ParamField>

            <ParamField body="paymentStatus" type="string">

              Order payment status.

              **Supported values:**

              - `UNSPECIFIED`
              - `NOT_PAID` — `NOT_PAID` can apply to an order made online, but not yet paid. In such cases `order.status` will be `INITIALIZED`.
              This status also applies when an offline order needs to be manually marked as paid. In such cases `order.status` will be `APPROVED`.
              - `PAID` — All required payments associated with this order are paid.
              - `PARTIALLY_REFUNDED` — Order partially refunded, but the refunded amount is less than the order's total price. See `order.balanceSummary` for more details.
              - `FULLY_REFUNDED` — Order fully refunded. Refund amount equals total price. See `order.balanceSummary` for more details.
              - `PENDING` — All payments pending.

              This can happen with two-step payments, when a payment requires manual review, or when a payment is in progress and will be concluded shortly.
              Learn more about [pending orders](https://support.wix.com/en/article/pending-orders).
              - `PARTIALLY_PAID` — At least one payment received and approved, but it covers less than the order's total price. See `order.balanceSummary` for more details.
              - `PENDING_MERCHANT` — Payment received, but not yet confirmed by the payment provider.

              In most cases, when a payment provider is holding payment it's because setup hasn't been successfully completed by the merchant/site owner.
              To solve this, the merchant/site owner should log in to the payment provider's dashboard and make sure their account is set up correctly, or contact their support for further assistance.
              - `CANCELED` — One or more payments canceled.
              - `DECLINED` — One or more payments declined.

            </ParamField>

            <ParamField body="fulfillmentStatus" type="string">

              Order fulfillment status.

              **Read-only.**

              **Supported values:**

              - `NOT_FULFILLED` — None of the order items are fulfilled or the order was manually marked as unfulfilled.
              - `FULFILLED` — All of the order items are fulfilled or the order was manually marked as fulfilled.
              Orders without shipping info are fulfilled automatically.
              - `PARTIALLY_FULFILLED` — Some, but not all, of the order items are fulfilled.

            </ParamField>

            <ParamField body="buyerLanguage" type="string">

              Language for communication with the buyer. Defaults to the site language.
              For a site that supports multiple languages, this is the language the buyer selected.

            </ParamField>

            <ParamField body="weightUnit" type="string">

              Weight measurement unit - defaults to site's weight unit.

              **Supported values:**

              - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
              - `KG` — Kilograms.
              - `LB` — Pounds.

            </ParamField>

            <ParamField body="currency" type="string">

              Currency used for the pricing of this order in [ISO-4217](https://en.wikipedia.org/wiki/ISO_4217#List_of_ISO_4217_currency_codes) format.

              Default: The site's currency.

            </ParamField>

            <ParamField body="currencyConversionDetails" type="CurrencyConversionDetails">

              Currency conversion details. For use with multi-currency sites.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `CurrencyConversionDetails` object._

            </ParamField>

            <ParamField body="taxIncludedInPrices" type="boolean">

              Whether tax is included in line item prices.

              Constraints: `immutable`.

            </ParamField>

            <ParamField body="siteLanguage" type="string">

              Site language in which original values are shown.

              **Read-only.**

            </ParamField>

            <ParamField body="priceSummary" type="PriceSummary">

              Order price summary.

              **Read-only.**

              _Nested structure omitted for brevity — see the `PriceSummary` object._

            </ParamField>

            <ParamField body="billingInfo" type="AddressWithContact">

              Billing address and contact details.

              _Nested structure omitted for brevity — see the `AddressWithContact` object._

            </ParamField>

            <ParamField body="shippingInfo" type="ShippingInformation">

              Shipping info and selected shipping option details.

              _Nested structure omitted for brevity — see the `ShippingInformation` object._

            </ParamField>

            <ParamField body="buyerNote" type="string">

              [Buyer note](https://support.wix.com/en/article/wix-stores-viewing-buyer-notes) left by the customer.

            </ParamField>

            <ParamField body="status" type="string">

              Order status.

              **Supported values:**

              - `INITIALIZED` — Order created, but not yet approved or canceled.
              - `APPROVED` — Order approved.

              This happens when either an online payment is received **or** when `priceSummary.total` is `0` (a zero-total order).
              Offline orders (cash payment) are automatically approved.
              - `CANCELED` — Order canceled by the merchant or buyer.
              - `PENDING` — Order is waiting for the buyer to complete checkout.
              - `REJECTED` — Order rejected.

              This happens when pending payments fail.

            </ParamField>

            <ParamField body="archived" type="boolean">

              Whether the order is archived. Archived orders are hidden from the main orders view in the dashboard, but remain retrievable through the API.

              To archive or unarchive an order, call [Update Order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/orders/update-order) and set `archived` to `true` or `false`.

              Default: `false`

            </ParamField>

            <ParamField body="taxSummary" type="TaxSummary" deprecated>

              Tax summary.
              Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024.

              **Deprecated.** Tax summary.
              Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024. Use `tax_info` instead. Removal: 2024-09-30.

              _Nested structure omitted for brevity — see the `TaxSummary` object._

            </ParamField>

            <ParamField body="taxInfo" type="OrderTaxInfo">

              Tax information.

              _Nested structure omitted for brevity — see the `OrderTaxInfo` object._

            </ParamField>

            <ParamField body="appliedDiscounts" type="AppliedDiscount[]">

              Applied discounts.

              Constraints: `maxItems 320`.

              _Nested structure omitted for brevity — see the `AppliedDiscount[]` object._

            </ParamField>

            <ParamField body="activities" type="Activity[]">

              Order activities.

              **Read-only.** Constraints: `maxItems 1000`.

              _Nested structure omitted for brevity — see the `Activity[]` object._

            </ParamField>

            <ParamField body="attributionSource" type="string">

              Order attribution source.

              **Supported values:**

              - `UNSPECIFIED`
              - `FACEBOOK_ADS`

            </ParamField>

            <ParamField body="createdBy" type="CreatedBy">

              ID of the order's initiator.

              **Read-only.**

              _Nested structure omitted for brevity — see the `CreatedBy` object._

            </ParamField>

            <ParamField body="channelInfo" type="ChannelInfo">

              Information about the sales channel that submitted this order.

              _Nested structure omitted for brevity — see the `ChannelInfo` object._

            </ParamField>

            <ParamField body="seenByAHuman" type="boolean">

              Whether a human has seen the order. Set when an order is clicked on in the dashboard.

            </ParamField>

            <ParamField body="checkoutId" type="string">

              Checkout ID.

            </ParamField>

            <ParamField body="customFields" type="CustomField[]">

              Custom fields.

              _Nested structure omitted for brevity — see the `CustomField[]` object._

            </ParamField>

            <ParamField body="balanceSummary" type="BalanceSummary">

              Balance summary.

              **Read-only.**

              _Nested structure omitted for brevity — see the `BalanceSummary` object._

            </ParamField>

            <ParamField body="additionalFees" type="AdditionalFee[]">

              Additional fees applied to the order.

              Constraints: `maxItems 100`.

              _Nested structure omitted for brevity — see the `AdditionalFee[]` object._

            </ParamField>

            <ParamField body="extendedFields" type="ExtendedFields">

              Custom field data for the order object.

              [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.

              _Nested structure omitted for brevity — see the `ExtendedFields` object._

            </ParamField>

            <ParamField body="purchaseFlowId" type="string">

              Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

            </ParamField>

            <ParamField body="recipientInfo" type="AddressWithContact">

              Final recipient's address and contact details.

              This field represents who will ultimately receive the order. It may differ from `shippingInfo.logistics.shippingDestination` when:
              + The chosen shipping option is a pickup point or store pickup, where `shippingDestination` contains the pickup location.
              + No shipping option is selected.

              _Nested structure omitted for brevity — see the `AddressWithContact` object._

            </ParamField>

            <ParamField body="tags" type="Tags">

              Order tags.

              [Tags](https://dev.wix.com/docs/rest/business-management/tags/introduction) are labels attached to entities, allowing for flexible categorization and data management.

              _Nested structure omitted for brevity — see the `Tags` object._

            </ParamField>

            <ParamField body="purchasedDate" type="datetime">

              Date and time the order was originally purchased in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.
              Used for migration from external systems.

            </ParamField>

            <ParamField body="businessLocation" type="Location">

              Order location.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `Location` object._

            </ParamField>

            <ParamField body="payAfterFreeTrial" type="PriceSummary">

              Pay after free trial price summary. Price summary that should be paid for subscriptions after free trial period.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `PriceSummary` object._

            </ParamField>

            <ParamField body="platformFeeSummary" type="PlatformFeeSummary">

              Summary of platform fees for this order, including totals by charge type and a breakdown of individual fees.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `PlatformFeeSummary` object._

            </ParamField>

            <ParamField body="imported" type="boolean">

              Whether the order was imported from an external system using the Order Import API.

              When `true`, the order originated outside of Wix (for example, during a migration from another
              eCommerce platform) rather than being placed through the Wix checkout flow or created manually.
              Imported orders trigger the dedicated `OrderImported` and `ImportedOrderDeleted` domain events.

              Set automatically at import time and cannot be changed afterwards.

              Default: `false`

              **Read-only.** **Developer Preview.** Constraints: `immutable`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="paymentSettings" type="PaymentSettings">

      Retrieved payment settings.

      <Expandable title="paymentSettings properties">

        <ResponseField name="requires3dSecure" type="boolean">

          Whether to apply [3D Secure](https://support.wix.com/en/article/about-3d-secure-3ds-payments-with-third-party-payment-providers) during the payment process.

          > __Note:__
          > + Not all payment providers offer this feature in their Wix integration.
          > + If the service plugin call fails, the value set in the [extension configuration](https://dev.wix.com/docs/rest/api-reference/wix-e-commerce/payment-settings-integration-spi/introduction#configuration) for `fallbackValueForRequires3dSecure` will be used.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-payment-settings" id="ex-sdk-get-payment-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-payment-settings" id="ex-sdk-get-payment-settings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-payment-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-payment-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Example of a paymentSettings return value</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPaymentSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-payment-settings-0"><span className="wix-picker-name">Example of a paymentSettings return value</span></label>
    <label htmlFor="ex-sdk-get-payment-settings-1"><span className="wix-picker-name">getPaymentSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Example of a paymentSettings return value
    import { paymentSettings } from '@wix/ecom/service-plugins';

    paymentSettings.provideHandlers({

      getPaymentSettings: async ( payload ) => {
        const {request, metadata} = payload;
            // Use the `request` and `metadata` received from Wix and
            // apply custom logic.
          return {
            // Return your response exactly as documented to integrate with Wix.
            // Return value example:
            paymentSettings: {
              requires3dSecure: false
            }
          }
        }
    });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPaymentSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { paymentSettings } from '@wix/ecom/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { paymentSettings },
      // Include the auth strategy and host as relevant
    });

    async function getPaymentSettings(request,metadata) {
      const response = await myWixClient.paymentSettings.getPaymentSettings(request,metadata);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
