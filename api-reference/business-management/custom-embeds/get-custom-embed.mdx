---
title: "Get Custom Embed"
description: "Retrieves a custom embed."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a custom embed.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS">SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/embeds/v1/custom-embeds/{customEmbedId}
    ```

    ## Path Params

    <ParamField path="customEmbedId" type="string" required>

      Custom embed ID.

    </ParamField>

    ## Response Object

    <ResponseField name="customEmbed" type="CustomEmbed">

      Requested custom embed.

      See the [Custom Embed Object](/api-reference/business-management/custom-embeds/custom-embed-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CUSTOM_EMBED_NOT_FOUND` | Couldn't find the custom embed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/embeds/v1/custom-embeds/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "customEmbed": {
          "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
          "revision": "1",
          "name": "Header Custom Embed",
          "enabled": true,
          "loadOnce": false,
          "domain": "myDomain.com",
          "position": "HEAD",
          "embedData": {
            "category": "ESSENTIAL",
            "html": "<script>console.log('Header code')</script>"
          },
          "pageFilter": {
            "pageIds": ["page-id-1", "page-id-2"]
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a custom embed.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS">SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { customEmbeds } from '@wix/embeds';

    customEmbeds.getCustomEmbed(customEmbedId): Promise<CustomEmbed>
    ```

    ## Method Parameters

    <ParamField body="customEmbedId" type="string" required>

      Custom embed ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<CustomEmbed>`.

    See the [Custom Embed Object](/api-reference/business-management/custom-embeds/custom-embed-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CUSTOM_EMBED_NOT_FOUND` | Couldn't find the custom embed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-custom-embed" id="ex-sdk-get-custom-embed-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-custom-embed" id="ex-sdk-get-custom-embed-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-custom-embed-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-custom-embed-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getCustomEmbed</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCustomEmbed (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-custom-embed-0"><span className="wix-picker-name">getCustomEmbed</span></label>
    <label htmlFor="ex-sdk-get-custom-embed-1"><span className="wix-picker-name">getCustomEmbed (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getCustomEmbed
    import { customEmbeds } from '@wix/embeds';

    async function getCustomEmbed(customEmbedId) {
      const response = await customEmbeds.getCustomEmbed(customEmbedId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCustomEmbed (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customEmbeds } from '@wix/embeds';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customEmbeds },
      // Include the auth strategy and host as relevant
    });

    async function getCustomEmbed(customEmbedId) {
      const response = await myWixClient.customEmbeds.getCustomEmbed(customEmbedId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
