---
title: "Update Section"
description: "Note: The Section API only works with the Wix Restaurants Menus app."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Section API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Updates a section.

    Each time a section is updated, its revision increments by 1. The existing revision must be included when updating a section. This ensures you're working with the latest section information, and it prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/restaurants/menus/v1/sections/{section.id}
    ```

    ## Path Params

    <ParamField path="section.id" type="string" required>

      Section ID.

    </ParamField>

    ## Body Params

    <ParamField body="section" type="Section" required>

      Section update.

      See the [Section Object](/api-reference/business-solutions/restaurants/menus/sections/section-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="section" type="Section">

      Updated section.

      See the [Section Object](/api-reference/business-solutions/restaurants/menus/sections/section-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Section Updated | `wix.restaurants.menus.v1.section_updated` | Triggered when a section is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH https://www.wixapis.com/restaurants/menus-section/v1/sections/70f12b79-cd30-4750-91ab-1021a16cbbfa \
      -H 'Authorization: <AUTH>' \
      --data-binary '{
            "section": {
              "itemIds": [
                "f6222bdc-1ab2-4cd4-bf6c-8d4fca055d86",
                "c47a77e5-1df6-4c95-a04f-7cd8bb42da33"
              ],
              "additionalImages": [],
              "id": "70f12b79-cd30-4750-91ab-1021a16cbbfa",
              "name": "Drinks",
              "revision": "1"
            },
            "mask": {
              "paths": [
                "itemIds"
              ]
            }
          }'
      ```

      ```json Response
      {
        "section": {
          "id": "70f12b79-cd30-4750-91ab-1021a16cbbfa",
          "revision": "2",
          "createdDate": "2023-12-17T20:39:33.155Z",
          "updatedDate": "2024-01-15T08:02:19.981Z",
          "name": "Drinks",
          "description": "",
          "additionalImages": [],
          "itemIds": [
            "f6222bdc-1ab2-4cd4-bf6c-8d4fca055d86",
            "c47a77e5-1df6-4c95-a04f-7cd8bb42da33"
          ]
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Section API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Updates a section.

    Each time a section is updated, its revision increments by 1. The existing revision must be included when updating a section. This ensures you're working with the latest section information, and it prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { sections } from '@wix/restaurants';

    sections.updateSection(_id, section): Promise<Section>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Section ID.

      **Read-only.**

    </ParamField>

    <ParamField body="section" type="UpdateSection" required>

      Section update.

      <Expandable title="section properties">

        <ParamField body="_id" type="string">

          Section ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the section is updated. To prevent conflicting changes, the current revision must be passed when updating the section. Ignored when creating a section.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the section was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the section was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Section name.

          Constraints: `minLength 1`, `maxLength 500`.

        </ParamField>

        <ParamField body="description" type="string">

          Section description.

        </ParamField>

        <ParamField body="image" type="string">

          Main section image.

        </ParamField>

        <ParamField body="additionalImages" type="string[]">

          Additional section images.

          Constraints: `maxItems 100`.

        </ParamField>

        <ParamField body="itemIds" type="string[]">

          Item IDs.

          Constraints: `format GUID`, `maxItems 300`.

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Extended fields.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="visible" type="boolean">

          Whether the section is visible in the menu for site visitors.

        </ParamField>

        <ParamField body="businessLocationId" type="string">

          ID of the business location ([SDK](https://dev.wix.com/docs/sdk/backend-modules/restaurants/wix-restaurants-new/about-business-locations) | [REST](https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/about-business-locations)) where this section is available.

          **Read-only.**

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Section>`.

    See the [Section Object](/api-reference/business-solutions/restaurants/menus/sections/section-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Section Updated | `wix.restaurants.menus.v1.section_updated` | Triggered when a section is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-section" id="ex-sdk-update-section-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-section" id="ex-sdk-update-section-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-section-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-section-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update Section</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateSection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-section-0"><span className="wix-picker-name">Update Section</span></label>
    <label htmlFor="ex-sdk-update-section-1"><span className="wix-picker-name">updateSection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update Section
    import { sections } from "@wix/restaurants";

    const sectionId = "70f12b79-cd30-4750-91ab-1021a16cbbfa";

    const section = {
      _id: "70f12b79-cd30-4750-91ab-1021a16cbbfa",
      revision: "1",
      name: "Drinks",
      itemIds: [
        "f6222bdc-1ab2-4cd4-bf6c-8d4fca055d86",
        "c47a77e5-1df6-4c95-a04f-7cd8bb42da33"
      ]
    };

    async function updateSection() {
      const response = await sections.updateSection(sectionId, section);
    }

    /* Promise resolves to:
     * {
     *   "_id": "70f12b79-cd30-4750-91ab-1021a16cbbfa",
     *   "revision": "2",
     *   "name": "Drinks",
     *   "visible": true,
     *   "itemIds": [
     *     "f6222bdc-1ab2-4cd4-bf6c-8d4fca055d86",
     *     "c47a77e5-1df6-4c95-a04f-7cd8bb42da33"
     *   ]
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateSection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { sections } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { sections },
      // Include the auth strategy and host as relevant
    });

    async function updateSection(_id,section) {
      const response = await myWixClient.sections.updateSection(_id,section);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
