---
title: "onBadgeUpdated"
description: "Triggered when a badge is updated."
icon: "bolt"
---

Triggered when a badge is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v3.badge_updated` |
| Package | `@wix/members` |
| Permissions | `Manage Stores`, `Manage Challenges`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeUpdated
  import { badges } from '@wix/members';

  badges.onBadgeUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badges } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        badges,
      },
  });

  wixClient.badges.onBadgeUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
