---
title: "Get Supplier"
description: "Retrieves a supplier by ID, returning complete supplier information including current revision number for update operations."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a supplier by ID, returning complete supplier information including current revision number for update operations.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.READ_MARKETPLACE">Read Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/suppliers-hub/v1/suppliers/{supplierId}
    ```

    ## Path Params

    <ParamField path="supplierId" type="string" required>

      ID of the Supplier to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="supplier" type="Supplier">

      The requested Supplier.

      See the [Supplier Object](/api-reference/business-solutions/suppliers-hub/suppliers/supplier-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Gets supplier information by ID

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/suppliers-hub/v1/suppliers/0d24a5c5-45e6-4ace-ae47-3ce9bed851d8' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "supplier": {
          "id": "0d24a5c5-45e6-4ace-ae47-3ce9bed851d8",
          "revision": "1",
          "createdDate": "2025-09-25T12:52:03.538Z",
          "updatedDate": "2025-09-25T12:52:03.538Z",
          "name": "Top rated",
          "appId": "22bef345-3c5b-4c18-b782-74d4085112ff",
          "location": {
            "country": "CH"
          },
          "rating": "5",
          "verified": true,
          "types": [
            "WHOLESALE",
            "DROPSHIPPING"
          ],
          "reviewCount": 12544,
          "tags": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a supplier by ID, returning complete supplier information including current revision number for update operations.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.READ_MARKETPLACE">Read Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { suppliers } from '@wix/suppliers-hub';

    suppliers.getSupplier(supplierId): Promise
    ```

    ## Method Parameters

    <ParamField body="supplierId" type="string" required>

      ID of the Supplier to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Supplier`.

    See the [Supplier Object](/api-reference/business-solutions/suppliers-hub/suppliers/supplier-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-supplier" id="ex-sdk-get-supplier-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-supplier" id="ex-sdk-get-supplier-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-supplier" id="ex-sdk-get-supplier-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-supplier-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-supplier-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getSupplier</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSupplier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSupplier (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-supplier-0"><span className="wix-picker-name">getSupplier</span></label>
    <label htmlFor="ex-sdk-get-supplier-1"><span className="wix-picker-name">getSupplier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-supplier-2"><span className="wix-picker-name">getSupplier (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getSupplier
    import { suppliers } from '@wix/suppliers-hub';

    async function getSupplier(supplierId) {
      const response = await suppliers.getSupplier(supplierId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSupplier (self-hosted)
    import { createClient } from '@wix/sdk';
    import { suppliers } from '@wix/suppliers-hub';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { suppliers },
      // Include the auth strategy and host as relevant
    });

    async function getSupplier(supplierId) {
      const response = await myWixClient.suppliers.getSupplier(supplierId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSupplier (with elevated permissions)
    import { suppliers } from '@wix/suppliers-hub';
    import { auth } from '@wix/essentials';

    async function myGetSupplierMethod(supplierId) {
      const elevatedGetSupplier = auth.elevate(suppliers.getSupplier);
      const response = await elevatedGetSupplier(supplierId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
