---
title: "Delete Backup"
description: "Deletes a backup."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a backup.

    The process of deleting a backup takes time.
    You can check whether a backup has been deleted successfully with List Backups.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE-BACKUPS">Manage Data Backups</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/wix-data/v2/backups/{backupId}
    ```

    ## Path Params

    <ParamField path="backupId" type="string" required>

      ID of the backup to be deleted.

    </ParamField>

    ## Response Object

    Returns `DeleteBackupResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/wix-data/v2/backups/df941cc7-603e-4926-8034-8d8dc5025325' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a backup.

    The process of deleting a backup takes time.
    You can check whether a backup has been deleted successfully with List Backups.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE-BACKUPS">Manage Data Backups</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { backups } from '@wix/data';

    backups.deleteBackup(backupId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="backupId" type="string" required>

      ID of the backup to be deleted.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Backup Deleted | `wix.data.v2.backup_deleted` | Triggered when a backup is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-backup" id="ex-sdk-delete-backup-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-backup" id="ex-sdk-delete-backup-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-backup" id="ex-sdk-delete-backup-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-backup-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-backup-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteBackup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBackup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBackup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-backup-0"><span className="wix-picker-name">deleteBackup</span></label>
    <label htmlFor="ex-sdk-delete-backup-1"><span className="wix-picker-name">deleteBackup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-backup-2"><span className="wix-picker-name">deleteBackup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteBackup
    import { backups } from '@wix/data';

    async function deleteBackup(backupId) {
      const response = await backups.deleteBackup(backupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBackup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { backups } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { backups },
      // Include the auth strategy and host as relevant
    });

    async function deleteBackup(backupId) {
      const response = await myWixClient.backups.deleteBackup(backupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBackup (with elevated permissions)
    import { backups } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myDeleteBackupMethod(backupId) {
      const elevatedDeleteBackup = auth.elevate(backups.deleteBackup);
      const response = await elevatedDeleteBackup(backupId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
