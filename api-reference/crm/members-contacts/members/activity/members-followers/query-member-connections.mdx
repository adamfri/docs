---
title: "Query Member Connections"
description: "Retrieves a list of members and their connections to the given member."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of members and their connections to the given member.
    <Note>

    If an empty array is passed as `connectedMemberIds`, the call will succeed, but it will not return any data.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-FOLLOWERS">Read Member Connections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/members/v3/followers/{memberId}/connections
    ```

    ## Path Params

    <ParamField path="memberId" type="string" required>

      Member ID.

    </ParamField>

    ## Body Params

    <ParamField body="connectedMemberIds" type="string[]" required>

      List of member IDs whose connections to the given member will be retrieved.

      Constraints: `format GUID`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="connectedMembers" type="ConnectedMembers[]">

      Retrieved list of members whose connections to the given member were retrieved.

      <Expandable title="connectedMembers properties">

        <ResponseField name="connectedMemberId" type="string">

          Site member ID.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="followedByMember" type="boolean">

          Whether the listed member is followed by the given member.

        </ResponseField>

        <ResponseField name="followingMember" type="boolean">

          Whether the listed member follows the given member.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/members/v3/followers/24d1fada-b1fb-44e3-ab31-d48b243b9d34/connections' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>'
        -d $'{
          "connectedMemberIds": [
            "27142d62-fecd-4607-afe1-05c45cff8d95",
            "abc688da-ff74-43c6-9262-4ce2b5d2606"
          ]
        }'
      ```

      ```json Response
      {
        "connectedMembers": [
          {
            "connectedMemberId": "abc688da-ff74-43c6-9262-4ce2b5d26076",
            "followedByMember": false,
            "followingMember": false
          },
          {
            "connectedMemberId": "27142d62-fecd-4607-afe1-05c45cff8d95",
            "followedByMember": true,
            "followingMember": true
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of members and their connections to the given member.
    <Note>

    If an empty array is passed as `connectedMemberIds`, the call will succeed, but it will not return any data.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-FOLLOWERS">Read Member Connections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { memberFollowers } from '@wix/members';

    memberFollowers.queryMemberConnections(memberId, connectedMemberIds): Promise<QueryMemberConnectionsResponse>
    ```

    ## Method Parameters

    <ParamField body="memberId" type="string" required>

      Member ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="connectedMemberIds" type="string[]" required>

      List of member IDs whose connections to the given member will be retrieved.

      Constraints: `format GUID`, `maxItems 100`.

    </ParamField>

    ## Returns

    Returns `Promise<QueryMemberConnectionsResponse>`.

    <ResponseField name="connectedMembers" type="ConnectedMembers[]">

      Retrieved list of members whose connections to the given member were retrieved.

      <Expandable title="connectedMembers properties">

        <ResponseField name="connectedMemberId" type="string">

          Site member ID.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="followedByMember" type="boolean">

          Whether the listed member is followed by the given member.

        </ResponseField>

        <ResponseField name="followingMember" type="boolean">

          Whether the listed member follows the given member.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-member-connections" id="ex-sdk-query-member-connections-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-member-connections" id="ex-sdk-query-member-connections-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-member-connections-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-member-connections-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryMemberConnections</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryMemberConnections (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-member-connections-0"><span className="wix-picker-name">queryMemberConnections</span></label>
    <label htmlFor="ex-sdk-query-member-connections-1"><span className="wix-picker-name">queryMemberConnections (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryMemberConnections
    import { memberFollowers } from '@wix/members';

    async function queryMemberConnections(memberId,connectedMemberIds) {
      const response = await memberFollowers.queryMemberConnections(memberId,connectedMemberIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryMemberConnections (self-hosted)
    import { createClient } from '@wix/sdk';
    import { memberFollowers } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { memberFollowers },
      // Include the auth strategy and host as relevant
    });

    async function queryMemberConnections(memberId,connectedMemberIds) {
      const response = await myWixClient.memberFollowers.queryMemberConnections(memberId,connectedMemberIds);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
