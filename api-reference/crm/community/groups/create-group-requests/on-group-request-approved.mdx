---
title: "onGroupRequestApproved"
description: "Triggered when a group request is approved."
icon: "bolt"
---

Triggered when a group request is approved.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_request_approved` |
| Package | `@wix/groups` |
| Permissions | `Manage Social Groups` |

## Examples

<CodeGroup>

  ```javascript onGroupRequestApproved
  import { groupRequests } from '@wix/groups';

  groupRequests.onGroupRequestApproved((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupRequestApproved (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { groupRequests } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        groupRequests,
      },
  });

  wixClient.groupRequests.onGroupRequestApproved((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
