---
title: "Update Business Contact"
description: "Updates a site's business contact information."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a site's business contact information.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-SITEPROP.MANAGE-BUSINESS-CONTACT">Manage Business Contact</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/site-properties/v4/properties/business-contact
    ```

    ## Body Params

    <ParamField body="businessContact" type="BusinessContactData">

      The site's business contact information.

      <Expandable title="businessContact properties">

        <ParamField body="email" type="string">

          Publicly-available business email address.

        </ParamField>

        <ParamField body="phone" type="string">

          Publicly-available business phone number.

        </ParamField>

        <ParamField body="fax" type="string">

          Publicly-available business fax number.

        </ParamField>

        <ParamField body="address" type="Address">

          Publicly-available business address.

          <Expandable title="address properties">

            <ParamField body="street" type="string">

              Street name.

            </ParamField>

            <ParamField body="city" type="string">

              City name.

            </ParamField>

            <ParamField body="country" type="string">

              Two-letter country code in an [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) format.

            </ParamField>

            <ParamField body="state" type="string">

              State.

            </ParamField>

            <ParamField body="zip" type="string">

              Zip or postal code.

              Constraints: `maxLength 20`.

            </ParamField>

            <ParamField body="hint" type="AddressHint">

              Extra information to be displayed in the address.

              _Nested structure omitted for brevity — see the `AddressHint` object._

            </ParamField>

            <ParamField body="isPhysical" type="boolean">

              Whether this address represents a physical location.

            </ParamField>

            <ParamField body="googleFormattedAddress" type="string">

              Google-formatted version of this address.

            </ParamField>

            <ParamField body="streetNumber" type="string">

              Street number.

            </ParamField>

            <ParamField body="apartmentNumber" type="string">

              Apartment number.

            </ParamField>

            <ParamField body="coordinates" type="GeoCoordinates">

              Geographic coordinates of location.

              _Nested structure omitted for brevity — see the `GeoCoordinates` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="FieldMask">

      The properties of `businessContact` to be updated. Properties not explicitly specified here are ignored. Properties included here but excluded from `businessContact` are cleared.

      <Expandable title="fields properties">

        <ParamField body="paths" type="string[]" />

      </Expandable>

    </ParamField>

    ## Response Object

    Returns `UpdateBusinessContactResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `403` | `PERMISSION_DENIED` | `PERMISSION_DENIED` | The [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) used to call the method doesn't have the required permissions. |
    | `401` | `UNAUTHENTICATED` | `META_SITE_NOT_FOUND` | Couldn't find the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
         'https://www.wixapis.com/site-properties/v4/properties/business-contact'
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>' \
         --data-binary '{
          "businessContact": {
               "email": "useremail@domain.com",
               "phone": "+18008000000",
               "fax": "12345678",
               "address": {
                  "street": "",
                  "city": "New York",
                  "country": "US",
                  "state": "NY",
                  "zip": "10036",
                  "isPhysical": true,
                  "googleFormattedAddress": "Times Square, Manhattan, NY, USA",
                  "streetNumber": "",
                  "apartmentNumber": "",
                  "coordinates": {
                      "latitude": 40.7579747,
                      "longitude": -73.9855426
                  }
               }
            },
          "fields": {
              "paths":["phone","fax","email","address", "businessSchedule"]
            }
        }'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a site's business contact information.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-SITEPROP.MANAGE-BUSINESS-CONTACT">Manage Business Contact</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { siteProperties } from '@wix/business-tools';

    siteProperties.updateBusinessContact(businessContact, options): Promise
    ```

    ## Method Parameters

    <ParamField body="businessContact" type="BusinessContactData" required>

      The site's business contact information.

      <Expandable title="businessContact properties">

        <ParamField body="email" type="string">

          Publicly-available business email address.

        </ParamField>

        <ParamField body="phone" type="string">

          Publicly-available business phone number.

        </ParamField>

        <ParamField body="fax" type="string">

          Publicly-available business fax number.

        </ParamField>

        <ParamField body="address" type="Address">

          Publicly-available business address.

          <Expandable title="address properties">

            <ParamField body="street" type="string">

              Street name.

            </ParamField>

            <ParamField body="city" type="string">

              City name.

            </ParamField>

            <ParamField body="country" type="string">

              Two-letter country code in an [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) format.

            </ParamField>

            <ParamField body="state" type="string">

              State.

            </ParamField>

            <ParamField body="zip" type="string">

              Zip or postal code.

              Constraints: `maxLength 20`.

            </ParamField>

            <ParamField body="hint" type="AddressHint">

              Extra information to be displayed in the address.

              _Nested structure omitted for brevity — see the `AddressHint` object._

            </ParamField>

            <ParamField body="isPhysical" type="boolean">

              Whether this address represents a physical location.

            </ParamField>

            <ParamField body="googleFormattedAddress" type="string">

              Google-formatted version of this address.

            </ParamField>

            <ParamField body="streetNumber" type="string">

              Street number.

            </ParamField>

            <ParamField body="apartmentNumber" type="string">

              Apartment number.

            </ParamField>

            <ParamField body="coordinates" type="GeoCoordinates">

              Geographic coordinates of location.

              _Nested structure omitted for brevity — see the `GeoCoordinates` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateBusinessContactOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="FieldMask">

          The properties of `businessContact` to be updated. Properties not explicitly specified here are ignored. Properties included here but excluded from `businessContact` are cleared.

          <Expandable title="fields properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `UpdateBusinessContactResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `403` | `PERMISSION_DENIED` | `PERMISSION_DENIED` | The [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) used to call the method doesn't have the required permissions. |
    | `401` | `UNAUTHENTICATED` | `META_SITE_NOT_FOUND` | Couldn't find the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-business-contact" id="ex-sdk-update-business-contact-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-business-contact" id="ex-sdk-update-business-contact-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-business-contact" id="ex-sdk-update-business-contact-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-business-contact" id="ex-sdk-update-business-contact-3" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-business-contact" id="ex-sdk-update-business-contact-4" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-business-contact" id="ex-sdk-update-business-contact-5" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-business-contact-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-business-contact-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update Business Contact @description:</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update Business Contact (with elevated permissions) @description:</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update email of business contact @description:</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update email of business contact (with elevated permissions) @description:</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateBusinessContact() for only specific fields (with $w)</span><span className="wix-picker-desc">This code is an example of a webpage where the user updates the business' contact details. If they don't enter text for a specific field, it won't overwrite. Access to this page would be given only to managers</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateBusinessContact (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-business-contact-0"><span className="wix-picker-name">Update Business Contact @description:</span></label>
    <label htmlFor="ex-sdk-update-business-contact-1"><span className="wix-picker-name">Update Business Contact (with elevated permissions) @description:</span></label>
    <label htmlFor="ex-sdk-update-business-contact-2"><span className="wix-picker-name">Update email of business contact @description:</span></label>
    <label htmlFor="ex-sdk-update-business-contact-3"><span className="wix-picker-name">Update email of business contact (with elevated permissions) @description:</span></label>
    <label htmlFor="ex-sdk-update-business-contact-4"><span className="wix-picker-name">updateBusinessContact() for only specific fields (with $w)</span><span className="wix-picker-desc">This code is an example of a webpage where the user updates the business' contact details. If they don't enter text for a specific field, it won't overwrite. Access to this page would be given only to managers</span></label>
    <label htmlFor="ex-sdk-update-business-contact-5"><span className="wix-picker-name">updateBusinessContact (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update Business Contact @description:
    import { siteProperties } from '@wix/business-tools';

    /* Sample businessContact value:
     * {
     *   address: {
     *     apartmentNumber: '',
     *     city: 'New York',
     *     coordinates: {
     *       latitude: 40.7128,
     *       longitude: -74.0060
     *     },
     *     country: 'US',
     *     googleFormattedAddress: '123 Broadway, New York, NY 10001',
     *     hint: {
     *       placement: 'BEFORE',
     *       text: 'On the corner - front entrance on 5th Avenue'
     *     },
     *     isPhysical: true,
     *     state: 'NY',
     *     street: 'Broadway',
     *     streetNumber: '123',
     *     zip: '10001'
     *   },
     *   email: 'mainaddress@example.com',
     *   fax: '011-1-212-555-6789',
     *   phone: '212-555-1234'
     * }
     * 
     * Sample fields value: ['address', 'email', 'fax', 'phone']
     */

    export async function myUpdateBusinessContactFunction(businessContact, fields) {
      try {
        await siteProperties.updateBusinessContact(businessContact, fields);

        console.log('Successfully updated business contact');
        return true;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update Business Contact (with elevated permissions) @description:
    import { siteProperties } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    /* Sample businessContact value:
     * {
     *   address: {
     *     apartmentNumber: '',
     *     city: 'New York',
     *     coordinates: {
     *       latitude: 40.7128,
     *       longitude: -74.0060
     *     },
     *     country: 'US',
     *     googleFormattedAddress: '123 Broadway, New York, NY 10001',
     *     hint: {
     *       placement: 'BEFORE',
     *       text: 'On the corner - front entrance on 5th Avenue'
     *     },
     *     isPhysical: true,
     *     state: 'NY',
     *     street: 'Broadway',
     *     streetNumber: '123',
     *     zip: '10001'
     *   },
     *   email: 'mainaddress@example.com',
     *   fax: '011-1-212-555-6789',
     *   phone: '212-555-1234'
     * }
     * 
     * Sample fields value: ['address', 'email', 'fax', 'phone']
     */

    export async function myUpdateBusinessContactFunction(businessContact, fields) {
      try {
        const elevatedUpdateBusinessContact = auth.elevate(siteProperties.updateBusinessContact);
        await elevatedUpdateBusinessContact(businessContact, fields);

        console.log('Successfully updated business contact');
        return true;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update email of business contact @description:
    import { siteProperties } from '@wix/business-tools';

    /* Sample businessContact value:
     * {
     *   email: 'mainaddress@example.com'
     * }
     * 
     * Sample fields value: ['email']
     */

    export async function myUpdateBusinessContactFunction(businessContact, fields) {
      try {
        await siteProperties.updateBusinessContact(businessContact, fields);

        console.log('Successfully updated business contact');
        return true;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update email of business contact (with elevated permissions) @description:
    import { siteProperties } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    /* Sample businessContact value:
     * {
     *   email: 'mainaddress@example.com'
     * }
     * 
     * Sample fields value: ['email']
     */

    export async function myUpdateBusinessContactFunction(businessContact, fields) {
      try {
        const elevatedUpdateBusinessContact = auth.elevate(siteProperties.updateBusinessContact);
        await elevatedUpdateBusinessContact(businessContact, fields);

        console.log('Successfully updated business contact');
        return true;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateBusinessContact() for only specific fields (with $w)
    /****************************************************
     * Backend code - update-business-contact.web.js/ts *
     ***************************************************/
    import { Permissions, webMethod } from '@wix/web-methods';
    import { siteProperties } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    export const contactDetailsUpdate = webMethod(Permissions.Anyone, async (businessContact, fields) => {
      try {
        const elevatedUpdateBusinessContact = auth.elevate(siteProperties.updateBusinessContact);
        await elevatedUpdateBusinessContact(businessContact, fields);

        console.log('Success! Contact Details Updated');
        return true;
      } catch (error) {
        console.error(error);
        throw new Error(error);
      }
    });

    /*************
     * Page code *
     ************/

    import { contactDetailsUpdate } from 'backend/update-business-contact.web';

    $w.onReady(() => {
      $w('#submit').onClick(async () => {
        const paramDetails = getParamDetails();

        if (!paramDetails.checkedFields.length) {
          $w('#changeAFieldsMsg').show();
          setTimeout(() => {
            $w('#changeAFieldsMsg').hide();
          }, 10000);
        } else {
          const isUpdated = await contactDetailsUpdate(paramDetails.updateDetails, paramDetails.checkedFields);

          if (isUpdated) {
            console.log('Business Contact successfully updated');
            $w('#successfulUpdateMsg').show();
            setTimeout(() => {
              $w('#successfulUpdateMsg').hide();
            }, 10000);
          }
        }
      });
    });

    function getParamDetails() {
      const checkedFields = [];
      const updateDetails = {}
      if ($w('#address').value.length) {
          checkedFields.push('address');
          updateDetails.address = $w('#address').value;
      }
      if ($w('#email').value.length) {
          checkedFields.push('email');
          updateDetails.email = $w('#email').value;
      }
      if ($w('#fax').value.length) {
          checkedFields.push('fax');
          updateDetails.fax = $w('#fax').value;
      }
      if ($w('#phone').value.length) {
          checkedFields.push('phone');
          updateDetails.phone = $w('#phone').value;
      }

      return {updateDetails, checkedFields};
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateBusinessContact (self-hosted)
    import { createClient } from '@wix/sdk';
    import { siteProperties } from '@wix/business-tools';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { siteProperties },
      // Include the auth strategy and host as relevant
    });

    async function updateBusinessContact(businessContact,options) {
      const response = await myWixClient.siteProperties.updateBusinessContact(businessContact,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
