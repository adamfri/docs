---
title: "Bulk Update Modifier Groups"
description: "Note: The Item Modifier Groups API only works with the Wix Restaurants Menus app."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item Modifier Groups API only works with the Wix Restaurants Menus app. Make sure you downloaded this app from [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Updates multiple item modifier groups at once. This function supports partial updates.
    Each time a modifier group is updated, its revision increments by 1. The existing revision must be included when updating an item modifier group. This ensures you're working with the latest item information, and prevents unintended overwrites.
    Up to 100 modifier groups can be returned per request.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants/menus/v1/bulk/modifiers-groups/update
    ```

    ## Body Params

    <ParamField body="modifierGroups" type="MaskedModifierGroup[]" required>

      Modifier groups to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="modifierGroups properties">

        <ParamField body="modifierGroup" type="ModifierGroup">

          Modifier group to update.

          See the [Item Modifier Group Object](/api-reference/business-solutions/restaurants/menus/items/item-modifier-groups/item-modifier-group-object) for this object's fields.

        </ParamField>

        <ParamField body="mask" type="FieldMask">

          Explicit list of fields to update.

          <Expandable title="mask properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to receive the updated modifier groups in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkUpdateModifierGroupsResult[]">

      Information about the updated modifier groups.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for group modifier update.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="modifierGroup" type="ModifierGroup">

          Updated modifier group. Only returned if `returnEntity` is set to `true`.

          See the [Item Modifier Group Object](/api-reference/business-solutions/restaurants/menus/items/item-modifier-groups/item-modifier-group-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the API call.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Modifier Group Updated | `wix.restaurants.menus.v1.item_modifier_group_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/restaurants/item-modifier-group/v1/bulk/modifiers-groups/update \
          -H 'Authorization: <AUTH> \
          --data-binary '{
                "modifierGroups": [
                {
                  "modifierGroup":  {
                      "modifiers": [],
                      "id": "0e29cc7a-123c-4a7f-b9b3-b456cad20246",
                      "revision": "9",
                      "name": "sugar options"
                    },
                    "mask": {
                      "paths": [
                        "name"
                      ]
                    }
                  },
                  {
                     "modifierGroup":  {
                      "modifiers": [],
                      "id": "25878df0-f387-488f-8696-a023e3db0a2d",
                      "name": "Toppings",
                      "revision": "5"
                    },
                    "mask": {
                      "paths": [
                        "name"
                      ]
                    }
                  }
                ],
                "returnEntity": true
              }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "0e29cc7a-123c-4a7f-b9b3-b456cad20246",
              "originalIndex": 0,
              "success": true
            },
            "modifierGroup": {
              "id": "0e29cc7a-123c-4a7f-b9b3-b456cad20246",
              "revision": "10",
              "createdDate": "2023-10-23T08:48:50.618Z",
              "updatedDate": "2024-04-15T00:10:42.538Z",
              "name": "sugar options",
              "modifiers": [
                {
                  "id": "cd9a3ab8-258a-4ab2-bd9e-1374463cdaa5",
                  "additionalChargeInfo": {"additionalCharge": "0.00"},
                  "preSelected": false,
                  "additionalChargeInfo": {"additionalCharge": "0.00"}
                },
                {
                  "id": "42aa491c-da2d-4190-8100-b61f9edc2d34",
                  "additionalChargeInfo": {"additionalCharge": "0.00"},
                  "preSelected": false,
                  "additionalChargeInfo": {"additionalCharge": "0.00"}
                }
              ],
              "rule": {
                "required": false,
                "minSelections": 0
              }
            }
          },
          {
            "itemMetadata": {
              "id": "25878df0-f387-488f-8696-a023e3db0a2d",
              "originalIndex": 1,
              "success": true
            },
            "modifierGroup": {
              "id": "25878df0-f387-488f-8696-a023e3db0a2d",
              "revision": "6",
              "createdDate": "2023-10-23T11:34:41.207Z",
              "updatedDate": "2024-04-15T00:10:42.538Z",
              "name": "Toppings",
              "modifiers": [],
              "rule": {
                "required": true,
                "minSelections": 1
              }
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item Modifier Groups API only works with the Wix Restaurants Menus app. Make sure you downloaded this app from [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Updates multiple item modifier groups at once. This function supports partial updates.
    Each time a modifier group is updated, its revision increments by 1. The existing revision must be included when updating an item modifier group. This ensures you're working with the latest item information, and prevents unintended overwrites.
    Up to 100 modifier groups can be returned per request.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { itemModifierGroups } from '@wix/restaurants';

    itemModifierGroups.bulkUpdateModifierGroups(modifierGroups, options): Promise<BulkUpdateModifierGroupsResponse>
    ```

    ## Method Parameters

    <ParamField body="modifierGroups" type="MaskedModifierGroup[]" required>

      Modifier groups to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="modifierGroups properties">

        <ParamField body="modifierGroup" type="ModifierGroup">

          Modifier group to update.

          See the [Item Modifier Group Object](/api-reference/business-solutions/restaurants/menus/items/item-modifier-groups/item-modifier-group-object) for this object's fields.

        </ParamField>

        <ParamField body="mask" type="string[]">

          Explicit list of fields to update.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="BulkUpdateModifierGroupsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to receive the updated modifier groups in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkUpdateModifierGroupsResponse>`.

    <ResponseField name="results" type="BulkUpdateModifierGroupsResult[]">

      Information about the updated modifier groups.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for group modifier update.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="modifierGroup" type="ModifierGroup">

          Updated modifier group. Only returned if `returnEntity` is set to `true`.

          See the [Item Modifier Group Object](/api-reference/business-solutions/restaurants/menus/items/item-modifier-groups/item-modifier-group-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the API call.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Modifier Group Updated | `wix.restaurants.menus.v1.item_modifier_group_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-modifier-groups" id="ex-sdk-bulk-update-modifier-groups-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-modifier-groups" id="ex-sdk-bulk-update-modifier-groups-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-update-modifier-groups" id="ex-sdk-bulk-update-modifier-groups-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-modifier-groups-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-modifier-groups-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateModifierGroups</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateModifierGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateModifierGroups (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-modifier-groups-0"><span className="wix-picker-name">bulkUpdateModifierGroups</span></label>
    <label htmlFor="ex-sdk-bulk-update-modifier-groups-1"><span className="wix-picker-name">bulkUpdateModifierGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-update-modifier-groups-2"><span className="wix-picker-name">bulkUpdateModifierGroups (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUpdateModifierGroups
    import { itemModifierGroups } from '@wix/restaurants';

    async function bulkUpdateModifierGroups(modifierGroups,options) {
      const response = await itemModifierGroups.bulkUpdateModifierGroups(modifierGroups,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateModifierGroups (self-hosted)
    import { createClient } from '@wix/sdk';
    import { itemModifierGroups } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { itemModifierGroups },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdateModifierGroups(modifierGroups,options) {
      const response = await myWixClient.itemModifierGroups.bulkUpdateModifierGroups(modifierGroups,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateModifierGroups (with elevated permissions)
    import { itemModifierGroups } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myBulkUpdateModifierGroupsMethod(modifierGroups,options) {
      const elevatedBulkUpdateModifierGroups = auth.elevate(itemModifierGroups.bulkUpdateModifierGroups);
      const response = await elevatedBulkUpdateModifierGroups(modifierGroups,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
