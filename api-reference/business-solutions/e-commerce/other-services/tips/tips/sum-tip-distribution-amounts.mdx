---
title: "Sum Tip Distribution Amounts"
description: "Sums tip distribution amounts, given the provided filtering."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sums tip distribution amounts, given the provided filtering.

    See [Query Tip Distributions](https://dev.wix.com/docs/rest/business-solutions/e-commerce/other-services/tips/tips/query-tip-distributions) for a list of supported filters.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/tips/v1/tips/tip-distributions/sum-amounts
    ```

    ## Body Params

    <ParamField body="filter" type="object" required>

      Filter object in the following format:
      `"filter" : {`
      "fieldName1": "value1",
      "fieldName2":&#123;"$operator":"value2"&#125;
      &#125;``
      Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

    </ParamField>

    ## Response Object

    <ResponseField name="totalAmount" type="string">

      Total amount of tip distributions matching the filter.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/tips-service/v1/tips/tip-distributions/sum-amounts' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "filter": {
          "tipId": "6f54fea4-0504-4f19-9f49-cb04a55a99bc"
        }
      }'
      ```

      ```json Response
      {
        "totalAmount": "5.00"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sums tip distribution amounts, given the provided filtering.

    See [Query Tip Distributions](https://dev.wix.com/docs/rest/business-solutions/e-commerce/other-services/tips/tips/query-tip-distributions) for a list of supported filters.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tips } from '@wix/ecom';

    tips.sumTipDistributionAmounts(filter): Promise<SumTipDistributionAmountsResponse>
    ```

    ## Method Parameters

    <ParamField body="filter" type="object" required>

      Filter object in the following format:
      `"filter" : {`
      "fieldName1": "value1",
      "fieldName2":&#123;"$operator":"value2"&#125;
      &#125;``
      Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

    </ParamField>

    ## Returns

    Returns `Promise<SumTipDistributionAmountsResponse>`.

    <ResponseField name="totalAmount" type="string">

      Total amount of tip distributions matching the filter.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-sum-tip-distribution-amounts" id="ex-sdk-sum-tip-distribution-amounts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-sum-tip-distribution-amounts" id="ex-sdk-sum-tip-distribution-amounts-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-sum-tip-distribution-amounts" id="ex-sdk-sum-tip-distribution-amounts-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-sum-tip-distribution-amounts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-sum-tip-distribution-amounts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">sumTipDistributionAmounts</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">sumTipDistributionAmounts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">sumTipDistributionAmounts (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-sum-tip-distribution-amounts-0"><span className="wix-picker-name">sumTipDistributionAmounts</span></label>
    <label htmlFor="ex-sdk-sum-tip-distribution-amounts-1"><span className="wix-picker-name">sumTipDistributionAmounts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-sum-tip-distribution-amounts-2"><span className="wix-picker-name">sumTipDistributionAmounts (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript sumTipDistributionAmounts
    import { tips } from '@wix/ecom';

    async function sumTipDistributionAmounts(filter) {
      const response = await tips.sumTipDistributionAmounts(filter);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript sumTipDistributionAmounts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tips } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tips },
      // Include the auth strategy and host as relevant
    });

    async function sumTipDistributionAmounts(filter) {
      const response = await myWixClient.tips.sumTipDistributionAmounts(filter);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript sumTipDistributionAmounts (with elevated permissions)
    import { tips } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function mySumTipDistributionAmountsMethod(filter) {
      const elevatedSumTipDistributionAmounts = auth.elevate(tips.sumTipDistributionAmounts);
      const response = await elevatedSumTipDistributionAmounts(filter);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
