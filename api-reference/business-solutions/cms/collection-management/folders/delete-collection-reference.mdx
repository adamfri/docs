---
title: "Delete Collection Reference"
description: "Deletes a collection reference."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a collection reference.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.DATA-COLLECTIONS-MANAGE">Manage Data Collections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/wix-data/v1/folders/collection-references/delete
    ```

    ## Body Params

    <ParamField body="collectionName" type="string" required>

      Name of the collection to delete references for.

      Constraints: `maxLength 255`.

    </ParamField>

    <ParamField body="folderId" type="string">

      ID of the folder to delete the collection reference from.

      Defaults to the root folder if not specified.

    </ParamField>

    ## Response Object

    Returns `DeleteCollectionReferenceResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `REFERENCE_NOT_FOUND` | Couldn't find the reference. |
    | `428` | `FAILED_PRECONDITION` | `NOT_A_SHORTCUT` | Can't delete a non-shortcut collection reference. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST https://wixapis.com/autocms/v1/folders/collection-references/delete  \
          -H 'Authorization: <AUTH>' \
          -d '{
              "folderId": "593c732f-d781-4457-b4a9-7ad73ddf4b1a",
              "collectionName": "RestaurantMenu"
          }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a collection reference.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.DATA-COLLECTIONS-MANAGE">Manage Data Collections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { folders } from '@wix/autocms-folders-service';

    folders.deleteCollectionReference(collectionName, options): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="collectionName" type="string" required>

      Name of the collection to delete references for.

      Constraints: `maxLength 255`.

    </ParamField>

    <ParamField body="options" type="DeleteCollectionReferenceOptions">

      options

      <Expandable title="options properties">

        <ParamField body="folderId" type="string">

          ID of the folder to delete the collection reference from.

          Defaults to the root folder if not specified.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `REFERENCE_NOT_FOUND` | Couldn't find the reference. |
    | `428` | `FAILED_PRECONDITION` | `NOT_A_SHORTCUT` | Can't delete a non-shortcut collection reference. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-collection-reference" id="ex-sdk-delete-collection-reference-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-collection-reference" id="ex-sdk-delete-collection-reference-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-collection-reference" id="ex-sdk-delete-collection-reference-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-collection-reference-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-collection-reference-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteCollectionReference</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCollectionReference (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCollectionReference (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-collection-reference-0"><span className="wix-picker-name">deleteCollectionReference</span></label>
    <label htmlFor="ex-sdk-delete-collection-reference-1"><span className="wix-picker-name">deleteCollectionReference (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-collection-reference-2"><span className="wix-picker-name">deleteCollectionReference (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteCollectionReference
    import { folders } from '@wix/autocms-folders-service';

    async function deleteCollectionReference(collectionName,options) {
      const response = await folders.deleteCollectionReference(collectionName,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCollectionReference (self-hosted)
    import { createClient } from '@wix/sdk';
    import { folders } from '@wix/autocms-folders-service';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { folders },
      // Include the auth strategy and host as relevant
    });

    async function deleteCollectionReference(collectionName,options) {
      const response = await myWixClient.folders.deleteCollectionReference(collectionName,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCollectionReference (with elevated permissions)
    import { folders } from '@wix/autocms-folders-service';
    import { auth } from '@wix/essentials';

    async function myDeleteCollectionReferenceMethod(collectionName,options) {
      const elevatedDeleteCollectionReference = auth.elevate(folders.deleteCollectionReference);
      const response = await elevatedDeleteCollectionReference(collectionName,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
