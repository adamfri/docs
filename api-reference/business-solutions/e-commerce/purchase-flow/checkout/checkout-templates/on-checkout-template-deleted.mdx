---
title: "onCheckoutTemplateDeleted"
description: "The onCheckoutTemplateDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.checkout_template_deleted` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions` |

## Examples

<CodeGroup>

  ```javascript onCheckoutTemplateDeleted
  import { checkoutTemplates } from '@wix/ecom';

  checkoutTemplates.onCheckoutTemplateDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onCheckoutTemplateDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { checkoutTemplates } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        checkoutTemplates,
      },
  });

  wixClient.checkoutTemplates.onCheckoutTemplateDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
