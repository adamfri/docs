---
title: "onItemModifierDeleted"
description: "The onItemModifierDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_modifier_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemModifierDeleted
  import { itemModifiers } from '@wix/restaurants';

  itemModifiers.onItemModifierDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemModifierDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemModifiers } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemModifiers,
      },
  });

  wixClient.itemModifiers.onItemModifierDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
