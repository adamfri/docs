---
title: "Set Widget Settings"
description: "Sets the widget settings for the site."
icon: "upload"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Sets the widget settings for the site.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INNOVATION_LAB.ADMIN">Innovation Lab Administrator</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/innovation/widget/v1/settings
    ```

    ## Body Params

    <ParamField body="widgetSettings" type="WidgetSettings" required>

      Widget settings to update.

      See the [Widget Settings Object](/api-reference/business-management/ai-site-chat/widget-settings/widget-settings-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="widgetSettings" type="WidgetSettings">

      Updated widget settings.

      See the [Widget Settings Object](/api-reference/business-management/ai-site-chat/widget-settings/widget-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Sets the widget settings for the site.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INNOVATION_LAB.ADMIN">Innovation Lab Administrator</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { widgetSettings } from '@wix/ai-site-chat';

    widgetSettings.setWidgetSettings(widgetSettings): Promise
    ```

    ## Method Parameters

    <ParamField body="widgetSettings" type="WidgetSettings" required>

      Widget settings to update.

      See the [Widget Settings Object](/api-reference/business-management/ai-site-chat/widget-settings/widget-settings-object) for this object's fields.

    </ParamField>

    ## Returns

    <ResponseField name="widgetSettings" type="WidgetSettings">

      Updated widget settings.

      See the [Widget Settings Object](/api-reference/business-management/ai-site-chat/widget-settings/widget-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-set-widget-settings" id="ex-sdk-set-widget-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-set-widget-settings" id="ex-sdk-set-widget-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-set-widget-settings" id="ex-sdk-set-widget-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-set-widget-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-set-widget-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">setWidgetSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setWidgetSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setWidgetSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-set-widget-settings-0"><span className="wix-picker-name">setWidgetSettings</span></label>
    <label htmlFor="ex-sdk-set-widget-settings-1"><span className="wix-picker-name">setWidgetSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-set-widget-settings-2"><span className="wix-picker-name">setWidgetSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript setWidgetSettings
    import { widgetSettings } from '@wix/ai-site-chat';

    async function setWidgetSettings(widgetSettings) {
      const response = await widgetSettings.setWidgetSettings(widgetSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setWidgetSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { widgetSettings } from '@wix/ai-site-chat';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { widgetSettings },
      // Include the auth strategy and host as relevant
    });

    async function setWidgetSettings(widgetSettings) {
      const response = await myWixClient.widgetSettings.setWidgetSettings(widgetSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setWidgetSettings (with elevated permissions)
    import { widgetSettings } from '@wix/ai-site-chat';
    import { auth } from '@wix/essentials';

    async function mySetWidgetSettingsMethod(widgetSettings) {
      const elevatedSetWidgetSettings = auth.elevate(widgetSettings.setWidgetSettings);
      const response = await elevatedSetWidgetSettings(widgetSettings);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
