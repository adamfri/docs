---
title: "Get Site Seo Tags"
description: "Retrieves a site's site-wide SEO tags."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a site's site-wide SEO tags.

    Returns the tags you can change in `tags`, and any site verification tags added
    through the dashboard in `embedTags`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-SEO">View SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/promote/seo/v1/site-seo-tags
    ```

    ## Response Object

    <ResponseField name="siteSeoTags" type="SiteSeoTags">

      Retrieved site SEO tags.

      See the [Site Seo Tags Object](/api-reference/business-management/seo/site-seo-tags-v1/site-seo-tags-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    The site has no verification tags added through the dashboard, so `embedTags` is empty.

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/seo-metatags-server/v1/site-seo-tags' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "siteSeoTags": {
          "id": "118f445d-6b84-4268-821d-7d2b9f40ac60",
          "tags": [
            {
              "type": "meta",
              "props": {
                "name": "description",
                "content": ""
              },
              "children": "",
              "custom": false,
              "disabled": false
            },
            {
              "type": "meta",
              "props": {
                "name": "fb_admins_meta_tag",
                "content": ""
              },
              "children": "",
              "custom": false,
              "disabled": false
            },
            {
              "type": "meta",
              "props": {
                "name": "keywords",
                "content": ""
              },
              "children": "",
              "custom": false,
              "disabled": false
            }
          ],
          "embedTags": [],
          "origin": "ORIGIN_UNSPECIFIED"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a site's site-wide SEO tags.

    Returns the tags you can change in `tags`, and any site verification tags added
    through the dashboard in `embedTags`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-SEO">View SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { siteSeoTags } from '@wix/seo';

    siteSeoTags.getSiteSeoTags(): Promise
    ```

    ## Returns

    <ResponseField name="siteSeoTags" type="SiteSeoTags">

      Retrieved site SEO tags.

      See the [Site Seo Tags Object](/api-reference/business-management/seo/site-seo-tags-v1/site-seo-tags-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-site-seo-tags" id="ex-sdk-get-site-seo-tags-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-site-seo-tags" id="ex-sdk-get-site-seo-tags-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-site-seo-tags-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-site-seo-tags-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Retrieve a site's SEO tags</span><span className="wix-picker-desc">The site has no verification tags added through the dashboard, so embedTags is empty</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSiteSeoTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-site-seo-tags-0"><span className="wix-picker-name">Retrieve a site's SEO tags</span><span className="wix-picker-desc">The site has no verification tags added through the dashboard, so embedTags is empty</span></label>
    <label htmlFor="ex-sdk-get-site-seo-tags-1"><span className="wix-picker-name">getSiteSeoTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Retrieve a site's SEO tags
    import { siteSeoTags } from "@wix/seo";

    async function getSiteSeoTags() {
      const response = await siteSeoTags.getSiteSeoTags();
      return response;
    }

    /* Promise resolves to:
     * {
     *   "siteSeoTags": {
     *     "tags": [
     *       {
     *         "type": "meta",
     *         "props": { "name": "description", "content": "Handmade ceramics from a small studio in Lisbon." },
     *         "children": "",
     *         "custom": false,
     *         "disabled": false
     *       },
     *       {
     *         "type": "meta",
     *         "props": { "property": "og:image", "content": "https://static.wixstatic.com/media/a1b2c3_9f8e7d6c5b4a3210.jpg" },
     *         "children": "",
     *         "custom": false,
     *         "disabled": false
     *       }
     *     ],
     *     "embedTags": [],
     *     "origin": "ORIGIN_UNSPECIFIED",
     *     "_id": "118f445d-6b84-4268-821d-7d2b9f40ac60"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSiteSeoTags (self-hosted)
    import { createClient } from '@wix/sdk';
    import { siteSeoTags } from '@wix/seo';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { siteSeoTags },
      // Include the auth strategy and host as relevant
    });

    async function getSiteSeoTags() {
      const response = await myWixClient.siteSeoTags.getSiteSeoTags();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
