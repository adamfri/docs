---
title: "onCategoryUpdated"
description: "Triggered when a category is updated."
icon: "bolt"
---

Triggered when a category is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v1.category_updated` |
| Package | `@wix/comments` |

## Examples

<CodeGroup>

  ```javascript onCategoryUpdated
  import { categories } from '@wix/comments';

  categories.onCategoryUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categories } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        categories,
      },
  });

  wixClient.categories.onCategoryUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
