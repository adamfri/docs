---
title: "onPostUnpinned"
description: "An existing post was unpinned from the top of the post list."
icon: "bolt"
---

An existing post was unpinned from the top of the post list.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_unpinned` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostUnpinned
  import { posts } from '@wix/forum';

  posts.onPostUnpinned((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostUnpinned (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostUnpinned((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
