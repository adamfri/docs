---
title: "Create Reward"
description: "Creates a reward that customers can redeem with loyalty points."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a reward that customers can redeem with loyalty points.

    When a customer redeems a reward, a loyalty coupon is created
    based on the specifications detailed in the `discountAmount` or `couponReward` fields. The customer can
    then use this coupon to receive the discount. Note that while the Rewards API uses coupon scopes and specifications,
    no coupon is actually created until a customer redeems a reward with points.
    See the Coupons API for more information about coupons. You can't update the `type` of reward after it's created.

    A reward's `active` status defaults to `false`. To make the reward available to customers,
    either set the `active` field to `true` during creation or call Update Reward
    to change the status.

    To customize a reward for each loyalty tier, use `configsByTier`.
    This allows you to specify the amount of the earned discount, the cost in loyalty points
    to redeem the reward, and the tier to which this configuration applies. Each tier requires its own
    `configsByTier` configuration. To create a reward that's available to loyalty accounts in the base tier,
    leave the `tierId` field empty. See the Tiers API for more information on tiers.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/loyalty-rewards/v1/rewards
    ```

    ## Body Params

    <ParamField body="reward" type="Reward" required>

      Reward to create.

      See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="reward" type="Reward">

      Created reward.

      See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reward Created | `wix.loyalty.v1.reward_created` | Triggered when a reward is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/loyalty-rewards/v1/rewards' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-raw '{
                       "reward": {
                         "active": true,
                         "name": "Free shipping",
                         "type": "COUPON_REWARD",
                         "couponReward": {
                           "freeShipping": {
                             "configsByTier": [
                               {
                                 "tierId": null,
                                 "costInPoints": 20
                               }
                             ]
                           }
                         }
                       }
                     }'
      ```

      ```json Response
      {
        "reward": {
          "id": "46d7bbce-6bb4-4174-ae5a-7f44c19f95ce",
          "name": "Free shipping",
          "requiredPoints": 20,
          "active": true,
          "type": "COUPON_REWARD",
          "couponReward": {
            "freeShipping": {
              "configsByTier": [
                {
                  "costInPoints": 20
                }
              ]
            }
          },
          "revision": "1",
          "createdDate": "2024-04-18T10:35:41.071Z",
          "updatedDate": "2024-04-18T10:35:41.071Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a reward that customers can redeem with loyalty points.

    When a customer redeems a reward, a loyalty coupon is created
    based on the specifications detailed in the `discountAmount` or `couponReward` fields. The customer can
    then use this coupon to receive the discount. Note that while the Rewards API uses coupon scopes and specifications,
    no coupon is actually created until a customer redeems a reward with points.
    See the Coupons API for more information about coupons. You can't update the `type` of reward after it's created.

    A reward's `active` status defaults to `false`. To make the reward available to customers,
    either set the `active` field to `true` during creation or call Update Reward
    to change the status.

    To customize a reward for each loyalty tier, use `configsByTier`.
    This allows you to specify the amount of the earned discount, the cost in loyalty points
    to redeem the reward, and the tier to which this configuration applies. Each tier requires its own
    `configsByTier` configuration. To create a reward that's available to loyalty accounts in the base tier,
    leave the `tierId` field empty. See the Tiers API for more information on tiers.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rewards } from '@wix/loyalty';

    rewards.createReward(reward): Promise<Reward>
    ```

    ## Method Parameters

    <ParamField body="reward" type="Reward" required>

      Reward to create.

      See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Reward>`.

    See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reward Created | `wix.loyalty.v1.reward_created` | Triggered when a reward is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-reward" id="ex-sdk-create-reward-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-reward" id="ex-sdk-create-reward-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-reward-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-reward-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create reward</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createReward (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-reward-0"><span className="wix-picker-name">Create reward</span></label>
    <label htmlFor="ex-sdk-create-reward-1"><span className="wix-picker-name">createReward (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create reward
    import { rewards } from "@wix/loyalty";

    const reward = {
      active: true,
      name: "Free shipping",
      type: "COUPON_REWARD" as const,
      couponReward: {
        freeShipping: {
          configsByTier: [{ tierId: null, costInPoints: 20 }],
        },
      },
    };

    async function createReward() {
      const response = await rewards.createReward(reward);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createReward (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rewards } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rewards },
      // Include the auth strategy and host as relevant
    });

    async function createReward(reward) {
      const response = await myWixClient.rewards.createReward(reward);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
