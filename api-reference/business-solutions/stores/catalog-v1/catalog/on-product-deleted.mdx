---
title: "onProductDeleted"
description: "Triggered when a product is deleted."
icon: "bolt"
---

Triggered when a product is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `com.wix.ecommerce.catalog.api.v1.ProductDeleted` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onProductDeleted
  import { products } from '@wix/stores';

  products.onProductDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onProductDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { products } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        products,
      },
  });

  wixClient.products.onProductDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
