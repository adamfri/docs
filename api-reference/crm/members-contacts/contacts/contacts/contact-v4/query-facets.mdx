---
title: "Query Facets"
description: "Retrieves facets from the site’s contact list by filter."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves facets from the site’s contact list by filter.
    Facets include labels and subscription statuses.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/contacts/v4/contacts/facets/query
    ```

    ## Body Params

    <ParamField body="paging" type="Paging">

      Pagination options.

      <Expandable title="paging properties">

        <ParamField body="limit" type="integer">

          Number of items to return. Learn more about pagination in [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

          Default: `50`. &lt;br>
          Maximum: `1000`.

        </ParamField>

        <ParamField body="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="language" type="string">

      Language for localization.

    </ParamField>

    <ParamField body="filter" type="object">

      Filterable fields:
      - `'info.extendedFields.emailSubscriptions.effectiveEmail'` (`$exists`).
      - `'info.extendedFields.emailSubscriptions.subscriptionStatus'` (`$eq`, `$ne`, `$in`, `$nin`).
      - `'info.extendedFields.emailSubscriptions.deliverabilityStatus'` (`$eq`, `$ne`, `$in`, `$nin`).

    </ParamField>

    ## Response Object

    <ResponseField name="facets" type="ContactsFacet[]">

      List of facets.

      <Expandable title="facets properties">

        <ResponseField name="facetType" type="string">

          Type of facet.

          **Supported values:**

          - `ALL_CONTACTS`
          - `NOT_LABELED`
          - `LABEL`
          - `SUBSCRIPTION_STATUS`
          - `MEMBERSHIP_STATUS`

        </ResponseField>

        <ResponseField name="namespace" type="string">

          Scope for facet keys.
          When a facet key exists in a namespace,
          each key is unique within that namespace.

          Currently, facets not created by Wix are in the `custom` namespace.

        </ResponseField>

        <ResponseField name="namespaceDisplayName" type="string">

          Human-readable display name.

        </ResponseField>

        <ResponseField name="facetKey" type="string">

          Facet key, automatically generated.

        </ResponseField>

        <ResponseField name="facetDisplayName" type="string">

          Human-readable name. Shown in the Wix UI.

        </ResponseField>

        <ResponseField name="count" type="integer">

          Number of contacts the facet applies to.

        </ResponseField>

        <ResponseField name="queryFilter" type="object">

          Filter used to query contacts of this facet.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          The number of items returned in this response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          The offset which was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          The total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          A flag that indicates the server failed to calculate 'total' field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/contacts/v4/contacts/facets/query' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{
          "filter": {
              "$and": [
                {"info.extendedFields.emailSubscriptions.effectiveEmail": {"$exists": true}},
                {"info.extendedFields.emailSubscriptions.deliverabilityStatus": {"$in": ["VALID", "INACTIVE"]}}
              ]
            }
          }'
      ```

      ```json Response
      {
        "facets": [
          {
            "facetType": "ALL_CONTACTS",
            "facetKey": "contacts.all",
            "facetDisplayName": "All Contacts",
            "count": 8
          },
          {
            "facetType": "LABEL",
            "namespace": "custom",
            "namespaceDisplayName": "Labels",
            "facetKey": "custom.label1",
            "facetDisplayName": "label1",
            "count": 0,
            "queryFilter": {"info.labelKeys" : {"$hasSome": ["custom.label1"]}}
          },
          {
            "facetType": "LABEL",
            "namespace": "custom",
            "namespaceDisplayName": "Labels",
            "facetKey": "custom.label2",
            "facetDisplayName": "label2",
            "count": 1,
            "queryFilter": {"info.labelKeys" : {"$hasSome": ["custom.label2"]}}
          },
          {
            "facetType": "LABEL",
            "namespace": "contacts",
            "namespaceDisplayName": "Labels",
            "facetKey": "contacts.customers",
            "facetDisplayName": "Customers",
            "count": 0,
            "queryFilter": {"info.labelKeys" : {"$hasSome": ["contacts.customers"]}}
          },
          {
            "facetType": "LABEL",
            "namespace": "contacts",
            "namespaceDisplayName": "Labels",
            "facetKey": "contacts.contacted-me",
            "facetDisplayName": "Contacted Me",
            "count": 1,
            "queryFilter": {"info.labelKeys" : {"$hasSome": ["contacts.contacted-me"]}}
          },
          {
            "facetType": "SUBSCRIPTION_STATUS",
            "namespace": "emailSubscriptions",
            "namespaceDisplayName": "Email Subscription Status",
            "facetKey": "emailSubscriptions.subscribed",
            "facetDisplayName": "Subscribed",
            "count": 1,
            "queryFilter": {"info.extendedFields.emailSubscriptions.subscriptionStatus" : {"$eq": "SUBSCRIBED"}}
          },
          {
            "facetType": "SUBSCRIPTION_STATUS",
            "namespace": "emailSubscriptions",
            "namespaceDisplayName": "Email Subscription Status",
            "facetKey": "emailSubscriptions.unsubscribed",
            "facetDisplayName": "Unsubscribed",
            "count": 1,
            "queryFilter": {"info.extendedFields.emailSubscriptions.subscriptionStatus" : {"$eq": "UNSUBSCRIBED"}}
          },
          {
            "facetType": "SUBSCRIPTION_STATUS",
            "namespace": "emailSubscriptions",
            "namespaceDisplayName": "Email Subscription Status",
            "facetKey": "emailSubscriptions.pending",
            "facetDisplayName": "Pending",
            "count": 0,
            "queryFilter": {"info.extendedFields.emailSubscriptions.subscriptionStatus" : {"$eq": "PENDING"}}
          },
          {
            "facetType": "SUBSCRIPTION_STATUS",
            "namespace": "emailSubscriptions",
            "namespaceDisplayName": "Email Subscription Status",
            "facetKey": "emailSubscriptions.bounced",
            "facetDisplayName": "Bounced",
            "count": 0,
            "queryFilter": {"info.extendedFields.emailSubscriptions.deliverabilityStatus" : {"$eq": "BOUNCED"}}
          },
          {
            "facetType": "SUBSCRIPTION_STATUS",
            "namespace": "emailSubscriptions",
            "namespaceDisplayName": "Email Subscription Status",
            "facetKey": "emailSubscriptions.spam",
            "facetDisplayName": "Spam",
            "count": 0,
            "queryFilter": {"info.extendedFields.emailSubscriptions.deliverabilityStatus" : {"$eq": "SPAM_COMPLAINT"}}
          },
          {
            "facetType": "SUBSCRIPTION_STATUS",
            "namespace": "emailSubscriptions",
            "namespaceDisplayName": "Email Subscription Status",
            "facetKey": "emailSubscriptions.inactive",
            "facetDisplayName": "Inactive",
            "count": 0,
            "queryFilter": {"info.extendedFields.emailSubscriptions.deliverabilityStatus" : {"$eq": "INACTIVE"}}
          },
          {
            "facetType": "MEMBERSHIP_STATUS",
            "namespace": "membershipStatus",
            "namespaceDisplayName": "Membership Status",
            "facetKey": "membershipStatus.approved",
            "facetDisplayName": "Approved",
            "count": 1,
            "queryFilter": {"info.extendedFields.members.membershipStatus" : {"$eq": "APPROVED"}}
          },
          {
            "facetType": "MEMBERSHIP_STATUS",
            "namespace": "membershipStatus",
            "namespaceDisplayName": "Membership Status",
            "facetKey": "membershipStatus.denied",
            "facetDisplayName": "Blocked",
            "count": 1,
            "queryFilter": {"info.extendedFields.members.membershipStatus" : {"$eq": "DENIED"}}
          },
          {
            "facetType": "MEMBERSHIP_STATUS",
            "namespace": "membershipStatus",
            "namespaceDisplayName": "Membership Status",
            "facetKey": "membershipStatus.pending",
            "facetDisplayName": "Pending",
            "count": 2,
            "queryFilter": {"info.extendedFields.members.membershipStatus" : {"$eq": "PENDING"}}
          },
          {
            "facetType": "MEMBERSHIP_STATUS",
            "namespace": "membershipStatus",
            "namespaceDisplayName": "Membership Status",
            "facetKey": "membershipStatus.inactive",
            "facetDisplayName": "Inactive",
            "count": 0,
            "queryFilter": {"info.extendedFields.members.membershipStatus" : {"$eq": "INACTIVE"}}
          }
        ],
        "pagingMetadata": {
          "count": 15,
          "offset": 0,
          "total": 15
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves facets from the site’s contact list by filter.
    Facets include labels and subscription statuses.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contacts } from '@wix/crm';

    contacts.queryFacets(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="QueryFacetsOptions">

      Query facets options.

      <Expandable title="options properties">

        <ParamField body="paging" type="Paging">

          Pagination options.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to return. Learn more about pagination in [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

              Default: `50`. &lt;br>
              Maximum: `1000`.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="language" type="string">

          Language for localization.

        </ParamField>

        <ParamField body="filter" type="object">

          Filterable fields:
          - `'info.extendedFields.emailSubscriptions.effectiveEmail'` (`$exists`).
          - `'info.extendedFields.emailSubscriptions.subscriptionStatus'` (`$eq`, `$ne`, `$in`, `$nin`).
          - `'info.extendedFields.emailSubscriptions.deliverabilityStatus'` (`$eq`, `$ne`, `$in`, `$nin`).

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="facets" type="ContactsFacet[]">

      List of facets.

      <Expandable title="facets properties">

        <ResponseField name="facetType" type="string">

          Type of facet.

          **Supported values:**

          - `ALL_CONTACTS`
          - `NOT_LABELED`
          - `LABEL`
          - `SUBSCRIPTION_STATUS`
          - `MEMBERSHIP_STATUS`

        </ResponseField>

        <ResponseField name="namespace" type="string">

          Scope for facet keys.
          When a facet key exists in a namespace,
          each key is unique within that namespace.

          Currently, facets not created by Wix are in the `custom` namespace.

        </ResponseField>

        <ResponseField name="namespaceDisplayName" type="string">

          Human-readable display name.

        </ResponseField>

        <ResponseField name="facetKey" type="string">

          Facet key, automatically generated.

        </ResponseField>

        <ResponseField name="facetDisplayName" type="string">

          Human-readable name. Shown in the Wix UI.

        </ResponseField>

        <ResponseField name="count" type="integer">

          Number of contacts the facet applies to.

        </ResponseField>

        <ResponseField name="queryFilter" type="object">

          Filter used to query contacts of this facet.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          The number of items returned in this response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          The offset which was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          The total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          A flag that indicates the server failed to calculate 'total' field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-facets" id="ex-sdk-query-facets-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-facets" id="ex-sdk-query-facets-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-facets" id="ex-sdk-query-facets-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-facets-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-facets-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryFacets</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryFacets (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryFacets (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-facets-0"><span className="wix-picker-name">queryFacets</span></label>
    <label htmlFor="ex-sdk-query-facets-1"><span className="wix-picker-name">queryFacets (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-facets-2"><span className="wix-picker-name">queryFacets (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryFacets
    import { contacts } from '@wix/crm';

    async function queryFacets(options) {
      const response = await contacts.queryFacets(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryFacets (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contacts } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contacts },
      // Include the auth strategy and host as relevant
    });

    async function queryFacets(options) {
      const response = await myWixClient.contacts.queryFacets(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryFacets (with elevated permissions)
    import { contacts } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myQueryFacetsMethod(options) {
      const elevatedQueryFacets = auth.elevate(contacts.queryFacets);
      const response = await elevatedQueryFacets(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
