---
title: "onGuestEventCanceled"
description: "Triggered when an event is canceled."
icon: "bolt"
---

Triggered when an event is canceled.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.guests.v1.guest_guest_event_canceled` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onGuestEventCanceled
  import { guests } from '@wix/events';

  guests.onGuestEventCanceled((event) => {
      // handle your event here
  });
  ```

  ```javascript onGuestEventCanceled (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { guests } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        guests,
      },
  });

  wixClient.guests.onGuestEventCanceled((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
