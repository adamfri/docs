---
title: "Delete Payment Link"
description: "Deletes a payment link."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a payment link.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/paymentlinks/v1/payment-links/{paymentLinkId}
    ```

    ## Path Params

    <ParamField path="paymentLinkId" type="string" required>

      ID of the payment link to delete.

    </ParamField>

    ## Response Object

    Returns `DeletePaymentLinkResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `HAS_PAYMENTS` | The specified payment link has payments, and can't be deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Payment Link Deleted | `wix.paymentlinks.v1.payment_link_deleted` | Triggered when a payment link is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/payment-links/v1/payment-links/763736a8-b40b-4c1a-a8ea-f6dfa75e6197' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a payment link.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { paymentLinks } from '@wix/get-paid';

    paymentLinks.deletePaymentLink(paymentLinkId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="paymentLinkId" type="string" required>

      ID of the payment link to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `HAS_PAYMENTS` | The specified payment link has payments, and can't be deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Payment Link Deleted | `wix.paymentlinks.v1.payment_link_deleted` | Triggered when a payment link is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-payment-link" id="ex-sdk-delete-payment-link-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-payment-link" id="ex-sdk-delete-payment-link-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-payment-link" id="ex-sdk-delete-payment-link-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-payment-link-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-payment-link-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deletePaymentLink</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deletePaymentLink (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deletePaymentLink (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-payment-link-0"><span className="wix-picker-name">deletePaymentLink</span></label>
    <label htmlFor="ex-sdk-delete-payment-link-1"><span className="wix-picker-name">deletePaymentLink (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-payment-link-2"><span className="wix-picker-name">deletePaymentLink (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deletePaymentLink
    import { paymentLinks } from '@wix/get-paid';

    async function deletePaymentLink(paymentLinkId) {
      const response = await paymentLinks.deletePaymentLink(paymentLinkId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deletePaymentLink (self-hosted)
    import { createClient } from '@wix/sdk';
    import { paymentLinks } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { paymentLinks },
      // Include the auth strategy and host as relevant
    });

    async function deletePaymentLink(paymentLinkId) {
      const response = await myWixClient.paymentLinks.deletePaymentLink(paymentLinkId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deletePaymentLink (with elevated permissions)
    import { paymentLinks } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myDeletePaymentLinkMethod(paymentLinkId) {
      const elevatedDeletePaymentLink = auth.elevate(paymentLinks.deletePaymentLink);
      const response = await elevatedDeletePaymentLink(paymentLinkId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
