---
title: "Get Pool"
description: "Retrieves a pool."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a pool.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/benefit-programs/v1/pools/{poolId}
    ```

    ## Path Params

    <ParamField path="poolId" type="string" required>

      ID of the pool to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="pool" type="Pool">

      Retrieved pool.

      See the [Pool Object](/api-reference/business-solutions/benefit-programs/pools/pool-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request GET https://www.wixapis.com/benefit-programs/v1/pools/e189a369-bb35-42c2-9a49-27c9a0a205d9 \
        -H "Authorization: <AUTH>" \
        -H "Content-Type: application/json"
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "pool": {
          "id": "e189a369-bb35-42c2-9a49-27c9a0a205d9",
          "revision": "3",
          "created_date": "2024-12-27T11:08:07.992Z",
          "updated_date": "2025-01-03T09:09:22.418Z",
          "program_definition_id": "e8c3366e-0c4f-3024-a034-1e0c79146d4c",
          "program_id": "8faed6d2-dfdf-44f1-8a71-bcc19d2fbdde",
          "status": "ACTIVE",
          "beneficiary": {
            "member_id": "d8f4a6c2-0e6a-4b89-bc3a-15e2dfe3c7a1",
            "identity_type": "MEMBER"
          },
          "details": {
            "benefits": [
              {
                "benefit_key": "001",
                "item_set_id": "3e5c0ffa-fdf9-4dd2-8841-70f30d873dd9",
                "price": "5",
                "additional_data": null,
                "provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                "display_name": "Group session",
                "description": null
              },
              {
                "benefit_key": "002",
                "item_set_id": "6995d94e-4bdf-46e9-a741-c6f40806e40e",
                "price": "15",
                "additional_data": null,
                "provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                "display_name": "Private session",
                "description": null
              }
            ],
            "credit_configuration": {
              "amount": "25",
              "rollover_configuration": {
                "enabled": true,
                "balance_cap": "30"
              },
              "unit_display_name": null
            },
            "additional_data": null
          },
          "pool_definition_id": "64244667-7c54-46da-8d8b-4a1a4ee9a598",
          "display_name": "Yoga courses",
          "namespace": "benefit_programs_app",
          "extended_fields": null,
          "program_definition": {
            "id": "e8c3366e-0c4f-3024-a034-1e0c79146d4c",
            "external_id": "myProgramDefinition"
          },
          "program": {
            "id": "8faed6d2-dfdf-44f1-8a71-bcc19d2fbdde",
            "external_id": "91000000-0000-0000-0000-000000000000"
          },
          "pool_definition_revision": "6",
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a pool.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { pools } from '@wix/benefit-programs';

    pools.getPool(poolId): Promise
    ```

    ## Method Parameters

    <ParamField body="poolId" type="string" required>

      ID of the pool to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Pool`.

    See the [Pool Object](/api-reference/business-solutions/benefit-programs/pools/pool-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-pool" id="ex-sdk-get-pool-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-pool" id="ex-sdk-get-pool-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-pool" id="ex-sdk-get-pool-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-pool-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-pool-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getPool</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPool (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPool (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-pool-0"><span className="wix-picker-name">getPool</span></label>
    <label htmlFor="ex-sdk-get-pool-1"><span className="wix-picker-name">getPool (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-pool-2"><span className="wix-picker-name">getPool (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getPool
    import { pools } from '@wix/benefit-programs';

    async function getPool(poolId) {
      const response = await pools.getPool(poolId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPool (self-hosted)
    import { createClient } from '@wix/sdk';
    import { pools } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { pools },
      // Include the auth strategy and host as relevant
    });

    async function getPool(poolId) {
      const response = await myWixClient.pools.getPool(poolId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPool (with elevated permissions)
    import { pools } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myGetPoolMethod(poolId) {
      const elevatedGetPool = auth.elevate(pools.getPool);
      const response = await elevatedGetPool(poolId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
