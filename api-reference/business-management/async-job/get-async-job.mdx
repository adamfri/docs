---
title: "Get Async Job"
description: "Retrieves a job."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a job.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ASYNC_JOB.READ">READ ASYNC JOBS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/async-jobs/v1/async-jobs/{jobId}
    ```

    ## Path Params

    <ParamField path="jobId" type="string" required>

      Job ID.

    </ParamField>

    ## Response Object

    <ResponseField name="job" type="AsyncJob">

      Returned job.

      See the [Async Job Object](/api-reference/business-management/async-job/async-job-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/async-jobs/v1/async-jobs/076e0505-6944-4645-a248-80b197ee9cfd' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "job": {
          "id": "076e0505-6944-4645-a248-80b197ee9cfd",
          "metadata": {
            "meta1": "value1"
          },
          "status": "INITIALIZED",
          "createdDate": "2024-04-22T10:23:25.798Z",
          "updatedDate": "2024-04-22T10:23:25.798Z",
          "createdBy": {
            "type": "APP_ID",
            "appIdOptions": {
              "appId": "338acb04-5b9b-4cdd-beb3-ea6739546d3a"
            }
          },
          "counts": {
            "total": null,
            "successCount": 0,
            "failCount": 0,
            "errorByCodeCount": {}
          },
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a job.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ASYNC_JOB.READ">READ ASYNC JOBS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { asyncJobs } from '@wix/async-jobs';

    asyncJobs.getAsyncJob(jobId): Promise
    ```

    ## Method Parameters

    <ParamField body="jobId" type="string" required>

      Job ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `AsyncJob`.

    See the [Async Job Object](/api-reference/business-management/async-job/async-job-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-async-job" id="ex-sdk-get-async-job-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-async-job" id="ex-sdk-get-async-job-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-async-job" id="ex-sdk-get-async-job-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-async-job-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-async-job-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getAsyncJob</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getAsyncJob (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getAsyncJob (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-async-job-0"><span className="wix-picker-name">getAsyncJob</span></label>
    <label htmlFor="ex-sdk-get-async-job-1"><span className="wix-picker-name">getAsyncJob (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-async-job-2"><span className="wix-picker-name">getAsyncJob (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getAsyncJob
    import { asyncJobs } from '@wix/async-jobs';

    async function getAsyncJob(jobId) {
      const response = await asyncJobs.getAsyncJob(jobId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getAsyncJob (self-hosted)
    import { createClient } from '@wix/sdk';
    import { asyncJobs } from '@wix/async-jobs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { asyncJobs },
      // Include the auth strategy and host as relevant
    });

    async function getAsyncJob(jobId) {
      const response = await myWixClient.asyncJobs.getAsyncJob(jobId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getAsyncJob (with elevated permissions)
    import { asyncJobs } from '@wix/async-jobs';
    import { auth } from '@wix/essentials';

    async function myGetAsyncJobMethod(jobId) {
      const elevatedGetAsyncJob = auth.elevate(asyncJobs.getAsyncJob);
      const response = await elevatedGetAsyncJob(jobId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
