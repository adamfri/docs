---
title: "Bulk Cancel Review Requests By Filter"
description: "Cancels review requests given specified filters."
icon: "rotate"
mode: "wide"
---

<span className="wix-method wix-method-put">PUT</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Cancels review requests given specified filters.

    When this method is used, a bulk job is started and the job ID is returned. The job might not complete right away, depending on its size.
    The job's status can be retrieved with [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/get-async-job)
    or [List Async Job Items](https://dev.wix.com/docs/rest/business-management/async-job/list-async-job-items).

    See [Query Review Requests](https://dev.wix.com/docs/rest/crm/community/reviews/review-requests/query-review-requests) for a list of supported filters.

    To check which review requests will be canceled with this filter, you can run the filter with
    [Query Review Requests](https://dev.wix.com/docs/rest/crm/community/reviews/review-requests/query-review-requests)
    and confirm which review requests are returned with that filter.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REVIEWS.MANAGE-REVIEW_REQUESTS">Manage Review Requests</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PUT https://www.wixapis.com/reviews/v2/bulk/review-requests/cancel-by-filter
    ```

    ## Body Params

    <ParamField body="filter" type="object">

      Filter object in the following format:
      `"filter" : {`
      "fieldName1": "value1",
      "fieldName2":&#123;"$operator":"value2"&#125;
      &#125;``

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Bulk job ID. The job's status can be retrieved with Get Bulk Job or List Bulk Jobs.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Review Request Updated | `wix.reviews.v2.review_request_updated` | Triggered when a review request is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/reviews/v2/review-requests/cancel-by-filter'  \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "filter":
              {
                "namespace": "stores"
              }
            }'
      ```

      ```json Response
      {
        "jobId": "15f470a4-a960-43bb-b1ed-886e1050efcd"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Cancels review requests given specified filters.

    When this method is used, a bulk job is started and the job ID is returned. The job might not complete right away, depending on its size.
    The job's status can be retrieved with [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/get-async-job)
    or [List Async Job Items](https://dev.wix.com/docs/rest/business-management/async-job/list-async-job-items).

    See [Query Review Requests](https://dev.wix.com/docs/rest/crm/community/reviews/review-requests/query-review-requests) for a list of supported filters.

    To check which review requests will be canceled with this filter, you can run the filter with
    [Query Review Requests](https://dev.wix.com/docs/rest/crm/community/reviews/review-requests/query-review-requests)
    and confirm which review requests are returned with that filter.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REVIEWS.MANAGE-REVIEW_REQUESTS">Manage Review Requests</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { reviewRequests } from '@wix/reviews';

    reviewRequests.bulkCancelReviewRequestsByFilter(options): Promise<BulkCancelReviewRequestsByFilterResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkCancelReviewRequestsByFilterOptions">

      options

      <Expandable title="options properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkCancelReviewRequestsByFilterResponse>`.

    <ResponseField name="jobId" type="string">

      Bulk job ID. The job's status can be retrieved with Get Bulk Job or List Bulk Jobs.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Review Request Updated | `wix.reviews.v2.review_request_updated` | Triggered when a review request is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-cancel-review-requests-by-filter" id="ex-sdk-bulk-cancel-review-requests-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-cancel-review-requests-by-filter" id="ex-sdk-bulk-cancel-review-requests-by-filter-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-cancel-review-requests-by-filter" id="ex-sdk-bulk-cancel-review-requests-by-filter-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-cancel-review-requests-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-cancel-review-requests-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkCancelReviewRequestsByFilter</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCancelReviewRequestsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCancelReviewRequestsByFilter (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-cancel-review-requests-by-filter-0"><span className="wix-picker-name">bulkCancelReviewRequestsByFilter</span></label>
    <label htmlFor="ex-sdk-bulk-cancel-review-requests-by-filter-1"><span className="wix-picker-name">bulkCancelReviewRequestsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-cancel-review-requests-by-filter-2"><span className="wix-picker-name">bulkCancelReviewRequestsByFilter (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkCancelReviewRequestsByFilter
    import { reviewRequests } from '@wix/reviews';

    async function bulkCancelReviewRequestsByFilter(options) {
      const response = await reviewRequests.bulkCancelReviewRequestsByFilter(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCancelReviewRequestsByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reviewRequests } from '@wix/reviews';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reviewRequests },
      // Include the auth strategy and host as relevant
    });

    async function bulkCancelReviewRequestsByFilter(options) {
      const response = await myWixClient.reviewRequests.bulkCancelReviewRequestsByFilter(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCancelReviewRequestsByFilter (with elevated permissions)
    import { reviewRequests } from '@wix/reviews';
    import { auth } from '@wix/essentials';

    async function myBulkCancelReviewRequestsByFilterMethod(options) {
      const elevatedBulkCancelReviewRequestsByFilter = auth.elevate(reviewRequests.bulkCancelReviewRequestsByFilter);
      const response = await elevatedBulkCancelReviewRequestsByFilter(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
