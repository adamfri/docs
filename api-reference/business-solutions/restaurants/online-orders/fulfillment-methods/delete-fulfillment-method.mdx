---
title: "Delete Fulfillment Method"
description: "Deletes a fulfillment method."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a fulfillment method.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/fulfillment-methods/v1/fulfillment-methods/{fulfillmentMethodId}
    ```

    ## Path Params

    <ParamField path="fulfillmentMethodId" type="string" required>

      The ID of the fulfillment method to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteFulfillmentMethodResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Fulfillment Method Deleted | `wix.restaurants.v1.fulfillment_method_deleted` | Triggered when a fulfillment method is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE https://www.wixapis.com/restaurants/v1/fulfillment-methods/e827109b-0319-4955-aaaa-05a968dd285f \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          --data-raw '{
             "fulfillment_method_id": "e827109b-0319-4955-aaaa-05a968dd285f"
          }'
      ~~~
      ```

      ```json Response
      ~~~json

      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a fulfillment method.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { fulfillmentMethods } from '@wix/restaurants';

    fulfillmentMethods.deleteFulfillmentMethod(fulfillmentMethodId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="fulfillmentMethodId" type="string" required>

      The ID of the fulfillment method to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Fulfillment Method Deleted | `wix.restaurants.v1.fulfillment_method_deleted` | Triggered when a fulfillment method is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-fulfillment-method" id="ex-sdk-delete-fulfillment-method-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-fulfillment-method" id="ex-sdk-delete-fulfillment-method-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-fulfillment-method" id="ex-sdk-delete-fulfillment-method-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-fulfillment-method-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-fulfillment-method-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteFulfillmentMethod</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteFulfillmentMethod (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteFulfillmentMethod (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-fulfillment-method-0"><span className="wix-picker-name">deleteFulfillmentMethod</span></label>
    <label htmlFor="ex-sdk-delete-fulfillment-method-1"><span className="wix-picker-name">deleteFulfillmentMethod (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-fulfillment-method-2"><span className="wix-picker-name">deleteFulfillmentMethod (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteFulfillmentMethod
    import { fulfillmentMethods } from '@wix/restaurants';

    async function deleteFulfillmentMethod(fulfillmentMethodId) {
      const response = await fulfillmentMethods.deleteFulfillmentMethod(fulfillmentMethodId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteFulfillmentMethod (self-hosted)
    import { createClient } from '@wix/sdk';
    import { fulfillmentMethods } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { fulfillmentMethods },
      // Include the auth strategy and host as relevant
    });

    async function deleteFulfillmentMethod(fulfillmentMethodId) {
      const response = await myWixClient.fulfillmentMethods.deleteFulfillmentMethod(fulfillmentMethodId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteFulfillmentMethod (with elevated permissions)
    import { fulfillmentMethods } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myDeleteFulfillmentMethodMethod(fulfillmentMethodId) {
      const elevatedDeleteFulfillmentMethod = auth.elevate(fulfillmentMethods.deleteFulfillmentMethod);
      const response = await elevatedDeleteFulfillmentMethod(fulfillmentMethodId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
