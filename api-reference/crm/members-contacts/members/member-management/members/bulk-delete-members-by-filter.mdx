---
title: "Bulk Delete Members By Filter"
description: "Deletes multiple members by the specified filter."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple members by the specified filter.

    All content created by these members is transferred to a site owner's account. For example, if Wix users had blog posts, those posts are transferred to the site owner's account, which then becomes the owner of those posts.

    To delete only the current member, call [Delete My Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/delete-my-member).

    To delete a single member by `ID`, call [Delete Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/delete-member).

    To delete multiple members by `memberId`, call [Bulk Delete Members](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/bulk-delete-members).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/members/v1/members/bulk/delete-by-filter
    ```

    ## Body Params

    <ParamField body="filter" type="any" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

    </ParamField>

    <ParamField body="contentAssigneeId" type="string">

      ID of a member receiving the deleted member's content.

    </ParamField>

    <ParamField body="search" type="Search">

      Plain text search.

      <Expandable title="search properties">

        <ParamField body="expression" type="string">

          Search term or expression.

        </ParamField>

        <ParamField body="fields" type="string[]">

          Currently supported fields for search:

          - `loginEmail`
          - `contact.firstName`
          - `contact.lastName`
          - `profile.title`
          - `profile.nickname`
          - `profile.slug`

          Default: `profile.nickname`.

          Constraints: `maxItems 4`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID.
      Specify this ID when calling [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/introduction) to retrieve job details and metadata.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        https://www.wixapis.com/members/v1/members/bulk/delete-by-filter \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>'
        -d '{
            "filter": {
                "status": {
                  "$eq": "BLOCKED"
              }
          },
          "contentAssigneeId": "8f6fd6fd-eed1-4e13-8abd-27e8d61ad7be"
        },
      ```

      ```json Response
      {
        "jobId": "6c79d38a-f262-47df-915b-e544d26e3718"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple members by the specified filter.

    All content created by these members is transferred to a site owner's account. For example, if Wix users had blog posts, those posts are transferred to the site owner's account, which then becomes the owner of those posts.

    To delete only the current member, call [Delete My Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/delete-my-member).

    To delete a single member by `ID`, call [Delete Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/delete-member).

    To delete multiple members by `memberId`, call [Bulk Delete Members](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/bulk-delete-members).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { members } from '@wix/members';

    members.bulkDeleteMembersByFilter(filter, options): Promise<BulkDeleteMembersByFilterResponse>
    ```

    ## Method Parameters

    <ParamField body="filter" type="any" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

    </ParamField>

    <ParamField body="options" type="BulkDeleteMembersByFilterOptions">

      options

      <Expandable title="options properties">

        <ParamField body="contentAssigneeId" type="string">

          ID of a member receiving the deleted member's content.

        </ParamField>

        <ParamField body="search" type="Search">

          Plain text search.

          <Expandable title="search properties">

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Currently supported fields for search:

              - `loginEmail`
              - `contact.firstName`
              - `contact.lastName`
              - `profile.title`
              - `profile.nickname`
              - `profile.slug`

              Default: `profile.nickname`.

              Constraints: `maxItems 4`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkDeleteMembersByFilterResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID.
      Specify this ID when calling [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/introduction) to retrieve job details and metadata.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-members-by-filter" id="ex-sdk-bulk-delete-members-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-members-by-filter" id="ex-sdk-bulk-delete-members-by-filter-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-members-by-filter" id="ex-sdk-bulk-delete-members-by-filter-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-members-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-members-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteMembersByFilter</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteMembersByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteMembersByFilter (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-members-by-filter-0"><span className="wix-picker-name">bulkDeleteMembersByFilter</span></label>
    <label htmlFor="ex-sdk-bulk-delete-members-by-filter-1"><span className="wix-picker-name">bulkDeleteMembersByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-members-by-filter-2"><span className="wix-picker-name">bulkDeleteMembersByFilter (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteMembersByFilter
    import { members } from '@wix/members';

    async function bulkDeleteMembersByFilter(filter,options) {
      const response = await members.bulkDeleteMembersByFilter(filter,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteMembersByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { members } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { members },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteMembersByFilter(filter,options) {
      const response = await myWixClient.members.bulkDeleteMembersByFilter(filter,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteMembersByFilter (with elevated permissions)
    import { members } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteMembersByFilterMethod(filter,options) {
      const elevatedBulkDeleteMembersByFilter = auth.elevate(members.bulkDeleteMembersByFilter);
      const response = await elevatedBulkDeleteMembersByFilter(filter,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
