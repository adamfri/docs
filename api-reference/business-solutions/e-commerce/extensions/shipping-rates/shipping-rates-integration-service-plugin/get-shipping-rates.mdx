---
title: "Get Shipping Rates"
description: "This method retrieves applicable shipping rates for a delivery from your app."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    This method retrieves applicable shipping rates for a delivery from your app.

    Wix calls this method when certain actions are performed on the cart and/or checkout. For example, when an item is added to the cart, or the shipping destination is changed.

    <Note>

    Do not call the Estimate Cart Totals, Estimate Current Cart Totals or Get Checkout methods from your implementation code for Get Shipping Rates. Doing so will result in an error.

    </Note>

    ## Endpoint

    ```http
    POST /v1/getRates
    ```

    ## Body Params

    <ParamField body="lineItems" type="ProductItem[]">

      The line items to be shipped.

      Constraints: `minItems 1`, `maxItems 500`.

      <Expandable title="lineItems properties">

        <ParamField body="name" type="string">

          Item name.

        </ParamField>

        <ParamField body="quantity" type="integer">

          The number of items ordered.

          Constraints: `min 0`, `max 999999999`.

        </ParamField>

        <ParamField body="catalogReference" type="CatalogReference">

          Reference to the item's origin catalog.

          <Expandable title="catalogReference properties">

            <ParamField body="catalogItemId" type="string">

              ID of the item within the catalog it belongs to.

              Constraints: `minLength 1`, `maxLength 36`.

            </ParamField>

            <ParamField body="appId" type="string">

              ID of the app providing the catalog.

              You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

              For items from Wix catalogs, the following values always apply:
              + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
              + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
              + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

              Constraints: `minLength 1`.

            </ParamField>

            <ParamField body="options" type="object">

              Additional item details in `key:value` pairs.

              Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

              For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
              or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="physicalProperties" type="PhysicalProperties">

          Physical properties of the item.

          <Expandable title="physicalProperties properties">

            <ParamField body="weight" type="number">

              Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request.

            </ParamField>

            <ParamField body="sku" type="string">

              Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).

            </ParamField>

            <ParamField body="shippable" type="boolean">

              Whether this line item is shippable.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="price" type="string">

          Price of a single item after discounts.

          Constraints: `format DECIMAL_VALUE`.

        </ParamField>

        <ParamField body="totalPriceBeforeDiscount" type="string">

          Total line item price before discounts.

        </ParamField>

        <ParamField body="priceBeforeDiscount" type="string">

          Price of a single item before discounts.

        </ParamField>

        <ParamField body="totalPrice" type="string">

          Total line item price after discounts. This is equal to `price` multiplied by `quantity`.

        </ParamField>

        <ParamField body="deliveryDestinationIndex" type="integer">

          item delivery destination index in the shipping addresses list

        </ParamField>

        <ParamField body="deliveryOriginIndex" type="integer">

          where the item is delivered from index in the from addresses list

        </ParamField>

        <ParamField body="taxIncludedInPrice" type="boolean">

          whether tax is included in line item price

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="shippingDestination" type="Address">

      Address to ship to.

      <Expandable title="shippingDestination properties">

        <ParamField body="country" type="string">

          Country code.

        </ParamField>

        <ParamField body="subdivision" type="string">

          Subdivision shorthand. Usually, a short code (2 or 3 letters) that represents a state, region, prefecture, or province. e.g. NY

        </ParamField>

        <ParamField body="city" type="string">

          City name.

        </ParamField>

        <ParamField body="postalCode" type="string">

          Zip/postal code.

        </ParamField>

        <ParamField body="addressLine2" type="string">

          Free text providing more detailed address info. Usually contains Apt, Suite, and Floor.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="street" type="street">

          street

          <Expandable title="street properties">

            <ParamField body="streetAddress" type="StreetAddress">

              Street name and number.

              _Nested structure omitted for brevity — see the `StreetAddress` object._

            </ParamField>

            <ParamField body="addressLine" type="string">

              Main address line, usually street and number as free text.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="shippingOrigin" type="Address">

      Address item is shipped from. This is the site owner's business address.

      <Expandable title="shippingOrigin properties">

        <ParamField body="country" type="string">

          Country code.

        </ParamField>

        <ParamField body="subdivision" type="string">

          Subdivision shorthand. Usually, a short code (2 or 3 letters) that represents a state, region, prefecture, or province. e.g. NY

        </ParamField>

        <ParamField body="city" type="string">

          City name.

        </ParamField>

        <ParamField body="postalCode" type="string">

          Zip/postal code.

        </ParamField>

        <ParamField body="addressLine2" type="string">

          Free text providing more detailed address info. Usually contains Apt, Suite, and Floor.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="street" type="street">

          street

          <Expandable title="street properties">

            <ParamField body="streetAddress" type="StreetAddress">

              Street name and number.

              _Nested structure omitted for brevity — see the `StreetAddress` object._

            </ParamField>

            <ParamField body="addressLine" type="string">

              Main address line, usually street and number as free text.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="buyerContactDetails" type="FullAddressContactDetails">

      Buyer's contact details.

      <Expandable title="buyerContactDetails properties">

        <ParamField body="firstName" type="string">

          Contact's first name.

        </ParamField>

        <ParamField body="lastName" type="string">

          Contact's last name.

        </ParamField>

        <ParamField body="phone" type="string">

          Contact's phone number.

        </ParamField>

        <ParamField body="company" type="string">

          Contact's company name.

        </ParamField>

        <ParamField body="email" type="string">

          Email associated with the address.

        </ParamField>

        <ParamField body="vatId" type="VatId">

          Tax info. Currently usable only in Brazil.

          <Expandable title="vatId properties">

            <ParamField body="id" type="string">

              Customer's tax ID.

            </ParamField>

            <ParamField body="type" type="string">

              Tax type.

              Supported values:
              + `CPF`: for individual tax payers
              + `CNPJ`: for corporations

              **Supported values:**

              - `UNSPECIFIED`
              - `CPF` — CPF - for individual tax payers.
              - `CNPJ` — CNPJ - for corporations

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="weightUnit" type="string">

      Weight measurement unit used for all items in the shipment.

      Supported values:
      + `"KG"`: kilograms
      + `"LB"`: pounds

      **Supported values:**

      - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
      - `KG` — Kilograms.
      - `LB` — Pounds.

    </ParamField>

    <ParamField body="taxIncludedInPrices" type="boolean" deprecated>

      Whether tax is included in the items' prices.

      **Deprecated.** Whether tax is included in the items' prices. Removal: 2025-05-01.

    </ParamField>

    <ParamField body="deliveryPreferences" type="DeliveryPreferences">

      List of delivery preferences, for example preferred delivery code (`shippingOptionId`).

      <Expandable title="deliveryPreferences properties">

        <ParamField body="preferredCode" type="string" />

      </Expandable>

    </ParamField>

    <ParamField body="purchaseFlowId" type="string">

      Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

    </ParamField>

    <ParamField body="externalReferences" type="ExternalReference[]">

      Reference to an external resource ID. Used to link the purchase flow to a specific entity in an external system.
      Used for integration and tracking across different platforms.

      **Developer Preview.** Constraints: `maxItems 20`.

      <Expandable title="externalReferences properties">

        <ParamField body="appId" type="string">

          ID of the app associated with the purchase flow.
          For example, the Wix Pay Links app ID.

          Constraints: `format GUID`, `immutable`.

        </ParamField>

        <ParamField body="resourceId" type="string">

          Reference to an external resource ID. Used to link the purchase flow to a specific entity in an external system.
          For example, a Wix Pay Link ID.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="extendedFields" type="ExtendedFields">

      [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

      **Developer Preview.**

      <Expandable title="extendedFields properties">

        <ParamField body="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="object" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="shippingRates" type="ShippingOption[]">

      Available shipping rates. These define the shipping rate options that are displayed to site visitors on the cart and checkout pages.

      <Expandable title="shippingRates properties">

        <ResponseField name="code" type="string">

          Unique code that acts as an ID for a shipping rate. For example, `"usps_std_overnight"`.

        </ResponseField>

        <ResponseField name="title" type="string">

          Shipping rate title. For example, `"USPS Standard Overnight Delivery"`, `"Standard"`, or `"First-Class Package International"`.

        </ResponseField>

        <ResponseField name="logistics" type="DeliveryLogistics">

          Shipping logistics.

          <Expandable title="logistics properties">

            <ResponseField name="deliveryTime" type="string">

              When the item is expected to be delivered in free text. For example, `"3-5 business days"`.

            </ResponseField>

            <ResponseField name="instructions" type="string">

              Instructions for delivery. For example, for pickup: `"Ensure to come during business hours, and please don't park in the disabled spot"`.

            </ResponseField>

            <ResponseField name="pickupDetails" type="PickupDetails">

              Pickup details. Should be returned only if order is for pickup.

              _Nested structure omitted for brevity — see the `PickupDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="cost" type="ShippingPrice">

          Shipping cost.

          <Expandable title="cost properties">

            <ResponseField name="price" type="string">

              The shipping rate's price. Must align with the [currency's decimal separator](https://en.wikipedia.org/wiki/ISO_4217#Active_codes).

              Constraints: `format DECIMAL_VALUE`.

            </ResponseField>

            <ResponseField name="currency" type="string">

              Currency of the shipping rate price as a 3-letter [ISO-4217 currency code](https://en.wikipedia.org/wiki/ISO_4217). Must align with the `currency` passed to the function under the `metadata` field.

              Constraints: `format CURRENCY`.

            </ResponseField>

            <ResponseField name="additionalCharges" type="AdditionalCharge[]">

              Additional costs. For example, a handling fee for packaging fragile items.

              _Nested structure omitted for brevity — see the `AdditionalCharge[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `MISSING_POSTAL_CODE` | Missing postal code error. Throw this error when a postal code is not provided in the request. This error tells Wix that a valid postal code is required to calculate the shipping rate. You can optionally provide error data to help debug the issue. Error data you provide appears in the monitoring log but is not exposed to the user. This error may block the site visitor from proceeding to checkout, so throw it only if there is no other way to proceed. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_POSTAL_CODE` | Invalid postal code error. Throw this error when an invalid postal code is provided in the request. This error tells Wix that a valid postal code is required to calculate the shipping rate. You can optionally provide error data to help debug the issue. Error data you provide appears in the monitoring log but is not exposed to the user. This error may block the site visitor from proceeding to checkout, so throw it only if there is no other way to proceed. |
    | `400` | `INVALID_ARGUMENT` | `PARTIAL_ADDRESS` | Invalid address error. Throw this error when one or more fields in the address provided are invalid. This error tells Wix that more address details are needed to calculate the shipping rate. Wix may then prompt the user to provide more detailed address information. The error data must include the invalid fields in the address. For example: `ShippingDestination.subdivision`. |
    | `400` | `INVALID_ARGUMENT` | `GENERIC_ERROR` | Generic shipping rate calculation error. Throw this error for cases the other errors don't cover. You can optionally provide error data to help debug the issue. Error data you provide appears in the monitoring log but is not exposed to the user. This error may block the site visitor from proceeding to checkout, so throw it only if there is no other way to proceed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    The data payload will include the following object as an encoded JWT. For the purposes of this example, we show the request and response objects decoded.

    <CodeGroup>

      ```bash Request
      curl -X POST https://provider.example.com/v1/getRates \
          -H 'user-agent: Wix' \
          -H 'accept-encoding: gzip, deflate' \
          -H 'content-type: text/plain; charset=utf-8' \
          -d '{
              "data": {
                "request": {
                  "lineItems": [
                    {
                      "physicalProperties": {
                        "weight": 0.4,
                        "sku": "",
                        "shippable": true
                      },
                      "name": "coffee mug",
                      "quantity": 1,
                      "price": "100.00",
                      "catalogReference": {
                        "catalogItemId": "50b7223c-80e6-3539-325e-4ecfe99770dc",
                        "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                        "options": {
                          "options": {
                            "Size": "256",
                            "Color": "blue"
                          },
                          "variantId": "9d1bc95e-d6ed-4546-b89b-c61e334e2d5e"
                        }
                      }
                    }
                  ],
                  "buyerContactDetails": {
                    "phone": "123456789",
                    "email": "janedoe@gmail.com"
                  },
                  "taxIncludedInPrices": false,
                  "weightUnit": "LB",
                  "shippingDestination": {
                    "country": "US",
                    "subdivision": "US-NY",
                    "city": "New York",
                    "postalCode": "10011",
                    "addressLine": "235 West 23rd Street",
                    "addressLine2": "3rd floor",
                    "countryFullname": "United States",
                    "subdivisionFullname": "New York"
                  },
                  "shippingOrigin": {
                    "addressLine1": "34 Elizabeth Street",
                    "addressLine2": "4th floor",
                    "city": "Boston",
                    "country": "US",
                    "postalCode": "02108",
                    "subdivision": "US-MA"
                  }
                },
                "metadata": {
                  "instanceId": "9698acde-2e44-4656-9db3-f48c91da9828",
                  "languages": [
                    "en"
                  ],
                  "currency": "USD",
                  "requestId": "1670756264.11667254865110420",
                  "identity": {
                    "identityType": "ANONYMOUS_VISITOR",
                    "anonymousVisitorId": "a02f16af-6174-4f3e-a1e1-6eed2735d67d"
                  }
                },
                "aud": "e6a6c88b-8ed1-465b-8c10-5bb0e9285e3f",
                "iss": "wix.com",
                "iat": 1670756264,
                "exp": 1674356264
                }
              }
            }

      {
        "shippingRates": [
          {
            "code": "usps_international",
            "title": "USPS - International",
            "logistics": {
              "deliveryTime": "2-5 days"
            },
            "cost": {
              "price": "15",
              "currency": "USD",
              "additionalCharges": [
                {
                  "price": "10",
                  "type": "HANDLING_FEE",
                  "details": "Handling fee of $5 applied for fragile items."
                }
              ]
            }
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    This method retrieves applicable shipping rates for a delivery from your app.

    Wix calls this method when certain actions are performed on the cart and/or checkout. For example, when an item is added to the cart, or the shipping destination is changed.

    <Note>

    Do not call the Estimate Cart Totals, Estimate Current Cart Totals or Get Checkout methods from your implementation code for Get Shipping Rates. Doing so will result in an error.

    </Note>

    ## Method Declaration

    ```typescript
    import { shippingRates } from '@wix/ecom';

    shippingRates.getShippingRates(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="GetShippingRatesRequest">

      request

      <Expandable title="request properties">

        <ParamField body="lineItems" type="ProductItem[]">

          The line items to be shipped.

          Constraints: `minItems 1`, `maxItems 500`.

          <Expandable title="lineItems properties">

            <ParamField body="name" type="string">

              Item name.

            </ParamField>

            <ParamField body="quantity" type="integer">

              The number of items ordered.

              Constraints: `min 0`, `max 999999999`.

            </ParamField>

            <ParamField body="catalogReference" type="CatalogReference">

              Reference to the item's origin catalog.

              _Nested structure omitted for brevity — see the `CatalogReference` object._

            </ParamField>

            <ParamField body="physicalProperties" type="PhysicalProperties">

              Physical properties of the item.

              _Nested structure omitted for brevity — see the `PhysicalProperties` object._

            </ParamField>

            <ParamField body="price" type="string">

              Price of a single item after discounts.

              Constraints: `format DECIMAL_VALUE`.

            </ParamField>

            <ParamField body="totalPriceBeforeDiscount" type="string">

              Total line item price before discounts.

            </ParamField>

            <ParamField body="priceBeforeDiscount" type="string">

              Price of a single item before discounts.

            </ParamField>

            <ParamField body="totalPrice" type="string">

              Total line item price after discounts. This is equal to `price` multiplied by `quantity`.

            </ParamField>

            <ParamField body="deliveryDestinationIndex" type="integer">

              item delivery destination index in the shipping addresses list

            </ParamField>

            <ParamField body="deliveryOriginIndex" type="integer">

              where the item is delivered from index in the from addresses list

            </ParamField>

            <ParamField body="taxIncludedInPrice" type="boolean">

              whether tax is included in line item price

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="shippingDestination" type="Address">

          Address to ship to.

          <Expandable title="shippingDestination properties">

            <ParamField body="location" type="Location">

              location

              _Nested structure omitted for brevity — see the `Location` object._

            </ParamField>

            <ParamField body="streetAddress" type="StreetAddress">

              streetAddress

              _Nested structure omitted for brevity — see the `StreetAddress` object._

            </ParamField>

            <ParamField body="city" type="string" />

            <ParamField body="subdivision" type="string" />

            <ParamField body="country" type="string" />

            <ParamField body="postalCode" type="string" />

            <ParamField body="formatted" type="string" />

            <ParamField body="addressLine1" type="string" />

            <ParamField body="addressLine2" type="string" />

          </Expandable>

        </ParamField>

        <ParamField body="shippingOrigin" type="Address">

          Address item is shipped from. This is the site owner's business address.

          <Expandable title="shippingOrigin properties">

            <ParamField body="location" type="Location">

              location

              _Nested structure omitted for brevity — see the `Location` object._

            </ParamField>

            <ParamField body="streetAddress" type="StreetAddress">

              streetAddress

              _Nested structure omitted for brevity — see the `StreetAddress` object._

            </ParamField>

            <ParamField body="city" type="string" />

            <ParamField body="subdivision" type="string" />

            <ParamField body="country" type="string" />

            <ParamField body="postalCode" type="string" />

            <ParamField body="formatted" type="string" />

            <ParamField body="addressLine1" type="string" />

            <ParamField body="addressLine2" type="string" />

          </Expandable>

        </ParamField>

        <ParamField body="buyerContactDetails" type="FullAddressContactDetails">

          Buyer's contact details.

          <Expandable title="buyerContactDetails properties">

            <ParamField body="firstName" type="string">

              Contact's first name.

            </ParamField>

            <ParamField body="lastName" type="string">

              Contact's last name.

            </ParamField>

            <ParamField body="phone" type="string">

              Contact's phone number.

            </ParamField>

            <ParamField body="company" type="string">

              Contact's company name.

            </ParamField>

            <ParamField body="email" type="string">

              Email associated with the address.

            </ParamField>

            <ParamField body="vatId" type="VatId">

              Tax info. Currently usable only in Brazil.

              _Nested structure omitted for brevity — see the `VatId` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="weightUnit" type="string">

          Weight measurement unit used for all items in the shipment.

          Supported values:
          + `"KG"`: kilograms
          + `"LB"`: pounds

          **Supported values:**

          - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
          - `KG` — Kilograms.
          - `LB` — Pounds.

        </ParamField>

        <ParamField body="taxIncludedInPrices" type="boolean" deprecated>

          Whether tax is included in the items' prices.

          **Deprecated.** Whether tax is included in the items' prices. Removal: 2025-05-01.

        </ParamField>

        <ParamField body="deliveryPreferences" type="DeliveryPreferences">

          List of delivery preferences, for example preferred delivery code (`shippingOptionId`).

          <Expandable title="deliveryPreferences properties">

            <ParamField body="preferredCode" type="string" />

          </Expandable>

        </ParamField>

        <ParamField body="purchaseFlowId" type="string">

          Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

        </ParamField>

        <ParamField body="externalReferences" type="ExternalReference[]">

          Reference to an external resource ID. Used to link the purchase flow to a specific entity in an external system.
          Used for integration and tracking across different platforms.

          **Developer Preview.** Constraints: `maxItems 20`.

          <Expandable title="externalReferences properties">

            <ParamField body="appId" type="string">

              ID of the app associated with the purchase flow.
              For example, the Wix Pay Links app ID.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="resourceId" type="string">

              Reference to an external resource ID. Used to link the purchase flow to a specific entity in an external system.
              For example, a Wix Pay Link ID.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          **Developer Preview.**

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="shippingRates" type="ShippingOption[]">

      Available shipping rates. These define the shipping rate options that are displayed to site visitors on the cart and checkout pages.

      <Expandable title="shippingRates properties">

        <ResponseField name="code" type="string">

          Unique code that acts as an ID for a shipping rate. For example, `"usps_std_overnight"`.

        </ResponseField>

        <ResponseField name="title" type="string">

          Shipping rate title. For example, `"USPS Standard Overnight Delivery"`, `"Standard"`, or `"First-Class Package International"`.

        </ResponseField>

        <ResponseField name="logistics" type="DeliveryLogistics">

          Shipping logistics.

          <Expandable title="logistics properties">

            <ResponseField name="deliveryTime" type="string">

              When the item is expected to be delivered in free text. For example, `"3-5 business days"`.

            </ResponseField>

            <ResponseField name="instructions" type="string">

              Instructions for delivery. For example, for pickup: `"Ensure to come during business hours, and please don't park in the disabled spot"`.

            </ResponseField>

            <ResponseField name="pickupDetails" type="PickupDetails">

              Pickup details. Should be returned only if order is for pickup.

              _Nested structure omitted for brevity — see the `PickupDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="cost" type="ShippingPrice">

          Shipping cost.

          <Expandable title="cost properties">

            <ResponseField name="price" type="string">

              The shipping rate's price. Must align with the [currency's decimal separator](https://en.wikipedia.org/wiki/ISO_4217#Active_codes).

              Constraints: `format DECIMAL_VALUE`.

            </ResponseField>

            <ResponseField name="currency" type="string">

              Currency of the shipping rate price as a 3-letter [ISO-4217 currency code](https://en.wikipedia.org/wiki/ISO_4217). Must align with the `currency` passed to the function under the `metadata` field.

              Constraints: `format CURRENCY`.

            </ResponseField>

            <ResponseField name="additionalCharges" type="AdditionalCharge[]">

              Additional costs. For example, a handling fee for packaging fragile items.

              _Nested structure omitted for brevity — see the `AdditionalCharge[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `MISSING_POSTAL_CODE` | Missing postal code error. Throw this error when a postal code is not provided in the request. This error tells Wix that a valid postal code is required to calculate the shipping rate. You can optionally provide error data to help debug the issue. Error data you provide appears in the monitoring log but is not exposed to the user. This error may block the site visitor from proceeding to checkout, so throw it only if there is no other way to proceed. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_POSTAL_CODE` | Invalid postal code error. Throw this error when an invalid postal code is provided in the request. This error tells Wix that a valid postal code is required to calculate the shipping rate. You can optionally provide error data to help debug the issue. Error data you provide appears in the monitoring log but is not exposed to the user. This error may block the site visitor from proceeding to checkout, so throw it only if there is no other way to proceed. |
    | `400` | `INVALID_ARGUMENT` | `PARTIAL_ADDRESS` | Invalid address error. Throw this error when one or more fields in the address provided are invalid. This error tells Wix that more address details are needed to calculate the shipping rate. Wix may then prompt the user to provide more detailed address information. The error data must include the invalid fields in the address. For example: `ShippingDestination.subdivision`. |
    | `400` | `INVALID_ARGUMENT` | `GENERIC_ERROR` | Generic shipping rate calculation error. Throw this error for cases the other errors don't cover. You can optionally provide error data to help debug the issue. Error data you provide appears in the monitoring log but is not exposed to the user. This error may block the site visitor from proceeding to checkout, so throw it only if there is no other way to proceed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-shipping-rates" id="ex-sdk-get-shipping-rates-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-shipping-rates" id="ex-sdk-get-shipping-rates-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-shipping-rates-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-shipping-rates-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Example of a shippingRates return value</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getShippingRates (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-shipping-rates-0"><span className="wix-picker-name">Example of a shippingRates return value</span></label>
    <label htmlFor="ex-sdk-get-shipping-rates-1"><span className="wix-picker-name">getShippingRates (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Example of a shippingRates return value
    import { shippingRates } from '@wix/ecom/service-plugins';

    shippingRates.provideHandlers({

      getShippingRates: async ( payload ) => {
        const {request, metadata} = payload;
            // Use the `request` and `metadata` received from Wix and
            // apply custom logic.
          return {
            // Return your response exactly as documented to integrate with Wix.
            // Return value example:
            shippingRates: [
              {
                code: "usps-international",
                title: "USPS - International",
                logistics: {
                  deliveryTime: "2-5 days",
                },
                cost: {
                  price: "15",
                  currency: "USD",
                  additionalCharges: [
                    {
                      price: "10",
                      type: "HANDLING_FEE",
                      details: "Handling fee of $5 applied for fragile items.",
                    }
                  ]
                }
              }
            ]
          }
        }
    });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getShippingRates (self-hosted)
    import { createClient } from '@wix/sdk';
    import { shippingRates } from '@wix/ecom/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { shippingRates },
      // Include the auth strategy and host as relevant
    });

    async function getShippingRates(request,metadata) {
      const response = await myWixClient.shippingRates.getShippingRates(request,metadata);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
