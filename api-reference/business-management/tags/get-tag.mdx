---
title: "Get Tag"
description: "Retrieves a tag."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    Retrieves a tag.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-OS.READ-TAGS">Read Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/tags/v1/tags/{tagId}
    ```

    ## Path Params

    <ParamField path="tagId" type="string" required>

      The ID of the tag to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="tag" type="Tag">

      The requested tag.

      See the [Tag Object](/api-reference/business-management/tags/tag-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a tag.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-OS.READ-TAGS">Read Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tags } from '@wix/tags';

    tags.getTag(tagId): Promise<Tag>
    ```

    ## Method Parameters

    <ParamField body="tagId" type="string" required>

      The ID of the tag to retrieve.

      Constraints: `maxLength 5`.

    </ParamField>

    ## Returns

    Returns `Promise<Tag>`.

    See the [Tag Object](/api-reference/business-management/tags/tag-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-tag" id="ex-sdk-get-tag-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-tag" id="ex-sdk-get-tag-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-tag" id="ex-sdk-get-tag-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-tag-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-tag-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getTag</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTag (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-tag-0"><span className="wix-picker-name">getTag</span></label>
    <label htmlFor="ex-sdk-get-tag-1"><span className="wix-picker-name">getTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-tag-2"><span className="wix-picker-name">getTag (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getTag
    import { tags } from '@wix/tags';

    async function getTag(tagId) {
      const response = await tags.getTag(tagId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTag (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tags } from '@wix/tags';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tags },
      // Include the auth strategy and host as relevant
    });

    async function getTag(tagId) {
      const response = await myWixClient.tags.getTag(tagId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTag (with elevated permissions)
    import { tags } from '@wix/tags';
    import { auth } from '@wix/essentials';

    async function myGetTagMethod(tagId) {
      const elevatedGetTag = auth.elevate(tags.getTag);
      const response = await elevatedGetTag(tagId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
