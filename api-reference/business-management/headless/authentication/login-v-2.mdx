---
title: "Login V 2"
description: "Logs in an existing member."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Logs in an existing member.

    Typically, after a successful login, you generate and use member tokens for the
    logged-in member so that subsequent API calls are called as part of a member session.
    Use the `sessionToken` in the response to [get the site member's access and refresh tokens](https://dev.wix.com/docs/go-headless/coding/rest-api/visitors-and-members/handle-members-with-custom-login#get-the-site-members-access-and-refresh-tokens).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/iam/authentication/v2/login
    ```

    ## Body Params

    <ParamField body="loginId" type="LoginId" required>

      Identifier of identity logging in.

      <Expandable title="One of the following">

        <ParamField body="type" type="type">

          type

          <Expandable title="type properties">

            <ParamField body="email" type="string">

              Login email address.

              Constraints: `format EMAIL`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="password" type="string">

      Password of the identity logging in.

    </ParamField>

    <ParamField body="captchaTokens" type="CaptchaToken[]">

      CAPTCHA tokens, when CAPTCHA setting is on.

    </ParamField>

    <ParamField body="clientMetaData" type="object">

      Additional data that's relevant for the flow.

    </ParamField>

    <ParamField body="stateToken" type="string">

      State token from a login flow that was started beforehand.

      When provided, this request continues an in-progress login flow rather than starting a new one.

    </ParamField>

    ## Response Object

    <ResponseField name="state" type="string">

      Current state of the login or registration process.

      **Supported values:**

      - `UNKNOWN_STATE` — Initial unknown state.
      - `SUCCESS` — Login completed successfully.
      - `REQUIRE_OWNER_APPROVAL` — Member needs the owner to approve their registration.
      - `REQUIRE_EMAIL_VERIFICATION` — Member needs to verify their email.
      - `STATUS_CHECK` — The `status` is not one that prevents the member logging in, meaning it's not `OFFLINE`, `BLOCKED`, or `DELETED`.
      - `REQUIRE_MISSING_FIELDS` — Member is missing fields that are marked as required on the site's signup form, and needs to complete them to finish registration.

    </ResponseField>

    <ResponseField name="sessionToken" type="string">

      Session token. If `state` is not `SUCCESS`, this field is undefined.

    </ResponseField>

    <ResponseField name="stateToken" type="string">

      Token that represents the current state of the login or registration process.

    </ResponseField>

    <ResponseField name="identity" type="Identity">

      Identity of the current member.

      <Expandable title="identity properties">

        <ResponseField name="id" type="string">

          Identity ID.

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number, which increments by 1 each time the identity is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the identity.

          Ignored when creating an identity.

        </ResponseField>

        <ResponseField name="createdDate" type="datetime">

          Date and time the identity was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updatedDate" type="datetime">

          Date and time the identity was updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="connections" type="Connection[]">

          The identity configured connections to authenticate with.

        </ResponseField>

        <ResponseField name="identityProfile" type="IdentityProfile">

          Identity profile.

          <Expandable title="identityProfile properties">

            <ResponseField name="firstName" type="string">

              Member's first name.

            </ResponseField>

            <ResponseField name="lastName" type="string">

              Member's last name.

            </ResponseField>

            <ResponseField name="nickname" type="string">

              Member's display nickname.

            </ResponseField>

            <ResponseField name="picture" type="string">

              URL to member's profile picture.

            </ResponseField>

            <ResponseField name="emails" type="string[]" deprecated>

              Deprecated. Use `secondaryEmails` instead.

              **Deprecated.** Deprecated. Use `secondaryEmails` instead. Use `secondary_emails` instead. Removal: 2023-11-01.

            </ResponseField>

            <ResponseField name="phones" type="string[]" deprecated>

              Deprecated. Use `phonesV2` instead.

              **Deprecated.** Deprecated. Use `phonesV2` instead. Use `phones_v2` instead. Removal: 2023-11-01.

            </ResponseField>

            <ResponseField name="labels" type="string[]">

              Labels associated with the member.

              Constraints: `maxItems 2000`.

            </ResponseField>

            <ResponseField name="language" type="string">

              Member's preferred language code.

            </ResponseField>

            <ResponseField name="privacyStatus" type="string">

              Member's profile visibility setting.

              **Supported values:**

              - `UNDEFINED` — No privacy status defined.
              - `PUBLIC` — Profile details are visible to other members.
              - `PRIVATE` — Profile details are hidden from other members.

            </ResponseField>

            <ResponseField name="customFields" type="CustomField[]">

              Any number of custom fields. [Custom fields](https://support.wix.com/en/article/adding-custom-fields-to-contacts)
              are used to store additional information about your site or app's contacts.

              _Nested structure omitted for brevity — see the `CustomField[]` object._

            </ResponseField>

            <ResponseField name="secondaryEmails" type="SecondaryEmail[]">

              Additional email addresses for the member.

              _Nested structure omitted for brevity — see the `SecondaryEmail[]` object._

            </ResponseField>

            <ResponseField name="phonesV2" type="Phone[]">

              Phone numbers associated with the member.

              _Nested structure omitted for brevity — see the `Phone[]` object._

            </ResponseField>

            <ResponseField name="addresses" type="AddressWrapper[]">

              Physical addresses for the member.

              _Nested structure omitted for brevity — see the `AddressWrapper[]` object._

            </ResponseField>

            <ResponseField name="company" type="string">

              Company name.

            </ResponseField>

            <ResponseField name="position" type="string">

              Position within company.

            </ResponseField>

            <ResponseField name="birthdate" type="string">

              Member's birth date in `YYYY-MM-DD` format.

            </ResponseField>

            <ResponseField name="slug" type="string">

              URL-friendly slug for the member's profile.

            </ResponseField>

            <ResponseField name="subscription" type="boolean">

              Whether the member consents to receive marketing communications.

            </ResponseField>

            <ResponseField name="vatId" type="string">

              Tax ID or VAT number for personal or corporate use.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="metadata" type="Metadata">

          Additional information about the identity that can impact user access.
          This data cannot be set.

        </ResponseField>

        <ResponseField name="email" type="Email">

          Identity email address.

          <Expandable title="email properties">

            <ResponseField name="address" type="string">

              Email address.

              Constraints: `format EMAIL`.

            </ResponseField>

            <ResponseField name="isVerified" type="boolean">

              Whether the email address is verified.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="status" type="StatusV2">

          Identity's current status.

          <Expandable title="status properties">

            <ResponseField name="name" type="string">

              Descriptive name of the identity status.

              **Supported values:**

              - `UNKNOWN_STATUS` — Unknown status. This value isn't used.
              - `PENDING` — Identity is pending verification, owner approval, or both.
              - `ACTIVE` — Identity is active and can log in.
              - `DELETED` — Identity is deleted. This identity can't log in.
              - `BLOCKED` — Identity is blocked. This identity can't log in.
              - `OFFLINE` — Identity is offline. This identity can't log in.

            </ResponseField>

            <ResponseField name="reasons" type="string[]">

              Reasons for the current identity status.

              **Supported values:**

              - `UNKNOWN_REASON` — Unknown reason. This value isn't used.
              - `PENDING_ADMIN_APPROVAL_REQUIRED` — The identity is waiting for admin approval. After the approval, the identity has an `ACTIVE` status.
              - `PENDING_EMAIL_VERIFICATION_REQUIRED` — The identity has to verify the email. After the verification, the identity has an `ACTIVE` status.
              - `PENDING_REQUIRED_FIELDS_MISSING` — The identity is missing fields that are marked as required on the site's signup form. After completing them, the identity has an `ACTIVE` status.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="factors" type="Factor[]">

          Identity verification factors.

          **Read-only.** Constraints: `maxItems 10`.

          <Expandable title="factors properties">

            <ResponseField name="factorId" type="string">

              Factor ID.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Factor type.

              **Supported values:**

              - `PASSWORD` — Requires a password.
              - `SMS` — Requires a code sent via SMS.
              - `CALL` — Requires a code sent by phone call.
              - `EMAIL` — Requires a code sent by email.
              - `TOTP` — Requires authentication via an authenticator app.
              - `PUSH` — Requires authentication via a push notification.
              - `WEBAUTHN` — Requires authentication via WebAuthn/passkey.
              - `RECOVERY_CODE` — Requires a recovery code.

            </ResponseField>

            <ResponseField name="status" type="string">

              Factor status.

              **Supported values:**

              - `INACTIVE` — Factor requires activation.
              - `ACTIVE` — Factor is active and can be used for authentication.
              - `REQUIRE_REENROLL` — Factor is blocked and cannot be used for authentication. The user should reenroll the factor.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalData" type="object">

      Additional data relevant to the login or registration process.

      Constraints: `format map`.

      <Expandable title="additionalData properties">

        <ResponseField name="key" type="string" />

        <ResponseField name="value" type="CustomValue">

          <Expandable title="One of the following">

            <ResponseField name="value" type="value">

              value

              _Nested structure omitted for brevity — see the `value` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `429` | `RESOURCE_EXHAUSTED` | `THROTTLED_FEATURE` | Too many requests. Try again later. |
    | `403` | `PERMISSION_DENIED` | `INVALID_CAPTCHA_TOKEN` | The reCAPTCHA token is invalid or expired. |
    | `403` | `PERMISSION_DENIED` | `CAPTCHA_TOKEN_REQUIRED` | The reCAPTCHA token is required. |
    | `401` | `UNAUTHENTICATED` | `REENROLLMENT_REQUIRED_BY_ADMIN` | Admin requires re-enrollment for this identity. |
    | `500` | `INTERNAL` | `TRUSTED_CLIENTS_ONLY` | This method is available only to trusted clients. |
    | `401` | `UNAUTHENTICATED` | `WIX_EMPLOYEE_PASSWORD_LOGIN_NOT_ALLOWED` | Wix employees must use Google SSO to login and cannot use password authentication. |
    | `400` | `INVALID_ARGUMENT` | `PROVIDE_TENANT_ID` | Can't resolve tenant. For users, provide a valid Wix domain in the request. For members, provide a MSID in the request. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_AUTHENTICATOR_ID` | Authenticator ID is missing or invalid. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-login-v-2" id="ex-rest-login-v-2-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-login-v-2" id="ex-rest-login-v-2-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-login-v-2" id="ex-rest-login-v-2-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-login-v-2-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-login-v-2-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Login with email and password, without using reCAPTCHA.</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Login with email and password, using invisible reCAPTCHA.</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Login with email and password, using visible reCAPTCHA.</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-login-v-2-0"><span className="wix-picker-name">Login with email and password, without using reCAPTCHA.</span></label>
    <label htmlFor="ex-rest-login-v-2-1"><span className="wix-picker-name">Login with email and password, using invisible reCAPTCHA.</span></label>
    <label htmlFor="ex-rest-login-v-2-2"><span className="wix-picker-name">Login with email and password, using visible reCAPTCHA.</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -L -X POST \
      'https://www.wixapis.com/_api/iam/authentication/v2/login' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "login_id": {
              "email": "test@test.com"
          },
          "password": "my-password"
      }'
      ```

      ```json Response
      {
          "state": "SUCCESS",
          "session_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
          "identity": {
              "id": "16a5995f-4514-491b-b054-ffee5adf5af2",
              "revision": "1",
              "createdDate": "2022-09-15T14:28:36Z",
              "updatedDate": "2022-09-15T14:28:37Z",
              "identityProfile": {
                  "nickname": "test",
                  "emails": ["test@test.com"],
                  "phones": ["+1-72149124712"],
                  "privacyStatus": "PUBLIC",
                  "customFields": []
              },
              "email": {
                  "address": "test@test.com",
                  "isVerified": false
              },
              "status": {
                  "name": "ACTIVE",
                  "reasons": []
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/_api/iam/authentication/v2/login' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "login_id": {
              "email": "test@test.com"
          },
          "password": "my-password",
          "captcha_tokens": [{
              "InvisibleRecaptcha": "03AAYGu2Q5KvtfjhU8XQL2LBVmd4rystj0FSSAouegJjBO6P0HI..."
          }]
      }'
      ```

      ```json Response
      {
          "state": "SUCCESS",
          "session_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
          "identity": {
              "id": "16a5995f-4514-491b-b054-ffee5adf5af2",
              "revision": "1",
              "createdDate": "2022-09-15T14:28:36Z",
              "updatedDate": "2022-09-15T14:28:37Z",
              "identityProfile": {
                  "nickname": "test",
                  "emails": ["test@test.com"],
                  "phones": ["+1-72149124712"],
                  "privacyStatus": "PUBLIC",
                  "customFields": []
                  },
              "email": {
                  "address": "test@test.com",
                  "isVerified": false
              },
              "status": {
                  "name": "ACTIVE",
                  "reasons": []
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/_api/iam/authentication/v2/login' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "login_id": {
              "email": "test@test.com"
          },
          "password": "my-password",
          "captcha_tokens": [{
              "Recaptcha": "03AAYGu2Q0STS4gydphoHhvovlwgE-bpDbB1..."
          }]
      }'
      ```

      ```json Response
      {
          "state": "SUCCESS",
          "session_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
          "identity": {
              "id": "16a5995f-4514-491b-b054-ffee5adf5af2",
              "revision": "1",
              "createdDate": "2022-09-15T14:28:36Z",
              "updatedDate": "2022-09-15T14:28:37Z",
              "identityProfile": {
                  "nickname": "test",
                  "emails": ["test@test.com"],
                  "phones": ["+1-72149124712"],
                  "privacyStatus": "PUBLIC",
                  "customFields": []
              },
              "email": {
                  "address": "test@test.com",
                  "isVerified": false
              },
              "status": {
                  "name": "ACTIVE",
                  "reasons": []
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Logs in an existing member.

    Typically, after a successful login, you generate and use member tokens for the
    logged-in member so that subsequent API calls are called as part of a member session.
    Use the `sessionToken` in the response to [get the site member's access and refresh tokens](https://dev.wix.com/docs/go-headless/coding/rest-api/visitors-and-members/handle-members-with-custom-login#get-the-site-members-access-and-refresh-tokens).

    ## Method Declaration

    ```typescript
    import { authentication } from '@wix/identity';

    authentication.loginV2(loginId, options): Promise<StateMachineResponse>
    ```

    ## Method Parameters

    <ParamField body="loginId" type="LoginId" required>

      Identifier of identity logging in.

      <Expandable title="One of the following">

        <ParamField body="type" type="type">

          type

          <Expandable title="type properties">

            <ParamField body="email" type="string">

              Login email address.

              Constraints: `format EMAIL`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="LoginV2Options">

      options

      <Expandable title="options properties">

        <ParamField body="password" type="string">

          Password of the identity logging in.

        </ParamField>

        <ParamField body="captchaTokens" type="CaptchaToken[]">

          CAPTCHA tokens, when CAPTCHA setting is on.

        </ParamField>

        <ParamField body="clientMetaData" type="object">

          Additional data that's relevant for the flow.

        </ParamField>

        <ParamField body="stateToken" type="string">

          State token from a login flow that was started beforehand.

          When provided, this request continues an in-progress login flow rather than starting a new one.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<StateMachineResponse>`.

    <ResponseField name="state" type="string">

      Current state of the login or registration process.

      **Supported values:**

      - `UNKNOWN_STATE` — Initial unknown state.
      - `SUCCESS` — Login completed successfully.
      - `REQUIRE_OWNER_APPROVAL` — Member needs the owner to approve their registration.
      - `REQUIRE_EMAIL_VERIFICATION` — Member needs to verify their email.
      - `STATUS_CHECK` — The `status` is not one that prevents the member logging in, meaning it's not `OFFLINE`, `BLOCKED`, or `DELETED`.
      - `REQUIRE_MISSING_FIELDS` — Member is missing fields that are marked as required on the site's signup form, and needs to complete them to finish registration.

    </ResponseField>

    <ResponseField name="sessionToken" type="string">

      Session token. If `state` is not `SUCCESS`, this field is undefined.

    </ResponseField>

    <ResponseField name="stateToken" type="string">

      Token that represents the current state of the login or registration process.

    </ResponseField>

    <ResponseField name="identity" type="Identity">

      Identity of the current member.

      <Expandable title="identity properties">

        <ResponseField name="_id" type="string">

          Identity ID.

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number, which increments by 1 each time the identity is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the identity.

          Ignored when creating an identity.

        </ResponseField>

        <ResponseField name="_createdDate" type="datetime">

          Date and time the identity was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="_updatedDate" type="datetime">

          Date and time the identity was updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="connections" type="Connection[]">

          The identity configured connections to authenticate with.

        </ResponseField>

        <ResponseField name="identityProfile" type="IdentityProfile">

          Identity profile.

          <Expandable title="identityProfile properties">

            <ResponseField name="firstName" type="string">

              Member's first name.

            </ResponseField>

            <ResponseField name="lastName" type="string">

              Member's last name.

            </ResponseField>

            <ResponseField name="nickname" type="string">

              Member's display nickname.

            </ResponseField>

            <ResponseField name="picture" type="string">

              URL to member's profile picture.

            </ResponseField>

            <ResponseField name="emails" type="string[]" deprecated>

              Deprecated. Use `secondaryEmails` instead.

              **Deprecated.** Deprecated. Use `secondaryEmails` instead. Use `secondary_emails` instead. Removal: 2023-11-01.

            </ResponseField>

            <ResponseField name="phones" type="string[]" deprecated>

              Deprecated. Use `phonesV2` instead.

              **Deprecated.** Deprecated. Use `phonesV2` instead. Use `phones_v2` instead. Removal: 2023-11-01.

            </ResponseField>

            <ResponseField name="labels" type="string[]">

              Labels associated with the member.

              Constraints: `maxItems 2000`.

            </ResponseField>

            <ResponseField name="language" type="string">

              Member's preferred language code.

            </ResponseField>

            <ResponseField name="privacyStatus" type="string">

              Member's profile visibility setting.

              **Supported values:**

              - `UNDEFINED` — No privacy status defined.
              - `PUBLIC` — Profile details are visible to other members.
              - `PRIVATE` — Profile details are hidden from other members.

            </ResponseField>

            <ResponseField name="customFields" type="CustomField[]">

              Any number of custom fields. [Custom fields](https://support.wix.com/en/article/adding-custom-fields-to-contacts)
              are used to store additional information about your site or app's contacts.

              _Nested structure omitted for brevity — see the `CustomField[]` object._

            </ResponseField>

            <ResponseField name="secondaryEmails" type="SecondaryEmail[]">

              Additional email addresses for the member.

              _Nested structure omitted for brevity — see the `SecondaryEmail[]` object._

            </ResponseField>

            <ResponseField name="phonesV2" type="Phone[]">

              Phone numbers associated with the member.

              _Nested structure omitted for brevity — see the `Phone[]` object._

            </ResponseField>

            <ResponseField name="addresses" type="AddressWrapper[]">

              Physical addresses for the member.

              _Nested structure omitted for brevity — see the `AddressWrapper[]` object._

            </ResponseField>

            <ResponseField name="company" type="string">

              Company name.

            </ResponseField>

            <ResponseField name="position" type="string">

              Position within company.

            </ResponseField>

            <ResponseField name="birthdate" type="string">

              Member's birth date in `YYYY-MM-DD` format.

            </ResponseField>

            <ResponseField name="slug" type="string">

              URL-friendly slug for the member's profile.

            </ResponseField>

            <ResponseField name="subscription" type="boolean">

              Whether the member consents to receive marketing communications.

            </ResponseField>

            <ResponseField name="vatId" type="string">

              Tax ID or VAT number for personal or corporate use.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="email" type="Email">

          Identity email address.

          <Expandable title="email properties">

            <ResponseField name="address" type="string">

              Email address.

              Constraints: `format EMAIL`.

            </ResponseField>

            <ResponseField name="isVerified" type="boolean">

              Whether the email address is verified.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="status" type="StatusV2">

          Identity's current status.

          <Expandable title="status properties">

            <ResponseField name="name" type="string">

              Descriptive name of the identity status.

              **Supported values:**

              - `UNKNOWN_STATUS` — Unknown status. This value isn't used.
              - `PENDING` — Identity is pending verification, owner approval, or both.
              - `ACTIVE` — Identity is active and can log in.
              - `DELETED` — Identity is deleted. This identity can't log in.
              - `BLOCKED` — Identity is blocked. This identity can't log in.
              - `OFFLINE` — Identity is offline. This identity can't log in.

            </ResponseField>

            <ResponseField name="reasons" type="string[]">

              Reasons for the current identity status.

              **Supported values:**

              - `UNKNOWN_REASON` — Unknown reason. This value isn't used.
              - `PENDING_ADMIN_APPROVAL_REQUIRED` — The identity is waiting for admin approval. After the approval, the identity has an `ACTIVE` status.
              - `PENDING_EMAIL_VERIFICATION_REQUIRED` — The identity has to verify the email. After the verification, the identity has an `ACTIVE` status.
              - `PENDING_REQUIRED_FIELDS_MISSING` — The identity is missing fields that are marked as required on the site's signup form. After completing them, the identity has an `ACTIVE` status.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="factors" type="Factor[]">

          Identity verification factors.

          **Read-only.** Constraints: `maxItems 10`.

          <Expandable title="factors properties">

            <ResponseField name="factorId" type="string">

              Factor ID.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Factor type.

              **Supported values:**

              - `PASSWORD` — Requires a password.
              - `SMS` — Requires a code sent via SMS.
              - `CALL` — Requires a code sent by phone call.
              - `EMAIL` — Requires a code sent by email.
              - `TOTP` — Requires authentication via an authenticator app.
              - `PUSH` — Requires authentication via a push notification.
              - `WEBAUTHN` — Requires authentication via WebAuthn/passkey.
              - `RECOVERY_CODE` — Requires a recovery code.

            </ResponseField>

            <ResponseField name="status" type="string">

              Factor status.

              **Supported values:**

              - `INACTIVE` — Factor requires activation.
              - `ACTIVE` — Factor is active and can be used for authentication.
              - `REQUIRE_REENROLL` — Factor is blocked and cannot be used for authentication. The user should reenroll the factor.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalData" type="object">

      Additional data relevant to the login or registration process.

      Constraints: `format map`.

      <Expandable title="additionalData properties">

        <ResponseField name="key" type="string" />

        <ResponseField name="value" type="CustomValue">

          <Expandable title="One of the following">

            <ResponseField name="value" type="value">

              value

              _Nested structure omitted for brevity — see the `value` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `429` | `RESOURCE_EXHAUSTED` | `THROTTLED_FEATURE` | Too many requests. Try again later. |
    | `403` | `PERMISSION_DENIED` | `INVALID_CAPTCHA_TOKEN` | The reCAPTCHA token is invalid or expired. |
    | `403` | `PERMISSION_DENIED` | `CAPTCHA_TOKEN_REQUIRED` | The reCAPTCHA token is required. |
    | `401` | `UNAUTHENTICATED` | `REENROLLMENT_REQUIRED_BY_ADMIN` | Admin requires re-enrollment for this identity. |
    | `500` | `INTERNAL` | `TRUSTED_CLIENTS_ONLY` | This method is available only to trusted clients. |
    | `401` | `UNAUTHENTICATED` | `WIX_EMPLOYEE_PASSWORD_LOGIN_NOT_ALLOWED` | Wix employees must use Google SSO to login and cannot use password authentication. |
    | `400` | `INVALID_ARGUMENT` | `PROVIDE_TENANT_ID` | Can't resolve tenant. For users, provide a valid Wix domain in the request. For members, provide a MSID in the request. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_AUTHENTICATOR_ID` | Authenticator ID is missing or invalid. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-login-v-2" id="ex-sdk-login-v-2-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-login-v-2" id="ex-sdk-login-v-2-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-login-v-2" id="ex-sdk-login-v-2-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-login-v-2-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-login-v-2-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">loginV2</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">loginV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">loginV2 (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-login-v-2-0"><span className="wix-picker-name">loginV2</span></label>
    <label htmlFor="ex-sdk-login-v-2-1"><span className="wix-picker-name">loginV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-login-v-2-2"><span className="wix-picker-name">loginV2 (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript loginV2
    import { authentication } from '@wix/identity';

    async function loginV2(loginId,options) {
      const response = await authentication.loginV2(loginId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript loginV2 (self-hosted)
    import { createClient } from '@wix/sdk';
    import { authentication } from '@wix/identity';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { authentication },
      // Include the auth strategy and host as relevant
    });

    async function loginV2(loginId,options) {
      const response = await myWixClient.authentication.loginV2(loginId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript loginV2 (with elevated permissions)
    import { authentication } from '@wix/identity';
    import { auth } from '@wix/essentials';

    async function myLoginV2Method(loginId,options) {
      const elevatedLoginV2 = auth.elevate(authentication.loginV2);
      const response = await elevatedLoginV2(loginId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
