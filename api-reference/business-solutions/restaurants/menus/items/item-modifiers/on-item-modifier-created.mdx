---
title: "onItemModifierCreated"
description: "The onItemModifierCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_modifier_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemModifierCreated
  import { itemModifiers } from '@wix/restaurants';

  itemModifiers.onItemModifierCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemModifierCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemModifiers } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemModifiers,
      },
  });

  wixClient.itemModifiers.onItemModifierCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
