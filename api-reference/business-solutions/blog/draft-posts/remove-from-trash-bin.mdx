---
title: "Remove From Trash Bin"
description: "Permanently deletes a draft post that is currently in the trash bin."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a draft post that is currently in the trash bin.

    This action is permanent and can't be reversed.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/blog/v3/draft-posts/trash-bin/{draftPostId}
    ```

    ## Path Params

    <ParamField path="draftPostId" type="string" required>

      Draft post ID.

    </ParamField>

    ## Response Object

    Returns `RemoveFromTrashBinResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE \
        'https://www.wixapis.com/blog/v3/draft-posts/trash-bin/996cb649-d428-405d-b9ed-4facd46e2443' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a draft post that is currently in the trash bin.

    This action is permanent and can't be reversed.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { draftPosts } from '@wix/blog';

    draftPosts.removeFromTrashBin(draftPostId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="draftPostId" type="string" required>

      Draft post ID.

      Constraints: `maxLength 38`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-from-trash-bin" id="ex-sdk-remove-from-trash-bin-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-from-trash-bin" id="ex-sdk-remove-from-trash-bin-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-remove-from-trash-bin" id="ex-sdk-remove-from-trash-bin-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-from-trash-bin-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-from-trash-bin-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Permanently delete a draft post</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Permanently delete a draft post (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-from-trash-bin-0"><span className="wix-picker-name">Permanently delete a draft post</span></label>
    <label htmlFor="ex-sdk-remove-from-trash-bin-1"><span className="wix-picker-name">Permanently delete a draft post (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-remove-from-trash-bin-2"><span className="wix-picker-name">removeFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Permanently delete a draft post
    import { draftPosts } from '@wix/blog';

    // Sample draftPostId value: "dd0907fa-ca4b-4fe4-ab9e-625e4733691e"

    export async function myRemoveFromTrashBinFunction(draftPostId) {
      try {
        await draftPosts.removeFromTrashBin(draftPostId);
        console.log('Permanently deleted the following draft post:', draftPostId);
        return;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    // Promise returns void
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Permanently delete a draft post (with elevated permissions)
    import { draftPosts } from '@wix/blog';
    import { auth } from '@wix/essentials';

    const elevatedRemoveFromTrashBin = auth.elevate(draftPosts.removeFromTrashBin);

    // Sample draftPostId value: "dd0907fa-ca4b-4fe4-ab9e-625e4733691e"

    export async function myRemoveFromTrashBinFunction(draftPostId) {
      try {
        await elevatedRemoveFromTrashBin(draftPostId);
        console.log('Permanently deleted the following draft post:', draftPostId);
        return;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    // Promise returns void
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeFromTrashBin (self-hosted)
    import { createClient } from '@wix/sdk';
    import { draftPosts } from '@wix/blog';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { draftPosts },
      // Include the auth strategy and host as relevant
    });

    async function removeFromTrashBin(draftPostId) {
      const response = await myWixClient.draftPosts.removeFromTrashBin(draftPostId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
