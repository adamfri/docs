---
title: "Update Category"
description: "Updates a category."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a category.

    Each time the category is updated, `revision` increments by 1.
    The current `revision` must be passed when updating the category.
    This ensures you're working with the latest category and prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_WRITE">Category write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/categories/v1/categories/{category.id}
    ```

    ## Path Params

    <ParamField path="category.id" type="string" required>

      Category ID.

    </ParamField>

    ## Body Params

    <ParamField body="category" type="Category" required>

      Category to update.

      See the [Category Object](/api-reference/business-solutions/stores/catalog-v3/categories/category-object) for this object's fields.

    </ParamField>

    <ParamField body="treeReference" type="TreeReference" required>

      Category tree reference details.

      <Expandable title="treeReference properties">

        <ParamField body="appNamespace" type="string" required>

          Namespace of the app whose catalog this category tree belongs to.

          Currently, only `"@wix/stores"` is supported.

          Constraints: `minLength 4`, `maxLength 150`.

        </ParamField>

        <ParamField body="treeKey" type="string">

          Key to differentiate between multiple trees managed by the same app.

          For Wix Stores, pass `null` or omit this field, as each store has only one category tree.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Constraints: `maxItems 100`.

      **Supported values:**

      - `BREADCRUMBS_INFO`
      - `DESCRIPTION`
      - `RICH_CONTENT_DESCRIPTION`

    </ParamField>

    ## Response Object

    <ResponseField name="category" type="Category">

      Updated category.

      See the [Category Object](/api-reference/business-solutions/stores/catalog-v3/categories/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `DUPLICATE_SLUG` | There is already a category with this `slug`. |
    | `400` | `INVALID_ARGUMENT` | `REMOVED_SLUG` | The `slug` field can't be removed. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SLUG_FORMAT` | The `slug` provided is not a valid URL slug. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Updated | `wix.categories.v1.category_updated` | Triggered when a category is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Updates the name field of a category.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/categories/v1/categories/bbd52b32-21d2-4dbe-bfc5-d320f93f8911' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "category": {
            "revision": "1",
            "name": "Drink Containers"
          },
          "treeReference": {
            "appNamespace": "@wix/stores",
            "treeKey": null
          }
        }'
      ```

      ```json Response
      {
        "category": {
          "id": "bbd52b32-21d2-4dbe-bfc5-d320f93f8911",
          "revision": "2",
          "createdDate": "2024-06-26T09:12:48.985Z",
          "updatedDate": "2024-06-26T11:08:14.348Z",
          "name": "Drink Containers",
          "itemCounter": 0,
          "description": "The Drinkware category includes a wide range of containers designed for holding beverages",
          "visible": true,
          "parentCategory": {
            "index": 8
          },
          "slug": "drinkware",
          "treeReference": {
            "appNamespace": "@wix/stores"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a category.

    Each time the category is updated, `revision` increments by 1.
    The current `revision` must be passed when updating the category.
    This ensures you're working with the latest category and prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_WRITE">Category write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/categories';

    categories.updateCategory(_id, category, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Category ID.

    </ParamField>

    <ParamField body="category" type="UpdateCategory" required>

      Category to update.

      <Expandable title="category properties">

        <ParamField body="_id" type="string">

          Category ID.

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the category is updated.
          To prevent conflicting changes, the current revision must be passed when updating.

          Ignored when creating a category.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the category was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the category was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Category name.

        </ParamField>

        <ParamField body="image" type="string">

          Category image.

          + Pass WixMedia image ID for media previously saved in the [Wix Media Manager](https://support.wix.com/en/article/wix-media-about-the-media-manager).
          + Pass full image URL to upload to Wix Media Manager.

        </ParamField>

        <ParamField body="itemCounter" type="integer">

          Number of items directly assigned to this category. Doesn't include items in subcategories.

          **Read-only.**

        </ParamField>

        <ParamField body="description" type="string">

          Plain-text category description.

          Returned only when you pass `"DESCRIPTION"` to the `fields` array.

        </ParamField>

        <ParamField body="visible" type="boolean">

          Whether the category is visible to site visitors.

          Visibility cascades to subcategories: if a parent category's visibility is set to `false`, all its subcategories become hidden as well.
          Setting a subcategory's visibility to `true` fails if any of its parent categories are hidden.

          > **Notes:**
          > 1. Hidden categories and their items are still accessible via API calls. For example, List Items In Category returns items even for hidden categories.
          > 2. The `visible` field can only be changed using the [Update Category Visibility](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/categories/update-category-visibility) method.
          > 3. Default: `true`

        </ParamField>

        <ParamField body="breadcrumbsInfo" type="BreadcrumbsInfo">

          Category breadcrumbs, representing the path from the top-level ancestor category down to the parent of the current category.

          Breadcrumbs are useful for building navigation UI and for SEO purposes. For top-level categories, the breadcrumbs list is empty.

          Returned only when you pass `"BREADCRUMBS_INFO"` to the `fields` array.

          **Read-only.**

          <Expandable title="breadcrumbsInfo properties">

            <ParamField body="breadcrumbs" type="Breadcrumb[]">

              List of ancestor categories from the top-level category down to the parent of the current category. The current category isn't included in the list.

              **Read-only.** Constraints: `maxItems 5`.

              _Nested structure omitted for brevity — see the `Breadcrumb[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="parentCategory" type="ParentCategory">

          Information about the category's parent. Top-level categories don't have a parent.

          Constraints: `immutable`.

          <Expandable title="parentCategory properties">

            <ParamField body="_id" type="string">

              ID of the parent category.

              Leave empty or omit to create a top-level category.

            </ParamField>

            <ParamField body="index" type="integer">

              Position of this category among its sibling categories within the parent.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="slug" type="string">

          Category slug. A URL-friendly identifier used in the category page address.

          If not provided, the slug is autogenerated based on the category name.

        </ParamField>

        <ParamField body="seoData" type="SeoSchema">

          Custom SEO data for the category.

          <Expandable title="seoData properties">

            <ParamField body="tags" type="Tag[]">

              SEO tag information.

              _Nested structure omitted for brevity — see the `Tag[]` object._

            </ParamField>

            <ParamField body="settings" type="Settings">

              SEO general settings.

              _Nested structure omitted for brevity — see the `Settings` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="richContentDescription" type="RichContent">

          Category description using rich content.
          > **Note:** Returned only when you pass `"RICH_CONTENT_DESCRIPTION"` to the `fields` array in Categories API requests.

          &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="indent.emoji.divider.codeBlock.file.gallery.giphy.image.table.link.textHighlight.textColor" exampleid="7dc9240e-d548-417a-abcf-0291b68b4303">
          &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
          &lt;/widget>

          <Expandable title="richContentDescription properties">

            <ParamField body="nodes" type="Node[]">

              Node objects representing a rich content document.

              _Nested structure omitted for brevity — see the `Node[]` object._

            </ParamField>

            <ParamField body="metadata" type="Metadata">

              Object metadata.

              _Nested structure omitted for brevity — see the `Metadata` object._

            </ParamField>

            <ParamField body="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              _Nested structure omitted for brevity — see the `DocumentStyle` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="treeReference" type="TreeReference">

          Reference to the category tree this category belongs to.

          When working with Wix Stores categories, always pass `appNamespace: "@wix/stores"` and `treeKey: null`.

          **Read-only.**

          <Expandable title="treeReference properties">

            <ParamField body="appNamespace" type="string">

              Namespace of the app whose catalog this category tree belongs to.

              Currently, only `"@wix/stores"` is supported.

              Constraints: `minLength 4`, `maxLength 150`.

            </ParamField>

            <ParamField body="treeKey" type="string">

              Key to differentiate between multiple trees managed by the same app.

              For Wix Stores, pass `null` or omit this field, as each store has only one category tree.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="managingAppId" type="string">

          ID of the app responsible for managing items in this category.

          When set, only the specified app can add or remove items from this category. This is useful for third-party apps that want to create categories exclusively managed by their app, preventing site owners or other apps from modifying the category's items.

          You can get your app's ID from the [app dashboard](https://dev.wix.com/dc3/my-apps/).

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom extended fields for the category object.

          [Extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions) must be configured in the [app dashboard](https://dev.wix.com/dc3/my-apps/) before they can be accessed with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateCategoryOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="treeReference" type="TreeReference" required>

          Category tree reference details.

          <Expandable title="treeReference properties">

            <ParamField body="appNamespace" type="string" required>

              Namespace of the app whose catalog this category tree belongs to.

              Currently, only `"@wix/stores"` is supported.

              Constraints: `minLength 4`, `maxLength 150`.

            </ParamField>

            <ParamField body="treeKey" type="string">

              Key to differentiate between multiple trees managed by the same app.

              For Wix Stores, pass `null` or omit this field, as each store has only one category tree.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Constraints: `maxItems 100`.

          **Supported values:**

          - `BREADCRUMBS_INFO`
          - `DESCRIPTION`
          - `RICH_CONTENT_DESCRIPTION`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    See the [Category Object](/api-reference/business-solutions/stores/catalog-v3/categories/category-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `DUPLICATE_SLUG` | There is already a category with this `slug`. |
    | `400` | `INVALID_ARGUMENT` | `REMOVED_SLUG` | The `slug` field can't be removed. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SLUG_FORMAT` | The `slug` provided is not a valid URL slug. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Updated | `wix.categories.v1.category_updated` | Triggered when a category is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-category" id="ex-sdk-update-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-category" id="ex-sdk-update-category-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a category</span><span className="wix-picker-desc">Update a category's name</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-category-0"><span className="wix-picker-name">Update a category</span><span className="wix-picker-desc">Update a category's name</span></label>
    <label htmlFor="ex-sdk-update-category-1"><span className="wix-picker-name">updateCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a category
    import { categories } from "@wix/categories";

    const categoryId = "bbd52b32-21d2-4dbe-bfc5-d320f93f8911";

    const category = {
      id: "bbd52b32-21d2-4dbe-bfc5-d320f93f8911",
      revision: "1",
      name: "Drink Containers"
    };

    const options = {
      treeReference: {
        appNamespace: "@wix/stores",
        treeKey: null
      }
    };

    async function updateCategory() {
      const response = await categories.updateCategory(categoryId, category, options);
    }

    /* Promise resolves to:
     * {
     *   "_id": "bbd52b32-21d2-4dbe-bfc5-d320f93f8911",
     *   "_createdDate": "2025-12-11T09:27:16.000Z",
     *   "_updatedDate": "2025-12-11T09:27:20.000Z",
     *   "revision": "2",
     *   "name": "Drink Containers",
     *   "slug": "drinkware",
     *   "visible": true,
     *   "itemCounter": 0,
     *   "treeReference": { "appNamespace": "@wix/stores" }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/categories';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function updateCategory(_id,category,options) {
      const response = await myWixClient.categories.updateCategory(_id,category,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
