---
title: "onAttachmentDeleted"
description: "The onAttachmentDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.contacts.v4.attachment_deleted` |
| Package | `@wix/crm` |
| Permissions | `Manage Events`, `Manage Contact Attachments` |

## Examples

<CodeGroup>

  ```javascript onAttachmentDeleted
  import { attachments } from '@wix/crm';

  attachments.onAttachmentDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onAttachmentDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { attachments } from '@wix/crm';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        attachments,
      },
  });

  wixClient.attachments.onAttachmentDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
