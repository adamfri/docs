---
title: "onOAuthAppDeleted"
description: "Triggered when an OAuth app is deleted."
icon: "bolt"
---

Triggered when an OAuth app is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.headless.v1.o_auth_app_deleted` |
| Package | `@wix/auth-management` |
| Permissions | `Headless Manage`, `Manage OAuth Apps`, `Read OAuth Apps` |

## Examples

<CodeGroup>

  ```javascript onOAuthAppDeleted
  import { oAuthApps } from '@wix/auth-management';

  oAuthApps.onOAuthAppDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onOAuthAppDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { oAuthApps } from '@wix/auth-management';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        oAuthApps,
      },
  });

  wixClient.oAuthApps.onOAuthAppDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
