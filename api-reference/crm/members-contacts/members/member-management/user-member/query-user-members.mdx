---
title: "Query User Members"
description: "Retrieves a list of user members, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of user members, given the provided paging, filtering, and sorting.

    Query User Members runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language), [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/rest/articles/getting-started/field-projection).

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `memberId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `siteMembersInstanceId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `userId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `metaSiteId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-USER-MEMBERS">SCOPE.DC-MEMBERS.READ-USER-MEMBERS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/members/v1/user-members/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC` — Sorting is in ascending order.
              - `DESC` — Sorting is in descending order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="userMembers" type="UserMember[]">

      Retrieved user members.

      See the [User Member Object](/api-reference/crm/members-contacts/members/member-management/user-member/user-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        https://www.wixapis.com/members/v1/user-members/query \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>'
        -d '{
              "query": {
                "sort": [
                    {
                      "fieldName": "createdDate",
                      "order": "ASC"
                    }
                  ],
                  "filter": {
                    "userId": "e62e3011-55cf-4de3-a497-e097b52d86b7"
                  }
                }
              }
          }'
      ```

      ```json Response
      {
        "userMembers": [
          {
            "memberId": "e62e3011-55cf-4de3-a497-e097b52d86b7",
            "userId": "e62e3011-55cf-4de3-a497-e097b52d86b7",
            "contactId": "05a89fb8-190f-4db8-aea3-844b4683db9d",
            "siteMembersInstanceId": "9369f9de-4160-4a4f-9012-6194af1e3350",
            "metaSiteId": "1a87e1f2-4448-416d-948c-cba33546ded0",
            "photo": {
              "id": "",
              "url": "https://lh3.googleusercontent.com/a/ACg8ocJWGytgFklzRL3dKHsxBHATAKhIuU6BOwEcJR3m4IuFvZQ_Aw%3Ds96-c",
              "height": 0,
              "width": 0,
              "offsetX": 0,
              "offsetY": 0
            },
            "nickname": "John Smith",
            "privacyStatus": "PUBLIC",
            "createdDate": "2023-01-01T01:01:0Z",
            "updatedDate": "2024-04-01T01:01:0Z",
            "status": "APPROVED",
            "activityStatus": "ACTIVE"
          },
          {
            "memberId": "e62e3011-55cf-4de3-a497-e097b52d86b7",
            "userId": "e62e3011-55cf-4de3-a497-e097b52d86b7",
            "contactId": "1018621a-2811-4f13-b209-399f2c276ce8",
            "siteMembersInstanceId": "6d8a76aa-f601-41f5-ba86-28ffb2c4d92c",
            "metaSiteId": "042a5019-0387-47fc-8207-df695344428d",
            "photo": {
              "id": "",
              "url": "https://lh3.googleusercontent.com/a/ACg8ocJWGytgFklzRL3dKHsxBHATAKhIuU6BOwEcJR3m4IuFvZQ_Aw%3Ds96-c",
              "height": 0,
              "width": 0,
              "offsetX": 0,
              "offsetY": 0
            },
            "nickname": "John Doe",
            "privacyStatus": "PUBLIC",
            "createdDate": "2024-01-01T01:01:0Z",
            "updatedDate": "2025-01-01T01:01:0Z",
            "status": "APPROVED",
            "activityStatus": "ACTIVE"
          }
        ],
        "metadata": {
          "count": 2,
          "cursors": {
            "next": null,
            "prev": null
          },
          "hasNext": false,
          "total": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of user members, given the provided paging, filtering, and sorting.

    Query User Members runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language), [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/rest/articles/getting-started/field-projection).

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `memberId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `siteMembersInstanceId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `userId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `metaSiteId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-USER-MEMBERS">SCOPE.DC-MEMBERS.READ-USER-MEMBERS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { userMember } from '@wix/members';

    userMember.queryUserMembers(query): UserMembersQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="UserMemberQuery" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC` — Sorting is in ascending order.
              - `DESC` — Sorting is in descending order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `UserMembersQueryBuilder`.

    <ResponseField name="userMembers" type="UserMember[]">

      Retrieved user members.

      See the [User Member Object](/api-reference/crm/members-contacts/members/member-management/user-member/user-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-user-members" id="ex-sdk-query-user-members-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-user-members" id="ex-sdk-query-user-members-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-user-members" id="ex-sdk-query-user-members-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-user-members-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-user-members-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryUserMembers</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryUserMembers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryUserMembers (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-user-members-0"><span className="wix-picker-name">queryUserMembers</span></label>
    <label htmlFor="ex-sdk-query-user-members-1"><span className="wix-picker-name">queryUserMembers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-user-members-2"><span className="wix-picker-name">queryUserMembers (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryUserMembers
    import { userMember } from '@wix/members';

    async function queryUserMembers(query) {
      const response = await userMember.queryUserMembers(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryUserMembers (self-hosted)
    import { createClient } from '@wix/sdk';
    import { userMember } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { userMember },
      // Include the auth strategy and host as relevant
    });

    async function queryUserMembers(query) {
      const response = await myWixClient.userMember.queryUserMembers(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryUserMembers (with elevated permissions)
    import { userMember } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myQueryUserMembersMethod(query) {
      const elevatedQueryUserMembers = auth.elevate(userMember.queryUserMembers);
      const response = await elevatedQueryUserMembers(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
