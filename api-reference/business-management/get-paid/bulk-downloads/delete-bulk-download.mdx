---
title: "Delete Bulk Download"
description: "Deletes a bulk download."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a bulk download.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RECEIPTS.MANAGE">Manage Receipts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/get-paid/v1/bulk-downloads/{bulkDownloadId}
    ```

    ## Path Params

    <ParamField path="bulkDownloadId" type="string" required>

      Bulk download ID to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteBulkDownloadResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `USER_ID_NOT_FOUND` | User identification ID is missing. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Bulk Download Deleted | `wix.get_paid.downloader.v1.bulk_download_deleted` | Triggered when a bulk download is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a specific bulk download by ID

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/get-paid/v1/bulk-downloads/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a bulk download.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RECEIPTS.MANAGE">Manage Receipts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { bulkDownloads } from '@wix/get-paid';

    bulkDownloads.deleteBulkDownload(bulkDownloadId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="bulkDownloadId" type="string" required>

      Bulk download ID to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `USER_ID_NOT_FOUND` | User identification ID is missing. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Bulk Download Deleted | `wix.get_paid.downloader.v1.bulk_download_deleted` | Triggered when a bulk download is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-bulk-download" id="ex-sdk-delete-bulk-download-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-bulk-download" id="ex-sdk-delete-bulk-download-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-bulk-download" id="ex-sdk-delete-bulk-download-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-bulk-download-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-bulk-download-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteBulkDownload</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBulkDownload (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBulkDownload (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-bulk-download-0"><span className="wix-picker-name">deleteBulkDownload</span></label>
    <label htmlFor="ex-sdk-delete-bulk-download-1"><span className="wix-picker-name">deleteBulkDownload (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-bulk-download-2"><span className="wix-picker-name">deleteBulkDownload (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteBulkDownload
    import { bulkDownloads } from '@wix/get-paid';

    async function deleteBulkDownload(bulkDownloadId) {
      const response = await bulkDownloads.deleteBulkDownload(bulkDownloadId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBulkDownload (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bulkDownloads } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bulkDownloads },
      // Include the auth strategy and host as relevant
    });

    async function deleteBulkDownload(bulkDownloadId) {
      const response = await myWixClient.bulkDownloads.deleteBulkDownload(bulkDownloadId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBulkDownload (with elevated permissions)
    import { bulkDownloads } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myDeleteBulkDownloadMethod(bulkDownloadId) {
      const elevatedDeleteBulkDownload = auth.elevate(bulkDownloads.deleteBulkDownload);
      const response = await elevatedDeleteBulkDownload(bulkDownloadId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
