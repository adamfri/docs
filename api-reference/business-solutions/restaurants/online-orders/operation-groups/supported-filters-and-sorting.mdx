---
title: "Supported Filters and Sorting"
description: "The following table shows field support for filters and sorting for the operation group object: | Field | Supported Filters | Sortable | | --------------------…"
icon: "file-lines"
---

<Note>This article applies to the REST API only.</Note>

The following table shows field support for filters and sorting for the operation group object:

| Field                                            | Supported Filters                          | Sortable |
| -------------------------------------------------|--------------------------------------------| -------- |
| `id`                                             | `$eq`, `$ne`, `$in`, `$nin`                | Sortable |
| `createdDate`                                    | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sortable |
| `updatedDate`                                    | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sortable |
| `name`                                           | `$eq`, `$ne`, `$in`, `$nin`, `$startsWith` | Sortable |

**Related content:**
[API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language),
[Query Operation Groups](fallback::https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/online-orders/operation-groups/query-operation-groups).
