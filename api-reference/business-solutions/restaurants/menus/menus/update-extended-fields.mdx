---
title: "Update Extended Fields"
description: "Note: The Menu API only works with the Wix Restaurants Menus app."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Menu API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Updates only the `extendedFields` field.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants/menus/v1/menus/{id}/updateExtendedFields
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the entity to update.

    </ParamField>

    ## Body Params

    <ParamField body="namespace" type="string" required>

      Identifier for the app whose extended fields are being updated.

    </ParamField>

    <ParamField body="namespaceData" type="object" required>

      Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured.

    </ParamField>

    ## Response Object

    <ResponseField name="namespace" type="string">

      Namespace of the app.

    </ResponseField>

    <ResponseField name="namespaceData" type="object">

      Updated extended fields data.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/restaurants/menus-menu/v1/menus/d819eb86-0181-4f67-a383-ae1683e9e2fc/updateExtendedFields \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
        --data-binary '{
                        "namespace": "@restaurants/menus",
                        "namespaceData": {
                          "seoData": {
                            "tags": [
                              {
                                "props": {
                                  "content": "123",
                                  "name": "description"
                                },
                                "type": "meta"
                              },
                              {
                                "children": "1",
                                "type": "title"
                              }
                            ]
                          }
                        }
                      }'
      ```

      ```json Response
      {
        "namespace": "@restaurants/menus",
        "namespaceData": {
          "seoData": {
            "tags": [
              {
                "type": "meta",
                "props": {
                  "name": "description",
                  "content": "123"
                }
              },
              {
                "type": "title",
                "children": "1"
              }
            ]
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Menu API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Updates only the `extendedFields` field.

    ## Method Declaration

    ```typescript
    import { menus } from '@wix/restaurants';

    menus.updateExtendedFields(_id, namespace, options): Promise<UpdateExtendedFieldsResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the entity to update.

    </ParamField>

    <ParamField body="namespace" type="string" required>

      Identifier for the app whose extended fields are being updated.

    </ParamField>

    <ParamField body="options" type="UpdateExtendedFieldsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="namespaceData" type="object" required>

          Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateExtendedFieldsResponse>`.

    <ResponseField name="namespace" type="string">

      Namespace of the app.

    </ResponseField>

    <ResponseField name="namespaceData" type="object">

      Updated extended fields data.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-extended-fields" id="ex-sdk-update-extended-fields-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-extended-fields" id="ex-sdk-update-extended-fields-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-extended-fields-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-extended-fields-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update Extended Fields</span><span className="wix-picker-desc">Update SEO metadata for a menu</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateExtendedFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-extended-fields-0"><span className="wix-picker-name">Update Extended Fields</span><span className="wix-picker-desc">Update SEO metadata for a menu</span></label>
    <label htmlFor="ex-sdk-update-extended-fields-1"><span className="wix-picker-name">updateExtendedFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update Extended Fields
    import { menus } from "@wix/restaurants";

    const menuId = "d819eb86-0181-4f67-a383-ae1683e9e2fc";

    const options = {
      namespace: "wix.restaurants.menus.v1.menu",
      namespaceData: {
        seoData: {
          tags: [
            {
              props: {
                content: "Fresh breakfast options served daily",
                name: "description"
              },
              type: "meta"
            },
            {
              children: "Breakfast Menu",
              type: "title"
            }
          ]
        }
      }
    };

    async function updateExtendedFields() {
      const response = await menus.updateExtendedFields(menuId, options);
    }

    /* Promise resolves to:
     * {
     *   "namespace": "wix.restaurants.menus.v1.menu",
     *   "namespaceData": {
     *     "seoData": {
     *       "tags": [
     *         {
     *           "type": "meta",
     *           "props": {
     *             "name": "description",
     *             "content": "Fresh breakfast options served daily"
     *           }
     *         },
     *         {
     *           "type": "title",
     *           "children": "Breakfast Menu"
     *         }
     *       ]
     *     }
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateExtendedFields (self-hosted)
    import { createClient } from '@wix/sdk';
    import { menus } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { menus },
      // Include the auth strategy and host as relevant
    });

    async function updateExtendedFields(_id,namespace,options) {
      const response = await myWixClient.menus.updateExtendedFields(_id,namespace,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
