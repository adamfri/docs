---
title: "Query Messages"
description: "Retrieves a list of up to 1,000 messages in a conversation, given the provided paging and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1,000 messages in a conversation, given the provided paging and sorting.

    The response includes the in-flight assistant turn when its status is `WORKING`.
    To track the assistant's response, poll this method after calling [Send Message](https://dev.wix.com/docs/api-reference/mobile/containers-app/containers-chat/conversation-v1/send-message).

    Query Messages runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `50`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="containers:chat:v1:conversation:query_messages">containers:chat:v1:conversation:query_messages</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/containers/chat/v1/conversations/{conversationId}/messages/query
    ```

    ## Path Params

    <ParamField path="conversationId" type="string" required>

      Conversation ID.

    </ParamField>

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Filtering, sorting, and pagination options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="messages" type="Message[]">

      Retrieved messages.

      <Expandable title="messages properties">

        <ResponseField name="id" type="string">

          Message ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision, incremented on each update; used for optimistic concurrency.

          **Read-only.**

        </ResponseField>

        <ResponseField name="createdDate" type="datetime">

          Date and time the Message was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updatedDate" type="datetime">

          Date and time the Message was last updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="conversationId" type="string">

          ID of the conversation this Message belongs to. Set on create; immutable afterwards.

        </ResponseField>

        <ResponseField name="inReplyToId" type="string">

          For an assistant turn, the ID of the user Message it answers.

        </ResponseField>

        <ResponseField name="role" type="string">

          Who authored the turn (user / assistant / tool).

          **Supported values:**

          - `USER`
          - `ASSISTANT`
          - `TOOL`

        </ResponseField>

        <ResponseField name="status" type="string">

          Lifecycle status of the turn (WORKING -> DONE / ERROR).

          **Supported values:**

          - `WORKING`
          - `DONE`
          - `ERROR`

        </ResponseField>

        <ResponseField name="parts" type="MessagePart[]">

          Ordered content parts making up the turn.

          Constraints: `maxItems 200`.

          <Expandable title="parts properties">

            <ResponseField name="type" type="string">

              Kind of content carried by this part.

              **Supported values:**

              - `TEXT`
              - `CODE`
              - `IMAGE`
              - `FILE`
              - `TOOL_CALL`

            </ResponseField>

            <ResponseField name="text" type="string">

              Inline textual content (used by TEXT / CODE / TOOL_CALL parts).

              Constraints: `maxLength 100000`.

            </ResponseField>

            <ResponseField name="uri" type="string">

              Reference to external content (used by IMAGE / FILE parts).

              Constraints: `maxLength 2000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tasks" type="Task[]">

          Live work breakdown shown as a checklist on an in-flight assistant turn.

          Constraints: `maxItems 200`.

          <Expandable title="tasks properties">

            <ResponseField name="id" type="string">

              Client-supplied task ID, unique within the turn.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Human-readable task description.

              Constraints: `maxLength 500`.

            </ResponseField>

            <ResponseField name="status" type="string">

              Current status of the task.

              **Supported values:**

              - `PENDING`
              - `IN_PROGRESS`
              - `DONE_TASK`
              - `FAILED`

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="jobId" type="string">

          ID of the build job backing this turn, when applicable.

        </ResponseField>

        <ResponseField name="versionId" type="string">

          Opaque code version produced by an edit turn, when applicable.

        </ResponseField>

        <ResponseField name="followUpQuestions" type="string[]">

          Optional suggested next prompts attached to an assistant turn (planning & build).
          No entity of its own — carried on the message and set on DONE.

          Constraints: `maxLength 4000`, `maxItems 50`.

        </ResponseField>

        <ResponseField name="followUpQuestionOptions" type="FollowUpQuestion[]">

          Structured follow-up questions with selectable answer options. Superseding follow_up_questions;
          that field is kept and populated in parallel for callers that have not migrated to this one.

          Constraints: `maxItems 4`.

          <Expandable title="followUpQuestionOptions properties">

            <ResponseField name="question" type="string">

              The question text.

              Constraints: `maxLength 4000`.

            </ResponseField>

            <ResponseField name="options" type="string[]">

              Selectable answer options for this question.

              Constraints: `maxLength 200`, `maxItems 10`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="type" type="string">

          Kind of content this Message carries. Must be explicitly set by the creator.

          **Supported values:**

          - `TEXT` — Plain conversational content, carried in `parts`. No payload.
          - `QUESTIONNAIRE` — Assistant is presenting clarifying questions; see QuestionnairePayload.
          - `QUESTIONNAIRE_RESPONSE` — User is answering a QUESTIONNAIRE message; see QuestionnaireResponsePayload.
          - `CHECKPOINT` — A new project version was saved; see CheckpointPayload. No payload on TEXT.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a list of messages in a conversation.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/containers/chat/v1/conversations/1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10/messages/query' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "query": {
            "cursorPaging": {
              "limit": 50
            }
          }
        }'
      ```

      ```json Response
      {
        "messages": [
          {
            "id": "d5e9f3c2-1a4b-4c8d-9e7f-2b3c4d5e6f70",
            "conversationId": "1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10",
            "role": "USER",
            "status": "DONE",
            "parts": [
              {
                "type": "TEXT",
                "text": "Add a contact form to the homepage."
              }
            ],
            "revision": "1",
            "createdDate": "2026-07-29T09:02:00.000Z",
            "updatedDate": "2026-07-29T09:02:00.000Z"
          },
          {
            "id": "e6f0a4d3-2b5c-4d9e-af80-3c4d5e6f7a81",
            "conversationId": "1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10",
            "inReplyToId": "d5e9f3c2-1a4b-4c8d-9e7f-2b3c4d5e6f70",
            "role": "ASSISTANT",
            "status": "DONE",
            "parts": [
              {
                "type": "TEXT",
                "text": "I've added a contact form to the homepage. The form includes fields for name, email, and message."
              }
            ],
            "tasks": [
              {
                "id": "task-1",
                "title": "Add contact form component",
                "status": "DONE_TASK"
              }
            ],
            "followUpQuestionOptions": [
              {
                "question": "Would you like to customize the form fields?",
                "options": ["Yes, add more fields", "No, keep it simple"]
              }
            ],
            "revision": "3",
            "createdDate": "2026-07-29T09:02:00.000Z",
            "updatedDate": "2026-07-29T09:02:30.000Z"
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1,000 messages in a conversation, given the provided paging and sorting.

    The response includes the in-flight assistant turn when its status is `WORKING`.
    To track the assistant's response, poll this method after calling [Send Message](https://dev.wix.com/docs/api-reference/mobile/containers-app/containers-chat/conversation-v1/send-message).

    Query Messages runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `50`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="containers:chat:v1:conversation:query_messages">containers:chat:v1:conversation:query_messages</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { chat } from '@wix/containers';

    chat.queryMessages(conversationId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="conversationId" type="string" required>

      Conversation ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="QueryMessagesOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="CursorQuery">

          Filtering, sorting, and pagination options.

          <Expandable title="query properties">

            <ParamField body="filter" type="object">

              Filter object in the following format:
              `"filter" : {`
              "fieldName1": "value1",
              "fieldName2":&#123;"$operator":"value2"&#125;
              &#125;``
              Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object in the following format:
              `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

              Constraints: `maxItems 5`.

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="pagingMethod" type="pagingMethod">

              pagingMethod

              _Nested structure omitted for brevity — see the `pagingMethod` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="messages" type="Message[]">

      Retrieved messages.

      <Expandable title="messages properties">

        <ResponseField name="_id" type="string">

          Message ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision, incremented on each update; used for optimistic concurrency.

          **Read-only.**

        </ResponseField>

        <ResponseField name="_createdDate" type="datetime">

          Date and time the Message was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="_updatedDate" type="datetime">

          Date and time the Message was last updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="conversationId" type="string">

          ID of the conversation this Message belongs to. Set on create; immutable afterwards.

        </ResponseField>

        <ResponseField name="inReplyToId" type="string">

          For an assistant turn, the ID of the user Message it answers.

        </ResponseField>

        <ResponseField name="role" type="string">

          Who authored the turn (user / assistant / tool).

          **Supported values:**

          - `USER`
          - `ASSISTANT`
          - `TOOL`

        </ResponseField>

        <ResponseField name="status" type="string">

          Lifecycle status of the turn (WORKING -> DONE / ERROR).

          **Supported values:**

          - `WORKING`
          - `DONE`
          - `ERROR`

        </ResponseField>

        <ResponseField name="parts" type="MessagePart[]">

          Ordered content parts making up the turn.

          Constraints: `maxItems 200`.

          <Expandable title="parts properties">

            <ResponseField name="type" type="string">

              Kind of content carried by this part.

              **Supported values:**

              - `TEXT`
              - `CODE`
              - `IMAGE`
              - `FILE`
              - `TOOL_CALL`

            </ResponseField>

            <ResponseField name="text" type="string">

              Inline textual content (used by TEXT / CODE / TOOL_CALL parts).

              Constraints: `maxLength 100000`.

            </ResponseField>

            <ResponseField name="uri" type="string">

              Reference to external content (used by IMAGE / FILE parts).

              Constraints: `maxLength 2000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tasks" type="Task[]">

          Live work breakdown shown as a checklist on an in-flight assistant turn.

          Constraints: `maxItems 200`.

          <Expandable title="tasks properties">

            <ResponseField name="_id" type="string">

              Client-supplied task ID, unique within the turn.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Human-readable task description.

              Constraints: `maxLength 500`.

            </ResponseField>

            <ResponseField name="status" type="string">

              Current status of the task.

              **Supported values:**

              - `PENDING`
              - `IN_PROGRESS`
              - `DONE_TASK`
              - `FAILED`

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="jobId" type="string">

          ID of the build job backing this turn, when applicable.

        </ResponseField>

        <ResponseField name="versionId" type="string">

          Opaque code version produced by an edit turn, when applicable.

        </ResponseField>

        <ResponseField name="followUpQuestions" type="string[]">

          Optional suggested next prompts attached to an assistant turn (planning & build).
          No entity of its own — carried on the message and set on DONE.

          Constraints: `maxLength 4000`, `maxItems 50`.

        </ResponseField>

        <ResponseField name="followUpQuestionOptions" type="FollowUpQuestion[]">

          Structured follow-up questions with selectable answer options. Superseding follow_up_questions;
          that field is kept and populated in parallel for callers that have not migrated to this one.

          Constraints: `maxItems 4`.

          <Expandable title="followUpQuestionOptions properties">

            <ResponseField name="question" type="string">

              The question text.

              Constraints: `maxLength 4000`.

            </ResponseField>

            <ResponseField name="options" type="string[]">

              Selectable answer options for this question.

              Constraints: `maxLength 200`, `maxItems 10`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="type" type="string">

          Kind of content this Message carries. Must be explicitly set by the creator.

          **Supported values:**

          - `TEXT` — Plain conversational content, carried in `parts`. No payload.
          - `QUESTIONNAIRE` — Assistant is presenting clarifying questions; see QuestionnairePayload.
          - `QUESTIONNAIRE_RESPONSE` — User is answering a QUESTIONNAIRE message; see QuestionnaireResponsePayload.
          - `CHECKPOINT` — A new project version was saved; see CheckpointPayload. No payload on TEXT.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-messages" id="ex-sdk-query-messages-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-messages" id="ex-sdk-query-messages-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-messages-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-messages-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query messages in a conversation</span><span className="wix-picker-desc">Poll this method after Send Message to track the assistant's response</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryMessages (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-messages-0"><span className="wix-picker-name">Query messages in a conversation</span><span className="wix-picker-desc">Poll this method after Send Message to track the assistant's response</span></label>
    <label htmlFor="ex-sdk-query-messages-1"><span className="wix-picker-name">queryMessages (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query messages in a conversation
    import { chat } from "@wix/containers";

    async function queryMessages() {
      const response = await chat.queryMessages(
        "1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10",
        {
          query: {
            sort: [{ fieldName: "createdDate", order: "ASC" }],
            cursorPaging: { limit: 50 },
          },
        },
      );
    }

    /* Promise resolves to:
     * {
     *   "messages": [
     *     {
     *       "_id": "d5e9f3c2-1a4b-4c8d-9e7f-2b3c4d5e6f70",
     *       "conversationId": "1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10",
     *       "role": "USER",
     *       "status": "DONE",
     *       "parts": [{ "type": "TEXT", "text": "Add a contact form to the homepage." }],
     *       "revision": 1,
     *       "_createdDate": "2026-07-29T09:02:00.000Z",
     *       "_updatedDate": "2026-07-29T09:02:00.000Z"
     *     },
     *     {
     *       "_id": "f3a7b2c1-8e4d-4f9a-bc12-3d4e5f6a7b8c",
     *       "conversationId": "1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10",
     *       "inReplyToId": "d5e9f3c2-1a4b-4c8d-9e7f-2b3c4d5e6f70",
     *       "role": "ASSISTANT",
     *       "status": "DONE",
     *       "parts": [{ "type": "TEXT", "text": "I've added a contact form to your homepage." }],
     *       "revision": 1,
     *       "_createdDate": "2026-07-29T09:02:05.000Z",
     *       "_updatedDate": "2026-07-29T09:02:30.000Z"
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 2,
     *     "cursors": {},
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryMessages (self-hosted)
    import { createClient } from '@wix/sdk';
    import { chat } from '@wix/containers';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { chat },
      // Include the auth strategy and host as relevant
    });

    async function queryMessages(conversationId,options) {
      const response = await myWixClient.chat.queryMessages(conversationId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
