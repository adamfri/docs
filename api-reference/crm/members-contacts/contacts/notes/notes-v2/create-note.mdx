---
title: "Create Note"
description: "Creates a new note associated with a specific contact."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new note associated with a specific contact.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-NOTES">Manage Notes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/crm/notes/v2/notes
    ```

    ## Body Params

    <ParamField body="note" type="Note" required>

      Note to create.

      See the [Note Object](/api-reference/crm/members-contacts/contacts/notes/notes-v2/note-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="note" type="Note">

      Created note.

      See the [Note Object](/api-reference/crm/members-contacts/contacts/notes/notes-v2/note-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Note Created | `wix.crm.notes.v2.note_created` | Triggered when a note is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Creates a new Note

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/crm/notes/v2/notes' \
        -H 'Accept: application/json, text/plain, */*' \
        -H 'Authorization: <AUTH>' \
        --data-raw '
          {
             "note": {
               "contact_id": "2AD187DF-405C-47A1-A4EA-5F112FEBA874",
               "text": "Sample text of a note",
               "type": "MEETING_SUMMARY"
             }
          }'
      ```

      ```json Response
      {
          "note": {
            "id": "547bb310-8e15-45d2-8118-aa272dcb1d75",
            "revision": "1",
            "createdDate": "2023-08-21T12:43:31.104Z",
            "updatedDate": "2023-08-21T12:43:31.104Z",
            "contactId": "2ad187df-405c-47a1-a4ea-5f112feba874",
            "text": "Sample text of a note",
            "type": "MEETING_SUMMARY",
            "source": {
              "sourceType": "USER",
              "userId": "E75BCA89-5AEF-4992-9019-97EF6507AAC3"
            }
          }
        }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new note associated with a specific contact.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-NOTES">Manage Notes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { notes } from '@wix/crm';

    notes.createNote(note): Promise<Note>
    ```

    ## Method Parameters

    <ParamField body="note" type="Note" required>

      Note to create.

      See the [Note Object](/api-reference/crm/members-contacts/contacts/notes/notes-v2/note-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Note>`.

    See the [Note Object](/api-reference/crm/members-contacts/contacts/notes/notes-v2/note-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Note Created | `wix.crm.notes.v2.note_created` | Triggered when a note is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-note" id="ex-sdk-create-note-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-note" id="ex-sdk-create-note-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-note" id="ex-sdk-create-note-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-note-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-note-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createNote</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createNote (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createNote (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-note-0"><span className="wix-picker-name">createNote</span></label>
    <label htmlFor="ex-sdk-create-note-1"><span className="wix-picker-name">createNote (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-note-2"><span className="wix-picker-name">createNote (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createNote
    import { notes } from '@wix/crm';

    async function createNote(note) {
      const response = await notes.createNote(note);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createNote (self-hosted)
    import { createClient } from '@wix/sdk';
    import { notes } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { notes },
      // Include the auth strategy and host as relevant
    });

    async function createNote(note) {
      const response = await myWixClient.notes.createNote(note);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createNote (with elevated permissions)
    import { notes } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myCreateNoteMethod(note) {
      const elevatedCreateNote = auth.elevate(notes.createNote);
      const response = await elevatedCreateNote(note);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
