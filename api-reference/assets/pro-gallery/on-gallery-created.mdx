---
title: "onGalleryCreated"
description: "Triggered when a gallery is created."
icon: "bolt"
---

Triggered when a gallery is created.

<Note>

The event data doesn't include gallery items or their IDs.
To receive information about the created items you need to listen to the [Gallery Item Created webhook](https://dev.wix.com/api/rest/site-content/pro-gallery/gallery-item-created-webhook).

</Note>

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pro_gallery.gallery_v2_created` |
| Package | `@wix/pro-gallery` |
| Permissions | `Manage Galleries`, `Read Galleries` |

## Examples

<CodeGroup>

  ```javascript onGalleryCreated
  import { proGallery } from '@wix/pro-gallery';

  proGallery.onGalleryCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onGalleryCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { proGallery } from '@wix/pro-gallery';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        proGallery,
      },
  });

  wixClient.proGallery.onGalleryCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
