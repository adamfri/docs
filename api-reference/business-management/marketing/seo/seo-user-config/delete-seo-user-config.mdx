---
title: "Delete Seo User Config"
description: "Deletes the SEO configuration for the current site."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    Deletes the SEO configuration for the current site.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="seo_user_config:v1:seo_user_config:delete_seo_user_config">seo_user_config:v1:seo_user_config:delete_seo_user_config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/seo/user-config/v1/seo-user-config
    ```

    ## Response Object

    Returns `DeleteSeoUserConfigResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the SEO configuration for the current site.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="seo_user_config:v1:seo_user_config:delete_seo_user_config">seo_user_config:v1:seo_user_config:delete_seo_user_config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { seoUserConfig } from '@wix/seo-user-config';

    seoUserConfig.deleteSeoUserConfig(): Promise
    ```

    ## Returns

    Returns `DeleteSeoUserConfigResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Seo User Config Deleted | `wix.seo_user_config.v1.seo_user_config_deleted` | Triggered when an SEO configuration is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-seo-user-config" id="ex-sdk-delete-seo-user-config-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-seo-user-config" id="ex-sdk-delete-seo-user-config-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-seo-user-config" id="ex-sdk-delete-seo-user-config-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-seo-user-config-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-seo-user-config-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteSeoUserConfig</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSeoUserConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSeoUserConfig (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-seo-user-config-0"><span className="wix-picker-name">deleteSeoUserConfig</span></label>
    <label htmlFor="ex-sdk-delete-seo-user-config-1"><span className="wix-picker-name">deleteSeoUserConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-seo-user-config-2"><span className="wix-picker-name">deleteSeoUserConfig (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteSeoUserConfig
    import { seoUserConfig } from '@wix/seo-user-config';

    async function deleteSeoUserConfig() {
      const response = await seoUserConfig.deleteSeoUserConfig();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSeoUserConfig (self-hosted)
    import { createClient } from '@wix/sdk';
    import { seoUserConfig } from '@wix/seo-user-config';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { seoUserConfig },
      // Include the auth strategy and host as relevant
    });

    async function deleteSeoUserConfig() {
      const response = await myWixClient.seoUserConfig.deleteSeoUserConfig();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSeoUserConfig (with elevated permissions)
    import { seoUserConfig } from '@wix/seo-user-config';
    import { auth } from '@wix/essentials';

    async function myDeleteSeoUserConfigMethod() {
      const elevatedDeleteSeoUserConfig = auth.elevate(seoUserConfig.deleteSeoUserConfig);
      const response = await elevatedDeleteSeoUserConfig();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
