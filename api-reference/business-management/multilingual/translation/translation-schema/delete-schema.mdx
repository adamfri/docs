---
title: "Delete Schema"
description: "Deletes a translation schema."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a translation schema.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILINGUAL.WRITE_TRANSLATION_SCHEMA">Wix Multilingual - Translation Schema Write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/translation-schema/v1/schemas/{schemaId}
    ```

    ## Path Params

    <ParamField path="schemaId" type="string" required>

      ID of the translation schema to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteSchemaResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/translation-schema/v1/schemas/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a translation schema.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILINGUAL.WRITE_TRANSLATION_SCHEMA">Wix Multilingual - Translation Schema Write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { translationSchemas } from '@wix/multilingual';

    translationSchemas.deleteSchema(schemaId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="schemaId" type="string" required>

      ID of the translation schema to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Schema Deleted | `wix.multilingual.translation.v1.schema_deleted` | Triggered when translation schema is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-schema" id="ex-sdk-delete-schema-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-schema" id="ex-sdk-delete-schema-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-schema" id="ex-sdk-delete-schema-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-schema-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-schema-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteSchema</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSchema (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSchema (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-schema-0"><span className="wix-picker-name">deleteSchema</span></label>
    <label htmlFor="ex-sdk-delete-schema-1"><span className="wix-picker-name">deleteSchema (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-schema-2"><span className="wix-picker-name">deleteSchema (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteSchema
    import { translationSchemas } from '@wix/multilingual';

    async function deleteSchema(schemaId) {
      const response = await translationSchemas.deleteSchema(schemaId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSchema (self-hosted)
    import { createClient } from '@wix/sdk';
    import { translationSchemas } from '@wix/multilingual';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { translationSchemas },
      // Include the auth strategy and host as relevant
    });

    async function deleteSchema(schemaId) {
      const response = await myWixClient.translationSchemas.deleteSchema(schemaId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSchema (with elevated permissions)
    import { translationSchemas } from '@wix/multilingual';
    import { auth } from '@wix/essentials';

    async function myDeleteSchemaMethod(schemaId) {
      const elevatedDeleteSchema = auth.elevate(translationSchemas.deleteSchema);
      const response = await elevatedDeleteSchema(schemaId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
