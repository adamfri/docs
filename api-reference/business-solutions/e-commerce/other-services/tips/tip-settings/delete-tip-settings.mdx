---
title: "Delete Tip Settings"
description: "Deletes a tipSettings object."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a `tipSettings` object.

    You can't delete the default `tipSettings` object.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/tip-settings/v1/tip-settings/{tipSettingsId}
    ```

    ## Path Params

    <ParamField path="tipSettingsId" type="string" required>

      ID of the `tipSettings` object to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteTipSettingsResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CANNOT_DELETE_DEFAULT_SETTINGS` | Can't delete the default `tipSettings` object. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/tip-settings/v1/tip-settings/1338729c-7255-48f1-a66e-f6a8e4b6f9f3' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a `tipSettings` object.

    You can't delete the default `tipSettings` object.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tipSettings } from '@wix/ecom';

    tipSettings.deleteTipSettings(tipSettingsId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="tipSettingsId" type="string" required>

      ID of the `tipSettings` object to delete.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CANNOT_DELETE_DEFAULT_SETTINGS` | Can't delete the default `tipSettings` object. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tip Settings Deleted | `wix.tips.settings.v1.tip_settings_deleted` | Triggered when a `tipSettings` object is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-tip-settings" id="ex-sdk-delete-tip-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-tip-settings" id="ex-sdk-delete-tip-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-tip-settings" id="ex-sdk-delete-tip-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-tip-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-tip-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteTipSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteTipSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteTipSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-tip-settings-0"><span className="wix-picker-name">deleteTipSettings</span></label>
    <label htmlFor="ex-sdk-delete-tip-settings-1"><span className="wix-picker-name">deleteTipSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-tip-settings-2"><span className="wix-picker-name">deleteTipSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteTipSettings
    import { tipSettings } from '@wix/ecom';

    async function deleteTipSettings(tipSettingsId) {
      const response = await tipSettings.deleteTipSettings(tipSettingsId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteTipSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tipSettings } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tipSettings },
      // Include the auth strategy and host as relevant
    });

    async function deleteTipSettings(tipSettingsId) {
      const response = await myWixClient.tipSettings.deleteTipSettings(tipSettingsId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteTipSettings (with elevated permissions)
    import { tipSettings } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myDeleteTipSettingsMethod(tipSettingsId) {
      const elevatedDeleteTipSettings = auth.elevate(tipSettings.deleteTipSettings);
      const response = await elevatedDeleteTipSettings(tipSettingsId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
