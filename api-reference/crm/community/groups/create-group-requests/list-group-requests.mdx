---
title: "List Group Requests"
description: "Lists group requests across a site."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Lists group requests across a site.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/_api/social-groups-proxy/group-requests/v2/group-requests
    ```

    ## Query Params

    <ParamField query="limit" type="integer">

      Number of items to load. Maximum `100`.

    </ParamField>

    <ParamField query="offset" type="integer">

      Number of items to skip in the current sort order.

    </ParamField>

    ## Response Object

    <ResponseField name="groupRequests" type="GroupRequest[]">

      Group Requests.

      See the [Group Request Object](/api-reference/crm/community/groups/create-group-requests/group-request-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging information.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
          https://wixapis.com/social-groups/v2/group-requests?limit=100&ownershipFilter=CURRENT_MEMBER \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "groupRequests": [
              {
                  "id": "a277bc77-1812-4484-8888-dc8cd0c357f2",
                  "group": {
                      "id": "a277bc77-1812-4484-8888-dc8cd0c357f2",
                      "slug": "group-3",
                      "privacyLevel": "PUBLIC",
                      "title": "Group 3",
                      "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
                      "details": {
                          "membersTitle": "Friends"
                      },
                      "settings": {
                          "membersPermittedToInvite": false,
                          "membersPermittedToApprove": false,
                          "memberWelcomePostEnabled": true,
                          "groupDetailsChangedPostEnabled": true,
                          "showMemberList": true
                      },
                      "membersCount": 1,
                      "createdBy": {
                          "id": "34571ac2-2785-482b-924a-6cbede6d378f",
                          "identityType": "MEMBER"
                      },
                      "createdDate": "2020-12-21T09:29:39Z",
                      "updatedDate": "2020-12-21T09:29:42Z",
                      "recentActivityDate": "2020-12-21T09:29:39Z"
                  },
                  "status": "PENDING",
                  "requestDetails": {}
              },
              {
                  "id": "0ba99511-c625-4317-95ec-ebf26a362f2c",
                  "group": {
                      "id": "0ba99511-c625-4317-95ec-ebf26a362f2c",
                      "slug": "group-1",
                      "privacyLevel": "PUBLIC",
                      "title": "Group 1",
                      "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
                      "details": {
                          "membersTitle": "Friends"
                      },
                      "settings": {
                          "membersPermittedToInvite": false,
                          "membersPermittedToApprove": false,
                          "memberWelcomePostEnabled": true,
                          "groupDetailsChangedPostEnabled": true,
                          "showMemberList": true
                      },
                      "membersCount": 1,
                      "createdBy": {
                          "id": "34571ac2-2785-482b-924a-6cbede6d378f",
                          "identityType": "MEMBER"
                      },
                      "createdDate": "2020-12-21T09:05:06Z",
                      "updatedDate": "2020-12-21T09:16:06Z",
                      "recentActivityDate": "2020-12-21T09:05:06Z"
                  },
                  "status": "APPROVED",
                  "requestDetails": {}
              }
          ],
          "metadata": {
              "count": 2,
              "offset": 0,
              "total": 2
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Lists group requests across a site.

    ## Method Declaration

    ```typescript
    import { groupRequests } from '@wix/groups';

    groupRequests.listGroupRequests(options): Promise<ListGroupRequestsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListGroupRequestsOptions">

      Paging options.

      <Expandable title="options properties">

        <ParamField body="limit" type="integer">

          Number of items to load. Maximum `100`.

        </ParamField>

        <ParamField body="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListGroupRequestsResponse>`.

    <ResponseField name="groupRequests" type="GroupRequest[]">

      Group Requests.

      See the [Group Request Object](/api-reference/crm/community/groups/create-group-requests/group-request-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging information.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-group-requests" id="ex-sdk-list-group-requests-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-group-requests" id="ex-sdk-list-group-requests-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-group-requests" id="ex-sdk-list-group-requests-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-group-requests-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-group-requests-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listGroupRequests</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listGroupRequests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listGroupRequests (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-group-requests-0"><span className="wix-picker-name">listGroupRequests</span></label>
    <label htmlFor="ex-sdk-list-group-requests-1"><span className="wix-picker-name">listGroupRequests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-group-requests-2"><span className="wix-picker-name">listGroupRequests (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listGroupRequests
    import { groupRequests } from '@wix/groups';

    async function listGroupRequests(options) {
      const response = await groupRequests.listGroupRequests(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listGroupRequests (self-hosted)
    import { createClient } from '@wix/sdk';
    import { groupRequests } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { groupRequests },
      // Include the auth strategy and host as relevant
    });

    async function listGroupRequests(options) {
      const response = await myWixClient.groupRequests.listGroupRequests(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listGroupRequests (with elevated permissions)
    import { groupRequests } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myListGroupRequestsMethod(options) {
      const elevatedListGroupRequests = auth.elevate(groupRequests.listGroupRequests);
      const response = await elevatedListGroupRequests(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
