---
title: "Bulk Update Experience Tags By Filter"
description: "Asynchronously updates tags on multiple experiences using filter criteria."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Asynchronously updates [tags](https://dev.wix.com/docs/api-reference/business-management/tags/introduction) on multiple experiences using filter criteria. An empty filter updates all experiences. A tag that appears in both assign and unassign lists is assigned.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-LOCATIONS">Manage Reservation Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/table-reservations/experiences/v1/bulk/experiences/update-tags-by-filter
    ```

    ## Body Params

    <ParamField body="filter" type="object" required>

      Filter criteria to select experiences for tag updates.

    </ParamField>

    <ParamField body="assignTags" type="Tags">

      Tags to assign to the filtered experiences.

      <Expandable title="assignTags properties">

        <ParamField body="privateTags" type="TagList">

          Tags that require an additional permission in order to access them, normally not given to site members or visitors.

          <Expandable title="privateTags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 5`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="publicTags" type="TagList">

          Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

          <Expandable title="publicTags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 5`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="unassignTags" type="Tags">

      Tags to unassign from the filtered experiences.

      <Expandable title="unassignTags properties">

        <ParamField body="privateTags" type="TagList">

          Tags that require an additional permission in order to access them, normally not given to site members or visitors.

          <Expandable title="privateTags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 5`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="publicTags" type="TagList">

          Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

          <Expandable title="publicTags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 5`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID for tracking the asynchronous bulk update operation.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `EMPTY_ASSIGN_AND_UNASSIGN_LISTS` | No tags were specified to assign or unassign. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST "https://www.wixapis.com/table-reservations/experiences/v1/bulk/experiences/update-tags-by-filter" \
        -H "Authorization: <AUTH>" \
        -H "Content-Type: application/json" \
        --data '
      {
        "filter": {
          "operator": "and",
          "filters": [
            { "path": "tags", "operator": "hasAny", "value": ["brunch"] }
          ]
        },
        "assignTags": {
          "publicTags": {
            "tagIds": ["fall-2025"]
          }
        },
        "unassignTags": {
          "publicTags": {
            "tagIds": ["summer-2025"]
          }
        }
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "jobId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Asynchronously updates [tags](https://dev.wix.com/docs/api-reference/business-management/tags/introduction) on multiple experiences using filter criteria. An empty filter updates all experiences. A tag that appears in both assign and unassign lists is assigned.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-LOCATIONS">Manage Reservation Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { experiences } from '@wix/table-reservations';

    experiences.bulkUpdateExperienceTagsByFilter(filter, options): Promise
    ```

    ## Method Parameters

    <ParamField body="filter" type="object" required>

      Filter criteria to select experiences for tag updates.

    </ParamField>

    <ParamField body="options" type="BulkUpdateExperienceTagsByFilterOptions">

      options

      <Expandable title="options properties">

        <ParamField body="assignTags" type="Tags">

          Tags to assign to the filtered experiences.

          <Expandable title="assignTags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="publicTags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="unassignTags" type="Tags">

          Tags to unassign from the filtered experiences.

          <Expandable title="unassignTags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="publicTags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="jobId" type="string">

      Job ID for tracking the asynchronous bulk update operation.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `EMPTY_ASSIGN_AND_UNASSIGN_LISTS` | No tags were specified to assign or unassign. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-experience-tags-by-filter" id="ex-sdk-bulk-update-experience-tags-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-experience-tags-by-filter" id="ex-sdk-bulk-update-experience-tags-by-filter-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-update-experience-tags-by-filter" id="ex-sdk-bulk-update-experience-tags-by-filter-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-experience-tags-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-experience-tags-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateExperienceTagsByFilter</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateExperienceTagsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateExperienceTagsByFilter (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-experience-tags-by-filter-0"><span className="wix-picker-name">bulkUpdateExperienceTagsByFilter</span></label>
    <label htmlFor="ex-sdk-bulk-update-experience-tags-by-filter-1"><span className="wix-picker-name">bulkUpdateExperienceTagsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-update-experience-tags-by-filter-2"><span className="wix-picker-name">bulkUpdateExperienceTagsByFilter (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUpdateExperienceTagsByFilter
    import { experiences } from '@wix/table-reservations';

    async function bulkUpdateExperienceTagsByFilter(filter,options) {
      const response = await experiences.bulkUpdateExperienceTagsByFilter(filter,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateExperienceTagsByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { experiences } from '@wix/table-reservations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { experiences },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdateExperienceTagsByFilter(filter,options) {
      const response = await myWixClient.experiences.bulkUpdateExperienceTagsByFilter(filter,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateExperienceTagsByFilter (with elevated permissions)
    import { experiences } from '@wix/table-reservations';
    import { auth } from '@wix/essentials';

    async function myBulkUpdateExperienceTagsByFilterMethod(filter,options) {
      const elevatedBulkUpdateExperienceTagsByFilter = auth.elevate(experiences.bulkUpdateExperienceTagsByFilter);
      const response = await elevatedBulkUpdateExperienceTagsByFilter(filter,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
