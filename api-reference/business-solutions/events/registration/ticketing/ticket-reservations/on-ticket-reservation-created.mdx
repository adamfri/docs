---
title: "onTicketReservationCreated"
description: "Triggered when a ticket reservation is created."
icon: "bolt"
---

Triggered when a ticket reservation is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v1.ticket_reservation_created` |
| Package | `@wix/events` |
| Permissions | `Manage Events - all permissions`, `Manage Events`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onTicketReservationCreated
  import { ticketReservations } from '@wix/events';

  ticketReservations.onTicketReservationCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTicketReservationCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ticketReservations } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        ticketReservations,
      },
  });

  wixClient.ticketReservations.onTicketReservationCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
