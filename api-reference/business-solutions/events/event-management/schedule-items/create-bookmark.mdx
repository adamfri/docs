---
title: "Create Bookmark"
description: "Bookmarks a schedule item for a current member."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Bookmarks a schedule item for a current member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EVENTS.MANAGE-EVENTS">Manage Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/schedule/{itemId}/bookmark
    ```

    ## Path Params

    <ParamField path="itemId" type="string" required>

      Schedule item ID.

    </ParamField>

    ## Body Params

    <ParamField body="eventId" type="string" required>

      Event ID to which the schedule item belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    Returns `CreateBookmarkResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/events/v1/agenda/80a28db1-672b-45db-a62c-5315ee07ae22/bookmark' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Bookmarks a schedule item for a current member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EVENTS.MANAGE-EVENTS">Manage Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Method Declaration

    ```typescript
    import { scheduleBookmarks } from '@wix/events';

    scheduleBookmarks.createBookmark(itemId, eventId): Promise
    ```

    ## Method Parameters

    <ParamField body="itemId" type="string" required>

      Schedule item ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="eventId" type="string" required>

      Event ID to which the schedule item belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `CreateBookmarkResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-bookmark" id="ex-sdk-create-bookmark-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-bookmark" id="ex-sdk-create-bookmark-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-bookmark-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-bookmark-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createBookmark</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createBookmark (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-bookmark-0"><span className="wix-picker-name">createBookmark</span></label>
    <label htmlFor="ex-sdk-create-bookmark-1"><span className="wix-picker-name">createBookmark (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createBookmark
    import { scheduleBookmarks } from '@wix/events';

    async function createBookmark(itemId,eventId) {
      const response = await scheduleBookmarks.createBookmark(itemId,eventId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createBookmark (self-hosted)
    import { createClient } from '@wix/sdk';
    import { scheduleBookmarks } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { scheduleBookmarks },
      // Include the auth strategy and host as relevant
    });

    async function createBookmark(itemId,eventId) {
      const response = await myWixClient.scheduleBookmarks.createBookmark(itemId,eventId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
