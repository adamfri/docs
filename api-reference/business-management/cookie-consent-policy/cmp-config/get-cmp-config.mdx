---
title: "Get Cmp Config"
description: "Retrieves a CMP config."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a CMP config.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CMP_CONFIG.READ_ONLY">Read CMP Configurations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/consent/cmp/v2/cmp-configs
    ```

    ## Response Object

    <ResponseField name="cmpConfig" type="CmpConfig">

      Requested CMP config.

      See the [Cmp Config Object](/api-reference/business-management/cookie-consent-policy/cmp-config/cmp-config-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/consent/cmp/v2/cmp-configs' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json'
      ```

      ```json Response
      {
          "cmpConfig": {
              "revision": "1",
              "createdDate": "2025-02-16T11:10:47.613Z",
              "updatedDate": "2025-02-16T11:11:36.211Z",
              "socialElementsBlocked": true
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a CMP config.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CMP_CONFIG.READ_ONLY">Read CMP Configurations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { cmp } from '@wix/consent-policy';

    cmp.getCmpConfig(): Promise
    ```

    ## Returns

    Returns `CmpConfig`.

    See the [Cmp Config Object](/api-reference/business-management/cookie-consent-policy/cmp-config/cmp-config-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-cmp-config" id="ex-sdk-get-cmp-config-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-cmp-config" id="ex-sdk-get-cmp-config-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-cmp-config" id="ex-sdk-get-cmp-config-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-cmp-config-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-cmp-config-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getCmpConfig</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCmpConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCmpConfig (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-cmp-config-0"><span className="wix-picker-name">getCmpConfig</span></label>
    <label htmlFor="ex-sdk-get-cmp-config-1"><span className="wix-picker-name">getCmpConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-cmp-config-2"><span className="wix-picker-name">getCmpConfig (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getCmpConfig
    import { cmp } from '@wix/consent-policy';

    async function getCmpConfig() {
      const response = await cmp.getCmpConfig();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCmpConfig (self-hosted)
    import { createClient } from '@wix/sdk';
    import { cmp } from '@wix/consent-policy';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { cmp },
      // Include the auth strategy and host as relevant
    });

    async function getCmpConfig() {
      const response = await myWixClient.cmp.getCmpConfig();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCmpConfig (with elevated permissions)
    import { cmp } from '@wix/consent-policy';
    import { auth } from '@wix/essentials';

    async function myGetCmpConfigMethod() {
      const elevatedGetCmpConfig = auth.elevate(cmp.getCmpConfig);
      const response = await elevatedGetCmpConfig();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
