---
title: "Invoice Preset Object"
description: "An invoice preset is a reusable template that defines the appearance, content structure, and default settings for invoices."
icon: "cube"
---

An invoice preset is a reusable template that defines the appearance, content structure, and default settings for invoices. Each site always has at least one preset, and one preset is designated as the default, which is automatically applied when creating invoices.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Invoice preset ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the invoice preset is updated. To prevent conflicting changes, the current revision must be passed when updating the invoice preset.

      Ignored when creating an invoice preset.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the invoice preset was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time the invoice preset was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Name of the preset.

      Constraints: `minLength 1`, `maxLength 100`.

    </ResponseField>

    <ResponseField name="displaySettings" type="DisplaySettings">

      Display settings that control which sections and fields appear on invoices created with this preset.

      <Expandable title="displaySettings properties">

        <ResponseField name="businessInfo" type="BusinessDisplayOptions">

          Options for displaying business information on the invoice.

          <Expandable title="businessInfo properties">

            <ResponseField name="displayEmail" type="boolean">

              Whether to display the business email on the invoice.

            </ResponseField>

            <ResponseField name="displayPhoneNumber" type="boolean">

              Whether to display the business phone number on the invoice.

            </ResponseField>

            <ResponseField name="displayAddress" type="boolean">

              Whether to display the business address on the invoice.

            </ResponseField>

            <ResponseField name="displayCompanyId" type="boolean">

              Whether to display the business company ID on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customerInfo" type="CustomerDisplayOptions">

          Options for displaying customer information on the invoice.

          <Expandable title="customerInfo properties">

            <ResponseField name="displayEmail" type="boolean">

              Whether to display the customer email on the invoice.

            </ResponseField>

            <ResponseField name="displayPhoneNumber" type="boolean">

              Whether to display the customer phone number on the invoice.

            </ResponseField>

            <ResponseField name="displayShippingAddress" type="boolean">

              Whether to display the customer shipping address on the invoice.

            </ResponseField>

            <ResponseField name="displayBillingAddress" type="boolean">

              Whether to display the customer billing address on the invoice.

            </ResponseField>

            <ResponseField name="displayVatId" type="boolean">

              Whether to display the customer VAT ID on the invoice.

            </ResponseField>

            <ResponseField name="displayCompanyName" type="boolean">

              Whether to display the customer company name on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="itemsInfo" type="ItemsDisplayOptions">

          Options for displaying line item details on the invoice.

          <Expandable title="itemsInfo properties">

            <ResponseField name="displayDescription" type="boolean">

              Whether to display the item description on the invoice.

            </ResponseField>

            <ResponseField name="displayTax" type="boolean">

              Whether to display tax information on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalsInfo" type="TotalsDisplayOptions">

          Options for displaying totals and tax information on the invoice.

          <Expandable title="totalsInfo properties">

            <ResponseField name="displayTaxBreakdown" type="boolean">

              Whether to display a breakdown of taxes applied.

            </ResponseField>

            <ResponseField name="displayItemSubtotalPerTaxBreakdown" type="boolean">

              Whether to display the item subtotal for each tax rate in the breakdown.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paymentsInfo" type="PaymentsDisplayOptions">

          Options for displaying payment information on the invoice.

          <Expandable title="paymentsInfo properties">

            <ResponseField name="displayPaymentsInfo" type="boolean">

              Whether to display payment information on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customFields" type="CustomFields">

      Custom fields for adding additional information to invoices, such as header details, business information, customer information, and a footer.

      <Expandable title="customFields properties">

        <ResponseField name="headerCustomFields" type="CustomField[]">

          Custom fields displayed in the header section of the invoice.

          Constraints: `maxItems 4`.

          <Expandable title="headerCustomFields properties">

            <ResponseField name="title" type="string">

              Title of the custom field. If omitted, only the value is displayed on the invoice.

            </ResponseField>

            <ResponseField name="value" type="string">

              Value of the custom field.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="businessCustomFields" type="CustomField[]">

          Custom fields displayed in the business information section of the invoice.

          Constraints: `maxItems 5`.

          <Expandable title="businessCustomFields properties">

            <ResponseField name="title" type="string">

              Title of the custom field. If omitted, only the value is displayed on the invoice.

            </ResponseField>

            <ResponseField name="value" type="string">

              Value of the custom field.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customerCustomFields" type="CustomField[]">

          Custom fields displayed in the customer information section of the invoice.

          Constraints: `maxItems 4`.

          <Expandable title="customerCustomFields properties">

            <ResponseField name="title" type="string">

              Title of the custom field. If omitted, only the value is displayed on the invoice.

            </ResponseField>

            <ResponseField name="value" type="string">

              Value of the custom field.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="footerCustomField" type="RichContent">

          Footer content displayed at the bottom of the invoice. Supports text formatting and images. Individual invoices can override this content using `customFields.footerCustomField.overridePresetContent`.

          <Expandable title="footerCustomField properties">

            <ResponseField name="nodes" type="Node[]">

              Node objects representing a rich content document.

              <Expandable title="nodes properties">

                <ResponseField name="type" type="string">

                  Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

                  **Supported values:**

                  - `PARAGRAPH`
                  - `TEXT`
                  - `HEADING`
                  - `BULLETED_LIST`
                  - `ORDERED_LIST`
                  - `LIST_ITEM`
                  - `BLOCKQUOTE`
                  - `CODE_BLOCK`
                  - `VIDEO`
                  - `DIVIDER`
                  - `FILE`
                  - `GALLERY`
                  - `GIF`
                  - `HTML`
                  - `IMAGE`
                  - `LINK_PREVIEW`
                  - `MAP` — @deprecated
                  - `POLL`
                  - `APP_EMBED`
                  - `BUTTON`
                  - `COLLAPSIBLE_LIST`
                  - `TABLE`
                  - `EMBED`
                  - `COLLAPSIBLE_ITEM`
                  - `COLLAPSIBLE_ITEM_TITLE`
                  - `COLLAPSIBLE_ITEM_BODY`
                  - `TABLE_CELL`
                  - `TABLE_ROW`
                  - `EXTERNAL`
                  - `AUDIO`
                  - `CAPTION`
                  - `LAYOUT`
                  - `LAYOUT_CELL`
                  - `SHAPE`
                  - `CARD`
                  - `TOC`
                  - `SMART_BLOCK`
                  - `SMART_BLOCK_CELL`
                  - `CHECKBOX_LIST`

                </ResponseField>

                <ResponseField name="id" type="string">

                  Node ID.

                </ResponseField>

                <ResponseField name="nodes" type="Node[]">

                  A list of child nodes.

                  _Recursive reference to `Node[]`._

                </ResponseField>

                <ResponseField name="style" type="NodeStyle">

                  Padding and background color styling for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="metadata" type="Metadata">

              Object metadata.

              <Expandable title="metadata properties">

                <ResponseField name="version" type="integer">

                  Schema version.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="createdTimestamp" type="datetime">

                  When the object was created.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="updatedTimestamp" type="datetime">

                  When the object was most recently updated.

                </ResponseField>

                <ResponseField name="id" type="string">

                  Object ID.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              <Expandable title="documentStyle properties">

                <ResponseField name="headerOne" type="TextNodeStyle">

                  Styling for H1 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerTwo" type="TextNodeStyle">

                  Styling for H2 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerThree" type="TextNodeStyle">

                  Styling for H3 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFour" type="TextNodeStyle">

                  Styling for H4 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFive" type="TextNodeStyle">

                  Styling for H5 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerSix" type="TextNodeStyle">

                  Styling for H6 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="paragraph" type="TextNodeStyle">

                  Styling for paragraph nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="blockquote" type="TextNodeStyle">

                  Styling for block quote nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="codeBlock" type="TextNodeStyle">

                  Styling for code block nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="headerPlaceholders" type="CustomFieldPlaceholder[]">

          Placeholders for header fields that appear as blank titled fields on the invoice for the recipient to fill in.

          Constraints: `maxItems 4`.

          <Expandable title="headerPlaceholders properties">

            <ResponseField name="title" type="string">

              Title of the placeholder field.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customerPlaceholders" type="CustomFieldPlaceholder[]">

          Placeholders for customer fields that appear as blank titled fields on the invoice for the recipient to fill in.

          Constraints: `maxItems 4`.

          <Expandable title="customerPlaceholders properties">

            <ResponseField name="title" type="string">

              Title of the placeholder field.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="defaultPreset" type="boolean">

      Whether this preset is the default preset.

      **Read-only.**

    </ResponseField>

    <ResponseField name="displayValues" type="DisplayValues">

      Display value overrides for invoices created with this preset. When set, these values replace the corresponding values on the generated invoice document.

      <Expandable title="displayValues properties">

        <ResponseField name="titleOverride" type="string">

          Title to display on the generated invoice document, overriding the invoice's own `title`. If not set, the invoice's `title` is used.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="defaultDaysUntilDue" type="integer">

      Number of days after invoice creation until the invoice is due. Set to `0` for due on receipt.

      Default: `30`

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Invoice preset ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the invoice preset is updated. To prevent conflicting changes, the current revision must be passed when updating the invoice preset.

      Ignored when creating an invoice preset.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the invoice preset was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time the invoice preset was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Name of the preset.

      Constraints: `minLength 1`, `maxLength 100`.

    </ResponseField>

    <ResponseField name="displaySettings" type="DisplaySettings">

      Display settings that control which sections and fields appear on invoices created with this preset.

      <Expandable title="displaySettings properties">

        <ResponseField name="businessInfo" type="BusinessDisplayOptions">

          Options for displaying business information on the invoice.

          <Expandable title="businessInfo properties">

            <ResponseField name="displayEmail" type="boolean">

              Whether to display the business email on the invoice.

            </ResponseField>

            <ResponseField name="displayPhoneNumber" type="boolean">

              Whether to display the business phone number on the invoice.

            </ResponseField>

            <ResponseField name="displayAddress" type="boolean">

              Whether to display the business address on the invoice.

            </ResponseField>

            <ResponseField name="displayCompanyId" type="boolean">

              Whether to display the business company ID on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customerInfo" type="CustomerDisplayOptions">

          Options for displaying customer information on the invoice.

          <Expandable title="customerInfo properties">

            <ResponseField name="displayEmail" type="boolean">

              Whether to display the customer email on the invoice.

            </ResponseField>

            <ResponseField name="displayPhoneNumber" type="boolean">

              Whether to display the customer phone number on the invoice.

            </ResponseField>

            <ResponseField name="displayShippingAddress" type="boolean">

              Whether to display the customer shipping address on the invoice.

            </ResponseField>

            <ResponseField name="displayBillingAddress" type="boolean">

              Whether to display the customer billing address on the invoice.

            </ResponseField>

            <ResponseField name="displayVatId" type="boolean">

              Whether to display the customer VAT ID on the invoice.

            </ResponseField>

            <ResponseField name="displayCompanyName" type="boolean">

              Whether to display the customer company name on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="itemsInfo" type="ItemsDisplayOptions">

          Options for displaying line item details on the invoice.

          <Expandable title="itemsInfo properties">

            <ResponseField name="displayDescription" type="boolean">

              Whether to display the item description on the invoice.

            </ResponseField>

            <ResponseField name="displayTax" type="boolean">

              Whether to display tax information on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalsInfo" type="TotalsDisplayOptions">

          Options for displaying totals and tax information on the invoice.

          <Expandable title="totalsInfo properties">

            <ResponseField name="displayTaxBreakdown" type="boolean">

              Whether to display a breakdown of taxes applied.

            </ResponseField>

            <ResponseField name="displayItemSubtotalPerTaxBreakdown" type="boolean">

              Whether to display the item subtotal for each tax rate in the breakdown.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paymentsInfo" type="PaymentsDisplayOptions">

          Options for displaying payment information on the invoice.

          <Expandable title="paymentsInfo properties">

            <ResponseField name="displayPaymentsInfo" type="boolean">

              Whether to display payment information on the invoice.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customFields" type="CustomFields">

      Custom fields for adding additional information to invoices, such as header details, business information, customer information, and a footer.

      <Expandable title="customFields properties">

        <ResponseField name="headerCustomFields" type="CustomField[]">

          Custom fields displayed in the header section of the invoice.

          Constraints: `maxItems 4`.

          <Expandable title="headerCustomFields properties">

            <ResponseField name="title" type="string">

              Title of the custom field. If omitted, only the value is displayed on the invoice.

            </ResponseField>

            <ResponseField name="value" type="string">

              Value of the custom field.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="businessCustomFields" type="CustomField[]">

          Custom fields displayed in the business information section of the invoice.

          Constraints: `maxItems 5`.

          <Expandable title="businessCustomFields properties">

            <ResponseField name="title" type="string">

              Title of the custom field. If omitted, only the value is displayed on the invoice.

            </ResponseField>

            <ResponseField name="value" type="string">

              Value of the custom field.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customerCustomFields" type="CustomField[]">

          Custom fields displayed in the customer information section of the invoice.

          Constraints: `maxItems 4`.

          <Expandable title="customerCustomFields properties">

            <ResponseField name="title" type="string">

              Title of the custom field. If omitted, only the value is displayed on the invoice.

            </ResponseField>

            <ResponseField name="value" type="string">

              Value of the custom field.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="footerCustomField" type="RichContent">

          Footer content displayed at the bottom of the invoice. Supports text formatting and images. Individual invoices can override this content using `customFields.footerCustomField.overridePresetContent`.

          <Expandable title="footerCustomField properties">

            <ResponseField name="nodes" type="Node[]">

              Node objects representing a rich content document.

              <Expandable title="nodes properties">

                <ResponseField name="type" type="string">

                  Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

                  **Supported values:**

                  - `PARAGRAPH`
                  - `TEXT`
                  - `HEADING`
                  - `BULLETED_LIST`
                  - `ORDERED_LIST`
                  - `LIST_ITEM`
                  - `BLOCKQUOTE`
                  - `CODE_BLOCK`
                  - `VIDEO`
                  - `DIVIDER`
                  - `FILE`
                  - `GALLERY`
                  - `GIF`
                  - `HTML`
                  - `IMAGE`
                  - `LINK_PREVIEW`
                  - `MAP` — @deprecated
                  - `POLL`
                  - `APP_EMBED`
                  - `BUTTON`
                  - `COLLAPSIBLE_LIST`
                  - `TABLE`
                  - `EMBED`
                  - `COLLAPSIBLE_ITEM`
                  - `COLLAPSIBLE_ITEM_TITLE`
                  - `COLLAPSIBLE_ITEM_BODY`
                  - `TABLE_CELL`
                  - `TABLE_ROW`
                  - `EXTERNAL`
                  - `AUDIO`
                  - `CAPTION`
                  - `LAYOUT`
                  - `LAYOUT_CELL`
                  - `SHAPE`
                  - `CARD`
                  - `TOC`
                  - `SMART_BLOCK`
                  - `SMART_BLOCK_CELL`
                  - `CHECKBOX_LIST`

                </ResponseField>

                <ResponseField name="_id" type="string">

                  Node ID.

                </ResponseField>

                <ResponseField name="nodes" type="Node[]">

                  A list of child nodes.

                  _Recursive reference to `Node[]`._

                </ResponseField>

                <ResponseField name="style" type="NodeStyle">

                  Padding and background color styling for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="metadata" type="Metadata">

              Object metadata.

              <Expandable title="metadata properties">

                <ResponseField name="version" type="integer">

                  Schema version.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="createdTimestamp" type="datetime">

                  When the object was created.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="updatedTimestamp" type="datetime">

                  When the object was most recently updated.

                </ResponseField>

                <ResponseField name="_id" type="string">

                  Object ID.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              <Expandable title="documentStyle properties">

                <ResponseField name="headerOne" type="TextNodeStyle">

                  Styling for H1 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerTwo" type="TextNodeStyle">

                  Styling for H2 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerThree" type="TextNodeStyle">

                  Styling for H3 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFour" type="TextNodeStyle">

                  Styling for H4 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFive" type="TextNodeStyle">

                  Styling for H5 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerSix" type="TextNodeStyle">

                  Styling for H6 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="paragraph" type="TextNodeStyle">

                  Styling for paragraph nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="blockquote" type="TextNodeStyle">

                  Styling for block quote nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="codeBlock" type="TextNodeStyle">

                  Styling for code block nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="headerPlaceholders" type="CustomFieldPlaceholder[]">

          Placeholders for header fields that appear as blank titled fields on the invoice for the recipient to fill in.

          Constraints: `maxItems 4`.

          <Expandable title="headerPlaceholders properties">

            <ResponseField name="title" type="string">

              Title of the placeholder field.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customerPlaceholders" type="CustomFieldPlaceholder[]">

          Placeholders for customer fields that appear as blank titled fields on the invoice for the recipient to fill in.

          Constraints: `maxItems 4`.

          <Expandable title="customerPlaceholders properties">

            <ResponseField name="title" type="string">

              Title of the placeholder field.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="defaultPreset" type="boolean">

      Whether this preset is the default preset.

      **Read-only.**

    </ResponseField>

    <ResponseField name="displayValues" type="DisplayValues">

      Display value overrides for invoices created with this preset. When set, these values replace the corresponding values on the generated invoice document.

      <Expandable title="displayValues properties">

        <ResponseField name="titleOverride" type="string">

          Title to display on the generated invoice document, overriding the invoice's own `title`. If not set, the invoice's `title` is used.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="defaultDaysUntilDue" type="integer">

      Number of days after invoice creation until the invoice is due. Set to `0` for due on receipt.

      Default: `30`

    </ResponseField>

  </Tab>

</Tabs>
