---
title: "onPaymentLinkDeactivated"
description: "Triggered when a payment link is deactivated."
icon: "bolt"
---

Triggered when a payment link is deactivated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.paymentlinks.v1.payment_link_payment_link_deactivated` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Paylinks` |

## Examples

<CodeGroup>

  ```javascript onPaymentLinkDeactivated
  import { paymentLinks } from '@wix/get-paid';

  paymentLinks.onPaymentLinkDeactivated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPaymentLinkDeactivated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { paymentLinks } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        paymentLinks,
      },
  });

  wixClient.paymentLinks.onPaymentLinkDeactivated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
