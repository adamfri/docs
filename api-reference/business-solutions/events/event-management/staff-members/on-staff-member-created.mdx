---
title: "onStaffMemberCreated"
description: "Triggered when a staff member is created."
icon: "bolt"
---

Triggered when a staff member is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.staffmembers.v1.staff_member_created` |
| Package | `@wix/events` |

## Examples

<CodeGroup>

  ```javascript onStaffMemberCreated
  import { staffMembers } from '@wix/events';

  staffMembers.onStaffMemberCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onStaffMemberCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { staffMembers } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        staffMembers,
      },
  });

  wixClient.staffMembers.onStaffMemberCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
