---
title: "Search App Installation"
description: "Retrieves a list of up to 100 app installations that match a free-text search expression."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 app installations that match a free-text search expression.

    Search matches against the site URL, business name, business email, site owner email, instance ID, review title, and review description.
    Pass supported values to `fields` in the request to include those fields in the response.

    Search App Installation runs with these defaults, which you can override:
    - `paging.limit` is `50`

    The results include installations across all the apps in your developer account.

    To search a single field only, use [Query App Installations](https://dev.wix.com/docs/api-reference/app-management/app-installations/query-app-installations) with the appropriate filter.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `siteInfo.businessCategory.secondary` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `appId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `appVersion` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `firstInstallationDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `instanceId` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `planInfo.billingCycle.cycleDuration.count` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `planInfo.billingCycle.cycleDuration.unit` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `planInfo.billingCycle.cycleType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `planInfo.endDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `planInfo.freeTrialInfo.endDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `planInfo.freeTrialInfo.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `planInfo.planName` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `planInfo.planStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `review.id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `review.date` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `review.rating` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `siteInfo.accountId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.businessCategory.primary` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.businessEmail` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.businessName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.countryCode` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.editorType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `siteInfo.ownerEmail` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.premium` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `siteInfo.siteId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.siteUrl` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.APP_MARKET.READ_APP_INSTALLATIONS">Read App Installations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/app/installations/v1/app-installation/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search criteria including filter, sort, aggregations, and paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object for narrowing search results.
          For example, to return only upgraded app installations:
          `"filter": {"planInfo.planStatus": "UPGRADED"}`.

          For supported filters, see the table above.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Array of sort objects specifying result order.

          For example, to sort by creation date in descending order: [&#123;"fieldName":"sortField1"&#125;,&#123;"fieldName":"sortField2","direction":"DESC"&#125;]

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet.
          For example, use aggregations to allow site visitors to narrow down their
          search results by selecting specific categories, such as site info or plan info.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Unique, caller-defined aggregation name, returned in `aggregations.results`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation.

              **Supported values:**

              - `VALUE` — A value aggregation calculates metrics such as count for specific fields within a dataset,
              providing insights into the overall distribution and key statistics of those values.
              For example, use a value aggregation to get the amount of products for each price listed in the store.
              - `RANGE` — A range aggregation calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              For example, use a range aggregation to compare the number of reservations made for parties of 4 or less to the number of reservations made for parties with 5 or more.
              - `SCALAR` — A scalar aggregation calculates a single numerical value from a dataset,
              summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — A date histogram calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              For example, use a date histogram to find how many reviews were left on your app each week.
              - `NESTED` — A nested aggregation is applied within the results of another aggregation.
              Rather than aggregating directly on the primary dataset, first group data using one aggregation and then apply another aggregation within each group.
              It allows for more complex analyses where you can summarize data at different levels of detail or hierarchy.

              For example, to get the number of products that are in stock and out of stock for each price listed,
              first perform a value aggregation on `discountedPriceNumeric`, and a second value aggregation on `inStock`.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Path to the field to aggregate by in dot notation. For example `review.rating`.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined

              **Supported values:**

              - `OR` — At least 1 of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `siteInfo.businessCategory.primary`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search method to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database) or
          [ISO 8601 UTC offset format](https://en.wikipedia.org/wiki/ISO_8601#Time_offsets_from_UTC) for adjusting time fields in the specified filters and returned aggregation data. For example, `America/New_York`, `UTC`, or `+02:00`.

          Default: Time zone specified in the business [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/get-site-properties).

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor-based paging for result navigation. When requesting `cursor_paging.cursor`, `filter`, `sort`, or `search` can't be specified.

              Learn more about [cursor paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Additional fields to include in the response.

      Constraints: `maxItems 1`.

      **Supported values:**

      - `OWNER_EMAIL` — Include `siteInfo.ownerEmail` in each returned app installation.

      Note: Only returns if your app has the **Get Site Owner** permission.

    </ParamField>

    ## Response Object

    <ResponseField name="appInstallations" type="AppInstallation[]">

      Retrieved app installations.

      See the [App Installation Object](/api-reference/app-management/app-installations/app-installation-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata, including the cursor used to retrieve the next page of results.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data, returned when aggregations are requested.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Array of aggregation results, each containing the aggregation metadata and its calculated values for the specified search criteria.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match the one from the search request.

              **Supported values:**

              - `VALUE` — A value aggregation calculates metrics such as count for specific fields within a dataset,
              providing insights into the overall distribution and key statistics of those values.
              For example, use a value aggregation to get the amount of products for each price listed in the store.
              - `RANGE` — A range aggregation calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              For example, use a range aggregation to compare the number of reservations made for parties of 4 or less to the number of reservations made for parties with 5 or more.
              - `SCALAR` — A scalar aggregation calculates a single numerical value from a dataset,
              summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — A date histogram calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              For example, use a date histogram to find how many reviews were left on your app each week.
              - `NESTED` — A nested aggregation is applied within the results of another aggregation.
              Rather than aggregating directly on the primary dataset, first group data using one aggregation and then apply another aggregation within each group.
              It allows for more complex analyses where you can summarize data at different levels of detail or hierarchy.

              For example, to get the number of products that are in stock and out of stock for each price listed,
              first perform a value aggregation on `discountedPriceNumeric`, and a second value aggregation on `inStock`.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Path to the field to aggregate by in dot notation. For example `review.rating`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches app installations by free-text expression across site URL, business name, business email, site owner email, instance ID, and review title and description.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/app/installations/v1/app-installation/search' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "search": {
            "search": {
              "expression": "example.wixsite.com"
            },
            "cursorPaging": {
              "limit": 50
            }
          }
        }'
      ```

      ```json Response
      {
        "appInstallations": [
          {
            "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
            "instanceId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "appId": "2b4d6452-e1a7-4f28-9747-13d77225e6b6",
            "appVersion": "1.234",
            "status": "INSTALLED",
            "siteInfo": {
              "siteUrl": "https://example.wixsite.com",
              "businessName": "My Business",
              "businessEmail": "owner@example.com",
              "countryCode": "US"
            },
            "planInfo": {
              "planName": "Premium",
              "planStatus": "UPGRADED"
            },
            "firstInstallationDate": "2023-06-15T09:00:00.000Z",
            "createdDate": "2023-06-15T09:00:00.000Z",
            "updatedDate": "2024-01-10T14:32:00.000Z",
            "revision": "3"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 app installations that match a free-text search expression.

    Search matches against the site URL, business name, business email, site owner email, instance ID, review title, and review description.
    Pass supported values to `fields` in the request to include those fields in the response.

    Search App Installation runs with these defaults, which you can override:
    - `paging.limit` is `50`

    The results include installations across all the apps in your developer account.

    To search a single field only, use [Query App Installations](https://dev.wix.com/docs/api-reference/app-management/app-installations/query-app-installations) with the appropriate filter.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `siteInfo.businessCategory.secondary` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `appId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `appVersion` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `firstInstallationDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `instanceId` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `planInfo.billingCycle.cycleDuration.count` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `planInfo.billingCycle.cycleDuration.unit` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `planInfo.billingCycle.cycleType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `planInfo.endDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `planInfo.freeTrialInfo.endDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `planInfo.freeTrialInfo.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `planInfo.planName` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `planInfo.planStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `review._id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `review.date` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `review.rating` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `siteInfo.accountId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.businessCategory.primary` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.businessEmail` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.businessName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.countryCode` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.editorType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `siteInfo.ownerEmail` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.premium` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `siteInfo.siteId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `siteInfo.siteUrl` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.APP_MARKET.READ_APP_INSTALLATIONS">Read App Installations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { appInstallations } from '@wix/app-installations';

    appInstallations.searchAppInstallation(search, options): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search criteria including filter, sort, aggregations, and paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object for narrowing search results.
          For example, to return only upgraded app installations:
          `"filter": {"planInfo.planStatus": "UPGRADED"}`.

          For supported filters, see the table above.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Array of sort objects specifying result order.

          For example, to sort by creation date in descending order: [&#123;"fieldName":"sortField1"&#125;,&#123;"fieldName":"sortField2","direction":"DESC"&#125;]

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet.
          For example, use aggregations to allow site visitors to narrow down their
          search results by selecting specific categories, such as site info or plan info.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Unique, caller-defined aggregation name, returned in `aggregations.results`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation.

              **Supported values:**

              - `VALUE` — A value aggregation calculates metrics such as count for specific fields within a dataset,
              providing insights into the overall distribution and key statistics of those values.
              For example, use a value aggregation to get the amount of products for each price listed in the store.
              - `RANGE` — A range aggregation calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              For example, use a range aggregation to compare the number of reservations made for parties of 4 or less to the number of reservations made for parties with 5 or more.
              - `SCALAR` — A scalar aggregation calculates a single numerical value from a dataset,
              summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — A date histogram calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              For example, use a date histogram to find how many reviews were left on your app each week.
              - `NESTED` — A nested aggregation is applied within the results of another aggregation.
              Rather than aggregating directly on the primary dataset, first group data using one aggregation and then apply another aggregation within each group.
              It allows for more complex analyses where you can summarize data at different levels of detail or hierarchy.

              For example, to get the number of products that are in stock and out of stock for each price listed,
              first perform a value aggregation on `discountedPriceNumeric`, and a second value aggregation on `inStock`.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Path to the field to aggregate by in dot notation. For example `review.rating`.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined

              **Supported values:**

              - `OR` — At least 1 of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `siteInfo.businessCategory.primary`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search method to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database) or
          [ISO 8601 UTC offset format](https://en.wikipedia.org/wiki/ISO_8601#Time_offsets_from_UTC) for adjusting time fields in the specified filters and returned aggregation data. For example, `America/New_York`, `UTC`, or `+02:00`.

          Default: Time zone specified in the business [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/get-site-properties).

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor-based paging for result navigation. When requesting `cursor_paging.cursor`, `filter`, `sort`, or `search` can't be specified.

              Learn more about [cursor paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="SearchAppInstallationOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Additional fields to include in the response.

          Constraints: `maxItems 1`.

          **Supported values:**

          - `OWNER_EMAIL` — Include `siteInfo.ownerEmail` in each returned app installation.

          Note: Only returns if your app has the **Get Site Owner** permission.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="appInstallations" type="AppInstallation[]">

      Retrieved app installations.

      See the [App Installation Object](/api-reference/app-management/app-installations/app-installation-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata, including the cursor used to retrieve the next page of results.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data, returned when aggregations are requested.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Array of aggregation results, each containing the aggregation metadata and its calculated values for the specified search criteria.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match the one from the search request.

              **Supported values:**

              - `VALUE` — A value aggregation calculates metrics such as count for specific fields within a dataset,
              providing insights into the overall distribution and key statistics of those values.
              For example, use a value aggregation to get the amount of products for each price listed in the store.
              - `RANGE` — A range aggregation calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              For example, use a range aggregation to compare the number of reservations made for parties of 4 or less to the number of reservations made for parties with 5 or more.
              - `SCALAR` — A scalar aggregation calculates a single numerical value from a dataset,
              summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — A date histogram calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              For example, use a date histogram to find how many reviews were left on your app each week.
              - `NESTED` — A nested aggregation is applied within the results of another aggregation.
              Rather than aggregating directly on the primary dataset, first group data using one aggregation and then apply another aggregation within each group.
              It allows for more complex analyses where you can summarize data at different levels of detail or hierarchy.

              For example, to get the number of products that are in stock and out of stock for each price listed,
              first perform a value aggregation on `discountedPriceNumeric`, and a second value aggregation on `inStock`.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Path to the field to aggregate by in dot notation. For example `review.rating`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-app-installation" id="ex-sdk-search-app-installation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-app-installation" id="ex-sdk-search-app-installation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-search-app-installation" id="ex-sdk-search-app-installation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-app-installation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-app-installation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">searchAppInstallation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchAppInstallation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchAppInstallation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-app-installation-0"><span className="wix-picker-name">searchAppInstallation</span></label>
    <label htmlFor="ex-sdk-search-app-installation-1"><span className="wix-picker-name">searchAppInstallation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-search-app-installation-2"><span className="wix-picker-name">searchAppInstallation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript searchAppInstallation
    import { appInstallations } from '@wix/app-installations';

    async function searchAppInstallation(search,options) {
      const response = await appInstallations.searchAppInstallation(search,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchAppInstallation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { appInstallations } from '@wix/app-installations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { appInstallations },
      // Include the auth strategy and host as relevant
    });

    async function searchAppInstallation(search,options) {
      const response = await myWixClient.appInstallations.searchAppInstallation(search,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchAppInstallation (with elevated permissions)
    import { appInstallations } from '@wix/app-installations';
    import { auth } from '@wix/essentials';

    async function mySearchAppInstallationMethod(search,options) {
      const elevatedSearchAppInstallation = auth.elevate(appInstallations.searchAppInstallation);
      const response = await elevatedSearchAppInstallation(search,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
