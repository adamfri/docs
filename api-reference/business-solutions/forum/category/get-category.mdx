---
title: "Get Category"
description: "Returns a single category by ID."
icon: "download"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Returns a single category by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORUM.READ-FORUM">Read Forums</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/forum/v1/categories/{categoryId}
    ```

    ## Path Params

    <ParamField path="categoryId" type="string" required>

      ID of the retrieved category.

    </ParamField>

    ## Query Params

    <ParamField query="extraFields" type="string[]">

      Array of extra category fields to be added in the response. `UNKNOWN` must not be passed.

      **Supported values:**

      - `URL` — Include the category's URL.

    </ParamField>

    ## Response Object

    <ResponseField name="category" type="Category">

      Retrieved category for the provided category ID.

      See the [Category Object](/api-reference/business-solutions/forum/category/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET https://www.wixapis.com/forum/v1/categories/5cacd5fe04976c0015f35362
          -H 'Content-Type: application/json;charset=UTF-8'
          -H 'Authorization: <ACCESS_TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "category": {
              "id": "5cacd5fe04976c0015f35362",
              "parentId": "001005fe04976c0015f35362",
              "name": "Discussion corner",
              "headerTitle": "Discussion corner",
              "description": "Share stories, ideas, pictures and more!",
              "headerType": "IMAGE",
              "headerImage": {
                "id": "image.jpg",
                "url": "https://static.wixstatic.com/media/image.jpg",
                "height": 760,
                "width": 2100
              },
              "rank": 0,
              "postCount": 2,
              "postViewCount": 17,
              "slug": "discussion-corner",
              "noMemberPosting": true,
              "categoryAccess": "PUBLIC",
              "createdDate": "2019-04-09T17:27:26.171Z",
              "updatedDate": "2019-04-09T17:27:26.171Z",
              "postTypes": [
                "DISCUSSION",
                "QUESTION"
              ],
              "postInteraction": "REACTION",
              "mainPostReaction": {
                "type": "LOVE"
              },
              "additionalPostReactions": [
                {
                  "type": "THUMBS_UP"
                }
              ],
              "commentInteraction": "REACTION",
              "mainCommentReaction": {
                "type": "THUMBS_UP"
              },
              "additionalCommentReactions": [
                {
                  "type": "THUMBS_UP"
                }
              ],
              "url": {
                "base": "https://wix.com",
                "path": "/forum/discussion-corner"
              }
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Returns a single category by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORUM.READ-FORUM">Read Forums</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/forum';

    categories.getCategory(categoryId, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="categoryId" type="string" required>

      ID of the retrieved category.

    </ParamField>

    <ParamField body="options" type="GetCategoryOptions">

      Options specifying which fields to return.

      <Expandable title="options properties">

        <ParamField body="extraFields" type="string[]">

          Array of extra category fields to be added in the response. `UNKNOWN` must not be passed.

          **Supported values:**

          - `URL` — Include the category's URL.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    See the [Category Object](/api-reference/business-solutions/forum/category/category-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-category" id="ex-sdk-get-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-category" id="ex-sdk-get-category-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-category" id="ex-sdk-get-category-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getCategory</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCategory (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-category-0"><span className="wix-picker-name">getCategory</span></label>
    <label htmlFor="ex-sdk-get-category-1"><span className="wix-picker-name">getCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-category-2"><span className="wix-picker-name">getCategory (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getCategory
    import { categories } from '@wix/forum';

    async function getCategory(categoryId,options) {
      const response = await categories.getCategory(categoryId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/forum';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function getCategory(categoryId,options) {
      const response = await myWixClient.categories.getCategory(categoryId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCategory (with elevated permissions)
    import { categories } from '@wix/forum';
    import { auth } from '@wix/essentials';

    async function myGetCategoryMethod(categoryId,options) {
      const elevatedGetCategory = auth.elevate(categories.getCategory);
      const response = await elevatedGetCategory(categoryId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
