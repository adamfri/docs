---
title: "Schedule Time Frame Object"
description: "Information about the start and end dates of the first and last events that belong to a schedule and whether any events follow an unlimited repetition pattern."
icon: "cube"
---

Information about the start and end dates of the first and last
[events](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/introduction)
that belong to a [schedule](https://dev.wix.com/docs/rest/business-management/calendar/schedules-v3/introduction)
and whether any events follow an unlimited repetition pattern.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      ID of the [schedule](https://dev.wix.com/docs/rest/business-management/calendar/schedules-v3/schedule-object)
      to which the time frame belongs.

      **Read-only.**

    </ResponseField>

    <ResponseField name="status" type="string">

      Information about the presence of recurring events within the schedule.

      Supported values:
      + `NONE`: No [events](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object) are scheduled within the schedule.
      + `FINITE`: All recurring events within the schedule have a defined end date, or there are no recurring events.
      + `INFINITE`: At least one recurring event within the schedule has no end date, meaning it follows an unlimited repetition pattern.

      **Read-only.**

      **Supported values:**

      - `NONE` — No [events](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object) that belong to the schedule.
      - `FINITE` — There are no recurring events or all recurring events belonging to the schedule have a defined end date.
      - `INFINITE` — At least one recurring event belonging to the schedule has an unlimited repetition pattern (no end date).

    </ResponseField>

    <ResponseField name="firstEventStartDate" type="ZonedDate">

      Start date of the first [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      within the schedule. Identical to `event.start.localDate`. Available only if
      `status` is set to `FINITE` or `INFINITE`.

      **Read-only.**

      <Expandable title="firstEventStartDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="lastEventEndDate" type="ZonedDate">

      End date of the last [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      belonging to the schedule. Identical to `event.end.localDate`. Available only if
      `status` is set to `FINITE`.

      **Read-only.**

      <Expandable title="lastEventEndDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="adjustedFirstEventStartDate" type="ZonedDate">

      Start date of the first [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      belonging to the schedule, adjusted to `timeZone` of the
      [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/properties-object)
      or the `timeZone` specified in the request.
      Identical to `event.adjustedStart.localDate`.

      **Read-only.**

      <Expandable title="adjustedFirstEventStartDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="adjustedLastEventEndDate" type="ZonedDate">

      End date of the last [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      belonging to the schedule, adjusted to `timeZone` of the
      [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/properties-object)
      or the `timeZone` specified in the request.
      Identical to `event.adjustedEnd.localDate`.

      **Read-only.**

      <Expandable title="adjustedLastEventEndDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the schedule time frame is updated.

      **Read-only.**

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "id": "05279386-2859-49d6-9727-0096ad74e014",
        "status": "FINITE",
        "firstEventStartDate": {
          "localDate": "2024-09-24T16:30:00",
          "timeZone": "Europe/Dublin",
          "utcDate": "2024-09-24T15:30:00Z"
        },
        "lastEventEndDate": {
          "localDate": "2024-11-27T17:00:00",
          "timeZone": "Europe/Dublin",
          "utcDate": "2024-11-27T17:00:00Z"
        },
        "adjustedFirstEventStartDate": {
          "localDate": "2024-09-24T16:30:00",
          "timeZone": "Europe/Dublin"
        },
        "adjustedLastEventEndDate": {
          "localDate": "2024-11-27T17:00:00",
          "timeZone": "Europe/Dublin"
        },
        "revision": "15"
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      ID of the [schedule](https://dev.wix.com/docs/rest/business-management/calendar/schedules-v3/schedule-object)
      to which the time frame belongs.

      **Read-only.**

    </ResponseField>

    <ResponseField name="status" type="string">

      Information about the presence of recurring events within the schedule.

      Supported values:
      + `NONE`: No [events](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object) are scheduled within the schedule.
      + `FINITE`: All recurring events within the schedule have a defined end date, or there are no recurring events.
      + `INFINITE`: At least one recurring event within the schedule has no end date, meaning it follows an unlimited repetition pattern.

      **Read-only.**

      **Supported values:**

      - `NONE` — No [events](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object) that belong to the schedule.
      - `FINITE` — There are no recurring events or all recurring events belonging to the schedule have a defined end date.
      - `INFINITE` — At least one recurring event belonging to the schedule has an unlimited repetition pattern (no end date).

    </ResponseField>

    <ResponseField name="firstEventStartDate" type="ZonedDate">

      Start date of the first [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      within the schedule. Identical to `event.start.localDate`. Available only if
      `status` is set to `FINITE` or `INFINITE`.

      **Read-only.**

      <Expandable title="firstEventStartDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="lastEventEndDate" type="ZonedDate">

      End date of the last [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      belonging to the schedule. Identical to `event.end.localDate`. Available only if
      `status` is set to `FINITE`.

      **Read-only.**

      <Expandable title="lastEventEndDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="adjustedFirstEventStartDate" type="ZonedDate">

      Start date of the first [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      belonging to the schedule, adjusted to `timeZone` of the
      [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/properties-object)
      or the `timeZone` specified in the request.
      Identical to `event.adjustedStart.localDate`.

      **Read-only.**

      <Expandable title="adjustedFirstEventStartDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="adjustedLastEventEndDate" type="ZonedDate">

      End date of the last [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
      belonging to the schedule, adjusted to `timeZone` of the
      [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/properties-object)
      or the `timeZone` specified in the request.
      Identical to `event.adjustedEnd.localDate`.

      **Read-only.**

      <Expandable title="adjustedLastEventEndDate properties">

        <ResponseField name="localDate" type="string">

          Local date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Wix Calendar APIs ignore seconds.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Time zone in
          [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database).
          For example, `America/New_York`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="utcDate" type="datetime">

          UTC date time in [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601).
          For example, `2024-01-30T13:30:00`.
          Not available for adjusted date fields.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the schedule time frame is updated.

      **Read-only.**

    </ResponseField>

  </Tab>

</Tabs>
