---
title: "Setup"
description: "@packagemetadata:@wix/events To use the WixEventsV2 API, install the @wix/events package using npm or Yarn: or Then import { wixEventsV2 } from @wix/events:"
icon: "file-lines"
---

<Note>This article applies to the JavaScript SDK only.</Note>

@package_metadata:@wix/events

To use the WixEventsV2 API, install the `@wix/events` package using npm or Yarn:
  ```bash
npm install @wix/events
```

or

```bash
yarn add @wix/events
```

Then import `{ wixEventsV2 }` from `@wix/events`:

```js
import { wixEventsV2 } from '@wix/events'
```
