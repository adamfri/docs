---
title: "onOrderEndDatePostponed"
description: "Triggered when an order's endDate is postponed."
icon: "bolt"
---

Triggered when an order's `endDate` is postponed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.v2.order_end_date_postponed` |
| Package | `@wix/pricing-plans` |
| Permissions | `Manage Orders`, `Read Orders`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onOrderEndDatePostponed
  import { orders } from '@wix/pricing-plans';

  orders.onOrderEndDatePostponed((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderEndDatePostponed (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderEndDatePostponed((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
