---
title: "onInventoryItemUpdatedWithReason"
description: "Triggered when an inventory item is updated."
icon: "bolt"
---

Triggered when an inventory item is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.stores.catalog.v3.inventory_item_updated_with_reason` |
| Package | `@wix/stores` |
| Permissions | `Read inventory in v3 catalog`, `Read v3 catalog`, `Manage Products`, `Read Products`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onInventoryItemUpdatedWithReason: Wix-hosted
  import { inventoryItemsV3 } from '@wix/stores';

  inventoryItemsV3.onInventoryItemUpdatedWithReason((event) => {
      // handle your event here
  });
  ```

  ```javascript onInventoryItemUpdatedWithReason: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { inventoryItemsV3 } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        inventoryItemsV3,
      },
  });

  wixClient.inventoryItemsV3.onInventoryItemUpdatedWithReason((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
