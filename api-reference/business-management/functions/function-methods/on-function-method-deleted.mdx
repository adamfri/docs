---
title: "onFunctionMethodDeleted"
description: "Triggered when a function method is deleted."
icon: "bolt"
---

Triggered when a function method is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.functions.v1.function_method_deleted` |
| Package | `@wix/functions` |
| Permissions | `Manage Stores`, `Manage Functions`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onFunctionMethodDeleted
  import { functionMethods } from '@wix/functions';

  functionMethods.onFunctionMethodDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onFunctionMethodDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { functionMethods } from '@wix/functions';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        functionMethods,
      },
  });

  wixClient.functionMethods.onFunctionMethodDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
