---
title: "onCouponCreated"
description: "Triggered when a coupon is created."
icon: "bolt"
---

Triggered when a coupon is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecommerce.coupons.v2.coupon_created` |
| Package | `@wix/marketing` |
| Permissions | `Manage Stores - all permissions`, `Manage Coupons` |

## Examples

<CodeGroup>

  ```javascript onCouponCreated
  import { coupons } from '@wix/marketing';

  coupons.onCouponCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCouponCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { coupons } from '@wix/marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        coupons,
      },
  });

  wixClient.coupons.onCouponCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
