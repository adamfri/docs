---
title: "onCategoryCreated"
description: "Triggered when a category is created."
icon: "bolt"
---

Triggered when a category is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.faq.category.v2.category_created` |
| Package | `@wix/faq` |
| Permissions | `Manage FAQ`, `Read FAQ` |

## Examples

<CodeGroup>

  ```javascript onCategoryCreated
  import { category } from '@wix/faq';

  category.onCategoryCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { category } from '@wix/faq';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        category,
      },
  });

  wixClient.category.onCategoryCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
