---
title: "Delete Plan Marketing Activity"
description: "Deletes a marketing activity from the plan."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a marketing activity from the plan.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-plan/plan-marketing-activity
    ```

    ## Query Params

    <ParamField query="id" type="string" required>

      ID of the marketing activity to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    Returns `DeletePlanMarketingActivityResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Remove a single marketing activity from the plan, by its ID.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-plan/plan-marketing-activity?id=b3f9d2e1-7a4c-4d68-9f02-1c5e8a6b4d30' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a marketing activity from the plan.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingPlan } from '@wix/promote-growth-tools-marketing-plan';

    marketingPlan.deletePlanMarketingActivity(_id): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the marketing activity to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeletePlanMarketingActivityResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-plan-marketing-activity" id="ex-sdk-delete-plan-marketing-activity-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-plan-marketing-activity" id="ex-sdk-delete-plan-marketing-activity-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-plan-marketing-activity-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-plan-marketing-activity-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a marketing activity</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deletePlanMarketingActivity (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-plan-marketing-activity-0"><span className="wix-picker-name">Delete a marketing activity</span></label>
    <label htmlFor="ex-sdk-delete-plan-marketing-activity-1"><span className="wix-picker-name">deletePlanMarketingActivity (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a marketing activity
    import { marketingPlan } from "@wix/promote-growth-tools-marketing-plan";

    const id = "b3f9d2e1-7a4c-4d68-9f02-1c5e8a6b4d30";

    async function deletePlanMarketingActivity() {
      const response = await marketingPlan.deletePlanMarketingActivity(id);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deletePlanMarketingActivity (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingPlan } from '@wix/promote-growth-tools-marketing-plan';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingPlan },
      // Include the auth strategy and host as relevant
    });

    async function deletePlanMarketingActivity(_id) {
      const response = await myWixClient.marketingPlan.deletePlanMarketingActivity(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
