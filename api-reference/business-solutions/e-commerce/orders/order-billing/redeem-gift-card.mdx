---
title: "Redeem Gift Card"
description: "Redeems a gift card as payment for an order by applying the gift card balance to the order total."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Redeems a gift card as payment for an order by applying the gift card balance to the order total.

    Creates a gift card payment transaction and deducts the specified amount from the gift card balance.
    The redeemed amount reduces the order's outstanding balance.

    <Note>

    The following prerequisites apply:
    + Gift card must be active and not expired.
    + Gift card must have sufficient balance.
    + Order must not be cancelled or fully paid.
    + Gift card currency must match order currency.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/ecom/v1/order-billing/redeem-gift-card
    ```

    ## Body Params

    <ParamField body="orderId" type="string" required>

      Order ID to apply the gift card payment to.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="giftCardCode" type="string" required>

      Gift card redemption code.

      The gift card must be active, not expired, and have sufficient balance.

      Constraints: `minLength 8`, `maxLength 20`.

    </ParamField>

    <ParamField body="amount" type="Price" required>

      Amount to redeem from the gift card balance.

      Cannot exceed the gift card's available balance or the order's outstanding amount.

      <Expandable title="amount properties">

        <ParamField body="amount" type="string">

          Amount.

          Constraints: `>= 0`, `<= 1000000000000000`.

        </ParamField>

        <ParamField body="formattedAmount" type="string">

          Amount formatted with currency symbol.

          **Read-only.**

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="currency" type="string" required>

      Currency code, in [ISO-4217](https://en.wikipedia.org/wiki/ISO_4217#List_of_ISO_4217_currency_codes) format.

      Constraints: `format CURRENCY`.

    </ParamField>

    ## Response Object

    <ResponseField name="orderTransactions" type="OrderTransactions">

      Updated order transaction history including the gift card payment.

      <Expandable title="orderTransactions properties">

        <ResponseField name="orderId" type="string">

          Order ID.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="payments" type="Payment[]">

          Record of payments made to the merchant.

          Constraints: `maxItems 100`.

          <Expandable title="payments properties">

            <ResponseField name="id" type="string">

              Payment ID.

              **Read-only.**

            </ResponseField>

            <ResponseField name="createdDate" type="datetime">

              Date and time the payment was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format. Defaults to current time when not provided.

            </ResponseField>

            <ResponseField name="updatedDate" type="datetime">

              Date and time the payment was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

              **Read-only.**

            </ResponseField>

            <ResponseField name="amount" type="Price">

              Payment amount.

              _Nested structure omitted for brevity — see the `Price` object._

            </ResponseField>

            <ResponseField name="refundDisabled" type="boolean">

              Whether refunds for this payment are disabled.
              + `true`: This payment is not refundable.
              + `false`: This payment may be refunded. However, this ultimately depends on the payment provider.

            </ResponseField>

            <ResponseField name="cashRounding" type="CashRoundingDetails">

              Details about cash rounding applied to this payment, when relevant.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `CashRoundingDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="refunds" type="Refund[]">

          Record of refunds made to the buyer.

          Constraints: `maxItems 300`.

          <Expandable title="refunds properties">

            <ResponseField name="id" type="string">

              Refund ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="transactions" type="RefundTransaction[]">

              List of transactions.

              Constraints: `minItems 0`, `maxItems 50`.

              _Nested structure omitted for brevity — see the `RefundTransaction[]` object._

            </ResponseField>

            <ResponseField name="details" type="RefundDetails">

              Refund business details.

              _Nested structure omitted for brevity — see the `RefundDetails` object._

            </ResponseField>

            <ResponseField name="createdDate" type="datetime">

              Date and time the refund was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format. Defaults to current time when not provided.

              **Read-only.**

            </ResponseField>

            <ResponseField name="summary" type="AggregatedRefundSummary">

              Aggregated refund summary.

              **Read-only.**

              _Nested structure omitted for brevity — see the `AggregatedRefundSummary` object._

            </ResponseField>

            <ResponseField name="requestingServiceAppId" type="string">

              ID of the app that initiated this refund.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payment" type="Payment">

      Created gift card payment transaction with the redeemed amount.

      <Expandable title="payment properties">

        <ResponseField name="id" type="string">

          Payment ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="createdDate" type="datetime">

          Date and time the payment was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format. Defaults to current time when not provided.

        </ResponseField>

        <ResponseField name="updatedDate" type="datetime">

          Date and time the payment was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

          **Read-only.**

        </ResponseField>

        <ResponseField name="amount" type="Price">

          Payment amount.

          <Expandable title="amount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="refundDisabled" type="boolean">

          Whether refunds for this payment are disabled.
          + `true`: This payment is not refundable.
          + `false`: This payment may be refunded. However, this ultimately depends on the payment provider.

        </ResponseField>

        <ResponseField name="cashRounding" type="CashRoundingDetails">

          Details about cash rounding applied to this payment, when relevant.

          **Developer Preview.**

          <Expandable title="cashRounding properties">

            <ResponseField name="unroundedAmount" type="Price">

              Payment amount before cash rounding was applied.
              When provided, balance and status calculations use this value instead of `amount`
              to prevent false overpayment or underpayment due to rounding.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ResponseField>

            <ResponseField name="roundingAdjustment" type="CashRounding">

              The difference between 'amount' and 'unroundedAmount' (amount minus unroundedAmount).
              A positive value indicates the price was rounded up; a negative value indicates a round-down.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `CashRounding` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="paymentDetails" type="paymentDetails">

          paymentDetails

          <Expandable title="paymentDetails properties">

            <ResponseField name="regularPaymentDetails" type="RegularPaymentDetails">

              Regular payment details.

              _Nested structure omitted for brevity — see the `RegularPaymentDetails` object._

            </ResponseField>

            <ResponseField name="giftcardPaymentDetails" type="GiftCardPaymentDetails">

              Gift card payment details.

              _Nested structure omitted for brevity — see the `GiftCardPaymentDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_ALREADY_APPLIED` | Gift card was already applied to the order. |
    | `428` | `FAILED_PRECONDITION` | `CURRENCY_MISMATCH` | Currency doesn't match the order's currency. |
    | `404` | `NOT_FOUND` | `GIFT_CARD_NOT_FOUND` | Couldn't find the gift card. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_EXPIRED` | Gift card is expired. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_DISABLED` | Gift card is disabled. |
    | `428` | `FAILED_PRECONDITION` | `INSUFFICIENT_FUNDS` | Gift card has insufficient funds. |
    | `400` | `INVALID_ARGUMENT` | `CURRENCY_NOT_SUPPORTED` | Currency isn't supported. |
    | `409` | `ALREADY_EXISTS` | `ALREADY_REDEEMED` | Gift card was already redeemed. |
    | `404` | `NOT_FOUND` | `ORDER_NOT_FOUND` | Couldn't find the order. |
    | `428` | `FAILED_PRECONDITION` | `ORDER_CANCELED` | Order was already canceled. |
    | `428` | `FAILED_PRECONDITION` | `ORDER_PAID` | Order was already paid. |
    | `428` | `FAILED_PRECONDITION` | `MAX_GIFT_CARDS_EXCEEDED` | Maximum number of gift cards per order exceeded. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Redeem a gift card as payment towards an order's balance.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/ecom/v1/order-billing/redeem-gift-card' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
        	"orderId": "75a9d7dd-e00b-4b57-a837-2de83d837e44",
        	"giftCardCode": "1ZAVFLEJA557WGDT",
        	"amount": {
        		"amount": "20"
        	},
        	"currency": "USD"
      	}'
      ```

      ```json Response
      {
        "orderTransactions": {
          "orderId": "75a9d7dd-e00b-4b57-a837-2de83d837e44",
          "payments": [
            {
              "id": "46ae76bd-2f90-4faf-b6bb-e4f7455232ce",
              "giftcardPaymentDetails": {
                "giftCardPaymentId": "1d0ec4a4-8d2c-4d89-8d52-f9373c1a2bab",
                "appId": "d80111c5-a0f4-47a8-b63a-65b54d774a27",
                "voided": false
              },
              "createdDate": "2025-09-22T08:45:34.585Z",
              "updatedDate": "2025-09-22T08:45:34.585Z",
              "amount": {
                "amount": "20",
                "formattedAmount": "$20.00"
              },
              "refundDisabled": false
            }
          ],
          "refunds": []
        },
        "payment": {
          "id": "46ae76bd-2f90-4faf-b6bb-e4f7455232ce",
          "giftcardPaymentDetails": {
            "giftCardPaymentId": "1d0ec4a4-8d2c-4d89-8d52-f9373c1a2bab",
            "appId": "d80111c5-a0f4-47a8-b63a-65b54d774a27",
            "voided": false
          },
          "createdDate": "2025-09-22T08:45:34.585Z",
          "updatedDate": "2025-09-22T08:45:34.585Z",
          "amount": {
            "amount": "20",
            "formattedAmount": "$20.00"
          },
          "refundDisabled": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Redeems a gift card as payment for an order by applying the gift card balance to the order total.

    Creates a gift card payment transaction and deducts the specified amount from the gift card balance.
    The redeemed amount reduces the order's outstanding balance.

    <Note>

    The following prerequisites apply:
    + Gift card must be active and not expired.
    + Gift card must have sufficient balance.
    + Order must not be cancelled or fully paid.
    + Gift card currency must match order currency.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { orderBilling } from '@wix/ecom';

    orderBilling.redeemGiftCard(orderId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="orderId" type="string" required>

      Order ID to apply the gift card payment to.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="RedeemGiftCardOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="giftCardCode" type="string" required>

          Gift card redemption code.

          The gift card must be active, not expired, and have sufficient balance.

          Constraints: `minLength 8`, `maxLength 20`.

        </ParamField>

        <ParamField body="amount" type="Price" required>

          Amount to redeem from the gift card balance.

          Cannot exceed the gift card's available balance or the order's outstanding amount.

          <Expandable title="amount properties">

            <ParamField body="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ParamField>

            <ParamField body="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="currency" type="string" required>

          Currency code, in [ISO-4217](https://en.wikipedia.org/wiki/ISO_4217#List_of_ISO_4217_currency_codes) format.

          Constraints: `format CURRENCY`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="orderTransactions" type="OrderTransactions">

      Updated order transaction history including the gift card payment.

      <Expandable title="orderTransactions properties">

        <ResponseField name="orderId" type="string">

          Order ID.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="payments" type="Payment[]">

          Record of payments made to the merchant.

          Constraints: `maxItems 100`.

          <Expandable title="payments properties">

            <ResponseField name="_id" type="string">

              Payment ID.

              **Read-only.**

            </ResponseField>

            <ResponseField name="_createdDate" type="datetime">

              Date and time the payment was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format. Defaults to current time when not provided.

            </ResponseField>

            <ResponseField name="_updatedDate" type="datetime">

              Date and time the payment was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

              **Read-only.**

            </ResponseField>

            <ResponseField name="amount" type="Price">

              Payment amount.

              _Nested structure omitted for brevity — see the `Price` object._

            </ResponseField>

            <ResponseField name="refundDisabled" type="boolean">

              Whether refunds for this payment are disabled.
              + `true`: This payment is not refundable.
              + `false`: This payment may be refunded. However, this ultimately depends on the payment provider.

            </ResponseField>

            <ResponseField name="cashRounding" type="CashRoundingDetails">

              Details about cash rounding applied to this payment, when relevant.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `CashRoundingDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="refunds" type="Refund[]">

          Record of refunds made to the buyer.

          Constraints: `maxItems 300`.

          <Expandable title="refunds properties">

            <ResponseField name="_id" type="string">

              Refund ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="transactions" type="RefundTransaction[]">

              List of transactions.

              Constraints: `minItems 0`, `maxItems 50`.

              _Nested structure omitted for brevity — see the `RefundTransaction[]` object._

            </ResponseField>

            <ResponseField name="details" type="RefundDetails">

              Refund business details.

              _Nested structure omitted for brevity — see the `RefundDetails` object._

            </ResponseField>

            <ResponseField name="_createdDate" type="datetime">

              Date and time the refund was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format. Defaults to current time when not provided.

              **Read-only.**

            </ResponseField>

            <ResponseField name="summary" type="AggregatedRefundSummary">

              Aggregated refund summary.

              **Read-only.**

              _Nested structure omitted for brevity — see the `AggregatedRefundSummary` object._

            </ResponseField>

            <ResponseField name="requestingServiceAppId" type="string">

              ID of the app that initiated this refund.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payment" type="Payment">

      Created gift card payment transaction with the redeemed amount.

      <Expandable title="payment properties">

        <ResponseField name="_id" type="string">

          Payment ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="_createdDate" type="datetime">

          Date and time the payment was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format. Defaults to current time when not provided.

        </ResponseField>

        <ResponseField name="_updatedDate" type="datetime">

          Date and time the payment was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

          **Read-only.**

        </ResponseField>

        <ResponseField name="amount" type="Price">

          Payment amount.

          <Expandable title="amount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="refundDisabled" type="boolean">

          Whether refunds for this payment are disabled.
          + `true`: This payment is not refundable.
          + `false`: This payment may be refunded. However, this ultimately depends on the payment provider.

        </ResponseField>

        <ResponseField name="cashRounding" type="CashRoundingDetails">

          Details about cash rounding applied to this payment, when relevant.

          **Developer Preview.**

          <Expandable title="cashRounding properties">

            <ResponseField name="unroundedAmount" type="Price">

              Payment amount before cash rounding was applied.
              When provided, balance and status calculations use this value instead of `amount`
              to prevent false overpayment or underpayment due to rounding.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `Price` object._

            </ResponseField>

            <ResponseField name="roundingAdjustment" type="CashRounding">

              The difference between 'amount' and 'unroundedAmount' (amount minus unroundedAmount).
              A positive value indicates the price was rounded up; a negative value indicates a round-down.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `CashRounding` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="paymentDetails" type="paymentDetails">

          paymentDetails

          <Expandable title="paymentDetails properties">

            <ResponseField name="regularPaymentDetails" type="RegularPaymentDetails">

              Regular payment details.

              _Nested structure omitted for brevity — see the `RegularPaymentDetails` object._

            </ResponseField>

            <ResponseField name="giftcardPaymentDetails" type="GiftCardPaymentDetails">

              Gift card payment details.

              _Nested structure omitted for brevity — see the `GiftCardPaymentDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_ALREADY_APPLIED` | Gift card was already applied to the order. |
    | `428` | `FAILED_PRECONDITION` | `CURRENCY_MISMATCH` | Currency doesn't match the order's currency. |
    | `404` | `NOT_FOUND` | `GIFT_CARD_NOT_FOUND` | Couldn't find the gift card. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_EXPIRED` | Gift card is expired. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_DISABLED` | Gift card is disabled. |
    | `428` | `FAILED_PRECONDITION` | `INSUFFICIENT_FUNDS` | Gift card has insufficient funds. |
    | `400` | `INVALID_ARGUMENT` | `CURRENCY_NOT_SUPPORTED` | Currency isn't supported. |
    | `409` | `ALREADY_EXISTS` | `ALREADY_REDEEMED` | Gift card was already redeemed. |
    | `404` | `NOT_FOUND` | `ORDER_NOT_FOUND` | Couldn't find the order. |
    | `428` | `FAILED_PRECONDITION` | `ORDER_CANCELED` | Order was already canceled. |
    | `428` | `FAILED_PRECONDITION` | `ORDER_PAID` | Order was already paid. |
    | `428` | `FAILED_PRECONDITION` | `MAX_GIFT_CARDS_EXCEEDED` | Maximum number of gift cards per order exceeded. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-redeem-gift-card" id="ex-sdk-redeem-gift-card-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-redeem-gift-card" id="ex-sdk-redeem-gift-card-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-redeem-gift-card-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-redeem-gift-card-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Redeem Gift Card</span><span className="wix-picker-desc">Redeem a gift card as payment toward an order's balance</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">redeemGiftCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-redeem-gift-card-0"><span className="wix-picker-name">Redeem Gift Card</span><span className="wix-picker-desc">Redeem a gift card as payment toward an order's balance</span></label>
    <label htmlFor="ex-sdk-redeem-gift-card-1"><span className="wix-picker-name">redeemGiftCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Redeem Gift Card
    import { orderBilling } from "@wix/ecom";

    async function redeemGiftCard() {
      const response = await orderBilling.redeemGiftCard(
        "75a9d7dd-e00b-4b57-a837-2de83d837e44",
        {
          giftCardCode: "1ZAVFLEJA557WGDT",
          amount: { amount: "20" },
          currency: "USD",
        },
      );
      return response;
    }

    /* Promise resolves to:
     * {
     *   "payment": {
     *     "giftcardPaymentDetails": {
     *       "giftCardPaymentId": "1d0ec4a4-8d2c-4d89-8d52-f9373c1a2bab",
     *       "appId": "d80111c5-a0f4-47a8-b63a-65b54d774a27",
     *       "voided": false
     *     },
     *     "refundDisabled": false
     *   },
     *   "orderTransactions": {
     *     "orderId": "75a9d7dd-e00b-4b57-a837-2de83d837e44"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript redeemGiftCard (self-hosted)
    import { createClient } from '@wix/sdk';
    import { orderBilling } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { orderBilling },
      // Include the auth strategy and host as relevant
    });

    async function redeemGiftCard(orderId,options) {
      const response = await myWixClient.orderBilling.redeemGiftCard(orderId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
