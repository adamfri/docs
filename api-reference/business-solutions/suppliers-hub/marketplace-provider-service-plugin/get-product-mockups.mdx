---
title: "Get Product Mockups"
description: "Retrieves ready-made product mockup image URLs for one or more products."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves ready-made product mockup image URLs for one or more products.

    Wix calls this method when a site owner views product mockups in the Suppliers Hub Marketplace. Wix calls it only when your implementation enables it by setting `toggleGetProductMockupsEnabled` to `true` in the marketplace provider config.

    Return your existing, ready-made mockup images for each requested product. Unlike [Bulk Generate Mockups](https://dev.wix.com/docs/api-reference/business-solutions/suppliers-hub/marketplace-provider-service-plugin/bulk-generate-mockups), this method doesn't generate new images or apply a site owner's logo or custom image.

    Wix uses the response to display mockup images for the requested products.

    ## Endpoint

    ```http
    POST /v2/get-product-mockups
    ```

    ## Body Params

    <ParamField body="providerProductIds" type="string[]">

      Product IDs on the provider's platform to retrieve mockups for.

      Constraints: `minLength 1`, `maxLength 36`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="productMockups" type="ProductMockups[]">

      Mockups for the requested products.

      Constraints: `maxItems 100`.

      <Expandable title="productMockups properties">

        <ResponseField name="providerProductId" type="string">

          Product ID on the provider's platform.

          Constraints: `minLength 1`, `maxLength 36`.

        </ResponseField>

        <ResponseField name="mockupImageUrls" type="string[]">

          Ready-made mockup image URLs for the product.

          Constraints: `format WEB_URL`, `maxItems 100`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves ready-made product mockup image URLs for one or more products

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://provider.example.com/v2/get-product-mockups' \
      -H 'user-agent: Wix' \
      -H 'accept-encoding: gzip, deflate' \
      -H 'content-type: text/plain; charset=utf-8' \
      -d '{
        "providerProductIds": [
          "73cfa507-221b-417a-bd62-c7b62065068e",
          "9b1d3c2a-4f5e-4a8b-8c7d-1e2f3a4b5c6d"
        ]
      }'
      ```

      ```json Response
      {
        "productMockups": [
          {
            "providerProductId": "73cfa507-221b-417a-bd62-c7b62065068e",
            "mockupImageUrls": [
              "https://provider.example.com/mockups/73cfa507-front.png",
              "https://provider.example.com/mockups/73cfa507-back.png"
            ]
          },
          {
            "providerProductId": "9b1d3c2a-4f5e-4a8b-8c7d-1e2f3a4b5c6d",
            "mockupImageUrls": [
              "https://provider.example.com/mockups/9b1d3c2a.png"
            ]
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves ready-made product mockup image URLs for one or more products.

    Wix calls this method when a site owner views product mockups in the Suppliers Hub Marketplace. Wix calls it only when your implementation enables it by setting `toggleGetProductMockupsEnabled` to `true` in the marketplace provider config.

    Return your existing, ready-made mockup images for each requested product. Unlike [Bulk Generate Mockups](https://dev.wix.com/docs/api-reference/business-solutions/suppliers-hub/marketplace-provider-service-plugin/bulk-generate-mockups), this method doesn't generate new images or apply a site owner's logo or custom image.

    Wix uses the response to display mockup images for the requested products.

    ## Method Declaration

    ```typescript
    import { marketplaceProvider } from '@wix/suppliers-hub';

    marketplaceProvider.getProductMockups(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="GetProductMockupsRequest">

      request

      <Expandable title="request properties">

        <ParamField body="providerProductIds" type="string[]">

          Product IDs on the provider's platform to retrieve mockups for.

          Constraints: `minLength 1`, `maxLength 36`, `minItems 1`, `maxItems 100`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="productMockups" type="ProductMockups[]">

      Mockups for the requested products.

      Constraints: `maxItems 100`.

      <Expandable title="productMockups properties">

        <ResponseField name="providerProductId" type="string">

          Product ID on the provider's platform.

          Constraints: `minLength 1`, `maxLength 36`.

        </ResponseField>

        <ResponseField name="mockupImageUrls" type="string[]">

          Ready-made mockup image URLs for the product.

          Constraints: `format WEB_URL`, `maxItems 100`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-product-mockups" id="ex-sdk-get-product-mockups-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-product-mockups" id="ex-sdk-get-product-mockups-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-product-mockups" id="ex-sdk-get-product-mockups-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-product-mockups-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-product-mockups-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getProductMockups</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getProductMockups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getProductMockups (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-product-mockups-0"><span className="wix-picker-name">getProductMockups</span></label>
    <label htmlFor="ex-sdk-get-product-mockups-1"><span className="wix-picker-name">getProductMockups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-product-mockups-2"><span className="wix-picker-name">getProductMockups (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getProductMockups
    import { marketplaceProvider } from '@wix/suppliers-hub/service-plugins';

    async function getProductMockups(request,metadata) {
      const response = await marketplaceProvider.getProductMockups(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getProductMockups (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketplaceProvider } from '@wix/suppliers-hub/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketplaceProvider },
      // Include the auth strategy and host as relevant
    });

    async function getProductMockups(request,metadata) {
      const response = await myWixClient.marketplaceProvider.getProductMockups(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getProductMockups (with elevated permissions)
    import { marketplaceProvider } from '@wix/suppliers-hub/service-plugins';
    import { auth } from '@wix/essentials';

    async function myGetProductMockupsMethod(request,metadata) {
      const elevatedGetProductMockups = auth.elevate(marketplaceProvider.getProductMockups);
      const response = await elevatedGetProductMockups(request,metadata);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
