---
title: "onGroupMemberRemoved"
description: "Triggered when a member is removed from a group."
icon: "bolt"
---

Triggered when a member is removed from a group.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_member_removed` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupMemberRemoved
  import { members } from '@wix/groups';

  members.onGroupMemberRemoved((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupMemberRemoved (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { members } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        members,
      },
  });

  wixClient.members.onGroupMemberRemoved((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
