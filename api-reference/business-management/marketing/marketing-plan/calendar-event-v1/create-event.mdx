---
title: "Create Event"
description: "Creates a calendar event."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a calendar event.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/promote/marketing-plan-service/v1/events
    ```

    ## Body Params

    <ParamField body="calendarEvent" type="CalendarEvent">

      Event to create. Send the event in this `calendarEvent` field; the response returns the
      created event in the `event` field.

      See the [Calendar Event Object](/api-reference/business-management/marketing/marketing-plan/calendar-event-v1/calendar-event-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="event" type="CalendarEvent">

      The created event.

      See the [Calendar Event Object](/api-reference/business-management/marketing/marketing-plan/calendar-event-v1/calendar-event-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Create a single-occurrence marketing calendar event by setting its date.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/promote/marketing-plan-service/v1/events' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "calendarEvent": {
          "name": "Summer Sale Launch",
          "description": "Kick off our summer promotion across all social channels.",
          "date": "2026-07-01T09:00:00.000Z"
        }
      }'
      ```

      ```json Response
      {
        "event": {
          "id": "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
          "name": "Summer Sale Launch",
          "description": "Kick off our summer promotion across all social channels.",
          "date": "2026-07-01T09:00:00.000Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a calendar event.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { calendarEvents } from '@wix/promote-growth-tools-marketing-plan';

    calendarEvents.createEvent(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="CreateEventOptions">

      options

      <Expandable title="options properties">

        <ParamField body="calendarEvent" type="CalendarEvent">

          Event to create. Send the event in this `calendarEvent` field; the response returns the
          created event in the `event` field.

          See the [Calendar Event Object](/api-reference/business-management/marketing/marketing-plan/calendar-event-v1/calendar-event-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `CalendarEvent`.

    See the [Calendar Event Object](/api-reference/business-management/marketing/marketing-plan/calendar-event-v1/calendar-event-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Calendar Event Created | `wix.promote.marketing_plan_service.v1.calendar_event_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-event" id="ex-sdk-create-event-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-event" id="ex-sdk-create-event-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-event-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-event-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a one-time event</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-event-0"><span className="wix-picker-name">Create a one-time event</span></label>
    <label htmlFor="ex-sdk-create-event-1"><span className="wix-picker-name">createEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a one-time event
    import { calendarEvents } from "@wix/promote-growth-tools-marketing-plan";

    const calendarEvent = {
      name: "Summer Sale Launch",
      description: "Kick off our summer promotion across all social channels.",
      date: new Date("2026-07-01T09:00:00.000Z"),
    };

    async function createEvent() {
      const response = await calendarEvents.createEvent({ calendarEvent });
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createEvent (self-hosted)
    import { createClient } from '@wix/sdk';
    import { calendarEvents } from '@wix/promote-growth-tools-marketing-plan';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { calendarEvents },
      // Include the auth strategy and host as relevant
    });

    async function createEvent(options) {
      const response = await myWixClient.calendarEvents.createEvent(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
