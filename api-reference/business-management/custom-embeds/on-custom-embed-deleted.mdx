---
title: "onCustomEmbedDeleted"
description: "Triggered when a custom embed is deleted."
icon: "bolt"
---

Triggered when a custom embed is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.embeds.v1.custom_embed_deleted` |
| Package | `@wix/embeds` |
| Permissions | `SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS`, `Manage Marketing Tags` |

## Examples

<CodeGroup>

  ```javascript onCustomEmbedDeleted
  import { customEmbeds } from '@wix/embeds';

  customEmbeds.onCustomEmbedDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onCustomEmbedDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { customEmbeds } from '@wix/embeds';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        customEmbeds,
      },
  });

  wixClient.customEmbeds.onCustomEmbedDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
