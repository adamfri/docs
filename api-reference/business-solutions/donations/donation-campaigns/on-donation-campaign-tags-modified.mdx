---
title: "onDonationCampaignTagsModified"
description: "Triggered when donation campaign tags are modified."
icon: "bolt"
---

Triggered when donation campaign tags are modified.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.donations.v2.donation_campaign_tags_modified` |
| Package | `@wix/donations` |
| Permissions | `Manage Donation Campaigns` |

## Examples

<CodeGroup>

  ```javascript onDonationCampaignTagsModified
  import { donationCampaigns } from '@wix/donations';

  donationCampaigns.onDonationCampaignTagsModified((event) => {
      // handle your event here
  });
  ```

  ```javascript onDonationCampaignTagsModified (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { donationCampaigns } from '@wix/donations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        donationCampaigns,
      },
  });

  wixClient.donationCampaigns.onDonationCampaignTagsModified((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
