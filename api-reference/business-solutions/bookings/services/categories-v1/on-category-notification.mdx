---
title: "onCategoryNotification"
description: "The onCategoryNotification event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `com.wixpress.bookings.services.api.v1.CategoryNotification` |
| Package | `@wix/bookings` |
| Permissions | `Manage Stores`, `Read Bookings - Public Data`, `Manage Bookings Services and Settings`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onCategoryNotification
  import { categories } from '@wix/bookings';

  categories.onCategoryNotification((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryNotification (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categories } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        categories,
      },
  });

  wixClient.categories.onCategoryNotification((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
