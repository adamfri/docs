---
title: "Assign Instructor To Program"
description: "Assigns an instructor to the program"
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Assigns an instructor to the program

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_api/instructors-service/v2/instructors/{instructorId}/assign
    ```

    ## Path Params

    <ParamField path="instructorId" type="string" required>

      ID of the instructor to assign

    </ParamField>

    ## Body Params

    <ParamField body="programId" type="string" required>

      ID of the program

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="actionId" type="string">

      trace id used by BI.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    Returns `AssignInstructorToProgramResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Assigns an instructor to the program

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { instructors } from '@wix/online-programs';

    instructors.assignInstructorToProgram(instructorId, programId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="instructorId" type="string" required>

      ID of the instructor to assign

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="programId" type="string" required>

      ID of the program

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="AssignInstructorToProgramOptions">

      options

      <Expandable title="options properties">

        <ParamField body="actionId" type="string">

          trace id used by BI.

          Constraints: `format GUID`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `AssignInstructorToProgramResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Instructor Instructor Was Assigned | `wix.achievements.instructors.v2.instructor_instructor_was_assigned` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-assign-instructor-to-program" id="ex-sdk-assign-instructor-to-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-assign-instructor-to-program" id="ex-sdk-assign-instructor-to-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-assign-instructor-to-program" id="ex-sdk-assign-instructor-to-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-assign-instructor-to-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-assign-instructor-to-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">assignInstructorToProgram</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">assignInstructorToProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">assignInstructorToProgram (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-assign-instructor-to-program-0"><span className="wix-picker-name">assignInstructorToProgram</span></label>
    <label htmlFor="ex-sdk-assign-instructor-to-program-1"><span className="wix-picker-name">assignInstructorToProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-assign-instructor-to-program-2"><span className="wix-picker-name">assignInstructorToProgram (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript assignInstructorToProgram
    import { instructors } from '@wix/online-programs';

    async function assignInstructorToProgram(instructorId,programId,options) {
      const response = await instructors.assignInstructorToProgram(instructorId,programId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript assignInstructorToProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { instructors } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { instructors },
      // Include the auth strategy and host as relevant
    });

    async function assignInstructorToProgram(instructorId,programId,options) {
      const response = await myWixClient.instructors.assignInstructorToProgram(instructorId,programId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript assignInstructorToProgram (with elevated permissions)
    import { instructors } from '@wix/online-programs';
    import { auth } from '@wix/essentials';

    async function myAssignInstructorToProgramMethod(instructorId,programId,options) {
      const elevatedAssignInstructorToProgram = auth.elevate(instructors.assignInstructorToProgram);
      const response = await elevatedAssignInstructorToProgram(instructorId,programId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
