---
title: "onLocaleDeleted"
description: "Triggered when a locale is deleted."
icon: "bolt"
---

Triggered when a locale is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.locales.v2.locale_deleted` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API` |

## Examples

<CodeGroup>

  ```javascript onLocaleDeleted
  import { locales } from '@wix/multilingual';

  locales.onLocaleDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocaleDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { locales } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        locales,
      },
  });

  wixClient.locales.onLocaleDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
