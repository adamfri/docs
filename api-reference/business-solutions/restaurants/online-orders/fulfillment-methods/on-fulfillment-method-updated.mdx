---
title: "onFulfillmentMethodUpdated"
description: "Triggered when a fulfillment method is updated."
icon: "bolt"
---

Triggered when a fulfillment method is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.fulfillment_method_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onFulfillmentMethodUpdated
  import { fulfillmentMethods } from '@wix/restaurants';

  fulfillmentMethods.onFulfillmentMethodUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onFulfillmentMethodUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { fulfillmentMethods } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        fulfillmentMethods,
      },
  });

  wixClient.fulfillmentMethods.onFulfillmentMethodUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
