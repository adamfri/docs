---
title: "Get Availability Exception"
description: "Retrieves an availability exception."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an availability exception.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/restaurants-availability-exceptions/v1/availability-exceptions/{availabilityExceptionId}
    ```

    ## Path Params

    <ParamField path="availabilityExceptionId" type="string" required>

      ID of the availability exception to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="availabilityException" type="AvailabilityException">

      The requested availability exception.

      See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://www.wixapis.com/restaurants-availability-exceptions/v1/availability-exceptions/48605ac5-53fc-49b4-96b2-87f7636f5ce0 \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "availabilityException": {
            "id": "63eb0bf7-96ea-4034-822b-7dd9a24197e9",
            "revision": "1",
            "createdDate": "2024-07-09T10:17:08.865Z",
            "updatedDate": "2024-07-09T10:17:08.865Z",
            "startTime": "2024-07-08T21:00:00.587Z",
            "endTime": "2024-07-09T20:59:59.587Z",
            "available": false,
            "name": "Passover",
            "affectedFulfillmentMethods": {
              "affectedMethods": "ALL_FULFILLMENT_METHODS"
            },
            "operationId": "48605ac5-53fc-49b4-96b2-87f7636f5ce0"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an availability exception.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { availabilityExceptions } from '@wix/restaurants';

    availabilityExceptions.getAvailabilityException(availabilityExceptionId): Promise<AvailabilityException>
    ```

    ## Method Parameters

    <ParamField body="availabilityExceptionId" type="string" required>

      ID of the availability exception to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<AvailabilityException>`.

    See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-availability-exception" id="ex-sdk-get-availability-exception-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-availability-exception" id="ex-sdk-get-availability-exception-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-availability-exception-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-availability-exception-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getAvailabilityException</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getAvailabilityException (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-availability-exception-0"><span className="wix-picker-name">getAvailabilityException</span></label>
    <label htmlFor="ex-sdk-get-availability-exception-1"><span className="wix-picker-name">getAvailabilityException (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getAvailabilityException
    import { availabilityExceptions } from '@wix/restaurants';

    async function getAvailabilityException(availabilityExceptionId) {
      const response = await availabilityExceptions.getAvailabilityException(availabilityExceptionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getAvailabilityException (self-hosted)
    import { createClient } from '@wix/sdk';
    import { availabilityExceptions } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { availabilityExceptions },
      // Include the auth strategy and host as relevant
    });

    async function getAvailabilityException(availabilityExceptionId) {
      const response = await myWixClient.availabilityExceptions.getAvailabilityException(availabilityExceptionId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
