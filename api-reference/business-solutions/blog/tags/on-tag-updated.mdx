---
title: "onTagUpdated"
description: "Triggered when a tag is updated."
icon: "bolt"
---

Triggered when a tag is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v3.tag_updated` |
| Package | `@wix/blog` |
| Permissions | `Read Blog`, `Manage Blog`, `Read Draft Blog Posts` |

## Examples

<CodeGroup>

  ```javascript onTagUpdated
  import { tags } from '@wix/blog';

  tags.onTagUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTagUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { tags } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        tags,
      },
  });

  wixClient.tags.onTagUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
