---
title: "Get Refund"
description: "Retrieves a refund."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a refund.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.READ-REFUNDS">Read Refunds</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/payments/refunds/v1/refunds/{refundId}
    ```

    ## Path Params

    <ParamField path="refundId" type="string" required>

      ID of the refund to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="refund" type="Refund">

      The requested refund.

      See the [Refund Object](/api-reference/business-management/payments/refunds/refund-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Gets existing refund by ID

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/payments/refunds/v1/refunds/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "refund": {
          "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
          "revision": 2,
          "createdDate": "2019-10-30T17:22:10.458Z",
          "updatedDate": "2019-10-30T17:22:11.299Z",
          "chargeId": "602b516f-e8a2-4e1a-9a17-0708099a74db",
          "currencyCode": "USD",
          "amount": "100",
          "full": true,
          "status": "SUCCEEDED",
          "providerRefundId": "re_1Nispe2eZvKYlo2Cd31jOCsZ"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a refund.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.READ-REFUNDS">Read Refunds</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { refunds } from '@wix/payments';

    refunds.getRefund(refundId): Promise<Refund>
    ```

    ## Method Parameters

    <ParamField body="refundId" type="string" required>

      ID of the refund to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Refund>`.

    See the [Refund Object](/api-reference/business-management/payments/refunds/refund-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-refund" id="ex-sdk-get-refund-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-refund" id="ex-sdk-get-refund-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-refund" id="ex-sdk-get-refund-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-refund-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-refund-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getRefund</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getRefund (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getRefund (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-refund-0"><span className="wix-picker-name">getRefund</span></label>
    <label htmlFor="ex-sdk-get-refund-1"><span className="wix-picker-name">getRefund (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-refund-2"><span className="wix-picker-name">getRefund (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getRefund
    import { refunds } from '@wix/payments';

    async function getRefund(refundId) {
      const response = await refunds.getRefund(refundId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getRefund (self-hosted)
    import { createClient } from '@wix/sdk';
    import { refunds } from '@wix/payments';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { refunds },
      // Include the auth strategy and host as relevant
    });

    async function getRefund(refundId) {
      const response = await myWixClient.refunds.getRefund(refundId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getRefund (with elevated permissions)
    import { refunds } from '@wix/payments';
    import { auth } from '@wix/essentials';

    async function myGetRefundMethod(refundId) {
      const elevatedGetRefund = auth.elevate(refunds.getRefund);
      const response = await elevatedGetRefund(refundId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
