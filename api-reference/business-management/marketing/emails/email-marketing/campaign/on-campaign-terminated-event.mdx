---
title: "onCampaignTerminatedEvent"
description: "Triggered when the mailing list isn't clean and campaign was terminated."
icon: "bolt"
---

Triggered when the mailing list isn't clean and campaign was terminated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.email_marketing.v1.campaign_campaign_terminated_event` |
| Package | `@wix/email-marketing` |
| Permissions | `Manage Stores`, `Access Verticals by Automations`, `Set Up Automations`, `Read Email Marketing`, `Manage Email Marketing` |

## Examples

<CodeGroup>

  ```javascript onCampaignTerminatedEvent
  import { campaigns } from '@wix/email-marketing';

  campaigns.onCampaignTerminatedEvent((event) => {
      // handle your event here
  });
  ```

  ```javascript onCampaignTerminatedEvent (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { campaigns } from '@wix/email-marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        campaigns,
      },
  });

  wixClient.campaigns.onCampaignTerminatedEvent((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
