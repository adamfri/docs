---
title: "Delete Automation Earning Rule"
description: "Deletes a custom automated earning rule."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a custom automated earning rule.
    Pre-installed automated rules can only be paused, not deleted.

    To update an earning rule's status instead of deleting it, call
    Update Loyalty Earning Rule.

    To delete a non-automated earning rule, call
    Delete Loyalty Earning Rule.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/_api/loyalty-earning-rules/v1/automation-earning-rules/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the earning rule to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteAutomationEarningRuleResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/loyalty-earning-rules/v1/automation-earning-rules/61f971c4-b33b-4f39-8ebe-2677bce55789' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a custom automated earning rule.
    Pre-installed automated rules can only be paused, not deleted.

    To update an earning rule's status instead of deleting it, call
    Update Loyalty Earning Rule.

    To delete a non-automated earning rule, call
    Delete Loyalty Earning Rule.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { earningRules } from '@wix/loyalty';

    earningRules.deleteAutomationEarningRule(_id): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the earning rule to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-automation-earning-rule" id="ex-sdk-delete-automation-earning-rule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-automation-earning-rule" id="ex-sdk-delete-automation-earning-rule-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-automation-earning-rule" id="ex-sdk-delete-automation-earning-rule-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-automation-earning-rule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-automation-earning-rule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteAutomationEarningRule</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAutomationEarningRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAutomationEarningRule (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-automation-earning-rule-0"><span className="wix-picker-name">deleteAutomationEarningRule</span></label>
    <label htmlFor="ex-sdk-delete-automation-earning-rule-1"><span className="wix-picker-name">deleteAutomationEarningRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-automation-earning-rule-2"><span className="wix-picker-name">deleteAutomationEarningRule (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteAutomationEarningRule
    import { earningRules } from '@wix/loyalty';

    async function deleteAutomationEarningRule(_id) {
      const response = await earningRules.deleteAutomationEarningRule(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAutomationEarningRule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { earningRules } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { earningRules },
      // Include the auth strategy and host as relevant
    });

    async function deleteAutomationEarningRule(_id) {
      const response = await myWixClient.earningRules.deleteAutomationEarningRule(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAutomationEarningRule (with elevated permissions)
    import { earningRules } from '@wix/loyalty';
    import { auth } from '@wix/essentials';

    async function myDeleteAutomationEarningRuleMethod(_id) {
      const elevatedDeleteAutomationEarningRule = auth.elevate(earningRules.deleteAutomationEarningRule);
      const response = await elevatedDeleteAutomationEarningRule(_id);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
