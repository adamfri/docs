---
title: "onDonationCampaignCreated"
description: "Triggered when a donation campaign is created."
icon: "bolt"
---

Triggered when a donation campaign is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.donations.v2.donation_campaign_created` |
| Package | `@wix/donations` |
| Permissions | `Manage Donation Campaigns` |

## Examples

<CodeGroup>

  ```javascript onDonationCampaignCreated
  import { donationCampaigns } from '@wix/donations';

  donationCampaigns.onDonationCampaignCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onDonationCampaignCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { donationCampaigns } from '@wix/donations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        donationCampaigns,
      },
  });

  wixClient.donationCampaigns.onDonationCampaignCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
