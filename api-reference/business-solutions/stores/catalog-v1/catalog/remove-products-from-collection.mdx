---
title: "Remove Products From Collection"
description: "Deletes products from a specified collection."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes products from a specified collection.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v1/collections/{id}/productIds/delete
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Collection ID.

    </ParamField>

    ## Body Params

    <ParamField body="productIds" type="string[]" required>

      IDs of the products to remove from the collection.

      Constraints: `maxItems 1000`.

    </ParamField>

    ## Response Object

    Returns `RemoveProductsFromCollectionResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
         'https://www.wixapis.com/stores/v1/collections/1044e7e4-37d1-0705-c5b3-623baae212fd/productIds/delete' \
          --data-binary '{
                           "productIds": [
                             "a60fef92-ee29-070f-a7ed-9bbc3cc1c2f4",
                             "d9cd1d2f-8318-486b-a6f3-aa0c4e81ccd2"
                           ]
                         }' \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes products from a specified collection.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { products } from '@wix/stores';

    products.removeProductsFromCollection(_id, productIds): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Collection ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="productIds" type="string[]" required>

      IDs of the products to remove from the collection.

      Constraints: `maxItems 1000`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-products-from-collection" id="ex-sdk-remove-products-from-collection-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-products-from-collection" id="ex-sdk-remove-products-from-collection-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-products-from-collection-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-products-from-collection-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Remove products from a product collection</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeProductsFromCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-products-from-collection-0"><span className="wix-picker-name">Remove products from a product collection</span></label>
    <label htmlFor="ex-sdk-remove-products-from-collection-1"><span className="wix-picker-name">removeProductsFromCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Remove products from a product collection
    /**************************************
     * Backend code - products.web.js/ts *
     **************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { products } from '@wix/stores';

    export const removeProductsFromCollection = webMethod(Permissions.Anyone, (_id, productIds) => {
      return products.removeProductsFromCollection(_id, productIds);
    });

    /*************
     * Page code *
     *************/

    import { removeProductsFromCollection } from 'backend/products.web';

    // ...

    const _id =  ... // get collection ID
    const productIds = ["id1", "id2", "id3"];

    removeProductsFromCollection(_id, productIds)
      .then(() => {
        // products removed from the collection 
      })
      .catch((error) => {
        // products not removed from the collection 
      });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeProductsFromCollection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { products } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { products },
      // Include the auth strategy and host as relevant
    });

    async function removeProductsFromCollection(_id,productIds) {
      const response = await myWixClient.products.removeProductsFromCollection(_id,productIds);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
