---
title: "Update Tiers Program Settings"
description: "Updates the global settings of a loyalty tier program."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the global settings of a loyalty tier program.

    Use this method to update settings that apply to all of a site's loyalty tiers.
    To update tier-specific settings for an individual tier, use Update Tier.

    By default, the `status` of a tiers program is set to `DISABLED` and must be manually updated to `ACTIVE` using
    this method or through a Wix user's dashboard.

    <Note>

    The `status`, `revision`, and `rollingWindow` parameters must be passed to update the tiers program settings.
    The `baseTierDefinition` fields aren't required, however, if you don't pass them they reset to
    their default values of empty fields.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/loyalty-tiers/v1/tiers/program-settings
    ```

    ## Body Params

    <ParamField body="programSettings" type="TiersProgramSettings" required>

      Settings for the tiers program.

      <Expandable title="programSettings properties">

        <ParamField body="status" type="string" required>

          Tiers program status.

          **Supported values:**

          - `DISABLED` — Tiers are disabled.
          - `DRAFT` — Tiers are enabled but not yet active.
          - `ACTIVE` — Tiers are active.
          - `PAUSED` — Tiers are paused.

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the loyalty tiers settings are updated.

          To prevent conflicting changes, the current `revision` must be passed when updating the loyalty tiers settings.

        </ParamField>

        <ParamField body="createdDate" type="datetime">

          Date and time the loyalty tiers program was created.

          **Read-only.**

        </ParamField>

        <ParamField body="updatedDate" type="datetime">

          Date and time the loyalty tiers program was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="baseTierDefinition" type="TierDefinition">

          Information about the base loyalty tier.

          The base tier is the default tier for any account that's unassigned for not meeting
          the required points threshold of any other tier.

          <Expandable title="baseTierDefinition properties">

            <ParamField body="icon" type="Image">

              Details about the tier icon.

              _Nested structure omitted for brevity — see the `Image` object._

            </ParamField>

            <ParamField body="name" type="string">

              Tier name.

            </ParamField>

            <ParamField body="description" type="string">

              Tier description.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="period" type="period">

          period

          <Expandable title="period properties">

            <ParamField body="rollingWindow" type="RollingWindow">

              **Required.**
              Period of time used to calculate loyalty points for tier assignment.

              The loyalty points accumulated during this period determine if the account meets a tier's required point threshold.

              _Nested structure omitted for brevity — see the `RollingWindow` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="programSettings" type="TiersProgramSettings">

      Updated program settings.

      <Expandable title="programSettings properties">

        <ResponseField name="status" type="string">

          Tiers program status.

          **Supported values:**

          - `DISABLED` — Tiers are disabled.
          - `DRAFT` — Tiers are enabled but not yet active.
          - `ACTIVE` — Tiers are active.
          - `PAUSED` — Tiers are paused.

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number, which increments by 1 each time the loyalty tiers settings are updated.

          To prevent conflicting changes, the current `revision` must be passed when updating the loyalty tiers settings.

        </ResponseField>

        <ResponseField name="createdDate" type="datetime">

          Date and time the loyalty tiers program was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updatedDate" type="datetime">

          Date and time the loyalty tiers program was last updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="baseTierDefinition" type="TierDefinition">

          Information about the base loyalty tier.

          The base tier is the default tier for any account that's unassigned for not meeting
          the required points threshold of any other tier.

          <Expandable title="baseTierDefinition properties">

            <ResponseField name="icon" type="Image">

              Details about the tier icon.

              _Nested structure omitted for brevity — see the `Image` object._

            </ResponseField>

            <ResponseField name="name" type="string">

              Tier name.

            </ResponseField>

            <ResponseField name="description" type="string">

              Tier description.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="period" type="period">

          period

          <Expandable title="period properties">

            <ResponseField name="rollingWindow" type="RollingWindow">

              **Required.**
              Period of time used to calculate loyalty points for tier assignment.

              The loyalty points accumulated during this period determine if the account meets a tier's required point threshold.

              _Nested structure omitted for brevity — see the `RollingWindow` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/loyalty-tiers/v1/tiers/program-settings' \
        -H 'Authorization: <AUTH>'
        -H 'Content-Type: application/json' \
        --data-raw '{
                      "programSettings": {
                        "status": "PAUSED",
                        "revision": "1",
                        "baseTierDefinition": {
                          "name": "Basic",
                          "description": "Basic tier",
                          "icon": {
                            "url": "shapes/11062b_0a8e17936d5b4d46971649be036c781b.svg"
                          }
                        },
                        "rollingWindow": {
                          "durationInMonths": 12
                        }
                      }
                    }'
      ```

      ```json Response
      {
          "programSettings": {
              "status": "PAUSED",
              "revision": "2",
              "createdDate": "2024-02-09T09:06:21.823Z",
              "updatedDate": "2024-04-17T14:23:22.985Z",
              "baseTierDefinition": {
                  "icon": {
                      "id": "",
                      "url": "shapes/11062b_0a8e17936d5b4d46971649be036c781b.svg",
                      "height": 0,
                      "width": 0
                  },
                  "name": "Basic",
                  "description": "Basic tier"
              },
              "rollingWindow": {
                  "durationInMonths": 12
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the global settings of a loyalty tier program.

    Use this method to update settings that apply to all of a site's loyalty tiers.
    To update tier-specific settings for an individual tier, use Update Tier.

    By default, the `status` of a tiers program is set to `DISABLED` and must be manually updated to `ACTIVE` using
    this method or through a Wix user's dashboard.

    <Note>

    The `status`, `revision`, and `rollingWindow` parameters must be passed to update the tiers program settings.
    The `baseTierDefinition` fields aren't required, however, if you don't pass them they reset to
    their default values of empty fields.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tiers } from '@wix/loyalty';

    tiers.updateTiersProgramSettings(programSettings): Promise<UpdateTiersProgramSettingsResponse>
    ```

    ## Method Parameters

    <ParamField body="programSettings" type="TiersProgramSettings" required>

      Settings for the tiers program.

      <Expandable title="programSettings properties">

        <ParamField body="status" type="string" required>

          Tiers program status.

          **Supported values:**

          - `DISABLED` — Tiers are disabled.
          - `DRAFT` — Tiers are enabled but not yet active.
          - `ACTIVE` — Tiers are active.
          - `PAUSED` — Tiers are paused.

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the loyalty tiers settings are updated.

          To prevent conflicting changes, the current `revision` must be passed when updating the loyalty tiers settings.

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the loyalty tiers program was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the loyalty tiers program was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="baseTierDefinition" type="TierDefinition">

          Information about the base loyalty tier.

          The base tier is the default tier for any account that's unassigned for not meeting
          the required points threshold of any other tier.

          <Expandable title="baseTierDefinition properties">

            <ParamField body="icon" type="string">

              Details about the tier icon.

            </ParamField>

            <ParamField body="name" type="string">

              Tier name.

            </ParamField>

            <ParamField body="description" type="string">

              Tier description.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="period" type="period">

          period

          <Expandable title="period properties">

            <ParamField body="rollingWindow" type="RollingWindow">

              **Required.**
              Period of time used to calculate loyalty points for tier assignment.

              The loyalty points accumulated during this period determine if the account meets a tier's required point threshold.

              _Nested structure omitted for brevity — see the `RollingWindow` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateTiersProgramSettingsResponse>`.

    <ResponseField name="programSettings" type="TiersProgramSettings">

      Updated program settings.

      <Expandable title="programSettings properties">

        <ResponseField name="status" type="string">

          Tiers program status.

          **Supported values:**

          - `DISABLED` — Tiers are disabled.
          - `DRAFT` — Tiers are enabled but not yet active.
          - `ACTIVE` — Tiers are active.
          - `PAUSED` — Tiers are paused.

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number, which increments by 1 each time the loyalty tiers settings are updated.

          To prevent conflicting changes, the current `revision` must be passed when updating the loyalty tiers settings.

        </ResponseField>

        <ResponseField name="_createdDate" type="datetime">

          Date and time the loyalty tiers program was created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="_updatedDate" type="datetime">

          Date and time the loyalty tiers program was last updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="baseTierDefinition" type="TierDefinition">

          Information about the base loyalty tier.

          The base tier is the default tier for any account that's unassigned for not meeting
          the required points threshold of any other tier.

          <Expandable title="baseTierDefinition properties">

            <ResponseField name="icon" type="string">

              Details about the tier icon.

            </ResponseField>

            <ResponseField name="name" type="string">

              Tier name.

            </ResponseField>

            <ResponseField name="description" type="string">

              Tier description.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="period" type="period">

          period

          <Expandable title="period properties">

            <ResponseField name="rollingWindow" type="RollingWindow">

              **Required.**
              Period of time used to calculate loyalty points for tier assignment.

              The loyalty points accumulated during this period determine if the account meets a tier's required point threshold.

              _Nested structure omitted for brevity — see the `RollingWindow` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-tiers-program-settings" id="ex-sdk-update-tiers-program-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-tiers-program-settings" id="ex-sdk-update-tiers-program-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-tiers-program-settings" id="ex-sdk-update-tiers-program-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-tiers-program-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-tiers-program-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update global settings of a loyalty tier program</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update global settings of a loyalty tier program (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateTiersProgramSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-tiers-program-settings-0"><span className="wix-picker-name">Update global settings of a loyalty tier program</span></label>
    <label htmlFor="ex-sdk-update-tiers-program-settings-1"><span className="wix-picker-name">Update global settings of a loyalty tier program (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-update-tiers-program-settings-2"><span className="wix-picker-name">updateTiersProgramSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update global settings of a loyalty tier program
    import { tiers } from "@wix/loyalty";

    /* Sample programSettings object:
     *
     * {
     *   "programSettings": {
     *     "revision": "2",
     *     "status": "PAUSED",
     *     "rollingWindow": {
     *       "durationInMonths": 12
     *     }
     *   }
     * }
     */ 

    export async function updateTiersProgramSettings(programSettings) {
      try {
        const result = await tiers.updateTiersProgramSettings(programSettings);
        return result;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

     /* Promise resolves to:
      *
      * {
      *   "programSettings": {
      *     "_createdDate": "2024-06-05T20:27:19.147Z",
      *     "_updatedDate": "2024-06-11T14:12:48.551Z",
      *     "status": "PAUSED",
      *     "revision": "3",
      *     "rollingWindow": {
      *       "durationInMonths": 12
      *     }
      *   }
      * }
      */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update global settings of a loyalty tier program (with elevated permissions)
    import { tiers } from "@wix/loyalty";
    import { auth } from "@wix/essentials";

    /* Sample programSettings object:
     *
     * {
     *   "programSettings": {
     *     "revision": "2",
     *     "status": "PAUSED",
     *     "rollingWindow": {
     *       "durationInMonths": 12
     *     }
     *   }
     * }
     */ 

    const elevatedUpdateTiersProgramSettings = auth.elevate(
      tiers.updateTiersProgramSettings
    );

    export async function updateTiersProgramSettings(programSettings) {
      try {
        const result = await elevatedUpdateTiersProgramSettings(programSettings);
        return result;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

     /* Promise resolves to:
      *
      * {
      *   "programSettings": {
      *     "_createdDate": "2024-06-05T20:27:19.147Z",
      *     "_updatedDate": "2024-06-11T14:12:48.551Z",
      *     "status": "PAUSED",
      *     "revision": "3",
      *     "rollingWindow": {
      *       "durationInMonths": 12
      *     }
      *   }
      * }
      */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateTiersProgramSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tiers } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tiers },
      // Include the auth strategy and host as relevant
    });

    async function updateTiersProgramSettings(programSettings) {
      const response = await myWixClient.tiers.updateTiersProgramSettings(programSettings);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
