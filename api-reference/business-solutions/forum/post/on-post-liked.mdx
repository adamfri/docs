---
title: "onPostLiked"
description: "An existing post was liked."
icon: "bolt"
---

An existing post was liked.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_liked` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostLiked
  import { posts } from '@wix/forum';

  posts.onPostLiked((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostLiked (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostLiked((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
