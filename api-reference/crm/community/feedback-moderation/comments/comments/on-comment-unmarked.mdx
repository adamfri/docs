---
title: "onCommentUnmarked"
description: "Triggered when a comment is unmarked."
icon: "bolt"
---

Triggered when a comment is unmarked.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v2.comment_comment_unmarked` |
| Package | `@wix/comments` |
| Permissions | `Read Comments`, `Manage Comments` |

## Examples

<CodeGroup>

  ```javascript onCommentUnmarked
  import { comments } from '@wix/comments';

  comments.onCommentUnmarked((event) => {
      // handle your event here
  });
  ```

  ```javascript onCommentUnmarked (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { comments } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        comments,
      },
  });

  wixClient.comments.onCommentUnmarked((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
