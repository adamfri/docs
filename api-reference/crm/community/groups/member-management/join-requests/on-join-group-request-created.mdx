---
title: "onJoinGroupRequestCreated"
description: "The onJoinGroupRequestCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.join_group_request_created` |
| Package | `@wix/groups` |
| Permissions | `Manage Social Groups` |

## Examples

<CodeGroup>

  ```javascript onJoinGroupRequestCreated
  import { joinGroupRequests } from '@wix/groups';

  joinGroupRequests.onJoinGroupRequestCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onJoinGroupRequestCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { joinGroupRequests } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        joinGroupRequests,
      },
  });

  wixClient.joinGroupRequests.onJoinGroupRequestCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
