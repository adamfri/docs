---
title: "Delete Discount Rule"
description: "Deletes a discount rule."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a discount rule.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/ecom/v1/discount-rules/{discountRuleId}
    ```

    ## Path Params

    <ParamField path="discountRuleId" type="string" required>

      ID of the discount rule to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteDiscountRuleResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Discount Rule Deleted | `wix.ecom.discounts.v1.discount_rule_deleted` | Triggered when a discount rule is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE \
        'https://www.wixapis.com/ecom/v1/discount-rules/c31f8d15-2077-bab2-4065-f01c0e3152b7' \
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a discount rule.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { discountRules } from '@wix/ecom';

    discountRules.deleteDiscountRule(discountRuleId): Promise
    ```

    ## Method Parameters

    <ParamField body="discountRuleId" type="string" required>

      ID of the discount rule to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteDiscountRuleResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Discount Rule Deleted | `wix.ecom.discounts.v1.discount_rule_deleted` | Triggered when a discount rule is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-discount-rule" id="ex-sdk-delete-discount-rule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-discount-rule" id="ex-sdk-delete-discount-rule-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-discount-rule" id="ex-sdk-delete-discount-rule-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-discount-rule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-discount-rule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteDiscountRule</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteDiscountRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteDiscountRule (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-discount-rule-0"><span className="wix-picker-name">deleteDiscountRule</span></label>
    <label htmlFor="ex-sdk-delete-discount-rule-1"><span className="wix-picker-name">deleteDiscountRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-discount-rule-2"><span className="wix-picker-name">deleteDiscountRule (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteDiscountRule
    import { discountRules } from '@wix/ecom';

    async function deleteDiscountRule(discountRuleId) {
      const response = await discountRules.deleteDiscountRule(discountRuleId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteDiscountRule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { discountRules } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { discountRules },
      // Include the auth strategy and host as relevant
    });

    async function deleteDiscountRule(discountRuleId) {
      const response = await myWixClient.discountRules.deleteDiscountRule(discountRuleId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteDiscountRule (with elevated permissions)
    import { discountRules } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myDeleteDiscountRuleMethod(discountRuleId) {
      const elevatedDeleteDiscountRule = auth.elevate(discountRules.deleteDiscountRule);
      const response = await elevatedDeleteDiscountRule(discountRuleId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
