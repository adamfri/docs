---
title: "Update Portfolio Settings"
description: "Updates a site's portfolio settings."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a site's portfolio settings.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/portfolio/v1/settings
    ```

    ## Body Params

    <ParamField body="portfolioSettings" type="PortfolioSettings" required>

      The portfolio settings to update.

      See the [Portfolio Settings Object](/api-reference/business-solutions/portfolio/portfolio-settings/portfolio-settings-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="updatedPortfolioSettings" type="PortfolioSettings">

      Updated portfolio settings.

      See the [Portfolio Settings Object](/api-reference/business-solutions/portfolio/portfolio-settings/portfolio-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/portfolio/v1/settings' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "portfolioSettings": {
          "revision": 2,
          "projectItemSettings": {
            "addItemDirection": "GALLERY_START",
            "defaultItemName": "EMPTY"
          },
          "mediaSettings": {
            "folderId": "42460836-02b9-4640-8d47-2d9fe9aba7e4"
          }
        }
      }'
      ```

      ```json Response
      {
        "updatedPortfolioSettings": {
          "revision": 2,
          "projectItemSettings": {
            "addItemDirection": "GALLERY_START",
            "defaultItemName": "EMPTY"
          },
          "mediaSettings": {
            "folderId": "42460836-02b9-4640-8d47-2d9fe9aba7e4"
          }
          "createdDate": "2024-07-31T12:00:00Z",
          "updatedDate": "2024-07-31T12:05:00Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a site's portfolio settings.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { portfolioSettings } from '@wix/portfolio';

    portfolioSettings.updatePortfolioSettings(portfolioSettings): Promise<UpdatePortfolioSettingsResponse>
    ```

    ## Method Parameters

    <ParamField body="portfolioSettings" type="PortfolioSettings" required>

      The portfolio settings to update.

      See the [Portfolio Settings Object](/api-reference/business-solutions/portfolio/portfolio-settings/portfolio-settings-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<UpdatePortfolioSettingsResponse>`.

    <ResponseField name="updatedPortfolioSettings" type="PortfolioSettings">

      Updated portfolio settings.

      See the [Portfolio Settings Object](/api-reference/business-solutions/portfolio/portfolio-settings/portfolio-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-portfolio-settings" id="ex-sdk-update-portfolio-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-portfolio-settings" id="ex-sdk-update-portfolio-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-portfolio-settings" id="ex-sdk-update-portfolio-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-portfolio-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-portfolio-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updatePortfolioSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updatePortfolioSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updatePortfolioSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-portfolio-settings-0"><span className="wix-picker-name">updatePortfolioSettings</span></label>
    <label htmlFor="ex-sdk-update-portfolio-settings-1"><span className="wix-picker-name">updatePortfolioSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-portfolio-settings-2"><span className="wix-picker-name">updatePortfolioSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updatePortfolioSettings
    import { portfolioSettings } from '@wix/portfolio';

    async function updatePortfolioSettings(portfolioSettings) {
      const response = await portfolioSettings.updatePortfolioSettings(portfolioSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updatePortfolioSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { portfolioSettings } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { portfolioSettings },
      // Include the auth strategy and host as relevant
    });

    async function updatePortfolioSettings(portfolioSettings) {
      const response = await myWixClient.portfolioSettings.updatePortfolioSettings(portfolioSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updatePortfolioSettings (with elevated permissions)
    import { portfolioSettings } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myUpdatePortfolioSettingsMethod(portfolioSettings) {
      const elevatedUpdatePortfolioSettings = auth.elevate(portfolioSettings.updatePortfolioSettings);
      const response = await elevatedUpdatePortfolioSettings(portfolioSettings);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
