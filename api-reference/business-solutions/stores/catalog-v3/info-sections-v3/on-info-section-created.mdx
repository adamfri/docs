---
title: "onInfoSectionCreated"
description: "Triggered when an info section is created."
icon: "bolt"
---

Triggered when an info section is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.stores.catalog.v3.info_section_created` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Read info sections in v3 catalog`, `Read v3 catalog`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Product write in v3 catalog`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onInfoSectionCreated
  import { infoSectionsV3 } from '@wix/stores';

  infoSectionsV3.onInfoSectionCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onInfoSectionCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { infoSectionsV3 } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        infoSectionsV3,
      },
  });

  wixClient.infoSectionsV3.onInfoSectionCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
