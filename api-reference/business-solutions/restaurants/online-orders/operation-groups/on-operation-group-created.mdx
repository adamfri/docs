---
title: "onOperationGroupCreated"
description: "The onOperationGroupCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.operation_group_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onOperationGroupCreated
  import { operationGroups } from '@wix/restaurants';

  operationGroups.onOperationGroupCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOperationGroupCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { operationGroups } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        operationGroups,
      },
  });

  wixClient.operationGroups.onOperationGroupCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
