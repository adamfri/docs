---
title: "onReferredFriendUpdated"
description: "Triggered when a referred friend is updated."
icon: "bolt"
---

Triggered when a referred friend is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.referral.v1.referred_friend_updated` |
| Package | `@wix/referral` |

## Examples

<CodeGroup>

  ```javascript onReferredFriendUpdated
  import { friends } from '@wix/referral';

  friends.onReferredFriendUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReferredFriendUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { friends } from '@wix/referral';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        friends,
      },
  });

  wixClient.friends.onReferredFriendUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
