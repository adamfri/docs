---
title: "onNoteUpdated"
description: "Triggered when a note is updated."
icon: "bolt"
---

Triggered when a note is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.crm.notes.v2.note_updated` |
| Package | `@wix/crm` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Read Members and Contacts - all read permissions`, `Manage Notes`, `Manage Members and Contacts - all permissions`, `Read Notes` |

## Examples

<CodeGroup>

  ```javascript onNoteUpdated
  import { notes } from '@wix/crm';

  notes.onNoteUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onNoteUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { notes } from '@wix/crm';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        notes,
      },
  });

  wixClient.notes.onNoteUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
