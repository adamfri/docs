---
title: "Delete Abandoned Checkout"
description: "Deletes an abandoned checkout."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an abandoned checkout.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/ecom/v1/abandoned-checkout/{abandonedCheckoutId}
    ```

    ## Path Params

    <ParamField path="abandonedCheckoutId" type="string" required>

      Id of the abandoned checkout to delete

    </ParamField>

    ## Response Object

    Returns `DeleteAbandonedCheckoutResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Abandoned Checkout Deleted | `wix.ecom.v1.abandoned_checkout_deleted` | Triggerred when an abandoned checkout is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/ecom/v1/abandoned-checkout/330c3e52-73d4-4cc7-8eb2-61f6c5a07478' \
        -H 'Authorization: <AUTH>' \
        -H "Content-Type:application/json"
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an abandoned checkout.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { abandonedCheckouts } from '@wix/ecom';

    abandonedCheckouts.deleteAbandonedCheckout(abandonedCheckoutId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="abandonedCheckoutId" type="string" required>

      Id of the abandoned checkout to delete

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Abandoned Checkout Deleted | `wix.ecom.v1.abandoned_checkout_deleted` | Triggerred when an abandoned checkout is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-abandoned-checkout" id="ex-sdk-delete-abandoned-checkout-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-abandoned-checkout" id="ex-sdk-delete-abandoned-checkout-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-abandoned-checkout" id="ex-sdk-delete-abandoned-checkout-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-abandoned-checkout-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-abandoned-checkout-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteAbandonedCheckout</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAbandonedCheckout (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAbandonedCheckout (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-abandoned-checkout-0"><span className="wix-picker-name">deleteAbandonedCheckout</span></label>
    <label htmlFor="ex-sdk-delete-abandoned-checkout-1"><span className="wix-picker-name">deleteAbandonedCheckout (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-abandoned-checkout-2"><span className="wix-picker-name">deleteAbandonedCheckout (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteAbandonedCheckout
    import { abandonedCheckouts } from '@wix/ecom';

    async function deleteAbandonedCheckout(abandonedCheckoutId) {
      const response = await abandonedCheckouts.deleteAbandonedCheckout(abandonedCheckoutId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAbandonedCheckout (self-hosted)
    import { createClient } from '@wix/sdk';
    import { abandonedCheckouts } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { abandonedCheckouts },
      // Include the auth strategy and host as relevant
    });

    async function deleteAbandonedCheckout(abandonedCheckoutId) {
      const response = await myWixClient.abandonedCheckouts.deleteAbandonedCheckout(abandonedCheckoutId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAbandonedCheckout (with elevated permissions)
    import { abandonedCheckouts } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myDeleteAbandonedCheckoutMethod(abandonedCheckoutId) {
      const elevatedDeleteAbandonedCheckout = auth.elevate(abandonedCheckouts.deleteAbandonedCheckout);
      const response = await elevatedDeleteAbandonedCheckout(abandonedCheckoutId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
