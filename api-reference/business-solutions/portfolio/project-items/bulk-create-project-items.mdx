---
title: "Bulk Create Project Items"
description: "Creates multiple project items."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple project items.

    To create a single project item, call Create Project Item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/portfolio/project-items/api/v1/bulk/portfolio/items/create
    ```

    ## Body Params

    <ParamField body="items" type="Item[]" required>

      Project items to create.

      Constraints: `minItems 1`, `maxItems 1000`.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ParamField>

    <ParamField body="returnFullEntity" type="boolean">

      Whether to return the created project items.

      Set to `true` to return the project items in the response.

      Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkCreateProjectItemResult[]">

      List of individual Bulk Create Project Items results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Information about the created project item.
          Including its ID, index in the bulk request and whether it was successfully created.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Item">

          Created project item. Only returned if `returnEntity` is set to `true` in the request.

          See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total number of successes and failures for Bulk Create Project Items.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Created | `wix.portfolio.project_items.v1.project_item_created` | Triggered when a project item is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/api/v1/bulk/portfolio/items/create' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "items": [
          {
            "projectId": "4c9e4567-e89b-12d3-a456-426614174002",
            "title": "Item Title 1",
            "description": "This is item description 1.",
            "sortOrder": 1.0,
            "type": "IMAGE",
            "image": {
              "imageInfo": {
                "url": "https://example.com/image1.jpg",
                "id": "8b7eef_afc3d989928f45198a16175e3c7aba3d"
              }
            },
            "link": {
              "text": "Visit Site 1",
              "url": "https://site-1-example.com",
              "target": "_blank"
            }
          },
          {
            "projectId": "ca3e4567-e89b-12d3-a756-426844174320",
            "title": "Item Title 2",
            "description": "This is item description 2.",
            "sortOrder": 2.0,
            "type": "VIDEO",
            "video": {
              "videoInfo": {
                "id": "4889bbcc7fb-66b77-888aaf-3c45227f",
                "url": "https://example.com/video.mp4"
              }
            },
            "link": {
              "text": "Visit Site 2",
              "url": "https://site-2-example.com",
              "target": "_self"
            }
          }
        ],
        "returnFullEntity": true
      }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "456e7890-e89b-12d3-a456-426614174001",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "456e7890-e89b-12d3-a456-426614174001",
              "projectId": "4c9e4567-e89b-12d3-a456-426614174002",
              "title": "Item Title 1",
              "description": "This is item description 1.",
              "sortOrder": 1.0,
              "type": "IMAGE",
              "image": {
                "imageInfo": {
                  "url": "https://example.com/image1.jpg",
                  "id": "8b7eef_afc3d989928f45198a16175e3c7aba3d"
                }
              },
              "createdDate": "2024-07-31T12:00:00Z",
              "updatedDate": "2024-07-31T12:00:00Z",
              "link": {
                "text": "Visit Site 1",
                "url": "https://example.com",
                "target": "_blank"
              }
            }
          },
          {
            "itemMetadata": {
              "id": "456e7890-e89b-12d3-a456-426614174002",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "id": "456e7890-e89b-12d3-a456-426614174002",
              "projectId": "ca3e4567-e89b-12d3-a756-426844174320",
              "title": "Item Title 2",
              "description": "This is item description 2.",
              "sortOrder": 2.0,
              "type": "VIDEO",
              "video": {
                "videoInfo": {
                  "id": "4889bbcc7fb-66b77-888aaf-3c45227f",
                  "url": "https://example.com/video.mp4"
                }
              },
              "createdDate": "2024-07-31T12:00:00Z",
              "updatedDate": "2024-07-31T12:00:00Z",
              "link": {
                "text": "Visit Site 2",
                "url": "https://example2.com",
                "target": "_self"
              }
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple project items.

    To create a single project item, call Create Project Item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { projectItems } from '@wix/portfolio';

    projectItems.bulkCreateProjectItems(options): Promise<BulkCreateProjectItemsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkCreateProjectItemsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="items" type="Item[]" required>

          Project items to create.

          Constraints: `minItems 1`, `maxItems 1000`.

          See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

        </ParamField>

        <ParamField body="returnFullEntity" type="boolean">

          Whether to return the created project items.

          Set to `true` to return the project items in the response.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkCreateProjectItemsResponse>`.

    <ResponseField name="results" type="BulkCreateProjectItemResult[]">

      List of individual Bulk Create Project Items results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Information about the created project item.
          Including its ID, index in the bulk request and whether it was successfully created.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Item">

          Created project item. Only returned if `returnEntity` is set to `true` in the request.

          See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total number of successes and failures for Bulk Create Project Items.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Created | `wix.portfolio.project_items.v1.project_item_created` | Triggered when a project item is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-project-items" id="ex-sdk-bulk-create-project-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-project-items" id="ex-sdk-bulk-create-project-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-create-project-items" id="ex-sdk-bulk-create-project-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-project-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-project-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateProjectItems</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateProjectItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateProjectItems (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-project-items-0"><span className="wix-picker-name">bulkCreateProjectItems</span></label>
    <label htmlFor="ex-sdk-bulk-create-project-items-1"><span className="wix-picker-name">bulkCreateProjectItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-create-project-items-2"><span className="wix-picker-name">bulkCreateProjectItems (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkCreateProjectItems
    import { projectItems } from '@wix/portfolio';

    async function bulkCreateProjectItems(options) {
      const response = await projectItems.bulkCreateProjectItems(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateProjectItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { projectItems } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { projectItems },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateProjectItems(options) {
      const response = await myWixClient.projectItems.bulkCreateProjectItems(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateProjectItems (with elevated permissions)
    import { projectItems } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myBulkCreateProjectItemsMethod(options) {
      const elevatedBulkCreateProjectItems = auth.elevate(projectItems.bulkCreateProjectItems);
      const response = await elevatedBulkCreateProjectItems(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
