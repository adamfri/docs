---
title: "Get Installed Apps"
description: "<blockquote class=\"note\" Note: Only logged-in Wix users or API key admins can use this API."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    Only logged-in [Wix users](https://dev.wix.com/docs/rest/articles/get-started/about-identities#wix-user) or [API key admins](https://dev.wix.com/docs/rest/articles/get-started/about-identities#api-key-admin) can use this API.

    </Note>

    Returns the apps that are installed on the site in context

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/apps-installer-service/v1/app-instances
    ```

    ## Response Object

    <ResponseField name="appInstances" type="AppInstance[]">

      The installed app instances

      See the [App Instance Object](/api-reference/business-management/app-installation/app-installation/app-instance-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request GET \
      	https://www.wixapis.com/apps-installer-service/v1/app-instances \
          -H 'Content-Type: application/json' \
          -H 'Authorization: <AUTH>'
      	-d '{}'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "appInstances":[
              {
                  "id":"07d49a20-531f-487a-8928-29426768dac7",
                  "appDefId":"96186799-d99a-4b96-adf9-7cb2c1cf1e79",
                  "version":"1",
                  "enabled":true
               }
           ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    Only logged-in [Wix users](https://dev.wix.com/docs/rest/articles/get-started/about-identities#wix-user) or [API key admins](https://dev.wix.com/docs/rest/articles/get-started/about-identities#api-key-admin) can use this API.

    </Note>

    Returns the apps that are installed on the site in context

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { appsInstaller } from '@wix/apps-installer';

    appsInstaller.getInstalledApps(): Promise
    ```

    ## Returns

    <ResponseField name="appInstances" type="AppInstance[]">

      The installed app instances

      See the [App Instance Object](/api-reference/business-management/app-installation/app-installation/app-instance-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-installed-apps" id="ex-sdk-get-installed-apps-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-installed-apps" id="ex-sdk-get-installed-apps-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-installed-apps" id="ex-sdk-get-installed-apps-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-installed-apps-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-installed-apps-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getInstalledApps</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getInstalledApps (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getInstalledApps (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-installed-apps-0"><span className="wix-picker-name">getInstalledApps</span></label>
    <label htmlFor="ex-sdk-get-installed-apps-1"><span className="wix-picker-name">getInstalledApps (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-installed-apps-2"><span className="wix-picker-name">getInstalledApps (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getInstalledApps
    import { appsInstaller } from '@wix/apps-installer';

    async function getInstalledApps() {
      const response = await appsInstaller.getInstalledApps();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getInstalledApps (self-hosted)
    import { createClient } from '@wix/sdk';
    import { appsInstaller } from '@wix/apps-installer';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { appsInstaller },
      // Include the auth strategy and host as relevant
    });

    async function getInstalledApps() {
      const response = await myWixClient.appsInstaller.getInstalledApps();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getInstalledApps (with elevated permissions)
    import { appsInstaller } from '@wix/apps-installer';
    import { auth } from '@wix/essentials';

    async function myGetInstalledAppsMethod() {
      const elevatedGetInstalledApps = auth.elevate(appsInstaller.getInstalledApps);
      const response = await elevatedGetInstalledApps();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
