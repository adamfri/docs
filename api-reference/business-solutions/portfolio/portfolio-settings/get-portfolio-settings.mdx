---
title: "Get Portfolio Settings"
description: "Retrieves a site's portfolio settings."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a site's portfolio settings.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/portfolio/v1/settings
    ```

    ## Response Object

    <ResponseField name="portfolioSettings" type="PortfolioSettings">

      Portfolio settings.

      See the [Portfolio Settings Object](/api-reference/business-solutions/portfolio/portfolio-settings/portfolio-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/portfolio/v1/settings' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "portfolioSettings": {
          "revision": 1,
          "projectItemSettings": {
            "addItemDirection": "GALLERY_END",
            "defaultItemName": "FILE_NAME"
          },
          "mediaSettings": {
            "folderId": "5f6197ab-a4b4-426d-b1fb-02603e24c0be"
          },
          "createdDate": "2024-07-31T12:00:00Z",
          "updatedDate": "2024-07-31T12:00:00Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a site's portfolio settings.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { portfolioSettings } from '@wix/portfolio';

    portfolioSettings.getPortfolioSettings(): Promise<GetPortfolioSettingsResponse>
    ```

    ## Returns

    Returns `Promise<GetPortfolioSettingsResponse>`.

    <ResponseField name="portfolioSettings" type="PortfolioSettings">

      Portfolio settings.

      See the [Portfolio Settings Object](/api-reference/business-solutions/portfolio/portfolio-settings/portfolio-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-portfolio-settings" id="ex-sdk-get-portfolio-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-portfolio-settings" id="ex-sdk-get-portfolio-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-portfolio-settings" id="ex-sdk-get-portfolio-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-portfolio-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-portfolio-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getPortfolioSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPortfolioSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPortfolioSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-portfolio-settings-0"><span className="wix-picker-name">getPortfolioSettings</span></label>
    <label htmlFor="ex-sdk-get-portfolio-settings-1"><span className="wix-picker-name">getPortfolioSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-portfolio-settings-2"><span className="wix-picker-name">getPortfolioSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getPortfolioSettings
    import { portfolioSettings } from '@wix/portfolio';

    async function getPortfolioSettings() {
      const response = await portfolioSettings.getPortfolioSettings();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPortfolioSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { portfolioSettings } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { portfolioSettings },
      // Include the auth strategy and host as relevant
    });

    async function getPortfolioSettings() {
      const response = await myWixClient.portfolioSettings.getPortfolioSettings();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPortfolioSettings (with elevated permissions)
    import { portfolioSettings } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myGetPortfolioSettingsMethod() {
      const elevatedGetPortfolioSettings = auth.elevate(portfolioSettings.getPortfolioSettings);
      const response = await elevatedGetPortfolioSettings();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
