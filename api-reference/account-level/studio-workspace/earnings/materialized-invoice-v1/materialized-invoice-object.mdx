---
title: "Materialized Invoice Object"
description: "A materialized invoice surfaces the invoices and payments a partner received from the payment handler (Tipalti), aggregated for the partner to read."
icon: "cube"
---

A materialized invoice surfaces the invoices and payments a partner received from the payment handler (Tipalti), aggregated for the partner to read. The underlying invoices are grouped into one materialized invoice per earnings month per payment status, and each total is broken down by revenue stream (premium, GPV, marketplace, and app market). Records are created automatically by Wix as the partner is paid, so partners can't create or update them. All amounts are in USD.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Materialized invoice ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="earningsMonth" type="datetime">

      Earnings month the invoice covers.

      This is a month bucket, not an exact instant. It's normalized to the start of the day on the first day of the month. The underlying invoices are grouped into one materialized invoice per earnings month per payment status.

    </ResponseField>

    <ResponseField name="invoicesRefCodes" type="string[]">

      Reference codes of the underlying invoices included in this aggregation.

      A reference code is the identifier assigned by the payment handler (Tipalti) to each invoice. It's the same value that a premium, GPV, or marketplace payout carries in its `invoiceId`, so you can filter on this field to find the invoice a payout was included in.

      Constraints: `minLength 1`, `maxLength 16`, `maxItems 9999`.

    </ResponseField>

    <ResponseField name="amount" type="number">

      Total amount across the grouped invoices, in USD. Equals the sum of the `revenueStreamsAggregation` sub-totals.

    </ResponseField>

    <ResponseField name="status" type="string">

      Payment status of the grouped invoices for the earnings month.

      **Supported values:**

      - `UNPAID` — The invoice is scheduled for payment but not yet paid.
      - `PAID` — The invoice has been paid.
      - `BELOW_THRESHOLD` — The total unpaid amount across all of the partner's unpaid invoices is below the payout threshold, so payment isn't yet scheduled. Payment is scheduled once the threshold is reached.

    </ResponseField>

    <ResponseField name="revenueStreamsAggregation" type="RevenueStreamsAggregation">

      Invoice total broken down into its premium, GPV, marketplace, and app market revenue-stream sub-totals.

      <Expandable title="revenueStreamsAggregation properties">

        <ResponseField name="premium" type="number">

          Total premium earnings from premium subscriptions the partner sold, in USD.

        </ResponseField>

        <ResponseField name="gpv" type="number">

          Total GPV earnings, in USD. GPV (gross payment volume) is the payment volume processed across the sites the partner manages.

        </ResponseField>

        <ResponseField name="marketplace" type="number">

          Total marketplace earnings from marketplace transactions, such as template sales, in USD.

        </ResponseField>

        <ResponseField name="appMarket" type="number">

          Total app market earnings, in USD. This is a fourth revenue stream alongside premium, GPV, and marketplace, so include it when you add the sub-totals up to the invoice's `amount`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "materializedInvoice": {
          "id": "f6a7b8c9-d0e1-4234-ef01-67890abcdef0",
          "earningsMonth": "2024-03-01T00:00:00.000Z",
          "invoicesRefCodes": ["TIP-2024-03-001", "TIP-2024-03-002"],
          "amount": 47.2,
          "status": "PAID",
          "revenueStreamsAggregation": {
            "premium": 38.4,
            "gpv": 3.2,
            "marketplace": 5.6,
            "appMarket": 0.0
          }
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Materialized invoice ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="earningsMonth" type="datetime">

      Earnings month the invoice covers.

      This is a month bucket, not an exact instant. It's normalized to the start of the day on the first day of the month. The underlying invoices are grouped into one materialized invoice per earnings month per payment status.

    </ResponseField>

    <ResponseField name="invoicesRefCodes" type="string[]">

      Reference codes of the underlying invoices included in this aggregation.

      A reference code is the identifier assigned by the payment handler (Tipalti) to each invoice. It's the same value that a premium, GPV, or marketplace payout carries in its `invoiceId`, so you can filter on this field to find the invoice a payout was included in.

      Constraints: `minLength 1`, `maxLength 16`, `maxItems 9999`.

    </ResponseField>

    <ResponseField name="amount" type="number">

      Total amount across the grouped invoices, in USD. Equals the sum of the `revenueStreamsAggregation` sub-totals.

    </ResponseField>

    <ResponseField name="status" type="string">

      Payment status of the grouped invoices for the earnings month.

      **Supported values:**

      - `UNPAID` — The invoice is scheduled for payment but not yet paid.
      - `PAID` — The invoice has been paid.
      - `BELOW_THRESHOLD` — The total unpaid amount across all of the partner's unpaid invoices is below the payout threshold, so payment isn't yet scheduled. Payment is scheduled once the threshold is reached.

    </ResponseField>

    <ResponseField name="revenueStreamsAggregation" type="RevenueStreamsAggregation">

      Invoice total broken down into its premium, GPV, marketplace, and app market revenue-stream sub-totals.

      <Expandable title="revenueStreamsAggregation properties">

        <ResponseField name="premium" type="number">

          Total premium earnings from premium subscriptions the partner sold, in USD.

        </ResponseField>

        <ResponseField name="gpv" type="number">

          Total GPV earnings, in USD. GPV (gross payment volume) is the payment volume processed across the sites the partner manages.

        </ResponseField>

        <ResponseField name="marketplace" type="number">

          Total marketplace earnings from marketplace transactions, such as template sales, in USD.

        </ResponseField>

        <ResponseField name="appMarket" type="number">

          Total app market earnings, in USD. This is a fourth revenue stream alongside premium, GPV, and marketplace, so include it when you add the sub-totals up to the invoice's `amount`.

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
