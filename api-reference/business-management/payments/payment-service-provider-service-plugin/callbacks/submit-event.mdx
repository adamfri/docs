---
title: "Submit Event"
description: "This Wix API is used by a Payment Service Provider (PSP) to send webhooks about payment and refund states to Wix."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    This Wix API is used by a Payment Service Provider (PSP) to send webhooks about payment and refund states to Wix.

    Calls to this endpoint must include a `User-Agent` header with the name of the PSP and the integration version in this format: `{PSP}/{version}`.
    PSP's create their own version numbers.

    > You cannot try out this endpoint because an `Authorization` header value has to be obtained
    > with the OAuth 2.0 client credentials flow for a specific scope.
    > So please ignore the **Authorization** section below as well as the **Try It Out** button.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="CASHIER.GET_ACCESS">CASHIER.GET_ACCESS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/payments/v1/provider-platform-events
    ```

    ## Body Params

    <ParamField body="event" type="ProviderPlatformEvent" required>

      Event data.

      <Expandable title="event properties">

        <ParamField body="pluginId" type="string" deprecated>

          This field is deprecated.

          **Deprecated.**

        </ParamField>

        <ParamField body="siteId" type="string">

          Wix site ID.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="resource" type="resource">

          resource

          <Expandable title="resource properties">

            <ParamField body="refund" type="RefundEvent">

              Refund event data.

              _Nested structure omitted for brevity — see the `RefundEvent` object._

            </ParamField>

            <ParamField body="transaction" type="TransactionEvent">

              Transaction event data.

              _Nested structure omitted for brevity — see the `TransactionEvent` object._

            </ParamField>

            <ParamField body="capture" type="CaptureEvent">

              Capture event data.

              _Nested structure omitted for brevity — see the `CaptureEvent` object._

            </ParamField>

            <ParamField body="void" type="VoidEvent">

              Void event data.

              _Nested structure omitted for brevity — see the `VoidEvent` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    Returns `SubmitEventResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-2" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-3" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-4" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-5" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-6" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-7" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-8" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-submit-event" id="ex-rest-submit-event-9" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-submit-event-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-submit-event-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Payment Captured</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Payment Authorized</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Refund succeeded (initiated by PSP)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Refund failed (initiated by Wix)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Refund succeeded (initiated by Wix)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Payment canceled</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Payment pending</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Payment declined</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Payment Voided</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Payment approved</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-submit-event-0"><span className="wix-picker-name">Payment Captured</span></label>
    <label htmlFor="ex-rest-submit-event-1"><span className="wix-picker-name">Payment Authorized</span></label>
    <label htmlFor="ex-rest-submit-event-2"><span className="wix-picker-name">Refund succeeded (initiated by PSP)</span></label>
    <label htmlFor="ex-rest-submit-event-3"><span className="wix-picker-name">Refund failed (initiated by Wix)</span></label>
    <label htmlFor="ex-rest-submit-event-4"><span className="wix-picker-name">Refund succeeded (initiated by Wix)</span></label>
    <label htmlFor="ex-rest-submit-event-5"><span className="wix-picker-name">Payment canceled</span></label>
    <label htmlFor="ex-rest-submit-event-6"><span className="wix-picker-name">Payment pending</span></label>
    <label htmlFor="ex-rest-submit-event-7"><span className="wix-picker-name">Payment declined</span></label>
    <label htmlFor="ex-rest-submit-event-8"><span className="wix-picker-name">Payment Voided</span></label>
    <label htmlFor="ex-rest-submit-event-9"><span className="wix-picker-name">Payment approved</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/payments/v1/provider-platform-events' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -H 'User-Agent: banana-pay/1.0.0' \
      -d '{
        "event": {
          "capture": {
            "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
            "reasonCode": 0,
            "amount": "1540"
          }
        }
      }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/payments/v1/provider-platform-events' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -H 'User-Agent: banana-pay/1.0.0' \
      -d '{
        "event": {
          "transaction": {
            "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
            "pluginTransactionId": "e89b-12d3-a456-42665"
            "reasonCode": 5010
          }
        }
      }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/payments/v1/provider-platform-events \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -H 'User-Agent: banana-pay/1.0.0' \
       -d '{
             "event": {
               "refund": {
                 "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
                 "pluginRefundId": "ce590272-87bf-428f-943a-0ff594059712",
                 "amount": "1000"
               }
             }
           }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/payments/v1/provider-platform-events \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -H 'User-Agent: banana-pay/1.0.0' \
       -d '{
             "event": {
               "refund": {
                 "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
                 "pluginRefundId": "ce590272-87bf-428f-943a-0ff594059712",
                 "amount": "1000",
                 "wixRefundId": "fcd2655f-e261-4c5b-8129-72a241461a27",
                 "reasonCode": "3025",
                 "errorCode": "INSUFFICIENT_FUNDS_FOR_REFUND",
                 "errorMessage": "Insufficient funds for refund."
               }
             }
           }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/payments/v1/provider-platform-events \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -H 'User-Agent: banana-pay/1.0.0' \
       -d '{
             "event": {
               "refund": {
                 "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
                 "pluginRefundId": "ce590272-87bf-428f-943a-0ff594059712",
                 "amount": "1000",
                 "wixRefundId": "fcd2655f-e261-4c5b-8129-72a241461a27"
               }
             }
           }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/payments/v1/provider-platform-events' \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -H 'User-Agent: banana-pay/1.0.0' \
       -d '{
             "event": {
               "transaction": {
                 "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
                 "pluginTransactionId": "e89b-12d3-a456-42665",
                 "reasonCode": 3030,
                 "errorCode": "BUYER_CANCELED",
                 "errorMessage": "Buyer canceled"
               }
             } 
           }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/payments/v1/provider-platform-events' \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -H 'User-Agent: banana-pay/1.0.0' \
       -d '{
             "event": {
               "transaction": {
                 "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
                 "pluginTransactionId": "e89b-12d3-a456-42665",
                 "reasonCode": 5005
               }
             }
           }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/payments/v1/provider-platform-events' \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -H 'User-Agent: banana-pay/1.0.0' \
       -d '{
             "event": {
               "transaction": {
                 "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
                 "pluginTransactionId": "e89b-12d3-a456-42665",
                 "reasonCode": 3012,
                 "errorCode": "INSUFFICIENT_FUNDS",
                 "errorMessage": "Insufficient funds"
               }
             }
           }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/payments/v1/provider-platform-events' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -H 'User-Agent: banana-pay/1.0.0' \
      -d '{
        "event": {
          "void": {
            "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
            "reasonCode": 0,
            "amount": "1540"
          }
        }
      }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/payments/v1/provider-platform-events' \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -H 'User-Agent: banana-pay/1.0.0' \
       -d '{
             "event": {
               "transaction": {
                 "wixTransactionId": "a15a3ee3-22d3-4a3f-920e-2186e13a19d1",
                 "pluginTransactionId": "e89b-12d3-a456-42665"
               }
             }
           }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    This Wix API is used by a Payment Service Provider (PSP) to send webhooks about payment and refund states to Wix.

    Calls to this endpoint must include a `User-Agent` header with the name of the PSP and the integration version in this format: `{PSP}/{version}`.
    PSP's create their own version numbers.

    > You cannot try out this endpoint because an `Authorization` header value has to be obtained
    > with the OAuth 2.0 client credentials flow for a specific scope.
    > So please ignore the **Authorization** section below as well as the **Try It Out** button.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="CASHIER.GET_ACCESS">CASHIER.GET_ACCESS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { pspCallbacks } from '@wix/payments';

    pspCallbacks.submitEvent(event): Promise
    ```

    ## Method Parameters

    <ParamField body="event" type="ProviderPlatformEvent" required>

      Event data.

      <Expandable title="event properties">

        <ParamField body="pluginId" type="string" deprecated>

          This field is deprecated.

          **Deprecated.**

        </ParamField>

        <ParamField body="siteId" type="string">

          Wix site ID.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="resource" type="resource">

          resource

          <Expandable title="resource properties">

            <ParamField body="refund" type="RefundEvent">

              Refund event data.

              _Nested structure omitted for brevity — see the `RefundEvent` object._

            </ParamField>

            <ParamField body="transaction" type="TransactionEvent">

              Transaction event data.

              _Nested structure omitted for brevity — see the `TransactionEvent` object._

            </ParamField>

            <ParamField body="capture" type="CaptureEvent">

              Capture event data.

              _Nested structure omitted for brevity — see the `CaptureEvent` object._

            </ParamField>

            <ParamField body="void" type="VoidEvent">

              Void event data.

              _Nested structure omitted for brevity — see the `VoidEvent` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `SubmitEventResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-submit-event" id="ex-sdk-submit-event-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-submit-event" id="ex-sdk-submit-event-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-submit-event" id="ex-sdk-submit-event-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-submit-event-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-submit-event-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">submitEvent</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">submitEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">submitEvent (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-submit-event-0"><span className="wix-picker-name">submitEvent</span></label>
    <label htmlFor="ex-sdk-submit-event-1"><span className="wix-picker-name">submitEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-submit-event-2"><span className="wix-picker-name">submitEvent (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript submitEvent
    import { pspCallbacks } from '@wix/payments';

    async function submitEvent(event) {
      const response = await pspCallbacks.submitEvent(event);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript submitEvent (self-hosted)
    import { createClient } from '@wix/sdk';
    import { pspCallbacks } from '@wix/payments';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { pspCallbacks },
      // Include the auth strategy and host as relevant
    });

    async function submitEvent(event) {
      const response = await myWixClient.pspCallbacks.submitEvent(event);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript submitEvent (with elevated permissions)
    import { pspCallbacks } from '@wix/payments';
    import { auth } from '@wix/essentials';

    async function mySubmitEventMethod(event) {
      const elevatedSubmitEvent = auth.elevate(pspCallbacks.submitEvent);
      const response = await elevatedSubmitEvent(event);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
