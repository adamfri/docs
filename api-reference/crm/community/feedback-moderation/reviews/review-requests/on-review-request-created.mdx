---
title: "onReviewRequestCreated"
description: "Triggered when a review request is created."
icon: "bolt"
---

Triggered when a review request is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reviews.v2.review_request_created` |
| Package | `@wix/reviews` |
| Permissions | `Manage Review Requests` |

## Examples

<CodeGroup>

  ```javascript onReviewRequestCreated
  import { reviewRequests } from '@wix/reviews';

  reviewRequests.onReviewRequestCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReviewRequestCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reviewRequests } from '@wix/reviews';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reviewRequests,
      },
  });

  wixClient.reviewRequests.onReviewRequestCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
