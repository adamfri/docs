---
title: "onReceiptPresetUpdated"
description: "Triggered when a receipt preset is updated."
icon: "bolt"
---

Triggered when a receipt preset is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.receipts.presets.v1.receipt_preset_updated` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Receipts` |

## Examples

<CodeGroup>

  ```javascript onReceiptPresetUpdated
  import { receiptPresets } from '@wix/get-paid';

  receiptPresets.onReceiptPresetUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReceiptPresetUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { receiptPresets } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        receiptPresets,
      },
  });

  wixClient.receiptPresets.onReceiptPresetUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
