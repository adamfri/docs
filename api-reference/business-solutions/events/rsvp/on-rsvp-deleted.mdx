---
title: "onRsvpDeleted"
description: "The onRsvpDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.rsvp.events.RsvpDeleted` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onRsvpDeleted
  import { rsvp } from '@wix/events';

  rsvp.onRsvpDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onRsvpDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { rsvp } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        rsvp,
      },
  });

  wixClient.rsvp.onRsvpDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
