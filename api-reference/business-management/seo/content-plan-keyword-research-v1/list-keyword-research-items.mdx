---
title: "List Keyword Research Items"
description: "Retrieves the keyword research behind the site's current content plan."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the keyword research behind the site's current content plan.

    Returns the research produced by the last completed generation. Research
    from a generation that's still running, or that failed, isn't readable.
    Poll Get Content Plan Flow until its status is `SUCCESS`, then call this
    method.

    The result is scoped to the site rather than to a flow. This method
    always returns the site's most recent research and can't target an
    earlier one. If another generation finishes between your trigger and this
    read, you receive the newer research instead. Compare `keywordResearchId`
    across reads to detect that the research was replaced.

    Omit `paging` to return every item in a single response. When `paging` is
    present, `limit` accepts 1 to 100 and defaults to 50.

    A site with no keyword research returns a not found error rather than an
    empty list.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-SEO">View SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/promote/seo/v1/content-plan-keyword-research-items
    ```

    ## Query Params

    <ParamField query="contentPlanFlowId" type="string">

      Reserved for future use.

      This method currently resolves the site's most recent keyword research,
      and this field is ignored.

    </ParamField>

    <ParamField query="paging" type="CursorPaging">

      Cursor paging. Omit for the first page.

      Omit this object entirely to return every item in a single response.

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Max items per page. Default 50, max 100.

        </ParamField>

        <ParamField query="cursor" type="string">

          Cursor from a previous response. Omit for the first page.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="keywordResearchItems" type="KeywordResearch[]">

      Retrieved keyword research items.

      Constraints: `maxItems 1000`.

      See the [Content Plan Keyword Research Object](/api-reference/business-management/seo/content-plan-keyword-research-v1/content-plan-keyword-research-object) for this object's fields.

    </ResponseField>

    <ResponseField name="keywordResearchId" type="string">

      ID of the keyword research these items belong to.

      Send this ID when you update an item. Compare it across reads to detect
      that a newer generation replaced the research.

    </ResponseField>

    <ResponseField name="createdByUser" type="boolean">

      Whether this research has been edited.

      `false` means the research is exactly as the content plan generated it,
      so the next edit copies it onto a new `keywordResearchId`.

    </ResponseField>

    <ResponseField name="mainKeywordMap" type="object">

      ID of the main keyword for each page, keyed by page ID.

      Built from the site's full item set, so on a paged response it can name
      items that aren't in `keywordResearchItems`.

      Constraints: `format map`.

      <Expandable title="mainKeywordMap properties">

        <ResponseField name="key" type="string" />

        <ResponseField name="value" type="string" />

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Items returned in this page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors for next/previous page.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor for the next page.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor for the previous page.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Returns the site's most recent keyword research. Omit paging to receive every item in one response.

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/seo-content-plan-service/v1/content-plan-keyword-research-items' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "keywordResearchItems": [
          {
            "id": "8c14e7d0-2a63-4b9f-91d5-7ae0c3f28b64",
            "keyword": "best coffee beans home brewing",
            "pageId": "c1f83b57",
            "itemType": "STATIC_PAGE",
            "pageUrl": "https://www.example.com/shop/coffee-beans",
            "competition": 42,
            "searchVolume": 2900,
            "pageDeleted": false,
            "primary": true,
            "clusterName": "coffee beans"
          },
          {
            "id": "b70d5a92-31ef-4c08-8a6d-45f19be7c203",
            "keyword": "how to store coffee beans",
            "pageId": "c1f83b57",
            "itemType": "STATIC_PAGE",
            "pageUrl": "https://www.example.com/shop/coffee-beans",
            "competition": 18,
            "searchVolume": 1300,
            "pageDeleted": false,
            "primary": false,
            "clusterName": "coffee beans"
          }
        ],
        "keywordResearchId": "3f9c2b71-4d8e-4a15-9c63-b0e7d21a5f84",
        "createdByUser": false,
        "mainKeywordMap": {
          "c1f83b57": "8c14e7d0-2a63-4b9f-91d5-7ae0c3f28b64"
        },
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the keyword research behind the site's current content plan.

    Returns the research produced by the last completed generation. Research
    from a generation that's still running, or that failed, isn't readable.
    Poll Get Content Plan Flow until its status is `SUCCESS`, then call this
    method.

    The result is scoped to the site rather than to a flow. This method
    always returns the site's most recent research and can't target an
    earlier one. If another generation finishes between your trigger and this
    read, you receive the newer research instead. Compare `keywordResearchId`
    across reads to detect that the research was replaced.

    Omit `paging` to return every item in a single response. When `paging` is
    present, `limit` accepts 1 to 100 and defaults to 50.

    A site with no keyword research returns a not found error rather than an
    empty list.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-SEO">View SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { keywordResearch } from '@wix/seo';

    keywordResearch.listKeywordResearchItems(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="ListKeywordResearchItemsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="contentPlanFlowId" type="string">

          Reserved for future use.

          This method currently resolves the site's most recent keyword research,
          and this field is ignored.

        </ParamField>

        <ParamField body="paging" type="CursorPaging">

          Cursor paging. Omit for the first page.

          Omit this object entirely to return every item in a single response.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Max items per page. Default 50, max 100.

            </ParamField>

            <ParamField body="cursor" type="string">

              Cursor from a previous response. Omit for the first page.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="keywordResearchItems" type="KeywordResearch[]">

      Retrieved keyword research items.

      Constraints: `maxItems 1000`.

      See the [Content Plan Keyword Research Object](/api-reference/business-management/seo/content-plan-keyword-research-v1/content-plan-keyword-research-object) for this object's fields.

    </ResponseField>

    <ResponseField name="keywordResearchId" type="string">

      ID of the keyword research these items belong to.

      Send this ID when you update an item. Compare it across reads to detect
      that a newer generation replaced the research.

    </ResponseField>

    <ResponseField name="createdByUser" type="boolean">

      Whether this research has been edited.

      `false` means the research is exactly as the content plan generated it,
      so the next edit copies it onto a new `keywordResearchId`.

    </ResponseField>

    <ResponseField name="mainKeywordMap" type="object">

      ID of the main keyword for each page, keyed by page ID.

      Built from the site's full item set, so on a paged response it can name
      items that aren't in `keywordResearchItems`.

      Constraints: `format map`.

      <Expandable title="mainKeywordMap properties">

        <ResponseField name="key" type="string" />

        <ResponseField name="value" type="string" />

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Items returned in this page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors for next/previous page.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor for the next page.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor for the previous page.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-keyword-research-items" id="ex-sdk-list-keyword-research-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-keyword-research-items" id="ex-sdk-list-keyword-research-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-keyword-research-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-keyword-research-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Read the site's current keyword research</span><span className="wix-picker-desc">Returns the keywords behind the site's most recent successful content plan generation. Omit paging to get every item in one response</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listKeywordResearchItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-keyword-research-items-0"><span className="wix-picker-name">Read the site's current keyword research</span><span className="wix-picker-desc">Returns the keywords behind the site's most recent successful content plan generation. Omit paging to get every item in one response</span></label>
    <label htmlFor="ex-sdk-list-keyword-research-items-1"><span className="wix-picker-name">listKeywordResearchItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Read the site's current keyword research
    import { keywordResearch } from "@wix/seo";

    async function listKeywordResearchItems() {
      const response = await keywordResearch.listKeywordResearchItems({
        paging: { limit: 10 },
      });
    }

    /* Promise resolves to:
     * {
     *   "keywordResearchItems": [
     *     {
     *       "_id": "c5f9e1a4-7b62-4d18-9a35-1e8f4c2d6b70",
     *       "keyword": "handmade ceramic mugs",
     *       "pageId": "a1b2c3d4-e5f6-4890-abcd-ef1234567890",
     *       "itemType": "STATIC_PAGE",
     *       "pageUrl": "https://example.com/shop",
     *       "competition": 34,
     *       "searchVolume": 2900,
     *       "pageDeleted": false,
     *       "primary": true,
     *       "clusterName": "ceramic drinkware"
     *     },
     *     {
     *       "_id": "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
     *       "keyword": "glazed stoneware mug",
     *       "pageId": "a1b2c3d4-e5f6-4890-abcd-ef1234567890",
     *       "itemType": "STATIC_PAGE",
     *       "pageUrl": "https://example.com/shop",
     *       "competition": 21,
     *       "searchVolume": 480,
     *       "pageDeleted": false,
     *       "primary": false,
     *       "clusterName": "ceramic drinkware"
     *     }
     *   ],
     *   "keywordResearchId": "8f3b6d2e-4c17-4a90-b5e3-9d70c1af2b48",
     *   "createdByUser": false,
     *   "mainKeywordMap": {
     *     "a1b2c3d4-e5f6-4890-abcd-ef1234567890": "c5f9e1a4-7b62-4d18-9a35-1e8f4c2d6b70"
     *   },
     *   "pagingMetadata": {
     *     "count": 2,
     *     "cursors": {},
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listKeywordResearchItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { keywordResearch } from '@wix/seo';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { keywordResearch },
      // Include the auth strategy and host as relevant
    });

    async function listKeywordResearchItems(options) {
      const response = await myWixClient.keywordResearch.listKeywordResearchItems(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
