---
title: "onGroupDeleted"
description: "Triggered when a group is deleted."
icon: "bolt"
---

Triggered when a group is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_deleted` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupDeleted
  import { groups } from '@wix/groups';

  groups.onGroupDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { groups } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        groups,
      },
  });

  wixClient.groups.onGroupDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
