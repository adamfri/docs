---
title: "List Project Items"
description: "Retrieves a list of all project items in the specified project."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of all project items in the specified project.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/portfolio/v1/projectItems/{projectId}/items
    ```

    ## Path Params

    <ParamField path="projectId" type="string" required>

      Project ID.

    </ParamField>

    ## Query Params

    <ParamField query="paging" type="Paging">

      Maximum number of items to return in the results.

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of items to load.

        </ParamField>

        <ParamField query="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="items" type="Item[]">

      Project items.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadataV2" type="PagingMetadataV2" deprecated>

      pagingMetadataV2

      **Deprecated.** Removal: 2025-05-25.

      <Expandable title="pagingMetadataV2 properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/portfolio/v1/projectItems/123e4567-e89b-12d3-a456-426614174000/items' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      ```

      ```json Response
      {
        "items": [
          {
            "id": "456e7890-e89b-12d3-a456-426614174001",
            "projectId": "123e4567-e89b-12d3-a456-426614174000",
            "title": "Item Title 1",
            "description": "This is item description 1.",
            "sortOrder": 1.0,
            "type": "IMAGE",
            "image": {
              "imageInfo": {
                "url": "https://example.com/image1.jpg",
                "id": "4889bbcc7fb-66b77-888aaf-3c45227f"
              }
            },
            "createdDate": "2024-07-31T12:00:00Z",
            "updatedDate": "2024-07-31T12:00:00Z",
            "link": {
              "text": "Visit Site 1",
              "url": "https://example.com",
              "target": "_blank"
            }
          },
          {
            "id": "456e7890-e89b-12d3-a456-426614174002",
            "projectId": "123e4567-e89b-12d3-a456-426614174000",
            "title": "Item Title 2",
            "description": "This is item description 2.",
            "sortOrder": 2.0,
            "type": "VIDEO",
            "video": {
              "videoInfo": {
                "url": "https://example.com/video3.mp4",
                "id": "4ee89bbcc7fb-69b77-888aaf-3c4522eec"
              }
            },
            "createdDate": "2024-07-31T12:10:00Z",
            "updatedDate": "2024-07-31T12:10:00Z",
            "link": {
              "text": "Visit Site 2",
              "url": "https://example2.com",
              "target": "_self"
            }
          }
        ],
        "metadata": {
          "count": 2,
          "offset": 0,
          "total": 2,
          "tooManyToCount": false,
          "cursors": {
            "next": "next_cursor",
            "prev": "prev_cursor"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of all project items in the specified project.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { projectItems } from '@wix/portfolio';

    projectItems.listProjectItems(projectId, options): Promise<ListProjectItemsResponse>
    ```

    ## Method Parameters

    <ParamField body="projectId" type="string" required>

      Project ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="ListProjectItemsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="Paging">

          Maximum number of items to return in the results.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListProjectItemsResponse>`.

    <ResponseField name="items" type="Item[]">

      Project items.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadataV2" type="PagingMetadataV2" deprecated>

      pagingMetadataV2

      **Deprecated.** Removal: 2025-05-25.

      <Expandable title="pagingMetadataV2 properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-project-items" id="ex-sdk-list-project-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-project-items" id="ex-sdk-list-project-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-project-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-project-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listProjectItems</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listProjectItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-project-items-0"><span className="wix-picker-name">listProjectItems</span></label>
    <label htmlFor="ex-sdk-list-project-items-1"><span className="wix-picker-name">listProjectItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listProjectItems
    import { projectItems } from '@wix/portfolio';

    async function listProjectItems(projectId,options) {
      const response = await projectItems.listProjectItems(projectId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listProjectItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { projectItems } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { projectItems },
      // Include the auth strategy and host as relevant
    });

    async function listProjectItems(projectId,options) {
      const response = await myWixClient.projectItems.listProjectItems(projectId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
