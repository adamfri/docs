---
title: "onCommentHidden"
description: "Triggered when a comment is hidden."
icon: "bolt"
---

Triggered when a comment is hidden.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v2.comment_comment_hidden` |
| Package | `@wix/comments` |
| Permissions | `Read Comments`, `Manage Comments` |

## Examples

<CodeGroup>

  ```javascript onCommentHidden
  import { comments } from '@wix/comments';

  comments.onCommentHidden((event) => {
      // handle your event here
  });
  ```

  ```javascript onCommentHidden (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { comments } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        comments,
      },
  });

  wixClient.comments.onCommentHidden((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
