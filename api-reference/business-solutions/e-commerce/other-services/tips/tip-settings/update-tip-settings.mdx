---
title: "Update Tip Settings"
description: "Updates a tipSettings object."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a `tipSettings` object.

    You can't update the `default` field.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/tip-settings/v1/tip-settings
    ```

    ## Body Params

    <ParamField body="tipSettings" type="TipSettings" required>

      Tip settings to update.

      See the [Tip Settings Object](/api-reference/business-solutions/e-commerce/other-services/tips/tip-settings/tip-settings-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="tipSettings" type="TipSettings">

      Updated tip settings.

      See the [Tip Settings Object](/api-reference/business-solutions/e-commerce/other-services/tips/tip-settings/tip-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `DEFAULT_SETTINGS_ALREADY_EXISTS` | Default `tipSettings` object already exists. |
    | `428` | `FAILED_PRECONDITION` | `CANNOT_HAVE_MORE_THAN_ONE_DEFAULT_PRESET` | Only 1 preset can be set as default. |
    | `428` | `FAILED_PRECONDITION` | `PRESET_PERCENTAGE_TOO_HIGH` | Preset percentage exceeds the maximum allowed value. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/tip-settings/v1/tip-settings' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "tipSettings": {
          "id": "1338729c-7255-48f1-a66e-f6a8e4b6f9f3",
          "presets": [
            {
              "value": 5,
              "default": true
            },
            {
              "value": 10
            },
            {
              "value": 15
            }
          ],
          "tipType": "PERCENTAGE",
          "allowCustomerTip": true,
          "allowBusinessTipAtPayment": true,
          "allowCustomAmount": true,
          "staffDistributionMethod": "PROPORTIONAL",
          "default": false,
          "revision": "1"
        },
        "field_mask": {
          "paths": [
            "allowCustomerTip"
          ]
        }
      }'
      ```

      ```json Response
      {
        "tipSettings": {
          "id": "1338729c-7255-48f1-a66e-f6a8e4b6f9f3",
          "tipType": "PERCENTAGE",
          "presets": [
            {
              "value": 5,
              "default": true
            },
            {
              "value": 10,
              "default": null
            },
            {
              "value": 15,
              "default": null
            }
          ],
          "allowCustomerTip": true,
          "allowBusinessTipAtPayment": true,
          "allowCustomAmount": true,
          "staffDistributionMethod": "PROPORTIONAL",
          "revision": "2",
          "createdDate": "2024-09-23T08:16:35.039Z",
          "updatedDate": "2024-09-23T08:16:35.039Z",
          "default": false,
          "appId": null,
          "extendedFields": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a `tipSettings` object.

    You can't update the `default` field.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tipSettings } from '@wix/ecom';

    tipSettings.updateTipSettings(tipSettings): Promise<TipSettings>
    ```

    ## Method Parameters

    <ParamField body="tipSettings" type="TipSettings" required>

      Tip settings to update.

      See the [Tip Settings Object](/api-reference/business-solutions/e-commerce/other-services/tips/tip-settings/tip-settings-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<TipSettings>`.

    See the [Tip Settings Object](/api-reference/business-solutions/e-commerce/other-services/tips/tip-settings/tip-settings-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `DEFAULT_SETTINGS_ALREADY_EXISTS` | Default `tipSettings` object already exists. |
    | `428` | `FAILED_PRECONDITION` | `CANNOT_HAVE_MORE_THAN_ONE_DEFAULT_PRESET` | Only 1 preset can be set as default. |
    | `428` | `FAILED_PRECONDITION` | `PRESET_PERCENTAGE_TOO_HIGH` | Preset percentage exceeds the maximum allowed value. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tip Settings Updated | `wix.tips.settings.v1.tip_settings_updated` | Triggered when a `tipSettings` object is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-tip-settings" id="ex-sdk-update-tip-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-tip-settings" id="ex-sdk-update-tip-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-tip-settings" id="ex-sdk-update-tip-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-tip-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-tip-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateTipSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateTipSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateTipSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-tip-settings-0"><span className="wix-picker-name">updateTipSettings</span></label>
    <label htmlFor="ex-sdk-update-tip-settings-1"><span className="wix-picker-name">updateTipSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-tip-settings-2"><span className="wix-picker-name">updateTipSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateTipSettings
    import { tipSettings } from '@wix/ecom';

    async function updateTipSettings(tipSettings) {
      const response = await tipSettings.updateTipSettings(tipSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateTipSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tipSettings } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tipSettings },
      // Include the auth strategy and host as relevant
    });

    async function updateTipSettings(tipSettings) {
      const response = await myWixClient.tipSettings.updateTipSettings(tipSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateTipSettings (with elevated permissions)
    import { tipSettings } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myUpdateTipSettingsMethod(tipSettings) {
      const elevatedUpdateTipSettings = auth.elevate(tipSettings.updateTipSettings);
      const response = await elevatedUpdateTipSettings(tipSettings);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
