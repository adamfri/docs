---
title: "Get Item Seo Tags"
description: "Retrieves the SEO tags of a single item."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the SEO tags of a single item.

    Returns the tags set for the item itself in `tags`, and the tags it's expected to
    render with in `resolvedTags`, each marked with where it came from. An item with no
    tags of its own returns empty `tags` and `hasOverride: false`, because it inherits its
    tags.

    Limitation: for an item type that keeps a separate published revision (currently
    `STATIC_PAGE`), `tags`, `focusKeywords`, and `resolvedTags` always reflect the saved
    revision, never the published one. Calling [Set Item SEO
    Tags](https://dev.wix.com/docs/api-reference/business-management/seo/item-seo-tags-v1/set-item-seo-tags)
    with `publish: true` updates the published revision without updating the saved one, so a
    page's live tags can differ from what this method (or List Item SEO Tags) returns for it,
    with no way to read the published revision back through this API.

    To retrieve the items of a type together with their IDs, call [List Item SEO
    Tags](https://dev.wix.com/docs/api-reference/business-management/seo/item-seo-tags-v1/list-item-seo-tags).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-SEO">View SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/promote/seo/v1/item-seo-tags/{itemType}/{itemId}
    ```

    ## Path Params

    <ParamField path="itemType" type="string" required>

      Type of item to retrieve tags for. For example, `STATIC_PAGE` or `STORES_PRODUCT`.

    </ParamField>

    <ParamField path="itemId" type="string" required>

      ID of the item to retrieve tags for.

    </ParamField>

    ## Response Object

    <ResponseField name="itemSeoTags" type="ItemSeoTags">

      Retrieved item SEO tags.

      See the [Item Seo Tags Object](/api-reference/business-management/seo/item-seo-tags-v1/item-seo-tags-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_ITEM_TYPE` | Wix doesn't support the specified `itemType`. The error message lists the item types that are supported. |
    | `404` | `NOT_FOUND` | `ITEM_NOT_FOUND` | Couldn't find the item. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    This page has no tags of its own, so `tags` is empty and `hasOverride` is false. `resolvedTags` shows the tags it inherits, each marked with its source.

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/seo-metatags-server/v1/item-seo-tags/STATIC_PAGE/c1dmp' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "itemSeoTags": {
          "id": "STATIC_PAGE:c1dmp",
          "itemType": "STATIC_PAGE",
          "itemId": "c1dmp",
          "tags": [],
          "focusKeywords": [],
          "hasOverride": false,
          "resolvedTags": [
            {
              "tag": {
                "type": "title",
                "children": "Home | Code Sample Validator Sandbox",
                "custom": false,
                "disabled": false
              },
              "source": "TAG_SOURCE_DEFAULT_PATTERN"
            },
            {
              "tag": {
                "type": "link",
                "props": {
                  "rel": "canonical",
                  "href": "https://twsinglesource.wixstudio.com/code-sample-val"
                },
                "children": "",
                "custom": false,
                "disabled": false
              },
              "source": "TAG_SOURCE_DEFAULT_PATTERN"
            }
          ],
          "hostPageId": "c1dmp",
          "publishStatus": "PUBLISH_STATUS_UNSPECIFIED",
          "origin": "ORIGIN_UNSPECIFIED"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the SEO tags of a single item.

    Returns the tags set for the item itself in `tags`, and the tags it's expected to
    render with in `resolvedTags`, each marked with where it came from. An item with no
    tags of its own returns empty `tags` and `hasOverride: false`, because it inherits its
    tags.

    Limitation: for an item type that keeps a separate published revision (currently
    `STATIC_PAGE`), `tags`, `focusKeywords`, and `resolvedTags` always reflect the saved
    revision, never the published one. Calling [Set Item SEO
    Tags](https://dev.wix.com/docs/api-reference/business-management/seo/item-seo-tags-v1/set-item-seo-tags)
    with `publish: true` updates the published revision without updating the saved one, so a
    page's live tags can differ from what this method (or List Item SEO Tags) returns for it,
    with no way to read the published revision back through this API.

    To retrieve the items of a type together with their IDs, call [List Item SEO
    Tags](https://dev.wix.com/docs/api-reference/business-management/seo/item-seo-tags-v1/list-item-seo-tags).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.VIEW-SEO">View SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { itemSeoTags } from '@wix/seo';

    itemSeoTags.getItemSeoTags(identifiers): Promise
    ```

    ## Method Parameters

    <ParamField body="identifiers" type="GetItemSeoTagsIdentifiers" required>

      identifiers

      <Expandable title="identifiers properties">

        <ParamField body="itemType" type="string" required>

          Type of item to retrieve tags for. For example, `STATIC_PAGE` or `STORES_PRODUCT`.

          Constraints: `maxLength 100`.

        </ParamField>

        <ParamField body="itemId" type="string" required>

          ID of the item to retrieve tags for.

          Constraints: `maxLength 200`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `ItemSeoTags`.

    See the [Item Seo Tags Object](/api-reference/business-management/seo/item-seo-tags-v1/item-seo-tags-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_ITEM_TYPE` | Wix doesn't support the specified `itemType`. The error message lists the item types that are supported. |
    | `404` | `NOT_FOUND` | `ITEM_NOT_FOUND` | Couldn't find the item. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-item-seo-tags" id="ex-sdk-get-item-seo-tags-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-item-seo-tags" id="ex-sdk-get-item-seo-tags-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-item-seo-tags-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-item-seo-tags-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Retrieve one item's SEO tags</span><span className="wix-picker-desc">This page has no tags of its own, so tags is empty and hasOverride is false. resolvedTags shows the tags it inherits</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getItemSeoTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-item-seo-tags-0"><span className="wix-picker-name">Retrieve one item's SEO tags</span><span className="wix-picker-desc">This page has no tags of its own, so tags is empty and hasOverride is false. resolvedTags shows the tags it inherits</span></label>
    <label htmlFor="ex-sdk-get-item-seo-tags-1"><span className="wix-picker-name">getItemSeoTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Retrieve one item's SEO tags
    import { itemSeoTags } from "@wix/seo";

    async function getItemSeoTags() {
      const response = await itemSeoTags.getItemSeoTags({
        itemType: "STATIC_PAGE",
        itemId: "c1dmp",
      });
      return response;
    }

    /* Promise resolves to:
     * {
     *   "_id": "STATIC_PAGE:c1dmp",
     *   "itemType": "STATIC_PAGE",
     *   "itemId": "c1dmp",
     *   "tags": [],
     *   "focusKeywords": [],
     *   "hasOverride": false,
     *   "resolvedTags": [
     *     {
     *       "tag": { "type": "title", "children": "Home | Ceramics studio" },
     *       "source": "TAG_SOURCE_DEFAULT_PATTERN"
     *     },
     *     {
     *       "tag": {
     *         "type": "link",
     *         "props": { "rel": "canonical", "href": "https://example.wixsite.com/ceramics" }
     *       },
     *       "source": "TAG_SOURCE_DEFAULT_PATTERN"
     *     }
     *   ],
     *   "publishStatus": "PUBLISH_STATUS_UNSPECIFIED",
     *   "origin": "ORIGIN_UNSPECIFIED"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getItemSeoTags (self-hosted)
    import { createClient } from '@wix/sdk';
    import { itemSeoTags } from '@wix/seo';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { itemSeoTags },
      // Include the auth strategy and host as relevant
    });

    async function getItemSeoTags(identifiers) {
      const response = await myWixClient.itemSeoTags.getItemSeoTags(identifiers);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
