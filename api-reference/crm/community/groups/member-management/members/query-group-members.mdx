---
title: "Query Group Members"
description: "Retrieves a list of up to 100 group members, given the provided paging, sorting and filtering."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 group members, given the provided paging, sorting and filtering.
    Groups managers can retrieve group members of any group.

    Supported fields for filtering:
    - `role`

    Supported fields for sorting:
    - `joinedOn`
    - `role`

    ## Endpoint

    ```http
    POST https://www.wixapis.com/social-groups-proxy/members/v2/groups/{groupId}/members/query
    ```

    ## Path Params

    <ParamField path="groupId" type="string" required>

      Group ID.

    </ParamField>

    ## Body Params

    <ParamField body="query" type="Query">

      query

      <Expandable title="query properties">

        <ParamField body="filter" type="any">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paging" type="Paging">

          Paging options to limit and skip the number of items.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="members" type="GroupMember[]">

      Group members.

      See the [Group Member Object](/api-reference/crm/community/groups/member-management/members/group-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging information.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
          https://wixapis.com/social-groups/v2/groups/8a258cf5-49f3-41dc-b320-65ec2598315b/members/query \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
          -d '{
              "query": {
                  "filter": {
                      "role": {
                          "$eq": "ADMIN"
                      }
                  },
                  "sort": [
                      {
                          "fieldName": "joinedOn",
                          "order": "DESC"
                      }
                  ],
                  "paging": {
                      "offset": 0,
                      "limit": 100
                  }
              }
          }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "members": [
              {
                  "siteMemberId": "223c37e0-3474-43c5-8d47-e374b414ef68",
                  "role": {
                      "value": "ADMIN"
                  },
                  "joinedDate": "2020-12-19T17:40:23Z"
              },
              {
                  "siteMemberId": "88e40cba-392b-4333-8717-9375d57f4de7",
                  "role": {
                      "value": "ADMIN"
                  },
                  "joinedDate": "2020-09-17T09:17:56Z"
              }
          ],
          "metadata": {
              "count": 2,
              "offset": 0,
              "total": 2
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 group members, given the provided paging, sorting and filtering.
    Groups managers can retrieve group members of any group.

    Supported fields for filtering:
    - `role`

    Supported fields for sorting:
    - `joinedOn`
    - `role`

    ## Method Declaration

    ```typescript
    import { members } from '@wix/groups';

    members.queryGroupMembers(groupId, options): MembersQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="groupId" type="string" required>

      Group ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="QueryGroupMembersOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="Query">

          query

          <Expandable title="query properties">

            <ParamField body="filter" type="any">

              Filter object in the following format:
              `"filter" : {`
              "fieldName1": "value1",
              "fieldName2":&#123;"$operator":"value2"&#125;
              &#125;``
              Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object in the following format:
              `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="fields" type="string[]">

              Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

            <ParamField body="fieldsets" type="string[]">

              Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `MembersQueryBuilder`.

    <ResponseField name="members" type="GroupMember[]">

      Group members.

      See the [Group Member Object](/api-reference/crm/community/groups/member-management/members/group-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging information.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-group-members" id="ex-sdk-query-group-members-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-group-members" id="ex-sdk-query-group-members-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-group-members" id="ex-sdk-query-group-members-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-group-members-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-group-members-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query members of a group</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query members of a group (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryGroupMembers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-group-members-0"><span className="wix-picker-name">Query members of a group</span></label>
    <label htmlFor="ex-sdk-query-group-members-1"><span className="wix-picker-name">Query members of a group (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-query-group-members-2"><span className="wix-picker-name">queryGroupMembers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query members of a group
    import { members } from '@wix/groups';

    export function myQueryGroupMembersFunction(groupId) {
      return members.queryGroupMembers(groupId)
        .find()
        .then((results) => {
          const items = results.items;
          const firstItem = items[0];
          const pageSize = results.pageSize;
          const totalPages = results.totalPages;
          const totalCount = results.totalCount;
          const currentPage = results.currentPage;
          const next = results.next();
          const previous = results.prev();
          const hasNext = results.hasNext();
          const hasPrev = results.hasPrev();
          const length = results.length;

          return items;
        })
        .catch((error) => {
          console.error(error);
        });
    }

    /* Returns items:
     * [
     *  { 
     *     "joinedDate": "Sun Aug 01 2021 12:32:35 GMT+0300"
     *     "memberId": "437cd3db-e9be-4980-93c1-a6d767a11050"
     *     "role": {"value": "ADMIN"}
     *   },
     *   {
     *     "joinedDate": "Tues Jun 30 2019 10:09:33 GMT+0300"
     *     "memberId": "7fe8e9e1-d050-4c86-9684-e7f231600a34"
     *     "role": {"value": "MEMBER"}
     *   }
     * ]
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Query members of a group (with elevated permissions)
    import { members } from '@wix/groups';
    import { auth } from '@wix/essentials';

    const elevatedQueryGroupMembers = auth.elevate(members.queryGroupMembers);

    export function myQueryGroupMembersFunction() {
      return elevatedQueryGroupMembers(groupId)
        .find()
        .then((results) => {
          const items = results.items;
          const firstItem = items[0];
          const pageSize = results.pageSize;
          const totalPages = results.totalPages;
          const totalCount = results.totalCount;
          const currentPage = results.currentPage;
          const next = results.next();
          const previous = results.prev();
          const hasNext = results.hasNext();
          const hasPrev = results.hasPrev();
          const length = results.length;

          return items;
        })
        .catch((error) => {
          console.error(error);
        });
    }

    /* Returns items:
     * [
     *  { 
     *     "joinedDate": "Sun Aug 01 2021 12:32:35 GMT+0300"
     *     "memberId": "437cd3db-e9be-4980-93c1-a6d767a11050"
     *     "role": {"value": "ADMIN"}
     *   },
     *   {
     *     "joinedDate": "Tues Jun 30 2019 10:09:33 GMT+0300"
     *     "memberId": "7fe8e9e1-d050-4c86-9684-e7f231600a34"
     *     "role": {"value": "MEMBER"}
     *   }
     * ]
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryGroupMembers (self-hosted)
    import { createClient } from '@wix/sdk';
    import { members } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { members },
      // Include the auth strategy and host as relevant
    });

    async function queryGroupMembers(groupId,options) {
      const response = await myWixClient.members.queryGroupMembers(groupId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
