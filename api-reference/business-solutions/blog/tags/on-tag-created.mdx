---
title: "onTagCreated"
description: "Triggered when a tag is created."
icon: "bolt"
---

Triggered when a tag is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v3.tag_created` |
| Package | `@wix/blog` |
| Permissions | `Read Blog`, `Manage Blog`, `Read Draft Blog Posts` |

## Examples

<CodeGroup>

  ```javascript onTagCreated
  import { tags } from '@wix/blog';

  tags.onTagCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTagCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { tags } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        tags,
      },
  });

  wixClient.tags.onTagCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
