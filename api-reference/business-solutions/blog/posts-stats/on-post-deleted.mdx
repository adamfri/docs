---
title: "onPostDeleted"
description: "Triggered when a post is deleted."
icon: "bolt"
---

Triggered when a post is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v3.post_deleted` |
| Package | `@wix/blog` |
| Permissions | `Read Blog`, `Manage Blog`, `Read Draft Blog Posts` |

## Examples

<CodeGroup>

  ```javascript onPostDeleted
  import { posts } from '@wix/blog';

  posts.onPostDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
