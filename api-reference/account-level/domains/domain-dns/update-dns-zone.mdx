---
title: "Update Dns Zone"
description: "Adds DNS records to and removes DNS records from a DNS zone in Google's Cloud DNS."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds DNS records to and removes DNS records from a DNS zone in
    [Google's Cloud DNS](https://cloud.google.com/dns).

    The call is synchronous, that means it fails in case of an error on
    Google's side.

    You can only set up a single `record` object per DNS record
    type. If you want to specify multiple values for the same record type, you
    must save them in the `values` for the relevant type. This means, you must
    delete the relevant record and add a new `addition` object for this type
    to the request, in case you want to add values for an existing DNS record
    type.

    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.MANAGE_DNS_ZONES">DOMAINS.MANAGE_DNS_ZONES</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/domains/v1/dns-zones/{domainName}
    ```

    ## Path Params

    <ParamField path="domainName" type="string" required>

      Domain to update the DNS zone for. Must include the TLD.

    </ParamField>

    ## Body Params

    <ParamField body="additions" type="DnsRecord[]">

      DNS records to add to the DNS zone.

      Max: 50 additions

      Constraints: `maxItems 50`.

      <Expandable title="additions properties">

        <ParamField body="type" type="string" required>

          [DNS record type](https://en.wikipedia.org/wiki/List_of_DNS_record_types).

          **Supported values:**

          - `A` — Address record. Most fundamental type of DNS record that indicates the IP address of a given domain.
          - `AAAA` — Address record in [IPv6 format](https://en.wikipedia.org/wiki/IPv6).
          - `NS` — Name server record. Describes which server contains the actual DNS record.
          - `SOA` — [Start of authority record](https://en.wikipedia.org/wiki/SOA_record). Holds administrative information about the zone.
          - `CNAME` — [CNAME record](https://en.wikipedia.org/wiki/CNAME_record). Maps an alias name to the canonical domain name.
          - `MX` — MX record. Directs emails to mail servers.
          - `TXT` — [TXT record](https://en.wikipedia.org/wiki/TXT_record). Used by domain admins to add human-readable descriptions.
          - `SPF` — SPF record. Used for email authentication.
          - `SRV` — [Service record](https://en.wikipedia.org/wiki/SRV_record). Defines the server location.
          - `PTR` — Pointer record. Specifies which host is supported for a given IP address.
          - `NAPTR` — [Name Authority Pointer record](https://en.wikipedia.org/wiki/NAPTR_record). Used by Internet telephony applications.

        </ParamField>

        <ParamField body="hostName" type="string" required>

          Host name. Can be a subdomain. For example, `domain.com` or
          `mail.domain.com`. Equal to the domain name, except for `CNAME` records.

          Constraints: `maxLength 256`.

        </ParamField>

        <ParamField body="ttl" type="integer">

          [Time to live](https://en.wikipedia.org/wiki/Time_to_live) in seconds.

        </ParamField>

        <ParamField body="values" type="string[]" required>

          DNS record values. Wix limits DNS records to 50 values per type. External
          providers may support a different maximum number of DNS records for a
          specific type, Wix doesn't validate that you don't exceed these external
          limits.

          Max: 50 records

          Constraints: `maxLength 1000`, `minItems 1`, `maxItems 50`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="deletions" type="DnsRecord[]">

      DNS records to remove from the DNS zone.

      Max: 50 deletions

      Constraints: `maxItems 50`.

      <Expandable title="deletions properties">

        <ParamField body="type" type="string" required>

          [DNS record type](https://en.wikipedia.org/wiki/List_of_DNS_record_types).

          **Supported values:**

          - `A` — Address record. Most fundamental type of DNS record that indicates the IP address of a given domain.
          - `AAAA` — Address record in [IPv6 format](https://en.wikipedia.org/wiki/IPv6).
          - `NS` — Name server record. Describes which server contains the actual DNS record.
          - `SOA` — [Start of authority record](https://en.wikipedia.org/wiki/SOA_record). Holds administrative information about the zone.
          - `CNAME` — [CNAME record](https://en.wikipedia.org/wiki/CNAME_record). Maps an alias name to the canonical domain name.
          - `MX` — MX record. Directs emails to mail servers.
          - `TXT` — [TXT record](https://en.wikipedia.org/wiki/TXT_record). Used by domain admins to add human-readable descriptions.
          - `SPF` — SPF record. Used for email authentication.
          - `SRV` — [Service record](https://en.wikipedia.org/wiki/SRV_record). Defines the server location.
          - `PTR` — Pointer record. Specifies which host is supported for a given IP address.
          - `NAPTR` — [Name Authority Pointer record](https://en.wikipedia.org/wiki/NAPTR_record). Used by Internet telephony applications.

        </ParamField>

        <ParamField body="hostName" type="string" required>

          Host name. Can be a subdomain. For example, `domain.com` or
          `mail.domain.com`. Equal to the domain name, except for `CNAME` records.

          Constraints: `maxLength 256`.

        </ParamField>

        <ParamField body="ttl" type="integer">

          [Time to live](https://en.wikipedia.org/wiki/Time_to_live) in seconds.

        </ParamField>

        <ParamField body="values" type="string[]" required>

          DNS record values. Wix limits DNS records to 50 values per type. External
          providers may support a different maximum number of DNS records for a
          specific type, Wix doesn't validate that you don't exceed these external
          limits.

          Max: 50 records

          Constraints: `maxLength 1000`, `minItems 1`, `maxItems 50`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="dnssecEnabled" type="boolean">

      Whether you want to enable or disable [DNS Security Extensions (DNSSEC)](https://en.wikipedia.org/wiki/Domain_Name_System_Security_Extensions).

    </ParamField>

    ## Response Object

    <ResponseField name="dnsZone" type="DnsZone">

      Updated DNS zone.

      See the [Dns Zone Object](/api-reference/account-level/domains/domain-dns/dns-zone-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH
      'https://www.wixapis.com/domains/v1/dns-zones/mysite.com' \
      -H 'wix-account-id: <ACCOUNT_ID>' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "deletions": [
          {
            "values": ["google-site-verification=epsWybsHVqJqkw0dD2xrIaSO_jYJAmczAU7IqLWToVM"],
            "type": "TXT",
            "hostName": "mysite.com",
            "ttl": 3600
          }
        ],
        "additions": [
          {
            "values": ["10 aspmx.l.google.com"],
            "type": "MX",
            "hostName": "mysite.com",
            "ttl": 3600
          }
        ],
        "domainName": "mysite.com"
      }'
      ```

      ```json Response
      {
        "dnsZone": {
          "records": [
            {
              "values": [
                "10 aspmx.l.google.com"
              ],
              "ttl": 3600,
              "hostName": "mysite.com",
              "type": "MX"
            }
          ],
          "domainName": "mysite.com"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds DNS records to and removes DNS records from a DNS zone in
    [Google's Cloud DNS](https://cloud.google.com/dns).

    The call is synchronous, that means it fails in case of an error on
    Google's side.

    You can only set up a single `record` object per DNS record
    type. If you want to specify multiple values for the same record type, you
    must save them in the `values` for the relevant type. This means, you must
    delete the relevant record and add a new `addition` object for this type
    to the request, in case you want to add values for an existing DNS record
    type.

    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.MANAGE_DNS_ZONES">DOMAINS.MANAGE_DNS_ZONES</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { domainDns } from '@wix/domains';

    domainDns.updateDnsZone(domainName, options): Promise<UpdateDnsZoneResponse>
    ```

    ## Method Parameters

    <ParamField body="domainName" type="string" required>

      Domain to update the DNS zone for. Must include the TLD.

      Constraints: `format HOSTNAME`.

    </ParamField>

    <ParamField body="options" type="UpdateDnsZoneOptions">

      options

      <Expandable title="options properties">

        <ParamField body="additions" type="DnsRecord[]">

          DNS records to add to the DNS zone.

          Max: 50 additions

          Constraints: `maxItems 50`.

          <Expandable title="additions properties">

            <ParamField body="type" type="string" required>

              [DNS record type](https://en.wikipedia.org/wiki/List_of_DNS_record_types).

              **Supported values:**

              - `A` — Address record. Most fundamental type of DNS record that indicates the IP address of a given domain.
              - `AAAA` — Address record in [IPv6 format](https://en.wikipedia.org/wiki/IPv6).
              - `NS` — Name server record. Describes which server contains the actual DNS record.
              - `SOA` — [Start of authority record](https://en.wikipedia.org/wiki/SOA_record). Holds administrative information about the zone.
              - `CNAME` — [CNAME record](https://en.wikipedia.org/wiki/CNAME_record). Maps an alias name to the canonical domain name.
              - `MX` — MX record. Directs emails to mail servers.
              - `TXT` — [TXT record](https://en.wikipedia.org/wiki/TXT_record). Used by domain admins to add human-readable descriptions.
              - `SPF` — SPF record. Used for email authentication.
              - `SRV` — [Service record](https://en.wikipedia.org/wiki/SRV_record). Defines the server location.
              - `PTR` — Pointer record. Specifies which host is supported for a given IP address.
              - `NAPTR` — [Name Authority Pointer record](https://en.wikipedia.org/wiki/NAPTR_record). Used by Internet telephony applications.

            </ParamField>

            <ParamField body="hostName" type="string" required>

              Host name. Can be a subdomain. For example, `domain.com` or
              `mail.domain.com`. Equal to the domain name, except for `CNAME` records.

              Constraints: `maxLength 256`.

            </ParamField>

            <ParamField body="ttl" type="integer">

              [Time to live](https://en.wikipedia.org/wiki/Time_to_live) in seconds.

            </ParamField>

            <ParamField body="values" type="string[]" required>

              DNS record values. Wix limits DNS records to 50 values per type. External
              providers may support a different maximum number of DNS records for a
              specific type, Wix doesn't validate that you don't exceed these external
              limits.

              Max: 50 records

              Constraints: `maxLength 1000`, `minItems 1`, `maxItems 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="deletions" type="DnsRecord[]">

          DNS records to remove from the DNS zone.

          Max: 50 deletions

          Constraints: `maxItems 50`.

          <Expandable title="deletions properties">

            <ParamField body="type" type="string" required>

              [DNS record type](https://en.wikipedia.org/wiki/List_of_DNS_record_types).

              **Supported values:**

              - `A` — Address record. Most fundamental type of DNS record that indicates the IP address of a given domain.
              - `AAAA` — Address record in [IPv6 format](https://en.wikipedia.org/wiki/IPv6).
              - `NS` — Name server record. Describes which server contains the actual DNS record.
              - `SOA` — [Start of authority record](https://en.wikipedia.org/wiki/SOA_record). Holds administrative information about the zone.
              - `CNAME` — [CNAME record](https://en.wikipedia.org/wiki/CNAME_record). Maps an alias name to the canonical domain name.
              - `MX` — MX record. Directs emails to mail servers.
              - `TXT` — [TXT record](https://en.wikipedia.org/wiki/TXT_record). Used by domain admins to add human-readable descriptions.
              - `SPF` — SPF record. Used for email authentication.
              - `SRV` — [Service record](https://en.wikipedia.org/wiki/SRV_record). Defines the server location.
              - `PTR` — Pointer record. Specifies which host is supported for a given IP address.
              - `NAPTR` — [Name Authority Pointer record](https://en.wikipedia.org/wiki/NAPTR_record). Used by Internet telephony applications.

            </ParamField>

            <ParamField body="hostName" type="string" required>

              Host name. Can be a subdomain. For example, `domain.com` or
              `mail.domain.com`. Equal to the domain name, except for `CNAME` records.

              Constraints: `maxLength 256`.

            </ParamField>

            <ParamField body="ttl" type="integer">

              [Time to live](https://en.wikipedia.org/wiki/Time_to_live) in seconds.

            </ParamField>

            <ParamField body="values" type="string[]" required>

              DNS record values. Wix limits DNS records to 50 values per type. External
              providers may support a different maximum number of DNS records for a
              specific type, Wix doesn't validate that you don't exceed these external
              limits.

              Max: 50 records

              Constraints: `maxLength 1000`, `minItems 1`, `maxItems 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="dnssecEnabled" type="boolean">

          Whether you want to enable or disable [DNS Security Extensions (DNSSEC)](https://en.wikipedia.org/wiki/Domain_Name_System_Security_Extensions).

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateDnsZoneResponse>`.

    <ResponseField name="dnsZone" type="DnsZone">

      Updated DNS zone.

      See the [Dns Zone Object](/api-reference/account-level/domains/domain-dns/dns-zone-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Dns Zone Updated | `wix.premium.domains.v1.dns_zone_updated` | Triggered when a dns zone was updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-dns-zone" id="ex-sdk-update-dns-zone-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-dns-zone" id="ex-sdk-update-dns-zone-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-dns-zone-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-dns-zone-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a DNS zone with an API Key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateDnsZone (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-dns-zone-0"><span className="wix-picker-name">Update a DNS zone with an API Key</span></label>
    <label htmlFor="ex-sdk-update-dns-zone-1"><span className="wix-picker-name">updateDnsZone (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a DNS zone with an API Key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { domainDns } from "@wix/domains";

    const wixClient = createClient({
      modules: { domainDns },
      auth: ApiKeyStrategy({
        siteId: "MY-SITE-ID",
        apiKey: "MY-API-KEY",
      }),
    });

    async function updateDnsZone(domainName, options) {
      const response = await domainDns.updateDnsZone(domainName, options);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateDnsZone (self-hosted)
    import { createClient } from '@wix/sdk';
    import { domainDns } from '@wix/domains';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { domainDns },
      // Include the auth strategy and host as relevant
    });

    async function updateDnsZone(domainName,options) {
      const response = await myWixClient.domainDns.updateDnsZone(domainName,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
