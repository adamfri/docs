---
title: "Start Project"
description: "Marks a project as in progress, indicating the provider has started work on the engagement."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Marks a project as in progress, indicating the provider has started work on the engagement.

    Each time the project is updated, `revision` increments by 1. The current `revision` must be passed when updating the project. This ensures you're working with the latest project and prevents unintended overwrites.

    The project's `status` must be `NEW`, `CONTACTED`, or `IN_DISCUSSION`. Calling this method on a
    project in any other status fails.

    A project that's still `NEW` no longer expires once you start it. To end the engagement
    instead, call Cancel Project. When the work is delivered, call Complete Project.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ARENA.MANAGE-LEADS">Manage CRM and Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/partners/marketplace/v1/projects/{projectId}/start
    ```

    ## Path Params

    <ParamField path="projectId" type="string" required>

      ID of the project to start.

    </ParamField>

    ## Body Params

    <ParamField body="revision" type="integer" required>

      Revision number, which increments by 1 each time the project is updated. To prevent conflicting
      changes, the current revision must be passed.

    </ParamField>

    ## Response Object

    <ResponseField name="project" type="Project">

      Updated project.

      See the [Project Object](/api-reference/account-level/studio-workspace/marketplace/project-v1/project-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Marks a project as in progress when the provider begins work

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/partners/marketplace/v1/projects/2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c/start' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "revision": "2"
      }'
      ```

      ```json Response
      {
        "project": {
          "id": "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
          "revision": "3",
          "createdDate": "2024-01-15T10:30:00.000Z",
          "updatedDate": "2024-06-10T11:00:00.000Z",
          "serviceName": "ADVANCED_WEBSITE",
          "contactId": "5c6d7e8f-9a1b-4c2d-9e3f-4a5b6c7d8e9f",
          "status": "IN_PROGRESS",
          "statusTimeline": [
            {
              "status": "NEW",
              "date": "2024-01-15T10:30:00.000Z"
            },
            {
              "status": "CONTACTED",
              "date": "2024-01-16T08:20:00.000Z"
            }
          ],
          "budget": 500.0,
          "expirationDate": "2024-01-19T10:30:00.000Z",
          "source": "MATCHING",
          "briefDescription": "Looking for a modern portfolio site with a blog and contact form."
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Marks a project as in progress, indicating the provider has started work on the engagement.

    Each time the project is updated, `revision` increments by 1. The current `revision` must be passed when updating the project. This ensures you're working with the latest project and prevents unintended overwrites.

    The project's `status` must be `NEW`, `CONTACTED`, or `IN_DISCUSSION`. Calling this method on a
    project in any other status fails.

    A project that's still `NEW` no longer expires once you start it. To end the engagement
    instead, call Cancel Project. When the work is delivered, call Complete Project.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ARENA.MANAGE-LEADS">Manage CRM and Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketplaceProjects } from '@wix/marketplace-projects';

    marketplaceProjects.startProject(projectId, revision): Promise
    ```

    ## Method Parameters

    <ParamField body="projectId" type="string" required>

      ID of the project to start.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="revision" type="integer" required>

      Revision number, which increments by 1 each time the project is updated. To prevent conflicting
      changes, the current revision must be passed.

    </ParamField>

    ## Returns

    <ResponseField name="project" type="Project">

      Updated project.

      See the [Project Object](/api-reference/account-level/studio-workspace/marketplace/project-v1/project-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Updated | `wix.partners.marketplace.v1.project_updated` | Triggered when a project is updated, including every status transition. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-start-project" id="ex-sdk-start-project-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-start-project" id="ex-sdk-start-project-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-start-project-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-start-project-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Start a project</span><span className="wix-picker-desc">Marks a project as in progress when the provider begins work</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">startProject (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-start-project-0"><span className="wix-picker-name">Start a project</span><span className="wix-picker-desc">Marks a project as in progress when the provider begins work</span></label>
    <label htmlFor="ex-sdk-start-project-1"><span className="wix-picker-name">startProject (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Start a project
    import { marketplaceProjects } from "@wix/marketplace-projects";

    async function startProject() {
      const response = await marketplaceProjects.startProject(
        "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
        "2",
      );
    }

    /* Promise resolves to:
     * {
     *   "project": {
     *     "_id": "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
     *     "revision": "3",
     *     "_createdDate": "2024-01-15T10:30:00.000Z",
     *     "_updatedDate": "2024-06-10T11:00:00.000Z",
     *     "serviceName": "ADVANCED_WEBSITE",
     *     "contactId": "5c6d7e8f-9a1b-4c2d-9e3f-4a5b6c7d8e9f",
     *     "status": "IN_PROGRESS",
     *     "statusTimeline": [
     *       { "status": "NEW", "date": "2024-01-15T10:30:00.000Z" },
     *       { "status": "CONTACTED", "date": "2024-01-16T08:20:00.000Z" }
     *     ],
     *     "budget": 500,
     *     "expirationDate": "2024-01-19T10:30:00.000Z",
     *     "source": "MATCHING",
     *     "briefDescription": "Looking for a modern portfolio site with a blog and contact form."
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript startProject (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketplaceProjects } from '@wix/marketplace-projects';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketplaceProjects },
      // Include the auth strategy and host as relevant
    });

    async function startProject(projectId,revision) {
      const response = await myWixClient.marketplaceProjects.startProject(projectId,revision);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
