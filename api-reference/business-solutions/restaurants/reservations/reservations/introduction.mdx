---
title: "Introduction"
description: "The Reservations API allows you to create and manage reservations at restaurants."
icon: "file-lines"
---

The Reservations API allows you to create and manage reservations at restaurants.

This API provides all the necessary functionality to move a reservation through the phases of its [lifecycle](fallback::https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/reservations/reservations/the-reservation-lifecycle), such as from the reservation request until completion.

With the Reservations API, you can:

* Create and manage reservations at a restaurant.
* Get information about existing reservations at a restaurant.

The Reservations API works together with:

- The [Reservation Locations API](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/reservations/reservation-locations/introduction), which provides information about the physical location of a restaurant, as well as that restaurant’s availability and reservation conditions.
- The [Time Slots API](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/reservations/time-slots/introduction), which allows you to retrieve availability information about time slots at a restaurant on a specific date and for a specific party size.
- The [Experiences API](fallback::https://dev.wix.com/docs/api-reference/business-solutions/restaurants/wix-restaurants-new/reservations/experiences/introduction), which lets you create and manage unique dining experiences at restaurants, such as wine tastings, chef’s tables, or special menus.

## Before you begin
It’s important to note the following points before starting to code:
* The Wix user must install the Wix Table Reservations app.
* The Wix user must have at least 1 location configured in the Dashboard under [Business Info](fallback::https://www.wix.com/my-account/site-selector/?buttonText=Select%20Site&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Fbusiness-info).

## Permissions information

The following methods have different permission requirements depending on the fields specified, and the value of certain fields.

### Create Reservation

[`Create Reservation`](fallback::https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/reservations/reservations/create-reservation) requires additional permissions when the following fields are specified:
* `status`
* `source`
* `reservation.details.tableIds`
* `reservation.details.endDate`
* `ignoreReservationLocationConflicts`
* `ignoreTableCombinationConflicts`
* `ignoreConflcts`

These additional permission requirements are as follows:
* In an app, specifying the above fields requires the `MANAGE RESERVATIONS (FULL)` [permission scope](##wix-app*fallback::https://dev.wix.com/docs/sdk/articles/getting-started/access-types-and-permissions#wix-app). 
* On a headless site, specifying the above fields requires [API key authorization](##api-key*fallback::https://dev.wix.com/docs/sdk/articles/getting-started/access-types-and-permissions#api-key) with appropriate permissions.
* On a Wix site, specifying the above fields requires [elevated permissions](fallback::https://www.wix.com/velo/reference/wix-auth/elevate).

If a `source` is not specified, the value assigned to it depends on the permissions of the [identity](fallback::https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) making the call: 
* In an app, if you're authorized as an identity with the `MANAGE RESERVATIONS (FULL)` permission scope, `source` is set to `UNDEFINED`. Otherwise, `source` is set to `ONLINE`.
* On a headless site, if you're authorized as a visitor or member, `source` is set to `ONLINE`.
* On a Wix site, if the method is called with elevated permissions, `source` is set to `UNDEFINED`. Otherwise, `source` is set to `ONLINE`.

### Get Reservation

Calling [Get Reservation](fallback::https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/reservations/reservations/get-reservation) with `fieldsets` set to `FULL` requires additional permissions:
* In an app, the `FULL` fieldset requires either the `MANAGE RESERVATIONS (MEDIUM)` or `MANAGE RESERVATIONS (FULL)` [permission scope](##wix-app*fallback::https://dev.wix.com/docs/sdk/articles/getting-started/access-types-and-permissions#wix-app).  
* On a headless site, retrieving the `FULL` fieldset requires [API key authorization](##api-key*fallback::https://dev.wix.com/docs/sdk/articles/getting-started/access-types-and-permissions#api-key) with appropriate permissions.
* On a Wix site, retrieving the `FULL` fieldset requires [elevated permissions](fallback::https://www.wix.com/velo/reference/wix-auth/elevate).

## Use Cases
[Reservation app for restaurants on a Wix site](##reservation-app-for-restaurants-on-a-wix-site*fallback::https://dev.wix.com/docs/rest/api-reference/wix-restaurants/reservations/sample-flows#reservation-app-for-restaurants-on-a-wix-site)

## Terminology
For a comprehensive glossary of Reservations terms, see [Terminology](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/reservations/terminology).

@sdk_package_setup
