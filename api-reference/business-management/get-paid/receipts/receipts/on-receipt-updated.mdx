---
title: "onReceiptUpdated"
description: "Triggered when a receipt's number allocation is updated or a receipt document is generated."
icon: "bolt"
---

Triggered when a receipt's number allocation is updated or a receipt document is generated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.receipts.v1.receipt_updated` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Receipts`, `Manage Stores` |

## Examples

<CodeGroup>

  ```javascript onReceiptUpdated
  import { receipts } from '@wix/get-paid';

  receipts.onReceiptUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReceiptUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { receipts } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        receipts,
      },
  });

  wixClient.receipts.onReceiptUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
