---
title: "Query Google Locations"
description: "Retrieves a list of up to 100 GBP locations with their current Google data, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 GBP locations with their current Google data,
    given the provided paging, filtering, and sorting.

    Takes the same filters as Query GBP Locations, and additionally retrieves each
    location's live profile data from Google: title, address, opening hours and so
    on. Use it when you need both the stored identifiers and the current profile
    in one call.

    A location whose Google data can't be retrieved is still returned, with the
    reason in that location's `googleError`. The call as a whole succeeds and
    paging is unaffected, so check `googleError` per location rather than relying
    on the response status.

    Supported properties for filtering and sorting: `id`, `googleLocationId`,
    `accountId`.

    To learn about working with Query methods, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/gbp/v1/locations/query-with-google
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query to filter, sort and page the results. Takes the same properties as
      Query GBP Locations.

      Supported properties: `id`, `googleLocationId`, `accountId`.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="locations" type="LocationWithGoogle[]">

      Retrieved locations, each with its Google data or, if that couldn't be
      retrieved, a `googleError` explaining why.

      <Expandable title="locations properties">

        <ResponseField name="location" type="GBPLocation">

          The location as it's stored for the site, including its `id` and
          `accountId`.

          See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata. Paging reflects the site's stored locations, so a location
      whose Google data couldn't be retrieved still occupies its place in the page.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/gbp/v1/locations/query-with-google' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "accountId": "104312867792031877552"
          },
          "cursorPaging": {
            "limit": 10
          }
        }
      }'
      ```

      ```json Response
      {
        "locations": [
          {
            "location": {
              "id": "8034569321457853640",
              "accountId": "104312867792031877552",
              "createdDate": "2026-06-02T11:38:54.207Z",
              "updatedDate": "2026-06-02T11:38:54.207Z",
              "revision": "1",
              "googleLocationId": "8034569321457853640"
            },
            "googleLocation": {
              "name": "locations/8034569321457853640",
              "title": "Sunset Coffee Roasters",
              "languageCode": "en-US",
              "primaryCategory": {
                "id": "categories/gcid:coffee_shop",
                "displayName": "Coffee shop"
              },
              "storefrontAddress": {
                "countryCode": "US",
                "postalCode": "94110",
                "administrativeArea": "CA",
                "locality": "San Francisco",
                "addressLines": ["2436 Mission St"]
              },
              "primaryPhone": "+1 415-555-0129",
              "websiteUri": "https://www.sunsetcoffeeroasters.com",
              "openInfo": {
                "status": "OPEN",
                "canReopen": true
              }
            },
            "voiceOfMerchant": {
              "hasVoiceOfMerchant": true,
              "hasBusinessAuthority": true,
              "state": "STATE_NONE"
            }
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 GBP locations with their current Google data,
    given the provided paging, filtering, and sorting.

    Takes the same filters as Query GBP Locations, and additionally retrieves each
    location's live profile data from Google: title, address, opening hours and so
    on. Use it when you need both the stored identifiers and the current profile
    in one call.

    A location whose Google data can't be retrieved is still returned, with the
    reason in that location's `googleError`. The call as a whole succeeds and
    paging is unaffected, so check `googleError` per location rather than relying
    on the response status.

    Supported properties for filtering and sorting: `id`, `googleLocationId`,
    `accountId`.

    To learn about working with Query methods, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/gbp';

    locations.queryGoogleLocations(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="QueryGoogleLocationsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="CursorQuery">

          Query to filter, sort and page the results. Takes the same properties as
          Query GBP Locations.

          Supported properties: `id`, `googleLocationId`, `accountId`.

          <Expandable title="query properties">

            <ParamField body="filter" type="object">

              Filter object.

              Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object.

              Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

              Constraints: `maxItems 5`.

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="pagingMethod" type="pagingMethod">

              pagingMethod

              _Nested structure omitted for brevity — see the `pagingMethod` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="locations" type="LocationWithGoogle[]">

      Retrieved locations, each with its Google data or, if that couldn't be
      retrieved, a `googleError` explaining why.

      <Expandable title="locations properties">

        <ResponseField name="location" type="GBPLocation">

          The location as it's stored for the site, including its `id` and
          `accountId`.

          See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata. Paging reflects the site's stored locations, so a location
      whose Google data couldn't be retrieved still occupies its place in the page.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-google-locations" id="ex-sdk-query-google-locations-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-google-locations" id="ex-sdk-query-google-locations-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-google-locations-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-google-locations-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query locations with their live Google data</span><span className="wix-picker-desc">Retrieve stored locations together with each one's current Google profile</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryGoogleLocations (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-google-locations-0"><span className="wix-picker-name">Query locations with their live Google data</span><span className="wix-picker-desc">Retrieve stored locations together with each one's current Google profile</span></label>
    <label htmlFor="ex-sdk-query-google-locations-1"><span className="wix-picker-name">queryGoogleLocations (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query locations with their live Google data
    import { locations } from "@wix/gbp";

    async function queryGoogleLocations() {
      const response = await locations.queryGoogleLocations({
        query: {
          filter: { accountId: "104312867792031877552" },
          cursorPaging: { limit: 10 },
        },
      });

      console.log(response.locations);
    }

    /* Promise resolves to:
     * {
     *   "locations": [
     *     {
     *       "location": {
     *         "_id": "8034569321457853640",
     *         "accountId": "104312867792031877552",
     *         "_createdDate": "2026-06-02T11:38:54.207Z",
     *         "_updatedDate": "2026-06-02T11:38:54.207Z",
     *         "revision": "1",
     *         "googleLocationId": "8034569321457853640"
     *       },
     *       "googleLocation": {
     *         "name": "locations/8034569321457853640",
     *         "title": "Sunset Coffee Roasters",
     *         "languageCode": "en-US",
     *         "primaryCategory": {
     *           "_id": "categories/gcid:coffee_shop",
     *           "displayName": "Coffee shop"
     *         },
     *         "storefrontAddress": {
     *           "countryCode": "US",
     *           "postalCode": "94110",
     *           "administrativeArea": "CA",
     *           "locality": "San Francisco",
     *           "addressLines": ["2436 Mission St"]
     *         },
     *         "primaryPhone": "+1 415-555-0129",
     *         "websiteUri": "https://www.sunsetcoffeeroasters.com",
     *         "openInfo": {
     *           "status": "OPEN",
     *           "canReopen": true
     *         }
     *       },
     *       "voiceOfMerchant": {
     *         "hasVoiceOfMerchant": true,
     *         "hasBusinessAuthority": true,
     *         "state": "STATE_NONE"
     *       }
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 1,
     *     "cursors": {},
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryGoogleLocations (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/gbp';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function queryGoogleLocations(options) {
      const response = await myWixClient.locations.queryGoogleLocations(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
