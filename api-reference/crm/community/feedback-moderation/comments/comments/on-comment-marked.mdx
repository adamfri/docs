---
title: "onCommentMarked"
description: "Triggered when a comment is marked."
icon: "bolt"
---

Triggered when a comment is marked.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v2.comment_comment_marked` |
| Package | `@wix/comments` |
| Permissions | `Read Comments`, `Manage Comments` |

## Examples

<CodeGroup>

  ```javascript onCommentMarked
  import { comments } from '@wix/comments';

  comments.onCommentMarked((event) => {
      // handle your event here
  });
  ```

  ```javascript onCommentMarked (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { comments } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        comments,
      },
  });

  wixClient.comments.onCommentMarked((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
