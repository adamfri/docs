---
title: "Delete Chat Settings"
description: "Deletes a chat settings entity."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    Deletes a chat settings entity.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/forms/ai/v1/chat-settings/{chatSettingsId}
    ```

    ## Path Params

    <ParamField path="chatSettingsId" type="string" required>

      Chat settings ID.

    </ParamField>

    ## Response Object

    Returns `DeleteChatSettingsResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Chat Settings Deleted | `wix.forms.ai.v1.chat_settings_deleted` |  |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a chat settings entity.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { chatSettings } from '@wix/forms';

    chatSettings.deleteChatSettings(chatSettingsId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="chatSettingsId" type="string" required>

      Chat settings ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Chat Settings Deleted | `wix.forms.ai.v1.chat_settings_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-chat-settings" id="ex-sdk-delete-chat-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-chat-settings" id="ex-sdk-delete-chat-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-chat-settings" id="ex-sdk-delete-chat-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-chat-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-chat-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteChatSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteChatSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteChatSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-chat-settings-0"><span className="wix-picker-name">deleteChatSettings</span></label>
    <label htmlFor="ex-sdk-delete-chat-settings-1"><span className="wix-picker-name">deleteChatSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-chat-settings-2"><span className="wix-picker-name">deleteChatSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteChatSettings
    import { chatSettings } from '@wix/forms';

    async function deleteChatSettings(chatSettingsId) {
      const response = await chatSettings.deleteChatSettings(chatSettingsId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteChatSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { chatSettings } from '@wix/forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { chatSettings },
      // Include the auth strategy and host as relevant
    });

    async function deleteChatSettings(chatSettingsId) {
      const response = await myWixClient.chatSettings.deleteChatSettings(chatSettingsId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteChatSettings (with elevated permissions)
    import { chatSettings } from '@wix/forms';
    import { auth } from '@wix/essentials';

    async function myDeleteChatSettingsMethod(chatSettingsId) {
      const elevatedDeleteChatSettings = auth.elevate(chatSettings.deleteChatSettings);
      const response = await elevatedDeleteChatSettings(chatSettingsId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
