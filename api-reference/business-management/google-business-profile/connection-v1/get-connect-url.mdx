---
title: "Get Connect Url"
description: "Retrieves the Google authorization URL for the site owner to open."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the Google authorization URL for the site owner to open.

    Surface the returned URL to the site owner. After they authorize in their
    browser, Wix completes the OAuth exchange server-side; poll Get Connection
    until the status is `VALID`.

    Each call starts a new authorization attempt: the returned URL is single-use
    and expires after 15 minutes, so request a fresh one for every attempt
    rather than caching or reusing one.

    An already-connected site may call this too — reconnecting is a supported
    flow. Authorizing with the same Google account heals the connection in
    place, as long as its stored credentials are still there (Get Connection
    reports `VALID`); authorizing with a different one replaces it —
    permanently removing every location on the connection it replaced — the
    ones created through this API as well as the ones imported from Google.

    `NEEDS_RECONNECT` is the exception, and it is the one status that asks you
    to reconnect. The stored credentials are already gone, so Wix can no longer
    prove which Google account the connection belonged to and treats the
    reconnect as a replacement even when the owner authorizes with the same
    account. Warn the site owner before starting this flow from
    `NEEDS_RECONNECT`: it permanently removes every location on the old
    connection.

    The resulting connection is owned by the calling user. When the caller is an
    app rather than a user, it is owned by the site owner instead.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/gbp/v1/connect-url
    ```

    ## Response Object

    <ResponseField name="connectUrl" type="string">

      URL the site owner opens to authorize Wix to manage their Business Profile.
      Single-use and short-lived; request a new one for each attempt.

      Constraints: `format SECURE_WEB_URL`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CONNECTING_USER_NOT_RESOLVABLE` |  |
    | `503` | `UNAVAILABLE` | `CONNECTING_USER_LOOKUP_UNAVAILABLE` | Transient: the site-owner lookup failed. Retryable, unlike the two FAILED_PRECONDITION cases above. |
    | `428` | `FAILED_PRECONDITION` | `MULTIPLE_CONNECTIONS_NOT_REPRESENTABLE` | The singleton premise ("a site has at most one Google connection") can stop holding — either because multi-connection mode was enabled, or because two simultaneous authorizations both passed the one-connection check. Reported rather than acted on with an arbitrary connection. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/gbp/v1/connect-url' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "connectUrl": "https://accounts.google.com/o/oauth2/v2/auth?client_id=407408718192.apps.googleusercontent.com&redirect_uri=https%3A%2F%2Fwww.wix.com%2Fgbp%2Fv1%2Fconnect-callback&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fbusiness.manage&state=f8261611-7f65-4b1a-9d80-3d16aa9db01c&access_type=offline&prompt=consent"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the Google authorization URL for the site owner to open.

    Surface the returned URL to the site owner. After they authorize in their
    browser, Wix completes the OAuth exchange server-side; poll Get Connection
    until the status is `VALID`.

    Each call starts a new authorization attempt: the returned URL is single-use
    and expires after 15 minutes, so request a fresh one for every attempt
    rather than caching or reusing one.

    An already-connected site may call this too — reconnecting is a supported
    flow. Authorizing with the same Google account heals the connection in
    place, as long as its stored credentials are still there (Get Connection
    reports `VALID`); authorizing with a different one replaces it —
    permanently removing every location on the connection it replaced — the
    ones created through this API as well as the ones imported from Google.

    `NEEDS_RECONNECT` is the exception, and it is the one status that asks you
    to reconnect. The stored credentials are already gone, so Wix can no longer
    prove which Google account the connection belonged to and treats the
    reconnect as a replacement even when the owner authorizes with the same
    account. Warn the site owner before starting this flow from
    `NEEDS_RECONNECT`: it permanently removes every location on the old
    connection.

    The resulting connection is owned by the calling user. When the caller is an
    app rather than a user, it is owned by the site owner instead.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { connection } from '@wix/gbp';

    connection.getConnectUrl(): Promise
    ```

    ## Returns

    <ResponseField name="connectUrl" type="string">

      URL the site owner opens to authorize Wix to manage their Business Profile.
      Single-use and short-lived; request a new one for each attempt.

      Constraints: `format SECURE_WEB_URL`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CONNECTING_USER_NOT_RESOLVABLE` |  |
    | `503` | `UNAVAILABLE` | `CONNECTING_USER_LOOKUP_UNAVAILABLE` | Transient: the site-owner lookup failed. Retryable, unlike the two FAILED_PRECONDITION cases above. |
    | `428` | `FAILED_PRECONDITION` | `MULTIPLE_CONNECTIONS_NOT_REPRESENTABLE` | The singleton premise ("a site has at most one Google connection") can stop holding — either because multi-connection mode was enabled, or because two simultaneous authorizations both passed the one-connection check. Reported rather than acted on with an arbitrary connection. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-connect-url" id="ex-sdk-get-connect-url-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-connect-url" id="ex-sdk-get-connect-url-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-connect-url-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-connect-url-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get a Google authorization URL</span><span className="wix-picker-desc">Retrieve a single-use URL the site owner opens to authorize Wix to manage their Business Profile</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getConnectUrl (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-connect-url-0"><span className="wix-picker-name">Get a Google authorization URL</span><span className="wix-picker-desc">Retrieve a single-use URL the site owner opens to authorize Wix to manage their Business Profile</span></label>
    <label htmlFor="ex-sdk-get-connect-url-1"><span className="wix-picker-name">getConnectUrl (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get a Google authorization URL
    import { connection } from "@wix/gbp";

    async function getConnectUrl() {
      const response = await connection.getConnectUrl();

      console.log(response.connectUrl);
    }

    /* Promise resolves to:
     * {
     *   "connectUrl": "https://accounts.google.com/o/oauth2/v2/auth?client_id=407408718192.apps.googleusercontent.com&redirect_uri=https%3A%2F%2Fwww.wix.com%2Fgbp%2Fv1%2Fconnect-callback&response_type=code&scope=https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fbusiness.manage&state=f8261611-7f65-4b1a-9d80-3d16aa9db01c&access_type=offline&prompt=consent"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getConnectUrl (self-hosted)
    import { createClient } from '@wix/sdk';
    import { connection } from '@wix/gbp';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { connection },
      // Include the auth strategy and host as relevant
    });

    async function getConnectUrl() {
      const response = await myWixClient.connection.getConnectUrl();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
