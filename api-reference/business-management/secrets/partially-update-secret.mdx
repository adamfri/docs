---
title: "Partially Update Secret"
description: "Updates 1 or all fields of a secret."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates 1 or all fields of a secret.

    To get the secret ID, call [List Secret Info](https://dev.wix.com/docs/rest/business-management/secrets/list-secret-info).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.VELO.MANAGE_SECRETS">Manage Secrets</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/secrets-service/api/v1/secrets/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the secret to update.

    </ParamField>

    ## Body Params

    <ParamField body="secret" type="Secret" required>

      Details of a secret.

      See the [Secret Object](/api-reference/business-management/secrets/secret-object) for this object's fields.

    </ParamField>

    ## Response Object

    Returns `UpdateSecretResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X PATCH 'https://www.wixapis.com/_api/cloud-secrets-vault-server/api/v1/secrets/a560ff70-3e9b-41b6-b66d-cf3434085f7d' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
           "secret": {
              "name": "updated_secret_key",
              "description": "AWS secret access key",
              "value": "44aSQCljxL9FLvTVA.9dKbpwueYoQ8isyQhvun19pOT9gHEdgxam39LJ0Ts70"
          }
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates 1 or all fields of a secret.

    To get the secret ID, call [List Secret Info](https://dev.wix.com/docs/rest/business-management/secrets/list-secret-info).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.VELO.MANAGE_SECRETS">Manage Secrets</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { secrets } from '@wix/secrets';

    secrets.updateSecret(_id, secret): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the secret to update.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="secret" type="Secret" required>

      Details of a secret.

      See the [Secret Object](/api-reference/business-management/secrets/secret-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Secret Updated | `wix.velo.secrets_vault.v1.secret_updated` | Triggered when a secret is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-partially-update-secret" id="ex-sdk-partially-update-secret-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-partially-update-secret" id="ex-sdk-partially-update-secret-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-partially-update-secret" id="ex-sdk-partially-update-secret-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-partially-update-secret-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-partially-update-secret-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateSecret</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateSecret (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateSecret (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-partially-update-secret-0"><span className="wix-picker-name">updateSecret</span></label>
    <label htmlFor="ex-sdk-partially-update-secret-1"><span className="wix-picker-name">updateSecret (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-partially-update-secret-2"><span className="wix-picker-name">updateSecret (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateSecret
    import { secrets } from '@wix/secrets';

    async function updateSecret(_id,secret) {
      const response = await secrets.updateSecret(_id,secret);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateSecret (self-hosted)
    import { createClient } from '@wix/sdk';
    import { secrets } from '@wix/secrets';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { secrets },
      // Include the auth strategy and host as relevant
    });

    async function updateSecret(_id,secret) {
      const response = await myWixClient.secrets.updateSecret(_id,secret);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateSecret (with elevated permissions)
    import { secrets } from '@wix/secrets';
    import { auth } from '@wix/essentials';

    async function myUpdateSecretMethod(_id,secret) {
      const elevatedUpdateSecret = auth.elevate(secrets.updateSecret);
      const response = await elevatedUpdateSecret(_id,secret);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
