---
title: "onAddOnDeleted"
description: "Triggered when an add-on is deleted."
icon: "bolt"
---

Triggered when an add-on is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.addons.v1.add_on_deleted` |
| Package | `@wix/bookings` |
| Permissions | `Manage Bookings`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onAddOnDeleted
  import { addOns } from '@wix/bookings';

  addOns.onAddOnDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onAddOnDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { addOns } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        addOns,
      },
  });

  wixClient.addOns.onAddOnDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
