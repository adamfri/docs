---
title: "onFollowMemberFollowed"
description: "Triggered when a member follows another member."
icon: "bolt"
---

Triggered when a member follows another member.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.members.v3.follow_member_followed` |
| Package | `@wix/members` |
| Permissions | `Manage Stores`, `Manage Members`, `Read Member Connections` |

## Examples

<CodeGroup>

  ```javascript onFollowMemberFollowed
  import { memberFollowers } from '@wix/members';

  memberFollowers.onFollowMemberFollowed((event) => {
      // handle your event here
  });
  ```

  ```javascript onFollowMemberFollowed (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { memberFollowers } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        memberFollowers,
      },
  });

  wixClient.memberFollowers.onFollowMemberFollowed((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
