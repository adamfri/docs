---
title: "onPublishedContentDeleted"
description: "Triggered when published content is deleted."
icon: "bolt"
---

Triggered when published content is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.localization_public.v3.published_content_deleted` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API`, `Wix Multilingual - Translation Content + Published Content Read` |

## Examples

<CodeGroup>

  ```javascript onPublishedContentDeleted
  import { translationPublishedContents } from '@wix/multilingual';

  translationPublishedContents.onPublishedContentDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onPublishedContentDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { translationPublishedContents } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        translationPublishedContents,
      },
  });

  wixClient.translationPublishedContents.onPublishedContentDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
