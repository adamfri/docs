---
title: "Get Pipeline"
description: "Retrieves a pipeline by ID."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a pipeline by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.READ-PIPELINES">Read Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/crm/pipelines/v1/pipelines/{pipelineId}
    ```

    ## Path Params

    <ParamField path="pipelineId" type="string" required>

      Pipeline ID to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="pipeline" type="Pipeline">

      Retrieved pipeline.

      See the [Pipeline Object](/api-reference/crm/crm/pipelines-management/pipelines/pipeline-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a specific pipeline by ID.

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/crm/pipelines/v1/pipelines/a1b2c3d4-e5f6-7890-abcd-1234567890ef' \
        -H 'Authorization:<AUTH>'
      ```

      ```json Response
      {
        "pipeline": {
          "id": "a1b2c3d4-e5f6-7890-abcd-1234567890ef",
          "revision": "1",
          "createdDate": "2025-07-23T10:30:00.123Z",
          "updatedDate": "2025-07-23T10:30:00.123Z",
          "name": "Sales Pipeline 2025",
          "currency": "EUR",
          "stages": [
            {
              "id": "9700847b-d27a-42dd-82ab-991de143ec68",
              "name": "New lead"
            },
            {
              "id": "aef0c122-a51d-4840-8882-07bc46a2b3eb",
              "name": "Lead assigned",
              "staleDurationInHours": 1
            },
            {
              "id": "37019f34-49e3-48ec-873f-f9872dd36fa7",
              "name": "Lead contacted",
              "staleDurationInHours": 4
            },
            {
              "id": "e0834e58-554b-41d7-92ec-5472a8c24edf",
              "name": "Proposal & negotiation",
              "staleDurationInHours": 24
            },
            {
              "id": "eeba4bcb-64f7-4bcf-9dc0-8a55b4afe840",
              "name": "In progress"
            },
            {
              "id": "dae09b70-5cb4-4dc4-89c4-9beb1cd2629c",
              "name": "Invoice sent"
            }
          ],
          "doneStage": {
            "id": "dff3e739-f409-4796-83b8-35e12f5355df",
            "name": "Done",
            "allowedOutcomes": ["WON", "LOST"]
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a pipeline by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.READ-PIPELINES">Read Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { pipelines } from '@wix/crm';

    pipelines.getPipeline(pipelineId): Promise
    ```

    ## Method Parameters

    <ParamField body="pipelineId" type="string" required>

      Pipeline ID to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Pipeline`.

    See the [Pipeline Object](/api-reference/crm/crm/pipelines-management/pipelines/pipeline-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-pipeline" id="ex-sdk-get-pipeline-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-pipeline" id="ex-sdk-get-pipeline-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-pipeline" id="ex-sdk-get-pipeline-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-pipeline-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-pipeline-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getPipeline</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPipeline (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPipeline (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-pipeline-0"><span className="wix-picker-name">getPipeline</span></label>
    <label htmlFor="ex-sdk-get-pipeline-1"><span className="wix-picker-name">getPipeline (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-pipeline-2"><span className="wix-picker-name">getPipeline (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getPipeline
    import { pipelines } from '@wix/crm';

    async function getPipeline(pipelineId) {
      const response = await pipelines.getPipeline(pipelineId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPipeline (self-hosted)
    import { createClient } from '@wix/sdk';
    import { pipelines } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { pipelines },
      // Include the auth strategy and host as relevant
    });

    async function getPipeline(pipelineId) {
      const response = await myWixClient.pipelines.getPipeline(pipelineId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPipeline (with elevated permissions)
    import { pipelines } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myGetPipelineMethod(pipelineId) {
      const elevatedGetPipeline = auth.elevate(pipelines.getPipeline);
      const response = await elevatedGetPipeline(pipelineId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
