---
title: "Disable Points Expiration"
description: "Updates the pointsExpiration status to \"DISABLED\"."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the `pointsExpiration` status to `"DISABLED"`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/loyalty-programs/v1/program/points-expiration/disable
    ```

    ## Response Object

    <ResponseField name="loyaltyProgram" type="LoyaltyProgram">

      Loyalty program with disabled points expiration feature.

      See the [Program Object](/api-reference/crm/loyalty-program/loyalty-program-management/program/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Loyalty Program Updated | `wix.loyalty.v1.program_updated` | Triggered when a loyalty program is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/loyalty-programs/v1/program/points-expiration/disable' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "loyaltyProgram": {
          "name": "Star Loyalty Program",
          "pointDefinition": {
            "customName": "Stars",
            "icon": {
              "id": "",
              "url": "shapes/39dce0a5d1ce498f95526b1390eaf585.svg",
              "height": 0,
              "width": 0
            }
          },
          "status": "ACTIVE",
          "pointsExpiration": {
            "status": "DISABLED",
            "monthsOfInactivity": 3,
            "expiringPointsPercentage": 100
          },
          "premiumFeatures": {
            "loyaltyProgram": true,
            "tiers": true,
            "pointsExpiration": true
          },
          "createdDate": "2022-05-22T08:18:32.186Z",
          "updatedDate": "2022-05-25T10:36:32.430Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the `pointsExpiration` status to `"DISABLED"`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/loyalty';

    programs.disablePointsExpiration(): Promise<DisablePointsExpirationResponse>
    ```

    ## Returns

    Returns `Promise<DisablePointsExpirationResponse>`.

    <ResponseField name="loyaltyProgram" type="LoyaltyProgram">

      Loyalty program with disabled points expiration feature.

      See the [Program Object](/api-reference/crm/loyalty-program/loyalty-program-management/program/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Updated | `wix.loyalty.v1.program_updated` | Triggered when a loyalty program is updated. |
    | Program Points Expiration Disabled | `wix.loyalty.v1.program_points_expiration_disabled` | Triggered when points expiration disabled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-disable-points-expiration" id="ex-sdk-disable-points-expiration-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-disable-points-expiration" id="ex-sdk-disable-points-expiration-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-disable-points-expiration" id="ex-sdk-disable-points-expiration-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-disable-points-expiration-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-disable-points-expiration-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Disable points expiration</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Disable points expiration (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">disablePointsExpiration (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-disable-points-expiration-0"><span className="wix-picker-name">Disable points expiration</span></label>
    <label htmlFor="ex-sdk-disable-points-expiration-1"><span className="wix-picker-name">Disable points expiration (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-disable-points-expiration-2"><span className="wix-picker-name">disablePointsExpiration (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Disable points expiration
    import { programs } from "@wix/loyalty";

    async function disablePointsExpiration() {
      try {
        const result = await programs.disablePointsExpiration();
        return result;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     *
     * {
     *   "loyaltyProgram": {
     *     "_createdDate": "2024-06-05T20:31:49.155Z",
     *     "_updatedDate": "2024-06-06T10:36:24.525Z",
     *     "name": "Loyalty",
     *     "pointDefinition": {
     *       "icon": "shapes/8de7ee72dcd944caa60f9e226d900f1d.svg"
     *     },
     *     "pointsExpiration": {
     *       "status": "DISABLED",
     *       "monthsOfInactivity": 3,
     *       "expiringPointsPercentage": 100
     *     },
     *     "premiumFeatures": {
     *       "loyaltyProgram": true,
     *       "tiers": true,
     *       "pointsExpiration": true
     *     },
     *     "status": "ACTIVE"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Disable points expiration (with elevated permissions)
    import { programs } from "@wix/loyalty";
    import { auth } from "@wix/essentials";

    const elevatedDisablePointsExpiration = auth.elevate(programs.disablePointsExpiration);

    async function disablePointsExpiration() {
      try {
        const result = await elevatedDisablePointsExpiration();
        return result;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     *
     * {
     *   "loyaltyProgram": {
     *     "_createdDate": "2024-06-05T20:31:49.155Z",
     *     "_updatedDate": "2024-06-06T10:36:24.525Z",
     *     "name": "Loyalty",
     *     "pointDefinition": {
     *       "icon": "shapes/8de7ee72dcd944caa60f9e226d900f1d.svg"
     *     },
     *     "pointsExpiration": {
     *       "status": "DISABLED",
     *       "monthsOfInactivity": 3,
     *       "expiringPointsPercentage": 100
     *     },
     *     "premiumFeatures": {
     *       "loyaltyProgram": true,
     *       "tiers": true,
     *       "pointsExpiration": true
     *     },
     *     "status": "ACTIVE"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript disablePointsExpiration (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function disablePointsExpiration() {
      const response = await myWixClient.programs.disablePointsExpiration();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
