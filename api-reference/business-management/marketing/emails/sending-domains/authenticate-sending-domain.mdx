---
title: "Authenticate Sending Domain"
description: "Authenticates a sending domain by verifying the required DNS records."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Authenticates a sending domain by verifying the required DNS records.

    Call this method only when the domain's `status` property is `NOT_AUTHENTICATED` and after DNS records have been added and propagated. If Wix manages DNS for the domain (domain purchased from Wix or connected via nameservers), you don't need to call this method.

    If authentication succeeds, the domain's `status` property changes to `AUTHENTICATED` and the domain can be used for sending emails.

    For detailed steps and prerequisites, see [Authenticate a custom domain for email sending](https://dev.wix.com/docs/api-reference/business-management/marketing/emails/sending-domains/sample-flows#authenticate-a-custom-domain-for-email-sending).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SENDING-DOMAINS.MANAGE">Manage Sending Domains</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/sending-domains/v1/sending-domains/{sendingDomainId}/authenticate
    ```

    ## Path Params

    <ParamField path="sendingDomainId" type="string" required>

      Sending domain ID.

    </ParamField>

    ## Response Object

    <ResponseField name="sendingDomain" type="SendingDomain">

      Authenticated sending domain.

      See the [Sending Domain Object](/api-reference/business-management/marketing/emails/sending-domains/sending-domain-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `NOT_READY_FOR_AUTHENTICATION` | Domain isn't ready for authentication yet. DNS records are still being generated. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Verifies DNS records and authenticates a sending domain

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/sending-domains/v1/sending-domains/12345678-1234-1234-1234-123456789012/authenticate' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Authenticates a sending domain by verifying the required DNS records.

    Call this method only when the domain's `status` property is `NOT_AUTHENTICATED` and after DNS records have been added and propagated. If Wix manages DNS for the domain (domain purchased from Wix or connected via nameservers), you don't need to call this method.

    If authentication succeeds, the domain's `status` property changes to `AUTHENTICATED` and the domain can be used for sending emails.

    For detailed steps and prerequisites, see [Authenticate a custom domain for email sending](https://dev.wix.com/docs/api-reference/business-management/marketing/emails/sending-domains/sample-flows#authenticate-a-custom-domain-for-email-sending).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SENDING-DOMAINS.MANAGE">Manage Sending Domains</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { sendingDomains } from '@wix/sending-domains';

    sendingDomains.authenticateSendingDomain(sendingDomainId): Promise
    ```

    ## Method Parameters

    <ParamField body="sendingDomainId" type="string" required>

      Sending domain ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="sendingDomain" type="SendingDomain">

      Authenticated sending domain.

      See the [Sending Domain Object](/api-reference/business-management/marketing/emails/sending-domains/sending-domain-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `NOT_READY_FOR_AUTHENTICATION` | Domain isn't ready for authentication yet. DNS records are still being generated. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-authenticate-sending-domain" id="ex-sdk-authenticate-sending-domain-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-authenticate-sending-domain" id="ex-sdk-authenticate-sending-domain-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-authenticate-sending-domain" id="ex-sdk-authenticate-sending-domain-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-authenticate-sending-domain-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-authenticate-sending-domain-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">authenticateSendingDomain</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">authenticateSendingDomain (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">authenticateSendingDomain (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-authenticate-sending-domain-0"><span className="wix-picker-name">authenticateSendingDomain</span></label>
    <label htmlFor="ex-sdk-authenticate-sending-domain-1"><span className="wix-picker-name">authenticateSendingDomain (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-authenticate-sending-domain-2"><span className="wix-picker-name">authenticateSendingDomain (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript authenticateSendingDomain
    import { sendingDomains } from '@wix/sending-domains';

    async function authenticateSendingDomain(sendingDomainId) {
      const response = await sendingDomains.authenticateSendingDomain(sendingDomainId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript authenticateSendingDomain (self-hosted)
    import { createClient } from '@wix/sdk';
    import { sendingDomains } from '@wix/sending-domains';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { sendingDomains },
      // Include the auth strategy and host as relevant
    });

    async function authenticateSendingDomain(sendingDomainId) {
      const response = await myWixClient.sendingDomains.authenticateSendingDomain(sendingDomainId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript authenticateSendingDomain (with elevated permissions)
    import { sendingDomains } from '@wix/sending-domains';
    import { auth } from '@wix/essentials';

    async function myAuthenticateSendingDomainMethod(sendingDomainId) {
      const elevatedAuthenticateSendingDomain = auth.elevate(sendingDomains.authenticateSendingDomain);
      const response = await elevatedAuthenticateSendingDomain(sendingDomainId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
