---
title: "Query Transactions"
description: "Retrieves a list of transactions, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of transactions, given the provided paging, filtering, and sorting.

    Query Transactions has a default paging limit of 50, which you can override.

    For field support for filters and sorting, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/transactions/filtering-and-sorting).

    To learn how to query transactions, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `relatedTransactionId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `pool.id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `beneficiary.memberId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `beneficiary.wixUserId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `details.item.id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/transactions/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Filter, sort, and paging to apply to the query.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.
          See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language)
          for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order. Use `ASC` for ascending order or `DESC` for descending order.

              Default: `ASC`

              **Supported values:**

              - `ASC` — Ascending sort order.
              - `DESC` — Descending sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="transactions" type="Transaction[]">

      List of retrieved transactions.

      See the [Transaction Object](/api-reference/business-solutions/benefit-programs/transactions/transaction-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl https://www.wixapis.com/benefit-programs/v1/transactions/query \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>'
        --data '{
          "query": {
            "filter": {
              "beneficiary.memberId": {
                "$eq": "d8f4a6c2-0e6a-4b89-bc3a-15e2dfe3c7a1"
              }
            }
          }
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "transactions": [
          {
            "id": "00a86f2f-f16e-4822-9663-dfca21505be1",
            "revision": "2",
            "created_date": "2024-11-20T14:48:01.652Z",
            "updated_date": "2024-11-20T14:48:02.343Z",
            "pool": {
              "id": "4df381e4-3406-469f-acf3-686a616020e5",
              "pool_definition_id": "14d357ab-ce56-459d-a7bd-6a3a7ba4d2d9",
              "program_definition_id": "e8c3366e-0c4f-3024-a034-1e0c79146d4c",
              "program_id": "8faed6d2-dfdf-44f1-8a71-bcc19d2fbdde",
              "credit_amount": "5",
              "namespace": "benefit_programs_app"
            },
            "amount": "0",
            "source": "EXTERNAL",
            "target": "AVAILABLE",
            "idempotency_key": "4df381e4-3406-469f-acf3-686a616020e5-2-renew",
            "related_transaction_id": null,
            "beneficiary": {
              "member_id": "d8f4a6c2-0e6a-4b89-bc3a-15e2dfe3c7a1",
              "identity_type": "MEMBER"
            },
            "instructing_party": null,
            "status": "COMPLETED",
            "details": {
              "item": null,
              "item_count": null,
              "effective_date": null,
              "reason": "Set balance",
              "origin": null
            },
            "extended_fields": null
          },
          {
            "id": "024890be-dadc-4731-bcff-ef80cc5b4808",
            "revision": "2",
            "created_date": "2024-11-20T09:54:22.753Z",
            "updated_date": "2024-11-20T09:54:23.174Z",
            "pool": {
              "id": "09010c62-181e-49b9-909c-099f4574c0f2",
              "pool_definition_id": "14d357ab-ce56-459d-a7bd-6a3a7ba4d2d9",
              "program_definition_id": "e8c3366e-0c4f-3024-a034-1e0c79146d4c",
              "program_id": "e5c42e1e-cd10-4210-b483-bb59932b1fa7",
              "credit_amount": "5",
              "namespace": "benefit_programs_app"
            },
            "amount": "1",
            "source": "EXTERNAL",
            "target": "AVAILABLE",
            "idempotency_key": "09010c62-181e-49b9-909c-099f4574c0f2-r2-renew",
            "related_transaction_id": null,
            "beneficiary": {
              "member_id": "d8f4a6c2-0e6a-4b89-bc3a-15e2dfe3c7a1",
              "identity_type": "MEMBER"
            },
            "instructing_party": null,
            "status": "COMPLETED",
            "details": {
              "item": null,
              "item_count": null,
              "effective_date": null,
              "reason": "Set balance",
              "origin": null
            },
            "extended_fields": null
          }
        ],
        "metadata": {
          "count": 2,
          "cursors": {
            "next": null,
            "prev": null
          },
          "has_next": false
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of transactions, given the provided paging, filtering, and sorting.

    Query Transactions has a default paging limit of 50, which you can override.

    For field support for filters and sorting, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/transactions/filtering-and-sorting).

    To learn how to query transactions, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `relatedTransactionId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `pool._id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `beneficiary.memberId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `beneficiary.wixUserId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `details.item._id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { transactions } from '@wix/benefit-programs';

    transactions.queryTransactions(query): TransactionsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="TransactionQuery" required>

      Filter, sort, and paging to apply to the query.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.
          See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language)
          for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order. Use `ASC` for ascending order or `DESC` for descending order.

              Default: `ASC`

              **Supported values:**

              - `ASC` — Ascending sort order.
              - `DESC` — Descending sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `TransactionsQueryBuilder`.

    <ResponseField name="transactions" type="Transaction[]">

      List of retrieved transactions.

      See the [Transaction Object](/api-reference/business-solutions/benefit-programs/transactions/transaction-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-transactions" id="ex-sdk-query-transactions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-transactions" id="ex-sdk-query-transactions-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-transactions" id="ex-sdk-query-transactions-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-transactions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-transactions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryTransactions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryTransactions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryTransactions (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-transactions-0"><span className="wix-picker-name">queryTransactions</span></label>
    <label htmlFor="ex-sdk-query-transactions-1"><span className="wix-picker-name">queryTransactions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-transactions-2"><span className="wix-picker-name">queryTransactions (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryTransactions
    import { transactions } from '@wix/benefit-programs';

    async function queryTransactions(query) {
      const response = await transactions.queryTransactions(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryTransactions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { transactions } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { transactions },
      // Include the auth strategy and host as relevant
    });

    async function queryTransactions(query) {
      const response = await myWixClient.transactions.queryTransactions(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryTransactions (with elevated permissions)
    import { transactions } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myQueryTransactionsMethod(query) {
      const elevatedQueryTransactions = auth.elevate(transactions.queryTransactions);
      const response = await elevatedQueryTransactions(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
