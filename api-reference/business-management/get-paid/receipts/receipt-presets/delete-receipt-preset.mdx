---
title: "Delete Receipt Preset"
description: "Permanently deletes a receipt preset."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a receipt preset.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RECEIPTS.MANAGE">Manage Receipts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/receipts/v1/receipt-presets/{receiptPresetId}
    ```

    ## Path Params

    <ParamField path="receiptPresetId" type="string" required>

      Receipt preset ID to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteReceiptPresetResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `NO_META_SITE` | Site not found. |
    | `428` | `FAILED_PRECONDITION` | `DEFAULT_RECEIPT_PRESET` | Default preset can't be deleted. To delete this preset, assign another one as the default. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Receipt Preset Deleted | `wix.receipts.presets.v1.receipt_preset_deleted` | Triggered when a receipt preset is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a Receipt Preset

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/receipts/v1/receipt-presets/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a receipt preset.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RECEIPTS.MANAGE">Manage Receipts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { receiptPresets } from '@wix/get-paid';

    receiptPresets.deleteReceiptPreset(receiptPresetId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="receiptPresetId" type="string" required>

      Receipt preset ID to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `NO_META_SITE` | Site not found. |
    | `428` | `FAILED_PRECONDITION` | `DEFAULT_RECEIPT_PRESET` | Default preset can't be deleted. To delete this preset, assign another one as the default. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Receipt Preset Deleted | `wix.receipts.presets.v1.receipt_preset_deleted` | Triggered when a receipt preset is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-receipt-preset" id="ex-sdk-delete-receipt-preset-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-receipt-preset" id="ex-sdk-delete-receipt-preset-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-receipt-preset" id="ex-sdk-delete-receipt-preset-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-receipt-preset-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-receipt-preset-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteReceiptPreset</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReceiptPreset (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReceiptPreset (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-receipt-preset-0"><span className="wix-picker-name">deleteReceiptPreset</span></label>
    <label htmlFor="ex-sdk-delete-receipt-preset-1"><span className="wix-picker-name">deleteReceiptPreset (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-receipt-preset-2"><span className="wix-picker-name">deleteReceiptPreset (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteReceiptPreset
    import { receiptPresets } from '@wix/get-paid';

    async function deleteReceiptPreset(receiptPresetId) {
      const response = await receiptPresets.deleteReceiptPreset(receiptPresetId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReceiptPreset (self-hosted)
    import { createClient } from '@wix/sdk';
    import { receiptPresets } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { receiptPresets },
      // Include the auth strategy and host as relevant
    });

    async function deleteReceiptPreset(receiptPresetId) {
      const response = await myWixClient.receiptPresets.deleteReceiptPreset(receiptPresetId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReceiptPreset (with elevated permissions)
    import { receiptPresets } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myDeleteReceiptPresetMethod(receiptPresetId) {
      const elevatedDeleteReceiptPreset = auth.elevate(receiptPresets.deleteReceiptPreset);
      const response = await elevatedDeleteReceiptPreset(receiptPresetId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
