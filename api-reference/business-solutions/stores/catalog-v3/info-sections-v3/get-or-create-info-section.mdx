---
title: "Get Or Create Info Section"
description: "Retrieves an info section by ID or uniqueName, or creates an info section if one with the passed uniqueName doesn't exist."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an info section by ID or `uniqueName`, or creates an info section if one with the passed `uniqueName` doesn't exist.

    <Note>

    If an info section with the passed `uniqueName` doesn't exist, the `uniqueName` and `title` fields are required to create a new info section.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INFO_SECTION_WRITE">Info section write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/info-sections/get-or-create
    ```

    ## Body Params

    <ParamField body="infoSection" type="InfoSectionForGetOrCreate">

      Info section to retrieve or create.

      <Expandable title="infoSection properties">

        <ParamField body="id" type="string">

          Info section ID.
          Pass an ID to retrieve an existing info section.

        </ParamField>

        <ParamField body="uniqueName" type="string">

          Info section unique name.

          > **Note:**
          > + Pass the `uniqueName` of an existing info section to return it.
          > + If no such info section exists - pass `uniqueName` and `title` to create an info section.

        </ParamField>

        <ParamField body="title" type="string">

          Info section title.

          Required when passing `uniqueName` to create a new info section.

        </ParamField>

        <ParamField body="description" type="RichContent">

          Info section description using rich content.

          Learn more about [Working with Rich Content](https://dev.wix.com/docs/go-headless/tutorials-templates/other-tutorials/working-with-rich-content).

          <Expandable title="description properties">

            <ParamField body="nodes" type="Node[]">

              Node objects representing a rich content document.

              _Nested structure omitted for brevity — see the `Node[]` object._

            </ParamField>

            <ParamField body="metadata" type="Metadata">

              Object metadata.

              _Nested structure omitted for brevity — see the `Metadata` object._

            </ParamField>

            <ParamField body="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              _Nested structure omitted for brevity — see the `DocumentStyle` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="plainDescription" type="string">

          Info section description in HTML.

          When provided on create/update, this string must be a valid HTML. It will then be converted to rich content.
          `plainDescription` is ignored when value is also passed to the `description` field.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Supported values: `ASSIGNED_PRODUCTS_COUNT`

      Constraints: `maxItems 100`.

      **Supported values:**

      - `ASSIGNED_PRODUCTS_COUNT`

    </ParamField>

    ## Response Object

    <ResponseField name="infoSection" type="InfoSection">

      Info section.

      See the [Info Section Object](/api-reference/business-solutions/stores/catalog-v3/info-sections-v3/info-section-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INFO_SECTIONS_LIMIT_EXCEEDED` | Number of info sections exceeded the limit of 200. |
    | `400` | `INVALID_ARGUMENT` | `ONLY_SPACES_STRING_ERROR` | `uniqueName` contains only spaces. Provide a valid `uniqueName`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Info Section Created | `wix.stores.catalog.v3.info_section_created` | Triggered when an info section is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-get-or-create-info-section" id="ex-rest-get-or-create-info-section-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-get-or-create-info-section" id="ex-rest-get-or-create-info-section-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-get-or-create-info-section-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-get-or-create-info-section-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get Or Create Info Section</span><span className="wix-picker-desc">Fetches Info Section by UniqueName, or creates it if it doesn't exist</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get Or Create Info Section</span><span className="wix-picker-desc">Fetches Info Section by UniqueName, or creates it if it doesn't exist</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-get-or-create-info-section-0"><span className="wix-picker-name">Get Or Create Info Section</span><span className="wix-picker-desc">Fetches Info Section by UniqueName, or creates it if it doesn't exist</span></label>
    <label htmlFor="ex-rest-get-or-create-info-section-1"><span className="wix-picker-name">Get Or Create Info Section</span><span className="wix-picker-desc">Fetches Info Section by UniqueName, or creates it if it doesn't exist</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
       'https://www.wixapis.com/stores/v3/info-sections/get-or-create' \
       -H 'Content-type: application/json' \
       -H 'Authorization: <AUTH>' \
       -d '{
       "infoSection": {
         "uniqueName": "New Series Cup",
         "title": "Product Features"
        }
      }'
      ```

      ```json Response
      {
        "infoSection": {
          "id": "e56fb895-b7ee-4af4-9ba4-3029fea1b7ec",
          "revision": "1",
          "createdDate": "2024-06-30T10:40:14.920Z",
          "updatedDate": "2024-06-30T10:40:14.920Z",
          "uniqueName": "New Series Cup",
          "title": "Product Features"
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
       'https://www.wixapis.com/stores/v3/info-sections/get-or-create' \
       -H 'Content-type: application/json' \
       -H 'Authorization: <AUTH>' \
       -d '{
         "infoSection": {
           "id": "5db34212-4870-4a28-b210-187be25b24cd"
         }
      }'
      ```

      ```json Response
      {
        "infoSection": {
          "id": "5db34212-4870-4a28-b210-187be25b24cd",
          "revision": "1",
          "createdDate": "2024-06-30T12:58:08.297Z",
          "updatedDate": "2024-06-30T12:58:08.297Z",
          "uniqueName": "Series 5 Cup",
          "title": "Product Features",
          "description": {
            "nodes": [
              {
                "type": "PARAGRAPH",
                "id": "ohot5271",
                "nodes": [
                  {
                    "type": "TEXT",
                    "id": "",
                    "nodes": [],
                    "textData": {
                      "text": "Dual-wall",
                      "decorations": [
                        {
                          "type": "COLOR",
                          "colorData": {
                            "foreground": "#ff0000"
                          }
                        },
                        {
                          "type": "BOLD",
                          "fontWeightValue": 700
                        }
                      ]
                    }
                  },
                  {
                    "type": "TEXT",
                    "id": "",
                    "nodes": [],
                    "textData": {
                      "text": " design for the hydration bottle keeps drinks cold for up to 24 hours & hot for up to 12 hours ",
                      "decorations": []
                    }
                  }
                ]
              },
              {
                "type": "TABLE",
                "id": "jx3te102",
                "nodes": [
                  {
                    "type": "TABLE_ROW",
                    "id": "yysx7107",
                    "nodes": [
                      {
                        "type": "TABLE_CELL",
                        "id": "gl0nw108",
                        "nodes": [
                          {
                            "type": "PARAGRAPH",
                            "id": "h0mav109",
                            "nodes": [
                              {
                                "type": "TEXT",
                                "id": "",
                                "nodes": [],
                                "textData": {
                                  "text": "product",
                                  "decorations": []
                                }
                              }
                            ]
                          }
                        ]
                      }
                    ]
                  }
                ],
                "tableData": {
                  "dimensions": {
                    "colsWidthRatio": [
                      587
                    ],
                    "rowsHeight": [
                      47
                    ],
                    "colsMinWidth": [
                      120
                    ]
                  }
                }
              },
              {
                "type": "PARAGRAPH",
                "id": "cjje5104",
                "nodes": []
              }
            ],
            "metadata": {
              "version": 1
            }
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an info section by ID or `uniqueName`, or creates an info section if one with the passed `uniqueName` doesn't exist.

    <Note>

    If an info section with the passed `uniqueName` doesn't exist, the `uniqueName` and `title` fields are required to create a new info section.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INFO_SECTION_WRITE">Info section write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { infoSectionsV3 } from '@wix/stores';

    infoSectionsV3.getOrCreateInfoSection(options): Promise<GetOrCreateInfoSectionResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="GetOrCreateInfoSectionOptions">

      options

      <Expandable title="options properties">

        <ParamField body="infoSection" type="InfoSectionForGetOrCreate">

          Info section to retrieve or create.

          <Expandable title="infoSection properties">

            <ParamField body="_id" type="string">

              Info section ID.
              Pass an ID to retrieve an existing info section.

            </ParamField>

            <ParamField body="uniqueName" type="string">

              Info section unique name.

              > **Note:**
              > + Pass the `uniqueName` of an existing info section to return it.
              > + If no such info section exists - pass `uniqueName` and `title` to create an info section.

            </ParamField>

            <ParamField body="title" type="string">

              Info section title.

              Required when passing `uniqueName` to create a new info section.

            </ParamField>

            <ParamField body="description" type="RichContent">

              Info section description using rich content.

              Learn more about [Working with Rich Content](https://dev.wix.com/docs/go-headless/tutorials-templates/other-tutorials/working-with-rich-content).

              _Nested structure omitted for brevity — see the `RichContent` object._

            </ParamField>

            <ParamField body="plainDescription" type="string">

              Info section description in HTML.

              When provided on create/update, this string must be a valid HTML. It will then be converted to rich content.
              `plainDescription` is ignored when value is also passed to the `description` field.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Supported values: `ASSIGNED_PRODUCTS_COUNT`

          Constraints: `maxItems 100`.

          **Supported values:**

          - `ASSIGNED_PRODUCTS_COUNT`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<GetOrCreateInfoSectionResponse>`.

    <ResponseField name="infoSection" type="InfoSection">

      Info section.

      See the [Info Section Object](/api-reference/business-solutions/stores/catalog-v3/info-sections-v3/info-section-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INFO_SECTIONS_LIMIT_EXCEEDED` | Number of info sections exceeded the limit of 200. |
    | `400` | `INVALID_ARGUMENT` | `ONLY_SPACES_STRING_ERROR` | `uniqueName` contains only spaces. Provide a valid `uniqueName`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Info Section Created | `wix.stores.catalog.v3.info_section_created` | Triggered when an info section is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-or-create-info-section" id="ex-sdk-get-or-create-info-section-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-or-create-info-section" id="ex-sdk-get-or-create-info-section-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-or-create-info-section" id="ex-sdk-get-or-create-info-section-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-or-create-info-section-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-or-create-info-section-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get or create an info section by ID</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get or create an info section by unique name</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getOrCreateInfoSection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-or-create-info-section-0"><span className="wix-picker-name">Get or create an info section by ID</span></label>
    <label htmlFor="ex-sdk-get-or-create-info-section-1"><span className="wix-picker-name">Get or create an info section by unique name</span></label>
    <label htmlFor="ex-sdk-get-or-create-info-section-2"><span className="wix-picker-name">getOrCreateInfoSection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get or create an info section by ID
    import { infoSectionsV3 } from "@wix/stores";

    const options = {
      infoSection: {
        id: "5db34212-4870-4a28-b210-187be25b24cd"
      }
    };

    async function getOrCreateInfoSection() {
      const response = await infoSectionsV3.getOrCreateInfoSection(options);
    }

    /* Promise resolves to:
     * {
     *   "infoSection": {
     *     "_id": "5db34212-4870-4a28-b210-187be25b24cd",
     *     "_createdDate": "2025-12-11T09:27:08.000Z",
     *     "_updatedDate": "2025-12-11T09:27:08.000Z",
     *     "revision": "1",
     *     "uniqueName": "Series Cup",
     *     "title": "Product Features"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get or create an info section by unique name
    import { infoSectionsV3 } from "@wix/stores";

    const options = {
      infoSection: {
        uniqueName: "New Series Cup",
        title: "Product Features"
      }
    };

    async function getOrCreateInfoSection() {
      const response = await infoSectionsV3.getOrCreateInfoSection(options);
    }

    /* Promise resolves to:
     * {
     *   "infoSection": {
     *     "_id": "182df67c-2b0a-4b9c-8e54-a97bf2e380b0",
     *     "_createdDate": "2025-12-11T09:27:09.000Z",
     *     "_updatedDate": "2025-12-11T09:27:09.000Z",
     *     "revision": "1",
     *     "uniqueName": "New Series Cup",
     *     "title": "Product Features"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getOrCreateInfoSection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { infoSectionsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { infoSectionsV3 },
      // Include the auth strategy and host as relevant
    });

    async function getOrCreateInfoSection(options) {
      const response = await myWixClient.infoSectionsV3.getOrCreateInfoSection(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
