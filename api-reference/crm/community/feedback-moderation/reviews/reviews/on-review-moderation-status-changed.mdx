---
title: "onReviewModerationStatusChanged"
description: "Triggered when a review's moderation status is changed or a review is created."
icon: "bolt"
---

Triggered when a review's moderation status is changed or a review is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reviews.v1.review_moderation_status_changed` |
| Package | `@wix/reviews` |
| Permissions | `Manage Stores`, `Manage Reviews` |

## Examples

<CodeGroup>

  ```javascript onReviewModerationStatusChanged
  import { reviews } from '@wix/reviews';

  reviews.onReviewModerationStatusChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onReviewModerationStatusChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reviews } from '@wix/reviews';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reviews,
      },
  });

  wixClient.reviews.onReviewModerationStatusChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
