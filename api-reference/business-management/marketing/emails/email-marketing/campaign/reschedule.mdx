---
title: "Reschedule"
description: "Changes the sending time for an already scheduled campaign."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Changes the sending time for an already scheduled campaign.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/email-marketing/v1/campaigns/{campaignId}/reschedule
    ```

    ## Path Params

    <ParamField path="campaignId" type="string" required>

      ID of the campaign to reschedule.

    </ParamField>

    ## Body Params

    <ParamField body="sendAt" type="datetime" required>

      New time for sending out the campaign.

    </ParamField>

    ## Response Object

    Returns `RescheduleResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Reschedule a scheduled campaign to send at a new time.

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/email-marketing/v1/campaigns/d699798f-dc07-4668-89f9-6db2e8ab52a3/reschedule' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
            "sendAt": "2026-09-01T14:00:00Z"
          }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Changes the sending time for an already scheduled campaign.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { campaigns } from '@wix/email-marketing';

    campaigns.reschedule(campaignId, sendAt): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="campaignId" type="string" required>

      ID of the campaign to reschedule.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="sendAt" type="datetime" required>

      New time for sending out the campaign.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-reschedule" id="ex-sdk-reschedule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-reschedule" id="ex-sdk-reschedule-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-reschedule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-reschedule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Reschedule a scheduled campaign</span><span className="wix-picker-desc">Changes the sending time for a campaign that is already scheduled</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">reschedule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-reschedule-0"><span className="wix-picker-name">Reschedule a scheduled campaign</span><span className="wix-picker-desc">Changes the sending time for a campaign that is already scheduled</span></label>
    <label htmlFor="ex-sdk-reschedule-1"><span className="wix-picker-name">reschedule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Reschedule a scheduled campaign
    import { campaigns } from "@wix/email-marketing";

    export async function rescheduleCampaign(campaignId) {
      await campaigns.reschedule(campaignId, "2026-09-01T14:00:00.000Z");
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript reschedule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { campaigns } from '@wix/email-marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { campaigns },
      // Include the auth strategy and host as relevant
    });

    async function reschedule(campaignId,sendAt) {
      const response = await myWixClient.campaigns.reschedule(campaignId,sendAt);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
