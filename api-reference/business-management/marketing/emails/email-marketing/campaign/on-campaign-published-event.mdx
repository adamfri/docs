---
title: "onCampaignPublishedEvent"
description: "Triggered when a draft campaign is published."
icon: "bolt"
---

Triggered when a draft campaign is published.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.email_marketing.v1.campaign_campaign_published_event` |
| Package | `@wix/email-marketing` |
| Permissions | `Manage Stores`, `Access Verticals by Automations`, `Set Up Automations`, `Read Email Marketing`, `Manage Email Marketing` |

## Examples

<CodeGroup>

  ```javascript onCampaignPublishedEvent
  import { campaigns } from '@wix/email-marketing';

  campaigns.onCampaignPublishedEvent((event) => {
      // handle your event here
  });
  ```

  ```javascript onCampaignPublishedEvent (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { campaigns } from '@wix/email-marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        campaigns,
      },
  });

  wixClient.campaigns.onCampaignPublishedEvent((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
