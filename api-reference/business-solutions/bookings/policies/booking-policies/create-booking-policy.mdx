---
title: "Create Booking Policy"
description: "Creates a booking policy."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a booking policy.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v1/booking-policies
    ```

    ## Body Params

    <ParamField body="bookingPolicy" type="BookingPolicy" required>

      Booking policy to create.

      See the [Booking Policy Object](/api-reference/business-solutions/bookings/policies/booking-policies/booking-policy-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="bookingPolicy" type="BookingPolicy">

      Created booking policy.

      See the [Booking Policy Object](/api-reference/business-solutions/bookings/policies/booking-policies/booking-policy-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `BOTH_LATE_AND_AFTER_START_POLICIES_NOT_ALLOWED` | Both `limitLateBookingPolicy.enabled` and `bookAfterStartPolicy.enabled` can't be set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_BOOKING_WINDOW` | `limitEarlyBookingPolicy.earliestBookingInMinutes` must be greater than `limitLateBookingPolicy.latestBookingInMinutes` when both policies are enabled. |
    | `400` | `INVALID_ARGUMENT` | `CANCELLATION_FEE_WITHOUT_CREDIT_CARD` | `saveCreditCardPolicy.enabled` must be set to `true` when `cancellationFeePolicy.enabled` is `true`. |
    | `400` | `INVALID_ARGUMENT` | `CANCELLATION_FEE_WITHOUT_DESCRIPTION` | `customPolicyDescription` is required when `cancellationFeePolicy.enabled` is set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CANCELLATION_WINDOWS_ORDER` | `cancellationFeePolicy.cancellationWindows` must be provided in descending order. |
    | `400` | `INVALID_ARGUMENT` | `EMPTY_CANCELLATION_WINDOWS_LIST` | `cancellationFeePolicy.cancellationWindows` is required when `cancellationFeePolicy.enabled` is set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_ID_REQUIRED` | `intakeFormPolicy.formId` is required when `intakeFormPolicy.enabled` is `true`. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_NOT_FOUND` | The referenced intake form must exist. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_ARCHIVED` | The referenced intake form must not be archived. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_APP_NOT_INSTALLED` | The Intake Forms app must be installed. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_COMPLETION_REQUIREMENT_FOR_TIMING` | `REQUIRED_BEFORE_BOOKING` can only be used with `"timing": "BEFORE_BOOKING"`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Booking Policy Created | `wix.bookings.v1.booking_policy_created` | Triggered when a booking policy is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-create-booking-policy" id="ex-rest-create-booking-policy-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-create-booking-policy" id="ex-rest-create-booking-policy-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-create-booking-policy-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-create-booking-policy-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a bookingPolicy object when only some of sub-policy fields were provided.</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create a bookingPolicy object when all sub-policy fields were provided.</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-create-booking-policy-0"><span className="wix-picker-name">Create a bookingPolicy object when only some of sub-policy fields were provided.</span></label>
    <label htmlFor="ex-rest-create-booking-policy-1"><span className="wix-picker-name">Create a bookingPolicy object when all sub-policy fields were provided.</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://wixapis.com/bookings/v1/booking-policies' \
      -H 'Authorization: <AUTH>' \
      -d '{
            "bookingPolicy": {
              "name": "My custom policy 2",
              "participantsPolicy": {
                "maxParticipantsPerBooking": 1
              },
              "limitLateBookingPolicy": {
                "enabled": true,
                "latestBookingInMinutes": 180
              },
              "limitEarlyBookingPolicy": {
                "enabled": true,
                "earliestBookingInMinutes": 43200
              }
            }
          }'
      ```

      ```json Response
      {
        "bookingPolicy": {
          "id": "6806bf45-1d65-4983-a607-6cd277ec732a",
          "revision": "1",
          "createdDate": "2023-02-16T10:48:59.975Z",
          "updatedDate": "2023-02-16T10:48:59.975Z",
          "name": "My custom policy 2",
          "customPolicyDescription": {
            "enabled": false,
            "description": ""
          },
          "default": false,
          "limitEarlyBookingPolicy": {
            "enabled": true,
            "earliestBookingInMinutes": 43200
          },
          "limitLateBookingPolicy": {
            "enabled": true,
            "latestBookingInMinutes": 180
          },
          "bookAfterStartPolicy": {
            "enabled": false
          },
          "cancellationPolicy": {
            "enabled": false,
            "limitLatestCancellation": false,
            "latestCancellationInMinutes": 1440
          },
          "reschedulePolicy": {
            "enabled": false,
            "limitLatestReschedule": false,
            "latestRescheduleInMinutes": 1440
          },
          "waitlistPolicy": {
            "enabled": false,
            "capacity": 10,
            "reservationTimeInMinutes": 10
          },
          "participantsPolicy": {
            "maxParticipantsPerBooking": 1
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://wixapis.com/bookings/v1/booking-policies' \
      -H 'Authorization: <AUTH>' \
      -d '{
            "bookingPolicy": {
              "name": "My custom policy",
              "customPolicyDescription": {
                "enabled": true,
                "description": "To cancel or reschedule, please contact us at least 24 hours in advance"
              },
              "limitEarlyBookingPolicy": {
                "enabled": true,
                "earliestBookingInMinutes": 43200
              },
              "limitLateBookingPolicy": {
                "enabled": true,
                "latestBookingInMinutes": 180
              },
              "bookAfterStartPolicy": {
                "enabled": false
              },
              "cancellationPolicy": {
                "enabled": true,
                "limitLatestCancellation": true,
                "latestCancellationInMinutes": 1440
              },
              "reschedulePolicy": {
                "enabled": true,
                "limitLatestReschedule": true,
                "latestRescheduleInMinutes": 1440
              },
              "waitlistPolicy": {
                "enabled": true,
                "capacity": 10,
                "reservationTimeInMinutes": 30
              },
              "participantsPolicy": {
                "maxParticipantsPerBooking": 1
              }
            }
          }'
      ```

      ```json Response
      {
        "bookingPolicy": {
          "id": "1a426c47-0c86-46c7-8573-c7bce734f660",
          "revision": "1",
          "createdDate": "2023-02-16T10:46:17.420Z",
          "updatedDate": "2023-02-16T10:46:17.420Z",
          "name": "My custom policy",
          "customPolicyDescription": {
            "enabled": true,
            "description": "To cancel or reschedule, please contact us at least 24 hours in advance"
          },
          "default": false,
          "limitEarlyBookingPolicy": {
            "enabled": true,
            "earliestBookingInMinutes": 43200
          },
          "limitLateBookingPolicy": {
            "enabled": true,
            "latestBookingInMinutes": 180
          },
          "bookAfterStartPolicy": {
            "enabled": false
          },
          "cancellationPolicy": {
            "enabled": true,
            "limitLatestCancellation": true,
            "latestCancellationInMinutes": 1440
          },
          "reschedulePolicy": {
            "enabled": true,
            "limitLatestReschedule": true,
            "latestRescheduleInMinutes": 1440
          },
          "waitlistPolicy": {
            "enabled": true,
            "capacity": 10,
            "reservationTimeInMinutes": 30
          },
          "participantsPolicy": {
            "maxParticipantsPerBooking": 1
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a booking policy.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { bookingPolicies } from '@wix/bookings';

    bookingPolicies.createBookingPolicy(bookingPolicy): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="bookingPolicy" type="BookingPolicy" required>

      Booking policy to create.

      See the [Booking Policy Object](/api-reference/business-solutions/bookings/policies/booking-policies/booking-policy-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    See the [Booking Policy Object](/api-reference/business-solutions/bookings/policies/booking-policies/booking-policy-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `BOTH_LATE_AND_AFTER_START_POLICIES_NOT_ALLOWED` | Both `limitLateBookingPolicy.enabled` and `bookAfterStartPolicy.enabled` can't be set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_BOOKING_WINDOW` | `limitEarlyBookingPolicy.earliestBookingInMinutes` must be greater than `limitLateBookingPolicy.latestBookingInMinutes` when both policies are enabled. |
    | `400` | `INVALID_ARGUMENT` | `CANCELLATION_FEE_WITHOUT_CREDIT_CARD` | `saveCreditCardPolicy.enabled` must be set to `true` when `cancellationFeePolicy.enabled` is `true`. |
    | `400` | `INVALID_ARGUMENT` | `CANCELLATION_FEE_WITHOUT_DESCRIPTION` | `customPolicyDescription` is required when `cancellationFeePolicy.enabled` is set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CANCELLATION_WINDOWS_ORDER` | `cancellationFeePolicy.cancellationWindows` must be provided in descending order. |
    | `400` | `INVALID_ARGUMENT` | `EMPTY_CANCELLATION_WINDOWS_LIST` | `cancellationFeePolicy.cancellationWindows` is required when `cancellationFeePolicy.enabled` is set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_ID_REQUIRED` | `intakeFormPolicy.formId` is required when `intakeFormPolicy.enabled` is `true`. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_NOT_FOUND` | The referenced intake form must exist. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_ARCHIVED` | The referenced intake form must not be archived. |
    | `400` | `INVALID_ARGUMENT` | `INTAKE_FORM_APP_NOT_INSTALLED` | The Intake Forms app must be installed. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_COMPLETION_REQUIREMENT_FOR_TIMING` | `REQUIRED_BEFORE_BOOKING` can only be used with `"timing": "BEFORE_BOOKING"`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Booking Policy Created | `wix.bookings.v1.booking_policy_created` | Triggered when a booking policy is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-booking-policy" id="ex-sdk-create-booking-policy-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-booking-policy" id="ex-sdk-create-booking-policy-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-booking-policy" id="ex-sdk-create-booking-policy-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-booking-policy-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-booking-policy-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createBookingPolicy</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createBookingPolicy (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createBookingPolicy (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-booking-policy-0"><span className="wix-picker-name">createBookingPolicy</span></label>
    <label htmlFor="ex-sdk-create-booking-policy-1"><span className="wix-picker-name">createBookingPolicy (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-booking-policy-2"><span className="wix-picker-name">createBookingPolicy (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createBookingPolicy
    import { bookingPolicies } from '@wix/bookings';

    async function createBookingPolicy(bookingPolicy) {
      const response = await bookingPolicies.createBookingPolicy(bookingPolicy);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createBookingPolicy (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bookingPolicies } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bookingPolicies },
      // Include the auth strategy and host as relevant
    });

    async function createBookingPolicy(bookingPolicy) {
      const response = await myWixClient.bookingPolicies.createBookingPolicy(bookingPolicy);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createBookingPolicy (with elevated permissions)
    import { bookingPolicies } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myCreateBookingPolicyMethod(bookingPolicy) {
      const elevatedCreateBookingPolicy = auth.elevate(bookingPolicies.createBookingPolicy);
      const response = await elevatedCreateBookingPolicy(bookingPolicy);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
