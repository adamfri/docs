---
title: "onBadgeDeleted"
description: "Triggered when a badge is deleted."
icon: "bolt"
---

Triggered when a badge is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v4.badge_deleted` |
| Package | `@wix/members` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Manage Stores`, `Manage Events`, `Manage Online Programs`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeDeleted
  import { badgesV2 } from '@wix/members';

  badgesV2.onBadgeDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badgesV2 } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        badgesV2,
      },
  });

  wixClient.badgesV2.onBadgeDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
