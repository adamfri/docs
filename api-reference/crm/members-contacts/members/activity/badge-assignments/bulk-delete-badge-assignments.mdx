---
title: "Bulk Delete Badge Assignments"
description: "Deletes multiple badge assignments, removing badges from the specified members."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple badge assignments, removing badges from the specified members.

    When a badge assignment is deleted, the member loses any permissions or privileges associated with that badge.
    This action can't be undone.

    To delete a single assignment, call [Delete Badge Assignment](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/delete-badge-assignment).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-BADGES">Manage Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/badges/v4/bulk/assignments/delete
    ```

    ## Body Params

    <ParamField body="badgeAssignmentIds" type="string[]" required>

      ID of the badge assignments to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkBadgeAssignmentResult[]">

      Results of the bulk delete operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata regarding the specific single update operation

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="BadgeAssignment">

          Only exists if `returnEntity` was set to true in the request

          See the [Badge Assignment Object](/api-reference/crm/members-contacts/members/activity/badge-assignments/badge-assignment-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata about the bulk operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Badge Assignment Deleted | `wix.badges.v4.badge_assignment_deleted` | Triggered when a badge assignment is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        https://www.wixapis.com/badges/v4/bulk/assignments/delete \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
              "badgeAssignmentIds": [
                "3ed982d6-2f45-41b8-a6f8-fdae05c3ea43",
                "921cac5d-acc6-45ef-9b55-99f8954f6f86"
              ]
            }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "3ed982d6-2f45-41b8-a6f8-fdae05c3ea43",
              "originalIndex": 0,
              "success": true
            },
            "item": null
          },
          {
            "itemMetadata": {
              "id": "921cac5d-acc6-45ef-9b55-99f8954f6f86",
              "originalIndex": 1,
              "success": true
            },
            "item": null
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple badge assignments, removing badges from the specified members.

    When a badge assignment is deleted, the member loses any permissions or privileges associated with that badge.
    This action can't be undone.

    To delete a single assignment, call [Delete Badge Assignment](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/delete-badge-assignment).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-BADGES">Manage Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { badgeAssignments } from '@wix/members';

    badgeAssignments.bulkDeleteBadgeAssignments(badgeAssignmentIds): Promise<BulkDeleteBadgeAssignmentsResponse>
    ```

    ## Method Parameters

    <ParamField body="badgeAssignmentIds" type="string[]" required>

      ID of the badge assignments to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Returns

    Returns `Promise<BulkDeleteBadgeAssignmentsResponse>`.

    <ResponseField name="results" type="BulkBadgeAssignmentResult[]">

      Results of the bulk delete operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata regarding the specific single update operation

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="BadgeAssignment">

          Only exists if `returnEntity` was set to true in the request

          See the [Badge Assignment Object](/api-reference/crm/members-contacts/members/activity/badge-assignments/badge-assignment-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata about the bulk operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Badge Assignment Deleted | `wix.badges.v4.badge_assignment_deleted` | Triggered when a badge assignment is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-badge-assignments" id="ex-sdk-bulk-delete-badge-assignments-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-badge-assignments" id="ex-sdk-bulk-delete-badge-assignments-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-badge-assignments" id="ex-sdk-bulk-delete-badge-assignments-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-badge-assignments-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-badge-assignments-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteBadgeAssignments</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteBadgeAssignments (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteBadgeAssignments (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-badge-assignments-0"><span className="wix-picker-name">bulkDeleteBadgeAssignments</span></label>
    <label htmlFor="ex-sdk-bulk-delete-badge-assignments-1"><span className="wix-picker-name">bulkDeleteBadgeAssignments (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-badge-assignments-2"><span className="wix-picker-name">bulkDeleteBadgeAssignments (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteBadgeAssignments
    import { badgeAssignments } from '@wix/members';

    async function bulkDeleteBadgeAssignments(badgeAssignmentIds) {
      const response = await badgeAssignments.bulkDeleteBadgeAssignments(badgeAssignmentIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteBadgeAssignments (self-hosted)
    import { createClient } from '@wix/sdk';
    import { badgeAssignments } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { badgeAssignments },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteBadgeAssignments(badgeAssignmentIds) {
      const response = await myWixClient.badgeAssignments.bulkDeleteBadgeAssignments(badgeAssignmentIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteBadgeAssignments (with elevated permissions)
    import { badgeAssignments } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteBadgeAssignmentsMethod(badgeAssignmentIds) {
      const elevatedBulkDeleteBadgeAssignments = auth.elevate(badgeAssignments.bulkDeleteBadgeAssignments);
      const response = await elevatedBulkDeleteBadgeAssignments(badgeAssignmentIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
