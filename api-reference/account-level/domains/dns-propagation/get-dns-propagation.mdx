---
title: "Get Dns Propagation"
description: "Retrieves DNS propagation status for a domain."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves DNS propagation status for a domain.

    Use this method to check if DNS records are properly configured and propagated after connecting a custom domain,
    during domain setup to see required DNS records, or to monitor ongoing propagation status after making DNS changes.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.READ_DNS_PROPAGATION">DOMAINS.READ_DNS_PROPAGATION</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    GET https://www.wixapis.com/premium/domains/v1/dns-propagations/{dnsPropagationId}
    ```

    ## Path Params

    <ParamField path="dnsPropagationId" type="string" required>

      ID of the DNS propagation to retrieve. Identical to the domain name, including [TLD](https://dev.wix.com/docs/api-reference/account-level/domains/dns-propagation/introduction#terminology).

    </ParamField>

    ## Response Object

    <ResponseField name="dnsPropagation" type="DnsPropagation">

      Retrieved DNS propagation.

      See the [Dns Propagation Object](/api-reference/account-level/domains/dns-propagation/dns-propagation-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-get-dns-propagation" id="ex-rest-get-dns-propagation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-get-dns-propagation" id="ex-rest-get-dns-propagation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-get-dns-propagation" id="ex-rest-get-dns-propagation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-get-dns-propagation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-get-dns-propagation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get DNS Propagation Status - In Progress</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when DNS records are still being checked and propagated</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get DNS Propagation Status - Success Case</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when all DNS records are properly configured and propagated</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get DNS Propagation Status</span><span className="wix-picker-desc">Retrieves the DNS propagation status for a domain to check if DNS records are properly configured and propagated</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-get-dns-propagation-0"><span className="wix-picker-name">Get DNS Propagation Status - In Progress</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when DNS records are still being checked and propagated</span></label>
    <label htmlFor="ex-rest-get-dns-propagation-1"><span className="wix-picker-name">Get DNS Propagation Status - Success Case</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when all DNS records are properly configured and propagated</span></label>
    <label htmlFor="ex-rest-get-dns-propagation-2"><span className="wix-picker-name">Get DNS Propagation Status</span><span className="wix-picker-desc">Retrieves the DNS propagation status for a domain to check if DNS records are properly configured and propagated</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/v1/dns-propagations/newdomain.com' \
      -H 'Authorization: <AUTH>' \
      -H 'wix-site-id: <SITE_ID>'
      ```

      ```json Response
      {
        "dnsPropagation": {
          "id": "newdomain.com",
          "domain": "newdomain.com",
          "revision": "1",
          "createdDate": "2024-01-15T16:00:00.000Z",
          "updatedDate": "2024-01-15T16:00:00.000Z",
          "status": "IN_PROGRESS",
          "dnsResolverIncludesWixNs": false
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/v1/dns-propagations/mystore.com' \
      -H 'Authorization: <AUTH>' \
      -H 'wix-site-id: <SITE_ID>'
      ```

      ```json Response
      {
        "dnsPropagation": {
          "id": "mystore.com",
          "domain": "mystore.com",
          "revision": "3",
          "createdDate": "2024-01-10T08:00:00.000Z",
          "updatedDate": "2024-01-12T14:30:00.000Z",
          "status": "SUCCEEDED",
          "dnsResolverIncludesWixNs": true
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/v1/dns-propagations/example.com' \
      -H 'Authorization: <AUTH>' \
      -H 'wix-site-id: <SITE_ID>'
      ```

      ```json Response
      {
        "dnsPropagation": {
          "id": "example.com",
          "domain": "example.com",
          "revision": "1",
          "createdDate": "2024-01-15T10:30:00.000Z",
          "updatedDate": "2024-01-15T12:45:00.000Z",
          "status": "FAILED",
          "failureInfo": {
            "invalidRecords": ["INVALID_A_RECORD", "INVALID_CNAME_RECORD"],
            "invalidARecordInfo": {
              "expectedDnsRecords": [
                {
                  "hostName": "example.com",
                  "values": ["185.230.63.107", "185.230.63.108"]
                }
              ],
              "actualDnsRecords": [
                {
                  "hostName": "example.com",
                  "values": ["192.168.1.1"]
                }
              ]
            },
            "invalidCnameRecordInfo": {
              "expectedDnsRecords": [
                {
                  "hostName": "www.example.com",
                  "values": ["example.com.wixdns.net"]
                }
              ],
              "actualDnsRecords": [
                {
                  "hostName": "www.example.com",
                  "values": ["old-server.example.com"]
                }
              ]
            }
          },
          "dnsResolverIncludesWixNs": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves DNS propagation status for a domain.

    Use this method to check if DNS records are properly configured and propagated after connecting a custom domain,
    during domain setup to see required DNS records, or to monitor ongoing propagation status after making DNS changes.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.READ_DNS_PROPAGATION">DOMAINS.READ_DNS_PROPAGATION</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { dnsPropagation } from '@wix/domains';

    dnsPropagation.getDnsPropagation(dnsPropagationId): Promise
    ```

    ## Method Parameters

    <ParamField body="dnsPropagationId" type="string" required>

      ID of the DNS propagation to retrieve. Identical to the domain name, including [TLD](https://dev.wix.com/docs/api-reference/account-level/domains/dns-propagation/introduction#terminology).

      Constraints: `format HOSTNAME`.

    </ParamField>

    ## Returns

    Returns `DnsPropagation`.

    See the [Dns Propagation Object](/api-reference/account-level/domains/dns-propagation/dns-propagation-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-dns-propagation" id="ex-sdk-get-dns-propagation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-dns-propagation" id="ex-sdk-get-dns-propagation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-dns-propagation" id="ex-sdk-get-dns-propagation-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-dns-propagation" id="ex-sdk-get-dns-propagation-3" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-dns-propagation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-dns-propagation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get DNS Propagation Status</span><span className="wix-picker-desc">Retrieves the DNS propagation status for a domain to check if DNS records are properly configured and propagated</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get DNS Propagation Status - Success Case</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when all DNS records are properly configured and propagated</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get DNS Propagation Status - In Progress</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when DNS records are still being checked and propagated</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDnsPropagation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-dns-propagation-0"><span className="wix-picker-name">Get DNS Propagation Status</span><span className="wix-picker-desc">Retrieves the DNS propagation status for a domain to check if DNS records are properly configured and propagated</span></label>
    <label htmlFor="ex-sdk-get-dns-propagation-1"><span className="wix-picker-name">Get DNS Propagation Status - Success Case</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when all DNS records are properly configured and propagated</span></label>
    <label htmlFor="ex-sdk-get-dns-propagation-2"><span className="wix-picker-name">Get DNS Propagation Status - In Progress</span><span className="wix-picker-desc">Example of retrieving DNS propagation status when DNS records are still being checked and propagated</span></label>
    <label htmlFor="ex-sdk-get-dns-propagation-3"><span className="wix-picker-name">getDnsPropagation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get DNS Propagation Status
    import { dnsPropagation } from "@wix/dns-propagation";

    async function getDnsPropagation() {
      const response = await dnsPropagation.getDnsPropagation("example.com");

      return response;
    }

    /* Promise resolves to:
     * {
     *   "_id": "example.com",
     *   "domain": "example.com",
     *   "revision": "1",
     *   "_createdDate": "2024-01-15T10:30:00.000Z",
     *   "_updatedDate": "2024-01-15T12:45:00.000Z",
     *   "status": "FAILED",
     *   "failureInfo": {
     *     "invalidRecords": ["INVALID_A_RECORD", "INVALID_CNAME_RECORD"],
     *     "invalidARecordInfo": {
     *       "expectedDnsRecords": [
     *         {
     *           "hostName": "example.com",
     *           "values": ["185.230.63.107", "185.230.63.108"]
     *         }
     *       ],
     *       "actualDnsRecords": [
     *         {
     *           "hostName": "example.com",
     *           "values": ["192.168.1.1"]
     *         }
     *       ]
     *     },
     *     "invalidCnameRecordInfo": {
     *       "expectedDnsRecords": [
     *         {
     *           "hostName": "www.example.com",
     *           "values": ["example.com.wixdns.net"]
     *         }
     *       ],
     *       "actualDnsRecords": [
     *         {
     *           "hostName": "www.example.com",
     *           "values": ["old-server.example.com"]
     *         }
     *       ]
     *     }
     *   },
     *   "dnsResolverIncludesWixNs": true
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get DNS Propagation Status - Success Case
    import { dnsPropagation } from "@wix/dns-propagation";

    async function getDnsPropagationSuccess() {
      const response = await dnsPropagation.getDnsPropagation("mystore.com");

      return response;
    }

    /* Promise resolves to:
     * {
     *   "_id": "mystore.com",
     *   "domain": "mystore.com",
     *   "revision": "3",
     *   "_createdDate": "2024-01-10T08:00:00.000Z",
     *   "_updatedDate": "2024-01-12T14:30:00.000Z",
     *   "status": "SUCCEEDED",
     *   "dnsResolverIncludesWixNs": true
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get DNS Propagation Status - In Progress
    import { dnsPropagation } from "@wix/dns-propagation";

    async function getDnsPropagationInProgress() {
      const response = await dnsPropagation.getDnsPropagation("newdomain.com");

      return response;
    }

    /* Promise resolves to:
     * {
     *   "_id": "newdomain.com",
     *   "domain": "newdomain.com",
     *   "revision": "1",
     *   "_createdDate": "2024-01-15T16:00:00.000Z",
     *   "_updatedDate": "2024-01-15T16:00:00.000Z",
     *   "status": "IN_PROGRESS",
     *   "dnsResolverIncludesWixNs": false
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDnsPropagation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { dnsPropagation } from '@wix/domains';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { dnsPropagation },
      // Include the auth strategy and host as relevant
    });

    async function getDnsPropagation(dnsPropagationId) {
      const response = await myWixClient.dnsPropagation.getDnsPropagation(dnsPropagationId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
