---
title: "Bulk Update Contacts"
description: "Updates the specified properties for multiple contacts."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the specified properties for multiple contacts.
    Fields that are included in fieldMask.paths are updated,
    while all other fields stay the same.

    All contacts that meet the specified `filter` and `search` criteria are updated.
    To perform a dry run, call Query Contacts with the intended filter options.

    When this method is used, a bulk job is started and the job ID is returned.
    The job may not complete right away, depending on its size.
    The job's status can be retrieved with Get Bulk Job.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACTS">Manage Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/contacts/v4/bulk/contacts/update
    ```

    ## Body Params

    <ParamField body="filter" type="object">

      Filter object.

      Possible filters:
      `$eq`, `$exists`, `$gt`, `$gte`, `$hasAll`, `$hasSome`, `$in`, `$lt`, `$lte`, `$ne`, `$startsWith`.

      See [Sort, Filter, and Search](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/contact-v4/sort-filter-and-search)
      for a detailed list of supported filters for contact properties, extended fields, and custom fields.

      Example:
      `{ "filter": { "info.name.last": "Smith" } }`

    </ParamField>

    <ParamField body="search" type="string">

      Plain text search for an exact match, up to 100 characters.

      Searchable fields:

      - `info.name.first`
      - `info.name.last`
      - `info.emails.email`
      - `info.phones.phone`

    </ParamField>

    <ParamField body="info" type="ContactInfo">

      Contact info.

      <Expandable title="info properties">

        <ParamField body="name" type="ContactName">

          Contact's first and last name.

          <Expandable title="name properties">

            <ParamField body="first" type="string">

              Contact's first name.

            </ParamField>

            <ParamField body="last" type="string">

              Contact's last name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="emails" type="ContactEmailsWrapper">

          Contact's email addresses.

          <Expandable title="emails properties">

            <ParamField body="items" type="ContactEmail[]">

              List of up to 50 email addresses.

              Constraints: `maxItems 50`.

              _Nested structure omitted for brevity — see the `ContactEmail[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="phones" type="ContactPhonesWrapper">

          Contact's phone numbers.

          <Expandable title="phones properties">

            <ParamField body="items" type="ContactPhone[]">

              List of up to 50 phone numbers.

              Constraints: `maxItems 50`.

              _Nested structure omitted for brevity — see the `ContactPhone[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="addresses" type="ContactAddressesWrapper">

          Contact's street addresses.

          <Expandable title="addresses properties">

            <ParamField body="items" type="ContactAddress[]">

              List of up to 50 addresses.

              Constraints: `maxItems 50`.

              _Nested structure omitted for brevity — see the `ContactAddress[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="company" type="string">

          Contact's company name.

        </ParamField>

        <ParamField body="jobTitle" type="string">

          Contact's job title.

        </ParamField>

        <ParamField body="birthdate" type="string">

          Birth date in `YYYY-MM-DD` format. For example, `2020-03-15`.

        </ParamField>

        <ParamField body="locale" type="string">

          Locale in
          [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
          Typically, this is a lowercase 2-letter language code,
          followed by a hyphen, followed by an uppercase 2-letter country code.
          For example, `en-US` for U.S. English, and `de-DE` for Germany German.

        </ParamField>

        <ParamField body="labelKeys" type="LabelsWrapper">

          List of contact's labels.

          Labels are used to organize contacts. Labels can be
          added and removed using Label Contact and Unlabel Contact, respectively.

          To view or manage contact labels, use the Labels API.

          <Expandable title="labelKeys properties">

            <ParamField body="items" type="string[]">

              List of contact label keys.

              Contact labels help categorize contacts. Label keys must exist to be added to the contact.
              Contact labels can be created or retrieved with Find or Create Label or List Labels.

              Constraints: `maxItems 2000`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFieldsWrapper">

          Additional custom fields.

          Empty fields are not returned.

          <Expandable title="extendedFields properties">

            <ParamField body="items" type="object">

              Contact's extended fields,
              where each key is the field key,
              and each value is the field's value for the contact.

              To view and manage extended fields, use the Extended Fields API.

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="picture" type="ContactPicture">

          Contact's profile picture.

          <Expandable title="picture properties">

            <ParamField body="image" type="Image">

              Image.
              This can contain an image URL or a Wix Media image ID.

              _Nested structure omitted for brevity — see the `Image` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Bulk job ID.
      The job's status can be retrieved with Get Bulk Job.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Contact Updated | `wix.contacts.v4.contact_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    Updates last name and company for all contacts with first name "John".

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/contacts/v4/bulk/contacts/update' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{
          "filter": {
            "info.name.first": "John"
          },
          "info": {
            "name": {
              "first": "John",
              "last": "Changed"
            },
            "company": "Wix"
          }
        }'
      ```

      ```json Response
      {
        "jobId": "8046df3c-1234-0aa7-a5ab-c91ad8f33c47"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the specified properties for multiple contacts.
    Fields that are included in fieldMask.paths are updated,
    while all other fields stay the same.

    All contacts that meet the specified `filter` and `search` criteria are updated.
    To perform a dry run, call Query Contacts with the intended filter options.

    When this method is used, a bulk job is started and the job ID is returned.
    The job may not complete right away, depending on its size.
    The job's status can be retrieved with Get Bulk Job.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACTS">Manage Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contacts } from '@wix/crm';

    contacts.bulkUpdateContacts(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkUpdateContactsOptions">

      Bulk update contacts options.

      <Expandable title="options properties">

        <ParamField body="filter" type="object">

          Filter object.

          Possible filters:
          `$eq`, `$exists`, `$gt`, `$gte`, `$hasAll`, `$hasSome`, `$in`, `$lt`, `$lte`, `$ne`, `$startsWith`.

          See [Sort, Filter, and Search](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/contact-v4/sort-filter-and-search)
          for a detailed list of supported filters for contact properties, extended fields, and custom fields.

          Example:
          `{ "filter": { "info.name.last": "Smith" } }`

        </ParamField>

        <ParamField body="search" type="string">

          Plain text search for an exact match, up to 100 characters.

          Searchable fields:

          - `info.name.first`
          - `info.name.last`
          - `info.emails.email`
          - `info.phones.phone`

        </ParamField>

        <ParamField body="info" type="ContactInfo">

          Contact info.

          <Expandable title="info properties">

            <ParamField body="name" type="ContactName">

              Contact's first and last name.

              _Nested structure omitted for brevity — see the `ContactName` object._

            </ParamField>

            <ParamField body="emails" type="ContactEmailsWrapper">

              Contact's email addresses.

              _Nested structure omitted for brevity — see the `ContactEmailsWrapper` object._

            </ParamField>

            <ParamField body="phones" type="ContactPhonesWrapper">

              Contact's phone numbers.

              _Nested structure omitted for brevity — see the `ContactPhonesWrapper` object._

            </ParamField>

            <ParamField body="addresses" type="ContactAddressesWrapper">

              Contact's street addresses.

              _Nested structure omitted for brevity — see the `ContactAddressesWrapper` object._

            </ParamField>

            <ParamField body="company" type="string">

              Contact's company name.

            </ParamField>

            <ParamField body="jobTitle" type="string">

              Contact's job title.

            </ParamField>

            <ParamField body="birthdate" type="string">

              Birth date in `YYYY-MM-DD` format. For example, `2020-03-15`.

            </ParamField>

            <ParamField body="locale" type="string">

              Locale in
              [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
              Typically, this is a lowercase 2-letter language code,
              followed by a hyphen, followed by an uppercase 2-letter country code.
              For example, `en-US` for U.S. English, and `de-DE` for Germany German.

            </ParamField>

            <ParamField body="labelKeys" type="LabelsWrapper">

              List of contact's labels.

              Labels are used to organize contacts. Labels can be
              added and removed using Label Contact and Unlabel Contact, respectively.

              To view or manage contact labels, use the Labels API.

              _Nested structure omitted for brevity — see the `LabelsWrapper` object._

            </ParamField>

            <ParamField body="extendedFields" type="ExtendedFieldsWrapper">

              Additional custom fields.

              Empty fields are not returned.

              _Nested structure omitted for brevity — see the `ExtendedFieldsWrapper` object._

            </ParamField>

            <ParamField body="picture" type="ContactPicture">

              Contact's profile picture.

              _Nested structure omitted for brevity — see the `ContactPicture` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="jobId" type="string">

      Bulk job ID.
      The job's status can be retrieved with Get Bulk Job.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Contact Updated | `wix.contacts.v4.contact_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-contacts" id="ex-sdk-bulk-update-contacts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-contacts" id="ex-sdk-bulk-update-contacts-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-update-contacts" id="ex-sdk-bulk-update-contacts-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-contacts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-contacts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateContacts</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateContacts (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-contacts-0"><span className="wix-picker-name">bulkUpdateContacts</span></label>
    <label htmlFor="ex-sdk-bulk-update-contacts-1"><span className="wix-picker-name">bulkUpdateContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-update-contacts-2"><span className="wix-picker-name">bulkUpdateContacts (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUpdateContacts
    import { contacts } from '@wix/crm';

    async function bulkUpdateContacts(options) {
      const response = await contacts.bulkUpdateContacts(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateContacts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contacts } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contacts },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdateContacts(options) {
      const response = await myWixClient.contacts.bulkUpdateContacts(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateContacts (with elevated permissions)
    import { contacts } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myBulkUpdateContactsMethod(options) {
      const elevatedBulkUpdateContacts = auth.elevate(contacts.bulkUpdateContacts);
      const response = await elevatedBulkUpdateContacts(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
