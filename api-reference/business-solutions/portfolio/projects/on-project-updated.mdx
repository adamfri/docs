---
title: "onProjectUpdated"
description: "Triggered when a project is updated."
icon: "bolt"
---

Triggered when a project is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.projects.v1.project_updated` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onProjectUpdated
  import { projects } from '@wix/portfolio';

  projects.onProjectUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onProjectUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { projects } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        projects,
      },
  });

  wixClient.projects.onProjectUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
