---
title: "Bulk Get Multi Service Booking Allowed Actions"
description: "Retrieves information about which actions the customer can perform for up to 50 multi-service bookings."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves information about which actions the customer can perform for up to 50 multi-service bookings.

    For each multi-service booking, the response indicates which actions are currently allowed:
    - `cancel`: Whether the customer can cancel the multi-service booking.
    - `reschedule`: Whether the customer can adjust the multi-service booking's timing.

    Bear the following considerations in mind when calling this method:

    __Real-time validation__: Wix Bookings calculates allowed actions based on current multi-service booking status, [booking policies](https://dev.wix.com/docs/rest/business-solutions/bookings/policies/booking-policies/introduction), and [available resources](https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resources-v2/introduction) at the time of the call.

    __Permission context__: Depending on the permissions of the calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities), you may see different allowed actions for the same multi-service booking. For example, if the identity has permissions to read only part of the multi-service booking, the response indicates which single-service bookings the identity can read.

    __Status dependencies__: Allowed actions change as bookings progress through their lifecycle (`CREATED` → `PENDING` → `CONFIRMED`/`DECLINED` → `CANCELED`).
    Bookings can skip `PENDING` and move directly from `CREATED` to `CONFIRMED`/`DECLINED` based on service configuration.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/multiServiceBookings/v2/bulk/multi_service_bookings/get_allowed_actions
    ```

    ## Body Params

    <ParamField body="multiServiceBookingIds" type="string[]" required>

      IDs of the multi-service bookings to retrieve allowed actions for.

      Constraints: `format GUID`, `minItems 1`, `maxItems 50`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkCalculateAllowedActionsResult[]">

      Information about the multi-service bookings that were retrieved.
      Includes their ID, index in the bulk request and whether they were successfully processed.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the booking. Including ID, index in the provided sequence, success status, and error.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="AllowedActions">

          Booking entity.

          <Expandable title="item properties">

            <ResponseField name="cancel" type="boolean">

              Whether canceling the booking is allowed.

            </ResponseField>

            <ResponseField name="reschedule" type="boolean">

              Whether rescheduling the booking is allowed.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total number of successes and failures for Bulk Get Multi Service Booking Allowed Actions.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/bookings/multiServiceBookings/v2/bulk/multi_service_bookings/get_allowed_actions' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "multiServiceBookingIds": [
            "msb-12345",
            "msb-67890",
            "msb-11111"
          ]
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "msb-12345",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "cancel": true,
              "reschedule": true
            }
          },
          {
            "itemMetadata": {
              "id": "msb-67890",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "cancel": true,
              "reschedule": false
            }
          },
          {
            "itemMetadata": {
              "id": "msb-11111",
              "originalIndex": 2,
              "success": true
            },
            "item": {
              "cancel": false,
              "reschedule": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 3,
          "totalFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves information about which actions the customer can perform for up to 50 multi-service bookings.

    For each multi-service booking, the response indicates which actions are currently allowed:
    - `cancel`: Whether the customer can cancel the multi-service booking.
    - `reschedule`: Whether the customer can adjust the multi-service booking's timing.

    Bear the following considerations in mind when calling this method:

    __Real-time validation__: Wix Bookings calculates allowed actions based on current multi-service booking status, [booking policies](https://dev.wix.com/docs/rest/business-solutions/bookings/policies/booking-policies/introduction), and [available resources](https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resources-v2/introduction) at the time of the call.

    __Permission context__: Depending on the permissions of the calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities), you may see different allowed actions for the same multi-service booking. For example, if the identity has permissions to read only part of the multi-service booking, the response indicates which single-service bookings the identity can read.

    __Status dependencies__: Allowed actions change as bookings progress through their lifecycle (`CREATED` → `PENDING` → `CONFIRMED`/`DECLINED` → `CANCELED`).
    Bookings can skip `PENDING` and move directly from `CREATED` to `CONFIRMED`/`DECLINED` based on service configuration.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { bookings } from '@wix/bookings';

    bookings.bulkGetMultiServiceBookingAllowedActions(multiServiceBookingIds): Promise<BulkGetMultiServiceBookingAllowedActionsResponse>
    ```

    ## Method Parameters

    <ParamField body="multiServiceBookingIds" type="string[]" required>

      IDs of the multi-service bookings to retrieve allowed actions for.

      Constraints: `format GUID`, `minItems 1`, `maxItems 50`.

    </ParamField>

    ## Returns

    Returns `Promise<BulkGetMultiServiceBookingAllowedActionsResponse>`.

    <ResponseField name="results" type="BulkCalculateAllowedActionsResult[]">

      Information about the multi-service bookings that were retrieved.
      Includes their ID, index in the bulk request and whether they were successfully processed.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the booking. Including ID, index in the provided sequence, success status, and error.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="AllowedActions">

          Booking entity.

          <Expandable title="item properties">

            <ResponseField name="cancel" type="boolean">

              Whether canceling the booking is allowed.

            </ResponseField>

            <ResponseField name="reschedule" type="boolean">

              Whether rescheduling the booking is allowed.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total number of successes and failures for Bulk Get Multi Service Booking Allowed Actions.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-get-multi-service-booking-allowed-actions" id="ex-sdk-bulk-get-multi-service-booking-allowed-actions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-get-multi-service-booking-allowed-actions" id="ex-sdk-bulk-get-multi-service-booking-allowed-actions-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-get-multi-service-booking-allowed-actions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-get-multi-service-booking-allowed-actions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkGetMultiServiceBookingAllowedActions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkGetMultiServiceBookingAllowedActions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-get-multi-service-booking-allowed-actions-0"><span className="wix-picker-name">bulkGetMultiServiceBookingAllowedActions</span></label>
    <label htmlFor="ex-sdk-bulk-get-multi-service-booking-allowed-actions-1"><span className="wix-picker-name">bulkGetMultiServiceBookingAllowedActions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkGetMultiServiceBookingAllowedActions
    import { bookings } from '@wix/bookings';

    async function bulkGetMultiServiceBookingAllowedActions(multiServiceBookingIds) {
      const response = await bookings.bulkGetMultiServiceBookingAllowedActions(multiServiceBookingIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkGetMultiServiceBookingAllowedActions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bookings } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bookings },
      // Include the auth strategy and host as relevant
    });

    async function bulkGetMultiServiceBookingAllowedActions(multiServiceBookingIds) {
      const response = await myWixClient.bookings.bulkGetMultiServiceBookingAllowedActions(multiServiceBookingIds);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
