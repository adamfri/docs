---
title: "onGuestOrderCanceled"
description: "Triggered when a ticket order is canceled."
icon: "bolt"
---

Triggered when a ticket order is canceled.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.guests.v1.guest_guest_order_canceled` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onGuestOrderCanceled
  import { guests } from '@wix/events';

  guests.onGuestOrderCanceled((event) => {
      // handle your event here
  });
  ```

  ```javascript onGuestOrderCanceled (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { guests } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        guests,
      },
  });

  wixClient.guests.onGuestOrderCanceled((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
