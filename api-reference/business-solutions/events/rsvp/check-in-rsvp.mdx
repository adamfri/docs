---
title: "Check In Rsvp"
description: "Checks-in an RSVP."
icon: "upload"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Checks-in an RSVP.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/rsvp/check-in
    ```

    ## Body Params

    <ParamField body="eventId" type="string" required>

      Event ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="rsvpId" type="string" required>

      RSVP ID to check-in.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="guestId" type="integer[]">

      Guest IDs to check-in.

      Constraints: `min 1`, `minItems 1`, `maxItems 11`.

    </ParamField>

    ## Response Object

    <ResponseField name="rsvp" type="Rsvp">

      Updated RSVP.

      See the [Rsvp Object](/api-reference/business-solutions/events/rsvp/rsvp-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Updated | `wix.events.rsvp.events.RsvpUpdated` |  |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Checks-in an RSVP.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rsvp } from '@wix/events';

    rsvp.checkInRsvp(eventId, options): Promise<CheckInRsvpResponse>
    ```

    ## Method Parameters

    <ParamField body="eventId" type="string" required>

      Event ID to which RSVP belongs.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="CheckInRsvpOptions" required>

      Optional fields.

      <Expandable title="options properties">

        <ParamField body="rsvpId" type="string" required>

          RSVP ID to check-in.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="guestId" type="integer[]">

          Guest IDs to check-in.

          Constraints: `min 1`, `minItems 1`, `maxItems 11`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CheckInRsvpResponse>`.

    <ResponseField name="rsvp" type="Rsvp">

      Updated RSVP.

      See the [Rsvp Object](/api-reference/business-solutions/events/rsvp/rsvp-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Rsvp Updated | `wix.events.rsvp.events.RsvpUpdated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-check-in-rsvp" id="ex-sdk-check-in-rsvp-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-check-in-rsvp" id="ex-sdk-check-in-rsvp-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-check-in-rsvp" id="ex-sdk-check-in-rsvp-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-check-in-rsvp-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-check-in-rsvp-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">checkInRsvp</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkInRsvp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkInRsvp (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-check-in-rsvp-0"><span className="wix-picker-name">checkInRsvp</span></label>
    <label htmlFor="ex-sdk-check-in-rsvp-1"><span className="wix-picker-name">checkInRsvp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-check-in-rsvp-2"><span className="wix-picker-name">checkInRsvp (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript checkInRsvp
    import { rsvp } from '@wix/events';

    async function checkInRsvp(eventId,options) {
      const response = await rsvp.checkInRsvp(eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkInRsvp (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rsvp } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rsvp },
      // Include the auth strategy and host as relevant
    });

    async function checkInRsvp(eventId,options) {
      const response = await myWixClient.rsvp.checkInRsvp(eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkInRsvp (with elevated permissions)
    import { rsvp } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myCheckInRsvpMethod(eventId,options) {
      const elevatedCheckInRsvp = auth.elevate(rsvp.checkInRsvp);
      const response = await elevatedCheckInRsvp(eventId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
