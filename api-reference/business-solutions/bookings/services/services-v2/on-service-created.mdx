---
title: "onServiceCreated"
description: "Triggered when a service is created."
icon: "bolt"
---

Triggered when a service is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.services.v2.service_created` |
| Package | `@wix/bookings` |
| Permissions | `Manage Stores`, `Read Bookings - Public Data`, `Manage Bookings Services and Settings`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onServiceCreated
  import { services } from '@wix/bookings';

  services.onServiceCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onServiceCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { services } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        services,
      },
  });

  wixClient.services.onServiceCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
