---
title: "Get Tag By Label"
description: "Retrieves a tag by the provided label."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a tag by the provided label.

    Sub-labels can also be specified using a `/`. For example, you can have `'dessert/icecream'` and `'dessert/pie'` as two different tag labels.

    <Note>

    The full URL path following `labels/` is counted as 1 label. Adding a `/` to a label does _not_ create multiple labels. This means that `'dessert/icecream'` is a single label.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.READ-BLOGS">Read Blog </span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/v3/tags/labels/{label=**}
    ```

    ## Query Params

    <ParamField query="label" type="string" required>

      Tag label.

      Constraints: `minLength 1`, `maxLength 100`.

    </ParamField>

    <ParamField query="language" type="string">

      Tag language.

      2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
      If omitted, tags in all languages are returned.

    </ParamField>

    <ParamField query="fieldsets" type="string[]">

      List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
      the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
      only the tag's base fields are returned.

      Constraints: `maxItems 10`.

      **Supported values:**

      - `URL` — Includes Tag URL when present.

    </ParamField>

    ## Response Object

    <ResponseField name="tag" type="Tag">

      Retrieved tag.

      See the [Tag Object](/api-reference/business-solutions/blog/tags/tag-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl \
        'https://www.wixapis.com/blog/v3/tags/labels/vacation' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "tag": {
          "id": "6d72a3bb-053c-4de5-a897-5ef6be30b1b0",
          "label": "vacation",
          "slug": "vacation",
          "createdDate": "2021-08-13T08:58:20.145Z",
          "updatedDate": "2021-08-13T08:58:20.145Z",
          "publicationCount": 2,
          "postCount": 1,
          "language": "en"
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a tag by the provided label.

    Sub-labels can also be specified using a `/`. For example, you can have `'dessert/icecream'` and `'dessert/pie'` as two different tag labels.

    <Note>

    The full URL path following `labels/` is counted as 1 label. Adding a `/` to a label does _not_ create multiple labels. This means that `'dessert/icecream'` is a single label.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.READ-BLOGS">Read Blog </span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { tags } from '@wix/blog';

    tags.getTagByLabel(label, options): Promise<GetTagByLabelResponse>
    ```

    ## Method Parameters

    <ParamField body="label" type="string" required>

      Tag label.

      Constraints: `minLength 1`, `maxLength 100`.

    </ParamField>

    <ParamField body="options" type="GetTagByLabelOptions">

      Options specifying which additional fields to return.

      <Expandable title="options properties">

        <ParamField body="language" type="string">

          Tag language.

          2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
          If omitted, tags in all languages are returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
          the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
          only the tag's base fields are returned.

          Constraints: `maxItems 10`.

          **Supported values:**

          - `URL` — Includes Tag URL when present.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<GetTagByLabelResponse>`.

    <ResponseField name="tag" type="Tag">

      Retrieved tag.

      See the [Tag Object](/api-reference/business-solutions/blog/tags/tag-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-tag-by-label" id="ex-sdk-get-tag-by-label-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-tag-by-label" id="ex-sdk-get-tag-by-label-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-tag-by-label" id="ex-sdk-get-tag-by-label-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-tag-by-label-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-tag-by-label-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get a tag by its label</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get a tag by its label with additional fields</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTagByLabel (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-tag-by-label-0"><span className="wix-picker-name">Get a tag by its label</span></label>
    <label htmlFor="ex-sdk-get-tag-by-label-1"><span className="wix-picker-name">Get a tag by its label with additional fields</span></label>
    <label htmlFor="ex-sdk-get-tag-by-label-2"><span className="wix-picker-name">getTagByLabel (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get a tag by its label
    import { tags } from '@wix/blog';

    /* Sample label value:
     * 'my tag'
     */

    export async function getTagByLabelFunction(tagId) {
      try {
        const result = await tags.getTagByLabel(tagId);
        const label = result.tag.label;
        const postCount = result.tag.postCount;
        console.log('Retrieved result:', result);
        return result;
      } catch (error) {
        console.error(error);
      };
    }

    /* Promise resolves to:
     * {
     *   "tag": {
     *     "_createdDate": "2022-07-19T10:30:03.607Z",
     *     "_id": "32970480-e53b-46e7-b52f-fba810a0b45d",
     *     "_updatedDate": "2022-07-19T10:30:03.607Z",
     *     "label": "my tag",
     *     "language": "en",
     *     "postCount": 1,
     *     "publishedPostCount": 1,
     *     "slug": "my-tag",
     *     "translationId": ""
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get a tag by its label with additional fields
    import { tags } from '@wix/blog';

    /* Sample label value:
     * 'my tag'
     * Sample options value:
     * {
     *   fieldsets:
     *   [
     *     'URL'
     *   ]
     * }
     */

    export async function getTagByLabelFunction(tagId, options) {
      try {
        const result = await tags.getTagByLabel(tagId, options);
        const label = result.tag.label;
        const postCount = result.tag.postCount;
        const url = result.tag.url;
        console.log('Retrieved result:', result);
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise resolves to:
     * {
     *   "tag": {
     *     "_createdDate": "2022-07-19T10:30:03.607Z",
     *     "_id": "32970480-e53b-46e7-b52f-fba810a0b45d",
     *     "label": "my tag",
     *     "slug": "my-tag",
     *     "_updatedDate": "2022-07-19T10:30:03.607Z",
     *     "postCount": 1,
     *     "publishedPostCount": 1,
     *     "language": "en",
     *     "translationId": "",
     *     "url": "http://https://tadasz7.wixsite.com/blog-velo-events/my-blog/tags/my-tag"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTagByLabel (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tags } from '@wix/blog';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tags },
      // Include the auth strategy and host as relevant
    });

    async function getTagByLabel(label,options) {
      const response = await myWixClient.tags.getTagByLabel(label,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
