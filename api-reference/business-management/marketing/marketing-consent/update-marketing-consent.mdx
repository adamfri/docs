---
title: "Update Marketing Consent"
description: "Updates a marketing consent."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a marketing consent.
    Required fields:
    - `details.type`.
    - `details.email` OR `details.phone`.
    - `state`.

    When a marketing consent's `state` is `PENDING` or `CONFIRMED`, the `info.lastConfirmationActivity` field is required.
    When a marketing consent's `state` is `REVOKED`, the `info.lastRevokeActivity` field is required.

    <Note>

    For existing marketing consents with `{"type": "EMAIL"}`, you can't update the `state` to `UNKNOWN_STATE`. Trying to do so maintains the current state. However, you can create a new marketing consent and set the `state` to `UNKNOWN_STATE`. Note that you can't create more than a single consent per email or phone number.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.CONSENT-MANAGE">Manage Marketing Consents</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/marketing-consent/v1/marketing-consent/{marketingConsent.id}
    ```

    ## Path Params

    <ParamField path="marketingConsent.id" type="string" required>

      Marketing consent ID.

    </ParamField>

    ## Body Params

    <ParamField body="marketingConsent" type="MarketingConsent">

      Marketing consent to update.

      See the [Marketing Consent Object](/api-reference/business-management/marketing/marketing-consent/marketing-consent-object) for this object's fields.

    </ParamField>

    <ParamField body="mask" type="FieldMask" required>

      Explicit list of fields to update

      <Expandable title="mask properties">

        <ParamField body="paths" type="string[]" />

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="marketingConsent" type="MarketingConsent">

      Updated marketing consent.

      See the [Marketing Consent Object](/api-reference/business-management/marketing/marketing-consent/marketing-consent-object) for this object's fields.

    </ResponseField>

    <ResponseField name="link" type="Link">

      Marketing consent confirmation or cancelation link.

      <Expandable title="link properties">

        <ResponseField name="type" type="string">

          Link type.

          **Supported values:**

          - `UNKNOWN_LINK_TYPE`
          - `CONFIRMATION`
          - `REVOKE_CONFIRMATION`

        </ResponseField>

        <ResponseField name="url" type="string">

          The link's URL.

          Constraints: `format WEB_URL`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Marketing Consent Updated | `wix.marketing.marketing_consent.v1.marketing_consent_updated` | Triggered when a marketing consent is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/marketing-consent/v1/marketing-consent/aa8a62f5-9a79-4b21-b1a4-5c835c08d937' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "marketingConsent": {
            "details": {
              "type": "EMAIL",
              "email": "mytest@test.com"
            },
            "state": "REVOKED",
            "revision": "3",
            "lastConfirmationActivity": {
              "source": "IN_PERSON"
            }
          },
          "mask": {
            "paths": [
              "state",
              "lastConfirmationActivity.source"
            ]
          }
        }'
      ```

      ```json Response
      {
        "marketingConsent": {
          "id": "aa8a62f5-9a79-4b21-b1a4-5c835c08d937",
          "revision": "3",
          "details": {
            "type": "EMAIL",
            "email": "mytest@test.com"
          },
          "state": "REVOKED",
          "lastConfirmationActivity": {
            "source": "IN_PERSON",
            "description": "person filled a printed document",
            "updatedDate": "2023-01-01T00:00:00Z",
            "optInLevel": "DOUBLE_CONFIRMATION"
          },
          "extendedFields": {},
          "createdDate": "2022-11-16T14:07:50.150Z",
          "updatedDate": "2022-11-16T14:07:50.150Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a marketing consent.
    Required fields:
    - `details.type`.
    - `details.email` OR `details.phone`.
    - `state`.

    When a marketing consent's `state` is `PENDING` or `CONFIRMED`, the `info.lastConfirmationActivity` field is required.
    When a marketing consent's `state` is `REVOKED`, the `info.lastRevokeActivity` field is required.

    <Note>

    For existing marketing consents with `{"type": "EMAIL"}`, you can't update the `state` to `UNKNOWN_STATE`. Trying to do so maintains the current state. However, you can create a new marketing consent and set the `state` to `UNKNOWN_STATE`. Note that you can't create more than a single consent per email or phone number.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.CONSENT-MANAGE">Manage Marketing Consents</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingConsent } from '@wix/marketing';

    marketingConsent.updateMarketingConsent(_id, options): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Marketing consent ID.

      **Read-only.**

    </ParamField>

    <ParamField body="options" type="UpdateMarketingConsentOptions" required>

      Field options. The following fields **must** be passed: `details.type`, the relevant `email` OR `phone`, and `state`.

      <Expandable title="options properties">

        <ParamField body="marketingConsent" type="MarketingConsent">

          Marketing consent to update.

          See the [Marketing Consent Object](/api-reference/business-management/marketing/marketing-consent/marketing-consent-object) for this object's fields.

        </ParamField>

        <ParamField body="mask" type="FieldMask" required>

          Explicit list of fields to update

          <Expandable title="mask properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `MarketingConsent`.

    See the [Marketing Consent Object](/api-reference/business-management/marketing/marketing-consent/marketing-consent-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Marketing Consent Updated | `wix.marketing.marketing_consent.v1.marketing_consent_updated` | Triggered when a marketing consent is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-marketing-consent" id="ex-sdk-update-marketing-consent-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-marketing-consent" id="ex-sdk-update-marketing-consent-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-marketing-consent" id="ex-sdk-update-marketing-consent-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-marketing-consent-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-marketing-consent-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateMarketingConsent</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateMarketingConsent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateMarketingConsent (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-marketing-consent-0"><span className="wix-picker-name">updateMarketingConsent</span></label>
    <label htmlFor="ex-sdk-update-marketing-consent-1"><span className="wix-picker-name">updateMarketingConsent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-marketing-consent-2"><span className="wix-picker-name">updateMarketingConsent (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateMarketingConsent
    import { marketingConsent } from '@wix/marketing';

    async function updateMarketingConsent(_id,options) {
      const response = await marketingConsent.updateMarketingConsent(_id,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateMarketingConsent (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingConsent } from '@wix/marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingConsent },
      // Include the auth strategy and host as relevant
    });

    async function updateMarketingConsent(_id,options) {
      const response = await myWixClient.marketingConsent.updateMarketingConsent(_id,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateMarketingConsent (with elevated permissions)
    import { marketingConsent } from '@wix/marketing';
    import { auth } from '@wix/essentials';

    async function myUpdateMarketingConsentMethod(_id,options) {
      const elevatedUpdateMarketingConsent = auth.elevate(marketingConsent.updateMarketingConsent);
      const response = await elevatedUpdateMarketingConsent(_id,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
