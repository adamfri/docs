---
title: "onReviewUpdated"
description: "Triggered when a review is updated."
icon: "bolt"
---

Triggered when a review is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reviews.v1.review_updated` |
| Package | `@wix/reviews` |
| Permissions | `Manage Stores`, `Manage Reviews` |

## Examples

<CodeGroup>

  ```javascript onReviewUpdated
  import { reviews } from '@wix/reviews';

  reviews.onReviewUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReviewUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reviews } from '@wix/reviews';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reviews,
      },
  });

  wixClient.reviews.onReviewUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
