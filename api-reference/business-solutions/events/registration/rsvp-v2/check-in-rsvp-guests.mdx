---
title: "Check In Rsvp Guests"
description: "Checks in RSVP guests."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks in RSVP guests.

    You can check in up to 11 guests per request.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v2/rsvps/{rsvpId}/check-in
    ```

    ## Path Params

    <ParamField path="rsvpId" type="string" required>

      ID of the RSVP to check-in.

    </ParamField>

    ## Body Params

    <ParamField body="guestIds" type="integer[]">

      IDs of guests to check-in.

      Constraints: `min 1`, `minItems 1`, `maxItems 11`.

    </ParamField>

    <ParamField body="fields" type="string[]">

      Predefined sets of fields to return.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `DETAILS` — Returns `createdDate`, `updatedDate`, `firstName`, `lastName`, `totalGuests`, `guests`.
      - `FORM` — Returns `form`.
      - `CONTACT_DETAILS` — Returns `email`.

    </ParamField>

    <ParamField body="checkInDate" type="datetime">

      Optional check-in date. If omitted, runtime timestamp is used.

    </ParamField>

    ## Response Object

    <ResponseField name="rsvp" type="Rsvp">

      Updated RSVP.

      See the [Rsvp Object](/api-reference/business-solutions/events/registration/rsvp-v2/rsvp-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `GUEST_NOT_FOUND` | Couldn't find the guest. |
    | `428` | `FAILED_PRECONDITION` | `GUEST_ALREADY_CHECKED_IN` | Guest was already checked in. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CHECK_IN_TIME` | Custom check-in date is outside the allowed window. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Updated | `wix.events.v2.rsvp_updated` | Triggered when an RSVP is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/rsvps/8c30fd39-c927-49c5-95d4-dc75c991681d/check-in' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "guestIds": [1]
      }'
      ```

      ```json Response
      {
        "rsvp": {
          "id": "8c30fd39-c927-49c5-95d4-dc75c991681d",
          "revision": "1",
          "eventId": "32c0eab1-b7a0-4ec2-9fb6-db76f76ee488",
          "createdDate": "2023-10-20T08:46:46.665Z",
          "updatedDate": "2023-10-20T12:53:17.862Z",
          "memberId": "8a8b9b73-4da8-47a5-8268-4396e68a0605",
          "firstName": "John",
          "lastName": "Doe",
          "email": "johndoe@mail.com",
          "form": {
            "inputValues": [
              {
                "inputName": "firstName",
                "value": "John",
                "values": []
              },
              {
                "inputName": "lastName",
                "value": "Doe",
                "values": []
              },
              {
                "inputName": "email",
                "value": "johndoe@mail.com",
                "values": []
              }
            ]
          },
          "status": "YES",
          "totalGuests": 1,
          "guestList": {
            "guests": [
              {
                "index": 0,
                "fullName": "John Doe",
                "checkInDetails": {
                  "checkedIn": true,
                  "checkInDate": "2023-10-20T12:53:17.760Z"
                },
                "guestId": 1
              }
            ]
          },
          "anonymized": false,
          "fullyCheckedIn": true,
          "marketingConsent": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks in RSVP guests.

    You can check in up to 11 guests per request.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rsvpV2 } from '@wix/events';

    rsvpV2.checkInRsvpGuests(rsvpId, options): Promise<CheckInRsvpGuestsResponse>
    ```

    ## Method Parameters

    <ParamField body="rsvpId" type="string" required>

      ID of the RSVP to check-in.

    </ParamField>

    <ParamField body="options" type="CheckInRsvpGuestsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="guestIds" type="integer[]">

          IDs of guests to check-in.

          Constraints: `min 1`, `minItems 1`, `maxItems 11`.

        </ParamField>

        <ParamField body="fields" type="string[]">

          Predefined sets of fields to return.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `DETAILS` — Returns `createdDate`, `updatedDate`, `firstName`, `lastName`, `totalGuests`, `guests`.
          - `FORM` — Returns `form`.
          - `CONTACT_DETAILS` — Returns `email`.

        </ParamField>

        <ParamField body="checkInDate" type="datetime">

          Optional check-in date. If omitted, runtime timestamp is used.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CheckInRsvpGuestsResponse>`.

    <ResponseField name="rsvp" type="Rsvp">

      Updated RSVP.

      See the [Rsvp Object](/api-reference/business-solutions/events/registration/rsvp-v2/rsvp-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `GUEST_NOT_FOUND` | Couldn't find the guest. |
    | `428` | `FAILED_PRECONDITION` | `GUEST_ALREADY_CHECKED_IN` | Guest was already checked in. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_CHECK_IN_TIME` | Custom check-in date is outside the allowed window. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Updated | `wix.events.v2.rsvp_updated` | Triggered when an RSVP is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-check-in-rsvp-guests" id="ex-sdk-check-in-rsvp-guests-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-check-in-rsvp-guests" id="ex-sdk-check-in-rsvp-guests-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-check-in-rsvp-guests-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-check-in-rsvp-guests-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Check in guests</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkInRsvpGuests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-check-in-rsvp-guests-0"><span className="wix-picker-name">Check in guests</span></label>
    <label htmlFor="ex-sdk-check-in-rsvp-guests-1"><span className="wix-picker-name">checkInRsvpGuests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Check in guests
    import { rsvpV2 } from "@wix/events";

    async function checkInRsvpGuests() {
      const response = await rsvpV2.checkInRsvpGuests({
        rsvpId: "8c30fd39-c927-49c5-95d4-dc75c991681d",
        guestIds: [1],
      });
      return response;
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkInRsvpGuests (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rsvpV2 } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rsvpV2 },
      // Include the auth strategy and host as relevant
    });

    async function checkInRsvpGuests(rsvpId,options) {
      const response = await myWixClient.rsvpV2.checkInRsvpGuests(rsvpId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
