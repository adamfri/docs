---
title: "onDonationCampaignStatusChanged"
description: "Triggered when a donation campaign's status changes."
icon: "bolt"
---

Triggered when a donation campaign's status changes.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.donations.v2.donation_campaign_status_changed` |
| Package | `@wix/donations` |
| Permissions | `Manage Donation Campaigns` |

## Examples

<CodeGroup>

  ```javascript onDonationCampaignStatusChanged
  import { donationCampaigns } from '@wix/donations';

  donationCampaigns.onDonationCampaignStatusChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onDonationCampaignStatusChanged (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { donationCampaigns } from '@wix/donations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        donationCampaigns,
      },
  });

  wixClient.donationCampaigns.onDonationCampaignStatusChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
