---
title: "Query Attendance"
description: "Retrieves up to 100 attendance objects, given the specified paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 `attendance` objects, given the specified
    paging, filtering, and sorting.

    When querying attendance information, you can query from the perspective of:
    + **A booking.** Specify a booking ID to retrieve attendance information
    for all sessions related to that booking. For example, query by `bookingId`
    for a course booking to retrieve a participant's attendance for all course
    sessions.
    + **A session.** Specify a session ID to retrieve attendance information
    for all bookings related to that session.

    Query Attendance runs with the following defaults, which you can override:
    - `id` sorted in `ASC` order.
    - `cursorPaging.limit` set to `50`.

    Only a single filter is supported per query. If you define multiple filters
    in the same query, only the first is processed.

    To learn about working with query methods, see
    [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `bookingId` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `sessionId` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `eventId` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `status` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `numberOfAttendees` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-SENSITIVE">Read Bookings - Including Participants</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/attendance/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more information.

          Max: 1 filter

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[ {"fieldName":"sortField1","order":"ASC"}, {"fieldName":"sortField2","order":"DESC"} ]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. In the first request,
              specify `cursorPaging.limit`. For following requests, specify the
              retrieved `cursorPaging.cursor` token and not `query.filter` or
              `query.sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="attendances" type="Attendance[]">

      List of `attendance` objects that contain attendance information for a booked session.

      See the [Attendance Object](/api-reference/business-solutions/bookings/bookings/attendance/attendance-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Metadata for the paged set of results.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="cursors" type="Cursors">

          Use these cursors to paginate between results. [Read more](https://dev.wix.com/api/rest/getting-started/api-query-language#getting-started_api-query-language_cursor-paging).

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves information about sessions that were not attended.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/attendance/query' \
      -H 'Authorization:  <AUTH>' \
      -d '{
        "query": {
          "filter": {
            "status": "NOT_ATTENDED"
          }
        }
      }'
      ```

      ```json Response
      {
        "attendances":
        [
          {
              "id": "9fc7a1cc-55ba-4943-afac-5849f1a3cdd9",
              "bookingId": "88214716-6ed1-4273-b089-8015c4ab2f4b",
              "sessionId":" 2mmoW0vwKcSFyxtOfCdMqTntk7dVhOyHRtg53bu4e0cvIFpCA9sAC0M0ZiAJcAcni80UUc0DZWeZiSWqQEpqEHzb6cLa99kNRZDp",
              "status": "NOT_ATTENDED",
              "numberOfAttendees": 1
          }
         ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 `attendance` objects, given the specified
    paging, filtering, and sorting.

    When querying attendance information, you can query from the perspective of:
    + **A booking.** Specify a booking ID to retrieve attendance information
    for all sessions related to that booking. For example, query by `bookingId`
    for a course booking to retrieve a participant's attendance for all course
    sessions.
    + **A session.** Specify a session ID to retrieve attendance information
    for all bookings related to that session.

    Query Attendance runs with the following defaults, which you can override:
    - `id` sorted in `ASC` order.
    - `cursorPaging.limit` set to `50`.

    Only a single filter is supported per query. If you define multiple filters
    in the same query, only the first is processed.

    To learn about working with query methods, see
    [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `bookingId` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `sessionId` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `eventId` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `status` | `$eq`, `$ne`, `$in`, `$nin` | Sort: `ASC` |
    | `numberOfAttendees` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-SENSITIVE">Read Bookings - Including Participants</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { attendance } from '@wix/bookings';

    attendance.queryAttendance(query): AttendancesQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="AttendanceQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more information.

          Max: 1 filter

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[ {"fieldName":"sortField1","order":"ASC"}, {"fieldName":"sortField2","order":"DESC"} ]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. In the first request,
              specify `cursorPaging.limit`. For following requests, specify the
              retrieved `cursorPaging.cursor` token and not `query.filter` or
              `query.sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `AttendancesQueryBuilder`.

    <ResponseField name="attendances" type="Attendance[]">

      List of `attendance` objects that contain attendance information for a booked session.

      See the [Attendance Object](/api-reference/business-solutions/bookings/bookings/attendance/attendance-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Metadata for the paged set of results.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="cursors" type="Cursors">

          Use these cursors to paginate between results. [Read more](https://dev.wix.com/api/rest/getting-started/api-query-language#getting-started_api-query-language_cursor-paging).

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-attendance" id="ex-sdk-query-attendance-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-attendance" id="ex-sdk-query-attendance-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-attendance-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-attendance-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query attendance for a booking</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryAttendance (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-attendance-0"><span className="wix-picker-name">Query attendance for a booking</span></label>
    <label htmlFor="ex-sdk-query-attendance-1"><span className="wix-picker-name">queryAttendance (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query attendance for a booking
    import { attendance } from "@wix/bookings";

    async function queryAttendance() {
      const response = await attendance.queryAttendance({
        filter: { bookingId: "12345678-1234-1234-1234-123456789abc" },
        sort: [{ fieldName: "numberOfAttendees", order: "DESC" }],
        cursorPaging: { limit: 50 },
      });
      return response;
    }

    /* Promise resolves to:
     * {
     *   attendances: [
     *     {
     *       _id: "f3197ab6-4900-4533-a765-f0c719fe00b9",
     *       bookingId: "12345678-1234-1234-1234-123456789abc",
     *       status: "ATTENDED",
     *       numberOfAttendees: 2,
     *       eventId: "11111111-1111-1111-1111-111111111111"
     *     }
     *   ],
     *   pagingMetadata: {
     *     totalCount: 1,
     *     cursors: {},
     *     hasNext: false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryAttendance (self-hosted)
    import { createClient } from '@wix/sdk';
    import { attendance } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { attendance },
      // Include the auth strategy and host as relevant
    });

    async function queryAttendance(query) {
      const response = await myWixClient.attendance.queryAttendance(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
