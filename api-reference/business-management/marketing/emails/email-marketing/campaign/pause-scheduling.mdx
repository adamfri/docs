---
title: "Pause Scheduling"
description: "Pauses a scheduled campaign."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Pauses a scheduled campaign.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/email-marketing/v1/campaigns/{campaignId}/pause-scheduling
    ```

    ## Path Params

    <ParamField path="campaignId" type="string" required>

      Campaign ID.

    </ParamField>

    ## Response Object

    Returns `PauseSchedulingResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Campaign Paused Event | `wix.email_marketing.v1.campaign_campaign_paused_event` | Triggered when a scheduled campaign is paused. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/email-marketing/v1/campaigns/926bfb51-33c8-47d5-9b38-546d0e7b46d7/pause-scheduling' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Pauses a scheduled campaign.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { campaigns } from '@wix/email-marketing';

    campaigns.pauseScheduling(campaignId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="campaignId" type="string" required>

      Campaign ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Campaign Campaign Paused Event | `wix.email_marketing.v1.campaign_campaign_paused_event` | Triggered when a scheduled campaign is paused. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-pause-scheduling" id="ex-sdk-pause-scheduling-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-pause-scheduling" id="ex-sdk-pause-scheduling-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-pause-scheduling" id="ex-sdk-pause-scheduling-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-pause-scheduling-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-pause-scheduling-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Pauses a scheduled campaign</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Pauses a scheduled campaign (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">pauseScheduling (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-pause-scheduling-0"><span className="wix-picker-name">Pauses a scheduled campaign</span></label>
    <label htmlFor="ex-sdk-pause-scheduling-1"><span className="wix-picker-name">Pauses a scheduled campaign (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-pause-scheduling-2"><span className="wix-picker-name">pauseScheduling (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Pauses a scheduled campaign
    import { campaigns } from '@wix/email-marketing';

    // Sample campaignId = "ea46013c-bbbf-4617-ad5d-9247bc4c0970";

    export async function myPauseSchedulingFunction(campaignId) {
      try {
        const result = campaigns.pauseScheduling(campaignId);

        console.log(`Success! Your campaign id: ${campaignId} has been paused.`)
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise returns void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Pauses a scheduled campaign (with elevated permissions)
    import { campaigns } from '@wix/email-marketing';
    import { auth } from '@wix/essentials';

    const elevatedPauseScheduling = auth.elevate(campaigns.pauseScheduling);

    // Sample campaignId = "ea46013c-bbbf-4617-ad5d-9247bc4c0970";

    export async function myPauseSchedulingFunction(campaignId) {
      try {
        const result = elevatedPauseScheduling(campaignId);

        console.log(`Success! Your campaign id: ${campaignId} has been paused.`)
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise returns void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript pauseScheduling (self-hosted)
    import { createClient } from '@wix/sdk';
    import { campaigns } from '@wix/email-marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { campaigns },
      // Include the auth strategy and host as relevant
    });

    async function pauseScheduling(campaignId) {
      const response = await myWixClient.campaigns.pauseScheduling(campaignId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
