---
title: "Setup"
description: "To use the SiteProperties API, install the @wix/business-tools package using npm or Yarn: or Then import { siteProperties } from @wix/business-tools:"
icon: "file-lines"
---

<Note>This article applies to the JavaScript SDK only.</Note>

To use the SiteProperties API, install the `@wix/business-tools` package using npm or Yarn:
  ```bash
npm install @wix/business-tools
```

or

```bash
yarn add @wix/business-tools
```

Then import `{ siteProperties }` from `@wix/business-tools`:

```js
import { siteProperties } from '@wix/business-tools'
```
