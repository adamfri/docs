---
title: "Set Attendance"
description: "Sets or updates attendance information for a booking session."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets or updates attendance information for a booking session. This
    information is stored in an `attendance` object.

    If an `attendance` object already exists for the session, it's updated.
    Otherwise, a new object is created.

    By default, `numberOfAttendees` is set to `1`, but you can specify a higher
    number if multiple participants attended. Do not set `numberOfAttendees` to
    `0` to indicate no attendance, instead specify `{"status": "NOT_ATTENDED"}`.

    Validation guidelines:

    + The call succeeds for mismatches between `numberOfAttendees`
    and `status`. For example, make sure that your code doesn't specify
    `{"status": "NOT_ATTENDED"}` with `{"numberOfAttendees": 5}`.
    + The API also allows `numberOfAttendees` to exceed the booking's
    `numberOfParticipants`. Use higher values only when scenarios like
    walk-ins justify the exception.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/attendance/set
    ```

    ## Body Params

    <ParamField body="attendance" type="Attendance" required>

      Attendance to create or update.

      See the [Attendance Object](/api-reference/business-solutions/bookings/bookings/attendance/attendance-object) for this object's fields.

    </ParamField>

    <ParamField body="participantNotification" type="ParticipantNotification">

      Information about whether to send a message to a customer after their attendance was set.

      <Expandable title="participantNotification properties">

        <ParamField body="notifyParticipants" type="boolean">

          Specify whether to send a message about the changes to the customer.

          Default: `false`

        </ParamField>

        <ParamField body="message" type="string">

          Optional custom message to send to the participants about the changes to the booking.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="attendance" type="Attendance">

      Created or updated attendance.

      See the [Attendance Object](/api-reference/business-solutions/bookings/bookings/attendance/attendance-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `SESSION_ID_NOT_PROVIDED` | The session ID wasn't provided. Specify a valid session ID for the attendance. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-set-attendance" id="ex-rest-set-attendance-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-set-attendance" id="ex-rest-set-attendance-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-set-attendance-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-set-attendance-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Mark a booked session as not attended</span><span className="wix-picker-desc">Whether a session is attended is determined by the status field, and not by the number_of_attendees field</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Mark a booked session as attended</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-set-attendance-0"><span className="wix-picker-name">Mark a booked session as not attended</span><span className="wix-picker-desc">Whether a session is attended is determined by the status field, and not by the number_of_attendees field</span></label>
    <label htmlFor="ex-rest-set-attendance-1"><span className="wix-picker-name">Mark a booked session as attended</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/attendance/set' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "attendance": {
          "bookingId":"88214716-6ed1-4273-b089-8015c4ab2f4b",
          "sessionId":"2mmoW0vwKcSFyxtOfCdMqTntk7dVhOyHRtg53bu4e0cvIFpCA9sAC0M0ZiAJcAcni80UUc0DZWeZiSWqQEpqEHzb6cLa99kNRZDp",
          "status": "NOT_ATTENDED",
          "numberOfAttendees": 1
        }
      }'
      ```

      ```json Response
      {
        "attendance":
          {
            "id": "9fc7a1cc-55ba-4943-afac-5849f1a3cdd9",
            "bookingId": "88214716-6ed1-4273-b089-8015c4ab2f4b",
            "sessionId": "2mmoW0vwKcSFyxtOfCdMqTntk7dVhOyHRtg53bu4e0cvIFpCA9sAC0M0ZiAJcAcni80UUc0DZWeZiSWqQEpqEHzb6cLa99kNRZDp",
            "status": "NOT_ATTENDED",
            "numberOfAttendees": 1
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/attendance/set' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "attendance": {
          "bookingId":"88214716-6ed1-4273-b089-8015c4ab2f4b",
          "sessionId":"2mmoW0vwKcSFyxtOfCdMqTntk7dVhOyHRtg53bu4e0cvIFpCA9sAC0M0ZiAJcAcni80UUc0DZWeZiSWqQEpqEHzb6cLa99kNRZDp",
          "status": "ATTENDED",
          "numberOfAttendees": 1
        }
      }'
      ```

      ```json Response
      {
        "attendance":
          {
            "id": "9fc7a1cc-55ba-4943-afac-5849f1a3cdd9",
            "bookingId": "88214716-6ed1-4273-b089-8015c4ab2f4b",
            "sessionId": "2mmoW0vwKcSFyxtOfCdMqTntk7dVhOyHRtg53bu4e0cvIFpCA9sAC0M0ZiAJcAcni80UUc0DZWeZiSWqQEpqEHzb6cLa99kNRZDp",
            "status": "ATTENDED",
            "numberOfAttendees": 1
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets or updates attendance information for a booking session. This
    information is stored in an `attendance` object.

    If an `attendance` object already exists for the session, it's updated.
    Otherwise, a new object is created.

    By default, `numberOfAttendees` is set to `1`, but you can specify a higher
    number if multiple participants attended. Do not set `numberOfAttendees` to
    `0` to indicate no attendance, instead specify `{"status": "NOT_ATTENDED"}`.

    Validation guidelines:

    + The call succeeds for mismatches between `numberOfAttendees`
    and `status`. For example, make sure that your code doesn't specify
    `{"status": "NOT_ATTENDED"}` with `{"numberOfAttendees": 5}`.
    + The API also allows `numberOfAttendees` to exceed the booking's
    `numberOfParticipants`. Use higher values only when scenarios like
    walk-ins justify the exception.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { attendance } from '@wix/bookings';

    attendance.setAttendance(attendance, options): Promise<SetAttendanceResponse>
    ```

    ## Method Parameters

    <ParamField body="attendance" type="Attendance" required>

      Attendance to create or update.

      See the [Attendance Object](/api-reference/business-solutions/bookings/bookings/attendance/attendance-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="SetAttendanceOptions">

      Options to use when setting an attendance.

      <Expandable title="options properties">

        <ParamField body="participantNotification" type="ParticipantNotification">

          Information about whether to send a message to a customer after their attendance was set.

          <Expandable title="participantNotification properties">

            <ParamField body="notifyParticipants" type="boolean">

              Specify whether to send a message about the changes to the customer.

              Default: `false`

            </ParamField>

            <ParamField body="message" type="string">

              Optional custom message to send to the participants about the changes to the booking.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<SetAttendanceResponse>`.

    <ResponseField name="attendance" type="Attendance">

      Created or updated attendance.

      See the [Attendance Object](/api-reference/business-solutions/bookings/bookings/attendance/attendance-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `SESSION_ID_NOT_PROVIDED` | The session ID wasn't provided. Specify a valid session ID for the attendance. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Attendance Created | `wix.bookings.v2.attendance_created` |  |
    | Attendance Updated | `wix.bookings.v2.attendance_updated` |  |
    | Attendance Marked As Not Attended | `wix.bookings.v2.attendance_marked_as_not_attended` | Triggered when an attendance `status` is set to `NOT_ATTENDED`. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-set-attendance" id="ex-sdk-set-attendance-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-set-attendance" id="ex-sdk-set-attendance-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-set-attendance-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-set-attendance-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Set attendance for a booked session //</span><span className="wix-picker-desc">If a record already exists for the session it is updated; otherwise a new one is created</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setAttendance (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-set-attendance-0"><span className="wix-picker-name">Set attendance for a booked session //</span><span className="wix-picker-desc">If a record already exists for the session it is updated; otherwise a new one is created</span></label>
    <label htmlFor="ex-sdk-set-attendance-1"><span className="wix-picker-name">setAttendance (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Set attendance for a booked session //
    import { attendance } from "@wix/bookings";

    const options = {
      participantNotification: {
        notifyParticipants: false,
      },
    };

    async function setAttendance() {
      const response = await attendance.setAttendance(
        {
          bookingId: "12345678-1234-1234-1234-123456789abc",
          eventId: "11111111-1111-1111-1111-111111111111",
          status: "ATTENDED",
          numberOfAttendees: 2,
        },
        options,
      );
      return response;
    }

    /* Promise resolves to:
     * {
     *   attendance: {
     *     _id: "42d142df-c18e-47ef-8e98-92e938637c91",
     *     bookingId: "12345678-1234-1234-1234-123456789abc",
     *     status: "ATTENDED",
     *     numberOfAttendees: 2,
     *     eventId: "11111111-1111-1111-1111-111111111111"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setAttendance (self-hosted)
    import { createClient } from '@wix/sdk';
    import { attendance } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { attendance },
      // Include the auth strategy and host as relevant
    });

    async function setAttendance(attendance,options) {
      const response = await myWixClient.attendance.setAttendance(attendance,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
