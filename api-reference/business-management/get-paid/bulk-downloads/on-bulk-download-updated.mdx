---
title: "onBulkDownloadUpdated"
description: "Triggered when a bulk download is updated."
icon: "bolt"
---

Triggered when a bulk download is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.get_paid.downloader.v1.bulk_download_updated` |
| Package | `@wix/get-paid` |

## Examples

<CodeGroup>

  ```javascript onBulkDownloadUpdated
  import { bulkDownloads } from '@wix/get-paid';

  bulkDownloads.onBulkDownloadUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBulkDownloadUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bulkDownloads } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bulkDownloads,
      },
  });

  wixClient.bulkDownloads.onBulkDownloadUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
