---
title: "Complete Free Coupon Payment"
description: "Completes payment for a join application when an applied coupon covers the full amount."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Completes payment for a join application when an applied coupon covers the full amount.

    The method verifies that a coupon is applied and brings the order total to zero, then marks the payment as successful and redeems the coupon. On success, `paymentStatus` becomes `PAYMENT_SUCCESSFUL` and a participant is created.

    Before calling this method:

    - Call [Prepare Payment](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/prepare-payment) to create a payment order.
    - Call [Apply Coupon](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/apply-coupon) with a coupon that covers the full price.

    `paymentStatus` must be `PAYMENT_IN_PROGRESS`, `PAYMENT_FAILED`, or `PAYMENT_CANCELED`. The last two let a member close out a free-coupon retry after a failed or canceled payment without calling Prepare Payment again.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/online-programs/participants/v3/join-applications/{joinApplicationId}/complete-free-coupon-payment
    ```

    ## Path Params

    <ParamField path="joinApplicationId" type="string" required>

      ID of the join application to complete the free-coupon payment for.

    </ParamField>

    ## Response Object

    <ResponseField name="joinApplication" type="JoinApplication">

      Join application with `paymentStatus` set to `PAYMENT_SUCCESSFUL`.

      See the [Join Application Object](/api-reference/business-management/online-programs/join-applications/join-application-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `MISSING_PAYMENT_ORDER` | No payment order exists for the join application. Call Prepare Payment first. |
    | `428` | `FAILED_PRECONDITION` | `NO_COUPON_APPLIED` | No coupon is applied to the payment order. Call Apply Coupon first. |
    | `428` | `FAILED_PRECONDITION` | `COUPON_DOES_NOT_COVER_FULL_AMOUNT` | The applied coupon doesn't reduce the order total to zero. Use the regular checkout flow instead. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_PAYMENT_STATUS` | The join application's `paymentStatus` doesn't permit completing a free-coupon payment. Allowed from `PAYMENT_IN_PROGRESS`, `PAYMENT_FAILED`, and `PAYMENT_CANCELED`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Join Application Updated | `wix.online_programs.participants.v3.join_application_updated` | Triggered when a join application is updated. |
    | Join Application Payment Status Changed | `wix.online_programs.participants.v3.join_application_payment_status_changed` | Triggered when a join application's `paymentStatus` changes. |

    </div>

    <div className="wix-examples">

    ## Examples

    Completes payment when an applied coupon covers the full order amount

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/online-programs/participants/v3/join-applications/4f87b2e3-2a0d-4b9f-9a1c-2d8e71b7e5c4/complete-free-coupon-payment' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "joinApplication": {
          "id": "4f87b2e3-2a0d-4b9f-9a1c-2d8e71b7e5c4",
          "revision": "4",
          "createdDate": "2026-06-01T14:21:15.691Z",
          "updatedDate": "2026-06-01T14:23:12.410Z",
          "memberId": "2a01d8c7-54e5-4f42-9c9f-7a5d4d2ec4f1",
          "programId": "8d3f2b9f-2c31-4f45-8e5a-1f4b9259a6c2",
          "status": "APPROVED",
          "paymentStatus": "PAYMENT_SUCCESSFUL",
          "paymentInfo": {
            "paymentOrderId": "0f0c80c2-cf4c-49db-93b4-30d0409aeb2b",
            "paidPlanIds": [],
            "couponId": "748c7a79-7b13-4b0d-9c25-e1d090bd11f6"
          },
          "member": {
            "email": "alex.member@example.com",
            "contactId": "2a01d8c7-54e5-4f42-9c9f-7a5d4d2ec4f1",
            "nickname": "alex.member"
          },
          "participantId": "51a19210-f09c-4910-b8f9-b6ff899ebc8a"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Completes payment for a join application when an applied coupon covers the full amount.

    The method verifies that a coupon is applied and brings the order total to zero, then marks the payment as successful and redeems the coupon. On success, `paymentStatus` becomes `PAYMENT_SUCCESSFUL` and a participant is created.

    Before calling this method:

    - Call [Prepare Payment](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/prepare-payment) to create a payment order.
    - Call [Apply Coupon](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/apply-coupon) with a coupon that covers the full price.

    `paymentStatus` must be `PAYMENT_IN_PROGRESS`, `PAYMENT_FAILED`, or `PAYMENT_CANCELED`. The last two let a member close out a free-coupon retry after a failed or canceled payment without calling Prepare Payment again.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Method Declaration

    ```typescript
    import { joinApplications } from '@wix/online-programs';

    joinApplications.completeFreeCouponPayment(joinApplicationId): Promise
    ```

    ## Method Parameters

    <ParamField body="joinApplicationId" type="string" required>

      ID of the join application to complete the free-coupon payment for.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="joinApplication" type="JoinApplication">

      Join application with `paymentStatus` set to `PAYMENT_SUCCESSFUL`.

      See the [Join Application Object](/api-reference/business-management/online-programs/join-applications/join-application-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `MISSING_PAYMENT_ORDER` | No payment order exists for the join application. Call Prepare Payment first. |
    | `428` | `FAILED_PRECONDITION` | `NO_COUPON_APPLIED` | No coupon is applied to the payment order. Call Apply Coupon first. |
    | `428` | `FAILED_PRECONDITION` | `COUPON_DOES_NOT_COVER_FULL_AMOUNT` | The applied coupon doesn't reduce the order total to zero. Use the regular checkout flow instead. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_PAYMENT_STATUS` | The join application's `paymentStatus` doesn't permit completing a free-coupon payment. Allowed from `PAYMENT_IN_PROGRESS`, `PAYMENT_FAILED`, and `PAYMENT_CANCELED`. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Join Application Updated | `wix.online_programs.participants.v3.join_application_updated` | Triggered when a join application is updated. |
    | Join Application Payment Status Changed | `wix.online_programs.participants.v3.join_application_payment_status_changed` | Triggered when a join application's `paymentStatus` changes. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-complete-free-coupon-payment" id="ex-sdk-complete-free-coupon-payment-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-complete-free-coupon-payment" id="ex-sdk-complete-free-coupon-payment-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-complete-free-coupon-payment-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-complete-free-coupon-payment-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Complete a free coupon payment</span><span className="wix-picker-desc">Completes payment when an applied coupon covers the full order amount</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">completeFreeCouponPayment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-complete-free-coupon-payment-0"><span className="wix-picker-name">Complete a free coupon payment</span><span className="wix-picker-desc">Completes payment when an applied coupon covers the full order amount</span></label>
    <label htmlFor="ex-sdk-complete-free-coupon-payment-1"><span className="wix-picker-name">completeFreeCouponPayment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Complete a free coupon payment
    import { joinApplications } from "@wix/online-programs";

    async function completeFreeCouponPayment() {
      const response = await joinApplications.completeFreeCouponPayment(
        "4f87b2e3-2a0d-4b9f-9a1c-2d8e71b7e5c4",
      );
    }

    /* Promise resolves to:
     * {
     *   "joinApplication": {
     *     "_id": "4f87b2e3-2a0d-4b9f-9a1c-2d8e71b7e5c4",
     *     "revision": "4",
     *     "_createdDate": "2026-06-01T14:21:15.691Z",
     *     "_updatedDate": "2026-06-01T14:23:12.410Z",
     *     "memberId": "2a01d8c7-54e5-4f42-9c9f-7a5d4d2ec4f1",
     *     "programId": "8d3f2b9f-2c31-4f45-8e5a-1f4b9259a6c2",
     *     "status": "APPROVED",
     *     "paymentStatus": "PAYMENT_SUCCESSFUL",
     *     "paymentInfo": {
     *       "paymentOrderId": "0f0c80c2-cf4c-49db-93b4-30d0409aeb2b",
     *       "paidPlanIds": [],
     *       "couponId": "748c7a79-7b13-4b0d-9c25-e1d090bd11f6"
     *     },
     *     "participantId": "4f87b2e3-2a0d-4b9f-9a1c-2d8e71b7e5c4"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript completeFreeCouponPayment (self-hosted)
    import { createClient } from '@wix/sdk';
    import { joinApplications } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { joinApplications },
      // Include the auth strategy and host as relevant
    });

    async function completeFreeCouponPayment(joinApplicationId) {
      const response = await myWixClient.joinApplications.completeFreeCouponPayment(joinApplicationId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
