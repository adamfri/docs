---
title: "Get Rule"
description: "Retrieves a rule by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a rule by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MODERATION.MANAGE-MODERATION-RULES">Manage Moderation Rules</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/moderation/v1/rules/{ruleId}
    ```

    ## Path Params

    <ParamField path="ruleId" type="string" required>

      Rule ID.

    </ParamField>

    ## Response Object

    <ResponseField name="rule" type="Rule">

      Requested rule.

      See the [Rule Object](/api-reference/crm/community/feedback-moderation/moderation-rules/rule-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://www.wixapis.com/moderation/v1/rules/500ea62c-5f4c-4a51-b813-0bd7f239c6dd  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "rule": {
          "id": "500ea62c-5f4c-4a51-b813-0bd7f239c6dd",
          "revision": "1",
          "createdDate": "2023-10-10T06:52:04.718Z",
          "updatedDate": "2023-10-10T06:52:04.718Z",
          "namespace": "reviews/stores",
          "name": "Check for Rating",
          "audience": {
            "type": "MEMBERS_AND_VISITORS"
          },
          "trigger": {
            "type": "ATTRIBUTE",
            "attributeOptions": {
              "name": "rating",
              "values": ["1", "2"]
            }
          },
          "exemptions": {
            "memberGroups": [],
            "memberIds": []
          },
          "action": {
            "type": "NEEDS_MANUAL_APPROVAL"
          },
          "enabled": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a rule by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MODERATION.MANAGE-MODERATION-RULES">Manage Moderation Rules</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { moderationRules } from '@wix/moderation-rules';

    moderationRules.getRule(ruleId): Promise<Rule>
    ```

    ## Method Parameters

    <ParamField body="ruleId" type="string" required>

      Rule ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Rule>`.

    See the [Rule Object](/api-reference/crm/community/feedback-moderation/moderation-rules/rule-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-rule" id="ex-sdk-get-rule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-rule" id="ex-sdk-get-rule-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-rule" id="ex-sdk-get-rule-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-rule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-rule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getRule</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getRule (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-rule-0"><span className="wix-picker-name">getRule</span></label>
    <label htmlFor="ex-sdk-get-rule-1"><span className="wix-picker-name">getRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-rule-2"><span className="wix-picker-name">getRule (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getRule
    import { moderationRules } from '@wix/moderation-rules';

    async function getRule(ruleId) {
      const response = await moderationRules.getRule(ruleId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getRule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { moderationRules } from '@wix/moderation-rules';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { moderationRules },
      // Include the auth strategy and host as relevant
    });

    async function getRule(ruleId) {
      const response = await myWixClient.moderationRules.getRule(ruleId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getRule (with elevated permissions)
    import { moderationRules } from '@wix/moderation-rules';
    import { auth } from '@wix/essentials';

    async function myGetRuleMethod(ruleId) {
      const elevatedGetRule = auth.elevate(moderationRules.getRule);
      const response = await elevatedGetRule(ruleId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
