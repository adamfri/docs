---
title: "onChatSettingsDeleted"
description: "The onChatSettingsDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forms.ai.v1.chat_settings_deleted` |
| Package | `@wix/forms` |
| Permissions | `Manage forms`, `Manage Online Programs` |

## Examples

<CodeGroup>

  ```javascript onChatSettingsDeleted
  import { chatSettings } from '@wix/forms';

  chatSettings.onChatSettingsDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onChatSettingsDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { chatSettings } from '@wix/forms';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        chatSettings,
      },
  });

  wixClient.chatSettings.onChatSettingsDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
