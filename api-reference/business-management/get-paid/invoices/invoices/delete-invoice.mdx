---
title: "Delete Invoice"
description: "Deletes an invoice."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes an invoice.

    Deleting an invoice permanently removes it from the invoice list. Only invoices in `DRAFT` status can be deleted.

    To delete multiple invoices in a single API call, call [Bulk Delete Invoices](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoices/bulk-delete-invoices).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/invoices/v4/invoices/{invoiceId}
    ```

    ## Path Params

    <ParamField path="invoiceId" type="string" required>

      Invoice ID.

    </ParamField>

    ## Response Object

    Returns `DeleteInvoiceResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_ACTION` | Invoice isn't in a state that allows deletion. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Deleted | `wix.invoices.invoices.v4.invoice_deleted` | Triggered when an invoice is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes an invoice permanently.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/invoices/v4/invoices/51be0e68-75c3-4b7a-97e7-3a6643ecf3e2' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes an invoice.

    Deleting an invoice permanently removes it from the invoice list. Only invoices in `DRAFT` status can be deleted.

    To delete multiple invoices in a single API call, call [Bulk Delete Invoices](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoices/bulk-delete-invoices).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { invoices } from '@wix/get-paid';

    invoices.deleteInvoice(invoiceId): Promise
    ```

    ## Method Parameters

    <ParamField body="invoiceId" type="string" required>

      Invoice ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteInvoiceResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_ACTION` | Invoice isn't in a state that allows deletion. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Deleted | `wix.invoices.invoices.v4.invoice_deleted` | Triggered when an invoice is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-invoice" id="ex-sdk-delete-invoice-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-invoice" id="ex-sdk-delete-invoice-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-invoice" id="ex-sdk-delete-invoice-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-invoice-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-invoice-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteInvoice</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteInvoice (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteInvoice (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-invoice-0"><span className="wix-picker-name">deleteInvoice</span></label>
    <label htmlFor="ex-sdk-delete-invoice-1"><span className="wix-picker-name">deleteInvoice (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-invoice-2"><span className="wix-picker-name">deleteInvoice (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteInvoice
    import { invoices } from '@wix/get-paid';

    async function deleteInvoice(invoiceId) {
      const response = await invoices.deleteInvoice(invoiceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteInvoice (self-hosted)
    import { createClient } from '@wix/sdk';
    import { invoices } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { invoices },
      // Include the auth strategy and host as relevant
    });

    async function deleteInvoice(invoiceId) {
      const response = await myWixClient.invoices.deleteInvoice(invoiceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteInvoice (with elevated permissions)
    import { invoices } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myDeleteInvoiceMethod(invoiceId) {
      const elevatedDeleteInvoice = auth.elevate(invoices.deleteInvoice);
      const response = await elevatedDeleteInvoice(invoiceId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
