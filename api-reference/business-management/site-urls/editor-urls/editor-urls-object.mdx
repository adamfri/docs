---
title: "Editor Urls Object"
description: "The Editor URLs object provides a list of editor-related URLs, including a link to the site's editor interface (one of Wix Studio, Wix Editor, or Editor X), an…"
icon: "cube"
---

The Editor URLs object provides a list of editor-related URLs, including a link to the site's editor interface (one of Wix Studio, Wix Editor, or Editor X), and a link to preview the site with the latest changes made in the editor.

<Tabs>

  <Tab title="REST">

    <ResponseField name="editorType" type="string">

      Editor type.

      **Read-only.**

      **Supported values:**

      - `WIX_EDITOR` — The classic Wix Editor.
      - `WIX_STUDIO` — Wix's editor built for agencies and enterprises.
      - `ODEDITOR` — Wix Harmony editor.
      - `WIXEL` — Wixel.
      - `EDITORLESS` — Headless site with no editor.

    </ResponseField>

    <ResponseField name="editorUrl" type="string">

      Editor URL.

      **Read-only.**

    </ResponseField>

    <ResponseField name="previewUrl" type="string">

      Preview URL.

      **Read-only.**

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="editorType" type="string">

      Editor type.

      **Read-only.**

      **Supported values:**

      - `WIX_EDITOR` — The classic Wix Editor.
      - `WIX_STUDIO` — Wix's editor built for agencies and enterprises.
      - `ODEDITOR` — Wix Harmony editor.
      - `WIXEL` — Wixel.
      - `EDITORLESS` — Headless site with no editor.

    </ResponseField>

    <ResponseField name="editorUrl" type="string">

      Editor URL.

      **Read-only.**

    </ResponseField>

    <ResponseField name="previewUrl" type="string">

      Preview URL.

      **Read-only.**

    </ResponseField>

  </Tab>

</Tabs>
