---
title: "Create Held Reservation"
description: "Creates a new temporary reservation and holds it for the customer for 10 minutes."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new temporary reservation and holds it for the customer for 10 minutes.

    Creates a new reservation with the `HELD` status. `HELD` reservations are intended to reserve seats and tables for a party in a selected time slot while they enter further reservation details, such as names and contact information. Reservations with the `HELD` status are only valid for 10 minutes. Trying to change a `HELD` reservation’s status after 10 minutes returns an error.

    You cannot call `Update Reservation` to change a reservation’s status from `HELD`. Trying to do so returns an error. Instead, you should use `Reserve Reservation`.

    If you do not wish to have `HELD` reservations in your flow, you can create a reservation with all required details using `Create Reservation`.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-RESERVATIONS-BASIC">Manage Reservations (Basic)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/table-reservations/reservations/v1/reservations/hold
    ```

    ## Body Params

    <ParamField body="reservationDetails" type="HeldReservationDetails" required>

      Held reservation information to update.

      <Expandable title="reservationDetails properties">

        <ParamField body="reservationLocationId" type="string" required>

          ID of the reservation location where the reservation is made.

        </ParamField>

        <ParamField body="startDate" type="datetime" required>

          Start date and time of the reservation.

        </ParamField>

        <ParamField body="partySize" type="integer" required>

          Party size.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="reservation" type="Reservation">

      Reservation.

      See the [Reservation Object](/api-reference/business-solutions/restaurants/reservations/reservations/reservation-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `MUST_BE_PREMIUM` | This feature requires a premium plan. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reservation Created | `wix.table_reservations.v1.reservation_created` | Triggered when a reservation is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/table-reservations/reservations/v1/reservations/hold' \
          -H 'Authorization: <AUTH>'
          --data-binary '{
           "reservationDetails": {
                "reservationLocationId": "28bca9ee-e5b9-4081-9f45-a6dded8593a1",
                "startDate": "2023-05-26T12:30:00Z",
                "partySize": 2
           }
         }'
      ```

      ```json Response
      {
          "reservation": {
              "id": "af878917-e3ab-42e1-9587-e8ee217418af",
              "status": "HELD",
              "details": {
                  "reservationLocationId": "28bca9ee-e5b9-4081-9f45-a6dded8593a1",
                  "tables": {
                      "ids": []
                  }
                  "startDate": "2023-05-26T12:30:00Z",
                  "endDate": "2023-05-26T14:00:00Z",
                  "partySize": 2
              },
              "createdDate": "2023-04-06T14:43:31.926Z",
              "updatedDate": "2023-04-06T14:43:31.926Z",
              "revision": "1"
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new temporary reservation and holds it for the customer for 10 minutes.

    Creates a new reservation with the `HELD` status. `HELD` reservations are intended to reserve seats and tables for a party in a selected time slot while they enter further reservation details, such as names and contact information. Reservations with the `HELD` status are only valid for 10 minutes. Trying to change a `HELD` reservation’s status after 10 minutes returns an error.

    You cannot call `Update Reservation` to change a reservation’s status from `HELD`. Trying to do so returns an error. Instead, you should use `Reserve Reservation`.

    If you do not wish to have `HELD` reservations in your flow, you can create a reservation with all required details using `Create Reservation`.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-RESERVATIONS.MANAGE-RESERVATIONS-BASIC">Manage Reservations (Basic)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { reservations } from '@wix/table-reservations';

    reservations.createHeldReservation(reservationDetails): Promise<CreateHeldReservationResponse>
    ```

    ## Method Parameters

    <ParamField body="reservationDetails" type="HeldReservationDetails" required>

      Held reservation information to update.

      <Expandable title="reservationDetails properties">

        <ParamField body="reservationLocationId" type="string" required>

          ID of the reservation location where the reservation is made.

        </ParamField>

        <ParamField body="startDate" type="datetime" required>

          Start date and time of the reservation.

        </ParamField>

        <ParamField body="partySize" type="integer" required>

          Party size.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CreateHeldReservationResponse>`.

    <ResponseField name="reservation" type="Reservation">

      Reservation.

      See the [Reservation Object](/api-reference/business-solutions/restaurants/reservations/reservations/reservation-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `MUST_BE_PREMIUM` | This feature requires a premium plan. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reservation Created | `wix.table_reservations.v1.reservation_created` | Triggered when a reservation is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-held-reservation" id="ex-sdk-create-held-reservation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-held-reservation" id="ex-sdk-create-held-reservation-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-held-reservation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-held-reservation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createHeldReservation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createHeldReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-held-reservation-0"><span className="wix-picker-name">createHeldReservation</span></label>
    <label htmlFor="ex-sdk-create-held-reservation-1"><span className="wix-picker-name">createHeldReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createHeldReservation
    import { reservations } from '@wix/table-reservations';

    async function createHeldReservation(reservationDetails) {
      const response = await reservations.createHeldReservation(reservationDetails);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createHeldReservation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reservations } from '@wix/table-reservations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reservations },
      // Include the auth strategy and host as relevant
    });

    async function createHeldReservation(reservationDetails) {
      const response = await myWixClient.reservations.createHeldReservation(reservationDetails);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
