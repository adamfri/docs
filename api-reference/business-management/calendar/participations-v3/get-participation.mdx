---
title: "Get Participation"
description: "Retrieves a participation object."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a `participation` object.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CALENDAR.MANAGE">Manage Calendars</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/calendar/v3/participations/{participationId}
    ```

    ## Path Params

    <ParamField path="participationId" type="string" required>

      ID of the `participation` object to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="participation" type="Participation">

      Retrieved `participation` object.

      See the [Participation Object](/api-reference/business-management/calendar/participations-v3/participation-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/calendar/v3/participations/b123e549-eb05-4c6e-8dab-0409c7ebc006' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "participation": {
          "id": "b123e549-eb05-4c6e-8dab-0409c7ebc006",
          "externalId": "c7e4cd82-1d17-49b0-b1df-6e1eaed60a20",
          "participant": {
            "name": "Jane Doe",
            "phone": "55-555-5555",
            "email": "jane.doe@example.com",
            "contactId": "5f4a86c5-fadf-427e-96e7-d57c14a4f49d"
          },
          "eventId": "130o4ncdcmm8csGKUihodsF8Lnx5vQuUmP9uqeurIYireDpysq5nOxSIMvXKf80QrOXzc09i1GXRepTUj2L01bzmifAqWHwp2pTmmtR3TmHJx7Tv6ODrmgS",
          "partySize": 1,
          "status": "CONFIRMED",
          "appId": "13d21c63-b5ec-5912-8397-c3a5ddb27a97",
          "revision": "1",
          "createdDate": "2024-10-07T09:14:54.796Z",
          "updatedDate": "2024-10-07T09:14:54.796Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a `participation` object.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CALENDAR.MANAGE">Manage Calendars</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { participations } from '@wix/calendar';

    participations.getParticipation(participationId): Promise<Participation>
    ```

    ## Method Parameters

    <ParamField body="participationId" type="string" required>

      ID of the `participation` object to retrieve.

    </ParamField>

    ## Returns

    Returns `Promise<Participation>`.

    See the [Participation Object](/api-reference/business-management/calendar/participations-v3/participation-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-participation" id="ex-sdk-get-participation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-participation" id="ex-sdk-get-participation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-participation" id="ex-sdk-get-participation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-participation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-participation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getParticipation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getParticipation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getParticipation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-participation-0"><span className="wix-picker-name">getParticipation</span></label>
    <label htmlFor="ex-sdk-get-participation-1"><span className="wix-picker-name">getParticipation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-participation-2"><span className="wix-picker-name">getParticipation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getParticipation
    import { participations } from '@wix/calendar';

    async function getParticipation(participationId) {
      const response = await participations.getParticipation(participationId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getParticipation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { participations } from '@wix/calendar';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { participations },
      // Include the auth strategy and host as relevant
    });

    async function getParticipation(participationId) {
      const response = await myWixClient.participations.getParticipation(participationId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getParticipation (with elevated permissions)
    import { participations } from '@wix/calendar';
    import { auth } from '@wix/essentials';

    async function myGetParticipationMethod(participationId) {
      const elevatedGetParticipation = auth.elevate(participations.getParticipation);
      const response = await elevatedGetParticipation(participationId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
