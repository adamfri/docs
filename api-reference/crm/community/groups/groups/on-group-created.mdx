---
title: "onGroupCreated"
description: "Triggered when a group is created."
icon: "bolt"
---

Triggered when a group is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_created` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupCreated
  import { groups } from '@wix/groups';

  groups.onGroupCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { groups } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        groups,
      },
  });

  wixClient.groups.onGroupCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
