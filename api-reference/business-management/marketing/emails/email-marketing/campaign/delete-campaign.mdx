---
title: "Delete Campaign"
description: "Permanently deletes a campaign."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a campaign.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/email-marketing/v1/campaigns/{campaignId}
    ```

    ## Path Params

    <ParamField path="campaignId" type="string" required>

      Campaign ID.

    </ParamField>

    ## Response Object

    Returns `DeleteCampaignResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Campaign Deleted | `wix.email_marketing.v1.campaign_deleted` | Triggered when a campaign is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Permanently delete a campaign.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/email-marketing/v1/campaigns/cb2170c1-fa67-4e98-872b-864594e6d1e7' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a campaign.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { campaigns } from '@wix/email-marketing';

    campaigns.deleteCampaign(campaignId): Promise
    ```

    ## Method Parameters

    <ParamField body="campaignId" type="string" required>

      Campaign ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteCampaignResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Campaign Deleted | `wix.email_marketing.v1.campaign_deleted` | Triggered when a campaign is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-campaign" id="ex-sdk-delete-campaign-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-campaign" id="ex-sdk-delete-campaign-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-campaign" id="ex-sdk-delete-campaign-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-campaign-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-campaign-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Permanently deletes a campaign</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Permanently deletes a campaign (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-campaign-0"><span className="wix-picker-name">Permanently deletes a campaign</span></label>
    <label htmlFor="ex-sdk-delete-campaign-1"><span className="wix-picker-name">Permanently deletes a campaign (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-delete-campaign-2"><span className="wix-picker-name">deleteCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Permanently deletes a campaign
    import { campaigns } from '@wix/email-marketing';

    //  Sample campaignId value: "fb21c999-40b5-4364-8e75-18273da41983";

    export async function myDeleteCampaignFunction(campaignId) {
      try {
        const result = await campaigns.deleteCampaign(campaignId);

        console.log(`Campaign ${campaignId} successfully deleted`)
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Permanently deletes a campaign (with elevated permissions)
    import { campaigns } from '@wix/email-marketing';

    //  Sample campaignId value: "fb21c999-40b5-4364-8e75-18273da41983";

    export async function myDeleteCampaignFunction(campaignId) {
      try {
        const result = await campaigns.deleteCampaign(campaignId);

        console.log(`Campaign ${campaignId} successfully deleted`)
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCampaign (self-hosted)
    import { createClient } from '@wix/sdk';
    import { campaigns } from '@wix/email-marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { campaigns },
      // Include the auth strategy and host as relevant
    });

    async function deleteCampaign(campaignId) {
      const response = await myWixClient.campaigns.deleteCampaign(campaignId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
