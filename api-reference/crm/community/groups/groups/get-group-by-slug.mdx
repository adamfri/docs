---
title: "Get Group By Slug"
description: "Retrieves a group by slug."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a group by slug.

    The slug is the end of a group's URL that refers to a specific group.
    For example, if a group's URL is `https:/example.com/groups/{my-fitness-group}`, the slug is `my-fitness-group`.
    Slugs are case-sensitive. It is generally based on the group name, but for secret groups it is an autogenerated string of characters,
    for example, `https:/example.com/groups/{5D3yTX}`.

    For groups with `group.privacyStatus` set to `SECRET`, only group admins and group members can see the group and its content.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/social-groups-proxy/groups/v2/groups/slugs/{slug}
    ```

    ## Path Params

    <ParamField path="slug" type="string" required>

      Unique part of the group's URL. For example, if a group's URL is `https:/example.com/groups/{my-fitness-group}`, the slug is `my-fitness-group`. Case-sensitive.

    </ParamField>

    ## Query Params

    <ParamField query="autoInviteId" type="string" />

    <ParamField query="contentType" type="string">

      Content type.

      **Supported values:**

      - `PLAIN_TEXT`
      - `DRAFTJS`
      - `RICH_CONTENT`

    </ParamField>

    ## Response Object

    <ResponseField name="group" type="Group">

      Retrieved group.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
          https://www.wixapis.com/social-groups-proxy/groups/v2/groups/slugs/group-1 \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "group": {
              "id": "8a258cf5-49f3-41dc-b320-65ec2598315b",
              "slug": "group-1",
              "privacyStatus": "PUBLIC",
              "name": "Group 1",
              "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
              "memberTitle": "Friends",
              "coverImage": {
                  "image": {
                      "mediaId": "4d6eab_666134ee68464d76aa838619344c4369~mv2.jpg",
                      "width": 1024,
                      "height": 150
                  }
              },
              "settings": {
                  "membersCanInvite": false,
                  "membersCanApprove": false,
                  "welcomeMemberPostEnabled": true,
                  "groupDetailsChangedPostEnabled": true,
                  "showMemberList": true
              },
              "membersCount": 1,
              "ownerId": "f98e4407-15bf-43a3-87fb-a954c49bb20f",
              "createdDate": "2020-11-06T14:03:50Z",
              "updatedDate": "2020-11-19T12:38:07Z",
              "lastActivityDate": "2020-11-19T12:38:08Z"
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a group by slug.

    The slug is the end of a group's URL that refers to a specific group.
    For example, if a group's URL is `https:/example.com/groups/{my-fitness-group}`, the slug is `my-fitness-group`.
    Slugs are case-sensitive. It is generally based on the group name, but for secret groups it is an autogenerated string of characters,
    for example, `https:/example.com/groups/{5D3yTX}`.

    For groups with `group.privacyStatus` set to `SECRET`, only group admins and group members can see the group and its content.

    ## Method Declaration

    ```typescript
    import { groups } from '@wix/groups';

    groups.getGroupBySlug(slug, options): Promise<GetGroupBySlugResponse>
    ```

    ## Method Parameters

    <ParamField body="slug" type="string" required>

      Unique part of the group's URL. For example, if a group's URL is `https:/example.com/groups/{my-fitness-group}`, the slug is `my-fitness-group`. Case-sensitive.

      Constraints: `minLength 1`, `maxLength 100`.

    </ParamField>

    <ParamField body="options" type="GetGroupBySlugOptions">

      options

      <Expandable title="options properties">

        <ParamField body="autoInviteId" type="string" />

        <ParamField body="contentType" type="string">

          Content type.

          **Supported values:**

          - `PLAIN_TEXT`
          - `DRAFTJS`
          - `RICH_CONTENT`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<GetGroupBySlugResponse>`.

    <ResponseField name="group" type="Group">

      Retrieved group.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-group-by-slug" id="ex-sdk-get-group-by-slug-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-group-by-slug" id="ex-sdk-get-group-by-slug-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-group-by-slug" id="ex-sdk-get-group-by-slug-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-group-by-slug-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-group-by-slug-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getGroupBySlug</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getGroupBySlug (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getGroupBySlug (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-group-by-slug-0"><span className="wix-picker-name">getGroupBySlug</span></label>
    <label htmlFor="ex-sdk-get-group-by-slug-1"><span className="wix-picker-name">getGroupBySlug (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-group-by-slug-2"><span className="wix-picker-name">getGroupBySlug (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getGroupBySlug
    import { groups } from '@wix/groups';

    async function getGroupBySlug(slug,options) {
      const response = await groups.getGroupBySlug(slug,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getGroupBySlug (self-hosted)
    import { createClient } from '@wix/sdk';
    import { groups } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { groups },
      // Include the auth strategy and host as relevant
    });

    async function getGroupBySlug(slug,options) {
      const response = await myWixClient.groups.getGroupBySlug(slug,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getGroupBySlug (with elevated permissions)
    import { groups } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myGetGroupBySlugMethod(slug,options) {
      const elevatedGetGroupBySlug = auth.elevate(groups.getGroupBySlug);
      const response = await elevatedGetGroupBySlug(slug,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
