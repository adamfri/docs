---
title: "Delete Attachment"
description: "Deletes the specified attachment from the specified contact."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the specified attachment from the specified contact.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACT-ATTACHMENTS">Manage Contact Attachments</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/contacts/v4/attachments/{contactId}/{attachmentId}
    ```

    ## Path Params

    <ParamField path="contactId" type="string" required>

      Contact ID for the attachment to delete.

    </ParamField>

    <ParamField path="attachmentId" type="string" required>

      Attachment ID to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteAttachmentResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Attachment Deleted | `wix.contacts.v4.attachment_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE \
         'https://www.wixapis.com/contacts/v4/attachments/513c1bfc-61b6-427d-a7f8-ceeedb1eb763/1e7efe60-355a-11eb-b032-19d0ca8630af' \
         -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the specified attachment from the specified contact.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACT-ATTACHMENTS">Manage Contact Attachments</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { attachments } from '@wix/crm';

    attachments.deleteAttachment(identifiers): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="identifiers" type="DeleteAttachmentIdentifiers" required>

      identifiers

      <Expandable title="identifiers properties">

        <ParamField body="contactId" type="string" required>

          Contact ID for the attachment to delete.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="attachmentId" type="string" required>

          Attachment ID to delete.

          Constraints: `format GUID`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Attachment Deleted | `wix.contacts.v4.attachment_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-attachment" id="ex-sdk-delete-attachment-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-attachment" id="ex-sdk-delete-attachment-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-attachment" id="ex-sdk-delete-attachment-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-attachment-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-attachment-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteAttachment</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAttachment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAttachment (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-attachment-0"><span className="wix-picker-name">deleteAttachment</span></label>
    <label htmlFor="ex-sdk-delete-attachment-1"><span className="wix-picker-name">deleteAttachment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-attachment-2"><span className="wix-picker-name">deleteAttachment (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteAttachment
    import { attachments } from '@wix/crm';

    async function deleteAttachment(identifiers) {
      const response = await attachments.deleteAttachment(identifiers);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAttachment (self-hosted)
    import { createClient } from '@wix/sdk';
    import { attachments } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { attachments },
      // Include the auth strategy and host as relevant
    });

    async function deleteAttachment(identifiers) {
      const response = await myWixClient.attachments.deleteAttachment(identifiers);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAttachment (with elevated permissions)
    import { attachments } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myDeleteAttachmentMethod(identifiers) {
      const elevatedDeleteAttachment = auth.elevate(attachments.deleteAttachment);
      const response = await elevatedDeleteAttachment(identifiers);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
