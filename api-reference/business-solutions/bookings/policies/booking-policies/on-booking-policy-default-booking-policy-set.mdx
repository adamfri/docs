---
title: "onBookingPolicyDefaultBookingPolicySet"
description: "Triggered when the site's default policy changes."
icon: "bolt"
---

Triggered when the site's default policy changes. Then,
_Booking Policy Updated_
([SDK](https://dev.wix.com/docs/sdk/backend-modules/bookings/booking-policies/on-booking-policy-updated) | [REST](https://dev.wix.com/docs/rest/business-solutions/bookings/policies/booking-policies/booking-policy-updated))
is also triggered both for the new and the previous default policy.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.v1.booking_policy_default_booking_policy_set` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings Services and Settings`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onBookingPolicyDefaultBookingPolicySet
  import { bookingPolicies } from '@wix/bookings';

  bookingPolicies.onBookingPolicyDefaultBookingPolicySet((event) => {
      // handle your event here
  });
  ```

  ```javascript onBookingPolicyDefaultBookingPolicySet (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bookingPolicies } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bookingPolicies,
      },
  });

  wixClient.bookingPolicies.onBookingPolicyDefaultBookingPolicySet((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
