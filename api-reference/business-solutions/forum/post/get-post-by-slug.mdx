---
title: "Get Post By Slug"
description: "Retrieves a single post by URL slug."
icon: "download"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a single post by URL slug.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORUM.READ-FORUM">Read Forums</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/forum/v1/posts/slugs/{slug}
    ```

    ## Path Params

    <ParamField path="slug" type="string" required>

      URL slug.

    </ParamField>

    ## Query Params

    <ParamField query="extraFields" type="string[]">

      Array of extra post fields to be added in the response.

      **Supported values:**

      - `IMAGES` — Include images from post content.
      - `VIDEOS` — Include videos from post content.
      - `URL` — Include post URL.
      - `REACTION_IDENTITIES` — Include reaction identities.
      - `RICH_CONTENT` — Includes the post content as a stringified [Rich Content document](https://dev.wix.com/docs/ricos/api-reference/ricos-document)

    </ParamField>

    ## Response Object

    <ResponseField name="post" type="Post">

      Post for the provided post ID.

      See the [Post Object](/api-reference/business-solutions/forum/post/post-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET https://www.wixapis.com/forum/v1/posts/slugs/welcome-to-the-forum
          -H 'Content-Type: application/json;charset=UTF-8'
          -H 'Authorization: <ACCESS_TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "post": {
              "id": "5cacd5fe04976c0015f35363",
              "categoryId": "5cacd5fe04976c0015f35362",
              "ownerId": "a27d2491-90b0-4a3a-87d9-7a9de00bf17a",
              "title": "Welcome to the Forum!",
              "contentText": "It's good to have you here! Feel free to share anything - stories, ideas, pictures or whatever is on your mind.",
              "bestAnswerCommentId": "5ce7bb354f055300a4b7f945",
              "pinned": false,
              "closed": false,
              "commentCount": 3,
              "likeCount": 0,
              "viewCount": 11,
              "createdDate": "2019-04-09T17:27:26.171Z",
              "updatedDate": "2019-04-09T17:27:26.171Z",
              "lastActivityDate": "2019-05-24T11:55:29.339Z",
              "slug": "welcome-to-the-forum",
              "commentInteraction": "REACTION",
              "reactionIdentities": [
                {
                  "reactionCode": ":like:",
                  "identities": [
                    {
                      "identityId": "0933513a-fcd7-459e-b76e-508d4806d908",
                      "identityType": "MEMBER"
                    }
                  ],
                  "count": 1
                }
              ],
              "url": {
                "base": "https://wix.com",
                "path": "/forum/discussion-corner/welcome-to-the-forum"
              }
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a single post by URL slug.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORUM.READ-FORUM">Read Forums</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { posts } from '@wix/forum';

    posts.getPostBySlug(slug, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="slug" type="string" required>

      URL slug.

    </ParamField>

    <ParamField body="options" type="GetPostBySlugOptions">

      Options specifying which additional fields to return. 

      <Expandable title="options properties">

        <ParamField body="extraFields" type="string[]">

          Array of extra post fields to be added in the response.

          **Supported values:**

          - `IMAGES` — Include images from post content.
          - `VIDEOS` — Include videos from post content.
          - `URL` — Include post URL.
          - `REACTION_IDENTITIES` — Include reaction identities.
          - `RICH_CONTENT` — Includes the post content as a stringified [Rich Content document](https://dev.wix.com/docs/ricos/api-reference/ricos-document)

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="post" type="Post">

      Post for the provided post ID.

      See the [Post Object](/api-reference/business-solutions/forum/post/post-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-post-by-slug" id="ex-sdk-get-post-by-slug-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-post-by-slug" id="ex-sdk-get-post-by-slug-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-post-by-slug" id="ex-sdk-get-post-by-slug-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-post-by-slug-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-post-by-slug-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getPostBySlug</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPostBySlug (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPostBySlug (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-post-by-slug-0"><span className="wix-picker-name">getPostBySlug</span></label>
    <label htmlFor="ex-sdk-get-post-by-slug-1"><span className="wix-picker-name">getPostBySlug (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-post-by-slug-2"><span className="wix-picker-name">getPostBySlug (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getPostBySlug
    import { posts } from '@wix/forum';

    async function getPostBySlug(slug,options) {
      const response = await posts.getPostBySlug(slug,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPostBySlug (self-hosted)
    import { createClient } from '@wix/sdk';
    import { posts } from '@wix/forum';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { posts },
      // Include the auth strategy and host as relevant
    });

    async function getPostBySlug(slug,options) {
      const response = await myWixClient.posts.getPostBySlug(slug,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPostBySlug (with elevated permissions)
    import { posts } from '@wix/forum';
    import { auth } from '@wix/essentials';

    async function myGetPostBySlugMethod(slug,options) {
      const elevatedGetPostBySlug = auth.elevate(posts.getPostBySlug);
      const response = await elevatedGetPostBySlug(slug,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
