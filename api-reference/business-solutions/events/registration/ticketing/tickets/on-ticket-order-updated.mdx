---
title: "onTicketOrderUpdated"
description: "The onTicketOrderUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.ticketing.events.OrderUpdated` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Read Basic Events Order Info`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onTicketOrderUpdated: Wix-hosted
  import { tickets } from '@wix/events';

  tickets.onTicketOrderUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTicketOrderUpdated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { tickets } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        tickets,
      },
  });

  wixClient.tickets.onTicketOrderUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
