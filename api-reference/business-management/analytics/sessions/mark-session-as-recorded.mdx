---
title: "Mark Session As Recorded"
description: "Marks a browser session as recorded."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Marks a browser session as recorded.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ANALYTICS-AND-REPORTS.MANAGE-SESSIONS">Manage Session Recording Analytics - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/analytics/v1/sessions/session-recorded
    ```

    ## Body Params

    <ParamField body="sessionId" type="string" required>

      Browser session ID.

      Constraints: `minLength 1`, `maxLength 100`.

    </ParamField>

    ## Response Object

    Returns `MarkSessionAsRecordedResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/analytics/v1/sessions/session-recorded' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "sessionId": "7f08f28b-f4ca-47be-879e-c871966ee0c0"
        }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Marks a browser session as recorded.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ANALYTICS-AND-REPORTS.MANAGE-SESSIONS">Manage Session Recording Analytics - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { analyticsSession } from '@wix/analytics-session';

    analyticsSession.markSessionAsRecorded(sessionId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="sessionId" type="string" required>

      Browser session ID.

      Constraints: `minLength 1`, `maxLength 100`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-mark-session-as-recorded" id="ex-sdk-mark-session-as-recorded-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-mark-session-as-recorded" id="ex-sdk-mark-session-as-recorded-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-mark-session-as-recorded" id="ex-sdk-mark-session-as-recorded-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-mark-session-as-recorded-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-mark-session-as-recorded-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">markSessionAsRecorded</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markSessionAsRecorded (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markSessionAsRecorded (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-mark-session-as-recorded-0"><span className="wix-picker-name">markSessionAsRecorded</span></label>
    <label htmlFor="ex-sdk-mark-session-as-recorded-1"><span className="wix-picker-name">markSessionAsRecorded (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-mark-session-as-recorded-2"><span className="wix-picker-name">markSessionAsRecorded (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript markSessionAsRecorded
    import { analyticsSession } from '@wix/analytics-session';

    async function markSessionAsRecorded(sessionId) {
      const response = await analyticsSession.markSessionAsRecorded(sessionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markSessionAsRecorded (self-hosted)
    import { createClient } from '@wix/sdk';
    import { analyticsSession } from '@wix/analytics-session';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { analyticsSession },
      // Include the auth strategy and host as relevant
    });

    async function markSessionAsRecorded(sessionId) {
      const response = await myWixClient.analyticsSession.markSessionAsRecorded(sessionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markSessionAsRecorded (with elevated permissions)
    import { analyticsSession } from '@wix/analytics-session';
    import { auth } from '@wix/essentials';

    async function myMarkSessionAsRecordedMethod(sessionId) {
      const elevatedMarkSessionAsRecorded = auth.elevate(analyticsSession.markSessionAsRecorded);
      const response = await elevatedMarkSessionAsRecorded(sessionId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
