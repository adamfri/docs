---
title: "onCustomEmbedCreated"
description: "Triggered when a custom embed is created."
icon: "bolt"
---

Triggered when a custom embed is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.embeds.v1.custom_embed_created` |
| Package | `@wix/embeds` |
| Permissions | `SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS`, `Manage Marketing Tags` |

## Examples

<CodeGroup>

  ```javascript onCustomEmbedCreated
  import { customEmbeds } from '@wix/embeds';

  customEmbeds.onCustomEmbedCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCustomEmbedCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { customEmbeds } from '@wix/embeds';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        customEmbeds,
      },
  });

  wixClient.customEmbeds.onCustomEmbedCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
