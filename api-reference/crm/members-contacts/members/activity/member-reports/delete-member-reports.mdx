---
title: "Delete Member Reports"
description: "Deletes all reports of a member."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes all reports of a member.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/members/v1/member-reports/members/{memberId}
    ```

    ## Path Params

    <ParamField path="memberId" type="string" required>

      ID of the member whose reports are to be deleted.

    </ParamField>

    ## Response Object

    Returns `DeleteMemberReportsResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `CANNOT_DELETE_MEMBER_REPORTS` | Member reports couldn't be deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Member Report Deleted | `wix.members.v1.member_report_deleted` | Triggered when a report is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        https://www.wixapis.com/members/v1/member-reports/members/e62e3011-55cf-4de3-a497-e097b52d86b7 \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes all reports of a member.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { memberReport } from '@wix/members';

    memberReport.deleteMemberReports(memberId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="memberId" type="string" required>

      ID of the member whose reports are to be deleted.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `CANNOT_DELETE_MEMBER_REPORTS` | Member reports couldn't be deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Member Report Deleted | `wix.members.v1.member_report_deleted` | Triggered when a report is deleted. |
    | Member Report Reported Member Deleted | `wix.members.v1.member_report_reported_member_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-member-reports" id="ex-sdk-delete-member-reports-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-member-reports" id="ex-sdk-delete-member-reports-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-member-reports" id="ex-sdk-delete-member-reports-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-member-reports-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-member-reports-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteMemberReports</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMemberReports (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMemberReports (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-member-reports-0"><span className="wix-picker-name">deleteMemberReports</span></label>
    <label htmlFor="ex-sdk-delete-member-reports-1"><span className="wix-picker-name">deleteMemberReports (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-member-reports-2"><span className="wix-picker-name">deleteMemberReports (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteMemberReports
    import { memberReport } from '@wix/members';

    async function deleteMemberReports(memberId) {
      const response = await memberReport.deleteMemberReports(memberId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMemberReports (self-hosted)
    import { createClient } from '@wix/sdk';
    import { memberReport } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { memberReport },
      // Include the auth strategy and host as relevant
    });

    async function deleteMemberReports(memberId) {
      const response = await myWixClient.memberReport.deleteMemberReports(memberId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMemberReports (with elevated permissions)
    import { memberReport } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myDeleteMemberReportsMethod(memberId) {
      const elevatedDeleteMemberReports = auth.elevate(memberReport.deleteMemberReports);
      const response = await elevatedDeleteMemberReports(memberId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
