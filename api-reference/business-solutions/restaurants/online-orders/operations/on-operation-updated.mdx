---
title: "onOperationUpdated"
description: "Triggered when an operation is updated."
icon: "bolt"
---

Triggered when an operation is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.operations.v1.operation_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onOperationUpdated
  import { operations } from '@wix/restaurants';

  operations.onOperationUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOperationUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { operations } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        operations,
      },
  });

  wixClient.operations.onOperationUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
