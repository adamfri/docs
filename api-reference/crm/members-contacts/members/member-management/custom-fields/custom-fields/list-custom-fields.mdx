---
title: "List Custom Fields"
description: "Retrieves a list of custom fields and fields provided by Members Area, given the provided paging."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of custom fields and fields provided by Members Area, given the provided paging.

    The fields are ordered by section in the following sequence:
    - `GENERAL`
    - `DISPLAY_INFO`
    - `SOCIAL`

    The fields in the same section are ordered by the field creation date.

    To change the order of fields in sections, call Update Custom Fields Order.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/members/v1/custom-fields
    ```

    ## Query Params

    <ParamField query="paging" type="Paging">

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of items to load.

        </ParamField>

        <ParamField query="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="fields" type="CustomField[]">

      Retrieved fields.

      See the [Custom Field Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-fields/custom-field-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        https://www.wixapis.com/members/v1/custom-fields \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "fields": [
          {
            "id": "b0a387ef-0bbe-424f-ad18-1960b285dbb2",
            "name": "First Name",
            "key": "customfields.system.firstname",
            "defaultPrivacy": "PRIVATE",
            "fieldType": "TEXT",
            "socialType": "OTHER",
            "fieldOrigin": "SYSTEM",
            "appliesTo": "ALL_MEMBERS",
            "section": "GENERAL",
            "createdDate": "2024-07-04T14:40:10.792Z",
            "updatedDate": "2024-07-04T14:40:10.792Z",
            "revision": "1"
          },
          {
            "id": "2b8a8b49-356c-45c2-93cd-0f1fa4ec4d3e",
            "name": "Favorite Animal",
            "key": "custom.favorite-animal",
            "defaultPrivacy": "PRIVATE",
            "fieldType": "TEXT",
            "socialType": "UNKNOWN",
            "fieldOrigin": "CUSTOM",
            "appliesTo": "ALL_MEMBERS",
            "section": "GENERAL",
            "createdDate": "2024-01-01T01:01:01.1Z",
            "updatedDate": "2024-01-01T01:01:01.1Z",
            "revision": "1"
          }
        ],
        "metadata": {
          "count": 3,
          "offset": 0,
          "total": 3,
          "tooManyToCount": null,
          "hasNext": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of custom fields and fields provided by Members Area, given the provided paging.

    The fields are ordered by section in the following sequence:
    - `GENERAL`
    - `DISPLAY_INFO`
    - `SOCIAL`

    The fields in the same section are ordered by the field creation date.

    To change the order of fields in sections, call Update Custom Fields Order.

    ## Method Declaration

    ```typescript
    import { customFields } from '@wix/members';

    customFields.listCustomFields(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="ListCustomFieldsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="Paging">

          paging

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="fields" type="CustomField[]">

      Retrieved fields.

      See the [Custom Field Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-fields/custom-field-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-custom-fields" id="ex-sdk-list-custom-fields-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-custom-fields" id="ex-sdk-list-custom-fields-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-custom-fields" id="ex-sdk-list-custom-fields-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-custom-fields-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-custom-fields-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listCustomFields</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listCustomFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listCustomFields (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-custom-fields-0"><span className="wix-picker-name">listCustomFields</span></label>
    <label htmlFor="ex-sdk-list-custom-fields-1"><span className="wix-picker-name">listCustomFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-custom-fields-2"><span className="wix-picker-name">listCustomFields (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listCustomFields
    import { customFields } from '@wix/members';

    async function listCustomFields(options) {
      const response = await customFields.listCustomFields(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listCustomFields (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customFields } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customFields },
      // Include the auth strategy and host as relevant
    });

    async function listCustomFields(options) {
      const response = await myWixClient.customFields.listCustomFields(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listCustomFields (with elevated permissions)
    import { customFields } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myListCustomFieldsMethod(options) {
      const elevatedListCustomFields = auth.elevate(customFields.listCustomFields);
      const response = await elevatedListCustomFields(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
