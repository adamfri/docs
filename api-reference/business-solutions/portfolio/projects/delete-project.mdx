---
title: "Delete Project"
description: "Deletes a project."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a project.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/portfolio/v1/projects/{projectId}
    ```

    ## Path Params

    <ParamField path="projectId" type="string" required>

      ID of the project to delete.

    </ParamField>

    ## Response Object

    <ResponseField name="projectId" type="string">

      ID of the deleted project.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Deleted | `wix.portfolio.projects.v1.project_deleted` | Triggered when a project is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/portfolio/v1/projects/c68c0dc5-088f-4205-b202-3eef797ea0ab' \
      -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {
        "projectId": "c68c0dc5-088f-4205-b202-3eef797ea0ab"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a project.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { projects } from '@wix/portfolio';

    projects.deleteProject(projectId): Promise<DeleteProjectResponse>
    ```

    ## Method Parameters

    <ParamField body="projectId" type="string" required>

      ID of the project to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<DeleteProjectResponse>`.

    <ResponseField name="projectId" type="string">

      ID of the deleted project.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Deleted | `wix.portfolio.projects.v1.project_deleted` | Triggered when a project is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-project" id="ex-sdk-delete-project-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-project" id="ex-sdk-delete-project-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-project" id="ex-sdk-delete-project-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-project-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-project-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteProject</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteProject (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteProject (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-project-0"><span className="wix-picker-name">deleteProject</span></label>
    <label htmlFor="ex-sdk-delete-project-1"><span className="wix-picker-name">deleteProject (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-project-2"><span className="wix-picker-name">deleteProject (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteProject
    import { projects } from '@wix/portfolio';

    async function deleteProject(projectId) {
      const response = await projects.deleteProject(projectId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteProject (self-hosted)
    import { createClient } from '@wix/sdk';
    import { projects } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { projects },
      // Include the auth strategy and host as relevant
    });

    async function deleteProject(projectId) {
      const response = await myWixClient.projects.deleteProject(projectId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteProject (with elevated permissions)
    import { projects } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myDeleteProjectMethod(projectId) {
      const elevatedDeleteProject = auth.elevate(projects.deleteProject);
      const response = await elevatedDeleteProject(projectId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
