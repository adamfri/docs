---
title: "onParticipationCreated"
description: "Triggered when a participation object is created."
icon: "bolt"
---

Triggered when a `participation` object is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.calendar.v3.participation_created` |
| Package | `@wix/calendar` |
| Permissions | `Manage Calendars`, `Manage Bookings`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onParticipationCreated
  import { participations } from '@wix/calendar';

  participations.onParticipationCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onParticipationCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { participations } from '@wix/calendar';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        participations,
      },
  });

  wixClient.participations.onParticipationCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
