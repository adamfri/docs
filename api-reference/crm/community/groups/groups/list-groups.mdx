---
title: "List Groups"
description: "Retrieves up to 100 groups."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 groups.

    Default sorts by the created date in descending order.
    For `SECRET` groups, only group admins and group members can see a list of groups and their content.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/social-groups-proxy/groups/v2/groups
    ```

    ## Query Params

    <ParamField query="limit" type="integer">

      Number of items to load. Maximum `100`.

    </ParamField>

    <ParamField query="offset" type="integer">

      Number of items to skip in the current sort order.

    </ParamField>

    <ParamField query="contentType" type="string">

      Content type.

      **Supported values:**

      - `PLAIN_TEXT`
      - `DRAFTJS`
      - `RICH_CONTENT`

    </ParamField>

    ## Response Object

    <ResponseField name="groups" type="Group[]">

      Retrieved Groups.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
          https://www.wixapis.com/social-groups-proxy/groups/v2/groups?limit=100 \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "groups": [
              {
                  "id": "8a258cf5-49f3-41dc-b320-65ec2598315b",
                  "slug": "group-1",
                  "privacyStatus": "PUBLIC",
                  "name": "Group 1",
                  "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
                  "memberTitle": "Friends",
                  "settings": {
                      "membersCanInvite": false,
                      "membersCanApprove": false,
                      "welcomeMemberPostEnabled": true,
                      "groupDetailsChangedPostEnabled": true,
                      "showMemberList": true
                  },
                  "membersCount": 1,
                  "ownerId": "f98e4407-15bf-43a3-87fb-a954c49bb20f",
                  "createdDate": "2020-11-06T14:03:50Z",
                  "updatedDate": "2020-11-19T12:38:07Z",
                  "lastActivityDate": "2020-11-19T12:38:08Z"
              },
              {
                  "id": "1de57bda-c03e-4367-91fb-7c334d6a613d",
                  "slug": "group-2",
                  "privacyStatus": "PUBLIC",
                  "name": "Group 2",
                  "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
                  "memberTitle": "Members",
                  "settings": {
                      "membersCanInvite": false,
                      "membersCanApprove": false,
                      "welcomeMemberPostEnabled": true,
                      "groupDetailsChangedPostEnabled": true,
                      "showMemberList": true
                  },
                  "membersCount": 2,
                  "ownerId": "6369e045-763d-4876-8220-91ee3f0381c0",
                  "createdDate": "2020-11-06T14:03:50Z",
                  "updatedDate": "2020-11-19T12:38:07Z",
                  "lastActivityDate": "2020-11-19T12:38:08Z"
              }
          ],
          "metadata": {
              "count": 2,
              "offset": 0,
              "total": 2
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 groups.

    Default sorts by the created date in descending order.
    For `SECRET` groups, only group admins and group members can see a list of groups and their content.

    ## Method Declaration

    ```typescript
    import { groups } from '@wix/groups';

    groups.listGroups(options): Promise<ListGroupsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListGroupsOptions">

      Limit and offset options.

      <Expandable title="options properties">

        <ParamField body="limit" type="integer">

          Number of items to load. Maximum `100`.

        </ParamField>

        <ParamField body="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

        <ParamField body="contentType" type="string">

          Content type.

          **Supported values:**

          - `PLAIN_TEXT`
          - `DRAFTJS`
          - `RICH_CONTENT`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListGroupsResponse>`.

    <ResponseField name="groups" type="Group[]">

      Retrieved Groups.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-groups" id="ex-sdk-list-groups-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-groups" id="ex-sdk-list-groups-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-groups" id="ex-sdk-list-groups-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-groups-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-groups-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listGroups</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listGroups (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-groups-0"><span className="wix-picker-name">listGroups</span></label>
    <label htmlFor="ex-sdk-list-groups-1"><span className="wix-picker-name">listGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-groups-2"><span className="wix-picker-name">listGroups (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listGroups
    import { groups } from '@wix/groups';

    async function listGroups(options) {
      const response = await groups.listGroups(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listGroups (self-hosted)
    import { createClient } from '@wix/sdk';
    import { groups } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { groups },
      // Include the auth strategy and host as relevant
    });

    async function listGroups(options) {
      const response = await myWixClient.groups.listGroups(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listGroups (with elevated permissions)
    import { groups } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myListGroupsMethod(options) {
      const elevatedListGroups = auth.elevate(groups.listGroups);
      const response = await elevatedListGroups(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
