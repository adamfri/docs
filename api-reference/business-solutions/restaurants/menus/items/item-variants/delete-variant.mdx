---
title: "Delete Variant"
description: "Note: The Item Variants API only works with the Wix Restaurants Menus app."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item Variants API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Permanently deletes an item variant.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/restaurants/menus/v1/variants/{variantId}
    ```

    ## Path Params

    <ParamField path="variantId" type="string" required>

      Item variant ID.

    </ParamField>

    ## Response Object

    Returns `DeleteVariantResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Variant Deleted | `wix.restaurants.menus.v1.item_variant_deleted` | Triggered when a variant is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE https://www.wixapis.com/restaurants/item-variants/v1/variants/cbbf4358-292c-4e9e-a820-7e75b4551120 \
      -H 'Authorization: <AUTH>'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item Variants API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Permanently deletes an item variant.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { itemVariants } from '@wix/restaurants';

    itemVariants.deleteVariant(variantId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="variantId" type="string" required>

      Item variant ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Variant Deleted | `wix.restaurants.menus.v1.item_variant_deleted` | Triggered when a variant is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-variant" id="ex-sdk-delete-variant-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-variant" id="ex-sdk-delete-variant-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-variant" id="ex-sdk-delete-variant-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-variant-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-variant-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteVariant</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteVariant (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteVariant (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-variant-0"><span className="wix-picker-name">deleteVariant</span></label>
    <label htmlFor="ex-sdk-delete-variant-1"><span className="wix-picker-name">deleteVariant (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-variant-2"><span className="wix-picker-name">deleteVariant (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteVariant
    import { itemVariants } from '@wix/restaurants';

    async function deleteVariant(variantId) {
      const response = await itemVariants.deleteVariant(variantId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteVariant (self-hosted)
    import { createClient } from '@wix/sdk';
    import { itemVariants } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { itemVariants },
      // Include the auth strategy and host as relevant
    });

    async function deleteVariant(variantId) {
      const response = await myWixClient.itemVariants.deleteVariant(variantId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteVariant (with elevated permissions)
    import { itemVariants } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myDeleteVariantMethod(variantId) {
      const elevatedDeleteVariant = auth.elevate(itemVariants.deleteVariant);
      const response = await elevatedDeleteVariant(variantId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
