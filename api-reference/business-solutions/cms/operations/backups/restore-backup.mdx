---
title: "Restore Backup"
description: "Restores all data from a backup."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Restores all data from a backup.

    The process of restoring data from a backup takes time.
    You can check whether your restoration has completed successfully with List Restorations.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE-BACKUPS">Manage Data Backups</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/wix-data/v2/backups/{backupId}/restore
    ```

    ## Path Params

    <ParamField path="backupId" type="string" required>

      ID of backup to be restored.

    </ParamField>

    ## Response Object

    <ResponseField name="restoration" type="Restoration">

      Details of data restoration from backup.

      <Expandable title="restoration properties">

        <ResponseField name="id" type="string">

          Restoration ID.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="backup" type="Backup">

          Details of the backup used for the restoration.

          **Read-only.**

          <Expandable title="backup properties">

            <ResponseField name="id" type="string">

              Backup ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="status" type="string">

              Backup status.

              **Read-only.**

              **Supported values:**

              - `PENDING` — Backup creation is in progress.
              - `READY` — Backup has been created successfully and can be used for data restoration.
              - `FAILED` — Backup creation has failed.
              - `DELETED` — Backup has been deleted.
              - `CANCELED` — Backup has been canceled.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of backup, based on how it was triggered.

              **Read-only.**

              **Supported values:**

              - `ON_DEMAND` — Backup taken on demand.
              - `AUTO` — Backup taken automatically by the system on a regular schedule.

            </ResponseField>

            <ResponseField name="requestedDate" type="datetime">

              Date and time the backup was requested.

              **Read-only.**

            </ResponseField>

            <ResponseField name="startedDate" type="datetime">

              Date and time the backup commenced. Value is `null` until the backup process begins in the background.

              **Read-only.**

            </ResponseField>

            <ResponseField name="finishedDate" type="datetime">

              Date and time the backup process finished. Value is `null` until the backup process is completed in the background.

              **Read-only.**

            </ResponseField>

            <ResponseField name="deletedDate" type="datetime">

              Date and time the backup was deleted. Value is `null` if that backup hasn't been deleted.

              **Read-only.**

            </ResponseField>

            <ResponseField name="sizeInBytes" type="integer">

              Backup size in bytes. Value is `null` until the backup process is completed.

              **Read-only.**

            </ResponseField>

            <ResponseField name="collections" type="Collection[]">

              IDs and display names of collections the backup contains.

              **Read-only.** Constraints: `maxItems 1000`.

              _Nested structure omitted for brevity — see the `Collection[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="status" type="string">

          Status of restoration.

          **Read-only.**

          **Supported values:**

          - `PENDING` — Restoration from a backup is in progress.
          - `COMPLETED` — Restoration from a backup has been successful.
          - `FAILED` — Restoration from a backup has failed.

        </ResponseField>

        <ResponseField name="requestedDate" type="datetime">

          Date and time the restoration was requested.

          **Read-only.**

        </ResponseField>

        <ResponseField name="startedDate" type="datetime">

          Date and time the restoration commenced. Value is `null` until the restoration process begins in the background.

          **Read-only.**

        </ResponseField>

        <ResponseField name="finishedDate" type="datetime">

          Date and time the restoration finished. Value is `null` until the restoration process is completed in the background.

          **Read-only.**

        </ResponseField>

        <ResponseField name="restorationCollections" type="RestorationCollection[]">

          Restored collections.

          **Read-only.** Constraints: `maxItems 1000`.

          <Expandable title="restorationCollections properties">

            <ResponseField name="dataCollectionId" type="string">

              Collections to be restored.

              Note: If collections have a multi-reference relationship,
              the preexisting references will be restored if at least one of those collections are restored.

              Constraints: `minLength 1`, `maxLength 1000`.

            </ResponseField>

            <ResponseField name="restoreDestination" type="RestoreDestination">

              Destination where to restore the collection.
              When not specified destination is taken from backup.

              _Nested structure omitted for brevity — see the `RestoreDestination` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/backups/bfab4dac-c835-4cf7-8b47-adbe1ca1ea34/restore' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
         "restoration": {
           "id": "0bd30197-2012-4c35-a4f0-a0d5ef7ecea0",
           "requestedDate": "2022-05-26T07:17:02.209Z",
           "startedDate": null,
           "finishedDate": null,
           "backup": {
             "id": "bfab4dac-c835-4cf7-8b47-adbe1ca1ea34",
             "requestedDate": "2022-05-25T15:10:56.394Z",
             "startedDate": "2022-05-25T15:10:56.603Z",
             "finishedDate": "2022-05-25T15:10:56.913Z",
             "deletedDate": null,
             "sizeInBytes": "1584",
             "status": "READY",
             "type": "ON_DEMAND"
           },
           "status": "PENDING"
         }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Restores all data from a backup.

    The process of restoring data from a backup takes time.
    You can check whether your restoration has completed successfully with List Restorations.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE-BACKUPS">Manage Data Backups</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { backups } from '@wix/data';

    backups.restoreBackup(backupId): Promise<RestoreBackupResponse>
    ```

    ## Method Parameters

    <ParamField body="backupId" type="string" required>

      ID of backup to be restored.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<RestoreBackupResponse>`.

    <ResponseField name="restoration" type="Restoration">

      Details of data restoration from backup.

      <Expandable title="restoration properties">

        <ResponseField name="_id" type="string">

          Restoration ID.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="backup" type="Backup">

          Details of the backup used for the restoration.

          **Read-only.**

          <Expandable title="backup properties">

            <ResponseField name="_id" type="string">

              Backup ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="status" type="string">

              Backup status.

              **Read-only.**

              **Supported values:**

              - `PENDING` — Backup creation is in progress.
              - `READY` — Backup has been created successfully and can be used for data restoration.
              - `FAILED` — Backup creation has failed.
              - `DELETED` — Backup has been deleted.
              - `CANCELED` — Backup has been canceled.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of backup, based on how it was triggered.

              **Read-only.**

              **Supported values:**

              - `ON_DEMAND` — Backup taken on demand.
              - `AUTO` — Backup taken automatically by the system on a regular schedule.

            </ResponseField>

            <ResponseField name="requestedDate" type="datetime">

              Date and time the backup was requested.

              **Read-only.**

            </ResponseField>

            <ResponseField name="startedDate" type="datetime">

              Date and time the backup commenced. Value is `null` until the backup process begins in the background.

              **Read-only.**

            </ResponseField>

            <ResponseField name="finishedDate" type="datetime">

              Date and time the backup process finished. Value is `null` until the backup process is completed in the background.

              **Read-only.**

            </ResponseField>

            <ResponseField name="deletedDate" type="datetime">

              Date and time the backup was deleted. Value is `null` if that backup hasn't been deleted.

              **Read-only.**

            </ResponseField>

            <ResponseField name="sizeInBytes" type="integer">

              Backup size in bytes. Value is `null` until the backup process is completed.

              **Read-only.**

            </ResponseField>

            <ResponseField name="collections" type="Collection[]">

              IDs and display names of collections the backup contains.

              **Read-only.** Constraints: `maxItems 1000`.

              _Nested structure omitted for brevity — see the `Collection[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="status" type="string">

          Status of restoration.

          **Read-only.**

          **Supported values:**

          - `PENDING` — Restoration from a backup is in progress.
          - `COMPLETED` — Restoration from a backup has been successful.
          - `FAILED` — Restoration from a backup has failed.

        </ResponseField>

        <ResponseField name="requestedDate" type="datetime">

          Date and time the restoration was requested.

          **Read-only.**

        </ResponseField>

        <ResponseField name="startedDate" type="datetime">

          Date and time the restoration commenced. Value is `null` until the restoration process begins in the background.

          **Read-only.**

        </ResponseField>

        <ResponseField name="finishedDate" type="datetime">

          Date and time the restoration finished. Value is `null` until the restoration process is completed in the background.

          **Read-only.**

        </ResponseField>

        <ResponseField name="restorationCollections" type="RestorationCollection[]">

          Restored collections.

          **Read-only.** Constraints: `maxItems 1000`.

          <Expandable title="restorationCollections properties">

            <ResponseField name="dataCollectionId" type="string">

              Collections to be restored.

              Note: If collections have a multi-reference relationship,
              the preexisting references will be restored if at least one of those collections are restored.

              Constraints: `minLength 1`, `maxLength 1000`.

            </ResponseField>

            <ResponseField name="restoreDestination" type="RestoreDestination">

              Destination where to restore the collection.
              When not specified destination is taken from backup.

              _Nested structure omitted for brevity — see the `RestoreDestination` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-restore-backup" id="ex-sdk-restore-backup-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-restore-backup" id="ex-sdk-restore-backup-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-restore-backup" id="ex-sdk-restore-backup-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-restore-backup-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-restore-backup-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">restoreBackup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">restoreBackup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">restoreBackup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-restore-backup-0"><span className="wix-picker-name">restoreBackup</span></label>
    <label htmlFor="ex-sdk-restore-backup-1"><span className="wix-picker-name">restoreBackup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-restore-backup-2"><span className="wix-picker-name">restoreBackup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript restoreBackup
    import { backups } from '@wix/data';

    async function restoreBackup(backupId) {
      const response = await backups.restoreBackup(backupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript restoreBackup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { backups } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { backups },
      // Include the auth strategy and host as relevant
    });

    async function restoreBackup(backupId) {
      const response = await myWixClient.backups.restoreBackup(backupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript restoreBackup (with elevated permissions)
    import { backups } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myRestoreBackupMethod(backupId) {
      const elevatedRestoreBackup = auth.elevate(backups.restoreBackup);
      const response = await elevatedRestoreBackup(backupId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
