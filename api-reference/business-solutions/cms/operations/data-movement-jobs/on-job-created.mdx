---
title: "onJobCreated"
description: "Triggered when a job is submitted."
icon: "bolt"
---

Triggered when a job is submitted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.hub.v1.job_created` |
| Package | `@wix/data` |
| Permissions | `Data Mover Manage Jobs` |

## Examples

<CodeGroup>

  ```javascript onJobCreated
  import { movementJobs } from '@wix/data';

  movementJobs.onJobCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onJobCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { movementJobs } from '@wix/data';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        movementJobs,
      },
  });

  wixClient.movementJobs.onJobCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
