---
title: "onRewardCreated"
description: "Triggered when a reward is created."
icon: "bolt"
---

Triggered when a reward is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.v1.reward_created` |
| Package | `@wix/loyalty` |
| Permissions | `Read Loyalty`, `Manage Loyalty` |

## Examples

<CodeGroup>

  ```javascript onRewardCreated
  import { rewards } from '@wix/loyalty';

  rewards.onRewardCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRewardCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { rewards } from '@wix/loyalty';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        rewards,
      },
  });

  wixClient.rewards.onRewardCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
