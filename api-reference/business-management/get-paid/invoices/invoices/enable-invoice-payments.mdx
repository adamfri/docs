---
title: "Enable Invoice Payments"
description: "Enables payments for an invoice."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Enables payments for an invoice.

    Use this to record manual (offline) or MOTO (Mail Order/Telephone Order) payments against an invoice. Creates a Wix eCommerce order if one doesn't exist, which enables payment recording against the invoice. To generate a checkout URL for online payment instead, call [Initiate Payment](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoices/initiate-payment).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/invoices/v4/invoices/{invoiceId}/enable-payments
    ```

    ## Path Params

    <ParamField path="invoiceId" type="string" required>

      Invoice ID.

    </ParamField>

    ## Response Object

    <ResponseField name="orderId" type="string">

      Wix eCommerce order ID.

      **Read-only.** Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_ACTION` | Invoice isn't in a state that allows payment. |
    | `400` | `INVALID_ARGUMENT` | `ORDER_TOTAL_EXCEEDS_LIMIT` | Invoice total must be less than 10^13. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Updated | `wix.invoices.invoices.v4.invoice_updated` | Triggered when an invoice is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Enables payments for an existing invoice, returning the associated Wix eCommerce order ID.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/invoices/v4/invoices/51be0e68-75c3-4b7a-97e7-3a6643ecf3e2/enable-payments' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "invoiceId": "51be0e68-75c3-4b7a-97e7-3a6643ecf3e2"
        }'
      ```

      ```json Response
      {
        "orderId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Enables payments for an invoice.

    Use this to record manual (offline) or MOTO (Mail Order/Telephone Order) payments against an invoice. Creates a Wix eCommerce order if one doesn't exist, which enables payment recording against the invoice. To generate a checkout URL for online payment instead, call [Initiate Payment](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoices/initiate-payment).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { invoices } from '@wix/get-paid';

    invoices.enableInvoicePayments(invoiceId): Promise
    ```

    ## Method Parameters

    <ParamField body="invoiceId" type="string" required>

      Invoice ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="orderId" type="string">

      Wix eCommerce order ID.

      **Read-only.** Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_ACTION` | Invoice isn't in a state that allows payment. |
    | `400` | `INVALID_ARGUMENT` | `ORDER_TOTAL_EXCEEDS_LIMIT` | Invoice total must be less than 10^13. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Updated | `wix.invoices.invoices.v4.invoice_updated` | Triggered when an invoice is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-enable-invoice-payments" id="ex-sdk-enable-invoice-payments-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-enable-invoice-payments" id="ex-sdk-enable-invoice-payments-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-enable-invoice-payments" id="ex-sdk-enable-invoice-payments-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-enable-invoice-payments-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-enable-invoice-payments-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">enableInvoicePayments</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">enableInvoicePayments (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">enableInvoicePayments (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-enable-invoice-payments-0"><span className="wix-picker-name">enableInvoicePayments</span></label>
    <label htmlFor="ex-sdk-enable-invoice-payments-1"><span className="wix-picker-name">enableInvoicePayments (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-enable-invoice-payments-2"><span className="wix-picker-name">enableInvoicePayments (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript enableInvoicePayments
    import { invoices } from '@wix/get-paid';

    async function enableInvoicePayments(invoiceId) {
      const response = await invoices.enableInvoicePayments(invoiceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript enableInvoicePayments (self-hosted)
    import { createClient } from '@wix/sdk';
    import { invoices } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { invoices },
      // Include the auth strategy and host as relevant
    });

    async function enableInvoicePayments(invoiceId) {
      const response = await myWixClient.invoices.enableInvoicePayments(invoiceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript enableInvoicePayments (with elevated permissions)
    import { invoices } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myEnableInvoicePaymentsMethod(invoiceId) {
      const elevatedEnableInvoicePayments = auth.elevate(invoices.enableInvoicePayments);
      const response = await elevatedEnableInvoicePayments(invoiceId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
