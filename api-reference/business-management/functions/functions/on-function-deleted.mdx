---
title: "onFunctionDeleted"
description: "Triggered when a function is deleted."
icon: "bolt"
---

Triggered when a function is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.functions.v1.function_deleted` |
| Package | `@wix/functions` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Manage Stores`, `View Forms`, `Manage form submissions.`, `Manage forms`, `Manage Events`, `Manage Functions`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onFunctionDeleted
  import { functions } from '@wix/functions';

  functions.onFunctionDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onFunctionDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { functions } from '@wix/functions';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        functions,
      },
  });

  wixClient.functions.onFunctionDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
