---
title: "Delete Tag"
description: "Deletes a tag."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    Deletes a tag.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-OS.MANAGE-TAGS">Manage Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/tags/v1/tags/{tagId}
    ```

    ## Path Params

    <ParamField path="tagId" type="string" required>

      ID of the tag to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteTagResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a tag.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-OS.MANAGE-TAGS">Manage Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tags } from '@wix/tags';

    tags.deleteTag(tagId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="tagId" type="string" required>

      ID of the tag to delete.

      Constraints: `maxLength 5`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tag Deleted | `wix.os.tags.v1.tag_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-tag" id="ex-sdk-delete-tag-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-tag" id="ex-sdk-delete-tag-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-tag" id="ex-sdk-delete-tag-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-tag-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-tag-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteTag</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteTag (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-tag-0"><span className="wix-picker-name">deleteTag</span></label>
    <label htmlFor="ex-sdk-delete-tag-1"><span className="wix-picker-name">deleteTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-tag-2"><span className="wix-picker-name">deleteTag (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteTag
    import { tags } from '@wix/tags';

    async function deleteTag(tagId) {
      const response = await tags.deleteTag(tagId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteTag (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tags } from '@wix/tags';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tags },
      // Include the auth strategy and host as relevant
    });

    async function deleteTag(tagId) {
      const response = await myWixClient.tags.deleteTag(tagId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteTag (with elevated permissions)
    import { tags } from '@wix/tags';
    import { auth } from '@wix/essentials';

    async function myDeleteTagMethod(tagId) {
      const elevatedDeleteTag = auth.elevate(tags.deleteTag);
      const response = await elevatedDeleteTag(tagId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
