---
title: "Checkout Object"
description: "A checkout represents a cart that is in the process of being purchased."
icon: "cube"
---

A checkout represents a cart that is in the process of being purchased. It holds calculated prices, tax, shipping, and buyer information needed to complete the purchase.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Checkout ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="lineItems" type="LineItem[]">

      Line items.

      Max: 300 items

      **Read-only.** Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="lineItems properties">

        <ResponseField name="id" type="string">

          Line item ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="quantity" type="integer">

          Item quantity.

          Min: `"1"`
          Max: `"100000"`

          Constraints: `min 1`, `max 100000`.

        </ResponseField>

        <ResponseField name="catalogReference" type="CatalogReference">

          Catalog and item reference. Includes IDs for the item and the catalog it came from, as well as further optional info. Optional for custom line items, which don't trigger the Catalog service plugin.

          <Expandable title="catalogReference properties">

            <ResponseField name="catalogItemId" type="string">

              ID of the item within the catalog it belongs to.

              Constraints: `minLength 1`, `maxLength 36`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the app providing the catalog.

              You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

              For items from Wix catalogs, the following values always apply:
              + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
              + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
              + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

              Constraints: `minLength 1`.

            </ResponseField>

            <ResponseField name="options" type="object">

              Additional item details in `key:value` pairs.

              Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

              For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
              or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="productName" type="ProductName">

          Item name.
          + Stores - `product.name`
          + Bookings - `service.info.name`
          + Events - `ticket.name`

          **Read-only.**

          <Expandable title="productName properties">

            <ResponseField name="original" type="string">

              __Required.__ Item name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

              Min: 1 character.
              Max: 200 characters.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="translated" type="string">

              Item name translated into the buyer's language.

              Min: 1 character.
              Max: 400 characters.
              Default: Same as `original`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="url" type="PageUrlV2">

          URL to the item's page on the site.

          **Read-only.**

          <Expandable title="url properties">

            <ResponseField name="relativePath" type="string">

              The relative path for the page within the site. For example, `/product-page/a-product`.

            </ResponseField>

            <ResponseField name="url" type="string">

              The page's full URL. For example, `https://mysite.com/product-page/a-product`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="price" type="MultiCurrencyPrice">

          Item price **after** catalog-defined discount and line item discounts.

          **Read-only.**

          <Expandable title="price properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemPrice" type="MultiCurrencyPrice">

          Total line item price **after** catalog-defined discount and line item discounts.

          **Read-only.**

          <Expandable title="lineItemPrice properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="fullPrice" type="MultiCurrencyPrice">

          Item price **before** catalog-defined discount. Defaults to `price` when not provided.

          **Read-only.**

          <Expandable title="fullPrice properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceBeforeDiscounts" type="MultiCurrencyPrice">

          Item price **before** line item discounts and **after** catalog-defined discount. Defaults to `price` when not provided.

          **Read-only.**

          <Expandable title="priceBeforeDiscounts properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalPriceAfterTax" type="MultiCurrencyPrice">

          Total price after all discounts and tax.

          **Read-only.**

          <Expandable title="totalPriceAfterTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalPriceBeforeTax" type="MultiCurrencyPrice">

          Total price after discounts, and before tax.

          **Read-only.**

          <Expandable title="totalPriceBeforeTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="ItemTaxFullDetails">

          Tax details for this line item.

          **Read-only.**

          <Expandable title="taxDetails properties">

            <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

              Amount for which tax is calculated.

              <Expandable title="taxableAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxRate" type="string">

              Tax rate %, as a decimal point between 0 and 1.

              Constraints: `>= 0`, `<= 1`.

            </ResponseField>

            <ResponseField name="totalTax" type="MultiCurrencyPrice">

              Calculated tax, based on `taxable_amount` and `tax_rate`.

              <Expandable title="totalTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rateBreakdown" type="TaxRateBreakdown[]" deprecated>

              If breakdown exists, the sum of rates in the breakdown must equal `tax_rate`. Deprecated - use 'tax_breakdown' instead.

              **Read-only.** **Deprecated.**

              <Expandable title="rateBreakdown properties">

                <ResponseField name="name" type="string">

                  Name of tax against which the calculation was performed.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="rate" type="string">

                  Rate at which this tax detail was calculated.

                  Constraints: `>= 0`, `<= 1`.

                </ResponseField>

                <ResponseField name="tax" type="MultiCurrencyPrice">

                  Amount of tax for this tax detail.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxBreakdown" type="TaxBreakdown[]">

              tax information for a line item.

              **Read-only.** Constraints: `minItems 0`, `maxItems 1000`.

              <Expandable title="taxBreakdown properties">

                <ResponseField name="jurisdiction" type="string">

                  The name of the jurisdiction to which this tax detail applies. For example, "New York" or "Quebec".

                </ResponseField>

                <ResponseField name="nonTaxableAmount" type="MultiCurrencyPrice">

                  The amount of this line item price that was considered nontaxable. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="rate" type="string">

                  The rate at which this tax detail was calculated, e.g 0.1000 signifies 10% tax and 2.0000 signifies 200% tax. (Decimal value)

                </ResponseField>

                <ResponseField name="taxAmount" type="MultiCurrencyPrice">

                  The amount of tax estimated for this line item. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

                  The taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxType" type="string">

                  The type of tax that was calculated. Depends on the jurisdiction's tax laws. For example, "Sales Tax", "Income Tax", "Value Added Tax", etc.

                </ResponseField>

                <ResponseField name="taxName" type="string">

                  The name of the tax against which this tax amount was calculated. For example, "NY State Sales Tax", "Quebec GST", etc.
                  This name should be explicit enough to allow the merchant to understand what tax was calculated.

                </ResponseField>

                <ResponseField name="jurisdictionType" type="string">

                  The type of the jurisdiction in which this tax detail applies.

                  **Supported values:**

                  - `UNDEFINED`
                  - `COUNTRY`
                  - `STATE`
                  - `COUNTY`
                  - `CITY`
                  - `SPECIAL`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Discount for this line item's entire quantity.

          **Read-only.**

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="descriptionLines" type="DescriptionLine[]">

          Line item description lines. Used for display purposes for the cart, checkout and order.

          **Read-only.**

          <Expandable title="descriptionLines properties">

            <ResponseField name="name" type="DescriptionLineName">

              Description line name.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  Description line name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Description line name translated into the buyer's language.

                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="media" type="Image">

          Line item image details.

          **Read-only.**

          <Expandable title="media properties">

            <ResponseField name="id" type="string">

              WixMedia image ID.

            </ResponseField>

            <ResponseField name="url" type="string">

              Image URL.

            </ResponseField>

            <ResponseField name="height" type="integer">

              Original image height.

              **Read-only.**

            </ResponseField>

            <ResponseField name="width" type="integer">

              Original image width.

              **Read-only.**

            </ResponseField>

            <ResponseField name="altText" type="string">

              Image alt text.

            </ResponseField>

            <ResponseField name="filename" type="string">

              Image filename.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="availability" type="ItemAvailabilityInfo">

          Item availability details.

          **Read-only.**

          <Expandable title="availability properties">

            <ResponseField name="status" type="string">

              Item availability status.

              **Supported values:**

              - `AVAILABLE`
              - `NOT_FOUND` — Item does not exist.
              - `NOT_AVAILABLE` — Item is not in stock.
              - `PARTIALLY_AVAILABLE` — Available quantity is less than requested.

            </ResponseField>

            <ResponseField name="quantityAvailable" type="integer">

              Quantity available.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="physicalProperties" type="PhysicalProperties">

          Physical properties of the item. When relevant, contains information such as SKU, item weight, and shippability.

          **Read-only.**

          <Expandable title="physicalProperties properties">

            <ResponseField name="weight" type="number">

              Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request.

            </ResponseField>

            <ResponseField name="sku" type="string">

              Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).

            </ResponseField>

            <ResponseField name="shippable" type="boolean">

              Whether this line item is shippable.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="itemType" type="ItemType">

          Item type. Either a preset type or custom.

          **Read-only.**

          <Expandable title="One of the following">

            <ResponseField name="itemTypeData" type="itemTypeData">

              itemTypeData

              <Expandable title="itemTypeData properties">

                <ResponseField name="preset" type="string">

                  Preset item type.

                  **Supported values:**

                  - `UNRECOGNISED`
                  - `PHYSICAL`
                  - `DIGITAL`
                  - `GIFT_CARD`
                  - `SERVICE`

                </ResponseField>

                <ResponseField name="custom" type="string">

                  Custom item type. When none of the preset types are suitable, specifies the custom type.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="subscriptionOptionInfo" type="SubscriptionOptionInfo">

          Subscription option information.

          **Read-only.**

          <Expandable title="subscriptionOptionInfo properties">

            <ResponseField name="subscriptionSettings" type="SubscriptionSettings">

              Subscription option settings.

              <Expandable title="subscriptionSettings properties">

                <ResponseField name="frequency" type="string">

                  Frequency of recurring payment.

                  **Supported values:**

                  - `UNDEFINED`
                  - `DAY`
                  - `WEEK`
                  - `MONTH`
                  - `YEAR`

                </ResponseField>

                <ResponseField name="interval" type="integer">

                  Interval of recurring payment.

                  Default: `1`.
                  If SubscriptionFrequency is Day the minimum interval is 7

                </ResponseField>

                <ResponseField name="autoRenewal" type="boolean">

                  Whether subscription is renewed automatically at the end of each period.

                </ResponseField>

                <ResponseField name="billingCycles" type="integer">

                  Number of billing cycles before subscription ends. Ignored if `autoRenewal` is `true`.

                </ResponseField>

                <ResponseField name="enableCustomerCancellation" type="boolean">

                  Whether to allow the customer to cancel the subscription..

                  **Developer Preview.**

                </ResponseField>

                <ResponseField name="freeTrialPeriod" type="FreeTrialPeriod">

                  Period until first cycle starts. If applied payNow will be 0
                  If None => no free trial

                  **Developer Preview.**

                  _Nested structure omitted for brevity — see the `FreeTrialPeriod` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="title" type="Title">

              Subscription option title.

              <Expandable title="title properties">

                <ResponseField name="original" type="string">

                  Subscription option name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Constraints: `minLength 1`, `maxLength 150`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Subscription option name translated into the buyer's language.

                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="description" type="Description">

              Subscription option description.

              <Expandable title="description properties">

                <ResponseField name="original" type="string">

                  Subscription option description.

                  Constraints: `maxLength 500`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Translated subscription option description.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paymentOption" type="string">

          Type of selected payment option for current item.

          Default: `FULL_PAYMENT_ONLINE`

          **Read-only.**

          **Supported values:**

          - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
          - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
          - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
          - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
          - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
          - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

        </ResponseField>

        <ResponseField name="serviceProperties" type="ServiceProperties">

          Service properties. When relevant, this contains information such as date and number of participants.

          **Read-only.**

          <Expandable title="serviceProperties properties">

            <ResponseField name="scheduledDate" type="datetime">

              Date and time the service is to be provided, in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.
              For example, the start time of a class.

            </ResponseField>

            <ResponseField name="numberOfParticipants" type="integer">

              The number of people participating in the service. For example, the number of people attending a class or the number of people per hotel room.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="rootCatalogItemId" type="string">

          In cases where `catalogReference.catalogItemId` is NOT the actual catalog item ID, this field will return the true item's ID.
          + For example, for Wix Bookings, `catalogReference.catalogItemId` is the booking ID. Therefore this value is set to the service ID.
          + In most cases, this field has the same value as `catalogReference.catalogItemId`.
          + Used in membership validation.

          **Read-only.**

        </ResponseField>

        <ResponseField name="priceDescription" type="PriceDescription">

          Additional description for the price. For example, when price is 0 but additional details about the actual price are needed - "Starts at $67".

          **Read-only.**

          <Expandable title="priceDescription properties">

            <ResponseField name="original" type="string">

              __Required.__ Price description in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

            <ResponseField name="translated" type="string">

              Price description translated into the buyer's language.

              Default: Same as `original`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="depositAmount" type="MultiCurrencyPrice">

          Partial payment to be paid upfront during the checkout. Eligible for catalog items with `lineItem.paymentOption` type `DEPOSIT_ONLINE` only.

          **Read-only.**

          <Expandable title="depositAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="deliveryProfileId" type="string">

          Delivery profile ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="customLineItem" type="boolean">

          Whether the line item is a custom line item. Custom line items don't trigger the Catalog service plugin.

          **Read-only.** **Developer Preview.**

        </ResponseField>

        <ResponseField name="consentRequiredPaymentPolicy" type="string">

          Item payment policy that requires customer consent to complete purchase. The payment policy will be displayed on the checkout page.

          **Read-only.**

        </ResponseField>

        <ResponseField name="catalogOverrideFields" type="CatalogOverrideFields">

          Overriding values for catalog item properties.

          To override catalog fields, your app must have the permission scope named "Manage eCommerce - Admin Permissions".
          Learn more about [permission scopes](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

          **Developer Preview.**

          <Expandable title="catalogOverrideFields properties">

            <ResponseField name="productName" type="ProductName">

              Item name.

              <Expandable title="productName properties">

                <ResponseField name="original" type="string">

                  __Required.__ Item name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Item name translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="price" type="string">

              Item price **after** discounts.

            </ResponseField>

            <ResponseField name="fullPrice" type="string">

              Item price **before** discounts.

            </ResponseField>

            <ResponseField name="descriptionLines" type="DescriptionLine[]">

              Item description lines. Used when displaying the line item to customers.

              Constraints: `minItems 0`, `maxItems 20`.

              <Expandable title="descriptionLines properties">

                <ResponseField name="name" type="DescriptionLineName">

                  Description line name.

                  _Nested structure omitted for brevity — see the `DescriptionLineName` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="physicalProperties" type="PhysicalProperties">

              Physical properties of the item.

              <Expandable title="physicalProperties properties">

                <ResponseField name="weight" type="number">

                  Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request.

                </ResponseField>

                <ResponseField name="sku" type="string">

                  Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).

                </ResponseField>

                <ResponseField name="shippable" type="boolean">

                  Whether this line item is shippable.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="image" type="Image">

              Item image.

              <Expandable title="image properties">

                <ResponseField name="id" type="string">

                  WixMedia image ID.

                </ResponseField>

                <ResponseField name="height" type="integer">

                  Original image height.

                </ResponseField>

                <ResponseField name="width" type="integer">

                  Original image width.

                </ResponseField>

                <ResponseField name="altText" type="string">

                  Image alt text. Optional.

                </ResponseField>

                <ResponseField name="filename" type="string">

                  Image filename. Optional.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="paymentOption" type="PaymentOption">

              Payment method selected for the item.

              **Developer Preview.**

              <Expandable title="paymentOption properties">

                <ResponseField name="value" type="string">

                  Type of selected payment option for current item.

                  Default: `FULL_PAYMENT_ONLINE`

                  **Supported values:**

                  - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
                  - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
                  - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
                  - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
                  - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
                  - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="depositAmount" type="string">

              Only eligible for catalog items with `lineItem.paymentOption.value` type of `DEPOSIT_ONLINE`.

            </ResponseField>

            <ResponseField name="savePaymentMethod" type="boolean">

              Whether to save the payment method on the order.

              Default: `false`

            </ResponseField>

            <ResponseField name="inventoryAppId" type="string">

              ID of the app managing the inventory.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceUndetermined" type="boolean">

          Whether the price is not yet defined, and will be updated after the order is created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="fixedQuantity" type="boolean">

          Whether the line item quantity is fixed and cannot be changed.

          **Read-only.**

        </ResponseField>

        <ResponseField name="savePaymentMethod" type="boolean">

          Whether to save the payment method on the order.

          Default: `false`

          **Read-only.**

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          Address to use for tax calculation purposes.

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              <Expandable title="TaxableAddressData properties">

                <ResponseField name="addressType" type="string">

                  taxable address type. if this field is selected, the address is automatically resolved, and the tax is calculated accordingly.

                  **Supported values:**

                  - `BUSINESS`
                  - `BILLING`
                  - `SHIPPING`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="extendedFields" type="ExtendedFields">

          Custom extended fields for the line item object.

          [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured using the 'Checkout & Orders - Line Items Schema Plugin' in the app dashboard before they can be accessed with API calls.

          **Developer Preview.**

          <Expandable title="extendedFields properties">

            <ResponseField name="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              <Expandable title="namespaces properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="object" />

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="policies" type="Policy[]">

          Policies to be displayed to the customer on the checkout page.

          **Read-only.** Constraints: `maxItems 5`.

          <Expandable title="policies properties">

            <ResponseField name="title" type="string">

              Policy title - should be translated

            </ResponseField>

            <ResponseField name="content" type="string">

              Policy content - should be translated

              Constraints: `minLength 1`, `maxLength 3000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="membersOnly" type="boolean">

          Whether the item can only be purchased by site members.

          Default: `false`

          **Read-only.**

        </ResponseField>

        <ResponseField name="modifierGroups" type="ModifierGroup[]">

          Modifier groups that were added to the item.

          **Read-only.** Constraints: `maxItems 25`.

          <Expandable title="modifierGroups properties">

            <ResponseField name="id" type="string">

              Modifier group ID.

              Constraints: `minLength 1`, `maxLength 36`.

            </ResponseField>

            <ResponseField name="name" type="TranslatableString">

              Modifier group name.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  __Required.__ String in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  String translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="modifiers" type="ItemModifier[]">

              List of modifiers in this group.

              Constraints: `minItems 1`, `maxItems 25`.

              <Expandable title="modifiers properties">

                <ResponseField name="id" type="string">

                  Modifier ID.

                  Constraints: `minLength 1`, `maxLength 36`.

                </ResponseField>

                <ResponseField name="quantity" type="integer">

                  The quantity of this modifier.

                </ResponseField>

                <ResponseField name="label" type="TranslatableString">

                  Primary display label for the modifier.

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

                <ResponseField name="details" type="TranslatableString">

                  Additional details.

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

                <ResponseField name="price" type="MultiCurrencyPrice">

                  The price of the modifier.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="modifiersTotalPrice" type="MultiCurrencyPrice">

          Total price of all item modifiers. Included in the item price.

          **Read-only.**

          <Expandable title="modifiersTotalPrice properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="billingInfo" type="AddressWithContact">

      Billing information.

      <Expandable title="billingInfo properties">

        <ResponseField name="address" type="Address">

          Address.

          <Expandable title="address properties">

            <ResponseField name="country" type="string">

              Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

            </ResponseField>

            <ResponseField name="subdivision" type="string">

              Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

            </ResponseField>

            <ResponseField name="city" type="string">

              City name.

            </ResponseField>

            <ResponseField name="postalCode" type="string">

              Postal or zip code.

            </ResponseField>

            <ResponseField name="streetAddress" type="StreetAddress">

              Street address.

              <Expandable title="streetAddress properties">

                <ResponseField name="number" type="string">

                  Street number.

                </ResponseField>

                <ResponseField name="name" type="string">

                  Street name.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="addressLine" type="string">

              Main address line (usually street name and number).

            </ResponseField>

            <ResponseField name="addressLine2" type="string">

              Free text providing more detailed address info. Usually contains apt, suite, floor.

            </ResponseField>

            <ResponseField name="geocode" type="AddressLocation">

              Geocode object containing latitude and longitude coordinates.

              <Expandable title="geocode properties">

                <ResponseField name="latitude" type="number">

                  Address latitude.

                </ResponseField>

                <ResponseField name="longitude" type="number">

                  Address longitude.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="countryFullname" type="string">

              Country's full name.

              **Read-only.**

            </ResponseField>

            <ResponseField name="subdivisionFullname" type="string">

              Subdivision full-name.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="contactDetails" type="FullAddressContactDetails">

          Contact details.

          <Expandable title="contactDetails properties">

            <ResponseField name="firstName" type="string">

              First name.

            </ResponseField>

            <ResponseField name="lastName" type="string">

              Last name.

            </ResponseField>

            <ResponseField name="phone" type="string">

              Phone number.

            </ResponseField>

            <ResponseField name="company" type="string">

              Company name.

            </ResponseField>

            <ResponseField name="vatId" type="VatId">

              Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

              <Expandable title="vatId properties">

                <ResponseField name="id" type="string">

                  Customer's tax ID.

                </ResponseField>

                <ResponseField name="type" type="string">

                  Tax type.

                  Supported values:
                  + `CPF`: for individual tax payers
                  + `CNPJ`: for corporations

                  **Supported values:**

                  - `UNSPECIFIED`
                  - `CPF` — CPF - for individual tax payers.
                  - `CNPJ` — CNPJ - for corporations

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="shippingInfo" type="ShippingInfo">

      Shipping information.

      <Expandable title="shippingInfo properties">

        <ResponseField name="shippingDestination" type="AddressWithContact">

          Shipping address and contact details.

          <Expandable title="shippingDestination properties">

            <ResponseField name="address" type="Address">

              Address.

              <Expandable title="address properties">

                <ResponseField name="country" type="string">

                  Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

                </ResponseField>

                <ResponseField name="subdivision" type="string">

                  Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

                </ResponseField>

                <ResponseField name="city" type="string">

                  City name.

                </ResponseField>

                <ResponseField name="postalCode" type="string">

                  Postal or zip code.

                </ResponseField>

                <ResponseField name="streetAddress" type="StreetAddress">

                  Street address.

                  _Nested structure omitted for brevity — see the `StreetAddress` object._

                </ResponseField>

                <ResponseField name="addressLine" type="string">

                  Main address line (usually street name and number).

                </ResponseField>

                <ResponseField name="addressLine2" type="string">

                  Free text providing more detailed address info. Usually contains apt, suite, floor.

                </ResponseField>

                <ResponseField name="geocode" type="AddressLocation">

                  Geocode object containing latitude and longitude coordinates.

                  _Nested structure omitted for brevity — see the `AddressLocation` object._

                </ResponseField>

                <ResponseField name="countryFullname" type="string">

                  Country's full name.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="subdivisionFullname" type="string">

                  Subdivision full-name.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="contactDetails" type="FullAddressContactDetails">

              Contact details.

              <Expandable title="contactDetails properties">

                <ResponseField name="firstName" type="string">

                  First name.

                </ResponseField>

                <ResponseField name="lastName" type="string">

                  Last name.

                </ResponseField>

                <ResponseField name="phone" type="string">

                  Phone number.

                </ResponseField>

                <ResponseField name="company" type="string">

                  Company name.

                </ResponseField>

                <ResponseField name="vatId" type="VatId">

                  Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

                  _Nested structure omitted for brevity — see the `VatId` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedCarrierServiceOption" type="SelectedCarrierServiceOption">

          Selected option out of the options allowed for the `region`.

          <Expandable title="selectedCarrierServiceOption properties">

            <ResponseField name="code" type="string">

              Unique identifier of selected option. For example, "usps_std_overnight".

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
              For example, "Standard" or "First-Class Package International".

              **Read-only.** Constraints: `maxLength 250`.

            </ResponseField>

            <ResponseField name="logistics" type="DeliveryLogistics">

              Delivery logistics.

              **Read-only.**

              <Expandable title="logistics properties">

                <ResponseField name="deliveryTime" type="string">

                  Expected delivery time, in free text. For example, "3-5 business days".

                </ResponseField>

                <ResponseField name="instructions" type="string">

                  Instructions for caller, e.g for pickup: "Please deliver during opening hours, and please don't park in disabled parking spot".

                </ResponseField>

                <ResponseField name="pickupDetails" type="PickupDetails">

                  Pickup details.

                  _Nested structure omitted for brevity — see the `PickupDetails` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="cost" type="SelectedCarrierServiceOptionPrices">

              Shipping costs.

              **Read-only.**

              <Expandable title="cost properties">

                <ResponseField name="totalPriceAfterTax" type="MultiCurrencyPrice">

                  Total shipping price, after discount and after tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="totalPriceBeforeTax" type="MultiCurrencyPrice">

                  Total price of shipping after discounts (when relevant), and before tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxDetails" type="ItemTaxFullDetails">

                  Tax details.

                  _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

                </ResponseField>

                <ResponseField name="totalDiscount" type="MultiCurrencyPrice">

                  Shipping discount before tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="price" type="MultiCurrencyPrice">

                  Shipping price before discount and before tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="requestedShippingOption" type="boolean">

              Were we able to find the requested shipping option, or otherwise we fallback to the default one (the first)

              **Read-only.**

            </ResponseField>

            <ResponseField name="otherCharges" type="SelectedCarrierServiceOptionOtherCharge[]" deprecated>

              Other charges

              **Deprecated.** Other charges Use `additional_fees` instead. Removal: 2025-10-01.

              <Expandable title="otherCharges properties">

                <ResponseField name="type" type="string">

                  Type of additional cost.

                  **Supported values:**

                  - `HANDLING_FEE`
                  - `INSURANCE`

                </ResponseField>

                <ResponseField name="details" type="string">

                  Details of the charge, such as 'Full Coverage Insurance of up to 80% of value of shipment'.

                </ResponseField>

                <ResponseField name="cost" type="SelectedCarrierServiceOptionPrices">

                  Price of added charge.

                  _Nested structure omitted for brevity — see the `SelectedCarrierServiceOptionPrices` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="carrierId" type="string">

              This carrier's unique ID

            </ResponseField>

            <ResponseField name="deliveryAllocations" type="DeliveryAllocation[]">

              Delivery solution allocations to different delivery carriers and delivery regions

              **Developer Preview.** Constraints: `maxItems 300`.

              <Expandable title="deliveryAllocations properties">

                <ResponseField name="deliveryCarrier" type="Carrier">

                  The delivery option's carrier details, could be multiple if the delivery option is a combination of multiple carriers

                  _Nested structure omitted for brevity — see the `Carrier` object._

                </ResponseField>

                <ResponseField name="deliveryRegion" type="Region">

                  The delivery region that are relevant for this delivery solution.

                  _Nested structure omitted for brevity — see the `Region` object._

                </ResponseField>

                <ResponseField name="applicableLineItems" type="ApplicableLineItems">

                  Populated if the delivery solution is a partially supplied by this carrier.

                  _Nested structure omitted for brevity — see the `ApplicableLineItems` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="partial" type="boolean">

              If the delivery solution is a partial and doesn't apply to all items.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="region" type="ShippingRegion">

          Shipping region. Based on the address provided.

          **Read-only.**

          <Expandable title="region properties">

            <ResponseField name="id" type="string">

              Shipping region ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="string">

              Shipping region name.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="carrierServiceOptions" type="CarrierServiceOption[]">

          All carrier options for this shipping rule.

          **Read-only.**

          <Expandable title="carrierServiceOptions properties">

            <ResponseField name="carrierId" type="string">

              Carrier ID.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="shippingOptions" type="ShippingOption[]">

              Shipping options offered by this carrier for this request.

              <Expandable title="shippingOptions properties">

                <ResponseField name="code" type="string">

                  Unique code of provided shipping option like "usps_std_overnight".
                  For legacy calculators this would be the UUID of the option.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="title" type="string">

                  Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
                  For example, "Standard" or "First-Class Package International".

                  Constraints: `maxLength 250`.

                </ResponseField>

                <ResponseField name="logistics" type="DeliveryLogistics">

                  Delivery logistics.

                  _Nested structure omitted for brevity — see the `DeliveryLogistics` object._

                </ResponseField>

                <ResponseField name="cost" type="ShippingPrice">

                  Sipping price information.

                  _Nested structure omitted for brevity — see the `ShippingPrice` object._

                </ResponseField>

                <ResponseField name="deliveryAllocations" type="DeliveryAllocation[]">

                  Delivery solution allocations to different delivery carriers and delivery regions

                  **Developer Preview.** Constraints: `maxItems 300`.

                  _Nested structure omitted for brevity — see the `DeliveryAllocation[]` object._

                </ResponseField>

                <ResponseField name="partial" type="boolean">

                  If the delivery solution is a partial and doesn't apply to all items.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="buyerNote" type="string">

      [Buyer note](https://support.wix.com/en/article/collecting-and-viewing-buyer-notes) left by the customer.

    </ResponseField>

    <ResponseField name="buyerInfo" type="BuyerInfo">

      Buyer information.

      <Expandable title="buyerInfo properties">

        <ResponseField name="contactId" type="string">

          Contact ID. Auto-created if one does not yet exist. For more information, see the Contacts API.

          **Read-only.**

        </ResponseField>

        <ResponseField name="email" type="string">

          Buyer email address.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="visitorId" type="string">

              Visitor ID (if site visitor is **not** a member).

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="memberId" type="string">

              Member ID (if site visitor is a site member).

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="conversionCurrency" type="string">

      The currency [selected by the customer](https://support.wix.com/en/article/multicurrency-an-overview) during the purchase flow, in three-letter [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format.

      This reflects the customer’s preferred display currency and may differ from the site’s default currency.
      When no specific currency is selected by the customer, this matches the `currency` property.

      **Read-only.** Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="priceSummary" type="PriceSummary">

      Calculated price summary for the checkout.

      **Read-only.**

      <Expandable title="priceSummary properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="calculationErrors" type="CalculationErrors">

      Errors when calculating totals.

      **Read-only.**

      <Expandable title="calculationErrors properties">

        <ResponseField name="taxCalculationError" type="Details">

          Tax calculation error.

          <Expandable title="taxCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="couponCalculationError" type="Details">

          Coupon calculation error.

          <Expandable title="couponCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="giftCardCalculationError" type="Details" deprecated>

          DEPRECATED: Use gift_card_calculation_errors instead.

          **Deprecated.** DEPRECATED: Use gift_card_calculation_errors instead. Use `gift_card_calculation_errors` instead. Removal: 2026-12-01.

          <Expandable title="giftCardCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="orderValidationErrors" type="ApplicationError[]">

          Order validation errors.

          <Expandable title="orderValidationErrors properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="membershipError" type="Details">

          Membership payment methods calculation errors
          For example, will indicate that a line item that must be paid with membership payment doesn't have one or selected memberships are invalid

          <Expandable title="membershipError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discountsCalculationError" type="Details">

          Discount Rule calculation error.

          <Expandable title="discountsCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="shippingCalculationError" type="shippingCalculationError">

          shippingCalculationError

          <Expandable title="shippingCalculationError properties">

            <ResponseField name="generalShippingCalculationError" type="Details">

              General shipping calculation error.

              <Expandable title="generalShippingCalculationError properties">

                <ResponseField name="tracing" type="object" deprecated>

                  deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

                  **Deprecated.** Constraints: `format map`.

                  _Nested structure omitted for brevity — see the `object` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="kind" type="kind">

                  kind

                  _Nested structure omitted for brevity — see the `kind` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="carrierErrors" type="CarrierErrors">

              Errors related to the delivery carrier.

              <Expandable title="carrierErrors properties">

                <ResponseField name="errors" type="CarrierError[]">

                  Carrier errors.

                  _Nested structure omitted for brevity — see the `CarrierError[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="selectedShippingOptionError" type="Details">

              Error related to the selected shipping option.

              <Expandable title="selectedShippingOptionError properties">

                <ResponseField name="tracing" type="object" deprecated>

                  deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

                  **Deprecated.** Constraints: `format map`.

                  _Nested structure omitted for brevity — see the `object` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="kind" type="kind">

                  kind

                  _Nested structure omitted for brevity — see the `kind` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="giftCard" type="GiftCard">

      Applied gift card details.

      >**Note:** Gift cards are supported through the Wix UI, though the service plugin is not currently available. Learn more about [Wix Gift Cards](https://support.wix.com/en/article/setting-up-wix-gift-cards-9451439).

      **Read-only.**

      <Expandable title="giftCard properties">

        <ResponseField name="id" type="string" deprecated>

          Gift Card ID.

          **Deprecated.**

        </ResponseField>

        <ResponseField name="obfuscatedCode" type="string">

          Gift card obfuscated code.

        </ResponseField>

        <ResponseField name="amount" type="MultiCurrencyPrice">

          Actual amount to be redeemed from the gift card.

          <Expandable title="amount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="appId" type="string">

          App ID of the gift card provider.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="externalId" type="string">

          External ID in the gift card provider's system.
          Used for integration and tracking across different platforms.

        </ResponseField>

        <ResponseField name="requestedAmount" type="MultiCurrencyPrice">

          Requested amount to redeem from the gift card.

          **Developer Preview.**

          <Expandable title="requestedAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="appliedDiscounts" type="AppliedDiscount[]">

      Applied discounts.

      **Read-only.**

      <Expandable title="appliedDiscounts properties">

        <ResponseField name="discountType" type="string">

          Discount type.

          **Supported values:**

          - `GLOBAL`
          - `SPECIFIC_ITEMS`
          - `SHIPPING`

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          IDs of line items the discount applies to.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customFields" type="CustomField[]">

      Custom fields.

      <Expandable title="customFields properties">

        <ResponseField name="value" type="any">

          Custom field value.

        </ResponseField>

        <ResponseField name="title" type="string">

          Custom field title.

          Constraints: `minLength 1`, `maxLength 500`.

        </ResponseField>

        <ResponseField name="translatedTitle" type="string">

          Translated custom field title.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="weightUnit" type="string">

      Weight measurement unit. Defaults to a site's weight unit.

      **Read-only.**

      **Supported values:**

      - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
      - `KG` — Kilograms.
      - `LB` — Pounds.

    </ResponseField>

    <ResponseField name="taxSummary" type="TaxSummary">

      Tax summary.

      **Read-only.**

      <Expandable title="taxSummary properties">

        <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

          Amount for which tax is calculated, added from line items.

          **Read-only.**

          <Expandable title="taxableAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalTax" type="MultiCurrencyPrice">

          Calculated tax, added from line items.

          **Read-only.**

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="calculationDetails" type="TaxCalculationDetails" deprecated>

          Tax calculator that was active when the order was created.

          **Deprecated.**

          <Expandable title="calculationDetails properties">

            <ResponseField name="rateType" type="string">

              Rate calculation type.

              **Supported values:**

              - `NO_TAX_COLLECTED` — no tax being collected for this request due to location of purchase
              - `MANUAL_RATE` — manual rate used for calculation
              - `AUTO_RATE` — autotax rate used for calculation
              - `FALLBACK_RATE` — fallback rate used for calculation

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="CalculationDetails" type="CalculationDetails">

              CalculationDetails

              <Expandable title="CalculationDetails properties">

                <ResponseField name="manualRateReason" type="string">

                  Reason the manual calculation was used.

                  **Supported values:**

                  - `GLOBAL_SETTING_TO_MANUAL` — user set calculator in Business Manager to be Manual
                  - `REGION_SETTING_TO_MANUAL` — specific region is on manual even though Global setting is Auto-tax

                </ResponseField>

                <ResponseField name="autoTaxFallbackDetails" type="AutoTaxFallbackCalculationDetails">

                  Details of the fallback rate calculation.

                  _Nested structure omitted for brevity — see the `AutoTaxFallbackCalculationDetails` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="currency" type="string">

      The site’s default currency, in three-letter [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format. This represents the base currency configured for the site and remains constant regardless of the customer’s currency selection.

      **Read-only.** Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="channelType" type="string">

      Sales channel that submitted the order.

      **Read-only.**

      **Supported values:**

      - `UNSPECIFIED` — Unspecified sales channel. This value is not supported.
      - `WEB` — A web client.
      - `POS` — [Point of sale solutions](https://support.wix.com/en/wix-mobile-pos-2196395).
      - `EBAY` — [eBay shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-ebay-shop).
      - `AMAZON` — [Amazon shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-amazon-shop).
      - `OTHER_PLATFORM` — Other sales platform.
      - `WIX_APP_STORE` — [Wix Owner app](https://support.wix.com/article/wix-owner-app-an-overview).
      - `WIX_INVOICES` — Wix Invoices app in [your dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Select%20Site&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Finvoices/settings/general-settings)
      - `BACKOFFICE_MERCHANT` — Wix merchant backoffice.
      - `WISH` — Wish sales channel.
      - `CLASS_PASS` — [ClassPass sales channel](https://support.wix.com/en/article/wix-bookings-letting-clients-book-your-services-with-classpass).
      - `GLOBAL_E` — Global-E sales channel.
      - `FACEBOOK` — [Facebook shop](https://support.wix.com/en/article/wix-stores-changes-to-facebook-shops).
      - `ETSY` — [Etsy sales channel](https://support.wix.com/en/article/wix-stores-request-adding-etsy-as-a-sales-channel).
      - `TIKTOK` — [TikTok sales channel](https://support.wix.com/en/article/wix-stores-request-adding-tiktok-as-a-sales-channel).
      - `FAIRE_COM` — [Faire marketplace integration](https://support.wix.com/en/article/wix-stores-creating-a-faire-store-using-the-faire-integration-app).
      - `PAYPAL_AGENTIC_CHECKOUT` — PayPal Agentic Checkout sales channel.
      - `STRIPE_AGENTIC_CHECKOUT` — Stripe Agentic Checkout sales channel.

    </ResponseField>

    <ResponseField name="siteLanguage" type="string">

      Site language in which original values are shown.

      **Read-only.**

    </ResponseField>

    <ResponseField name="buyerLanguage" type="string">

      Language for communication with the buyer. Defaults to the site language.

      For a site that supports multiple languages, this is the language the buyer selected.

      **Read-only.**

    </ResponseField>

    <ResponseField name="completed" type="boolean">

      Whether an order was successfully created from this checkout.

      For an order to be successful, it must be successfully paid for (unless the total is 0).

      **Read-only.**

    </ResponseField>

    <ResponseField name="taxIncludedInPrice" type="boolean">

      Whether tax is included in line item prices.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdBy" type="CreatedBy">

      ID of the checkout's initiator.

      **Read-only.**

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="userId" type="string">

              User ID. When the order was created by a Wix user on behalf of a buyer.
              For example, via POS (point of service).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="memberId" type="string">

              Member ID. When the order was created by a **logged in** site visitor.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="visitorId" type="string">

              Visitor ID. When the order was created by a site visitor that was **not** logged in.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              App ID. When the order was created by an external application or Wix service.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the checkout was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time the checkout was updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="payNow" type="PriceSummary">

      Minimal amount to pay in order to place the order.

      **Read-only.**

      <Expandable title="payNow properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payLater" type="PriceSummary">

      Remaining amount for the order to be fully paid.

      **Read-only.**

      <Expandable title="payLater properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="membershipOptions" type="MembershipOptions">

      Memberships to apply when creating the order.

      <Expandable title="membershipOptions properties">

        <ResponseField name="eligibleMemberships" type="Membership[]">

          Reserved for internal use.

          **Read-only.** Constraints: `maxItems 300`.

          <Expandable title="eligibleMemberships properties">

            <ResponseField name="id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the application providing this payment option.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="MembershipName">

              The name of this membership.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  Membership name.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Translated membership name. Defaults to `original` when not provided.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

              Only populated in the legacy `ListEligibleMemberships` flow, which still requires it. Empty on
              `ListMembershipsByIds`, a by-id lookup that receives no line items to report against.

              Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="credits" type="MembershipPaymentCredits">

              Membership's payment credits (balance information).

              <Expandable title="credits properties">

                <ResponseField name="total" type="integer">

                  Membership's initial value.

                  Constraints: `min 1`.

                </ResponseField>

                <ResponseField name="remaining" type="integer">

                  Membership's remaining value.

                  Constraints: `min 0`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="expirationDate" type="datetime">

              Membership expiration date.

            </ResponseField>

            <ResponseField name="additionalData" type="object">

              Deprecated. Use `offering_description`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="invalidMemberships" type="InvalidMembership[]">

          Reserved for internal use.

          **Read-only.** Constraints: `maxItems 300`.

          <Expandable title="invalidMemberships properties">

            <ResponseField name="membership" type="Membership">

              Membership details.

              <Expandable title="membership properties">

                <ResponseField name="id" type="string">

                  Membership ID.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  ID of the application providing this payment option.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="name" type="MembershipName">

                  The name of this membership.

                  _Nested structure omitted for brevity — see the `MembershipName` object._

                </ResponseField>

                <ResponseField name="lineItemIds" type="string[]">

                  Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

                  Only populated in the legacy `ListEligibleMemberships` flow, which still requires it. Empty on
                  `ListMembershipsByIds`, a by-id lookup that receives no line items to report against.

                  Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

                </ResponseField>

                <ResponseField name="credits" type="MembershipPaymentCredits">

                  Membership's payment credits (balance information).

                  _Nested structure omitted for brevity — see the `MembershipPaymentCredits` object._

                </ResponseField>

                <ResponseField name="expirationDate" type="datetime">

                  Membership expiration date.

                </ResponseField>

                <ResponseField name="additionalData" type="object">

                  Deprecated. Use `offering_description`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="reason" type="string">

              Reason why this membership is invalid and cannot be used.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedMemberships" type="SelectedMemberships">

          Selected membership to apply to this checkout.

          <Expandable title="selectedMemberships properties">

            <ResponseField name="memberships" type="SelectedMembership[]">

              Selected memberships.

              Constraints: `maxItems 300`.

              <Expandable title="memberships properties">

                <ResponseField name="id" type="string">

                  Membership ID.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  ID of the app providing this payment option.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="lineItemIds" type="string[]">

                  IDs of the line items this membership applies to.

                  Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalFees" type="AdditionalFee[]">

      Additional Fees.

      **Read-only.** Constraints: `maxItems 100`.

      <Expandable title="additionalFees properties">

        <ResponseField name="code" type="string">

          Additional fee's unique code (or ID) for future processing.

        </ResponseField>

        <ResponseField name="name" type="string">

          Additional fee's name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ResponseField>

        <ResponseField name="price" type="MultiCurrencyPrice">

          Additional fee's price.

          <Expandable title="price properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="ItemTaxFullDetails">

          Tax details.

          <Expandable title="taxDetails properties">

            <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

              Amount for which tax is calculated.

              <Expandable title="taxableAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxRate" type="string">

              Tax rate %, as a decimal point between 0 and 1.

              Constraints: `>= 0`, `<= 1`.

            </ResponseField>

            <ResponseField name="totalTax" type="MultiCurrencyPrice">

              Calculated tax, based on `taxable_amount` and `tax_rate`.

              <Expandable title="totalTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rateBreakdown" type="TaxRateBreakdown[]" deprecated>

              If breakdown exists, the sum of rates in the breakdown must equal `tax_rate`. Deprecated - use 'tax_breakdown' instead.

              **Read-only.** **Deprecated.**

              <Expandable title="rateBreakdown properties">

                <ResponseField name="name" type="string">

                  Name of tax against which the calculation was performed.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="rate" type="string">

                  Rate at which this tax detail was calculated.

                  Constraints: `>= 0`, `<= 1`.

                </ResponseField>

                <ResponseField name="tax" type="MultiCurrencyPrice">

                  Amount of tax for this tax detail.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxBreakdown" type="TaxBreakdown[]">

              tax information for a line item.

              **Read-only.** Constraints: `minItems 0`, `maxItems 1000`.

              <Expandable title="taxBreakdown properties">

                <ResponseField name="jurisdiction" type="string">

                  The name of the jurisdiction to which this tax detail applies. For example, "New York" or "Quebec".

                </ResponseField>

                <ResponseField name="nonTaxableAmount" type="MultiCurrencyPrice">

                  The amount of this line item price that was considered nontaxable. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="rate" type="string">

                  The rate at which this tax detail was calculated, e.g 0.1000 signifies 10% tax and 2.0000 signifies 200% tax. (Decimal value)

                </ResponseField>

                <ResponseField name="taxAmount" type="MultiCurrencyPrice">

                  The amount of tax estimated for this line item. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

                  The taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxType" type="string">

                  The type of tax that was calculated. Depends on the jurisdiction's tax laws. For example, "Sales Tax", "Income Tax", "Value Added Tax", etc.

                </ResponseField>

                <ResponseField name="taxName" type="string">

                  The name of the tax against which this tax amount was calculated. For example, "NY State Sales Tax", "Quebec GST", etc.
                  This name should be explicit enough to allow the merchant to understand what tax was calculated.

                </ResponseField>

                <ResponseField name="jurisdictionType" type="string">

                  The type of the jurisdiction in which this tax detail applies.

                  **Supported values:**

                  - `UNDEFINED`
                  - `COUNTRY`
                  - `STATE`
                  - `COUNTY`
                  - `CITY`
                  - `SPECIAL`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="providerAppId" type="string">

          Provider's app id.

        </ResponseField>

        <ResponseField name="priceBeforeTax" type="MultiCurrencyPrice">

          Additional fee's price before tax.

          <Expandable title="priceBeforeTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceAfterTax" type="MultiCurrencyPrice">

          Additional fee's price after tax.

          <Expandable title="priceAfterTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          Optional - Line items associated with this additional fee.
          If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="source" type="string">

          the source the additional fee was added from

          **Supported values:**

          - `UNKNOWN_ADDITIONAL_FEE_SOURCE`
          - `SERVICE_PLUGIN` — The additional fee was added by an additional fee service plugin
          - `ITEM` — The additional fee was added on the item either via the catalog or on custom line item
          - `MANUAL` — The additional fee was added manually on the request
          - `SHIPPING` — The additional fee was added by the shipping provider
          - `PLATFORM` — The additional fee was added by a Wix vertical and represents a Wix platform fee

        </ResponseField>

        <ResponseField name="translatedName" type="string">

          The translated name of the additional fee. The translation language is determined by the `languages` field in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          Address type tax was calculated against for this fee.

          **Developer Preview.**

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              <Expandable title="TaxableAddressData properties">

                <ResponseField name="addressType" type="string">

                  taxable address type. if this field is selected, the address is automatically resolved, and the tax is calculated accordingly.

                  **Supported values:**

                  - `BUSINESS`
                  - `BILLING`
                  - `SHIPPING`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="cartId" type="string">

      Cart ID that this checkout was created from. Empty if this checkout wasn't created from a cart.

    </ResponseField>

    <ResponseField name="violations" type="Violation[]">

      List of validation violations raised by the [Validations service plugin](https://dev.wix.com/docs/rest/business-solutions/e-commerce/service-plugins/validations-integration-service-plugin/introduction).

      **Read-only.**

      <Expandable title="violations properties">

        <ResponseField name="severity" type="string">

          Severity of the violation. The violations are shown on the cart and checkout pages. A warning is displayed as yellow, and allows a site visitor to proceed with caution. An error is displayed as red, and doesn't allow a site visitor to proceed with the eCommerce flow.

          **Supported values:**

          - `WARNING` — The user is allowed to move forward in the flow.
          - `ERROR` — The user is blocked from moving forward in the flow.
          For example, if callerContext is CART - moving to checkout is blocked. if callerContext is CHECKOUT, placing an order is blocked.

        </ResponseField>

        <ResponseField name="target" type="Target">

          Target location on a checkout or cart page where the violation will be displayed.

          <Expandable title="One of the following">

            <ResponseField name="targetType" type="targetType">

              targetType

              <Expandable title="targetType properties">

                <ResponseField name="other" type="Other">

                  General (other) violation.

                  _Nested structure omitted for brevity — see the `Other` object._

                </ResponseField>

                <ResponseField name="lineItem" type="LineItem">

                  Specific line item violation.

                  _Nested structure omitted for brevity — see the `LineItem` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="description" type="string">

          Violation description. Can include rich text. Only HTTP or HTTPS links in the following format are allowed: `<a href="https://www.wix.com">Click me</a>`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Custom field data for the checkout object.

      [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="purchaseFlowId" type="string">

      Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

      **Read-only.**

    </ResponseField>

    <ResponseField name="customSettings" type="CustomSettings">

      Additional settings for customization of the checkout process.

      > **Notes:**
      > * Custom settings can only be set when creating a checkout.
      > * To access and manage custom checkout page content, your app must have the permission scope named "Manage eCommerce - Admin Permissions". Learn more about [permission scopes](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

      Constraints: `immutable`.

      <Expandable title="customSettings properties">

        <ResponseField name="lockGiftCard" type="boolean">

          Whether to restrict the option to add or remove a gift card on the checkout page.

          Default: `false`

        </ResponseField>

        <ResponseField name="lockCouponCode" type="boolean">

          Whether to restrict the option to add or remove a coupon code on the checkout page.

          Default: `false`

        </ResponseField>

        <ResponseField name="disabledPolicyAgreementCheckbox" type="boolean">

          Whether to disable policy agreement checkout in the checkout page

          Default: `false`

        </ResponseField>

        <ResponseField name="disabledManualPayment" type="boolean">

          Whether to disable manual payment option for this checkout.

          Default: `false`

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customContentReference" type="CustomContentReference">

      Reference IDs for the app and component providing custom checkout page content.

      To access and manage custom checkout page content, your app must have the permission scope named "Manage eCommerce - Admin Permissions".
      Learn more about [permission scopes](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

      **Developer Preview.**

      <Expandable title="customContentReference properties">

        <ResponseField name="appId" type="string">

          ID of the app providing the content.

          You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="componentId" type="string">

          ID of the component within the app it belongs to.

          You can get your component's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="externalReference" type="ExternalReference">

      References to an external app and resource associated with this checkout.
      Used for integration and tracking across different platforms.

      **Developer Preview.** Constraints: `immutable`.

      <Expandable title="externalReference properties">

        <ResponseField name="appId" type="string">

          ID of the app associated with the purchase flow.
          For example, the Wix Pay Links app ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="resourceId" type="string">

          Reference to an external resource ID. Used to link the purchase flow to a specific entity in an external system.
          For example, a Wix Pay Link ID.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payAfterFreeTrial" type="PriceSummary">

      Payment for subscriptions after free trial period.

      **Read-only.** **Developer Preview.**

      <Expandable title="payAfterFreeTrial properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="businessLocationId" type="string">

      The business location ID associated with the checkout.

      To learn more, see the Locations API.

    </ResponseField>

    <ResponseField name="paymentCurrency" type="string">

      The currency used for payment, in three-letter [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format.

      This is determined by the [customer's selected currency](https://support.wix.com/en/article/multicurrency-an-overview) and the site’s supported payment currencies.
      If the customer’s selected currency is supported for payment, this matches the `conversionCurrency` property. If not supported, this falls back to the `currency` property.

      **Read-only.** **Developer Preview.** Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="taxExemptGroupId" type="string">

      Tax exempt group ID to use for tax exemptions.

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "checkout": {
          "id": "ddbeb7fc-fab7-4ab8-9ce2-f87a8f5f4327",
          "lineItems": [
            {
              "id": "00000000-0000-0000-0000-000000000001",
              "quantity": 1,
              "catalogReference": {
                "catalogItemId": "1a2d7e83-4bef-31d5-09e1-3326ee271c09",
                "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                "options": {
                  "options": {
                    "Size": "Medium",
                    "Color": "Grey"
                  },
                  "variantId": "132b84e8-aab8-47a1-a1f6-2c47557b64a4"
                }
              },
              "productName": {
                "original": "Watch",
                "translated": "Watch"
              },
              "url": {
                "relativePath": "/product-page/watch",
                "url": "https://example.wixsite.com/ep-tester"
              },
              "price": {
                "amount": "30.00",
                "convertedAmount": "30.00",
                "formattedAmount": "$30.00",
                "formattedConvertedAmount": "$30.00"
              },
              "lineItemPrice": {
                "amount": "30.00",
                "convertedAmount": "30.00",
                "formattedAmount": "$30.00",
                "formattedConvertedAmount": "$30.00"
              },
              "fullPrice": {
                "amount": "30.00",
                "convertedAmount": "30.00",
                "formattedAmount": "$30.00",
                "formattedConvertedAmount": "$30.00"
              },
              "priceBeforeDiscounts": {
                "amount": "30.00",
                "convertedAmount": "30.00",
                "formattedAmount": "$30.00",
                "formattedConvertedAmount": "$30.00"
              },
              "totalPriceAfterTax": {
                "amount": "30.00",
                "convertedAmount": "30.00",
                "formattedAmount": "$30.00",
                "formattedConvertedAmount": "$30.00"
              },
              "totalPriceBeforeTax": {
                "amount": "30.00",
                "convertedAmount": "30.00",
                "formattedAmount": "$30.00",
                "formattedConvertedAmount": "$30.00"
              },
              "taxDetails": {
                "taxableAmount": {
                  "amount": "30.00",
                  "convertedAmount": "30.00",
                  "formattedAmount": "$30.00",
                  "formattedConvertedAmount": "$30.00"
                },
                "taxRate": "0.0",
                "totalTax": {
                  "amount": "0.0",
                  "convertedAmount": "0.0",
                  "formattedAmount": "$0.00",
                  "formattedConvertedAmount": "$0.00"
                },
                "rateBreakdown": []
              },
              "discount": {
                "amount": "0",
                "convertedAmount": "0",
                "formattedAmount": "$0.00",
                "formattedConvertedAmount": "$0.00"
              },
              "descriptionLines": [
                {
                  "name": {
                    "original": "Size",
                    "translated": "Size"
                  },
                  "plainText": {
                    "original": "Medium",
                    "translated": "Medium"
                  }
                },
                {
                  "name": {
                    "original": "Color",
                    "translated": "Color"
                  },
                  "colorInfo": {
                    "original": "Grey",
                    "translated": "Grey",
                    "code": "rgb(128, 128, 128)"
                  }
                }
              ],
              "media": {
                "id": "3c76e2_8891bbe3372a428aac976ac59aa0ac74~mv2.jpg",
                "url": "https://static.wixstatic.com/media/3c76e2_8891bbe3372a428aac976ac59aa0ac74~mv2.jpg",
                "height": 1000,
                "width": 1000
              },
              "availability": {
                "status": "AVAILABLE",
                "quantityAvailable": 10
              },
              "physicalProperties": {
                "sku": "217537123517253",
                "shippable": true
              },
              "itemType": {
                "preset": "PHYSICAL"
              },
              "paymentOption": "FULL_PAYMENT_ONLINE",
              "rootCatalogItemId": "1a2d7e83-4bef-31d5-09e1-3326ee271c09"
            },
            {
              "id": "00000000-0000-0000-0000-000000000002",
              "quantity": 2,
              "catalogReference": {
                "catalogItemId": "c8539b66-7a44-fe18-affc-afec4be8562a",
                "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                "options": {
                  "options": {},
                  "variantId": "00000000-0000-0000-0000-000000000000"
                }
              },
              "productName": {
                "original": "Shirt",
                "translated": "Shirt"
              },
              "url": {
                "relativePath": "/product-page/shirt",
                "url": "https://example.wixsite.com/ep-tester"
              },
              "price": {
                "amount": "10.00",
                "convertedAmount": "10.00",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "lineItemPrice": {
                "amount": "20.00",
                "convertedAmount": "20.00",
                "formattedAmount": "$20.00",
                "formattedConvertedAmount": "$20.00"
              },
              "fullPrice": {
                "amount": "10.00",
                "convertedAmount": "10.00",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "priceBeforeDiscounts": {
                "amount": "10.00",
                "convertedAmount": "10.00",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "totalPriceAfterTax": {
                "amount": "10.0",
                "convertedAmount": "10.0",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "totalPriceBeforeTax": {
                "amount": "10.0",
                "convertedAmount": "10.0",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "taxDetails": {
                "taxableAmount": {
                  "amount": "10.0",
                  "convertedAmount": "10.0",
                  "formattedAmount": "$10.00",
                  "formattedConvertedAmount": "$10.00"
                },
                "taxRate": "0.0",
                "totalTax": {
                  "amount": "0.0",
                  "convertedAmount": "0.0",
                  "formattedAmount": "$0.00",
                  "formattedConvertedAmount": "$0.00"
                },
                "rateBreakdown": []
              },
              "discount": {
                "amount": "10.0",
                "convertedAmount": "10.0",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "descriptionLines": [],
              "media": {
                "id": "3c76e2_c5331f937348492a97df87b0a3b34ea4~mv2.jpg",
                "url": "https://static.wixstatic.com/media/3c76e2_c5331f937348492a97df87b0a3b34ea4~mv2.jpg",
                "height": 1000,
                "width": 1000
              },
              "availability": {
                "status": "AVAILABLE"
              },
              "physicalProperties": {
                "sku": "364115376135191",
                "shippable": true
              },
              "itemType": {
                "preset": "PHYSICAL"
              },
              "paymentOption": "FULL_PAYMENT_ONLINE",
              "rootCatalogItemId": "c8539b66-7a44-fe18-affc-afec4be8562a"
            }
          ],
          "billingInfo": {
            "address": {
              "country": "US",
              "subdivision": "US-NY",
              "city": "New York",
              "postalCode": "10173",
              "addressLine": "525 5th Avenue",
              "countryFullname": "United States",
              "subdivisionFullname": "New York"
            },
            "contactDetails": {
              "firstName": "Jane",
              "lastName": "Doe",
              "phone": "0555555555"
            }
          },
          "shippingInfo": {
            "shippingDestination": {
              "address": {
                "country": "US",
                "subdivision": "US-NY",
                "city": "New York",
                "postalCode": "10173",
                "addressLine": "525 5th Avenue",
                "countryFullname": "United States",
                "subdivisionFullname": "New York"
              },
              "contactDetails": {
                "firstName": "Jane",
                "lastName": "Doe",
                "phone": "0555555555"
              }
            },
            "selectedCarrierServiceOption": {
              "code": "ed5bbce2-9533-dff4-7db0-13702fd139c5",
              "title": "Standard US Shipping",
              "logistics": {
                "deliveryTime": ""
              },
              "cost": {
                "totalPriceAfterTax": {
                  "amount": "10.0",
                  "convertedAmount": "10.0",
                  "formattedAmount": "$10.00",
                  "formattedConvertedAmount": "$10.00"
                },
                "totalPriceBeforeTax": {
                  "amount": "10",
                  "convertedAmount": "10",
                  "formattedAmount": "$10.00",
                  "formattedConvertedAmount": "$10.00"
                },
                "taxDetails": {
                  "taxRate": "0.0",
                  "totalTax": {
                    "amount": "0.0",
                    "convertedAmount": "0.0",
                    "formattedAmount": "$0.00",
                    "formattedConvertedAmount": "$0.00"
                  },
                  "rateBreakdown": []
                },
                "price": {
                  "amount": "10",
                  "convertedAmount": "10",
                  "formattedAmount": "$10.00",
                  "formattedConvertedAmount": "$10.00"
                }
              },
              "requestedShippingOption": true,
              "otherCharges": [],
              "carrierId": "c8a08776-c095-4dec-8553-8f9698d86adc"
            },
            "region": {
              "id": "009fbe5d-89d3-7825-cbbf-1aab4d908b73",
              "name": "USA shipping"
            },
            "carrierServiceOptions": [
              {
                "carrierId": "c8a08776-c095-4dec-8553-8f9698d86adc",
                "shippingOptions": [
                  {
                    "code": "ed5bbce2-9533-dff4-7db0-13702fd139c5",
                    "title": "Standard US Shipping",
                    "logistics": {
                      "deliveryTime": ""
                    },
                    "cost": {
                      "price": {
                        "amount": "10",
                        "convertedAmount": "10",
                        "formattedAmount": "$10.00",
                        "formattedConvertedAmount": "$10.00"
                      },
                      "otherCharges": []
                    }
                  }
                ]
              }
            ]
          },
          "buyerInfo": {
            "contactId": "24de6f0b-60ce-4faa-9138-2e39f1415615",
            "email": "janedoe@gmail.com",
            "visitorId": "4c7ce95c-9fb3-417d-9f02-b41e82b841f7"
          },
          "conversionCurrency": "USD",
          "priceSummary": {
            "subtotal": {
              "amount": "50.00",
              "convertedAmount": "50.00",
              "formattedAmount": "$50.00",
              "formattedConvertedAmount": "$50.00"
            },
            "shipping": {
              "amount": "10.0",
              "convertedAmount": "10.0",
              "formattedAmount": "$10.00",
              "formattedConvertedAmount": "$10.00"
            },
            "tax": {
              "amount": "0.0",
              "convertedAmount": "0.0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            },
            "discount": {
              "amount": "10.0",
              "convertedAmount": "10.0",
              "formattedAmount": "$10.00",
              "formattedConvertedAmount": "$10.00"
            },
            "total": {
              "amount": "50.00",
              "convertedAmount": "50.00",
              "formattedAmount": "$50.00",
              "formattedConvertedAmount": "$50.00"
            },
            "additionalFees": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            }
          },
          "calculationErrors": {
            "orderValidationErrors": []
          },
          "appliedDiscounts": [
            {
              "discountType": "GLOBAL",
              "lineItemIds": [],
              "coupon": {
                "id": "fbb94b06-7447-4161-9c48-59bfcdc39e77",
                "code": "SUMMERSALE10",
                "amount": {
                  "amount": "10.0",
                  "convertedAmount": "10.0",
                  "formattedAmount": "$10.00",
                  "formattedConvertedAmount": "$10.00"
                },
                "name": "SUMMERSALE10",
                "couponType": "MoneyOff"
              }
            }
          ],
          "customFields": [],
          "weightUnit": "KG",
          "currency": "USD",
          "channelType": "WEB",
          "siteLanguage": "en",
          "buyerLanguage": "en",
          "completed": true,
          "taxIncludedInPrice": false,
          "createdBy": {
            "visitorId": "4c7ce95c-9fb3-417d-9f02-b41e82b841f7"
          },
          "createdDate": "2022-12-12T13:36:40.690Z",
          "updatedDate": "2022-12-12T13:43:15.132Z",
          "payNow": {
            "subtotal": {
              "amount": "50.00",
              "convertedAmount": "50.00",
              "formattedAmount": "$50.00",
              "formattedConvertedAmount": "$50.00"
            },
            "shipping": {
              "amount": "10.0",
              "convertedAmount": "10.0",
              "formattedAmount": "$10.00",
              "formattedConvertedAmount": "$10.00"
            },
            "tax": {
              "amount": "0.0",
              "convertedAmount": "0.0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            },
            "discount": {
              "amount": "10.0",
              "convertedAmount": "10.0",
              "formattedAmount": "$10.00",
              "formattedConvertedAmount": "$10.00"
            },
            "total": {
              "amount": "50.00",
              "convertedAmount": "50.00",
              "formattedAmount": "$50.00",
              "formattedConvertedAmount": "$50.00"
            },
            "additionalFees": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            }
          },
          "payLater": {
            "subtotal": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            },
            "shipping": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            },
            "tax": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            },
            "discount": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            },
            "total": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            },
            "additionalFees": {
              "amount": "0",
              "convertedAmount": "0",
              "formattedAmount": "$0.00",
              "formattedConvertedAmount": "$0.00"
            }
          },
          "additionalFees": [],
          "cartId": "5f000867-100a-445c-968d-88368d263da5",
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Checkout ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="lineItems" type="LineItem[]">

      Line items.

      Max: 300 items

      **Read-only.** Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="lineItems properties">

        <ResponseField name="_id" type="string">

          Line item ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="quantity" type="integer">

          Item quantity.

          Min: `"1"`
          Max: `"100000"`

          Constraints: `min 1`, `max 100000`.

        </ResponseField>

        <ResponseField name="catalogReference" type="CatalogReference">

          Catalog and item reference. Includes IDs for the item and the catalog it came from, as well as further optional info. Optional for custom line items, which don't trigger the Catalog service plugin.

          <Expandable title="catalogReference properties">

            <ResponseField name="catalogItemId" type="string">

              ID of the item within the catalog it belongs to.

              Constraints: `minLength 1`, `maxLength 36`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the app providing the catalog.

              You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

              For items from Wix catalogs, the following values always apply:
              + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
              + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
              + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

              Constraints: `minLength 1`.

            </ResponseField>

            <ResponseField name="options" type="object">

              Additional item details in `key:value` pairs.

              Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

              For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
              or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="productName" type="ProductName">

          Item name.
          + Stores - `product.name`
          + Bookings - `service.info.name`
          + Events - `ticket.name`

          **Read-only.**

          <Expandable title="productName properties">

            <ResponseField name="original" type="string">

              __Required.__ Item name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

              Min: 1 character.
              Max: 200 characters.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="translated" type="string">

              Item name translated into the buyer's language.

              Min: 1 character.
              Max: 400 characters.
              Default: Same as `original`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="url" type="string">

          URL to the item's page on the site.

          **Read-only.**

        </ResponseField>

        <ResponseField name="price" type="MultiCurrencyPrice">

          Item price **after** catalog-defined discount and line item discounts.

          **Read-only.**

          <Expandable title="price properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemPrice" type="MultiCurrencyPrice">

          Total line item price **after** catalog-defined discount and line item discounts.

          **Read-only.**

          <Expandable title="lineItemPrice properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="fullPrice" type="MultiCurrencyPrice">

          Item price **before** catalog-defined discount. Defaults to `price` when not provided.

          **Read-only.**

          <Expandable title="fullPrice properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceBeforeDiscounts" type="MultiCurrencyPrice">

          Item price **before** line item discounts and **after** catalog-defined discount. Defaults to `price` when not provided.

          **Read-only.**

          <Expandable title="priceBeforeDiscounts properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalPriceAfterTax" type="MultiCurrencyPrice">

          Total price after all discounts and tax.

          **Read-only.**

          <Expandable title="totalPriceAfterTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalPriceBeforeTax" type="MultiCurrencyPrice">

          Total price after discounts, and before tax.

          **Read-only.**

          <Expandable title="totalPriceBeforeTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="ItemTaxFullDetails">

          Tax details for this line item.

          **Read-only.**

          <Expandable title="taxDetails properties">

            <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

              Amount for which tax is calculated.

              <Expandable title="taxableAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxRate" type="string">

              Tax rate %, as a decimal point between 0 and 1.

              Constraints: `>= 0`, `<= 1`.

            </ResponseField>

            <ResponseField name="totalTax" type="MultiCurrencyPrice">

              Calculated tax, based on `taxable_amount` and `tax_rate`.

              <Expandable title="totalTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rateBreakdown" type="TaxRateBreakdown[]" deprecated>

              If breakdown exists, the sum of rates in the breakdown must equal `tax_rate`. Deprecated - use 'tax_breakdown' instead.

              **Read-only.** **Deprecated.**

              <Expandable title="rateBreakdown properties">

                <ResponseField name="name" type="string">

                  Name of tax against which the calculation was performed.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="rate" type="string">

                  Rate at which this tax detail was calculated.

                  Constraints: `>= 0`, `<= 1`.

                </ResponseField>

                <ResponseField name="tax" type="MultiCurrencyPrice">

                  Amount of tax for this tax detail.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxBreakdown" type="TaxBreakdown[]">

              tax information for a line item.

              **Read-only.** Constraints: `minItems 0`, `maxItems 1000`.

              <Expandable title="taxBreakdown properties">

                <ResponseField name="jurisdiction" type="string">

                  The name of the jurisdiction to which this tax detail applies. For example, "New York" or "Quebec".

                </ResponseField>

                <ResponseField name="nonTaxableAmount" type="MultiCurrencyPrice">

                  The amount of this line item price that was considered nontaxable. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="rate" type="string">

                  The rate at which this tax detail was calculated, e.g 0.1000 signifies 10% tax and 2.0000 signifies 200% tax. (Decimal value)

                </ResponseField>

                <ResponseField name="taxAmount" type="MultiCurrencyPrice">

                  The amount of tax estimated for this line item. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

                  The taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxType" type="string">

                  The type of tax that was calculated. Depends on the jurisdiction's tax laws. For example, "Sales Tax", "Income Tax", "Value Added Tax", etc.

                </ResponseField>

                <ResponseField name="taxName" type="string">

                  The name of the tax against which this tax amount was calculated. For example, "NY State Sales Tax", "Quebec GST", etc.
                  This name should be explicit enough to allow the merchant to understand what tax was calculated.

                </ResponseField>

                <ResponseField name="jurisdictionType" type="string">

                  The type of the jurisdiction in which this tax detail applies.

                  **Supported values:**

                  - `UNDEFINED`
                  - `COUNTRY`
                  - `STATE`
                  - `COUNTY`
                  - `CITY`
                  - `SPECIAL`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Discount for this line item's entire quantity.

          **Read-only.**

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="descriptionLines" type="DescriptionLine[]">

          Line item description lines. Used for display purposes for the cart, checkout and order.

          **Read-only.**

          <Expandable title="descriptionLines properties">

            <ResponseField name="name" type="DescriptionLineName">

              Description line name.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  Description line name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Description line name translated into the buyer's language.

                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="media" type="string">

          Line item image details.

          **Read-only.**

        </ResponseField>

        <ResponseField name="availability" type="ItemAvailabilityInfo">

          Item availability details.

          **Read-only.**

          <Expandable title="availability properties">

            <ResponseField name="status" type="string">

              Item availability status.

              **Supported values:**

              - `AVAILABLE`
              - `NOT_FOUND` — Item does not exist.
              - `NOT_AVAILABLE` — Item is not in stock.
              - `PARTIALLY_AVAILABLE` — Available quantity is less than requested.

            </ResponseField>

            <ResponseField name="quantityAvailable" type="integer">

              Quantity available.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="physicalProperties" type="PhysicalProperties">

          Physical properties of the item. When relevant, contains information such as SKU, item weight, and shippability.

          **Read-only.**

          <Expandable title="physicalProperties properties">

            <ResponseField name="weight" type="number">

              Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request.

            </ResponseField>

            <ResponseField name="sku" type="string">

              Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).

            </ResponseField>

            <ResponseField name="shippable" type="boolean">

              Whether this line item is shippable.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="itemType" type="ItemType">

          Item type. Either a preset type or custom.

          **Read-only.**

          <Expandable title="One of the following">

            <ResponseField name="itemTypeData" type="itemTypeData">

              itemTypeData

              <Expandable title="itemTypeData properties">

                <ResponseField name="preset" type="string">

                  Preset item type.

                  **Supported values:**

                  - `UNRECOGNISED`
                  - `PHYSICAL`
                  - `DIGITAL`
                  - `GIFT_CARD`
                  - `SERVICE`

                </ResponseField>

                <ResponseField name="custom" type="string">

                  Custom item type. When none of the preset types are suitable, specifies the custom type.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="subscriptionOptionInfo" type="SubscriptionOptionInfo">

          Subscription option information.

          **Read-only.**

          <Expandable title="subscriptionOptionInfo properties">

            <ResponseField name="subscriptionSettings" type="SubscriptionSettings">

              Subscription option settings.

              <Expandable title="subscriptionSettings properties">

                <ResponseField name="frequency" type="string">

                  Frequency of recurring payment.

                  **Supported values:**

                  - `UNDEFINED`
                  - `DAY`
                  - `WEEK`
                  - `MONTH`
                  - `YEAR`

                </ResponseField>

                <ResponseField name="interval" type="integer">

                  Interval of recurring payment.

                  Default: `1`.
                  If SubscriptionFrequency is Day the minimum interval is 7

                </ResponseField>

                <ResponseField name="autoRenewal" type="boolean">

                  Whether subscription is renewed automatically at the end of each period.

                </ResponseField>

                <ResponseField name="billingCycles" type="integer">

                  Number of billing cycles before subscription ends. Ignored if `autoRenewal` is `true`.

                </ResponseField>

                <ResponseField name="enableCustomerCancellation" type="boolean">

                  Whether to allow the customer to cancel the subscription..

                  **Developer Preview.**

                </ResponseField>

                <ResponseField name="freeTrialPeriod" type="FreeTrialPeriod">

                  Period until first cycle starts. If applied payNow will be 0
                  If None => no free trial

                  **Developer Preview.**

                  _Nested structure omitted for brevity — see the `FreeTrialPeriod` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="title" type="Title">

              Subscription option title.

              <Expandable title="title properties">

                <ResponseField name="original" type="string">

                  Subscription option name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Constraints: `minLength 1`, `maxLength 150`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Subscription option name translated into the buyer's language.

                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="description" type="Description">

              Subscription option description.

              <Expandable title="description properties">

                <ResponseField name="original" type="string">

                  Subscription option description.

                  Constraints: `maxLength 500`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Translated subscription option description.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paymentOption" type="string">

          Type of selected payment option for current item.

          Default: `FULL_PAYMENT_ONLINE`

          **Read-only.**

          **Supported values:**

          - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
          - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
          - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
          - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
          - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
          - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

        </ResponseField>

        <ResponseField name="serviceProperties" type="ServiceProperties">

          Service properties. When relevant, this contains information such as date and number of participants.

          **Read-only.**

          <Expandable title="serviceProperties properties">

            <ResponseField name="scheduledDate" type="datetime">

              Date and time the service is to be provided, in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.
              For example, the start time of a class.

            </ResponseField>

            <ResponseField name="numberOfParticipants" type="integer">

              The number of people participating in the service. For example, the number of people attending a class or the number of people per hotel room.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="rootCatalogItemId" type="string">

          In cases where `catalogReference.catalogItemId` is NOT the actual catalog item ID, this field will return the true item's ID.
          + For example, for Wix Bookings, `catalogReference.catalogItemId` is the booking ID. Therefore this value is set to the service ID.
          + In most cases, this field has the same value as `catalogReference.catalogItemId`.
          + Used in membership validation.

          **Read-only.**

        </ResponseField>

        <ResponseField name="priceDescription" type="PriceDescription">

          Additional description for the price. For example, when price is 0 but additional details about the actual price are needed - "Starts at $67".

          **Read-only.**

          <Expandable title="priceDescription properties">

            <ResponseField name="original" type="string">

              __Required.__ Price description in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

            <ResponseField name="translated" type="string">

              Price description translated into the buyer's language.

              Default: Same as `original`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="depositAmount" type="MultiCurrencyPrice">

          Partial payment to be paid upfront during the checkout. Eligible for catalog items with `lineItem.paymentOption` type `DEPOSIT_ONLINE` only.

          **Read-only.**

          <Expandable title="depositAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="deliveryProfileId" type="string">

          Delivery profile ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="customLineItem" type="boolean">

          Whether the line item is a custom line item. Custom line items don't trigger the Catalog service plugin.

          **Read-only.** **Developer Preview.**

        </ResponseField>

        <ResponseField name="consentRequiredPaymentPolicy" type="string">

          Item payment policy that requires customer consent to complete purchase. The payment policy will be displayed on the checkout page.

          **Read-only.**

        </ResponseField>

        <ResponseField name="catalogOverrideFields" type="CatalogOverrideFields">

          Overriding values for catalog item properties.

          To override catalog fields, your app must have the permission scope named "Manage eCommerce - Admin Permissions".
          Learn more about [permission scopes](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

          **Developer Preview.**

          <Expandable title="catalogOverrideFields properties">

            <ResponseField name="productName" type="ProductName">

              Item name.

              <Expandable title="productName properties">

                <ResponseField name="original" type="string">

                  __Required.__ Item name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Item name translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="price" type="string">

              Item price **after** discounts.

            </ResponseField>

            <ResponseField name="fullPrice" type="string">

              Item price **before** discounts.

            </ResponseField>

            <ResponseField name="descriptionLines" type="DescriptionLine[]">

              Item description lines. Used when displaying the line item to customers.

              Constraints: `minItems 0`, `maxItems 20`.

              <Expandable title="descriptionLines properties">

                <ResponseField name="name" type="DescriptionLineName">

                  Description line name.

                  _Nested structure omitted for brevity — see the `DescriptionLineName` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="physicalProperties" type="PhysicalProperties">

              Physical properties of the item.

              <Expandable title="physicalProperties properties">

                <ResponseField name="weight" type="number">

                  Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request.

                </ResponseField>

                <ResponseField name="sku" type="string">

                  Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).

                </ResponseField>

                <ResponseField name="shippable" type="boolean">

                  Whether this line item is shippable.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="image" type="string">

              Item image.

            </ResponseField>

            <ResponseField name="paymentOption" type="PaymentOption">

              Payment method selected for the item.

              **Developer Preview.**

              <Expandable title="paymentOption properties">

                <ResponseField name="value" type="string">

                  Type of selected payment option for current item.

                  Default: `FULL_PAYMENT_ONLINE`

                  **Supported values:**

                  - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
                  - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
                  - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
                  - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
                  - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
                  - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="depositAmount" type="string">

              Only eligible for catalog items with `lineItem.paymentOption.value` type of `DEPOSIT_ONLINE`.

            </ResponseField>

            <ResponseField name="savePaymentMethod" type="boolean">

              Whether to save the payment method on the order.

              Default: `false`

            </ResponseField>

            <ResponseField name="inventoryAppId" type="string">

              ID of the app managing the inventory.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceUndetermined" type="boolean">

          Whether the price is not yet defined, and will be updated after the order is created.

          **Read-only.**

        </ResponseField>

        <ResponseField name="fixedQuantity" type="boolean">

          Whether the line item quantity is fixed and cannot be changed.

          **Read-only.**

        </ResponseField>

        <ResponseField name="savePaymentMethod" type="boolean">

          Whether to save the payment method on the order.

          Default: `false`

          **Read-only.**

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          Address to use for tax calculation purposes.

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              <Expandable title="TaxableAddressData properties">

                <ResponseField name="addressType" type="string">

                  taxable address type. if this field is selected, the address is automatically resolved, and the tax is calculated accordingly.

                  **Supported values:**

                  - `BUSINESS`
                  - `BILLING`
                  - `SHIPPING`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="extendedFields" type="ExtendedFields">

          Custom extended fields for the line item object.

          [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured using the 'Checkout & Orders - Line Items Schema Plugin' in the app dashboard before they can be accessed with API calls.

          **Developer Preview.**

          <Expandable title="extendedFields properties">

            <ResponseField name="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              <Expandable title="namespaces properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="object" />

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="policies" type="Policy[]">

          Policies to be displayed to the customer on the checkout page.

          **Read-only.** Constraints: `maxItems 5`.

          <Expandable title="policies properties">

            <ResponseField name="title" type="string">

              Policy title - should be translated

            </ResponseField>

            <ResponseField name="content" type="string">

              Policy content - should be translated

              Constraints: `minLength 1`, `maxLength 3000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="membersOnly" type="boolean">

          Whether the item can only be purchased by site members.

          Default: `false`

          **Read-only.**

        </ResponseField>

        <ResponseField name="modifierGroups" type="ModifierGroup[]">

          Modifier groups that were added to the item.

          **Read-only.** Constraints: `maxItems 25`.

          <Expandable title="modifierGroups properties">

            <ResponseField name="_id" type="string">

              Modifier group ID.

              Constraints: `minLength 1`, `maxLength 36`.

            </ResponseField>

            <ResponseField name="name" type="TranslatableString">

              Modifier group name.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  __Required.__ String in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  String translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="modifiers" type="ItemModifier[]">

              List of modifiers in this group.

              Constraints: `minItems 1`, `maxItems 25`.

              <Expandable title="modifiers properties">

                <ResponseField name="_id" type="string">

                  Modifier ID.

                  Constraints: `minLength 1`, `maxLength 36`.

                </ResponseField>

                <ResponseField name="quantity" type="integer">

                  The quantity of this modifier.

                </ResponseField>

                <ResponseField name="label" type="TranslatableString">

                  Primary display label for the modifier.

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

                <ResponseField name="details" type="TranslatableString">

                  Additional details.

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

                <ResponseField name="price" type="MultiCurrencyPrice">

                  The price of the modifier.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="modifiersTotalPrice" type="MultiCurrencyPrice">

          Total price of all item modifiers. Included in the item price.

          **Read-only.**

          <Expandable title="modifiersTotalPrice properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="billingInfo" type="AddressWithContact">

      Billing information.

      <Expandable title="billingInfo properties">

        <ResponseField name="address" type="Address">

          Address.

          <Expandable title="address properties">

            <ResponseField name="streetAddress" type="StreetAddress">

              streetAddress

              <Expandable title="streetAddress properties">

                <ResponseField name="name" type="string" />

                <ResponseField name="number" type="string" />

              </Expandable>

            </ResponseField>

            <ResponseField name="city" type="string" />

            <ResponseField name="subdivision" type="string" />

            <ResponseField name="country" type="string" />

            <ResponseField name="postalCode" type="string" />

            <ResponseField name="addressLine2" type="string" />

          </Expandable>

        </ResponseField>

        <ResponseField name="contactDetails" type="FullAddressContactDetails">

          Contact details.

          <Expandable title="contactDetails properties">

            <ResponseField name="firstName" type="string">

              First name.

            </ResponseField>

            <ResponseField name="lastName" type="string">

              Last name.

            </ResponseField>

            <ResponseField name="phone" type="string">

              Phone number.

            </ResponseField>

            <ResponseField name="company" type="string">

              Company name.

            </ResponseField>

            <ResponseField name="vatId" type="VatId">

              Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

              <Expandable title="vatId properties">

                <ResponseField name="_id" type="string">

                  Customer's tax ID.

                </ResponseField>

                <ResponseField name="type" type="string">

                  Tax type.

                  Supported values:
                  + `CPF`: for individual tax payers
                  + `CNPJ`: for corporations

                  **Supported values:**

                  - `UNSPECIFIED`
                  - `CPF` — CPF - for individual tax payers.
                  - `CNPJ` — CNPJ - for corporations

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="shippingInfo" type="ShippingInfo">

      Shipping information.

      <Expandable title="shippingInfo properties">

        <ResponseField name="shippingDestination" type="AddressWithContact">

          Shipping address and contact details.

          <Expandable title="shippingDestination properties">

            <ResponseField name="address" type="Address">

              Address.

              <Expandable title="address properties">

                <ResponseField name="streetAddress" type="StreetAddress">

                  streetAddress

                  _Nested structure omitted for brevity — see the `StreetAddress` object._

                </ResponseField>

                <ResponseField name="city" type="string" />

                <ResponseField name="subdivision" type="string" />

                <ResponseField name="country" type="string" />

                <ResponseField name="postalCode" type="string" />

                <ResponseField name="addressLine2" type="string" />

              </Expandable>

            </ResponseField>

            <ResponseField name="contactDetails" type="FullAddressContactDetails">

              Contact details.

              <Expandable title="contactDetails properties">

                <ResponseField name="firstName" type="string">

                  First name.

                </ResponseField>

                <ResponseField name="lastName" type="string">

                  Last name.

                </ResponseField>

                <ResponseField name="phone" type="string">

                  Phone number.

                </ResponseField>

                <ResponseField name="company" type="string">

                  Company name.

                </ResponseField>

                <ResponseField name="vatId" type="VatId">

                  Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

                  _Nested structure omitted for brevity — see the `VatId` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedCarrierServiceOption" type="SelectedCarrierServiceOption">

          Selected option out of the options allowed for the `region`.

          <Expandable title="selectedCarrierServiceOption properties">

            <ResponseField name="code" type="string">

              Unique identifier of selected option. For example, "usps_std_overnight".

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
              For example, "Standard" or "First-Class Package International".

              **Read-only.** Constraints: `maxLength 250`.

            </ResponseField>

            <ResponseField name="logistics" type="DeliveryLogistics">

              Delivery logistics.

              **Read-only.**

              <Expandable title="logistics properties">

                <ResponseField name="deliveryTime" type="string">

                  Expected delivery time, in free text. For example, "3-5 business days".

                </ResponseField>

                <ResponseField name="instructions" type="string">

                  Instructions for caller, e.g for pickup: "Please deliver during opening hours, and please don't park in disabled parking spot".

                </ResponseField>

                <ResponseField name="pickupDetails" type="PickupDetails">

                  Pickup details.

                  _Nested structure omitted for brevity — see the `PickupDetails` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="cost" type="SelectedCarrierServiceOptionPrices">

              Shipping costs.

              **Read-only.**

              <Expandable title="cost properties">

                <ResponseField name="totalPriceAfterTax" type="MultiCurrencyPrice">

                  Total shipping price, after discount and after tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="totalPriceBeforeTax" type="MultiCurrencyPrice">

                  Total price of shipping after discounts (when relevant), and before tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxDetails" type="ItemTaxFullDetails">

                  Tax details.

                  _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

                </ResponseField>

                <ResponseField name="totalDiscount" type="MultiCurrencyPrice">

                  Shipping discount before tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="price" type="MultiCurrencyPrice">

                  Shipping price before discount and before tax.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="requestedShippingOption" type="boolean">

              Were we able to find the requested shipping option, or otherwise we fallback to the default one (the first)

              **Read-only.**

            </ResponseField>

            <ResponseField name="otherCharges" type="SelectedCarrierServiceOptionOtherCharge[]" deprecated>

              Other charges

              **Deprecated.** Other charges Use `additional_fees` instead. Removal: 2025-10-01.

              <Expandable title="otherCharges properties">

                <ResponseField name="type" type="string">

                  Type of additional cost.

                  **Supported values:**

                  - `HANDLING_FEE`
                  - `INSURANCE`

                </ResponseField>

                <ResponseField name="details" type="string">

                  Details of the charge, such as 'Full Coverage Insurance of up to 80% of value of shipment'.

                </ResponseField>

                <ResponseField name="cost" type="SelectedCarrierServiceOptionPrices">

                  Price of added charge.

                  _Nested structure omitted for brevity — see the `SelectedCarrierServiceOptionPrices` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="carrierId" type="string">

              This carrier's unique ID

            </ResponseField>

            <ResponseField name="deliveryAllocations" type="DeliveryAllocation[]">

              Delivery solution allocations to different delivery carriers and delivery regions

              **Developer Preview.** Constraints: `maxItems 300`.

              <Expandable title="deliveryAllocations properties">

                <ResponseField name="deliveryCarrier" type="Carrier">

                  The delivery option's carrier details, could be multiple if the delivery option is a combination of multiple carriers

                  _Nested structure omitted for brevity — see the `Carrier` object._

                </ResponseField>

                <ResponseField name="deliveryRegion" type="Region">

                  The delivery region that are relevant for this delivery solution.

                  _Nested structure omitted for brevity — see the `Region` object._

                </ResponseField>

                <ResponseField name="applicableLineItems" type="ApplicableLineItems">

                  Populated if the delivery solution is a partially supplied by this carrier.

                  _Nested structure omitted for brevity — see the `ApplicableLineItems` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="partial" type="boolean">

              If the delivery solution is a partial and doesn't apply to all items.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="region" type="ShippingRegion">

          Shipping region. Based on the address provided.

          **Read-only.**

          <Expandable title="region properties">

            <ResponseField name="_id" type="string">

              Shipping region ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="string">

              Shipping region name.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="carrierServiceOptions" type="CarrierServiceOption[]">

          All carrier options for this shipping rule.

          **Read-only.**

          <Expandable title="carrierServiceOptions properties">

            <ResponseField name="carrierId" type="string">

              Carrier ID.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="shippingOptions" type="ShippingOption[]">

              Shipping options offered by this carrier for this request.

              <Expandable title="shippingOptions properties">

                <ResponseField name="code" type="string">

                  Unique code of provided shipping option like "usps_std_overnight".
                  For legacy calculators this would be the UUID of the option.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="title" type="string">

                  Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
                  For example, "Standard" or "First-Class Package International".

                  Constraints: `maxLength 250`.

                </ResponseField>

                <ResponseField name="logistics" type="DeliveryLogistics">

                  Delivery logistics.

                  _Nested structure omitted for brevity — see the `DeliveryLogistics` object._

                </ResponseField>

                <ResponseField name="cost" type="ShippingPrice">

                  Sipping price information.

                  _Nested structure omitted for brevity — see the `ShippingPrice` object._

                </ResponseField>

                <ResponseField name="deliveryAllocations" type="DeliveryAllocation[]">

                  Delivery solution allocations to different delivery carriers and delivery regions

                  **Developer Preview.** Constraints: `maxItems 300`.

                  _Nested structure omitted for brevity — see the `DeliveryAllocation[]` object._

                </ResponseField>

                <ResponseField name="partial" type="boolean">

                  If the delivery solution is a partial and doesn't apply to all items.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="buyerNote" type="string">

      [Buyer note](https://support.wix.com/en/article/collecting-and-viewing-buyer-notes) left by the customer.

    </ResponseField>

    <ResponseField name="buyerInfo" type="BuyerInfo">

      Buyer information.

      <Expandable title="buyerInfo properties">

        <ResponseField name="contactId" type="string">

          Contact ID. Auto-created if one does not yet exist. For more information, see the Contacts API.

          **Read-only.**

        </ResponseField>

        <ResponseField name="email" type="string">

          Buyer email address.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="visitorId" type="string">

              Visitor ID (if site visitor is **not** a member).

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="memberId" type="string">

              Member ID (if site visitor is a site member).

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="conversionCurrency" type="string">

      The currency [selected by the customer](https://support.wix.com/en/article/multicurrency-an-overview) during the purchase flow, in three-letter [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format.

      This reflects the customer’s preferred display currency and may differ from the site’s default currency.
      When no specific currency is selected by the customer, this matches the `currency` property.

      **Read-only.** Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="priceSummary" type="PriceSummary">

      Calculated price summary for the checkout.

      **Read-only.**

      <Expandable title="priceSummary properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="calculationErrors" type="CalculationErrors">

      Errors when calculating totals.

      **Read-only.**

      <Expandable title="calculationErrors properties">

        <ResponseField name="taxCalculationError" type="Details">

          Tax calculation error.

          <Expandable title="taxCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="couponCalculationError" type="Details">

          Coupon calculation error.

          <Expandable title="couponCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="giftCardCalculationError" type="Details" deprecated>

          DEPRECATED: Use gift_card_calculation_errors instead.

          **Deprecated.** DEPRECATED: Use gift_card_calculation_errors instead. Use `gift_card_calculation_errors` instead. Removal: 2026-12-01.

          <Expandable title="giftCardCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="orderValidationErrors" type="ApplicationError[]">

          Order validation errors.

          <Expandable title="orderValidationErrors properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="membershipError" type="Details">

          Membership payment methods calculation errors
          For example, will indicate that a line item that must be paid with membership payment doesn't have one or selected memberships are invalid

          <Expandable title="membershipError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discountsCalculationError" type="Details">

          Discount Rule calculation error.

          <Expandable title="discountsCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              <Expandable title="tracing properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="string" />

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              <Expandable title="kind properties">

                <ResponseField name="applicationError" type="ApplicationError">

                  applicationError

                  _Nested structure omitted for brevity — see the `ApplicationError` object._

                </ResponseField>

                <ResponseField name="validationError" type="ValidationError">

                  example result:
                  &#123;
                  "fieldViolations": [
                  &#123;
                  "field": "fieldA",
                  "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
                  "violatedRule": "OTHER",
                  "ruleName": "INVALID_NOTE",
                  "data": &#123;
                  "value": "FI"
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldB",
                  "description": "field value out of range. supported range: [0-20]",
                  "violatedRule": "MAX",
                  "data": &#123;
                  "threshold": 20
                  &#125;
                  &#125;,
                  &#123;
                  "field": "fieldC",
                  "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
                  "violatedRule": "FORMAT",
                  "data": &#123;
                  "type": "PHONE"
                  &#125;
                  &#125;
                  ]
                  &#125;

                  _Nested structure omitted for brevity — see the `ValidationError` object._

                </ResponseField>

                <ResponseField name="systemError" type="SystemError">

                  systemError

                  _Nested structure omitted for brevity — see the `SystemError` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="shippingCalculationError" type="shippingCalculationError">

          shippingCalculationError

          <Expandable title="shippingCalculationError properties">

            <ResponseField name="generalShippingCalculationError" type="Details">

              General shipping calculation error.

              <Expandable title="generalShippingCalculationError properties">

                <ResponseField name="tracing" type="object" deprecated>

                  deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

                  **Deprecated.** Constraints: `format map`.

                  _Nested structure omitted for brevity — see the `object` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="kind" type="kind">

                  kind

                  _Nested structure omitted for brevity — see the `kind` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="carrierErrors" type="CarrierErrors">

              Errors related to the delivery carrier.

              <Expandable title="carrierErrors properties">

                <ResponseField name="errors" type="CarrierError[]">

                  Carrier errors.

                  _Nested structure omitted for brevity — see the `CarrierError[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="selectedShippingOptionError" type="Details">

              Error related to the selected shipping option.

              <Expandable title="selectedShippingOptionError properties">

                <ResponseField name="tracing" type="object" deprecated>

                  deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

                  **Deprecated.** Constraints: `format map`.

                  _Nested structure omitted for brevity — see the `object` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="kind" type="kind">

                  kind

                  _Nested structure omitted for brevity — see the `kind` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="giftCard" type="GiftCard">

      Applied gift card details.

      >**Note:** Gift cards are supported through the Wix UI, though the service plugin is not currently available. Learn more about [Wix Gift Cards](https://support.wix.com/en/article/setting-up-wix-gift-cards-9451439).

      **Read-only.**

      <Expandable title="giftCard properties">

        <ResponseField name="_id" type="string" deprecated>

          Gift Card ID.

          **Deprecated.**

        </ResponseField>

        <ResponseField name="obfuscatedCode" type="string">

          Gift card obfuscated code.

        </ResponseField>

        <ResponseField name="amount" type="MultiCurrencyPrice">

          Actual amount to be redeemed from the gift card.

          <Expandable title="amount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="appId" type="string">

          App ID of the gift card provider.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="externalId" type="string">

          External ID in the gift card provider's system.
          Used for integration and tracking across different platforms.

        </ResponseField>

        <ResponseField name="requestedAmount" type="MultiCurrencyPrice">

          Requested amount to redeem from the gift card.

          **Developer Preview.**

          <Expandable title="requestedAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="appliedDiscounts" type="AppliedDiscount[]">

      Applied discounts.

      **Read-only.**

      <Expandable title="appliedDiscounts properties">

        <ResponseField name="discountType" type="string">

          Discount type.

          **Supported values:**

          - `GLOBAL`
          - `SPECIFIC_ITEMS`
          - `SHIPPING`

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          IDs of line items the discount applies to.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customFields" type="CustomField[]">

      Custom fields.

      <Expandable title="customFields properties">

        <ResponseField name="value" type="any">

          Custom field value.

        </ResponseField>

        <ResponseField name="title" type="string">

          Custom field title.

          Constraints: `minLength 1`, `maxLength 500`.

        </ResponseField>

        <ResponseField name="translatedTitle" type="string">

          Translated custom field title.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="weightUnit" type="string">

      Weight measurement unit. Defaults to a site's weight unit.

      **Read-only.**

      **Supported values:**

      - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
      - `KG` — Kilograms.
      - `LB` — Pounds.

    </ResponseField>

    <ResponseField name="taxSummary" type="TaxSummary">

      Tax summary.

      **Read-only.**

      <Expandable title="taxSummary properties">

        <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

          Amount for which tax is calculated, added from line items.

          **Read-only.**

          <Expandable title="taxableAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalTax" type="MultiCurrencyPrice">

          Calculated tax, added from line items.

          **Read-only.**

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="calculationDetails" type="TaxCalculationDetails" deprecated>

          Tax calculator that was active when the order was created.

          **Deprecated.**

          <Expandable title="calculationDetails properties">

            <ResponseField name="rateType" type="string">

              Rate calculation type.

              **Supported values:**

              - `NO_TAX_COLLECTED` — no tax being collected for this request due to location of purchase
              - `MANUAL_RATE` — manual rate used for calculation
              - `AUTO_RATE` — autotax rate used for calculation
              - `FALLBACK_RATE` — fallback rate used for calculation

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="CalculationDetails" type="CalculationDetails">

              CalculationDetails

              <Expandable title="CalculationDetails properties">

                <ResponseField name="manualRateReason" type="string">

                  Reason the manual calculation was used.

                  **Supported values:**

                  - `GLOBAL_SETTING_TO_MANUAL` — user set calculator in Business Manager to be Manual
                  - `REGION_SETTING_TO_MANUAL` — specific region is on manual even though Global setting is Auto-tax

                </ResponseField>

                <ResponseField name="autoTaxFallbackDetails" type="AutoTaxFallbackCalculationDetails">

                  Details of the fallback rate calculation.

                  _Nested structure omitted for brevity — see the `AutoTaxFallbackCalculationDetails` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="currency" type="string">

      The site’s default currency, in three-letter [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format. This represents the base currency configured for the site and remains constant regardless of the customer’s currency selection.

      **Read-only.** Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="channelType" type="string">

      Sales channel that submitted the order.

      **Read-only.**

      **Supported values:**

      - `UNSPECIFIED` — Unspecified sales channel. This value is not supported.
      - `WEB` — A web client.
      - `POS` — [Point of sale solutions](https://support.wix.com/en/wix-mobile-pos-2196395).
      - `EBAY` — [eBay shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-ebay-shop).
      - `AMAZON` — [Amazon shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-amazon-shop).
      - `OTHER_PLATFORM` — Other sales platform.
      - `WIX_APP_STORE` — [Wix Owner app](https://support.wix.com/article/wix-owner-app-an-overview).
      - `WIX_INVOICES` — Wix Invoices app in [your dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Select%20Site&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Finvoices/settings/general-settings)
      - `BACKOFFICE_MERCHANT` — Wix merchant backoffice.
      - `WISH` — Wish sales channel.
      - `CLASS_PASS` — [ClassPass sales channel](https://support.wix.com/en/article/wix-bookings-letting-clients-book-your-services-with-classpass).
      - `GLOBAL_E` — Global-E sales channel.
      - `FACEBOOK` — [Facebook shop](https://support.wix.com/en/article/wix-stores-changes-to-facebook-shops).
      - `ETSY` — [Etsy sales channel](https://support.wix.com/en/article/wix-stores-request-adding-etsy-as-a-sales-channel).
      - `TIKTOK` — [TikTok sales channel](https://support.wix.com/en/article/wix-stores-request-adding-tiktok-as-a-sales-channel).
      - `FAIRE_COM` — [Faire marketplace integration](https://support.wix.com/en/article/wix-stores-creating-a-faire-store-using-the-faire-integration-app).
      - `PAYPAL_AGENTIC_CHECKOUT` — PayPal Agentic Checkout sales channel.
      - `STRIPE_AGENTIC_CHECKOUT` — Stripe Agentic Checkout sales channel.

    </ResponseField>

    <ResponseField name="siteLanguage" type="string">

      Site language in which original values are shown.

      **Read-only.**

    </ResponseField>

    <ResponseField name="buyerLanguage" type="string">

      Language for communication with the buyer. Defaults to the site language.

      For a site that supports multiple languages, this is the language the buyer selected.

      **Read-only.**

    </ResponseField>

    <ResponseField name="completed" type="boolean">

      Whether an order was successfully created from this checkout.

      For an order to be successful, it must be successfully paid for (unless the total is 0).

      **Read-only.**

    </ResponseField>

    <ResponseField name="taxIncludedInPrice" type="boolean">

      Whether tax is included in line item prices.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdBy" type="CreatedBy">

      ID of the checkout's initiator.

      **Read-only.**

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="userId" type="string">

              User ID. When the order was created by a Wix user on behalf of a buyer.
              For example, via POS (point of service).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="memberId" type="string">

              Member ID. When the order was created by a **logged in** site visitor.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="visitorId" type="string">

              Visitor ID. When the order was created by a site visitor that was **not** logged in.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              App ID. When the order was created by an external application or Wix service.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the checkout was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time the checkout was updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="payNow" type="PriceSummary">

      Minimal amount to pay in order to place the order.

      **Read-only.**

      <Expandable title="payNow properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payLater" type="PriceSummary">

      Remaining amount for the order to be fully paid.

      **Read-only.**

      <Expandable title="payLater properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="membershipOptions" type="MembershipOptions">

      Memberships to apply when creating the order.

      <Expandable title="membershipOptions properties">

        <ResponseField name="eligibleMemberships" type="Membership[]">

          Reserved for internal use.

          **Read-only.** Constraints: `maxItems 300`.

          <Expandable title="eligibleMemberships properties">

            <ResponseField name="_id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the application providing this payment option.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="MembershipName">

              The name of this membership.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  Membership name.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Translated membership name. Defaults to `original` when not provided.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

              Only populated in the legacy `ListEligibleMemberships` flow, which still requires it. Empty on
              `ListMembershipsByIds`, a by-id lookup that receives no line items to report against.

              Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="credits" type="MembershipPaymentCredits">

              Membership's payment credits (balance information).

              <Expandable title="credits properties">

                <ResponseField name="total" type="integer">

                  Membership's initial value.

                  Constraints: `min 1`.

                </ResponseField>

                <ResponseField name="remaining" type="integer">

                  Membership's remaining value.

                  Constraints: `min 0`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="expirationDate" type="datetime">

              Membership expiration date.

            </ResponseField>

            <ResponseField name="additionalData" type="object">

              Deprecated. Use `offering_description`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="invalidMemberships" type="InvalidMembership[]">

          Reserved for internal use.

          **Read-only.** Constraints: `maxItems 300`.

          <Expandable title="invalidMemberships properties">

            <ResponseField name="membership" type="Membership">

              Membership details.

              <Expandable title="membership properties">

                <ResponseField name="_id" type="string">

                  Membership ID.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  ID of the application providing this payment option.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="name" type="MembershipName">

                  The name of this membership.

                  _Nested structure omitted for brevity — see the `MembershipName` object._

                </ResponseField>

                <ResponseField name="lineItemIds" type="string[]">

                  Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

                  Only populated in the legacy `ListEligibleMemberships` flow, which still requires it. Empty on
                  `ListMembershipsByIds`, a by-id lookup that receives no line items to report against.

                  Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

                </ResponseField>

                <ResponseField name="credits" type="MembershipPaymentCredits">

                  Membership's payment credits (balance information).

                  _Nested structure omitted for brevity — see the `MembershipPaymentCredits` object._

                </ResponseField>

                <ResponseField name="expirationDate" type="datetime">

                  Membership expiration date.

                </ResponseField>

                <ResponseField name="additionalData" type="object">

                  Deprecated. Use `offering_description`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="reason" type="string">

              Reason why this membership is invalid and cannot be used.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedMemberships" type="SelectedMemberships">

          Selected membership to apply to this checkout.

          <Expandable title="selectedMemberships properties">

            <ResponseField name="memberships" type="SelectedMembership[]">

              Selected memberships.

              Constraints: `maxItems 300`.

              <Expandable title="memberships properties">

                <ResponseField name="_id" type="string">

                  Membership ID.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  ID of the app providing this payment option.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="lineItemIds" type="string[]">

                  IDs of the line items this membership applies to.

                  Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalFees" type="AdditionalFee[]">

      Additional Fees.

      **Read-only.** Constraints: `maxItems 100`.

      <Expandable title="additionalFees properties">

        <ResponseField name="code" type="string">

          Additional fee's unique code (or ID) for future processing.

        </ResponseField>

        <ResponseField name="name" type="string">

          Additional fee's name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ResponseField>

        <ResponseField name="price" type="MultiCurrencyPrice">

          Additional fee's price.

          <Expandable title="price properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="ItemTaxFullDetails">

          Tax details.

          <Expandable title="taxDetails properties">

            <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

              Amount for which tax is calculated.

              <Expandable title="taxableAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxRate" type="string">

              Tax rate %, as a decimal point between 0 and 1.

              Constraints: `>= 0`, `<= 1`.

            </ResponseField>

            <ResponseField name="totalTax" type="MultiCurrencyPrice">

              Calculated tax, based on `taxable_amount` and `tax_rate`.

              <Expandable title="totalTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="convertedAmount" type="string">

                  Converted amount.

                  **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formattedConvertedAmount" type="string">

                  Converted amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="rateBreakdown" type="TaxRateBreakdown[]" deprecated>

              If breakdown exists, the sum of rates in the breakdown must equal `tax_rate`. Deprecated - use 'tax_breakdown' instead.

              **Read-only.** **Deprecated.**

              <Expandable title="rateBreakdown properties">

                <ResponseField name="name" type="string">

                  Name of tax against which the calculation was performed.

                  Constraints: `maxLength 100`.

                </ResponseField>

                <ResponseField name="rate" type="string">

                  Rate at which this tax detail was calculated.

                  Constraints: `>= 0`, `<= 1`.

                </ResponseField>

                <ResponseField name="tax" type="MultiCurrencyPrice">

                  Amount of tax for this tax detail.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxBreakdown" type="TaxBreakdown[]">

              tax information for a line item.

              **Read-only.** Constraints: `minItems 0`, `maxItems 1000`.

              <Expandable title="taxBreakdown properties">

                <ResponseField name="jurisdiction" type="string">

                  The name of the jurisdiction to which this tax detail applies. For example, "New York" or "Quebec".

                </ResponseField>

                <ResponseField name="nonTaxableAmount" type="MultiCurrencyPrice">

                  The amount of this line item price that was considered nontaxable. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="rate" type="string">

                  The rate at which this tax detail was calculated, e.g 0.1000 signifies 10% tax and 2.0000 signifies 200% tax. (Decimal value)

                </ResponseField>

                <ResponseField name="taxAmount" type="MultiCurrencyPrice">

                  The amount of tax estimated for this line item. (Decimal value)

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

                  The taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

                </ResponseField>

                <ResponseField name="taxType" type="string">

                  The type of tax that was calculated. Depends on the jurisdiction's tax laws. For example, "Sales Tax", "Income Tax", "Value Added Tax", etc.

                </ResponseField>

                <ResponseField name="taxName" type="string">

                  The name of the tax against which this tax amount was calculated. For example, "NY State Sales Tax", "Quebec GST", etc.
                  This name should be explicit enough to allow the merchant to understand what tax was calculated.

                </ResponseField>

                <ResponseField name="jurisdictionType" type="string">

                  The type of the jurisdiction in which this tax detail applies.

                  **Supported values:**

                  - `UNDEFINED`
                  - `COUNTRY`
                  - `STATE`
                  - `COUNTY`
                  - `CITY`
                  - `SPECIAL`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="providerAppId" type="string">

          Provider's app id.

        </ResponseField>

        <ResponseField name="priceBeforeTax" type="MultiCurrencyPrice">

          Additional fee's price before tax.

          <Expandable title="priceBeforeTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceAfterTax" type="MultiCurrencyPrice">

          Additional fee's price after tax.

          <Expandable title="priceAfterTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          Optional - Line items associated with this additional fee.
          If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="source" type="string">

          the source the additional fee was added from

          **Supported values:**

          - `UNKNOWN_ADDITIONAL_FEE_SOURCE`
          - `SERVICE_PLUGIN` — The additional fee was added by an additional fee service plugin
          - `ITEM` — The additional fee was added on the item either via the catalog or on custom line item
          - `MANUAL` — The additional fee was added manually on the request
          - `SHIPPING` — The additional fee was added by the shipping provider
          - `PLATFORM` — The additional fee was added by a Wix vertical and represents a Wix platform fee

        </ResponseField>

        <ResponseField name="translatedName" type="string">

          The translated name of the additional fee. The translation language is determined by the `languages` field in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          Address type tax was calculated against for this fee.

          **Developer Preview.**

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              <Expandable title="TaxableAddressData properties">

                <ResponseField name="addressType" type="string">

                  taxable address type. if this field is selected, the address is automatically resolved, and the tax is calculated accordingly.

                  **Supported values:**

                  - `BUSINESS`
                  - `BILLING`
                  - `SHIPPING`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="cartId" type="string">

      Cart ID that this checkout was created from. Empty if this checkout wasn't created from a cart.

    </ResponseField>

    <ResponseField name="violations" type="Violation[]">

      List of validation violations raised by the [Validations service plugin](https://dev.wix.com/docs/rest/business-solutions/e-commerce/service-plugins/validations-integration-service-plugin/introduction).

      **Read-only.**

      <Expandable title="violations properties">

        <ResponseField name="severity" type="string">

          Severity of the violation. The violations are shown on the cart and checkout pages. A warning is displayed as yellow, and allows a site visitor to proceed with caution. An error is displayed as red, and doesn't allow a site visitor to proceed with the eCommerce flow.

          **Supported values:**

          - `WARNING` — The user is allowed to move forward in the flow.
          - `ERROR` — The user is blocked from moving forward in the flow.
          For example, if callerContext is CART - moving to checkout is blocked. if callerContext is CHECKOUT, placing an order is blocked.

        </ResponseField>

        <ResponseField name="target" type="Target">

          Target location on a checkout or cart page where the violation will be displayed.

          <Expandable title="One of the following">

            <ResponseField name="targetType" type="targetType">

              targetType

              <Expandable title="targetType properties">

                <ResponseField name="other" type="Other">

                  General (other) violation.

                  _Nested structure omitted for brevity — see the `Other` object._

                </ResponseField>

                <ResponseField name="lineItem" type="LineItem">

                  Specific line item violation.

                  _Nested structure omitted for brevity — see the `LineItem` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="description" type="string">

          Violation description. Can include rich text. Only HTTP or HTTPS links in the following format are allowed: `<a href="https://www.wix.com">Click me</a>`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Custom field data for the checkout object.

      [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="purchaseFlowId" type="string">

      Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

      **Read-only.**

    </ResponseField>

    <ResponseField name="customSettings" type="CustomSettings">

      Additional settings for customization of the checkout process.

      > **Notes:**
      > * Custom settings can only be set when creating a checkout.
      > * To access and manage custom checkout page content, your app must have the permission scope named "Manage eCommerce - Admin Permissions". Learn more about [permission scopes](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

      Constraints: `immutable`.

      <Expandable title="customSettings properties">

        <ResponseField name="lockGiftCard" type="boolean">

          Whether to restrict the option to add or remove a gift card on the checkout page.

          Default: `false`

        </ResponseField>

        <ResponseField name="lockCouponCode" type="boolean">

          Whether to restrict the option to add or remove a coupon code on the checkout page.

          Default: `false`

        </ResponseField>

        <ResponseField name="disabledPolicyAgreementCheckbox" type="boolean">

          Whether to disable policy agreement checkout in the checkout page

          Default: `false`

        </ResponseField>

        <ResponseField name="disabledManualPayment" type="boolean">

          Whether to disable manual payment option for this checkout.

          Default: `false`

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customContentReference" type="CustomContentReference">

      Reference IDs for the app and component providing custom checkout page content.

      To access and manage custom checkout page content, your app must have the permission scope named "Manage eCommerce - Admin Permissions".
      Learn more about [permission scopes](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

      **Developer Preview.**

      <Expandable title="customContentReference properties">

        <ResponseField name="appId" type="string">

          ID of the app providing the content.

          You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="componentId" type="string">

          ID of the component within the app it belongs to.

          You can get your component's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="externalReference" type="ExternalReference">

      References to an external app and resource associated with this checkout.
      Used for integration and tracking across different platforms.

      **Developer Preview.** Constraints: `immutable`.

      <Expandable title="externalReference properties">

        <ResponseField name="appId" type="string">

          ID of the app associated with the purchase flow.
          For example, the Wix Pay Links app ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="resourceId" type="string">

          Reference to an external resource ID. Used to link the purchase flow to a specific entity in an external system.
          For example, a Wix Pay Link ID.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payAfterFreeTrial" type="PriceSummary">

      Payment for subscriptions after free trial period.

      **Read-only.** **Developer Preview.**

      <Expandable title="payAfterFreeTrial properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="businessLocationId" type="string">

      The business location ID associated with the checkout.

      To learn more, see the Locations API.

    </ResponseField>

    <ResponseField name="paymentCurrency" type="string">

      The currency used for payment, in three-letter [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format.

      This is determined by the [customer's selected currency](https://support.wix.com/en/article/multicurrency-an-overview) and the site’s supported payment currencies.
      If the customer’s selected currency is supported for payment, this matches the `conversionCurrency` property. If not supported, this falls back to the `currency` property.

      **Read-only.** **Developer Preview.** Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="taxExemptGroupId" type="string">

      Tax exempt group ID to use for tax exemptions.

    </ResponseField>

  </Tab>

</Tabs>
