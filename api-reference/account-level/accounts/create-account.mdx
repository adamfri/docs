---
title: "Create Account"
description: "Creates a new Wix account, and creates a new Wix user as the account owner."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new Wix account, and creates a new Wix user as the account owner.
    The newly created account is a child account of the account used to create it, making that account its parent.

    <Note>

    This call requires an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="ACCOUNT.CREATE_ACCOUNT">ACCOUNT.CREATE_ACCOUNT</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/accounts/v1/accounts/create
    ```

    ## Body Params

    <ParamField body="user" type="User" required>

      The user to create under the new account, with the roles defined in `roles`.

      <Expandable title="user properties">

        <ParamField body="email" type="Email">

          User's unique email address details. Required.

          <Expandable title="email properties">

            <ParamField body="emailAddress" type="string">

              User's email address.

              Constraints: `format EMAIL`.

            </ParamField>

            <ParamField body="isVerified" type="boolean">

              Whether the caller has verified the user's email address.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="ssoIdentities" type="SsoIdentity[]">

          User's single sign on identity, when the user is identified via SSO authentication response token params, as specified by [OpenID Connect](https://openid.net/developers/how-connect-works/) (aka. OIDC) protocol.

          <Expandable title="ssoIdentities properties">

            <ParamField body="ssoId" type="string">

              An SSO setting (URLs, clientId, secret, etc. as required by OIDC protocol) for a specific Identity-Provider (aka. IdP) for a specific Wix account.

            </ParamField>

            <ParamField body="userId" type="string">

              User ID as stored in IdP. For example a "sub" claim of OIDC protocol,
              or any other alternative, specified by IdP (Identity Provider).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="userDetails" type="UserDetails">

          Additional user details.

          <Expandable title="userDetails properties">

            <ParamField body="firstName" type="string">

              User's first name.

            </ParamField>

            <ParamField body="lastName" type="string">

              User's last name.

            </ParamField>

            <ParamField body="profilePictureUrl" type="string">

              URL to location of user's profile picture.

            </ParamField>

            <ParamField body="language" type="string">

              User's preferred language in [ISO 639-1:2002](https://en.wikipedia.org/wiki/ISO_639-1) format. For example, "en", "es".

            </ParamField>

            <ParamField body="clientIp" type="string">

              Original Client IP from which a request was made.
              This is useful in case where a createUser API is called by some server call, which, in turn, has been called by some client from another IP.
              Wix checks this IP against the [OFAC sanctioned countries](https://ofac.treasury.gov/sanctions-programs-and-country-information).

            </ParamField>

            <ParamField body="contactEmail" type="string">

              if defined then emails to the user will be sent to this email instead of the user's primary email

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="roles" type="string[]">

      Roles to be assigned to the user in the new account. To retrieve all available roles, call Get Roles Info in the Users API.
      Default: OWNER.

    </ParamField>

    ## Response Object

    <ResponseField name="account" type="AccountV2">

      The created account.

      <Expandable title="account properties">

        <ResponseField name="accountId" type="string">

          Account ID.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="user" type="UserV2">

      The created user

      <Expandable title="user properties">

        <ResponseField name="userId" type="string">

          User ID

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/accounts/v1/accounts/create' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
            "roles": [
              "6600344420111308827"
            ],
            "user": {
              "sso_identities": [
                {
                  "sso_id": "<SOME-SSO-ID>",
                  "user_id": "<SOME-USER-ID-FROM-IDP>"
                }
              ],
              "email": {
                "is_verified": true,
                "email_address": "some@email.com"
              },
              "userDetails": {
                "first_name": "First",
                "last_name": "Name",
                "profile_picture_url": "http://picture.com/pic.jpg",
                "client_ip": "127.0.0.1",
                "language": "en"
              }
            }
          }'
      ```

      ```json Response
      {
          "accountId": "0092ff87-6028-40ab-9999-980d5474abcf"
        }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new Wix account, and creates a new Wix user as the account owner.
    The newly created account is a child account of the account used to create it, making that account its parent.

    <Note>

    This call requires an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="ACCOUNT.CREATE_ACCOUNT">ACCOUNT.CREATE_ACCOUNT</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { accounts } from '@wix/user-management';

    accounts.createAccount(user, options): Promise<CreateAccountResponse>
    ```

    ## Method Parameters

    <ParamField body="user" type="User" required>

      The user to create under the new account, with the roles defined in `roles`.

      <Expandable title="user properties">

        <ParamField body="email" type="Email">

          User's unique email address details. Required.

          <Expandable title="email properties">

            <ParamField body="emailAddress" type="string">

              User's email address.

              Constraints: `format EMAIL`.

            </ParamField>

            <ParamField body="isVerified" type="boolean">

              Whether the caller has verified the user's email address.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="ssoIdentities" type="SsoIdentity[]">

          User's single sign on identity, when the user is identified via SSO authentication response token params, as specified by [OpenID Connect](https://openid.net/developers/how-connect-works/) (aka. OIDC) protocol.

          <Expandable title="ssoIdentities properties">

            <ParamField body="ssoId" type="string">

              An SSO setting (URLs, clientId, secret, etc. as required by OIDC protocol) for a specific Identity-Provider (aka. IdP) for a specific Wix account.

            </ParamField>

            <ParamField body="userId" type="string">

              User ID as stored in IdP. For example a "sub" claim of OIDC protocol,
              or any other alternative, specified by IdP (Identity Provider).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="userDetails" type="UserDetails">

          Additional user details.

          <Expandable title="userDetails properties">

            <ParamField body="firstName" type="string">

              User's first name.

            </ParamField>

            <ParamField body="lastName" type="string">

              User's last name.

            </ParamField>

            <ParamField body="profilePictureUrl" type="string">

              URL to location of user's profile picture.

            </ParamField>

            <ParamField body="language" type="string">

              User's preferred language in [ISO 639-1:2002](https://en.wikipedia.org/wiki/ISO_639-1) format. For example, "en", "es".

            </ParamField>

            <ParamField body="clientIp" type="string">

              Original Client IP from which a request was made.
              This is useful in case where a createUser API is called by some server call, which, in turn, has been called by some client from another IP.
              Wix checks this IP against the [OFAC sanctioned countries](https://ofac.treasury.gov/sanctions-programs-and-country-information).

            </ParamField>

            <ParamField body="contactEmail" type="string">

              if defined then emails to the user will be sent to this email instead of the user's primary email

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="createAccountOptions">

      Filter options.

      <Expandable title="options properties">

        <ParamField body="roles" type="string[]">

          Roles to be assigned to the user in the new account. To retrieve all available roles, call Get Roles Info in the Users API.
          Default: OWNER.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CreateAccountResponse>`.

    <ResponseField name="account" type="AccountV2">

      The created account.

      <Expandable title="account properties">

        <ResponseField name="accountId" type="string">

          Account ID.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="user" type="UserV2">

      The created user

      <Expandable title="user properties">

        <ResponseField name="userId" type="string">

          User ID

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-account" id="ex-sdk-create-account-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-account" id="ex-sdk-create-account-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-account" id="ex-sdk-create-account-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-account-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-account-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createAccount</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createAccount (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createAccount (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-account-0"><span className="wix-picker-name">createAccount</span></label>
    <label htmlFor="ex-sdk-create-account-1"><span className="wix-picker-name">createAccount (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-account-2"><span className="wix-picker-name">createAccount (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createAccount
    import { accounts } from '@wix/user-management';

    async function createAccount(user,options) {
      const response = await accounts.createAccount(user,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createAccount (self-hosted)
    import { createClient } from '@wix/sdk';
    import { accounts } from '@wix/user-management';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { accounts },
      // Include the auth strategy and host as relevant
    });

    async function createAccount(user,options) {
      const response = await myWixClient.accounts.createAccount(user,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createAccount (with elevated permissions)
    import { accounts } from '@wix/user-management';
    import { auth } from '@wix/essentials';

    async function myCreateAccountMethod(user,options) {
      const elevatedCreateAccount = auth.elevate(accounts.createAccount);
      const response = await elevatedCreateAccount(user,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
