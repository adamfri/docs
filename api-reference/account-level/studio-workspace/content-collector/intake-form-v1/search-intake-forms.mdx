---
title: "Search Intake Forms"
description: "Searches intake forms using a free-text expression matched against the client name and project name."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Searches intake forms using a free-text expression matched against the
    client name and project name.

    No site ID is required. Results are scoped to the caller's account. Intake
    forms belonging to other accounts are never returned.

    Use Search Intake Forms for free-text lookup (for example, the client a user
    asked about) along with filtering, sorting, and aggregations. For precise
    structured filtering and sorting without free-text matching, call
    [Query Intake Forms](https://dev.wix.com/docs/api-reference/account-level/partners/content-collector/intake-form-v1/query-intake-forms) instead.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `teamMemberIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `accountId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `clientFormInteraction.submissionDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `project.industry` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `project.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `publishDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `userId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `clientName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.CONTENT-COLLECTOR">SCOPE.PARTNERS.CONTENT-COLLECTOR</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/partners/content-collector/v1/intake-forms/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search query with free-text expression and optional filters/sorting.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use auto fuzzy search (allowing typos by a managed proximity algorithm).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Constraints: `maxItems 5`.

      **Supported values:**

      - `CONTEXT`
      - `SOURCE_MATERIALS`
      - `INTRO_DATA`

    </ParamField>

    ## Response Object

    <ResponseField name="intakeForms" type="IntakeForm[]">

      List of matching IntakeForms.

      See the [Intake Form Object](/api-reference/account-level/studio-workspace/content-collector/intake-form-v1/intake-form-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request).

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches intake forms by a free-text expression across client and project name

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/intake-forms/v1/intake-forms/search' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "search": {
            "search": {
              "expression": "bloom bakery"
            },
            "cursorPaging": { "limit": 50 }
          }
        }'
      ```

      ```json Response
      {
        "intakeForms": [
          {
            "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
            "revision": "3",
            "createdDate": "2024-03-18T17:22:10.299Z",
            "updatedDate": "2024-03-20T11:05:44.000Z",
            "clientName": "Jane Cohen",
            "project": {
              "name": "Bloom Bakery Website",
              "industry": "FOOD_AND_BEVERAGE"
            },
            "userId": "f1a2b3c4-d5e6-4789-9abc-1234567890ab",
            "accountId": "a9b8c7d6-e5f4-4321-8765-0fedcba98765",
            "publishDate": "2024-03-19T09:14:02.118Z"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Searches intake forms using a free-text expression matched against the
    client name and project name.

    No site ID is required. Results are scoped to the caller's account. Intake
    forms belonging to other accounts are never returned.

    Use Search Intake Forms for free-text lookup (for example, the client a user
    asked about) along with filtering, sorting, and aggregations. For precise
    structured filtering and sorting without free-text matching, call
    [Query Intake Forms](https://dev.wix.com/docs/api-reference/account-level/partners/content-collector/intake-form-v1/query-intake-forms) instead.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `teamMemberIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `accountId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `clientFormInteraction.submissionDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `project.industry` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `project.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `publishDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `userId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `clientName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.CONTENT-COLLECTOR">SCOPE.PARTNERS.CONTENT-COLLECTOR</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contentCollectorIntakeForms } from '@wix/content-collector-intake-forms';

    contentCollectorIntakeForms.searchIntakeForms(search, options): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search query with free-text expression and optional filters/sorting.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use auto fuzzy search (allowing typos by a managed proximity algorithm).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="SearchIntakeFormsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Constraints: `maxItems 5`.

          **Supported values:**

          - `CONTEXT`
          - `SOURCE_MATERIALS`
          - `INTRO_DATA`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="intakeForms" type="IntakeForm[]">

      List of matching IntakeForms.

      See the [Intake Form Object](/api-reference/account-level/studio-workspace/content-collector/intake-form-v1/intake-form-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request).

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-intake-forms" id="ex-sdk-search-intake-forms-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-intake-forms" id="ex-sdk-search-intake-forms-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-intake-forms-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-intake-forms-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search intake forms</span><span className="wix-picker-desc">Searches intake forms by a free-text expression across client and project name</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchIntakeForms (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-intake-forms-0"><span className="wix-picker-name">Search intake forms</span><span className="wix-picker-desc">Searches intake forms by a free-text expression across client and project name</span></label>
    <label htmlFor="ex-sdk-search-intake-forms-1"><span className="wix-picker-name">searchIntakeForms (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Search intake forms
    import { contentCollectorIntakeForms } from "@wix/content-collector-intake-forms";

    const search = {
      search: {
        expression: "bloom bakery",
      },
      cursorPaging: {
        limit: 50,
      },
    };

    async function searchIntakeForms() {
      const response = await contentCollectorIntakeForms.searchIntakeForms(search);
    }

    /* Promise resolves to:
     * {
     *   "intakeForms": [
     *     {
     *       "_id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
     *       "revision": "3",
     *       "_createdDate": "2024-03-18T17:22:10.299Z",
     *       "_updatedDate": "2024-03-20T11:05:44.000Z",
     *       "clientName": "Jane Cohen",
     *       "project": {
     *         "name": "Bloom Bakery Website",
     *         "industry": "FOOD_AND_BEVERAGE"
     *       },
     *       "userId": "f1a2b3c4-d5e6-4789-9abc-1234567890ab",
     *       "accountId": "a9b8c7d6-e5f4-4321-8765-0fedcba98765",
     *       "publishDate": "2024-03-19T09:14:02.118Z"
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 1,
     *     "cursors": {},
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchIntakeForms (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contentCollectorIntakeForms } from '@wix/content-collector-intake-forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contentCollectorIntakeForms },
      // Include the auth strategy and host as relevant
    });

    async function searchIntakeForms(search,options) {
      const response = await myWixClient.contentCollectorIntakeForms.searchIntakeForms(search,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
