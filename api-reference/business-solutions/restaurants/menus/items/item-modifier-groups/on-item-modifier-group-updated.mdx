---
title: "onItemModifierGroupUpdated"
description: "The onItemModifierGroupUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_modifier_group_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemModifierGroupUpdated
  import { itemModifierGroups } from '@wix/restaurants';

  itemModifierGroups.onItemModifierGroupUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemModifierGroupUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemModifierGroups } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemModifierGroups,
      },
  });

  wixClient.itemModifierGroups.onItemModifierGroupUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
