---
title: "Create Loyalty Import File Url"
description: "Creates a URL for uploading a loyalty import file."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a URL for uploading a loyalty import file.

    This method is the first step in the loyalty import process. It returns a file path and an upload URL, which you'll use in subsequent steps of the import process.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_api/loyalty-imports/v1/loyalty-imports/wixmp-upload-url
    ```

    ## Response Object

    <ResponseField name="filePath" type="string">

      Path where the file will be stored.

    </ResponseField>

    <ResponseField name="uploadUrl" type="string">

      URL for uploading the loyalty import file.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/_api/loyalty-imports/v1/loyalty-imports/wixmp-upload-url' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{}'
      ```

      ```json Response
      {
          "filePath": "/sites/dbba027b-3ad1-49dc-abdc-e1167a75290b/6099ebc5-0cce-4a2b-9903-2429c214d757.csv",
          "uploadUrl": "https://upload.wixmp.com/upload/example_eccI15IeIinubB3btjWY"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a URL for uploading a loyalty import file.

    This method is the first step in the loyalty import process. It returns a file path and an upload URL, which you'll use in subsequent steps of the import process.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { imports } from '@wix/loyalty';

    imports.createLoyaltyImportFileUrl(): Promise<CreateLoyaltyImportFileUrlResponse>
    ```

    ## Returns

    Returns `Promise<CreateLoyaltyImportFileUrlResponse>`.

    <ResponseField name="filePath" type="string">

      Path where the file will be stored.

    </ResponseField>

    <ResponseField name="uploadUrl" type="string">

      URL for uploading the loyalty import file.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-loyalty-import-file-url" id="ex-sdk-create-loyalty-import-file-url-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-loyalty-import-file-url" id="ex-sdk-create-loyalty-import-file-url-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-loyalty-import-file-url" id="ex-sdk-create-loyalty-import-file-url-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-loyalty-import-file-url-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-loyalty-import-file-url-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createLoyaltyImportFileUrl</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createLoyaltyImportFileUrl (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createLoyaltyImportFileUrl (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-loyalty-import-file-url-0"><span className="wix-picker-name">createLoyaltyImportFileUrl</span></label>
    <label htmlFor="ex-sdk-create-loyalty-import-file-url-1"><span className="wix-picker-name">createLoyaltyImportFileUrl (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-loyalty-import-file-url-2"><span className="wix-picker-name">createLoyaltyImportFileUrl (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createLoyaltyImportFileUrl
    import { imports } from '@wix/loyalty';

    async function createLoyaltyImportFileUrl() {
      const response = await imports.createLoyaltyImportFileUrl();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createLoyaltyImportFileUrl (self-hosted)
    import { createClient } from '@wix/sdk';
    import { imports } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { imports },
      // Include the auth strategy and host as relevant
    });

    async function createLoyaltyImportFileUrl() {
      const response = await myWixClient.imports.createLoyaltyImportFileUrl();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createLoyaltyImportFileUrl (with elevated permissions)
    import { imports } from '@wix/loyalty';
    import { auth } from '@wix/essentials';

    async function myCreateLoyaltyImportFileUrlMethod() {
      const elevatedCreateLoyaltyImportFileUrl = auth.elevate(imports.createLoyaltyImportFileUrl);
      const response = await elevatedCreateLoyaltyImportFileUrl();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
