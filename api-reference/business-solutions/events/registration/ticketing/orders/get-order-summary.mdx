---
title: "Get Order Summary"
description: "Retrieves a summary of total ticket sales."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a summary of total ticket sales.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-BASIC-ORDERS">Read Basic Events Order Info</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/events/v1/orders/summary
    ```

    ## Query Params

    <ParamField query="eventId" type="string">

      Event ID to which the order belongs.

    </ParamField>

    ## Response Object

    <ResponseField name="sales" type="TicketSales[]">

      Ticket sales grouped by currency.

      <Expandable title="sales properties">

        <ResponseField name="total" type="Money">

          Total balance of confirmed transactions.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string" deprecated>

              *Deprecated:** Use `value` instead.

              **Deprecated.** Constraints: `format DECIMAL_VALUE`.

            </ResponseField>

            <ResponseField name="currency" type="string">

              3-letter currency code in [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format. For example, `USD`.

              Constraints: `format CURRENCY`.

            </ResponseField>

            <ResponseField name="value" type="string">

              Monetary amount. Decimal string with a period as a decimal separator (e.g., 3.99). Optionally, starts with a single (-), to indicate that the amount is negative.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalOrders" type="integer">

          Total number of confirmed orders.

        </ResponseField>

        <ResponseField name="totalTickets" type="integer" deprecated>

          Deprecated: use the Ticket Definitions API instead. The `salesDetails` field on the
          ticket definition object (`wix.events.ticketdef.v3.TicketDefinition`) contains the required data;
          request it with the `SALES_DETAILS` fieldset.

          **Deprecated.** Deprecated: use the Ticket Definitions API instead. The `salesDetails` field on the
          ticket definition object (`wix.events.ticketdef.v3.TicketDefinition`) contains the required data;
          request it with the `SALES_DETAILS` fieldset. Use `TicketDefinition` instead. Removal: 2026-12-31.

        </ResponseField>

        <ResponseField name="revenue" type="Money">

          Total revenue, excluding fees (taxes and payment provider fees are not deducted).

          <Expandable title="revenue properties">

            <ResponseField name="amount" type="string" deprecated>

              *Deprecated:** Use `value` instead.

              **Deprecated.** Constraints: `format DECIMAL_VALUE`.

            </ResponseField>

            <ResponseField name="currency" type="string">

              3-letter currency code in [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format. For example, `USD`.

              Constraints: `format CURRENCY`.

            </ResponseField>

            <ResponseField name="value" type="string">

              Monetary amount. Decimal string with a period as a decimal separator (e.g., 3.99). Optionally, starts with a single (-), to indicate that the amount is negative.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET 'https://api.wix.com/events/v1/orders/summary?eventId=619d52c0-c93b-4155-a55c-108048f20b3f' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "sales": [
          {
            "total": {
              "amount": "14.00",
              "currency": "EUR"
            },
            "totalOrders": 4,
            "totalTickets": 8,
            "revenue": {
              "amount": "14.00",
              "currency": "EUR"
            }
          }
        ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a summary of total ticket sales.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-BASIC-ORDERS">Read Basic Events Order Info</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { orders } from '@wix/events';

    orders.getSummary(options): Promise<GetSummaryResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="GetSummaryOptions">

      An object representing the available options for retrieving a summary of total ticket sales.

      <Expandable title="options properties">

        <ParamField body="eventId" type="string">

          Event ID to which the order belongs.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<GetSummaryResponse>`.

    <ResponseField name="sales" type="TicketSales[]">

      Ticket sales grouped by currency.

      <Expandable title="sales properties">

        <ResponseField name="total" type="Money">

          Total balance of confirmed transactions.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string" deprecated>

              *Deprecated:** Use `value` instead.

              **Deprecated.** Constraints: `format DECIMAL_VALUE`.

            </ResponseField>

            <ResponseField name="currency" type="string">

              3-letter currency code in [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format. For example, `USD`.

              Constraints: `format CURRENCY`.

            </ResponseField>

            <ResponseField name="value" type="string">

              Monetary amount. Decimal string with a period as a decimal separator (e.g., 3.99). Optionally, starts with a single (-), to indicate that the amount is negative.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalOrders" type="integer">

          Total number of confirmed orders.

        </ResponseField>

        <ResponseField name="totalTickets" type="integer" deprecated>

          Deprecated: use the Ticket Definitions API instead. The `salesDetails` field on the
          ticket definition object (`wix.events.ticketdef.v3.TicketDefinition`) contains the required data;
          request it with the `SALES_DETAILS` fieldset.

          **Deprecated.** Deprecated: use the Ticket Definitions API instead. The `salesDetails` field on the
          ticket definition object (`wix.events.ticketdef.v3.TicketDefinition`) contains the required data;
          request it with the `SALES_DETAILS` fieldset. Use `TicketDefinition` instead. Removal: 2026-12-31.

        </ResponseField>

        <ResponseField name="revenue" type="Money">

          Total revenue, excluding fees (taxes and payment provider fees are not deducted).

          <Expandable title="revenue properties">

            <ResponseField name="amount" type="string" deprecated>

              *Deprecated:** Use `value` instead.

              **Deprecated.** Constraints: `format DECIMAL_VALUE`.

            </ResponseField>

            <ResponseField name="currency" type="string">

              3-letter currency code in [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format. For example, `USD`.

              Constraints: `format CURRENCY`.

            </ResponseField>

            <ResponseField name="value" type="string">

              Monetary amount. Decimal string with a period as a decimal separator (e.g., 3.99). Optionally, starts with a single (-), to indicate that the amount is negative.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-order-summary" id="ex-sdk-get-order-summary-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-order-summary" id="ex-sdk-get-order-summary-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-order-summary" id="ex-sdk-get-order-summary-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-order-summary-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-order-summary-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getSummary</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSummary (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSummary (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-order-summary-0"><span className="wix-picker-name">getSummary</span></label>
    <label htmlFor="ex-sdk-get-order-summary-1"><span className="wix-picker-name">getSummary (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-order-summary-2"><span className="wix-picker-name">getSummary (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getSummary
    import { orders } from '@wix/events';

    async function getSummary(options) {
      const response = await orders.getSummary(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSummary (self-hosted)
    import { createClient } from '@wix/sdk';
    import { orders } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { orders },
      // Include the auth strategy and host as relevant
    });

    async function getSummary(options) {
      const response = await myWixClient.orders.getSummary(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSummary (with elevated permissions)
    import { orders } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myGetSummaryMethod(options) {
      const elevatedGetSummary = auth.elevate(orders.getSummary);
      const response = await elevatedGetSummary(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
