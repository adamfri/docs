---
title: "Update Extended Fields"
description: "Updates extended fields of a question entry without incrementing revision."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates extended fields of a question entry without incrementing `revision`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LABS.MANAGE-FAQ">Manage FAQ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/faq/v2/question-entries/{id}/update-extended-fields
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the entity to update.

    </ParamField>

    ## Body Params

    <ParamField body="namespace" type="string" required>

      Identifier for the app whose extended fields are being updated.

    </ParamField>

    <ParamField body="namespaceData" type="object" required>

      Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured.

    </ParamField>

    ## Response Object

    <ResponseField name="questionEntry" type="QuestionEntry">

      Question entry with updated extended fields.

      See the [Question Entry Object](/api-reference/business-management/faq-app/question-entry-v2/question-entry-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request POST \
        --url https://www.wixapis.com/faq/v2/question-entries/a1a2b3c4-d5e6-7890-abcd-ef1234567890/update-extended-fields \
        --header 'Authorization: <AUTH>' \
        --header 'Content-Type: application/json' \
        --data '{
          "id": "a1a2b3c4-d5e6-7890-abcd-ef1234567890",
          "namespace": "custom.question.metadata",
          "namespaceData": {
            "priority": "high",
            "internal_notes": "Frequently asked shipping question",
            "last_reviewed": "2024-12-06"
          }
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "questionEntry": {
          "id": "a1a2b3c4-d5e6-7890-abcd-ef1234567890",
          "revision": "2",
          "createdDate": "2024-12-06T14:30:00.123Z",
          "updatedDate": "2024-12-06T14:30:00.123Z",
          "question": "How long does shipping take?",
          "draftjs": "{\"blocks\":[{\"key\":\"abc123\",\"text\":\"Standard shipping takes 3-5 business days. Express shipping is available and takes 1-2 business days.\",\"type\":\"unstyled\",\"depth\":0,\"inlineStyleRanges\":[],\"entityRanges\":[],\"data\":{}}],\"entityMap\":{}}",
          "labels": [
            {
              "title": "customer-service",
              "sortOrder": 1.0
            },
            {
              "title": "shipping",
              "sortOrder": 2.0
            }
          ],
          "sortOrder": 1.0,
          "categoryId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "slug": "how-long-does-shipping-take",
          "shareLink": "https://example.wixsite.com/faq/how-long-does-shipping-take",
          "extendedFields": {
            "namespaces": {
              "custom.question.metadata": {
                "priority": "high",
                "internal_notes": "Frequently asked shipping question",
                "last_reviewed": "2024-12-06"
              }
            }
          }
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates extended fields of a question entry without incrementing `revision`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LABS.MANAGE-FAQ">Manage FAQ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { questionEntry } from '@wix/faq';

    questionEntry.updateExtendedFields(_id, namespace, options): Promise<UpdateExtendedFieldsResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the entity to update.

    </ParamField>

    <ParamField body="namespace" type="string" required>

      Identifier for the app whose extended fields are being updated.

    </ParamField>

    <ParamField body="options" type="UpdateExtendedFieldsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="namespaceData" type="object" required>

          Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateExtendedFieldsResponse>`.

    <ResponseField name="questionEntry" type="QuestionEntry">

      Question entry with updated extended fields.

      See the [Question Entry Object](/api-reference/business-management/faq-app/question-entry-v2/question-entry-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-extended-fields" id="ex-sdk-update-extended-fields-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-extended-fields" id="ex-sdk-update-extended-fields-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-extended-fields" id="ex-sdk-update-extended-fields-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-extended-fields-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-extended-fields-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateExtendedFields</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateExtendedFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateExtendedFields (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-extended-fields-0"><span className="wix-picker-name">updateExtendedFields</span></label>
    <label htmlFor="ex-sdk-update-extended-fields-1"><span className="wix-picker-name">updateExtendedFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-extended-fields-2"><span className="wix-picker-name">updateExtendedFields (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateExtendedFields
    import { questionEntry } from '@wix/faq';

    async function updateExtendedFields(_id,namespace,options) {
      const response = await questionEntry.updateExtendedFields(_id,namespace,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateExtendedFields (self-hosted)
    import { createClient } from '@wix/sdk';
    import { questionEntry } from '@wix/faq';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { questionEntry },
      // Include the auth strategy and host as relevant
    });

    async function updateExtendedFields(_id,namespace,options) {
      const response = await myWixClient.questionEntry.updateExtendedFields(_id,namespace,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateExtendedFields (with elevated permissions)
    import { questionEntry } from '@wix/faq';
    import { auth } from '@wix/essentials';

    async function myUpdateExtendedFieldsMethod(_id,namespace,options) {
      const elevatedUpdateExtendedFields = auth.elevate(questionEntry.updateExtendedFields);
      const response = await elevatedUpdateExtendedFields(_id,namespace,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
