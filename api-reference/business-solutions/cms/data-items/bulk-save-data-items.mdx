---
title: "Bulk Save Data Items"
description: "Inserts or updates multiple items in a collection."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Inserts or updates multiple items in a collection.

    The Bulk Save Data Items method inserts or updates each item provided, depending on whether it already exists in the collection. For each item:

    + If you don't provide an ID, a new item is created.

    + If you provide an ID that doesn't exist in the collection, a new item is created with that ID.

    + If an item with the ID you provide already exists in the collection, that item is updated. When an item is updated, its `data._updatedDate` field is changed to the current date and time.

    <Note>

    When you provide an item with an ID that already exists in the collection, the item you provide completely replaces the existing item with that ID.
    This means that all of the item's previous fields and values are lost.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data/v2/bulk/items/save
    ```

    ## Body Params

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection in which to insert or update the items.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="dataItems" type="DataItem[]" required>

      Data items to insert or update.

      Constraints: `maxItems 1000`.

      See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the saved data items.
      When `true`, the `results` objects contain a `dataItem` field.

      Default: `false`

    </ParamField>

    <ParamField body="appOptions" type="object">

      Additional parameters specific to the [Wix app collection](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections) you are querying.

      When querying the Wix Stores [Products collection](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-e-commerce-stores/wix-stores-products-collection-fields), pass the following optional parameters:
      - `includeHiddenProducts`: Whether to include hidden products in the response. Default: `false`.
      - `includeVariants`: Whether to include product variants in the query. Default: `false`.

    </ParamField>

    <ParamField body="publishPluginOptions" type="PublishPluginOptions">

      Options for the Publish plugin.
      This plugin allows items in a [data collection](https://dev.wix.com/docs/rest/business-solutions/cms/data-collections/data-collection-object) to be marked as draft or published. Published items are visible to site visitors, while draft items are not.

      <Expandable title="publishPluginOptions properties">

        <ParamField body="includeDraftItems" type="boolean">

          Whether to include draft items.

          When `true`, both published and draft items are affected. Default: `false`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkDataItemResult[]">

      Information about the saved items.

      <Expandable title="results properties">

        <ResponseField name="action" type="string">

          Action attempted for the data item.

          **Supported values:**

          - `UNKNOWN_ACTION_TYPE`
          - `INSERT`
          - `UPDATE`
          - `DELETE`
          - `PATCH`

        </ResponseField>

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Does not appear if the item ID is missing. For example, when item creation fails.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Original index of the item in the array specified in the request. Use this to match items in the request with those in the response.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              In case of failure, the error details.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="dataItem" type="DataItem">

          Updated data item.

          Returned if `returnEntity` is set to `true` and the action is successful.

          See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Data Item Updated | `wix.data.v2.data_item_updated` | Triggered when a data item is updated or patched. |
    | Data Item Created | `wix.data.v2.data_item_created` | Triggered when a data item is inserted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-bulk-save-data-items" id="ex-rest-bulk-save-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-bulk-save-data-items" id="ex-rest-bulk-save-data-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-bulk-save-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-bulk-save-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk save new items by inserting first one and updating another</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Bulk save new items</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-bulk-save-data-items-0"><span className="wix-picker-name">Bulk save new items by inserting first one and updating another</span></label>
    <label htmlFor="ex-rest-bulk-save-data-items-1"><span className="wix-picker-name">Bulk save new items</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/bulk/items/save' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "cities",
          "dataItems": [
              {
                  "data": {
                      "state": "California",
                      "year": 2022,
                      "city": "Los Angeles",
                      "population": 3800000
                  }
              },
              {
                  "id": "6c38b4f7-7b8d-4702-9283-66a5889f8e17",
                  "data": {
                      "state": "California",
                      "year": 2022,
                      "city": "San Francisco",
                      "population": 840000
                  }
              }
          ],
          "returnEntity": true
      }'
      ```

      ```json Response
      {
          "results": [
              {
                  "action": "INSERT",
                  "itemMetadata": {
                      "id": "6d717171-6f4d-4794-b6ea-7aea0071a76b",
                      "originalIndex": 0,
                      "success": true
                  },
                  "dataItem": {
                      "id": "6d717171-6f4d-4794-b6ea-7aea0071a76b",
                      "dataCollectionId": "cities",
                      "data": {
                          "_id": "6d717171-6f4d-4794-b6ea-7aea0071a76b",
                          "_createdDate": { "$date": "2023-03-28T13:24:45.845Z" },
                          "_updatedDate": { "$date": "2023-03-28T13:24:45.845Z" },
                          "_owner": "690264f5-29a0-4aa8-a9b5-0842fc5ab360",
                          "city": "Los Angeles",
                          "state": "California",
                          "population": 3800000.0,
                          "year": 2022.0
                      }
                  }
              },
              {
                  "action": "UPDATE",
                  "itemMetadata": {
                      "id": "6c38b4f7-7b8d-4702-9283-66a5889f8e17",
                      "originalIndex": 1,
                      "success": true
                  },
                  "dataItem": {
                      "id": "6c38b4f7-7b8d-4702-9283-66a5889f8e17",
                      "dataCollectionId": "cities",
                      "data": {
                          "_id": "6c38b4f7-7b8d-4702-9283-66a5889f8e17",
                          "_createdDate": { "$date": "2023-03-28T12:20:43.745Z" },
                          "_updatedDate": { "$date": "2023-03-28T13:24:45.845Z" },
                          "_owner": "690264f5-29a0-4aa8-a9b5-0842fc5ab360",
                          "city": "San Francisco",
                          "state": "California",
                          "population": 840000.0,
                          "year": 2022.0
                      }
                  }
              }
          ],
          "bulkActionMetadata": {
              "totalSuccesses":2,
              "totalFailures": 0
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/bulk/items/save' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "cities",
          "dataItems": [
              {
                  "data": {
                      "state": "California",
                      "year": 2022,
                      "city": "Los Angeles",
                      "population": 3800000
                  }
              },
              {
                  "data": {
                      "state": "California",
                      "year": 2022,
                      "city": "San Francisco",
                      "population": 840000
                  }
              }
          ]
      }'
      ```

      ```json Response
      {
          "results": [
              {
                  "action": "INSERT",
                  "itemMetadata": {
                      "id": "aa019290-b546-4b3e-b302-8f6474baedf9",
                      "originalIndex": 0,
                      "success": true
                  }
              },
              {
                  "action": "INSERT",
                  "itemMetadata": {
                      "id": "39cbd938-7b42-4e33-8acf-ff1ce278664c",
                      "originalIndex": 1,
                      "success": true
                  }
              }
          ],
          "bulkActionMetadata": {
              "totalSuccesses": 2,
              "totalFailures": 0
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Inserts or updates multiple items in a collection.

    The Bulk Save Data Items method inserts or updates each item provided, depending on whether it already exists in the collection. For each item:

    + If you don't provide an ID, a new item is created.

    + If you provide an ID that doesn't exist in the collection, a new item is created with that ID.

    + If an item with the ID you provide already exists in the collection, that item is updated. When an item is updated, its `data._updatedDate` field is changed to the current date and time.

    <Note>

    When you provide an item with an ID that already exists in the collection, the item you provide completely replaces the existing item with that ID.
    This means that all of the item's previous fields and values are lost.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/data';

    items.bulkSave(dataCollectionId, items, options): Promise
    ```

    ## Method Parameters

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection in which to insert or update the items.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="items" type="WixDataItem[]" required>

      Data items to insert or update.

      See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="WixDataBulkSaveOptions">

      Options to use when processing this operation.

      <Expandable title="options properties">

        <ParamField body="suppressHooks" type="boolean">

          Whether to suppress data hooks.
          When `true`, data hooks typically triggered by this endpoint don't run.

          **Note:** This option can only be used in code that runs in [the Wix site backend](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/backend-code/about-the-site-backend).

          Default: `false`

        </ParamField>

        <ParamField body="appOptions" type="object">

          Additional parameters specific to the [Wix app collection](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections) you are querying.

          When querying the Wix Stores [Products collection](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-e-commerce-stores/wix-stores-products-collection-fields), pass the following optional parameters:
          - `includeHiddenProducts`: Whether to include hidden products in the response. Default: `false`.
          - `includeVariants`: Whether to include product variants in the query. Default: `false`.

        </ParamField>

        <ParamField body="saveReferences" type="boolean">

          Whether to create or replace references from multi-reference fields.
          When `true`, array values in multi-reference fields are stored as references.
          For new items, references are created. For existing items, existing references are replaced
          with the provided values.
          Multi-reference field values must be arrays of referenced item IDs (strings)
          or referenced items containing an `_id` field.

          Default: `false`

        </ParamField>

        <ParamField body="showDrafts" type="boolean">

          When `true`, operations include draft items. Read operations include draft items in their response, and write operations modify draft items.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="inserted" type="integer">

      Number of inserted items.

    </ResponseField>

    <ResponseField name="updated" type="integer">

      Number of updated items.

    </ResponseField>

    <ResponseField name="removed" type="integer">

      Number of removed items.

    </ResponseField>

    <ResponseField name="skipped" type="integer">

      Number of skipped items.

    </ResponseField>

    <ResponseField name="errors" type="WixDataBulkError[]">

      List of errors.

      <Expandable title="errors properties">

        <ResponseField name="message" type="string">

          Description of the error.

        </ResponseField>

        <ResponseField name="code" type="string">

          Error code.

        </ResponseField>

        <ResponseField name="originalIndex" type="integer">

          Index of the item within the request array. Allows for correlation between request and response items.

        </ResponseField>

        <ResponseField name="item" type="string">

          Failed item (or ID in case of `bulkRemove()`).

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="insertedItemIds" type="string[]">

      List of IDs of inserted items.

    </ResponseField>

    <ResponseField name="updatedItemIds" type="string[]">

      List of IDs of updated items.

    </ResponseField>

    <ResponseField name="removedItemIds" type="string[]">

      List of IDs of removed items.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Data Item Updated | `wix.data.v2.data_item_updated` | Triggered when a data item is updated or patched. |
    | Data Item Created | `wix.data.v2.data_item_created` | Triggered when a data item is inserted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-save-data-items" id="ex-sdk-bulk-save-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-save-data-items" id="ex-sdk-bulk-save-data-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-save-data-items" id="ex-sdk-bulk-save-data-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-save-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-save-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkSave</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkSave (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkSave (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-save-data-items-0"><span className="wix-picker-name">bulkSave</span></label>
    <label htmlFor="ex-sdk-bulk-save-data-items-1"><span className="wix-picker-name">bulkSave (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-save-data-items-2"><span className="wix-picker-name">bulkSave (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkSave
    import { items } from '@wix/data';

    async function bulkSave(dataCollectionId,items,options) {
      const response = await items.bulkSave(dataCollectionId,items,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkSave (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function bulkSave(dataCollectionId,items,options) {
      const response = await myWixClient.items.bulkSave(dataCollectionId,items,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkSave (with elevated permissions)
    import { items } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myBulkSaveMethod(dataCollectionId,items,options) {
      const elevatedBulkSave = auth.elevate(items.bulkSave);
      const response = await elevatedBulkSave(dataCollectionId,items,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
