---
title: "Publish Draft"
description: "Publishes a draft schedule."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Publishes a draft schedule.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/schedule/publish
    ```

    ## Body Params

    <ParamField body="eventId" type="string" required>

      Event ID to which the draft schedule item belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    Returns `PublishDraftResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/events/v1/agenda/publish' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Publishes a draft schedule.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { schedule } from '@wix/events';

    schedule.publishDraft(eventId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="eventId" type="string" required>

      Event ID to which the draft schedule item belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-publish-draft" id="ex-sdk-publish-draft-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-publish-draft" id="ex-sdk-publish-draft-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-publish-draft" id="ex-sdk-publish-draft-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-publish-draft-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-publish-draft-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">publishDraft</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">publishDraft (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">publishDraft (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-publish-draft-0"><span className="wix-picker-name">publishDraft</span></label>
    <label htmlFor="ex-sdk-publish-draft-1"><span className="wix-picker-name">publishDraft (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-publish-draft-2"><span className="wix-picker-name">publishDraft (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript publishDraft
    import { schedule } from '@wix/events';

    async function publishDraft(eventId) {
      const response = await schedule.publishDraft(eventId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript publishDraft (self-hosted)
    import { createClient } from '@wix/sdk';
    import { schedule } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { schedule },
      // Include the auth strategy and host as relevant
    });

    async function publishDraft(eventId) {
      const response = await myWixClient.schedule.publishDraft(eventId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript publishDraft (with elevated permissions)
    import { schedule } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myPublishDraftMethod(eventId) {
      const elevatedPublishDraft = auth.elevate(schedule.publishDraft);
      const response = await elevatedPublishDraft(eventId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
