---
title: "Count Modifier Groups"
description: "Note: The Item API only works with the Wix Restaurants Menus app."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Retrieves the number of modifier groups that match a specified filter.

    If a filter isn't passed in the request, the endpoint returns the count of all modifier groups.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants/menus/v1/modifier-groups/count
    ```

    ## Body Params

    <ParamField body="filter" type="object">

      Filter for counting modifier groups.

    </ParamField>

    ## Response Object

    <ResponseField name="count" type="integer">

      Counted modifier groups.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `TOO_MANY_TO_COUNT` | Number of modifier groups exceeds the server's limit. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Retrieves the number of modifier groups that match a specified filter.

    If a filter isn't passed in the request, the endpoint returns the count of all modifier groups.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { itemModifierGroups } from '@wix/restaurants';

    itemModifierGroups.countModifierGroups(options): Promise<CountModifierGroupsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="CountModifierGroupsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="filter" type="object">

          Filter for counting modifier groups.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CountModifierGroupsResponse>`.

    <ResponseField name="count" type="integer">

      Counted modifier groups.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `TOO_MANY_TO_COUNT` | Number of modifier groups exceeds the server's limit. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-count-modifier-groups" id="ex-sdk-count-modifier-groups-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-count-modifier-groups" id="ex-sdk-count-modifier-groups-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-count-modifier-groups-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-count-modifier-groups-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">countModifierGroups</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">countModifierGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-count-modifier-groups-0"><span className="wix-picker-name">countModifierGroups</span></label>
    <label htmlFor="ex-sdk-count-modifier-groups-1"><span className="wix-picker-name">countModifierGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript countModifierGroups
    import { itemModifierGroups } from '@wix/restaurants';

    async function countModifierGroups(options) {
      const response = await itemModifierGroups.countModifierGroups(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript countModifierGroups (self-hosted)
    import { createClient } from '@wix/sdk';
    import { itemModifierGroups } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { itemModifierGroups },
      // Include the auth strategy and host as relevant
    });

    async function countModifierGroups(options) {
      const response = await myWixClient.itemModifierGroups.countModifierGroups(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
