---
title: "Bulk Adjust Points"
description: "Updates the points balance of multiple accounts."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Updates the points balance of multiple accounts.

    You can adjust the balance in two ways:
    - `balance` allows you to set the total points balance to this new amount. The transaction `type` will return as `"ADJUST"`.
    - `amount` allows you to adjust the points balance by this amount. This amount can be a positive number to increase the points balance or a negative number to decrease the balance. The transaction `type` will return as `"GIVE"`.

    You can use the job ID from the response to call [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job)
    to retrieve the job details and metadata.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/loyalty-accounts/v1/accounts/bulk-adjust
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      search

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation to perform. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.
              - `GEOTILE_GRID` — Groups geo point values into buckets that represent map tiles at a given precision, for map clustering.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="groupBy" type="GroupByAggregation" deprecated>

              Deprecated. Use `nested` instead.

              **Deprecated.** Deprecated. Use `nested` instead. Use `nested` instead. Removal: 2024-03-30.

              _Nested structure omitted for brevity — see the `GroupByAggregation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least one of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
          Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="asyncJobId" type="string">

      ID of the job.

      Use this ID to call [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job)
      to retrieve the job details and metadata.

      **Read-only.**

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the points balance of multiple accounts.

    You can adjust the balance in two ways:
    - `balance` allows you to set the total points balance to this new amount. The transaction `type` will return as `"ADJUST"`.
    - `amount` allows you to adjust the points balance by this amount. This amount can be a positive number to increase the points balance or a negative number to decrease the balance. The transaction `type` will return as `"GIVE"`.

    You can use the job ID from the response to call [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job)
    to retrieve the job details and metadata.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { accounts } from '@wix/loyalty';

    accounts.bulkAdjustPoints(options): Promise<BulkAdjustPointsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkAdjustPointsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="search" type="CursorSearch">

          search

          <Expandable title="search properties">

            <ParamField body="filter" type="object">

              Filter object.

              Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              List of sort objects.

              Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

              Constraints: `maxItems 10`.

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="aggregations" type="Aggregation[]">

              Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

              Constraints: `maxItems 10`.

              _Nested structure omitted for brevity — see the `Aggregation[]` object._

            </ParamField>

            <ParamField body="search" type="SearchDetails">

              Free text to match in searchable fields.

              _Nested structure omitted for brevity — see the `SearchDetails` object._

            </ParamField>

            <ParamField body="timeZone" type="string">

              Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
              Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="pagingMethod" type="pagingMethod">

              pagingMethod

              _Nested structure omitted for brevity — see the `pagingMethod` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="type" type="type">

          type

          <Expandable title="type properties">

            <ParamField body="balance" type="integer">

              Constraints: `min 0`, `max 999999999`.

            </ParamField>

            <ParamField body="amount" type="integer">

              Constraints: `min -9999999`, `max 9999999`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkAdjustPointsResponse>`.

    <ResponseField name="asyncJobId" type="string">

      ID of the job.

      Use this ID to call [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job)
      to retrieve the job details and metadata.

      **Read-only.**

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-adjust-points" id="ex-sdk-bulk-adjust-points-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-adjust-points" id="ex-sdk-bulk-adjust-points-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-adjust-points" id="ex-sdk-bulk-adjust-points-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-adjust-points-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-adjust-points-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkAdjustPoints</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkAdjustPoints (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkAdjustPoints (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-adjust-points-0"><span className="wix-picker-name">bulkAdjustPoints</span></label>
    <label htmlFor="ex-sdk-bulk-adjust-points-1"><span className="wix-picker-name">bulkAdjustPoints (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-adjust-points-2"><span className="wix-picker-name">bulkAdjustPoints (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkAdjustPoints
    import { accounts } from '@wix/loyalty';

    async function bulkAdjustPoints(options) {
      const response = await accounts.bulkAdjustPoints(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkAdjustPoints (self-hosted)
    import { createClient } from '@wix/sdk';
    import { accounts } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { accounts },
      // Include the auth strategy and host as relevant
    });

    async function bulkAdjustPoints(options) {
      const response = await myWixClient.accounts.bulkAdjustPoints(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkAdjustPoints (with elevated permissions)
    import { accounts } from '@wix/loyalty';
    import { auth } from '@wix/essentials';

    async function myBulkAdjustPointsMethod(options) {
      const elevatedBulkAdjustPoints = auth.elevate(accounts.bulkAdjustPoints);
      const response = await elevatedBulkAdjustPoints(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
