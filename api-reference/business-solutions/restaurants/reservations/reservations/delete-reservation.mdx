---
title: "Delete Reservation"
description: "Deletes a reservation."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a reservation. Only reservations with the `HELD` status can be deleted.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/table-reservations/reservations/v1/reservations/{reservationId}
    ```

    ## Path Params

    <ParamField path="reservationId" type="string" required>

      Reservation ID.

    </ParamField>

    ## Response Object

    Returns `DeleteReservationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reservation Deleted | `wix.table_reservations.v1.reservation_deleted` | Triggered when a reservation is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE 'https://www.wixapis.com/table-reservations/reservations/v1\
          /reservations/0310d65b-7af2-4f06-b139-38c01255d2ae' \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a reservation. Only reservations with the `HELD` status can be deleted.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { reservations } from '@wix/table-reservations';

    reservations.deleteReservation(reservationId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="reservationId" type="string" required>

      Reservation ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reservation Deleted | `wix.table_reservations.v1.reservation_deleted` | Triggered when a reservation is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-reservation" id="ex-sdk-delete-reservation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-reservation" id="ex-sdk-delete-reservation-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-reservation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-reservation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteReservation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-reservation-0"><span className="wix-picker-name">deleteReservation</span></label>
    <label htmlFor="ex-sdk-delete-reservation-1"><span className="wix-picker-name">deleteReservation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteReservation
    import { reservations } from '@wix/table-reservations';

    async function deleteReservation(reservationId) {
      const response = await reservations.deleteReservation(reservationId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReservation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reservations } from '@wix/table-reservations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reservations },
      // Include the auth strategy and host as relevant
    });

    async function deleteReservation(reservationId) {
      const response = await myWixClient.reservations.deleteReservation(reservationId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
