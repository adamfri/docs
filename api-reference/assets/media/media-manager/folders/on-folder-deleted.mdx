---
title: "onFolderDeleted"
description: "Triggered when a folder is deleted."
icon: "bolt"
---

Triggered when a folder is deleted.

If the `movedToTrash` property in the event object is `true`, the folder was moved to the Media Manager's trash bin. If the `movedToTrash` property in the event object is `false`, the folder was permanently deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.media.site_media.v1.folder_deleted` |
| Package | `@wix/media` |
| Permissions | `Manage Bookings Services and Settings`, `Manage Events`, `Manage Challenges`, `Read Media Manager`, `Manage Media Manager`, `Manage Portfolio`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onFolderDeleted
  import { folders } from '@wix/media';

  folders.onFolderDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onFolderDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { folders } from '@wix/media';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        folders,
      },
  });

  wixClient.folders.onFolderDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
