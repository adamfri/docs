---
title: "Query Rules"
description: "Retrieves a list of rules, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of rules, given the provided paging, filtering, and sorting.

    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    Up to 1,000 rules can be returned per request.

    To learn about working with *Query* endpoints, see [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in` | None |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `namespace` | `$eq`, `$ne`, `$in` | None |
    | `enabled` | `$eq`, `$ne` | None |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MODERATION.MANAGE-MODERATION-RULES">Manage Moderation Rules</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/moderation/v1/rules/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="rules" type="Rule[]">

      List of rules.

      Constraints: `maxItems 100`.

      See the [Rule Object](/api-reference/crm/community/feedback-moderation/moderation-rules/rule-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-query-rules" id="ex-rest-query-rules-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-query-rules" id="ex-rest-query-rules-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-query-rules-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-query-rules-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query rules by cursor</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query rules by stores reviews</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-query-rules-0"><span className="wix-picker-name">Query rules by cursor</span></label>
    <label htmlFor="ex-rest-query-rules-1"><span className="wix-picker-name">Query rules by stores reviews</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/moderation/v1/rules/query  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
                "query": {
                  "cursorPaging": {
                    "limit": 1,
                    "cursor": "79902130cbb60b4db8ae6b7f0020c04e411ee19b.IiQ1MDBlYTYyYy01ZjRjLTRhNTEtYjgxMy0wYmQ3ZjIzOWM2ZGQ"
                  }
                }
              }'
      ```

      ```json Response
      {
        "rules": [
          {
            "id": "500ea62c-5f4c-4a51-b813-0bd7f239c6dd",
            "revision": "1",
            "createdDate": "2023-10-10T06:52:04.718Z",
            "updatedDate": "2023-10-10T06:52:04.718Z",
            "namespace": "reviews/stores",
            "name": "Check for Rating",
            "audience": {
              "type": "MEMBERS_AND_VISITORS"
            },
            "trigger": {
              "type": "ATTRIBUTE",
              "attributeOptions": {
                "name": "rating",
                "values": ["1", "2"]
              }
            },
            "exemptions": {
              "memberGroups": [],
              "memberIds": []
            },
            "action": {
              "type": "NEEDS_MANUAL_APPROVAL"
            },
            "enabled": true
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {
            "next": null,
            "prev": "b58d8746f02766409f1e6861ea4672bf0d2cd0c4.CAEiJGIyZDRiMjBhLTZiNzUtNDg3Zi04YWMzLWVmYWU0ZWMzODFjZA"
          },
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/moderation/v1/rules/query  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
                "query": {
                  "sort": [],
                  "filter": {
                    "namespace": "reviews/stores"
                  },
                  "cursorPaging": {
                    "limit": 1
                  }
                }
              }'
      ```

      ```json Response
      {
        "rules": [
          {
            "id": "500ea62c-5f4c-4a51-b813-0bd7f239c6dd",
            "revision": "1",
            "createdDate": "2023-10-10T06:52:04.718Z",
            "updatedDate": "2023-10-10T06:52:04.718Z",
            "namespace": "reviews/stores",
            "name": "Check for Rating",
            "audience": {
              "type": "MEMBERS_AND_VISITORS"
            },
            "trigger": {
              "type": "ATTRIBUTE",
              "attributeOptions": {
                "name": "rating",
                "values": ["1", "2"]
              }
            },
            "exemptions": {
              "memberGroups": [],
              "memberIds": []
            },
            "action": {
              "type": "NEEDS_MANUAL_APPROVAL"
            },
            "enabled": true
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {
            "next": "79902130cbb60b4db8ae6b7f0020c04e411ee19b.IiQ1MDBlYTYyYy01ZjRjLTRhNTEtYjgxMy0wYmQ3ZjIzOWM2ZGQ",
            "prev": null
          },
          "hasNext": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of rules, given the provided paging, filtering, and sorting.

    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    Up to 1,000 rules can be returned per request.

    To learn about working with *Query* endpoints, see [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in` | None |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `namespace` | `$eq`, `$ne`, `$in` | None |
    | `enabled` | `$eq`, `$ne` | None |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MODERATION.MANAGE-MODERATION-RULES">Manage Moderation Rules</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { moderationRules } from '@wix/moderation-rules';

    moderationRules.queryRules(query): RulesQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="RuleQuery" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `RulesQueryBuilder`.

    <ResponseField name="rules" type="Rule[]">

      List of rules.

      Constraints: `maxItems 100`.

      See the [Rule Object](/api-reference/crm/community/feedback-moderation/moderation-rules/rule-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-rules" id="ex-sdk-query-rules-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-rules" id="ex-sdk-query-rules-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-rules" id="ex-sdk-query-rules-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-rules-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-rules-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryRules</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryRules (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryRules (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-rules-0"><span className="wix-picker-name">queryRules</span></label>
    <label htmlFor="ex-sdk-query-rules-1"><span className="wix-picker-name">queryRules (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-rules-2"><span className="wix-picker-name">queryRules (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryRules
    import { moderationRules } from '@wix/moderation-rules';

    async function queryRules(query) {
      const response = await moderationRules.queryRules(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryRules (self-hosted)
    import { createClient } from '@wix/sdk';
    import { moderationRules } from '@wix/moderation-rules';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { moderationRules },
      // Include the auth strategy and host as relevant
    });

    async function queryRules(query) {
      const response = await myWixClient.moderationRules.queryRules(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryRules (with elevated permissions)
    import { moderationRules } from '@wix/moderation-rules';
    import { auth } from '@wix/essentials';

    async function myQueryRulesMethod(query) {
      const elevatedQueryRules = auth.elevate(moderationRules.queryRules);
      const response = await elevatedQueryRules(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
