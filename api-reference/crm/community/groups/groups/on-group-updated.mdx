---
title: "onGroupUpdated"
description: "Triggered when a group is updated."
icon: "bolt"
---

Triggered when a group is updated. See payload for more information about the specific trigger.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_updated` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupUpdated
  import { groups } from '@wix/groups';

  groups.onGroupUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { groups } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        groups,
      },
  });

  wixClient.groups.onGroupUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
