---
title: "Query Groups"
description: "Retrieves up to 100 groups, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 groups, given the provided paging, filtering, and sorting.

    Supported fields for filtering:
    - `title`

    Supported fields for sorting:
    - `title`
    - `createdDate`
    - `membersCount`
    - `recentActivityDate`

    For groups with `group.privacyStatus` set to `SECRET`, only group admins and group members can see the group and its content.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/social-groups-proxy/groups/v2/groups/query
    ```

    ## Body Params

    <ParamField body="query" type="Query">

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="any">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paging" type="Paging">

          Paging options to limit and skip the number of items.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="contentType" type="string">

      Content type.

      **Supported values:**

      - `PLAIN_TEXT`
      - `DRAFTJS`
      - `RICH_CONTENT`

    </ParamField>

    ## Response Object

    <ResponseField name="groups" type="Group[]">

      Retrieved groups.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
          https://www.wixapis.com/social-groups-proxy/groups/v2/groups/query \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
                  "query": {
                      "filter": {
                          "title": {
                              "$contains": "Test"
                          }
                      },
                      "sort": [
                          {
                              "fieldName": "createdDate",
                              "order": "DESC"
                          }
                      ],
                      "paging": {
                          "offset": 10,
                          "limit": 15
                      }
                  }
              }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "groups": [
              {
                  "id": "8a258cf5-49f3-41dc-b320-65ec2598315b",
                  "slug": "group-1",
                  "privacyStatus": "PUBLIC",
                  "name": "Group 1",
                  "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
                  "memberTitle": "Friends",
                  "coverImage": {
                      "image": {
                          "mediaId": "4d6eab_666134ee68464d76aa838619344c4369~mv2.jpg",
                          "width": 1024,
                          "height": 150
                      }
                  },
                  "settings": {
                      "membersCanInvite": false,
                      "membersCanApprove": false,
                      "welcomeMemberPostEnabled": true,
                      "groupDetailsChangedPostEnabled": true,
                      "showMemberList": true
                  },
                  "membersCount": 1,
                  "ownerId": "f98e4407-15bf-43a3-87fb-a954c49bb20f",
                  "createdDate": "2020-11-06T14:03:50Z",
                  "updatedDate": "2020-11-19T12:38:07Z",
                  "lastActivityDate": "2020-11-19T12:38:08Z"
              },
              {
                  "id": "1de57bda-c03e-4367-91fb-7c334d6a613d",
                  "slug": "group-2",
                  "privacyStatus": "PUBLIC",
                  "name": "Group 2",
                  "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
                  "memberTitle": "Members",
                  "coverImage": {
                      "image": {
                          "mediaId": "4d6eab_666134ee68464d76aa838619344c4369~mv2.jpg",
                          "width": 1024,
                          "height": 150
                      }
                  },
                  "settings": {
                      "membersCanInvite": false,
                      "membersCanApprove": false,
                      "welcomeMemberPostEnabled": true,
                      "groupDetailsChangedPostEnabled": true,
                      "showMemberList": true
                  },
                  "membersCount": 2,
                  "ownerId": "6369e045-763d-4876-8220-91ee3f0381c0",
                  "createdDate": "2020-11-06T14:03:50Z",
                  "updatedDate": "2020-11-19T12:38:07Z",
                  "lastActivityDate": "2020-11-19T12:38:08Z"
              }
          ],
          "metadata": {
              "count": 2,
              "offset": 10,
              "total": 12
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 groups, given the provided paging, filtering, and sorting.

    Supported fields for filtering:
    - `title`

    Supported fields for sorting:
    - `title`
    - `createdDate`
    - `membersCount`
    - `recentActivityDate`

    For groups with `group.privacyStatus` set to `SECRET`, only group admins and group members can see the group and its content.

    ## Method Declaration

    ```typescript
    import { groups } from '@wix/groups';

    groups.queryGroups(options): GroupsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="options" type="QueryGroupsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="Query">

          Query options.

          <Expandable title="query properties">

            <ParamField body="filter" type="any">

              Filter object in the following format:
              `"filter" : {`
              "fieldName1": "value1",
              "fieldName2":&#123;"$operator":"value2"&#125;
              &#125;``
              Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object in the following format:
              `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="fields" type="string[]">

              Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

            <ParamField body="fieldsets" type="string[]">

              Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="contentType" type="string">

          Content type.

          **Supported values:**

          - `PLAIN_TEXT`
          - `DRAFTJS`
          - `RICH_CONTENT`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `GroupsQueryBuilder`.

    <ResponseField name="groups" type="Group[]">

      Retrieved groups.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-groups" id="ex-sdk-query-groups-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-groups" id="ex-sdk-query-groups-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-groups" id="ex-sdk-query-groups-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-groups-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-groups-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryGroups</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryGroups (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-groups-0"><span className="wix-picker-name">queryGroups</span></label>
    <label htmlFor="ex-sdk-query-groups-1"><span className="wix-picker-name">queryGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-groups-2"><span className="wix-picker-name">queryGroups (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryGroups
    import { groups } from '@wix/groups';

    async function queryGroups(options) {
      const response = await groups.queryGroups(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryGroups (self-hosted)
    import { createClient } from '@wix/sdk';
    import { groups } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { groups },
      // Include the auth strategy and host as relevant
    });

    async function queryGroups(options) {
      const response = await myWixClient.groups.queryGroups(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryGroups (with elevated permissions)
    import { groups } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myQueryGroupsMethod(options) {
      const elevatedQueryGroups = auth.elevate(groups.queryGroups);
      const response = await elevatedQueryGroups(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
