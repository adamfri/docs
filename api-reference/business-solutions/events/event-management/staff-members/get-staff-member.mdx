---
title: "Get Staff Member"
description: "Retrieves a staff member."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a staff member.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/events/v1/staff-members/{staffMemberId}
    ```

    ## Path Params

    <ParamField path="staffMemberId" type="string" required>

      ID of the staff member to retrieve.

    </ParamField>

    ## Query Params

    <ParamField query="fieldsets" type="string[]">

      List of additional fields to include in the response.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `LOGIN_LINK` — Returns the `loginLink` field.

    </ParamField>

    ## Response Object

    <ResponseField name="staffMember" type="StaffMember">

      The requested staff member.

      See the [Staff Member Object](/api-reference/business-solutions/events/event-management/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET 'https://www.wixapis.com/events/v1/staff-members/7ecacdb4-a6b6-4b83-89c8-171d26b14666?fieldsets=LOGIN_LINK' \
      -H 'Authorization: <AUTH TOKEN>' \
      ```

      ```json Response
      {
        "staffMember": {
          "id": "7ecacdb4-a6b6-4b83-89c8-171d26b14666",
          "revision": "1",
          "createdDate": "2025-01-08T11:43:07.181Z",
          "updatedDate": "2025-01-08T11:43:07.181Z",
          "name": "John Doe",
          "scopes": [
            "CHECK_IN_GUESTS"
          ],
          "allevents": {},
          "status": "PENDING",
          "extendedFields": null,
          "permissions": [
            "CHECK_IN_GUESTS"
          ],
          "loginLink": "https://events.wix.app/events-platform/af442a8f-c574-47d3-9222-87cd7ef6520e/staff/home/JWS.eyJraWQiOiIwdnJsTzJxbiIsImFsZyI6IkhTMjU2In0.eyJkYXRhIjoie1wiaWRcIjpcIjdlY2FjZGI0LWE2YjYtNGI4My04OWM4LTE3MWQyNmIxNDY2NlwifSIsImlhdCI6MTczNjMzNjU4NywiZXhwIjoxNzY3ODcyNTg3fQ.y0ALZjnGG3KcSeAbDVlMhnazOVUqAufqyQ0XBJQGxlg"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a staff member.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { staffMembers } from '@wix/events';

    staffMembers.getStaffMember(staffMemberId, options): Promise<StaffMember>
    ```

    ## Method Parameters

    <ParamField body="staffMemberId" type="string" required>

      ID of the staff member to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="GetStaffMemberOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fieldsets" type="string[]">

          List of additional fields to include in the response.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `LOGIN_LINK` — Returns the `loginLink` field.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<StaffMember>`.

    See the [Staff Member Object](/api-reference/business-solutions/events/event-management/staff-members/staff-member-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-staff-member" id="ex-sdk-get-staff-member-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-staff-member" id="ex-sdk-get-staff-member-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-staff-member" id="ex-sdk-get-staff-member-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-staff-member-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-staff-member-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getStaffMember</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getStaffMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getStaffMember (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-staff-member-0"><span className="wix-picker-name">getStaffMember</span></label>
    <label htmlFor="ex-sdk-get-staff-member-1"><span className="wix-picker-name">getStaffMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-staff-member-2"><span className="wix-picker-name">getStaffMember (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getStaffMember
    import { staffMembers } from '@wix/events';

    async function getStaffMember(staffMemberId,options) {
      const response = await staffMembers.getStaffMember(staffMemberId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getStaffMember (self-hosted)
    import { createClient } from '@wix/sdk';
    import { staffMembers } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { staffMembers },
      // Include the auth strategy and host as relevant
    });

    async function getStaffMember(staffMemberId,options) {
      const response = await myWixClient.staffMembers.getStaffMember(staffMemberId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getStaffMember (with elevated permissions)
    import { staffMembers } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myGetStaffMemberMethod(staffMemberId,options) {
      const elevatedGetStaffMember = auth.elevate(staffMembers.getStaffMember);
      const response = await elevatedGetStaffMember(staffMemberId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
