---
title: "Delete Program"
description: "Deletes a program."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a program.

    Deleting a program permanently removes it from the program list, cancels its
    scheduled end task, removes related member groups, and removes it from search.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/online-programs/v3/programs/{programId}
    ```

    ## Path Params

    <ParamField path="programId" type="string" required>

      ID of the program to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteProgramResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Permanently deletes a program.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/online-programs/v3/programs/8f6f3f3d-2c3b-4f8c-9e4a-1a5d2e0f7c11' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a program.

    Deleting a program permanently removes it from the program list, cancels its
    scheduled end task, removes related member groups, and removes it from search.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/online-programs';

    programs.deleteProgram(programId): Promise
    ```

    ## Method Parameters

    <ParamField body="programId" type="string" required>

      ID of the program to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteProgramResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Deleted | `wix.online_programs.v3.program_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-program" id="ex-sdk-delete-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-program" id="ex-sdk-delete-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-program" id="ex-sdk-delete-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteProgram</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteProgram (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-program-0"><span className="wix-picker-name">deleteProgram</span></label>
    <label htmlFor="ex-sdk-delete-program-1"><span className="wix-picker-name">deleteProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-program-2"><span className="wix-picker-name">deleteProgram (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteProgram
    import { programs } from '@wix/online-programs';

    async function deleteProgram(programId) {
      const response = await programs.deleteProgram(programId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function deleteProgram(programId) {
      const response = await myWixClient.programs.deleteProgram(programId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteProgram (with elevated permissions)
    import { programs } from '@wix/online-programs';
    import { auth } from '@wix/essentials';

    async function myDeleteProgramMethod(programId) {
      const elevatedDeleteProgram = auth.elevate(programs.deleteProgram);
      const response = await elevatedDeleteProgram(programId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
