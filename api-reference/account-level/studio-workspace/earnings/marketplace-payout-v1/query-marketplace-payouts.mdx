---
title: "Query Marketplace Payouts"
description: "Retrieves a list of up to 100 marketplace payouts, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 marketplace payouts, given the provided paging, filtering, and sorting.

    Query Marketplace Payouts runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `amount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionAmount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `paymentSharePercent` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `listing` | `$exists` | Sort: `ASC`, `DESC` |
    | `listing.id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `listing.type` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `invoiceId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.REVSHARE">SCOPE.PARTNERS.REVSHARE</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/partners/marketplace/v1/marketplace-payouts/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="marketplacePayouts" type="MarketplacePayout[]">

      Retrieved list of marketplace payouts.

      See the [Marketplace Payout Object](/api-reference/account-level/studio-workspace/earnings/marketplace-payout-v1/marketplace-payout-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Queries a partner's marketplace payouts for a date range, sorted by transaction date

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/partners/marketplace/v1/marketplace-payouts/query' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "query": {
            "filter": {
              "transactionDate": {
                "$gte": "2026-05-01T00:00:00.000Z",
                "$lt": "2026-06-01T00:00:00.000Z"
              }
            },
            "sort": [
              { "fieldName": "transactionDate", "order": "DESC" }
            ],
            "cursorPaging": { "limit": 100 }
          }
        }'
      ```

      ```json Response
      {
        "marketplacePayouts": [
          {
            "id": "3f1c8e2a-9b47-4d1e-8a6f-2c5b7e9d0a14",
            "revision": "1",
            "createdDate": "2026-05-14T08:21:33.512Z",
            "updatedDate": "2026-05-14T08:21:33.512Z",
            "listing": {
              "id": "a1c2e3f4-5b6d-4789-9012-3456789abcde",
              "type": "TEMPLATE"
            },
            "transactionAmount": 79,
            "paymentSharePercent": 100,
            "amount": 79,
            "transactionDate": "2026-05-14T08:21:30.000Z",
            "invoiceId": "",
            "status": "APPROVED",
            "negative": false
          },
          {
            "id": "c9e2a1b0-7d34-4e62-8f51-0a9b3c6d2e75",
            "revision": "1",
            "createdDate": "2026-05-09T15:02:11.004Z",
            "updatedDate": "2026-05-09T15:02:11.004Z",
            "listing": {
              "id": "f4e3d2c1-6b5a-4987-8210-fedcba987654",
              "type": "TEMPLATE"
            },
            "transactionAmount": 120,
            "paymentSharePercent": 100,
            "amount": 120,
            "transactionDate": "2026-05-09T15:02:08.000Z",
            "invoiceId": "INV-2026-05-0042",
            "status": "APPROVED",
            "negative": false
          }
        ],
        "pagingMetadata": {
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 marketplace payouts, given the provided paging, filtering, and sorting.

    Query Marketplace Payouts runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `amount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionAmount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `paymentSharePercent` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `listing` | `$exists` | Sort: `ASC`, `DESC` |
    | `listing._id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `listing.type` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `invoiceId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.REVSHARE">SCOPE.PARTNERS.REVSHARE</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketplacePayouts } from '@wix/marketplace-payouts';

    marketplacePayouts.queryMarketplacePayouts(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="MarketplacePayoutQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="marketplacePayouts" type="MarketplacePayout[]">

      Retrieved list of marketplace payouts.

      See the [Marketplace Payout Object](/api-reference/account-level/studio-workspace/earnings/marketplace-payout-v1/marketplace-payout-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-marketplace-payouts" id="ex-sdk-query-marketplace-payouts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-marketplace-payouts" id="ex-sdk-query-marketplace-payouts-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-marketplace-payouts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-marketplace-payouts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query Marketplace Payouts</span><span className="wix-picker-desc">Queries a partner's marketplace payouts for a date range, sorted by transaction date</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryMarketplacePayouts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-marketplace-payouts-0"><span className="wix-picker-name">Query Marketplace Payouts</span><span className="wix-picker-desc">Queries a partner's marketplace payouts for a date range, sorted by transaction date</span></label>
    <label htmlFor="ex-sdk-query-marketplace-payouts-1"><span className="wix-picker-name">queryMarketplacePayouts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query Marketplace Payouts
    import { marketplacePayouts } from "@wix/marketplace-payouts";

    const query = {
      filter: {
        transactionDate: {
          $gte: "2026-05-01T00:00:00.000Z",
          $lt: "2026-06-01T00:00:00.000Z",
        },
      },
      sort: [{ fieldName: "transactionDate", order: "DESC" }],
      cursorPaging: { limit: 100 },
    };

    async function queryMarketplacePayouts() {
      const response =
        await marketplacePayouts.queryMarketplacePayouts(
          query
        );
    }

    /* Promise resolves to:
    {
      "marketplacePayouts": [
        {
          "_id": "3f1c8e2a-9b47-4d1e-8a6f-2c5b7e9d0a14",
          "revision": "1",
          "_createdDate": "2026-05-14T08:21:33.512Z",
          "_updatedDate": "2026-05-14T08:21:33.512Z",
          "listing": {
            "id": "a1c2e3f4-5b6d-4789-9012-3456789abcde",
            "type": "TEMPLATE"
          },
          "transactionAmount": 79,
          "paymentSharePercent": 100,
          "amount": 79,
          "transactionDate": "2026-05-14T08:21:30.000Z",
          "invoiceId": "",
          "status": "APPROVED",
          "negative": false
        },
        {
          "_id": "c9e2a1b0-7d34-4e62-8f51-0a9b3c6d2e75",
          "revision": "1",
          "_createdDate": "2026-05-09T15:02:11.004Z",
          "_updatedDate": "2026-05-09T15:02:11.004Z",
          "listing": {
            "id": "f4e3d2c1-6b5a-4987-8210-fedcba987654",
            "type": "TEMPLATE"
          },
          "transactionAmount": 120,
          "paymentSharePercent": 100,
          "amount": 120,
          "transactionDate": "2026-05-09T15:02:08.000Z",
          "invoiceId": "INV-2026-05-0042",
          "status": "APPROVED",
          "negative": false
        }
      ],
      "pagingMetadata": {
        "cursors": {},
        "hasNext": false
      }
    }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryMarketplacePayouts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketplacePayouts } from '@wix/marketplace-payouts';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketplacePayouts },
      // Include the auth strategy and host as relevant
    });

    async function queryMarketplacePayouts(query) {
      const response = await myWixClient.marketplacePayouts.queryMarketplacePayouts(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
