---
title: "Extension Config"
description: "The JSON object below defines the configuration for this service plugin extension."
icon: "cube"
---

The JSON object below defines the configuration for this service plugin extension. How you supply values depends on the framework you’re using:

+ **Self-hosted apps:** After adding a service plugin extension to your app in the app dashboard, provide configuration details in your extension's page. Learn more about how to add and configure a self-hosted service plugin extension with [REST](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions-with-rest) or the [SDK](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions-with-the-sdk).
+ **CLI apps:** When you [add a service plugin extension to your app with the Wix CLI](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/wix-cli/supported-extensions/backend-extensions/service-plugins/add-service-plugin-extensions-with-the-cli), the required configuration details are populated automatically. You can then customize them in your extension's [`plugin.json`](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/wix-cli/supported-extensions/backend-extensions/service-plugins/service-plugin-extension-files-and-code) file.

<ResponseField name="deploymentUri" type="SpiBaseUri">

  Base URI of the service plugin implementation. Since no endpoint is calling this path, it may be left blank.

  <Expandable title="deploymentUri properties">

    <ResponseField name="baseUri" type="string">

      Base URI for your service plugin. Wix appends each method's path to this URI.

      For example, to receive requests at `https://my-app.com/v1/my-method`, set this field to `https://my-app.com/`.

      Constraints: `minLength 6`, `maxLength 2048`.

    </ResponseField>

    <ResponseField name="alternativeUris" type="AlternativeUri[]">

      Alternative URIs for specific methods. Use these to override the default URI for individual methods.

      <Expandable title="alternativeUris properties">

        <ResponseField name="methodName" type="string">

          Name of the method to call at the absolute URI, in PascalCase. For example, to call [Get Shipping Rates](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/shipping-rates/shipping-rates-integration-service-plugin/get-shipping-rates) on an alternative URI, specify `GetShippingRates`.

          Constraints: `minLength 3`, `maxLength 128`.

        </ResponseField>

        <ResponseField name="absoluteUri" type="string">

          Absolute URI that Wix calls for this method. Wix doesn't append any path to this URI.

          The URI must begin with `https://`, such as `https://my-app.com/v1/my-custom-method`.

          Constraints: `minLength 6`, `maxLength 2048`.

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Expandable>

</ResponseField>

<ResponseField name="productRestrictionsConfig" type="RestrictionsConfig">

  Product restrictions configuration.

  <Expandable title="productRestrictionsConfig properties">

    <ResponseField name="prices" type="Restriction">

      Prices restrictions.

      Affected fields:
      * `variantsInfo.variants.price.actualPrice.amount`
      * `variantsInfo.variants.price.compareAtPrice.amount`
      * `inventory.trackingMethod.quantity`

      <Expandable title="prices properties">

        <ResponseField name="restrictionLevel" type="string">

          Restriction level.

          **Supported values:**

          - `WARNING` — Users can edit the product field after acknowledging a warning modal.
          - `LOCKED` — Users **cannot** edit the product field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="options" type="Restriction">

      Options restrictions.

      Affected field: `options` (all nested fields except `linkedMedia`)

      <Expandable title="options properties">

        <ResponseField name="restrictionLevel" type="string">

          Restriction level.

          **Supported values:**

          - `WARNING` — Users can edit the product field after acknowledging a warning modal.
          - `LOCKED` — Users **cannot** edit the product field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="modifiers" type="Restriction">

      Modifier restrictions.

      Affected field: `modifiers` (all nested fields except `linkedMedia`)

      <Expandable title="modifiers properties">

        <ResponseField name="restrictionLevel" type="string">

          Restriction level.

          **Supported values:**

          - `WARNING` — Users can edit the product field after acknowledging a warning modal.
          - `LOCKED` — Users **cannot** edit the product field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="inventory" type="Restriction">

      Inventory restrictions.

      Affected fields:
      * `variantsInfo.variants.sku`
      * `variantsInfo.variants.barcode`
      * `inventory.trackingMethod.quantity`

      <Expandable title="inventory properties">

        <ResponseField name="restrictionLevel" type="string">

          Restriction level.

          **Supported values:**

          - `WARNING` — Users can edit the product field after acknowledging a warning modal.
          - `LOCKED` — Users **cannot** edit the product field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="preOrder" type="Restriction">

      Pre order restrictions.

      Affected field: `inventory.preorderInfo`

      <Expandable title="preOrder properties">

        <ResponseField name="restrictionLevel" type="string">

          Restriction level.

          **Supported values:**

          - `WARNING` — Users can edit the product field after acknowledging a warning modal.
          - `LOCKED` — Users **cannot** edit the product field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="duplicateProduct" type="Restriction">

      Whether to disallow product duplication or display a warning when duplicating a product.

      <Expandable title="duplicateProduct properties">

        <ResponseField name="restrictionLevel" type="string">

          Restriction level.

          **Supported values:**

          - `WARNING` — Users can edit the product field after acknowledging a warning modal.
          - `LOCKED` — Users **cannot** edit the product field.

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Expandable>

</ResponseField>
