---
title: "Remove Contact From Trash Bin"
description: "Permanently deletes a contact from the trash bin."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a contact from the trash bin.

    After removal, the contact can no longer be restored.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACTS">Manage Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/contacts/v5/contacts/trash-bin/{contactId}
    ```

    ## Path Params

    <ParamField path="contactId" type="string" required>

      ID of the contact to permanently delete from the trash bin.

    </ParamField>

    ## Response Object

    Returns `RemoveContactFromTrashBinResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Permanently removes a deleted contact from the trash bin by contact ID (cannot be restored afterwards).

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/contacts/v5/contacts/trash-bin/fa6fe461-6fe9-400b-9866-2ad7734dba57' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a contact from the trash bin.

    After removal, the contact can no longer be restored.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACTS">Manage Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contactsV5 } from '@wix/crm';

    contactsV5.removeContactFromTrashBin(contactId): Promise
    ```

    ## Method Parameters

    <ParamField body="contactId" type="string" required>

      ID of the contact to permanently delete from the trash bin.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `RemoveContactFromTrashBinResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-contact-from-trash-bin" id="ex-sdk-remove-contact-from-trash-bin-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-contact-from-trash-bin" id="ex-sdk-remove-contact-from-trash-bin-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-contact-from-trash-bin-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-contact-from-trash-bin-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Permanently remove a contact from the trash bin</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeContactFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-contact-from-trash-bin-0"><span className="wix-picker-name">Permanently remove a contact from the trash bin</span></label>
    <label htmlFor="ex-sdk-remove-contact-from-trash-bin-1"><span className="wix-picker-name">removeContactFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Permanently remove a contact from the trash bin
    import { contactsV5 } from "@wix/crm";

    const contactId = "c97ff9b0-345e-44b9-a93f-2a1442a11f3f";

    async function removeContactFromTrashBin() {
      const response = await contactsV5.removeContactFromTrashBin(contactId);
    }

    /* Promise resolves to:
     * {}
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeContactFromTrashBin (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contactsV5 } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contactsV5 },
      // Include the auth strategy and host as relevant
    });

    async function removeContactFromTrashBin(contactId) {
      const response = await myWixClient.contactsV5.removeContactFromTrashBin(contactId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
