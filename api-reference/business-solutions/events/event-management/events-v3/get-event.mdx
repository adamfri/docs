---
title: "Get Event"
description: "Retrieves an event by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an event by ID.

    Specify `fields` to request specific fieldsets.
    For example, `fields: ["FORM"]` includes the event's registration form definition in the response.
    Use the returned input names, mandatory fields, and predefined options before
    [creating an RSVP](https://dev.wix.com/docs/rest/business-solutions/events/rsvp-v2/create-rsvp).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-EVENTS">Read Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/events/v3/events/{eventId}
    ```

    ## Path Params

    <ParamField path="eventId" type="string" required>

      Event ID.

    </ParamField>

    ## Query Params

    <ParamField query="fields" type="string[]">

      Predefined sets of fields to return. For example, specify `"FORM"` to include the event's registration form definition, including the input names, mandatory fields, and predefined options needed when [creating an RSVP](https://dev.wix.com/docs/rest/business-solutions/events/rsvp-v2/create-rsvp).

      Constraints: `maxItems 20`.

      **Supported values:**

      - `DETAILS` — Returns `shortDescription`, `mainImage` and `calendarUrls`.
      - `TEXTS` — Returns `description`.
      - `REGISTRATION` — Returns `registration`.
      - `URLS` — Returns `eventPageUrl`.
      - `FORM` — Returns `form`.
      - `DASHBOARD` — Returns `summaries`.
      - `FEED` — Returns `feed`.
      - `ONLINE_CONFERENCING_SESSION` — Returns `onlineConferencing.session`.
      - `SEO_SETTINGS` — Returns `seoSettings`.
      - `AGENDA` — Returns `agendaSettings`.
      - `CATEGORIES` — Returns `categories`.

    </ParamField>

    ## Response Object

    <ResponseField name="event" type="Event">

      Event.

      See the [Event Object](/api-reference/business-solutions/events/event-management/events-v3/event-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET 'https://www.wixapis.com/events/v3/events/75d64b92-4078-457a-9259-fef2948459f7?fields=DETAILS&fields=TEXTS&fields=REGISTRATION&fields=URLS&fields=FORM&fields=DASHBOARD&fields=ONLINE_CONFERENCING_SESSION&fields=SEO_SETTINGS&fields=AGENDA&fields=CATEGORIES' \
      -H 'Authorization: <AUTH TOKEN>'
      ```

      ```json Response
      {
        "event": {
          "id": "75d64b92-4078-457a-9259-fef2948459f7",
          "location": {
            "name": "Javits Center",
            "type": "VENUE",
            "address": {
              "country": "US",
              "subdivision": "NY",
              "city": "New York",
              "postalCode": "10001",
              "streetAddress": {
                "number": "429",
                "name": "11th Avenue"
              },
              "addressLine2": null
            },
            "locationTbd": false
          },
          "dateAndTimeSettings": {
            "dateAndTimeTbd": false,
            "dateAndTimeTbdMessage": null,
            "startDate": "2023-10-15T13:00:00Z",
            "endDate": "2023-10-17T22:00:00Z",
            "timeZoneId": "America/Toronto",
            "hideEndDate": false,
            "showTimeZone": true,
            "recurrenceStatus": "ONE_TIME",
            "recurringEvents": {
              "individualEventDates": [],
              "categoryId": null
            },
            "formatted": {
              "dateAndTime": "15 October 2023 at 09:00 – 17 October 2023 at 18:00 GMT-4",
              "startDate": "15 October 2023",
              "startTime": "09:00",
              "endDate": "17 October 2023",
              "endTime": "18:00"
            }
          },
          "title": "Cosmic Odyssey: Exploring the Mysteries of the Universe",
          "shortDescription": "",
          "mainImage": null,
          "slug": "cosmic-odyssey-exploring-the-mysteries-of-the-universe",
          "createdDate": "2023-07-12T10:50:59.287Z",
          "updatedDate": "2023-07-12T10:58:58Z",
          "status": "UPCOMING",
          "registration": {
            "type": "TICKETING",
            "status": "OPEN_TICKETS",
            "rsvp": {
              "responseType": "YES_ONLY",
              "limit": null,
              "waitlistEnabled": false,
              "startDate": null,
              "endDate": null
            },
            "tickets": {
              "guestsAssignedSeparately": false,
              "ticketLimitPerOrder": 50,
              "currency": "EUR",
              "lowestPrice": {
                "value": "20.50",
                "currency": "EUR",
                "formattedValue": "€20.50"
              },
              "highestPrice": {
                "value": "20.50",
                "currency": "EUR",
                "formattedValue": "€20.50"
              },
              "soldOut": false,
              "taxSettings": null
            },
            "external": null,
            "allowedGuestTypes": "VISITOR_OR_MEMBER",
            "initialType": "TICKETING",
            "registrationPaused": false,
            "registrationDisabled": false
          },
          "calendarUrls": null,
          "eventPageUrl": null,
          "form": null,
          "summaries": null,
          "instanceId": "1f6dc906-803d-4650-83f5-72ea1d88100b",
          "guestListSettings": {
            "displayedPublicly": true
          },
          "userId": "8a8b9b73-4da8-47a5-8268-4396e68a0605",
          "onlineConferencing": {
            "session": null
          },
          "seoSettings": null,
          "contactLabel": "custom.cosmic-odyssey-exploring-the-mysteries-of-the-univ",
          "agendaSettings": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an event by ID.

    Specify `fields` to request specific fieldsets.
    For example, `fields: ["FORM"]` includes the event's registration form definition in the response.
    Use the returned input names, mandatory fields, and predefined options before
    [creating an RSVP](https://dev.wix.com/docs/rest/business-solutions/events/rsvp-v2/create-rsvp).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-EVENTS">Read Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { wixEventsV2 } from '@wix/events';

    wixEventsV2.getEvent(eventId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="eventId" type="string" required>

      Event ID.

    </ParamField>

    <ParamField body="options" type="GetEventOptions">

      Optional fields.

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Predefined sets of fields to return. For example, specify `"FORM"` to include the event's registration form definition, including the input names, mandatory fields, and predefined options needed when [creating an RSVP](https://dev.wix.com/docs/rest/business-solutions/events/rsvp-v2/create-rsvp).

          Constraints: `maxItems 20`.

          **Supported values:**

          - `DETAILS` — Returns `shortDescription`, `mainImage` and `calendarUrls`.
          - `TEXTS` — Returns `description`.
          - `REGISTRATION` — Returns `registration`.
          - `URLS` — Returns `eventPageUrl`.
          - `FORM` — Returns `form`.
          - `DASHBOARD` — Returns `summaries`.
          - `FEED` — Returns `feed`.
          - `ONLINE_CONFERENCING_SESSION` — Returns `onlineConferencing.session`.
          - `SEO_SETTINGS` — Returns `seoSettings`.
          - `AGENDA` — Returns `agendaSettings`.
          - `CATEGORIES` — Returns `categories`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Event`.

    See the [Event Object](/api-reference/business-solutions/events/event-management/events-v3/event-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-event" id="ex-sdk-get-event-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-event" id="ex-sdk-get-event-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-event-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-event-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get an event by ID</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-event-0"><span className="wix-picker-name">Get an event by ID</span></label>
    <label htmlFor="ex-sdk-get-event-1"><span className="wix-picker-name">getEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get an event by ID
    import { wixEventsV2 } from "@wix/events";

    const eventId = "201037a0-3ecc-4ee4-976b-a1c931c42a6b";

    async function getEvent() {
      const response = await wixEventsV2.getEvent(eventId);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getEvent (self-hosted)
    import { createClient } from '@wix/sdk';
    import { wixEventsV2 } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { wixEventsV2 },
      // Include the auth strategy and host as relevant
    });

    async function getEvent(eventId,options) {
      const response = await myWixClient.wixEventsV2.getEvent(eventId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
