---
title: "Remove Customization Choices"
description: "Removes choices from a customization."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes choices from a customization.

    <Note>

    A choice can't be removed if it's assigned to one or more product variants. This applies to option choices only; modifier choices can be removed freely.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CUSTOMIZATION_WRITE">Customization write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/customizations/{customizationId}/remove-choices
    ```

    ## Path Params

    <ParamField path="customizationId" type="string" required>

      Customization ID.

    </ParamField>

    ## Body Params

    <ParamField body="choiceIds" type="string[]" required>

      IDs of choices to remove.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    <ParamField body="revision" type="string">

      Customization revision.

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Supported values: `ASSIGNED_PRODUCTS_COUNT`

      Constraints: `maxItems 100`.

      **Supported values:**

      - `ASSIGNED_PRODUCTS_COUNT` — Assigned products count.
      - `CHOICE_DISPLAY_IMAGE` — Returns display images for choices.

    </ParamField>

    ## Response Object

    <ResponseField name="customization" type="Customization">

      Updated customization.

      See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CHOICE_ASSIGNED_TO_PRODUCT` | Choice is assigned to at least 1 product. Remove the choice from products before deleting. |
    | `400` | `INVALID_ARGUMENT` | `NOT_ALLOWED_CUSTOMIZATION_WITHOUT_CHOICES` | Attempt to remove all choices from customization. Customizations with choices must have at least 1 choice. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_DELETE_REFERENCED` | Can't remove a choice that is referenced as a primary by linked choices. Update or remove the linked choices first. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Customization Updated | `wix.stores.catalog.v3.customization_updated` | Triggered when a customization is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Removes choices from customization based on the customization and choices id's

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/stores/v3/customizations/06b6abfd-002d-437d-ad08-91f8537cd96d/remove-choices' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
       "choiceIds": [
           "ef6bf1ca-bdc8-4158-af95-c299de38dec6"
       ]
      }'
      ```

      ```json Response
      {
          "customization": {
              "id": "06b6abfd-002d-437d-ad08-91f8537cd96d",
              "revision": "6",
              "createdDate": "2024-06-26T09:44:11.306Z",
              "updatedDate": "2024-07-03T08:47:13.456Z",
              "key": "Color",
              "name": "Color",
              "customizationType": "PRODUCT_OPTION",
              "customizationRenderType": "SWATCH_CHOICES",
              "choicesSettings": {
                  "choices": [{
                      "id": "92e94db7-832c-42fd-be1e-55670df24ccf",
                      "choiceType": "ONE_COLOR",
                      "key": "Blue",
                      "name": "Blue",
                      "colorCode": "#0000FF"
                  }, {
                      "id": "30be51d2-cc1e-4ef6-84c0-ac59f9410bc2",
                      "choiceType": "ONE_COLOR",
                      "key": "Yellow",
                      "name": "Yellow",
                      "colorCode": "#FFFF00"
                  }, {
                      "id": "4e787c64-8901-4031-b4cc-63bb9fa0a96a",
                      "choiceType": "ONE_COLOR",
                      "key": "Green",
                      "name": "Green",
                      "colorCode": "#008000"
                  }]
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes choices from a customization.

    <Note>

    A choice can't be removed if it's assigned to one or more product variants. This applies to option choices only; modifier choices can be removed freely.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CUSTOMIZATION_WRITE">Customization write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customizationsV3 } from '@wix/stores';

    customizationsV3.removeCustomizationChoices(customizationId, choiceIds, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="customizationId" type="string" required>

      Customization ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="choiceIds" type="string[]" required>

      IDs of choices to remove.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    <ParamField body="options" type="RemoveCustomizationChoicesOptions">

      options

      <Expandable title="options properties">

        <ParamField body="revision" type="string">

          Customization revision.

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Supported values: `ASSIGNED_PRODUCTS_COUNT`

          Constraints: `maxItems 100`.

          **Supported values:**

          - `ASSIGNED_PRODUCTS_COUNT` — Assigned products count.
          - `CHOICE_DISPLAY_IMAGE` — Returns display images for choices.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="customization" type="Customization">

      Updated customization.

      See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CHOICE_ASSIGNED_TO_PRODUCT` | Choice is assigned to at least 1 product. Remove the choice from products before deleting. |
    | `400` | `INVALID_ARGUMENT` | `NOT_ALLOWED_CUSTOMIZATION_WITHOUT_CHOICES` | Attempt to remove all choices from customization. Customizations with choices must have at least 1 choice. |
    | `400` | `INVALID_ARGUMENT` | `PRIMARY_CHOICE_DELETE_REFERENCED` | Can't remove a choice that is referenced as a primary by linked choices. Update or remove the linked choices first. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Customization Updated | `wix.stores.catalog.v3.customization_updated` | Triggered when a customization is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-customization-choices" id="ex-sdk-remove-customization-choices-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-customization-choices" id="ex-sdk-remove-customization-choices-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-customization-choices-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-customization-choices-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Remove choices from a customization</span><span className="wix-picker-desc">Remove specific choices from a customization by choice IDs</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeCustomizationChoices (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-customization-choices-0"><span className="wix-picker-name">Remove choices from a customization</span><span className="wix-picker-desc">Remove specific choices from a customization by choice IDs</span></label>
    <label htmlFor="ex-sdk-remove-customization-choices-1"><span className="wix-picker-name">removeCustomizationChoices (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Remove choices from a customization
    import { customizationsV3 } from "@wix/stores";

    const customizationId = "06b6abfd-002d-437d-ad08-91f8537cd96d";

    const choiceIds = ["ef6bf1ca-bdc8-4158-af95-c299de38dec6"];

    async function removeCustomizationChoices() {
      const response = await customizationsV3.removeCustomizationChoices(customizationId, choiceIds);
    }

    /* Promise resolves to:
     * {
     *   "_id": "06b6abfd-002d-437d-ad08-91f8537cd96d",
     *   "revision": "6",
     *   "name": "Color",
     *   "customizationType": "PRODUCT_OPTION",
     *   "customizationRenderType": "SWATCH_CHOICES",
     *   "choicesSettings": {
     *     "choices": [
     *       {
     *         "id": "92e94db7-832c-42fd-be1e-55670df24ccf",
     *         "choiceType": "ONE_COLOR",
     *         "name": "Blue",
     *         "colorCode": "#0000FF"
     *       },
     *       {
     *         "id": "30be51d2-cc1e-4ef6-84c0-ac59f9410bc2",
     *         "choiceType": "ONE_COLOR",
     *         "name": "Yellow",
     *         "colorCode": "#FFFF00"
     *       }
     *     ]
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeCustomizationChoices (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customizationsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customizationsV3 },
      // Include the auth strategy and host as relevant
    });

    async function removeCustomizationChoices(customizationId,choiceIds,options) {
      const response = await myWixClient.customizationsV3.removeCustomizationChoices(customizationId,choiceIds,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
