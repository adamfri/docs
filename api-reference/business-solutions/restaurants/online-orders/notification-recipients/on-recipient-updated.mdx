---
title: "onRecipientUpdated"
description: "The onRecipientUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.recipient.v1.recipient_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onRecipientUpdated
  import { recipients } from '@wix/restaurants';

  recipients.onRecipientUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRecipientUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { recipients } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        recipients,
      },
  });

  wixClient.recipients.onRecipientUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
