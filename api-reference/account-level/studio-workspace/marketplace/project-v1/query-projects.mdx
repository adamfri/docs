---
title: "Query Projects"
description: "Retrieves a list of up to 100 projects, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 projects, given the provided paging, filtering, and sorting.

    Query Projects runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `50`

    Supported filter and sort fields: `id`, `status`, `source`, `contactId`, `createdDate`, `updatedDate`, `expirationDate`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `source` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `contactId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `expirationDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ARENA.MANAGE-LEADS">Manage CRM and Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/partners/marketplace/v1/projects/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options.

      Supported filter and sort fields: `id`, `status`, `source`, `contactId`, `createdDate`,
      `updatedDate`, `expirationDate`.

      See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language)
      for more information.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="projects" type="Project[]">

      Retrieved projects.

      See the [Project Object](/api-reference/account-level/studio-workspace/marketplace/project-v1/project-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves the provider's projects that no one has acted on yet

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/partners/marketplace/v1/projects/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "status": "NEW"
          },
          "sort": [
            {
              "fieldName": "createdDate",
              "order": "DESC"
            }
          ],
          "cursorPaging": {
            "limit": 10
          }
        }
      }'
      ```

      ```json Response
      {
        "projects": [
          {
            "id": "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
            "revision": "1",
            "createdDate": "2024-01-15T10:30:00.000Z",
            "updatedDate": "2024-01-15T10:30:00.000Z",
            "serviceName": "ADVANCED_WEBSITE",
            "contactId": "5c6d7e8f-9a1b-4c2d-9e3f-4a5b6c7d8e9f",
            "status": "NEW",
            "budget": 500.0,
            "expirationDate": "2024-01-19T10:30:00.000Z",
            "source": "MATCHING",
            "briefDescription": "Looking for a modern portfolio site with a blog and contact form."
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 projects, given the provided paging, filtering, and sorting.

    Query Projects runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `50`

    Supported filter and sort fields: `id`, `status`, `source`, `contactId`, `createdDate`, `updatedDate`, `expirationDate`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `source` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `contactId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `expirationDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ARENA.MANAGE-LEADS">Manage CRM and Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketplaceProjects } from '@wix/marketplace-projects';

    marketplaceProjects.queryProjects(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="ProjectQuery" required>

      Query options.

      Supported filter and sort fields: `id`, `status`, `source`, `contactId`, `createdDate`,
      `updatedDate`, `expirationDate`.

      See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language)
      for more information.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="projects" type="Project[]">

      Retrieved projects.

      See the [Project Object](/api-reference/account-level/studio-workspace/marketplace/project-v1/project-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-projects" id="ex-sdk-query-projects-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-projects" id="ex-sdk-query-projects-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-projects-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-projects-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query projects</span><span className="wix-picker-desc">Retrieves the provider's projects that no one has acted on yet, newest first</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryProjects (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-projects-0"><span className="wix-picker-name">Query projects</span><span className="wix-picker-desc">Retrieves the provider's projects that no one has acted on yet, newest first</span></label>
    <label htmlFor="ex-sdk-query-projects-1"><span className="wix-picker-name">queryProjects (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query projects
    import { marketplaceProjects } from "@wix/marketplace-projects";

    async function queryProjects() {
      const response = await marketplaceProjects
        .queryProjects()
        .eq("status", "NEW")
        .descending("_createdDate")
        .limit(10)
        .find();
    }

    /* Promise resolves to:
     * {
     *   "items": [
     *     {
     *       "_id": "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
     *       "revision": "1",
     *       "_createdDate": "2024-01-15T10:30:00.000Z",
     *       "_updatedDate": "2024-01-15T10:30:00.000Z",
     *       "serviceName": "ADVANCED_WEBSITE",
     *       "contactId": "5c6d7e8f-9a1b-4c2d-9e3f-4a5b6c7d8e9f",
     *       "status": "NEW",
     *       "budget": 500,
     *       "expirationDate": "2024-01-19T10:30:00.000Z",
     *       "source": "MATCHING",
     *       "briefDescription": "Looking for a modern portfolio site with a blog and contact form."
     *     }
     *   ],
     *   "length": 1,
     *   "pageSize": 10,
     *   "hasNext": false,
     *   "hasPrev": false
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryProjects (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketplaceProjects } from '@wix/marketplace-projects';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketplaceProjects },
      // Include the auth strategy and host as relevant
    });

    async function queryProjects(query) {
      const response = await myWixClient.marketplaceProjects.queryProjects(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
