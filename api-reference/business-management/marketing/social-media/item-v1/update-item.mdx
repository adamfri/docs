---
title: "Update Item"
description: "Updates an item."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an item.

    Provide the `id` of the item to update along with the fields to change. Only the fields you include are changed. To change a scheduled item's publish date, call Reschedule Item instead.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/social-publisher/v1/items/{item.id}
    ```

    ## Path Params

    <ParamField path="item.id" type="string" required>

      Item ID.

    </ParamField>

    ## Body Params

    <ParamField body="item" type="Item" required>

      Item to update.

      See the [Item Object](/api-reference/business-management/marketing/social-media/item-v1/item-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="item" type="Item">

      Updated item.

      See the [Item Object](/api-reference/business-management/marketing/social-media/item-v1/item-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `ITEM_NOT_EXISTS` | Couldn't find the item. |
    | `400` | `INVALID_ARGUMENT` | `SCHEDULED_ITEM_DATE_CHANGED` | The scheduled date of a scheduled item can't be changed with Update Item. Call Reschedule Item instead. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Update an item by providing only the fields to change. Only the provided fields are updated.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/social-publisher/v1/items/834dc801-3e5c-416a-bd14-b130a95d100e' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "item": {
          "id": "834dc801-3e5c-416a-bd14-b130a95d100e",
          "instagramPost": {
            "caption": "Summer collection just dropped! Limited stock available. #summerstyle #newarrivals #shopnow"
          }
        }
      }'
      ```

      ```json Response
      {
        "item": {
          "id": "834dc801-3e5c-416a-bd14-b130a95d100e",
          "channel": {
            "name": "INSTAGRAM",
            "accountId": "17841405822304914"
          },
          "type": "POST",
          "status": "DRAFT",
          "instagramPost": {
            "mediaWrapper": {
              "media": [
                {
                  "type": "IMAGE",
                  "url": "https://static.wixstatic.com/media/cf2434_4b3a2d8eb7f54bc89c0d4524430a2af3~mv2.jpg"
                }
              ]
            },
            "caption": "Summer collection just dropped! Limited stock available. #summerstyle #newarrivals #shopnow"
          },
          "draftInfo": {
            "createdDate": "2026-06-11T07:20:56.561Z",
            "updatedDate": "2026-06-11T07:25:22.336Z"
          },
          "suggested": false,
          "external": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an item.

    Provide the `id` of the item to update along with the fields to change. Only the fields you include are changed. To change a scheduled item's publish date, call Reschedule Item instead.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/promote-growth-tools-publisher';

    items.updateItem(_id, item): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Item ID.

      **Read-only.**

    </ParamField>

    <ParamField body="item" type="UpdateItem" required>

      Item to update.

      <Expandable title="item properties">

        <ParamField body="_id" type="string">

          Item ID.

          **Read-only.**

        </ParamField>

        <ParamField body="channel" type="Channel">

          Social channel the item is published to, and the account within that channel.

          <Expandable title="channel properties">

            <ParamField body="name" type="string">

              Social channel the item is published to.

              **Supported values:**

              - `INSTAGRAM`
              - `FACEBOOK`
              - `YOUTUBE`
              - `LINKEDIN`
              - `TWITTER` — Deprecated: No longer functional as of July 31, 2026.
              - `PINTEREST`
              - `GBP` — Google Business Profile.
              - `TIKTOK`

            </ParamField>

            <ParamField body="accountId" type="string">

              ID of the connected account the item is published to.

            </ParamField>

            <ParamField body="accountName" type="string">

              Display name of the connected account the item is published to.

              **Read-only.**

            </ParamField>

            <ParamField body="externalItemId" type="string">

              ID of the post on the social channel. Returned after the item is published.

              **Read-only.**

            </ParamField>

            <ParamField body="externalItemUrl" type="string">

              URL of the post on the social channel. Returned after the item is published.

              **Read-only.**

            </ParamField>

            <ParamField body="publishingTarget" type="PublishingTarget">

              Specific destination within the account, such as a Facebook page or a Pinterest board.

              _Nested structure omitted for brevity — see the `PublishingTarget` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="type" type="string">

          Item format. The `type` and the content set in `data` must match a combination supported by the item's `channel.name`:

          - Instagram: `POST` with `instagramPost`, or `STORY` with `instagramStory`.
          - Facebook: `POST` with `facebookPost`, `REEL` with `facebookReel`, or `STORY` with `facebookStory`.
          - YouTube: `VIDEO` with `youtubeVideo`, or `SHORT` with `youtubeShort`.
          - LinkedIn: `POST` with `linkedinPost`.
          - X (Twitter): `POST` with `twitterPost`. Deprecated: No longer functional as of July 31, 2026.
          - Pinterest: `POST` with `pinterestPost`.
          - Google Business Profile: `POST` with `gbpPost`.
          - TikTok: `POST` with `tiktokPhoto`, or `VIDEO` with `tiktokVideo`.

          **Supported values:**

          - `POST` — A standard post.
          - `REEL` — A short-form vertical video reel.
          - `VIDEO` — A video.
          - `SHORT` — A short-form vertical video.
          - `STORY` — A story that's available for a limited time.

        </ParamField>

        <ParamField body="status" type="string">

          Current status of the item in its lifecycle.

          **Read-only.**

          **Supported values:**

          - `PUBLISHED` — Published to the social channel.
          - `SCHEDULED` — Scheduled to be published at a future date.
          - `CANCELED` — Scheduling was canceled before the item was published.
          - `FAILED` — Publishing failed.
          - `DELETED` — Deleted.
          - `PROCESSING` — Publishing is in progress.
          - `DRAFT` — Saved as a draft, not yet published.
          - `IN_QUEUE` — Queued for publishing as part of a bulk operation.

        </ParamField>

        <ParamField body="publishedDate" type="datetime">

          Date and time the item was published. Returned only after the item is published.

          **Read-only.**

        </ParamField>

        <ParamField body="schedulingInfo" type="SchedulingInfo">

          Scheduling info. Set to schedule the item for a future date instead of publishing it immediately.

          <Expandable title="schedulingInfo properties">

            <ParamField body="scheduledDate" type="datetime">

              Date and time the item is scheduled to be published.

            </ParamField>

            <ParamField body="schedulingTimestamp" type="datetime">

              Date and time the item was scheduled.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="referenceId" type="string">

          Caller-defined reference that uniquely identifies the item. Can be used to prevent duplicate publishing: an attempt to publish a second item with the same `referenceId` fails. Can also be used as a UTM campaign ID.

        </ParamField>

        <ParamField body="siteAnalytics" type="SiteAnalytics">

          Aggregated analytics for the site, attributed to traffic from this post.

          <Expandable title="siteAnalytics properties">

            <ParamField body="traffic" type="integer">

              Number of site sessions attributed to this post.

            </ParamField>

            <ParamField body="pageView" type="integer">

              Number of page views attributed to this post.

            </ParamField>

            <ParamField body="siteBounceRate" type="float">

              Site bounce rate attributed to this post.

            </ParamField>

            <ParamField body="revenue" type="float">

              Revenue attributed to this post.

            </ParamField>

            <ParamField body="conversionFunnel" type="ConversionFunnel">

              Conversion funnel data attributed to this post.

              _Nested structure omitted for brevity — see the `ConversionFunnel` object._

            </ParamField>

            <ParamField body="_updatedDate" type="datetime">

              Date and time the insights were last updated.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="insightsSummary" type="InsightsSummary">

          Summary of the post's performance on the social channel, such as impressions and interactions.

          <Expandable title="insightsSummary properties">

            <ParamField body="impressions" type="integer">

              Number of unique impressions (reach).

            </ParamField>

            <ParamField body="interactions" type="integer">

              Number of interactions, such as likes and comments.

            </ParamField>

            <ParamField body="_updatedDate" type="datetime">

              Date and time the insights were last updated.

            </ParamField>

            <ParamField body="impressionsTotal" type="integer">

              Total number of impressions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="draftInfo" type="DraftInfo">

          Creation and update dates, returned for draft items.

          <Expandable title="draftInfo properties">

            <ParamField body="_createdDate" type="datetime">

              Date and time the draft was created.

              **Read-only.**

            </ParamField>

            <ParamField body="_updatedDate" type="datetime">

              Date and time the draft was updated.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="suggested" type="boolean">

          Whether the item was generated as an AI suggestion.

        </ParamField>

        <ParamField body="external" type="boolean">

          Whether the item is a post created natively on the social channel rather than published through Wix.

        </ParamField>

        <ParamField body="publicationError" type="PublicationErrorDetails">

          Details of why the item failed to publish. Returned only when `status` is `FAILED`.

          **Read-only.**

          <Expandable title="publicationError properties">

            <ParamField body="message" type="string">

              Error message shown when publishing fails.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="siteAssetRef" type="AssetReference">

          Reference to the site content asset this post promotes (product, blog post, etc.).
          Mirrors marketing-plan-service's MarketingActivity.asset (named `asset` there;
          `site_asset_ref` here for clarity next to consumer_info). Written per-channel.

          <Expandable title="siteAssetRef properties">

            <ParamField body="siteAssetRefType" type="string">

              Type of the site asset (for example, product or blog post). See `SiteAsset.Type` for the closed set of values.

              **Supported values:**

              - `BLOG_POST` — A Wix Blog post.
              - `EVENT` — A Wix Events event.
              - `STORES_PRODUCT` — A Wix Stores product.
              - `BOOKINGS_SERVICE` — A Wix Bookings service.
              - `STORES_COUPON` — A Wix Stores coupon.
              - `STORES_CATEGORY` — A Wix Stores category.

            </ParamField>

            <ParamField body="_id" type="string">

              ID of the asset within its provider's data store.

            </ParamField>

            <ParamField body="name" type="string">

              Display name of the asset (for example, the product title or blog post title), cached at post-creation time.

              Constraints: `maxLength 3000`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="data" type="data">

          data

          <Expandable title="data properties">

            <ParamField body="instagramPost" type="InstagramPost">

              Instagram post content.

              _Nested structure omitted for brevity — see the `InstagramPost` object._

            </ParamField>

            <ParamField body="instagramStory" type="InstagramStory">

              Instagram story content.

              _Nested structure omitted for brevity — see the `InstagramStory` object._

            </ParamField>

            <ParamField body="facebookPost" type="FacebookPost">

              Facebook post content.

              _Nested structure omitted for brevity — see the `FacebookPost` object._

            </ParamField>

            <ParamField body="facebookReel" type="FacebookReel">

              Facebook reel content.

              _Nested structure omitted for brevity — see the `FacebookReel` object._

            </ParamField>

            <ParamField body="facebookStory" type="FacebookStory">

              Facebook story content.

              _Nested structure omitted for brevity — see the `FacebookStory` object._

            </ParamField>

            <ParamField body="youtubeVideo" type="YouTubeVideo">

              YouTube video content.

              _Nested structure omitted for brevity — see the `YouTubeVideo` object._

            </ParamField>

            <ParamField body="youtubeShort" type="YouTubeShort">

              YouTube short content.

              _Nested structure omitted for brevity — see the `YouTubeShort` object._

            </ParamField>

            <ParamField body="linkedinPost" type="LinkedinPost">

              LinkedIn post content.

              _Nested structure omitted for brevity — see the `LinkedinPost` object._

            </ParamField>

            <ParamField body="twitterPost" type="TwitterPost" deprecated>

              Deprecated: No longer functional as of July 31, 2026.
              X (Twitter) post content.

              **Deprecated.** Deprecated: No longer functional as of July 31, 2026.
              X (Twitter) post content. Removal: 2026-07-31.

              _Nested structure omitted for brevity — see the `TwitterPost` object._

            </ParamField>

            <ParamField body="pinterestPost" type="PinterestPost">

              Pinterest post content.

              _Nested structure omitted for brevity — see the `PinterestPost` object._

            </ParamField>

            <ParamField body="gbpPost" type="GbpPost">

              Google Business Profile post content.

              _Nested structure omitted for brevity — see the `GbpPost` object._

            </ParamField>

            <ParamField body="tiktokVideo" type="TikTokVideo">

              TikTok video content.

              _Nested structure omitted for brevity — see the `TikTokVideo` object._

            </ParamField>

            <ParamField body="tiktokPhoto" type="TikTokPhoto">

              TikTok photo content.

              _Nested structure omitted for brevity — see the `TikTokPhoto` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Item`.

    See the [Item Object](/api-reference/business-management/marketing/social-media/item-v1/item-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `ITEM_NOT_EXISTS` | Couldn't find the item. |
    | `400` | `INVALID_ARGUMENT` | `SCHEDULED_ITEM_DATE_CHANGED` | The scheduled date of a scheduled item can't be changed with Update Item. Call Reschedule Item instead. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Updated | `wix.promote.publisher.v1.item_updated` | Triggered when an item is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-item" id="ex-sdk-update-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-item" id="ex-sdk-update-item-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a draft item's caption</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-item-0"><span className="wix-picker-name">Update a draft item's caption</span></label>
    <label htmlFor="ex-sdk-update-item-1"><span className="wix-picker-name">updateItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a draft item's caption
    import { items } from "@wix/promote-growth-tools-publisher";

    const itemId = "834dc801-3e5c-416a-bd14-b130a95d100e";

    const item = {
      instagramPost: {
        caption:
          "Summer collection just dropped! Limited stock available. #summerstyle #newarrivals #shopnow",
      },
    };

    async function updateItem() {
      const response = await items.updateItem(itemId, item);
    }

    /* Promise resolves to:
     * {
     *   "_id": "834dc801-3e5c-416a-bd14-b130a95d100e",
     *   "channel": {
     *     "name": "INSTAGRAM",
     *     "accountId": "17841405822304914"
     *   },
     *   "type": "POST",
     *   "status": "DRAFT",
     *   "instagramPost": {
     *     "mediaWrapper": {
     *       "media": [
     *         {
     *           "type": "IMAGE",
     *           "url": "https://static.wixstatic.com/media/cf2434_4b3a2d8eb7f54bc89c0d4524430a2af3~mv2.jpg"
     *         }
     *       ]
     *     },
     *     "caption": "Summer collection just dropped! Limited stock available. #summerstyle #newarrivals #shopnow"
     *   },
     *   "draftInfo": {
     *     "_createdDate": "2026-06-11T07:20:56.561Z",
     *     "_updatedDate": "2026-06-11T07:25:22.336Z"
     *   },
     *   "suggested": false,
     *   "external": false
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/promote-growth-tools-publisher';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function updateItem(_id,item) {
      const response = await myWixClient.items.updateItem(_id,item);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
