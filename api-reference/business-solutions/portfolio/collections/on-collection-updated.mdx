---
title: "onCollectionUpdated"
description: "Triggered when a collection is updated."
icon: "bolt"
---

Triggered when a collection is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.collections.v1.collection_updated` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onCollectionUpdated
  import { collections } from '@wix/portfolio';

  collections.onCollectionUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCollectionUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { collections } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        collections,
      },
  });

  wixClient.collections.onCollectionUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
