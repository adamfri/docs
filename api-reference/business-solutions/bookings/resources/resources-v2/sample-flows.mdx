---
title: "Sample Flows"
description: "This article presents a possible use case that you could support, along with a sample flow to implement it."
icon: "file-lines"
---

This article presents a possible use case that you could support, along with
a sample flow to implement it. This examples can serve as a helpful starting
point as you plan your implementation.

## Create a bookable resource

You could create a resource that's bookable only at a specific location.

To create a bookable resource:

1. Call *Query Resource Types*
   ([SDK](fallback::https://dev.wix.com/docs/sdk/backend-modules/bookings/resource-types/query-resource-types) | [REST](fallback::https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resource-types-v2/query-resource-types))
   and save the ID of the relevant resource type.
1. Call *Query Locations*
   ([SDK](fallback::https://dev.wix.com/docs/sdk/backend-modules/business-tools/locations/query-locations) | [REST](fallback::https://dev.wix.com/docs/rest/business-management/locations/query-locations))
   to retrieve the ID of the business location.
1. Call *Create Resource*
   ([SDK](fallback::https://dev.wix.com/docs/sdk/backend-modules/bookings/resources/create-resource) | [REST](fallback::https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resources-v2/create-resource))
   to create the resource. Specify a `name`, the ID of the resource type and
   the ID of the business location.
1. Call *Query Services*
   ([SDK](https://dev.wix.com/docs/sdk/backend-modules/bookings/services/query-services) | [REST](fallback::https://dev.wix.com/docs/rest/business-solutions/bookings/services/services-v2/query-services))
   Confirm that the resource type is associated with relevant services
   by making sure that `serviceResources.resourceType` is set to the correct
   resource Type ID.
1. Optional: For every service that doesn't specify the correct `resourceType`,
   call *Update Service*
   ([SDK](https://dev.wix.com/docs/sdk/backend-modules/bookings/services/update-service) | [REST](fallback::https://dev.wix.com/docs/rest/business-solutions/bookings/services/services-v2/update-service)).
   Specify the correct resource Type ID in `serviceResources.resourceType`.
