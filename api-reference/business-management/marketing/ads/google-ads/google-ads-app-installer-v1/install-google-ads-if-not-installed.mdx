---
title: "Install Google Ads If Not Installed"
description: "Installs the Wix Google Ads integration on the site if it isn't already installed."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Installs the Wix Google Ads integration on the site if it isn't already installed.

    This method is idempotent. Calling it when the app is already installed has no effect.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-GOOGLE-ADS">Manage google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_serverless/pa-google/v1/install-if-not-installed
    ```

    ## Response Object

    Returns `InstallGoogleAdsIfNotInstalledResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/_serverless/pa-google/v1/install-if-not-installed' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{}'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Installs the Wix Google Ads integration on the site if it isn't already installed.

    This method is idempotent. Calling it when the app is already installed has no effect.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-GOOGLE-ADS">Manage google ads campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { appInstallers } from '@wix/promote-google-ads';

    appInstallers.installGoogleAdsIfNotInstalled(): Promise
    ```

    ## Returns

    Returns `InstallGoogleAdsIfNotInstalledResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-install-google-ads-if-not-installed" id="ex-sdk-install-google-ads-if-not-installed-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-install-google-ads-if-not-installed" id="ex-sdk-install-google-ads-if-not-installed-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-install-google-ads-if-not-installed-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-install-google-ads-if-not-installed-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Install the Wix Google Ads integration on a site</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">installGoogleAdsIfNotInstalled (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-install-google-ads-if-not-installed-0"><span className="wix-picker-name">Install the Wix Google Ads integration on a site</span></label>
    <label htmlFor="ex-sdk-install-google-ads-if-not-installed-1"><span className="wix-picker-name">installGoogleAdsIfNotInstalled (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Install the Wix Google Ads integration on a site
    import { appInstallers } from '@wix/promote-google-ads';

    await appInstallers.installGoogleAdsIfNotInstalled();

    /* Promise resolves to:
     * {}
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript installGoogleAdsIfNotInstalled (self-hosted)
    import { createClient } from '@wix/sdk';
    import { appInstallers } from '@wix/promote-google-ads';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { appInstallers },
      // Include the auth strategy and host as relevant
    });

    async function installGoogleAdsIfNotInstalled() {
      const response = await myWixClient.appInstallers.installGoogleAdsIfNotInstalled();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
