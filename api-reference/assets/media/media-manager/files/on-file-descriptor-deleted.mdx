---
title: "onFileDescriptorDeleted"
description: "Triggered when a file is deleted."
icon: "bolt"
---

Triggered when a file is deleted.

If the `movedToTrash` property in the event object `true`, the file was moved to the Media Manager's trash bin. If the `movedToTrash` property in the event object is `false`, the file was permanently deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.media.site_media.v1.file_descriptor_deleted` |
| Package | `@wix/media` |
| Permissions | `Manage Bookings Services and Settings`, `Manage Events`, `Manage Challenges`, `Read Media Manager`, `Manage Media Manager`, `Manage Portfolio`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onFileDescriptorDeleted
  import { files } from '@wix/media';

  files.onFileDescriptorDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onFileDescriptorDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { files } from '@wix/media';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        files,
      },
  });

  wixClient.files.onFileDescriptorDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
