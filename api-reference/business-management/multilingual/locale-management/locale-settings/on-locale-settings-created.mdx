---
title: "onLocaleSettingsCreated"
description: "Triggered when the site is created."
icon: "bolt"
---

Triggered when the site is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.settings.v2.locale_settings_created` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API` |

## Examples

<CodeGroup>

  ```javascript onLocaleSettingsCreated
  import { localeSettings } from '@wix/multilingual';

  localeSettings.onLocaleSettingsCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocaleSettingsCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { localeSettings } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        localeSettings,
      },
  });

  wixClient.localeSettings.onLocaleSettingsCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
