---
title: "Delete Policy"
description: "Permanently deletes a policy."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a policy.

    Deleted policies are not returned by the Get Policy or Query Policy methods.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-POLICIES">Manage Policies</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/events/v2/policies/{policyId}
    ```

    ## Path Params

    <ParamField path="policyId" type="string" required>

      ID of the policy to delete.

    </ParamField>

    ## Response Object

    Returns `DeletePolicyResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Policy Deleted | `wix.events.v2.policy_deleted` | Triggered when a policy is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE 'https://www.wixapis.com/events/v2/policies/7243931d-74e4-4d6a-91f0-4835fa79161e' \
      -H 'Authorization: <AUTH TOKEN>' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a policy.

    Deleted policies are not returned by the Get Policy or Query Policy methods.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-POLICIES">Manage Policies</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { policies } from '@wix/events';

    policies.deletePolicy(policyId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="policyId" type="string" required>

      ID of the policy to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Policy Deleted | `wix.events.v2.policy_deleted` | Triggered when a policy is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-policy" id="ex-sdk-delete-policy-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-policy" id="ex-sdk-delete-policy-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-policy" id="ex-sdk-delete-policy-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-policy-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-policy-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete Policy</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Delete Policy (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deletePolicy (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-policy-0"><span className="wix-picker-name">Delete Policy</span></label>
    <label htmlFor="ex-sdk-delete-policy-1"><span className="wix-picker-name">Delete Policy (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-delete-policy-2"><span className="wix-picker-name">deletePolicy (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete Policy
    import { policies } from '@wix/events';

    //define policy ID
    const policyId = '0907cf78-5177-4482-a627-b17ef06badec';

    //delete policy
    function myDeletePolicyFunction(){
       try {
         return policies.deletePolicy(policyId);
       } 
       catch (error) {
         console.error(error);
         // Handle the error
       }
     };

    // Returns void
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Delete Policy (with elevated permissions)
    import { policies } from '@wix/events';
    import { auth } from '@wix/essentials';

    const elevatedDeletePolicy = auth.elevate(policies.deletePolicy);

    //define policy ID
    const policyId = '0907cf78-5177-4482-a627-b17ef06badec';

    //delete policy
    function myDeletePolicyFunction(){
       try {
         return elevatedDeletePolicy(policyId);
       } 
       catch (error) {
         console.error(error);
         // Handle the error
       }
     };

    // Returns void
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deletePolicy (self-hosted)
    import { createClient } from '@wix/sdk';
    import { policies } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { policies },
      // Include the auth strategy and host as relevant
    });

    async function deletePolicy(policyId) {
      const response = await myWixClient.policies.deletePolicy(policyId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
