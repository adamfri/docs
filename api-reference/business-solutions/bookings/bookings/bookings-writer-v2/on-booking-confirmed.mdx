---
title: "onBookingConfirmed"
description: "Triggered when a booking is confirmed."
icon: "bolt"
---

Triggered when a booking is confirmed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.v2.booking_confirmed` |
| Package | `@wix/bookings` |
| Permissions | `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions`, `Read bookings calendar - including participants` |

## Examples

<CodeGroup>

  ```javascript onBookingConfirmed
  import { bookings } from '@wix/bookings';

  bookings.onBookingConfirmed((event) => {
      // handle your event here
  });
  ```

  ```javascript onBookingConfirmed (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bookings } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bookings,
      },
  });

  wixClient.bookings.onBookingConfirmed((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
