---
title: "onTagDeleted"
description: "Triggered when a marketing tag is deleted."
icon: "bolt"
---

Triggered when a marketing tag is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.marketing.v1.tag_deleted` |
| Package | `@wix/marketing-tags` |
| Permissions | `Manage Marketing Tags`, `Read Marketing Tags` |

## Examples

<CodeGroup>

  ```javascript onTagDeleted: Wix-hosted
  import { marketingTags } from '@wix/marketing-tags';

  marketingTags.onTagDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onTagDeleted: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { marketingTags } from '@wix/marketing-tags';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        marketingTags,
      },
  });

  wixClient.marketingTags.onTagDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
