---
title: "onOrderInitiated"
description: "The onOrderInitiated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.ticketing.events.OrderInitiated` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Read Basic Events Order Info`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onOrderInitiated
  import { orders } from '@wix/events';

  orders.onOrderInitiated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderInitiated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderInitiated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
