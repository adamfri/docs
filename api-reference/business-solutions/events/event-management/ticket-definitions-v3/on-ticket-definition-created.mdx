---
title: "onTicketDefinitionCreated"
description: "Triggered when a ticket definition is created."
icon: "bolt"
---

Triggered when a ticket definition is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v3.ticket_definition_created` |
| Package | `@wix/events` |
| Permissions | `Manage Events - all permissions`, `Manage Orders`, `Manage Ticket Definitions` |

## Examples

<CodeGroup>

  ```javascript onTicketDefinitionCreated: Wix-hosted
  import { ticketDefinitionsV2 } from '@wix/events';

  ticketDefinitionsV2.onTicketDefinitionCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTicketDefinitionCreated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ticketDefinitionsV2 } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        ticketDefinitionsV2,
      },
  });

  wixClient.ticketDefinitionsV2.onTicketDefinitionCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
