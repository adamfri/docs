---
title: "Query Pool Definitions"
description: "Retrieves a list of pool definitions, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of pool definitions, given the provided paging, filtering, and sorting.

    Query Pool Definitions has a default paging limit of 50, which you can override.

    For field support for filters and sorting, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pool-definitions/filtering-and-sorting).

    To learn how to query pool definitions, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `namespace` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `programDefinitionIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |
    | `details.benefits.itemSetId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `details.benefits.providerAppId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/pool-definitions/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Filter, sort, and paging to apply to the query.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.
          See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language)
          for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order. Use `ASC` for ascending order or `DESC` for descending order.

              Default: `ASC`

              **Supported values:**

              - `ASC` — Ascending sort order.
              - `DESC` — Descending sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="poolDefinitions" type="PoolDefinition[]">

      List of retrieved pool definitions.

      See the [Pool Definition Object](/api-reference/business-solutions/benefit-programs/pool-definitions/pool-definition-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -XPOST https://www.wixapis.com/benefit-programs/v1/pool-definitions/query \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data '{
          "query": {
            "filter": {
              "$and": [
                {
                  "namespace": {
                    "$eq": "benefit_programs_app"
                  }
                },
                {
                  "programDefinitionIds": {
                    "$hasSome": [
                      "e8c3366e-0c4f-3024-a034-1e0c79146d4c"
                    ]
                  }
                }
              ]
            }
          }
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "pool_definitions": [
          {
            "id": "64244667-7c54-46da-8d8b-4a1a4ee9a598",
            "revision": "3",
            "created_date": "2024-12-27T10:20:18.625Z",
            "updated_date": "2024-12-27T10:43:36.170Z",
            "display_name": "Yoga courses",
            "program_definition_ids": [
              "e8c3366e-0c4f-3024-a034-1e0c79146d4c"
            ],
            "details": {
              "benefits": [
                {
                  "benefit_key": "001",
                  "item_set_id": "3e5c0ffa-fdf9-4dd2-8841-70f30d873dd9",
                  "price": "5",
                  "additional_data": null,
                  "provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                  "display_name": "Group session",
                  "description": null
                },
                {
                  "benefit_key": "002",
                  "item_set_id": "67ab0cd0-01d0-4bd9-acc0-bdf74dd5c14b",
                  "price": "15",
                  "additional_data": null,
                  "provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                  "display_name": "Private session",
                  "description": null
                }
              ],
              "credit_configuration": {
                "amount": "25",
                "rollover_configuration": {
                  "enabled": true,
                  "balance_cap": "30"
                },
                "unit_display_name": null
              },
              "additional_data": null
            },
            "namespace": "benefit_programs_app",
            "extended_fields": null
          },
        ],
        "metadata": {
          "count": 1,
          "cursors": {
            "next": null,
            "prev": null
          },
          "has_next": false
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of pool definitions, given the provided paging, filtering, and sorting.

    Query Pool Definitions has a default paging limit of 50, which you can override.

    For field support for filters and sorting, see [Filtering and Sorting](https://dev.wix.com/docs/rest/business-solutions/benefit-programs/pool-definitions/filtering-and-sorting).

    To learn how to query pool definitions, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `namespace` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `programDefinitionIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |
    | `details.benefits.itemSetId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `details.benefits.providerAppId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { poolDefinitions } from '@wix/benefit-programs';

    poolDefinitions.queryPoolDefinitions(query): PoolDefinitionsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="PoolDefinitionQuery" required>

      Filter, sort, and paging to apply to the query.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.
          See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language)
          for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order. Use `ASC` for ascending order or `DESC` for descending order.

              Default: `ASC`

              **Supported values:**

              - `ASC` — Ascending sort order.
              - `DESC` — Descending sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `PoolDefinitionsQueryBuilder`.

    <ResponseField name="poolDefinitions" type="PoolDefinition[]">

      List of retrieved pool definitions.

      See the [Pool Definition Object](/api-reference/business-solutions/benefit-programs/pool-definitions/pool-definition-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-pool-definitions" id="ex-sdk-query-pool-definitions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-pool-definitions" id="ex-sdk-query-pool-definitions-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-pool-definitions" id="ex-sdk-query-pool-definitions-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-pool-definitions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-pool-definitions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryPoolDefinitions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPoolDefinitions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPoolDefinitions (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-pool-definitions-0"><span className="wix-picker-name">queryPoolDefinitions</span></label>
    <label htmlFor="ex-sdk-query-pool-definitions-1"><span className="wix-picker-name">queryPoolDefinitions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-pool-definitions-2"><span className="wix-picker-name">queryPoolDefinitions (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryPoolDefinitions
    import { poolDefinitions } from '@wix/benefit-programs';

    async function queryPoolDefinitions(query) {
      const response = await poolDefinitions.queryPoolDefinitions(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPoolDefinitions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { poolDefinitions } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { poolDefinitions },
      // Include the auth strategy and host as relevant
    });

    async function queryPoolDefinitions(query) {
      const response = await myWixClient.poolDefinitions.queryPoolDefinitions(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPoolDefinitions (with elevated permissions)
    import { poolDefinitions } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myQueryPoolDefinitionsMethod(query) {
      const elevatedQueryPoolDefinitions = auth.elevate(poolDefinitions.queryPoolDefinitions);
      const response = await elevatedQueryPoolDefinitions(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
