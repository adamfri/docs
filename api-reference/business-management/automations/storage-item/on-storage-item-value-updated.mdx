---
title: "onStorageItemValueUpdated"
description: "The onStorageItemValueUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.automations.storage.v1.storage_item_storage_item_value_updated` |
| Package | `@wix/automations` |
| Permissions | `Manage Stores`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onStorageItemValueUpdated
  import { storageItem } from '@wix/automations';

  storageItem.onStorageItemValueUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onStorageItemValueUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { storageItem } from '@wix/automations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        storageItem,
      },
  });

  wixClient.storageItem.onStorageItemValueUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
