---
title: "List Badges"
description: "Retrieves up to 1,000 badges, given the requested paging."
icon: "download"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Retrieves up to 1,000 badges, given the requested paging.

    Default `paging.limit` is `100` and `paging.offset` is `0`.
    For more information, see
    [API Query Language: Paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-BADGES">Read Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/members/v3/badges
    ```

    ## Query Params

    <ParamField query="paging" type="Paging">

      Pagination options. For more information, see
      [API Query Language: Paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of items to load.

        </ParamField>

        <ParamField query="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="badges" type="Badge[]">

      Retrieved badges.

      See the [Badge Object](/api-reference/crm/members-contacts/members/activity/badges/badge-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Retrieves up to 1,000 badges, given the requested paging.

    Default `paging.limit` is `100` and `paging.offset` is `0`.
    For more information, see
    [API Query Language: Paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-BADGES">Read Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { badges } from '@wix/members';

    badges.listBadges(options): Promise<ListBadgesResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListBadgesOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="Paging">

          Pagination options. For more information, see
          [API Query Language: Paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListBadgesResponse>`.

    <ResponseField name="badges" type="Badge[]">

      Retrieved badges.

      See the [Badge Object](/api-reference/crm/members-contacts/members/activity/badges/badge-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-badges" id="ex-sdk-list-badges-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-badges" id="ex-sdk-list-badges-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-badges-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-badges-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Lists all badges</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listBadges (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-badges-0"><span className="wix-picker-name">Lists all badges</span></label>
    <label htmlFor="ex-sdk-list-badges-1"><span className="wix-picker-name">listBadges (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Lists all badges
    import { badges } from '@wix/members';

    /* Sample options value:
     * {
     *   paging: {
     *     limit: 3,
     *     offset: 0
     *   }
     * }
     */

    export async function myListBadgesFunction (options) {
      try {
        const myBadges = await badges.listBadges(options);

        console.log('List of badges: ', myBadges)
        return myBadges;
      } catch (error) {
        console.error(error);
        // Handle the error

      }
    }
    /* Promise resolves to:
     * {
     *  "badges": [
     *    {
     *      "_createdDate": "2024-01-18T13:53:11.659Z",
     *      "_id": "b657b857-774a-4dde-9b0e-f364d61092ea",
     *      "_updatedDate": "2024-01-18T13:53:11.659Z",
     *      "backgroundColor": "#796EFF",
     *      "description": "Completed challenging tasks and achieved milestones.",
     *      "icon": "https://static.wixstatic.com/shapes/11062b_30b982c4ceb141d884fa15ecfb07d26c.svg",
     *      "permissionsEnabled": false,
     *      "slug": "achievement-unlocked",
     *      "textColor": "#FFFFFF",
     *      "title": "Achievement Unlocked"
     *    },
     *    {
     *      "_createdDate": "2024-01-18T13:53:37.067Z",
     *      "_id": "e80fed60-687e-4b91-a24c-252c4b32227b",
     *      "_updatedDate": "2024-01-18T13:53:37.067Z",
     *      "backgroundColor": "#FF5C79",
     *      "description": "Recognized for outstanding community contributions and leadership.",
     *      "icon": "https://static.wixstatic.com/shapes/cb523e8b3f9b4908b85a4a1c577af32b.svg",
     *      "permissionsEnabled": false,
     *      "slug": "community-hero",
     *      "textColor": "#FFFFFF",
     *      "title": "Community Hero"
     *    },
     *    {
     *      "_createdDate": "2024-01-18T10:27:14.878Z",
     *      "_id": "215be5d9-4b32-4861-9eb5-2152930dd0b4",
     *      "_updatedDate": "2024-01-18T10:27:14.878Z",
     *      "backgroundColor": "#13785C",
     *      "description": "Most comments on site.",
     *      "icon": "https://static.wixstatic.com/shapes/11062b_73d6472a03884c758f9d39f83a1218c2.svg",
     *      "permissionsEnabled": false,
     *      "slug": "top-commenter",
     *      "textColor": "#FFFFFF",
     *      "title": "Top commenter"
     *    }
     *  ],
     *  "metadata": {
     *    "count": 3,
     *    "offset": 0,
     *    "tooManyToCount": false,
     *    "total": 3
     *  }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listBadges (self-hosted)
    import { createClient } from '@wix/sdk';
    import { badges } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { badges },
      // Include the auth strategy and host as relevant
    });

    async function listBadges(options) {
      const response = await myWixClient.badges.listBadges(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
