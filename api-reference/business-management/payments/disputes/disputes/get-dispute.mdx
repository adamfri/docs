---
title: "Get Dispute"
description: "Retrieves a dispute."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a dispute.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.READ-DISPUTES">Read Disputes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/payments/disputes/v1/disputes/{disputeId}
    ```

    ## Path Params

    <ParamField path="disputeId" type="string" required>

      ID of the dispute to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="dispute" type="Dispute">

      The retrieved dispute.

      See the [Dispute Object](/api-reference/business-management/payments/disputes/disputes/dispute-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves complete information about a specific dispute including status and available actions

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/payments/disputes/v1/disputes/12345678-1234-1234-1234-123456789012' \
      -H 'Authorization: <AUTH>'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a dispute.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.READ-DISPUTES">Read Disputes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { disputes } from '@wix/payments';

    disputes.getDispute(disputeId): Promise
    ```

    ## Method Parameters

    <ParamField body="disputeId" type="string" required>

      ID of the dispute to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Dispute`.

    See the [Dispute Object](/api-reference/business-management/payments/disputes/disputes/dispute-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-dispute" id="ex-sdk-get-dispute-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-dispute" id="ex-sdk-get-dispute-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-dispute" id="ex-sdk-get-dispute-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-dispute-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-dispute-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getDispute</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDispute (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDispute (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-dispute-0"><span className="wix-picker-name">getDispute</span></label>
    <label htmlFor="ex-sdk-get-dispute-1"><span className="wix-picker-name">getDispute (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-dispute-2"><span className="wix-picker-name">getDispute (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getDispute
    import { disputes } from '@wix/payments';

    async function getDispute(disputeId) {
      const response = await disputes.getDispute(disputeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDispute (self-hosted)
    import { createClient } from '@wix/sdk';
    import { disputes } from '@wix/payments';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { disputes },
      // Include the auth strategy and host as relevant
    });

    async function getDispute(disputeId) {
      const response = await myWixClient.disputes.getDispute(disputeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDispute (with elevated permissions)
    import { disputes } from '@wix/payments';
    import { auth } from '@wix/essentials';

    async function myGetDisputeMethod(disputeId) {
      const elevatedGetDispute = auth.elevate(disputes.getDispute);
      const response = await elevatedGetDispute(disputeId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
