---
title: "Search Data Items"
description: "Searches for items in a collection on the basis of the filtering, sorting, searching, and paging preferences you provide."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Searches for items in a collection on the basis of the filtering, sorting, searching, and paging preferences you provide.

    Learn more about [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    <Note>

    - You can only search collections [created in the CMS](https://support.wix.com/en/article/cms-formerly-content-manager-creating-a-collection) or with the [Data Collections API](https://dev.wix.com/docs/rest/business-solutions/cms/data-collections/introduction). Searching isn't currently supported for [Wix app collections](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections), [external collections](https://dev.wix.com/docs/develop-websites/articles/databases/external-databases/overview/integrating-external-databases-with-your-wix-site) or [Wix Blocks](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/wix-blocks/about-wix-blocks).
    - Filtering is only supported for fields explicitly defined in the [data collection structure](https://dev.wix.com/docs/rest/business-solutions/cms/data-collections/introduction#data-collections-and-their-structure). Fields not defined in the collection structure cannot be used for filtering.
    - Searching for data items is always [eventually consistent](https://dev.wix.com/docs/rest/business-solutions/cms/eventual-consistency).

    </Note>

    ### Supported filter operators by field type

    Each [field type](https://support.wix.com/en/article/cms-formerly-content-manager-about-your-collection-fields#field-type) supports a specific set of [filter operators](https://dev.wix.com/docs/rest/articles/get-started/api-query-language#operators). Using a filter operator that's not supported for a given field type causes the search to fail.

    | Field Type | Supported Filter Operators |
    |------------|----------------------------|
    | Text, Image, Document, URL, Video, Audio, Time, Reference, [Page Link](https://support.wix.com/en/article/cms-formerly-content-manager-about-your-collection-fields#page-link-fields), [Rich Text](https://dev.wix.com/docs/rest/articles/get-started/rich-text), [Rich Content](https://dev.wix.com/docs/ricos/getting-started/introduction), Address | `$in`, `$eq`, `$ne`, `$gt`, `$gte`, `$lt`, `$lte`, `$isEmpty`, `$startsWith` |
    | Number, Date, Date and Time | `$eq`, `$ne`, `$gt`, `$gte`, `$lt`, `$lte`, `$exists` |
    | Boolean | `$eq`, `$ne`, `$exists` |
    | Array of strings | `$hasSome`, `$hasAll`, `$exists` |
    | Array, Document Array, Object, Any, Media Gallery | `$exists` |

    Learn more about [data types in Wix Data](https://dev.wix.com/docs/rest/business-solutions/cms/data-types-in-wix-data).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.READ">Read Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data/v2/items/search
    ```

    ## Body Params

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection to search.

      Constraints: `maxLength 255`.

    </ParamField>

    <ParamField body="search" type="Search">

      Search query configuration.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about filters in [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about sorting in [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language#sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `minLength 1`, `maxLength 1000`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include for each retrieved item. Only fields specified in the array are included in the response. If the array is empty, all fields are returned.

          > **Note:** The `_id` system field is always returned.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Text to search in the specified collection.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Determines how multiple search terms are handled.

              Default: `OR`.

              **Supported values:**

              - `OR` — Logical **OR** relationship. Any search term must exist for an item to be retrieved.
              - `AND` — Logical **AND** relationship. All search terms must exist for an item to be retrieved.

            </ParamField>

            <ParamField body="expression" type="string">

              Search query. Search terms are separated by whitespace.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use fuzzy search to accommodate typos and partial matches.

              Default: `false`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="paging" type="Paging">

              Options for limiting and skipping the number of retrieved items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor pointing to a page of results. Not used in the first request.

              Subsequent requests use this cursor and ignore `filter` and `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="publishPluginOptions" type="PublishPluginOptions">

      Options for the Publish plugin.

      This plugin allows items to be marked as draft or published. Published items are visible to site visitors, while draft items are not.

      <Expandable title="publishPluginOptions properties">

        <ParamField body="includeDraftItems" type="boolean">

          Whether to include draft items.

          When `true`, both published and draft items are affected. Default: `false`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="referencedItemOptions" type="ReferencedItemOptions[]" deprecated>

      Options for retrieving referenced items.

      Learn more about [using reference fields](https://support.wix.com/en/article/cms-using-reference-fields-to-display-content-from-multiple-collections).

      **Deprecated.** Use `include_references` instead. Removal: 2026-08-01. Constraints: `maxItems 100`.

      <Expandable title="referencedItemOptions properties">

        <ParamField body="fieldName" type="string">

          Field in the retrieved item that contains references.

          Constraints: `maxLength 1000`.

        </ParamField>

        <ParamField body="limit" type="integer">

          Maximum number of referenced items to include for each retrieved item.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="includeReferences" type="IncludeReference[]">

      Fields for which to include referenced items. For each reference field specified, the response includes the full data items being referenced. Use this to retrieve related data in a single call without additional queries. Maximum 100 reference configurations can be specified.

      Constraints: `maxItems 100`.

      <Expandable title="includeReferences properties">

        <ParamField body="field" type="string">

          Field containing references in the item. This field must be configured as a reference or multi-reference field in the collection schema.

          Constraints: `maxLength 1000`.

        </ParamField>

        <ParamField body="limit" type="integer">

          Maximum number of referenced items to include for each item. When a reference field contains more items than this limit, only the first items up to the limit are included in the response. If not specified, up to 50 items are included.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="dataItems" type="DataItem[]">

      Retrieved items.

      Constraints: `maxItems 1000`.

      See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging configuration.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used, `returnTotalCount` is `true` in the request, and `tooManyToCount` is false.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Whether the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-search-data-items" id="ex-rest-search-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-search-data-items" id="ex-rest-search-data-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-search-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-search-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search for California cities containing "Palm" or "Valley", sorted in ascending order by population</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Search for cities that contain "San Francisco" in any field</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-search-data-items-0"><span className="wix-picker-name">Search for California cities containing "Palm" or "Valley", sorted in ascending order by population</span></label>
    <label htmlFor="ex-rest-search-data-items-1"><span className="wix-picker-name">Search for cities that contain "San Francisco" in any field</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items/search' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "cities",
          "search": {
              "filter": {
                  "state": "California"
              },
              "sort": [
                  {
                      "fieldName": "population",
                      "order": "ASC"
                  }
              ],
              "search": {
                  "expression": "Palm Valley"
              },
              "paging": {
                  "limit": 3
              }
          }
      }'
      ```

      ```json Response
      {
          "dataItems": [
              {
                  "id": "sv001",
                  "dataCollectionId": "cities",
                  "data": {
                      "_id": "sv001",
                      "city": "Scotts Valley",
                      "state": "California",
                      "population": 12224.0
                  }
              },
              {
                  "id": "ps001",
                  "dataCollectionId": "cities",
                  "data": {
                      "_id": "ps001",
                      "city": "Palm Springs",
                      "state": "California",
                      "population": 44575.0
                  }
              },
              {
                  "id": "av001",
                  "dataCollectionId": "cities",
                  "data": {
                      "_id": "av001",
                      "city": "Apple Valley",
                      "state": "California",
                      "population": 75791.0
                  }
              }
          ],
          "pagingMetadata": {
              "count": 3,
              "total": 15,
              "tooManyToCount": false
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items/search' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "cities",
          "search": {
              "search": {
                  "expression": "Francisco",
                  "mode": "AND"
              },
              "paging": {
                  "limit": 10
              },
              "fields": ["city", "state", "population"]
          }
      }'
      ```

      ```json Response
      {
          "dataItems": [
              {
                  "id": "sf001",
                  "dataCollectionId": "cities",
                  "data": {
                      "_id": "sf001",
                      "city": "San Francisco",
                      "state": "California",
                      "population": 873965.0
                  }
              }
          ],
          "pagingMetadata": {
              "count": 1,
              "total": 1,
              "tooManyToCount": false
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Searches for items in a collection on the basis of the filtering, sorting, searching, and paging preferences you provide.

    Learn more about [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    <Note>

    - You can only search collections [created in the CMS](https://support.wix.com/en/article/cms-formerly-content-manager-creating-a-collection) or with the [Data Collections API](https://dev.wix.com/docs/rest/business-solutions/cms/data-collections/introduction). Searching isn't currently supported for [Wix app collections](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections), [external collections](https://dev.wix.com/docs/develop-websites/articles/databases/external-databases/overview/integrating-external-databases-with-your-wix-site) or [Wix Blocks](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/wix-blocks/about-wix-blocks).
    - Filtering is only supported for fields explicitly defined in the [data collection structure](https://dev.wix.com/docs/rest/business-solutions/cms/data-collections/introduction#data-collections-and-their-structure). Fields not defined in the collection structure cannot be used for filtering.
    - Searching for data items is always [eventually consistent](https://dev.wix.com/docs/rest/business-solutions/cms/eventual-consistency).

    </Note>

    ### Supported filter operators by field type

    Each [field type](https://support.wix.com/en/article/cms-formerly-content-manager-about-your-collection-fields#field-type) supports a specific set of [filter operators](https://dev.wix.com/docs/rest/articles/get-started/api-query-language#operators). Using a filter operator that's not supported for a given field type causes the search to fail.

    | Field Type | Supported Filter Operators |
    |------------|----------------------------|
    | Text, Image, Document, URL, Video, Audio, Time, Reference, [Page Link](https://support.wix.com/en/article/cms-formerly-content-manager-about-your-collection-fields#page-link-fields), [Rich Text](https://dev.wix.com/docs/rest/articles/get-started/rich-text), [Rich Content](https://dev.wix.com/docs/ricos/getting-started/introduction), Address | `$in`, `$eq`, `$ne`, `$gt`, `$gte`, `$lt`, `$lte`, `$isEmpty`, `$startsWith` |
    | Number, Date, Date and Time | `$eq`, `$ne`, `$gt`, `$gte`, `$lt`, `$lte`, `$exists` |
    | Boolean | `$eq`, `$ne`, `$exists` |
    | Array of strings | `$hasSome`, `$hasAll`, `$exists` |
    | Array, Document Array, Object, Any, Media Gallery | `$exists` |

    Learn more about [data types in Wix Data](https://dev.wix.com/docs/rest/business-solutions/cms/data-types-in-wix-data).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.READ">Read Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/data';

    items.search(dataCollectionId, searchRequest, options): Promise
    ```

    ## Method Parameters

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection to search.

      Constraints: `maxLength 255`.

    </ParamField>

    <ParamField body="searchRequest" type="WixDataSearchRequest" required>

      searchRequest

      <Expandable title="searchRequest properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about filters in [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about sorting in [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language#sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `minLength 1`, `maxLength 1000`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include for each retrieved item. Only fields specified in the array are included in the response. If the array is empty, all fields are returned.

          > **Note:** The `_id` system field is always returned.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Text to search in the specified collection.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Determines how multiple search terms are handled.

              Default: `OR`.

              **Supported values:**

              - `OR` — Logical **OR** relationship. Any search term must exist for an item to be retrieved.
              - `AND` — Logical **AND** relationship. All search terms must exist for an item to be retrieved.

            </ParamField>

            <ParamField body="expression" type="string">

              Search query. Search terms are separated by whitespace.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use fuzzy search to accommodate typos and partial matches.

              Default: `false`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="paging" type="Paging">

              Options for limiting and skipping the number of retrieved items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor pointing to a page of results. Not used in the first request.

              Subsequent requests use this cursor and ignore `filter` and `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="WixDataDirectSearchOptions">

      Options to use when processing this operation.

      <Expandable title="options properties">

        <ParamField body="suppressHooks" type="boolean">

          Prevents hooks from running for the operation. Can only be used in the [backend code of a Wix site](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/backend-code/about-the-site-backend).

        </ParamField>

        <ParamField body="showDrafts" type="boolean">

          When `true`, operations include draft items. Read operations include draft items in their response, and write operations modify draft items.
          Default: `false`.

        </ParamField>

        <ParamField body="appOptions" type="object">

          Options for [querying Wix app collections](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-app-collections/querying-wix-app-collections).

        </ParamField>

        <ParamField body="language" type="string">

          Language to translate result text into, in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
          If provided, the result text is returned in the specified language. If not provided, the result text is not translated.

          > **Note:** Translation for the specified language must be enabled for the collection in [Wix Multilingual](https://www.wix.com/app-market/wix-multilingual).

        </ParamField>

        <ParamField body="consistentRead" type="boolean">

          When `true`, reads data from the primary database instance. This decreases performance but ensures data retrieved is up-to-date even immediately after an update. Learn more about [Wix Data and eventual consistency](https://dev.wix.com/docs/sdk/backend-modules/data/eventual-consistency).

        </ParamField>

        <ParamField body="includeReferences" type="IncludeReference[]">

          Reference fields to resolve in the results. When specified, the referenced items are included in the response for the given fields.

          Each entry identifies a reference `field` by name and an optional `limit` that caps the number of referenced items returned per queried item (relevant for multi-reference fields).

          <Expandable title="includeReferences properties">

            <ParamField body="field" type="string">

              Field containing references in the item. This field must be configured as a reference or multi-reference field in the collection schema.

              Constraints: `maxLength 1000`.

            </ParamField>

            <ParamField body="limit" type="integer">

              Maximum number of referenced items to include for each item. When a reference field contains more items than this limit, only the first items up to the limit are included in the response. If not specified, up to 50 items are included.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="items" type="WixDataItem[]">

      Retrieved items.

      See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadata">

      Paging information.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used, `returnTotalCount` is `true` in the request, and `tooManyToCount` is false.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Whether the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-data-items" id="ex-sdk-search-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-data-items" id="ex-sdk-search-data-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-search-data-items" id="ex-sdk-search-data-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">search</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">search (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">search (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-data-items-0"><span className="wix-picker-name">search</span></label>
    <label htmlFor="ex-sdk-search-data-items-1"><span className="wix-picker-name">search (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-search-data-items-2"><span className="wix-picker-name">search (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript search
    import { items } from '@wix/data';

    async function search(dataCollectionId,searchRequest,options) {
      const response = await items.search(dataCollectionId,searchRequest,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript search (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function search(dataCollectionId,searchRequest,options) {
      const response = await myWixClient.items.search(dataCollectionId,searchRequest,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript search (with elevated permissions)
    import { items } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function mySearchMethod(dataCollectionId,searchRequest,options) {
      const elevatedSearch = auth.elevate(items.search);
      const response = await elevatedSearch(dataCollectionId,searchRequest,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
