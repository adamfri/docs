---
title: "Pause Program"
description: "Pauses the specified program."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Pauses the specified program. Benefits in the program's pools can't be redeemed until the program is resumed.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/programs/pause
    ```

    ## Body Params

    <ParamField body="programId" type="string" required>

      ID of the program to pause.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="idempotencyKey" type="string" required>

      Idempotency key to ensure that the request is processed only once.

      Constraints: `maxLength 64`.

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID of the pausing of the program. Retrieve job details using the [Async Job API](https://dev.wix.com/docs/rest/business-management/async-job/introduction).

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PROGRAM_STATUS_TRANSITION_NOT_ALLOWED` | Program isn't active and can't be paused. |
    | `409` | `ALREADY_EXISTS` | `ALREADY_EXECUTED` | Program was already paused. |
    | `409` | `ALREADY_EXISTS` | `IN_PROGRESS` | Program pausing is in progress. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request POST https://www.wixapis.com/benefit-programs/v1/programs/pause \
        -H "Authorization: <AUTH>" \
        -H "Content-Type: application/json" \
        --data '{
          "program_id": "e5c42e1e-cd10-4210-b483-bb59932b1fa7"
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "job_id": "1f956c8f-35d1-490e-b478-018db73270f4"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Pauses the specified program. Benefits in the program's pools can't be redeemed until the program is resumed.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/benefit-programs';

    programs.pauseProgram(programId, options): Promise<PauseProgramResponse>
    ```

    ## Method Parameters

    <ParamField body="programId" type="string" required>

      ID of the program to pause.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="PauseProgramOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="idempotencyKey" type="string" required>

          Idempotency key to ensure that the request is processed only once.

          Constraints: `maxLength 64`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<PauseProgramResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID of the pausing of the program. Retrieve job details using the [Async Job API](https://dev.wix.com/docs/rest/business-management/async-job/introduction).

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PROGRAM_STATUS_TRANSITION_NOT_ALLOWED` | Program isn't active and can't be paused. |
    | `409` | `ALREADY_EXISTS` | `ALREADY_EXECUTED` | Program was already paused. |
    | `409` | `ALREADY_EXISTS` | `IN_PROGRESS` | Program pausing is in progress. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Updated | `wix.benefit_programs.v1.program_updated` | Triggered when a program is updated. |
    | Program Paused | `wix.benefit_programs.v1.program_paused` | Triggered when a program is paused. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-pause-program" id="ex-sdk-pause-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-pause-program" id="ex-sdk-pause-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-pause-program" id="ex-sdk-pause-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-pause-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-pause-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">pauseProgram</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">pauseProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">pauseProgram (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-pause-program-0"><span className="wix-picker-name">pauseProgram</span></label>
    <label htmlFor="ex-sdk-pause-program-1"><span className="wix-picker-name">pauseProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-pause-program-2"><span className="wix-picker-name">pauseProgram (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript pauseProgram
    import { programs } from '@wix/benefit-programs';

    async function pauseProgram(programId,options) {
      const response = await programs.pauseProgram(programId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript pauseProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function pauseProgram(programId,options) {
      const response = await myWixClient.programs.pauseProgram(programId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript pauseProgram (with elevated permissions)
    import { programs } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myPauseProgramMethod(programId,options) {
      const elevatedPauseProgram = auth.elevate(programs.pauseProgram);
      const response = await elevatedPauseProgram(programId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
