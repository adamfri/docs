---
title: "onLabelUpdated"
description: "Triggered when a label is updated."
icon: "bolt"
---

Triggered when a label is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.contacts.v4.label_updated` |
| Package | `@wix/crm` |
| Permissions | `Manage Stores`, `Manage Bookings Services and Settings`, `Manage Contact Labels`, `Manage Events`, `Manage Challenges`, `Manage Portfolio`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Manage Members and Contacts - all permissions` |

## Examples

<CodeGroup>

  ```javascript onLabelUpdated
  import { labels } from '@wix/crm';

  labels.onLabelUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLabelUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { labels } from '@wix/crm';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        labels,
      },
  });

  wixClient.labels.onLabelUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
