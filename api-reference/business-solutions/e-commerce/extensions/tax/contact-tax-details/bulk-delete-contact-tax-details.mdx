---
title: "Bulk Delete Contact Tax Details"
description: "Deletes the tax details of multiple contacts in a single request."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes the tax details of multiple contacts in a single request.

    To delete tax details for a single contact, call [Delete Contact Tax Details](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/contact-tax-details/delete-contact-tax-details).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/billing/v1/bulk/contact-tax-details/delete
    ```

    ## Body Params

    <ParamField body="contactTaxDetailsIds" type="string[]" required>

      IDs of the contacts whose tax details to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkContactTaxDetailsResult[]">

      Results of the bulk delete operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the single delete operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk delete operation metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Contact Tax Details Deleted | `wix.billing.v1.contact_tax_details_deleted` | Triggered when a contact's tax details are deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes the tax details of multiple contacts in a single request.

    <CodeGroup>

      ```bash Request
      curl -X POST \
          'https://www.wixapis.com/billing/v1/bulk/contact-tax-details/delete' \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
          "contactTaxDetailsIds": [
            "f1c3e2a0-1b2c-4d5e-8f90-1a2b3c4d5e6f",
            "a2b4d6e8-2c3d-4e5f-9a01-2b3c4d5e6f70"
          ]
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "f1c3e2a0-1b2c-4d5e-8f90-1a2b3c4d5e6f",
              "originalIndex": 0,
              "success": true
            }
          },
          {
            "itemMetadata": {
              "id": "a2b4d6e8-2c3d-4e5f-9a01-2b3c4d5e6f70",
              "originalIndex": 1,
              "success": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes the tax details of multiple contacts in a single request.

    To delete tax details for a single contact, call [Delete Contact Tax Details](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/contact-tax-details/delete-contact-tax-details).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contactTaxDetails } from '@wix/ecom';

    contactTaxDetails.bulkDeleteContactTaxDetails(contactTaxDetailsIds): Promise
    ```

    ## Method Parameters

    <ParamField body="contactTaxDetailsIds" type="string[]" required>

      IDs of the contacts whose tax details to delete.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkContactTaxDetailsResult[]">

      Results of the bulk delete operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the single delete operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk delete operation metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Contact Tax Details Deleted | `wix.billing.v1.contact_tax_details_deleted` | Triggered when a contact's tax details are deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-contact-tax-details" id="ex-sdk-bulk-delete-contact-tax-details-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-contact-tax-details" id="ex-sdk-bulk-delete-contact-tax-details-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-contact-tax-details" id="ex-sdk-bulk-delete-contact-tax-details-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-contact-tax-details-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-contact-tax-details-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteContactTaxDetails</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteContactTaxDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteContactTaxDetails (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-contact-tax-details-0"><span className="wix-picker-name">bulkDeleteContactTaxDetails</span></label>
    <label htmlFor="ex-sdk-bulk-delete-contact-tax-details-1"><span className="wix-picker-name">bulkDeleteContactTaxDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-contact-tax-details-2"><span className="wix-picker-name">bulkDeleteContactTaxDetails (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteContactTaxDetails
    import { contactTaxDetails } from '@wix/ecom';

    async function bulkDeleteContactTaxDetails(contactTaxDetailsIds) {
      const response = await contactTaxDetails.bulkDeleteContactTaxDetails(contactTaxDetailsIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteContactTaxDetails (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contactTaxDetails } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contactTaxDetails },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteContactTaxDetails(contactTaxDetailsIds) {
      const response = await myWixClient.contactTaxDetails.bulkDeleteContactTaxDetails(contactTaxDetailsIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteContactTaxDetails (with elevated permissions)
    import { contactTaxDetails } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteContactTaxDetailsMethod(contactTaxDetailsIds) {
      const elevatedBulkDeleteContactTaxDetails = auth.elevate(contactTaxDetails.bulkDeleteContactTaxDetails);
      const response = await elevatedBulkDeleteContactTaxDetails(contactTaxDetailsIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
