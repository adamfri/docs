---
title: "Delete Intake Form Submission"
description: "Deletes an intake form submission."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an intake form submission.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INTAKE-FORM.MANAGE_LIMITED">Manage Intake Form (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/_api/intake-forms/v1/submissions/{intakeFormSubmissionId}
    ```

    ## Path Params

    <ParamField path="intakeFormSubmissionId" type="string" required>

      Intake form submission ID.

    </ParamField>

    ## Response Object

    Returns `DeleteIntakeFormSubmissionResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/intake-forms/v1/intake-form-submissions/880e8400-e29b-41d4-a716-446655440003' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an intake form submission.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INTAKE-FORM.MANAGE_LIMITED">Manage Intake Form (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { intakeFormSubmissions } from '@wix/intake-forms';

    intakeFormSubmissions.deleteIntakeFormSubmission(intakeFormSubmissionId): Promise
    ```

    ## Method Parameters

    <ParamField body="intakeFormSubmissionId" type="string" required>

      Intake form submission ID.

    </ParamField>

    ## Returns

    Returns `DeleteIntakeFormSubmissionResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-intake-form-submission" id="ex-sdk-delete-intake-form-submission-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-intake-form-submission" id="ex-sdk-delete-intake-form-submission-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-intake-form-submission" id="ex-sdk-delete-intake-form-submission-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-intake-form-submission-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-intake-form-submission-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteIntakeFormSubmission</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteIntakeFormSubmission (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteIntakeFormSubmission (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-intake-form-submission-0"><span className="wix-picker-name">deleteIntakeFormSubmission</span></label>
    <label htmlFor="ex-sdk-delete-intake-form-submission-1"><span className="wix-picker-name">deleteIntakeFormSubmission (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-intake-form-submission-2"><span className="wix-picker-name">deleteIntakeFormSubmission (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteIntakeFormSubmission
    import { intakeFormSubmissions } from '@wix/intake-forms';

    async function deleteIntakeFormSubmission(intakeFormSubmissionId) {
      const response = await intakeFormSubmissions.deleteIntakeFormSubmission(intakeFormSubmissionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteIntakeFormSubmission (self-hosted)
    import { createClient } from '@wix/sdk';
    import { intakeFormSubmissions } from '@wix/intake-forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { intakeFormSubmissions },
      // Include the auth strategy and host as relevant
    });

    async function deleteIntakeFormSubmission(intakeFormSubmissionId) {
      const response = await myWixClient.intakeFormSubmissions.deleteIntakeFormSubmission(intakeFormSubmissionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteIntakeFormSubmission (with elevated permissions)
    import { intakeFormSubmissions } from '@wix/intake-forms';
    import { auth } from '@wix/essentials';

    async function myDeleteIntakeFormSubmissionMethod(intakeFormSubmissionId) {
      const elevatedDeleteIntakeFormSubmission = auth.elevate(intakeFormSubmissions.deleteIntakeFormSubmission);
      const response = await elevatedDeleteIntakeFormSubmission(intakeFormSubmissionId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
