---
title: "onReferredFriendDeleted"
description: "Triggered when a referred friend is deleted."
icon: "bolt"
---

Triggered when a referred friend is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.referral.v1.referred_friend_deleted` |
| Package | `@wix/referral` |

## Examples

<CodeGroup>

  ```javascript onReferredFriendDeleted
  import { friends } from '@wix/referral';

  friends.onReferredFriendDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onReferredFriendDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { friends } from '@wix/referral';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        friends,
      },
  });

  wixClient.friends.onReferredFriendDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
