---
title: "Delete Resource Type"
description: "Deletes a resource type."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a resource type.

    Deleting a resource type also automatically deletes all resources connected to it.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/bookings/v2/resources/resource-types/{resourceTypeId}
    ```

    ## Path Params

    <ParamField path="resourceTypeId" type="string" required>

      ID of the resource type to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteResourceTypeResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Resource Type Deleted | `wix.bookings.resources.v2.resource_type_deleted` | Triggered when a resource type is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/bookings/v2/resources/resource-types/28221941-9d0d-4c91-806b-e2a08073d37a' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a resource type.

    Deleting a resource type also automatically deletes all resources connected to it.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { resourceTypes } from '@wix/bookings';

    resourceTypes.deleteResourceType(resourceTypeId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="resourceTypeId" type="string" required>

      ID of the resource type to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Resource Type Deleted | `wix.bookings.resources.v2.resource_type_deleted` | Triggered when a resource type is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-resource-type" id="ex-sdk-delete-resource-type-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-resource-type" id="ex-sdk-delete-resource-type-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-resource-type" id="ex-sdk-delete-resource-type-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-resource-type-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-resource-type-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteResourceType</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteResourceType (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteResourceType (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-resource-type-0"><span className="wix-picker-name">deleteResourceType</span></label>
    <label htmlFor="ex-sdk-delete-resource-type-1"><span className="wix-picker-name">deleteResourceType (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-resource-type-2"><span className="wix-picker-name">deleteResourceType (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteResourceType
    import { resourceTypes } from '@wix/bookings';

    async function deleteResourceType(resourceTypeId) {
      const response = await resourceTypes.deleteResourceType(resourceTypeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteResourceType (self-hosted)
    import { createClient } from '@wix/sdk';
    import { resourceTypes } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { resourceTypes },
      // Include the auth strategy and host as relevant
    });

    async function deleteResourceType(resourceTypeId) {
      const response = await myWixClient.resourceTypes.deleteResourceType(resourceTypeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteResourceType (with elevated permissions)
    import { resourceTypes } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myDeleteResourceTypeMethod(resourceTypeId) {
      const elevatedDeleteResourceType = auth.elevate(resourceTypes.deleteResourceType);
      const response = await elevatedDeleteResourceType(resourceTypeId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
