---
title: "onPostReported"
description: "An existing post was reported as inappropriate."
icon: "bolt"
---

An existing post was reported as inappropriate.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_reported` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostReported
  import { posts } from '@wix/forum';

  posts.onPostReported((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostReported (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostReported((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
