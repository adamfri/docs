---
title: "Get Default Sender Details"
description: "Retrieves default sender details."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves default sender details.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/sender-details/v1/sender-details/default
    ```

    ## Response Object

    <ResponseField name="senderDetails" type="SenderDetails">

      Default sender details.

      See the [Sender Details Object](/api-reference/business-management/marketing/emails/sender-details/sender-details-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `DOES_NOT_EXIST` | Couldn't find the default sender details. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl 'https://www.wixapis.com/sender-details/v1/sender-details/default' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "senderDetails": {
          "id": "3dcaeca5-2b09-441c-86ab-808d85061511",
          "createdDate": "2024-01-01T00:00:00.000Z",
          "updatedDate": "2024-01-01T00:00:00.000Z",
          "fromName": "John Doe",
          "fromEmailAddress": "john.doe@example.com",
          "default": true
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves default sender details.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderDetails } from '@wix/sender-details';

    senderDetails.getDefaultSenderDetails(): Promise<GetDefaultSenderDetailsResponse>
    ```

    ## Returns

    Returns `Promise<GetDefaultSenderDetailsResponse>`.

    <ResponseField name="senderDetails" type="SenderDetails">

      Default sender details.

      See the [Sender Details Object](/api-reference/business-management/marketing/emails/sender-details/sender-details-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `DOES_NOT_EXIST` | Couldn't find the default sender details. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-default-sender-details" id="ex-sdk-get-default-sender-details-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-default-sender-details" id="ex-sdk-get-default-sender-details-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-default-sender-details" id="ex-sdk-get-default-sender-details-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-default-sender-details-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-default-sender-details-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getDefaultSenderDetails</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDefaultSenderDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDefaultSenderDetails (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-default-sender-details-0"><span className="wix-picker-name">getDefaultSenderDetails</span></label>
    <label htmlFor="ex-sdk-get-default-sender-details-1"><span className="wix-picker-name">getDefaultSenderDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-default-sender-details-2"><span className="wix-picker-name">getDefaultSenderDetails (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getDefaultSenderDetails
    import { senderDetails } from '@wix/sender-details';

    async function getDefaultSenderDetails() {
      const response = await senderDetails.getDefaultSenderDetails();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDefaultSenderDetails (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderDetails } from '@wix/sender-details';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderDetails },
      // Include the auth strategy and host as relevant
    });

    async function getDefaultSenderDetails() {
      const response = await myWixClient.senderDetails.getDefaultSenderDetails();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDefaultSenderDetails (with elevated permissions)
    import { senderDetails } from '@wix/sender-details';
    import { auth } from '@wix/essentials';

    async function myGetDefaultSenderDetailsMethod() {
      const elevatedGetDefaultSenderDetails = auth.elevate(senderDetails.getDefaultSenderDetails);
      const response = await elevatedGetDefaultSenderDetails();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
