---
title: "List Bookmarks"
description: "Retrieves a list of bookmarked schedule items for a current member."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of bookmarked schedule items for a current member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EVENTS.MANAGE-EVENTS">Manage Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/events/v1/schedule/bookmarks
    ```

    ## Query Params

    <ParamField query="eventId" type="string" required>

      Event ID to which the schedule item belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    <ResponseField name="items" type="ScheduleItem[]">

      Schedule items.

      See the [Schedule Item Object](/api-reference/business-solutions/events/event-management/schedule-items/schedule-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET 'https://www.wixapis.com/events/v1/agenda/bookmarks' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of bookmarked schedule items for a current member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EVENTS.MANAGE-EVENTS">Manage Events</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Method Declaration

    ```typescript
    import { scheduleBookmarks } from '@wix/events';

    scheduleBookmarks.listBookmarks(eventId): Promise
    ```

    ## Method Parameters

    <ParamField body="eventId" type="string" required>

      Event ID to which the schedule item belongs.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="items" type="ScheduleItem[]">

      Schedule items.

      See the [Schedule Item Object](/api-reference/business-solutions/events/event-management/schedule-items/schedule-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-bookmarks" id="ex-sdk-list-bookmarks-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-bookmarks" id="ex-sdk-list-bookmarks-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-bookmarks-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-bookmarks-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listBookmarks</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listBookmarks (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-bookmarks-0"><span className="wix-picker-name">listBookmarks</span></label>
    <label htmlFor="ex-sdk-list-bookmarks-1"><span className="wix-picker-name">listBookmarks (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listBookmarks
    import { scheduleBookmarks } from '@wix/events';

    async function listBookmarks(eventId) {
      const response = await scheduleBookmarks.listBookmarks(eventId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listBookmarks (self-hosted)
    import { createClient } from '@wix/sdk';
    import { scheduleBookmarks } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { scheduleBookmarks },
      // Include the auth strategy and host as relevant
    });

    async function listBookmarks(eventId) {
      const response = await myWixClient.scheduleBookmarks.listBookmarks(eventId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
