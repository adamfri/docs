---
title: "onBillableItemDeleted"
description: "Triggered when a billable item is deleted."
icon: "bolt"
---

Triggered when a billable item is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.billable_items.v1.billable_item_deleted` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Billable Items`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onBillableItemDeleted
  import { billableItems } from '@wix/get-paid';

  billableItems.onBillableItemDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onBillableItemDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { billableItems } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        billableItems,
      },
  });

  wixClient.billableItems.onBillableItemDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
