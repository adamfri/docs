---
title: "onTierUpdated"
description: "Triggered when a tier is updated."
icon: "bolt"
---

Triggered when a tier is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.v1.tier_updated` |
| Package | `@wix/loyalty` |

## Examples

<CodeGroup>

  ```javascript onTierUpdated
  import { tiers } from '@wix/loyalty';

  tiers.onTierUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTierUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { tiers } from '@wix/loyalty';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        tiers,
      },
  });

  wixClient.tiers.onTierUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
