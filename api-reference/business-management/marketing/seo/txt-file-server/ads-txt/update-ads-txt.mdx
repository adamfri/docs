---
title: "Update Ads Txt"
description: "Updates the Ads.txt file."
icon: "rotate"
mode: "wide"
---

<span className="wix-method wix-method-put">PUT</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the Ads.txt file.
    When setting the `content` object to an empty string, an empty Ads.txt file will be created.
    In order to reset Ads.txt to Wix's default, send `default: true` without a `content` object.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.MANAGE-ADS-TXT">Manage Ads.txt</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PUT https://www.wixapis.com/promote-seo-robots-server/v2/ads
    ```

    ## Body Params

    <ParamField body="adsTxt" type="AdsTxt">

      Submitted `adsTxT` object will replace the existing Ads.txt file.
      To reset Ads.txt to Wix's default, submit `default:true` without a `content` object.

      See the [Ads Txt Object](/api-reference/business-management/marketing/seo/txt-file-server/ads-txt/ads-txt-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="adsTxt" type="AdsTxt">

      Updated Ads txt object

      See the [Ads Txt Object](/api-reference/business-management/marketing/seo/txt-file-server/ads-txt/ads-txt-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | — | Site ID is missing. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ads Txt Updated | `wix.promote.marketing.ads_txt_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-update-ads-txt" id="ex-rest-update-ads-txt-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-update-ads-txt" id="ex-rest-update-ads-txt-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-update-ads-txt-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-update-ads-txt-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Reset Ads.txt to default Example</span><span className="wix-picker-desc">You can pass default: true in case you need to reset to default</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update Ads.txt Example</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-update-ads-txt-0"><span className="wix-picker-name">Reset Ads.txt to default Example</span><span className="wix-picker-desc">You can pass default: true in case you need to reset to default</span></label>
    <label htmlFor="ex-rest-update-ads-txt-1"><span className="wix-picker-name">Update Ads.txt Example</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl PUT https://www.wixapis.com/promote-seo-txt-file-server/v2/ads \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{"adsTxt": {"default": true, "subdomain": "www"}}'
      ```

      ```json Response
      {
          "adsTxt": {
              "content": "",
              "default": true,
              "subdomain": "www"
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl PUT https://www.wixapis.com/promote-seo-txt-file-server/v2/ads \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{"adsTxt": {"content": "google.com, pub-0000000000000000, DIRECT, f08c47fec0942fa0", "subdomain": "www"}}'
      ```

      ```json Response
      {
          "adsTxt": {
              "content": "google.com, pub-0000000000000000, DIRECT, f08c47fec0942fa0",
              "default": false,
              "subdomain": "www"
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the Ads.txt file.
    When setting the `content` object to an empty string, an empty Ads.txt file will be created.
    In order to reset Ads.txt to Wix's default, send `default: true` without a `content` object.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.MANAGE-ADS-TXT">Manage Ads.txt</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { adsTxt } from '@wix/marketing';

    adsTxt.updateAdsTxt(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="UpdateAdsTxtOptions">

      options

      <Expandable title="options properties">

        <ParamField body="adsTxt" type="AdsTxt">

          Submitted `adsTxT` object will replace the existing Ads.txt file.
          To reset Ads.txt to Wix's default, submit `default:true` without a `content` object.

          See the [Ads Txt Object](/api-reference/business-management/marketing/seo/txt-file-server/ads-txt/ads-txt-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="adsTxt" type="AdsTxt">

      Updated Ads txt object

      See the [Ads Txt Object](/api-reference/business-management/marketing/seo/txt-file-server/ads-txt/ads-txt-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | — | Site ID is missing. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ads Txt Updated | `wix.promote.marketing.ads_txt_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-ads-txt" id="ex-sdk-update-ads-txt-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-ads-txt" id="ex-sdk-update-ads-txt-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-ads-txt" id="ex-sdk-update-ads-txt-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-ads-txt-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-ads-txt-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateAdsTxt</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateAdsTxt (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateAdsTxt (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-ads-txt-0"><span className="wix-picker-name">updateAdsTxt</span></label>
    <label htmlFor="ex-sdk-update-ads-txt-1"><span className="wix-picker-name">updateAdsTxt (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-ads-txt-2"><span className="wix-picker-name">updateAdsTxt (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateAdsTxt
    import { adsTxt } from '@wix/marketing';

    async function updateAdsTxt(options) {
      const response = await adsTxt.updateAdsTxt(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateAdsTxt (self-hosted)
    import { createClient } from '@wix/sdk';
    import { adsTxt } from '@wix/marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { adsTxt },
      // Include the auth strategy and host as relevant
    });

    async function updateAdsTxt(options) {
      const response = await myWixClient.adsTxt.updateAdsTxt(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateAdsTxt (with elevated permissions)
    import { adsTxt } from '@wix/marketing';
    import { auth } from '@wix/essentials';

    async function myUpdateAdsTxtMethod(options) {
      const elevatedUpdateAdsTxt = auth.elevate(adsTxt.updateAdsTxt);
      const response = await elevatedUpdateAdsTxt(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
