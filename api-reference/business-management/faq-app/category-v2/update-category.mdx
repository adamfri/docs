---
title: "Update Category"
description: "Updates a category."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a category.

    Each time the category is updated, `revision` increments by 1.
    The current `revision` must be specified when updating the category.
    This ensures you're working with the latest category and prevents unintended overwrites.
    Updates can be partial.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LABS.MANAGE-FAQ">Manage FAQ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/faq/v2/categories/{category.id}
    ```

    ## Path Params

    <ParamField path="category.id" type="string" required>

      Category ID.

    </ParamField>

    ## Body Params

    <ParamField body="category" type="Category" required>

      Category to update.

      See the [Category Object](/api-reference/business-management/faq-app/category-v2/category-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="category" type="Category">

      Updated category.

      See the [Category Object](/api-reference/business-management/faq-app/category-v2/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Updated | `wix.faq.category.v2.category_updated` | Triggered when a category is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request PATCH \
        --url https://www.wixapis.com/faq/v2/categories/a1b2c3d4-e5f6-7890-abcd-ef1234567890 \
        --header 'Authorization: <AUTH>' \
        --header 'Content-Type: application/json' \
        --data '{
          "category": {
            "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "revision": 2,
            "title": "Shipping, Delivery & Tracking"
          },
          "field_mask": {
            "paths": ["title"]
          }
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "category": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "revision": 3,
          "created_date": "2024-12-06T10:27:38.123Z",
          "updated_date": "2024-12-06T11:41:15.789Z",
          "title": "Shipping, Delivery & Tracking",
          "sort_order": 1.0
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a category.

    Each time the category is updated, `revision` increments by 1.
    The current `revision` must be specified when updating the category.
    This ensures you're working with the latest category and prevents unintended overwrites.
    Updates can be partial.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LABS.MANAGE-FAQ">Manage FAQ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { category } from '@wix/faq';

    category.updateCategory(_id, category): Promise<Category>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Category ID.

      **Read-only.**

    </ParamField>

    <ParamField body="category" type="UpdateCategory" required>

      Category to update.

      <Expandable title="category properties">

        <ParamField body="_id" type="string">

          Category ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the category is updated.
          To prevent conflicting changes,
          the current revision must be specified when updating the category.

          Ignored when creating a category.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the category was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the category was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="title" type="string">

          Category title displayed on a site's Wix FAQ widgets.

        </ParamField>

        <ParamField body="sortOrder" type="number">

          Order of the category within a site's Wix FAQ dashboard page. Categories with lower sort order values appear first.

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom field data for the `category` object.

          [Extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions) must be configured in the app dashboard before they can be accessed with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Category>`.

    See the [Category Object](/api-reference/business-management/faq-app/category-v2/category-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Updated | `wix.faq.category.v2.category_updated` | Triggered when a category is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-category" id="ex-sdk-update-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-category" id="ex-sdk-update-category-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-category" id="ex-sdk-update-category-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateCategory</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCategory (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-category-0"><span className="wix-picker-name">updateCategory</span></label>
    <label htmlFor="ex-sdk-update-category-1"><span className="wix-picker-name">updateCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-category-2"><span className="wix-picker-name">updateCategory (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateCategory
    import { category } from '@wix/faq';

    async function updateCategory(_id,category) {
      const response = await category.updateCategory(_id,category);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { category } from '@wix/faq';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { category },
      // Include the auth strategy and host as relevant
    });

    async function updateCategory(_id,category) {
      const response = await myWixClient.category.updateCategory(_id,category);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCategory (with elevated permissions)
    import { category } from '@wix/faq';
    import { auth } from '@wix/essentials';

    async function myUpdateCategoryMethod(_id,category) {
      const elevatedUpdateCategory = auth.elevate(category.updateCategory);
      const response = await elevatedUpdateCategory(_id,category);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
