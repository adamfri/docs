---
title: "onCommentMoved"
description: "Triggered when a comment is moved."
icon: "bolt"
---

Triggered when a comment is moved.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v2.comment_comment_moved` |
| Package | `@wix/comments` |
| Permissions | `Read Comments`, `Manage Comments` |

## Examples

<CodeGroup>

  ```javascript onCommentMoved
  import { comments } from '@wix/comments';

  comments.onCommentMoved((event) => {
      // handle your event here
  });
  ```

  ```javascript onCommentMoved (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { comments } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        comments,
      },
  });

  wixClient.comments.onCommentMoved((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
