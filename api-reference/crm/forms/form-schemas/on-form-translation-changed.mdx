---
title: "onFormTranslationChanged"
description: "The onFormTranslationChanged event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forms.v4.form_form_translation_changed` |
| Package | `@wix/forms` |
| Permissions | `Manage Submissions`, `Manage Stores`, `Manage form submissions.`, `Read Intake Form (PII)`, `Manage Online Programs`, `Read Intake Form`, `Manage Intake Form`, `Manage Intake Form (PII)`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onFormTranslationChanged
  import { forms } from '@wix/forms';

  forms.onFormTranslationChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onFormTranslationChanged (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { forms } from '@wix/forms';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        forms,
      },
  });

  wixClient.forms.onFormTranslationChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
