---
title: "Update Resource"
description: "Updates a resource."
icon: "pen"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Warning>

  **Deprecated.** This method is deprecated.

</Warning>

<Info>

  **Developer Preview.** This method is in Developer Preview and may change without notice.

</Info>

Updates a resource.

Use this endpoint to update all resource information except for the resource's schedule. To update a resource's schedule use
the [Update Schedule](https://dev.wix.com/api/rest/wix-bookings/resources/update-schedule) endpoint.

<Note>

+ When updating a resource you cannot change the system tags used by the Wix Bookings app. Tags used by the app have the values `"business"` and `"staff"`.
+ When updating a resource's schedule you cannot change the resource tag used by the Wix Bookings app. Tags used by the app have the values `"INDIVIDUAL"`, `"GROUP"`, and `"COURSE”`.
+ The `businessLocation.businessSchedule` object in the `schedule.location` object is not supported.

</Note>

> **Permissions:**
> This endpoint requires the Manage Bookings [permission scope](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

<Note>

  **Authentication**

  You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

</Note>

## Permissions

This method requires one of the following permission scopes:

<div className="wix-scopes">

<span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

</div>

Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

Callable by: `APP`.

## Endpoint

```http
PATCH https://www.wixapis.com/bookings/v1/resources/{resource.id}
```

## Path Params

<ParamField path="resource.id" type="string" required>

  Resource ID.

</ParamField>

## Body Params

<ParamField body="resource" type="Resource">

  Resource to update.

  See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v1/resource-object) for this object's fields.

</ParamField>

<ParamField body="fieldMask" type="FieldMask">

  Field mask of fields to update.

  <Expandable title="fieldMask properties">

    <ParamField body="paths" type="string[]" />

  </Expandable>

</ParamField>

## Response Object

<ResponseField name="resource" type="Resource">

  Updated resource.

  See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v1/resource-object) for this object's fields.

</ResponseField>

<ResponseField name="schedules" type="BatchResponse" deprecated>

  Updated schedules.

  **Deprecated.**

  <Expandable title="schedules properties">

    <ResponseField name="created" type="Schedule[]">

      Created schedules.

      <Expandable title="created properties">

        <ResponseField name="id" type="string">

          Schedule ID.

        </ResponseField>

        <ResponseField name="scheduleOwnerId" type="string">

          ID of the schedule's owner entity. This may be a resource ID or a service ID.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Schedule's time zone in [Area/Location](https://en.wikipedia.org/wiki/Tz_database) format. Read-only.
          Derived from the Wix Business time zone.

          **Read-only.**

        </ResponseField>

        <ResponseField name="intervals" type="RecurringInterval[]" deprecated>

          Deprecated. Please use the [Sessions API](https://dev.wix.com/api/rest/wix-bookings/schedules-and-sessions/session) instead.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `RecurringInterval[]` object._

        </ResponseField>

        <ResponseField name="title" type="string">

          Default title for the schedule's sessions. Maximum length: 6000 characters.

        </ResponseField>

        <ResponseField name="tags" type="string[]" deprecated>

          __Deprecated.__
          Tags for grouping schedules. These tags are the default tags for the schedule's sessions.
          The Wix Bookings app uses the following predefined tags to set schedule type: `"INDIVIDUAL"`, `"GROUP"`, and `"COURSE"`. Once the schedule type is set using these tags, you cannot update it. In addition to the app's tags, you can create and update your own tags.

          **Deprecated.**

        </ResponseField>

        <ResponseField name="location" type="Location">

          Default location for the schedule's sessions.

          _Nested structure omitted for brevity — see the `Location` object._

        </ResponseField>

        <ResponseField name="capacity" type="integer">

          Maximum number of participants that can be added to the schedule's sessions.
          Must be at most `1` for schedule whose availability is affected by another schedule. E.g, appointment schedules of the Wix Bookings app.

        </ResponseField>

        <ResponseField name="rate" type="Rate" deprecated>

          Deprecated. Please use the [Booking Services V2](https://dev.wix.com/api/rest/wix-bookings/services-v2) payment instead.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `Rate` object._

        </ResponseField>

        <ResponseField name="availability" type="Availability" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `Availability` object._

        </ResponseField>

        <ResponseField name="totalNumberOfParticipants" type="integer">

          Number of participants registered to sessions in this schedule, calculated as the sum of the party sizes.

          **Read-only.**

        </ResponseField>

        <ResponseField name="participants" type="Participant[]">

          *Partial list** of participants which are registered to sessions in this schedule.
          Participants who are registered in the schedule are automatically registered to any session that is created for the schedule.
          To retrieve the full list of schedule participants please use the [Query Extended Bookings API](https://dev.wix.com/api/rest/wix-bookings/bookings-reader-v2/query-extended-bookings).

          **Read-only.**

          _Nested structure omitted for brevity — see the `Participant[]` object._

        </ResponseField>

        <ResponseField name="externalCalendarOverrides" type="ExternalCalendarOverrides" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `ExternalCalendarOverrides` object._

        </ResponseField>

        <ResponseField name="status" type="string">

          Schedule status. Default: Created

          **Read-only.**

          **Supported values:**

          - `UNDEFINED` — Undefined schedule status.
          - `CREATED` — The schedule was created.
          - `CANCELLED` — The schedule was cancelled.

        </ResponseField>

        <ResponseField name="created" type="datetime">

          Schedule creation date.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updated" type="datetime">

          Schedule last update date.

          **Read-only.**

        </ResponseField>

        <ResponseField name="version" type="integer">

          Schedule version number, updated each time the schedule is updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="inheritedFields" type="string[]">

          Fields which were inherited from the Business Info page under Settings in the Dashboard.

          **Read-only.**

        </ResponseField>

        <ResponseField name="conferenceProvider" type="ConferenceProvider" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `ConferenceProvider` object._

        </ResponseField>

        <ResponseField name="calendarConference" type="CalendarConference" deprecated>

          A conference created for the schedule. This is used when a participant is added to a schedule.
          **Partially deprecated.** Only `hostUrl` and `guestUrl` are to be supported.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `CalendarConference` object._

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="updated" type="Schedule[]">

      Updated schedules.

      <Expandable title="updated properties">

        <ResponseField name="id" type="string">

          Schedule ID.

        </ResponseField>

        <ResponseField name="scheduleOwnerId" type="string">

          ID of the schedule's owner entity. This may be a resource ID or a service ID.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Schedule's time zone in [Area/Location](https://en.wikipedia.org/wiki/Tz_database) format. Read-only.
          Derived from the Wix Business time zone.

          **Read-only.**

        </ResponseField>

        <ResponseField name="intervals" type="RecurringInterval[]" deprecated>

          Deprecated. Please use the [Sessions API](https://dev.wix.com/api/rest/wix-bookings/schedules-and-sessions/session) instead.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `RecurringInterval[]` object._

        </ResponseField>

        <ResponseField name="title" type="string">

          Default title for the schedule's sessions. Maximum length: 6000 characters.

        </ResponseField>

        <ResponseField name="tags" type="string[]" deprecated>

          __Deprecated.__
          Tags for grouping schedules. These tags are the default tags for the schedule's sessions.
          The Wix Bookings app uses the following predefined tags to set schedule type: `"INDIVIDUAL"`, `"GROUP"`, and `"COURSE"`. Once the schedule type is set using these tags, you cannot update it. In addition to the app's tags, you can create and update your own tags.

          **Deprecated.**

        </ResponseField>

        <ResponseField name="location" type="Location">

          Default location for the schedule's sessions.

          _Nested structure omitted for brevity — see the `Location` object._

        </ResponseField>

        <ResponseField name="capacity" type="integer">

          Maximum number of participants that can be added to the schedule's sessions.
          Must be at most `1` for schedule whose availability is affected by another schedule. E.g, appointment schedules of the Wix Bookings app.

        </ResponseField>

        <ResponseField name="rate" type="Rate" deprecated>

          Deprecated. Please use the [Booking Services V2](https://dev.wix.com/api/rest/wix-bookings/services-v2) payment instead.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `Rate` object._

        </ResponseField>

        <ResponseField name="availability" type="Availability" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `Availability` object._

        </ResponseField>

        <ResponseField name="totalNumberOfParticipants" type="integer">

          Number of participants registered to sessions in this schedule, calculated as the sum of the party sizes.

          **Read-only.**

        </ResponseField>

        <ResponseField name="participants" type="Participant[]">

          *Partial list** of participants which are registered to sessions in this schedule.
          Participants who are registered in the schedule are automatically registered to any session that is created for the schedule.
          To retrieve the full list of schedule participants please use the [Query Extended Bookings API](https://dev.wix.com/api/rest/wix-bookings/bookings-reader-v2/query-extended-bookings).

          **Read-only.**

          _Nested structure omitted for brevity — see the `Participant[]` object._

        </ResponseField>

        <ResponseField name="externalCalendarOverrides" type="ExternalCalendarOverrides" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `ExternalCalendarOverrides` object._

        </ResponseField>

        <ResponseField name="status" type="string">

          Schedule status. Default: Created

          **Read-only.**

          **Supported values:**

          - `UNDEFINED` — Undefined schedule status.
          - `CREATED` — The schedule was created.
          - `CANCELLED` — The schedule was cancelled.

        </ResponseField>

        <ResponseField name="created" type="datetime">

          Schedule creation date.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updated" type="datetime">

          Schedule last update date.

          **Read-only.**

        </ResponseField>

        <ResponseField name="version" type="integer">

          Schedule version number, updated each time the schedule is updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="inheritedFields" type="string[]">

          Fields which were inherited from the Business Info page under Settings in the Dashboard.

          **Read-only.**

        </ResponseField>

        <ResponseField name="conferenceProvider" type="ConferenceProvider" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `ConferenceProvider` object._

        </ResponseField>

        <ResponseField name="calendarConference" type="CalendarConference" deprecated>

          A conference created for the schedule. This is used when a participant is added to a schedule.
          **Partially deprecated.** Only `hostUrl` and `guestUrl` are to be supported.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `CalendarConference` object._

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="cancelled" type="Schedule[]">

      Cancelled schedules.

      <Expandable title="cancelled properties">

        <ResponseField name="id" type="string">

          Schedule ID.

        </ResponseField>

        <ResponseField name="scheduleOwnerId" type="string">

          ID of the schedule's owner entity. This may be a resource ID or a service ID.

        </ResponseField>

        <ResponseField name="timeZone" type="string">

          Schedule's time zone in [Area/Location](https://en.wikipedia.org/wiki/Tz_database) format. Read-only.
          Derived from the Wix Business time zone.

          **Read-only.**

        </ResponseField>

        <ResponseField name="intervals" type="RecurringInterval[]" deprecated>

          Deprecated. Please use the [Sessions API](https://dev.wix.com/api/rest/wix-bookings/schedules-and-sessions/session) instead.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `RecurringInterval[]` object._

        </ResponseField>

        <ResponseField name="title" type="string">

          Default title for the schedule's sessions. Maximum length: 6000 characters.

        </ResponseField>

        <ResponseField name="tags" type="string[]" deprecated>

          __Deprecated.__
          Tags for grouping schedules. These tags are the default tags for the schedule's sessions.
          The Wix Bookings app uses the following predefined tags to set schedule type: `"INDIVIDUAL"`, `"GROUP"`, and `"COURSE"`. Once the schedule type is set using these tags, you cannot update it. In addition to the app's tags, you can create and update your own tags.

          **Deprecated.**

        </ResponseField>

        <ResponseField name="location" type="Location">

          Default location for the schedule's sessions.

          _Nested structure omitted for brevity — see the `Location` object._

        </ResponseField>

        <ResponseField name="capacity" type="integer">

          Maximum number of participants that can be added to the schedule's sessions.
          Must be at most `1` for schedule whose availability is affected by another schedule. E.g, appointment schedules of the Wix Bookings app.

        </ResponseField>

        <ResponseField name="rate" type="Rate" deprecated>

          Deprecated. Please use the [Booking Services V2](https://dev.wix.com/api/rest/wix-bookings/services-v2) payment instead.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `Rate` object._

        </ResponseField>

        <ResponseField name="availability" type="Availability" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `Availability` object._

        </ResponseField>

        <ResponseField name="totalNumberOfParticipants" type="integer">

          Number of participants registered to sessions in this schedule, calculated as the sum of the party sizes.

          **Read-only.**

        </ResponseField>

        <ResponseField name="participants" type="Participant[]">

          *Partial list** of participants which are registered to sessions in this schedule.
          Participants who are registered in the schedule are automatically registered to any session that is created for the schedule.
          To retrieve the full list of schedule participants please use the [Query Extended Bookings API](https://dev.wix.com/api/rest/wix-bookings/bookings-reader-v2/query-extended-bookings).

          **Read-only.**

          _Nested structure omitted for brevity — see the `Participant[]` object._

        </ResponseField>

        <ResponseField name="externalCalendarOverrides" type="ExternalCalendarOverrides" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `ExternalCalendarOverrides` object._

        </ResponseField>

        <ResponseField name="status" type="string">

          Schedule status. Default: Created

          **Read-only.**

          **Supported values:**

          - `UNDEFINED` — Undefined schedule status.
          - `CREATED` — The schedule was created.
          - `CANCELLED` — The schedule was cancelled.

        </ResponseField>

        <ResponseField name="created" type="datetime">

          Schedule creation date.

          **Read-only.**

        </ResponseField>

        <ResponseField name="updated" type="datetime">

          Schedule last update date.

          **Read-only.**

        </ResponseField>

        <ResponseField name="version" type="integer">

          Schedule version number, updated each time the schedule is updated.

          **Read-only.**

        </ResponseField>

        <ResponseField name="inheritedFields" type="string[]">

          Fields which were inherited from the Business Info page under Settings in the Dashboard.

          **Read-only.**

        </ResponseField>

        <ResponseField name="conferenceProvider" type="ConferenceProvider" deprecated>

          __Deprecated.__

          **Deprecated.**

          _Nested structure omitted for brevity — see the `ConferenceProvider` object._

        </ResponseField>

        <ResponseField name="calendarConference" type="CalendarConference" deprecated>

          A conference created for the schedule. This is used when a participant is added to a schedule.
          **Partially deprecated.** Only `hostUrl` and `guestUrl` are to be supported.

          **Deprecated.**

          _Nested structure omitted for brevity — see the `CalendarConference` object._

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Expandable>

</ResponseField>

## Errors

| Status | Code | Application code | Description |
| --- | --- | --- | --- |
| `428` | `FAILED_PRECONDITION` | `FORBIDDEN_CHANGE_RESOURCE_TYPE` |  |

<Note>

  This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

</Note>

## Event Triggers

This method triggers the following events:

| Event | Type | Description |
| --- | --- | --- |
| Resource Notification | `com.wixpress.bookings.resources.core.api.v1.resource.ResourceNotification` |  |
