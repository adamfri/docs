---
title: "Get Gift Card"
description: "Retrieves a gift card by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a gift card by ID.

    Use this method to get current gift card details including balance, status, and metadata.
    The gift card code is obfuscated in the response for security.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/gift-cards/v1/gift-cards/{giftCardId}
    ```

    ## Path Params

    <ParamField path="giftCardId" type="string" required>

      Gift card ID.

    </ParamField>

    ## Response Object

    <ResponseField name="giftCard" type="GiftCard">

      Retrieved gift card with obfuscated code for security.

      See the [Gift Card Object](/api-reference/business-solutions/gift-cards/gift-cards/gift-card-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves an existing gift card by ID

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/gift-cards/v1/gift-cards/ec3c4a90-cf46-4325-913b-c2ab2ae36549' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
      ```

      ```json Response
      {
        "giftCard": {
          "id": "ec3c4a90-cf46-4325-913b-c2ab2ae36549",
          "code": "************4N6C",
          "initialValue": {
            "amount": "50",
            "formattedAmount": "$50.00"
          },
          "balance": {
            "amount": "50",
            "formattedAmount": "$50.00"
          },
          "currency": "USD",
          "expirationDate": null,
          "createdDate": "2024-10-30T14:52:02.588Z",
          "updatedDate": "2024-10-30T14:52:02.631Z",
          "disabledDate": null,
          "notificationInfo": {
            "recipient": {
              "email": "johndoe@gmail.com",
              "name": "John Doe"
            },
            "notificationDate": null,
            "personalizedMessage": null,
            "sender": {
              "email": "janedoe@gmail.com",
              "name": "Jane Doe"
            }
          },
          "source": "ORDER",
          "orderInfo": {
            "orderId": "582e3195-de2d-4a83-a2ec-90ffe0e4d191",
            "orderNumber": "10121"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a gift card by ID.

    Use this method to get current gift card details including balance, status, and metadata.
    The gift card code is obfuscated in the response for security.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { giftVouchers } from '@wix/gift-vouchers';

    giftVouchers.getGiftCard(giftCardId): Promise
    ```

    ## Method Parameters

    <ParamField body="giftCardId" type="string" required>

      Gift card ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `GiftCard`.

    See the [Gift Card Object](/api-reference/business-solutions/gift-cards/gift-cards/gift-card-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-gift-card" id="ex-sdk-get-gift-card-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-gift-card" id="ex-sdk-get-gift-card-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-gift-card-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-gift-card-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get gift card by ID</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getGiftCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-gift-card-0"><span className="wix-picker-name">Get gift card by ID</span></label>
    <label htmlFor="ex-sdk-get-gift-card-1"><span className="wix-picker-name">getGiftCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get gift card by ID
    import { giftVouchers } from "@wix/gift-vouchers";

    async function getGiftCard() {
      const response = await giftVouchers.getGiftCard(
        "ec3c4a90-cf46-4325-913b-c2ab2ae36549"
      );
      return response;
    }

    /* Promise resolves to:
     * {
     *   id: "ec3c4a90-cf46-4325-913b-c2ab2ae36549",
     *   code: "************4N6C",
     *   initialValue: { amount: "50", formattedAmount: "$50.00" },
     *   balance: { amount: "50", formattedAmount: "$50.00" },
     *   currency: "USD",
     *   expirationDate: null,
     *   createdDate: "2024-10-30T14:52:02.588Z",
     *   updatedDate: "2024-10-30T14:52:02.631Z",
     *   disabledDate: null,
     *   notificationInfo: {
     *     recipient: { email: "johndoe@gmail.com", name: "John Doe" },
     *     sender: { email: "janedoe@gmail.com", name: "Jane Doe" },
     *   },
     *   source: "ORDER",
     *   orderInfo: { orderId: "582e3195-de2d-4a83-a2ec-90ffe0e4d191", orderNumber: "10121" },
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getGiftCard (self-hosted)
    import { createClient } from '@wix/sdk';
    import { giftVouchers } from '@wix/gift-vouchers';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { giftVouchers },
      // Include the auth strategy and host as relevant
    });

    async function getGiftCard(giftCardId) {
      const response = await myWixClient.giftVouchers.getGiftCard(giftCardId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
