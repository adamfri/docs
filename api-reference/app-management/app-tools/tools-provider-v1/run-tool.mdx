---
title: "Run Tool"
description: "Runs a tool exposed by the service plugin."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Runs a tool exposed by the service plugin.

    Wix calls this method when Aria invokes one of the tools declared in the app's [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

    Wix passes the tool's response back to Aria, and presents the result to the Wix user in natural language.

    <Note>

    Wix doesn’t check if the payload matches your `requestSchema` before calling your service plugin. Handle missing or unexpected fields in your implementation. Learn more about `requestSchema` in [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

    </Note>

    **Error handling:** If your implementation returns an error or times out, Aria continues the conversation without the tool result.

    ## Endpoint

    ```http
    POST /v1/run-tool
    ```

    ## Body Params

    <ParamField body="methodName" type="string">

      Identifies which tool to run. Matches the `methodName` of an active tool declared in the app's [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

      Constraints: `minLength 1`, `maxLength 50`.

    </ParamField>

    <ParamField body="payload" type="object">

      Input data for the tool as a JSON object. The shape corresponds to the tool's `requestSchema` in the [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions), but isn't validated by the platform before the call.

    </ParamField>

    ## Response Object

    <ResponseField name="response" type="object">

      Output data returned by the tool as a JSON object. The schema should correspond to the tool's `responseSchema` in the [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Shows the request that Wix sends to your service plugin endpoint when the AI assistant invokes a tool, and the expected response format.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://your-app.com/v1/run-tool' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "methodName": "get-product-recommendations",
        "payload": {
          "customerId": "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
          "category": "electronics",
          "limit": 5
        }
      }'
      ```

      ```json Response
      {
        "response": {
          "products": [
            {
              "id": "a3f9c2b1-84d6-4e12-b7f0-9c3d51e2a8f4",
              "name": "Wireless Headphones",
              "price": 79.99
            },
            {
              "id": "f1e4d7c8-32a5-4b90-8e6f-2d1c47b3a9e5",
              "name": "Bluetooth Speaker",
              "price": 49.99
            }
          ]
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Runs a tool exposed by the service plugin.

    Wix calls this method when Aria invokes one of the tools declared in the app's [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

    Wix passes the tool's response back to Aria, and presents the result to the Wix user in natural language.

    <Note>

    Wix doesn’t check if the payload matches your `requestSchema` before calling your service plugin. Handle missing or unexpected fields in your implementation. Learn more about `requestSchema` in [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

    </Note>

    **Error handling:** If your implementation returns an error or times out, Aria continues the conversation without the tool result.

    ## Method Declaration

    ```typescript
    import { toolsProvider } from '@wix/app-tools';

    toolsProvider.runTool(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="RunToolRequest">

      request

      <Expandable title="request properties">

        <ParamField body="methodName" type="string">

          Identifies which tool to run. Matches the `methodName` of an active tool declared in the app's [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

          Constraints: `minLength 1`, `maxLength 50`.

        </ParamField>

        <ParamField body="payload" type="object">

          Input data for the tool as a JSON object. The shape corresponds to the tool's `requestSchema` in the [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions), but isn't validated by the platform before the call.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="response" type="object">

      Output data returned by the tool as a JSON object. The schema should correspond to the tool's `responseSchema` in the [App Tools extension](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/app-tools/about-app-tools-extensions).

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-run-tool" id="ex-sdk-run-tool-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-run-tool" id="ex-sdk-run-tool-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-run-tool" id="ex-sdk-run-tool-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-run-tool-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-run-tool-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">runTool</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">runTool (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">runTool (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-run-tool-0"><span className="wix-picker-name">runTool</span></label>
    <label htmlFor="ex-sdk-run-tool-1"><span className="wix-picker-name">runTool (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-run-tool-2"><span className="wix-picker-name">runTool (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript runTool
    import { toolsProvider } from '@wix/app-tools/service-plugins';

    async function runTool(request,metadata) {
      const response = await toolsProvider.runTool(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript runTool (self-hosted)
    import { createClient } from '@wix/sdk';
    import { toolsProvider } from '@wix/app-tools/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { toolsProvider },
      // Include the auth strategy and host as relevant
    });

    async function runTool(request,metadata) {
      const response = await myWixClient.toolsProvider.runTool(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript runTool (with elevated permissions)
    import { toolsProvider } from '@wix/app-tools/service-plugins';
    import { auth } from '@wix/essentials';

    async function myRunToolMethod(request,metadata) {
      const elevatedRunTool = auth.elevate(toolsProvider.runTool);
      const response = await elevatedRunTool(request,metadata);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
