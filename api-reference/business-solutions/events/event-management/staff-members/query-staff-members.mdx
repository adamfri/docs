---
title: "Query Staff Members"
description: "Retrieves a list of staff members, with the specified paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of staff members, with the specified paging, filtering, and sorting.
    Query Staff Members runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with _Query_ methods, see [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language), [Sorting and Paging](https://dev.wix.com/docs/rest/articles/get-started/sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/rest/articles/get-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/staff-members/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2">

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order (ASC/DESC). Defaults to ASC

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Predefined sets of fields to return.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fieldsets" type="string[]">

      A list of additional fields to include in the response.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `LOGIN_LINK` — Returns the `loginLink` field.

    </ParamField>

    ## Response Object

    <ResponseField name="staffMembers" type="StaffMember[]">

      A list of staff members.

      See the [Staff Member Object](/api-reference/business-solutions/events/event-management/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v1/staff-members/query' \
      -H 'Authorization: <AUTH TOKEN>' \
      -H 'Content-Type: application/json' \
      -d '{
      		"query": {
      			"filter": {
      				"createdDate": {
      					"$gt": "2025-01-01T00:00:00.000Z"
      				}
      			}
      		},
      		"fieldsets": [
      			"LOGIN_LINK"
      		]
      	}'
      ```

      ```json Response
      {
        "staffMembers": [
          {
            "id": "9c5a0ea3-0983-4323-991f-2dbd25a75225",
            "revision": "1",
            "createdDate": "2025-01-08T11:43:07.181Z",
            "updatedDate": "2025-01-08T11:43:07.181Z",
            "name": "John Doe",
            "scopes": [
              "CHECK_IN_GUESTS"
            ],
            "allEvents": {},
            "status": "PENDING",
            "extendedFields": null,
            "permissions": [
              "CHECK_IN_GUESTS"
            ],
            "loginLink": "https://events.wix.app/events-platform/af442a8f-c574-47d3-9222-87cd7ef6520e/staff/home/JWS.eyJraWQiOiIwdnJsTzJxbiIsImFsZyI6IkhTMjU2In0.eyJkYXRhIjoie1wiaWRcIjpcIjdlY2FjZGI0LWE2YjYtNGI4My04OWM4LTE3MWQyNmIxNDY2NlwifSIsImlhdCI6MTczNjMzNjU4NywiZXhwIjoxNzY3ODcyNTg3fQ.y0ALZjnGG3KcSeAbDVlMhnazOVUqAufqyQ0XBJQGxlg"
          },
          {
            "id": "6e69d26b-f57a-4e29-9e26-1cd500d5ff07",
            "revision": "2",
            "createdDate": "2025-01-09T14:03:59.964Z",
            "updatedDate": "2025-01-09T14:05:24.720Z",
            "name": "Jane Doe",
            "scopes": [
              "ADD_GUESTS",
              "CHECK_IN_GUESTS",
              "READ_ANALYTICS"
            ],
            "allEvents": {},
            "status": "HAS_ACCESS",
            "extendedFields": null,
            "permissions": [
              "ADD_GUESTS",
              "CHECK_IN_GUESTS",
              "READ_ANALYTICS"
            ],
            "loginLink": "https://events.wix.app/events-platform/af442a8f-c574-47d3-9222-87cd7ef6520e/staff/home/JWS.eyJraWQiOiIwdnJsTzJxbiIsImFsZyI6IkhTMjU2In0.eyJkYXRhIjoie1wiaWRcIjpcIjdlY2FjZGI0LWE2YjYtNGI4My04OWM4LTE3MWQyNmIxNDY2NlwifSIsImlhdCI6MTczNjMzNjU4NywiZXhwIjoxNzY3ODcyNTg3fQ.y0ALZjnGG3KcSeAbDVlMhnazOVUqAufqyQ0XBJQGxlg"
          },
        ],
        "pagingMetadata": {
          "count": 2,
          "offset": null,
          "total": 2,
          "tooManyToCount": null,
          "cursors": {
            "next": null,
            "prev": null
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of staff members, with the specified paging, filtering, and sorting.
    Query Staff Members runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with _Query_ methods, see [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language), [Sorting and Paging](https://dev.wix.com/docs/rest/articles/get-started/sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/rest/articles/get-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { staffMembers } from '@wix/events';

    staffMembers.queryStaffMembers(query, options): StaffMembersQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="StaffMemberQuery" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order (ASC/DESC). Defaults to ASC

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QueryStaffMembersOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fieldsets" type="string[]">

          A list of additional fields to include in the response.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `LOGIN_LINK` — Returns the `loginLink` field.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `StaffMembersQueryBuilder`.

    <ResponseField name="staffMembers" type="StaffMember[]">

      A list of staff members.

      See the [Staff Member Object](/api-reference/business-solutions/events/event-management/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-staff-members" id="ex-sdk-query-staff-members-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-staff-members" id="ex-sdk-query-staff-members-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-staff-members" id="ex-sdk-query-staff-members-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-staff-members-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-staff-members-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryStaffMembers</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryStaffMembers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryStaffMembers (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-staff-members-0"><span className="wix-picker-name">queryStaffMembers</span></label>
    <label htmlFor="ex-sdk-query-staff-members-1"><span className="wix-picker-name">queryStaffMembers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-staff-members-2"><span className="wix-picker-name">queryStaffMembers (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryStaffMembers
    import { staffMembers } from '@wix/events';

    async function queryStaffMembers(query,options) {
      const response = await staffMembers.queryStaffMembers(query,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryStaffMembers (self-hosted)
    import { createClient } from '@wix/sdk';
    import { staffMembers } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { staffMembers },
      // Include the auth strategy and host as relevant
    });

    async function queryStaffMembers(query,options) {
      const response = await myWixClient.staffMembers.queryStaffMembers(query,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryStaffMembers (with elevated permissions)
    import { staffMembers } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myQueryStaffMembersMethod(query,options) {
      const elevatedQueryStaffMembers = auth.elevate(staffMembers.queryStaffMembers);
      const response = await elevatedQueryStaffMembers(query,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
