---
title: "Query Event Guests"
description: "Retrieves a list of guests given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of guests given the provided paging, filtering, and sorting.
    Query Event Guests runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`
    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language), [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination), and [Field Projection](https://dev.wix.com/api/rest/getting-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `eventId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `rsvpId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `contactId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `memberId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `secondaryLanguageCode` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `ticketNumber` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `orderNumber` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `attendanceStatus` | `$eq`, `$ne`, `$in` | None |
    | `guestType` | `$eq`, `$ne`, `$in` | None |
    | `guestDetails.checkedIn` | `$eq`, `$ne`, `$exists` | None |
    | `tickets` | `$exists` | None |
    | `createdDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `attendanceStatusUpdatedDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-GUEST-LIST">Read Event Tickets and Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v2/guests/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2" required>

      Query options. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format: &lt;br/> `"filter" : { "fieldName1": "value1", "fieldName2":{"$operator":"value2"} }`. &lt;br/> &lt;br/> **Example:** &lt;br/> `"filter" : { "eventId": "sd3f-jhds-4fs77", "ticketNumber": {"$startsWith":"478"} }` &lt;br/> &lt;br/> See [supported fields and operators](https://dev.wix.com/api/rest/wix-events/wix-events/filter-and-sort) for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format: &lt;br/> `[{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]` &lt;br/> &lt;br/> **Example:** &lt;br/> `[{"fieldName":"createdDate","direction":"DESC"}]` &lt;br/> &lt;br/> See [supported fields](https://dev.wix.com/api/rest/wix-events/wix-events/filter-and-sort) for more information.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fieldsets" type="string[]" deprecated>

          Currently the only supported fieldset is `guestDetails`. The `WIX_EVENTS.READ_GUESTS_DETAILS` permission is required to access the guest details. &lt;br/> &lt;br/> See [field projection](https://dev.wix.com/api/rest/getting-started/field-projection) for more information.

          **Deprecated.** Currently the only supported fieldset is `guestDetails`. The `WIX_EVENTS.READ_GUESTS_DETAILS` permission is required to access the guest details. &lt;br/> &lt;br/> See [field projection](https://dev.wix.com/api/rest/getting-started/field-projection) for more information. Use `fields` instead. Removal: 2024-06-30.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Predefined sets of fields to return.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `GUEST_DETAILS` — Returns `guestDetails` and `tickets`.
      - `GUEST_TOTAL` — Returns `totalGuests`.

    </ParamField>

    ## Response Object

    <ResponseField name="guests" type="EventGuest[]">

      List of guests.

      See the [Guest Object](/api-reference/business-solutions/events/registration/event-guests/guest-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Metadata for the paginated results.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-query-event-guests" id="ex-rest-query-event-guests-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-query-event-guests" id="ex-rest-query-event-guests-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-query-event-guests" id="ex-rest-query-event-guests-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-query-event-guests-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-query-event-guests-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query Ticket holder-type guests with guest details</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query buyer-type guests with guest details</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query RSVP-type guests without guest details</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-query-event-guests-0"><span className="wix-picker-name">Query Ticket holder-type guests with guest details</span></label>
    <label htmlFor="ex-rest-query-event-guests-1"><span className="wix-picker-name">Query buyer-type guests with guest details</span></label>
    <label htmlFor="ex-rest-query-event-guests-2"><span className="wix-picker-name">Query RSVP-type guests without guest details</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/guests/query' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      --data-binary '{
        "query": {
          "filter": {
            "guestType": "TICKET_HOLDER"
          },
          "fieldsets": [
            "guestDetails"
          ]
        }
      }'
      ```

      ```json Response
      {
        "guests": [
          {
            "id": "60744671-db27-4715-9b86-85f301a736af",
            "eventId": "b89aafc0-5473-4a56-8144-333b6c514874",
            "orderNumber": "2T0K-2HLF-145",
            "ticketNumber": "2T90-H5P6-17J1Q",
            "tickets": [
              {
                "number": "2T90-H5P6-17J1Q",
                "definitionId": "13fba1ae-657f-47d0-8759-fd8df5a94e65",
                "name": "Participant ticket"
              }
            ],
            "contactId": "804e6beb-365f-4992-8e35-8f12788e62fa",
            "guestDetails": {
              "email": "patsy.mcbride@mail.com",
              "firstName": "Patsy",
              "lastName": "McBride",
              "formResponse": {
                "inputValues": [
                  {
                    "inputName": "firstName",
                    "value": "Patsy",
                    "values": []
                  },
                  {
                    "inputName": "lastName",
                    "value": "McBride",
                    "values": []
                  },
                  {
                    "inputName": "email",
                    "value": "patsy.mcbride@mail.com",
                    "values": []
                  }
                ]
              },
              "checkedIn": false
            },
            "attendanceStatus": "ATTENDING",
            "createdDate": "2023-01-11T09:17:07.925Z",
            "updatedDate": "2023-01-11T09:17:07.925Z",
            "attendanceStatusUpdatedDate": "2023-01-11T09:17:07.841Z",
            "guestType": "TICKET_HOLDER"
          },
          {
            "id": "d2ce4031-278d-4f39-a337-d83baf82a109",
            "eventId": "b89aafc0-5473-4a56-8144-333b6c514874",
            "orderNumber": "2T0K-2HLF-145",
            "ticketNumber": "2T90-H5P6-17J1P",
            "tickets": [
              {
                "number": "2T90-H5P6-17J1P",
                "definitionId": "13fba1ae-657f-47d0-8759-fd8df5a94e65",
                "name": "Participant ticket"
              }
            ],
            "contactId": "023b6403-17e6-42dc-b475-a5ae6c2b8df4",
            "guestDetails": {
              "email": "john.doe@mail.com",
              "firstName": "John",
              "lastName": "Doe",
              "formResponse": {
                "inputValues": [
                  {
                    "inputName": "firstName",
                    "value": "John",
                    "values": []
                  },
                  {
                    "inputName": "lastName",
                    "value": "Doe",
                    "values": []
                  },
                  {
                    "inputName": "email",
                    "value": "john.doe@mail.com",
                    "values": []
                  }
                ]
              },
              "checkedIn": false
            },
            "attendanceStatus": "ATTENDING",
            "createdDate": "2023-01-11T09:17:07.925Z",
            "updatedDate": "2023-01-11T09:17:07.925Z",
            "attendanceStatusUpdatedDate": "2023-01-11T09:17:07.841Z",
            "guestType": "TICKET_HOLDER"
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {}
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/guests/query' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      --data-binary
      '{
        "query": {
          "filter": {
            "guestType": "BUYER"
          },
          "fieldsets": [
            "guestDetails"
          ],
          "paging": {
            "limit": 1
          }
        }
      }'
      ```

      ```json Response
      {
        "guests": [
          {
            "id": "ccec1209-8a5f-4531-b097-09f4009832f6",
            "eventId": "b89aafc0-5473-4a56-8144-333b6c514874",
            "orderNumber": "2T0K-2HLF-145",
            "tickets": [
              {
                "number": "2T90-H5P6-17J1P",
                "definitionId": "13fba1ae-657f-47d0-8759-fd8df5a94e65",
                "name": "Participant ticket"
              },
              {
                "number": "2T90-H5P6-17J1Q",
                "definitionId": "13fba1ae-657f-47d0-8759-fd8df5a94e65",
                "name": "Participant ticket"
              }
            ],
            "contactId": "023b6403-17e6-42dc-b475-a5ae6c2b8df4",
            "guestDetails": {
              "email": "john.doe@mail.com",
              "firstName": "John",
              "lastName": "Doe",
              "formResponse": {
                "inputValues": [
                  {
                    "inputName": "firstName",
                    "value": "John",
                    "values": []
                  },
                  {
                    "inputName": "lastName",
                    "value": "Doe",
                    "values": []
                  },
                  {
                    "inputName": "email",
                    "value": "john.doe@mail.com",
                    "values": []
                  }
                ]
              },
              "checkedIn": false
            },
            "attendanceStatus": "ATTENDING",
            "createdDate": "2023-01-11T09:17:07.925Z",
            "updatedDate": "2023-01-11T09:17:07.925Z",
            "attendanceStatusUpdatedDate": "2023-01-11T09:17:07.841Z",
            "guestType": "BUYER"
          }
        ],
        "pagingMetadata": {
          "count": 1
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/guests/query' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      --data-binary '{
        "query": {
          "filter": {
            "guestType": "RSVP"
          }
        }
      }'
      ```

      ```json Response
      {
        "guests": [
          {
            "id": "ab249e78-edb0-4857-8b4b-d42b672ee764",
            "eventId": "b189cf8b-a61f-443b-95bb-d9e88af0a277",
            "rsvpId": "431321d0-f768-45c8-a390-9827c0107e0f",
            "tickets": [],
            "contactId": "023b6403-17e6-42dc-b475-a5ae6c2b8df4",
            "attendanceStatus": "IN_WAITLIST",
            "createdDate": "2023-01-10T13:22:23.302Z",
            "updatedDate": "2023-01-10T13:22:23.302Z",
            "attendanceStatusUpdatedDate": "2023-01-10T13:22:23Z",
            "guestType": "RSVP"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {}
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of guests given the provided paging, filtering, and sorting.
    Query Event Guests runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`
    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language), [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination), and [Field Projection](https://dev.wix.com/api/rest/getting-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `eventId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `rsvpId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `contactId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `memberId` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `secondaryLanguageCode` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `ticketNumber` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `orderNumber` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | None |
    | `attendanceStatus` | `$eq`, `$ne`, `$in` | None |
    | `guestType` | `$eq`, `$ne`, `$in` | None |
    | `guestDetails.checkedIn` | `$eq`, `$ne`, `$exists` | None |
    | `tickets` | `$exists` | None |
    | `_createdDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `attendanceStatusUpdatedDate` | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-GUEST-LIST">Read Event Tickets and Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { guests } from '@wix/events';

    guests.queryGuests(query, options): GuestsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="GuestQuery" required>

      Query options. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format: &lt;br/> `"filter" : { "fieldName1": "value1", "fieldName2":{"$operator":"value2"} }`. &lt;br/> &lt;br/> **Example:** &lt;br/> `"filter" : { "eventId": "sd3f-jhds-4fs77", "ticketNumber": {"$startsWith":"478"} }` &lt;br/> &lt;br/> See [supported fields and operators](https://dev.wix.com/api/rest/wix-events/wix-events/filter-and-sort) for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format: &lt;br/> `[{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]` &lt;br/> &lt;br/> **Example:** &lt;br/> `[{"fieldName":"createdDate","direction":"DESC"}]` &lt;br/> &lt;br/> See [supported fields](https://dev.wix.com/api/rest/wix-events/wix-events/filter-and-sort) for more information.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QueryEventGuestsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Predefined sets of fields to return.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `GUEST_DETAILS` — Returns `guestDetails` and `tickets`.
          - `GUEST_TOTAL` — Returns `totalGuests`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `GuestsQueryBuilder`.

    <ResponseField name="guests" type="EventGuest[]">

      List of guests.

      See the [Guest Object](/api-reference/business-solutions/events/registration/event-guests/guest-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Metadata for the paginated results.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-event-guests" id="ex-sdk-query-event-guests-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-event-guests" id="ex-sdk-query-event-guests-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-event-guests" id="ex-sdk-query-event-guests-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-event-guests" id="ex-sdk-query-event-guests-3" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-event-guests-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-event-guests-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query guests for an event</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Retrieve all guests</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Retrieve all guests (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryGuests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-event-guests-0"><span className="wix-picker-name">Query guests for an event</span></label>
    <label htmlFor="ex-sdk-query-event-guests-1"><span className="wix-picker-name">Retrieve all guests</span></label>
    <label htmlFor="ex-sdk-query-event-guests-2"><span className="wix-picker-name">Retrieve all guests (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-query-event-guests-3"><span className="wix-picker-name">queryGuests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query guests for an event
    import { guests } from "@wix/events";

    const eventId = "201037a0-3ecc-4ee4-976b-a1c931c42a6b";

    async function queryGuests() {
      const response = await guests
        .queryGuests()
        .eq("eventId", eventId)
        .limit(10)
        .find();
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Retrieve all guests
    import { guests } from "@wix/events";

    export async function myQueryGuestsFunction() {
      try {
        const items = await guests.queryGuests({ fields: ["GUEST_DETAILS"] }).find();
        console.log('Success! Guests: ', items);
        return items;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     * "_items": [
     *  {
     *     "eventId": "3a80a30c-643d-4b11-b38f-1150dbe7c538",
     *     "orderNumber": "2SVV-RH7C-P3D",
     *     "tickets": [],
     *     "contactId": "c64c558f-ed41-4395-b820-f8dd66c31702",
     *     "guestDetails": {
     *      "email": "johndoe@mail.com",
     *       "firstName": "John",
     *       "lastName": "Doe",
     *       "formResponse": {
     *         "inputValues": [
     *          {
     *            "inputName": "firstName",
     *             "value": "John",
     *             "values": []
     *           },
     *           {
     *             "inputName": "lastName",
     *            "value": "Doe",
     *             "values": []
     *           },
     *           {
     *             "inputName": "email",
     *             "value": "johndoe@mail.com",
     *             "values": []
     *          }
     *         ]
     *       },
     *       "checkedIn": true
     *     }
     * ],
     * "_originQuery": {
     *   "filterTree": {
     *     "$and": []
     *   },
     *  "invalidArguments": [],
     *   "encoder": {},
     *   "transformationPaths": {},
     *   "sort": [],
     *   "paging": {},
     *   "pagingMethod": "CURSOR",
     *   "builderOptions": {
     *     "cursorWithEmptyFilterAndSort": true
     *   }
     * },
     * "_limit": 50,
     * "_nextCursor": "",
     * "_prevCursor": "",
     * "cursors": {
     *   "next": "",
     *   "prev": ""
     * }
     * }
     * } 
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Retrieve all guests (with elevated permissions)
    import { guests } from "@wix/events";
    import { auth } from "@wix/essentials";

    const elevatedQueryGuests = auth.elevate(guests.queryGuests);

    export async function myQueryGuestsFunction() {
      try {
        const items = await elevatedQueryGuests({ fields: ["GUEST_DETAILS"] }).find();
        console.log('Success! Guests: ', items);
        return items;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     * "_items": [
     *  {
     *     "eventId": "3a80a30c-643d-4b11-b38f-1150dbe7c538",
     *     "orderNumber": "2SVV-RH7C-P3D",
     *     "tickets": [],
     *     "contactId": "c64c558f-ed41-4395-b820-f8dd66c31702",
     *     "guestDetails": {
     *      "email": "johndoe@mail.com",
     *       "firstName": "John",
     *       "lastName": "Doe",
     *       "formResponse": {
     *         "inputValues": [
     *          {
     *            "inputName": "firstName",
     *             "value": "John",
     *             "values": []
     *           },
     *           {
     *             "inputName": "lastName",
     *            "value": "Doe",
     *             "values": []
     *           },
     *           {
     *             "inputName": "email",
     *             "value": "johndoe@mail.com",
     *             "values": []
     *          }
     *         ]
     *       },
     *       "checkedIn": true
     *     }
     * ],
     * "_originQuery": {
     *   "filterTree": {
     *     "$and": []
     *   },
     *  "invalidArguments": [],
     *   "encoder": {},
     *   "transformationPaths": {},
     *   "sort": [],
     *   "paging": {},
     *   "pagingMethod": "CURSOR",
     *   "builderOptions": {
     *     "cursorWithEmptyFilterAndSort": true
     *   }
     * },
     * "_limit": 50,
     * "_nextCursor": "",
     * "_prevCursor": "",
     * "cursors": {
     *   "next": "",
     *   "prev": ""
     * }
     * }
     * } 
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryGuests (self-hosted)
    import { createClient } from '@wix/sdk';
    import { guests } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { guests },
      // Include the auth strategy and host as relevant
    });

    async function queryGuests(query,options) {
      const response = await myWixClient.guests.queryGuests(query,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
