---
title: "Get Menu Ordering Settings"
description: "Retrieves a menu ordering settings entity."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a menu ordering settings entity.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/menu-ordering-settings/v1/menu-ordering-settings/{menuOrderingSettingsId}
    ```

    ## Path Params

    <ParamField path="menuOrderingSettingsId" type="string" required>

      ID of the menu ordering settings entity to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="menuOrderingSettings" type="MenuOrderingSettings">

      The requested menu ordering settings entity.

      See the [Menu Ordering Settings Object](/api-reference/business-solutions/restaurants/online-orders/menu-ordering-settings/menu-ordering-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://www.wixapis.com/menu-ordering-settings/v1/menu-ordering-settings/48605ac5-53fc-49b4-96b2-87f7636f5ce0 \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "menuOrderingSettings": {
          "id": "f83c385c-1485-4daa-9bba-bc332e12c8ce",
          "revision": "3",
          "createdDate": "2024-07-08T13:44:00.443Z",
          "updatedDate": "2024-07-08T13:50:08.572Z",
          "operationId": "48605ac5-53fc-49b4-96b2-87f7636f5ce0",
          "menuId": "bbb65621-6338-4837-8fdd-aebc654523e0",
          "onlineOrderingEnabled": true,
          "availability": {
            "type": "ALWAYS_AVAILABLE",
            "timeZone": "Asia/Jerusalem"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a menu ordering settings entity.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { menuOrderingSettings } from '@wix/restaurants';

    menuOrderingSettings.getMenuOrderingSettings(menuOrderingSettingsId): Promise<MenuOrderingSettings>
    ```

    ## Method Parameters

    <ParamField body="menuOrderingSettingsId" type="string" required>

      ID of the menu ordering settings entity to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<MenuOrderingSettings>`.

    See the [Menu Ordering Settings Object](/api-reference/business-solutions/restaurants/online-orders/menu-ordering-settings/menu-ordering-settings-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-menu-ordering-settings" id="ex-sdk-get-menu-ordering-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-menu-ordering-settings" id="ex-sdk-get-menu-ordering-settings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-menu-ordering-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-menu-ordering-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getMenuOrderingSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getMenuOrderingSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-menu-ordering-settings-0"><span className="wix-picker-name">getMenuOrderingSettings</span></label>
    <label htmlFor="ex-sdk-get-menu-ordering-settings-1"><span className="wix-picker-name">getMenuOrderingSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getMenuOrderingSettings
    import { menuOrderingSettings } from '@wix/restaurants';

    async function getMenuOrderingSettings(menuOrderingSettingsId) {
      const response = await menuOrderingSettings.getMenuOrderingSettings(menuOrderingSettingsId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getMenuOrderingSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { menuOrderingSettings } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { menuOrderingSettings },
      // Include the auth strategy and host as relevant
    });

    async function getMenuOrderingSettings(menuOrderingSettingsId) {
      const response = await myWixClient.menuOrderingSettings.getMenuOrderingSettings(menuOrderingSettingsId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
