---
title: "onOperationGroupUpdated"
description: "The onOperationGroupUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.operation_group_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onOperationGroupUpdated
  import { operationGroups } from '@wix/restaurants';

  operationGroups.onOperationGroupUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOperationGroupUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { operationGroups } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        operationGroups,
      },
  });

  wixClient.operationGroups.onOperationGroupUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
