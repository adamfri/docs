---
title: "Delete Marketing Tag"
description: "Deletes a marketing tag."
icon: "trash"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Deletes a marketing tag.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.MANAGE-TAGS">Manage Marketing Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/marketing/v1/tags
    ```

    ## Query Params

    <ParamField query="type" type="string">

      Marketing tag to delete.

      **Supported values:**

      - `GOOGLE_ADS`
      - `GOOGLE_ANALYTICS`
      - `YANDEX_METRICA`
      - `FACEBOOK_PIXEL`
      - `GOOGLE_TAG_MANAGER`
      - `TIKTOK_PIXEL`
      - `GOOGLE_TAG_MANAGER_CONSENT_MODE`
      - `GOOGLE_ANALYTICS_CONSENT_MODE`

    </ParamField>

    ## Response Object

    Returns `DeleteMarketingTagResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Marketing Tag Deleted | `wix.marketing.v1.tag_deleted` | Triggered when a marketing tag is deleted. |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Deletes a marketing tag.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.MANAGE-TAGS">Manage Marketing Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingTags } from '@wix/marketing-tags';

    marketingTags.deleteMarketingTag(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="DeleteMarketingTagOptions">

      Options to use when deleting a marketing tag.

      <Expandable title="options properties">

        <ParamField body="type" type="string">

          Marketing tag to delete.

          **Supported values:**

          - `GOOGLE_ADS`
          - `GOOGLE_ANALYTICS`
          - `YANDEX_METRICA`
          - `FACEBOOK_PIXEL`
          - `GOOGLE_TAG_MANAGER`
          - `TIKTOK_PIXEL`
          - `GOOGLE_TAG_MANAGER_CONSENT_MODE`
          - `GOOGLE_ANALYTICS_CONSENT_MODE`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `DeleteMarketingTagResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tag Deleted | `wix.marketing.v1.tag_deleted` | Triggered when a marketing tag is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-marketing-tag" id="ex-sdk-delete-marketing-tag-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-marketing-tag" id="ex-sdk-delete-marketing-tag-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-marketing-tag" id="ex-sdk-delete-marketing-tag-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-marketing-tag-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-marketing-tag-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingTag</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingTag (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-marketing-tag-0"><span className="wix-picker-name">deleteMarketingTag</span></label>
    <label htmlFor="ex-sdk-delete-marketing-tag-1"><span className="wix-picker-name">deleteMarketingTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-marketing-tag-2"><span className="wix-picker-name">deleteMarketingTag (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteMarketingTag
    import { marketingTags } from '@wix/marketing-tags';

    async function deleteMarketingTag(options) {
      const response = await marketingTags.deleteMarketingTag(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMarketingTag (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingTags } from '@wix/marketing-tags';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingTags },
      // Include the auth strategy and host as relevant
    });

    async function deleteMarketingTag(options) {
      const response = await myWixClient.marketingTags.deleteMarketingTag(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMarketingTag (with elevated permissions)
    import { marketingTags } from '@wix/marketing-tags';
    import { auth } from '@wix/essentials';

    async function myDeleteMarketingTagMethod(options) {
      const elevatedDeleteMarketingTag = auth.elevate(marketingTags.deleteMarketingTag);
      const response = await elevatedDeleteMarketingTag(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
