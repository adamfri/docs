---
title: "Update Invoice"
description: "Updates an invoice."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an invoice.

    To update multiple invoices in a single API call, call [Bulk Update Invoices](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoices/bulk-update-invoices).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/invoices/v4/invoices/{invoice.id}
    ```

    ## Path Params

    <ParamField path="invoice.id" type="string" required>

      Invoice ID.

    </ParamField>

    ## Body Params

    <ParamField body="invoice" type="Invoice" required>

      Invoice to update, may be partial.

      See the [Invoice Object](/api-reference/business-management/get-paid/invoices/invoices/invoice-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="invoice" type="Invoice">

      Updated invoice.

      See the [Invoice Object](/api-reference/business-management/get-paid/invoices/invoices/invoice-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `NO_META_SITE` | Couldn't resolve the site. Make sure authentication includes a valid site ID. |
    | `400` | `INVALID_ARGUMENT` | `LINE_ITEM_CATALOG_ITEM_NOT_FOUND` | A line item references a catalog item that does not exist or is unavailable in its source catalog. |
    | `400` | `INVALID_ARGUMENT` | `DEPOSIT_MUST_BE_LESS_THAN_TOTAL` | Deposit amount must be less than the invoice total. |
    | `400` | `INVALID_ARGUMENT` | `TOTAL_MUST_BE_GREATER_OR_EQUAL_TO_ZERO` | Invoice total must be 0 or greater. |
    | `400` | `INVALID_ARGUMENT` | `BALANCE_MUST_BE_GREATER_OR_EQUAL_TO_ZERO` | Invoice balance must be 0 or greater. |
    | `400` | `INVALID_ARGUMENT` | `TOTAL_TOO_LOW_FOR_AUTO_CHARGE` | Invoice total must be at least 1 for invoices with `AUTO_CHARGE` reference type. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_ACTION` | Invoice isn't in a state that allows this action. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_FIELD_UPDATE_FOR_PAID_INVOICE` | This field can't be updated after a payment has been made. Restricted fields: `customerInfo`, `shippingInfo`, `businessDetails`, `regionalProperties`, `deposit`. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_FIELD_UPDATE_FOR_INVOICE` | Computed fields (`payments`, `taxInfo`, `totals`) can't be directly updated on standalone invoices. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_FIELD_UPDATE_FOR_PUBLISHED_INVOICE` | `presetProperties.presetId` can't be changed after the invoice is published. |
    | `428` | `FAILED_PRECONDITION` | `SUBSCRIPTION_SETTINGS_IMMUTABLE` | Subscription settings are immutable once the invoice's eCommerce order has been created (after payment is initiated). |
    | `428` | `FAILED_PRECONDITION` | `INVOICE_PRESET_NOT_FOUND` | Couldn't find the specified invoice preset. |
    | `428` | `FAILED_PRECONDITION` | `CUSTOMER_CONTACT_NOT_FOUND` | Couldn't find the specified customer contact. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_TO_CALCULATE_TAXES` | Tax calculation failed. Make sure invoice details are correct. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_TO_CALCULATE_SHIPPING` | Shipping calculation failed. Make sure shipping details are correct. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_TO_CALCULATE_CARRIER_SHIPPING` | Carrier shipping calculation failed. |
    | `500` | `INTERNAL` | `APP_INSTALLATION_FAILED` | The required apps couldn't be installed. |
    | `500` | `INTERNAL` | `SITE_PROPERTIES_FAILED` | Couldn't retrieve business details from the site properties service. |
    | `400` | `INVALID_ARGUMENT` | `ORDER_TOTAL_EXCEEDS_LIMIT` | Invoice total must be less than 10^13. |
    | `400` | `INVALID_ARGUMENT` | `DUE_DATE_BEFORE_ISSUE_DATE` | Invoice `dueDate` is before `issueDate`. |
    | `400` | `INVALID_ARGUMENT` | `INVOICE_DATE_OUT_OF_RANGE` | Invoice `issueDate` is more than 100 years before the current time, or `dueDate` is more than 100 years after the current time. |
    | `400` | `INVALID_ARGUMENT` | `TIME_ZONE_INVALID` | Invoice `regionalProperties.timeZone` must be a valid time zone. |
    | `400` | `INVALID_ARGUMENT` | `LINE_ITEMS_MUST_EXIST` | Invoice must have at least one line item. |
    | `400` | `INVALID_ARGUMENT` | `LINE_ITEMS_IDS_MUST_BE_UNIQUE` | Line item IDs must be unique. |
    | `400` | `INVALID_ARGUMENT` | `RICH_CONTENT_TOO_LARGE` | Rich content object exceeds the maximum allowed size. |
    | `400` | `INVALID_ARGUMENT` | `RICH_CONTENT_INVALID` | Rich content object couldn't be parsed. |
    | `400` | `INVALID_ARGUMENT` | `SUBSCRIPTION_MULTIPLE_DISCOUNTS_NOT_SUPPORTED` | A subscription invoice supports at most one discount: either one global discount or one discount per line item, not both. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Updated | `wix.invoices.invoices.v4.invoice_updated` | Triggered when an invoice is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Updates specific fields of an invoice using field mask.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/invoices/v4/invoices/51be0e68-75c3-4b7a-97e7-3a6643ecf3e2' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "invoice": {
            "id": "51be0e68-75c3-4b7a-97e7-3a6643ecf3e2",
            "revision": "1",
            "dueDate": "2024-03-01T23:59:59.000Z",
            "customerInfo": {
              "contactDetails": {
                "firstName": "John",
                "lastName": "Smith",
                "email": "john.smith@example.com",
                "phone": "+1-555-555-9999"
              }
            }
          }
        }'
      ```

      ```json Response
      {
        "invoice": {
          "id": "51be0e68-75c3-4b7a-97e7-3a6643ecf3e2",
          "revision": "2",
          "createdDate": "2024-01-15T10:05:23.299Z",
          "updatedDate": "2024-01-15T14:30:45.201Z",
          "status": "DRAFT",
          "statusQualifiers": [],
          "availableActions": [
            "DELETE",
            "UPDATE",
            "PUBLISH",
            "GENERATE_PDF_DOCUMENT"
          ],
          "issueDate": "2024-01-15T10:00:00.000Z",
          "dueDate": "2024-03-01T23:59:59.000Z",
          "currency": "USD",
          "sourceReference": {
            "appId": "13ee94c1-b635-8505-3391-97919052c16f",
            "externalReferenceId": "e8b6776d-9efc-4452-b528-a93f651f99a7"
          },
          "reference": {
            "referenceType": "STANDALONE",
            "standaloneReference": {}
          },
          "regionalProperties": {
            "locale": {
              "languageCode": "en",
              "country": "US"
            },
            "language": "en",
            "timeZone": "America/New_York"
          },
          "customerInfo": {
            "contactId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "contactDetails": {
              "firstName": "John",
              "lastName": "Smith",
              "email": "john.smith@example.com",
              "phone": "+1-555-555-9999",
              "company": "John's Company",
              "vatId": {
                "id": "123-45-6789",
                "type": "CPF"
              }
            },
            "billingAddress": {
              "streetAddress": {
                "number": "123",
                "name": "Billing Street"
              },
              "city": "New York",
              "subdivision": "NY",
              "country": "US",
              "postalCode": "12345"
            }
          },
          "shippingInfo": {
            "contactDetails": {
              "firstName": "John",
              "lastName": "Doe",
              "email": "john.doe@example.com",
              "phone": "+1-555-555-5555",
              "company": "John's Company",
              "vatId": {
                "id": "123-45-6789",
                "type": "CPF"
              }
            },
            "shippingAddress": {
              "streetAddress": {
                "number": "123",
                "name": "Delivery Street"
              },
              "city": "Brooklyn",
              "subdivision": "NY",
              "country": "US",
              "postalCode": "12345"
            }
          },
          "lineItems": [
            {
              "lineItemType": "CUSTOM",
              "customItem": {
                "id": "36ec066d-7c29-4169-96e6-cbb4e3c25bcd",
                "name": "Roses",
                "description": "Beautiful red roses.",
                "quantity": "30",
                "price": "4.00",
                "catalogReference": {
                  "catalogItemId": "ed618c8d-075d-4d20-9693-c9b1205a06e8",
                  "appId": "1380b703-ce81-ff05-f115-39571d94dfcd"
                }
              }
            },
            {
              "lineItemType": "CUSTOM",
              "customItem": {
                "id": "7d3c1a53-9dd4-456f-a705-db4618ae7b22",
                "name": "Big colorful wrapper",
                "description": "Complete system setup and configuration service.",
                "quantity": "1",
                "price": "5.00"
              }
            }
          ],
          "businessDetails": {
            "companyName": "Flowers Business",
            "email": "flowers@example.com",
            "phone": "+1-555-555-5555",
            "address": {
              "streetAddress": {
                "number": "123",
                "name": "Business Street"
              },
              "city": "San Francisco",
              "subdivision": "CA",
              "country": "US",
              "postalCode": "12345"
            }
          },
          "shipmentInfo": {
            "carrierId": "8252d207-557f-45bc-881d-b97e44fbef72",
            "code": "usps_express",
            "title": "Express Delivery",
            "logistics": {
              "deliveryTime": "1-2 business days",
              "instructions": "Handle with care",
              "deliveryTimeSlot": {
                "from": "2024-01-18T10:00:00.000Z",
                "to": "2024-01-20T10:00:00.000Z"
              }
            },
            "price": "29.99",
            "totals": {
              "priceBeforeTax": "29.99",
              "tax": "0.00",
              "priceAfterTax": "29.99"
            }
          },
          "additionalFees": [
            {
              "name": "Processing Fee",
              "amount": "15.00"
            }
          ],
          "totals": {
            "subtotal": "5197.00",
            "discount": "0.00",
            "shipping": "29.99",
            "tax": "467.73",
            "total": "5709.72",
            "paidAmount": "0.00",
            "balance": "5709.72"
          },
          "archived": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an invoice.

    To update multiple invoices in a single API call, call [Bulk Update Invoices](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoices/bulk-update-invoices).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { invoices } from '@wix/get-paid';

    invoices.updateInvoice(_id, invoice): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Invoice ID.

      **Read-only.**

    </ParamField>

    <ParamField body="invoice" type="UpdateInvoice" required>

      Invoice to update, may be partial.

      <Expandable title="invoice properties">

        <ParamField body="_id" type="string">

          Invoice ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the invoice is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the invoice.

          Ignored when creating an invoice.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the invoice was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the invoice was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="status" type="string">

          Invoice status.

          **Read-only.**

          **Supported values:**

          - `DRAFT` — Invoice is in draft status. Can be edited before publishing.
          - `PUBLISHING` — Invoice number is being allocated.
          - `PUBLISHED` — Invoice is published and ready for payment.
          - `PARTIALLY_PAID` — Invoice has received at least one payment but isn't fully paid.
          - `PAID` — Invoice is fully paid.
          - `VOIDED` — Invoice is voided and no longer valid.

        </ParamField>

        <ParamField body="statusQualifiers" type="string[]">

          Additional flags providing context about the invoice's state, independent of its primary status. These flags are system-managed and read-only.

          **Read-only.** Constraints: `maxItems 2`.

          **Supported values:**

          - `SENT` — Invoice was sent to a customer.
          - `VIEWED` — Mark Viewed was called for the invoice.
          - `OVERDUE` — Invoice `dueDate` has passed and the invoice isn't fully paid or voided.

        </ParamField>

        <ParamField body="availableActions" type="string[]">

          Actions that can be performed on this invoice based on its current status.

          **Read-only.** Constraints: `maxItems 11`.

          **Supported values:**

          - `DELETE` — Invoice can be deleted. Available when invoice is in `DRAFT` status.
          - `UPDATE` — Invoice can be updated. Available when invoice is in `DRAFT`, `PUBLISHED`, or `PARTIALLY_PAID` status.
          - `PUBLISH` — Invoice can be published. Available when invoice is in `DRAFT` status.
          - `PAY` — Payment can be recorded. Available when invoice is in `PUBLISHED` or `PARTIALLY_PAID` status.
          - `VOID` — Invoice can be voided. Available when invoice is in `PUBLISHED` or `PARTIALLY_PAID` status.
          - `SEND` — Invoice can be sent. Available when invoice is in `PUBLISHED` or later status.
          - `MARK_AS_VIEWED` — Invoice can be marked as viewed. Available when invoice isn't in `DRAFT` status.
          - `GENERATE_PDF_DOCUMENT` — PDF document can be generated. Not available while another PDF is being processed.
          - `ARCHIVE` — Invoice can be archived. Available when invoice isn't in `DRAFT` status and isn't already archived.
          - `UNARCHIVE` — Invoice can be unarchived. Available when invoice is archived.
          - `MARK_AS_SENT` — Invoice can be marked as sent. Available when invoice isn't in `DRAFT` status.

        </ParamField>

        <ParamField body="sourceReference" type="SourceReference">

          Information about the app that created the invoice.

          Constraints: `immutable`.

          <Expandable title="sourceReference properties">

            <ParamField body="appId" type="string">

              ID of the app that created the invoice.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="externalReferenceId" type="string">

              Developer-provided ID for linking this invoice to a record in an external system. For example, an order ID in a third-party platform.

            </ParamField>

            <ParamField body="externalProperties" type="object">

              Additional metadata provided by the source app. Used for passing app-specific context
              that does not fit into the standard fields, such as internal IDs or configuration details.

              Constraints: `format map`, `maxItems 10`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="reference" type="Reference">

          Reference linking the invoice to a Wix eCommerce order or standalone context.

          Constraints: `immutable`.

          <Expandable title="reference properties">

            <ParamField body="referenceType" type="string">

              Invoice type.

              **Supported values:**

              - `STANDALONE` — Independent invoice. An eCommerce order is created automatically when the first payment is made.
              - `ORDER` — Invoice created for an existing Wix eCommerce order. Lifecycle is bound to that order.
              - `MIGRATED` — Invoice migrated from an older invoicing system.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="referenceOptions" type="referenceOptions">

              referenceOptions

              _Nested structure omitted for brevity — see the `referenceOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="regionalProperties" type="RegionalProperties">

          Regional properties for the invoice. When not provided, defaults are taken from the [Site Properties API](https://dev.wix.com/docs/api-reference/business-management/site-properties/introduction).

          <Expandable title="regionalProperties properties">

            <ParamField body="locale" type="Locale">

              Locale used for formatting numbers, dates, and times.

              _Nested structure omitted for brevity — see the `Locale` object._

            </ParamField>

            <ParamField body="timeZone" type="string">

              Time zone in UTC offset or IANA format.

            </ParamField>

            <ParamField body="language" type="string">

              Invoice language in ISO 639-1 format.

              Constraints: `format LANGUAGE`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="numbering" type="Numbering">

          Invoice numbering. Assigned when the invoice is published.

          **Read-only.**

          <Expandable title="numbering properties">

            <ParamField body="number" type="integer">

              Sequential invoice number.

            </ParamField>

            <ParamField body="prefix" type="string">

              Prefix before the invoice number.

            </ParamField>

            <ParamField body="suffix" type="string">

              Suffix after the invoice number.

            </ParamField>

            <ParamField body="displayNumber" type="string">

              Full display number in `[prefix][number][suffix]` format.

              Constraints: `maxLength 30`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="issueDate" type="datetime">

          Date the invoice was issued.

        </ParamField>

        <ParamField body="dueDate" type="datetime">

          Date when payment is due. Cannot be before `issueDate`.

        </ParamField>

        <ParamField body="title" type="string">

          Invoice title.

        </ParamField>

        <ParamField body="currency" type="string">

          Currency code for all monetary amounts on the invoice.
          Three-letter code in [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format.
          Set at creation and can't be changed.

          Constraints: `format CURRENCY`, `immutable`.

        </ParamField>

        <ParamField body="customerInfo" type="CustomerInfo">

          Customer information including contact details and billing address.

          <Expandable title="customerInfo properties">

            <ParamField body="contactId" type="string">

              ID of the customer's contact in the [Contacts API](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/introduction).

            </ParamField>

            <ParamField body="contactDetails" type="FullAddressContactDetails">

              Customer's contact details including name, email, and phone.

              _Nested structure omitted for brevity — see the `FullAddressContactDetails` object._

            </ParamField>

            <ParamField body="billingAddress" type="Address">

              Customer's billing address.

              _Nested structure omitted for brevity — see the `Address` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="shippingInfo" type="ShippingInfo">

          Shipping destination for the invoice, including the recipient's contact details and address. Can exist independently without a shipment. When a shipment is configured, `shipmentInfo` uses this address as the delivery destination unless a pickup address is specified.

          <Expandable title="shippingInfo properties">

            <ParamField body="contactDetails" type="FullAddressContactDetails">

              Recipient's contact details.

              _Nested structure omitted for brevity — see the `FullAddressContactDetails` object._

            </ParamField>

            <ParamField body="shippingAddress" type="Address">

              Recipient's shipping address.

              _Nested structure omitted for brevity — see the `Address` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="businessDetails" type="BusinessDetails">

          Business details displayed on the invoice, such as company name, address, email, phone, and logo. Automatically populated from the [Site Properties API](https://dev.wix.com/docs/api-reference/business-management/site-properties/introduction) when the invoice is created, if not explicitly provided. These details aren't synced with Site Properties after creation and become permanently fixed once `status` reaches `PARTIALLY_PAID`, `PAID`, or `VOIDED`.

          <Expandable title="businessDetails properties">

            <ParamField body="companyName" type="string">

              Business name.

            </ParamField>

            <ParamField body="email" type="string">

              Business email.

            </ParamField>

            <ParamField body="phone" type="string">

              Business phone.

            </ParamField>

            <ParamField body="address" type="Address">

              Business address.

              _Nested structure omitted for brevity — see the `Address` object._

            </ParamField>

            <ParamField body="imageId" type="string">

              Wix Media image ID for the business logo.

            </ParamField>

            <ParamField body="companyId" type="string">

              Company registration number or other business identifier displayed on the invoice.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="lineItems" type="LineItem[]">

          Line items on the invoice.

          Constraints: `minItems 0`, `maxItems 300`.

          <Expandable title="lineItems properties">

            <ParamField body="lineItemType" type="string">

              Line item type.

              **Supported values:**

              - `CUSTOM` — Custom line item with manually specified details.
              - `CATALOG` — Line item from a product catalog.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="shipmentInfo" type="ShipmentInfo">

          Delivery and shipping cost details (carrier, method, price, tax) tied to the Wix eCommerce integration. Defaults to using `shippingInfo.shippingAddress` as the delivery destination unless `shipmentInfo.logistics.pickupDetails` is specified.

          <Expandable title="shipmentInfo properties">

            <ParamField body="carrierId" type="string">

              Shipping carrier ID.

            </ParamField>

            <ParamField body="code" type="string">

              Shipping method code.

            </ParamField>

            <ParamField body="title" type="string">

              Shipping method title.

            </ParamField>

            <ParamField body="logistics" type="DeliveryLogistics">

              Delivery logistics details.

              _Nested structure omitted for brevity — see the `DeliveryLogistics` object._

            </ParamField>

            <ParamField body="region" type="ShipmentRegion">

              Shipping region.

              _Nested structure omitted for brevity — see the `ShipmentRegion` object._

            </ParamField>

            <ParamField body="price" type="string">

              Shipping price.

            </ParamField>

            <ParamField body="taxInfo" type="ShipmentTaxInfo">

              Tax information for shipping.

              _Nested structure omitted for brevity — see the `ShipmentTaxInfo` object._

            </ParamField>

            <ParamField body="totals" type="ShipmentTotals">

              Shipping cost totals.

              **Read-only.**

              _Nested structure omitted for brevity — see the `ShipmentTotals` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="additionalFees" type="AdditionalFee[]">

          Additional fees applied to the invoice.

          Constraints: `maxItems 20`.

          <Expandable title="additionalFees properties">

            <ParamField body="_id" type="string">

              Fee ID.

            </ParamField>

            <ParamField body="name" type="string">

              Fee name.

              Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

            <ParamField body="amount" type="string">

              Fee amount.

              Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 1000000000000000`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="discounts" type="Discount[]">

          Invoice-level discounts.

          Constraints: `maxItems 20`.

          <Expandable title="discounts properties">

            <ParamField body="_id" type="string">

              Discount ID.

            </ParamField>

            <ParamField body="name" type="string">

              Discount name.

            </ParamField>

            <ParamField body="discountType" type="string">

              Type of the discount.

              **Supported values:**

              - `FIXED` — Fixed monetary amount.
              - `PERCENTAGE` — Percentage-based discount.

            </ParamField>

            <ParamField body="appliedAmount" type="string">

              Calculated discount amount actually applied.

              **Read-only.** Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 1000000000000000`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="deposit" type="Deposit">

          Deposit payment configuration.

          <Expandable title="deposit properties">

            <ParamField body="depositType" type="string">

              Type of the deposit.

              **Supported values:**

              - `FIXED` — Fixed monetary amount.
              - `PERCENTAGE` — Percentage-based deposit.

            </ParamField>

            <ParamField body="total" type="string">

              Calculated deposit amount.

              **Read-only.**

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="depositOptions" type="depositOptions">

              depositOptions

              _Nested structure omitted for brevity — see the `depositOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="taxInfo" type="TaxInfo">

          Tax information.

          **Read-only.**

          <Expandable title="taxInfo properties">

            <ParamField body="taxBreakdown" type="TaxBreakdown[]">

              Breakdown of taxes applied.

              Constraints: `maxItems 50`.

              _Nested structure omitted for brevity — see the `TaxBreakdown[]` object._

            </ParamField>

            <ParamField body="taxExempt" type="boolean">

              Whether the invoice is tax-exempt.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="payments" type="Payment[]">

          Payment records.

          **Read-only.** Constraints: `maxItems 100`.

          <Expandable title="payments properties">

            <ParamField body="_id" type="string">

              Payment ID.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="_createdDate" type="datetime">

              Date and time the payment was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

            </ParamField>

            <ParamField body="_updatedDate" type="datetime">

              Date and time the payment was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

            </ParamField>

            <ParamField body="amount" type="string">

              Payment amount.

              Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 1000000000000000`.

            </ParamField>

            <ParamField body="paymentDate" type="datetime">

              Date and time the payment was made.

            </ParamField>

            <ParamField body="paymentType" type="string">

              Type of payment method used.

              **Supported values:**

              - `REGULAR` — Standard payment made through Wix Payments, Stripe, or other [supported payment gateways](https://www.wix.com/payments/payment-gateways). Doesn't include gift cards.
              - `GIFT_CARD` — Payment using a Wix eCommerce gift card.

            </ParamField>

            <ParamField body="receiptType" type="string">

              Type of receipt.

              **Supported values:**

              - `WIX` — Wix receipt.
              - `EXTERNAL` — External receipt.

            </ParamField>

            <ParamField body="cashRounding" type="CashRounding">

              Details about cash rounding applied to this payment, when relevant.

              _Nested structure omitted for brevity — see the `CashRounding` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="totals" type="Totals">

          Financial summary including subtotal, taxes, and total amount.

          **Read-only.**

          <Expandable title="totals properties">

            <ParamField body="subtotal" type="string">

              Subtotal before taxes, discounts, and fees.

              Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 1000000000000000`.

            </ParamField>

            <ParamField body="discount" type="string">

              Total discount amount.

            </ParamField>

            <ParamField body="shipping" type="string">

              Total shipping costs.

            </ParamField>

            <ParamField body="exempt" type="string">

              Total tax-exempt amount.

            </ParamField>

            <ParamField body="tax" type="string">

              Total tax amount.

            </ParamField>

            <ParamField body="total" type="string">

              Total amount due.

              Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 1000000000000000`.

            </ParamField>

            <ParamField body="paidAmount" type="string">

              Total amount already paid.

              Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 1000000000000000`.

            </ParamField>

            <ParamField body="balance" type="string">

              Outstanding balance.

              **Read-only.** Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 1000000000000000`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="archived" type="boolean">

          Whether the invoice is archived.

          **Read-only.**

        </ParamField>

        <ParamField body="customFields" type="CustomFields">

          Custom fields displayed on the invoice document.

          <Expandable title="customFields properties">

            <ParamField body="headerCustomFields" type="CustomField[]">

              Custom fields in the invoice header.

              Constraints: `maxItems 4`.

              _Nested structure omitted for brevity — see the `CustomField[]` object._

            </ParamField>

            <ParamField body="businessCustomFields" type="CustomField[]">

              Custom fields in the business details section.

              Constraints: `maxItems 5`.

              _Nested structure omitted for brevity — see the `CustomField[]` object._

            </ParamField>

            <ParamField body="customerCustomFields" type="CustomField[]">

              Custom fields in the customer information section.

              Constraints: `maxItems 4`.

              _Nested structure omitted for brevity — see the `CustomField[]` object._

            </ParamField>

            <ParamField body="footerCustomField" type="FooterContent">

              Footer content.

              _Nested structure omitted for brevity — see the `FooterContent` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="presetProperties" type="PresetProperties">

          Invoice preset applied to this invoice. Determines the appearance and custom fields on the invoice document. If not provided, the [default preset](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoice-presets/set-default-invoice-preset) is used.

          <Expandable title="presetProperties properties">

            <ParamField body="presetId" type="string">

              Invoice preset ID. If not provided, the [default preset](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/invoice-presets/get-default-invoice-preset) is used.

            </ParamField>

            <ParamField body="invoicePreset" type="InvoicePreset">

              Snapshot of the invoice preset at the time the invoice was published.

              **Read-only.**

              _Nested structure omitted for brevity — see the `InvoicePreset` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="attachments" type="Attachment[]">

          Files attached to the invoice.

          Constraints: `maxItems 5`.

          <Expandable title="attachments properties">

            <ParamField body="fileName" type="string">

              File name. For example, `invoice.pdf`.

              Constraints: `minLength 1`, `maxLength 100`.

            </ParamField>

            <ParamField body="fileUrl" type="string">

              File URL.

              Constraints: `format WEB_URL`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="documentInfo" type="DocumentInfo">

          PDF document generation status.

          **Read-only.**

          <Expandable title="documentInfo properties">

            <ParamField body="status" type="string">

              Document generation status.

              **Supported values:**

              - `PROCESSING` — PDF generation is in progress.
              - `AVAILABLE` — PDF is ready to download.
              - `FAILED` — PDF generation failed.
              - `EXPIRED` — PDF download URL has expired.
              - `OUTDATED` — PDF is out of date because the invoice has been modified after generation.

            </ParamField>

            <ParamField body="downloadUrl" type="string">

              URL to download the generated PDF. Available when `status` is `AVAILABLE`.

            </ParamField>

            <ParamField body="documentExpirationDate" type="datetime">

              Date and time the document download URL expires.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="links" type="Links">

          Invoice URL.

          **Read-only.**

          <Expandable title="links properties">

            <ParamField body="relativePath" type="string">

              Relative URL path to the invoice.

            </ParamField>

            <ParamField body="url" type="string">

              Full URL to the invoice.

            </ParamField>

            <ParamField body="previewPassword" type="string">

              Password required to view the invoice when HIPAA compliance is enabled for the site.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="businessLocation" type="BusinessLocation">

          Business location.

          <Expandable title="businessLocation properties">

            <ParamField body="_id" type="string">

              Location ID. Learn more about the [Wix Locations API](https://dev.wix.com/docs/api-reference/business-management/locations/introduction).

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="name" type="string">

              Location name.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="activityInfo" type="ActivityInfo">

          Read-only record of when the invoice was last sent and last viewed.

          **Read-only.**

          <Expandable title="activityInfo properties">

            <ParamField body="sentToContactIds" type="string[]">

              Contact IDs that the invoice was sent to.

              Constraints: `format GUID`, `maxItems 5`.

            </ParamField>

            <ParamField body="lastSentDate" type="datetime">

              Date and time the invoice was last sent.

            </ParamField>

            <ParamField body="lastViewedDate" type="datetime">

              Date and time the invoice was last viewed.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="hasSubscriptions" type="boolean">

          Whether the invoice contains subscription line items. `true` when at least one line item carries subscription info.

          **Read-only.**

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tags for organizing invoices. `publicTags` are visible to anyone with access to the invoice. `privateTags` are restricted to site owners and require additional permissions to access.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, typically restricted from site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="publicTags" type="TagList">

              Tags that are exposed to anyone with access to the entity, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Invoice`.

    See the [Invoice Object](/api-reference/business-management/get-paid/invoices/invoices/invoice-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `NO_META_SITE` | Couldn't resolve the site. Make sure authentication includes a valid site ID. |
    | `400` | `INVALID_ARGUMENT` | `LINE_ITEM_CATALOG_ITEM_NOT_FOUND` | A line item references a catalog item that does not exist or is unavailable in its source catalog. |
    | `400` | `INVALID_ARGUMENT` | `DEPOSIT_MUST_BE_LESS_THAN_TOTAL` | Deposit amount must be less than the invoice total. |
    | `400` | `INVALID_ARGUMENT` | `TOTAL_MUST_BE_GREATER_OR_EQUAL_TO_ZERO` | Invoice total must be 0 or greater. |
    | `400` | `INVALID_ARGUMENT` | `BALANCE_MUST_BE_GREATER_OR_EQUAL_TO_ZERO` | Invoice balance must be 0 or greater. |
    | `400` | `INVALID_ARGUMENT` | `TOTAL_TOO_LOW_FOR_AUTO_CHARGE` | Invoice total must be at least 1 for invoices with `AUTO_CHARGE` reference type. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_ACTION` | Invoice isn't in a state that allows this action. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_FIELD_UPDATE_FOR_PAID_INVOICE` | This field can't be updated after a payment has been made. Restricted fields: `customerInfo`, `shippingInfo`, `businessDetails`, `regionalProperties`, `deposit`. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_FIELD_UPDATE_FOR_INVOICE` | Computed fields (`payments`, `taxInfo`, `totals`) can't be directly updated on standalone invoices. |
    | `428` | `FAILED_PRECONDITION` | `ILLEGAL_FIELD_UPDATE_FOR_PUBLISHED_INVOICE` | `presetProperties.presetId` can't be changed after the invoice is published. |
    | `428` | `FAILED_PRECONDITION` | `SUBSCRIPTION_SETTINGS_IMMUTABLE` | Subscription settings are immutable once the invoice's eCommerce order has been created (after payment is initiated). |
    | `428` | `FAILED_PRECONDITION` | `INVOICE_PRESET_NOT_FOUND` | Couldn't find the specified invoice preset. |
    | `428` | `FAILED_PRECONDITION` | `CUSTOMER_CONTACT_NOT_FOUND` | Couldn't find the specified customer contact. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_TO_CALCULATE_TAXES` | Tax calculation failed. Make sure invoice details are correct. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_TO_CALCULATE_SHIPPING` | Shipping calculation failed. Make sure shipping details are correct. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_TO_CALCULATE_CARRIER_SHIPPING` | Carrier shipping calculation failed. |
    | `500` | `INTERNAL` | `APP_INSTALLATION_FAILED` | The required apps couldn't be installed. |
    | `500` | `INTERNAL` | `SITE_PROPERTIES_FAILED` | Couldn't retrieve business details from the site properties service. |
    | `400` | `INVALID_ARGUMENT` | `ORDER_TOTAL_EXCEEDS_LIMIT` | Invoice total must be less than 10^13. |
    | `400` | `INVALID_ARGUMENT` | `DUE_DATE_BEFORE_ISSUE_DATE` | Invoice `dueDate` is before `issueDate`. |
    | `400` | `INVALID_ARGUMENT` | `INVOICE_DATE_OUT_OF_RANGE` | Invoice `issueDate` is more than 100 years before the current time, or `dueDate` is more than 100 years after the current time. |
    | `400` | `INVALID_ARGUMENT` | `TIME_ZONE_INVALID` | Invoice `regionalProperties.timeZone` must be a valid time zone. |
    | `400` | `INVALID_ARGUMENT` | `LINE_ITEMS_MUST_EXIST` | Invoice must have at least one line item. |
    | `400` | `INVALID_ARGUMENT` | `LINE_ITEMS_IDS_MUST_BE_UNIQUE` | Line item IDs must be unique. |
    | `400` | `INVALID_ARGUMENT` | `RICH_CONTENT_TOO_LARGE` | Rich content object exceeds the maximum allowed size. |
    | `400` | `INVALID_ARGUMENT` | `RICH_CONTENT_INVALID` | Rich content object couldn't be parsed. |
    | `400` | `INVALID_ARGUMENT` | `SUBSCRIPTION_MULTIPLE_DISCOUNTS_NOT_SUPPORTED` | A subscription invoice supports at most one discount: either one global discount or one discount per line item, not both. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Updated | `wix.invoices.invoices.v4.invoice_updated` | Triggered when an invoice is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-invoice" id="ex-sdk-update-invoice-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-invoice" id="ex-sdk-update-invoice-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-invoice" id="ex-sdk-update-invoice-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-invoice-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-invoice-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateInvoice</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateInvoice (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateInvoice (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-invoice-0"><span className="wix-picker-name">updateInvoice</span></label>
    <label htmlFor="ex-sdk-update-invoice-1"><span className="wix-picker-name">updateInvoice (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-invoice-2"><span className="wix-picker-name">updateInvoice (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateInvoice
    import { invoices } from '@wix/get-paid';

    async function updateInvoice(_id,invoice) {
      const response = await invoices.updateInvoice(_id,invoice);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateInvoice (self-hosted)
    import { createClient } from '@wix/sdk';
    import { invoices } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { invoices },
      // Include the auth strategy and host as relevant
    });

    async function updateInvoice(_id,invoice) {
      const response = await myWixClient.invoices.updateInvoice(_id,invoice);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateInvoice (with elevated permissions)
    import { invoices } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myUpdateInvoiceMethod(_id,invoice) {
      const elevatedUpdateInvoice = auth.elevate(invoices.updateInvoice);
      const response = await elevatedUpdateInvoice(_id,invoice);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
