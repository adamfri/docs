---
title: "Count Ticket Definitions"
description: "Counts ticket definitions by the saleStatus field, including those with the hidden status."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Counts ticket definitions by the `saleStatus` field, including those with the `hidden` status.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-TICKET-DEF">Manage Ticket Definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v3/ticket-definitions/count
    ```

    ## Body Params

    <ParamField body="filter" type="object">

      Filter object in the following format: &lt;br/> `"filter" : { "fieldName1": "value1" }`.

    </ParamField>

    <ParamField body="facet" type="string[]">

      Parameters to count ticket definitions by.

      - Max: 20 facets.

      Constraints: `maxLength 100`, `maxItems 20`.

    </ParamField>

    ## Response Object

    <ResponseField name="metadata" type="PagingMetadataV2">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="facets" type="object">

      Filter facets.

      Constraints: `format map`.

      <Expandable title="facets properties">

        <ResponseField name="key" type="string" />

        <ResponseField name="value" type="FacetCounts">

          <Expandable title="value properties">

            <ResponseField name="counts" type="object">

              Facet counts aggregated per value

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `FILTER_PARSER_ERROR` | The `filter` contains invalid syntax or unsupported fields. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v3/ticket-definitions/count' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
          "filter": {
            "saleStatus": {
              "$eq": "SALE_STARTED"
            }
          },
          "facet": ["saleStatus"]
      }'
      ```

      ```json Response
      {
        "metadata": {
          "total": 4
        },
        "facets": {
          "saleStatus": {
            "counts": {
              "SALE_STARTED": 4
            }
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Counts ticket definitions by the `saleStatus` field, including those with the `hidden` status.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-TICKET-DEF">Manage Ticket Definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { ticketDefinitionsV2 } from '@wix/events';

    ticketDefinitionsV2.countTicketDefinitions(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="CountTicketDefinitionsOptions">

      Optional fields.

      <Expandable title="options properties">

        <ParamField body="filter" type="object">

          Filter object in the following format: &lt;br/> `"filter" : { "fieldName1": "value1" }`.

        </ParamField>

        <ParamField body="facet" type="string[]">

          Parameters to count ticket definitions by.

          - Max: 20 facets.

          Constraints: `maxLength 100`, `maxItems 20`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="metadata" type="PagingMetadataV2">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="facets" type="object">

      Filter facets.

      Constraints: `format map`.

      <Expandable title="facets properties">

        <ResponseField name="key" type="string" />

        <ResponseField name="value" type="FacetCounts">

          <Expandable title="value properties">

            <ResponseField name="counts" type="object">

              Facet counts aggregated per value

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `FILTER_PARSER_ERROR` | The `filter` contains invalid syntax or unsupported fields. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-count-ticket-definitions" id="ex-sdk-count-ticket-definitions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-count-ticket-definitions" id="ex-sdk-count-ticket-definitions-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-count-ticket-definitions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-count-ticket-definitions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Count ticket definitions for an event</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">countTicketDefinitions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-count-ticket-definitions-0"><span className="wix-picker-name">Count ticket definitions for an event</span></label>
    <label htmlFor="ex-sdk-count-ticket-definitions-1"><span className="wix-picker-name">countTicketDefinitions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Count ticket definitions for an event
    import { ticketDefinitionsV2 } from "@wix/events";

    const eventId = "201037a0-3ecc-4ee4-976b-a1c931c42a6b";

    async function countTicketDefinitions() {
      const response = await ticketDefinitionsV2.countTicketDefinitions({
        filter: { eventId },
      });
      return response;
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript countTicketDefinitions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { ticketDefinitionsV2 } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { ticketDefinitionsV2 },
      // Include the auth strategy and host as relevant
    });

    async function countTicketDefinitions(options) {
      const response = await myWixClient.ticketDefinitionsV2.countTicketDefinitions(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
