---
title: "Query Memberships"
description: "Retrieves a list of up to 100 members and their membership status, given the provided paging and filtering."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 members and their membership status, given the provided paging and filtering.
    Groups managers can see all memberships for any group.

    Supported fields for filtering:
    - `role`
    - `status`

    No supported fields for sorting.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/social-groups-proxy/members/v2/members/{memberId}/memberships/query
    ```

    ## Path Params

    <ParamField path="memberId" type="string" required>

      Member ID. See the Members API for details.

    </ParamField>

    ## Body Params

    <ParamField body="query" type="Query">

      query

      <Expandable title="query properties">

        <ParamField body="filter" type="any">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paging" type="Paging">

          Paging options to limit and skip the number of items.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="memberships" type="Membership[]">

      Site member's memberships.

      <Expandable title="memberships properties">

        <ResponseField name="groupId" type="string">

          Group ID.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="status" type="string">

          Membership status with group.

          **Supported values:**

          - `UNKNOWN_STATUS` — Undefined membership status.
          - `JOINED` — Site member is a group member.
          - `PENDING` — Site member has submitted a request to join this group. The request is still `PENDING`.

        </ResponseField>

        <ResponseField name="role" type="GroupRole">

          Group member role. When membership status is not `JOINED`, this is empty.

          <Expandable title="role properties">

            <ResponseField name="value" type="string">

              Member's role.

              **Supported values:**

              - `MEMBER` — Regular group member.
              - `ADMIN` — Group administrator.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging information.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
          https://wixapis.com/social-groups/v2/members/223c37e0-3474-43c5-8d47-e374b414ef68/memberships/query \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
          -d '{
                  "query": {
                      "filter": {
                          "status": {
                              "$eq": "JOINED"
                          }
                      },
                      "paging": {
                          "offset": 0,
                          "limit": 100
                      }
                  }
          }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "memberships": [
              {
                  "groupId": "8a258cf5-49f3-41dc-b320-65ec2598315b",
                  "status": "JOINED",
                  "role": {
                      "value": "ADMIN"
                  }
              },
              {
                  "groupId": "3e3b08b6-9fdf-48b9-b2c3-762d6953fa0b",
                  "status": "JOINED",
                  "role": {
                      "value": "MEMBER"
                  }
              }
          ],
          "metadata": {
              "count": 2,
              "offset": 0,
              "total": 2
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 members and their membership status, given the provided paging and filtering.
    Groups managers can see all memberships for any group.

    Supported fields for filtering:
    - `role`
    - `status`

    No supported fields for sorting.

    ## Method Declaration

    ```typescript
    import { members } from '@wix/groups';

    members.queryMemberships(memberId, options): Promise<QueryMembershipsResponse>
    ```

    ## Method Parameters

    <ParamField body="memberId" type="string" required>

      Site member ID.

    </ParamField>

    <ParamField body="options" type="QueryMembershipsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="Query">

          query

          <Expandable title="query properties">

            <ParamField body="filter" type="any">

              Filter object in the following format:
              `"filter" : {`
              "fieldName1": "value1",
              "fieldName2":&#123;"$operator":"value2"&#125;
              &#125;``
              Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object in the following format:
              `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="fields" type="string[]">

              Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

            <ParamField body="fieldsets" type="string[]">

              Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<QueryMembershipsResponse>`.

    <ResponseField name="memberships" type="Membership[]">

      Site member's memberships.

      <Expandable title="memberships properties">

        <ResponseField name="groupId" type="string">

          Group ID.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="status" type="string">

          Membership status with group.

          **Supported values:**

          - `UNKNOWN_STATUS` — Undefined membership status.
          - `JOINED` — Site member is a group member.
          - `PENDING` — Site member has submitted a request to join this group. The request is still `PENDING`.

        </ResponseField>

        <ResponseField name="role" type="GroupRole">

          Group member role. When membership status is not `JOINED`, this is empty.

          <Expandable title="role properties">

            <ResponseField name="value" type="string">

              Member's role.

              **Supported values:**

              - `MEMBER` — Regular group member.
              - `ADMIN` — Group administrator.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Paging information.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-memberships" id="ex-sdk-query-memberships-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-memberships" id="ex-sdk-query-memberships-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-memberships" id="ex-sdk-query-memberships-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-memberships-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-memberships-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryMemberships</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryMemberships (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryMemberships (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-memberships-0"><span className="wix-picker-name">queryMemberships</span></label>
    <label htmlFor="ex-sdk-query-memberships-1"><span className="wix-picker-name">queryMemberships (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-memberships-2"><span className="wix-picker-name">queryMemberships (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryMemberships
    import { members } from '@wix/groups';

    async function queryMemberships(memberId,options) {
      const response = await members.queryMemberships(memberId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryMemberships (self-hosted)
    import { createClient } from '@wix/sdk';
    import { members } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { members },
      // Include the auth strategy and host as relevant
    });

    async function queryMemberships(memberId,options) {
      const response = await myWixClient.members.queryMemberships(memberId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryMemberships (with elevated permissions)
    import { members } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myQueryMembershipsMethod(memberId,options) {
      const elevatedQueryMemberships = auth.elevate(members.queryMemberships);
      const response = await elevatedQueryMemberships(memberId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
