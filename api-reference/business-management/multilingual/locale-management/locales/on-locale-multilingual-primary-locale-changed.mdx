---
title: "onLocaleMultilingualPrimaryLocaleChanged"
description: "Triggered when a new primary locale is created."
icon: "bolt"
---

Triggered when a new primary locale is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.locales.v2.locale_multilingual_primary_locale_changed` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API` |

## Examples

<CodeGroup>

  ```javascript onLocaleMultilingualPrimaryLocaleChanged
  import { locales } from '@wix/multilingual';

  locales.onLocaleMultilingualPrimaryLocaleChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocaleMultilingualPrimaryLocaleChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { locales } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        locales,
      },
  });

  wixClient.locales.onLocaleMultilingualPrimaryLocaleChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
