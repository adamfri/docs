---
title: "onLocationUpdated"
description: "The onLocationUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.locations.v1.location_updated` |
| Package | `@wix/business-tools` |
| Permissions | `Manage Locations` |

## Examples

<CodeGroup>

  ```javascript onLocationUpdated: Wix-hosted
  import { locations } from '@wix/business-tools';

  locations.onLocationUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocationUpdated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { locations } from '@wix/business-tools';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        locations,
      },
  });

  wixClient.locations.onLocationUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
