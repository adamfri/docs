---
title: "Calculate First Available Time Slots Per Menu"
description: "Retrieves the first available time slot of each fulfillment type for each specified menu."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the first available time slot of each fulfillment type for each specified menu.

    The response includes time slots with delivery fulfillment types only if you specify a delivery address.
    Only fulfillment methods capable of delivering to the specified address are considered when calculating time slot availability.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/restaurants/operations/v1/operations/{operationId}/first-available-time-slots-per-menus
    ```

    ## Path Params

    <ParamField path="operationId" type="string" required>

      Operation ID.

    </ParamField>

    ## Query Params

    <ParamField query="deliveryAddress" type="Address">

      Delivery address.

      The response includes time slots with delivery fulfillment types only if you specify a delivery address.

      <Expandable title="deliveryAddress properties">

        <ParamField query="country" type="string">

          Country code.

        </ParamField>

        <ParamField query="subdivision" type="string">

          Subdivision. Usually a state, region, prefecture, or province code, according to [ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2).

        </ParamField>

        <ParamField query="city" type="string">

          City name.

        </ParamField>

        <ParamField query="postalCode" type="string">

          Zip/postal code.

        </ParamField>

        <ParamField query="addressLine2" type="string">

          Free text providing more detailed address info. Usually contains Apt, Suite, and Floor.

        </ParamField>

        <ParamField query="formattedAddress" type="string">

          A string containing the full address of this location.

        </ParamField>

        <ParamField query="hint" type="string">

          Free text to help find the address.

        </ParamField>

        <ParamField query="geocode" type="AddressLocation">

          Coordinates of the physical address.

          <Expandable title="geocode properties">

            <ParamField query="latitude" type="number">

              Address latitude.

            </ParamField>

            <ParamField query="longitude" type="number">

              Address longitude.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField query="countryFullname" type="string">

          Country full name.

        </ParamField>

        <ParamField query="subdivisionFullname" type="string">

          Subdivision full name.

        </ParamField>

        <ParamField query="subdivisions" type="Subdivision[]">

          Multi-level subdivisions from top to bottom.

          Constraints: `maxItems 6`.

          <Expandable title="subdivisions properties">

            <ParamField query="code" type="string">

              Short subdivision code.

            </ParamField>

            <ParamField query="name" type="string">

              Subdivision full name.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField query="street" type="street">

          street

          <Expandable title="street properties">

            <ParamField query="streetAddress" type="StreetAddress">

              Street name and number.

              <Expandable title="streetAddress properties">

                <ParamField query="number" type="string">

                  Street number.

                </ParamField>

                <ParamField query="name" type="string">

                  Street name.

                </ParamField>

                <ParamField query="apt" type="string">

                  Apartment number.

                </ParamField>

                <ParamField query="formattedAddressLine" type="string">

                  Optional address line 1

                </ParamField>

              </Expandable>

            </ParamField>

            <ParamField query="addressLine" type="string">

              Main address line, usually street and number as free text.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="cursorPaging" type="CursorPaging">

      Cursor paging.

      <Expandable title="cursorPaging properties">

        <ParamField query="limit" type="integer">

          Maximum number of items to return in the results.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="onlyOnlineOrderingEnabled" type="boolean">

      Only retrieve time slots for menus with online ordering enabled.

    </ParamField>

    ## Response Object

    <ResponseField name="timeSlotsPerMenu" type="FulfillmentTimeSlotsPerMenu[]">

      First available time slot of each fulfillment type for each menu.

      A time slot with the delivery fulfillment type is returned only if a delivery address was specified.

      Constraints: `maxItems 100`.

      <Expandable title="timeSlotsPerMenu properties">

        <ResponseField name="menuId" type="string">

          Menu ID.

        </ResponseField>

        <ResponseField name="timeslotsPerFulfillmentType" type="TimeSlotForFulfillment[]">

          Available time slots of each fulfillment type for this menu.

          Constraints: `maxItems 2`.

          <Expandable title="timeslotsPerFulfillmentType properties">

            <ResponseField name="timeSlot" type="TimeSlot">

              Time Slot details.

              _Nested structure omitted for brevity — see the `TimeSlot` object._

            </ResponseField>

            <ResponseField name="fulfillmentInfo" type="FulfillmentInfo[]">

              Details for each available fulfillment option in the time slot.

              Constraints: `maxItems 500`.

              _Nested structure omitted for brevity — see the `FulfillmentInfo[]` object._

            </ResponseField>

            <ResponseField name="fulfilmentType" type="string">

              Fulfillment type.

              **Supported values:**

              - `PICKUP` — Pickup fulfillment. The customer picks up the order from the restaurant.
              - `DELIVERY` — Delivery fulfillment. The restaurant, or a representative, delivers the order to the customer.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadata" deprecated>

      Metadata for the paginated results.

      **Deprecated.** Metadata for the paginated results. Removal: 2025-08-31.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="cursorPagingMetadata" type="CursorPagingMetadata">

      Metadata for the paginated results.

      <Expandable title="cursorPagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET 'https://www.wixapis.com/restaurants-operations/v1/operations/48605ac5-53fc-49b4-96b2-87f7636f5ce0/first-available-time-slots-per-menus?' \
          'cursor_paging.limit=10' \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "timeSlotsPerMenu": [
          {
            "menuId": "bbb65621-6338-4837-8fdd-aebc654523e0",
            "timeslotsPerFulfillmentType": [
              {
                "timeSlot": {
                  "startTime": "2025-12-20T00:05:15Z",
                  "endTime": "2025-12-20T00:25:35Z",
                  "orderSchedulingType": "ASAP"
                },
                "fulfillmentInfo": [
                  {
                    "fee": "2.5",
                    "minOrderPrice": "10.5",
                    "maxTime": 30,
                    "address": {
                      "country": "US",
                      "city": "San Francisco",
                      "postalCode": "94158",
                      "subdivision": "CA"
                    }
                  }
                ],
                "fulfilmentType": "PICKUP"
              },
              {
                "timeSlot": {
                  "startTime": "2025-12-20T00:10:15Z",
                  "endTime": "2025-12-20T00:30:35Z",
                  "orderSchedulingType": "ASAP"
                },
                "fulfillmentInfo": [
                  {
                    "fee": "3.0",
                    "minOrderPrice": "15.0",
                    "durationRange": {
                      "minDuration": 31,
                      "maxDuration": 45
                    }
                  }
                ],
                "fulfilmentType": "DELIVERY"
              }
            ]
          },
          {
            "menuId": "ccc75632-7449-4948-9fee-bfcd765634f1",
            "timeslotsPerFulfillmentType": [
              {
                "timeSlot": {
                  "startTime": "2025-12-20T00:15:00Z",
                  "endTime": "2025-12-20T00:35:00Z",
                  "orderSchedulingType": "ASAP"
                },
                "fulfillmentInfo": [
                  {
                    "fee": "2.0",
                    "minOrderPrice": "12.0",
                    "maxTime": 25,
                    "address": {
                      "country": "US",
                      "city": "San Francisco",
                      "postalCode": "94158",
                      "subdivision": "CA"
                    }
                  }
                ],
                "fulfilmentType": "PICKUP"
              },
              {
                "timeSlot": {
                  "startTime": "2025-12-20T00:20:00Z",
                  "endTime": "2025-12-20T00:40:00Z",
                  "orderSchedulingType": "ASAP"
                },
                "fulfillmentInfo": [
                  {
                    "fee": "3.5",
                    "minOrderPrice": "18.0",
                    "durationRange": {
                      "minDuration": 35,
                      "maxDuration": 50
                    }
                  }
                ],
                "fulfilmentType": "DELIVERY"
              }
            ]
          },
          {
            "menuId": "ddd86743-8550-4a59-a0ff-c0de876745a2",
            "timeslotsPerFulfillmentType": [
              {
                "timeSlot": {
                  "startTime": "2025-12-20T00:25:00Z",
                  "endTime": "2025-12-20T00:45:00Z",
                  "orderSchedulingType": "PREORDER"
                },
                "fulfillmentInfo": [
                  {
                    "fee": "1.5",
                    "minOrderPrice": "8.0",
                    "maxTime": 20,
                    "address": {
                      "country": "US",
                      "city": "San Francisco",
                      "postalCode": "94158",
                      "subdivision": "CA"
                    }
                  }
                ],
                "fulfilmentType": "PICKUP"
              }
            ]
          }
        ],
        "cursorPagingMetadata": {
          "count": 3,
          "cursors": {
            "next": "eyJpZCI6Im5leHRfY3Vyc29yIn0="
          },
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the first available time slot of each fulfillment type for each specified menu.

    The response includes time slots with delivery fulfillment types only if you specify a delivery address.
    Only fulfillment methods capable of delivering to the specified address are considered when calculating time slot availability.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { operations } from '@wix/restaurants';

    operations.calculateFirstAvailableTimeSlotsPerMenu(operationId, options): Promise<CalculateFirstAvailableTimeSlotsPerMenuResponse>
    ```

    ## Method Parameters

    <ParamField body="operationId" type="string" required>

      Operation ID.

    </ParamField>

    <ParamField body="options" type="CalculateFirstAvailableTimeSlotsPerMenuOptions">

      options

      <Expandable title="options properties">

        <ParamField body="deliveryAddress" type="Address">

          Delivery address.

          The response includes time slots with delivery fulfillment types only if you specify a delivery address.

          <Expandable title="deliveryAddress properties">

            <ParamField body="country" type="string">

              Country code.

            </ParamField>

            <ParamField body="subdivision" type="string">

              Subdivision. Usually a state, region, prefecture, or province code, according to [ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2).

            </ParamField>

            <ParamField body="city" type="string">

              City name.

            </ParamField>

            <ParamField body="postalCode" type="string">

              Zip/postal code.

            </ParamField>

            <ParamField body="addressLine2" type="string">

              Free text providing more detailed address info. Usually contains Apt, Suite, and Floor.

            </ParamField>

            <ParamField body="formattedAddress" type="string">

              A string containing the full address of this location.

            </ParamField>

            <ParamField body="hint" type="string">

              Free text to help find the address.

            </ParamField>

            <ParamField body="geocode" type="AddressLocation">

              Coordinates of the physical address.

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

            <ParamField body="countryFullname" type="string">

              Country full name.

            </ParamField>

            <ParamField body="subdivisionFullname" type="string">

              Subdivision full name.

            </ParamField>

            <ParamField body="subdivisions" type="Subdivision[]">

              Multi-level subdivisions from top to bottom.

              Constraints: `maxItems 6`.

              _Nested structure omitted for brevity — see the `Subdivision[]` object._

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="street" type="street">

              street

              _Nested structure omitted for brevity — see the `street` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="cursorPaging" type="CursorPaging">

          Cursor paging.

          <Expandable title="cursorPaging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return in the results.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="onlyOnlineOrderingEnabled" type="boolean">

          Only retrieve time slots for menus with online ordering enabled.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CalculateFirstAvailableTimeSlotsPerMenuResponse>`.

    <ResponseField name="timeSlotsPerMenu" type="FulfillmentTimeSlotsPerMenu[]">

      First available time slot of each fulfillment type for each menu.

      A time slot with the delivery fulfillment type is returned only if a delivery address was specified.

      Constraints: `maxItems 100`.

      <Expandable title="timeSlotsPerMenu properties">

        <ResponseField name="menuId" type="string">

          Menu ID.

        </ResponseField>

        <ResponseField name="timeslotsPerFulfillmentType" type="TimeSlotForFulfillment[]">

          Available time slots of each fulfillment type for this menu.

          Constraints: `maxItems 2`.

          <Expandable title="timeslotsPerFulfillmentType properties">

            <ResponseField name="timeSlot" type="TimeSlot">

              Time Slot details.

              _Nested structure omitted for brevity — see the `TimeSlot` object._

            </ResponseField>

            <ResponseField name="fulfillmentInfo" type="FulfillmentInfo[]">

              Details for each available fulfillment option in the time slot.

              Constraints: `maxItems 500`.

              _Nested structure omitted for brevity — see the `FulfillmentInfo[]` object._

            </ResponseField>

            <ResponseField name="fulfilmentType" type="string">

              Fulfillment type.

              **Supported values:**

              - `PICKUP` — Pickup fulfillment. The customer picks up the order from the restaurant.
              - `DELIVERY` — Delivery fulfillment. The restaurant, or a representative, delivers the order to the customer.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadata" deprecated>

      Metadata for the paginated results.

      **Deprecated.** Metadata for the paginated results. Removal: 2025-08-31.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="cursorPagingMetadata" type="CursorPagingMetadata">

      Metadata for the paginated results.

      <Expandable title="cursorPagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-calculate-first-available-time-slots-per-menu" id="ex-sdk-calculate-first-available-time-slots-per-menu-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-calculate-first-available-time-slots-per-menu" id="ex-sdk-calculate-first-available-time-slots-per-menu-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-calculate-first-available-time-slots-per-menu-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-calculate-first-available-time-slots-per-menu-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">calculateFirstAvailableTimeSlotsPerMenu</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">calculateFirstAvailableTimeSlotsPerMenu (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-calculate-first-available-time-slots-per-menu-0"><span className="wix-picker-name">calculateFirstAvailableTimeSlotsPerMenu</span></label>
    <label htmlFor="ex-sdk-calculate-first-available-time-slots-per-menu-1"><span className="wix-picker-name">calculateFirstAvailableTimeSlotsPerMenu (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript calculateFirstAvailableTimeSlotsPerMenu
    import { operations } from '@wix/restaurants';

    async function calculateFirstAvailableTimeSlotsPerMenu(operationId,options) {
      const response = await operations.calculateFirstAvailableTimeSlotsPerMenu(operationId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript calculateFirstAvailableTimeSlotsPerMenu (self-hosted)
    import { createClient } from '@wix/sdk';
    import { operations } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { operations },
      // Include the auth strategy and host as relevant
    });

    async function calculateFirstAvailableTimeSlotsPerMenu(operationId,options) {
      const response = await myWixClient.operations.calculateFirstAvailableTimeSlotsPerMenu(operationId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
