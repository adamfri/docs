---
title: "onLocaleSettingsMultilingualModeUpdated"
description: "Triggered when the site's multilingual mode is updated."
icon: "bolt"
---

Triggered when the site's multilingual mode is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.settings.v2.locale_settings_multilingual_mode_updated` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API` |

## Examples

<CodeGroup>

  ```javascript onLocaleSettingsMultilingualModeUpdated
  import { localeSettings } from '@wix/multilingual';

  localeSettings.onLocaleSettingsMultilingualModeUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocaleSettingsMultilingualModeUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { localeSettings } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        localeSettings,
      },
  });

  wixClient.localeSettings.onLocaleSettingsMultilingualModeUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
