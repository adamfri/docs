---
title: "onCampaignDistributedEvent"
description: "Triggered when a campaign is successfully distributed to all recipients."
icon: "bolt"
---

Triggered when a campaign is successfully distributed to all recipients.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.email_marketing.v1.campaign_campaign_distributed_event` |
| Package | `@wix/email-marketing` |
| Permissions | `Manage Stores`, `Access Verticals by Automations`, `Set Up Automations`, `Read Email Marketing`, `Manage Email Marketing` |

## Examples

<CodeGroup>

  ```javascript onCampaignDistributedEvent
  import { campaigns } from '@wix/email-marketing';

  campaigns.onCampaignDistributedEvent((event) => {
      // handle your event here
  });
  ```

  ```javascript onCampaignDistributedEvent (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { campaigns } from '@wix/email-marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        campaigns,
      },
  });

  wixClient.campaigns.onCampaignDistributedEvent((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
