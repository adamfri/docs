---
title: "Delete Member About"
description: "Deletes the \"About\" section content for a member."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the "About" section content for a member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-ABOUT">SCOPE.DC-MEMBERS.MANAGE-ABOUT</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/members/v2/abouts/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the "About" section content to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteMemberAboutResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Member About Deleted | `wix.members.about.v2.member_about_deleted` | Triggered when a member's "About" section content is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        https://www.wixapis.com/members/v2/abouts/e901b292-70f0-4a20-8aaf-e06cd5dc5ca2 \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the "About" section content for a member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-ABOUT">SCOPE.DC-MEMBERS.MANAGE-ABOUT</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Method Declaration

    ```typescript
    import { membersAbout } from '@wix/members';

    membersAbout.deleteMemberAbout(_id): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the "About" section content to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteMemberAboutResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Member About Deleted | `wix.members.about.v2.member_about_deleted` | Triggered when a member's "About" section content is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-member-about" id="ex-sdk-delete-member-about-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-member-about" id="ex-sdk-delete-member-about-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-member-about-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-member-about-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteMemberAbout</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMemberAbout (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-member-about-0"><span className="wix-picker-name">deleteMemberAbout</span></label>
    <label htmlFor="ex-sdk-delete-member-about-1"><span className="wix-picker-name">deleteMemberAbout (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteMemberAbout
    import { membersAbout } from '@wix/members';

    async function deleteMemberAbout(_id) {
      const response = await membersAbout.deleteMemberAbout(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMemberAbout (self-hosted)
    import { createClient } from '@wix/sdk';
    import { membersAbout } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { membersAbout },
      // Include the auth strategy and host as relevant
    });

    async function deleteMemberAbout(_id) {
      const response = await myWixClient.membersAbout.deleteMemberAbout(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
