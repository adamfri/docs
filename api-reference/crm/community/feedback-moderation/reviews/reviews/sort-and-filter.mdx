---
title: "Sort and Filter"
description: "The following table shows field support for filters and sorting for review properties: | Fields | Supported Filters | Sortable | | ----------- | ----------- |…"
icon: "file-lines"
---

<Note>This article applies to the REST API only.</Note>

The following table shows field support for filters and sorting for review properties:

| Fields      | Supported Filters | Sortable |
| ----------- | ----------- | ----------- |
| `author.contactId` | `$eq`, `$ne`, `$in` |          |
| `content.rating` | `$eq`, `$ne`, `$in`, `$lt`, `$lte`, `$gt`, `$gte` | Sortable |
| `content.media` | `$isEmpty` |          |
| `createdDate` | `$eq`, `$ne`, `$in`, `$lt`, `$lte`, `$gt`, `$gte` | Sortable |
| `entityId` | `$eq`, `$ne`, `$in` |          |
| `helpfulness` | `$eq`, `$ne`, `$in`, `$lt`, `$lte`, `$gt`, `$gte` | Sortable |
| `id` | `$eq`, `$ne`, `$in` |          |
| `moderation.moderationStatus` | `$eq`, `$ne`, `$in` |          |
| `namespace` | `$eq`, `$ne`, `$in` |          |
| `origin.type` | `$eq`, `$ne` | Sortable |
| `relevanceScore` | `$eq`, `$ne`, `$in`, `$lt`, `$lte`, `$gt`, `$gte` | Sortable |
| `verified` | `$eq`, `$ne` | Sortable |
