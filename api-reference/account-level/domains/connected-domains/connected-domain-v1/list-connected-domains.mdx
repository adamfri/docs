---
title: "List Connected Domains"
description: "Retrieves a list of up to 100 connected domains."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 connected domains.

    You must pass the relevant Wix account ID in the header of the call.

    The retrieved domains are sorted by `createdDate` in descending order.

    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.READ_CONNECTED_DOMAINS">DOMAINS.READ_CONNECTED_DOMAINS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    GET https://www.wixapis.com/domains/v1/connected-domains
    ```

    ## Query Params

    <ParamField query="paging" type="CursorPaging">

      Cursor paging options.

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of domains to load.

          Min: `0`
          Max: `100`

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          You can get the relevant cursor token
          from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Metadata about the returned list of connected domains.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of domains returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="connectedDomains" type="ConnectedDomain[]">

      Retrieved connected domains.

      See the [Connected Domain Object](/api-reference/account-level/domains/connected-domains/connected-domain-v1/connected-domain-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET 'https://www.wixapis.com/domains/v1/connected-domains' \
      -H 'wix-account-id: <ACCOUNT_ID>' \
      -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {
        "pagingMetadata":{
          "count":1,
          "cursors":{
            "next":null,
            "prev":null
          },
          "hasNext":false
        },
        "connectedDomains":[
          {
            "id":"mysite.com",
            "domain":"mysite.com",
            "connectionType":"NAMESERVERS",
            "siteInfo":{
              "id":"<SITE_ID>",
              "assignmentType":"PRIMARY"
            },
            "suppressNotifications":false,
            "dnsPropagationStatus":"UNKNOWN_DNS_PROPAGATION_STATUS"
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 connected domains.

    You must pass the relevant Wix account ID in the header of the call.

    The retrieved domains are sorted by `createdDate` in descending order.

    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.READ_CONNECTED_DOMAINS">DOMAINS.READ_CONNECTED_DOMAINS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { connectedDomains } from '@wix/domains';

    connectedDomains.listConnectedDomains(options): Promise<ListConnectedDomainsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListConnectedDomainsOptions">

      Filter options.

      <Expandable title="options properties">

        <ParamField body="paging" type="CursorPaging">

          Cursor paging options.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of domains to load.

              Min: `0`
              Max: `100`

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              You can get the relevant cursor token
              from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListConnectedDomainsResponse>`.

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Metadata about the returned list of connected domains.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of domains returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="connectedDomains" type="ConnectedDomain[]">

      Retrieved connected domains.

      See the [Connected Domain Object](/api-reference/account-level/domains/connected-domains/connected-domain-v1/connected-domain-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-connected-domains" id="ex-sdk-list-connected-domains-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-connected-domains" id="ex-sdk-list-connected-domains-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-connected-domains-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-connected-domains-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List connected domains with an API key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listConnectedDomains (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-connected-domains-0"><span className="wix-picker-name">List connected domains with an API key</span></label>
    <label htmlFor="ex-sdk-list-connected-domains-1"><span className="wix-picker-name">listConnectedDomains (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List connected domains with an API key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { connectedDomains } from "@wix/domains";

    const wixClient = createClient({
      modules: { connectedDomains },
      auth: ApiKeyStrategy({
        siteId: "MY-SITE-ID",
        apiKey: "MY-API-KEY",
      }),
    });

    async function listConnectedDomains(options) {
      const response =
        await wixClient.connectedDomains.listConnectedDomains(options);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listConnectedDomains (self-hosted)
    import { createClient } from '@wix/sdk';
    import { connectedDomains } from '@wix/domains';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { connectedDomains },
      // Include the auth strategy and host as relevant
    });

    async function listConnectedDomains(options) {
      const response = await myWixClient.connectedDomains.listConnectedDomains(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
