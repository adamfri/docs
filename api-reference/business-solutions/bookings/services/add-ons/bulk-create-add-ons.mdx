---
title: "Bulk Create Add Ons"
description: "Creates multiple add-ons in a single request."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates multiple add-ons in a single request.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/addons/v1/bulk/add-ons/create
    ```

    ## Body Params

    <ParamField body="addOns" type="AddOn[]" required>

      List of add-ons to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the created add-ons in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkAddOnResult[]">

      Results of the bulk create operation including add-ons and metadata.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the individual create operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="AddOn">

          Created add-on. Only present if `return_entity` was set to true in the request.

          See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the bulk create operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Add On Created | `wix.addons.v1.add_on_created` | Triggered when an add-on is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/addons/v1/bulk/add-ons/create' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "addOns": [
            {
              "name": "Yoga Mat Rental",
              "price": {
                "value": "800",
                "currency": "USD",
                "formattedValue": "$8.00"
              },
              "maxQuantity": 1,
              "taxGroup": "12345678-1234-1234-1234-123456789012"
            },
            {
              "name": "Personal Training Add-on (30 minutes)",
              "price": {
                "value": "5000",
                "currency": "USD",
                "formattedValue": "$50.00"
              },
              "durationInMinutes": 30,
              "taxGroup": "12345678-1234-1234-1234-123456789012"
            }
          ],
          "returnEntity": true
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
              "revision": "1",
              "createdDate": "2019-10-30T17:22:10.299Z",
              "updatedDate": "2019-10-30T17:22:10.299Z",
              "name": "Yoga Mat Rental",
              "price": {
                "value": "800",
                "currency": "USD",
                "formattedValue": "$8.00"
              },
              "maxQuantity": 1,
              "taxGroup": "12345678-1234-1234-1234-123456789012"
            }
          },
          {
            "itemMetadata": {
              "id": "b2c3d4e5-f6a7-8901-bcde-f23456789012",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "id": "b2c3d4e5-f6a7-8901-bcde-f23456789012",
              "revision": "1",
              "createdDate": "2019-10-30T17:22:11.456Z",
              "updatedDate": "2019-10-30T17:22:11.456Z",
              "name": "Personal Training Add-on (30 minutes)",
              "price": {
                "value": "5000",
                "currency": "USD",
                "formattedValue": "$50.00"
              },
              "durationInMinutes": 30,
              "taxGroup": "12345678-1234-1234-1234-123456789012"
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates multiple add-ons in a single request.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { addOns } from '@wix/bookings';

    addOns.bulkCreateAddOns(addOns, options): Promise<BulkCreateAddOnsResponse>
    ```

    ## Method Parameters

    <ParamField body="addOns" type="AddOn[]" required>

      List of add-ons to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateAddOnsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return the created add-ons in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkCreateAddOnsResponse>`.

    <ResponseField name="results" type="BulkAddOnResult[]">

      Results of the bulk create operation including add-ons and metadata.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the individual create operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="AddOn">

          Created add-on. Only present if `return_entity` was set to true in the request.

          See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the bulk create operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Add On Created | `wix.addons.v1.add_on_created` | Triggered when an add-on is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-add-ons" id="ex-sdk-bulk-create-add-ons-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-add-ons" id="ex-sdk-bulk-create-add-ons-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-create-add-ons" id="ex-sdk-bulk-create-add-ons-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-add-ons-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-add-ons-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateAddOns</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateAddOns (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateAddOns (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-add-ons-0"><span className="wix-picker-name">bulkCreateAddOns</span></label>
    <label htmlFor="ex-sdk-bulk-create-add-ons-1"><span className="wix-picker-name">bulkCreateAddOns (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-create-add-ons-2"><span className="wix-picker-name">bulkCreateAddOns (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkCreateAddOns
    import { addOns } from '@wix/bookings';

    async function bulkCreateAddOns(addOns,options) {
      const response = await addOns.bulkCreateAddOns(addOns,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateAddOns (self-hosted)
    import { createClient } from '@wix/sdk';
    import { addOns } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { addOns },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateAddOns(addOns,options) {
      const response = await myWixClient.addOns.bulkCreateAddOns(addOns,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateAddOns (with elevated permissions)
    import { addOns } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myBulkCreateAddOnsMethod(addOns,options) {
      const elevatedBulkCreateAddOns = auth.elevate(addOns.bulkCreateAddOns);
      const response = await elevatedBulkCreateAddOns(addOns,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
