---
title: "List Manual Tax Mappings"
description: "Retrieves up to 1000 manual tax mappings."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 1000 manual tax mappings.
    The returned manual tax mappings are sorted by the `createdDate` field in ascending order.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/billing/v1/manual-tax-mappings
    ```

    ## Response Object

    <ResponseField name="manualTaxMappings" type="ManualTaxMapping[]">

      List of manual tax mappings.

      Constraints: `maxItems 1000`.

      See the [Manual Tax Mapping Object](/api-reference/business-solutions/e-commerce/extensions/tax/manual-tax-mappings/manual-tax-mapping-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Pagination metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Lists up to 1000 manual tax mappings, sorted by creation date in ascending order

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/billing/v1/manual-tax-mappings' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "manualTaxMappings": [
          {
            "id": "c3d4e5f6-a7b8-9c0d-1e2f-a3b4c5d6e7f8",
            "revision": "1",
            "createdDate": "2025-08-18T09:57:35.926Z",
            "updatedDate": "2025-08-18T09:57:35.926Z",
            "taxGroupId": "a1b2c3d4-e5f6-7a8b-9c0d-e1f2a3b4c5d6",
            "taxRegionId": "b2c3d4e5-f6a7-8b9c-0d1e-f2a3b4c5d6e7",
            "taxRate": "0.080000",
            "taxType": "Sales Tax",
            "taxName": "STATE TAX",
            "jurisdiction": "New York",
            "jurisdictionType": "STATE"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 1000 manual tax mappings.
    The returned manual tax mappings are sorted by the `createdDate` field in ascending order.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { manualTaxMappings } from '@wix/billing';

    manualTaxMappings.listManualTaxMappings(): Promise
    ```

    ## Returns

    <ResponseField name="manualTaxMappings" type="ManualTaxMapping[]">

      List of manual tax mappings.

      Constraints: `maxItems 1000`.

      See the [Manual Tax Mapping Object](/api-reference/business-solutions/e-commerce/extensions/tax/manual-tax-mappings/manual-tax-mapping-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Pagination metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-manual-tax-mappings" id="ex-sdk-list-manual-tax-mappings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-manual-tax-mappings" id="ex-sdk-list-manual-tax-mappings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-manual-tax-mappings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-manual-tax-mappings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List Manual Tax Mappings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listManualTaxMappings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-manual-tax-mappings-0"><span className="wix-picker-name">List Manual Tax Mappings</span></label>
    <label htmlFor="ex-sdk-list-manual-tax-mappings-1"><span className="wix-picker-name">listManualTaxMappings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List Manual Tax Mappings
    import { manualTaxMappings } from "@wix/billing";

    async function listManualTaxMappings() {
      const response = await manualTaxMappings.listManualTaxMappings();
      return response;
    }

    /* Promise resolves to:
     * {
     *   "manualTaxMappings": [
     *     {
     *       "_id": "c3d4e5f6-a7b8-9c0d-1e2f-a3b4c5d6e7f8",
     *       "taxGroupId": "a1b2c3d4-e5f6-7a8b-9c0d-e1f2a3b4c5d6",
     *       "taxRegionId": "b2c3d4e5-f6a7-8b9c-0d1e-f2a3b4c5d6e7",
     *       "taxRate": "0.080000",
     *       "taxType": "Sales Tax",
     *       "taxName": "STATE TAX",
     *       "jurisdiction": "New York",
     *       "jurisdictionType": "STATE"
     *     }
     *   ]
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listManualTaxMappings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { manualTaxMappings } from '@wix/billing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { manualTaxMappings },
      // Include the auth strategy and host as relevant
    });

    async function listManualTaxMappings() {
      const response = await myWixClient.manualTaxMappings.listManualTaxMappings();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
