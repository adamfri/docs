---
title: "Complete Project"
description: "Marks a project as completed, indicating the provider has delivered the engagement."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Marks a project as completed, indicating the provider has delivered the engagement.

    Each time the project is updated, `revision` increments by 1. The current `revision` must be passed when updating the project. This ensures you're working with the latest project and prevents unintended overwrites.

    Completing a project requires proof of delivery, so `completedProjectSnapshot` or `metasiteId`
    must either be specified in the request or already set on the project. You can also specify
    `serviceName` and `budget` to record which marketplace service the work is attributed to and
    what the client agreed to pay.

    A project whose `status` is `COMPLETED`, `EXPIRED`, `CANCELED_BY_CLIENT`, or
    `CANCELED_BY_PROVIDER` can't be completed, because those statuses are final.

    Once a project is completed, the client can review it, and the provider can respond by calling
    Reply to Project Review.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ARENA.MANAGE-LEADS">Manage CRM and Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/partners/marketplace/v1/projects/{projectId}/complete
    ```

    ## Path Params

    <ParamField path="projectId" type="string" required>

      ID of the project to complete.

    </ParamField>

    ## Body Params

    <ParamField body="revision" type="integer" required>

      Revision number, which increments by 1 each time the project is updated. To prevent conflicting
      changes, the current revision must be passed.

    </ParamField>

    <ParamField body="completedProjectSnapshot" type="string">

      URL of a visual snapshot of the delivered work, such as an uploaded thumbnail image or the URL of
      the delivered site.

      Required if the project doesn't already have a `completedProjectSnapshot` and `metasiteId` isn't
      specified.

    </ParamField>

    <ParamField body="metasiteId" type="string">

      ID of the site delivered for the project.

      Required if the project doesn't already have a `metasiteId` and `completedProjectSnapshot` isn't
      specified.

    </ParamField>

    <ParamField body="serviceName" type="string">

      Marketplace service the completed project is attributed to.

      Specify this field to set or change the project's `serviceName`. Omit it to leave the current
      value unchanged.

      **Supported values:**

      - `UNKNOWN_SERVICE_NAME` — No service, or an unrecognized service.
      - `DOMAIN` — Connect or set up a domain.
      - `ACCESSIBILITY` — Improve site accessibility.
      - `RESTAURANT_MENU_SETUP` — Set up a restaurant menu.
      - `STORE_OPTIMIZATION` — Optimize an online store.
      - `ADVANCED_WEBSITE` — Build an advanced website.
      - `CUSTOM_INPUT_FORMS` — Build custom input forms.
      - `SITE_GUIDANCE` — Guidance on building a site.
      - `ECOMMERCE_SITE` — Build an eCommerce site.
      - `ADS` — Set up and manage ads.
      - `PROFESSIONAL_TRANSLATION` — Professional content translation.
      - `BRAND_DEVELOPMENT` — Develop a brand.
      - `EMAIL` — Set up business email.
      - `CUSTOM_SITE_FUNCTIONALITIES` — Build custom site functionalities.
      - `THIRD_PARTY_SERVICES` — Integrate third-party services.
      - `VISUALS` — Create visual assets.
      - `CODING_GUIDANCE` — Guidance on custom code.
      - `EMAIL_MARKETING` — Set up email marketing.
      - `WRITTEN` — Written content.
      - `PHOTOS` — Photography.
      - `STORE_SETTINGS` — Configure store settings.
      - `ANALYTICS` — Set up analytics.
      - `CLASSIC_WEBSITE` — Build a classic website.
      - `PIXEL` — Set up a tracking pixel.
      - `REDESIGN_SITE` — Redesign an existing site.
      - `MOBILE_APP` — Build a mobile app.
      - `CONTENT_WITH_DATABASE` — Build content backed by a database.
      - `UPDATES` — Site updates and maintenance.
      - `INSTALL_APPS` — Install and set up apps.
      - `MOBILE_SITE` — Optimize the mobile site.
      - `LOGO` — Design a logo.
      - `CUSTOM_DESIGN` — Custom design work.
      - `ADVANCED_STORE` — Build an advanced store.
      - `SEO` — Search engine optimization.
      - `MOVE_SITE` — Move a site to Wix.
      - `EDIT_IMAGE` — Edit images.

    </ParamField>

    <ParamField body="budget" type="number">

      Budget the client agreed to pay for the engagement.

      Specify this field to set or change the project's `budget`. Omit it to leave the current value
      unchanged.

    </ParamField>

    ## Response Object

    <ResponseField name="project" type="Project">

      Updated project.

      See the [Project Object](/api-reference/account-level/studio-workspace/marketplace/project-v1/project-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Marks a project as completed with proof of delivery, the service, and the agreed budget

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/partners/marketplace/v1/projects/2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c/complete' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "revision": "3",
        "completedProjectSnapshot": "https://static.wixstatic.com/media/portfolio-site-thumbnail.jpg",
        "metasiteId": "8b1f4d3e-2c5a-4e7b-9d0f-6a3c8e1b2d4f",
        "serviceName": "ADVANCED_WEBSITE",
        "budget": 500.0
      }'
      ```

      ```json Response
      {
        "project": {
          "id": "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
          "revision": "4",
          "createdDate": "2024-01-15T10:30:00.000Z",
          "updatedDate": "2024-06-10T11:00:00.000Z",
          "serviceName": "ADVANCED_WEBSITE",
          "contactId": "5c6d7e8f-9a1b-4c2d-9e3f-4a5b6c7d8e9f",
          "status": "COMPLETED",
          "metasiteId": "8b1f4d3e-2c5a-4e7b-9d0f-6a3c8e1b2d4f",
          "completedProjectSnapshot": "https://static.wixstatic.com/media/portfolio-site-thumbnail.jpg",
          "statusTimeline": [
            {
              "status": "NEW",
              "date": "2024-01-15T10:30:00.000Z"
            },
            {
              "status": "CONTACTED",
              "date": "2024-01-16T08:20:00.000Z"
            },
            {
              "status": "IN_PROGRESS",
              "date": "2024-02-01T09:00:00.000Z"
            }
          ],
          "budget": 500.0,
          "expirationDate": "2024-01-19T10:30:00.000Z",
          "source": "MATCHING",
          "briefDescription": "Looking for a modern portfolio site with a blog and contact form."
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Marks a project as completed, indicating the provider has delivered the engagement.

    Each time the project is updated, `revision` increments by 1. The current `revision` must be passed when updating the project. This ensures you're working with the latest project and prevents unintended overwrites.

    Completing a project requires proof of delivery, so `completedProjectSnapshot` or `metasiteId`
    must either be specified in the request or already set on the project. You can also specify
    `serviceName` and `budget` to record which marketplace service the work is attributed to and
    what the client agreed to pay.

    A project whose `status` is `COMPLETED`, `EXPIRED`, `CANCELED_BY_CLIENT`, or
    `CANCELED_BY_PROVIDER` can't be completed, because those statuses are final.

    Once a project is completed, the client can review it, and the provider can respond by calling
    Reply to Project Review.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ARENA.MANAGE-LEADS">Manage CRM and Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketplaceProjects } from '@wix/marketplace-projects';

    marketplaceProjects.completeProject(projectId, revision, options): Promise
    ```

    ## Method Parameters

    <ParamField body="projectId" type="string" required>

      ID of the project to complete.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="revision" type="CompleteProject" required>

      Revision number, which increments by 1 each time the project is updated. To prevent conflicting
      changes, the current revision must be passed.

      <Expandable title="revision properties">

        <ParamField body="value" type="string" />

      </Expandable>

    </ParamField>

    <ParamField body="options" type="CompleteProjectOptions">

      options

      <Expandable title="options properties">

        <ParamField body="completedProjectSnapshot" type="string">

          URL of a visual snapshot of the delivered work, such as an uploaded thumbnail image or the URL of
          the delivered site.

          Required if the project doesn't already have a `completedProjectSnapshot` and `metasiteId` isn't
          specified.

        </ParamField>

        <ParamField body="metasiteId" type="string">

          ID of the site delivered for the project.

          Required if the project doesn't already have a `metasiteId` and `completedProjectSnapshot` isn't
          specified.

        </ParamField>

        <ParamField body="serviceName" type="string">

          Marketplace service the completed project is attributed to.

          Specify this field to set or change the project's `serviceName`. Omit it to leave the current
          value unchanged.

          **Supported values:**

          - `UNKNOWN_SERVICE_NAME` — No service, or an unrecognized service.
          - `DOMAIN` — Connect or set up a domain.
          - `ACCESSIBILITY` — Improve site accessibility.
          - `RESTAURANT_MENU_SETUP` — Set up a restaurant menu.
          - `STORE_OPTIMIZATION` — Optimize an online store.
          - `ADVANCED_WEBSITE` — Build an advanced website.
          - `CUSTOM_INPUT_FORMS` — Build custom input forms.
          - `SITE_GUIDANCE` — Guidance on building a site.
          - `ECOMMERCE_SITE` — Build an eCommerce site.
          - `ADS` — Set up and manage ads.
          - `PROFESSIONAL_TRANSLATION` — Professional content translation.
          - `BRAND_DEVELOPMENT` — Develop a brand.
          - `EMAIL` — Set up business email.
          - `CUSTOM_SITE_FUNCTIONALITIES` — Build custom site functionalities.
          - `THIRD_PARTY_SERVICES` — Integrate third-party services.
          - `VISUALS` — Create visual assets.
          - `CODING_GUIDANCE` — Guidance on custom code.
          - `EMAIL_MARKETING` — Set up email marketing.
          - `WRITTEN` — Written content.
          - `PHOTOS` — Photography.
          - `STORE_SETTINGS` — Configure store settings.
          - `ANALYTICS` — Set up analytics.
          - `CLASSIC_WEBSITE` — Build a classic website.
          - `PIXEL` — Set up a tracking pixel.
          - `REDESIGN_SITE` — Redesign an existing site.
          - `MOBILE_APP` — Build a mobile app.
          - `CONTENT_WITH_DATABASE` — Build content backed by a database.
          - `UPDATES` — Site updates and maintenance.
          - `INSTALL_APPS` — Install and set up apps.
          - `MOBILE_SITE` — Optimize the mobile site.
          - `LOGO` — Design a logo.
          - `CUSTOM_DESIGN` — Custom design work.
          - `ADVANCED_STORE` — Build an advanced store.
          - `SEO` — Search engine optimization.
          - `MOVE_SITE` — Move a site to Wix.
          - `EDIT_IMAGE` — Edit images.

        </ParamField>

        <ParamField body="budget" type="number">

          Budget the client agreed to pay for the engagement.

          Specify this field to set or change the project's `budget`. Omit it to leave the current value
          unchanged.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="project" type="Project">

      Updated project.

      See the [Project Object](/api-reference/account-level/studio-workspace/marketplace/project-v1/project-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Updated | `wix.partners.marketplace.v1.project_updated` | Triggered when a project is updated, including every status transition. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-complete-project" id="ex-sdk-complete-project-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-complete-project" id="ex-sdk-complete-project-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-complete-project-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-complete-project-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Complete a project</span><span className="wix-picker-desc">Marks a project as completed with proof of delivery, the service, and the agreed budget</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">completeProject (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-complete-project-0"><span className="wix-picker-name">Complete a project</span><span className="wix-picker-desc">Marks a project as completed with proof of delivery, the service, and the agreed budget</span></label>
    <label htmlFor="ex-sdk-complete-project-1"><span className="wix-picker-name">completeProject (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Complete a project
    import { marketplaceProjects } from "@wix/marketplace-projects";

    async function completeProject() {
      const response = await marketplaceProjects.completeProject(
        "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
        "3",
        {
          completedProjectSnapshot: "https://static.wixstatic.com/media/portfolio-site-thumbnail.jpg",
          metasiteId: "8b1f4d3e-2c5a-4e7b-9d0f-6a3c8e1b2d4f",
          serviceName: "ADVANCED_WEBSITE",
          budget: 500,
        },
      );
    }

    /* Promise resolves to:
     * {
     *   "project": {
     *     "_id": "2f7c9a1b-8d3e-4a5f-9c6b-1e0d2f3a4b5c",
     *     "revision": "4",
     *     "_createdDate": "2024-01-15T10:30:00.000Z",
     *     "_updatedDate": "2024-06-10T11:00:00.000Z",
     *     "serviceName": "ADVANCED_WEBSITE",
     *     "contactId": "5c6d7e8f-9a1b-4c2d-9e3f-4a5b6c7d8e9f",
     *     "status": "COMPLETED",
     *     "metasiteId": "8b1f4d3e-2c5a-4e7b-9d0f-6a3c8e1b2d4f",
     *     "completedProjectSnapshot": "https://static.wixstatic.com/media/portfolio-site-thumbnail.jpg",
     *     "statusTimeline": [
     *       { "status": "NEW", "date": "2024-01-15T10:30:00.000Z" },
     *       { "status": "CONTACTED", "date": "2024-01-16T08:20:00.000Z" }
     *     ],
     *     "budget": 500,
     *     "expirationDate": "2024-01-19T10:30:00.000Z",
     *     "source": "MATCHING",
     *     "briefDescription": "Looking for a modern portfolio site with a blog and contact form."
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript completeProject (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketplaceProjects } from '@wix/marketplace-projects';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketplaceProjects },
      // Include the auth strategy and host as relevant
    });

    async function completeProject(projectId,revision,options) {
      const response = await myWixClient.marketplaceProjects.completeProject(projectId,revision,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
