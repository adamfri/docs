---
title: "onLocationArchiveStatus"
description: "Triggered when a location is archived."
icon: "bolt"
---

Triggered when a location is archived.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.locations.v1.location_archive_status` |
| Package | `@wix/business-tools` |
| Permissions | `Manage Locations` |

## Examples

<CodeGroup>

  ```javascript onLocationArchiveStatus: Wix-hosted
  import { locations } from '@wix/business-tools';

  locations.onLocationArchiveStatus((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocationArchiveStatus: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { locations } from '@wix/business-tools';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        locations,
      },
  });

  wixClient.locations.onLocationArchiveStatus((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
