---
title: "Draft Object"
description: "The Draft object represents the information for blog draft posts."
icon: "cube"
---

The `Draft` object represents the information for blog draft posts.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Draft post ID.

      **Read-only.** Constraints: `maxLength 38`.

    </ResponseField>

    <ResponseField name="title" type="string">

      Draft post title.

      Constraints: `maxLength 200`.

    </ResponseField>

    <ResponseField name="excerpt" type="string">

      Draft post excerpt.

      If no excerpt has been manually set, an excerpt is automatically generated from the post's text.
      This can be retrieved using the `GENERATED_EXCERPT` fieldset.

    </ResponseField>

    <ResponseField name="featured" type="boolean">

      Whether the draft post is marked as featured.

    </ResponseField>

    <ResponseField name="categoryIds" type="string[]">

      Category IDs of the draft post.

      Constraints: `maxLength 38`, `maxItems 10`.

    </ResponseField>

    <ResponseField name="memberId" type="string">

      Draft post owner's member ID.

    </ResponseField>

    <ResponseField name="hashtags" type="string[]">

      Hashtags in the post.

      Constraints: `maxLength 100`, `maxItems 100`.

    </ResponseField>

    <ResponseField name="commentingEnabled" type="boolean">

      Whether commenting on the draft post is enabled.

    </ResponseField>

    <ResponseField name="minutesToRead" type="integer">

      Estimated reading time of the draft post (calculated automatically).

      **Read-only.**

    </ResponseField>

    <ResponseField name="heroImage" type="Image">

      Image placed at the top of the blog page.

      <Expandable title="heroImage properties">

        <ResponseField name="id" type="string">

          WixMedia image ID.

        </ResponseField>

        <ResponseField name="url" type="string">

          Image URL.

        </ResponseField>

        <ResponseField name="height" type="integer">

          Original image height.

          **Read-only.**

        </ResponseField>

        <ResponseField name="width" type="integer">

          Original image width.

          **Read-only.**

        </ResponseField>

        <ResponseField name="altText" type="string">

          Image alt text.

        </ResponseField>

        <ResponseField name="filename" type="string">

          Image filename.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="tagIds" type="string[]">

      Tag IDs the draft post is tagged with.

      Constraints: `maxLength 38`, `maxItems 30`.

    </ResponseField>

    <ResponseField name="relatedPostIds" type="string[]">

      IDs of posts related to this draft post.

      Constraints: `maxLength 38`, `maxItems 3`.

    </ResponseField>

    <ResponseField name="pricingPlanIds" type="string[]">

      Pricing plan IDs. Only relevant if a post is assigned to a specific pricing plan. See the Pricing Plans API for more info.

      Constraints: `format GUID`, `maxItems 100`.

    </ResponseField>

    <ResponseField name="translationId" type="string">

      ID of the draft post's translations.

      All translations of a single post share the same `translationId`.
      Available only if the [Multilingual](https://support.wix.com/en/article/wix-multilingual-an-overview) app is installed.

    </ResponseField>

    <ResponseField name="language" type="string">

      Language the draft post is written in.

      2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.

    </ResponseField>

    <ResponseField name="richContent" type="RichContent">

      Draft Post rich content.

      &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="image.gallery.video.audio.divider.linkButton.html.giphy.codeBlock.file.hashtag.lineSpacing.indent.link.textColor.textHighlight.heading.verticalEmbed.table.collapsibleList.poll" exampleid="72c23a25-524f-4f70-a260-4a2777b6f5d5">
      &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
      &lt;/widget>

      <Expandable title="richContent properties">

        <ResponseField name="nodes" type="Node[]">

          Node objects representing a rich content document.

          <Expandable title="nodes properties">

            <ResponseField name="type" type="string">

              Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

              **Supported values:**

              - `PARAGRAPH`
              - `TEXT`
              - `HEADING`
              - `BULLETED_LIST`
              - `ORDERED_LIST`
              - `LIST_ITEM`
              - `BLOCKQUOTE`
              - `CODE_BLOCK`
              - `VIDEO`
              - `DIVIDER`
              - `FILE`
              - `GALLERY`
              - `GIF`
              - `HTML`
              - `IMAGE`
              - `LINK_PREVIEW`
              - `MAP` — @deprecated
              - `POLL`
              - `APP_EMBED`
              - `BUTTON`
              - `COLLAPSIBLE_LIST`
              - `TABLE`
              - `EMBED`
              - `COLLAPSIBLE_ITEM`
              - `COLLAPSIBLE_ITEM_TITLE`
              - `COLLAPSIBLE_ITEM_BODY`
              - `TABLE_CELL`
              - `TABLE_ROW`
              - `EXTERNAL`
              - `AUDIO`
              - `CAPTION`
              - `LAYOUT`
              - `LAYOUT_CELL`
              - `SHAPE`
              - `CARD`
              - `TOC`
              - `SMART_BLOCK`
              - `SMART_BLOCK_CELL`
              - `CHECKBOX_LIST`

            </ResponseField>

            <ResponseField name="id" type="string">

              Node ID.

            </ResponseField>

            <ResponseField name="nodes" type="Node[]">

              A list of child nodes.

              _Recursive reference to `Node[]`._

            </ResponseField>

            <ResponseField name="style" type="NodeStyle">

              Padding and background color styling for the node.

              <Expandable title="style properties">

                <ResponseField name="paddingTop" type="string">

                  The top padding value in pixels.

                </ResponseField>

                <ResponseField name="paddingBottom" type="string">

                  The bottom padding value in pixels.

                </ResponseField>

                <ResponseField name="backgroundColor" type="string">

                  The background color as a hexadecimal value.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="metadata" type="Metadata">

          Object metadata.

          <Expandable title="metadata properties">

            <ResponseField name="version" type="integer">

              Schema version.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="createdTimestamp" type="datetime">

              When the object was created.

              **Read-only.**

            </ResponseField>

            <ResponseField name="updatedTimestamp" type="datetime">

              When the object was most recently updated.

            </ResponseField>

            <ResponseField name="id" type="string">

              Object ID.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="documentStyle" type="DocumentStyle">

          Global styling for header, paragraph, block quote, and code block nodes in the object.

          <Expandable title="documentStyle properties">

            <ResponseField name="headerOne" type="TextNodeStyle">

              Styling for H1 nodes.

              <Expandable title="headerOne properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerTwo" type="TextNodeStyle">

              Styling for H2 nodes.

              <Expandable title="headerTwo properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerThree" type="TextNodeStyle">

              Styling for H3 nodes.

              <Expandable title="headerThree properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFour" type="TextNodeStyle">

              Styling for H4 nodes.

              <Expandable title="headerFour properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFive" type="TextNodeStyle">

              Styling for H5 nodes.

              <Expandable title="headerFive properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerSix" type="TextNodeStyle">

              Styling for H6 nodes.

              <Expandable title="headerSix properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="paragraph" type="TextNodeStyle">

              Styling for paragraph nodes.

              <Expandable title="paragraph properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="blockquote" type="TextNodeStyle">

              Styling for block quote nodes.

              <Expandable title="blockquote properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="codeBlock" type="TextNodeStyle">

              Styling for code block nodes.

              <Expandable title="codeBlock properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="status" type="string">

      Status of the draft post.

      **Read-only.**

      **Supported values:**

      - `PUBLISHED` — Status indicating the draft post is published.
      - `UNPUBLISHED` — Status indicating the draft post is unpublished.
      - `SCHEDULED` — Status indicating the draft post is scheduled for publication.
      - `DELETED` — Status indicating the draft post is deleted.
      - `IN_REVIEW` — Status indicating the draft post is in review.

    </ResponseField>

    <ResponseField name="moderationDetails" type="ModerationDetails">

      Details of the draft post in review. Only relevant to posts submitted by guest writers.

      <Expandable title="moderationDetails properties">

        <ResponseField name="submittedBy" type="string">

          Member ID of the person submitting the draft post for review.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="submittedDate" type="datetime">

          Date the post was submitted for review.

        </ResponseField>

        <ResponseField name="status" type="string">

          Status indicating whether the submission was approved or rejected by the moderator.

          **Supported values:**

          - `APPROVED`
          - `REJECTED`

        </ResponseField>

        <ResponseField name="moderatedBy" type="string">

          Member ID of the person who approved or rejected the post.

        </ResponseField>

        <ResponseField name="moderationDate" type="datetime">

          Date the post was approved or rejected.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="hasUnpublishedChanges" type="boolean">

      Indicates if there are changes made to the draft post that have not yet been published.

      **Read-only.**

    </ResponseField>

    <ResponseField name="editedDate" type="datetime">

      Date the draft post was last edited.

      **Read-only.**

    </ResponseField>

    <ResponseField name="scheduledPublishDate" type="datetime">

      Date the draft post is scheduled to be published.

      **Read-only.**

    </ResponseField>

    <ResponseField name="firstPublishedDate" type="datetime">

      Date the post was first published.

    </ResponseField>

    <ResponseField name="seoData" type="SeoSchema">

      SEO data.

      <Expandable title="seoData properties">

        <ResponseField name="tags" type="Tag[]">

          SEO tag information.

          <Expandable title="tags properties">

            <ResponseField name="type" type="string">

              SEO tag type.

              Supported values: `title`, `meta`, `script`, `link`.

            </ResponseField>

            <ResponseField name="props" type="object">

              A `{"key": "value"}` pair object where each SEO tag property (`"name"`, `"content"`, `"rel"`, `"href"`) contains a value.
              For example: `{"name": "description", "content": "the description itself"}`.

            </ResponseField>

            <ResponseField name="meta" type="object">

              SEO tag metadata. For example, `{"height": 300, "width": 240}`.

            </ResponseField>

            <ResponseField name="children" type="string">

              SEO tag inner content. For example, `<title> inner content </title>`.

            </ResponseField>

            <ResponseField name="custom" type="boolean">

              Whether the tag is a [custom tag](https://support.wix.com/en/article/adding-additional-meta-tags-to-your-sites-pages) —
              i.e. an entry from the site's Advanced/Custom Tags list.

              This flag is scoped to that feature only. It is NOT an indicator of whether a standard tag
              (`title`, `description`, Open Graph, etc.) reflects a manually configured value versus a computed
              default/pattern — standard tags always resolve with `custom: false`, even when their content comes
              from a page's manually saved SEO title/description override. Don't use this field to verify whether
              a live page reflects a manual per-page SEO edit.

              Limitation: when `ResolveStaticPageSeoTags` is called without `seoData`, the per-page SEO data is read
              only from the Vibe/Wix-managed-headless override store. That store holds nothing for classic Wix Editor
              or Wix Studio pages, so even genuine Advanced/Custom Tags saved on those pages are missing from the
              response entirely — there, `false` means this API could not tell, not that no custom tag exists.
              Pass `seoData` explicitly to resolve against a known set of tags.

            </ResponseField>

            <ResponseField name="disabled" type="boolean">

              Whether the tag is disabled. If the tag is disabled, people can't find your page when searching for this phrase in search engines.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="settings" type="Settings">

          SEO general settings.

          <Expandable title="settings properties">

            <ResponseField name="preventAutoRedirect" type="boolean">

              Whether the [automatical redirect visits](https://support.wix.com/en/article/customizing-your-pages-seo-settings-in-the-seo-panel) from the old URL to the new one is enabled.

              Default: `false` (automatical redirect is enabled).

            </ResponseField>

            <ResponseField name="keywords" type="Keyword[]">

              User-selected keyword terms for a specific page.

              Constraints: `maxItems 5`.

              <Expandable title="keywords properties">

                <ResponseField name="term" type="string">

                  Keyword value.

                </ResponseField>

                <ResponseField name="isMain" type="boolean">

                  Whether the keyword is the main focus keyword.

                </ResponseField>

                <ResponseField name="origin" type="string">

                  The source that added the keyword terms to the SEO settings.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="url" type="PageUrl">

      Draft post URL preview. What the URL will look like once the post is published.

      **Read-only.**

      <Expandable title="url properties">

        <ResponseField name="base" type="string">

          The base URL. For premium sites, this is the domain. For free sites, this is the site URL. For example, `mysite.wixsite.com/mysite`.

        </ResponseField>

        <ResponseField name="path" type="string">

          The relative path for the page within the site. For example, `/product-page/a-product`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date the draft post was first created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="seoSlug" type="string">

      SEO slug.

    </ResponseField>

    <ResponseField name="media" type="Media">

      Post cover media.

      <Expandable title="media properties">

        <ResponseField name="displayed" type="boolean">

          Whether cover media is displayed.

        </ResponseField>

        <ResponseField name="custom" type="boolean">

          Whether custom cover media has been specified. If `false`, the first media item in the post's content serves as cover media.

        </ResponseField>

        <ResponseField name="altText" type="string">

          Media alternative text.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="media" type="media">

          media

          <Expandable title="media properties">

            <ResponseField name="wixMedia" type="WixMedia">

              Wix Media details.

              <Expandable title="wixMedia properties">

                <ResponseField name="image" type="Image">

                  Image details.

                  _Nested structure omitted for brevity — see the `Image` object._

                </ResponseField>

                <ResponseField name="videoV2" type="VideoV2">

                  Video details.

                  _Nested structure omitted for brevity — see the `VideoV2` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="embedMedia" type="EmbedMedia">

              Embed media details.

              <Expandable title="embedMedia properties">

                <ResponseField name="thumbnail" type="EmbedThumbnail">

                  Thumbnail details.

                  _Nested structure omitted for brevity — see the `EmbedThumbnail` object._

                </ResponseField>

                <ResponseField name="video" type="EmbedVideo">

                  Video details.

                  _Nested structure omitted for brevity — see the `EmbedVideo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="previewTextParagraph" type="integer">

      Number of paragraphs to display in a paid content preview for non-paying users.

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "draftPost": {
          "id": "448d1238-0072-4458-a280-bf81c2dd8af1",
          "title": "Places to visit in Europe",
          "featured": false,
          "categoryIds": [],
          "memberId": "2d24cb8a-adcc-466a-ab59-fa74e0889a37",
          "hashtags": [],
          "commentingEnabled": true,
          "minutesToRead": 0,
          "tagIds": [],
          "relatedPostIds": [],
          "pricingPlanIds": [],
          "language": "en",
          "changeOrigin": "AUTO_SAVE",
          "contentId": "622f285f7952527f485bc138",
          "editingSessionId": "0aca7f65-1237-482e-bf51-6d14102ee691",
          "status": "UNPUBLISHED",
          "mostRecentContributorId": "2d24cb8a-adcc-466a-ab59-fa74e0889a37",
          "hasUnpublishedChanges": true,
          "editedDate": "2022-03-14T11:34:55.121Z",
          "seoData": {
            "tags": []
          },
          "seoShowInSearch": true,
          "seoShowSnippetInSearch": true,
          "slugs": [],
          "createdDate": "2022-03-14T11:34:55.121Z"
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Draft post ID.

      **Read-only.** Constraints: `maxLength 38`.

    </ResponseField>

    <ResponseField name="title" type="string">

      Draft post title.

      Constraints: `maxLength 200`.

    </ResponseField>

    <ResponseField name="excerpt" type="string">

      Draft post excerpt.

      If no excerpt has been manually set, an excerpt is automatically generated from the post's text.
      This can be retrieved using the `GENERATED_EXCERPT` fieldset.

    </ResponseField>

    <ResponseField name="featured" type="boolean">

      Whether the draft post is marked as featured.

    </ResponseField>

    <ResponseField name="categoryIds" type="string[]">

      Category IDs of the draft post.

      Constraints: `maxLength 38`, `maxItems 10`.

    </ResponseField>

    <ResponseField name="memberId" type="string">

      Draft post owner's member ID.

    </ResponseField>

    <ResponseField name="hashtags" type="string[]">

      Hashtags in the post.

      Constraints: `maxLength 100`, `maxItems 100`.

    </ResponseField>

    <ResponseField name="commentingEnabled" type="boolean">

      Whether commenting on the draft post is enabled.

    </ResponseField>

    <ResponseField name="minutesToRead" type="integer">

      Estimated reading time of the draft post (calculated automatically).

      **Read-only.**

    </ResponseField>

    <ResponseField name="heroImage" type="string">

      Image placed at the top of the blog page.

    </ResponseField>

    <ResponseField name="tagIds" type="string[]">

      Tag IDs the draft post is tagged with.

      Constraints: `maxLength 38`, `maxItems 30`.

    </ResponseField>

    <ResponseField name="relatedPostIds" type="string[]">

      IDs of posts related to this draft post.

      Constraints: `maxLength 38`, `maxItems 3`.

    </ResponseField>

    <ResponseField name="pricingPlanIds" type="string[]">

      Pricing plan IDs. Only relevant if a post is assigned to a specific pricing plan. See the Pricing Plans API for more info.

      Constraints: `format GUID`, `maxItems 100`.

    </ResponseField>

    <ResponseField name="translationId" type="string">

      ID of the draft post's translations.

      All translations of a single post share the same `translationId`.
      Available only if the [Multilingual](https://support.wix.com/en/article/wix-multilingual-an-overview) app is installed.

    </ResponseField>

    <ResponseField name="language" type="string">

      Language the draft post is written in.

      2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.

    </ResponseField>

    <ResponseField name="richContent" type="RichContent">

      Draft Post rich content.

      &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="image.gallery.video.audio.divider.linkButton.html.giphy.codeBlock.file.hashtag.lineSpacing.indent.link.textColor.textHighlight.heading.verticalEmbed.table.collapsibleList.poll" exampleid="72c23a25-524f-4f70-a260-4a2777b6f5d5">
      &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
      &lt;/widget>

      <Expandable title="richContent properties">

        <ResponseField name="nodes" type="Node[]">

          Node objects representing a rich content document.

          <Expandable title="nodes properties">

            <ResponseField name="type" type="string">

              Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

              **Supported values:**

              - `PARAGRAPH`
              - `TEXT`
              - `HEADING`
              - `BULLETED_LIST`
              - `ORDERED_LIST`
              - `LIST_ITEM`
              - `BLOCKQUOTE`
              - `CODE_BLOCK`
              - `VIDEO`
              - `DIVIDER`
              - `FILE`
              - `GALLERY`
              - `GIF`
              - `HTML`
              - `IMAGE`
              - `LINK_PREVIEW`
              - `MAP` — @deprecated
              - `POLL`
              - `APP_EMBED`
              - `BUTTON`
              - `COLLAPSIBLE_LIST`
              - `TABLE`
              - `EMBED`
              - `COLLAPSIBLE_ITEM`
              - `COLLAPSIBLE_ITEM_TITLE`
              - `COLLAPSIBLE_ITEM_BODY`
              - `TABLE_CELL`
              - `TABLE_ROW`
              - `EXTERNAL`
              - `AUDIO`
              - `CAPTION`
              - `LAYOUT`
              - `LAYOUT_CELL`
              - `SHAPE`
              - `CARD`
              - `TOC`
              - `SMART_BLOCK`
              - `SMART_BLOCK_CELL`
              - `CHECKBOX_LIST`

            </ResponseField>

            <ResponseField name="_id" type="string">

              Node ID.

            </ResponseField>

            <ResponseField name="nodes" type="Node[]">

              A list of child nodes.

              _Recursive reference to `Node[]`._

            </ResponseField>

            <ResponseField name="style" type="NodeStyle">

              Padding and background color styling for the node.

              <Expandable title="style properties">

                <ResponseField name="paddingTop" type="string">

                  The top padding value in pixels.

                </ResponseField>

                <ResponseField name="paddingBottom" type="string">

                  The bottom padding value in pixels.

                </ResponseField>

                <ResponseField name="backgroundColor" type="string">

                  The background color as a hexadecimal value.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="metadata" type="Metadata">

          Object metadata.

          <Expandable title="metadata properties">

            <ResponseField name="version" type="integer">

              Schema version.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="createdTimestamp" type="datetime">

              When the object was created.

              **Read-only.**

            </ResponseField>

            <ResponseField name="updatedTimestamp" type="datetime">

              When the object was most recently updated.

            </ResponseField>

            <ResponseField name="_id" type="string">

              Object ID.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="documentStyle" type="DocumentStyle">

          Global styling for header, paragraph, block quote, and code block nodes in the object.

          <Expandable title="documentStyle properties">

            <ResponseField name="headerOne" type="TextNodeStyle">

              Styling for H1 nodes.

              <Expandable title="headerOne properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerTwo" type="TextNodeStyle">

              Styling for H2 nodes.

              <Expandable title="headerTwo properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerThree" type="TextNodeStyle">

              Styling for H3 nodes.

              <Expandable title="headerThree properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFour" type="TextNodeStyle">

              Styling for H4 nodes.

              <Expandable title="headerFour properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFive" type="TextNodeStyle">

              Styling for H5 nodes.

              <Expandable title="headerFive properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerSix" type="TextNodeStyle">

              Styling for H6 nodes.

              <Expandable title="headerSix properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="paragraph" type="TextNodeStyle">

              Styling for paragraph nodes.

              <Expandable title="paragraph properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="blockquote" type="TextNodeStyle">

              Styling for block quote nodes.

              <Expandable title="blockquote properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="codeBlock" type="TextNodeStyle">

              Styling for code block nodes.

              <Expandable title="codeBlock properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="status" type="string">

      Status of the draft post.

      **Read-only.**

      **Supported values:**

      - `PUBLISHED` — Status indicating the draft post is published.
      - `UNPUBLISHED` — Status indicating the draft post is unpublished.
      - `SCHEDULED` — Status indicating the draft post is scheduled for publication.
      - `DELETED` — Status indicating the draft post is deleted.
      - `IN_REVIEW` — Status indicating the draft post is in review.

    </ResponseField>

    <ResponseField name="moderationDetails" type="ModerationDetails">

      Details of the draft post in review. Only relevant to posts submitted by guest writers.

      <Expandable title="moderationDetails properties">

        <ResponseField name="submittedBy" type="string">

          Member ID of the person submitting the draft post for review.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="submittedDate" type="datetime">

          Date the post was submitted for review.

        </ResponseField>

        <ResponseField name="status" type="string">

          Status indicating whether the submission was approved or rejected by the moderator.

          **Supported values:**

          - `APPROVED`
          - `REJECTED`

        </ResponseField>

        <ResponseField name="moderatedBy" type="string">

          Member ID of the person who approved or rejected the post.

        </ResponseField>

        <ResponseField name="moderationDate" type="datetime">

          Date the post was approved or rejected.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="hasUnpublishedChanges" type="boolean">

      Indicates if there are changes made to the draft post that have not yet been published.

      **Read-only.**

    </ResponseField>

    <ResponseField name="editedDate" type="datetime">

      Date the draft post was last edited.

      **Read-only.**

    </ResponseField>

    <ResponseField name="scheduledPublishDate" type="datetime">

      Date the draft post is scheduled to be published.

      **Read-only.**

    </ResponseField>

    <ResponseField name="firstPublishedDate" type="datetime">

      Date the post was first published.

    </ResponseField>

    <ResponseField name="seoData" type="SeoSchema">

      SEO data.

      <Expandable title="seoData properties">

        <ResponseField name="tags" type="Tag[]">

          SEO tag information.

          <Expandable title="tags properties">

            <ResponseField name="type" type="string">

              SEO tag type.

              Supported values: `title`, `meta`, `script`, `link`.

            </ResponseField>

            <ResponseField name="props" type="object">

              A `{"key": "value"}` pair object where each SEO tag property (`"name"`, `"content"`, `"rel"`, `"href"`) contains a value.
              For example: `{"name": "description", "content": "the description itself"}`.

            </ResponseField>

            <ResponseField name="meta" type="object">

              SEO tag metadata. For example, `{"height": 300, "width": 240}`.

            </ResponseField>

            <ResponseField name="children" type="string">

              SEO tag inner content. For example, `<title> inner content </title>`.

            </ResponseField>

            <ResponseField name="custom" type="boolean">

              Whether the tag is a [custom tag](https://support.wix.com/en/article/adding-additional-meta-tags-to-your-sites-pages) —
              i.e. an entry from the site's Advanced/Custom Tags list.

              This flag is scoped to that feature only. It is NOT an indicator of whether a standard tag
              (`title`, `description`, Open Graph, etc.) reflects a manually configured value versus a computed
              default/pattern — standard tags always resolve with `custom: false`, even when their content comes
              from a page's manually saved SEO title/description override. Don't use this field to verify whether
              a live page reflects a manual per-page SEO edit.

              Limitation: when `ResolveStaticPageSeoTags` is called without `seoData`, the per-page SEO data is read
              only from the Vibe/Wix-managed-headless override store. That store holds nothing for classic Wix Editor
              or Wix Studio pages, so even genuine Advanced/Custom Tags saved on those pages are missing from the
              response entirely — there, `false` means this API could not tell, not that no custom tag exists.
              Pass `seoData` explicitly to resolve against a known set of tags.

            </ResponseField>

            <ResponseField name="disabled" type="boolean">

              Whether the tag is disabled. If the tag is disabled, people can't find your page when searching for this phrase in search engines.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="settings" type="Settings">

          SEO general settings.

          <Expandable title="settings properties">

            <ResponseField name="preventAutoRedirect" type="boolean">

              Whether the [automatical redirect visits](https://support.wix.com/en/article/customizing-your-pages-seo-settings-in-the-seo-panel) from the old URL to the new one is enabled.

              Default: `false` (automatical redirect is enabled).

            </ResponseField>

            <ResponseField name="keywords" type="Keyword[]">

              User-selected keyword terms for a specific page.

              Constraints: `maxItems 5`.

              <Expandable title="keywords properties">

                <ResponseField name="term" type="string">

                  Keyword value.

                </ResponseField>

                <ResponseField name="isMain" type="boolean">

                  Whether the keyword is the main focus keyword.

                </ResponseField>

                <ResponseField name="origin" type="string">

                  The source that added the keyword terms to the SEO settings.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="url" type="string">

      Draft post URL preview. What the URL will look like once the post is published.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date the draft post was first created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="seoSlug" type="string">

      SEO slug.

    </ResponseField>

    <ResponseField name="media" type="Media">

      Post cover media.

      <Expandable title="media properties">

        <ResponseField name="displayed" type="boolean">

          Whether cover media is displayed.

        </ResponseField>

        <ResponseField name="custom" type="boolean">

          Whether custom cover media has been specified. If `false`, the first media item in the post's content serves as cover media.

        </ResponseField>

        <ResponseField name="altText" type="string">

          Media alternative text.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="media" type="media">

          media

          <Expandable title="media properties">

            <ResponseField name="wixMedia" type="WixMedia">

              Wix Media details.

              <Expandable title="wixMedia properties">

                <ResponseField name="image" type="string">

                  Image details.

                </ResponseField>

                <ResponseField name="videoV2" type="string">

                  Video details.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="embedMedia" type="EmbedMedia">

              Embed media details.

              <Expandable title="embedMedia properties">

                <ResponseField name="thumbnail" type="EmbedThumbnail">

                  Thumbnail details.

                  _Nested structure omitted for brevity — see the `EmbedThumbnail` object._

                </ResponseField>

                <ResponseField name="video" type="EmbedVideo">

                  Video details.

                  _Nested structure omitted for brevity — see the `EmbedVideo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="previewTextParagraph" type="integer">

      Number of paragraphs to display in a paid content preview for non-paying users.

    </ResponseField>

  </Tab>

</Tabs>
