---
title: "onPostMoved"
description: "An existing post was moved to a different category."
icon: "bolt"
---

An existing post was moved to a different category.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_moved` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostMoved
  import { posts } from '@wix/forum';

  posts.onPostMoved((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostMoved (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostMoved((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
