---
title: "Delete Marketing Tag V 2"
description: "Deletes a marketing tag."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a marketing tag.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.MANAGE-TAGS">Manage Marketing Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/marketing/v2/tags
    ```

    ## Query Params

    <ParamField query="tagType" type="string">

      Marketing tag to delete.

      **Supported values:**

      - `GOOGLE_ADS`
      - `GOOGLE_ANALYTICS`
      - `YANDEX_METRICA`
      - `FACEBOOK_PIXEL`
      - `GOOGLE_TAG_MANAGER`
      - `TIKTOK_PIXEL`
      - `GOOGLE_TAG_MANAGER_CONSENT_MODE`
      - `GOOGLE_ANALYTICS_CONSENT_MODE`

    </ParamField>

    ## Response Object

    Returns `DeleteMarketingTagV2Response`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Marketing Tag Deleted | `wix.marketing.v1.tag_deleted` | Triggered when a marketing tag is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/marketing/v2/tags?tag_type=GOOGLE_ANALYTICS_CONSENT_MODE' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {

      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a marketing tag.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.MANAGE-TAGS">Manage Marketing Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingTags } from '@wix/marketing-tags';

    marketingTags.deleteMarketingTagV2(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="DeleteMarketingTagV2Options">

      Options to use when deleting a marketing tag.

      <Expandable title="options properties">

        <ParamField body="tagType" type="string">

          Marketing tag to delete.

          **Supported values:**

          - `GOOGLE_ADS`
          - `GOOGLE_ANALYTICS`
          - `YANDEX_METRICA`
          - `FACEBOOK_PIXEL`
          - `GOOGLE_TAG_MANAGER`
          - `TIKTOK_PIXEL`
          - `GOOGLE_TAG_MANAGER_CONSENT_MODE`
          - `GOOGLE_ANALYTICS_CONSENT_MODE`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `DeleteMarketingTagV2Response`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tag Deleted | `wix.marketing.v1.tag_deleted` | Triggered when a marketing tag is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-marketing-tag-v-2" id="ex-sdk-delete-marketing-tag-v-2-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-marketing-tag-v-2" id="ex-sdk-delete-marketing-tag-v-2-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-marketing-tag-v-2" id="ex-sdk-delete-marketing-tag-v-2-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-marketing-tag-v-2-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-marketing-tag-v-2-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingTagV2</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingTagV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingTagV2 (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-marketing-tag-v-2-0"><span className="wix-picker-name">deleteMarketingTagV2</span></label>
    <label htmlFor="ex-sdk-delete-marketing-tag-v-2-1"><span className="wix-picker-name">deleteMarketingTagV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-marketing-tag-v-2-2"><span className="wix-picker-name">deleteMarketingTagV2 (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteMarketingTagV2
    import { marketingTags } from '@wix/marketing-tags';

    async function deleteMarketingTagV2(options) {
      const response = await marketingTags.deleteMarketingTagV2(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMarketingTagV2 (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingTags } from '@wix/marketing-tags';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingTags },
      // Include the auth strategy and host as relevant
    });

    async function deleteMarketingTagV2(options) {
      const response = await myWixClient.marketingTags.deleteMarketingTagV2(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMarketingTagV2 (with elevated permissions)
    import { marketingTags } from '@wix/marketing-tags';
    import { auth } from '@wix/essentials';

    async function myDeleteMarketingTagV2Method(options) {
      const elevatedDeleteMarketingTagV2 = auth.elevate(marketingTags.deleteMarketingTagV2);
      const response = await elevatedDeleteMarketingTagV2(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
