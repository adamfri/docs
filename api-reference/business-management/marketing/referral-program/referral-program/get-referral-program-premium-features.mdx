---
title: "Get Referral Program Premium Features"
description: "Retrieves information about the enabled premium features for the referral program."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves information about the enabled premium features for the referral program.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/_api/referral-programs/v1/program/premium-features
    ```

    ## Response Object

    <ResponseField name="referralProgram" type="boolean">

      Whether the site has the referral program feature enabled.

      **Read-only.**

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/_api/referral-programs/v1/program/premium-features' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "referralProgram": true
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves information about the enabled premium features for the referral program.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/referral';

    programs.getReferralProgramPremiumFeatures(): Promise<GetReferralProgramPremiumFeaturesResponse>
    ```

    ## Returns

    Returns `Promise<GetReferralProgramPremiumFeaturesResponse>`.

    <ResponseField name="referralProgram" type="boolean">

      Whether the site has the referral program feature enabled.

      **Read-only.**

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-referral-program-premium-features" id="ex-sdk-get-referral-program-premium-features-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-referral-program-premium-features" id="ex-sdk-get-referral-program-premium-features-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-referral-program-premium-features-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-referral-program-premium-features-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getReferralProgramPremiumFeatures</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferralProgramPremiumFeatures (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-referral-program-premium-features-0"><span className="wix-picker-name">getReferralProgramPremiumFeatures</span></label>
    <label htmlFor="ex-sdk-get-referral-program-premium-features-1"><span className="wix-picker-name">getReferralProgramPremiumFeatures (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getReferralProgramPremiumFeatures
    import { programs } from '@wix/referral';

    async function getReferralProgramPremiumFeatures() {
      const response = await programs.getReferralProgramPremiumFeatures();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferralProgramPremiumFeatures (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/referral';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function getReferralProgramPremiumFeatures() {
      const response = await myWixClient.programs.getReferralProgramPremiumFeatures();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
