---
title: "Upsert Menu Ordering Settings By Menu Id"
description: "Upserts a menu ordering settings entity for a given menuId."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Upserts a menu ordering settings entity for a given `menuId`.
    Try to create a menu ordering settings, in case it already exists - update the menu ordering settings

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/menu-ordering-settings/v1/menu-ordering-settings/upsert/menu-id/{menuOrderingSettings.menuId}
    ```

    ## Path Params

    <ParamField path="menuOrderingSettings.menuId" type="string" required>

      ID of the menu these settings apply to.

    </ParamField>

    ## Body Params

    <ParamField body="menuOrderingSettings" type="MenuOrderingSettings" required>

      Details of the menu ordering settings entity to be created or updated.

      See the [Menu Ordering Settings Object](/api-reference/business-solutions/restaurants/online-orders/menu-ordering-settings/menu-ordering-settings-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="menuOrderingSettings" type="MenuOrderingSettings">

      The created or updated menu ordering settings entity.

      See the [Menu Ordering Settings Object](/api-reference/business-solutions/restaurants/online-orders/menu-ordering-settings/menu-ordering-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Menu Ordering Settings Created | `wix.restaurants.menu_settings.v1.menu_ordering_settings_created` |  |
    | Menu Ordering Settings Updated | `wix.restaurants.menu_settings.v1.menu_ordering_settings_updated` |  |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Upserts a menu ordering settings entity for a given `menuId`.
    Try to create a menu ordering settings, in case it already exists - update the menu ordering settings

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { menuOrderingSettings } from '@wix/restaurants';

    menuOrderingSettings.upsertMenuOrderingSettingsByMenuId(menuId, menuOrderingSettings): Promise<UpsertMenuOrderingSettingsByMenuIdResponse>
    ```

    ## Method Parameters

    <ParamField body="menuId" type="string" required>

      ID of the menu these settings apply to.

      **Read-only.**

    </ParamField>

    <ParamField body="menuOrderingSettings" type="UpsertMenuOrderingSettingsByMenuId" required>

      Details of the menu ordering settings entity to be created or updated.

      <Expandable title="menuOrderingSettings properties">

        <ParamField body="_id" type="string">

          Menu ordering settings object ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer">

          Revision number. Increments by 1 each time the menu ordering settings object is updated. To prevent conflicting changes, the existing revision must be specified when updating a menu ordering settings object.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the menu ordering settings object was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the menu ordering settings object was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

          **Read-only.**

        </ParamField>

        <ParamField body="operationId" type="string">

          ID of the restaurant operation the menu belongs to. (See the Restaurants Operations API for more information.)

        </ParamField>

        <ParamField body="menuId" type="string">

          ID of the menu these settings apply to.

          **Read-only.**

        </ParamField>

        <ParamField body="onlineOrderingEnabled" type="boolean">

          Whether online ordering is enabled for the menu.

        </ParamField>

        <ParamField body="availability" type="Availability">

          Menu availability settings.

          <Expandable title="availability properties">

            <ParamField body="type" type="string">

              Availability type.

              **Supported values:**

              - `UNSPECIFIED_AVAILABILITY_OPTION` — Missing type due to an error.
              - `ALWAYS_AVAILABLE` — Available all the time.
              - `WEEKLY_SCHEDULE` — Available on specific days and times throughout the week.
              - `TIMESTAMP_RANGES` — Available during a specific time range.

            </ParamField>

            <ParamField body="timeZone" type="string">

              Time zone in [Time Zone Database](https://www.iana.org/time-zones) format.

              **Read-only.**

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="availabilityTypeOptions" type="availabilityTypeOptions">

              availabilityTypeOptions

              _Nested structure omitted for brevity — see the `availabilityTypeOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="businessLocationId" type="string">

          Business location ID ([SDK](https://dev.wix.com/docs/sdk/backend-modules/restaurants/wix-restaurants-new/about-business-locations) | [REST](https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/about-business-locations)) of the menu.

          **Read-only.**

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Extended fields.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tags ([SDK](https://dev.wix.com/docs/sdk/backend-modules/tags/tags/introduction) | [REST](https://dev.wix.com/docs/rest/business-management/tags/introduction)) used to classify and sort different types of menu ordering settings.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpsertMenuOrderingSettingsByMenuIdResponse>`.

    <ResponseField name="menuOrderingSettings" type="MenuOrderingSettings">

      The created or updated menu ordering settings entity.

      See the [Menu Ordering Settings Object](/api-reference/business-solutions/restaurants/online-orders/menu-ordering-settings/menu-ordering-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Menu Ordering Settings Created | `wix.restaurants.menu_settings.v1.menu_ordering_settings_created` |  |
    | Menu Ordering Settings Updated | `wix.restaurants.menu_settings.v1.menu_ordering_settings_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-upsert-menu-ordering-settings-by-menu-id" id="ex-sdk-upsert-menu-ordering-settings-by-menu-id-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-upsert-menu-ordering-settings-by-menu-id" id="ex-sdk-upsert-menu-ordering-settings-by-menu-id-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-upsert-menu-ordering-settings-by-menu-id" id="ex-sdk-upsert-menu-ordering-settings-by-menu-id-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-upsert-menu-ordering-settings-by-menu-id-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-upsert-menu-ordering-settings-by-menu-id-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">upsertMenuOrderingSettingsByMenuId</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">upsertMenuOrderingSettingsByMenuId (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">upsertMenuOrderingSettingsByMenuId (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-upsert-menu-ordering-settings-by-menu-id-0"><span className="wix-picker-name">upsertMenuOrderingSettingsByMenuId</span></label>
    <label htmlFor="ex-sdk-upsert-menu-ordering-settings-by-menu-id-1"><span className="wix-picker-name">upsertMenuOrderingSettingsByMenuId (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-upsert-menu-ordering-settings-by-menu-id-2"><span className="wix-picker-name">upsertMenuOrderingSettingsByMenuId (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript upsertMenuOrderingSettingsByMenuId
    import { menuOrderingSettings } from '@wix/restaurants';

    async function upsertMenuOrderingSettingsByMenuId(menuId,menuOrderingSettings) {
      const response = await menuOrderingSettings.upsertMenuOrderingSettingsByMenuId(menuId,menuOrderingSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript upsertMenuOrderingSettingsByMenuId (self-hosted)
    import { createClient } from '@wix/sdk';
    import { menuOrderingSettings } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { menuOrderingSettings },
      // Include the auth strategy and host as relevant
    });

    async function upsertMenuOrderingSettingsByMenuId(menuId,menuOrderingSettings) {
      const response = await myWixClient.menuOrderingSettings.upsertMenuOrderingSettingsByMenuId(menuId,menuOrderingSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript upsertMenuOrderingSettingsByMenuId (with elevated permissions)
    import { menuOrderingSettings } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myUpsertMenuOrderingSettingsByMenuIdMethod(menuId,menuOrderingSettings) {
      const elevatedUpsertMenuOrderingSettingsByMenuId = auth.elevate(menuOrderingSettings.upsertMenuOrderingSettingsByMenuId);
      const response = await elevatedUpsertMenuOrderingSettingsByMenuId(menuId,menuOrderingSettings);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
