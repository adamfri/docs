---
title: "onItemModifierGroupCreated"
description: "The onItemModifierGroupCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_modifier_group_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemModifierGroupCreated
  import { itemModifierGroups } from '@wix/restaurants';

  itemModifierGroups.onItemModifierGroupCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemModifierGroupCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemModifierGroups } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemModifierGroups,
      },
  });

  wixClient.itemModifierGroups.onItemModifierGroupCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
