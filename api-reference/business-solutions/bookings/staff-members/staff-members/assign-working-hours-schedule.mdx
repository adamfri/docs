---
title: "Assign Working Hours Schedule"
description: "Updates a staff member's working hours based on the specified schedule ID."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a staff member's working hours based on the specified [schedule ID](https://dev.wix.com/docs/api-reference/business-management/calendar/schedules-v3/introduction).

    ### Default working hours

    By default, staff members work during the opening hours of the business's default [location](https://dev.wix.com/docs/api-reference/business-management/locations/introduction).

    ### Schedule ID

    You can specify either the ID of the business's working hour schedule or the  staff member's event schedule.
    The call fails if you specify a different schedule ID.

    #### Staff event schedule

    To customize a staff member's working hours, specify their event schedule ID as `scheduleId`.
    Refer to this [sample flow](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/staff-members/sample-flows#assign-a-staff-working-hour-schedule) for more details.

    #### Business's working hour schedule

    To reset a staff member's working hours to the default business hours, specify the ID of the business's working hour schedule as `scheduleId`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v1/staff-members/{staffMemberId}/assign-working-hours-schedule
    ```

    ## Path Params

    <ParamField path="staffMemberId" type="string" required>

      ID of the staff member to assign the schedule to.

    </ParamField>

    ## Body Params

    <ParamField body="scheduleId" type="string" required>

      ID of the [schedule](https://dev.wix.com/docs/api-reference/business-management/calendar/schedules-v3/introduction)
      to assign to the staff member.

      Must be either the staff member's event schedule ID or the working hour
      schedule ID for a business location.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="fields" type="string[]">

      Conditional fields to return.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `RESOURCE_DETAILS` — Includes `resource` in the response.
      - `ASSOCIATED_IDENTITY_STATUS` — Includes `associatedIdentityStatus` in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="staffMember" type="StaffMember">

      Updated staff member.

      See the [Staff Member Object](/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Staff Member Updated | `wix.bookings.staff.v1.staff_member_updated` | Triggered when a staff member is updated. |
    | Staff Member Updated | `wix.bookings.staff.v1.staff_member_updated` | Triggered when a staff member is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v1/staff-members/0954bbb2-88cd-445c-9827-44253eb8b039/assign-working-hours-schedule' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "scheduleId": "7c147d9e-89a8-4e2e-a723-b095db0610c1",
        "fields": ["RESOURCE_DETAILS"]
      }'
      ```

      ```json Response
      {
        "staffMember": {
          "id": "0954bbb2-88cd-445c-9827-44253eb8b039",
          "email": "MateusAraujoSantos1979@example.com",
          "name": "Mateus Araujo Santos",
          "resourceId": "136d9d9a-97ab-49fa-a0cf-ec24f500bd98",
          "revision": "3",
          "createdDate": "2024-08-28T13:22:32.784Z",
          "updatedDate": "2024-08-28T14:16:11.948Z",
          "resource": {
            "id": "e0936563-7fd8-4e08-9db0-a327b2cb653e",
            "workingHoursSchedules": [
              {
                "id": "7c147d9e-89a8-4e2e-a723-b095db0610c1",
                "shared": false
              }
            ],
            "eventsSchedule": {
              "id": "7c147d9e-89a8-4e2e-a723-b095db0610c1"
            },
            "usesDefaultWorkingHours": false
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a staff member's working hours based on the specified [schedule ID](https://dev.wix.com/docs/api-reference/business-management/calendar/schedules-v3/introduction).

    ### Default working hours

    By default, staff members work during the opening hours of the business's default [location](https://dev.wix.com/docs/api-reference/business-management/locations/introduction).

    ### Schedule ID

    You can specify either the ID of the business's working hour schedule or the  staff member's event schedule.
    The call fails if you specify a different schedule ID.

    #### Staff event schedule

    To customize a staff member's working hours, specify their event schedule ID as `scheduleId`.
    Refer to this [sample flow](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/staff-members/sample-flows#assign-a-staff-working-hour-schedule) for more details.

    #### Business's working hour schedule

    To reset a staff member's working hours to the default business hours, specify the ID of the business's working hour schedule as `scheduleId`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { staffMembers } from '@wix/bookings';

    staffMembers.assignWorkingHoursSchedule(staffMemberId, scheduleId, options): Promise<AssignWorkingHoursScheduleResponse>
    ```

    ## Method Parameters

    <ParamField body="staffMemberId" type="string" required>

      ID of the staff member to assign the schedule to.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="scheduleId" type="string" required>

      ID of the [schedule](https://dev.wix.com/docs/api-reference/business-management/calendar/schedules-v3/introduction)
      to assign to the staff member.

      Must be either the staff member's event schedule ID or the working hour
      schedule ID for a business location.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="AssignWorkingHoursScheduleOptions">

      Options for setting the staff member's working hours schedule.

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Conditional fields to return.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `RESOURCE_DETAILS` — Includes `resource` in the response.
          - `ASSOCIATED_IDENTITY_STATUS` — Includes `associatedIdentityStatus` in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<AssignWorkingHoursScheduleResponse>`.

    <ResponseField name="staffMember" type="StaffMember">

      Updated staff member.

      See the [Staff Member Object](/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Staff Member Updated | `wix.bookings.staff.v1.staff_member_updated` | Triggered when a staff member is updated. |
    | Staff Member Updated | `wix.bookings.staff.v1.staff_member_updated` | Triggered when a staff member is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-assign-working-hours-schedule" id="ex-sdk-assign-working-hours-schedule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-assign-working-hours-schedule" id="ex-sdk-assign-working-hours-schedule-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-assign-working-hours-schedule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-assign-working-hours-schedule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Give a staff member custom working hours</span><span className="wix-picker-desc">Pass the staff member's own event schedule ID to detach them from the business default schedule</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">assignWorkingHoursSchedule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-assign-working-hours-schedule-0"><span className="wix-picker-name">Give a staff member custom working hours</span><span className="wix-picker-desc">Pass the staff member's own event schedule ID to detach them from the business default schedule</span></label>
    <label htmlFor="ex-sdk-assign-working-hours-schedule-1"><span className="wix-picker-name">assignWorkingHoursSchedule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Give a staff member custom working hours
    import { staffMembers } from "@wix/bookings";

    async function assignWorkingHoursSchedule() {
      const response = await staffMembers.assignWorkingHoursSchedule(
        "3afa39f1-7638-42d3-8ed6-266ff9648531",
        "86f18d72-f1ea-41ac-b48a-f8081735eff6",
      );
    }

    /* Promise resolves to:
     * {
     *   "staffMember": {
     *     "name": "Dana Cohen",
     *     "email": "dana.cohen@example.com",
     *     "description": "Senior stylist",
     *     "resourceId": "8b0c7207-95f1-4e35-81ed-5189fa015c70",
     *     "revision": "3",
     *     "serviceProvider": false,
     *     "_id": "3afa39f1-7638-42d3-8ed6-266ff9648531",
     *     "_createdDate": "2026-08-16T11:53:33.679Z",
     *     "_updatedDate": "2026-08-16T11:53:35.831Z"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript assignWorkingHoursSchedule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { staffMembers } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { staffMembers },
      // Include the auth strategy and host as relevant
    });

    async function assignWorkingHoursSchedule(staffMemberId,scheduleId,options) {
      const response = await myWixClient.staffMembers.assignWorkingHoursSchedule(staffMemberId,scheduleId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
