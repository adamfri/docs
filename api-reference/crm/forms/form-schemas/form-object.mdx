---
title: "Form Object"
description: "The Form object in the Wix Form API."
icon: "cube"
---

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Form schema ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="formFields" type="Field[]">

      Form fields that define the input elements and their validation rules.

      The order of this array isn't the order the fields are displayed in. Display order comes from `steps`, where each layout item positions a field by `row` and `column`. Don't render fields in array order.

      For a detailed explanation of how to compose each kind of field, including which `identifier`, `inputType`, and `componentType` values to combine, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

      Constraints: `maxItems 500`.

      <Expandable title="formFields properties">

        <ResponseField name="id" type="string">

          Field ID.

          The ID is stored in lowercase. Because the form's layout references each field by this ID, pass a lowercase GUID so that `id` and the layout's `fieldId` still match after the form schema is stored. IDs must be unique in the form.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the field is hidden from submitters.

          Default: `false`

        </ResponseField>

        <ResponseField name="identifier" type="string">

          Identification for the field, describing its purpose. For example, `TEXT_INPUT` or `CONTACTS_EMAIL`.

          Set this to the known value for the kind of field you're creating.
          An unrecognized value, such as a custom string or a GUID, is dropped rather than rejected: the request still succeeds, but the field doesn't appear in the response's `formFields`, and neither the field nor its submitted values appear in the Wix dashboard.

          Several fields share a general-purpose `componentType`, so `identifier` is often the only value that distinguishes them.
          For example, both a short answer field and a long answer field use the `TEXT_INPUT` component, and are told apart by their `TEXT_INPUT` and `TEXT_AREA` identifiers.

          For the full list of known values, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

        </ResponseField>

        <ResponseField name="fieldType" type="string">

          Field type.

          `INPUT` fields collect data from submitters and are configured in `inputOptions`.
          `DISPLAY` fields show information without collecting data and are configured in `displayOptions`.

          **Supported values:**

          - `UNKNOWN_FIELD_TYPE` — Unknown field type.
          - `INPUT` — Input field that collects data from users.
          - `DISPLAY` — Display field that shows information without collecting input.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="steps" type="Step[]">

      Layout configuration that defines how form fields are organized across multiple steps.
      Each step represents a page of the form.

      Every field in `formFields`, including the submit button, must be placed by a layout item whose `fieldId` matches the field's `id`. A field that isn't placed still stores submitted values, but the Wix dashboard renders it, and the submissions made against it, as empty.

      Use a single step unless the form is split across pages.

      Constraints: `maxItems 100`.

      <Expandable title="steps properties">

        <ResponseField name="id" type="string">

          Step ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="name" type="string">

          Name of the step.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the step is hidden.

        </ResponseField>

        <ResponseField name="layout" type="FormLayout">

          Layout configuration that defines how form fields are arranged within this step.

          <Expandable title="layout properties">

            <ResponseField name="large" type="BreakPoint">

              Layout configuration for large screen breakpoints (desktop).

              <Expandable title="large properties">

                <ResponseField name="items" type="ItemLayout[]">

                  Configuration for the layout of items in the breakpoint.
                  Every field of the step must have exactly 1 corresponding item in each breakpoint that is defined.

                  Constraints: `maxItems 500`.

                  _Nested structure omitted for brevity — see the `ItemLayout[]` object._

                </ResponseField>

                <ResponseField name="columns" type="integer">

                  Number of columns in the layout grid.

                </ResponseField>

                <ResponseField name="rowHeight" type="integer">

                  Height of each row in the layout grid, measured in pixels.

                </ResponseField>

                <ResponseField name="margin" type="Margin">

                  Margin spacing configuration for the form elements.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

                <ResponseField name="padding" type="Margin">

                  Padding spacing inside form elements in pixels.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="medium" type="BreakPoint">

              Layout configuration for medium screen breakpoints (tablet).

              <Expandable title="medium properties">

                <ResponseField name="items" type="ItemLayout[]">

                  Configuration for the layout of items in the breakpoint.
                  Every field of the step must have exactly 1 corresponding item in each breakpoint that is defined.

                  Constraints: `maxItems 500`.

                  _Nested structure omitted for brevity — see the `ItemLayout[]` object._

                </ResponseField>

                <ResponseField name="columns" type="integer">

                  Number of columns in the layout grid.

                </ResponseField>

                <ResponseField name="rowHeight" type="integer">

                  Height of each row in the layout grid, measured in pixels.

                </ResponseField>

                <ResponseField name="margin" type="Margin">

                  Margin spacing configuration for the form elements.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

                <ResponseField name="padding" type="Margin">

                  Padding spacing inside form elements in pixels.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="small" type="BreakPoint">

              Layout configuration for small screen breakpoints (mobile).

              <Expandable title="small properties">

                <ResponseField name="items" type="ItemLayout[]">

                  Configuration for the layout of items in the breakpoint.
                  Every field of the step must have exactly 1 corresponding item in each breakpoint that is defined.

                  Constraints: `maxItems 500`.

                  _Nested structure omitted for brevity — see the `ItemLayout[]` object._

                </ResponseField>

                <ResponseField name="columns" type="integer">

                  Number of columns in the layout grid.

                </ResponseField>

                <ResponseField name="rowHeight" type="integer">

                  Height of each row in the layout grid, measured in pixels.

                </ResponseField>

                <ResponseField name="margin" type="Margin">

                  Margin spacing configuration for the form elements.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

                <ResponseField name="padding" type="Margin">

                  Padding spacing inside form elements in pixels.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="rules" type="FormRule[]" deprecated>

      Form rules, can be applied to layout and items properties.

      **Deprecated.** Form rules, can be applied to layout and items properties. Use `form_rules` instead. Removal: 2026-04-01. Constraints: `maxItems 100`.

      <Expandable title="rules properties">

        <ResponseField name="id" type="string">

          Rule ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="condition" type="object">

          Condition that determines when this rule's overrides should be applied.

        </ResponseField>

        <ResponseField name="overrides" type="FormOverride[]">

          Changes to apply to form elements when the condition evaluates to true.

          Constraints: `maxItems 500`.

          <Expandable title="overrides properties">

            <ResponseField name="entityType" type="string">

              Type of form element that this override targets.

              Constraints: `immutable`.

              **Supported values:**

              - `FIELD` — Override applies to a form field.

            </ResponseField>

            <ResponseField name="entityId" type="string">

              ID of the form element to modify.
              For nested form fields, use the format: "&#123;fieldIdWithNestedForm&#125;/&#123;nestedFormFieldId&#125;".

            </ResponseField>

            <ResponseField name="valueChanges" type="object">

              Property changes to apply to the form element when the rule condition is met.
              Each key represents a property path, and the value is the new value to set.

              Constraints: `format map`.

              <Expandable title="valueChanges properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="any" />

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="name" type="string">

          Name for the rule.

        </ResponseField>

        <ResponseField name="namespaceRule" type="boolean">

          Namespace rules are part of business logic of app owning namespace, have limited editing possibilities

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the form schema is updated.
      To prevent conflicting changes, the current revision must be passed when updating the form schema.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time when the form schema was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time when the form schema was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="deletedFields" type="FormField[]" deprecated>

      Deprecated. Use `deletedFormFields` instead.

      **Read-only.** **Deprecated.** Deprecated. Use `deletedFormFields` instead. Use `deleted_form_fields` instead. Removal: 2027-02-01. Constraints: `maxItems 150`.

      <Expandable title="deletedFields properties">

        <ResponseField name="id" type="string">

          Item ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="target" type="string">

          Definition of a target where the value of field belongs.

        </ResponseField>

        <ResponseField name="validation" type="Validation">

          Validation of field output value.

          <Expandable title="validation properties">

            <ResponseField name="required" type="boolean">

              Whether the field is required.

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="validation" type="validation">

              validation

              <Expandable title="validation properties">

                <ResponseField name="string" type="StringType">

                  Validation of string type.

                  _Nested structure omitted for brevity — see the `StringType` object._

                </ResponseField>

                <ResponseField name="number" type="NumberType">

                  Validation of number type.

                  _Nested structure omitted for brevity — see the `NumberType` object._

                </ResponseField>

                <ResponseField name="integer" type="IntegerType">

                  Validation of integer type.

                  _Nested structure omitted for brevity — see the `IntegerType` object._

                </ResponseField>

                <ResponseField name="boolean" type="BooleanType">

                  Validation of boolean type.

                  _Nested structure omitted for brevity — see the `BooleanType` object._

                </ResponseField>

                <ResponseField name="array" type="ArrayType">

                  Validation of array type.

                  _Nested structure omitted for brevity — see the `ArrayType` object._

                </ResponseField>

                <ResponseField name="object" type="ObjectType">

                  Validation of object type.

                  _Nested structure omitted for brevity — see the `ObjectType` object._

                </ResponseField>

                <ResponseField name="predefined" type="PredefinedValidation">

                  Predefined validation of specific format

                  _Nested structure omitted for brevity — see the `PredefinedValidation` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pii" type="boolean">

          Mark the field as containing personal information. This will encrypt user data when storing it.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the field is hidden.

        </ResponseField>

        <ResponseField name="view" type="object">

          Field view properties.

        </ResponseField>

        <ResponseField name="dataExtensionsDetails" type="DataExtensionsDetails">

          Details identifying field, which is extension of other entity

          <Expandable title="dataExtensionsDetails properties">

            <ResponseField name="fqdns" type="string[]">

              FQDNS which can be extended with this field

              Constraints: `maxLength 100`, `maxItems 10`, `immutable`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="readOnly" type="boolean">

          Whether the field is read only.
          Default: false

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="deletedFormFields" type="Field[]">

      Form fields that were soft deleted.

      **Read-only.** Constraints: `maxItems 150`.

      <Expandable title="deletedFormFields properties">

        <ResponseField name="id" type="string">

          Field ID.

          The ID is stored in lowercase. Because the form's layout references each field by this ID, pass a lowercase GUID so that `id` and the layout's `fieldId` still match after the form schema is stored. IDs must be unique in the form.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the field is hidden from submitters.

          Default: `false`

        </ResponseField>

        <ResponseField name="identifier" type="string">

          Identification for the field, describing its purpose. For example, `TEXT_INPUT` or `CONTACTS_EMAIL`.

          Set this to the known value for the kind of field you're creating.
          An unrecognized value, such as a custom string or a GUID, is dropped rather than rejected: the request still succeeds, but the field doesn't appear in the response's `formFields`, and neither the field nor its submitted values appear in the Wix dashboard.

          Several fields share a general-purpose `componentType`, so `identifier` is often the only value that distinguishes them.
          For example, both a short answer field and a long answer field use the `TEXT_INPUT` component, and are told apart by their `TEXT_INPUT` and `TEXT_AREA` identifiers.

          For the full list of known values, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

        </ResponseField>

        <ResponseField name="fieldType" type="string">

          Field type.

          `INPUT` fields collect data from submitters and are configured in `inputOptions`.
          `DISPLAY` fields show information without collecting data and are configured in `displayOptions`.

          **Supported values:**

          - `UNKNOWN_FIELD_TYPE` — Unknown field type.
          - `INPUT` — Input field that collects data from users.
          - `DISPLAY` — Display field that shows information without collecting input.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Data extensions for storing additional custom properties.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="namespace" type="string">

      Unique identifier for the app that owns this form schema.
      For example, `wix.form_app.form` for Wix Forms.

      See a list of namespaces for [apps made by Wix](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/introduction#namespaces-of-apps-made-by-wix) section.

      Constraints: `minLength 10`, `maxLength 50`, `immutable`.

    </ResponseField>

    <ResponseField name="mediaFolderId" type="string">

      Media folder ID for storing uploaded files from form submissions.

      **Read-only.**

    </ResponseField>

    <ResponseField name="limitationRule" type="LimitationRule">

      Submission limits that can automatically disable the form when reached.

      <Expandable title="limitationRule properties">

        <ResponseField name="maxAllowedSubmissions" type="integer">

          Limitation by submission count, disables form when a set amount of submissions is reached.

        </ResponseField>

        <ResponseField name="dateTimeDeadline" type="datetime">

          Limitation by submission date, disables form when a set date and time is reached.

        </ResponseField>

        <ResponseField name="submissionLimitPerUser" type="integer">

          Limitation per user submission count, disables form when a set amount of submissions per user is reached.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="spamFilterProtectionLevel" type="string">

      Level of spam protection applied to form submissions.

      Default: `ADVANCED`

      **Supported values:**

      - `NONE` — No spam filtering. All submissions are accepted without screening.
      - `BASIC` — Basic spam filtering. Provides minimal protection against obvious spam and the form is open to a high risk of spam submissions.
      - `ADVANCED` — Advanced spam filtering. Provides comprehensive protection with intelligent screening and the form is open to a low risk of spam submissions.

    </ResponseField>

    <ResponseField name="requiredIndicatorProperties" type="RequiredIndicatorProperties">

      Configuration for how required field indicators are displayed to the submitter.

      <Expandable title="requiredIndicatorProperties properties">

        <ResponseField name="requiredIndicator" type="string">

          Type of visual indicator used to mark required fields.

          **Supported values:**

          - `ASTERISK` — Display an asterisk (*) symbol to indicate required fields.
          - `TEXT` — Display the text "Required" to indicate required fields.
          - `NONE` — No visual indicator is shown for required fields.

        </ResponseField>

        <ResponseField name="requiredIndicatorPlacement" type="string">

          Position where the required indicator appears relative to the field title.

          **Supported values:**

          - `AFTER_FIELD_TITLE` — Place the required indicator after the field title.
          - `BEFORE_FIELD_TITLE` — Place the required indicator before the field title.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="submitSettings" type="SubmitSettings">

      Configuration for actions taken after a form is successfully submitted.

      <Expandable title="submitSettings properties">

        <ResponseField name="submitSuccessAction" type="string">

          Action that's triggered after a form is successfully submitted.

          **Supported values:**

          - `NO_ACTION` — No action is taken after form submission.
          - `THANK_YOU_MESSAGE` — Display a thank you message after form submission.
          - `REDIRECT` — Redirect the user to a specified URL after form submission.
          - `POPUP` — Display a popup after form submission.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="submitSuccessActionOptions" type="submitSuccessActionOptions">

          submitSuccessActionOptions

          <Expandable title="submitSuccessActionOptions properties">

            <ResponseField name="thankYouMessageOptions" type="ThankYouMessageOptions">

              Configuration for displaying a thank you message after submission.

              <Expandable title="thankYouMessageOptions properties">

                <ResponseField name="durationInSeconds" type="integer">

                  Duration in seconds before the thank you message automatically disappears.
                  If set to 0, the message remains visible until manually dismissed.

                </ResponseField>

                <ResponseField name="richContent" type="RichContent">

                  Rich content message displayed to users after successful form submission.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="redirectOptions" type="RedirectOptions">

              Configuration for redirecting submitters to a URL after submission.

              <Expandable title="redirectOptions properties">

                <ResponseField name="redirectUrl" type="string">

                  URL to which the user should be redirected after successfully submitting the form.

                </ResponseField>

                <ResponseField name="target" type="string">

                  How the redirect URL should be opened in the browser.

                  **Supported values:**

                  - `UNKNOWN_TARGET` — Unknown target behavior.
                  - `SELF` — Open the URL in the same browser tab, replacing the current page.
                  - `BLANK` — Open the URL in a new browser tab.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="popupOptions" type="PopupOptions">

              Configuration for displaying a popup after submission.

              <Expandable title="popupOptions properties">

                <ResponseField name="popupId" type="string">

                  ID of the site popup to open after successful form submission.

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="disabledFormMessage" type="RichContent">

      Message displayed to visitors when the form is disabled.
      This allows you to inform submitters why the form isn't available.

      <Expandable title="disabledFormMessage properties">

        <ResponseField name="nodes" type="Node[]">

          Node objects representing a rich content document.

          <Expandable title="nodes properties">

            <ResponseField name="type" type="string">

              Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

              **Supported values:**

              - `PARAGRAPH`
              - `TEXT`
              - `HEADING`
              - `BULLETED_LIST`
              - `ORDERED_LIST`
              - `LIST_ITEM`
              - `BLOCKQUOTE`
              - `CODE_BLOCK`
              - `VIDEO`
              - `DIVIDER`
              - `FILE`
              - `GALLERY`
              - `GIF`
              - `HTML`
              - `IMAGE`
              - `LINK_PREVIEW`
              - `MAP` — @deprecated
              - `POLL`
              - `APP_EMBED`
              - `BUTTON`
              - `COLLAPSIBLE_LIST`
              - `TABLE`
              - `EMBED`
              - `COLLAPSIBLE_ITEM`
              - `COLLAPSIBLE_ITEM_TITLE`
              - `COLLAPSIBLE_ITEM_BODY`
              - `TABLE_CELL`
              - `TABLE_ROW`
              - `EXTERNAL`
              - `AUDIO`
              - `CAPTION`
              - `LAYOUT`
              - `LAYOUT_CELL`
              - `SHAPE`
              - `CARD`
              - `TOC`
              - `SMART_BLOCK`
              - `SMART_BLOCK_CELL`
              - `CHECKBOX_LIST`

            </ResponseField>

            <ResponseField name="id" type="string">

              Node ID.

            </ResponseField>

            <ResponseField name="nodes" type="Node[]">

              A list of child nodes.

              _Recursive reference to `Node[]`._

            </ResponseField>

            <ResponseField name="style" type="NodeStyle">

              Padding and background color styling for the node.

              <Expandable title="style properties">

                <ResponseField name="paddingTop" type="string">

                  The top padding value in pixels.

                </ResponseField>

                <ResponseField name="paddingBottom" type="string">

                  The bottom padding value in pixels.

                </ResponseField>

                <ResponseField name="backgroundColor" type="string">

                  The background color as a hexadecimal value.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="metadata" type="Metadata">

          Object metadata.

          <Expandable title="metadata properties">

            <ResponseField name="version" type="integer">

              Schema version.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="createdTimestamp" type="datetime">

              When the object was created.

              **Read-only.**

            </ResponseField>

            <ResponseField name="updatedTimestamp" type="datetime">

              When the object was most recently updated.

            </ResponseField>

            <ResponseField name="id" type="string">

              Object ID.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="documentStyle" type="DocumentStyle">

          Global styling for header, paragraph, block quote, and code block nodes in the object.

          <Expandable title="documentStyle properties">

            <ResponseField name="headerOne" type="TextNodeStyle">

              Styling for H1 nodes.

              <Expandable title="headerOne properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerTwo" type="TextNodeStyle">

              Styling for H2 nodes.

              <Expandable title="headerTwo properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerThree" type="TextNodeStyle">

              Styling for H3 nodes.

              <Expandable title="headerThree properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFour" type="TextNodeStyle">

              Styling for H4 nodes.

              <Expandable title="headerFour properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFive" type="TextNodeStyle">

              Styling for H5 nodes.

              <Expandable title="headerFive properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerSix" type="TextNodeStyle">

              Styling for H6 nodes.

              <Expandable title="headerSix properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="paragraph" type="TextNodeStyle">

              Styling for paragraph nodes.

              <Expandable title="paragraph properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="blockquote" type="TextNodeStyle">

              Styling for block quote nodes.

              <Expandable title="blockquote properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="codeBlock" type="TextNodeStyle">

              Styling for code block nodes.

              <Expandable title="codeBlock properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="enabled" type="boolean">

      Whether the form is effectively active — `true` only when the user has not disabled the form
      **and** no system service has placed an `activationBlock` on it.

      Writing `enabled: true` records the user's intent to activate the form, but the value read
      back may still be `false` if `activationBlock` is present (for example, a plan restriction is in
      effect). Once the block is lifted the form becomes active automatically without any additional
      write.

      Default: `true`

    </ResponseField>

    <ResponseField name="name" type="string">

      Display name of the form schema.

      Names are unique in a namespace. If the name you pass is already taken, the request still succeeds, and the form schema is stored under a numbered variation of that name instead of returning an error. Read `name` from the response to learn the name the schema was actually saved under.

    </ResponseField>

    <ResponseField name="formRules" type="Rule[]">

      Business logic rules that can modify form behavior based on user input or other conditions.
      These rules can show/hide fields, change validation, or trigger actions.

      Constraints: `maxItems 100`.

      <Expandable title="formRules properties">

        <ResponseField name="id" type="string">

          Rule ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="expression" type="ConditionNode">

          Conditional expression that determines when this rule should be applied.
          The overrides defined in this rule are applied when this expression evaluates to true.

          <Expandable title="One of the following">

            <ResponseField name="node" type="node">

              node

              <Expandable title="node properties">

                <ResponseField name="and" type="AndCondition">

                  Logical AND condition with 1 or more child conditions. All child conditions must be true.

                  _Nested structure omitted for brevity — see the `AndCondition` object._

                </ResponseField>

                <ResponseField name="or" type="OrCondition">

                  Logical OR condition with 1 or more child conditions. At least 1 child condition must be true.

                  _Nested structure omitted for brevity — see the `OrCondition` object._

                </ResponseField>

                <ResponseField name="condition" type="Condition">

                  Single condition to evaluate.

                  _Nested structure omitted for brevity — see the `Condition` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="overrides" type="FormOverride[]">

          Form modifications that are applied when the rule expression evaluates to true.
          Each override specifies how a form element should be changed.

          Constraints: `maxItems 500`.

          <Expandable title="overrides properties">

            <ResponseField name="entityType" type="string">

              Type of form element being modified by this override.

              Constraints: `immutable`.

              **Supported values:**

              - `FIELD` — Override applies to a form field.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="name" type="string">

          Human-readable name for the rule.

        </ResponseField>

        <ResponseField name="namespaceRule" type="boolean">

          Namespace rules are part of business logic of app owning namespace, have limited editing possibilities

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="tags" type="Tags">

      Tag IDs collections associated with the current entity.

      <Expandable title="tags properties">

        <ResponseField name="privateTags" type="TagList">

          Tags that require an additional permission in order to access them, normally not given to site members or visitors.

          <Expandable title="privateTags properties">

            <ResponseField name="tagIds" type="string[]">

              List of tag IDs

              Constraints: `maxLength 5`, `maxItems 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tags" type="TagList">

          Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

          <Expandable title="tags properties">

            <ResponseField name="tagIds" type="string[]">

              List of tag IDs

              Constraints: `maxLength 5`, `maxItems 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="quizSettings" type="QuizSettings">

      Quiz configuration for a scored form. Set this when the form is a quiz whose
      answers are scored: it holds the passing criteria (score threshold in points or
      percentage), how the result is shown on the confirmation page (score, pass/fail
      status, or both), and the pass/fail messages. Absent on non-quiz forms.

      <Expandable title="quizSettings properties">

        <ResponseField name="message" type="RichContent">

          Rich-content message shown on the quiz result (confirmation) page, alongside any
          score or pass/fail status configured in `results_display`.

          <Expandable title="message properties">

            <ResponseField name="nodes" type="Node[]">

              Node objects representing a rich content document.

              <Expandable title="nodes properties">

                <ResponseField name="type" type="string">

                  Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

                  **Supported values:**

                  - `PARAGRAPH`
                  - `TEXT`
                  - `HEADING`
                  - `BULLETED_LIST`
                  - `ORDERED_LIST`
                  - `LIST_ITEM`
                  - `BLOCKQUOTE`
                  - `CODE_BLOCK`
                  - `VIDEO`
                  - `DIVIDER`
                  - `FILE`
                  - `GALLERY`
                  - `GIF`
                  - `HTML`
                  - `IMAGE`
                  - `LINK_PREVIEW`
                  - `MAP` — @deprecated
                  - `POLL`
                  - `APP_EMBED`
                  - `BUTTON`
                  - `COLLAPSIBLE_LIST`
                  - `TABLE`
                  - `EMBED`
                  - `COLLAPSIBLE_ITEM`
                  - `COLLAPSIBLE_ITEM_TITLE`
                  - `COLLAPSIBLE_ITEM_BODY`
                  - `TABLE_CELL`
                  - `TABLE_ROW`
                  - `EXTERNAL`
                  - `AUDIO`
                  - `CAPTION`
                  - `LAYOUT`
                  - `LAYOUT_CELL`
                  - `SHAPE`
                  - `CARD`
                  - `TOC`
                  - `SMART_BLOCK`
                  - `SMART_BLOCK_CELL`
                  - `CHECKBOX_LIST`

                </ResponseField>

                <ResponseField name="id" type="string">

                  Node ID.

                </ResponseField>

                <ResponseField name="nodes" type="Node[]">

                  A list of child nodes.

                  _Recursive reference to `Node[]`._

                </ResponseField>

                <ResponseField name="style" type="NodeStyle">

                  Padding and background color styling for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="metadata" type="Metadata">

              Object metadata.

              <Expandable title="metadata properties">

                <ResponseField name="version" type="integer">

                  Schema version.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="createdTimestamp" type="datetime">

                  When the object was created.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="updatedTimestamp" type="datetime">

                  When the object was most recently updated.

                </ResponseField>

                <ResponseField name="id" type="string">

                  Object ID.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              <Expandable title="documentStyle properties">

                <ResponseField name="headerOne" type="TextNodeStyle">

                  Styling for H1 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerTwo" type="TextNodeStyle">

                  Styling for H2 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerThree" type="TextNodeStyle">

                  Styling for H3 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFour" type="TextNodeStyle">

                  Styling for H4 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFive" type="TextNodeStyle">

                  Styling for H5 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerSix" type="TextNodeStyle">

                  Styling for H6 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="paragraph" type="TextNodeStyle">

                  Styling for paragraph nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="blockquote" type="TextNodeStyle">

                  Styling for block quote nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="codeBlock" type="TextNodeStyle">

                  Styling for code block nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="resultsDisplay" type="string">

          How to display quiz results in the confirmation page.
          Default: `DO_NOT_DISPLAY`

          **Supported values:**

          - `DO_NOT_DISPLAY` — Do not show quiz score and passing status.
          - `SCORE` — Display only the user's score.
          - `PASSING_STATUS` — Display the pass or fail message based on the user's score.
          - `SCORE_AND_PASSING_STATUS` — Display both the score and pass/fail message.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="passingCriteria" type="passingCriteria">

          passingCriteria

          <Expandable title="passingCriteria properties">

            <ResponseField name="passingScorePoints" type="number">

              Score required to pass the quiz (in points).

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="passingScorePercentage" type="number">

              Score required to pass the quiz as a percentage (0-100).

              Constraints: `min 0`, `max 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="resultsDisplayOptions" type="resultsDisplayOptions">

          resultsDisplayOptions

          <Expandable title="resultsDisplayOptions properties">

            <ResponseField name="passingStatusOptions" type="PassFailMessages">

              Configuration for displaying only the pass/fail message.

              <Expandable title="passingStatusOptions properties">

                <ResponseField name="passMessage" type="RichContent">

                  Message displayed when the user passes the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

                <ResponseField name="failMessage" type="RichContent">

                  Message displayed when the user fails the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="scoreAndPassingStatusOptions" type="PassFailMessages">

              Configuration for displaying both the score and pass/fail message.

              <Expandable title="scoreAndPassingStatusOptions properties">

                <ResponseField name="passMessage" type="RichContent">

                  Message displayed when the user passes the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

                <ResponseField name="failMessage" type="RichContent">

                  Message displayed when the user fails the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="autoFillContact" type="string">

      Source used for contact autofill when filling form fields. Default: FORM_INPUT.

      **Supported values:**

      - `MEMBER_DATA` — Use site member data to autofill contact fields.
      - `FORM_INPUT` — Use form input data to autofill contact fields.

    </ResponseField>

    <ResponseField name="submissionAccess" type="string">

      Controls who can read submissions of this form beyond the standard permission.

      Default: `OWNER_AND_COLLABORATORS`

      **Supported values:**

      - `UNKNOWN_SUBMISSION_ACCESS` — Unknown submission access.
      - `OWNER_AND_COLLABORATORS` — Only users with `WIX_FORM.SUBMISSIONS_READ_ANY` permission. Default behavior.
      - `MEMBERS` — Logged-in site members can read submissions.
      - `PUBLIC` — Any site visitor with a valid Wix session can read submissions.

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Form schema ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="formFields" type="Field[]">

      Form fields that define the input elements and their validation rules.

      The order of this array isn't the order the fields are displayed in. Display order comes from `steps`, where each layout item positions a field by `row` and `column`. Don't render fields in array order.

      For a detailed explanation of how to compose each kind of field, including which `identifier`, `inputType`, and `componentType` values to combine, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

      Constraints: `maxItems 500`.

      <Expandable title="formFields properties">

        <ResponseField name="_id" type="string">

          Field ID.

          The ID is stored in lowercase. Because the form's layout references each field by this ID, pass a lowercase GUID so that `id` and the layout's `fieldId` still match after the form schema is stored. IDs must be unique in the form.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the field is hidden from submitters.

          Default: `false`

        </ResponseField>

        <ResponseField name="identifier" type="string">

          Identification for the field, describing its purpose. For example, `TEXT_INPUT` or `CONTACTS_EMAIL`.

          Set this to the known value for the kind of field you're creating.
          An unrecognized value, such as a custom string or a GUID, is dropped rather than rejected: the request still succeeds, but the field doesn't appear in the response's `formFields`, and neither the field nor its submitted values appear in the Wix dashboard.

          Several fields share a general-purpose `componentType`, so `identifier` is often the only value that distinguishes them.
          For example, both a short answer field and a long answer field use the `TEXT_INPUT` component, and are told apart by their `TEXT_INPUT` and `TEXT_AREA` identifiers.

          For the full list of known values, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

        </ResponseField>

        <ResponseField name="fieldType" type="string">

          Field type.

          `INPUT` fields collect data from submitters and are configured in `inputOptions`.
          `DISPLAY` fields show information without collecting data and are configured in `displayOptions`.

          **Supported values:**

          - `UNKNOWN_FIELD_TYPE` — Unknown field type.
          - `INPUT` — Input field that collects data from users.
          - `DISPLAY` — Display field that shows information without collecting input.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="steps" type="Step[]">

      Layout configuration that defines how form fields are organized across multiple steps.
      Each step represents a page of the form.

      Every field in `formFields`, including the submit button, must be placed by a layout item whose `fieldId` matches the field's `id`. A field that isn't placed still stores submitted values, but the Wix dashboard renders it, and the submissions made against it, as empty.

      Use a single step unless the form is split across pages.

      Constraints: `maxItems 100`.

      <Expandable title="steps properties">

        <ResponseField name="_id" type="string">

          Step ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="name" type="string">

          Name of the step.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the step is hidden.

        </ResponseField>

        <ResponseField name="layout" type="FormLayout">

          Layout configuration that defines how form fields are arranged within this step.

          <Expandable title="layout properties">

            <ResponseField name="large" type="BreakPoint">

              Layout configuration for large screen breakpoints (desktop).

              <Expandable title="large properties">

                <ResponseField name="items" type="ItemLayout[]">

                  Configuration for the layout of items in the breakpoint.
                  Every field of the step must have exactly 1 corresponding item in each breakpoint that is defined.

                  Constraints: `maxItems 500`.

                  _Nested structure omitted for brevity — see the `ItemLayout[]` object._

                </ResponseField>

                <ResponseField name="columns" type="integer">

                  Number of columns in the layout grid.

                </ResponseField>

                <ResponseField name="rowHeight" type="integer">

                  Height of each row in the layout grid, measured in pixels.

                </ResponseField>

                <ResponseField name="margin" type="Margin">

                  Margin spacing configuration for the form elements.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

                <ResponseField name="padding" type="Margin">

                  Padding spacing inside form elements in pixels.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="medium" type="BreakPoint">

              Layout configuration for medium screen breakpoints (tablet).

              <Expandable title="medium properties">

                <ResponseField name="items" type="ItemLayout[]">

                  Configuration for the layout of items in the breakpoint.
                  Every field of the step must have exactly 1 corresponding item in each breakpoint that is defined.

                  Constraints: `maxItems 500`.

                  _Nested structure omitted for brevity — see the `ItemLayout[]` object._

                </ResponseField>

                <ResponseField name="columns" type="integer">

                  Number of columns in the layout grid.

                </ResponseField>

                <ResponseField name="rowHeight" type="integer">

                  Height of each row in the layout grid, measured in pixels.

                </ResponseField>

                <ResponseField name="margin" type="Margin">

                  Margin spacing configuration for the form elements.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

                <ResponseField name="padding" type="Margin">

                  Padding spacing inside form elements in pixels.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="small" type="BreakPoint">

              Layout configuration for small screen breakpoints (mobile).

              <Expandable title="small properties">

                <ResponseField name="items" type="ItemLayout[]">

                  Configuration for the layout of items in the breakpoint.
                  Every field of the step must have exactly 1 corresponding item in each breakpoint that is defined.

                  Constraints: `maxItems 500`.

                  _Nested structure omitted for brevity — see the `ItemLayout[]` object._

                </ResponseField>

                <ResponseField name="columns" type="integer">

                  Number of columns in the layout grid.

                </ResponseField>

                <ResponseField name="rowHeight" type="integer">

                  Height of each row in the layout grid, measured in pixels.

                </ResponseField>

                <ResponseField name="margin" type="Margin">

                  Margin spacing configuration for the form elements.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

                <ResponseField name="padding" type="Margin">

                  Padding spacing inside form elements in pixels.

                  _Nested structure omitted for brevity — see the `Margin` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="rules" type="FormRule[]" deprecated>

      Form rules, can be applied to layout and items properties.

      **Deprecated.** Form rules, can be applied to layout and items properties. Use `form_rules` instead. Removal: 2026-04-01. Constraints: `maxItems 100`.

      <Expandable title="rules properties">

        <ResponseField name="_id" type="string">

          Rule ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="condition" type="object">

          Condition that determines when this rule's overrides should be applied.

        </ResponseField>

        <ResponseField name="overrides" type="FormOverride[]">

          Changes to apply to form elements when the condition evaluates to true.

          Constraints: `maxItems 500`.

          <Expandable title="overrides properties">

            <ResponseField name="entityType" type="string">

              Type of form element that this override targets.

              Constraints: `immutable`.

              **Supported values:**

              - `FIELD` — Override applies to a form field.

            </ResponseField>

            <ResponseField name="entityId" type="string">

              ID of the form element to modify.
              For nested form fields, use the format: "&#123;fieldIdWithNestedForm&#125;/&#123;nestedFormFieldId&#125;".

            </ResponseField>

            <ResponseField name="valueChanges" type="object">

              Property changes to apply to the form element when the rule condition is met.
              Each key represents a property path, and the value is the new value to set.

              Constraints: `format map`.

              <Expandable title="valueChanges properties">

                <ResponseField name="key" type="string" />

                <ResponseField name="value" type="any" />

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="name" type="string">

          Name for the rule.

        </ResponseField>

        <ResponseField name="namespaceRule" type="boolean">

          Namespace rules are part of business logic of app owning namespace, have limited editing possibilities

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the form schema is updated.
      To prevent conflicting changes, the current revision must be passed when updating the form schema.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time when the form schema was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time when the form schema was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="deletedFields" type="FormField[]" deprecated>

      Deprecated. Use `deletedFormFields` instead.

      **Read-only.** **Deprecated.** Deprecated. Use `deletedFormFields` instead. Use `deleted_form_fields` instead. Removal: 2027-02-01. Constraints: `maxItems 150`.

      <Expandable title="deletedFields properties">

        <ResponseField name="_id" type="string">

          Item ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="target" type="string">

          Definition of a target where the value of field belongs.

        </ResponseField>

        <ResponseField name="validation" type="Validation">

          Validation of field output value.

          <Expandable title="validation properties">

            <ResponseField name="required" type="boolean">

              Whether the field is required.

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="validation" type="validation">

              validation

              <Expandable title="validation properties">

                <ResponseField name="string" type="StringType">

                  Validation of string type.

                  _Nested structure omitted for brevity — see the `StringType` object._

                </ResponseField>

                <ResponseField name="number" type="NumberType">

                  Validation of number type.

                  _Nested structure omitted for brevity — see the `NumberType` object._

                </ResponseField>

                <ResponseField name="integer" type="IntegerType">

                  Validation of integer type.

                  _Nested structure omitted for brevity — see the `IntegerType` object._

                </ResponseField>

                <ResponseField name="boolean" type="BooleanType">

                  Validation of boolean type.

                  _Nested structure omitted for brevity — see the `BooleanType` object._

                </ResponseField>

                <ResponseField name="array" type="ArrayType">

                  Validation of array type.

                  _Nested structure omitted for brevity — see the `ArrayType` object._

                </ResponseField>

                <ResponseField name="object" type="ObjectType">

                  Validation of object type.

                  _Nested structure omitted for brevity — see the `ObjectType` object._

                </ResponseField>

                <ResponseField name="predefined" type="PredefinedValidation">

                  Predefined validation of specific format

                  _Nested structure omitted for brevity — see the `PredefinedValidation` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pii" type="boolean">

          Mark the field as containing personal information. This will encrypt user data when storing it.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the field is hidden.

        </ResponseField>

        <ResponseField name="view" type="object">

          Field view properties.

        </ResponseField>

        <ResponseField name="dataExtensionsDetails" type="DataExtensionsDetails">

          Details identifying field, which is extension of other entity

          <Expandable title="dataExtensionsDetails properties">

            <ResponseField name="fqdns" type="string[]">

              FQDNS which can be extended with this field

              Constraints: `maxLength 100`, `maxItems 10`, `immutable`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="readOnly" type="boolean">

          Whether the field is read only.
          Default: false

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="deletedFormFields" type="Field[]">

      Form fields that were soft deleted.

      **Read-only.** Constraints: `maxItems 150`.

      <Expandable title="deletedFormFields properties">

        <ResponseField name="_id" type="string">

          Field ID.

          The ID is stored in lowercase. Because the form's layout references each field by this ID, pass a lowercase GUID so that `id` and the layout's `fieldId` still match after the form schema is stored. IDs must be unique in the form.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="hidden" type="boolean">

          Whether the field is hidden from submitters.

          Default: `false`

        </ResponseField>

        <ResponseField name="identifier" type="string">

          Identification for the field, describing its purpose. For example, `TEXT_INPUT` or `CONTACTS_EMAIL`.

          Set this to the known value for the kind of field you're creating.
          An unrecognized value, such as a custom string or a GUID, is dropped rather than rejected: the request still succeeds, but the field doesn't appear in the response's `formFields`, and neither the field nor its submitted values appear in the Wix dashboard.

          Several fields share a general-purpose `componentType`, so `identifier` is often the only value that distinguishes them.
          For example, both a short answer field and a long answer field use the `TEXT_INPUT` component, and are told apart by their `TEXT_INPUT` and `TEXT_AREA` identifiers.

          For the full list of known values, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

        </ResponseField>

        <ResponseField name="fieldType" type="string">

          Field type.

          `INPUT` fields collect data from submitters and are configured in `inputOptions`.
          `DISPLAY` fields show information without collecting data and are configured in `displayOptions`.

          **Supported values:**

          - `UNKNOWN_FIELD_TYPE` — Unknown field type.
          - `INPUT` — Input field that collects data from users.
          - `DISPLAY` — Display field that shows information without collecting input.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Data extensions for storing additional custom properties.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="namespace" type="string">

      Unique identifier for the app that owns this form schema.
      For example, `wix.form_app.form` for Wix Forms.

      See a list of namespaces for [apps made by Wix](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/introduction#namespaces-of-apps-made-by-wix) section.

      Constraints: `minLength 10`, `maxLength 50`, `immutable`.

    </ResponseField>

    <ResponseField name="mediaFolderId" type="string">

      Media folder ID for storing uploaded files from form submissions.

      **Read-only.**

    </ResponseField>

    <ResponseField name="limitationRule" type="LimitationRule">

      Submission limits that can automatically disable the form when reached.

      <Expandable title="limitationRule properties">

        <ResponseField name="maxAllowedSubmissions" type="integer">

          Limitation by submission count, disables form when a set amount of submissions is reached.

        </ResponseField>

        <ResponseField name="dateTimeDeadline" type="datetime">

          Limitation by submission date, disables form when a set date and time is reached.

        </ResponseField>

        <ResponseField name="submissionLimitPerUser" type="integer">

          Limitation per user submission count, disables form when a set amount of submissions per user is reached.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="spamFilterProtectionLevel" type="string">

      Level of spam protection applied to form submissions.

      Default: `ADVANCED`

      **Supported values:**

      - `NONE` — No spam filtering. All submissions are accepted without screening.
      - `BASIC` — Basic spam filtering. Provides minimal protection against obvious spam and the form is open to a high risk of spam submissions.
      - `ADVANCED` — Advanced spam filtering. Provides comprehensive protection with intelligent screening and the form is open to a low risk of spam submissions.

    </ResponseField>

    <ResponseField name="requiredIndicatorProperties" type="RequiredIndicatorProperties">

      Configuration for how required field indicators are displayed to the submitter.

      <Expandable title="requiredIndicatorProperties properties">

        <ResponseField name="requiredIndicator" type="string">

          Type of visual indicator used to mark required fields.

          **Supported values:**

          - `ASTERISK` — Display an asterisk (*) symbol to indicate required fields.
          - `TEXT` — Display the text "Required" to indicate required fields.
          - `NONE` — No visual indicator is shown for required fields.

        </ResponseField>

        <ResponseField name="requiredIndicatorPlacement" type="string">

          Position where the required indicator appears relative to the field title.

          **Supported values:**

          - `AFTER_FIELD_TITLE` — Place the required indicator after the field title.
          - `BEFORE_FIELD_TITLE` — Place the required indicator before the field title.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="submitSettings" type="SubmitSettings">

      Configuration for actions taken after a form is successfully submitted.

      <Expandable title="submitSettings properties">

        <ResponseField name="submitSuccessAction" type="string">

          Action that's triggered after a form is successfully submitted.

          **Supported values:**

          - `NO_ACTION` — No action is taken after form submission.
          - `THANK_YOU_MESSAGE` — Display a thank you message after form submission.
          - `REDIRECT` — Redirect the user to a specified URL after form submission.
          - `POPUP` — Display a popup after form submission.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="submitSuccessActionOptions" type="submitSuccessActionOptions">

          submitSuccessActionOptions

          <Expandable title="submitSuccessActionOptions properties">

            <ResponseField name="thankYouMessageOptions" type="ThankYouMessageOptions">

              Configuration for displaying a thank you message after submission.

              <Expandable title="thankYouMessageOptions properties">

                <ResponseField name="durationInSeconds" type="integer">

                  Duration in seconds before the thank you message automatically disappears.
                  If set to 0, the message remains visible until manually dismissed.

                </ResponseField>

                <ResponseField name="richContent" type="RichContent">

                  Rich content message displayed to users after successful form submission.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="redirectOptions" type="RedirectOptions">

              Configuration for redirecting submitters to a URL after submission.

              <Expandable title="redirectOptions properties">

                <ResponseField name="redirectUrl" type="string">

                  URL to which the user should be redirected after successfully submitting the form.

                </ResponseField>

                <ResponseField name="target" type="string">

                  How the redirect URL should be opened in the browser.

                  **Supported values:**

                  - `UNKNOWN_TARGET` — Unknown target behavior.
                  - `SELF` — Open the URL in the same browser tab, replacing the current page.
                  - `BLANK` — Open the URL in a new browser tab.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="popupOptions" type="PopupOptions">

              Configuration for displaying a popup after submission.

              <Expandable title="popupOptions properties">

                <ResponseField name="popupId" type="string">

                  ID of the site popup to open after successful form submission.

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="disabledFormMessage" type="RichContent">

      Message displayed to visitors when the form is disabled.
      This allows you to inform submitters why the form isn't available.

      <Expandable title="disabledFormMessage properties">

        <ResponseField name="nodes" type="Node[]">

          Node objects representing a rich content document.

          <Expandable title="nodes properties">

            <ResponseField name="type" type="string">

              Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

              **Supported values:**

              - `PARAGRAPH`
              - `TEXT`
              - `HEADING`
              - `BULLETED_LIST`
              - `ORDERED_LIST`
              - `LIST_ITEM`
              - `BLOCKQUOTE`
              - `CODE_BLOCK`
              - `VIDEO`
              - `DIVIDER`
              - `FILE`
              - `GALLERY`
              - `GIF`
              - `HTML`
              - `IMAGE`
              - `LINK_PREVIEW`
              - `MAP` — @deprecated
              - `POLL`
              - `APP_EMBED`
              - `BUTTON`
              - `COLLAPSIBLE_LIST`
              - `TABLE`
              - `EMBED`
              - `COLLAPSIBLE_ITEM`
              - `COLLAPSIBLE_ITEM_TITLE`
              - `COLLAPSIBLE_ITEM_BODY`
              - `TABLE_CELL`
              - `TABLE_ROW`
              - `EXTERNAL`
              - `AUDIO`
              - `CAPTION`
              - `LAYOUT`
              - `LAYOUT_CELL`
              - `SHAPE`
              - `CARD`
              - `TOC`
              - `SMART_BLOCK`
              - `SMART_BLOCK_CELL`
              - `CHECKBOX_LIST`

            </ResponseField>

            <ResponseField name="_id" type="string">

              Node ID.

            </ResponseField>

            <ResponseField name="nodes" type="Node[]">

              A list of child nodes.

              _Recursive reference to `Node[]`._

            </ResponseField>

            <ResponseField name="style" type="NodeStyle">

              Padding and background color styling for the node.

              <Expandable title="style properties">

                <ResponseField name="paddingTop" type="string">

                  The top padding value in pixels.

                </ResponseField>

                <ResponseField name="paddingBottom" type="string">

                  The bottom padding value in pixels.

                </ResponseField>

                <ResponseField name="backgroundColor" type="string">

                  The background color as a hexadecimal value.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="metadata" type="Metadata">

          Object metadata.

          <Expandable title="metadata properties">

            <ResponseField name="version" type="integer">

              Schema version.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="createdTimestamp" type="datetime">

              When the object was created.

              **Read-only.**

            </ResponseField>

            <ResponseField name="updatedTimestamp" type="datetime">

              When the object was most recently updated.

            </ResponseField>

            <ResponseField name="_id" type="string">

              Object ID.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="documentStyle" type="DocumentStyle">

          Global styling for header, paragraph, block quote, and code block nodes in the object.

          <Expandable title="documentStyle properties">

            <ResponseField name="headerOne" type="TextNodeStyle">

              Styling for H1 nodes.

              <Expandable title="headerOne properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerTwo" type="TextNodeStyle">

              Styling for H2 nodes.

              <Expandable title="headerTwo properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerThree" type="TextNodeStyle">

              Styling for H3 nodes.

              <Expandable title="headerThree properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFour" type="TextNodeStyle">

              Styling for H4 nodes.

              <Expandable title="headerFour properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerFive" type="TextNodeStyle">

              Styling for H5 nodes.

              <Expandable title="headerFive properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="headerSix" type="TextNodeStyle">

              Styling for H6 nodes.

              <Expandable title="headerSix properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="paragraph" type="TextNodeStyle">

              Styling for paragraph nodes.

              <Expandable title="paragraph properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="blockquote" type="TextNodeStyle">

              Styling for block quote nodes.

              <Expandable title="blockquote properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="codeBlock" type="TextNodeStyle">

              Styling for code block nodes.

              <Expandable title="codeBlock properties">

                <ResponseField name="decorations" type="Decoration[]">

                  The decorations to apply to the node.

                  _Nested structure omitted for brevity — see the `Decoration[]` object._

                </ResponseField>

                <ResponseField name="nodeStyle" type="NodeStyle">

                  Padding and background color for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

                <ResponseField name="lineHeight" type="string">

                  Line height for text in the node.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="enabled" type="boolean">

      Whether the form is effectively active — `true` only when the user has not disabled the form
      **and** no system service has placed an `activationBlock` on it.

      Writing `enabled: true` records the user's intent to activate the form, but the value read
      back may still be `false` if `activationBlock` is present (for example, a plan restriction is in
      effect). Once the block is lifted the form becomes active automatically without any additional
      write.

      Default: `true`

    </ResponseField>

    <ResponseField name="name" type="string">

      Display name of the form schema.

      Names are unique in a namespace. If the name you pass is already taken, the request still succeeds, and the form schema is stored under a numbered variation of that name instead of returning an error. Read `name` from the response to learn the name the schema was actually saved under.

    </ResponseField>

    <ResponseField name="formRules" type="Rule[]">

      Business logic rules that can modify form behavior based on user input or other conditions.
      These rules can show/hide fields, change validation, or trigger actions.

      Constraints: `maxItems 100`.

      <Expandable title="formRules properties">

        <ResponseField name="_id" type="string">

          Rule ID.

          Constraints: `format GUID`, `immutable`.

        </ResponseField>

        <ResponseField name="expression" type="ConditionNode">

          Conditional expression that determines when this rule should be applied.
          The overrides defined in this rule are applied when this expression evaluates to true.

          <Expandable title="One of the following">

            <ResponseField name="node" type="node">

              node

              <Expandable title="node properties">

                <ResponseField name="and" type="AndCondition">

                  Logical AND condition with 1 or more child conditions. All child conditions must be true.

                  _Nested structure omitted for brevity — see the `AndCondition` object._

                </ResponseField>

                <ResponseField name="or" type="OrCondition">

                  Logical OR condition with 1 or more child conditions. At least 1 child condition must be true.

                  _Nested structure omitted for brevity — see the `OrCondition` object._

                </ResponseField>

                <ResponseField name="condition" type="Condition">

                  Single condition to evaluate.

                  _Nested structure omitted for brevity — see the `Condition` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="overrides" type="FormOverride[]">

          Form modifications that are applied when the rule expression evaluates to true.
          Each override specifies how a form element should be changed.

          Constraints: `maxItems 500`.

          <Expandable title="overrides properties">

            <ResponseField name="entityType" type="string">

              Type of form element being modified by this override.

              Constraints: `immutable`.

              **Supported values:**

              - `FIELD` — Override applies to a form field.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="name" type="string">

          Human-readable name for the rule.

        </ResponseField>

        <ResponseField name="namespaceRule" type="boolean">

          Namespace rules are part of business logic of app owning namespace, have limited editing possibilities

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="tags" type="Tags">

      Tag IDs collections associated with the current entity.

      <Expandable title="tags properties">

        <ResponseField name="privateTags" type="TagList">

          Tags that require an additional permission in order to access them, normally not given to site members or visitors.

          <Expandable title="privateTags properties">

            <ResponseField name="tagIds" type="string[]">

              List of tag IDs

              Constraints: `maxLength 5`, `maxItems 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tags" type="TagList">

          Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

          <Expandable title="tags properties">

            <ResponseField name="tagIds" type="string[]">

              List of tag IDs

              Constraints: `maxLength 5`, `maxItems 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="quizSettings" type="QuizSettings">

      Quiz configuration for a scored form. Set this when the form is a quiz whose
      answers are scored: it holds the passing criteria (score threshold in points or
      percentage), how the result is shown on the confirmation page (score, pass/fail
      status, or both), and the pass/fail messages. Absent on non-quiz forms.

      <Expandable title="quizSettings properties">

        <ResponseField name="message" type="RichContent">

          Rich-content message shown on the quiz result (confirmation) page, alongside any
          score or pass/fail status configured in `results_display`.

          <Expandable title="message properties">

            <ResponseField name="nodes" type="Node[]">

              Node objects representing a rich content document.

              <Expandable title="nodes properties">

                <ResponseField name="type" type="string">

                  Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format.

                  **Supported values:**

                  - `PARAGRAPH`
                  - `TEXT`
                  - `HEADING`
                  - `BULLETED_LIST`
                  - `ORDERED_LIST`
                  - `LIST_ITEM`
                  - `BLOCKQUOTE`
                  - `CODE_BLOCK`
                  - `VIDEO`
                  - `DIVIDER`
                  - `FILE`
                  - `GALLERY`
                  - `GIF`
                  - `HTML`
                  - `IMAGE`
                  - `LINK_PREVIEW`
                  - `MAP` — @deprecated
                  - `POLL`
                  - `APP_EMBED`
                  - `BUTTON`
                  - `COLLAPSIBLE_LIST`
                  - `TABLE`
                  - `EMBED`
                  - `COLLAPSIBLE_ITEM`
                  - `COLLAPSIBLE_ITEM_TITLE`
                  - `COLLAPSIBLE_ITEM_BODY`
                  - `TABLE_CELL`
                  - `TABLE_ROW`
                  - `EXTERNAL`
                  - `AUDIO`
                  - `CAPTION`
                  - `LAYOUT`
                  - `LAYOUT_CELL`
                  - `SHAPE`
                  - `CARD`
                  - `TOC`
                  - `SMART_BLOCK`
                  - `SMART_BLOCK_CELL`
                  - `CHECKBOX_LIST`

                </ResponseField>

                <ResponseField name="_id" type="string">

                  Node ID.

                </ResponseField>

                <ResponseField name="nodes" type="Node[]">

                  A list of child nodes.

                  _Recursive reference to `Node[]`._

                </ResponseField>

                <ResponseField name="style" type="NodeStyle">

                  Padding and background color styling for the node.

                  _Nested structure omitted for brevity — see the `NodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="metadata" type="Metadata">

              Object metadata.

              <Expandable title="metadata properties">

                <ResponseField name="version" type="integer">

                  Schema version.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="createdTimestamp" type="datetime">

                  When the object was created.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="updatedTimestamp" type="datetime">

                  When the object was most recently updated.

                </ResponseField>

                <ResponseField name="_id" type="string">

                  Object ID.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              <Expandable title="documentStyle properties">

                <ResponseField name="headerOne" type="TextNodeStyle">

                  Styling for H1 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerTwo" type="TextNodeStyle">

                  Styling for H2 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerThree" type="TextNodeStyle">

                  Styling for H3 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFour" type="TextNodeStyle">

                  Styling for H4 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerFive" type="TextNodeStyle">

                  Styling for H5 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="headerSix" type="TextNodeStyle">

                  Styling for H6 nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="paragraph" type="TextNodeStyle">

                  Styling for paragraph nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="blockquote" type="TextNodeStyle">

                  Styling for block quote nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

                <ResponseField name="codeBlock" type="TextNodeStyle">

                  Styling for code block nodes.

                  _Nested structure omitted for brevity — see the `TextNodeStyle` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="resultsDisplay" type="string">

          How to display quiz results in the confirmation page.
          Default: `DO_NOT_DISPLAY`

          **Supported values:**

          - `DO_NOT_DISPLAY` — Do not show quiz score and passing status.
          - `SCORE` — Display only the user's score.
          - `PASSING_STATUS` — Display the pass or fail message based on the user's score.
          - `SCORE_AND_PASSING_STATUS` — Display both the score and pass/fail message.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="passingCriteria" type="passingCriteria">

          passingCriteria

          <Expandable title="passingCriteria properties">

            <ResponseField name="passingScorePoints" type="number">

              Score required to pass the quiz (in points).

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="passingScorePercentage" type="number">

              Score required to pass the quiz as a percentage (0-100).

              Constraints: `min 0`, `max 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="resultsDisplayOptions" type="resultsDisplayOptions">

          resultsDisplayOptions

          <Expandable title="resultsDisplayOptions properties">

            <ResponseField name="passingStatusOptions" type="PassFailMessages">

              Configuration for displaying only the pass/fail message.

              <Expandable title="passingStatusOptions properties">

                <ResponseField name="passMessage" type="RichContent">

                  Message displayed when the user passes the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

                <ResponseField name="failMessage" type="RichContent">

                  Message displayed when the user fails the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="scoreAndPassingStatusOptions" type="PassFailMessages">

              Configuration for displaying both the score and pass/fail message.

              <Expandable title="scoreAndPassingStatusOptions properties">

                <ResponseField name="passMessage" type="RichContent">

                  Message displayed when the user passes the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

                <ResponseField name="failMessage" type="RichContent">

                  Message displayed when the user fails the quiz.

                  _Nested structure omitted for brevity — see the `RichContent` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="autoFillContact" type="string">

      Source used for contact autofill when filling form fields. Default: FORM_INPUT.

      **Supported values:**

      - `MEMBER_DATA` — Use site member data to autofill contact fields.
      - `FORM_INPUT` — Use form input data to autofill contact fields.

    </ResponseField>

    <ResponseField name="submissionAccess" type="string">

      Controls who can read submissions of this form beyond the standard permission.

      Default: `OWNER_AND_COLLABORATORS`

      **Supported values:**

      - `UNKNOWN_SUBMISSION_ACCESS` — Unknown submission access.
      - `OWNER_AND_COLLABORATORS` — Only users with `WIX_FORM.SUBMISSIONS_READ_ANY` permission. Default behavior.
      - `MEMBERS` — Logged-in site members can read submissions.
      - `PUBLIC` — Any site visitor with a valid Wix session can read submissions.

    </ResponseField>

  </Tab>

</Tabs>
