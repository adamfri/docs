---
title: "Upsert Marketing Settings"
description: "Creates or updates the site's marketing settings."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates or updates the site's marketing settings.

    If no settings exist yet, this method creates them. Otherwise it updates only the fields listed
    in `fieldMask`; all other fields are left unchanged. Both `marketing_settings` and `field_mask`
    are required. Every path listed in `fieldMask` must be present in the `marketingSettings` you
    send; listing a path that isn't present returns a `FAILED_PRECONDITION` error. To clear a
    field, include it in `fieldMask` and send it explicitly as null or empty.

    Changing settings doesn't update an existing plan. After updating settings, regenerate the plan
    for the changes to take effect (use Regenerate Marketing Plan with Keyword Research after
    changing the content pillars in `topics`).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-settings
    ```

    ## Body Params

    <ParamField body="marketingSettings" type="MarketingSettings" required>

      Marketing settings to create or update.

      See the [Marketing Settings Object](/api-reference/business-management/marketing/marketing-plan/marketing-settings-v1/marketing-settings-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="marketingSettings" type="MarketingSettings">

      The marketing settings after the create or update.

      See the [Marketing Settings Object](/api-reference/business-management/marketing/marketing-plan/marketing-settings-v1/marketing-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Create or update marketing settings. The fieldMask determines which fields are written; every path in the fieldMask must be populated in the marketingSettings object.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-settings' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "marketingSettings": {
          "settings": {
            "marketingPlanGoal": {
              "goalKey": "BOOST_SALES"
            },
            "socialChannels": [
              "INSTAGRAM",
              "FACEBOOK",
              "TIKTOK"
            ]
          }
        },
        "fieldMask": [
          "settings.marketingPlanGoal",
          "settings.socialChannels"
        ]
      }'
      ```

      ```json Response
      {
        "marketingSettings": {
          "id": "f1c2e3a4-5b6c-47d8-9e0f-1a2b3c4d5e6f",
          "settings": {
            "marketingPlanGoal": {
              "goalKey": "BOOST_SALES"
            },
            "socialChannels": [
              "INSTAGRAM",
              "FACEBOOK",
              "TIKTOK"
            ]
          },
          "createdAt": "2026-05-01T09:15:00.000Z",
          "updatedAt": "2026-06-25T11:04:27.000Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates or updates the site's marketing settings.

    If no settings exist yet, this method creates them. Otherwise it updates only the fields listed
    in `fieldMask`; all other fields are left unchanged. Both `marketing_settings` and `field_mask`
    are required. Every path listed in `fieldMask` must be present in the `marketingSettings` you
    send; listing a path that isn't present returns a `FAILED_PRECONDITION` error. To clear a
    field, include it in `fieldMask` and send it explicitly as null or empty.

    Changing settings doesn't update an existing plan. After updating settings, regenerate the plan
    for the changes to take effect (use Regenerate Marketing Plan with Keyword Research after
    changing the content pillars in `topics`).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingSettings } from '@wix/promote-growth-tools-marketing-plan';

    marketingSettings.upsertMarketingSettings(marketingSettings): Promise
    ```

    ## Method Parameters

    <ParamField body="marketingSettings" type="MarketingSettings" required>

      Marketing settings to create or update.

      See the [Marketing Settings Object](/api-reference/business-management/marketing/marketing-plan/marketing-settings-v1/marketing-settings-object) for this object's fields.

    </ParamField>

    ## Returns

    <ResponseField name="marketingSettings" type="MarketingSettings">

      The marketing settings after the create or update.

      See the [Marketing Settings Object](/api-reference/business-management/marketing/marketing-plan/marketing-settings-v1/marketing-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-upsert-marketing-settings" id="ex-sdk-upsert-marketing-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-upsert-marketing-settings" id="ex-sdk-upsert-marketing-settings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-upsert-marketing-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-upsert-marketing-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Upsert marketing settings</span><span className="wix-picker-desc">Every path in fieldMask must be populated in the marketingSettings object</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">upsertMarketingSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-upsert-marketing-settings-0"><span className="wix-picker-name">Upsert marketing settings</span><span className="wix-picker-desc">Every path in fieldMask must be populated in the marketingSettings object</span></label>
    <label htmlFor="ex-sdk-upsert-marketing-settings-1"><span className="wix-picker-name">upsertMarketingSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Upsert marketing settings
    import { marketingSettings } from "@wix/promote-growth-tools-marketing-plan";

    const settings = {
      settings: {
        marketingPlanGoal: {
          goalKey: "BOOST_SALES" as const,
        },
        socialChannels: ["INSTAGRAM", "FACEBOOK", "TIKTOK"] as const,
      },
    };

    const options = {
      fieldMask: ["settings.marketingPlanGoal", "settings.socialChannels"],
    };

    async function upsertMarketingSettings() {
      const response = await marketingSettings.upsertMarketingSettings(
        settings,
        options,
      );
    }

    /* Promise resolves to:
     * {
     *   "_id": "f1c2e3a4-5b6c-47d8-9e0f-1a2b3c4d5e6f",
     *   "settings": {
     *     "marketingPlanGoal": {
     *       "goalKey": "BOOST_SALES"
     *     },
     *     "socialChannels": [
     *       "INSTAGRAM",
     *       "FACEBOOK"
     *     ]
     *   },
     *   "createdAt": "2026-05-01T09:15:00.000Z",
     *   "updatedAt": "2026-06-25T11:04:27.000Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript upsertMarketingSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingSettings } from '@wix/promote-growth-tools-marketing-plan';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingSettings },
      // Include the auth strategy and host as relevant
    });

    async function upsertMarketingSettings(marketingSettings) {
      const response = await myWixClient.marketingSettings.upsertMarketingSettings(marketingSettings);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
