---
title: "Resolve Actual From Address"
description: "Checks if the sender's email address will be used as from-address or replaced (not related to current sender details)."
icon: "upload"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Checks if the sender's email address will be used as from-address or replaced (not related to current sender details).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/email-marketing/v1/sender-details/actual-from-address
    ```

    ## Body Params

    <ParamField body="fromAddress" type="string" required>

      User's provided address from which to send email marketing campaign.

      Constraints: `format EMAIL`.

    </ParamField>

    ## Response Object

    <ResponseField name="actualFromAddress" type="string">

      Actual from-address that will be used for email distribution.

      Constraints: `format EMAIL`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/email-marketing/v1/sender-details/actual-from-address' \
      --data-binary '{
                       "fromAddress": "john.doe@gmail.com"
                     }' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "actualFromAddress": "john.doe@pb02.ascendbywix.com"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Checks if the sender's email address will be used as from-address or replaced (not related to current sender details).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.EMAIL-MARKETING">Manage Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderDetails } from '@wix/email-marketing';

    senderDetails.resolveActualFromAddress(fromAddress): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="fromAddress" type="string" required>

      User's provided address from which to send email marketing campaign.

      Constraints: `format EMAIL`.

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="actualFromAddress" type="string">

      Actual from-address that will be used for email distribution.

      Constraints: `format EMAIL`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-resolve-actual-from-address" id="ex-sdk-resolve-actual-from-address-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-resolve-actual-from-address" id="ex-sdk-resolve-actual-from-address-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-resolve-actual-from-address" id="ex-sdk-resolve-actual-from-address-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-resolve-actual-from-address-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-resolve-actual-from-address-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">resolveActualFromAddress</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">resolveActualFromAddress (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">resolveActualFromAddress (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-resolve-actual-from-address-0"><span className="wix-picker-name">resolveActualFromAddress</span></label>
    <label htmlFor="ex-sdk-resolve-actual-from-address-1"><span className="wix-picker-name">resolveActualFromAddress (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-resolve-actual-from-address-2"><span className="wix-picker-name">resolveActualFromAddress (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript resolveActualFromAddress
    import { senderDetails } from '@wix/email-marketing';

    async function resolveActualFromAddress(fromAddress) {
      const response = await senderDetails.resolveActualFromAddress(fromAddress);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript resolveActualFromAddress (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderDetails } from '@wix/email-marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderDetails },
      // Include the auth strategy and host as relevant
    });

    async function resolveActualFromAddress(fromAddress) {
      const response = await myWixClient.senderDetails.resolveActualFromAddress(fromAddress);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript resolveActualFromAddress (with elevated permissions)
    import { senderDetails } from '@wix/email-marketing';
    import { auth } from '@wix/essentials';

    async function myResolveActualFromAddressMethod(fromAddress) {
      const elevatedResolveActualFromAddress = auth.elevate(senderDetails.resolveActualFromAddress);
      const response = await elevatedResolveActualFromAddress(fromAddress);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
