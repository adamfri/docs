---
title: "Update Rsvp"
description: "Updates an RSVP."
icon: "pen"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Updates an RSVP.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/events/v1/rsvp/{rsvpId}
    ```

    ## Path Params

    <ParamField path="rsvpId" type="string" required>

      RSVP ID.

    </ParamField>

    ## Body Params

    <ParamField body="eventId" type="string" required>

      Event ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="fields" type="FieldMask" required>

      Set of field paths, specifying which parts of RSVP to update.

      <Expandable title="fields properties">

        <ParamField body="paths" type="string[]" />

      </Expandable>

    </ParamField>

    <ParamField body="rsvpForm" type="FormResponse">

      RSVP form response.

      <Expandable title="rsvpForm properties">

        <ParamField body="inputValues" type="InputValue[]">

          Form field inputs.

          Constraints: `maxItems 200`.

          <Expandable title="inputValues properties">

            <ParamField body="inputName" type="string">

              Form field input name.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="value" type="string">

              Form field value.

              Constraints: `maxLength 5000`.

            </ParamField>

            <ParamField body="values" type="string[]">

              Form field values.

              Constraints: `maxLength 5000`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="status" type="string">

      RSVP response status.

      **Supported values:**

      - `YES`
      - `NO`
      - `WAITING`

    </ParamField>

    <ParamField body="options" type="ModificationOptions">

      Update RSVP options.
      WIX_EVENTS.MANAGE_RSVP permission is required.

      <Expandable title="options properties">

        <ParamField body="silent" type="boolean">

          Whether to ignore notification settings (when hen true, no notifications to contact or user are sent).

        </ParamField>

        <ParamField body="ignoreLimits" type="boolean">

          Whether to create/update regardless of event guest limit.

        </ParamField>

        <ParamField body="ignoreFormValidation" type="boolean">

          Whether to ignore the form validation.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="rsvp" type="Rsvp">

      Updated RSVP.

      See the [Rsvp Object](/api-reference/business-solutions/events/rsvp/rsvp-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Updated | `wix.events.rsvp.events.RsvpUpdated` |  |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Updates an RSVP.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rsvp } from '@wix/events';

    rsvp.updateRsvp(rsvpId, eventId, options): Promise<UpdateRsvpResponse>
    ```

    ## Method Parameters

    <ParamField body="rsvpId" type="string" required>

      RSVP ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="eventId" type="string" required>

      Event ID to which RSVP belongs.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="UpdateRsvpOptions" required>

      Optional fields.

      <Expandable title="options properties">

        <ParamField body="fields" type="FieldMask" required>

          Set of field paths, specifying which parts of RSVP to update.

          <Expandable title="fields properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

        <ParamField body="rsvpForm" type="FormResponse">

          RSVP form response.

          <Expandable title="rsvpForm properties">

            <ParamField body="inputValues" type="InputValue[]">

              Form field inputs.

              Constraints: `maxItems 200`.

              _Nested structure omitted for brevity — see the `InputValue[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="status" type="string">

          RSVP response status.

          **Supported values:**

          - `YES`
          - `NO`
          - `WAITING`

        </ParamField>

        <ParamField body="options" type="ModificationOptions">

          Update RSVP options.
          WIX_EVENTS.MANAGE_RSVP permission is required.

          <Expandable title="options properties">

            <ParamField body="silent" type="boolean">

              Whether to ignore notification settings (when hen true, no notifications to contact or user are sent).

            </ParamField>

            <ParamField body="ignoreLimits" type="boolean">

              Whether to create/update regardless of event guest limit.

            </ParamField>

            <ParamField body="ignoreFormValidation" type="boolean">

              Whether to ignore the form validation.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateRsvpResponse>`.

    <ResponseField name="rsvp" type="Rsvp">

      Updated RSVP.

      See the [Rsvp Object](/api-reference/business-solutions/events/rsvp/rsvp-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Rsvp Updated | `wix.events.rsvp.events.RsvpUpdated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-rsvp" id="ex-sdk-update-rsvp-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-rsvp" id="ex-sdk-update-rsvp-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-rsvp" id="ex-sdk-update-rsvp-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-rsvp-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-rsvp-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateRsvp</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateRsvp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateRsvp (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-rsvp-0"><span className="wix-picker-name">updateRsvp</span></label>
    <label htmlFor="ex-sdk-update-rsvp-1"><span className="wix-picker-name">updateRsvp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-rsvp-2"><span className="wix-picker-name">updateRsvp (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateRsvp
    import { rsvp } from '@wix/events';

    async function updateRsvp(rsvpId,eventId,options) {
      const response = await rsvp.updateRsvp(rsvpId,eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateRsvp (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rsvp } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rsvp },
      // Include the auth strategy and host as relevant
    });

    async function updateRsvp(rsvpId,eventId,options) {
      const response = await myWixClient.rsvp.updateRsvp(rsvpId,eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateRsvp (with elevated permissions)
    import { rsvp } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myUpdateRsvpMethod(rsvpId,eventId,options) {
      const elevatedUpdateRsvp = auth.elevate(rsvp.updateRsvp);
      const response = await elevatedUpdateRsvp(rsvpId,eventId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
