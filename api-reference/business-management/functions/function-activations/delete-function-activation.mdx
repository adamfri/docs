---
title: "Delete Function Activation"
description: "Deletes a function activation."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a function activation.

    This deactivates the function whose ID is specified. The function's activation status is set to `"INACTIVE"`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FUNCTIONS.MANAGE-FUNCTIONS">Manage Functions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/functions/v1/activations/{functionId}
    ```

    ## Path Params

    <ParamField path="functionId" type="string" required>

      ID of the function to deactivate.

    </ParamField>

    ## Response Object

    Returns `DeleteFunctionActivationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
       https://www.wixapis.com/functions/v1/activations/0481a48d-0dff-40a9-abd1-6267a3355e70 \
       -H "Authorization: <AUTH>" \
       -H "Content-Type:application/json"
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a function activation.

    This deactivates the function whose ID is specified. The function's activation status is set to `"INACTIVE"`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FUNCTIONS.MANAGE-FUNCTIONS">Manage Functions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { functionActivations } from '@wix/functions';

    functionActivations.deleteFunctionActivation(functionId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="functionId" type="string" required>

      ID of the function to deactivate.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Function Activation Deleted | `wix.functions.v1.function_activation_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-function-activation" id="ex-sdk-delete-function-activation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-function-activation" id="ex-sdk-delete-function-activation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-function-activation" id="ex-sdk-delete-function-activation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-function-activation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-function-activation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteFunctionActivation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteFunctionActivation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteFunctionActivation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-function-activation-0"><span className="wix-picker-name">deleteFunctionActivation</span></label>
    <label htmlFor="ex-sdk-delete-function-activation-1"><span className="wix-picker-name">deleteFunctionActivation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-function-activation-2"><span className="wix-picker-name">deleteFunctionActivation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteFunctionActivation
    import { functionActivations } from '@wix/functions';

    async function deleteFunctionActivation(functionId) {
      const response = await functionActivations.deleteFunctionActivation(functionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteFunctionActivation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { functionActivations } from '@wix/functions';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { functionActivations },
      // Include the auth strategy and host as relevant
    });

    async function deleteFunctionActivation(functionId) {
      const response = await myWixClient.functionActivations.deleteFunctionActivation(functionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteFunctionActivation (with elevated permissions)
    import { functionActivations } from '@wix/functions';
    import { auth } from '@wix/essentials';

    async function myDeleteFunctionActivationMethod(functionId) {
      const elevatedDeleteFunctionActivation = auth.elevate(functionActivations.deleteFunctionActivation);
      const response = await elevatedDeleteFunctionActivation(functionId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
