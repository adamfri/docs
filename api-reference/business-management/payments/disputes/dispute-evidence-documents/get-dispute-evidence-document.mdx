---
title: "Get Dispute Evidence Document"
description: "Retrieves a dispute evidence document."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a dispute evidence document.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.READ-DISPUTES">Read Disputes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/payments/dispute-evidence-documents/v1/dispute-evidence-documents/{disputeEvidenceDocumentId}
    ```

    ## Path Params

    <ParamField path="disputeEvidenceDocumentId" type="string" required>

      ID of the dispute evidence document to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="disputeEvidenceDocument" type="DisputeEvidenceDocument">

      Retrieved dispute evidence document.
      Retrieved dispute evidence document.

      See the [Dispute Evidence Document Object](/api-reference/business-management/payments/disputes/dispute-evidence-documents/dispute-evidence-document-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a specific evidence document by its ID

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/dispute-evidence-documents/v1/dispute-evidence-documents/87654321-4321-4321-4321-210987654321' \
      -H 'Authorization: <AUTH>'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a dispute evidence document.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.READ-DISPUTES">Read Disputes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { disputeEvidenceDocuments } from '@wix/payments';

    disputeEvidenceDocuments.getDisputeEvidenceDocument(disputeEvidenceDocumentId): Promise
    ```

    ## Method Parameters

    <ParamField body="disputeEvidenceDocumentId" type="string" required>

      ID of the dispute evidence document to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DisputeEvidenceDocument`.

    See the [Dispute Evidence Document Object](/api-reference/business-management/payments/disputes/dispute-evidence-documents/dispute-evidence-document-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-dispute-evidence-document" id="ex-sdk-get-dispute-evidence-document-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-dispute-evidence-document" id="ex-sdk-get-dispute-evidence-document-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-dispute-evidence-document" id="ex-sdk-get-dispute-evidence-document-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-dispute-evidence-document-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-dispute-evidence-document-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getDisputeEvidenceDocument</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDisputeEvidenceDocument (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDisputeEvidenceDocument (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-dispute-evidence-document-0"><span className="wix-picker-name">getDisputeEvidenceDocument</span></label>
    <label htmlFor="ex-sdk-get-dispute-evidence-document-1"><span className="wix-picker-name">getDisputeEvidenceDocument (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-dispute-evidence-document-2"><span className="wix-picker-name">getDisputeEvidenceDocument (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getDisputeEvidenceDocument
    import { disputeEvidenceDocuments } from '@wix/payments';

    async function getDisputeEvidenceDocument(disputeEvidenceDocumentId) {
      const response = await disputeEvidenceDocuments.getDisputeEvidenceDocument(disputeEvidenceDocumentId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDisputeEvidenceDocument (self-hosted)
    import { createClient } from '@wix/sdk';
    import { disputeEvidenceDocuments } from '@wix/payments';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { disputeEvidenceDocuments },
      // Include the auth strategy and host as relevant
    });

    async function getDisputeEvidenceDocument(disputeEvidenceDocumentId) {
      const response = await myWixClient.disputeEvidenceDocuments.getDisputeEvidenceDocument(disputeEvidenceDocumentId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDisputeEvidenceDocument (with elevated permissions)
    import { disputeEvidenceDocuments } from '@wix/payments';
    import { auth } from '@wix/essentials';

    async function myGetDisputeEvidenceDocumentMethod(disputeEvidenceDocumentId) {
      const elevatedGetDisputeEvidenceDocument = auth.elevate(disputeEvidenceDocuments.getDisputeEvidenceDocument);
      const response = await elevatedGetDisputeEvidenceDocument(disputeEvidenceDocumentId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
