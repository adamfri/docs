---
title: "Create Location"
description: "Creates a location."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a location.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/locations/v1/locations
    ```

    ## Body Params

    <ParamField body="location" type="Location" required>

      Location to create.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="location" type="Location">

      Created location.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_PERFORM_ACTION_ON_ARCHIVED_LOCATION` | Can't perform this action on an archived location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Location Created | `wix.locations.v1.location_created` | Triggered when a location is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
         'https://www.wixapis.com/locations/v1/locations' \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>' \
         --data-binary '{
           "location": {
               "name":"South Figueroa Street",
               "default":false,
               "status":"ACTIVE",
               "locationType":"UNKNOWN",
               "timeZone":"America/Los_Angeles",
               "address":{
                  "country":"US",
                  "subdivision":"CA",
                  "city":"Los Angeles",
                  "postalCode":"90015",
                  "streetAddress":{
                     "number":"1111",
                     "name":"South Figueroa Street",
                     "apt":""
                  },
                  "formattedAddress":"STAPLES Center, South Figueroa Street, Los Angeles, CA, USA",
                  "geocode":{
                     "latitude":34.0430175,
                     "longitude":-118.2672541
                  }
               }
           }
         }'
      ```

      ```json Response
      {
        "location": {
           "id":"ea50573e-35fa-47ca-bba7-3e45a935d9bc",
           "name":"South Figueroa Street",
           "default":false,
           "status":"ACTIVE",
           "locationType":"UNKNOWN",
           "timeZone":"America/Los_Angeles",
           "address":{
              "country":"US",
              "subdivision":"CA",
              "city":"Los Angeles",
              "postalCode":"90015",
              "streetAddress":{
                 "number":"1111",
                 "name":"South Figueroa Street",
                 "apt":""
              },
              "formattedAddress":"STAPLES Center, South Figueroa Street, Los Angeles, CA, USA",
              "geocode":{
                 "latitude":34.0430175,
                 "longitude":-118.2672541
              }
           },
           "revision":"1",
           "archived":false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a location.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/business-tools';

    locations.createLocation(location): Promise<Location>
    ```

    ## Method Parameters

    <ParamField body="location" type="Location" required>

      Location to create.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Location>`.

    See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_PERFORM_ACTION_ON_ARCHIVED_LOCATION` | Can't perform this action on an archived location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Location Created | `wix.locations.v1.location_created` | Triggered when a location is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-location" id="ex-sdk-create-location-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-location" id="ex-sdk-create-location-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-location" id="ex-sdk-create-location-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-location" id="ex-sdk-create-location-3" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-location-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-location-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a new location</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create a new location (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create a new location from input on the site's page (with $w)</span><span className="wix-picker-desc">This code takes the values a user inputs into a form on the page, and uses them as the values for the properties of a new location. This page is accessible to only company managers so they can add locations to the database</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-location-0"><span className="wix-picker-name">Create a new location</span></label>
    <label htmlFor="ex-sdk-create-location-1"><span className="wix-picker-name">Create a new location (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-create-location-2"><span className="wix-picker-name">Create a new location from input on the site's page (with $w)</span><span className="wix-picker-desc">This code takes the values a user inputs into a form on the page, and uses them as the values for the properties of a new location. This page is accessible to only company managers so they can add locations to the database</span></label>
    <label htmlFor="ex-sdk-create-location-3"><span className="wix-picker-name">createLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a new location
    import { locations } from '@wix/business-tools';

    /* Sample location value:
     * {
     *   address: {
     *     formatted: '12 5th Street, New York'
     *   },
     *   name: 'New York Store',
     *   timeZone: 'America/New_York'
     * }
     */

    export async function myCreateLocationFunction(location) {
      try {
        const newLocation = await locations.createLocation(location);
        console.log('Created new location:', newLocation);

        return newLocation;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "_id": "125e9107-2d4b-4790-bb49-37e7983ff9b1",
     *   "address": {
     *     "formatted": "12 5th Street, New York"
     *   },
     *   "archived": false,
     *   "default": false,
     *   "name": "New York Store",
     *   "revision": "1",
     *   "status": "ACTIVE",
     *   "timeZone": "America/New_York"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Create a new location (with elevated permissions)
    import { locations } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    /* Sample location value:
     * {
     *   address: {
     *     formatted: '12 5th Street, New York'
     *   },
     *   name: 'New York Store',
     *   timeZone: 'America/New_York'
     * }
     */

    export async function myCreateLocationFunction(location) {
      try {
        const elevatedCreateLocation = auth.elevate(locations.createLocation);
        const newLocation = await elevatedCreateLocation(location);
        console.log('Created new location:', newLocation);

        return newLocation;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "_id": "125e9107-2d4b-4790-bb49-37e7983ff9b1",
     *   "address": {
     *     "formatted": "12 5th Street, New York"
     *   },
     *   "archived": false,
     *   "default": false,
     *   "name": "New York Store",
     *   "revision": "1",
     *   "status": "ACTIVE",
     *   "timeZone": "America/New_York"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Create a new location from input on the site's page (with $w)
    /********************************************
     * Backend code - create-location.web.js/ts *
     *******************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { locations } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    export const createNewLocation = webMethod(Permissions.Anyone, async (location) => {
      try {
        const elevatedCreateLocation = auth.elevate(locations.createLocation);
        const newLocation = await elevatedCreateLocation(location);

        return newLocation;
      } catch (error) {
        console.error(error);
        throw new Error(error);
      }
    });

    /*************
     * Page code *
     ************/

    import { createNewLocation } from 'backend/create-location.web';

    $w.onReady(() => {
      $w('#createLocationBtn').onClick(async () => {
        const location = {
          address: {
            streetAddress: {
                number: $w('#streetNumber').value,
                name: $w('#streetName').value
            },
            city: $w('#city').value,
            postalCode: $w('#postalCode').value,
          },
          description: $w('#description').value,
          email: $w('#email').value,
          name: $w('#name').value,
          phone: $w('#phone').value,
          timeZone: $w('#timeZone').value
        };

        const newLocation = await createNewLocation(location);
        console.log('You have successfully created a new location.\n', newLocation);
        $w('#newLocationSuccessMsg').show();
      });
    });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createLocation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/business-tools';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function createLocation(location) {
      const response = await myWixClient.locations.createLocation(location);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
