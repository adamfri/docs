---
title: "onQuestionEntryDeleted"
description: "The onQuestionEntryDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.faq.question_entry.v2.question_entry_deleted` |
| Package | `@wix/faq` |
| Permissions | `Manage FAQ`, `Read FAQ` |

## Examples

<CodeGroup>

  ```javascript onQuestionEntryDeleted
  import { questionEntry } from '@wix/faq';

  questionEntry.onQuestionEntryDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onQuestionEntryDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { questionEntry } from '@wix/faq';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        questionEntry,
      },
  });

  wixClient.questionEntry.onQuestionEntryDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
