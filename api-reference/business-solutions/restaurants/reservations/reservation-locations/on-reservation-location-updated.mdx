---
title: "onReservationLocationUpdated"
description: "Triggered when a reservation location is updated."
icon: "bolt"
---

Triggered when a reservation location is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.table_reservations.v1.reservation_location_updated` |
| Package | `@wix/table-reservations` |
| Permissions | `Read Reservation Locations (Full)`, `Manage Reservation Locations`, `Manage Restaurants - all permissions`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onReservationLocationUpdated
  import { reservationLocations } from '@wix/table-reservations';

  reservationLocations.onReservationLocationUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReservationLocationUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reservationLocations } from '@wix/table-reservations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reservationLocations,
      },
  });

  wixClient.reservationLocations.onReservationLocationUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
