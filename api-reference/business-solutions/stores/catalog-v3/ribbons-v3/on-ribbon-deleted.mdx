---
title: "onRibbonDeleted"
description: "The onRibbonDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.stores.catalog.v3.ribbon_deleted` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Read ribbons in v3 catalog`, `Manage Stores`, `Read v3 catalog`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Product write in v3 catalog`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onRibbonDeleted
  import { ribbonsV3 } from '@wix/stores';

  ribbonsV3.onRibbonDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onRibbonDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ribbonsV3 } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        ribbonsV3,
      },
  });

  wixClient.ribbonsV3.onRibbonDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
