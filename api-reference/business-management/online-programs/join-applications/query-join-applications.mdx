---
title: "Query Join Applications"
description: "Retrieves a list of up to 1,000 join applications, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1,000 join applications, given the provided paging, filtering, and sorting.

    Members can retrieve their own join applications. Wix users can retrieve applications across all members.

    Query Join Applications runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `50`
    - `paging.offset` is `0`

    To search join applications by member name, email, or nickname, call [Search Join Applications](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/search-join-applications).

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `memberId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `programId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `participantId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` |  | Sort: `ASC`, `DESC` |
    | `updatedDate` |  | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$in`, `$nin` | Sort: `ASC`, `DESC` |
    | `paymentStatus` | `$eq`, `$in`, `$nin` | Sort: `ASC`, `DESC` |
    | `tags.publicTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |
    | `tags.privateTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/online-programs/participants/v3/join-applications/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2">

      WQL expression.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 1000`, `maxItems 100`.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 1000`, `maxItems 100`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="joinApplications" type="JoinApplication[]">

      Retrieved join applications.

      See the [Join Application Object](/api-reference/business-management/online-programs/join-applications/join-application-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-query-join-applications" id="ex-rest-query-join-applications-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-query-join-applications" id="ex-rest-query-join-applications-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-query-join-applications-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-query-join-applications-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query join applications by status</span><span className="wix-picker-desc">Retrieves join applications filtered by status and sorted by creation date</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query join applications by program</span><span className="wix-picker-desc">Retrieves join applications for a specific program</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-query-join-applications-0"><span className="wix-picker-name">Query join applications by status</span><span className="wix-picker-desc">Retrieves join applications filtered by status and sorted by creation date</span></label>
    <label htmlFor="ex-rest-query-join-applications-1"><span className="wix-picker-name">Query join applications by program</span><span className="wix-picker-desc">Retrieves join applications for a specific program</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/online-programs/participants/v3/join-applications/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "status": {
              "$eq": "APPROVAL_PENDING"
            }
          },
          "sort": [{
            "fieldName": "createdDate",
            "order": "DESC"
          }],
          "paging": {
            "limit": 50
          }
        }
      }'
      ```

      ```json Response
      {
        "joinApplications": [
          {
            "id": "0f4be143-f402-4960-985b-5c3a6ce6b5f7",
            "revision": "1",
            "createdDate": "2026-06-01T14:23:26.498Z",
            "updatedDate": "2026-06-01T14:23:27.219Z",
            "memberId": "6c7c7b22-67a2-43c7-9d27-3b8ed07d9a45",
            "programId": "8d3f2b9f-2c31-4f45-8e5a-1f4b9259a6c2",
            "status": "APPROVAL_PENDING",
            "paymentStatus": "PAYMENT_PENDING",
            "member": {
              "firstName": "Jordan",
              "lastName": "Taylor",
              "email": "jordan.taylor@example.com",
              "contactId": "6c7c7b22-67a2-43c7-9d27-3b8ed07d9a45",
              "nickname": "jordan.taylor"
            }
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "total": 1,
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/online-programs/participants/v3/join-applications/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "programId": {
              "$eq": "3f8e26e7-c5d1-4a12-86f8-33d64f4c2a0e"
            }
          }
        }
      }'
      ```

      ```json Response
      {
        "joinApplications": [
          {
            "id": "79a0d4f7-66a2-47ef-a268-f1c7fcd3a0c5",
            "revision": "2",
            "createdDate": "2026-06-01T14:23:28.975Z",
            "updatedDate": "2026-06-01T14:24:04.343Z",
            "memberId": "2a01d8c7-54e5-4f42-9c9f-7a5d4d2ec4f1",
            "programId": "3f8e26e7-c5d1-4a12-86f8-33d64f4c2a0e",
            "status": "APPROVED",
            "paymentStatus": "PAYMENT_PENDING"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "total": 1,
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1,000 join applications, given the provided paging, filtering, and sorting.

    Members can retrieve their own join applications. Wix users can retrieve applications across all members.

    Query Join Applications runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `50`
    - `paging.offset` is `0`

    To search join applications by member name, email, or nickname, call [Search Join Applications](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/search-join-applications).

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `memberId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `programId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `participantId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` |  | Sort: `ASC`, `DESC` |
    | `_updatedDate` |  | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$in`, `$nin` | Sort: `ASC`, `DESC` |
    | `paymentStatus` | `$eq`, `$in`, `$nin` | Sort: `ASC`, `DESC` |
    | `tags.publicTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |
    | `tags.privateTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { joinApplications } from '@wix/online-programs';

    joinApplications.queryJoinApplications(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="JoinApplicationQuery" required>

      WQL expression.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="joinApplications" type="JoinApplication[]">

      Retrieved join applications.

      See the [Join Application Object](/api-reference/business-management/online-programs/join-applications/join-application-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-join-applications" id="ex-sdk-query-join-applications-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-join-applications" id="ex-sdk-query-join-applications-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-join-applications" id="ex-sdk-query-join-applications-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-join-applications-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-join-applications-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query join applications by status</span><span className="wix-picker-desc">Retrieves join applications filtered by status and sorted by creation date</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query join applications by program</span><span className="wix-picker-desc">Retrieves join applications for a specific program</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryJoinApplications (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-join-applications-0"><span className="wix-picker-name">Query join applications by status</span><span className="wix-picker-desc">Retrieves join applications filtered by status and sorted by creation date</span></label>
    <label htmlFor="ex-sdk-query-join-applications-1"><span className="wix-picker-name">Query join applications by program</span><span className="wix-picker-desc">Retrieves join applications for a specific program</span></label>
    <label htmlFor="ex-sdk-query-join-applications-2"><span className="wix-picker-name">queryJoinApplications (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query join applications by status
    import { joinApplications } from "@wix/online-programs";

    async function queryJoinApplications() {
      const results = await joinApplications
        .queryJoinApplications()
        .eq("status", "APPROVAL_PENDING")
        .descending("_createdDate")
        .limit(50)
        .find();
    }

    /* Promise resolves to:
     * {
     *   "items": [
     *     {
     *       "_id": "0f4be143-f402-4960-985b-5c3a6ce6b5f7",
     *       "revision": "1",
     *       "_createdDate": "2026-06-01T14:23:26.498Z",
     *       "_updatedDate": "2026-06-01T14:23:27.219Z",
     *       "memberId": "6c7c7b22-67a2-43c7-9d27-3b8ed07d9a45",
     *       "programId": "8d3f2b9f-2c31-4f45-8e5a-1f4b9259a6c2",
     *       "status": "APPROVAL_PENDING",
     *       "paymentStatus": "PAYMENT_PENDING",
     *       "member": {
     *         "firstName": "Jordan",
     *         "lastName": "Taylor",
     *         "email": "jordan.taylor@example.com",
     *         "contactId": "6c7c7b22-67a2-43c7-9d27-3b8ed07d9a45",
     *         "nickname": "jordan.taylor"
     *       }
     *     }
     *   ],
     *   "length": 1,
     *   "pageSize": 50,
     *   "hasNext": false,
     *   "hasPrev": false
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Query join applications by program
    import { joinApplications } from "@wix/online-programs";

    async function queryJoinApplications() {
      const results = await joinApplications
        .queryJoinApplications()
        .eq("programId", "3f8e26e7-c5d1-4a12-86f8-33d64f4c2a0e")
        .find();
    }

    /* Promise resolves to:
     * {
     *   "items": [
     *     {
     *       "_id": "79a0d4f7-66a2-47ef-a268-f1c7fcd3a0c5",
     *       "revision": "1",
     *       "_createdDate": "2026-06-01T14:23:28.975Z",
     *       "_updatedDate": "2026-06-01T14:23:28.975Z",
     *       "memberId": "6c7c7b22-67a2-43c7-9d27-3b8ed07d9a45",
     *       "programId": "3f8e26e7-c5d1-4a12-86f8-33d64f4c2a0e",
     *       "status": "INVITED",
     *       "paymentStatus": "NO_PAYMENT_REQUIRED"
     *     }
     *   ],
     *   "length": 1,
     *   "pageSize": 50,
     *   "hasNext": false,
     *   "hasPrev": false
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryJoinApplications (self-hosted)
    import { createClient } from '@wix/sdk';
    import { joinApplications } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { joinApplications },
      // Include the auth strategy and host as relevant
    });

    async function queryJoinApplications(query) {
      const response = await myWixClient.joinApplications.queryJoinApplications(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
