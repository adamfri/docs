---
title: "onInventoryVariantsChanged"
description: "Triggered when inventory variants are changed."
icon: "bolt"
---

Triggered when inventory variants are changed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `com.wix.ecommerce.inventory.api.v1.InventoryVariantsChanged` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Products`, `Read Stores - all read permissions`, `Read Products` |

## Examples

<CodeGroup>

  ```javascript onInventoryVariantsChanged
  import { inventory } from '@wix/stores';

  inventory.onInventoryVariantsChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onInventoryVariantsChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { inventory } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        inventory,
      },
  });

  wixClient.inventory.onInventoryVariantsChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
