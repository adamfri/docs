---
title: "Query Email Transmissions"
description: "Retrieves a list of up to 100 email transmissions, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 email transmissions, given the provided paging, filtering, and sorting.

    `emailHtmlContent` is not returned in the results. To retrieve the full HTML content of a specific transmission,
    call [Get Email Transmission](https://dev.wix.com/docs/api-reference/business-management/marketing/emails/email-transmission-v1/get-email-transmission).

    Query Email Transmissions runs with these defaults, which you can override:

    - `updatedDate` is sorted in `DESC` order
    - `paging.limit` is `50`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in` | Sort: `ASC`, `DESC` |
    | `type` | `$eq`, `$ne`, `$in` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `emailSubject` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.READ-EMAIL-MARKETING">Read Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/emails/transmissions/v1/email-transmissions/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options, including filter, sorting, and pagination.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="emailTransmissions" type="EmailTransmission[]">

      Retrieved email transmissions.

      See the [Email Transmission Object](/api-reference/business-management/marketing/emails/email-transmission-v1/email-transmission-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieve email transmissions filtered by status, sorted by most recently updated.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/email-transmissions/v1/email-transmissions/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "status": {"$eq": "PROCESSED"}
          },
          "sort": [{"fieldName": "updatedDate", "order": "DESC"}],
          "cursorPaging": {"limit": 3}
        }
      }'
      ```

      ```json Response
      {
        "emailTransmissions": [
          {
            "id": "82d70fd4-09af-4399-9464-4a898f3321f8",
            "createdDate": "2026-07-20T14:12:50.195Z",
            "updatedDate": "2026-07-20T14:12:52.298Z",
            "emailSubject": "Your order has been confirmed",
            "emailHtmlContent": "",
            "metadata": {},
            "toRecipients": [
              {
                "contactId": "2b79d09c-6c61-4fe3-9769-2c791343e8d3",
                "failureReason": "EMAIL_ADDRESS_BOUNCED"
              }
            ],
            "type": "TRANSACTIONAL",
            "status": "PROCESSED"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 email transmissions, given the provided paging, filtering, and sorting.

    `emailHtmlContent` is not returned in the results. To retrieve the full HTML content of a specific transmission,
    call [Get Email Transmission](https://dev.wix.com/docs/api-reference/business-management/marketing/emails/email-transmission-v1/get-email-transmission).

    Query Email Transmissions runs with these defaults, which you can override:

    - `updatedDate` is sorted in `DESC` order
    - `paging.limit` is `50`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in` | Sort: `ASC`, `DESC` |
    | `type` | `$eq`, `$ne`, `$in` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `emailSubject` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PROMOTE.READ-EMAIL-MARKETING">Read Email Marketing</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { emailTransmissions } from '@wix/email-transmissions';

    emailTransmissions.queryEmailTransmissions(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="EmailTransmissionQuery" required>

      Query options, including filter, sorting, and pagination.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="emailTransmissions" type="EmailTransmission[]">

      Retrieved email transmissions.

      See the [Email Transmission Object](/api-reference/business-management/marketing/emails/email-transmission-v1/email-transmission-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-email-transmissions" id="ex-sdk-query-email-transmissions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-email-transmissions" id="ex-sdk-query-email-transmissions-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-email-transmissions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-email-transmissions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query email transmissions</span><span className="wix-picker-desc">Retrieve email transmissions filtered by status, sorted by most recently updated</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryEmailTransmissions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-email-transmissions-0"><span className="wix-picker-name">Query email transmissions</span><span className="wix-picker-desc">Retrieve email transmissions filtered by status, sorted by most recently updated</span></label>
    <label htmlFor="ex-sdk-query-email-transmissions-1"><span className="wix-picker-name">queryEmailTransmissions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query email transmissions
    import { emailTransmissions } from "@wix/email-transmissions";

    async function queryEmailTransmissions() {
      const response = await emailTransmissions.queryEmailTransmissions(
        {
          filter: { status: { $eq: "PROCESSED" } },
          sort: [{ fieldName: "updatedDate", order: "DESC" }],
          cursorPaging: { limit: 3 },
        },
      );
    }

    /* Promise resolves to:
     * {
     *   "emailTransmissions": [
     *     {
     *       "_id": "dd266d88-6aac-48f5-a7b4-4ce61a92732f",
     *       "_createdDate": "2026-07-20T14:43:11.093Z",
     *       "_updatedDate": "2026-07-20T14:43:12.579Z",
     *       "emailSubject": "Your order has been confirmed",
     *       "emailHtmlContent": "",
     *       "senderName": "My Store",
     *       "toRecipients": [
     *         {
     *           "contactId": "2b79d09c-6c61-4fe3-9769-2c791343e8d3",
     *           "failureReason": "EMAIL_ADDRESS_BOUNCED"
     *         }
     *       ],
     *       "type": "TRANSACTIONAL",
     *       "status": "PROCESSED"
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 1,
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryEmailTransmissions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { emailTransmissions } from '@wix/email-transmissions';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { emailTransmissions },
      // Include the auth strategy and host as relevant
    });

    async function queryEmailTransmissions(query) {
      const response = await myWixClient.emailTransmissions.queryEmailTransmissions(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
