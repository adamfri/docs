---
title: "onCommentUpdated"
description: "Triggered when a comment is updated."
icon: "bolt"
---

Triggered when a comment is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v2.comment_updated` |
| Package | `@wix/comments` |
| Permissions | `Read Comments`, `Manage Comments` |

## Examples

<CodeGroup>

  ```javascript onCommentUpdated
  import { comments } from '@wix/comments';

  comments.onCommentUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCommentUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { comments } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        comments,
      },
  });

  wixClient.comments.onCommentUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
