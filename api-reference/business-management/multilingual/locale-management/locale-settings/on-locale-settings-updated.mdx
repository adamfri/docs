---
title: "onLocaleSettingsUpdated"
description: "Triggered when locale settings are updated."
icon: "bolt"
---

Triggered when locale settings are updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.settings.v2.locale_settings_updated` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API` |

## Examples

<CodeGroup>

  ```javascript onLocaleSettingsUpdated
  import { localeSettings } from '@wix/multilingual';

  localeSettings.onLocaleSettingsUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocaleSettingsUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { localeSettings } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        localeSettings,
      },
  });

  wixClient.localeSettings.onLocaleSettingsUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
