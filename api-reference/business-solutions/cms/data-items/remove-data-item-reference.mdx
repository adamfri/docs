---
title: "Remove Data Item Reference"
description: "Removes the specified reference from the specified field."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes the specified reference from the specified field.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data/v2/items/remove-reference
    ```

    ## Body Params

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection containing the referring item.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="dataItemReference" type="DataItemReference" required>

      Reference to remove.

      <Expandable title="dataItemReference properties">

        <ParamField body="referringItemFieldName" type="string" required>

          Referring item field containing the references to the referenced items.

          Constraints: `maxLength 1000`.

        </ParamField>

        <ParamField body="referringItemId" type="string" required>

          ID of the referring item.

          Constraints: `maxLength 128`.

        </ParamField>

        <ParamField body="referencedItemId" type="string" required>

          ID of the referenced item.

          Constraints: `maxLength 128`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="dataItemReference" type="DataItemReference">

      Removed reference.

      <Expandable title="dataItemReference properties">

        <ResponseField name="referringItemFieldName" type="string">

          Referring item field containing the references to the referenced items.

          Constraints: `maxLength 1000`.

        </ResponseField>

        <ResponseField name="referringItemId" type="string">

          ID of the referring item.

          Constraints: `maxLength 128`.

        </ResponseField>

        <ResponseField name="referencedItemId" type="string">

          ID of the referenced item.

          Constraints: `maxLength 128`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items/remove-reference' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "albums",
          "dataItemReference": {
              "referringItemFieldName": "songs",
              "referringItemId": "37de298e-026d-4b2e-b87f-fbec11d53105",
              "referencedItemId": "aafeaaf4-6192-4cc2-a79b-97ce0f1b3646"
          }
      }'
      ```

      ```json Response
      {
          "dataItemReference": {
              "referringItemFieldName": "songs",
              "referringItemId": "37de298e-026d-4b2e-b87f-fbec11d53105",
              "referencedItemId": "aafeaaf4-6192-4cc2-a79b-97ce0f1b3646"
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes the specified reference from the specified field.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/data';

    items.removeReference(dataCollectionId, field, referringItem, referencedItem, options): Promise
    ```

    ## Method Parameters

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection containing the referring item.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="field" type="string" required>

      Field from which to remove the reference.

    </ParamField>

    <ParamField body="referringItem" type="string" required>

      Referring item or referring item's ID.

    </ParamField>

    <ParamField body="referencedItem" type="string[]" required>

      Referenced item, referenced item's ID, an array of referenced items, or an array of referenced item IDs.

    </ParamField>

    <ParamField body="options" type="WixDataOptions">

      Options to use when processing this operation.

      <Expandable title="options properties">

        <ParamField body="suppressHooks" type="boolean">

          Prevents hooks from running for the operation. Can only be used in the [backend code of a Wix site](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/backend-code/about-the-site-backend).

        </ParamField>

        <ParamField body="showDrafts" type="boolean">

          When `true`, operations include draft items. Read operations include draft items in their response, and write operations modify draft items.
          Default: `false`.

        </ParamField>

        <ParamField body="appOptions" type="object">

          Options for [querying Wix app collections](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-app-collections/querying-wix-app-collections).

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `VoidResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-data-item-reference" id="ex-sdk-remove-data-item-reference-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-data-item-reference" id="ex-sdk-remove-data-item-reference-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-remove-data-item-reference" id="ex-sdk-remove-data-item-reference-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-data-item-reference-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-data-item-reference-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">removeReference</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeReference (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeReference (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-data-item-reference-0"><span className="wix-picker-name">removeReference</span></label>
    <label htmlFor="ex-sdk-remove-data-item-reference-1"><span className="wix-picker-name">removeReference (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-remove-data-item-reference-2"><span className="wix-picker-name">removeReference (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript removeReference
    import { items } from '@wix/data';

    async function removeReference(dataCollectionId,field,referringItem,referencedItem,options) {
      const response = await items.removeReference(dataCollectionId,field,referringItem,referencedItem,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeReference (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function removeReference(dataCollectionId,field,referringItem,referencedItem,options) {
      const response = await myWixClient.items.removeReference(dataCollectionId,field,referringItem,referencedItem,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeReference (with elevated permissions)
    import { items } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myRemoveReferenceMethod(dataCollectionId,field,referringItem,referencedItem,options) {
      const elevatedRemoveReference = auth.elevate(items.removeReference);
      const response = await elevatedRemoveReference(dataCollectionId,field,referringItem,referencedItem,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
