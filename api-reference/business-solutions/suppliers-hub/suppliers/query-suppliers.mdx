---
title: "Query Suppliers"
description: "Retrieves a list of suppliers with filtering, sorting, and cursor-based pagination."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of suppliers with filtering, sorting, and cursor-based pagination.

    Up to 100 suppliers can be returned per request. For larger datasets, use cursor paging to iterate through results by following the `next` cursor in `pagingMetadata` until all suppliers are retrieved.

    To learn how to query suppliers, see [API Query Language][https://dev.wix.com/docs/rest/articles/getting-started/api-query-language].

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `location.country` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `verified` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `types` |  | None |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `rating` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `tags` | `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.READ_MARKETPLACE">Read Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/suppliers-hub/v1/suppliers/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      WQL expression.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="suppliers" type="Supplier[]">

      List of Suppliers.

      See the [Supplier Object](/api-reference/business-solutions/suppliers-hub/suppliers/supplier-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches for verified suppliers with top rating cursor paging

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/suppliers-hub/v1/suppliers/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "$and": [
              {
                "rating": {
                  "$gt": 4.9
                }
              },
              {
                "verified": {
                  "$eq": true
                }
              }
            ]
          },
          "sort": [
            {
              "fieldName": "rating",
              "order": "DESC"
            }
          ],
          "cursorPaging": {
            "limit": 5
          }
        }
      }'
      ```

      ```json Response
      {
        "suppliers": [
          {
            "id": "92a5a3f4-ee95-4e14-9644-328892bd6da2",
            "revision": "1",
            "createdDate": "2025-09-25T13:22:54.922Z",
            "updatedDate": "2025-09-25T13:22:54.922Z",
            "name": "Zenith Drop",
            "appId": "22bef345-3c5b-4c18-b782-74d4085112ff",
            "location": {
              "country": "SE"
            },
            "rating": "4.98",
            "verified": true,
            "types": [
              "DROPSHIPPING"
            ],
            "reviewCount": 1202,
            "tags": null
          },
          {
            "id": "a04dc7e3-3441-4666-867b-f8c1fd3af052",
            "revision": "1",
            "createdDate": "2025-09-25T13:21:45.042Z",
            "updatedDate": "2025-09-25T13:21:45.042Z",
            "name": "Zenith Drop",
            "appId": "22bef345-3c5b-4c18-b782-74d4085112ff",
            "location": {
              "country": "SE"
            },
            "rating": "4.98",
            "verified": true,
            "types": [
              "DROPSHIPPING"
            ],
            "reviewCount": 1202,
            "tags": null
          },
          {
            "id": "f0a7cc87-4cd0-4de2-81c1-398edd86f70d",
            "revision": "1",
            "createdDate": "2025-09-25T13:21:18.982Z",
            "updatedDate": "2025-09-25T13:21:18.982Z",
            "name": "Zenith Drop",
            "appId": "22bef345-3c5b-4c18-b782-74d4085112ff",
            "location": {
              "country": "SE"
            },
            "rating": "4.98",
            "verified": true,
            "types": [
              "DROPSHIPPING"
            ],
            "reviewCount": 1202,
            "tags": null
          },
          {
            "id": "0d24a5c5-45e6-4ace-ae47-3ce9bed851d8",
            "revision": "3",
            "createdDate": "2025-09-25T12:52:03.538Z",
            "updatedDate": "2025-09-25T13:17:04.597Z",
            "name": "Not top rated anymore",
            "appId": "22bef345-3c5b-4c18-b782-74d4085112ff",
            "location": {
              "country": "CH"
            },
            "rating": "4.95",
            "verified": true,
            "types": [
              "WHOLESALE",
              "DROPSHIPPING"
            ],
            "reviewCount": 13223,
            "tags": null
          },
          {
            "id": "0d9efdaf-d7a5-4a64-96e7-7451551c5527",
            "revision": "1",
            "createdDate": "2025-09-25T13:22:54.922Z",
            "updatedDate": "2025-09-25T13:22:54.922Z",
            "name": "SourceSync",
            "appId": "22bef345-3c5b-4c18-b782-74d4085112ff",
            "location": {
              "country": "US"
            },
            "rating": "4.92",
            "verified": true,
            "types": [
              "DROPSHIPPING"
            ],
            "reviewCount": null,
            "tags": null
          }
        ],
        "pagingMetadata": {
          "count": 5,
          "cursors": {
            "next": "694f0f29aca052120385d884f615861127885494.EjIqMAouCgQkYW5kEiYyJAoiKiAKHgoGcmF0aW5nEhQqEgoQCgMkZ3QSCRGamZmZmZkTQBoSCgZyYXRpbmcQARoGGgQ0LjkyIiQwZDllZmRhZi1kN2E1LTRhNjQtOTZlNy03NDUxNTUxYzU1Mjc",
            "prev": null
          },
          "hasNext": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of suppliers with filtering, sorting, and cursor-based pagination.

    Up to 100 suppliers can be returned per request. For larger datasets, use cursor paging to iterate through results by following the `next` cursor in `pagingMetadata` until all suppliers are retrieved.

    To learn how to query suppliers, see [API Query Language][https://dev.wix.com/docs/rest/articles/getting-started/api-query-language].

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `location.country` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |
    | `verified` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `types` |  | None |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `rating` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `tags` | `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.READ_MARKETPLACE">Read Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { suppliers } from '@wix/suppliers-hub';

    suppliers.querySuppliers(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="SupplierQuery" required>

      WQL expression.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="suppliers" type="Supplier[]">

      List of Suppliers.

      See the [Supplier Object](/api-reference/business-solutions/suppliers-hub/suppliers/supplier-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-suppliers" id="ex-sdk-query-suppliers-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-suppliers" id="ex-sdk-query-suppliers-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-suppliers" id="ex-sdk-query-suppliers-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-suppliers-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-suppliers-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">querySuppliers</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">querySuppliers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">querySuppliers (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-suppliers-0"><span className="wix-picker-name">querySuppliers</span></label>
    <label htmlFor="ex-sdk-query-suppliers-1"><span className="wix-picker-name">querySuppliers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-suppliers-2"><span className="wix-picker-name">querySuppliers (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript querySuppliers
    import { suppliers } from '@wix/suppliers-hub';

    async function querySuppliers(query) {
      const response = await suppliers.querySuppliers(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript querySuppliers (self-hosted)
    import { createClient } from '@wix/sdk';
    import { suppliers } from '@wix/suppliers-hub';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { suppliers },
      // Include the auth strategy and host as relevant
    });

    async function querySuppliers(query) {
      const response = await myWixClient.suppliers.querySuppliers(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript querySuppliers (with elevated permissions)
    import { suppliers } from '@wix/suppliers-hub';
    import { auth } from '@wix/essentials';

    async function myQuerySuppliersMethod(query) {
      const elevatedQuerySuppliers = auth.elevate(suppliers.querySuppliers);
      const response = await elevatedQuerySuppliers(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
