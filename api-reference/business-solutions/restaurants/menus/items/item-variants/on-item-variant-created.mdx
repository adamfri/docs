---
title: "onItemVariantCreated"
description: "The onItemVariantCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_variant_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemVariantCreated
  import { itemVariants } from '@wix/restaurants';

  itemVariants.onItemVariantCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemVariantCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemVariants } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemVariants,
      },
  });

  wixClient.itemVariants.onItemVariantCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
