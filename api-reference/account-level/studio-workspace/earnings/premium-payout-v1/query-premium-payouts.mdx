---
title: "Query Premium Payouts"
description: "Retrieves a list of up to 100 premium payouts, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 premium payouts, given the provided paging, filtering, and sorting.

    Query Premium Payouts runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionAmount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `amount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `paymentRevenueSharePercent` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `premiumProductId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.REVSHARE">SCOPE.PARTNERS.REVSHARE</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/partners/revshare/v1/premium-payouts/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="premiumPayouts" type="PremiumPayout[]">

      Retrieved premium payouts.

      See the [Premium Payout Object](/api-reference/account-level/studio-workspace/earnings/premium-payout-v1/premium-payout-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Queries a partner's premium revenue-share payouts for a month, sorted by transaction date.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/partners/revshare/v1/premium-payouts/query' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "query": {
            "filter": {
              "transactionDate": {
                "$gte": "2026-03-01T00:00:00.000Z",
                "$lt": "2026-04-01T00:00:00.000Z"
              }
            },
            "sort": [
              { "fieldName": "transactionDate", "order": "ASC" }
            ],
            "cursorPaging": { "limit": 100 }
          }
        }'
      ```

      ```json Response
      {
        "premiumPayouts": [
          {
            "id": "3f29c1d4-7b8a-4e2f-9c11-5a6b7c8d9e01",
            "revision": "2",
            "createdDate": "2026-03-04T08:12:45.120Z",
            "updatedDate": "2026-03-18T00:00:00.000Z",
            "premiumProductId": "premium-business-vip",
            "premiumPlanType": "WIX_COM",
            "metaSiteId": "b2c3d4e5-f6a7-4890-8bcd-2345678901bc",
            "siteName": "Harborline Studio",
            "status": "APPROVED",
            "transactionAmount": 192.00,
            "amount": 28.80,
            "negative": false,
            "paymentRevenueSharePercent": 15.0,
            "transactionDate": "2026-03-04T00:00:00.000Z",
            "invoiceId": "REF-2026-03-001"
          },
          {
            "id": "8e1f0a2b-4c3d-4e5f-9081-2a3b4c5d6e7f",
            "revision": "1",
            "createdDate": "2026-03-21T10:45:02.880Z",
            "updatedDate": "2026-03-21T10:45:02.880Z",
            "premiumProductId": "studio-pro",
            "premiumPlanType": "STUDIO",
            "metaSiteId": "c3d4e5f6-a7b8-4901-9cde-3456789012cd",
            "siteName": "Northgate Dental",
            "status": "PENDING",
            "transactionAmount": 240.00,
            "amount": 36.00,
            "negative": false,
            "paymentRevenueSharePercent": 15.0,
            "transactionDate": "2026-03-21T00:00:00.000Z"
          }
        ],
        "pagingMetadata": {
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 100 premium payouts, given the provided paging, filtering, and sorting.

    Query Premium Payouts runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `transactionAmount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `amount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `paymentRevenueSharePercent` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `premiumProductId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PARTNERS.REVSHARE">SCOPE.PARTNERS.REVSHARE</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { revsharePremiumPayouts } from '@wix/revshare-premium-payouts';

    revsharePremiumPayouts.queryPremiumPayouts(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="PremiumPayoutQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="premiumPayouts" type="PremiumPayout[]">

      Retrieved premium payouts.

      See the [Premium Payout Object](/api-reference/account-level/studio-workspace/earnings/premium-payout-v1/premium-payout-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-premium-payouts" id="ex-sdk-query-premium-payouts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-premium-payouts" id="ex-sdk-query-premium-payouts-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-premium-payouts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-premium-payouts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query Premium Payouts</span><span className="wix-picker-desc">Queries a partner's premium revenue-share payouts for a month, sorted by transaction date</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPremiumPayouts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-premium-payouts-0"><span className="wix-picker-name">Query Premium Payouts</span><span className="wix-picker-desc">Queries a partner's premium revenue-share payouts for a month, sorted by transaction date</span></label>
    <label htmlFor="ex-sdk-query-premium-payouts-1"><span className="wix-picker-name">queryPremiumPayouts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query Premium Payouts
    import { revsharePremiumPayouts } from "@wix/revshare-premium-payouts";

    const query = {
      filter: {
        transactionDate: {
          $gte: "2026-03-01T00:00:00.000Z",
          $lt: "2026-04-01T00:00:00.000Z",
        },
      },
      sort: [{ fieldName: "transactionDate", order: "ASC" }],
      cursorPaging: { limit: 100 },
    };

    async function queryPremiumPayouts() {
      const response = await revsharePremiumPayouts.queryPremiumPayouts(query);
    }

    /* Promise resolves to:
    {
      "premiumPayouts": [
        {
          "_id": "3f29c1d4-7b8a-4e2f-9c11-5a6b7c8d9e01",
          "revision": "2",
          "_createdDate": "2026-03-04T08:12:45.120Z",
          "_updatedDate": "2026-03-18T00:00:00.000Z",
          "premiumProductId": "premium-business-vip",
          "premiumPlanType": "WIX_COM",
          "metaSiteId": "b2c3d4e5-f6a7-4890-8bcd-2345678901bc",
          "siteName": "Harborline Studio",
          "status": "APPROVED",
          "transactionAmount": 192.00,
          "amount": 28.80,
          "negative": false,
          "paymentRevenueSharePercent": 15.0,
          "transactionDate": "2026-03-04T00:00:00.000Z",
          "invoiceId": "REF-2026-03-001"
        },
        {
          "_id": "8e1f0a2b-4c3d-4e5f-9081-2a3b4c5d6e7f",
          "revision": "1",
          "_createdDate": "2026-03-21T10:45:02.880Z",
          "_updatedDate": "2026-03-21T10:45:02.880Z",
          "premiumProductId": "studio-pro",
          "premiumPlanType": "STUDIO",
          "metaSiteId": "c3d4e5f6-a7b8-4901-9cde-3456789012cd",
          "siteName": "Northgate Dental",
          "status": "PENDING",
          "transactionAmount": 240.00,
          "amount": 36.00,
          "negative": false,
          "paymentRevenueSharePercent": 15.0,
          "transactionDate": "2026-03-21T00:00:00.000Z"
        }
      ],
      "pagingMetadata": {
        "cursors": {},
        "hasNext": false
      }
    }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPremiumPayouts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { revsharePremiumPayouts } from '@wix/revshare-premium-payouts';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { revsharePremiumPayouts },
      // Include the auth strategy and host as relevant
    });

    async function queryPremiumPayouts(query) {
      const response = await myWixClient.revsharePremiumPayouts.queryPremiumPayouts(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
