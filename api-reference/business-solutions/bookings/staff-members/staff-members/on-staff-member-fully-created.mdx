---
title: "onStaffMemberFullyCreated"
description: "Triggered when a staff member is created."
icon: "bolt"
---

Triggered when a staff member is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.staff.v1.staff_member_fully_created` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onStaffMemberFullyCreated
  import { staffMembers } from '@wix/bookings';

  staffMembers.onStaffMemberFullyCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onStaffMemberFullyCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { staffMembers } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        staffMembers,
      },
  });

  wixClient.staffMembers.onStaffMemberFullyCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
