---
title: "onBookingCanceled"
description: "Triggered when a booking is canceled."
icon: "bolt"
---

Triggered when a booking is canceled.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.v2.booking_canceled` |
| Package | `@wix/bookings` |
| Permissions | `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions`, `Read bookings calendar - including participants` |

## Examples

<CodeGroup>

  ```javascript onBookingCanceled
  import { bookings } from '@wix/bookings';

  bookings.onBookingCanceled((event) => {
      // handle your event here
  });
  ```

  ```javascript onBookingCanceled (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bookings } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bookings,
      },
  });

  wixClient.bookings.onBookingCanceled((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
