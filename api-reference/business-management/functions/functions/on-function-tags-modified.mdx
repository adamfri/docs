---
title: "onFunctionTagsModified"
description: "Triggered when tags are modified."
icon: "bolt"
---

Triggered when tags are modified.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.functions.v1.function_tags_modified` |
| Package | `@wix/functions` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Manage Stores`, `View Forms`, `Manage form submissions.`, `Manage forms`, `Manage Events`, `Manage Functions`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onFunctionTagsModified
  import { functions } from '@wix/functions';

  functions.onFunctionTagsModified((event) => {
      // handle your event here
  });
  ```

  ```javascript onFunctionTagsModified (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { functions } from '@wix/functions';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        functions,
      },
  });

  wixClient.functions.onFunctionTagsModified((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
