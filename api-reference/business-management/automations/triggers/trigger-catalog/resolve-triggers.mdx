---
title: "Resolve Triggers"
description: "Retrieves the triggers available on the current site."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the triggers available on the current site.

    Resolve Triggers returns only the triggers provided by apps that are installed on the current site, so it requires a site context. To retrieve all triggers in the catalog regardless of installation, call Query Triggers instead.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/v1/triggers/resolve
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2">

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="paging" type="Paging">

              Paging options to limit and offset the number of items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="appId" type="string">

      If provided, all triggers associated with the app ID are retrieved, regardless of maturity. The app must be installed on the site.

    </ParamField>

    <ParamField body="basicFieldsOnly" type="boolean">

      When `true`, the trigger payload schema, automation config schema, and filters are not returned.
      Default: `false`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="Trigger[]">

      List of retrieved triggers.

      See the [Trigger Object](/api-reference/business-management/automations/triggers/trigger-catalog/trigger-object) for this object's fields.

    </ResponseField>

    <ResponseField name="paging" type="PagingMetadataV2">

      Paging metadata of the response.

      <Expandable title="paging properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/v1/triggers/resolve' \
          -H 'Authorization:<AUTH>' \
          -H 'Content-Type:application/json' \
          -d "{\"query\":{\"paging\":{\"limit\":1}}}"
      ```

      ```json Response
      {
        "results": [
          {
            "appId": "225dd912-7dea-4738-8688-4b8c6955ffc2",
            "triggerKey": "wix_form_app-form_specific_submit",
            "payloadDataSchema": {
              "$schema": "http://json-schema.org/draft-07/schema",
              "additionalProperties": true,
              "properties": {
                "contactId": {
                  "description": "A unique identifier for the contact who submitted the form.",
                  "examples": [
                    "edca2245-7ce3-4d95-bfe9-b2012110eb8f"
                  ],
                  "format": "uuid",
                  "identityType": "contact",
                  "title": "Contact ID",
                  "type": "string"
                },
                "fieldId": {
                  "description": "A unique identifier for a specific form field that was submitted.",
                  "examples": [
                    "edca2245-7ce3-4d95-bfe9-b2012110eb8f"
                  ],
                  "title": "Field ID",
                  "type": "string"
                },
                "filterValue": {
                  "description": "The option chosen on a choice field.",
                  "examples": [
                    "Pepperoni"
                  ],
                  "title": "Option chosen",
                  "type": "string"
                },
                "formId": {
                  "description": "A unique identifier for a specific form that was submitted.",
                  "examples": [
                    "edca2245-7ce3-4d95-bfe9-b2012110eb8f"
                  ],
                  "title": "Form ID",
                  "type": "string"
                },
                "formName": {
                  "description": "The name of the form.",
                  "examples": [
                    "My form"
                  ],
                  "title": "Form name",
                  "type": "string"
                },
                "submissionId": {
                  "description": "A unique identifier for a specific form submission.",
                  "examples": [
                    "edca2245-7ce3-4d95-bfe9-b2012110eb8f"
                  ],
                  "format": "uuid",
                  "title": "Submission ID",
                  "type": "string"
                },
                "submissionTime": {
                  "description": "The date and time the form was submitted.",
                  "examples": [
                    "2018-11-13T20:20:39+00:00"
                  ],
                  "format": "date-time",
                  "title": "Submission date and time",
                  "type": "string"
                }
              },
              "required": [],
              "type": "object"
            },
            "displayName": "Option chosen on a form",
            "maturity": "GA",
            "filters": [
              {
                "id": "ef44c787-ea33-4818-af3b-8a7b5b1d69a9",
                "fieldKeyType": "STATIC_FIELD_KEY",
                "staticFieldKey": "formId",
                "valueInput": {
                  "type": "ENTITY_SELECTOR",
                  "entitySelector": {
                    "id": "225dd912-7dea-4738-8688-4b8c6955ffc2_formAppSelectionProvider",
                    "queryFieldToFilterIdMapping": {},
                    "multiSelect": false,
                    "queryFieldToValueMapping": {
                      "formFilterType": "forms-with-selection-fields",
                      "namespace": "wix.form_app.form"
                    }
                  },
                  "label": "Select a form",
                  "description": "Only forms with choice fields are available. Choice fields are single and multi choice, dropdown & checkbox.",
                  "required": true,
                  "reevaluateDynamicSchema": true
                },
                "followUpFilters": [
                  {
                    "id": "ef44c787-ea33-4818-af3b-8a7b5b1d69a8",
                    "fieldKeyType": "DYNAMIC_FIELD_KEY",
                    "dynamicFieldKey": {
                      "label": "Select a field",
                      "description": "Select a choice field. Choice fields are single and multi choice, dropdown & checkbox.",
                      "entitySelector": {
                        "id": "225dd912-7dea-4738-8688-4b8c6955ffc2_formAppSelectionProvider",
                        "queryFieldToFilterIdMapping": {
                          "formId": "ef44c787-ea33-4818-af3b-8a7b5b1d69a9"
                        },
                        "multiSelect": false,
                        "queryFieldToValueMapping": {
                          "formFilterType": "form-fields",
                          "namespace": "wix.form_app.form"
                        }
                      }
                    },
                    "valueInput": {
                      "type": "ENTITY_SELECTOR",
                      "entitySelector": {
                        "id": "225dd912-7dea-4738-8688-4b8c6955ffc2_formAppSelectionProvider",
                        "queryFieldToFilterIdMapping": {
                          "fieldId": "ef44c787-ea33-4818-af3b-8a7b5b1d69a8",
                          "formId": "ef44c787-ea33-4818-af3b-8a7b5b1d69a9"
                        },
                        "multiSelect": true,
                        "queryFieldToValueMapping": {
                          "formFilterType": "form-field-values",
                          "namespace": "wix.form_app.form"
                        }
                      },
                      "label": "Select one or more choices",
                      "description": "If a visitor selects any of these choices, this automation will be triggered.",
                      "required": true,
                      "reevaluateDynamicSchema": true
                    },
                    "followUpFilters": []
                  }
                ]
              }
            ],
            "metadata": {
              "hidden": false
            },
            "implementedMethods": {
              "validateConfiguration": true,
              "getDynamicSchema": true,
              "refreshPayload": false
            },
            "source": {
              "type": "DEV_CENTER"
            }
          }
        ],
        "paging": {
          "count": 1,
          "offset": 0,
          "total": 84,
          "hasNext": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the triggers available on the current site.

    Resolve Triggers returns only the triggers provided by apps that are installed on the current site, so it requires a site context. To retrieve all triggers in the catalog regardless of installation, call Query Triggers instead.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { triggerCatalog } from '@wix/automations';

    triggerCatalog.resolveTriggers(options): Promise<ResolveTriggersResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ResolveTriggersOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="QueryV2">

          Query options.

          <Expandable title="query properties">

            <ParamField body="filter" type="object">

              Filter object.

              Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object.

              Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="fields" type="string[]">

              Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

            <ParamField body="fieldsets" type="string[]">

              Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="pagingMethod" type="pagingMethod">

              pagingMethod

              _Nested structure omitted for brevity — see the `pagingMethod` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="appId" type="string">

          If provided, all triggers associated with the app ID are retrieved, regardless of maturity. The app must be installed on the site.

        </ParamField>

        <ParamField body="basicFieldsOnly" type="boolean">

          When `true`, the trigger payload schema, automation config schema, and filters are not returned.
          Default: `false`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ResolveTriggersResponse>`.

    <ResponseField name="results" type="Trigger[]">

      List of retrieved triggers.

      See the [Trigger Object](/api-reference/business-management/automations/triggers/trigger-catalog/trigger-object) for this object's fields.

    </ResponseField>

    <ResponseField name="paging" type="PagingMetadataV2">

      Paging metadata of the response.

      <Expandable title="paging properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-resolve-triggers" id="ex-sdk-resolve-triggers-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-resolve-triggers" id="ex-sdk-resolve-triggers-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-resolve-triggers-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-resolve-triggers-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Resolve Triggers</span><span className="wix-picker-desc">Returns triggers installed on the site with paging</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">resolveTriggers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-resolve-triggers-0"><span className="wix-picker-name">Resolve Triggers</span><span className="wix-picker-desc">Returns triggers installed on the site with paging</span></label>
    <label htmlFor="ex-sdk-resolve-triggers-1"><span className="wix-picker-name">resolveTriggers (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Resolve Triggers
    import { triggerCatalog } from "@wix/automations";

    async function resolveTriggers() {
      const response = await triggerCatalog.resolveTriggers({
        query: {
          paging: { limit: 1 },
        },
      });
      return response;
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "appId": "0f5829ee-2c2a-4ba1-b1e2-2caa8edbf188",
     *       "triggerKey": "analytics_ng_reports-subscription_v1",
     *       "payloadDataSchema": {
     *         "type": "object",
     *         "$schema": "http://json-schema.org/draft-07/schema",
     *         "properties": {
     *           "emailBody": { "type": "string", "title": "Email body" },
     *           "emailSubject": { "type": "string", "title": "Email subject" },
     *           "contactId": { "format": "uuid", "title": "Contact id", "type": "string" }
     *         }
     *       },
     *       "displayName": "Site subscribed to Native Reports v1",
     *       "maturity": "BETA",
     *       "filters": [],
     *       "metadata": { "hidden": true },
     *       "implementedMethods": {
     *         "validateConfiguration": false,
     *         "getDynamicSchema": false,
     *         "refreshPayload": false
     *       },
     *       "source": { "type": "DEV_CENTER" },
     *       "namespace": "automations",
     *       "componentId": "a66fafde-7447-45cd-88b6-3165d5946144",
     *       "componentVersion": "19",
     *       "_id": "05ef43cb-d9ca-43de-96c9-457b0d21a65d",
     *       "_createdDate": "2025-08-17T07:36:18.174Z",
     *       "_updatedDate": "2025-11-24T08:43:19.371Z"
     *     }
     *   ],
     *   "paging": {
     *     "count": 1,
     *     "offset": 0,
     *     "total": 154,
     *     "hasNext": true
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript resolveTriggers (self-hosted)
    import { createClient } from '@wix/sdk';
    import { triggerCatalog } from '@wix/automations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { triggerCatalog },
      // Include the auth strategy and host as relevant
    });

    async function resolveTriggers(options) {
      const response = await myWixClient.triggerCatalog.resolveTriggers(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
