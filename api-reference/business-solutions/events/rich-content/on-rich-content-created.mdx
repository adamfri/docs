---
title: "onRichContentCreated"
description: "Triggered when rich content is created."
icon: "bolt"
---

Triggered when rich content is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v1.rich_content_created` |
| Package | `@wix/events` |
| Permissions | `Manage Stores`, `Read Events - all read permissions`, `Manage Bookings Services and Settings`, `Manage Events - all permissions`, `Manage Events`, `Read Events`, `Manage Challenges`, `Manage Portfolio`, `Manage Blog`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Manage Guest List` |

## Examples

<CodeGroup>

  ```javascript onRichContentCreated
  import { ricos } from '@wix/events';

  ricos.onRichContentCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRichContentCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ricos } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        ricos,
      },
  });

  wixClient.ricos.onRichContentCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
