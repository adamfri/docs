---
title: "onRefundCreated"
description: "The onRefundCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.payments.refunds.v1.refund_created` |
| Package | `@wix/payments` |
| Permissions | `Manage Refunds`, `Managed Payments- All Permissions` |

## Examples

<CodeGroup>

  ```javascript onRefundCreated
  import { refunds } from '@wix/payments';

  refunds.onRefundCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRefundCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { refunds } from '@wix/payments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        refunds,
      },
  });

  wixClient.refunds.onRefundCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
