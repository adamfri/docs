---
title: "Bulk Update Label Order"
description: "Updates a question entry's visibility."
icon: "upload"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Warning>

  **Deprecated.** This method is deprecated.

</Warning>

Updates a question entry's visibility.

<Note>

  **Authentication**

  You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

</Note>

## Permissions

This method requires one of the following permission scopes:

<div className="wix-scopes">

<span className="wix-scope" title="SCOPE.DC-LABS.MANAGE-FAQ">Manage FAQ</span>

</div>

Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

Callable by: `APP`.

## Endpoint

```http
POST https://www.wixapis.com/faq/v1/question-entries/update-label-order
```

## Body Params

<ParamField body="updates" type="QuestionLabelUpdate[]">

  List of updates for questions within a label, each containing the ID of the question to update, and optional new values for label and its order.

  <Expandable title="updates properties">

    <ParamField body="questionId" type="string">

      ID of question entry to update.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="label" type="Label">

      Label to add or update.

      <Expandable title="label properties">

        <ParamField body="title" type="string">

          Label title.

          Constraints: `minLength 1`, `maxLength 36`.

        </ParamField>

        <ParamField body="sortOrder" type="number">

          Order of question entry within label.

        </ParamField>

      </Expandable>

    </ParamField>

  </Expandable>

</ParamField>

## Response Object

<ResponseField name="failures" type="string[]">

  List of failed question entry IDs that should retried.

  Constraints: `format GUID`.

</ResponseField>

## Errors

<Note>

  This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

</Note>
