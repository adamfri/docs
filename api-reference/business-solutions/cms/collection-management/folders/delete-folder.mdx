---
title: "Delete Folder"
description: "Deletes a folder."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a folder.

    Collection references are moved to the root folder.

    <Note>

    The root folder can't be deleted.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.DATA-COLLECTIONS-MANAGE">Manage Data Collections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/wix-data/v1/folders/{folderId}
    ```

    ## Path Params

    <ParamField path="folderId" type="string" required>

      Folder ID.

    </ParamField>

    ## Response Object

    Returns `DeleteFolderResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `COLLECTION_REFERENCE_EXISTS` | A collection reference from the deleted folder already exists in the root folder. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE https://wixapis.com/autocms/v1/folders/593c732f-d781-4457-b4a9-7ad73ddf4b1a  \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a folder.

    Collection references are moved to the root folder.

    <Note>

    The root folder can't be deleted.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.DATA-COLLECTIONS-MANAGE">Manage Data Collections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { folders } from '@wix/autocms-folders-service';

    folders.deleteFolder(folderId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="folderId" type="string" required>

      Folder ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `COLLECTION_REFERENCE_EXISTS` | A collection reference from the deleted folder already exists in the root folder. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Folder Deleted | `wix.cloud.autocms.v1.folder_deleted` | This event is never triggered. When a folder is deleted, the [Folder Updated](https://dev.wix.com/docs/rest/business-solutions/cms/folders/folder-updated) event is triggered. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-folder" id="ex-sdk-delete-folder-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-folder" id="ex-sdk-delete-folder-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-folder" id="ex-sdk-delete-folder-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-folder-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-folder-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteFolder</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteFolder (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteFolder (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-folder-0"><span className="wix-picker-name">deleteFolder</span></label>
    <label htmlFor="ex-sdk-delete-folder-1"><span className="wix-picker-name">deleteFolder (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-folder-2"><span className="wix-picker-name">deleteFolder (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteFolder
    import { folders } from '@wix/autocms-folders-service';

    async function deleteFolder(folderId) {
      const response = await folders.deleteFolder(folderId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteFolder (self-hosted)
    import { createClient } from '@wix/sdk';
    import { folders } from '@wix/autocms-folders-service';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { folders },
      // Include the auth strategy and host as relevant
    });

    async function deleteFolder(folderId) {
      const response = await myWixClient.folders.deleteFolder(folderId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteFolder (with elevated permissions)
    import { folders } from '@wix/autocms-folders-service';
    import { auth } from '@wix/essentials';

    async function myDeleteFolderMethod(folderId) {
      const elevatedDeleteFolder = auth.elevate(folders.deleteFolder);
      const response = await elevatedDeleteFolder(folderId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
