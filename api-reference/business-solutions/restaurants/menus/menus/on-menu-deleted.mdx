---
title: "onMenuDeleted"
description: "Triggered when a menu is deleted."
icon: "bolt"
---

Triggered when a menu is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.menu_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onMenuDeleted
  import { menus } from '@wix/restaurants';

  menus.onMenuDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onMenuDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { menus } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        menus,
      },
  });

  wixClient.menus.onMenuDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
