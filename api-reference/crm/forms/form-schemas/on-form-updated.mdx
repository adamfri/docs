---
title: "onFormUpdated"
description: "Triggers when a form schema is updated."
icon: "bolt"
---

Triggers when a form schema is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forms.v4.form_updated` |
| Package | `@wix/forms` |
| Permissions | `Manage Submissions`, `Manage Stores`, `Manage form submissions.`, `Read Intake Form (PII)`, `Manage Online Programs`, `Read Intake Form`, `Manage Intake Form`, `Manage Intake Form (PII)`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onFormUpdated
  import { forms } from '@wix/forms';

  forms.onFormUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onFormUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { forms } from '@wix/forms';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        forms,
      },
  });

  wixClient.forms.onFormUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
