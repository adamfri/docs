---
title: "Create Category"
description: "Creates a category."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a category.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/categories
    ```

    ## Body Params

    <ParamField body="category" type="Category" required>

      Category to create.

      See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="category" type="Category">

      Created category.

      See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/events/v1/categories' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "category": {
          "name": "leather",
          "states": ["MANUAL"]
        }
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "category": {
          "id": "e9779de3-a085-4255-b3a6-9559990d4436",
          "name": "leather",
          "states": ["MANUAL"],
          "createdDate": "2021-04-01T09:49:37.068Z"
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a category.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/events';

    categories.createCategory(category): Promise<Category>
    ```

    ## Method Parameters

    <ParamField body="category" type="Category" required>

      Category to create.

      See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Category>`.

    See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Created | `wix.events.v1.category_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-category" id="ex-sdk-create-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-category" id="ex-sdk-create-category-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-category" id="ex-sdk-create-category-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a category</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create a category (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-category-0"><span className="wix-picker-name">Create a category</span></label>
    <label htmlFor="ex-sdk-create-category-1"><span className="wix-picker-name">Create a category (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-create-category-2"><span className="wix-picker-name">createCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a category
    import { categories } from '@wix/events';

    /*
     * Sample category value:
     * {
     *  "name": "leather",
     *  "states": ["MANUAL"]
     * }
     */

    export async function myCreateCategoryFunction(category) {
      try {
        const createdCategory = await categories.createCategory(category);
        console.log('Created category: ', createdCategory);
        return createdCategory;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "name": "leather",
     *   "states": [
     *     "MANUAL"
     *   ],
     *   "_id": "7952f110-26fa-45a2-b9eb-d2997784659d",
     *   "_createdDate": "2024-04-23T11:16:38.455Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Create a category (with elevated permissions)
    import { categories } from '@wix/events';
    import { auth } from '@wix/essentials';

    const elevatedCreateCategory = auth.elevate(categories.createCategory);

    /*
     * Sample category value:
     * {
     *  "name": "leather",
     *  "states": ["MANUAL"]
     * }
     */

    export async function myCreateCategoryFunction(category) {
      try {
        const createdCategory = await elevatedCreateCategory(category);
        console.log('Created category: ', createdCategory);
        return createdCategory;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "name": "leather",
     *   "states": [
     *     "MANUAL"
     *   ],
     *   "_id": "7952f110-26fa-45a2-b9eb-d2997784659d",
     *   "_createdDate": "2024-04-23T11:16:38.455Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function createCategory(category) {
      const response = await myWixClient.categories.createCategory(category);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
