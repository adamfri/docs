---
title: "Create Seo Pattern"
description: "Creates a pattern for a page type that's still on its Wix default."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a pattern for a page type that's still on its Wix default.

    The pattern must contain at least one tag. To leave a page type on its Wix default,
    create nothing for it.

    To change a pattern that already exists, call [Set SEO
    Pattern](https://dev.wix.com/docs/api-reference/business-management/seo/seo-pattern-v1/set-seo-pattern).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/promote/seo/v1/seo-patterns/{pageType}
    ```

    ## Path Params

    <ParamField path="pageType" type="string" required>

      Page type to create a pattern for. For example, `STATIC_PAGE`, `BLOG_POST`, or
      `STORES_PRODUCT`.

    </ParamField>

    ## Body Params

    <ParamField body="seoPattern" type="SeoPattern" required>

      Pattern to create for the page type.

      `pattern` is the only field to send. The page type comes from the request path, and
      every other field is read-only. To create a pattern for a single page built from a
      Wix Data collection, set `pageId`.

      See the [Seo Pattern Object](/api-reference/business-management/seo/seo-pattern-v1/seo-pattern-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="seoPattern" type="SeoPattern">

      The created pattern, with `source` reporting that the site's own pattern is now in
      effect.

      See the [Seo Pattern Object](/api-reference/business-management/seo/seo-pattern-v1/seo-pattern-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_PAGE_TYPE` | Wix doesn't recognize the specified `pageType`. The error message lists the page types that are supported. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_PATTERN` | The pattern contains no tags, references a variable the page type doesn't offer, or contains a tag that isn't valid. When a variable is the problem, the error message lists the variables the page type offers. |
    | `409` | `ALREADY_EXISTS` | `PATTERN_ALREADY_EXISTS` | The site already has a pattern for this page type. Call Set SEO Pattern to change it. |
    | `404` | `NOT_FOUND` | `PAGE_TYPE_NOT_ON_SITE` | The page type is valid, but the Wix business solution that provides it isn't installed on the site. |
    | `400` | `INVALID_ARGUMENT` | `PAGE_ID_NOT_SUPPORTED` | A `pageId` was specified for a page type that has a single pattern for the whole type. Only `WIX_DATA_PAGE_ITEM` supports a per-page pattern. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/seo-metatags-server/v1/seo-patterns/STATIC_PAGE' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "seoPattern": {
          "pattern": {
            "tags": [
              {
                "type": "title",
                "children": "{{page.name}} | {{site.name}}"
              },
              {
                "type": "meta",
                "props": {
                  "name": "description",
                  "content": "{{page.name}} at {{site.business.name}}."
                }
              }
            ]
          }
        }
      }'
      ```

      ```json Response
      {
        "seoPattern": {
          "id": "STATIC_PAGE",
          "pageType": "STATIC_PAGE",
          "pattern": {
            "tags": [
              {
                "type": "title",
                "children": "{{page.name}} | {{site.name}}",
                "custom": false,
                "disabled": false
              },
              {
                "type": "meta",
                "props": {
                  "name": "description",
                  "content": "{{page.name}} at {{site.business.name}}."
                },
                "children": "",
                "custom": false,
                "disabled": false
              }
            ]
          },
          "defaultPattern": {
            "tags": [
              {
                "type": "title",
                "children": "{{page.name}} | {{site.name}}",
                "custom": false,
                "disabled": false
              },
              {
                "type": "meta",
                "props": {
                  "property": "og:title",
                  "content": "{{@TITLE}}"
                },
                "children": "",
                "custom": false,
                "disabled": false
              },
              {
                "type": "meta",
                "props": {
                  "property": "og:image",
                  "content": "{{site.image}}"
                },
                "children": "",
                "custom": false,
                "disabled": false
              },
              {
                "type": "link",
                "props": {
                  "rel": "canonical",
                  "href": "{{page.url}}"
                },
                "children": "",
                "custom": false,
                "disabled": false
              }
            ]
          },
          "source": "PATTERN_SOURCE_USER",
          "origin": "ORIGIN_UNSPECIFIED"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a pattern for a page type that's still on its Wix default.

    The pattern must contain at least one tag. To leave a page type on its Wix default,
    create nothing for it.

    To change a pattern that already exists, call [Set SEO
    Pattern](https://dev.wix.com/docs/api-reference/business-management/seo/seo-pattern-v1/set-seo-pattern).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { seoPatterns } from '@wix/seo';

    seoPatterns.createSeoPattern(pageType, seoPattern): Promise
    ```

    ## Method Parameters

    <ParamField body="pageType" type="string" required>

      Page type to create a pattern for. For example, `STATIC_PAGE`, `BLOG_POST`, or
      `STORES_PRODUCT`.

      Constraints: `maxLength 100`.

    </ParamField>

    <ParamField body="seoPattern" type="SeoPattern" required>

      Pattern to create for the page type.

      `pattern` is the only field to send. The page type comes from the request path, and
      every other field is read-only. To create a pattern for a single page built from a
      Wix Data collection, set `pageId`.

      See the [Seo Pattern Object](/api-reference/business-management/seo/seo-pattern-v1/seo-pattern-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `SeoPattern`.

    See the [Seo Pattern Object](/api-reference/business-management/seo/seo-pattern-v1/seo-pattern-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_PAGE_TYPE` | Wix doesn't recognize the specified `pageType`. The error message lists the page types that are supported. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_PATTERN` | The pattern contains no tags, references a variable the page type doesn't offer, or contains a tag that isn't valid. When a variable is the problem, the error message lists the variables the page type offers. |
    | `409` | `ALREADY_EXISTS` | `PATTERN_ALREADY_EXISTS` | The site already has a pattern for this page type. Call Set SEO Pattern to change it. |
    | `404` | `NOT_FOUND` | `PAGE_TYPE_NOT_ON_SITE` | The page type is valid, but the Wix business solution that provides it isn't installed on the site. |
    | `400` | `INVALID_ARGUMENT` | `PAGE_ID_NOT_SUPPORTED` | A `pageId` was specified for a page type that has a single pattern for the whole type. Only `WIX_DATA_PAGE_ITEM` supports a per-page pattern. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Seo Pattern Created | `wix.promote.seo.v1.seo_pattern_created` | CREATED is the declared domain event (emitted by CreateSeoPattern). UPDATED / DELETED are emitted at write time by SetSeoPattern / ResetSeoPatternToDefault, which are field-targeted custom actions rather than UPDATE/DELETE crud verbs. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-seo-pattern" id="ex-sdk-create-seo-pattern-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-seo-pattern" id="ex-sdk-create-seo-pattern-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-seo-pattern-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-seo-pattern-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a pattern for a page type</span><span className="wix-picker-desc">The page type had no pattern of its own. A pattern must contain at least one tag, and may reference only the variables the page type offers</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createSeoPattern (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-seo-pattern-0"><span className="wix-picker-name">Create a pattern for a page type</span><span className="wix-picker-desc">The page type had no pattern of its own. A pattern must contain at least one tag, and may reference only the variables the page type offers</span></label>
    <label htmlFor="ex-sdk-create-seo-pattern-1"><span className="wix-picker-name">createSeoPattern (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a pattern for a page type
    import { seoPatterns } from "@wix/seo";

    async function createSeoPattern() {
      const response = await seoPatterns.createSeoPattern("STATIC_PAGE", {
        pattern: {
          tags: [
            {
              type: "title",
              children: "{{page.name}} | {{site.name}}",
            },
            {
              type: "meta",
              props: {
                name: "description",
                content: "{{page.name}} at {{site.business.name}}.",
              },
            },
          ],
        },
      });
      return response;
    }

    /* Promise resolves to the entity BARE, not wrapped in `seoPattern` the way Set and Reset
     * return it:
     * {
     *   "pageType": "STATIC_PAGE",
     *   "pattern": {
     *     "tags": [
     *       {
     *         "type": "title",
     *         "children": "{{page.name}} | {{site.name}}",
     *         "custom": false,
     *         "disabled": false
     *       },
     *       {
     *         "type": "meta",
     *         "props": { "name": "description", "content": "{{page.name}} at {{site.business.name}}." },
     *         "children": "",
     *         "custom": false,
     *         "disabled": false
     *       }
     *     ]
     *   },
     *   "defaultPattern": {
     *     "tags": [
     *       {
     *         "type": "title",
     *         "children": "{{page.name}} | {{site.name}}",
     *         "custom": false,
     *         "disabled": false
     *       }
     *     ]
     *   },
     *   "source": "PATTERN_SOURCE_USER",
     *   "origin": "ORIGIN_UNSPECIFIED",
     *   "_id": "STATIC_PAGE"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createSeoPattern (self-hosted)
    import { createClient } from '@wix/sdk';
    import { seoPatterns } from '@wix/seo';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { seoPatterns },
      // Include the auth strategy and host as relevant
    });

    async function createSeoPattern(pageType,seoPattern) {
      const response = await myWixClient.seoPatterns.createSeoPattern(pageType,seoPattern);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
