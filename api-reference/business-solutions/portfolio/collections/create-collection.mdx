---
title: "Create Collection"
description: "Creates a collection."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a collection.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/portfolio/v1/collections
    ```

    ## Body Params

    <ParamField body="collection" type="Collection" required>

      Collection to create.

      See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="collection" type="Collection">

      Created collection.

      See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Collection Created | `wix.portfolio.collections.v1.collection_created` | Triggered when a collection is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/portfolio/v1/collections' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "collection": {
          "title": "New Collection Title",
          "description": "This is a new collection description.",
          "slug": "new-collection-title",
          "coverImage": {
            "imageInfo": {
              "id": "8c074c67-2926-4518-92ef-c79d7810b3a4"
            }
          },
          "hidden": false,
          "sortOrder": 1.0,
          "seoData": {
            "settings": {
              "keywords": [
                {
                "isMain": true,
                "origin": "marketing-persona",
                "term": "investment management"
                }
              ],
              "preventAutoRedirect": false
            },
            "tags": [
              {
                "children": "Investment Experts Share Growth Strategies",
                "custom": false,
                "disabled": false,
                "type": "title"
              },
              {
                "children": "",
                "custom": false,
                "disabled": false,
                "props": {
                "content": "Investment strategies explained by investment experts. Investment experts offer valuable insights for growth.",
                "name": "description"
                },
                "type": "meta"
              }
            ]
          }
        }
      }'
      ```

      ```json Response
      {
        "collection": {
          "id": "123e4567-e89b-12d3-a456-426614174000",
          "revision": "1",
          "title": "New Collection Title",
          "description": "This is a new collection description.",
          "slug": "new-collection-title",
          "coverImage": {
            "imageInfo": {
              "id": "8c074c67-2926-4518-92ef-c79d7810b3a4"
            }
          },
          "hidden": false,
          "sortOrder": 1.0,
          "createdDate": "2024-07-31T12:00:00Z",
          "updatedDate": "2024-07-31T12:00:00Z",
          "seoData": {
            "settings": {
              "keywords": [
                {
                "isMain": true,
                "origin": "marketing-persona",
                "term": "investment management"
                }
              ],
              "preventAutoRedirect": false
            },
            "tags": [
              {
                "children": "Investment Experts Share Growth Strategies",
                "custom": false,
                "disabled": false,
                "type": "title"
              },
              {
                "children": "",
                "custom": false,
                "disabled": false,
                "props": {
                "content": "Investment strategies explained by investment experts. Investment experts offer valuable insights for growth.",
                "name": "description"
                },
                "type": "meta"
              }
            ]
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a collection.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { collections } from '@wix/portfolio';

    collections.createCollection(collection): Promise<Collection>
    ```

    ## Method Parameters

    <ParamField body="collection" type="Collection" required>

      Collection to create.

      See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Collection>`.

    See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Collection Created | `wix.portfolio.collections.v1.collection_created` | Triggered when a collection is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-collection" id="ex-sdk-create-collection-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-collection" id="ex-sdk-create-collection-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-collection" id="ex-sdk-create-collection-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-collection-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-collection-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createCollection</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCollection (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-collection-0"><span className="wix-picker-name">createCollection</span></label>
    <label htmlFor="ex-sdk-create-collection-1"><span className="wix-picker-name">createCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-collection-2"><span className="wix-picker-name">createCollection (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createCollection
    import { collections } from '@wix/portfolio';

    async function createCollection(collection) {
      const response = await collections.createCollection(collection);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCollection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { collections } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { collections },
      // Include the auth strategy and host as relevant
    });

    async function createCollection(collection) {
      const response = await myWixClient.collections.createCollection(collection);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCollection (with elevated permissions)
    import { collections } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myCreateCollectionMethod(collection) {
      const elevatedCreateCollection = auth.elevate(collections.createCollection);
      const response = await elevatedCreateCollection(collection);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
