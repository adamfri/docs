---
title: "onBadgeAssigned"
description: "Triggered when a member receives a badge."
icon: "bolt"
---

Triggered when a member receives a badge.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v3.badge_badge_assigned` |
| Package | `@wix/members` |
| Permissions | `Manage Stores`, `Manage Challenges`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeAssigned
  import { badges } from '@wix/members';

  badges.onBadgeAssigned((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeAssigned (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badges } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        badges,
      },
  });

  wixClient.badges.onBadgeAssigned((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
