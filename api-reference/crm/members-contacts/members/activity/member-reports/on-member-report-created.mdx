---
title: "onMemberReportCreated"
description: "Triggered when a member is reported."
icon: "bolt"
---

Triggered when a member is reported.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.members.v1.member_report_created` |
| Package | `@wix/members` |
| Permissions | `Manage Members` |

## Examples

<CodeGroup>

  ```javascript onMemberReportCreated
  import { memberReport } from '@wix/members';

  memberReport.onMemberReportCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onMemberReportCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { memberReport } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        memberReport,
      },
  });

  wixClient.memberReport.onMemberReportCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
