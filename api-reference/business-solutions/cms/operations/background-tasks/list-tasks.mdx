---
title: "List Tasks"
description: "Lists all existing tasks."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Lists all existing tasks.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/cms/v1/tasks
    ```

    ## Query Params

    <ParamField query="paging" type="Paging">

      Paginating information. This has the following default values:
      - `limit`: 30
      - `offset`: 0

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of items to load.

        </ParamField>

        <ParamField query="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="tasks" type="Task[]">

      Retrieved tasks.

      See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://wixapis.com/autocms/v1/tasks?paging.offset=5  \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "tasks": [
        {
          "id": "097a5564-0fec-4425-8d1e-3e753b48a422",
          "type": "DELETE_BY_QUERY",
          "status": "NEW",
          "startedAt": "2024-09-05T10:34:59.507Z",
          "estimatedItemCount": 3,
          "itemsSucceeded": 0,
          "itemsFailed": 0,
          "failures": [],
          "deleteByQueryOptions": {
            "dataCollectionId": "Items",
            "environment": "LIVE",
            "filter": {
              "holidays": {
                "$in": [
                  "Halloween"
                ]
              }
            }
          }
        },
        {
          "id": "097a5564-0fec-4425-8d1e-3e753b48a422",
          "type": "EDIT_FIELD_DATA",
          "status": "NEW",
          "startedAt": "2024-09-05T10:34:59.507Z",
          "estimatedItemCount": 3,
          "itemsSucceeded": 0,
          "itemsFailed": 0,
          "failures": [],
          "editFieldDataOptions": {
            "dataCollectionId": "Items",
            "environment": "LIVE",
            "filter": {
              "holidays": {
                "$in": [
                  "Halloween"
                ]
              }
            },
            "fieldKey": "tags",
            "operations": {
              "addItems": [
                "sale"
              ],
              "removeItems": [
                "outdated"
              ],
              "replaceItems": [
                {
                  "from": "Goob",
                  "to": "Good"
                }
              ],
              "removeDuplicates": true
            }
          }
        }],
        "metadata": {
          "count": 0,
          "offset": 5,
          "total": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Lists all existing tasks.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tasks } from '@wix/data';

    tasks.listTasks(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="ListTasksOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="Paging">

          Paginating information. This has the following default values:
          - `limit`: 30
          - `offset`: 0

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="tasks" type="Task[]">

      Retrieved tasks.

      See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-tasks" id="ex-sdk-list-tasks-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-tasks" id="ex-sdk-list-tasks-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-tasks" id="ex-sdk-list-tasks-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-tasks-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-tasks-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listTasks</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listTasks (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listTasks (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-tasks-0"><span className="wix-picker-name">listTasks</span></label>
    <label htmlFor="ex-sdk-list-tasks-1"><span className="wix-picker-name">listTasks (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-tasks-2"><span className="wix-picker-name">listTasks (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listTasks
    import { tasks } from '@wix/data';

    async function listTasks(options) {
      const response = await tasks.listTasks(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listTasks (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tasks } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tasks },
      // Include the auth strategy and host as relevant
    });

    async function listTasks(options) {
      const response = await myWixClient.tasks.listTasks(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listTasks (with elevated permissions)
    import { tasks } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myListTasksMethod(options) {
      const elevatedListTasks = auth.elevate(tasks.listTasks);
      const response = await elevatedListTasks(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
