---
title: "onCommentCreated"
description: "Triggered when a comment is created."
icon: "bolt"
---

Triggered when a comment is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v2.comment_created` |
| Package | `@wix/comments` |
| Permissions | `Read Comments`, `Manage Comments` |

## Examples

<CodeGroup>

  ```javascript onCommentCreated
  import { comments } from '@wix/comments';

  comments.onCommentCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCommentCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { comments } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        comments,
      },
  });

  wixClient.comments.onCommentCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
