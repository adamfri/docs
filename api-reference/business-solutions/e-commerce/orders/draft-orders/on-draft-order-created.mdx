---
title: "onDraftOrderCreated"
description: "Triggered when a draft order is created."
icon: "bolt"
---

Triggered when a draft order is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.draft_order_created` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions` |

## Examples

<CodeGroup>

  ```javascript onDraftOrderCreated
  import { draftOrders } from '@wix/ecom';

  draftOrders.onDraftOrderCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onDraftOrderCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { draftOrders } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        draftOrders,
      },
  });

  wixClient.draftOrders.onDraftOrderCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
