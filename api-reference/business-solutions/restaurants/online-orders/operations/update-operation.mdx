---
title: "Update Operation"
description: "Updates an operation."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an operation.

    If you update part of the `orderScheduling` property, the whole object is overwritten,
    so you must include the entire object unless you are not updating `orderScheduling` at all. <br />

    Each time the operation is updated,
    `revision` increments by 1.
    The current `revision` must be passed when updating the operation.
    This ensures you're working with the latest operation
    and prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/restaurants/operations/v1/operations/{operation.id}
    ```

    ## Path Params

    <ParamField path="operation.id" type="string" required>

      Operation ID.

    </ParamField>

    ## Body Params

    <ParamField body="operation" type="Operation" required>

      Operation to update.

      See the [Operation Object](/api-reference/business-solutions/restaurants/online-orders/operations/operation-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="operation" type="Operation">

      Updated operation.

      See the [Operation Object](/api-reference/business-solutions/restaurants/online-orders/operations/operation-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Updated | `wix.restaurants.operations.v1.operation_updated` | Triggered when an operation is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH https://www.wixapis.com/restaurants-operations/v1/operations/48605ac5-53fc-49b4-96b2-87f7636f5ce0 \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>' \
        --data-raw '{
            "operation": {
              "fulfillmentIds": [],
              "revision": "5",
              "orderScheduling": {
                "type": "ASAP",
                "asapOptions": {
                  "preparationTime": {
                    "type": "TIME_RANGE",
                    "timeRangeOptions": {
                      "timeUnit": "MINUTES",
                      "minDuration": 20,
                      "maxDuration": 40
                    }
                  },
                  "asapFutureHandlingType": "BUSINESS_DAYS_AHEAD_HANDLING",
                  "businessDaysAheadHandlingOptions": {
                    "daysCount": 0
                  }
                }
              }
            }
          }'
      ```

      ```json Response
      {
        "operation": {
          "id": "48605ac5-53fc-49b4-96b2-87f7636f5ce0",
          "revision": "6",
          "createdDate": "2023-11-27T06:51:54.146Z",
          "updatedDate": "2023-12-10T08:32:03.316Z",
          "name": "Asap operation",
          "orderScheduling": {
            "type": "ASAP",
            "asapOptions": {
              "preparationTime": {
                "type": "TIME_RANGE",
                "timeRangeOptions": {
                  "timeUnit": "MINUTES",
                  "minDuration": 20,
                  "maxDuration": 40
                }
              },
              "asapFutureHandlingType": "BUSINESS_DAYS_AHEAD_HANDLING",
              "businessDaysAheadHandlingOptions": {
                "daysCount": 0
              }
            }
          },
          "default": true,
          "fulfillmentIds": [
            "12590791-d0d9-418f-a226-a7dcfd37b507",
            "71831b22-a043-4998-a10d-01bf9f2472f2",
            "642dd960-ab45-4eac-ab64-de0ad2215fe0",
            "32e82c15-f518-44e3-800f-406f13f9bb2e"
          ],
          "onlineOrderingStatus": "ENABLED"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates an operation.

    If you update part of the `orderScheduling` property, the whole object is overwritten,
    so you must include the entire object unless you are not updating `orderScheduling` at all. <br />

    Each time the operation is updated,
    `revision` increments by 1.
    The current `revision` must be passed when updating the operation.
    This ensures you're working with the latest operation
    and prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { operations } from '@wix/restaurants';

    operations.updateOperation(_id, operation): Promise<Operation>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Operation ID.

      **Read-only.**

    </ParamField>

    <ParamField body="operation" type="UpdateOperation" required>

      Operation to update.

      <Expandable title="operation properties">

        <ParamField body="_id" type="string">

          Operation ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number. Increments by 1 each time the operation is updated.
          To prevent conflicting changes,
          the existing `revision` must be specified when updating an operation.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the operation was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the operation was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Operation name.

        </ParamField>

        <ParamField body="default" type="boolean">

          Whether the operation is the default operation. &lt;br />
          Default: `false`.

        </ParamField>

        <ParamField body="fulfillmentIds" type="string[]">

          IDs of the fulfillment methods ([SDK](https://dev.wix.com/docs/sdk/backend-modules/restaurants/wix-restaurants-new/fulfillment-methods/introduction) | [REST](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/fulfillment-methods/introduction)) associated with the operation.

          Constraints: `format GUID`, `maxItems 500`.

        </ParamField>

        <ParamField body="onlineOrderingStatus" type="string">

          Online ordering status of the operation. &lt;br />

          **Supported values:**

          - `UNDEFINED_ONLINE_ORDERING_STATUS` — Online ordering status is not defined.
          - `ENABLED` — Operation currently accepts online orders.
          - `DISABLED` — Operation currently does not accept online orders.
          - `PAUSED_UNTIL` — Operation currently does not accept online orders, but will accept online orders from a specified time and date. When applied, `pausedUntilOptions` is a required field.

        </ParamField>

        <ParamField body="defaultFulfillmentType" type="string">

          Default fulfillment type of the operation.

          **Supported values:**

          - `PICKUP` — Pickup fulfillment. The customer picks up the order from the restaurant.
          - `DELIVERY` — Delivery fulfillment. The restaurant, or a representative, delivers the order to the customer.

        </ParamField>

        <ParamField body="orderScheduling" type="OrderScheduling">

          Information about when an order can be placed for.

          <Expandable title="orderScheduling properties">

            <ParamField body="type" type="string">

              Scheduling type. &lt;br />
              - When `ASAP`, `asapOptions` is a required field.
              - When `PREORDER`, `preorderOptions` is a required field.

              **Supported values:**

              - `ASAP` — Orders can be scheduled for the future and to be handled immediately.
              - `PREORDER` — Orders can be scheduled only for the future.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="orderSchedulingOptions" type="orderSchedulingOptions">

              orderSchedulingOptions

              _Nested structure omitted for brevity — see the `orderSchedulingOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="operationGroupId" type="string">

          ID of the operation group this operation belongs to.

        </ParamField>

        <ParamField body="businessLocationId" type="string">

          ID of the business location ([SDK](https://dev.wix.com/docs/sdk/backend-modules/restaurants/wix-restaurants-new/about-business-locations) | [REST](https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/about-business-locations)) of this operation.

          **Read-only.**

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Extended fields.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tags ([SDK](https://dev.wix.com/docs/sdk/backend-modules/tags/tags/introduction) | [REST](https://dev.wix.com/docs/rest/business-management/tags/introduction)) used to classify and sort different types of operations.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="onlineOrderingStatusOptions" type="onlineOrderingStatusOptions">

          onlineOrderingStatusOptions

          <Expandable title="onlineOrderingStatusOptions properties">

            <ParamField body="pausedUntilOptions" type="OnlineOrderingPausedUntilOptions">

              Options for online ordering status. Required when `onlineOrderingStatus` is `PAUSED_UNTIL`.

              _Nested structure omitted for brevity — see the `OnlineOrderingPausedUntilOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Operation>`.

    See the [Operation Object](/api-reference/business-solutions/restaurants/online-orders/operations/operation-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Updated | `wix.restaurants.operations.v1.operation_updated` | Triggered when an operation is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-operation" id="ex-sdk-update-operation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-operation" id="ex-sdk-update-operation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-operation" id="ex-sdk-update-operation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-operation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-operation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateOperation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateOperation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateOperation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-operation-0"><span className="wix-picker-name">updateOperation</span></label>
    <label htmlFor="ex-sdk-update-operation-1"><span className="wix-picker-name">updateOperation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-operation-2"><span className="wix-picker-name">updateOperation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateOperation
    import { operations } from '@wix/restaurants';

    async function updateOperation(_id,operation) {
      const response = await operations.updateOperation(_id,operation);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateOperation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { operations } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { operations },
      // Include the auth strategy and host as relevant
    });

    async function updateOperation(_id,operation) {
      const response = await myWixClient.operations.updateOperation(_id,operation);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateOperation (with elevated permissions)
    import { operations } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myUpdateOperationMethod(_id,operation) {
      const elevatedUpdateOperation = auth.elevate(operations.updateOperation);
      const response = await elevatedUpdateOperation(_id,operation);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
