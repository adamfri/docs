---
title: "Get Inventory Variants"
description: "Get Inventory Variants in the Wix Inventory API."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.READ-PRODUCTS">Read Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v2/inventoryItems/{inventoryId}/getVariants
    ```

    ## Body Params

    <ParamField body="variantIds" type="string[]">

      Variant IDs to query for this inventory item (optional).

    </ParamField>

    ## Response Object

    <ResponseField name="inventoryItem" type="InventoryItemV2">

      Inventory item.

      See the [Inventory Object](/api-reference/business-solutions/stores/catalog-v1/inventory/inventory-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST https://www.wixapis.com/stores/v2/inventoryItems/be547028-fb08-b962-75e8-3f3e56da9ee3/getVariants \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>' \
         --data-binary '
             {}
             }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "inventoryItem": {
           "id": "b22b2a79-52b3-e8e2-721e-1779a82dc414",
           "externalId": "4dd4d586-ad4c-171d-8de1-e88657d23beb",
           "productId": "4dd4d586-ad4c-171d-8de1-e88657d23beb",
           "trackQuantity": false,
           "variants": [
             {
               "variantId": "00000000-0000-0000-0000-000000000000",
               "inStock": false,
               "quantity": 0,
               "availableForPreorder": true
             }
           ],
           "lastUpdated": "2022-11-24T15:10:55.789Z",
           "numericId": "1669215583914000",
           "preorderInfo": {
             "enabled": true,
             "message": "Expected to ship on June"
           }
         }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.READ-PRODUCTS">Read Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { inventory } from '@wix/stores';

    inventory.getInventoryVariants(inventoryId, options): Promise<GetInventoryVariantsResponse>
    ```

    ## Method Parameters

    <ParamField body="inventoryId" type="string" required>

      Inventory item ID.

    </ParamField>

    <ParamField body="options" type="GetInventoryVariantsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="variantIds" type="string[]">

          Variant IDs to query for this inventory item (optional).

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="id" type="id">

          id

          <Expandable title="id properties">

            <ParamField body="inventoryId" type="string">

              Inventory item ID.

            </ParamField>

            <ParamField body="externalId" type="string" deprecated>

              Deprecated (use productID instead).

              **Deprecated.** Deprecated (use productID instead). Use `product_id` instead. Removal: 2024-12-31.

            </ParamField>

            <ParamField body="productId" type="string">

              Product ID.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<GetInventoryVariantsResponse>`.

    <ResponseField name="inventoryItem" type="InventoryItemV2">

      Inventory item.

      See the [Inventory Object](/api-reference/business-solutions/stores/catalog-v1/inventory/inventory-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-inventory-variants" id="ex-sdk-get-inventory-variants-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-inventory-variants" id="ex-sdk-get-inventory-variants-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-inventory-variants" id="ex-sdk-get-inventory-variants-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-inventory-variants-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-inventory-variants-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get inventory variants</span><span className="wix-picker-desc">Get inventory variants for a specific inventory item</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get inventory variants</span><span className="wix-picker-desc">Get inventory variants for a specific inventory item</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getInventoryVariants (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-inventory-variants-0"><span className="wix-picker-name">Get inventory variants</span><span className="wix-picker-desc">Get inventory variants for a specific inventory item</span></label>
    <label htmlFor="ex-sdk-get-inventory-variants-1"><span className="wix-picker-name">Get inventory variants</span><span className="wix-picker-desc">Get inventory variants for a specific inventory item</span></label>
    <label htmlFor="ex-sdk-get-inventory-variants-2"><span className="wix-picker-name">getInventoryVariants (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get inventory variants
    import { inventory } from "@wix/stores";

    const inventoryId = "3bc2c93a-3ca5-6e13-e3cc-2d71038f2a59";

    async function getInventoryVariants() {
      const response = await inventory.getInventoryVariants(inventoryId);
      return response;
    }

    const result = await inventory.getInventoryVariants(inventoryId);

    /* Promise resolves to:
     * {
     *   "inventoryItem": {
     *     "externalId": "c43d36c5-c35a-91ec-1c33-d28efc70d5a6",
     *     "productId": "c43d36c5-c35a-91ec-1c33-d28efc70d5a6",
     *     "trackQuantity": true,
     *     "variants": [
     *       {
     *         "variantId": "00000000-0000-0000-0000-000000000000",
     *         "inStock": true,
     *         "quantity": 10,
     *         "availableForPreorder": false
     *       }
     *     ],
     *     "numericId": "1766909903931000",
     *     "preorderInfo": {
     *       "enabled": false
     *     },
     *     "_id": "3bc2c93a-3ca5-6e13-e3cc-2d71038f2a59"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get inventory variants
    import { inventory } from "@wix/stores";

    const inventoryId = "3bc2c93a-3ca5-6e13-e3cc-2d71038f2a59";

    async function getInventoryVariants() {
      const response = await inventory.getInventoryVariants(inventoryId);
      return response;
    }

    const result = await inventory.getInventoryVariants(inventoryId);

    /* Promise resolves to:
     * {
     *   "inventoryItem": {
     *     "externalId": "c43d36c5-c35a-91ec-1c33-d28efc70d5a6",
     *     "productId": "c43d36c5-c35a-91ec-1c33-d28efc70d5a6",
     *     "trackQuantity": true,
     *     "variants": [
     *       {
     *         "variantId": "00000000-0000-0000-0000-000000000000",
     *         "inStock": true,
     *         "quantity": 10,
     *         "availableForPreorder": false
     *       }
     *     ],
     *     "numericId": "1766909903931000",
     *     "preorderInfo": {
     *       "enabled": false
     *     },
     *     "_id": "3bc2c93a-3ca5-6e13-e3cc-2d71038f2a59"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getInventoryVariants (self-hosted)
    import { createClient } from '@wix/sdk';
    import { inventory } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { inventory },
      // Include the auth strategy and host as relevant
    });

    async function getInventoryVariants(inventoryId,options) {
      const response = await myWixClient.inventory.getInventoryVariants(inventoryId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
