---
title: "Delete Badge"
description: "Deletes a badge."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a badge.

    Deleting a badge will remove it from all members who currently have it assigned.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-BADGES">Manage Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/badges/v4/badges/{badgeId}
    ```

    ## Path Params

    <ParamField path="badgeId" type="string" required>

      Badge ID.

    </ParamField>

    ## Response Object

    Returns `DeleteBadgeResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Badge Deleted | `wix.badges.v4.badge_deleted` | Triggered when a badge is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/badges/v4/badges/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a badge.

    Deleting a badge will remove it from all members who currently have it assigned.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-BADGES">Manage Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { badgesV2 } from '@wix/members';

    badgesV2.deleteBadge(badgeId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="badgeId" type="string" required>

      Badge ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Badge Deleted | `wix.badges.v4.badge_deleted` | Triggered when a badge is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-badge" id="ex-sdk-delete-badge-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-badge" id="ex-sdk-delete-badge-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-badge" id="ex-sdk-delete-badge-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-badge-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-badge-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteBadge</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBadge (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBadge (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-badge-0"><span className="wix-picker-name">deleteBadge</span></label>
    <label htmlFor="ex-sdk-delete-badge-1"><span className="wix-picker-name">deleteBadge (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-badge-2"><span className="wix-picker-name">deleteBadge (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteBadge
    import { badgesV2 } from '@wix/members';

    async function deleteBadge(badgeId) {
      const response = await badgesV2.deleteBadge(badgeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBadge (self-hosted)
    import { createClient } from '@wix/sdk';
    import { badgesV2 } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { badgesV2 },
      // Include the auth strategy and host as relevant
    });

    async function deleteBadge(badgeId) {
      const response = await myWixClient.badgesV2.deleteBadge(badgeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBadge (with elevated permissions)
    import { badgesV2 } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myDeleteBadgeMethod(badgeId) {
      const elevatedDeleteBadge = auth.elevate(badgesV2.deleteBadge);
      const response = await elevatedDeleteBadge(badgeId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
