---
title: "onFollowMemberUnfollowed"
description: "Triggered when a member unfollows another member."
icon: "bolt"
---

Triggered when a member unfollows another member.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.members.v3.follow_member_unfollowed` |
| Package | `@wix/members` |
| Permissions | `Manage Stores`, `Manage Members`, `Read Member Connections` |

## Examples

<CodeGroup>

  ```javascript onFollowMemberUnfollowed
  import { memberFollowers } from '@wix/members';

  memberFollowers.onFollowMemberUnfollowed((event) => {
      // handle your event here
  });
  ```

  ```javascript onFollowMemberUnfollowed (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { memberFollowers } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        memberFollowers,
      },
  });

  wixClient.memberFollowers.onFollowMemberUnfollowed((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
