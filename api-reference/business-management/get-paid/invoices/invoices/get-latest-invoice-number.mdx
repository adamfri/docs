---
title: "Get Latest Invoice Number"
description: "Retrieves the highest invoice number currently in use."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the highest invoice number currently in use.

    Invoice numbering is managed in the [Invoices Settings API](https://dev.wix.com/docs/rest/business-management/get-paid/invoices/invoices-settings/introduction).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/invoices/v4/invoices/get-latest-number
    ```

    ## Query Params

    <ParamField query="prefix" type="string">

      Invoice number prefix to filter by.

    </ParamField>

    ## Response Object

    <ResponseField name="number" type="integer">

      Highest invoice number currently in use for the specified prefix.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieve the latest invoice number for a specific business.

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/invoices/v4/invoices/get-latest-number?prefix=2025-' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "number": 1000
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the highest invoice number currently in use.

    Invoice numbering is managed in the [Invoices Settings API](https://dev.wix.com/docs/rest/business-management/get-paid/invoices/invoices-settings/introduction).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { invoices } from '@wix/get-paid';

    invoices.getLatestInvoiceNumber(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="GetLatestInvoiceNumberOptions">

      options

      <Expandable title="options properties">

        <ParamField body="prefix" type="string">

          Invoice number prefix to filter by.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="number" type="integer">

      Highest invoice number currently in use for the specified prefix.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-latest-invoice-number" id="ex-sdk-get-latest-invoice-number-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-latest-invoice-number" id="ex-sdk-get-latest-invoice-number-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-latest-invoice-number" id="ex-sdk-get-latest-invoice-number-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-latest-invoice-number-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-latest-invoice-number-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getLatestInvoiceNumber</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getLatestInvoiceNumber (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getLatestInvoiceNumber (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-latest-invoice-number-0"><span className="wix-picker-name">getLatestInvoiceNumber</span></label>
    <label htmlFor="ex-sdk-get-latest-invoice-number-1"><span className="wix-picker-name">getLatestInvoiceNumber (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-latest-invoice-number-2"><span className="wix-picker-name">getLatestInvoiceNumber (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getLatestInvoiceNumber
    import { invoices } from '@wix/get-paid';

    async function getLatestInvoiceNumber(options) {
      const response = await invoices.getLatestInvoiceNumber(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getLatestInvoiceNumber (self-hosted)
    import { createClient } from '@wix/sdk';
    import { invoices } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { invoices },
      // Include the auth strategy and host as relevant
    });

    async function getLatestInvoiceNumber(options) {
      const response = await myWixClient.invoices.getLatestInvoiceNumber(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getLatestInvoiceNumber (with elevated permissions)
    import { invoices } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myGetLatestInvoiceNumberMethod(options) {
      const elevatedGetLatestInvoiceNumber = auth.elevate(invoices.getLatestInvoiceNumber);
      const response = await elevatedGetLatestInvoiceNumber(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
