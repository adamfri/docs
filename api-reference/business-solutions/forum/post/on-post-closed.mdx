---
title: "onPostClosed"
description: "An existing post was closed for further comments."
icon: "bolt"
---

An existing post was closed for further comments.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_closed` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostClosed
  import { posts } from '@wix/forum';

  posts.onPostClosed((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostClosed (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostClosed((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
