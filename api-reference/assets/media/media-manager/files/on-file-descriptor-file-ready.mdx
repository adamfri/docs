---
title: "onFileDescriptorFileReady"
description: "Triggered when a file is ready to be used, after any post-upload processing."
icon: "bolt"
---

Triggered when a file is ready to be used, after any post-upload processing.

This event is also triggred when a file is restored from the Media Manager's trash bin.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.media.site_media.v1.file_descriptor_file_ready` |
| Package | `@wix/media` |
| Permissions | `Manage Bookings Services and Settings`, `Manage Events`, `Manage Challenges`, `Read Media Manager`, `Manage Media Manager`, `Manage Portfolio`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onFileDescriptorFileReady
  import { files } from '@wix/media';

  files.onFileDescriptorFileReady((event) => {
      // handle your event here
  });
  ```

  ```javascript onFileDescriptorFileReady (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { files } from '@wix/media';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        files,
      },
  });

  wixClient.files.onFileDescriptorFileReady((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
