---
title: "Delete Join Application"
description: "Deletes a join application."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a join application.

    Deleting a join application permanently removes it. Any participant already created from the application isn't affected. Deleting that participant, however, also deletes the join application.

    To delete multiple join applications in a single API call, call [Bulk Delete Join Applications](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/bulk-delete-join-applications).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/online-programs/participants/v3/join-applications/{joinApplicationId}
    ```

    ## Path Params

    <ParamField path="joinApplicationId" type="string" required>

      ID of the join application to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteJoinApplicationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Join Application Deleted | `wix.online_programs.participants.v3.join_application_deleted` | Triggered when a join application is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a join application by ID

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/online-programs/participants/v3/join-applications/f5b74327-4e94-4d42-a214-03e7f1f805ef' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a join application.

    Deleting a join application permanently removes it. Any participant already created from the application isn't affected. Deleting that participant, however, also deletes the join application.

    To delete multiple join applications in a single API call, call [Bulk Delete Join Applications](https://dev.wix.com/docs/api-reference/business-management/online-programs/join-applications/bulk-delete-join-applications).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { joinApplications } from '@wix/online-programs';

    joinApplications.deleteJoinApplication(joinApplicationId): Promise
    ```

    ## Method Parameters

    <ParamField body="joinApplicationId" type="string" required>

      ID of the join application to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteJoinApplicationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Join Application Deleted | `wix.online_programs.participants.v3.join_application_deleted` | Triggered when a join application is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-join-application" id="ex-sdk-delete-join-application-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-join-application" id="ex-sdk-delete-join-application-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-join-application-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-join-application-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a join application</span><span className="wix-picker-desc">Deletes a join application by ID</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteJoinApplication (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-join-application-0"><span className="wix-picker-name">Delete a join application</span><span className="wix-picker-desc">Deletes a join application by ID</span></label>
    <label htmlFor="ex-sdk-delete-join-application-1"><span className="wix-picker-name">deleteJoinApplication (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a join application
    import { joinApplications } from "@wix/online-programs";

    async function deleteJoinApplication() {
      await joinApplications.deleteJoinApplication(
        "f5b74327-4e94-4d42-a214-03e7f1f805ef",
      );
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteJoinApplication (self-hosted)
    import { createClient } from '@wix/sdk';
    import { joinApplications } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { joinApplications },
      // Include the auth strategy and host as relevant
    });

    async function deleteJoinApplication(joinApplicationId) {
      const response = await myWixClient.joinApplications.deleteJoinApplication(joinApplicationId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
