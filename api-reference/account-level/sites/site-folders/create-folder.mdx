---
title: "Create Folder"
description: "Creates a folder."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a folder.
    <Note>

    You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="folders.create">folders.create</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/site-folders/v2/folders
    ```

    ## Body Params

    <ParamField body="folder" type="Folder">

      Folder object to create.

      See the [Folder Object](/api-reference/account-level/sites/site-folders/folder-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="folder" type="Folder">

      Folder data.

      See the [Folder Object](/api-reference/account-level/sites/site-folders/folder-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `duplicate_folder_name` | There's already a folder with this name. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Folder Created | `wix.site_folders.v2.folder_created` | Triggered when a folder is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/site-folders/v2/folders' \
        -H 'Content-Type: application/json' \
        -H 'Accept: application/json, text/plain, */*' \
        -H 'Authorization: <AUTH>' \
        -H 'wix-account-id: 4339fba7-b533-4d10-a91b-822d825f29a5' \
        -d '{ "folder": {"name": "My Folder", "parentId": "root"}}'
      ```

      ```json Response
      {"folder":{"id":"283f4660-8f6f-462b-94a1-160a57338032","name":"My Folder","createdDate":"2021-12-13T11:33:56.973Z","updatedDate":"2021-12-13T11:33:56.973Z","siteCount":0,"parentId":""}}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a folder.
    <Note>

    You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="folders.create">folders.create</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { siteFolders } from '@wix/sites';

    siteFolders.createFolder(options): Promise<Folder>
    ```

    ## Method Parameters

    <ParamField body="options" type="CreateFolderOptions">

      options

      <Expandable title="options properties">

        <ParamField body="folder" type="Folder">

          Folder object to create.

          See the [Folder Object](/api-reference/account-level/sites/site-folders/folder-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Folder>`.

    See the [Folder Object](/api-reference/account-level/sites/site-folders/folder-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `duplicate_folder_name` | There's already a folder with this name. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Folder Created | `wix.site_folders.v2.folder_created` | Triggered when a folder is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-folder" id="ex-sdk-create-folder-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-folder" id="ex-sdk-create-folder-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-folder" id="ex-sdk-create-folder-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-folder-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-folder-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createFolder</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createFolder (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createFolder (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-folder-0"><span className="wix-picker-name">createFolder</span></label>
    <label htmlFor="ex-sdk-create-folder-1"><span className="wix-picker-name">createFolder (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-folder-2"><span className="wix-picker-name">createFolder (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createFolder
    import { siteFolders } from '@wix/sites';

    async function createFolder(options) {
      const response = await siteFolders.createFolder(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createFolder (self-hosted)
    import { createClient } from '@wix/sdk';
    import { siteFolders } from '@wix/sites';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { siteFolders },
      // Include the auth strategy and host as relevant
    });

    async function createFolder(options) {
      const response = await myWixClient.siteFolders.createFolder(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createFolder (with elevated permissions)
    import { siteFolders } from '@wix/sites';
    import { auth } from '@wix/essentials';

    async function myCreateFolderMethod(options) {
      const elevatedCreateFolder = auth.elevate(siteFolders.createFolder);
      const response = await elevatedCreateFolder(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
