---
title: "Create Rule"
description: "Creates a rule."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a rule.

    To create multiple rules at once, use [Bulk Create Rules](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/service-fees/rules/bulk-create-rules).

    Read more about [constructing a rule](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/service-fees/rules/constructing-a-rule).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/service-fees/v1/rules
    ```

    ## Body Params

    <ParamField body="rule" type="Rule" required>

      Rule to create.

      See the [Rule Object](/api-reference/business-solutions/restaurants/online-orders/service-fees/rules/rule-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="rule" type="Rule">

      Created rule

      See the [Rule Object](/api-reference/business-solutions/restaurants/online-orders/service-fees/rules/rule-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rule Created | `wix.service.fees.v1.rule_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-create-rule" id="ex-rest-create-rule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-create-rule" id="ex-rest-create-rule-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-create-rule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-create-rule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create rule with percentage fee</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create rule with a fixed fee</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-create-rule-0"><span className="wix-picker-name">Create rule with percentage fee</span></label>
    <label htmlFor="ex-rest-create-rule-1"><span className="wix-picker-name">Create rule with a fixed fee</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/service-fees-rules/v1/rules \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      --data-raw '{
          "rule": {
              "name": "rule_name",
              "taxRate": "12.9",
              "conditionType": "CONDITION",
              "enabled": false,
              "roundingStrategy": "HALF_EVEN",
              "customTaxRate": "11",
              "conditionOptions": {
                  "orderFieldPath": "priceSummary.subtotal",
                  "expectedFieldType": "NUMBER",
                  "number": {
                      "value": "10",
                      "operation": "EQ"
                  }
              },
              "percentageFee": "11.9"
          }
      }'
      ```

      ```json Response
      {
          "rule": {
              "id": "4aea505e-ac49-4f72-b4f1-b07c064a20cd",
              "revision": "1",
              "createdDate": "2023-12-10T07:06:39.306Z",
              "updatedDate": "2023-12-10T07:06:39.306Z",
              "name": "rule_name",
              "taxRate": "12.9",
              "conditionType": "CONDITION",
              "enabled": false,
              "roundingStrategy": "HALF_EVEN",
              "customTaxRate": "11",
              "conditionOptions": {
                  "orderFieldPath": "priceSummary.subtotal",
                  "expectedFieldType": "NUMBER",
                  "number": {
                      "value": "10",
                      "operation": "EQ"
                  }
              },
              "percentageFee": "11.9"
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/service-fees-rules/v1/rules \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      --data-raw '{
          "rule": {
              "name": "rule_name",
              "taxRate": "21.9",
              "conditionType": "CONDITION",
              "enabled": true,
              "roundingStrategy": "HALF_UP",
              "customTaxRate": "20",
              "conditionOptions": {
                  "orderFieldPath": "priceSummary.subtotal",
                  "expectedFieldType": "NUMBER",
                  "number": {
                      "value": "5.9",
                      "operation": "GT"
                  }
              },
              "fixedFee": {
                  "value": "21.3",
                  "currency": "USD"
              }
          }
      }'
      ```

      ```json Response
      {
          "rule": {
              "id": "4aea505e-ac49-4f72-b4f1-b07c064a20cd",
              "revision": "1",
              "createdDate": "2023-12-10T07:06:39.306Z",
              "updatedDate": "2023-12-10T07:06:39.306Z",
              "name": "rule_name",
              "taxRate": "21.9",
              "conditionType": "CONDITION",
              "enabled": true,
              "roundingStrategy": "HALF_UP",
              "customTaxRate": "20",
              "conditionOptions": {
                  "orderFieldPath": "priceSummary.subtotal",
                  "expectedFieldType": "NUMBER",
                  "number": {
                      "value": "5.9",
                      "operation": "GT"
                  }
              },
              "fixedFee": {
                  "value": "21.3",
                  "currency": "USD"
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates a rule.

    To create multiple rules at once, use [Bulk Create Rules](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/service-fees/rules/bulk-create-rules).

    Read more about [constructing a rule](https://dev.wix.com/docs/rest/api-reference/wix-restaurants/service-fees/rules/constructing-a-rule).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { serviceFees } from '@wix/restaurants';

    serviceFees.createRule(rule): Promise
    ```

    ## Method Parameters

    <ParamField body="rule" type="Rule" required>

      Rule to create.

      See the [Rule Object](/api-reference/business-solutions/restaurants/online-orders/service-fees/rules/rule-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Rule`.

    See the [Rule Object](/api-reference/business-solutions/restaurants/online-orders/service-fees/rules/rule-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rule Created | `wix.service.fees.v1.rule_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-rule" id="ex-sdk-create-rule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-rule" id="ex-sdk-create-rule-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-rule" id="ex-sdk-create-rule-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-rule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-rule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createRule</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createRule (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-rule-0"><span className="wix-picker-name">createRule</span></label>
    <label htmlFor="ex-sdk-create-rule-1"><span className="wix-picker-name">createRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-rule-2"><span className="wix-picker-name">createRule (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createRule
    import { serviceFees } from '@wix/restaurants';

    async function createRule(rule) {
      const response = await serviceFees.createRule(rule);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createRule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { serviceFees } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { serviceFees },
      // Include the auth strategy and host as relevant
    });

    async function createRule(rule) {
      const response = await myWixClient.serviceFees.createRule(rule);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createRule (with elevated permissions)
    import { serviceFees } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myCreateRuleMethod(rule) {
      const elevatedCreateRule = auth.elevate(serviceFees.createRule);
      const response = await elevatedCreateRule(rule);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
