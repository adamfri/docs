---
title: "Set Site Seo Tags"
description: "Sets a site's site-wide SEO tags."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Sets a site's site-wide SEO tags.

    List the fields to change in `fieldMask`.

    Setting `tags` replaces the site's tags in full, so retrieve the current tags with
    [Get Site SEO
    Tags](https://dev.wix.com/docs/api-reference/business-management/seo/site-seo-tags-v1/get-site-seo-tags)
    and send back the complete set you want the site to have. Two consequences are easy
    to miss:

    - Leaving out the `robots` tag makes the site indexable again, because no `noindex`
    directive remains.
    - Leaving out `og:image` clears the site's default social share image.

    To remove every site tag, send `tags` as an empty list with `tags` in the field mask.
    Site verification tags added through the dashboard are stored separately and aren't
    affected.

    Tags are validated before anything is saved, so a request that contains an invalid
    tag changes nothing.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/promote/seo/v1/site-seo-tags
    ```

    ## Body Params

    <ParamField body="siteSeoTags" type="SiteSeoTags" required>

      Site SEO tags to set. Only `tags` can be changed.

      See the [Site Seo Tags Object](/api-reference/business-management/seo/site-seo-tags-v1/site-seo-tags-object) for this object's fields.

    </ParamField>

    <ParamField body="fieldMask" type="FieldMask" required>

      Fields to update.

      Supported properties: `tags`

      <Expandable title="fieldMask properties">

        <ParamField body="paths" type="string[]" />

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="siteSeoTags" type="SiteSeoTags">

      Updated site SEO tags.

      See the [Site Seo Tags Object](/api-reference/business-management/seo/site-seo-tags-v1/site-seo-tags-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_TAGS` | A tag isn't valid at site level. For example, the tag belongs to a single page rather than the whole site, its value isn't valid for that tag, it uses an unsupported `robots` directive, or it repeats a verification provider that supports a single tag. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_FIELD_MASK` | The `fieldMask` is empty, or names a field that doesn't exist or can't be changed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    The request carries the tags the site already has together with the new `og:image` tag, because the tags sent replace the site's tags in full.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/seo-metatags-server/v1/site-seo-tags' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "siteSeoTags": {
          "tags": [
            {
              "type": "meta",
              "props": {
                "name": "description",
                "content": "Handmade ceramics from a small studio in Lisbon."
              }
            },
            {
              "type": "meta",
              "props": {
                "property": "og:image",
                "content": "https://static.wixstatic.com/media/a1b2c3_9f8e7d6c5b4a3210.jpg"
              }
            }
          ]
        },
        "fieldMask": "tags"
      }'
      ```

      ```json Response
      {
        "siteSeoTags": {
          "id": "118f445d-6b84-4268-821d-7d2b9f40ac60",
          "tags": [
            {
              "type": "meta",
              "props": {
                "name": "description",
                "content": "Handmade ceramics from a small studio in Lisbon."
              },
              "children": "",
              "custom": false,
              "disabled": false
            },
            {
              "type": "meta",
              "props": {
                "property": "og:image",
                "content": "https://static.wixstatic.com/media/a1b2c3_9f8e7d6c5b4a3210.jpg"
              },
              "children": "",
              "custom": false,
              "disabled": false
            }
          ],
          "embedTags": [],
          "origin": "ORIGIN_UNSPECIFIED"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Sets a site's site-wide SEO tags.

    List the fields to change in `fieldMask`.

    Setting `tags` replaces the site's tags in full, so retrieve the current tags with
    [Get Site SEO
    Tags](https://dev.wix.com/docs/api-reference/business-management/seo/site-seo-tags-v1/get-site-seo-tags)
    and send back the complete set you want the site to have. Two consequences are easy
    to miss:

    - Leaving out the `robots` tag makes the site indexable again, because no `noindex`
    directive remains.
    - Leaving out `og:image` clears the site's default social share image.

    To remove every site tag, send `tags` as an empty list with `tags` in the field mask.
    Site verification tags added through the dashboard are stored separately and aren't
    affected.

    Tags are validated before anything is saved, so a request that contains an invalid
    tag changes nothing.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { siteSeoTags } from '@wix/seo';

    siteSeoTags.setSiteSeoTags(siteSeoTags, options): Promise
    ```

    ## Method Parameters

    <ParamField body="siteSeoTags" type="SiteSeoTags" required>

      Site SEO tags to set. Only `tags` can be changed.

      See the [Site Seo Tags Object](/api-reference/business-management/seo/site-seo-tags-v1/site-seo-tags-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="SetSiteSeoTagsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="fieldMask" type="FieldMask" required>

          Fields to update.

          Supported properties: `tags`

          <Expandable title="fieldMask properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="siteSeoTags" type="SiteSeoTags">

      Updated site SEO tags.

      See the [Site Seo Tags Object](/api-reference/business-management/seo/site-seo-tags-v1/site-seo-tags-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `INVALID_TAGS` | A tag isn't valid at site level. For example, the tag belongs to a single page rather than the whole site, its value isn't valid for that tag, it uses an unsupported `robots` directive, or it repeats a verification provider that supports a single tag. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_FIELD_MASK` | The `fieldMask` is empty, or names a field that doesn't exist or can't be changed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-set-site-seo-tags" id="ex-sdk-set-site-seo-tags-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-set-site-seo-tags" id="ex-sdk-set-site-seo-tags-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-set-site-seo-tags-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-set-site-seo-tags-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Add a social share image to a site's SEO tags</span><span className="wix-picker-desc">The request carries the tags the site already has together with the new og:image tag, because the tags sent replace the site's tags in full</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setSiteSeoTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-set-site-seo-tags-0"><span className="wix-picker-name">Add a social share image to a site's SEO tags</span><span className="wix-picker-desc">The request carries the tags the site already has together with the new og:image tag, because the tags sent replace the site's tags in full</span></label>
    <label htmlFor="ex-sdk-set-site-seo-tags-1"><span className="wix-picker-name">setSiteSeoTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Add a social share image to a site's SEO tags
    import { siteSeoTags } from "@wix/seo";

    async function setSiteSeoTags() {
      const response = await siteSeoTags.setSiteSeoTags(
        {
          tags: [
            {
              type: "meta",
              props: { name: "description", content: "Handmade ceramics from a small studio in Lisbon." },
            },
            {
              type: "meta",
              props: {
                property: "og:image",
                content: "https://static.wixstatic.com/media/a1b2c3_9f8e7d6c5b4a3210.jpg",
              },
            },
          ],
        },
        { fieldMask: ["tags"] },
      );
      return response;
    }

    /* Promise resolves to:
     * {
     *   "siteSeoTags": {
     *     "tags": [
     *       {
     *         "type": "meta",
     *         "props": { "name": "description", "content": "Handmade ceramics from a small studio in Lisbon." },
     *         "children": "",
     *         "custom": false,
     *         "disabled": false
     *       },
     *       {
     *         "type": "meta",
     *         "props": { "property": "og:image", "content": "https://static.wixstatic.com/media/a1b2c3_9f8e7d6c5b4a3210.jpg" },
     *         "children": "",
     *         "custom": false,
     *         "disabled": false
     *       }
     *     ],
     *     "embedTags": [],
     *     "origin": "ORIGIN_UNSPECIFIED",
     *     "_id": "118f445d-6b84-4268-821d-7d2b9f40ac60"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setSiteSeoTags (self-hosted)
    import { createClient } from '@wix/sdk';
    import { siteSeoTags } from '@wix/seo';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { siteSeoTags },
      // Include the auth strategy and host as relevant
    });

    async function setSiteSeoTags(siteSeoTags,options) {
      const response = await myWixClient.siteSeoTags.setSiteSeoTags(siteSeoTags,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
