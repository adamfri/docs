---
title: "List Availability Time Slot End Options"
description: "Retrieves the available end times for a selected start time of an appointment-based service configured with a duration range."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the available end times for a selected start time of an appointment-based service configured with a duration range.

    Call this after the customer selects a start time with [List Availability Time Slots](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/list-availability-time-slots). Each returned entry is a `TimeSlot` that shares the requested `localStartDate` and differs only in `localEndDate`. The response doesn't include resource lists.

    Only supported for hourly duration-range services (services with `availabilityConstraints.durationRange.hourConfig` configured in [Services V2](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/services-v2/introduction)). Calling this for a fixed-duration or daily duration-range service returns `END_OPTIONS_NOT_SUPPORTED`.

    ### Resource selection

    Resource selection depends on the booking flow:
    - If the customer hasn't selected a resource yet, call [Get Availability Time Slot](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/get-availability-time-slot) with the selected `localEndDate` to retrieve candidate resources before booking.
    - If the customer selected a resource before the end time, pass that resource in `resourceTypes` so the response only includes end times that the selected resource can cover.

    ### Time slot shape

    The response uses the `TimeSlot` model with constrained semantics:
    - `localStartDate` and `location` are identical on every entry, echoing the request.
    - `localEndDate` varies across entries and represents each end option.
    - `availableResources` is always empty. Use [Get Availability Time Slot](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/get-availability-time-slot) to retrieve resources for a selected end time.
    - `eventInfo`, `nestedTimeSlots`, and `allDay` are not populated.
    - `totalCapacity` is always `1`.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-CALENDAR">Read Bookings Calendar Availability</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/service-availability/v2/time-slots/end-options
    ```

    ## Body Params

    <ParamField body="serviceId" type="string" required>

      [Service](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/services-v2/introduction) ID. Must be an appointment-based service configured with `availabilityConstraints.durationRange.hourConfig`.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="localStartDate" type="string" required>

      Selected start time in `YYYY-MM-DDThh:mm:ss` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-03-23T10:00:00`.
      Each returned end option shares this start time.

      Constraints: `format LOCAL_DATE_TIME`.

    </ParamField>

    <ParamField body="maxLocalEndDate" type="string">

      Latest end time to include in `YYYY-MM-DDThh:mm:ss` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-03-23T14:00:00`.
      The service's maximum duration always caps the result, even if a later value is specified.

      Default: `localStartDate` plus the service's maximum duration from `durationRange.hourConfig`.

    </ParamField>

    <ParamField body="timeZone" type="string">

      Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database). For example, `America/New_York` or `UTC`.

      Default: `timeZone` specified in the business [site properties](https://dev.wix.com/docs/api-reference/business-management/site-properties/properties/get-site-properties).

    </ParamField>

    <ParamField body="location" type="Location" required>

      Location of the selected start time. Must match the location of the slot returned by [List Availability Time Slots](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/list-availability-time-slots).

      <Expandable title="location properties">

        <ParamField body="id" type="string">

          [Location ID](https://dev.wix.com/docs/api-reference/business-management/locations/introduction).
          Available only for business locations.

        </ParamField>

        <ParamField body="name" type="string">

          Location name.

        </ParamField>

        <ParamField body="formattedAddress" type="string">

          Formatted location address.

        </ParamField>

        <ParamField body="locationType" type="string">

          Location type.

          **Supported values:**

          - `BUSINESS` — A business location, either the default business address, or locations defined for the business by the Business Info.
          - `CUSTOM` — The location is unique to this service and isn't defined as 1 of the business locations.
          - `CUSTOMER` — The location can be determined by the customer and isn't set up beforehand.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="resourceTypes" type="ResourceType[]">

      Resource types to constrain the end options.
      When the customer has already selected a resource before choosing an end time, pass that resource here so the response only includes end times the selected resource can cover.

      If no resource has been preselected, omit this field. End options then reflect the full resource pool. Select the resource later by calling [Get Availability Time Slot](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/get-availability-time-slot) with the chosen `localEndDate`.

      Constraints: `maxItems 8`.

      <Expandable title="resourceTypes properties">

        <ParamField body="resourceTypeId" type="string">

          [Resource type ID](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/introduction).

        </ParamField>

        <ParamField body="resourceIds" type="string[]">

          Resource IDs.
          Available only if there is at least 1 resource available for the slot.

          Constraints: `format GUID`, `maxItems 135`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="endOptions" type="TimeSlot[]">

      Available end options for the requested start.
      Each entry is a `TimeSlot` sharing `localStartDate` with the request and varying only in `localEndDate`.
      Sorted by `localEndDate` in ascending order (shortest duration first).

      Constraints: `maxItems 1000`.

      See the [Time Slot Object](/api-reference/business-solutions/bookings/time-slots/time-slots-v2/time-slot-object) for this object's fields.

    </ResponseField>

    <ResponseField name="timeZone" type="string">

      Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database). For example, `America/New_York` or `UTC`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `END_OPTIONS_NOT_SUPPORTED` | The service doesn't support end-options retrieval. The service must have `availabilityConstraints.durationRange.hourConfig` configured. |
    | `400` | `INVALID_ARGUMENT` | `MAX_END_DATE_EXCEEDS_MAXIMUM` | The specified `maxLocalEndDate` exceeds the service's maximum duration. |
    | `404` | `NOT_FOUND` | `SERVICE_NOT_FOUND` | Couldn't find the service. |
    | `404` | `NOT_FOUND` | `NO_IMPLEMENTERS_FOUND` | No availability provider is installed or configured for this operation. |
    | `404` | `NOT_FOUND` | `MULTIPLE_IMPLEMENTERS_FOUND` | Multiple availability providers are installed. Only 1 provider can be active at a time. |
    | `403` | `PERMISSION_DENIED` | `UNAUTHORIZED_OPERATION` | The [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) used to call the method doesn't have the required permissions. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    After the customer picks a start time from List Availability Time Slots, retrieves the available end times for that start. The service must be configured with a duration range.

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/_api/service-availability/v2/time-slots/end-options' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "serviceId": "27f2fb02-8925-4ede-be26-991411d6c905",
        "localStartDate": "2026-03-23T10:00:00",
        "timeZone": "America/New_York",
        "location": {
          "id": "92310bc9-10db-4163-85d2-65f83e0ddda9",
          "locationType": "BUSINESS"
        }
      }'
      ```

      ```json Response
      {
        "endOptions": [
          {
            "serviceId": "27f2fb02-8925-4ede-be26-991411d6c905",
            "localStartDate": "2026-03-23T10:00:00",
            "localEndDate": "2026-03-23T11:00:00",
            "bookable": true,
            "location": {
              "id": "92310bc9-10db-4163-85d2-65f83e0ddda9",
              "locationType": "BUSINESS"
            },
            "totalCapacity": 1,
            "remainingCapacity": 1,
            "bookingPolicyViolations": {
              "tooEarlyToBook": false,
              "tooLateToBook": false,
              "bookOnlineDisabled": false
            },
            "availableResources": [],
            "nestedTimeSlots": []
          },
          {
            "serviceId": "27f2fb02-8925-4ede-be26-991411d6c905",
            "localStartDate": "2026-03-23T10:00:00",
            "localEndDate": "2026-03-23T12:00:00",
            "bookable": true,
            "location": {
              "id": "92310bc9-10db-4163-85d2-65f83e0ddda9",
              "locationType": "BUSINESS"
            },
            "totalCapacity": 1,
            "remainingCapacity": 1,
            "bookingPolicyViolations": {
              "tooEarlyToBook": false,
              "tooLateToBook": false,
              "bookOnlineDisabled": false
            },
            "availableResources": [],
            "nestedTimeSlots": []
          },
          {
            "serviceId": "27f2fb02-8925-4ede-be26-991411d6c905",
            "localStartDate": "2026-03-23T10:00:00",
            "localEndDate": "2026-03-23T13:00:00",
            "bookable": true,
            "location": {
              "id": "92310bc9-10db-4163-85d2-65f83e0ddda9",
              "locationType": "BUSINESS"
            },
            "totalCapacity": 1,
            "remainingCapacity": 1,
            "bookingPolicyViolations": {
              "tooEarlyToBook": false,
              "tooLateToBook": false,
              "bookOnlineDisabled": false
            },
            "availableResources": [],
            "nestedTimeSlots": []
          }
        ],
        "timeZone": "America/New_York"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the available end times for a selected start time of an appointment-based service configured with a duration range.

    Call this after the customer selects a start time with [List Availability Time Slots](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/list-availability-time-slots). Each returned entry is a `TimeSlot` that shares the requested `localStartDate` and differs only in `localEndDate`. The response doesn't include resource lists.

    Only supported for hourly duration-range services (services with `availabilityConstraints.durationRange.hourConfig` configured in [Services V2](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/services-v2/introduction)). Calling this for a fixed-duration or daily duration-range service returns `END_OPTIONS_NOT_SUPPORTED`.

    ### Resource selection

    Resource selection depends on the booking flow:
    - If the customer hasn't selected a resource yet, call [Get Availability Time Slot](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/get-availability-time-slot) with the selected `localEndDate` to retrieve candidate resources before booking.
    - If the customer selected a resource before the end time, pass that resource in `resourceTypes` so the response only includes end times that the selected resource can cover.

    ### Time slot shape

    The response uses the `TimeSlot` model with constrained semantics:
    - `localStartDate` and `location` are identical on every entry, echoing the request.
    - `localEndDate` varies across entries and represents each end option.
    - `availableResources` is always empty. Use [Get Availability Time Slot](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/get-availability-time-slot) to retrieve resources for a selected end time.
    - `eventInfo`, `nestedTimeSlots`, and `allDay` are not populated.
    - `totalCapacity` is always `1`.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-CALENDAR">Read Bookings Calendar Availability</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { availabilityTimeSlots } from '@wix/bookings';

    availabilityTimeSlots.listAvailabilityTimeSlotEndOptions(serviceId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="serviceId" type="string" required>

      [Service](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/services-v2/introduction) ID. Must be an appointment-based service configured with `availabilityConstraints.durationRange.hourConfig`.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="ListAvailabilityTimeSlotEndOptionsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="localStartDate" type="string" required>

          Selected start time in `YYYY-MM-DDThh:mm:ss` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-03-23T10:00:00`.
          Each returned end option shares this start time.

          Constraints: `format LOCAL_DATE_TIME`.

        </ParamField>

        <ParamField body="maxLocalEndDate" type="string">

          Latest end time to include in `YYYY-MM-DDThh:mm:ss` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-03-23T14:00:00`.
          The service's maximum duration always caps the result, even if a later value is specified.

          Default: `localStartDate` plus the service's maximum duration from `durationRange.hourConfig`.

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database). For example, `America/New_York` or `UTC`.

          Default: `timeZone` specified in the business [site properties](https://dev.wix.com/docs/api-reference/business-management/site-properties/properties/get-site-properties).

        </ParamField>

        <ParamField body="location" type="Location" required>

          Location of the selected start time. Must match the location of the slot returned by [List Availability Time Slots](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/list-availability-time-slots).

          <Expandable title="location properties">

            <ParamField body="_id" type="string">

              [Location ID](https://dev.wix.com/docs/api-reference/business-management/locations/introduction).
              Available only for business locations.

            </ParamField>

            <ParamField body="name" type="string">

              Location name.

            </ParamField>

            <ParamField body="formattedAddress" type="string">

              Formatted location address.

            </ParamField>

            <ParamField body="locationType" type="string">

              Location type.

              **Supported values:**

              - `BUSINESS` — A business location, either the default business address, or locations defined for the business by the Business Info.
              - `CUSTOM` — The location is unique to this service and isn't defined as 1 of the business locations.
              - `CUSTOMER` — The location can be determined by the customer and isn't set up beforehand.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="resourceTypes" type="ResourceType[]">

          Resource types to constrain the end options.
          When the customer has already selected a resource before choosing an end time, pass that resource here so the response only includes end times the selected resource can cover.

          If no resource has been preselected, omit this field. End options then reflect the full resource pool. Select the resource later by calling [Get Availability Time Slot](https://dev.wix.com/docs/api-reference/business-solutions/bookings/time-slots/time-slots-v2/get-availability-time-slot) with the chosen `localEndDate`.

          Constraints: `maxItems 8`.

          <Expandable title="resourceTypes properties">

            <ParamField body="resourceTypeId" type="string">

              [Resource type ID](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/introduction).

            </ParamField>

            <ParamField body="resourceIds" type="string[]">

              Resource IDs.
              Available only if there is at least 1 resource available for the slot.

              Constraints: `format GUID`, `maxItems 135`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="endOptions" type="TimeSlot[]">

      Available end options for the requested start.
      Each entry is a `TimeSlot` sharing `localStartDate` with the request and varying only in `localEndDate`.
      Sorted by `localEndDate` in ascending order (shortest duration first).

      Constraints: `maxItems 1000`.

      See the [Time Slot Object](/api-reference/business-solutions/bookings/time-slots/time-slots-v2/time-slot-object) for this object's fields.

    </ResponseField>

    <ResponseField name="timeZone" type="string">

      Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database). For example, `America/New_York` or `UTC`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `END_OPTIONS_NOT_SUPPORTED` | The service doesn't support end-options retrieval. The service must have `availabilityConstraints.durationRange.hourConfig` configured. |
    | `400` | `INVALID_ARGUMENT` | `MAX_END_DATE_EXCEEDS_MAXIMUM` | The specified `maxLocalEndDate` exceeds the service's maximum duration. |
    | `404` | `NOT_FOUND` | `SERVICE_NOT_FOUND` | Couldn't find the service. |
    | `404` | `NOT_FOUND` | `NO_IMPLEMENTERS_FOUND` | No availability provider is installed or configured for this operation. |
    | `404` | `NOT_FOUND` | `MULTIPLE_IMPLEMENTERS_FOUND` | Multiple availability providers are installed. Only 1 provider can be active at a time. |
    | `403` | `PERMISSION_DENIED` | `UNAUTHORIZED_OPERATION` | The [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) used to call the method doesn't have the required permissions. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-availability-time-slot-end-options" id="ex-sdk-list-availability-time-slot-end-options-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-availability-time-slot-end-options" id="ex-sdk-list-availability-time-slot-end-options-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-availability-time-slot-end-options-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-availability-time-slot-end-options-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listAvailabilityTimeSlotEndOptions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listAvailabilityTimeSlotEndOptions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-availability-time-slot-end-options-0"><span className="wix-picker-name">listAvailabilityTimeSlotEndOptions</span></label>
    <label htmlFor="ex-sdk-list-availability-time-slot-end-options-1"><span className="wix-picker-name">listAvailabilityTimeSlotEndOptions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listAvailabilityTimeSlotEndOptions
    import { availabilityTimeSlots } from '@wix/bookings';

    async function listAvailabilityTimeSlotEndOptions(serviceId,options) {
      const response = await availabilityTimeSlots.listAvailabilityTimeSlotEndOptions(serviceId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listAvailabilityTimeSlotEndOptions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { availabilityTimeSlots } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { availabilityTimeSlots },
      // Include the auth strategy and host as relevant
    });

    async function listAvailabilityTimeSlotEndOptions(serviceId,options) {
      const response = await myWixClient.availabilityTimeSlots.listAvailabilityTimeSlotEndOptions(serviceId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
