---
title: "Get Referring Customer"
description: "Retrieves a referring customer by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referring customer by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/referral-customers/v1/referring-customers/{referringCustomerId}
    ```

    ## Path Params

    <ParamField path="referringCustomerId" type="string" required>

      ID of the referring customer to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="referringCustomer" type="ReferringCustomer">

      Retrieved referring customer.

      See the [Referring Customer Object](/api-reference/business-management/marketing/referral-program/referring-customers/referring-customer-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/referral-customers/v1/referring-customers/46d7bbce-6bb4-4174-ae5a-7f44c19f95' \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
      ```

      ```json Response
      {
          "referringCustomer": {
              "id": "5cc0b472-e488-4de3-92ba-bd718a44933a",
              "contactId": "5cc0b472-e488-4de3-92ba-bd718a44933a",
              "referralCode": "9zb9JvjwrvQF",
              "createdDate": "2024-09-17T10:58:00.100Z",
              "updatedDate": "2024-09-17T10:58:00.100Z"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referring customer by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customers } from '@wix/referral';

    customers.getReferringCustomer(referringCustomerId): Promise<ReferringCustomer>
    ```

    ## Method Parameters

    <ParamField body="referringCustomerId" type="string" required>

      ID of the referring customer to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<ReferringCustomer>`.

    See the [Referring Customer Object](/api-reference/business-management/marketing/referral-program/referring-customers/referring-customer-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-referring-customer" id="ex-sdk-get-referring-customer-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-referring-customer" id="ex-sdk-get-referring-customer-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-referring-customer" id="ex-sdk-get-referring-customer-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-referring-customer-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-referring-customer-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getReferringCustomer</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferringCustomer (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferringCustomer (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-referring-customer-0"><span className="wix-picker-name">getReferringCustomer</span></label>
    <label htmlFor="ex-sdk-get-referring-customer-1"><span className="wix-picker-name">getReferringCustomer (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-referring-customer-2"><span className="wix-picker-name">getReferringCustomer (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getReferringCustomer
    import { customers } from '@wix/referral';

    async function getReferringCustomer(referringCustomerId) {
      const response = await customers.getReferringCustomer(referringCustomerId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferringCustomer (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customers } from '@wix/referral';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customers },
      // Include the auth strategy and host as relevant
    });

    async function getReferringCustomer(referringCustomerId) {
      const response = await myWixClient.customers.getReferringCustomer(referringCustomerId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferringCustomer (with elevated permissions)
    import { customers } from '@wix/referral';
    import { auth } from '@wix/essentials';

    async function myGetReferringCustomerMethod(referringCustomerId) {
      const elevatedGetReferringCustomer = auth.elevate(customers.getReferringCustomer);
      const response = await elevatedGetReferringCustomer(referringCustomerId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
