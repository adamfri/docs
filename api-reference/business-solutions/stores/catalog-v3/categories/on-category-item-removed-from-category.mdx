---
title: "onCategoryItemRemovedFromCategory"
description: "Triggered when an item is removed from a category."
icon: "bolt"
---

Triggered when an item is removed from a category.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.categories.v1.category_item_removed_from_category` |
| Package | `@wix/categories` |
| Permissions | `Manage Stores - all permissions`, `Read categories`, `Read v3 catalog`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onCategoryItemRemovedFromCategory
  import { categories } from '@wix/categories';

  categories.onCategoryItemRemovedFromCategory((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryItemRemovedFromCategory (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categories } from '@wix/categories';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        categories,
      },
  });

  wixClient.categories.onCategoryItemRemovedFromCategory((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
