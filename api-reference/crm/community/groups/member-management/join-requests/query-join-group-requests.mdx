---
title: "Query Join Group Requests"
description: "Retrieves a list of up to 100 join group requests, given the provided paging and filtering."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 join group requests, given the provided paging and filtering.
    Group managers always have access to this functionality (site members can access their own join group requests in the site).

    Supported fields for filtering:
    - `status`

    No supported fields for sorting

    ## Endpoint

    ```http
    POST https://www.wixapis.com/social-groups-proxy/join/v2/groups/{groupId}/join-requests/query
    ```

    ## Path Params

    <ParamField path="groupId" type="string" required>

      Group ID.

    </ParamField>

    ## Body Params

    <ParamField body="query" type="Query">

      query

      <Expandable title="query properties">

        <ParamField body="filter" type="any">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paging" type="Paging">

          Paging options to limit and skip the number of items.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="joinGroupRequests" type="JoinGroupRequest[]">

      Join group requests.

      See the [Join Group Request Object](/api-reference/crm/community/groups/member-management/join-requests/join-group-request-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      metadata

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
          https://wixapis.com/social-groups/v2/groups/a4caf853-c0d7-498e-8eaa-3db36522dcbf/join-requests/query \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
          -d '{
              "ownershipFilter": "CURRENT_MEMBER",
              "query": {
                  "filter": {
                      "status": {
                          "$eq": "APPROVED"
                      }
                  },
                  "paging": {
                      "offset": 0,
                      "limit": 100
                  }
              }
          }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "joinGroupRequests": [
              {
                  "siteMemberId": "e6674661-f294-4f1f-9f22-bc11fa8164e7",
                  "status": "APPROVED"
              }
          ],
          "metadata": {
              "count": 1,
              "offset": 0
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 join group requests, given the provided paging and filtering.
    Group managers always have access to this functionality (site members can access their own join group requests in the site).

    Supported fields for filtering:
    - `status`

    No supported fields for sorting

    ## Method Declaration

    ```typescript
    import { joinGroupRequests } from '@wix/groups';

    joinGroupRequests.queryJoinGroupRequests(groupId, options): JoinGroupRequestsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="groupId" type="string" required>

      ID of the group requested to join.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="QueryJoinGroupRequestsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="Query">

          query

          <Expandable title="query properties">

            <ParamField body="filter" type="any">

              Filter object in the following format:
              `"filter" : {`
              "fieldName1": "value1",
              "fieldName2":&#123;"$operator":"value2"&#125;
              &#125;``
              Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object in the following format:
              `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="fields" type="string[]">

              Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

            <ParamField body="fieldsets" type="string[]">

              Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `JoinGroupRequestsQueryBuilder`.

    <ResponseField name="joinGroupRequests" type="JoinGroupRequest[]">

      Join group requests.

      See the [Join Group Request Object](/api-reference/crm/community/groups/member-management/join-requests/join-group-request-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      metadata

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-join-group-requests" id="ex-sdk-query-join-group-requests-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-join-group-requests" id="ex-sdk-query-join-group-requests-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-join-group-requests" id="ex-sdk-query-join-group-requests-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-join-group-requests-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-join-group-requests-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query requests to join a group</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query requests to join a group (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryJoinGroupRequests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-join-group-requests-0"><span className="wix-picker-name">Query requests to join a group</span></label>
    <label htmlFor="ex-sdk-query-join-group-requests-1"><span className="wix-picker-name">Query requests to join a group (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-query-join-group-requests-2"><span className="wix-picker-name">queryJoinGroupRequests (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query requests to join a group
    import { joinGroupRequests } from '@wix/groups';

    export function myQueryJoinGroupRequestsFunction() {
      return joinGroupRequests.queryJoinGroupRequests(groupId, options)
        .find()
        .then((results) => {
          const items = results.items;
          const firstItem = items[0];
          const pageSize = results.pageSize;
          const totalPages = results.totalPages;
          const totalCount = results.totalCount;
          const currentPage = results.currentPage;
          const next = results.next();
          const previous = results.prev();
          const hasNext = results.hasNext();
          const hasPrev = results.hasPrev();
          const length = results.length;

          return items;
        })
        .catch((error) => {
          console.error(error);
        });
    }

    /* Returns items:
     * [
     *  {
     *    "memberId": "7fe8e9e1-d050-4c86-9684-e7f231600a34"
     *    "_createdDate": "Fri Oct 24 2021 22:45:50 GMT+0300"
     *    "status": "PENDING"
     *  },
     *  {
     *    "memberId": "937cd3db-e9be-4980-93c1-a6d767a11050"
     *    "_createdDate": "Wed May 14 2021 10:05:20 GMT+0300"
     *    "status": "REJECTED"
     *    "rejectionReason": "Wrong group"
     *  },
     *  {
     *    "memberId": "2CD58761-d050-4c86-9684-e7f2316229b3"
     *    "_createdDate": "Sun July 11 2020 03:25:30 GMT+0300"
     *    "status": "APPROVED"
     *  }
     * ]
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Query requests to join a group (with elevated permissions)
    import { joinGroupRequests } from '@wix/groups';
    import { auth } from '@wix/essentials';

    const elevatedQueryJoinGroupRequests = auth.elevate(joinGroupRequests.queryJoinGroupRequests);

    export function myQueryJoinGroupRequestsFunction() {
      return elevatedQueryJoinGroupRequests(groupId, options)
        .find()
        .then((results) => {
          const items = results.items;
          const firstItem = items[0];
          const pageSize = results.pageSize;
          const totalPages = results.totalPages;
          const totalCount = results.totalCount;
          const currentPage = results.currentPage;
          const next = results.next();
          const previous = results.prev();
          const hasNext = results.hasNext();
          const hasPrev = results.hasPrev();
          const length = results.length;

          return items;
        })
        .catch((error) => {
          console.error(error);
        });
    }

    /* Returns items:
     * [
     *  {
     *    "memberId": "7fe8e9e1-d050-4c86-9684-e7f231600a34"
     *    "_createdDate": "Fri Oct 24 2021 22:45:50 GMT+0300"
     *    "status": "PENDING"
     *  },
     *  {
     *    "memberId": "937cd3db-e9be-4980-93c1-a6d767a11050"
     *    "_createdDate": "Wed May 14 2021 10:05:20 GMT+0300"
     *    "status": "REJECTED"
     *    "rejectionReason": "Wrong group"
     *  },
     *  {
     *    "memberId": "2CD58761-d050-4c86-9684-e7f2316229b3"
     *    "_createdDate": "Sun July 11 2020 03:25:30 GMT+0300"
     *    "status": "APPROVED"
     *  }
     * ]
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryJoinGroupRequests (self-hosted)
    import { createClient } from '@wix/sdk';
    import { joinGroupRequests } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { joinGroupRequests },
      // Include the auth strategy and host as relevant
    });

    async function queryJoinGroupRequests(groupId,options) {
      const response = await myWixClient.joinGroupRequests.queryJoinGroupRequests(groupId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
