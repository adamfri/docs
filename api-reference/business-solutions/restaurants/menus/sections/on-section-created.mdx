---
title: "onSectionCreated"
description: "The onSectionCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.section_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onSectionCreated
  import { sections } from '@wix/restaurants';

  sections.onSectionCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onSectionCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { sections } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        sections,
      },
  });

  wixClient.sections.onSectionCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
