---
title: "Set Add Ons For Group"
description: "Sets the add-ons for a specific group, replacing any existing add-ons in the group."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets the add-ons for a specific group, replacing any existing add-ons in the group.

    Call this method when you want to:
    - Add new add-ons to a group.
    - Remove add-ons from a group.
    - Reorder existing add-ons for optimal customer presentation.

    The order of add-ons in the request determines their display sequence to customers.
    All specified add-ons must already exist before adding them to a group.

    For updating other add-on group properties, for example `name`, `prompt`, or `constraints`, call [Update Add On Group](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/services-v2/update-add-on-group) instead.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/services/add-on-groups/set-add-ons-for-group
    ```

    ## Body Params

    <ParamField body="serviceId" type="string" required>

      Service ID containing the group.

    </ParamField>

    <ParamField body="groupId" type="string" required>

      Group ID to set add-ons for.

    </ParamField>

    <ParamField body="addOnIds" type="string[]" required>

      IDs of add-ons to set for the group in display order.

      Constraints: `format GUID`, `minItems 1`, `maxItems 7`.

    </ParamField>

    ## Response Object

    <ResponseField name="addOnGroup" type="AddOnGroup">

      Updated add-on group.

      <Expandable title="addOnGroup properties">

        <ResponseField name="id" type="string">

          ID of the add-on group.
          Wix Bookings automatically populates this field when creating or updating an add-on group.

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Name of the add-on group.

        </ResponseField>

        <ResponseField name="maxNumberOfAddOns" type="integer">

          Maximum number of different [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) from the group customers can add when booking the service.
          When empty, there's no upper limit.

        </ResponseField>

        <ResponseField name="addOnIds" type="string[]">

          List of IDs of all [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) included in the group.

          Constraints: `format GUID`, `maxItems 7`.

        </ResponseField>

        <ResponseField name="prompt" type="string">

          Description or instructional prompt of the add-on group that's displayed to customers when booking the service.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `ADD_ON_GROUP_NOT_FOUND` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/bookings/v2/services/add-on-groups/set-add-ons-for-group' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "serviceId": "d779a301-398d-4552-aa8c-3bef0b65cedb",
        "groupId": "f8e7d6c5-b4a3-9281-7605-948372615038",
        "addOnIds": [
          "b2c3d4e5-f6g7-8901-bcde-f23456789012",
          "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "g6h7i8j9-k0l1-2345-ghij-678901234567"
        ]
      }'
      ```

      ```json Response
      {
        "addOnGroup": {
          "id": "f8e7d6c5-b4a3-9281-7605-948372615038",
          "name": "Spa Treatments",
          "maxNumberOfAddOns": 2,
          "addOnIds": [
            "b2c3d4e5-f6g7-8901-bcde-f23456789012",
            "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "g6h7i8j9-k0l1-2345-ghij-678901234567"
          ],
          "prompt": "Choose up to 2 relaxing spa treatments to enhance your experience"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets the add-ons for a specific group, replacing any existing add-ons in the group.

    Call this method when you want to:
    - Add new add-ons to a group.
    - Remove add-ons from a group.
    - Reorder existing add-ons for optimal customer presentation.

    The order of add-ons in the request determines their display sequence to customers.
    All specified add-ons must already exist before adding them to a group.

    For updating other add-on group properties, for example `name`, `prompt`, or `constraints`, call [Update Add On Group](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/services-v2/update-add-on-group) instead.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { services } from '@wix/bookings';

    services.setAddOnsForGroup(serviceId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="serviceId" type="string" required>

      Service ID containing the group.

    </ParamField>

    <ParamField body="options" type="SetAddOnsForGroupOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="groupId" type="string" required>

          Group ID to set add-ons for.

        </ParamField>

        <ParamField body="addOnIds" type="string[]" required>

          IDs of add-ons to set for the group in display order.

          Constraints: `format GUID`, `minItems 1`, `maxItems 7`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="addOnGroup" type="AddOnGroup">

      Updated add-on group.

      <Expandable title="addOnGroup properties">

        <ResponseField name="_id" type="string">

          ID of the add-on group.
          Wix Bookings automatically populates this field when creating or updating an add-on group.

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Name of the add-on group.

        </ResponseField>

        <ResponseField name="maxNumberOfAddOns" type="integer">

          Maximum number of different [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) from the group customers can add when booking the service.
          When empty, there's no upper limit.

        </ResponseField>

        <ResponseField name="addOnIds" type="string[]">

          List of IDs of all [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) included in the group.

          Constraints: `format GUID`, `maxItems 7`.

        </ResponseField>

        <ResponseField name="prompt" type="string">

          Description or instructional prompt of the add-on group that's displayed to customers when booking the service.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `ADD_ON_GROUP_NOT_FOUND` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-set-add-ons-for-group" id="ex-sdk-set-add-ons-for-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-set-add-ons-for-group" id="ex-sdk-set-add-ons-for-group-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-set-add-ons-for-group" id="ex-sdk-set-add-ons-for-group-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-set-add-ons-for-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-set-add-ons-for-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">setAddOnsForGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setAddOnsForGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setAddOnsForGroup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-set-add-ons-for-group-0"><span className="wix-picker-name">setAddOnsForGroup</span></label>
    <label htmlFor="ex-sdk-set-add-ons-for-group-1"><span className="wix-picker-name">setAddOnsForGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-set-add-ons-for-group-2"><span className="wix-picker-name">setAddOnsForGroup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript setAddOnsForGroup
    import { services } from '@wix/bookings';

    async function setAddOnsForGroup(serviceId,options) {
      const response = await services.setAddOnsForGroup(serviceId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setAddOnsForGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { services } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { services },
      // Include the auth strategy and host as relevant
    });

    async function setAddOnsForGroup(serviceId,options) {
      const response = await myWixClient.services.setAddOnsForGroup(serviceId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setAddOnsForGroup (with elevated permissions)
    import { services } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function mySetAddOnsForGroupMethod(serviceId,options) {
      const elevatedSetAddOnsForGroup = auth.elevate(services.setAddOnsForGroup);
      const response = await elevatedSetAddOnsForGroup(serviceId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
