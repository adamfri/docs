---
title: "Delete Reward"
description: "Deletes a reward."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a reward.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/loyalty-rewards/v1/rewards/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the reward to delete.

    </ParamField>

    ## Query Params

    <ParamField query="revision" type="number">

      Revision number, which increments by 1 each time the reward is updated.

      To prevent conflicting changes, pass the current `revision` when deleting the reward.

    </ParamField>

    ## Response Object

    Returns `DeleteRewardResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reward Deleted | `wix.loyalty.v1.reward_deleted` | Triggered when a reward is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/loyalty-rewards/v1/rewards/46d7bbce-6bb4-4174-ae5a-7f44c19f95ce?revision=1' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a reward.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rewards } from '@wix/loyalty';

    rewards.deleteReward(_id, revision): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the reward to delete.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="revision" type="number" required>

      Revision number, which increments by 1 each time the reward is updated.

      To prevent conflicting changes, pass the current `revision` when deleting the reward.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reward Deleted | `wix.loyalty.v1.reward_deleted` | Triggered when a reward is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-reward" id="ex-sdk-delete-reward-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-reward" id="ex-sdk-delete-reward-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-reward" id="ex-sdk-delete-reward-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-reward-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-reward-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteReward</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReward (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReward (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-reward-0"><span className="wix-picker-name">deleteReward</span></label>
    <label htmlFor="ex-sdk-delete-reward-1"><span className="wix-picker-name">deleteReward (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-reward-2"><span className="wix-picker-name">deleteReward (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteReward
    import { rewards } from '@wix/loyalty';

    async function deleteReward(_id,revision) {
      const response = await rewards.deleteReward(_id,revision);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReward (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rewards } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rewards },
      // Include the auth strategy and host as relevant
    });

    async function deleteReward(_id,revision) {
      const response = await myWixClient.rewards.deleteReward(_id,revision);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReward (with elevated permissions)
    import { rewards } from '@wix/loyalty';
    import { auth } from '@wix/essentials';

    async function myDeleteRewardMethod(_id,revision) {
      const elevatedDeleteReward = auth.elevate(rewards.deleteReward);
      const response = await elevatedDeleteReward(_id,revision);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
