---
title: "onOrderResumed"
description: "Triggered when a paused order is resumed."
icon: "bolt"
---

Triggered when a paused order is resumed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.v2.order_resumed` |
| Package | `@wix/pricing-plans` |
| Permissions | `Manage Orders`, `Read Orders`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onOrderResumed
  import { orders } from '@wix/pricing-plans';

  orders.onOrderResumed((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderResumed (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderResumed((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
