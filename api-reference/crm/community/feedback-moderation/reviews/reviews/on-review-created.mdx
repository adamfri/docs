---
title: "onReviewCreated"
description: "Triggered when a review is created."
icon: "bolt"
---

Triggered when a review is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reviews.v1.review_created` |
| Package | `@wix/reviews` |
| Permissions | `Manage Stores`, `Manage Reviews` |

## Examples

<CodeGroup>

  ```javascript onReviewCreated
  import { reviews } from '@wix/reviews';

  reviews.onReviewCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReviewCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reviews } from '@wix/reviews';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reviews,
      },
  });

  wixClient.reviews.onReviewCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
