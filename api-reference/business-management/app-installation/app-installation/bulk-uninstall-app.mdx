---
title: "Bulk Uninstall App"
description: "<blockquote class=\"note\" Note: Only logged-in Wix users or API key admins can use this API."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    Only logged-in [Wix users](https://dev.wix.com/docs/rest/articles/get-started/about-identities#wix-user) or [API key admins](https://dev.wix.com/docs/rest/articles/get-started/about-identities#api-key-admin) can use this API.

    </Note>

    Uninstalls apps from a tenant.

    This removes the instances of the specified apps from the tenant.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="APPS_INSTALLER.UNINSTALL_SITE_LEVEL">APPS_INSTALLER.UNINSTALL_SITE_LEVEL</span>
    <span className="wix-scope" title="APPS_INSTALLER.UNINSTALL_ACCOUNT_LEVEL">APPS_INSTALLER.UNINSTALL_ACCOUNT_LEVEL</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/apps-installer-service/v1/bulk/app-instance/uninstall
    ```

    ## Body Params

    <ParamField body="tenant" type="Tenant">

      Tenant details.

      <Expandable title="tenant properties">

        <ParamField body="id" type="string">

          Tenant ID. For a site, this is the [site ID](https://dev.wix.com/docs/rest/account-level/sites/sites/introduction). For an account this is the [account ID](https://dev.wix.com/docs/rest/account-level/user-management/accounts/accounts/about-accounts).

        </ParamField>

        <ParamField body="tenantType" type="string">

          Tenant type. This can be a Wix site or account.

          **Supported values:**

          - `SITE`
          - `ACCOUNT`

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="appDefIds" type="string[]" required>

      List of apps to be uninstalled.

      Constraints: `format GUID`, `maxItems 20`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkUninstallAppResult[]">

      Information and metadata about the uninstalled apps.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata about the uninstalled app instance.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="appDefId" type="string" deprecated>

          **Deprecated.** Use `app_instance` instead. Removal: 2025-09-01. Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="appInstance" type="AppInstance">

          Details of the uninstalled app instance.

          See the [App Instance Object](/api-reference/business-management/app-installation/app-installation/app-instance-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata about the bulk uninstallation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request POST \
      	https://www.wixapis.com/apps-installer-service/v1/bulk/app-instance/uninstall \
          -H 'Content-Type: application/json' \
          -H 'Authorization: <AUTH>'
      	-d '{
      		"tenant": {
      			"tenantType": "SITE",
      			"id": "573cf0bc-a4d0-434c-ab5f-c8babcfa4a5b"
      		},
      		"appDefIds": [
                  "96186799-d99a-4b96-adf9-7cb2c1cf1e79"
              ]
      	}'
      ~~~
      ```

      ```json Response
      ~~~json
      {
         "results":[
            {
               "itemMetadata":{
                  "id":"96186799-d99a-4b96-adf9-7cb2c1cf1e79",
                  "originalIndex":0,
                  "success":true
               },
               "appDefId":"96186799-d99a-4b96-adf9-7cb2c1cf1e79",
               "appInstance":{
                  "id":"07d49a20-531f-487a-8928-29426768dac7",
                  "appDefId":"96186799-d99a-4b96-adf9-7cb2c1cf1e79",
                  "enabled":false
               }
            }
         ],
         "bulkActionMetadata":{
            "totalSuccesses":1,
            "totalFailures":0,
            "undetailedFailures":0
         }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    Only logged-in [Wix users](https://dev.wix.com/docs/rest/articles/get-started/about-identities#wix-user) or [API key admins](https://dev.wix.com/docs/rest/articles/get-started/about-identities#api-key-admin) can use this API.

    </Note>

    Uninstalls apps from a tenant.

    This removes the instances of the specified apps from the tenant.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="APPS_INSTALLER.UNINSTALL_SITE_LEVEL">APPS_INSTALLER.UNINSTALL_SITE_LEVEL</span>
    <span className="wix-scope" title="APPS_INSTALLER.UNINSTALL_ACCOUNT_LEVEL">APPS_INSTALLER.UNINSTALL_ACCOUNT_LEVEL</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { appsInstaller } from '@wix/apps-installer';

    appsInstaller.bulkUninstallApp(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkUninstallAppOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="tenant" type="Tenant">

          Tenant details.

          <Expandable title="tenant properties">

            <ParamField body="_id" type="string">

              Tenant ID. For a site, this is the [site ID](https://dev.wix.com/docs/rest/account-level/sites/sites/introduction). For an account this is the [account ID](https://dev.wix.com/docs/rest/account-level/user-management/accounts/accounts/about-accounts).

            </ParamField>

            <ParamField body="tenantType" type="string">

              Tenant type. This can be a Wix site or account.

              **Supported values:**

              - `SITE`
              - `ACCOUNT`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="appDefIds" type="string[]" required>

          List of apps to be uninstalled.

          Constraints: `format GUID`, `maxItems 20`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkUninstallAppResult[]">

      Information and metadata about the uninstalled apps.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata about the uninstalled app instance.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="appDefId" type="string" deprecated>

          **Deprecated.** Use `app_instance` instead. Removal: 2025-09-01. Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="appInstance" type="AppInstance">

          Details of the uninstalled app instance.

          See the [App Instance Object](/api-reference/business-management/app-installation/app-installation/app-instance-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata about the bulk uninstallation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | App Instance Deleted | `wix.apps.v1.app_instance_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-uninstall-app" id="ex-sdk-bulk-uninstall-app-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-uninstall-app" id="ex-sdk-bulk-uninstall-app-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-uninstall-app" id="ex-sdk-bulk-uninstall-app-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-uninstall-app-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-uninstall-app-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUninstallApp</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUninstallApp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUninstallApp (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-uninstall-app-0"><span className="wix-picker-name">bulkUninstallApp</span></label>
    <label htmlFor="ex-sdk-bulk-uninstall-app-1"><span className="wix-picker-name">bulkUninstallApp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-uninstall-app-2"><span className="wix-picker-name">bulkUninstallApp (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUninstallApp
    import { appsInstaller } from '@wix/apps-installer';

    async function bulkUninstallApp(options) {
      const response = await appsInstaller.bulkUninstallApp(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUninstallApp (self-hosted)
    import { createClient } from '@wix/sdk';
    import { appsInstaller } from '@wix/apps-installer';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { appsInstaller },
      // Include the auth strategy and host as relevant
    });

    async function bulkUninstallApp(options) {
      const response = await myWixClient.appsInstaller.bulkUninstallApp(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUninstallApp (with elevated permissions)
    import { appsInstaller } from '@wix/apps-installer';
    import { auth } from '@wix/essentials';

    async function myBulkUninstallAppMethod(options) {
      const elevatedBulkUninstallApp = auth.elevate(appsInstaller.bulkUninstallApp);
      const response = await elevatedBulkUninstallApp(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
