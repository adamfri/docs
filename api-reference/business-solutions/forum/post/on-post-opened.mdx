---
title: "onPostOpened"
description: "An existing post was opened for further comments."
icon: "bolt"
---

An existing post was opened for further comments.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_opened` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostOpened
  import { posts } from '@wix/forum';

  posts.onPostOpened((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostOpened (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostOpened((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
