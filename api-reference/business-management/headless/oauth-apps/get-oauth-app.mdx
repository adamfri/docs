---
title: "Get OAuth App"
description: "Retrieves an OAuth app by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an OAuth app by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.OAUTH_APP.READ">Read OAuth Apps</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/oauth-app/v1/oauth-apps/{oAuthAppId}
    ```

    ## Path Params

    <ParamField path="oAuthAppId" type="string" required>

      ID of the OAuth app to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="oAuthApp" type="OAuthApp">

      Retrieved OAuth app info.

      See the [O Auth App Object](/api-reference/business-management/headless/oauth-apps/o-auth-app-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/oauth-app/v1/oauth-apps/adddb436-aa2c-474a-8f0e-af719f3acd5b' \
      -H 'Authorization: <AUTH> \
      -H 'Content-Type: application/json' \
      ```

      ```json Response
      {
        "oAuthApp": {
          "id": "adddb436-aa2c-474a-8f0e-af719f3acd5b",
          "createdDate": "2023-03-28T13:43:48.959Z",
          "name": "Test app",
          "description": "Some test app",
          "allowedRedirectDomains": [],
          "loginUrl": "",
          "logoutUrl": "",
          "allowedRedirectUris": [],
          "applicationType": "OAUTH_APP_TYPE_UNSPECIFIED",
          "technology": "OAUTH_APP_TECHNOLOGY_UNSPECIFIED",
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves an OAuth app by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.OAUTH_APP.READ">Read OAuth Apps</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { oAuthApps } from '@wix/auth-management';

    oAuthApps.getOAuthApp(oAuthAppId): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="oAuthAppId" type="string" required>

      ID of the OAuth app to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    See the [O Auth App Object](/api-reference/business-management/headless/oauth-apps/o-auth-app-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-oauth-app" id="ex-sdk-get-oauth-app-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-oauth-app" id="ex-sdk-get-oauth-app-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-oauth-app" id="ex-sdk-get-oauth-app-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-oauth-app-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-oauth-app-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getOAuthApp</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getOAuthApp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getOAuthApp (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-oauth-app-0"><span className="wix-picker-name">getOAuthApp</span></label>
    <label htmlFor="ex-sdk-get-oauth-app-1"><span className="wix-picker-name">getOAuthApp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-oauth-app-2"><span className="wix-picker-name">getOAuthApp (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getOAuthApp
    import { oAuthApps } from '@wix/auth-management';

    async function getOAuthApp(oAuthAppId) {
      const response = await oAuthApps.getOAuthApp(oAuthAppId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getOAuthApp (self-hosted)
    import { createClient } from '@wix/sdk';
    import { oAuthApps } from '@wix/auth-management';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { oAuthApps },
      // Include the auth strategy and host as relevant
    });

    async function getOAuthApp(oAuthAppId) {
      const response = await myWixClient.oAuthApps.getOAuthApp(oAuthAppId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getOAuthApp (with elevated permissions)
    import { oAuthApps } from '@wix/auth-management';
    import { auth } from '@wix/essentials';

    async function myGetOAuthAppMethod(oAuthAppId) {
      const elevatedGetOAuthApp = auth.elevate(oAuthApps.getOAuthApp);
      const response = await elevatedGetOAuthApp(oAuthAppId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
