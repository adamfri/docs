---
title: "Create Program Definition"
description: "Creates a program definition."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a program definition.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/program-definitions
    ```

    ## Body Params

    <ParamField body="programDefinition" type="ProgramDefinition" required>

      Program definition to create.

      See the [Program Definition Object](/api-reference/business-solutions/benefit-programs/program-definitions/program-definition-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="programDefinition" type="ProgramDefinition">

      Created program definition.

      See the [Program Definition Object](/api-reference/business-solutions/benefit-programs/program-definitions/program-definition-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request POST \
        https://www.wixapis.com/benefit-programs/v1/program-definitions \
        -H 'Authorization: <AUTH>' \
        -H "Content-Type: application/json" \
        --data '{
          "program_definition": {
            "namespace": "benefit_programs_app",
            "external_id": "myProgramDefinition",
            "display_name": "My Gold plan"
          }
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "program_definition": {
          "id": "1f4ef6ff-7228-3c24-9fe2-252f18959285",
          "revision": "1",
          "created_date": "2025-02-18T13:04:14.908Z",
          "updated_date": "2025-02-18T13:04:14.908Z",
          "display_name": "My Gold plan",
          "namespace": "benefit_programs_app",
          "extended_fields": null,
          "external_id": "myProgramDefinition"
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a program definition.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programDefinitions } from '@wix/benefit-programs';

    programDefinitions.createProgramDefinition(programDefinition): Promise<ProgramDefinition>
    ```

    ## Method Parameters

    <ParamField body="programDefinition" type="ProgramDefinition" required>

      Program definition to create.

      See the [Program Definition Object](/api-reference/business-solutions/benefit-programs/program-definitions/program-definition-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<ProgramDefinition>`.

    See the [Program Definition Object](/api-reference/business-solutions/benefit-programs/program-definitions/program-definition-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Definition Created | `wix.benefit_programs.v1.program_definition_created` | Triggered when a program definition is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-program-definition" id="ex-sdk-create-program-definition-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-program-definition" id="ex-sdk-create-program-definition-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-program-definition" id="ex-sdk-create-program-definition-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-program-definition-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-program-definition-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createProgramDefinition</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createProgramDefinition (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createProgramDefinition (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-program-definition-0"><span className="wix-picker-name">createProgramDefinition</span></label>
    <label htmlFor="ex-sdk-create-program-definition-1"><span className="wix-picker-name">createProgramDefinition (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-program-definition-2"><span className="wix-picker-name">createProgramDefinition (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createProgramDefinition
    import { programDefinitions } from '@wix/benefit-programs';

    async function createProgramDefinition(programDefinition) {
      const response = await programDefinitions.createProgramDefinition(programDefinition);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createProgramDefinition (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programDefinitions } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programDefinitions },
      // Include the auth strategy and host as relevant
    });

    async function createProgramDefinition(programDefinition) {
      const response = await myWixClient.programDefinitions.createProgramDefinition(programDefinition);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createProgramDefinition (with elevated permissions)
    import { programDefinitions } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myCreateProgramDefinitionMethod(programDefinition) {
      const elevatedCreateProgramDefinition = auth.elevate(programDefinitions.createProgramDefinition);
      const response = await elevatedCreateProgramDefinition(programDefinition);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
