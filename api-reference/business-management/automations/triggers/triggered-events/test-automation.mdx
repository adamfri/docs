---
title: "Test Automation"
description: "Runs an automation in test mode, without reporting an event."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Runs an automation in test mode, without reporting an event.

    Test mode runs the automation and its actions for real, but it can also run unpublished draft automations and skips any configured delays so you see the results immediately. Use it to verify an automation's behavior before publishing it.

    Identify the automation to run the same way as in [Run Automation](https://dev.wix.com/docs/api-reference/business-management/automations/triggers/triggered-events/run-automation).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/automations/v1/events/test-automation
    ```

    ## Body Params

    <ParamField body="identifierType" type="string" required>

      How the automation to run is identified. Set to `AUTOMATION` to use `automationIdentifier`, or `PREINSTALLED` to use `preinstalledIdentifier`.

      Constraints: `immutable`.

      **Supported values:**

      - `PREINSTALLED` — Identifies a preinstalled automation by its app ID and component ID.
      - `AUTOMATION` — Identifies an automation by its ID.

    </ParamField>

    <ParamField body="payload" type="object" required>

      Event payload, formatted as key:value pairs.
      Must comply with the payload schema
      if you provided one when configuring your trigger.

      Key names can include only alphanumeric characters or underscores
      (`A-Z`, `a-z`, `0-9`, `_`).
      They cannot start with an underscore.

      Values can be strings, numbers, integers, booleans, or arrays.
      If a value is an array, the array items must be objects,
      and nested object properties must be
      strings, numbers, integers, or booleans only.

    </ParamField>

    ## Response Object

    <ResponseField name="activationId" type="string">

      ID of the activation created for this test run.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST https://www.wixapis.com/automations/v1/events/test-automation  \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "identifierType": "AUTOMATION",
          "automationIdentifier": {
            "automationId": "d7a341c4-196b-46e7-813c-2b0fc1847ec6"
          },
          "payload": {
            "customerName": "John Doe",
            "bookingId": "a1b2c3d4-5e6f-4a1b-9c8d-7e6f5a4b3c2d"
          }
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "activationId": "b8e4d1f2-3a6c-4d9b-8e7f-1c2d3a4b5c6e"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Runs an automation in test mode, without reporting an event.

    Test mode runs the automation and its actions for real, but it can also run unpublished draft automations and skips any configured delays so you see the results immediately. Use it to verify an automation's behavior before publishing it.

    Identify the automation to run the same way as in [Run Automation](https://dev.wix.com/docs/api-reference/business-management/automations/triggers/triggered-events/run-automation).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { activations } from '@wix/automations';

    activations.testAutomation(identifierType, options): Promise
    ```

    ## Method Parameters

    <ParamField body="identifierType" type="string" required>

      How the automation to run is identified. Set to `AUTOMATION` to use `automationIdentifier`, or `PREINSTALLED` to use `preinstalledIdentifier`.

      Constraints: `immutable`.

      **Supported values:**

      - `PREINSTALLED` — Identifies a preinstalled automation by its app ID and component ID.
      - `AUTOMATION` — Identifies an automation by its ID.

    </ParamField>

    <ParamField body="options" type="TestAutomationOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="payload" type="object" required>

          Event payload, formatted as key:value pairs.
          Must comply with the payload schema
          if you provided one when configuring your trigger.

          Key names can include only alphanumeric characters or underscores
          (`A-Z`, `a-z`, `0-9`, `_`).
          They cannot start with an underscore.

          Values can be strings, numbers, integers, booleans, or arrays.
          If a value is an array, the array items must be objects,
          and nested object properties must be
          strings, numbers, integers, or booleans only.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="identifier" type="identifier">

          identifier

          <Expandable title="identifier properties">

            <ParamField body="preinstalledIdentifier" type="PreinstalledIdentifier">

              Identifies a preinstalled automation by its app ID and component ID.

              _Nested structure omitted for brevity — see the `PreinstalledIdentifier` object._

            </ParamField>

            <ParamField body="automationIdentifier" type="AutomationIdentifier">

              Identifies an automation by its ID.

              _Nested structure omitted for brevity — see the `AutomationIdentifier` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="activationId" type="string">

      ID of the activation created for this test run.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-test-automation" id="ex-sdk-test-automation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-test-automation" id="ex-sdk-test-automation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-test-automation" id="ex-sdk-test-automation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-test-automation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-test-automation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">testAutomation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">testAutomation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">testAutomation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-test-automation-0"><span className="wix-picker-name">testAutomation</span></label>
    <label htmlFor="ex-sdk-test-automation-1"><span className="wix-picker-name">testAutomation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-test-automation-2"><span className="wix-picker-name">testAutomation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript testAutomation
    import { activations } from '@wix/automations';

    async function testAutomation(identifierType,options) {
      const response = await activations.testAutomation(identifierType,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript testAutomation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { activations } from '@wix/automations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { activations },
      // Include the auth strategy and host as relevant
    });

    async function testAutomation(identifierType,options) {
      const response = await myWixClient.activations.testAutomation(identifierType,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript testAutomation (with elevated permissions)
    import { activations } from '@wix/automations';
    import { auth } from '@wix/essentials';

    async function myTestAutomationMethod(identifierType,options) {
      const elevatedTestAutomation = auth.elevate(activations.testAutomation);
      const response = await elevatedTestAutomation(identifierType,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
