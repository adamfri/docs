---
title: "onScheduleUpdated"
description: "Triggered when a schedule is updated."
icon: "bolt"
---

Triggered when a schedule is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.calendar.v3.schedule_updated` |
| Package | `@wix/calendar` |
| Permissions | `Read Calendar - Including PI`, `Manage Calendars`, `Manage Bookings`, `Read Bookings - Including Participants`, `Manage Bookings - all permissions`, `Read bookings calendar - including participants` |

## Examples

<CodeGroup>

  ```javascript onScheduleUpdated
  import { schedules } from '@wix/calendar';

  schedules.onScheduleUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onScheduleUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { schedules } from '@wix/calendar';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        schedules,
      },
  });

  wixClient.schedules.onScheduleUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
