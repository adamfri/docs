---
title: "onPostLiked"
description: "Triggered when a post is liked."
icon: "bolt"
---

Triggered when a post is liked.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v3.post_liked` |
| Package | `@wix/blog` |
| Permissions | `Read Blog`, `Manage Blog`, `Read Draft Blog Posts` |

## Examples

<CodeGroup>

  ```javascript onPostLiked
  import { posts } from '@wix/blog';

  posts.onPostLiked((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostLiked (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostLiked((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
