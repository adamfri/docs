---
title: "onOAuthAppCreated"
description: "Triggered when an OAuth app is created."
icon: "bolt"
---

Triggered when an OAuth app is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.headless.v1.o_auth_app_created` |
| Package | `@wix/auth-management` |
| Permissions | `Headless Manage`, `Manage OAuth Apps`, `Read OAuth Apps` |

## Examples

<CodeGroup>

  ```javascript onOAuthAppCreated
  import { oAuthApps } from '@wix/auth-management';

  oAuthApps.onOAuthAppCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOAuthAppCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { oAuthApps } from '@wix/auth-management';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        oAuthApps,
      },
  });

  wixClient.oAuthApps.onOAuthAppCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
