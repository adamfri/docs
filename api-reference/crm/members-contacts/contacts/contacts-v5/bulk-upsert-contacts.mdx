---
title: "Bulk Upsert Contacts"
description: "Creates or updates multiple contacts in a single request."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates or updates multiple contacts in a single request. Works synchronously.

    Each contact is matched against existing contacts using the [contact matching](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts) logic. If a match is found, the contact is updated. Otherwise, a new contact is created.

    When a contact is updated, `upsertMode` controls how:
    - `OVERWRITE` (default): Replaces existing field values.
    - `APPEND`: Sets a non-array field only if it doesn't already have a value, and appends to array fields.
    - `OVERWRITE_APPEND_ARRAYS`: Overwrites non-array fields and appends to array fields.

    In `APPEND` or `OVERWRITE_APPEND_ARRAYS` mode, the default address flag is ignored unless the contact has no existing addresses.

    When you provide an image URL and the upsert mode allows the image to be updated, the image is uploaded in the background. The returned contact temporarily has no image if it was created, or its previous image if it was updated. Once the upload succeeds, the contact's image is updated.

    To create or update a single contact, call [Upsert Contact](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/upsert-contact).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACTS">Manage Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/contacts/v5/bulk/contacts/upsert
    ```

    ## Body Params

    <ParamField body="contacts" type="MaskedContact[]" required>

      Contacts to create or update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="contacts properties">

        <ParamField body="contact" type="Contact">

          Contact to create or update. May be partial.

          See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to include the upserted contacts in the response.

    </ParamField>

    <ParamField body="upsertMode" type="string">

      Mode that determines how an existing contact is updated when a match is found. See the endpoint description for the supported modes.

      **Supported values:**

      - `OVERWRITE` — Replaces existing field values. Default value.
      - `APPEND` — Sets a non-array field only if it doesn't already have a value, and appends to array fields.
      - `OVERWRITE_APPEND_ARRAYS` — Overwrites non-array fields and appends to array fields.

    </ParamField>

    <ParamField body="updateMember" type="boolean">

      Whether to update a contact even if it's a site member.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkContactResult[]">

      Results of the upsert for each contact.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata regarding the upsert of this specific contact.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Contact">

          Returned only when `returnEntity` is `true` in the request.

          See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

        </ResponseField>

        <ResponseField name="action" type="string">

          The action performed on the contact.

          **Supported values:**

          - `UNKNOWN_ACTION`
          - `UPDATED`
          - `CREATED`

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata regarding the bulk upsert operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Matches contacts by their identifiers. Existing contacts are updated, otherwise new ones are created.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/contacts/v5/bulk/contacts/upsert' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "contacts": [
          {
            "contact": {
              "email": {
                "email": "maria.hill@example.com"
              },
              "company": {
                "name": "ExampleCo",
                "jobTitle": "Head of Marketing"
              }
            }
          },
          {
            "contact": {
              "name": {
                "first": "Owen",
                "last": "Carter"
              },
              "email": {
                "email": "owen.carter@example.com"
              },
              "company": {
                "name": "ExampleCo",
                "jobTitle": "Sales Director"
              }
            }
          }
        ],
        "returnEntity": true,
        "upsertMode": "OVERWRITE"
      }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "fa6fe461-6fe9-400b-9866-2ad7734dba57",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "id": "fa6fe461-6fe9-400b-9866-2ad7734dba57",
              "revision": "1",
              "createdDate": "2026-07-01T18:26:15.723Z",
              "updatedDate": "2026-07-01T18:26:15.723Z",
              "name": {
                "first": "Owen",
                "last": "Carter"
              },
              "email": {
                "email": "owen.carter@example.com",
                "subscriptionStatus": {
                  "status": "NOT_SET"
                },
                "deliverabilityStatus": "NOT_SET"
              },
              "additionalEmails": [],
              "additionalPhones": [],
              "addresses": [],
              "company": {
                "name": "ExampleCo",
                "jobTitle": "Sales Director"
              },
              "locationIds": [],
              "source": {
                "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438"
              },
              "lastActivity": {
                "date": "2026-07-01T18:26:15.723Z",
                "templateId": "202edbe8-e365-490d-aa18-4335b26ab8c1"
              },
              "segmentIds": []
            },
            "action": "CREATED"
          },
          {
            "itemMetadata": {
              "id": "3078e8d6-6bfc-4304-9a84-bbc3fa64ffdb",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "3078e8d6-6bfc-4304-9a84-bbc3fa64ffdb",
              "revision": "5",
              "createdDate": "2026-07-01T18:24:15.364Z",
              "updatedDate": "2026-07-01T18:26:15.784Z",
              "name": {
                "first": "Maria",
                "last": "Hill"
              },
              "email": {
                "email": "maria.hill@example.com",
                "subscriptionStatus": {
                  "status": "SUBSCRIBED",
                  "updatedDate": "2026-06-29T10:33:17.590Z"
                },
                "deliverabilityStatus": "NOT_SET"
              },
              "additionalEmails": [],
              "additionalPhones": [],
              "addresses": [],
              "company": {
                "name": "ExampleCo",
                "jobTitle": "Head of Marketing"
              },
              "locationIds": [],
              "source": {
                "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438"
              },
              "lastActivity": {
                "date": "2026-07-01T18:24:15.364Z",
                "templateId": "202edbe8-e365-490d-aa18-4335b26ab8c1"
              },
              "segmentIds": [],
              "tags": {
                "privateTags": {
                  "tagIds": [
                    "zsdCR"
                  ]
                }
              }
            },
            "action": "UPDATED"
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates or updates multiple contacts in a single request. Works synchronously.

    Each contact is matched against existing contacts using the [contact matching](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts) logic. If a match is found, the contact is updated. Otherwise, a new contact is created.

    When a contact is updated, `upsertMode` controls how:
    - `OVERWRITE` (default): Replaces existing field values.
    - `APPEND`: Sets a non-array field only if it doesn't already have a value, and appends to array fields.
    - `OVERWRITE_APPEND_ARRAYS`: Overwrites non-array fields and appends to array fields.

    In `APPEND` or `OVERWRITE_APPEND_ARRAYS` mode, the default address flag is ignored unless the contact has no existing addresses.

    When you provide an image URL and the upsert mode allows the image to be updated, the image is uploaded in the background. The returned contact temporarily has no image if it was created, or its previous image if it was updated. Once the upload succeeds, the contact's image is updated.

    To create or update a single contact, call [Upsert Contact](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/upsert-contact).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-CONTACTS">Manage Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contactsV5 } from '@wix/crm';

    contactsV5.bulkUpsertContacts(contacts, options): Promise
    ```

    ## Method Parameters

    <ParamField body="contacts" type="MaskedContact[]" required>

      Contacts to create or update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="contacts properties">

        <ParamField body="contact" type="Contact">

          Contact to create or update. May be partial.

          See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="BulkUpsertContactsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to include the upserted contacts in the response.

        </ParamField>

        <ParamField body="upsertMode" type="string">

          Mode that determines how an existing contact is updated when a match is found. See the endpoint description for the supported modes.

          **Supported values:**

          - `OVERWRITE` — Replaces existing field values. Default value.
          - `APPEND` — Sets a non-array field only if it doesn't already have a value, and appends to array fields.
          - `OVERWRITE_APPEND_ARRAYS` — Overwrites non-array fields and appends to array fields.

        </ParamField>

        <ParamField body="updateMember" type="boolean">

          Whether to update a contact even if it's a site member.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkContactResult[]">

      Results of the upsert for each contact.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata regarding the upsert of this specific contact.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Contact">

          Returned only when `returnEntity` is `true` in the request.

          See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

        </ResponseField>

        <ResponseField name="action" type="string">

          The action performed on the contact.

          **Supported values:**

          - `UNKNOWN_ACTION`
          - `UPDATED`
          - `CREATED`

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata regarding the bulk upsert operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Contact Created | `wix.contacts.v5.contact_created` | Triggered when a contact is created. |
    | Contact Updated | `wix.contacts.v5.contact_updated` | Triggered when a contact is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-upsert-contacts" id="ex-sdk-bulk-upsert-contacts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-upsert-contacts" id="ex-sdk-bulk-upsert-contacts-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-upsert-contacts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-upsert-contacts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create or update contacts in bulk</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpsertContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-upsert-contacts-0"><span className="wix-picker-name">Create or update contacts in bulk</span></label>
    <label htmlFor="ex-sdk-bulk-upsert-contacts-1"><span className="wix-picker-name">bulkUpsertContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create or update contacts in bulk
    import { contactsV5 } from "@wix/crm";

    const contacts = [
      {
        contact: {
          email: { email: "maria.hill@example.com" },
          company: { name: "ExampleCo", jobTitle: "Head of Marketing" },
        },
      },
      {
        contact: {
          name: { first: "Owen", last: "Carter" },
          email: { email: "owen.carter@example.com" },
          company: { name: "ExampleCo", jobTitle: "Sales Director" },
        },
      },
    ];

    const options = {
      returnEntity: true,
      upsertMode: contactsV5.UpsertMode.OVERWRITE,
    };

    async function bulkUpsertContacts() {
      const response = await contactsV5.bulkUpsertContacts(contacts, options);
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 1,
     *         "success": true,
     *         "_id": "5d7608ee-9198-4ee1-a045-cc72e9cc6cb9"
     *       },
     *       "item": {
     *         "revision": "1",
     *         "name": { "first": "Owen", "last": "Carter" },
     *         "company": { "name": "ExampleCo", "jobTitle": "Sales Director" },
     *         "_id": "5d7608ee-9198-4ee1-a045-cc72e9cc6cb9"
     *       },
     *       "action": "CREATED"
     *     },
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 0,
     *         "success": true,
     *         "_id": "55022a00-b54c-4ccc-a131-020cbb1b45e8"
     *       },
     *       "item": {
     *         "revision": "5",
     *         "name": { "first": "Maria", "last": "Hill" },
     *         "company": { "name": "ExampleCo", "jobTitle": "Head of Marketing" },
     *         "_id": "55022a00-b54c-4ccc-a131-020cbb1b45e8"
     *       },
     *       "action": "UPDATED"
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 2,
     *     "totalFailures": 0,
     *     "undetailedFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpsertContacts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contactsV5 } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contactsV5 },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpsertContacts(contacts,options) {
      const response = await myWixClient.contactsV5.bulkUpsertContacts(contacts,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
