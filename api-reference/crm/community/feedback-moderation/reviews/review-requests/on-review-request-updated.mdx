---
title: "onReviewRequestUpdated"
description: "Triggered when a review request is updated."
icon: "bolt"
---

Triggered when a review request is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reviews.v2.review_request_updated` |
| Package | `@wix/reviews` |
| Permissions | `Manage Review Requests` |

## Examples

<CodeGroup>

  ```javascript onReviewRequestUpdated
  import { reviewRequests } from '@wix/reviews';

  reviewRequests.onReviewRequestUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReviewRequestUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reviewRequests } from '@wix/reviews';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reviewRequests,
      },
  });

  wixClient.reviewRequests.onReviewRequestUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
