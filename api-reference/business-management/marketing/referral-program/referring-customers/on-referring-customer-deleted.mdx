---
title: "onReferringCustomerDeleted"
description: "Triggered when a referring customer is deleted."
icon: "bolt"
---

Triggered when a referring customer is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.referral.v1.referring_customer_deleted` |
| Package | `@wix/referral` |
| Permissions | `Manage Referrals` |

## Examples

<CodeGroup>

  ```javascript onReferringCustomerDeleted
  import { customers } from '@wix/referral';

  customers.onReferringCustomerDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onReferringCustomerDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { customers } from '@wix/referral';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        customers,
      },
  });

  wixClient.customers.onReferringCustomerDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
