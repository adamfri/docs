---
title: "onBadgeAssignmentCreated"
description: "Triggered when a badge assignment is created."
icon: "bolt"
---

Triggered when a badge assignment is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v4.badge_assignment_created` |
| Package | `@wix/members` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Manage Stores`, `Manage Events`, `Manage Online Programs`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeAssignmentCreated
  import { badgeAssignments } from '@wix/members';

  badgeAssignments.onBadgeAssignmentCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeAssignmentCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badgeAssignments } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        badgeAssignments,
      },
  });

  wixClient.badgeAssignments.onBadgeAssignmentCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
