---
title: "Renew Program"
description: "Renews the specified program."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Renews the specified program.

    The number of available credits in each pool in the program is defined in the corresponding pool's credit configuration.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/programs/renew
    ```

    ## Body Params

    <ParamField body="programId" type="string" required>

      ID of the program to renew.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="idempotencyKey" type="string" required>

      Idempotency key to ensure that the request is processed only once.

      Constraints: `maxLength 64`.

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID of the program renewal. Retrieve job details using the [Async Job API](https://dev.wix.com/docs/rest/business-management/async-job/introduction).

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request POST https://www.wixapis.com/benefit-programs/v1/programs/renew \
        -H "Content-Type: application/json" \
        -H "Authorization: <AUTH>" \
        --data '{
          "program_id": "e5c42e1e-cd10-4210-b483-bb59932b1fa7"
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "job_id": "60d5a38e-abc2-4446-8959-0b7ba5bded73"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Renews the specified program.

    The number of available credits in each pool in the program is defined in the corresponding pool's credit configuration.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/benefit-programs';

    programs.renewProgram(programId, options): Promise<RenewProgramResponse>
    ```

    ## Method Parameters

    <ParamField body="programId" type="string" required>

      ID of the program to renew.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="RenewProgramOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="idempotencyKey" type="string" required>

          Idempotency key to ensure that the request is processed only once.

          Constraints: `maxLength 64`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<RenewProgramResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID of the program renewal. Retrieve job details using the [Async Job API](https://dev.wix.com/docs/rest/business-management/async-job/introduction).

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Updated | `wix.benefit_programs.v1.program_updated` | Triggered when a program is updated. |
    | Program Renewed | `wix.benefit_programs.v1.program_renewed` | Triggered when a program is renewed. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-renew-program" id="ex-sdk-renew-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-renew-program" id="ex-sdk-renew-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-renew-program" id="ex-sdk-renew-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-renew-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-renew-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">renewProgram</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">renewProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">renewProgram (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-renew-program-0"><span className="wix-picker-name">renewProgram</span></label>
    <label htmlFor="ex-sdk-renew-program-1"><span className="wix-picker-name">renewProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-renew-program-2"><span className="wix-picker-name">renewProgram (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript renewProgram
    import { programs } from '@wix/benefit-programs';

    async function renewProgram(programId,options) {
      const response = await programs.renewProgram(programId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript renewProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function renewProgram(programId,options) {
      const response = await myWixClient.programs.renewProgram(programId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript renewProgram (with elevated permissions)
    import { programs } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myRenewProgramMethod(programId,options) {
      const elevatedRenewProgram = auth.elevate(programs.renewProgram);
      const response = await elevatedRenewProgram(programId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
