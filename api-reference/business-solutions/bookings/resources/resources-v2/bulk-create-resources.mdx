---
title: "Bulk Create Resources"
description: "Creates up to 50 resources."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates up to 50 resources.

    Refer to [Create Resource](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resources-v2/create-resource)
    for more details.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/bulk/resources/create
    ```

    ## Body Params

    <ParamField body="resources" type="Resource[]" required>

      Resources to create.

      Constraints: `maxItems 50`.

      See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to include the created resources in the response.

      Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkResourceResult[]">

      The result of each resource creation.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Resource">

          The resulting resource after the bulk operation.

          See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Create statistics.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_LOCATION_ID_IS_REQUIRED` | Business location ID is required. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_SPECIFIC_LOCATION_OPTIONS_NOT_ALLOWED` | `specificLocationOptions` can't be set when `availableInAllLocations` is set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_SPECIFIC_LOCATION_OPTIONS_MUST_BE_SET` | `specificLocationOptions` must be set when `availableInAllLocations` is set to `false` or empty. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_AVAILABLE_IN_BUSINESS_LOCATION_MUST_BE_TRUE` | `availableInBusinessLocations` must be set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_WORKING_HOURS_NOT_SUPPORTED_IN_BULK_REQUEST` | `workingHoursSchedules` isn't supported in bulk requests. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Resource Created | `wix.bookings.resources.v2.resource_created` | Triggered when a resource is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
          'https://www.wixapis.com/bookings/v2/bulk/resources/create' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "resources": [
          {
            "name": "Small meeting room",
            "typeId": "dd7790c7-f3ac-4821-accb-52ed385fd932",
            "locationOptions": {
              "specificLocationOptions": {
                "businessLocations": [
                  {
                    "locationId": "808dfb00-0a74-4e70-bb3b-f96ff6ba1246"
                  }
                ],
                "availableInBusinessLocations": true
              }
            }
          }
        ],
        "returnEntity": true
      }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "ef5f8d3d-9269-490c-a067-a3d226fcb5d8",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "ef5f8d3d-9269-490c-a067-a3d226fcb5d8",
              "revision": "1",
              "createdDate": "2024-11-22T14:02:34.993Z",
              "updatedDate": "2024-11-22T14:02:34.993Z",
              "name": "Small meeting room",
              "typeId": "dd7790c7-f3ac-4821-accb-52ed385fd932",
              "locationOptions": {
                "availableInAllLocations": false,
                "specificLocationOptions": {
                  "availableInBusinessLocations": true,
                  "businessLocations": [
                    {
                      "locationId": "808dfb00-0a74-4e70-bb3b-f96ff6ba1246"
                    }
                  ]
                }
              },
              "eventsSchedule": {
                "scheduleId": "ee500718-7ce3-478a-89fb-1d839a37e324"
              }
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 1,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates up to 50 resources.

    Refer to [Create Resource](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resources-v2/create-resource)
    for more details.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { resources } from '@wix/bookings';

    resources.bulkCreateResources(resources, options): Promise<BulkCreateResourcesResponse>
    ```

    ## Method Parameters

    <ParamField body="resources" type="Resource[]" required>

      Resources to create.

      Constraints: `maxItems 50`.

      See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateResourcesOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to include the created resources in the response.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkCreateResourcesResponse>`.

    <ResponseField name="results" type="BulkResourceResult[]">

      The result of each resource creation.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Resource">

          The resulting resource after the bulk operation.

          See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Create statistics.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_LOCATION_ID_IS_REQUIRED` | Business location ID is required. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_SPECIFIC_LOCATION_OPTIONS_NOT_ALLOWED` | `specificLocationOptions` can't be set when `availableInAllLocations` is set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_SPECIFIC_LOCATION_OPTIONS_MUST_BE_SET` | `specificLocationOptions` must be set when `availableInAllLocations` is set to `false` or empty. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_AVAILABLE_IN_BUSINESS_LOCATION_MUST_BE_TRUE` | `availableInBusinessLocations` must be set to `true`. |
    | `400` | `INVALID_ARGUMENT` | `RESOURCE_WORKING_HOURS_NOT_SUPPORTED_IN_BULK_REQUEST` | `workingHoursSchedules` isn't supported in bulk requests. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Resource Created | `wix.bookings.resources.v2.resource_created` | Triggered when a resource is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-resources" id="ex-sdk-bulk-create-resources-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-resources" id="ex-sdk-bulk-create-resources-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-create-resources" id="ex-sdk-bulk-create-resources-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-resources-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-resources-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateResources</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateResources (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateResources (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-resources-0"><span className="wix-picker-name">bulkCreateResources</span></label>
    <label htmlFor="ex-sdk-bulk-create-resources-1"><span className="wix-picker-name">bulkCreateResources (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-create-resources-2"><span className="wix-picker-name">bulkCreateResources (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkCreateResources
    import { resources } from '@wix/bookings';

    async function bulkCreateResources(resources,options) {
      const response = await resources.bulkCreateResources(resources,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateResources (self-hosted)
    import { createClient } from '@wix/sdk';
    import { resources } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { resources },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateResources(resources,options) {
      const response = await myWixClient.resources.bulkCreateResources(resources,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateResources (with elevated permissions)
    import { resources } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myBulkCreateResourcesMethod(resources,options) {
      const elevatedBulkCreateResources = auth.elevate(resources.bulkCreateResources);
      const response = await elevatedBulkCreateResources(resources,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
