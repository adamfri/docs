---
title: "Archive Program"
description: "Archives a program."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Archives a program.

    Archiving closes the program, removes it from normal public discovery, and
    replaces its SEO slug with the program ID so the previous slug can be reused.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/online-programs/v3/programs/{programId}/archive
    ```

    ## Path Params

    <ParamField path="programId" type="string" required>

      ID of the program to archive.

    </ParamField>

    ## Response Object

    <ResponseField name="program" type="Program">

      Archived program.

      See the [Program Object](/api-reference/business-management/online-programs/programs/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Archives a program and removes it from normal public discovery.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/online-programs/v3/programs/8f6f3f3d-2c3b-4f8c-9e4a-1a5d2e0f7c11/archive' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "program": {
          "id": "8f6f3f3d-2c3b-4f8c-9e4a-1a5d2e0f7c11",
          "revision": "7",
          "createdDate": "2026-06-01T12:45:58.570Z",
          "updatedDate": "2026-06-01T12:47:51.280Z",
          "status": "ARCHIVED",
          "statusUpdatedDate": "2026-06-01T12:47:51.274Z",
          "categoryIds": [],
          "description": {
            "title": "30-Day Wellness Challenge: Spring Edition",
            "details": "A guided program with daily activities and progress tracking."
          },
          "timeline": {
            "selfPaced": true
          },
          "restrictions": {
            "maxParticipants": 200,
            "hideFutureSteps": false,
            "resolveStepsInOrder": true,
            "shareProgress": true,
            "accessType": "PUBLIC"
          },
          "seo": {
            "slug": "8f6f3f3d-2c3b-4f8c-9e4a-1a5d2e0f7c11",
            "url": {
              "base": "https://example.wixsite.com/site",
              "path": "/challenge-page/8f6f3f3d-2c3b-4f8c-9e4a-1a5d2e0f7c11"
            },
            "imageAlt": "",
            "courseWorkload": "P0D"
          },
          "rewards": [],
          "shouldSendInvoice": true,
          "videoStepsSettings": {
            "autoNavigate": true,
            "requiredCompletionPercentage": 80
          },
          "contentSummary": {
            "stepCount": 0,
            "sectionCount": 0
          },
          "ownerUserId": "2b2f5a4d-93f1-4d71-a8a6-10235c50d9f3"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Archives a program.

    Archiving closes the program, removes it from normal public discovery, and
    replaces its SEO slug with the program ID so the previous slug can be reused.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/online-programs';

    programs.archiveProgram(programId): Promise
    ```

    ## Method Parameters

    <ParamField body="programId" type="string" required>

      ID of the program to archive.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="program" type="Program">

      Archived program.

      See the [Program Object](/api-reference/business-management/online-programs/programs/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Program Archived | `wix.online_programs.v3.program_program_archived` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-archive-program" id="ex-sdk-archive-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-archive-program" id="ex-sdk-archive-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-archive-program" id="ex-sdk-archive-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-archive-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-archive-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">archiveProgram</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">archiveProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">archiveProgram (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-archive-program-0"><span className="wix-picker-name">archiveProgram</span></label>
    <label htmlFor="ex-sdk-archive-program-1"><span className="wix-picker-name">archiveProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-archive-program-2"><span className="wix-picker-name">archiveProgram (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript archiveProgram
    import { programs } from '@wix/online-programs';

    async function archiveProgram(programId) {
      const response = await programs.archiveProgram(programId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript archiveProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function archiveProgram(programId) {
      const response = await myWixClient.programs.archiveProgram(programId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript archiveProgram (with elevated permissions)
    import { programs } from '@wix/online-programs';
    import { auth } from '@wix/essentials';

    async function myArchiveProgramMethod(programId) {
      const elevatedArchiveProgram = auth.elevate(programs.archiveProgram);
      const response = await elevatedArchiveProgram(programId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
