---
title: "onCategoryUpdated"
description: "Triggered when a category is updated."
icon: "bolt"
---

Triggered when a category is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.categories.v2.category_updated` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings Services and Settings`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onCategoryUpdated
  import { categoriesV2 } from '@wix/bookings';

  categoriesV2.onCategoryUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categoriesV2 } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        categoriesV2,
      },
  });

  wixClient.categoriesV2.onCategoryUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
