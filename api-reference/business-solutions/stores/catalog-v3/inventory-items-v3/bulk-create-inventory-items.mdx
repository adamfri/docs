---
title: "Bulk Create Inventory Items"
description: "Creates multiple inventory items."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple inventory items.

    The combination of `variantId` and `locationId` must be unique for each inventory item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INVENTORY_ITEM_WRITE">Inventory write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/bulk/inventory-items/create
    ```

    ## Body Params

    <ParamField body="inventoryItems" type="InventoryItem[]" required>

      Inventory items to create.

      Constraints: `minItems 1`, `maxItems 1000`.

      See the [Inventory Item Object](/api-reference/business-solutions/stores/catalog-v3/inventory-items-v3/inventory-item-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the full inventory item entities in the response.

      Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkInventoryItemResult[]">

      Inventory items created by bulk action.

      Constraints: `minItems 1`, `maxItems 1000`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata for inventory item.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="InventoryItem">

          Full inventory item entity.

          Returned only if `returnEntity: true` is passed in the request.

          See the [Inventory Item Object](/api-reference/business-solutions/stores/catalog-v3/inventory-items-v3/inventory-item-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PRE_ORDER_VALIDATION` | Preorder enabled for a digital product or subscription product. Preorder isn't supported for these product types. |
    | `400` | `INVALID_ARGUMENT` | `REQUESTED_QUANTITY_MUST_BE_NON_NEGATIVE` | Tracked inventory creation attempted with negative `quantity`. Quantity must be non-negative. |
    | `400` | `INVALID_ARGUMENT` | `PREORDER_LIMIT_NOT_SUPPORTED_FOR_UNTRACKED_INVENTORY` | Preorder limit set for untracked inventory. Preorder limits are only supported for tracked inventory. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Inventory Item Created | `wix.stores.catalog.v3.inventory_item_created` | Triggered when an inventory item is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Bulk creates a new Inventories items associated with product, variant, and location. to product, variant and location.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/stores/v3/bulk/inventory-items/create' \
        -H 'Content-type: application/json' \
        -H 'Authorization:  <AUTH>' \
        -d '{
          "inventoryItems": [
            {
              "variantId": "590cef15-c81d-4ed7-970c-1ff879946306",
              "locationId": "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
              "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
              "quantity": 105
            },
            {
              "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
              "locationId": "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
              "variantId": "f27ee649-ba6b-493d-b758-75816208cf2e",
              "quantity": 110
            }
          ],
          "returnEntity": true
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "a83212bc-1c1e-4645-b391-a9fed7f930f6",
              "originalIndex": 0,
              "success": true,
              "error": null
            },
            "item": {
              "id": "a83212bc-1c1e-4645-b391-a9fed7f930f6",
              "revision": "1",
              "createdDate": "2024-06-27T09:33:47.432Z",
              "updatedDate": "2024-06-27T09:33:47.432Z",
              "variantId": "590cef15-c81d-4ed7-970c-1ff879946306",
              "locationId": "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
              "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
              "quantity": 105,
              "trackQuantity": true,
              "availability": {
                "status": "PURCHASE",
                "quantity": 105
              },
              "availabilityStatus": "IN_STOCK",
              "preorderInfo": {
                "enabled": false,
                "counter": null,
                "limit": null,
                "message": null,
                "quantity": null
              },
              "product": null,
              "extendedFields": null
            }
          },
          {
            "itemMetadata": {
              "id": "fb913045-b421-4d09-804f-352fbe784465",
              "originalIndex": 1,
              "success": true,
              "error": null
            },
            "item": {
              "id": "fb913045-b421-4d09-804f-352fbe784465",
              "revision": "1",
              "createdDate": "2024-06-27T09:33:47.432Z",
              "updatedDate": "2024-06-27T09:33:47.432Z",
              "variantId": "f27ee649-ba6b-493d-b758-75816208cf2e",
              "locationId": "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
              "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
              "quantity": 110,
              "trackQuantity": true,
              "availability": {
                "status": "PURCHASE",
                "quantity": 110
              },
              "availabilityStatus": "IN_STOCK",
              "preorderInfo": {
                "enabled": false,
                "counter": null,
                "limit": null,
                "message": null,
                "quantity": null
              },
              "product": null,
              "extendedFields": null
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple inventory items.

    The combination of `variantId` and `locationId` must be unique for each inventory item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INVENTORY_ITEM_WRITE">Inventory write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { inventoryItemsV3 } from '@wix/stores';

    inventoryItemsV3.bulkCreateInventoryItems(inventoryItems, options): Promise
    ```

    ## Method Parameters

    <ParamField body="inventoryItems" type="InventoryItem[]" required>

      Inventory items to create.

      Constraints: `minItems 1`, `maxItems 1000`.

      See the [Inventory Item Object](/api-reference/business-solutions/stores/catalog-v3/inventory-items-v3/inventory-item-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateInventoryItemsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return the full inventory item entities in the response.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkInventoryItemResult[]">

      Inventory items created by bulk action.

      Constraints: `minItems 1`, `maxItems 1000`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata for inventory item.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="InventoryItem">

          Full inventory item entity.

          Returned only if `returnEntity: true` is passed in the request.

          See the [Inventory Item Object](/api-reference/business-solutions/stores/catalog-v3/inventory-items-v3/inventory-item-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PRE_ORDER_VALIDATION` | Preorder enabled for a digital product or subscription product. Preorder isn't supported for these product types. |
    | `400` | `INVALID_ARGUMENT` | `REQUESTED_QUANTITY_MUST_BE_NON_NEGATIVE` | Tracked inventory creation attempted with negative `quantity`. Quantity must be non-negative. |
    | `400` | `INVALID_ARGUMENT` | `PREORDER_LIMIT_NOT_SUPPORTED_FOR_UNTRACKED_INVENTORY` | Preorder limit set for untracked inventory. Preorder limits are only supported for tracked inventory. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Inventory Item Created | `wix.stores.catalog.v3.inventory_item_created` | Triggered when an inventory item is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-inventory-items" id="ex-sdk-bulk-create-inventory-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-inventory-items" id="ex-sdk-bulk-create-inventory-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-inventory-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-inventory-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk create inventory items</span><span className="wix-picker-desc">Create multiple inventory items associated with products, variants, and locations</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateInventoryItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-inventory-items-0"><span className="wix-picker-name">Bulk create inventory items</span><span className="wix-picker-desc">Create multiple inventory items associated with products, variants, and locations</span></label>
    <label htmlFor="ex-sdk-bulk-create-inventory-items-1"><span className="wix-picker-name">bulkCreateInventoryItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk create inventory items
    import { inventoryItemsV3 } from "@wix/stores";

    const inventoryItems = [
      {
        variantId: "590cef15-c81d-4ed7-970c-1ff879946306",
        locationId: "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
        productId: "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
        quantity: 105
      },
      {
        productId: "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
        locationId: "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
        variantId: "f27ee649-ba6b-493d-b758-75816208cf2e",
        quantity: 110
      }
    ];

    const options = {
      returnEntity: true
    };

    async function bulkCreateInventoryItems() {
      const response = await inventoryItemsV3.bulkCreateInventoryItems(inventoryItems, options);
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "id": "a83212bc-1c1e-4645-b391-a9fed7f930f6",
     *         "originalIndex": 0,
     *         "success": true
     *       },
     *       "item": {
     *         "_id": "a83212bc-1c1e-4645-b391-a9fed7f930f6",
     *         "variantId": "590cef15-c81d-4ed7-970c-1ff879946306",
     *         "locationId": "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
     *         "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
     *         "quantity": 105,
     *         "trackQuantity": true,
     *         "availabilityStatus": "IN_STOCK"
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 2,
     *     "totalFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateInventoryItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { inventoryItemsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { inventoryItemsV3 },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateInventoryItems(inventoryItems,options) {
      const response = await myWixClient.inventoryItemsV3.bulkCreateInventoryItems(inventoryItems,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
