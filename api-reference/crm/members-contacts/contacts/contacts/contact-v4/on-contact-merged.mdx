---
title: "onContactMerged"
description: "Triggered when one or more source contacts are merged into a target contact."
icon: "bolt"
---

Triggered when one or more source contacts are merged into a target contact.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.contacts.v4.contact_merged` |
| Package | `@wix/crm` |
| Permissions | `Manage Stores`, `Manage Events`, `Manage Challenges`, `Read Contacts`, `Read Members and Contacts - all read permissions`, `Manage Restaurants - all permissions`, `Manage Contacts`, `Set Up Automations`, `Manage Members and Contacts - all permissions` |

## Examples

<CodeGroup>

  ```javascript onContactMerged
  import { contacts } from '@wix/crm';

  contacts.onContactMerged((event) => {
      // handle your event here
  });
  ```

  ```javascript onContactMerged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { contacts } from '@wix/crm';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        contacts,
      },
  });

  wixClient.contacts.onContactMerged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
