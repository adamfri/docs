---
title: "onAppPermissionDeleted"
description: "Triggered when a permission is deleted from an app."
icon: "bolt"
---

Triggered when a permission is deleted from an app.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.devcenter.apps.v1.app_permission_deleted` |
| Package | `@wix/app-management` |

## Examples

<CodeGroup>

  ```javascript onAppPermissionDeleted
  import { appPermissions } from '@wix/app-management';

  appPermissions.onAppPermissionDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onAppPermissionDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { appPermissions } from '@wix/app-management';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        appPermissions,
      },
  });

  wixClient.appPermissions.onAppPermissionDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
