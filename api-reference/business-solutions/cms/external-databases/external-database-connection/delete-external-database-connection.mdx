---
title: "Delete External Database Connection"
description: "Deletes an external database connection."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an external database connection.

    <Note>

    Once an external database connection is deleted, it can't be restored. To reconnect the database, create a new external database connection.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.EXTERNAL-DATABASE-CONNECTIONS-MANAGE">Manage External Database Connections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/data/v1/external-database-connections/{name=**}
    ```

    ## Query Params

    <ParamField query="name" type="string" required>

      Name of the external database connection to delete.

      Constraints: `maxLength 255`.

    </ParamField>

    ## Response Object

    Returns `DeleteExternalDatabaseConnectionResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/wix-data/v1/external-database-connections/MyExternalDatabase' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an external database connection.

    <Note>

    Once an external database connection is deleted, it can't be restored. To reconnect the database, create a new external database connection.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.EXTERNAL-DATABASE-CONNECTIONS-MANAGE">Manage External Database Connections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { externalDatabaseConnections } from '@wix/data';

    externalDatabaseConnections.deleteExternalDatabaseConnection(name): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="name" type="string" required>

      Name of the external database connection to delete.

      Constraints: `maxLength 255`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | External Database Connection Deleted | `wix.data.v1.external_database_connection_deleted` | Triggered when an external database connection is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-external-database-connection" id="ex-sdk-delete-external-database-connection-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-external-database-connection" id="ex-sdk-delete-external-database-connection-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-external-database-connection" id="ex-sdk-delete-external-database-connection-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-external-database-connection-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-external-database-connection-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteExternalDatabaseConnection</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteExternalDatabaseConnection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteExternalDatabaseConnection (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-external-database-connection-0"><span className="wix-picker-name">deleteExternalDatabaseConnection</span></label>
    <label htmlFor="ex-sdk-delete-external-database-connection-1"><span className="wix-picker-name">deleteExternalDatabaseConnection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-external-database-connection-2"><span className="wix-picker-name">deleteExternalDatabaseConnection (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteExternalDatabaseConnection
    import { externalDatabaseConnections } from '@wix/data';

    async function deleteExternalDatabaseConnection(name) {
      const response = await externalDatabaseConnections.deleteExternalDatabaseConnection(name);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteExternalDatabaseConnection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { externalDatabaseConnections } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { externalDatabaseConnections },
      // Include the auth strategy and host as relevant
    });

    async function deleteExternalDatabaseConnection(name) {
      const response = await myWixClient.externalDatabaseConnections.deleteExternalDatabaseConnection(name);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteExternalDatabaseConnection (with elevated permissions)
    import { externalDatabaseConnections } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myDeleteExternalDatabaseConnectionMethod(name) {
      const elevatedDeleteExternalDatabaseConnection = auth.elevate(externalDatabaseConnections.deleteExternalDatabaseConnection);
      const response = await elevatedDeleteExternalDatabaseConnection(name);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
