---
title: "onOperationGroupDeleted"
description: "The onOperationGroupDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.operation_group_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onOperationGroupDeleted
  import { operationGroups } from '@wix/restaurants';

  operationGroups.onOperationGroupDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onOperationGroupDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { operationGroups } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        operationGroups,
      },
  });

  wixClient.operationGroups.onOperationGroupDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
