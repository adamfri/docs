---
title: "Time Slot Object"
description: "Time Slots Submodule Docs"
icon: "cube"
---

Time Slots Submodule Docs

<Tabs>

  <Tab title="REST">

    <ResponseField name="startDate" type="datetime">

      Start date and time of this time slot.

    </ResponseField>

    <ResponseField name="duration" type="integer">

      Duration in minutes of this time slot.

    </ResponseField>

    <ResponseField name="status" type="string">

      Availability status of this time slot.

      **Supported values:**

      - `AVAILABLE` — The restaurant can accommodate a party of the given size in this time slot.
      - `UNAVAILABLE` — The restaurant can't accommodate a party of the given size in this time slot.
      - `NON_WORKING_HOURS` — The restaurant is not open during this time slot. Time slots retrieved by Get Scheduled Time Slots never have this status.

    </ResponseField>

    <ResponseField name="manualApproval" type="boolean">

      Whether manual approval is required to make a reservation in this time slot.

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "timeSlot": {
          "startDate": "2023-06-08T13:00:00Z",
          "duration": 90,
          "status": "AVAILABLE",
          "tableCombinations": [],
          "manualApproval": true
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="startDate" type="datetime">

      Start date and time of this time slot.

    </ResponseField>

    <ResponseField name="duration" type="integer">

      Duration in minutes of this time slot.

    </ResponseField>

    <ResponseField name="status" type="string">

      Availability status of this time slot.

      **Supported values:**

      - `AVAILABLE` — The restaurant can accommodate a party of the given size in this time slot.
      - `UNAVAILABLE` — The restaurant can't accommodate a party of the given size in this time slot.
      - `NON_WORKING_HOURS` — The restaurant is not open during this time slot. Time slots retrieved by Get Scheduled Time Slots never have this status.

    </ResponseField>

    <ResponseField name="manualApproval" type="boolean">

      Whether manual approval is required to make a reservation in this time slot.

    </ResponseField>

  </Tab>

</Tabs>
