---
title: "onDonationCampaignUpdated"
description: "Triggered when a donation campaign is updated."
icon: "bolt"
---

Triggered when a donation campaign is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.donations.v2.donation_campaign_updated` |
| Package | `@wix/donations` |
| Permissions | `Manage Donation Campaigns` |

## Examples

<CodeGroup>

  ```javascript onDonationCampaignUpdated
  import { donationCampaigns } from '@wix/donations';

  donationCampaigns.onDonationCampaignUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onDonationCampaignUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { donationCampaigns } from '@wix/donations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        donationCampaigns,
      },
  });

  wixClient.donationCampaigns.onDonationCampaignUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
