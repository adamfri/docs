---
title: "onLikeDeleted"
description: "Triggers when a like is deleted."
icon: "bolt"
---

Triggers when a like is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v1.like_deleted` |
| Package | `@wix/blog` |
| Permissions | `Read Blog`, `Manage Blog`, `Read Draft Blog Posts` |

## Examples

<CodeGroup>

  ```javascript onLikeDeleted
  import { likes } from '@wix/blog';

  likes.onLikeDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onLikeDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { likes } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        likes,
      },
  });

  wixClient.likes.onLikeDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
