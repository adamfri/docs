---
title: "Set Default Invoice Preset"
description: "Sets the default invoice preset."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Sets the default invoice preset.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/invoices/v4/invoice-presets/{invoicePresetId}/set-default
    ```

    ## Path Params

    <ParamField path="invoicePresetId" type="string" required>

      ID of the invoice preset to set as default.

    </ParamField>

    ## Response Object

    <ResponseField name="invoicePreset" type="InvoicePreset">

      Updated default invoice preset.

      See the [Invoice Preset Object](/api-reference/business-management/get-paid/invoices/invoice-presets/invoice-preset-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Sets the default invoice preset

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/invoices/v4/invoice-presets/13bbf8c7-5a6c-4c91-b4c5-7c8b9d0e1f2a/set-default' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "invoicePreset": {
          "id": "13bbf8c7-5a6c-4c91-b4c5-7c8b9d0e1f2a",
          "revision": "2",
          "createdDate": "2024-01-15T10:30:00.000Z",
          "updatedDate": "2024-01-15T10:30:00.000Z",
          "name": "Updated Professional Template",
          "displaySettings": {
            "businessInfo": {
              "displayEmail": true,
              "displayPhoneNumber": true,
              "displayAddress": true,
              "displayCompanyId": false
            },
            "customerInfo": {
              "displayEmail": true,
              "displayPhoneNumber": false,
              "displayShippingAddress": true,
              "displayBillingAddress": true,
              "displayVatId": true,
              "displayCompanyName": true
            },
            "itemsInfo": {
              "displayDescription": true,
              "displayTax": true
            },
            "totalsInfo": {
              "displayTaxBreakdown": true,
              "displayItemSubtotalPerTaxBreakdown": false
            },
            "paymentsInfo": {
              "displayPaymentsInfo": true
            }
          },
          "displayValues": {
            "titleOverride": "INVOICE"
          },
          "defaultDaysUntilDue": 14,
          "defaultPreset": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Sets the default invoice preset.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { invoicePresets } from '@wix/get-paid';

    invoicePresets.setDefaultInvoicePreset(invoicePresetId): Promise
    ```

    ## Method Parameters

    <ParamField body="invoicePresetId" type="string" required>

      ID of the invoice preset to set as default.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="invoicePreset" type="InvoicePreset">

      Updated default invoice preset.

      See the [Invoice Preset Object](/api-reference/business-management/get-paid/invoices/invoice-presets/invoice-preset-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-set-default-invoice-preset" id="ex-sdk-set-default-invoice-preset-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-set-default-invoice-preset" id="ex-sdk-set-default-invoice-preset-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-set-default-invoice-preset" id="ex-sdk-set-default-invoice-preset-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-set-default-invoice-preset-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-set-default-invoice-preset-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">setDefaultInvoicePreset</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setDefaultInvoicePreset (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setDefaultInvoicePreset (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-set-default-invoice-preset-0"><span className="wix-picker-name">setDefaultInvoicePreset</span></label>
    <label htmlFor="ex-sdk-set-default-invoice-preset-1"><span className="wix-picker-name">setDefaultInvoicePreset (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-set-default-invoice-preset-2"><span className="wix-picker-name">setDefaultInvoicePreset (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript setDefaultInvoicePreset
    import { invoicePresets } from '@wix/get-paid';

    async function setDefaultInvoicePreset(invoicePresetId) {
      const response = await invoicePresets.setDefaultInvoicePreset(invoicePresetId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setDefaultInvoicePreset (self-hosted)
    import { createClient } from '@wix/sdk';
    import { invoicePresets } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { invoicePresets },
      // Include the auth strategy and host as relevant
    });

    async function setDefaultInvoicePreset(invoicePresetId) {
      const response = await myWixClient.invoicePresets.setDefaultInvoicePreset(invoicePresetId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setDefaultInvoicePreset (with elevated permissions)
    import { invoicePresets } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function mySetDefaultInvoicePresetMethod(invoicePresetId) {
      const elevatedSetDefaultInvoicePreset = auth.elevate(invoicePresets.setDefaultInvoicePreset);
      const response = await elevatedSetDefaultInvoicePreset(invoicePresetId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
