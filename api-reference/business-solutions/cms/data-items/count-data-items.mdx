---
title: "Count Data Items"
description: "Counts the number of items in a data collection that match the provided filtering preferences."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Counts the number of items in a data collection that match the provided filtering preferences.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.READ">Read Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data/v2/items/count
    ```

    ## Body Params

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection for which to count query results.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="filter" type="object">

      Filter object in the following format:

      `"filter" : {`
      "fieldName1": "value1",
      "fieldName2":&#123;"$operator":"value2"&#125;
      &#125;`.`

      Examples of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`.

      **Note:** The values you provide for each field must adhere to that field's type. For example, when filtering by a field whose type is Date and Time, use an object in the following format: `"someDateAndTimeFieldKey": { "$date": "YYYY-MM-DDTHH:mm:ss.sssZ"}`. Learn more about [data types in Wix Data](https://dev.wix.com/docs/rest/business-solutions/cms/data-items/data-types-in-wix-data).

    </ParamField>

    <ParamField body="consistentRead" type="boolean">

      Whether to retrieve data from the primary database instance.
      This decreases performance but ensures data retrieved is up to date even immediately after an update.
      Learn more about [Wix Data and eventual consistency](https://dev.wix.com/api/rest/wix-data/wix-data/eventual-consistency).

      Default: `false`

    </ParamField>

    <ParamField body="language" type="string">

      Language to translate result text into, in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
      If provided, the result text is returned in the specified language.
      **Note:** Translation for the specified language must be enabled for the collection in [Wix Multilingual](https://www.wix.com/app-market/wix-multilingual).

      If not provided, result text is not translated.

    </ParamField>

    <ParamField body="appOptions" type="object">

      Additional parameters specific to the [Wix app collection](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections) you are querying.

      When querying the Wix Stores [Products collection](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-e-commerce-stores/wix-stores-products-collection-fields), pass the following optional parameters:
      - `includeHiddenProducts`: Whether to include hidden products in the response. Default: `false`.
      - `includeVariants`: Whether to include product variants in the query. Default: `false`.

    </ParamField>

    <ParamField body="publishPluginOptions" type="PublishPluginOptions">

      Options for the Publish plugin.
      This plugin allows items in a [data collection](https://dev.wix.com/docs/rest/business-solutions/cms/data-collections/data-collection-object) to be marked as draft or published. Published items are visible to site visitors, while draft items are not.

      <Expandable title="publishPluginOptions properties">

        <ParamField body="includeDraftItems" type="boolean">

          Whether to include draft items.

          When `true`, both published and draft items are affected. Default: `false`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="totalCount" type="integer">

      Number of items matching the query.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items/count' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "cities",
          "filter": {
              "state": "California"
          }
      }'
      ```

      ```json Response
      {
          "totalCount": 4
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Counts the number of items in a data collection that match the provided filtering preferences.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.READ">Read Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/data';

    items.count(dataCollectionId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection for which to count query results.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="options" type="WixDataCountOptions">

      Options to use when processing this operation.

      <Expandable title="options properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:

          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;`.`

          Examples of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`.

          **Note:** The values you provide for each field must adhere to that field's type. For example, when filtering by a field whose type is Date and Time, use an object in the following format: `"someDateAndTimeFieldKey": { "$date": "YYYY-MM-DDTHH:mm:ss.sssZ"}`. Learn more about [data types in Wix Data](https://dev.wix.com/docs/rest/business-solutions/cms/data-items/data-types-in-wix-data).

        </ParamField>

        <ParamField body="consistentRead" type="boolean">

          Whether to retrieve data from the primary database instance.
          This decreases performance but ensures data retrieved is up to date even immediately after an update.
          Learn more about [Wix Data and eventual consistency](https://dev.wix.com/api/rest/wix-data/wix-data/eventual-consistency).

          Default: `false`

        </ParamField>

        <ParamField body="language" type="string">

          Language to translate result text into, in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
          If provided, the result text is returned in the specified language.
          **Note:** Translation for the specified language must be enabled for the collection in [Wix Multilingual](https://www.wix.com/app-market/wix-multilingual).

          If not provided, result text is not translated.

        </ParamField>

        <ParamField body="suppressHooks" type="boolean">

          Whether to suppress data hooks.
          When `true`, data hooks typically triggered by this endpoint don't run.

          **Note:** This option can only be used in code that runs in [the Wix site backend](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/backend-code/about-the-site-backend).

          Default: `false`

        </ParamField>

        <ParamField body="appOptions" type="object">

          Additional parameters specific to the [Wix app collection](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections) you are querying.

          When querying the Wix Stores [Products collection](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-e-commerce-stores/wix-stores-products-collection-fields), pass the following optional parameters:
          - `includeHiddenProducts`: Whether to include hidden products in the response. Default: `false`.
          - `includeVariants`: Whether to include product variants in the query. Default: `false`.

        </ParamField>

        <ParamField body="showDrafts" type="boolean">

          When `true`, operations include draft items. Read operations include draft items in their response, and write operations modify draft items.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `number`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-count-data-items" id="ex-sdk-count-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-count-data-items" id="ex-sdk-count-data-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-count-data-items" id="ex-sdk-count-data-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-count-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-count-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">count</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">count (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">count (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-count-data-items-0"><span className="wix-picker-name">count</span></label>
    <label htmlFor="ex-sdk-count-data-items-1"><span className="wix-picker-name">count (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-count-data-items-2"><span className="wix-picker-name">count (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript count
    import { items } from '@wix/data';

    async function count(dataCollectionId,options) {
      const response = await items.count(dataCollectionId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript count (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function count(dataCollectionId,options) {
      const response = await myWixClient.items.count(dataCollectionId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript count (with elevated permissions)
    import { items } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myCountMethod(dataCollectionId,options) {
      const elevatedCount = auth.elevate(items.count);
      const response = await elevatedCount(dataCollectionId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
