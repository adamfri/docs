---
title: "Update Draft Post"
description: "Updates a draft post."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a draft post.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/blog/v3/draft-posts/{draftPost.id}
    ```

    ## Path Params

    <ParamField path="draftPost.id" type="string" required>

      Draft post ID.

    </ParamField>

    ## Body Params

    <ParamField body="draftPost" type="DraftPost" required>

      Draft post to update.

      See the [Draft Object](/api-reference/business-solutions/blog/draft-posts/draft-object) for this object's fields.

    </ParamField>

    <ParamField body="action" type="string">

      Action to perform on the post.

      Default: `UPDATE`

      **Supported values:**

      - `UPDATE` — Update the draft post.
      - `UPDATE_PUBLISH` — Update and publish a draft post.
      - `UPDATE_SCHEDULE` — Update the draft post and schedule a publish date.
      - `UPDATE_REVERT_TO_DRAFT` — Update the post and revert it to draft.
      - `UPDATE_CANCEL_SCHEDULE` — Update the draft post and cancel the scheduled publish date.
      - `UPDATE_REJECT` — Update and reject draft post.
      - `UPDATE_PUBLICATION` — Update a post that's already been published. This creates and updates a draft version of the post. The original post is still published.

    </ParamField>

    <ParamField body="scheduledPublishDate" type="datetime">

      Post publish schedule date if `action` is set to `UPDATE_SCHEDULE`.

    </ParamField>

    <ParamField body="fieldsets" type="string[]">

      List of additional draft post fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
      the response in addition to the draft post's base fields. Base fields don’t include any of the supported fieldset values. By default
      only the draft post's base fields are returned.

      Constraints: `maxItems 10`.

      **Supported values:**

      - `URL` — Includes draft post preview URL.
      - `CONTENT` — Includes content field.
      - `RICH_CONTENT` — Includes rich content field.
      - `GENERATED_EXCERPT` — If the user has not set excerpt, returns the one autogenerated from content.

    </ParamField>

    ## Response Object

    <ResponseField name="draftPost" type="DraftPost">

      Updated draft post info.

      See the [Draft Object](/api-reference/business-solutions/blog/draft-posts/draft-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Draft Post Updated | `wix.blog.v3.draft_updated` | Triggered when a draft post is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X PATCH \
        'https://www.wixapis.com/blog/v3/draft-posts/448d1238-0072-4458-a280-bf81c2dd8af1' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>'
        -d '{
              "draftPost": {
                "title": "Places to visit in Europe"
              }
            }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "draftPost": {
          "id": "448d1238-0072-4458-a280-bf81c2dd8af1",
          "title": "Places to visit in Europe",
          "featured": false,
          "categoryIds": [],
          "memberId": "2d24cb8a-adcc-466a-ab59-fa74e0889a39",
          "hashtags": [],
          "commentingEnabled": true,
          "minutesToRead": 0,
          "tagIds": [],
          "relatedPostIds": [],
          "pricingPlanIds": [],
          "language": "en",
          "status": "UNPUBLISHED",
          "hasUnpublishedChanges": true,
          "editedDate": "2022-03-14T11:34:55.121Z",
          "seoData": {
            "tags": []
          },
          "createdDate": "2022-03-14T11:34:55.121Z"
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a draft post.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { draftPosts } from '@wix/blog';

    draftPosts.updateDraftPost(_id, draftPost, options): Promise<UpdateDraftPostResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Draft post ID.

      **Read-only.** Constraints: `maxLength 38`.

    </ParamField>

    <ParamField body="draftPost" type="UpdateDraftPost" required>

      Draft Post info.

      <Expandable title="draftPost properties">

        <ParamField body="_id" type="string">

          Draft post ID.

          **Read-only.** Constraints: `maxLength 38`.

        </ParamField>

        <ParamField body="title" type="string">

          Draft post title.

          Constraints: `maxLength 200`.

        </ParamField>

        <ParamField body="excerpt" type="string">

          Draft post excerpt.

          If no excerpt has been manually set, an excerpt is automatically generated from the post's text.
          This can be retrieved using the `GENERATED_EXCERPT` fieldset.

        </ParamField>

        <ParamField body="featured" type="boolean">

          Whether the draft post is marked as featured.

        </ParamField>

        <ParamField body="categoryIds" type="string[]">

          Category IDs of the draft post.

          Constraints: `maxLength 38`, `maxItems 10`.

        </ParamField>

        <ParamField body="memberId" type="string">

          Draft post owner's member ID.

        </ParamField>

        <ParamField body="hashtags" type="string[]">

          Hashtags in the post.

          Constraints: `maxLength 100`, `maxItems 100`.

        </ParamField>

        <ParamField body="commentingEnabled" type="boolean">

          Whether commenting on the draft post is enabled.

        </ParamField>

        <ParamField body="minutesToRead" type="integer">

          Estimated reading time of the draft post (calculated automatically).

          **Read-only.**

        </ParamField>

        <ParamField body="heroImage" type="string">

          Image placed at the top of the blog page.

        </ParamField>

        <ParamField body="tagIds" type="string[]">

          Tag IDs the draft post is tagged with.

          Constraints: `maxLength 38`, `maxItems 30`.

        </ParamField>

        <ParamField body="relatedPostIds" type="string[]">

          IDs of posts related to this draft post.

          Constraints: `maxLength 38`, `maxItems 3`.

        </ParamField>

        <ParamField body="pricingPlanIds" type="string[]">

          Pricing plan IDs. Only relevant if a post is assigned to a specific pricing plan. See the Pricing Plans API for more info.

          Constraints: `format GUID`, `maxItems 100`.

        </ParamField>

        <ParamField body="translationId" type="string">

          ID of the draft post's translations.

          All translations of a single post share the same `translationId`.
          Available only if the [Multilingual](https://support.wix.com/en/article/wix-multilingual-an-overview) app is installed.

        </ParamField>

        <ParamField body="language" type="string">

          Language the draft post is written in.

          2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.

        </ParamField>

        <ParamField body="richContent" type="RichContent">

          Draft Post rich content.

          &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="image.gallery.video.audio.divider.linkButton.html.giphy.codeBlock.file.hashtag.lineSpacing.indent.link.textColor.textHighlight.heading.verticalEmbed.table.collapsibleList.poll" exampleid="72c23a25-524f-4f70-a260-4a2777b6f5d5">
          &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
          &lt;/widget>

          <Expandable title="richContent properties">

            <ParamField body="nodes" type="Node[]">

              Node objects representing a rich content document.

              _Nested structure omitted for brevity — see the `Node[]` object._

            </ParamField>

            <ParamField body="metadata" type="Metadata">

              Object metadata.

              _Nested structure omitted for brevity — see the `Metadata` object._

            </ParamField>

            <ParamField body="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              _Nested structure omitted for brevity — see the `DocumentStyle` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="status" type="string">

          Status of the draft post.

          **Read-only.**

          **Supported values:**

          - `PUBLISHED` — Status indicating the draft post is published.
          - `UNPUBLISHED` — Status indicating the draft post is unpublished.
          - `SCHEDULED` — Status indicating the draft post is scheduled for publication.
          - `DELETED` — Status indicating the draft post is deleted.
          - `IN_REVIEW` — Status indicating the draft post is in review.

        </ParamField>

        <ParamField body="moderationDetails" type="ModerationDetails">

          Details of the draft post in review. Only relevant to posts submitted by guest writers.

          <Expandable title="moderationDetails properties">

            <ParamField body="submittedBy" type="string">

              Member ID of the person submitting the draft post for review.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="submittedDate" type="datetime">

              Date the post was submitted for review.

            </ParamField>

            <ParamField body="status" type="string">

              Status indicating whether the submission was approved or rejected by the moderator.

              **Supported values:**

              - `APPROVED`
              - `REJECTED`

            </ParamField>

            <ParamField body="moderatedBy" type="string">

              Member ID of the person who approved or rejected the post.

            </ParamField>

            <ParamField body="moderationDate" type="datetime">

              Date the post was approved or rejected.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="hasUnpublishedChanges" type="boolean">

          Indicates if there are changes made to the draft post that have not yet been published.

          **Read-only.**

        </ParamField>

        <ParamField body="editedDate" type="datetime">

          Date the draft post was last edited.

          **Read-only.**

        </ParamField>

        <ParamField body="scheduledPublishDate" type="datetime">

          Date the draft post is scheduled to be published.

          **Read-only.**

        </ParamField>

        <ParamField body="firstPublishedDate" type="datetime">

          Date the post was first published.

        </ParamField>

        <ParamField body="seoData" type="SeoSchema">

          SEO data.

          <Expandable title="seoData properties">

            <ParamField body="tags" type="Tag[]">

              SEO tag information.

              _Nested structure omitted for brevity — see the `Tag[]` object._

            </ParamField>

            <ParamField body="settings" type="Settings">

              SEO general settings.

              _Nested structure omitted for brevity — see the `Settings` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="url" type="string">

          Draft post URL preview. What the URL will look like once the post is published.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date the draft post was first created.

          **Read-only.**

        </ParamField>

        <ParamField body="seoSlug" type="string">

          SEO slug.

        </ParamField>

        <ParamField body="media" type="Media">

          Post cover media.

          <Expandable title="media properties">

            <ParamField body="displayed" type="boolean">

              Whether cover media is displayed.

            </ParamField>

            <ParamField body="custom" type="boolean">

              Whether custom cover media has been specified. If `false`, the first media item in the post's content serves as cover media.

            </ParamField>

            <ParamField body="altText" type="string">

              Media alternative text.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="media" type="media">

              media

              _Nested structure omitted for brevity — see the `media` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="previewTextParagraph" type="integer">

          Number of paragraphs to display in a paid content preview for non-paying users.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateDraftPostOptions">

      Options for updating a draft post.

      <Expandable title="options properties">

        <ParamField body="action" type="string">

          Action to perform on the post.

          Default: `UPDATE`

          **Supported values:**

          - `UPDATE` — Update the draft post.
          - `UPDATE_PUBLISH` — Update and publish a draft post.
          - `UPDATE_SCHEDULE` — Update the draft post and schedule a publish date.
          - `UPDATE_REVERT_TO_DRAFT` — Update the post and revert it to draft.
          - `UPDATE_CANCEL_SCHEDULE` — Update the draft post and cancel the scheduled publish date.
          - `UPDATE_REJECT` — Update and reject draft post.
          - `UPDATE_PUBLICATION` — Update a post that's already been published. This creates and updates a draft version of the post. The original post is still published.

        </ParamField>

        <ParamField body="scheduledPublishDate" type="datetime">

          Post publish schedule date if `action` is set to `UPDATE_SCHEDULE`.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          List of additional draft post fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
          the response in addition to the draft post's base fields. Base fields don’t include any of the supported fieldset values. By default
          only the draft post's base fields are returned.

          Constraints: `maxItems 10`.

          **Supported values:**

          - `URL` — Includes draft post preview URL.
          - `CONTENT` — Includes content field.
          - `RICH_CONTENT` — Includes rich content field.
          - `GENERATED_EXCERPT` — If the user has not set excerpt, returns the one autogenerated from content.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateDraftPostResponse>`.

    <ResponseField name="draftPost" type="DraftPost">

      Updated draft post info.

      See the [Draft Object](/api-reference/business-solutions/blog/draft-posts/draft-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Draft Updated | `wix.blog.v3.draft_updated` | Triggered when a draft post is updated. |
    | Draft Draft Post Owner Changed | `wix.blog.v3.draft_draft_post_owner_changed` | Triggered when a draft post changes owner. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-draft-post" id="ex-sdk-update-draft-post-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-draft-post" id="ex-sdk-update-draft-post-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-draft-post" id="ex-sdk-update-draft-post-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-draft-post-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-draft-post-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a draft post</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update a draft post using options</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateDraftPost (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-draft-post-0"><span className="wix-picker-name">Update a draft post</span></label>
    <label htmlFor="ex-sdk-update-draft-post-1"><span className="wix-picker-name">Update a draft post using options</span></label>
    <label htmlFor="ex-sdk-update-draft-post-2"><span className="wix-picker-name">updateDraftPost (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a draft post
    import { draftPosts } from '@wix/blog';

    /* Sample id value: "c4696594-b02e-4d24-afc1-97da073783b4"
     *  
     * Sample draftPost value:
     * {
     *   "categoryIds": [],
     *   "commentingEnabled": true,
     *   "editingSessionId": "5e9c24a9-6318-4ca7-9df5-0d9a6a10d3ee",
     *   "excerpt": "When we start, we start from the beginning.",
     *   "featured": false,
     *   "hashtags": [],
     *   "language": "en",
     *   "media": {
     *       "custom": false,
     *       "displayed": true
     *   },
     *   "memberId": "c00e8a5c-322b-4e77-8813-002e3ea7e811",
     *   "paidContentParagraph": 4,
     *   "pricingPlanIds": [],
     *   "relatedPostIds": [
     *       "c4696594-b02e-4d24-afc1-97da073783b4",
     *       "66935097-31c5-4fab-9693-aa1b53aeea77"
     *   ],
     *   "seoData": {
     *       "tags": []
     *   },
     *   "tagIds": [],
     *   "title": "How to make friends and influence people.",
     *   "translations": []
     * }
     */

    export async function myUpdateDraftPostFunction(id, draftPost) {
      try {
        const updatedDraftPost = await draftPosts.updateDraftPost(id, draftPost);
        console.log('Successfully updated the following draft post":', updatedDraftPost);
        return updatedDraftPost;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "categoryIds": [],
     *   "commentingEnabled": true,
     *   "editingSessionId": "5e9c24a9-6318-4ca7-9df5-0d9a6a10d3ee",
     *   "excerpt": "When we start, we start from the beginning.",
     *   "featured": false,
     *   "hashtags": [],
     *   "language": "en",
     *   "media": {
     *     "custom": false,
     *     "displayed": true
     *   },
     *   "memberId": "c00e8a5c-322b-4e77-8813-002e3ea7e811",
     *   "paidContentParagraph": 4,
     *   "pricingPlanIds": [],
     *   "relatedPostIds": [
     *     "c4696594-b02e-4d24-afc1-97da073783b4",
     *     "66935097-31c5-4fab-9693-aa1b53aeea77"
     *   ],
     *   "seoData": {
     *     "tags": []
     *   },
     *   "tagIds": [],
     *   "title": "How to make friends and influence people.",
     *   "translations": []
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update a draft post using options
    // This example updates the draft and publishes it
    import { draftPosts } from '@wix/blog';

    /* Sample id value = "c8d50953-b31c-435f-82ec-a53d8bff87b4"
     *
     * Sample draftPost value:
     * { 
     *   "categoryIds": [],
     *   "commentingEnabled": true,
     *   "excerpt": "If it wasn't for grit, we wouldn't succeed.",
     *   "featured": false,
     *   "hashtags": [
     *       "vacation",
     *       "dream",
     *       "summer",
     *   ],
     *   "language": "en",
     *   "media": {
     *       "custom": false,
     *       "displayed": true
     *   },
     *   "memberId": "c00e8a5c-322b-4e77-8813-002e3ea7e811",
     *   "paidContentParagraph": 4,
     *   "pricingPlanIds": [],
     *   "relatedPostIds": [],
     *   "seoData": {
     *       tags": []
     *   },
     *   "tagIds": [],
     *   "title": "Grit is the special sauce.",
     *   "translations": []
     * };
     *
     * Sample options value":
     *  {
     *   "action": "UPDATE_PUBLISH",
     *   "fieldsets": "GENERATED_EXCERPT",
     *   "scheduledPublishDate": new Date("2024-08-09T15:30:00Z")
     * }
     */

    export async function myUpdateDraftPostFunction(id, draftPost, options) {
      try {
        const updatedDraftPost = await draftPosts.updateDraftPost(id, draftPost, options);
        console.log('Successfully updated the following draft post":', updatedDraftPost);
        return updatedDraftPost;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "categoryIds": [],
     *   "commentingEnabled": true,
     *   "excerpt": "If it wasn't for grit, we wouldn't succeed.",
     *   "featured": false,
     *   "hashtags": [
     *     "vacation",
     *     "dream",
     *     "summer"
     *   ],
     *   "language": "en",
     *   "media": {
     *     "custom": false,
     *     "displayed": true
     *   },
     *   "memberId": "c00e8a5c-322b-4e77-8813-002e3ea7e811",
     *   "paidContentParagraph": 4,
     *   "pricingPlanIds": [],
     *   "relatedPostIds": [],
     *   "seoData": {
     *     "tags": []
     *   },
     *   "tagIds": [],
     *   "title": "Grit is the special sauce."
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateDraftPost (self-hosted)
    import { createClient } from '@wix/sdk';
    import { draftPosts } from '@wix/blog';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { draftPosts },
      // Include the auth strategy and host as relevant
    });

    async function updateDraftPost(_id,draftPost,options) {
      const response = await myWixClient.draftPosts.updateDraftPost(_id,draftPost,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
