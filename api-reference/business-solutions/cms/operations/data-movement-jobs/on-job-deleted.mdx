---
title: "onJobDeleted"
description: "Triggered when a job is terminated."
icon: "bolt"
---

Triggered when a job is terminated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.hub.v1.job_deleted` |
| Package | `@wix/data` |
| Permissions | `Data Mover Manage Jobs` |

## Examples

<CodeGroup>

  ```javascript onJobDeleted
  import { movementJobs } from '@wix/data';

  movementJobs.onJobDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onJobDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { movementJobs } from '@wix/data';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        movementJobs,
      },
  });

  wixClient.movementJobs.onJobDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
