---
title: "Mute Member"
description: "Mutes a member."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Mutes a member.

    Muted members can't engage with a community. For example, they can't leave comments, like posts or comments, or share content.

    You can mute members in [Wix Blog](https://dev.wix.com/docs/rest/business-solutions/blog/introduction) and [Wix Forum](https://dev.wix.com/docs/rest/business-solutions/forum/introduction).

    To unmute a member, call [Unmute Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/unmute-member).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/members/v1/members/{id}/mute
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the member to mute.

    </ParamField>

    ## Response Object

    <ResponseField name="member" type="Member">

      Muted member.

      See the [Member Object](/api-reference/crm/members-contacts/members/member-management/members/member-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
        https://www.wixapis.com/members/v1/members/e62e3011-55cf-4de3-a497-e097b52d86b7/mute \
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "member": {
              "id": "e62e3011-55cf-4de3-a497-e097b52d86b7",
              "loginEmail": "john@example.com",
              "status": "APPROVED",
              "contactId": "0fac596f-ff7c-4d2b-b826-63c60ee36ee8",
              "contact": {
                  "contactId": "0fac596f-ff7c-4d2b-b826-63c60ee36ee8",
                  "firstName": "John",
                  "lastName": "Doe",
                  "picture": "https://example.com",
                  "phones": [ "2075556300", "2075555217" ],
                  "emails": [ "john@example.com" ],
                  "addresses": [],
                  "customFields": {}
              },
              "profile": {
                  "nickname": "John Doe",
                  "slug": "johndoe",
                  "photo": {
                     "id": "a27d24_0dd318~mv2.jpg",
                     "url": "//static.wixstatic.com/media/a27d24_0dd318~mv2.jpg",
                     "height": 256,
                     "width": 256
                  },
                  "cover": {
                      "id": "",
                      "url": "https://example.com/myimage.jpg",
                      "height": 1080,
                      "width": 1920
                  }
              },
              "privacyStatus": "PUBLIC",
              "activityStatus": "MUTED",
              "createdDate": "2019-10-30T17:22:10.299Z",
              "updatedDate": "2019-11-13T20:14:49.458Z",
              "lastLoginDate": "2019-11-13T20:10:49.458Z"
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Mutes a member.

    Muted members can't engage with a community. For example, they can't leave comments, like posts or comments, or share content.

    You can mute members in [Wix Blog](https://dev.wix.com/docs/rest/business-solutions/blog/introduction) and [Wix Forum](https://dev.wix.com/docs/rest/business-solutions/forum/introduction).

    To unmute a member, call [Unmute Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/unmute-member).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { members } from '@wix/members';

    members.muteMember(_id): Promise<MuteMemberResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the member to mute.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<MuteMemberResponse>`.

    <ResponseField name="member" type="Member">

      Muted member.

      See the [Member Object](/api-reference/crm/members-contacts/members/member-management/members/member-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Member Member Muted | `wix.members.v1.member_member_muted` | Triggered when a member is muted. A muted member can't leave comments, like posts or comments, or share content. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-mute-member" id="ex-sdk-mute-member-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-mute-member" id="ex-sdk-mute-member-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-mute-member" id="ex-sdk-mute-member-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-mute-member-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-mute-member-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">muteMember</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">muteMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">muteMember (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-mute-member-0"><span className="wix-picker-name">muteMember</span></label>
    <label htmlFor="ex-sdk-mute-member-1"><span className="wix-picker-name">muteMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-mute-member-2"><span className="wix-picker-name">muteMember (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript muteMember
    import { members } from '@wix/members';

    async function muteMember(_id) {
      const response = await members.muteMember(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript muteMember (self-hosted)
    import { createClient } from '@wix/sdk';
    import { members } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { members },
      // Include the auth strategy and host as relevant
    });

    async function muteMember(_id) {
      const response = await myWixClient.members.muteMember(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript muteMember (with elevated permissions)
    import { members } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myMuteMemberMethod(_id) {
      const elevatedMuteMember = auth.elevate(members.muteMember);
      const response = await elevatedMuteMember(_id);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
