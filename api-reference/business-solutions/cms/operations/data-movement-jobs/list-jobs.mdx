---
title: "List Jobs"
description: "Lists data movement jobs for the current site."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Lists data movement jobs for the current site.

    The `listJobs()` method builds a query to retrieve a list of jobs for the current site, and returns a [`JobsQueryBuilder`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/ascending) object. The returned object contains the query definition, which is typically used to run the query using the [`find()`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/find) method.

    You can refine the query by chaining `JobsQueryBuilder` methods onto the query. `JobsQueryBuilder` methods enable you to sort, filter, and control the results `listJobs()` returns.

    `listJobs()` runs with these `JobsQueryBuilder` defaults, which you can override:

    - [`skipTo`: `0`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/skip-to)
    - [`limit`: `50`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/limit)
    - [`ascending`: by `_id`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/ascending)

    The methods chained to `listJobs()` are applied in the order they're called. For example, if you apply `ascending('firstName')` and then `descending('age')`, the results are sorted first by firstName, and then, if there are multiple results with the same `firstName`, the items are sorted by `age`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE_MOVEMENT_JOBS">Data Mover Manage Jobs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data-mover/v1/jobs/list
    ```

    ## Body Params

    <ParamField body="paging" type="CursorPaging">

      Paging metadata.

      <Expandable title="paging properties">

        <ParamField body="limit" type="integer">

          Maximum number of items to return in the results.

        </ParamField>

        <ParamField body="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="jobs" type="DataMovementJob[]">

      List of jobs for the current site.

      See the [Job Object](/api-reference/business-solutions/cms/operations/data-movement-jobs/job-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET 'https://www.wixapis.com/data-mover/v1/jobs' \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
          "jobs": [
              {
                "id": "8646fc6e-58f3-4d78-b3ab-6a63a5a46400",
                "name": "Wix Data Collection Export",
                "startedAt": "2024-03-15T14:30:29.162Z",
                "status": "INITIALIZING",
                "source": {
                  "wixDataCollection": {
                    "collectionId": "TodoList"
                  }
                },
                "destination": {
                  "file": {
                    "format": "CSV",
                    "fileName": "TodoList.csv",
                    "url": "http://example.com/files/TodoList.csv"
                  }
                },
                "transformations": []
              }
          ],
          "pagingMetadata": {
              "count": 1,
              "hasNext": false
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Lists data movement jobs for the current site.

    The `listJobs()` method builds a query to retrieve a list of jobs for the current site, and returns a [`JobsQueryBuilder`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/ascending) object. The returned object contains the query definition, which is typically used to run the query using the [`find()`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/find) method.

    You can refine the query by chaining `JobsQueryBuilder` methods onto the query. `JobsQueryBuilder` methods enable you to sort, filter, and control the results `listJobs()` returns.

    `listJobs()` runs with these `JobsQueryBuilder` defaults, which you can override:

    - [`skipTo`: `0`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/skip-to)
    - [`limit`: `50`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/limit)
    - [`ascending`: by `_id`](https://dev.wix.com/docs/sdk/backend-modules/data/movement-jobs/jobs-query-builder/ascending)

    The methods chained to `listJobs()` are applied in the order they're called. For example, if you apply `ascending('firstName')` and then `descending('age')`, the results are sorted first by firstName, and then, if there are multiple results with the same `firstName`, the items are sorted by `age`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE_MOVEMENT_JOBS">Data Mover Manage Jobs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { movementJobs } from '@wix/data';

    movementJobs.listJobs(options): Promise<ListJobsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListJobsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="CursorPaging">

          Paging metadata.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return in the results.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListJobsResponse>`.

    <ResponseField name="jobs" type="DataMovementJob[]">

      List of jobs for the current site.

      See the [Job Object](/api-reference/business-solutions/cms/operations/data-movement-jobs/job-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-jobs" id="ex-sdk-list-jobs-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-jobs" id="ex-sdk-list-jobs-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-jobs" id="ex-sdk-list-jobs-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-jobs-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-jobs-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listJobs</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listJobs (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listJobs (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-jobs-0"><span className="wix-picker-name">listJobs</span></label>
    <label htmlFor="ex-sdk-list-jobs-1"><span className="wix-picker-name">listJobs (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-jobs-2"><span className="wix-picker-name">listJobs (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listJobs
    import { movementJobs } from '@wix/data';

    async function listJobs(options) {
      const response = await movementJobs.listJobs(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listJobs (self-hosted)
    import { createClient } from '@wix/sdk';
    import { movementJobs } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { movementJobs },
      // Include the auth strategy and host as relevant
    });

    async function listJobs(options) {
      const response = await myWixClient.movementJobs.listJobs(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listJobs (with elevated permissions)
    import { movementJobs } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myListJobsMethod(options) {
      const elevatedListJobs = auth.elevate(movementJobs.listJobs);
      const response = await elevatedListJobs(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
