---
title: "onItemVariantUpdated"
description: "The onItemVariantUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_variant_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemVariantUpdated
  import { itemVariants } from '@wix/restaurants';

  itemVariants.onItemVariantUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemVariantUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemVariants } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemVariants,
      },
  });

  wixClient.itemVariants.onItemVariantUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
