---
title: "Create Task"
description: "Creates and runs a new background task."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates and runs a new background task.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/cms/v1/tasks
    ```

    ## Body Params

    <ParamField body="task" type="Task" required>

      Task to create and run.

      See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="task" type="Task">

      Created task.

      See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-create-task" id="ex-rest-create-task-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-create-task" id="ex-rest-create-task-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-create-task" id="ex-rest-create-task-2" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-create-task" id="ex-rest-create-task-3" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-create-task-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-create-task-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create Update Publish Status task</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create Copy Field Data task</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create Edit Field Data task</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create Delete By Query task</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-create-task-0"><span className="wix-picker-name">Create Update Publish Status task</span></label>
    <label htmlFor="ex-rest-create-task-1"><span className="wix-picker-name">Create Copy Field Data task</span></label>
    <label htmlFor="ex-rest-create-task-2"><span className="wix-picker-name">Create Edit Field Data task</span></label>
    <label htmlFor="ex-rest-create-task-3"><span className="wix-picker-name">Create Delete By Query task</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://wixapis.com/autocms/v1/tasks  \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
                "task": {
                  "type": "UPDATE_PUBLISH_STATUS",
                  "updatePublishStatusOptions": {
                    "dataCollectionId": "Items",
                    "environment": "LIVE",
                    "filter": {
                      "holidays": {
                        "$in": ["Halloween"]
                      }
                    },
                    "operation": "SCHEDULE_PUBLISHED_STATUS",
                    "schedulePublishedStatusOptions": {
                        "date": "2025-01-01T10:00:00Z"
                    }
                  }
                }
              }'
      ```

      ```json Response
      {
        "task": {
          "id": "097a5564-0fec-4425-8d1e-3e753b48a422",
          "type": "UPDATE_PUBLISH_STATUS",
          "status": "NEW",
          "startedAt": "2024-09-05T10:34:59.507Z",
          "estimatedItemCount": 3,
          "itemsSucceeded": 0,
          "itemsFailed": 0,
          "failures": [],
          "updatePublishStatusOptions": {
            "dataCollectionId": "Items",
            "environment": "LIVE",
            "filter": {
              "holidays": {
                "$in": ["Halloween"]
              }
            },
            "operation": "SCHEDULE_PUBLISHED_STATUS",
            "schedulePublishedStatusOptions": {
              "date": "2025-01-01T10:00:00Z"
            }
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://wixapis.com/autocms/v1/tasks  \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
              "task": {
                  "type": "COPY_FIELD_DATA",
                  "copyFieldDataOptions": {
                      "dataCollectionId": "Items",
                      "environment": "LIVE",
                      "sourceFieldKey": "fullName",
                      "targetFieldKey": "fullNameCopy"
                  }
              }
            }'
      ```

      ```json Response
      {
          "task": {
              "id": "097a5564-0fec-4425-8d1e-3e753b48a422",
              "type": "COPY_FIELD_DATA",
              "status": "NEW",
              "startedAt": "2024-09-05T10:34:59.507Z",
              "estimatedItemCount": 3,
              "itemsSucceeded": 0,
              "itemsFailed": 0,
              "failures": [],
              "copyFieldDataOptions": {
                  "dataCollectionId": "Items",
                  "environment": "LIVE",
                  "sourceFieldKey": "fullName",
                  "targetFieldKey": "fullNameCopy"
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://wixapis.com/autocms/v1/tasks  \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
              "task": {
                  "type": "EDIT_FIELD_DATA",
                  "editFieldDataOptions": {
                      "dataCollectionId": "Items",
                      "environment": "LIVE",
                      "filter": {
                        "holidays": {
                          "$in": ["Halloween"]
                        }
                      },
                      "fieldKey": "tags",
                      "operations": {
                          "addItems": ["sale"],
                          "removeItems": ["outdated"],
                          "replaceItems": [{
                              "from": "Goob",
                              "to": "Good"
                          }],
                          "removeDuplicates": true
                      }
                  }
              }
            }'
      ```

      ```json Response
      {
          "task": {
              "id": "097a5564-0fec-4425-8d1e-3e753b48a422",
              "type": "EDIT_FIELD_DATA",
              "status": "NEW",
              "startedAt": "2024-09-05T10:34:59.507Z",
              "estimatedItemCount": 3,
              "itemsSucceeded": 0,
              "itemsFailed": 0,
              "failures": [],
              "editFieldDataOptions": {
                  "dataCollectionId": "Items",
                  "environment": "LIVE",
                  "filter": {
                    "holidays": {
                      "$in": ["Halloween"]
                    }
                  },
                  "fieldKey": "tags",
                  "operations": {
                      "addItems": ["sale"],
                      "removeItems": ["outdated"],
                      "replaceItems": [{
                          "from": "Goob",
                          "to": "Good"
                      }],
                      "removeDuplicates": true
                  }
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://wixapis.com/autocms/v1/tasks  \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
              "task": {
                "type": "DELETE_BY_QUERY",
                "deleteByQueryOptions": {
                  "dataCollectionId": "Items",
                  "environment": "LIVE",
                  "filter": {
                    "holidays": {
                      "$in": ["Halloween"]
                    }
                  },
                }
              }
            }'
      ```

      ```json Response
      {
          "task": {
            "id": "097a5564-0fec-4425-8d1e-3e753b48a422",
            "type": "DELETE_BY_QUERY",
            "status": "NEW",
            "startedAt": "2024-09-05T10:34:59.507Z",
            "estimatedItemCount": 3,
            "itemsSucceeded": 0,
            "itemsFailed": 0,
            "failures": [],
            "deleteByQueryOptions": {
              "dataCollectionId": "Items",
              "environment": "LIVE",
              "filter": {
                "altText": {
                  "$in": "Halloween"
                }
              }
            }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates and runs a new background task.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tasks } from '@wix/data';

    tasks.createTask(task): Promise
    ```

    ## Method Parameters

    <ParamField body="task" type="Task" required>

      Task to create and run.

      See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Task`.

    See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Background Task Created | `wix.data.autocms.v2.background_task_created` | Triggered when a task is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-task" id="ex-sdk-create-task-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-task" id="ex-sdk-create-task-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-task" id="ex-sdk-create-task-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-task-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-task-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createTask</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createTask (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-task-0"><span className="wix-picker-name">createTask</span></label>
    <label htmlFor="ex-sdk-create-task-1"><span className="wix-picker-name">createTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-task-2"><span className="wix-picker-name">createTask (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createTask
    import { tasks } from '@wix/data';

    async function createTask(task) {
      const response = await tasks.createTask(task);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createTask (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tasks } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tasks },
      // Include the auth strategy and host as relevant
    });

    async function createTask(task) {
      const response = await myWixClient.tasks.createTask(task);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createTask (with elevated permissions)
    import { tasks } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myCreateTaskMethod(task) {
      const elevatedCreateTask = auth.elevate(tasks.createTask);
      const response = await elevatedCreateTask(task);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
