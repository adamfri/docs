---
title: "Get Tax Region"
description: "Retrieves a tax region."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a tax region.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/billing/v1/tax-regions/{taxRegionId}
    ```

    ## Path Params

    <ParamField path="taxRegionId" type="string" required>

      ID of the tax region to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="taxRegion" type="TaxRegion">

      Retrieved tax region.

      See the [Tax Region Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-regions/tax-region-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieve a tax region by ID

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/billing/v1/tax-regions/88a175be-7cc3-427d-bd79-d9aa21b10108' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      ```

      ```json Response
      {
        "taxRegion": {
          "id": "88a175be-7cc3-427d-bd79-d9aa21b10108",
          "country": "IE",
          "subdivision": "*",
          "appId": "7516f85b-0868-4c23-9fcb-cea7784243df",
          "taxIncludedInPrice": false,
          "revision": "1",
          "createdDate": "2024-02-06T07:48:26.246Z",
          "updatedDate": "2024-02-06T07:48:26.246Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a tax region.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { taxRegions } from '@wix/ecom';

    taxRegions.getTaxRegion(taxRegionId): Promise
    ```

    ## Method Parameters

    <ParamField body="taxRegionId" type="string" required>

      ID of the tax region to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `TaxRegion`.

    See the [Tax Region Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-regions/tax-region-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-tax-region" id="ex-sdk-get-tax-region-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-tax-region" id="ex-sdk-get-tax-region-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-tax-region-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-tax-region-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get Tax Region</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTaxRegion (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-tax-region-0"><span className="wix-picker-name">Get Tax Region</span></label>
    <label htmlFor="ex-sdk-get-tax-region-1"><span className="wix-picker-name">getTaxRegion (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get Tax Region
    import { taxRegions } from "@wix/ecom";

    async function getTaxRegion() {
      const response = await taxRegions.getTaxRegion(
        "a1b2c3d4-e5f6-7a8b-9c0d-e1f2a3b4c5d6",
      );
      return response;
    }

    /* Promise resolves to:
     * {
     *   "_id": "a1b2c3d4-e5f6-7a8b-9c0d-e1f2a3b4c5d6",
     *   "country": "US",
     *   "subdivision": "NY",
     *   "appId": "61f58705-419e-4c30-b498-751c399eb498",
     *   "taxIncludedInPrice": false,
     *   "revision": "1"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTaxRegion (self-hosted)
    import { createClient } from '@wix/sdk';
    import { taxRegions } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { taxRegions },
      // Include the auth strategy and host as relevant
    });

    async function getTaxRegion(taxRegionId) {
      const response = await myWixClient.taxRegions.getTaxRegion(taxRegionId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
