---
title: "onInventoryItemCreated"
description: "Triggered when an inventory item is created."
icon: "bolt"
---

Triggered when an inventory item is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.stores.catalog.v3.inventory_item_created` |
| Package | `@wix/stores` |
| Permissions | `Read inventory in v3 catalog`, `Read v3 catalog`, `Manage Products`, `Read Products`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onInventoryItemCreated: Wix-hosted
  import { inventoryItemsV3 } from '@wix/stores';

  inventoryItemsV3.onInventoryItemCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onInventoryItemCreated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { inventoryItemsV3 } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        inventoryItemsV3,
      },
  });

  wixClient.inventoryItemsV3.onInventoryItemCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
