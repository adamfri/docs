---
title: "List Messages"
description: "Retrieves messages between the business and participant."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves messages between the business and participant.

    Up to 30 messages are returned per request. If the number of messages in a conversation is larger than 30,
    `pagingMetadata.cursors` is returned in the response, indicating that another page of results is available.
    To retrieve the next page of messages, specify the `next` cursor value
    in the next request's `paging.cursor` parameter.

    To ensure you'll always retrieve the next record,
    use the same visibility and sorting
    in the first request and all subsequent requests.

    By default,
    30 messages are retrieved and sorted by `sequence` in descending order
    (the most recent messages are first in the list).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-INBOX.READ-MSGS">Read Inbox Messages</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/inbox/v2/messages
    ```

    ## Query Params

    <ParamField query="conversationId" type="string" required>

      ID of the conversation that contains the intended messages.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField query="visibility" type="string">

      __Required.__
      Filters for messages with the specified visibility setting.

      **Supported values:**

      - `UNKNOWN_VISIBILITY` — Unknown message visibility.
      - `BUSINESS_AND_PARTICIPANT` — Visible to the participant and site collaborators.
      - `BUSINESS` — Visible to site collaborators only.

    </ParamField>

    <ParamField query="paging" type="CursorPaging">

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Maximum number of items to return in the results.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="sorting" type="Sorting">

      only message_sequence is supported for field name

      <Expandable title="sorting properties">

        <ParamField query="fieldName" type="string">

          Name of the field to sort by.

          Constraints: `maxLength 512`.

        </ParamField>

        <ParamField query="order" type="string">

          Sort order.

          **Supported values:**

          - `ASC`
          - `DESC`

        </ParamField>

        <ParamField query="origin" type="AddressLocation">

          Origin point for geo-distance sorting on a GEO field
          results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

          <Expandable title="origin properties">

            <ParamField query="latitude" type="number">

              Address latitude.

            </ParamField>

            <ParamField query="longitude" type="number">

              Address longitude.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="messages" type="Message[]">

      List of messages between the specified visitor and the site.

      See the [Message Object](/api-reference/crm/communication/inbox/messages/message-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `401` | `UNAUTHENTICATED` | `NO_INBOX_INSTANCE` | No inbox instance available. |
    | `401` | `UNAUTHENTICATED` | `NOT_JOINED_CHATROOM` | Not joined to chatroom. |
    | `404` | `NOT_FOUND` | `CONVERSATION_NOT_FOUND` | Conversation not found. |
    | `404` | `NOT_FOUND` | `INBOX_INSTANCE_NOT_FOUND` | Inbox instance not found. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
      'https://www.wixapis.com/inbox/v2/messages?conversationId=6202c3bb-f263-3c16-8c90-d6387d9ed420&visibility=BUSINESS_AND_PARTICIPANT' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "messages": [
          {
            "id": "1640851963799825",
            "content": {
              "previewText": "Thanks for reaching out 💬. We’ll be right with you.",
              "basic": {
                "items": [
                  {
                    "text": "Thanks for reaching out 💬. We’ll be right with you."
                  }
                ]
              }
            },
            "sender": {
              "wixUserId": "5a0c4737-a2a0-4c37-a07e-d9fd87027023"
            },
            "badges": [],
            "targetChannels": [],
            "sourceChannel": "UNKNOWN_CHANNEL_TYPE",
            "visibility": "BUSINESS_AND_PARTICIPANT",
            "sequence": "1640851963799825",
            "direction": "BUSINESS_TO_PARTICIPANT",
            "createdDate": "2021-12-30T08:12:43.799Z"
          },
          {
            "id": "1640851962800972",
            "content": {
              "previewText": "A Template message!",
              "basic": {
                "items": [
                  {
                    "text": "A text message"
                  }
                ]
              }
            },
            "sender": {
              "anonymousVisitorId": "40096143-3643-4cc1-8329-30a7fc2c595e"
            },
            "badges": [],
            "targetChannels": [
              "CHAT"
            ],
            "sourceChannel": "CHAT",
            "appId": "141fbfae-511e-6817-c9f0-48993a7547d1",
            "visibility": "BUSINESS_AND_PARTICIPANT",
            "sequence": "1640851962800972",
            "direction": "PARTICIPANT_TO_BUSINESS",
            "createdDate": "2021-12-30T08:12:42.800Z"
          },
          {
            "id": "1640851398501777",
            "content": {
              "previewText": "A Template message!",
              "template": {
                "title": "A Template Message",
                "buttons": [
                  {
                    "label": "click me",
                    "postbackProperties": {
                      "appId": "141fbfae-511e-6817-c9f0-48993a7547d1",
                      "interactionId": "inter_id_1"
                    }
                  }
                ],
                "orientation": "PORTRAIT",
                "textLines": [
                  "oh",
                  "cool"
                ],
                "imageUrl": "https://static.wixstatic.com/media//0df0927d01d641338d1e0c59bbbabfde.jpg/v1/fill/w_315,h_315,q_80/0df0927d01d641338d1e0c59bbbabfde.jpg"
              }
            },
            "sender": {
              "appId": "141fbfae-511e-6817-c9f0-48993a7547d1"
            },
            "badges": [],
            "targetChannels": [
              "CHAT"
            ],
            "sourceChannel": "CHAT",
            "appId": "141fbfae-511e-6817-c9f0-48993a7547d1",
            "visibility": "BUSINESS_AND_PARTICIPANT",
            "sequence": "1640851398501777",
            "direction": "BUSINESS_TO_PARTICIPANT",
            "createdDate": "2021-12-30T08:03:18.501Z"
          },
          {
            "id": "1640849952353627",
            "content": {
              "previewText": "Hey!",
              "basic": {
                "items": [
                  {
                    "text": "Hey!"
                  }
                ]
              }
            },
            "sender": {
              "wixUserId": "5a0c4737-a2a0-4c37-a07e-d9fd87027023"
            },
            "badges": [],
            "targetChannels": [
              "EMAIL"
            ],
            "sourceChannel": "CHAT",
            "appId": "141fbfae-511e-6817-c9f0-48993a7547d1",
            "visibility": "BUSINESS_AND_PARTICIPANT",
            "sequence": "1640849952353627",
            "direction": "BUSINESS_TO_PARTICIPANT",
            "createdDate": "2021-12-30T07:39:12.353Z"
          }
        ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves messages between the business and participant.

    Up to 30 messages are returned per request. If the number of messages in a conversation is larger than 30,
    `pagingMetadata.cursors` is returned in the response, indicating that another page of results is available.
    To retrieve the next page of messages, specify the `next` cursor value
    in the next request's `paging.cursor` parameter.

    To ensure you'll always retrieve the next record,
    use the same visibility and sorting
    in the first request and all subsequent requests.

    By default,
    30 messages are retrieved and sorted by `sequence` in descending order
    (the most recent messages are first in the list).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-INBOX.READ-MSGS">Read Inbox Messages</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { messages } from '@wix/inbox';

    messages.listMessages(conversationId, visibility, options): Promise<ListMessagesResponse>
    ```

    ## Method Parameters

    <ParamField body="conversationId" type="string" required>

      ID of the conversation that contains the intended messages.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="visibility" type="string" required>

      __Required.__
      Filters for messages with the specified visibility setting.

      **Supported values:**

      - `UNKNOWN_VISIBILITY` — Unknown message visibility.
      - `BUSINESS_AND_PARTICIPANT` — Visible to the participant and site collaborators.
      - `BUSINESS` — Visible to site collaborators only.

    </ParamField>

    <ParamField body="options" type="ListMessagesOptions">

      Additional options for listing messages.

      <Expandable title="options properties">

        <ParamField body="paging" type="CursorPaging">

          paging

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return in the results.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="sorting" type="Sorting">

          only message_sequence is supported for field name

          <Expandable title="sorting properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListMessagesResponse>`.

    <ResponseField name="messages" type="Message[]">

      List of messages between the specified visitor and the site.

      See the [Message Object](/api-reference/crm/communication/inbox/messages/message-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `401` | `UNAUTHENTICATED` | `NO_INBOX_INSTANCE` | No inbox instance available. |
    | `401` | `UNAUTHENTICATED` | `NOT_JOINED_CHATROOM` | Not joined to chatroom. |
    | `404` | `NOT_FOUND` | `CONVERSATION_NOT_FOUND` | Conversation not found. |
    | `404` | `NOT_FOUND` | `INBOX_INSTANCE_NOT_FOUND` | Inbox instance not found. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-messages" id="ex-sdk-list-messages-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-messages" id="ex-sdk-list-messages-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-messages" id="ex-sdk-list-messages-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-messages-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-messages-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">View a list of sent messages</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">View a list of sent messages (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listMessages (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-messages-0"><span className="wix-picker-name">View a list of sent messages</span></label>
    <label htmlFor="ex-sdk-list-messages-1"><span className="wix-picker-name">View a list of sent messages (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-list-messages-2"><span className="wix-picker-name">listMessages (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript View a list of sent messages
    import { messages } from '@wix/inbox';

    /* Sample conversationId value: '9ac410b1-17f8-3b10-a3f4-0dd5c295d510'
     * 
     * Sample visibility value: 'BUSINESS_AND_PARTICIPANT'
     */

    export async function myListMessagesFunction(conversationId, visibility) {

      try {
        const messageList = await messages.listMessages(conversationId, visibility);
        const firstSender = messageList.messages[0].sender;

        return messageList;
      } catch(error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "messages": [
     *     {
     *       "_createdDate": "2023-12-28T09:41:06.660Z",
     *       "_id": "1703756466660667",
     *       "appId": "14517e1a-3ff0-af98-408e-2bd6953c36a2",
     *       "badges": [],
     *       "content": {
     *         "previewText": "thanks for the content",
     *         "basic": {
     *           "items": [
     *             {
     *               "text": "thanks for the content"
     *             }
     *           ]
     *         },
     *       },
     *       "direction": "PARTICIPANT_TO_BUSINESS",
     *       "sender": {
     *         "anonymousVisitorId": "3c06d38d-1433-4889-b303-1ff1779176e6"
     *       },
     *       "sequence": "1703756466660667",
     *       "sourceChannel": "UNKNOWN_CHANNEL_TYPE",
     *       "targetChannels": [],
     *       "visibility": "BUSINESS_AND_PARTICIPANT",
     *     },
     *     {
     *       "_createdDate": "2023-12-27T16:16:07.816Z",
     *       "_id": "1703693767816923",
     *       "badges": [],
     *       "content": {
     *         "previewText": "Thanks for the like :)",
     *         "basic": {
     *           "items": [
     *             {
     *               "text": "Thanks for the like :)"
     *             }
     *           ]
     *         },
     *       },
     *       "direction": "BUSINESS_TO_PARTICIPANT",
     *       "sender": {
     *         "wixUserId": "b17c523c-6ec9-4b56-9d9d-123cc6978bdf"
     *       },
     *       "sequence": "1703693767816923",
     *       "sourceChannel": "UNKNOWN_CHANNEL_TYPE",
     *       "targetChannels": [
     *         "CHAT"
     *       ],
     *       "visibility": "BUSINESS_AND_PARTICIPANT",
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "tooManyToCount": true
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript View a list of sent messages (with elevated permissions)
    import { messages } from '@wix/inbox';
    import { auth } from '@wix/essentials';

    /* Sample conversationId value: '9ac410b1-17f8-3b10-a3f4-0dd5c295d510'
     * 
     * Sample visibility value: 'BUSINESS_AND_PARTICIPANT'
     */

    const elevatedListMessages = auth.elevate(messages.listMessages);

    export async function myListMessagesFunction(conversationId, visibility) {

      try {
        const messageList = await elevatedListMessages(conversationId, visibility);
        const firstSender = messageList.messages[0].sender;

        return messageList;
      } catch(error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "messages": [
     *     {
     *       "_createdDate": "2023-12-28T09:41:06.660Z",
     *       "_id": "1703756466660667",
     *       "appId": "14517e1a-3ff0-af98-408e-2bd6953c36a2",
     *       "badges": [],
     *       "content": {
     *         "previewText": "thanks for the content",
     *         "basic": {
     *           "items": [
     *             {
     *               "text": "thanks for the content"
     *             }
     *           ]
     *         },
     *       },
     *       "direction": "PARTICIPANT_TO_BUSINESS",
     *       "sender": {
     *         "anonymousVisitorId": "3c06d38d-1433-4889-b303-1ff1779176e6"
     *       },
     *       "sequence": "1703756466660667",
     *       "sourceChannel": "UNKNOWN_CHANNEL_TYPE",
     *       "targetChannels": [],
     *       "visibility": "BUSINESS_AND_PARTICIPANT",
     *     },
     *     {
     *       "_createdDate": "2023-12-27T16:16:07.816Z",
     *       "_id": "1703693767816923",
     *       "badges": [],
     *       "content": {
     *         "previewText": "Thanks for the like :)",
     *         "basic": {
     *           "items": [
     *             {
     *               "text": "Thanks for the like :)"
     *             }
     *           ]
     *         },
     *       },
     *       "direction": "BUSINESS_TO_PARTICIPANT",
     *       "sender": {
     *         "wixUserId": "b17c523c-6ec9-4b56-9d9d-123cc6978bdf"
     *       },
     *       "sequence": "1703693767816923",
     *       "sourceChannel": "UNKNOWN_CHANNEL_TYPE",
     *       "targetChannels": [
     *         "CHAT"
     *       ],
     *       "visibility": "BUSINESS_AND_PARTICIPANT",
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "tooManyToCount": true
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listMessages (self-hosted)
    import { createClient } from '@wix/sdk';
    import { messages } from '@wix/inbox';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { messages },
      // Include the auth strategy and host as relevant
    });

    async function listMessages(conversationId,visibility,options) {
      const response = await myWixClient.messages.listMessages(conversationId,visibility,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
