---
title: "Search Inventory Items"
description: "Retrieves a list of inventory items, given the provided filtering, sorting, and cursor paging."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of inventory items, given the provided filtering, sorting, and cursor paging.

    Search Inventory Items runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `100`

    To learn about working with _Search_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    Learn more about the differences between [_Query_ and _Search_](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-search-query-and-list-methods) methods.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `locationId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `variantId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `product.directCategoryIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `availabilityStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `inStock` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `preorderInfo.enabled` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `product.brand.id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `product.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `product.variantName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `product.variantSku` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `productId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `quantity` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `trackQuantity` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INVENTORY_ITEM_READ">Read inventory in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/inventory-items/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [sorting](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation to perform. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="groupBy" type="GroupByAggregation" deprecated>

              Deprecated. Use `nested` instead.

              **Deprecated.** Deprecated. Use `nested` instead. Use `nested` instead. Removal: 2024-03-30.

              _Nested structure omitted for brevity — see the `GroupByAggregation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least one of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
          Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="inventoryItems" type="InventoryItem[]">

      List of inventory items.

      Constraints: `minItems 1`, `maxItems 1000`.

      See the [Inventory Item Object](/api-reference/business-solutions/stores/catalog-v3/inventory-items-v3/inventory-item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          List of the aggregated data results.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Aggregation type. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-search-inventory-items" id="ex-rest-search-inventory-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-search-inventory-items" id="ex-rest-search-inventory-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-search-inventory-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-search-inventory-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search available items in a specific location</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">SearchInventoryItems filter and sort by createdDate</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-search-inventory-items-0"><span className="wix-picker-name">Search available items in a specific location</span></label>
    <label htmlFor="ex-rest-search-inventory-items-1"><span className="wix-picker-name">SearchInventoryItems filter and sort by createdDate</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/stores/v3/inventory-items/search' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "search": {
              "filter": {
                "$and": [
                  {
                    "availabilityStatus": {
                      "$eq": "IN_STOCK"
                    }
                  },
                  {
                    "locationId": {
                      "$eq": "4c7b5c73-d7b4-4ab3-8dbf-410776df8008"
                    }
                  }
                ]
              }
            }
        }'
      ```

      ```json Response
      {
        "inventoryItems": [
          {
            "id": "13b94cf4-d2fb-4527-9210-e9bb11762387",
            "revision": "1",
            "createdDate": "2024-06-27T09:33:47.432Z",
            "updatedDate": "2024-06-27T09:33:54.060Z",
            "variantId": "4ed08998-d321-4079-9284-9c379f2f118c",
            "locationId": "4c7b5c73-d7b4-4ab3-8dbf-410776df8008",
            "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
            "quantity": 88,
            "trackQuantity": true,
            "availabilityStatus": "IN_STOCK",
            "preorderInfo": {
              "enabled": false,
              "counter": null,
              "limit": null,
              "message": null,
              "quantity": null
            },
            "product": {
              "name": "Shorts",
              "directCategoryIds": [
                "02e992f5-532f-4667-8e8a-3e7aee2a36de"
              ]
            },
            "extendedFields": null
          },
          {
            "id": "890265e3-53bd-4f2a-b3a9-3d1879ff547e",
            "revision": "2",
            "createdDate": "2024-06-27T09:33:47.432Z",
            "updatedDate": "2024-07-02T08:44:39.299Z",
            "variantId": "19fa7e38-840c-4d4e-a2a0-501a3d7c60c5",
            "locationId": "4c7b5c73-d7b4-4ab3-8dbf-410776df8008",
            "productId": "f16303c1-0fd9-4713-8a44-8f818cfa7044",
            "quantity": 77,
            "trackQuantity": true,
            "availabilityStatus": "IN_STOCK",
            "preorderInfo": {
              "enabled": false,
              "counter": null,
              "limit": null,
              "message": null,
              "quantity": null
            },
            "product": {
              "name": "Tennis Shoes",
              "directCategoryIds": [
                "02e992f5-532f-4667-8e8a-3e7aee2a36de"
              ]
            },
            "extendedFields": null
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/stores/v3/inventory-items/search' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "search": {
            "sort": [
              {
                "fieldName": "createdDate",
                "order": "DESC"
              }
            ],
            "filter": {
              "createdDate": {
                "$gt": "2024-04-24T10:22:53"
              }
            },
            "cursorPaging": {
              "limit": 2
            }
          }
        }'
      ```

      ```json Response
      {
        "inventoryItems": [
          {
            "id": "fb913045-b421-4d09-804f-352fbe784465",
            "revision": "1",
            "createdDate": "2024-06-27T09:33:47.432Z",
            "updatedDate": "2024-06-27T09:33:54.060Z",
            "variantId": "f27ee649-ba6b-493d-b758-75816208cf2e",
            "locationId": "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
            "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
            "quantity": 110,
            "trackQuantity": true,
            "availabilityStatus": "IN_STOCK",
            "preorderInfo": {
              "enabled": false,
              "counter": null,
              "limit": null,
              "message": null,
              "quantity": null
            },
            "product": {
              "name": "t shirt",
              "directCategoryIds": [
                "05f9cc2f-b5df-4287-aa32-3314179192b8"
              ]
            },
            "extendedFields": null
          },
          {
            "id": "a83212bc-1c1e-4645-b391-a9fed7f930f6",
            "revision": "2",
            "createdDate": "2024-06-27T09:33:47.432Z",
            "updatedDate": "2024-07-02T08:44:39.299Z",
            "variantId": "590cef15-c81d-4ed7-970c-1ff879946306",
            "locationId": "d85fbb4d-e415-49b1-98bc-9d22ec338cb1",
            "productId": "babd2bcc-ea03-4b63-8053-0ec59c73fc36",
            "quantity": 105,
            "trackQuantity": true,
            "availabilityStatus": "IN_STOCK",
            "preorderInfo": {
              "enabled": false,
              "counter": null,
              "limit": null,
              "message": null,
              "quantity": null
            },
            "product": {
              "name": "t shirt",
              "directCategoryIds": [
                "05f9cc2f-b5df-4287-aa32-3314179192b8"
              ]
            },
            "extendedFields": null
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {
            "next": "eyJjcmVhdGVkRGF0ZSI6IjIwMjQtMDYtMjdUMDk6MzM6NDcuNDMyWiJ9"
          },
          "hasNext": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of inventory items, given the provided filtering, sorting, and cursor paging.

    Search Inventory Items runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `100`

    To learn about working with _Search_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    Learn more about the differences between [_Query_ and _Search_](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-search-query-and-list-methods) methods.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `locationId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `variantId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `product.directCategoryIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `availabilityStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `inStock` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `preorderInfo.enabled` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `product.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `product.variantName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `product.variantSku` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `productId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `quantity` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `trackQuantity` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INVENTORY_ITEM_READ">Read inventory in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { inventoryItemsV3 } from '@wix/stores';

    inventoryItemsV3.searchInventoryItems(search): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [sorting](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation to perform. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="groupBy" type="GroupByAggregation" deprecated>

              Deprecated. Use `nested` instead.

              **Deprecated.** Deprecated. Use `nested` instead. Use `nested` instead. Removal: 2024-03-30.

              _Nested structure omitted for brevity — see the `GroupByAggregation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least one of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
          Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="inventoryItems" type="InventoryItem[]">

      List of inventory items.

      Constraints: `minItems 1`, `maxItems 1000`.

      See the [Inventory Item Object](/api-reference/business-solutions/stores/catalog-v3/inventory-items-v3/inventory-item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          List of the aggregated data results.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Aggregation type. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-inventory-items" id="ex-sdk-search-inventory-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-inventory-items" id="ex-sdk-search-inventory-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-search-inventory-items" id="ex-sdk-search-inventory-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-inventory-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-inventory-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search inventory items by creation date</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Search inventory items by location and availability</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchInventoryItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-inventory-items-0"><span className="wix-picker-name">Search inventory items by creation date</span></label>
    <label htmlFor="ex-sdk-search-inventory-items-1"><span className="wix-picker-name">Search inventory items by location and availability</span></label>
    <label htmlFor="ex-sdk-search-inventory-items-2"><span className="wix-picker-name">searchInventoryItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Search inventory items by creation date
    import { inventoryItemsV3 } from "@wix/stores";

    const options = {
      search: {
        sort: [
          {
            fieldName: "createdDate",
            order: "DESC"
          }
        ],
        filter: {
          createdDate: {
            $gt: "2024-04-24T10:22:53"
          }
        },
        cursorPaging: {
          limit: 2
        }
      }
    };

    async function searchInventoryItems() {
      const response = await inventoryItemsV3.searchInventoryItems(options);
    }

    /* Promise resolves to:
     * {
     *   "inventoryItems": [
     *     {
     *       "_id": "item-1",
     *       "revision": "1",
     *       "variantId": "variant-1",
     *       "productId": "product-1",
     *       "inStock": true,
     *       "quantity": 50,
     *       "createdDate": "2024-05-01T10:00:00.000Z"
     *     }
     *   ],
     *   "pagingMetadata": { "count": 1, "cursors": {}, "hasNext": false, "total": 1 }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Search inventory items by location and availability
    import { inventoryItemsV3 } from "@wix/stores";

    async function searchInventoryItems() {
      const response = await inventoryItemsV3.searchInventoryItems({
        filter: {
          $and: [
            {
              availabilityStatus: {
                $eq: "IN_STOCK"
              }
            },
            {
              locationId: {
                $eq: "4c7b5c73-d7b4-4ab3-8dbf-410776df8008"
              }
            }
          ]
        }
      });
    }

    /* Promise resolves to:
     * {
     *   "inventoryItems": [
     *     {
     *       "_id": "item-1",
     *       "revision": "1",
     *       "variantId": "variant-1",
     *       "productId": "product-1",
     *       "locationId": "4c7b5c73-d7b4-4ab3-8dbf-410776df8008",
     *       "availabilityStatus": "IN_STOCK",
     *       "quantity": 25
     *     }
     *   ],
     *   "pagingMetadata": { "count": 1, "cursors": {}, "hasNext": false, "total": 1 }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchInventoryItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { inventoryItemsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { inventoryItemsV3 },
      // Include the auth strategy and host as relevant
    });

    async function searchInventoryItems(search) {
      const response = await myWixClient.inventoryItemsV3.searchInventoryItems(search);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
