---
title: "Get Settings"
description: "Retrieves back in stock notification request settings."
icon: "rotate"
mode: "wide"
---

<span className="wix-method wix-method-put">PUT</span>

<Tabs>

  <Tab title="REST">

    Retrieves back in stock notification request settings.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.MANAGE-STORES">Manage Stores</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    PUT https://www.wixapis.com/back-in-stock-service/v1/back-in-stock-notification-requests/settings
    ```

    ## Response Object

    <ResponseField name="settings" type="BackInStockSettings">

      Retrieved back in stock request settings.

      See the [Back In Stock Settings Object](/api-reference/business-solutions/e-commerce/other-services/back-in-stock/back-in-stock-settings/back-in-stock-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves back in stock notification request settings.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.MANAGE-STORES">Manage Stores</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { backInStockSettings } from '@wix/ecom';

    backInStockSettings.getSettings(): Promise<GetSettingsResponse>
    ```

    ## Returns

    Returns `Promise<GetSettingsResponse>`.

    <ResponseField name="settings" type="BackInStockSettings">

      Retrieved back in stock request settings.

      See the [Back In Stock Settings Object](/api-reference/business-solutions/e-commerce/other-services/back-in-stock/back-in-stock-settings/back-in-stock-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-settings" id="ex-sdk-get-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-settings" id="ex-sdk-get-settings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-settings-0"><span className="wix-picker-name">getSettings</span></label>
    <label htmlFor="ex-sdk-get-settings-1"><span className="wix-picker-name">getSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getSettings
    import { backInStockSettings } from '@wix/ecom';

    async function getSettings() {
      const response = await backInStockSettings.getSettings();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { backInStockSettings } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { backInStockSettings },
      // Include the auth strategy and host as relevant
    });

    async function getSettings() {
      const response = await myWixClient.backInStockSettings.getSettings();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
