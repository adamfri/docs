---
title: "Count Resource Types"
description: "Counts resource types, given the provided filtering."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Counts resource types, given the provided filtering.

    See [Query Resource Types](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/query-resource-types) for a list of supported filters.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/resources/resource-types/count
    ```

    ## Body Params

    <ParamField body="filter" type="object">

      Filter to base the count on.
      See [Query Resource Types](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/query-resource-types) for a list of supported filters.

    </ParamField>

    ## Response Object

    <ResponseField name="count" type="integer">

      Number of resource types matching the filter.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-count-resource-types" id="ex-rest-count-resource-types-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-count-resource-types" id="ex-rest-count-resource-types-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-count-resource-types-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-count-resource-types-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Count resource types with a filter.</span><span className="wix-picker-desc">Count resource types, providing a filter to count only the ones whose name starts with a specific value</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Count resource types.</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-count-resource-types-0"><span className="wix-picker-name">Count resource types with a filter.</span><span className="wix-picker-desc">Count resource types, providing a filter to count only the ones whose name starts with a specific value</span></label>
    <label htmlFor="ex-rest-count-resource-types-1"><span className="wix-picker-name">Count resource types.</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/resources/resource-types/count' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "filter": {
            "name": {
              "$startsWith": "meeting"
            }
          }
      }'
      ```

      ```json Response
      {
        "count": 1
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/resources/resource-types/count' \
      -H 'Authorization: <AUTH>' \
      -d '{}'
      ```

      ```json Response
      {
        "count": 2
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Counts resource types, given the provided filtering.

    See [Query Resource Types](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/query-resource-types) for a list of supported filters.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { resourceTypes } from '@wix/bookings';

    resourceTypes.countResourceTypes(options): Promise<CountResourceTypesResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="CountResourceTypesOptions">

      Filter to base the count on. See
      [queryResourceTypes()](https://dev.wix.com/docs/sdk/backend-modules/bookings/resource-types/query-resource-types)
      for supported filters.

      <Expandable title="options properties">

        <ParamField body="filter" type="object">

          Filter to base the count on.
          See [Query Resource Types](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/query-resource-types) for a list of supported filters.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CountResourceTypesResponse>`.

    <ResponseField name="count" type="integer">

      Number of resource types matching the filter.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-count-resource-types" id="ex-sdk-count-resource-types-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-count-resource-types" id="ex-sdk-count-resource-types-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-count-resource-types-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-count-resource-types-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">countResourceTypes</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">countResourceTypes (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-count-resource-types-0"><span className="wix-picker-name">countResourceTypes</span></label>
    <label htmlFor="ex-sdk-count-resource-types-1"><span className="wix-picker-name">countResourceTypes (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript countResourceTypes
    import { resourceTypes } from '@wix/bookings';

    async function countResourceTypes(options) {
      const response = await resourceTypes.countResourceTypes(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript countResourceTypes (self-hosted)
    import { createClient } from '@wix/sdk';
    import { resourceTypes } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { resourceTypes },
      // Include the auth strategy and host as relevant
    });

    async function countResourceTypes(options) {
      const response = await myWixClient.resourceTypes.countResourceTypes(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
