---
title: "Cancel Package"
description: "Cancels all product instances included in the package and the customer immediately loses access to the canceled functionality."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Cancels all product instances included in the package and the customer
    immediately loses access to the canceled functionality.

    You must pass the ID of the Wix account that the package belongs to in the
    header of the call. The call fails, if the package and Wix account don't
    match.

    If a canceled product instance is a requirement for another Wix service, that
    functionality is also no longer available to the customer. For example, if
    you cancel a Premium plan, a previously connected domain is automatically
    disconnected from the site.

    <Note>

    This call requires an account level API key and cannot be
    authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="premium.reseller-package.manage">premium.reseller-package.manage</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/resellers/v1/packages/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the package to cancel.

    </ParamField>

    ## Query Params

    <ParamField query="idempotencyKey" type="string">

      Idempotency key.

    </ParamField>

    ## Response Object

    <ResponseField name="idempotencyKey" type="string">

      Idempotency key.

    </ResponseField>

    <ResponseField name="package" type="Package">

      Canceled package.

      See the [Packages Object](/api-reference/account-level/resellers/packages-and-product-instances/packages-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'www.wixapis.com/resellers/v1/packages/828b98fb-7114-4b3c-90fd-8d0db76aa72b' \
      -H 'wix-account-id: <ACCOUNT_ID>' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
          "package": {
              "id": "828b98fb-7114-4b3c-90fd-8d0db76aa72b",
              "accountId": "<ACCOUNT_ID>",
              "externalId": "some-external-id",
              "productInstances": [
                  {
                      "instanceId": "d3b88a39-f62e-4164-8b29-08369b9ea71c",
                      "siteId": "918aa943-ab4f-40bc-88c3-8dfd02fae7cd",
                      "catalogProductId": "00ba7923-2655-402a-852d-2b2100568bcf",
                      "status": "PENDING",
                      "billingInfo": {
                          "type": "RECURRING",
                          "cycleDuration": {
                              "unit": "YEAR",
                              "count": 1
                          }
                      },
                      "createdDate": "2021-12-02T15:45:30.941Z",
                      "updatedDate": "2021-12-02T15:45:30.941Z"
                  }
              ],
              "createdDate": "2021-12-02T15:45:31.815Z",
              "updatedDate": "2021-12-02T15:45:31.815Z"
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Cancels all product instances included in the package and the customer
    immediately loses access to the canceled functionality.

    You must pass the ID of the Wix account that the package belongs to in the
    header of the call. The call fails, if the package and Wix account don't
    match.

    If a canceled product instance is a requirement for another Wix service, that
    functionality is also no longer available to the customer. For example, if
    you cancel a Premium plan, a previously connected domain is automatically
    disconnected from the site.

    <Note>

    This call requires an account level API key and cannot be
    authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="premium.reseller-package.manage">premium.reseller-package.manage</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { packages } from '@wix/packages';

    packages.cancelPackage(_id, options): Promise<CancelPackageResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the package to cancel.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="CancelPackageOptions">

      options

      <Expandable title="options properties">

        <ParamField body="idempotencyKey" type="string">

          Idempotency key.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CancelPackageResponse>`.

    <ResponseField name="idempotencyKey" type="string">

      Idempotency key.

    </ResponseField>

    <ResponseField name="package" type="Package">

      Canceled package.

      See the [Packages Object](/api-reference/account-level/resellers/packages-and-product-instances/packages-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Packages Package Canceled | `wix.premium.reseller.v1.packages_package_canceled` | Triggered when a prackage is canceled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-cancel-package" id="ex-sdk-cancel-package-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-cancel-package" id="ex-sdk-cancel-package-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-cancel-package-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-cancel-package-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Cancel a package with an API key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">cancelPackage (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-cancel-package-0"><span className="wix-picker-name">Cancel a package with an API key</span></label>
    <label htmlFor="ex-sdk-cancel-package-1"><span className="wix-picker-name">cancelPackage (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Cancel a package with an API key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { packages } from "@wix/packages";

    const wixClient = createClient({
     modules: { packages },
     auth: ApiKeyStrategy({
        apiKey: "MY-API-KEY",
      }),
    });

    async function cancelPackage(id, options) {
      const response = await packages.cancelPackage(id, options);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript cancelPackage (self-hosted)
    import { createClient } from '@wix/sdk';
    import { packages } from '@wix/packages';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { packages },
      // Include the auth strategy and host as relevant
    });

    async function cancelPackage(_id,options) {
      const response = await myWixClient.packages.cancelPackage(_id,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
