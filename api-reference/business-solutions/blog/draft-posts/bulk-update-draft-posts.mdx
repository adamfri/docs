---
title: "Bulk Update Draft Posts"
description: "Updates multiple draft posts."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates multiple draft posts.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/blog/v3/draft-posts/update
    ```

    ## Body Params

    <ParamField body="draftPosts" type="MaskedDraftPosts[]">

      Draft posts to update.

      Constraints: `minItems 1`, `maxItems 20`.

      <Expandable title="draftPosts properties">

        <ParamField body="draftPost" type="DraftPost" required>

          Draft post

          See the [Draft Object](/api-reference/business-solutions/blog/draft-posts/draft-object) for this object's fields.

        </ParamField>

        <ParamField body="fieldMask" type="FieldMask">

          Field mask of fields to update.

          <Expandable title="fieldMask properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="action" type="string">

      Action to perform on the posts.

      Default: `UPDATE`

      **Supported values:**

      - `UPDATE` — Update the draft post.
      - `UPDATE_PUBLISH` — Update and publish a draft post.
      - `UPDATE_SCHEDULE` — Update the draft post and schedule a publish date.
      - `UPDATE_REVERT_TO_DRAFT` — Update the post and revert it to draft.
      - `UPDATE_CANCEL_SCHEDULE` — Update the draft post and cancel the scheduled publish date.
      - `UPDATE_REJECT` — Update and reject draft post.
      - `UPDATE_PUBLICATION` — Update a post that's already been published. This creates and updates a draft version of the post. The original post is still published.

    </ParamField>

    <ParamField body="scheduledPublishDate" type="datetime">

      Posts' scheduled publish date when `action` is set to `UPDATE_SCHEDULE`.

    </ParamField>

    <ParamField body="returnFullEntity" type="boolean">

      Whether to return the full updated draft post entities in the response.

    </ParamField>

    <ParamField body="fieldsets" type="string[]">

      List of additional draft post fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
      the response in addition to the draft post's base fields. Base fields don’t include any of the supported fieldset values. By default
      only the draft post's base fields are returned.

      Constraints: `maxItems 10`.

      **Supported values:**

      - `URL` — Includes draft post preview URL.
      - `CONTENT` — Includes content field.
      - `RICH_CONTENT` — Includes rich content field.
      - `GENERATED_EXCERPT` — If the user has not set excerpt, returns the one autogenerated from content.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkDraftPostResult[]">

      Draft posts updated by bulk action.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk actions metadata for draft post.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="DraftPost">

          Optional full draft post.

          See the [Draft Object](/api-reference/business-solutions/blog/draft-posts/draft-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Draft Post Updated | `wix.blog.v3.draft_updated` | Triggered when a draft post is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X PATCH \
        'https://www.wixapis.com/blog/v3/draft-posts/update' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "draftPosts": [
                {
                  "draftPost": {
                    "id": "448d1238-0072-4458-a280-bf81c2dd8af1",
                    "seoSlug": "places-to-visit-in-europe"
                  }
                },
                {
                  "draftPost": {
                    "id": "d1c6418d-322b-4976-a815-80ee35c2a5e7",
                    "seoSlug": "places-to-visit-in-america"
                  }
                }
              ],
              "action": "UPDATE_PUBLISH"
            }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "results": [
          {
            "itemMetadata": {
              "id": "448d1238-0072-4458-a280-bf81c2dd8af1",
              "originalIndex": 0,
              "success": true
            }
          },
          {
            "itemMetadata": {
              "id": "d1c6418d-322b-4976-a815-80ee35c2a5e7",
              "originalIndex": 1,
              "success": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates multiple draft posts.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { draftPosts } from '@wix/blog';

    draftPosts.bulkUpdateDraftPosts(options): Promise<BulkUpdateDraftPostsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkUpdateDraftPostsOptions">

      Options for updating multiple draft posts.

      <Expandable title="options properties">

        <ParamField body="draftPosts" type="MaskedDraftPosts[]">

          Draft posts to update.

          Constraints: `minItems 1`, `maxItems 20`.

          <Expandable title="draftPosts properties">

            <ParamField body="draftPost" type="DraftPost" required>

              Draft post

              See the [Draft Object](/api-reference/business-solutions/blog/draft-posts/draft-object) for this object's fields.

            </ParamField>

            <ParamField body="fieldMask" type="string[]">

              Field mask of fields to update.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="action" type="string">

          Action to perform on the posts.

          Default: `UPDATE`

          **Supported values:**

          - `UPDATE` — Update the draft post.
          - `UPDATE_PUBLISH` — Update and publish a draft post.
          - `UPDATE_SCHEDULE` — Update the draft post and schedule a publish date.
          - `UPDATE_REVERT_TO_DRAFT` — Update the post and revert it to draft.
          - `UPDATE_CANCEL_SCHEDULE` — Update the draft post and cancel the scheduled publish date.
          - `UPDATE_REJECT` — Update and reject draft post.
          - `UPDATE_PUBLICATION` — Update a post that's already been published. This creates and updates a draft version of the post. The original post is still published.

        </ParamField>

        <ParamField body="scheduledPublishDate" type="datetime">

          Posts' scheduled publish date when `action` is set to `UPDATE_SCHEDULE`.

        </ParamField>

        <ParamField body="returnFullEntity" type="boolean">

          Whether to return the full updated draft post entities in the response.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          List of additional draft post fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
          the response in addition to the draft post's base fields. Base fields don’t include any of the supported fieldset values. By default
          only the draft post's base fields are returned.

          Constraints: `maxItems 10`.

          **Supported values:**

          - `URL` — Includes draft post preview URL.
          - `CONTENT` — Includes content field.
          - `RICH_CONTENT` — Includes rich content field.
          - `GENERATED_EXCERPT` — If the user has not set excerpt, returns the one autogenerated from content.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkUpdateDraftPostsResponse>`.

    <ResponseField name="results" type="BulkDraftPostResult[]">

      Draft posts updated by bulk action.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk actions metadata for draft post.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="DraftPost">

          Optional full draft post.

          See the [Draft Object](/api-reference/business-solutions/blog/draft-posts/draft-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Draft Updated | `wix.blog.v3.draft_updated` | Triggered when a draft post is updated. |
    | Draft Draft Post Owner Changed | `wix.blog.v3.draft_draft_post_owner_changed` | Triggered when a draft post changes owner. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-draft-posts" id="ex-sdk-bulk-update-draft-posts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-draft-posts" id="ex-sdk-bulk-update-draft-posts-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-draft-posts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-draft-posts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk update draft posts</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateDraftPosts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-draft-posts-0"><span className="wix-picker-name">Bulk update draft posts</span></label>
    <label htmlFor="ex-sdk-bulk-update-draft-posts-1"><span className="wix-picker-name">bulkUpdateDraftPosts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk update draft posts
    import { draftPosts } from '@wix/blog';

    /*  Sample options value: 
     * {
     *  "action": "UPDATE_PUBLISH",
     *  "draftPosts": [ 
     *    "draftPost": {
     *      "id": "448d1238-0072-4458-a280-bf81c2dd8af1",
     *      "title": "Hello, world"
     *     },
     *     "draftPost": {
     *       "id": "d1c6418d-322b-4976-a815-80ee35c2a5e7",
     *       "title": "Hello to you too"
     *     },
     *   ]
     * }
    */

    export async function myBulkUpdateDraftPosts(options) {
        try {
          const updatedDraftPosts = await draftPosts.bulkUpdateDraftPosts(options);
          console.log('Updated draft posts:', updatedDraftPosts)
          return updatedDraftPosts;
        } catch (error) {
          console.error(error);
          // Handle the error
        }
      };

    /* Promise resolves to:
      {
        "bulkActionMetadada": {
          "totalFailures": 0,
          "totalSuccesses": 2,
          "undetailedFailures": 0
        },
        "results": [
          "item": {
              "categoryIds": [],
            "commentingEnabled": true,
            "featured": true,
            "language": "en",
            "media": {
                "custom": false,
                "displayed": true
            },
            "memberId": "c00e8a5c-322b-4e77-8813-002e3ea7e811",
            "previewTextParagraph": 2,
            "pricingPlanIds": [],
            "relatedPostIds": [],
            "richContent": {
              "nodes": [
                 {
                   "type": "PARAGRAPH",
                   "id": "pvirv1",
                   "nodes": [
                     {
                       "type": "TEXT",
                       "id": "",
                       "nodes": [],
                       "textData": {
                         "text": "Hello world",
                         "decorations": []
                       }
                     }
                   ],
                   "paragraphData": {}
                 }
               ],
            }
            "seoData": {
                "tags": []
            "},
            "seoSlug": "hello-world",
            "tagIds": [],
            "title": "Hello, world"
          },
          "item": {
            "categoryIds": [],
            "commentingEnabled": true,
            "featured": true,
            "language": "en",
            "media": {
                "custom": false,
                "displayed": true
            },
            "memberId": "c00e8a5c-322b-4e77-8813-002e3ea7e811",
            "previewTextParagraph": 2,
            "pricingPlanIds": [],
            "relatedPostIds": [],
            "richContent": {
              "nodes": [
                 {
                   "type": "PARAGRAPH",
                   "id": "pvirv1",
                   "nodes": [
                     {
                       "type": "TEXT",
                       "id": "",
                       "nodes": [],
                       "textData": {
                         "text": "Hello to you too",
                         "decorations": []
                       }
                     }
                   ],
                   "paragraphData": {}
                 }
               ],
            }
            "seoData": {
                "tags": []
            "},
            "seoSlug": "hello-to-you-too",
            "tagIds": [],
            "title": "Hello to you too"
          }
        ] 
      }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateDraftPosts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { draftPosts } from '@wix/blog';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { draftPosts },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdateDraftPosts(options) {
      const response = await myWixClient.draftPosts.bulkUpdateDraftPosts(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
