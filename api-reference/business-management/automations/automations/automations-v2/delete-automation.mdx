---
title: "Delete Automation"
description: "Deletes the specified automation."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the specified automation.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/automations-service/v2/automations/{automationId}
    ```

    ## Path Params

    <ParamField path="automationId" type="string" required>

      Automation ID.

    </ParamField>

    ## Response Object

    Returns `DeleteAutomationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Automation Deleted | `wix.automations.v2.automation_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE 'https://manage.wix.com/automations-service/v2/automations/6b5db1de-5d64-47f8-b7b2-087ff3b7217f' \
      -H 'authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the specified automation.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { automationsV2 } from '@wix/automations';

    automationsV2.deleteAutomation(automationId): Promise
    ```

    ## Method Parameters

    <ParamField body="automationId" type="string" required>

      Automation ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteAutomationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Automation Deleted | `wix.automations.v2.automation_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-automation" id="ex-sdk-delete-automation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-automation" id="ex-sdk-delete-automation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-automation" id="ex-sdk-delete-automation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-automation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-automation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteAutomation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAutomation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAutomation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-automation-0"><span className="wix-picker-name">deleteAutomation</span></label>
    <label htmlFor="ex-sdk-delete-automation-1"><span className="wix-picker-name">deleteAutomation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-automation-2"><span className="wix-picker-name">deleteAutomation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteAutomation
    import { automationsV2 } from '@wix/automations';

    async function deleteAutomation(automationId) {
      const response = await automationsV2.deleteAutomation(automationId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAutomation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { automationsV2 } from '@wix/automations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { automationsV2 },
      // Include the auth strategy and host as relevant
    });

    async function deleteAutomation(automationId) {
      const response = await myWixClient.automationsV2.deleteAutomation(automationId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAutomation (with elevated permissions)
    import { automationsV2 } from '@wix/automations';
    import { auth } from '@wix/essentials';

    async function myDeleteAutomationMethod(automationId) {
      const elevatedDeleteAutomation = auth.elevate(automationsV2.deleteAutomation);
      const response = await elevatedDeleteAutomation(automationId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
