---
title: "Delete Manual Tax Mapping"
description: "Deletes a manual tax mapping."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a manual tax mapping.

    This permanently removes the custom tax rate mapping.
    After deletion, no tax will be calculated for the associated tax group and region combination.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/billing/v1/manual-tax-mappings/{manualTaxMappingId}
    ```

    ## Path Params

    <ParamField path="manualTaxMappingId" type="string" required>

      Manual tax mapping ID.

    </ParamField>

    ## Response Object

    Returns `DeleteManualTaxMappingResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Manual Tax Mapping Deleted | `wix.billing.v1.manual_tax_mapping_deleted` | Triggered when a manual tax mapping is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes tax mapping by its ID

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/billing/v1/manual-tax-mappings/6611fcb8-3eba-452d-8d25-597571cf0153' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a manual tax mapping.

    This permanently removes the custom tax rate mapping.
    After deletion, no tax will be calculated for the associated tax group and region combination.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { manualTaxMappings } from '@wix/billing';

    manualTaxMappings.deleteManualTaxMapping(manualTaxMappingId): Promise
    ```

    ## Method Parameters

    <ParamField body="manualTaxMappingId" type="string" required>

      Manual tax mapping ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteManualTaxMappingResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Manual Tax Mapping Deleted | `wix.billing.v1.manual_tax_mapping_deleted` | Triggered when a manual tax mapping is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-manual-tax-mapping" id="ex-sdk-delete-manual-tax-mapping-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-manual-tax-mapping" id="ex-sdk-delete-manual-tax-mapping-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-manual-tax-mapping-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-manual-tax-mapping-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete Manual Tax Mapping</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteManualTaxMapping (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-manual-tax-mapping-0"><span className="wix-picker-name">Delete Manual Tax Mapping</span></label>
    <label htmlFor="ex-sdk-delete-manual-tax-mapping-1"><span className="wix-picker-name">deleteManualTaxMapping (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete Manual Tax Mapping
    import { manualTaxMappings } from "@wix/billing";

    async function deleteManualTaxMapping() {
      await manualTaxMappings.deleteManualTaxMapping(
        "c3d4e5f6-a7b8-9c0d-1e2f-a3b4c5d6e7f8",
      );
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteManualTaxMapping (self-hosted)
    import { createClient } from '@wix/sdk';
    import { manualTaxMappings } from '@wix/billing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { manualTaxMappings },
      // Include the auth strategy and host as relevant
    });

    async function deleteManualTaxMapping(manualTaxMappingId) {
      const response = await myWixClient.manualTaxMappings.deleteManualTaxMapping(manualTaxMappingId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
