---
title: "Update Instructor"
description: "Updates an instructor, supports partial update"
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    Updates an instructor, supports partial update

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/_api/instructors-service/v2/instructors/{instructor.id}
    ```

    ## Path Params

    <ParamField path="instructor.id" type="string" required>

      ID of the instructor that equals to memberId

    </ParamField>

    ## Body Params

    <ParamField body="instructor" type="Instructor" required>

      Instructor to be updated, may be partial

      See the [Instructor Object](/api-reference/business-management/online-programs/instructor-v2/instructor-object) for this object's fields.

    </ParamField>

    <ParamField body="actionId" type="string">

      trace id used by BI.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    <ResponseField name="instructor" type="Instructor">

      Updated instructor

      See the [Instructor Object](/api-reference/business-management/online-programs/instructor-v2/instructor-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `403` | `PERMISSION_DENIED` | `INVALID_IDENTITY` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates an instructor, supports partial update

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { instructors } from '@wix/online-programs';

    instructors.updateInstructor(_id, instructor, options): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the instructor that equals to memberId

      **Read-only.**

    </ParamField>

    <ParamField body="instructor" type="UpdateInstructor" required>

      Instructor to be updated, may be partial

      <Expandable title="instructor properties">

        <ParamField body="_id" type="string">

          ID of the instructor that equals to memberId

          **Read-only.**

        </ParamField>

        <ParamField body="userId" type="string">

          User ID for real instructors (will be filled only for real instructors)

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          The name of the instructor

        </ParamField>

        <ParamField body="description" type="string">

          A description of the instructor

        </ParamField>

        <ParamField body="photo" type="Image">

          URL of profile photo

          <Expandable title="photo properties">

            <ParamField body="_id" type="string">

              Wix Media image ID,
              set when the member selects an image from Wix Media.

            </ParamField>

            <ParamField body="url" type="string">

              Image URL.

            </ParamField>

            <ParamField body="height" type="integer">

              Original image width.

            </ParamField>

            <ParamField body="width" type="integer">

              Original image height.

            </ParamField>

            <ParamField body="offsetX" type="integer">

              X-axis offset.

              Default: `0`.

            </ParamField>

            <ParamField body="offsetY" type="integer">

              Y-axis offset.

              Default: `0`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="photoAltText" type="string">

          Alt text for profile photo (needed for a11y)

        </ParamField>

        <ParamField body="slug" type="string">

          Slug that determines the instructor's profile page URL.

          **Read-only.**

        </ParamField>

        <ParamField body="role" type="string">

          Role of the instructor

          **Read-only.**

          **Supported values:**

          - `DEFAULT` — VIRTUAL instructor
          - `OWNER` — REAL instructor who is the owner of the site
          - `INSTRUCTOR` — REAL instructor who has access to specified list of programs
          - `MANAGER` — REAL instructor who has the same rights as program owner

        </ParamField>

        <ParamField body="programIds" type="string[]">

          IDs of programs instructor is assigned to

          **Read-only.** Constraints: `format GUID`, `maxItems 100`.

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time when the instructor was created

          **Read-only.**

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateInstructorOptions">

      options

      <Expandable title="options properties">

        <ParamField body="actionId" type="string">

          trace id used by BI.

          Constraints: `format GUID`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Instructor`.

    See the [Instructor Object](/api-reference/business-management/online-programs/instructor-v2/instructor-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `403` | `PERMISSION_DENIED` | `INVALID_IDENTITY` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Instructor Updated | `wix.achievements.instructors.v2.instructor_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-instructor" id="ex-sdk-update-instructor-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-instructor" id="ex-sdk-update-instructor-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-instructor" id="ex-sdk-update-instructor-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-instructor-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-instructor-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateInstructor</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateInstructor (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateInstructor (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-instructor-0"><span className="wix-picker-name">updateInstructor</span></label>
    <label htmlFor="ex-sdk-update-instructor-1"><span className="wix-picker-name">updateInstructor (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-instructor-2"><span className="wix-picker-name">updateInstructor (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateInstructor
    import { instructors } from '@wix/online-programs';

    async function updateInstructor(_id,instructor,options) {
      const response = await instructors.updateInstructor(_id,instructor,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateInstructor (self-hosted)
    import { createClient } from '@wix/sdk';
    import { instructors } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { instructors },
      // Include the auth strategy and host as relevant
    });

    async function updateInstructor(_id,instructor,options) {
      const response = await myWixClient.instructors.updateInstructor(_id,instructor,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateInstructor (with elevated permissions)
    import { instructors } from '@wix/online-programs';
    import { auth } from '@wix/essentials';

    async function myUpdateInstructorMethod(_id,instructor,options) {
      const elevatedUpdateInstructor = auth.elevate(instructors.updateInstructor);
      const response = await elevatedUpdateInstructor(_id,instructor,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
