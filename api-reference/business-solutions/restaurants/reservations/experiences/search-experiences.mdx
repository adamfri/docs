---
title: "Search Experiences"
description: "Use this method to search the fields of the experiences on a site for a given expression."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Use this method to search the fields of the experiences on a site for a given expression.

    You can also use this method to perform data aggregations on a site's experience fields.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `configuration.onlineReservations.businessSchedule.entries.oneTimeOptions.startDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.oneTimeOptions.startTime` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.recurrence` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.endDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.startDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.startDaysAndTimes.day` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.startDaysAndTimes.time` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `tags.privateTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `tags.publicTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `reservationLocationId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.displayInfo.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `configuration.onlineReservations.approval.mode` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `configuration.onlineReservations.businessSchedule.durationInMinutes` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.onlineReservations.maxGuests.number` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.onlineReservations.partySize.max` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.onlineReservations.partySize.min` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.paymentPolicy.paymentPolicyType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `configuration.visible` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `configuration.displayInfo.richContent` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `configuration.displayInfo.shortDescription` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/table-reservations/experiences/v1/experiences/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search query.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

          Constraints: `maxItems 100`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string" />

            <ParamField body="type" type="string">

              **Supported values:**

              - `UNKNOWN_AGGREGATION_TYPE` — Undefined aggregation type.
              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation - such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built - one per time interval such as hour, day, or week.
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Constraints: `maxLength 1000`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least one of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 1000`, `maxItems 100`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
          Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="experiences" type="Experience[]">

      List of Experiences.

      See the [Experience Object](/api-reference/business-solutions/restaurants/reservations/experiences/experience-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          List of the aggregated data results.

          Constraints: `maxItems 1000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              Aggregation name defined in the request.

              Constraints: `maxLength 1000`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that was performed.

              **Supported values:**

              - `UNKNOWN_AGGREGATION_TYPE` — Undefined aggregation type.
              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation - such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built - one per time interval such as hour, day, or week.
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field the data was aggregated by.

              Constraints: `maxLength 1000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Use this method to search the fields of the experiences on a site for a given expression.

    You can also use this method to perform data aggregations on a site's experience fields.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `configuration.onlineReservations.businessSchedule.entries.oneTimeOptions.startDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.oneTimeOptions.startTime` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.recurrence` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.endDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.startDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.startDaysAndTimes.day` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `configuration.onlineReservations.businessSchedule.entries.weeklyOptions.startDaysAndTimes.time` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `tags.privateTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `tags.publicTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `reservationLocationId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.displayInfo.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `configuration.onlineReservations.approval.mode` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `configuration.onlineReservations.businessSchedule.durationInMinutes` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.onlineReservations.maxGuests.number` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.onlineReservations.partySize.max` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.onlineReservations.partySize.min` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `configuration.paymentPolicy.paymentPolicyType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `configuration.visible` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `configuration.displayInfo.richContent` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `configuration.displayInfo.shortDescription` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { experiences } from '@wix/table-reservations';

    experiences.searchExperiences(search): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search query.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

          Constraints: `maxItems 100`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string" />

            <ParamField body="type" type="string">

              **Supported values:**

              - `UNKNOWN_AGGREGATION_TYPE` — Undefined aggregation type.
              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation - such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built - one per time interval such as hour, day, or week.
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Constraints: `maxLength 1000`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least one of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 1000`, `maxItems 100`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
          Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="experiences" type="Experience[]">

      List of Experiences.

      See the [Experience Object](/api-reference/business-solutions/restaurants/reservations/experiences/experience-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          List of the aggregated data results.

          Constraints: `maxItems 1000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              Aggregation name defined in the request.

              Constraints: `maxLength 1000`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that was performed.

              **Supported values:**

              - `UNKNOWN_AGGREGATION_TYPE` — Undefined aggregation type.
              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation - such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built - one per time interval such as hour, day, or week.
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field the data was aggregated by.

              Constraints: `maxLength 1000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-experiences" id="ex-sdk-search-experiences-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-experiences" id="ex-sdk-search-experiences-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-experiences-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-experiences-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">searchExperiences</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchExperiences (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-experiences-0"><span className="wix-picker-name">searchExperiences</span></label>
    <label htmlFor="ex-sdk-search-experiences-1"><span className="wix-picker-name">searchExperiences (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript searchExperiences
    import { experiences } from '@wix/table-reservations';

    async function searchExperiences(search) {
      const response = await experiences.searchExperiences(search);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchExperiences (self-hosted)
    import { createClient } from '@wix/sdk';
    import { experiences } from '@wix/table-reservations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { experiences },
      // Include the auth strategy and host as relevant
    });

    async function searchExperiences(search) {
      const response = await myWixClient.experiences.searchExperiences(search);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
