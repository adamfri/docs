---
title: "Sample Use Cases and Flows"
description: "This article presents possible use cases and corresponding sample flows that you can support."
icon: "file-lines"
---

<Note>This article applies to the REST API only.</Note>

This article presents possible use cases and corresponding sample flows that you can support. It provides a useful starting point as you plan your implementation.

## Sync pizza menu across all sites

If you have several pizzeria sites, you might need to sync menus across those sites. For example, if there is a new label for the pizza called "Spicy (Level 3)", this change also needs to be reflected on other sites.

To sync the pizza menu across all sites:

1. Call [Query Labels](https://dev.wix.com/docs/rest/business-solutions/restaurants/menus/item-label/query-labels) by the `name` field on the site where you updated the menu.
1. Extract the, `labels.id`, `labels.name` and `labels.icon` field values.
1. Call [Create Label](https://dev.wix.com/docs/rest/business-solutions/restaurants/menus/item-label/create-label) for your other sites and pass the new item label to each.
1. Call [Update Item](https://dev.wix.com/docs/rest/business-solutions/restaurants/menus/item/update-item) and add the new label ID to the `item.labels` array.
