---
title: "onFileDescriptorUpdated"
description: "Triggered when a file is updated, including when a file is moved to a different folder."
icon: "bolt"
---

Triggered when a file is updated, including when a file is moved to a different folder.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.media.site_media.v1.file_descriptor_updated` |
| Package | `@wix/media` |
| Permissions | `Manage Bookings Services and Settings`, `Manage Events`, `Manage Challenges`, `Read Media Manager`, `Manage Media Manager`, `Manage Portfolio`, `Access Verticals by Automations`, `Manage Restaurants - all permissions`, `Set Up Automations`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onFileDescriptorUpdated
  import { files } from '@wix/media';

  files.onFileDescriptorUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onFileDescriptorUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { files } from '@wix/media';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        files,
      },
  });

  wixClient.files.onFileDescriptorUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
