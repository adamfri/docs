---
title: "Properties Object"
description: "The Properties object in the Wix Properties API."
icon: "cube"
---

<Tabs>

  <Tab title="REST">

    <ResponseField name="categories" type="Categories">

      Site categories.

      <Expandable title="categories properties">

        <ResponseField name="primary" type="string">

          Primary site category.

        </ResponseField>

        <ResponseField name="secondary" type="string[]">

          Secondary site category.

          Constraints: `maxItems 50`.

        </ResponseField>

        <ResponseField name="businessTermId" type="string">

          Business Term Id

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="locale" type="Locale">

      Site locale.

      <Expandable title="locale properties">

        <ResponseField name="languageCode" type="string">

          Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

        </ResponseField>

        <ResponseField name="country" type="string">

          Two-letter country code in [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements) format.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="language" type="string">

      Site language.

      Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

    </ResponseField>

    <ResponseField name="paymentCurrency" type="string">

      Site currency format used to bill customers.

      Three-letter currency code in [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format.

    </ResponseField>

    <ResponseField name="timeZone" type="string">

      Timezone in `America/New_York` format.

    </ResponseField>

    <ResponseField name="email" type="string">

      Email address.

    </ResponseField>

    <ResponseField name="phone" type="string">

      Phone number.

    </ResponseField>

    <ResponseField name="fax" type="string">

      Fax number.

    </ResponseField>

    <ResponseField name="address" type="Address">

      Address.

      <Expandable title="address properties">

        <ResponseField name="street" type="string">

          Street name.

        </ResponseField>

        <ResponseField name="city" type="string">

          City name.

        </ResponseField>

        <ResponseField name="country" type="string">

          Two-letter country code in an [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) format.

        </ResponseField>

        <ResponseField name="state" type="string">

          State.

        </ResponseField>

        <ResponseField name="zip" type="string">

          Zip or postal code.

          Constraints: `maxLength 20`.

        </ResponseField>

        <ResponseField name="hint" type="AddressHint">

          Extra information to be displayed in the address.

          <Expandable title="hint properties">

            <ResponseField name="text" type="string">

              Extra text displayed next to, or instead of, the actual address.

            </ResponseField>

            <ResponseField name="placement" type="string">

              Where the extra text should be displayed.

              **Supported values:**

              - `BEFORE`
              - `AFTER`
              - `REPLACE`

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="isPhysical" type="boolean">

          Whether this address represents a physical location.

        </ResponseField>

        <ResponseField name="googleFormattedAddress" type="string">

          Google-formatted version of this address.

        </ResponseField>

        <ResponseField name="streetNumber" type="string">

          Street number.

        </ResponseField>

        <ResponseField name="apartmentNumber" type="string">

          Apartment number.

        </ResponseField>

        <ResponseField name="coordinates" type="GeoCoordinates">

          Geographic coordinates of location.

          <Expandable title="coordinates properties">

            <ResponseField name="latitude" type="number">

              Latitude of the location. Must be between -90 and 90.

            </ResponseField>

            <ResponseField name="longitude" type="number">

              Longitude of the location. Must be between -180 and 180.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="siteDisplayName" type="string">

      Site display name.

    </ResponseField>

    <ResponseField name="businessName" type="string">

      Business name.

    </ResponseField>

    <ResponseField name="logo" type="string">

      Path to the site's logo in Wix Media (without Wix Media base URL).

    </ResponseField>

    <ResponseField name="description" type="string">

      Site description.

    </ResponseField>

    <ResponseField name="businessSchedule" type="BusinessSchedule">

      Business schedule. Regular and exceptional time periods when the business is open or the service is available.

      __Note:__ Not supported by Wix Bookings.

      <Expandable title="businessSchedule properties">

        <ResponseField name="periods" type="TimePeriod[]">

          Weekly recurring time periods when the business is regularly open or the service is available. Limited to 100 time periods.

          Constraints: `maxItems 100`.

          <Expandable title="periods properties">

            <ResponseField name="openDay" type="string">

              Day of the week the period starts on.

              **Supported values:**

              - `MONDAY`
              - `TUESDAY`
              - `WEDNESDAY`
              - `THURSDAY`
              - `FRIDAY`
              - `SATURDAY`
              - `SUNDAY`

            </ResponseField>

            <ResponseField name="openTime" type="string">

              Time the period starts in 24-hour [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) extended format. Valid values are `00:00` to `24:00`, where `24:00` represents
              midnight at the end of the specified day.

            </ResponseField>

            <ResponseField name="closeDay" type="string">

              Day of the week the period ends on.

              **Supported values:**

              - `MONDAY`
              - `TUESDAY`
              - `WEDNESDAY`
              - `THURSDAY`
              - `FRIDAY`
              - `SATURDAY`
              - `SUNDAY`

            </ResponseField>

            <ResponseField name="closeTime" type="string">

              Time the period ends in 24-hour [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) extended format. Valid values are `00:00` to `24:00`, where `24:00` represents
              midnight at the end of the specified day.

              __Note:__ If `openDay` and `closeDay` specify the same day of the week `closeTime` must be later than `openTime`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="specialHourPeriod" type="SpecialHourPeriod[]">

          Exceptions to the business's regular hours. The business can be open or closed during the exception.

          Constraints: `maxItems 100`.

          <Expandable title="specialHourPeriod properties">

            <ResponseField name="startDate" type="string">

              Start date and time of the exception in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format and [Coordinated Universal Time (UTC)](https://en.wikipedia.org/wiki/Coordinated_Universal_Time).

            </ResponseField>

            <ResponseField name="endDate" type="string">

              End date and time of the exception in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format and [Coordinated Universal Time (UTC)](https://en.wikipedia.org/wiki/Coordinated_Universal_Time).

            </ResponseField>

            <ResponseField name="isClosed" type="boolean">

              Whether the business is closed (or the service is not available) during the exception.

              Default: `true`.

            </ResponseField>

            <ResponseField name="comment" type="string">

              Additional info about the exception. For example, "We close earlier on New Year's Eve."

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="multilingual" type="Multilingual">

      Supported languages of a site and the primary language.

      <Expandable title="multilingual properties">

        <ResponseField name="supportedLanguages" type="SupportedLanguage[]">

          Supported languages list.

          Constraints: `maxItems 200`.

          <Expandable title="supportedLanguages properties">

            <ResponseField name="languageCode" type="string">

              Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

            </ResponseField>

            <ResponseField name="locale" type="Locale">

              Locale.

              <Expandable title="locale properties">

                <ResponseField name="languageCode" type="string">

                  Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

                </ResponseField>

                <ResponseField name="country" type="string">

                  Two-letter country code in [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements) format.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="isPrimary" type="boolean">

              Whether the supported language is the primary language for the site.

            </ResponseField>

            <ResponseField name="countryCode" type="string">

              Language icon.

            </ResponseField>

            <ResponseField name="resolutionMethod" type="string">

              How the language will be resolved. For internal use.

              **Supported values:**

              - `QUERY_PARAM`
              - `SUBDOMAIN`
              - `SUBDIRECTORY`

            </ResponseField>

            <ResponseField name="isVisitorPrimary" type="boolean">

              Whether the supported language is the primary language for site visitors.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="autoRedirect" type="boolean">

          Whether to redirect to user language.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="consentPolicy" type="ConsentPolicy">

      Cookie policy the Wix user defined for their site (before the site visitor interacts with/limits it).

      <Expandable title="consentPolicy properties">

        <ResponseField name="essential" type="boolean">

          Whether the site uses cookies that are essential to site operation. Always `true`.

        </ResponseField>

        <ResponseField name="functional" type="boolean">

          Whether the site uses cookies that affect site performance and other functional measurements.

        </ResponseField>

        <ResponseField name="analytics" type="boolean">

          Whether the site uses cookies that collect analytics about how the site is used (in order to improve it).

        </ResponseField>

        <ResponseField name="advertising" type="boolean">

          Whether the site uses cookies that collect information allowing better customization of the experience for a current visitor.

        </ResponseField>

        <ResponseField name="dataToThirdParty" type="boolean">

          CCPA compliance flag.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="businessConfig" type="string">

      Supported values: `FITNESS SERVICE`, `RESTAURANT`, `BLOG`, `STORE`, `EVENT`, `UNKNOWN`.

      Site business type.

    </ResponseField>

    <ResponseField name="externalSiteUrl" type="string">

      External site URL that uses Wix as its headless business solution.

    </ResponseField>

    <ResponseField name="trackClicksAnalytics" type="boolean">

      Track clicks analytics.

    </ResponseField>

    <ResponseField name="companyId" type="string">

      Company ID issued to the business by local authorities. Appears on invoices and price quotes.

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="categories" type="Categories">

      Site categories.

      <Expandable title="categories properties">

        <ResponseField name="primary" type="string">

          Primary site category.

        </ResponseField>

        <ResponseField name="secondary" type="string[]">

          Secondary site category.

          Constraints: `maxItems 50`.

        </ResponseField>

        <ResponseField name="businessTermId" type="string">

          Business Term Id

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="locale" type="Locale">

      Site locale.

      <Expandable title="locale properties">

        <ResponseField name="languageCode" type="string">

          Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

        </ResponseField>

        <ResponseField name="country" type="string">

          Two-letter country code in [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements) format.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="language" type="string">

      Site language.

      Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

    </ResponseField>

    <ResponseField name="paymentCurrency" type="string">

      Site currency format used to bill customers.

      Three-letter currency code in [ISO-4217 alphabetic](https://en.wikipedia.org/wiki/ISO_4217#Active_codes) format.

    </ResponseField>

    <ResponseField name="timeZone" type="string">

      Timezone in `America/New_York` format.

    </ResponseField>

    <ResponseField name="email" type="string">

      Email address.

    </ResponseField>

    <ResponseField name="phone" type="string">

      Phone number.

    </ResponseField>

    <ResponseField name="fax" type="string">

      Fax number.

    </ResponseField>

    <ResponseField name="address" type="Address">

      Address.

      <Expandable title="address properties">

        <ResponseField name="street" type="string">

          Street name.

        </ResponseField>

        <ResponseField name="city" type="string">

          City name.

        </ResponseField>

        <ResponseField name="country" type="string">

          Two-letter country code in an [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) format.

        </ResponseField>

        <ResponseField name="state" type="string">

          State.

        </ResponseField>

        <ResponseField name="zip" type="string">

          Zip or postal code.

          Constraints: `maxLength 20`.

        </ResponseField>

        <ResponseField name="hint" type="AddressHint">

          Extra information to be displayed in the address.

          <Expandable title="hint properties">

            <ResponseField name="text" type="string">

              Extra text displayed next to, or instead of, the actual address.

            </ResponseField>

            <ResponseField name="placement" type="string">

              Where the extra text should be displayed.

              **Supported values:**

              - `BEFORE`
              - `AFTER`
              - `REPLACE`

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="isPhysical" type="boolean">

          Whether this address represents a physical location.

        </ResponseField>

        <ResponseField name="googleFormattedAddress" type="string">

          Google-formatted version of this address.

        </ResponseField>

        <ResponseField name="streetNumber" type="string">

          Street number.

        </ResponseField>

        <ResponseField name="apartmentNumber" type="string">

          Apartment number.

        </ResponseField>

        <ResponseField name="coordinates" type="GeoCoordinates">

          Geographic coordinates of location.

          <Expandable title="coordinates properties">

            <ResponseField name="latitude" type="number">

              Latitude of the location. Must be between -90 and 90.

            </ResponseField>

            <ResponseField name="longitude" type="number">

              Longitude of the location. Must be between -180 and 180.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="siteDisplayName" type="string">

      Site display name.

    </ResponseField>

    <ResponseField name="businessName" type="string">

      Business name.

    </ResponseField>

    <ResponseField name="logo" type="string">

      Path to the site's logo in Wix Media (without Wix Media base URL).

    </ResponseField>

    <ResponseField name="description" type="string">

      Site description.

    </ResponseField>

    <ResponseField name="businessSchedule" type="BusinessSchedule">

      Business schedule. Regular and exceptional time periods when the business is open or the service is available.

      __Note:__ Not supported by Wix Bookings.

      <Expandable title="businessSchedule properties">

        <ResponseField name="periods" type="TimePeriod[]">

          Weekly recurring time periods when the business is regularly open or the service is available. Limited to 100 time periods.

          Constraints: `maxItems 100`.

          <Expandable title="periods properties">

            <ResponseField name="openDay" type="string">

              Day of the week the period starts on.

              **Supported values:**

              - `MONDAY`
              - `TUESDAY`
              - `WEDNESDAY`
              - `THURSDAY`
              - `FRIDAY`
              - `SATURDAY`
              - `SUNDAY`

            </ResponseField>

            <ResponseField name="openTime" type="string">

              Time the period starts in 24-hour [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) extended format. Valid values are `00:00` to `24:00`, where `24:00` represents
              midnight at the end of the specified day.

            </ResponseField>

            <ResponseField name="closeDay" type="string">

              Day of the week the period ends on.

              **Supported values:**

              - `MONDAY`
              - `TUESDAY`
              - `WEDNESDAY`
              - `THURSDAY`
              - `FRIDAY`
              - `SATURDAY`
              - `SUNDAY`

            </ResponseField>

            <ResponseField name="closeTime" type="string">

              Time the period ends in 24-hour [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) extended format. Valid values are `00:00` to `24:00`, where `24:00` represents
              midnight at the end of the specified day.

              __Note:__ If `openDay` and `closeDay` specify the same day of the week `closeTime` must be later than `openTime`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="specialHourPeriod" type="SpecialHourPeriod[]">

          Exceptions to the business's regular hours. The business can be open or closed during the exception.

          Constraints: `maxItems 100`.

          <Expandable title="specialHourPeriod properties">

            <ResponseField name="startDate" type="string">

              Start date and time of the exception in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format and [Coordinated Universal Time (UTC)](https://en.wikipedia.org/wiki/Coordinated_Universal_Time).

            </ResponseField>

            <ResponseField name="endDate" type="string">

              End date and time of the exception in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format and [Coordinated Universal Time (UTC)](https://en.wikipedia.org/wiki/Coordinated_Universal_Time).

            </ResponseField>

            <ResponseField name="isClosed" type="boolean">

              Whether the business is closed (or the service is not available) during the exception.

              Default: `true`.

            </ResponseField>

            <ResponseField name="comment" type="string">

              Additional info about the exception. For example, "We close earlier on New Year's Eve."

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="multilingual" type="Multilingual">

      Supported languages of a site and the primary language.

      <Expandable title="multilingual properties">

        <ResponseField name="supportedLanguages" type="SupportedLanguage[]">

          Supported languages list.

          Constraints: `maxItems 200`.

          <Expandable title="supportedLanguages properties">

            <ResponseField name="languageCode" type="string">

              Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

            </ResponseField>

            <ResponseField name="locale" type="Locale">

              Locale.

              <Expandable title="locale properties">

                <ResponseField name="languageCode" type="string">

                  Two-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.

                </ResponseField>

                <ResponseField name="country" type="string">

                  Two-letter country code in [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements) format.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="isPrimary" type="boolean">

              Whether the supported language is the primary language for the site.

            </ResponseField>

            <ResponseField name="countryCode" type="string">

              Language icon.

            </ResponseField>

            <ResponseField name="resolutionMethod" type="string">

              How the language will be resolved. For internal use.

              **Supported values:**

              - `QUERY_PARAM`
              - `SUBDOMAIN`
              - `SUBDIRECTORY`

            </ResponseField>

            <ResponseField name="isVisitorPrimary" type="boolean">

              Whether the supported language is the primary language for site visitors.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="autoRedirect" type="boolean">

          Whether to redirect to user language.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="consentPolicy" type="ConsentPolicy">

      Cookie policy the Wix user defined for their site (before the site visitor interacts with/limits it).

      <Expandable title="consentPolicy properties">

        <ResponseField name="essential" type="boolean">

          Whether the site uses cookies that are essential to site operation. Always `true`.

        </ResponseField>

        <ResponseField name="functional" type="boolean">

          Whether the site uses cookies that affect site performance and other functional measurements.

        </ResponseField>

        <ResponseField name="analytics" type="boolean">

          Whether the site uses cookies that collect analytics about how the site is used (in order to improve it).

        </ResponseField>

        <ResponseField name="advertising" type="boolean">

          Whether the site uses cookies that collect information allowing better customization of the experience for a current visitor.

        </ResponseField>

        <ResponseField name="dataToThirdParty" type="boolean">

          CCPA compliance flag.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="businessConfig" type="string">

      Supported values: `FITNESS SERVICE`, `RESTAURANT`, `BLOG`, `STORE`, `EVENT`, `UNKNOWN`.

      Site business type.

    </ResponseField>

    <ResponseField name="externalSiteUrl" type="string">

      External site URL that uses Wix as its headless business solution.

    </ResponseField>

    <ResponseField name="trackClicksAnalytics" type="boolean">

      Track clicks analytics.

    </ResponseField>

    <ResponseField name="companyId" type="string">

      Company ID issued to the business by local authorities. Appears on invoices and price quotes.

    </ResponseField>

  </Tab>

</Tabs>
