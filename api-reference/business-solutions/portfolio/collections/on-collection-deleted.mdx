---
title: "onCollectionDeleted"
description: "Triggered when a collection is deleted."
icon: "bolt"
---

Triggered when a collection is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.collections.v1.collection_deleted` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onCollectionDeleted
  import { collections } from '@wix/portfolio';

  collections.onCollectionDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onCollectionDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { collections } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        collections,
      },
  });

  wixClient.collections.onCollectionDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
