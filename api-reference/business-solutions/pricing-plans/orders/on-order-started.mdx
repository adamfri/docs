---
title: "onOrderStarted"
description: "Triggered when an order reaches its startDate."
icon: "bolt"
---

Triggered when an order reaches its `startDate`. Applies to both purchased and free orders.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.v2.order_started` |
| Package | `@wix/pricing-plans` |
| Permissions | `Manage Orders`, `Read Orders`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onOrderStarted
  import { orders } from '@wix/pricing-plans';

  orders.onOrderStarted((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderStarted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderStarted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
