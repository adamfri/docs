---
title: "Set Default Location"
description: "Sets a new default location."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets a new default location.

    <Note>

    + There can only be one default location per site.
    + The default location can't be archived.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/locations/v1/locations/{id}/set-default
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the location to set as the default location.

    </ParamField>

    ## Response Object

    <ResponseField name="location" type="Location">

      New default location.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_PERFORM_ACTION_ON_ARCHIVED_LOCATION` | Can't perform this action on an archived location. |
    | `400` | `INVALID_ARGUMENT` | `DEFAULT_NOT_CHANGED` | Location is already the default location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Set Default Location | `wix.locations.v1.location_set_default_location` | Triggered when a location is set as default. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/locations/v1/locations/ea50573e-35fa-47ca-bba7-3e45a935d9bc/set-default' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{}'
      ```

      ```json Response
      {
        "location": {
           "id":"ea50573e-35fa-47ca-bba7-3e45a935d9bc",
           "name":"New Location Name",
           "default":true,
           "status":"ACTIVE",
           "locationType":"UNKNOWN",
           "timeZone":"America/Los_Angeles",
           "address":{
              "country":"US",
              "subdivision":"CA",
              "city":"Los Angeles",
              "postalCode":"90015",
              "streetAddress":{
                 "number":"1111",
                 "name":"South Figueroa Street",
                 "apt":""
              },
              "formattedAddress":"STAPLES Center, South Figueroa Street, Los Angeles, CA, USA",
              "geocode":{
                 "latitude":34.0430175,
                 "longitude":-118.2672541
              }
           },
           "revision":"6",
           "archived":false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets a new default location.

    <Note>

    + There can only be one default location per site.
    + The default location can't be archived.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/business-tools';

    locations.setDefaultLocation(_id): Promise<SetDefaultLocationResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the location to set as the default location.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<SetDefaultLocationResponse>`.

    <ResponseField name="location" type="Location">

      New default location.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_PERFORM_ACTION_ON_ARCHIVED_LOCATION` | Can't perform this action on an archived location. |
    | `400` | `INVALID_ARGUMENT` | `DEFAULT_NOT_CHANGED` | Location is already the default location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Location Set Default Location | `wix.locations.v1.location_set_default_location` | Triggered when a location is set as default. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-set-default-location" id="ex-sdk-set-default-location-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-set-default-location" id="ex-sdk-set-default-location-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-set-default-location" id="ex-sdk-set-default-location-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-set-default-location-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-set-default-location-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Set the default location</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Set the default location (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">setDefaultLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-set-default-location-0"><span className="wix-picker-name">Set the default location</span></label>
    <label htmlFor="ex-sdk-set-default-location-1"><span className="wix-picker-name">Set the default location (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-set-default-location-2"><span className="wix-picker-name">setDefaultLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Set the default location
    import { locations } from '@wix/business-tools';

    /* Sample _id value: '0a965e36-4071-4df0-905b-75458817430a' */

    export async function changeDefaultLocation(_id) {
      try {
        const myDefaultLocation = await locations.setDefaultLocation(_id);
        console.log('Default location has been changed to:', myDefaultLocation);

        return myDefaultLocation;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   location: {
     *     "_id": "0a965e36-4071-4df0-905b-75458817430a",
     *     "address": {
     *       "streetAddress": {
     *         "apt": "34",
     *         "name": "Musterstraße",
     *         "number": "5"
     *       },
     *       "city": "Berlin",
     *       "postalCode": "53782"
     *     },
     *     "archived": false,
     *     "default": true,
     *     "description": "Our brand new store in Germany",
     *     "email": "store@example.com",
     *     "name": "Germany Store",
     *     "phone": "0208 209 9087",
     *     "revision": "2",
     *     "status": "ACTIVE",
     *     "timeZone": "Europe/Berlin"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Set the default location (with elevated permissions)
    import { locations } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    /* Sample _id value: '0a965e36-4071-4df0-905b-75458817430a' */

    export async function changeDefaultLocation(_id) {
      try {
        const elevatedSetDefaultLocation = auth.elevate(locations.setDefaultLocation);
        const myDefaultLocation = await elevatedSetDefaultLocation(_id);
        console.log('Default location has been changed to:', myDefaultLocation);

        return myDefaultLocation;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   location: {
     *     "_id": "0a965e36-4071-4df0-905b-75458817430a",
     *     "address": {
     *       "streetAddress": {
     *         "apt": "34",
     *         "name": "Musterstraße",
     *         "number": "5"
     *       },
     *       "city": "Berlin",
     *       "postalCode": "53782"
     *     },
     *     "archived": false,
     *     "default": true,
     *     "description": "Our brand new store in Germany",
     *     "email": "store@example.com",
     *     "name": "Germany Store",
     *     "phone": "0208 209 9087",
     *     "revision": "2",
     *     "status": "ACTIVE",
     *     "timeZone": "Europe/Berlin"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript setDefaultLocation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/business-tools';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function setDefaultLocation(_id) {
      const response = await myWixClient.locations.setDefaultLocation(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
