---
title: "onOrderTransactionsUpdated"
description: "The onOrderTransactionsUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.order_transactions_updated` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Read eCommerce - all read permissions`, `Read Orders`, `Read Stores - all read permissions`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onOrderTransactionsUpdated: Wix-hosted
  import { orderTransactions } from '@wix/ecom';

  orderTransactions.onOrderTransactionsUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderTransactionsUpdated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orderTransactions } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orderTransactions,
      },
  });

  wixClient.orderTransactions.onOrderTransactionsUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
