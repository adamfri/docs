---
title: "onStaffMemberUpdated"
description: "Triggered when a staff member is updated."
icon: "bolt"
---

Triggered when a staff member is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.staff.v1.staff_member_updated` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onStaffMemberUpdated
  import { staffMembers } from '@wix/bookings';

  staffMembers.onStaffMemberUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onStaffMemberUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { staffMembers } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        staffMembers,
      },
  });

  wixClient.staffMembers.onStaffMemberUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
