---
title: "Bulk Create Items"
description: "Note: The Items API only works with the Wix Restaurants Menus app."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    The Items API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Creates multiple items at once.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants/menus/v1/bulk/items/create
    ```

    ## Body Params

    <ParamField body="items" type="Item[]" required>

      Items details.

      Constraints: `maxItems 100`.

      See the [Item Object](/api-reference/business-solutions/restaurants/menus/items/items/item-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return entity in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkCreateItemResult[]">

      Information about the created items.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for item update.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Item">

          Created item.

          See the [Item Object](/api-reference/business-solutions/restaurants/menus/items/items/item-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the API call.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Created | `wix.restaurants.menus.v1.item_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/restaurants/menus-item/v1/bulk/items/create \
          -H 'Authorization: <AUTH>' \
            --data-binary '{
            "items": [
                {
                  "modifierGroups": [],
                  "labels": [
                      {
                        "id": "6046e53c-4ce3-41f7-9e2a-0f6652fe1175"
                      }
                  ],
                  "name": "Tofu skewers",
                  "description": "Grilled tofu skewers, marinated in a blend of soy and sesame with seasonal roast vegetables",
                  "priceInfo": {"price": "11"}
                },
                {
                  "modifierGroups": [],
                  "labels": [
                      {
                        "id": "6046e53c-4ce3-41f7-9e2a-0f7352fe4975"
                      }
                  ],
                  "id": "02022fe1-58ab-ee1c-33db-45521c855e11",
                  "name": "Classic burger",
                  "description": "Our classic burger with lettuce, pickles, heirloom tomatoes, served with a side of fries",
                  "priceInfo": {"price": "15"}
                }
            ],
            "returnEntity": false
          }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "02042f01-58ab-441c-33db-55221c855c11",
              "originalIndex": 0,
              "success": true
            }
          },
          {
            "itemMetadata": {
              "id": "02022fe1-58ab-ee1c-33db-45521c855e11",
              "originalIndex": 1,
              "success": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    The Items API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Creates multiple items at once.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/restaurants';

    items.bulkCreateItems(items, options): Promise
    ```

    ## Method Parameters

    <ParamField body="items" type="Item[]" required>

      Items details.

      Constraints: `maxItems 100`.

      See the [Item Object](/api-reference/business-solutions/restaurants/menus/items/items/item-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateItemsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return entity in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkCreateItemResult[]">

      Information about the created items.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for item update.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Item">

          Created item.

          See the [Item Object](/api-reference/business-solutions/restaurants/menus/items/items/item-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the API call.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Created | `wix.restaurants.menus.v1.item_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-items" id="ex-sdk-bulk-create-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-items" id="ex-sdk-bulk-create-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk Create Items</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-items-0"><span className="wix-picker-name">Bulk Create Items</span></label>
    <label htmlFor="ex-sdk-bulk-create-items-1"><span className="wix-picker-name">bulkCreateItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk Create Items
    import { items } from "@wix/restaurants";

    const itemsToCreate = [
      {
        name: "Tofu skewers",
        description: "Grilled tofu skewers, marinated in a blend of soy and sesame with seasonal roast vegetables",
        priceInfo: {
          price: "11"
        }
      },
      {
        name: "Classic burger",
        description: "Our classic burger with lettuce, pickles, heirloom tomatoes, served with a side of fries",
        priceInfo: {
          price: "15"
        }
      }
    ];

    const options = {
      returnEntity: false
    };

    async function bulkCreateItems() {
      const response = await items.bulkCreateItems(itemsToCreate, options);
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "_id": "2e2d04ea-71a6-4ec1-92d7-2375cf48ade0",
     *         "originalIndex": 0,
     *         "success": true
     *       },
     *       "item": {
     *         "_id": "2e2d04ea-71a6-4ec1-92d7-2375cf48ade0",
     *         "revision": "1",
     *         "name": "Tofu skewers",
     *         "description": "Grilled tofu skewers, marinated in a blend of soy and sesame with seasonal roast vegetables",
     *         "priceInfo": {
     *           "price": "11",
     *           "formattedPrice": "€11.00"
     *         },
     *         "visible": true
     *       }
     *     },
     *     {
     *       "itemMetadata": {
     *         "_id": "b8b049d4-0d2d-43a4-86e3-13ccf18f1ba5",
     *         "originalIndex": 1,
     *         "success": true
     *       },
     *       "item": {
     *         "_id": "b8b049d4-0d2d-43a4-86e3-13ccf18f1ba5",
     *         "revision": "1",
     *         "name": "Classic burger",
     *         "description": "Our classic burger with lettuce, pickles, heirloom tomatoes, served with a side of fries",
     *         "priceInfo": {
     *           "price": "15",
     *           "formattedPrice": "€15.00"
     *         },
     *         "visible": true
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 2,
     *     "totalFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateItems(items,options) {
      const response = await myWixClient.items.bulkCreateItems(items,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
