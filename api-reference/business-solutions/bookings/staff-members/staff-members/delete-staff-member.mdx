---
title: "Delete Staff Member"
description: "Deletes a staff member."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a staff member.

    Also deletes the [resource](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resources-v2/introduction) associated with the staff member.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/bookings/v1/staff-members/{staffMemberId}
    ```

    ## Path Params

    <ParamField path="staffMemberId" type="string" required>

      ID of the staff member to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteStaffMemberResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Staff Member Deleted | `wix.bookings.staff.v1.staff_member_deleted` | Triggered when a staff member is deleted. |
    | Staff Member Disconnected From User | `wix.bookings.staff.v1.staff_member_disconnected_from_user` | Triggered when a Wix user is disconnected from a staff member. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/bookings/v1/staff-members/0954bbb2-88cd-445c-9827-44253eb8b039' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a staff member.

    Also deletes the [resource](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resources-v2/introduction) associated with the staff member.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { staffMembers } from '@wix/bookings';

    staffMembers.deleteStaffMember(staffMemberId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="staffMemberId" type="string" required>

      ID of the staff member to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Staff Member Deleted | `wix.bookings.staff.v1.staff_member_deleted` | Triggered when a staff member is deleted. |
    | Staff Member Disconnected From User | `wix.bookings.staff.v1.staff_member_disconnected_from_user` | Triggered when a Wix user is disconnected from a staff member. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-staff-member" id="ex-sdk-delete-staff-member-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-staff-member" id="ex-sdk-delete-staff-member-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-staff-member-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-staff-member-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a staff member</span><span className="wix-picker-desc">The staff member moves to the trash bin, where you can still retrieve them with Get Deleted Staff Member</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteStaffMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-staff-member-0"><span className="wix-picker-name">Delete a staff member</span><span className="wix-picker-desc">The staff member moves to the trash bin, where you can still retrieve them with Get Deleted Staff Member</span></label>
    <label htmlFor="ex-sdk-delete-staff-member-1"><span className="wix-picker-name">deleteStaffMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a staff member
    import { staffMembers } from "@wix/bookings";

    async function deleteStaffMember() {
      await staffMembers.deleteStaffMember("ca3ec2ca-abf6-44dd-a6fc-1f31679a87de");
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteStaffMember (self-hosted)
    import { createClient } from '@wix/sdk';
    import { staffMembers } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { staffMembers },
      // Include the auth strategy and host as relevant
    });

    async function deleteStaffMember(staffMemberId) {
      const response = await myWixClient.staffMembers.deleteStaffMember(staffMemberId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
