---
title: "Insert Data Item"
description: "Adds an item to a collection."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds an item to a collection.

    Call this method when you want a create-only operation. If the supplied `dataItem.id` already exists in the collection, the request fails. For different behavior, you can call:

    - [Bulk Insert Data Items](https://dev.wix.com/docs/api-reference/business-solutions/cms/data-items/bulk-insert-data-items) to add multiple items in one call.
    - [Save Data Item](https://dev.wix.com/docs/api-reference/business-solutions/cms/data-items/save-data-item) for upsert behavior, meaning to create missing items and update existing items with the same stable ID.

    An item can only be inserted into an existing collection.
    You can create a new collection using the [Data Collections API](https://dev.wix.com/api/rest/wix-data/wix-data/data-collections).

    When an item is inserted into a collection, the item's ID is automatically assigned a random value.
    You can optionally provide a custom ID in `dataItem.id` when inserting the item.
    If you specify an ID that already exists in the collection, the insertion will fail.

    If `dataItem.data` is empty, a new item is created with no data fields.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data/v2/items
    ```

    ## Body Params

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection in which to insert the item.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="dataItem" type="DataItem" required>

      Item to insert.

      See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    </ParamField>

    <ParamField body="appOptions" type="object">

      Additional parameters specific to the [Wix app collection](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections) you are querying.

      When querying the Wix Stores [Products collection](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-e-commerce-stores/wix-stores-products-collection-fields), pass the following optional parameters:
      - `includeHiddenProducts`: Whether to include hidden products in the response. Default: `false`.
      - `includeVariants`: Whether to include product variants in the query. Default: `false`.

    </ParamField>

    ## Response Object

    <ResponseField name="dataItem" type="DataItem">

      Inserted data item.

      See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Data Item Created | `wix.data.v2.data_item_created` | Triggered when a data item is inserted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-insert-data-item" id="ex-rest-insert-data-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-insert-data-item" id="ex-rest-insert-data-item-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-insert-data-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-insert-data-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Insert new item with a predefined id value</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Insert new item</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-insert-data-item-0"><span className="wix-picker-name">Insert new item with a predefined id value</span></label>
    <label htmlFor="ex-rest-insert-data-item-1"><span className="wix-picker-name">Insert new item</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "cities",
          "dataItem": {
              "id": "abc123",
              "data": {
                  "state": "California",
                  "year": 2022,
                  "city": "Los Angeles",
                  "population": 3800000
              }
          }
      }'
      ```

      ```json Response
      {
          "dataItem": {
              "id": "abc123",
              "dataCollectionId": "cities",
              "data": {
                  "_id": "abc123",
                  "_createdDate": { "$date": "2023-03-28T12:20:43.745Z" },
                  "_updatedDate": { "$date": "2023-03-28T12:20:43.745Z" },
                  "_owner": "690264f5-29a0-4aa8-a9b5-0842fc5ab360",
                  "state": "California",
                  "year": 2022.0,
                  "city": "Los Angeles",
                  "population": 3800000.0
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "dataCollectionId": "cities",
          "dataItem": {
              "data": {
                  "state": "California",
                  "year": 2022,
                  "city": "Los Angeles",
                  "population": 3800000
              }
          }
      }'
      ```

      ```json Response
      {
          "dataItem": {
              "id": "5331fc15-9441-4fd4-bc7b-7f6870c69228",
              "dataCollectionId": "cities",
              "data": {
                  "_id": "5331fc15-9441-4fd4-bc7b-7f6870c69228",
                  "_createdDate": { "$date": "2023-03-28T12:20:43.745Z" },
                  "_updatedDate": { "$date": "2023-03-28T12:20:43.745Z" },
                  "_owner": "690264f5-29a0-4aa8-a9b5-0842fc5ab360",
                  "state": "California",
                  "year": 2022.0,
                  "city": "Los Angeles",
                  "population": 3800000.0
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds an item to a collection.

    Call this method when you want a create-only operation. If the supplied `dataItem.id` already exists in the collection, the request fails. For different behavior, you can call:

    - [Bulk Insert Data Items](https://dev.wix.com/docs/api-reference/business-solutions/cms/data-items/bulk-insert-data-items) to add multiple items in one call.
    - [Save Data Item](https://dev.wix.com/docs/api-reference/business-solutions/cms/data-items/save-data-item) for upsert behavior, meaning to create missing items and update existing items with the same stable ID.

    An item can only be inserted into an existing collection.
    You can create a new collection using the [Data Collections API](https://dev.wix.com/api/rest/wix-data/wix-data/data-collections).

    When an item is inserted into a collection, the item's ID is automatically assigned a random value.
    You can optionally provide a custom ID in `dataItem.id` when inserting the item.
    If you specify an ID that already exists in the collection, the insertion will fail.

    If `dataItem.data` is empty, a new item is created with no data fields.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/data';

    items.insert(dataCollectionId, item, options): Promise
    ```

    ## Method Parameters

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection in which to insert the item.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="item" type="WixDataItem" required>

      Data item to insert.

      See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="WixDataInsertOptions">

      Options to use when processing this operation.

      <Expandable title="options properties">

        <ParamField body="suppressHooks" type="boolean">

          Whether to suppress data hooks.
          When `true`, data hooks typically triggered by this endpoint don't run.

          **Note:** This option can only be used in code that runs in [the Wix site backend](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/backend-code/about-the-site-backend).

          Default: `false`

        </ParamField>

        <ParamField body="appOptions" type="object">

          Additional parameters specific to the [Wix app collection](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections) you are querying.

          When querying the Wix Stores [Products collection](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-e-commerce-stores/wix-stores-products-collection-fields), pass the following optional parameters:
          - `includeHiddenProducts`: Whether to include hidden products in the response. Default: `false`.
          - `includeVariants`: Whether to include product variants in the query. Default: `false`.

        </ParamField>

        <ParamField body="saveReferences" type="boolean">

          Whether to create references from multi-reference fields.
          When `true`, array values in multi-reference fields are extracted and stored as references
          linking the inserted item to the referenced items.
          Multi-reference field values must be arrays of referenced item IDs (strings)
          or referenced items containing an `_id` field.

          Default: `false`

        </ParamField>

        <ParamField body="showDrafts" type="boolean">

          When `true`, operations include draft items. Read operations include draft items in their response, and write operations modify draft items.
          Default: `false`.

        </ParamField>

        <ParamField body="includeReferences" type="boolean">

          If true, referenced items are included.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `WixDataItem`.

    See the [Data Item Object](/api-reference/business-solutions/cms/data-items/data-item-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Data Item Created | `wix.data.v2.data_item_created` | Triggered when a data item is inserted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-insert-data-item" id="ex-sdk-insert-data-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-insert-data-item" id="ex-sdk-insert-data-item-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-insert-data-item" id="ex-sdk-insert-data-item-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-insert-data-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-insert-data-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">insert</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">insert (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">insert (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-insert-data-item-0"><span className="wix-picker-name">insert</span></label>
    <label htmlFor="ex-sdk-insert-data-item-1"><span className="wix-picker-name">insert (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-insert-data-item-2"><span className="wix-picker-name">insert (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript insert
    import { items } from '@wix/data';

    async function insert(dataCollectionId,item,options) {
      const response = await items.insert(dataCollectionId,item,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript insert (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function insert(dataCollectionId,item,options) {
      const response = await myWixClient.items.insert(dataCollectionId,item,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript insert (with elevated permissions)
    import { items } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myInsertMethod(dataCollectionId,item,options) {
      const elevatedInsert = auth.elevate(items.insert);
      const response = await elevatedInsert(dataCollectionId,item,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
