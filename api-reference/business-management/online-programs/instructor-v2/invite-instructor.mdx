---
title: "Invite Instructor"
description: "Invite instructor by provided email"
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Invite instructor by provided email

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_api/instructors-service/v2/instructors/invite
    ```

    ## Body Params

    <ParamField body="email" type="string" required>

      Instructor's email to invite

      Constraints: `format EMAIL`.

    </ParamField>

    ## Response Object

    Returns `InviteInstructorResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Invite instructor by provided email

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { instructors } from '@wix/online-programs';

    instructors.inviteInstructor(email): Promise
    ```

    ## Method Parameters

    <ParamField body="email" type="string" required>

      Instructor's email to invite

      Constraints: `format EMAIL`.

    </ParamField>

    ## Returns

    Returns `InviteInstructorResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Instructor Created | `wix.achievements.instructors.v2.instructor_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-invite-instructor" id="ex-sdk-invite-instructor-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-invite-instructor" id="ex-sdk-invite-instructor-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-invite-instructor" id="ex-sdk-invite-instructor-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-invite-instructor-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-invite-instructor-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">inviteInstructor</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">inviteInstructor (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">inviteInstructor (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-invite-instructor-0"><span className="wix-picker-name">inviteInstructor</span></label>
    <label htmlFor="ex-sdk-invite-instructor-1"><span className="wix-picker-name">inviteInstructor (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-invite-instructor-2"><span className="wix-picker-name">inviteInstructor (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript inviteInstructor
    import { instructors } from '@wix/online-programs';

    async function inviteInstructor(email) {
      const response = await instructors.inviteInstructor(email);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript inviteInstructor (self-hosted)
    import { createClient } from '@wix/sdk';
    import { instructors } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { instructors },
      // Include the auth strategy and host as relevant
    });

    async function inviteInstructor(email) {
      const response = await myWixClient.instructors.inviteInstructor(email);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript inviteInstructor (with elevated permissions)
    import { instructors } from '@wix/online-programs';
    import { auth } from '@wix/essentials';

    async function myInviteInstructorMethod(email) {
      const elevatedInviteInstructor = auth.elevate(instructors.inviteInstructor);
      const response = await elevatedInviteInstructor(email);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
