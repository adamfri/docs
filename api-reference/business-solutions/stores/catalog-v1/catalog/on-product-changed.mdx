---
title: "onProductChanged"
description: "Triggered when a product is changed."
icon: "bolt"
---

Triggered when a product is changed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `com.wix.ecommerce.catalog.api.v1.ProductChanged` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onProductChanged
  import { products } from '@wix/stores';

  products.onProductChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onProductChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { products } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        products,
      },
  });

  wixClient.products.onProductChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
