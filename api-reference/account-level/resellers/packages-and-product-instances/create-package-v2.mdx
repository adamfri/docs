---
title: "Create Package V2"
description: "Creates a package of product instances."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a package of product instances.

    You must pass the relevant Wix account ID in the header of the call. In
    the DIY flow, we recommend to pass the customer's sub-account ID instead
    of your main reseller account ID.

    You may also pass a Wix site ID for each product in the body of the call.
    If you omit the site ID, a floating product instance is created.

    When Wix customers purchase a specific paid service or product, Wix may offer
    them time-limited free access to a different product. For example, customers
    get a voucher for a free 1-year domain registration when purchasing any Wix
    Premium plan. If you want to offer your customers the same benefit, create a
    package containing the original product first. Then, create a second package
    with the additional product. In the second Create Package call, pass the
    instance ID of the original product as `referenceProductInstanceId`. This way,
    Wix doesn't charge you for the additional product. Make sure that the status
    of the referenced product is either `"PENDING"` or `"ACTIVE"`. Note that you
    can use each product instance only a single time as reference instance.

    You need to pass a `countryCode` to specify where customers can claim
    vouchers that may come with a product. Contact the
    [Wix B2B sales team](mailto:bizdev@wix.com) to get information about
    vouchers and supported locations.

    <Note>

    This call requires an account level API key and cannot be
    authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="premium.reseller-package.manage">premium.reseller-package.manage</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/resellers/v2/packages
    ```

    ## Body Params

    <ParamField body="idempotencyKey" type="string">

      Idempotency key.

    </ParamField>

    <ParamField body="externalId" type="string">

      External reference. For example, an external subscription ID. **Note:** This field is not validated by Wix.

    </ParamField>

    <ParamField body="products" type="ProductInstance[]" required>

      Wix services that are resold.

      Constraints: `minItems 1`, `maxItems 1000`.

      <Expandable title="products properties">

        <ParamField body="instanceId" type="string">

          ID of the instance of the resold Wix service. **Note:** Differs from the
          `catalogProductId`. Allows you to identify different instances of the same
          product in a package.

          **Read-only.** Constraints: `format GUID`.

        </ParamField>

        <ParamField body="siteId" type="string">

          ID of the Wix site that the product instance is assigned to.
          See the [Query Sites API](https://dev.wix.com/api/rest/account-level-apis/sites/query-sites)
          for more information.

        </ParamField>

        <ParamField body="catalogProductId" type="string">

          Product ID, as defined in the Wix Premium Product Catalog.
          Contact the [Wix B2B sales team](mailto:bizdev@wix.com)
          for more details about available Wix services.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="status" type="string">

          Status of the product instance.

          **Read-only.**

          **Supported values:**

          - `PENDING` — The product instance isn't yet available to the customer.
          - `ENABLED` — The customer can use the product instance.
          - `CANCELED` — The product instance isn't any longer available to the customer.
          - `FAILED` — The product instance couldn't be delivered successfully and has never been available to the customer.
          - `AWAITING_ACTION` — The product instance isn't yet available to the customer, because an external provider or the customer must take an action.

        </ParamField>

        <ParamField body="failure" type="FailureReason">

          Failure object. Only present for status `FAILED`. Describes why the product instance `FAILED`.

          **Read-only.**

          <Expandable title="failure properties">

            <ParamField body="code" type="string">

              Failure code.

              **Supported values:**

              - `DELIVERY_TIMEOUT` — The product instance couldn't be created because the Resellers API timed out.
              - `EXTERNAL_FAILURE` — The product instance couldn't be created because an external process failed.

            </ParamField>

            <ParamField body="message" type="string">

              Failure message.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="createdDate" type="datetime">

          Date and time the product instance was created.

          **Read-only.**

        </ParamField>

        <ParamField body="updatedDate" type="datetime">

          Date and time the product instance was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="countryCode" type="string" required>

          Two-letter country code in
          [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements)
          format. Specifies where customers can claim vouchers that may come with the
          product. Contact the [Wix B2B sales team](mailto:bizdev@wix.com) to get more
          information about vouchers and supported locations.

        </ParamField>

        <ParamField body="expirationDate" type="datetime">

          Date and time the product instance expires in `YYYY-MM-DDThh:mm:ss.sssZ` format.
          Used only for instances that don't auto renew at the end of the current billing cycle.

          **Read-only.**

        </ParamField>

        <ParamField body="discountCode" type="string">

          Discount code indicating that the reseller provisioned the product instance
          during a sale. Wix doesn't guarantee that a discount code reduces the
          price between Wix and the reseller, even when it's valid. Contact the
          [Wix B2B sales team](mailto:bizdev@wix.com) for more information.

          Max: 25 characters

        </ParamField>

        <ParamField body="referenceProductInstanceId" type="string">

          ID of a different product instance that you can use to offer your customers
          time-limited free access to an additional product or service. For example,
          Wix offers a 1-year free domain registration to all customers who purchase a
          Premium plan. The referenced product instance must have either status `"PENDING"` or
          `"ACTIVE"`. You can use each product instance only a single time as reference
          instance.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="idempotencyKey" type="string">

      Idempotency key.

    </ResponseField>

    <ResponseField name="package" type="Package">

      Created package.

      See the [Packages Object](/api-reference/account-level/resellers/packages-and-product-instances/packages-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/resellers/v1/packages' \
      -H 'wix-account-id: <ACCOUNT_ID>' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
          "products": [
              {
                  "catalogProductId": "00ba7923-2655-402a-852d-2b2100568bcf",
                  "siteId": "918aa943-ab4f-40bc-88c3-8dfd02fae7cd",
                  "billingInfo": {
                      "type": "RECURRING",
                      "cycleDuration": {
                          "unit": "YEAR",
                          "count": 1
                      }
                  },
                  "countryCode": "BR"
              }
          ],
          "external_id": "some-external-id"
      }'
      ```

      ```json Response
      {
          "package": {
              "id": "828b98fb-7114-4b3c-90fd-8d0db76aa72b",
              "accountId": "<ACCOUNT_ID>",
              "externalId": "some-external-id",
              "productInstances": [
                  {
                      "instanceId": "d3b88a39-f62e-4164-8b29-08369b9ea71c",
                      "siteId": "918aa943-ab4f-40bc-88c3-8dfd02fae7cd",
                      "catalogProductId": "00ba7923-2655-402a-852d-2b2100568bcf",
                      "status": "PENDING",
                      "billingInfo": {
                          "type": "RECURRING",
                          "cycleDuration": {
                              "unit": "YEAR",
                              "count": 1
                          }
                      },
                      "countryCode": "BR",
                      "createdDate": "2021-12-02T15:45:30.941Z",
                      "updatedDate": "2021-12-02T15:45:30.941Z"
                  }
              ],
              "createdDate": "2021-12-02T15:45:31.815Z",
              "updatedDate": "2021-12-02T15:45:31.815Z"
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a package of product instances.

    You must pass the relevant Wix account ID in the header of the call. In
    the DIY flow, we recommend to pass the customer's sub-account ID instead
    of your main reseller account ID.

    You may also pass a Wix site ID for each product in the body of the call.
    If you omit the site ID, a floating product instance is created.

    When Wix customers purchase a specific paid service or product, Wix may offer
    them time-limited free access to a different product. For example, customers
    get a voucher for a free 1-year domain registration when purchasing any Wix
    Premium plan. If you want to offer your customers the same benefit, create a
    package containing the original product first. Then, create a second package
    with the additional product. In the second Create Package call, pass the
    instance ID of the original product as `referenceProductInstanceId`. This way,
    Wix doesn't charge you for the additional product. Make sure that the status
    of the referenced product is either `"PENDING"` or `"ACTIVE"`. Note that you
    can use each product instance only a single time as reference instance.

    You need to pass a `countryCode` to specify where customers can claim
    vouchers that may come with a product. Contact the
    [Wix B2B sales team](mailto:bizdev@wix.com) to get information about
    vouchers and supported locations.

    <Note>

    This call requires an account level API key and cannot be
    authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="premium.reseller-package.manage">premium.reseller-package.manage</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { packages } from '@wix/packages';

    packages.createPackageV2(options): Promise<CreatePackageResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="CreatePackageV2Options" required>

      options

      <Expandable title="options properties">

        <ParamField body="idempotencyKey" type="string">

          Idempotency key.

        </ParamField>

        <ParamField body="externalId" type="string">

          External reference. For example, an external subscription ID. **Note:** This field is not validated by Wix.

        </ParamField>

        <ParamField body="products" type="ProductInstance[]" required>

          Wix services that are resold.

          Constraints: `minItems 1`, `maxItems 1000`.

          <Expandable title="products properties">

            <ParamField body="instanceId" type="string">

              ID of the instance of the resold Wix service. **Note:** Differs from the
              `catalogProductId`. Allows you to identify different instances of the same
              product in a package.

              **Read-only.** Constraints: `format GUID`.

            </ParamField>

            <ParamField body="siteId" type="string">

              ID of the Wix site that the product instance is assigned to.
              See the [Query Sites API](https://dev.wix.com/api/rest/account-level-apis/sites/query-sites)
              for more information.

            </ParamField>

            <ParamField body="catalogProductId" type="string">

              Product ID, as defined in the Wix Premium Product Catalog.
              Contact the [Wix B2B sales team](mailto:bizdev@wix.com)
              for more details about available Wix services.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="status" type="string">

              Status of the product instance.

              **Read-only.**

              **Supported values:**

              - `PENDING` — The product instance isn't yet available to the customer.
              - `ENABLED` — The customer can use the product instance.
              - `CANCELED` — The product instance isn't any longer available to the customer.
              - `FAILED` — The product instance couldn't be delivered successfully and has never been available to the customer.
              - `AWAITING_ACTION` — The product instance isn't yet available to the customer, because an external provider or the customer must take an action.

            </ParamField>

            <ParamField body="failure" type="FailureReason">

              Failure object. Only present for status `FAILED`. Describes why the product instance `FAILED`.

              **Read-only.**

              _Nested structure omitted for brevity — see the `FailureReason` object._

            </ParamField>

            <ParamField body="_createdDate" type="datetime">

              Date and time the product instance was created.

              **Read-only.**

            </ParamField>

            <ParamField body="_updatedDate" type="datetime">

              Date and time the product instance was last updated.

              **Read-only.**

            </ParamField>

            <ParamField body="countryCode" type="string" required>

              Two-letter country code in
              [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements)
              format. Specifies where customers can claim vouchers that may come with the
              product. Contact the [Wix B2B sales team](mailto:bizdev@wix.com) to get more
              information about vouchers and supported locations.

            </ParamField>

            <ParamField body="expirationDate" type="datetime">

              Date and time the product instance expires in `YYYY-MM-DDThh:mm:ss.sssZ` format.
              Used only for instances that don't auto renew at the end of the current billing cycle.

              **Read-only.**

            </ParamField>

            <ParamField body="discountCode" type="string">

              Discount code indicating that the reseller provisioned the product instance
              during a sale. Wix doesn't guarantee that a discount code reduces the
              price between Wix and the reseller, even when it's valid. Contact the
              [Wix B2B sales team](mailto:bizdev@wix.com) for more information.

              Max: 25 characters

            </ParamField>

            <ParamField body="referenceProductInstanceId" type="string">

              ID of a different product instance that you can use to offer your customers
              time-limited free access to an additional product or service. For example,
              Wix offers a 1-year free domain registration to all customers who purchase a
              Premium plan. The referenced product instance must have either status `"PENDING"` or
              `"ACTIVE"`. You can use each product instance only a single time as reference
              instance.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CreatePackageResponse>`.

    <ResponseField name="idempotencyKey" type="string">

      Idempotency key.

    </ResponseField>

    <ResponseField name="package" type="Package">

      Created package.

      See the [Packages Object](/api-reference/account-level/resellers/packages-and-product-instances/packages-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Packages Created | `wix.premium.reseller.v1.packages_created` | Triggered when a package is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-package-v2" id="ex-sdk-create-package-v2-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-package-v2" id="ex-sdk-create-package-v2-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-package-v2-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-package-v2-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a package with an API key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createPackageV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-package-v2-0"><span className="wix-picker-name">Create a package with an API key</span></label>
    <label htmlFor="ex-sdk-create-package-v2-1"><span className="wix-picker-name">createPackageV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a package with an API key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { packages } from "@wix/packages";

    const wixClient = createClient({
     modules: { packages },
     auth: ApiKeyStrategy({
        siteId: "MY-SITE-ID",
        apiKey: "MY-API-KEY",
      }),
    });

    async function createPackageV2(options) {
      const response = await packages.createPackageV2(options);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createPackageV2 (self-hosted)
    import { createClient } from '@wix/sdk';
    import { packages } from '@wix/packages';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { packages },
      // Include the auth strategy and host as relevant
    });

    async function createPackageV2(options) {
      const response = await myWixClient.packages.createPackageV2(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
