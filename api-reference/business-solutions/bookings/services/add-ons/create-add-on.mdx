---
title: "Create Add On"
description: "Creates an add-on."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates an add-on.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/addons/v1/add-ons
    ```

    ## Body Params

    <ParamField body="addOn" type="AddOn" required>

      Add-on to create.

      See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="addOn" type="AddOn">

      Created add-on.

      See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Add On Created | `wix.addons.v1.add_on_created` | Triggered when an add-on is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/addons/v1/add-ons' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "addOn": {
            "name": "Deep Tissue Upgrade (15 minutes)",
            "price": {
              "value": "2500",
              "currency": "USD",
              "formattedValue": "$25.00"
            },
            "durationInMinutes": 15,
            "taxGroup": "12345678-1234-1234-1234-123456789012"
          }
        }'
      ```

      ```json Response
      {
        "addOn": {
          "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
          "revision": "1",
          "createdDate": "2019-10-30T17:22:10.299Z",
          "updatedDate": "2019-10-30T17:22:10.299Z",
          "name": "Deep Tissue Upgrade (15 minutes)",
          "price": {
            "value": "2500",
            "currency": "USD",
            "formattedValue": "$25.00"
          },
          "durationInMinutes": 15,
          "taxGroup": "12345678-1234-1234-1234-123456789012"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates an add-on.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { addOns } from '@wix/bookings';

    addOns.createAddOn(addOn): Promise<AddOn>
    ```

    ## Method Parameters

    <ParamField body="addOn" type="AddOn" required>

      Add-on to create.

      See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<AddOn>`.

    See the [Add On Object](/api-reference/business-solutions/bookings/services/add-ons/add-on-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Add On Created | `wix.addons.v1.add_on_created` | Triggered when an add-on is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-add-on" id="ex-sdk-create-add-on-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-add-on" id="ex-sdk-create-add-on-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-add-on" id="ex-sdk-create-add-on-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-add-on-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-add-on-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createAddOn</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createAddOn (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createAddOn (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-add-on-0"><span className="wix-picker-name">createAddOn</span></label>
    <label htmlFor="ex-sdk-create-add-on-1"><span className="wix-picker-name">createAddOn (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-add-on-2"><span className="wix-picker-name">createAddOn (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createAddOn
    import { addOns } from '@wix/bookings';

    async function createAddOn(addOn) {
      const response = await addOns.createAddOn(addOn);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createAddOn (self-hosted)
    import { createClient } from '@wix/sdk';
    import { addOns } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { addOns },
      // Include the auth strategy and host as relevant
    });

    async function createAddOn(addOn) {
      const response = await myWixClient.addOns.createAddOn(addOn);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createAddOn (with elevated permissions)
    import { addOns } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myCreateAddOnMethod(addOn) {
      const elevatedCreateAddOn = auth.elevate(addOns.createAddOn);
      const response = await elevatedCreateAddOn(addOn);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
