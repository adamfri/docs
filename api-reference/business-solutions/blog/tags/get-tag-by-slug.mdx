---
title: "Get Tag By Slug"
description: "Retrieves a tag with the provided slug."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a tag with the provided slug.

    The `slug` is the end of a tag's URL that refers to a specific tag.
    For example, if a tag's URL is `https://example.com/blog/tag/{my-tag-slug}`,
    the slug is `my-tag-slug`. The `slug` is case-sensitive and derived from the tag's `label`.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.READ-BLOGS">Read Blog </span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/v3/tags/slugs/{slug}
    ```

    ## Path Params

    <ParamField path="slug" type="string" required>

      Slug of the tag to retrieve.

    </ParamField>

    ## Query Params

    <ParamField query="fieldsets" type="string[]">

      List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
      the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
      only the tag's base fields are returned.

      Constraints: `maxItems 10`.

      **Supported values:**

      - `URL` — Includes Tag URL when present.

    </ParamField>

    <ParamField query="language" type="string">

      2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
      Language of the tag to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="tag" type="Tag">

      Retrieved tag.

      See the [Tag Object](/api-reference/business-solutions/blog/tags/tag-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl \
        'https://www.wixapis.com/blog/v3/tags/slugs/vacation' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "tag": {
          "id": "6d72a3bb-053c-4de5-a897-5ef6be30b1b0",
          "label": "vacation",
          "slug": "vacation",
          "createdDate": "2021-08-13T08:58:20.145Z",
          "updatedDate": "2021-08-13T08:58:20.145Z",
          "publicationCount": 2,
          "postCount": 1,
          "language": "en"
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a tag with the provided slug.

    The `slug` is the end of a tag's URL that refers to a specific tag.
    For example, if a tag's URL is `https://example.com/blog/tag/{my-tag-slug}`,
    the slug is `my-tag-slug`. The `slug` is case-sensitive and derived from the tag's `label`.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.READ-BLOGS">Read Blog </span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { tags } from '@wix/blog';

    tags.getTagBySlug(slug, options): Promise<GetTagBySlugResponse>
    ```

    ## Method Parameters

    <ParamField body="slug" type="string" required>

      Slug of the tag to retrieve.

      Constraints: `minLength 1`, `maxLength 100`.

    </ParamField>

    <ParamField body="options" type="GetTagBySlugOptions">

      Options specifying which additional fields to return. 

      <Expandable title="options properties">

        <ParamField body="fieldsets" type="string[]">

          List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
          the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
          only the tag's base fields are returned.

          Constraints: `maxItems 10`.

          **Supported values:**

          - `URL` — Includes Tag URL when present.

        </ParamField>

        <ParamField body="language" type="string">

          2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
          Language of the tag to retrieve.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<GetTagBySlugResponse>`.

    <ResponseField name="tag" type="Tag">

      Retrieved tag.

      See the [Tag Object](/api-reference/business-solutions/blog/tags/tag-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-tag-by-slug" id="ex-sdk-get-tag-by-slug-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-tag-by-slug" id="ex-sdk-get-tag-by-slug-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-tag-by-slug" id="ex-sdk-get-tag-by-slug-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-tag-by-slug-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-tag-by-slug-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get a tag by its slug</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get a tag by its slug with additional fields</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTagBySlug (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-tag-by-slug-0"><span className="wix-picker-name">Get a tag by its slug</span></label>
    <label htmlFor="ex-sdk-get-tag-by-slug-1"><span className="wix-picker-name">Get a tag by its slug with additional fields</span></label>
    <label htmlFor="ex-sdk-get-tag-by-slug-2"><span className="wix-picker-name">getTagBySlug (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get a tag by its slug
    import { tags } from '@wix/blog';

    /* Sample slug value:
     * 'my-tag'
     */

    export async function getTagBySlugFunction(slug) {
      try {
        const result = await tags.getTagBySlug(slug);
        const label = result.tag.label;
        const postCount = result.tag.postCount;
        console.log('Retrieved result:', result);
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise resolves to:
     * {
     *   "tag": {
     *     "_createdDate": "2022-05-03T10:10:49.499Z",
     *     "_id": "32970480-e53b-46e7-b52f-fba810a0b45d",
     *     "_updatedDate": "2022-05-03T10:10:49.499Z",
     *     "label": "my-tag",
     *     "language": "en",
     *     "postCount": 1,
     *     "publishedPostCount": 1,
     *     "slug": "my-tag",
     *     "translationId": ""
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get a tag by its slug with additional fields
    import { tags } from '@wix/blog';

    /* Sample slug value:
     * 'my-tag'
     * Sample options value:
     * {
     *   fieldsets:
     *   [
     *     'URL'
     *   ]
     * }
     */

    export async function getTagBySlugFunction(slug, options) {
      try {
        const result = await tags.getTagBySlug(slug, options);
        const label = result.tag.label;
        const postCount = result.tag.postCount;
        const url = result.tag.url;
        console.log('Retrieved result:', result);
        return result;
      } catch (error) {
        console.error(error);
      }
    }

    /* Promise resolves to:
     * {
     *   "tag": {
     *     "_createdDate": "2022-05-03T10:10:49.499Z",
     *     "_id": "32970480-e53b-46e7-b52f-fba810a0b45d",
     *     "_updatedDate": "2022-05-03T10:10:49.499Z",
     *     "label": "my-tag",
     *     "language": "en",
     *     "postCount": 1,
     *     "publishedPostCount": 1,
     *     "slug": "my-tag",
     *     "translationId": "",
     *     "url": "http://https://tadasz7.wixsite.com/blog-velo-events/my-blog/tags/my-tag"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTagBySlug (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tags } from '@wix/blog';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tags },
      // Include the auth strategy and host as relevant
    });

    async function getTagBySlug(slug,options) {
      const response = await myWixClient.tags.getTagBySlug(slug,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
