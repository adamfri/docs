---
title: "onCampaignEmailActivityUpdated"
description: "Triggered when an email campaign has a new recipient activity."
icon: "bolt"
---

Triggered when an email campaign has a new recipient activity.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.email_marketing.v1.campaign_email_activity_updated` |
| Package | `@wix/email-marketing` |
| Permissions | `Manage Stores`, `Access Verticals by Automations`, `Set Up Automations`, `Read Email Marketing`, `Manage Email Marketing` |

## Examples

<CodeGroup>

  ```javascript onCampaignEmailActivityUpdated
  import { campaigns } from '@wix/email-marketing';

  campaigns.onCampaignEmailActivityUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCampaignEmailActivityUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { campaigns } from '@wix/email-marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        campaigns,
      },
  });

  wixClient.campaigns.onCampaignEmailActivityUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
