---
title: "onOrderPaused"
description: "Triggered when an order is paused."
icon: "bolt"
---

Triggered when an order is paused.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.v2.order_paused` |
| Package | `@wix/pricing-plans` |
| Permissions | `Manage Orders`, `Read Orders`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onOrderPaused
  import { orders } from '@wix/pricing-plans';

  orders.onOrderPaused((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderPaused (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderPaused((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
