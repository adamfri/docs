---
title: "onStorageItemCreated"
description: "The onStorageItemCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.automations.storage.v1.storage_item_created` |
| Package | `@wix/automations` |
| Permissions | `Manage Stores`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onStorageItemCreated
  import { storageItem } from '@wix/automations';

  storageItem.onStorageItemCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onStorageItemCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { storageItem } from '@wix/automations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        storageItem,
      },
  });

  wixClient.storageItem.onStorageItemCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
