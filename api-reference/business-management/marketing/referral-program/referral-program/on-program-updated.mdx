---
title: "onProgramUpdated"
description: "Triggered when a referral program is updated."
icon: "bolt"
---

Triggered when a referral program is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.referral.v1.program_updated` |
| Package | `@wix/referral` |
| Permissions | `Manage Referrals` |

## Examples

<CodeGroup>

  ```javascript onProgramUpdated
  import { programs } from '@wix/referral';

  programs.onProgramUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onProgramUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { programs } from '@wix/referral';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        programs,
      },
  });

  wixClient.programs.onProgramUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
