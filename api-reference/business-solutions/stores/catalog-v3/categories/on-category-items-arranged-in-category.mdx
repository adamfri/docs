---
title: "onCategoryItemsArrangedInCategory"
description: "Triggered when items arrangement in category is changed."
icon: "bolt"
---

Triggered when items arrangement in category is changed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.categories.v1.category_items_arranged_in_category` |
| Package | `@wix/categories` |
| Permissions | `Manage Stores - all permissions`, `Read categories`, `Read v3 catalog`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onCategoryItemsArrangedInCategory
  import { categories } from '@wix/categories';

  categories.onCategoryItemsArrangedInCategory((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryItemsArrangedInCategory (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categories } from '@wix/categories';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        categories,
      },
  });

  wixClient.categories.onCategoryItemsArrangedInCategory((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
