---
title: "onMenuOrderingSettingsDeleted"
description: "The onMenuOrderingSettingsDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menu_settings.v1.menu_ordering_settings_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onMenuOrderingSettingsDeleted
  import { menuOrderingSettings } from '@wix/restaurants';

  menuOrderingSettings.onMenuOrderingSettingsDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onMenuOrderingSettingsDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { menuOrderingSettings } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        menuOrderingSettings,
      },
  });

  wixClient.menuOrderingSettings.onMenuOrderingSettingsDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
