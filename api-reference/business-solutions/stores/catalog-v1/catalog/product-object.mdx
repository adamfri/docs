---
title: "Product Object"
description: "Read and write products from Wix Stores Catalog"
icon: "cube"
---

Read and write products from Wix Stores Catalog

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Product ID (generated automatically by the catalog).

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Product name.

      Min: 1 character
      Max: 80 characters

    </ResponseField>

    <ResponseField name="slug" type="string">

      A friendly URL name (generated automatically by the catalog when a product is created), can be updated.

      Constraints: `maxLength 100`.

    </ResponseField>

    <ResponseField name="visible" type="boolean">

      Whether the product is visible to site visitors.

    </ResponseField>

    <ResponseField name="productType" type="string">

      Currently, only creating physical products ( `"productType": "physical"` ) is supported via the API.

      **Supported values:**

      - `unspecified_product_type`
      - `physical`
      - `digital`

    </ResponseField>

    <ResponseField name="description" type="string">

      Product description. Accepts [rich text](https://dev.wix.com/api/rest/wix-stores/rich-text).

    </ResponseField>

    <ResponseField name="sku" type="string">

      Stock keeping unit. If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, SKUs will be set per variant, and this field will be empty.

    </ResponseField>

    <ResponseField name="weight" type="number">

      Product weight. If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, weight will be set per variant, and this field will be empty.

    </ResponseField>

    <ResponseField name="weightRange" type="NumericPropertyRange">

      Product weight range. The minimum and maximum weights of all the variants.

      **Read-only.**

      <Expandable title="weightRange properties">

        <ResponseField name="minValue" type="number">

          Minimum value.

        </ResponseField>

        <ResponseField name="maxValue" type="number">

          Maximum value.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="productDimensions" type="Dimensions">

      Product dimensions (length, width, height of the physical product). If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, dimensions will be set per variant, and this field will be empty.

      <Expandable title="productDimensions properties">

        <ResponseField name="length" type="string">

          Length. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="width" type="string">

          Width. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="height" type="string">

          Height. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="unit" type="string">

          Unit of measurement for dimensions (length, width, height).

          **Supported values:**

          - `UNKNOWN_DIMENSIONS_UNIT`
          - `MM` — Millimeters.
          - `CM` — Centimeters.
          - `M` — Meters.
          - `IN` — Inches.
          - `FT` — Feet.
          - `YD` — Yards.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="packageDimensions" type="Dimensions">

      Package dimensions (length, width, height of the shipping package). If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, dimensions will be set per variant, and this field will be empty.

      <Expandable title="packageDimensions properties">

        <ResponseField name="length" type="string">

          Length. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="width" type="string">

          Width. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="height" type="string">

          Height. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="unit" type="string">

          Unit of measurement for dimensions (length, width, height).

          **Supported values:**

          - `UNKNOWN_DIMENSIONS_UNIT`
          - `MM` — Millimeters.
          - `CM` — Centimeters.
          - `M` — Meters.
          - `IN` — Inches.
          - `FT` — Feet.
          - `YD` — Yards.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="stock" type="Stock">

      Product inventory status (in future this will be writable via Inventory API).

      **Read-only.**

      <Expandable title="stock properties">

        <ResponseField name="trackInventory" type="boolean">

          Whether inventory is being tracked

        </ResponseField>

        <ResponseField name="quantity" type="integer">

          Quantity currently left in inventory

        </ResponseField>

        <ResponseField name="inStock" type="boolean" deprecated>

          Whether the product is currently in stock (relevant only when tracking manually)
          Deprecated (use `inventoryStatus` instead)

          **Deprecated.**

        </ResponseField>

        <ResponseField name="inventoryStatus" type="string">

          The current status of the inventory
          + `IN_STOCK` - In stock
          + `OUT_OF_STOCK` - Not in stock
          + `PARTIALLY_OUT_OF_STOCK` - Some of the variants are not in stock

          **Supported values:**

          - `IN_STOCK` — In stock
          - `OUT_OF_STOCK` — Not in stock
          - `PARTIALLY_OUT_OF_STOCK` — Some of the variants are not in stock

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="price" type="PriceData" deprecated>

      Deprecated (use `priceData` instead).

      **Read-only.** **Deprecated.**

      <Expandable title="price properties">

        <ResponseField name="currency" type="string">

          Product price currency

          **Read-only.**

        </ResponseField>

        <ResponseField name="price" type="number">

          Product price

        </ResponseField>

        <ResponseField name="discountedPrice" type="number">

          Discounted product price (if no discounted price is set, the product price is returned)

          **Read-only.**

        </ResponseField>

        <ResponseField name="formatted" type="FormattedPrice">

          The product price and discounted price, formatted with the currency

          **Read-only.**

          <Expandable title="formatted properties">

            <ResponseField name="price" type="string">

              Product price formatted with the currency

            </ResponseField>

            <ResponseField name="discountedPrice" type="string">

              Discounted product price formatted with the currency (if no discounted price is set, the product formatted price is returned)

            </ResponseField>

            <ResponseField name="pricePerUnit" type="string">

              Price per unit

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pricePerUnit" type="number">

          Price per unit

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="priceData" type="PriceData">

      Price data.

      <Expandable title="priceData properties">

        <ResponseField name="currency" type="string">

          Product price currency

          **Read-only.**

        </ResponseField>

        <ResponseField name="price" type="number">

          Product price

        </ResponseField>

        <ResponseField name="discountedPrice" type="number">

          Discounted product price (if no discounted price is set, the product price is returned)

          **Read-only.**

        </ResponseField>

        <ResponseField name="formatted" type="FormattedPrice">

          The product price and discounted price, formatted with the currency

          **Read-only.**

          <Expandable title="formatted properties">

            <ResponseField name="price" type="string">

              Product price formatted with the currency

            </ResponseField>

            <ResponseField name="discountedPrice" type="string">

              Discounted product price formatted with the currency (if no discounted price is set, the product formatted price is returned)

            </ResponseField>

            <ResponseField name="pricePerUnit" type="string">

              Price per unit

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pricePerUnit" type="number">

          Price per unit

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="convertedPriceData" type="PriceData">

      Price data, converted to the currency specified in request header.

      **Read-only.**

      <Expandable title="convertedPriceData properties">

        <ResponseField name="currency" type="string">

          Product price currency

          **Read-only.**

        </ResponseField>

        <ResponseField name="price" type="number">

          Product price

        </ResponseField>

        <ResponseField name="discountedPrice" type="number">

          Discounted product price (if no discounted price is set, the product price is returned)

          **Read-only.**

        </ResponseField>

        <ResponseField name="formatted" type="FormattedPrice">

          The product price and discounted price, formatted with the currency

          **Read-only.**

          <Expandable title="formatted properties">

            <ResponseField name="price" type="string">

              Product price formatted with the currency

            </ResponseField>

            <ResponseField name="discountedPrice" type="string">

              Discounted product price formatted with the currency (if no discounted price is set, the product formatted price is returned)

            </ResponseField>

            <ResponseField name="pricePerUnit" type="string">

              Price per unit

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pricePerUnit" type="number">

          Price per unit

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="priceRange" type="NumericPropertyRange">

      Product price range. The minimum and maximum prices of all the variants.

      **Read-only.**

      <Expandable title="priceRange properties">

        <ResponseField name="minValue" type="number">

          Minimum value.

        </ResponseField>

        <ResponseField name="maxValue" type="number">

          Maximum value.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="costAndProfitData" type="CostAndProfitData">

      Cost and profit data.

      <Expandable title="costAndProfitData properties">

        <ResponseField name="itemCost" type="number">

          Item cost.

        </ResponseField>

        <ResponseField name="formattedItemCost" type="string">

          Item cost formatted with currency symbol.

          **Read-only.**

        </ResponseField>

        <ResponseField name="profit" type="number">

          Profit. Calculated by reducing `cost` from `discounted_price`.

          **Read-only.** Constraints: `min 0`, `max 999999999.99`.

        </ResponseField>

        <ResponseField name="formattedProfit" type="string">

          Profit formatted with currency symbol.

          **Read-only.**

        </ResponseField>

        <ResponseField name="profitMargin" type="number">

          Profit Margin. Calculated by dividing `profit` by `discounted_price`.
          The result is rounded to 4 decimal places.

          **Read-only.** Constraints: `min 0`, `max 1`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="costRange" type="NumericPropertyRange">

      Product cost range. The minimum and maximum costs of all the variants.

      **Read-only.**

      <Expandable title="costRange properties">

        <ResponseField name="minValue" type="number">

          Minimum value.

        </ResponseField>

        <ResponseField name="maxValue" type="number">

          Maximum value.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pricePerUnitData" type="PricePerUnitData">

      Price per unit data.

      <Expandable title="pricePerUnitData properties">

        <ResponseField name="totalQuantity" type="number">

          Total quantity

          Constraints: `min 0.01`, `max 999999999.99`.

        </ResponseField>

        <ResponseField name="totalMeasurementUnit" type="string">

          Total measurement unit

          **Supported values:**

          - `UNSPECIFIED`
          - `ML`
          - `CL`
          - `L`
          - `CBM`
          - `MG`
          - `G`
          - `KG`
          - `MM`
          - `CM`
          - `M`
          - `SQM`
          - `OZ`
          - `LB`
          - `FLOZ`
          - `PT`
          - `QT`
          - `GAL`
          - `IN`
          - `FT`
          - `YD`
          - `SQFT`

        </ResponseField>

        <ResponseField name="baseQuantity" type="number">

          Base quantity

          Constraints: `min 0.01`, `max 999999999.99`.

        </ResponseField>

        <ResponseField name="baseMeasurementUnit" type="string">

          Base measurement unit

          **Supported values:**

          - `UNSPECIFIED`
          - `ML`
          - `CL`
          - `L`
          - `CBM`
          - `MG`
          - `G`
          - `KG`
          - `MM`
          - `CM`
          - `M`
          - `SQM`
          - `OZ`
          - `LB`
          - `FLOZ`
          - `PT`
          - `QT`
          - `GAL`
          - `IN`
          - `FT`
          - `YD`
          - `SQFT`

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalInfoSections" type="AdditionalInfoSection[]">

      Additional text that the store owner can assign to the product (e.g. shipping details, refund policy, etc.).

      <Expandable title="additionalInfoSections properties">

        <ResponseField name="title" type="string">

          Product info section title

          Constraints: `maxLength 50`.

        </ResponseField>

        <ResponseField name="description" type="string">

          Product info section description

          Constraints: `maxLength 16000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="ribbons" type="Ribbon[]" deprecated>

      Deprecated (use `ribbon` instead).

      **Read-only.** **Deprecated.**

      <Expandable title="ribbons properties">

        <ResponseField name="text" type="string">

          Ribbon text

          Constraints: `maxLength 30`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="media" type="Media">

      Media items (images, videos etc) associated with this product (writable via [Add Product Media](https://dev.wix.com/api/rest/wix-stores/catalog/products/add-product-media) endpoint).

      **Read-only.**

      <Expandable title="media properties">

        <ResponseField name="mainMedia" type="MediaItem">

          Primary media (image, video etc) associated with this product.

          <Expandable title="mainMedia properties">

            <ResponseField name="thumbnail" type="MediaItemUrlAndSize">

              Media item thumbnail details.

              <Expandable title="thumbnail properties">

                <ResponseField name="url" type="string">

                  Media item URL.

                  Constraints: `format WEB_URL`.

                </ResponseField>

                <ResponseField name="width" type="integer">

                  Media item width.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="height" type="integer">

                  Media item height.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="format" type="string">

                  Media format (mp4, png, etc.).

                </ResponseField>

                <ResponseField name="altText" type="string">

                  Alt text. This text will be shown in case the image is not available.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="mediaType" type="string">

              Media item type (image, video, etc.).

              **Supported values:**

              - `unspecified_media_item_type`
              - `image` — Image media type.
              - `video` — Video media type.
              - `audio` — Audio media type.
              - `document` — Document media type.
              - `zip` — Zip media type.

            </ResponseField>

            <ResponseField name="title" type="string">

              Media item title.

            </ResponseField>

            <ResponseField name="id" type="string">

              Media ID (for example, `"nsplsh_306d666a123a4a74306459~mv2_d_4517_2992_s_4_2.jpg"`).

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="item" type="item">

              item

              <Expandable title="item properties">

                <ResponseField name="image" type="MediaItemUrlAndSize">

                  Image data (URL, size).

                  _Nested structure omitted for brevity — see the `MediaItemUrlAndSize` object._

                </ResponseField>

                <ResponseField name="video" type="MediaItemVideo">

                  Video data (URL, size).

                  _Nested structure omitted for brevity — see the `MediaItemVideo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="items" type="MediaItem[]">

          Media (images, videos etc) associated with this product.

          <Expandable title="items properties">

            <ResponseField name="thumbnail" type="MediaItemUrlAndSize">

              Media item thumbnail details.

              <Expandable title="thumbnail properties">

                <ResponseField name="url" type="string">

                  Media item URL.

                  Constraints: `format WEB_URL`.

                </ResponseField>

                <ResponseField name="width" type="integer">

                  Media item width.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="height" type="integer">

                  Media item height.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="format" type="string">

                  Media format (mp4, png, etc.).

                </ResponseField>

                <ResponseField name="altText" type="string">

                  Alt text. This text will be shown in case the image is not available.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="mediaType" type="string">

              Media item type (image, video, etc.).

              **Supported values:**

              - `unspecified_media_item_type`
              - `image` — Image media type.
              - `video` — Video media type.
              - `audio` — Audio media type.
              - `document` — Document media type.
              - `zip` — Zip media type.

            </ResponseField>

            <ResponseField name="title" type="string">

              Media item title.

            </ResponseField>

            <ResponseField name="id" type="string">

              Media ID (for example, `"nsplsh_306d666a123a4a74306459~mv2_d_4517_2992_s_4_2.jpg"`).

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customTextFields" type="CustomTextField[]">

      Text box for the customer to add a message to their order (e.g., customization request). Currently writable only from the UI.

      **Read-only.**

      <Expandable title="customTextFields properties">

        <ResponseField name="title" type="string">

          Text box title

          Constraints: `maxLength 100`.

        </ResponseField>

        <ResponseField name="maxLength" type="integer">

          Text box input max length

          Constraints: `max 500`.

        </ResponseField>

        <ResponseField name="mandatory" type="boolean">

          Whether this text box is mandatory

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="manageVariants" type="boolean">

      Whether variants are being managed for this product - enables unique SKU, price and weight per variant. Also affects inventory data.

    </ResponseField>

    <ResponseField name="productOptions" type="ProductOption[]">

      Options for this product.

      Constraints: `maxItems 6`.

      <Expandable title="productOptions properties">

        <ResponseField name="optionType" type="string">

          Option type.

          **Read-only.**

          **Supported values:**

          - `unspecified_option_type` — Unspecified option type.
          - `drop_down` — Drop down.
          - `color` — Color.

        </ResponseField>

        <ResponseField name="name" type="string">

          Option name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ResponseField>

        <ResponseField name="choices" type="Choice[]">

          Choices available for this option.

          Constraints: `minItems 1`, `maxItems 100`.

          <Expandable title="choices properties">

            <ResponseField name="value" type="string">

              Choice value.

              Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="description" type="string">

              Choice description.

              Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="media" type="Media">

              Media items (images, videos) associated with this choice

              **Read-only.**

              <Expandable title="media properties">

                <ResponseField name="mainMedia" type="MediaItem">

                  Primary media (image, video etc) associated with this product.

                  _Nested structure omitted for brevity — see the `MediaItem` object._

                </ResponseField>

                <ResponseField name="items" type="MediaItem[]">

                  Media (images, videos etc) associated with this product.

                  _Nested structure omitted for brevity — see the `MediaItem[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="inStock" type="boolean">

              Based on the customer’s choices, which (if any) variants that include the selected choices are in stock

              **Read-only.**

            </ResponseField>

            <ResponseField name="visible" type="boolean">

              Based on the customer’s choices, which (if any) variants that include the selected choices are visible

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="productPageUrl" type="PageUrl">

      Product page URL for this product (generated automatically by the server).

      **Read-only.**

      <Expandable title="productPageUrl properties">

        <ResponseField name="base" type="string">

          Base URL. For premium sites, this is the domain. For free sites, this is the site URL (e.g mysite.wixsite.com/mysite).

        </ResponseField>

        <ResponseField name="path" type="string">

          Path to the product page - e.g /product-page/a-product.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="numericId" type="number">

      Product’s unique numeric ID (assigned in ascending order).
      Primarily used for sorting and filtering when crawling all products.

      **Read-only.**

    </ResponseField>

    <ResponseField name="inventoryItemId" type="string">

      Inventory item ID - ID referencing the inventory system.

      **Read-only.**

    </ResponseField>

    <ResponseField name="discount" type="Discount">

      Discount deducted from the product's original price.

      <Expandable title="discount properties">

        <ResponseField name="type" type="string">

          Discount type:
          + `"AMOUNT"`
          + `"PERCENT"`

          **Supported values:**

          - `UNDEFINED`
          - `NONE` — No discount
          - `AMOUNT` — Discount by a fixed amount
          - `PERCENT` — Discount by a percentage

        </ResponseField>

        <ResponseField name="value" type="number">

          Discount value

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="collectionIds" type="string[]">

      A list of all collection IDs that this product is included in (writable via the Catalog > Collection APIs).

      **Read-only.**

    </ResponseField>

    <ResponseField name="variants" type="Variant[]">

      Product variants, will be provided if the request was sent with the `includeVariants: true`.

      Max: 1,000 variants

      **Read-only.** Constraints: `maxItems 1000`.

      <Expandable title="variants properties">

        <ResponseField name="id" type="string">

          Requested Variant ID

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="choices" type="object">

          Specific choices within a selection, as option-choice key-value pairs

          Constraints: `format map`.

          <Expandable title="choices properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="string" />

          </Expandable>

        </ResponseField>

        <ResponseField name="variant" type="VariantDataWithNoStock">

          variant

          <Expandable title="variant properties">

            <ResponseField name="priceData" type="PriceData">

              Variant price.

              <Expandable title="priceData properties">

                <ResponseField name="currency" type="string">

                  Product price currency

                  **Read-only.**

                </ResponseField>

                <ResponseField name="price" type="number">

                  Product price

                </ResponseField>

                <ResponseField name="discountedPrice" type="number">

                  Discounted product price (if no discounted price is set, the product price is returned)

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formatted" type="FormattedPrice">

                  The product price and discounted price, formatted with the currency

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `FormattedPrice` object._

                </ResponseField>

                <ResponseField name="pricePerUnit" type="number">

                  Price per unit

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="convertedPriceData" type="PriceData">

              Variant price data, converted to currency requested in header.

              **Read-only.**

              <Expandable title="convertedPriceData properties">

                <ResponseField name="currency" type="string">

                  Product price currency

                  **Read-only.**

                </ResponseField>

                <ResponseField name="price" type="number">

                  Product price

                </ResponseField>

                <ResponseField name="discountedPrice" type="number">

                  Discounted product price (if no discounted price is set, the product price is returned)

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formatted" type="FormattedPrice">

                  The product price and discounted price, formatted with the currency

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `FormattedPrice` object._

                </ResponseField>

                <ResponseField name="pricePerUnit" type="number">

                  Price per unit

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="costAndProfitData" type="CostAndProfitData">

              Cost and profit data.

              <Expandable title="costAndProfitData properties">

                <ResponseField name="itemCost" type="number">

                  Item cost.

                </ResponseField>

                <ResponseField name="formattedItemCost" type="string">

                  Item cost formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="profit" type="number">

                  Profit. Calculated by reducing `cost` from `discounted_price`.

                  **Read-only.** Constraints: `min 0`, `max 999999999.99`.

                </ResponseField>

                <ResponseField name="formattedProfit" type="string">

                  Profit formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="profitMargin" type="number">

                  Profit Margin. Calculated by dividing `profit` by `discounted_price`.
                  The result is rounded to 4 decimal places.

                  **Read-only.** Constraints: `min 0`, `max 1`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="weight" type="number">

              Variant weight.

            </ResponseField>

            <ResponseField name="sku" type="string">

              Variant SKU (stock keeping unit).

            </ResponseField>

            <ResponseField name="visible" type="boolean">

              Whether the variant is visible to customers.

            </ResponseField>

            <ResponseField name="productDimensions" type="Dimensions">

              Product dimensions (length, width, height of the physical product).

              <Expandable title="productDimensions properties">

                <ResponseField name="length" type="string">

                  Length. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="width" type="string">

                  Width. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="height" type="string">

                  Height. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="unit" type="string">

                  Unit of measurement for dimensions (length, width, height).

                  **Supported values:**

                  - `UNKNOWN_DIMENSIONS_UNIT`
                  - `MM` — Millimeters.
                  - `CM` — Centimeters.
                  - `M` — Meters.
                  - `IN` — Inches.
                  - `FT` — Feet.
                  - `YD` — Yards.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="packageDimensions" type="Dimensions">

              Package dimensions (length, width, height of the shipping package).

              <Expandable title="packageDimensions properties">

                <ResponseField name="length" type="string">

                  Length. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="width" type="string">

                  Width. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="height" type="string">

                  Height. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="unit" type="string">

                  Unit of measurement for dimensions (length, width, height).

                  **Supported values:**

                  - `UNKNOWN_DIMENSIONS_UNIT`
                  - `MM` — Millimeters.
                  - `CM` — Centimeters.
                  - `M` — Meters.
                  - `IN` — Inches.
                  - `FT` — Feet.
                  - `YD` — Yards.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="stock" type="VariantStock">

          Variant inventory status.

          **Read-only.**

          <Expandable title="stock properties">

            <ResponseField name="trackQuantity" type="boolean">

              Whether inventory is being tracked.

            </ResponseField>

            <ResponseField name="quantity" type="integer">

              Quantity currently left in inventory.

            </ResponseField>

            <ResponseField name="inStock" type="boolean">

              Whether the product is currently in stock (relevant only when tracking manually).

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="lastUpdated" type="datetime">

      Date and time the product was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the product was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="seoData" type="SeoSchema">

      Custom SEO data for the product.

      <Expandable title="seoData properties">

        <ResponseField name="tags" type="Tag[]">

          SEO tag information.

          <Expandable title="tags properties">

            <ResponseField name="type" type="string">

              SEO tag type.

              Supported values: `title`, `meta`, `script`, `link`.

            </ResponseField>

            <ResponseField name="props" type="object">

              A `{"key": "value"}` pair object where each SEO tag property (`"name"`, `"content"`, `"rel"`, `"href"`) contains a value.
              For example: `{"name": "description", "content": "the description itself"}`.

            </ResponseField>

            <ResponseField name="meta" type="object">

              SEO tag metadata. For example, `{"height": 300, "width": 240}`.

            </ResponseField>

            <ResponseField name="children" type="string">

              SEO tag inner content. For example, `<title> inner content </title>`.

            </ResponseField>

            <ResponseField name="custom" type="boolean">

              Whether the tag is a [custom tag](https://support.wix.com/en/article/adding-additional-meta-tags-to-your-sites-pages) —
              i.e. an entry from the site's Advanced/Custom Tags list.

              This flag is scoped to that feature only. It is NOT an indicator of whether a standard tag
              (`title`, `description`, Open Graph, etc.) reflects a manually configured value versus a computed
              default/pattern — standard tags always resolve with `custom: false`, even when their content comes
              from a page's manually saved SEO title/description override. Don't use this field to verify whether
              a live page reflects a manual per-page SEO edit.

              Limitation: when `ResolveStaticPageSeoTags` is called without `seoData`, the per-page SEO data is read
              only from the Vibe/Wix-managed-headless override store. That store holds nothing for classic Wix Editor
              or Wix Studio pages, so even genuine Advanced/Custom Tags saved on those pages are missing from the
              response entirely — there, `false` means this API could not tell, not that no custom tag exists.
              Pass `seoData` explicitly to resolve against a known set of tags.

            </ResponseField>

            <ResponseField name="disabled" type="boolean">

              Whether the tag is disabled. If the tag is disabled, people can't find your page when searching for this phrase in search engines.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="settings" type="Settings">

          SEO general settings.

          <Expandable title="settings properties">

            <ResponseField name="preventAutoRedirect" type="boolean">

              Whether the [automatical redirect visits](https://support.wix.com/en/article/customizing-your-pages-seo-settings-in-the-seo-panel) from the old URL to the new one is enabled.

              Default: `false` (automatical redirect is enabled).

            </ResponseField>

            <ResponseField name="keywords" type="Keyword[]">

              User-selected keyword terms for a specific page.

              Constraints: `maxItems 5`.

              <Expandable title="keywords properties">

                <ResponseField name="term" type="string">

                  Keyword value.

                </ResponseField>

                <ResponseField name="isMain" type="boolean">

                  Whether the keyword is the main focus keyword.

                </ResponseField>

                <ResponseField name="origin" type="string">

                  The source that added the keyword terms to the SEO settings.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="ribbon" type="string">

      Product ribbon. Used to highlight relevant information about a product. For example, "Sale", "New Arrival", "Sold Out".

    </ResponseField>

    <ResponseField name="brand" type="string">

      Product brand. Including a brand name can help improve site and product [visibility on search engines](https://support.wix.com/en/article/adding-brand-names-to-boost-product-page-seo-in-wix-stores).

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "product": {
          "id": "91f7ac8b-2baa-289c-aa50-6d64764f35d3",
          "name": "Colombian Arabica",
          "slug": "colombian-arabica-1",
          "visible": true,
          "productType": "physical",
          "description": "<p>The best organic coffee that Colombia has to offer.</p>",
          "stock": {
            "trackInventory": false,
            "inStock": true,
            "inventoryStatus": "IN_STOCK"
          },
          "weightRange": {
            "minValue": 0.25,
            "maxValue": 1
          },
          "price": {
            "currency": "USD",
            "price": 35,
            "discountedPrice": 30,
            "formatted": {
              "price": "$35.00",
              "discountedPrice": "$30.00",
              "pricePerUnit": "$0.12"
            },
            "pricePerUnit": 0.12
          },
          "priceData": {
            "currency": "USD",
            "price": 35,
            "discountedPrice": 30,
            "formatted": {
              "price": "$35.00",
              "discountedPrice": "$30.00",
              "pricePerUnit": "$0.12"
            },
            "pricePerUnit": 0.12
          },
          "convertedPriceData": {
            "currency": "USD",
            "price": 35,
            "discountedPrice": 30,
            "formatted": {
              "price": "$35.00",
              "discountedPrice": "$30.00",
              "pricePerUnit": "$0.12"
            },
            "pricePerUnit": 0.12
          },
          "priceRange": {
            "minValue": 35,
            "maxValue": 70
          },
          "costRange": {
            "minValue": 20,
            "maxValue": 40
          },
          "pricePerUnitData": {
            "totalQuantity": 250,
            "totalMeasurementUnit": "G",
            "baseQuantity": 1,
            "baseMeasurementUnit": "G"
          },
          "additionalInfoSections": [
            {
              "title": "Storage recommendations",
              "description": "<p>To&nbsp;preserve&nbsp;your beans' fresh roasted flavor as long as possible, store them in an opaque, air-tight container at room temperature.</p>\n"
            }
          ],
          "ribbons": [
            {
              "text": "Organic and Fair trade"
            }
          ],
          "media": {
            "mainMedia": {
              "thumbnail": {
                "url": "https://static.wixstatic.com/media/nsplsh_5033504669385448625573~mv2_d_6000_3376_s_4_2.jpg/v1/fit/w_50,h_50,q_90/file.jpg",
                "width": 50,
                "height": 50
              },
              "mediaType": "image",
              "title": "",
              "image": {
                "url": "https://static.wixstatic.com/media/nsplsh_5033504669385448625573~mv2_d_6000_3376_s_4_2.jpg/v1/fit/w_6000,h_3376,q_90/file.jpg",
                "width": 6000,
                "height": 3376
              },
              "id": "nsplsh_5033504669385448625573~mv2_d_6000_3376_s_4_2.jpg"
            },
            "items": [
              {
                "thumbnail": {
                  "url": "https://static.wixstatic.com/media/nsplsh_5033504669385448625573~mv2_d_6000_3376_s_4_2.jpg/v1/fit/w_50,h_50,q_90/file.jpg",
                  "width": 50,
                  "height": 50
                },
                "mediaType": "image",
                "title": "",
                "image": {
                  "url": "https://static.wixstatic.com/media/nsplsh_5033504669385448625573~mv2_d_6000_3376_s_4_2.jpg/v1/fit/w_6000,h_3376,q_90/file.jpg",
                  "width": 6000,
                  "height": 3376
                },
                "id": "nsplsh_5033504669385448625573~mv2_d_6000_3376_s_4_2.jpg"
              }
            ]
          },
          "customTextFields": [
            {
              "title": "What would you like us to print on the custom label?",
              "maxLength": 200,
              "mandatory": false
            }
          ],
          "manageVariants": true,
          "productOptions": [
            {
              "optionType": "drop_down",
              "name": "Weight",
              "choices": [
                {
                  "value": "250g",
                  "description": "250g",
                  "media": {
                    "items": []
                  },
                  "inStock": true,
                  "visible": true
                },
                {
                  "value": "500g",
                  "description": "500g",
                  "media": {
                    "items": []
                  },
                  "inStock": true,
                  "visible": true
                }
              ]
            },
            {
              "optionType": "drop_down",
              "name": "Ground for",
              "choices": [
                {
                  "value": "Stovetop",
                  "description": "Stovetop",
                  "media": {
                    "items": []
                  },
                  "inStock": true,
                  "visible": true
                },
                {
                  "value": "Filter",
                  "description": "Filter",
                  "inStock": true,
                  "visible": true
                }
              ]
            }
          ],
          "productPageUrl": {
            "base": "https://wixsite.com/examplestore",
            "path": "/product-page/colombian-arabica-1"
          },
          "numericId": "1586693639134000",
          "inventoryItemId": "6e085374-d455-d763-55af-929b89b0ca2c",
          "discount": {
            "type": "AMOUNT",
            "value": 5
          },
          "collectionIds": [
            "32fd0b3a-2d38-2235-7754-78a3f819274a",
            "00000000-000000-000000-000000000001"
          ],
          "variants": [
            {
              "id": "00000000-0000-0020-0005-a316e6ba5b37",
              "choices": {
                "Weight": "250g",
                "Ground for": "Stovetop"
              },
              "variant": {
                "priceData": {
                  "currency": "USD",
                  "price": 35,
                  "discountedPrice": 30,
                  "formatted": {
                    "price": "$35.00",
                    "discountedPrice": "$30.00",
                    "pricePerUnit": "$0.12"
                  },
                  "pricePerUnit": 0.12
                },
                "convertedPriceData": {
                  "currency": "USD",
                  "price": 35,
                  "discountedPrice": 30,
                  "formatted": {
                    "price": "$35.00",
                    "discountedPrice": "$30.00",
                    "pricePerUnit": "$0.12"
                  },
                  "pricePerUnit": 0.12
                },
                "costAndProfitData": {
                  "itemCost": 20,
                  "formattedItemCost": "$20.00",
                  "profit": 10,
                  "formattedProfit": "$10.00",
                  "profitMargin": 0.3333
                },
                "weight": 0.25,
                "sku": "10001",
                "visible": true
              }
            },
            {
              "id": "00000000-0000-0021-0005-a316e6ba5b37",
              "choices": {
                "Weight": "250g",
                "Ground for": "Filter"
              },
              "variant": {
                "priceData": {
                  "currency": "USD",
                  "price": 35,
                  "discountedPrice": 30,
                  "formatted": {
                    "price": "$35.00",
                    "discountedPrice": "$30.00",
                    "pricePerUnit": "$0.12"
                  },
                  "pricePerUnit": 0.12
                },
                "convertedPriceData": {
                  "currency": "USD",
                  "price": 35,
                  "discountedPrice": 30,
                  "formatted": {
                    "price": "$35.00",
                    "discountedPrice": "$30.00",
                    "pricePerUnit": "$0.12"
                  },
                  "pricePerUnit": 0.12
                },
                "costAndProfitData": {
                  "itemCost": 20,
                  "formattedItemCost": "$20.00",
                  "profit": 10,
                  "formattedProfit": "$10.00",
                  "profitMargin": 0.3333
                },
                "weight": 0.25,
                "sku": "10003",
                "visible": true
              }
            },
            {
              "id": "00000000-0000-003f-0005-a316e6ba5b37",
              "choices": {
                "Weight": "500g",
                "Ground for": "Stovetop"
              },
              "variant": {
                "priceData": {
                  "currency": "USD",
                  "price": 65,
                  "discountedPrice": 60,
                  "formatted": {
                    "price": "$65.00",
                    "discountedPrice": "$60.00",
                    "pricePerUnit": "$0.24"
                  },
                  "pricePerUnit": 0.24
                },
                "convertedPriceData": {
                  "currency": "USD",
                  "price": 65,
                  "discountedPrice": 60,
                  "formatted": {
                    "price": "$65.00",
                    "discountedPrice": "$60.00",
                    "pricePerUnit": "$0.24"
                  },
                  "pricePerUnit": 0.24
                },
                "costAndProfitData": {
                  "itemCost": 40,
                  "formattedItemCost": "$40.00",
                  "profit": 20,
                  "formattedProfit": "$20.00",
                  "profitMargin": 0.3333
                },
                "weight": 0.5,
                "sku": "10002",
                "visible": true
              }
            },
            {
              "id": "00000000-0000-0040-0005-a316e6ba5b37",
              "choices": {
                "Weight": "500g",
                "Ground for": "Filter"
              },
              "variant": {
                "priceData": {
                  "currency": "USD",
                  "price": 70,
                  "discountedPrice": 65,
                  "formatted": {
                    "price": "$70.00",
                    "discountedPrice": "$65.00",
                    "pricePerUnit": "$0.26"
                  },
                  "pricePerUnit": 0.26
                },
                "convertedPriceData": {
                  "currency": "USD",
                  "price": 70,
                  "discountedPrice": 65,
                  "formatted": {
                    "price": "$70.00",
                    "discountedPrice": "$65.00",
                    "pricePerUnit": "$0.26"
                  },
                  "pricePerUnit": 0.26
                },
                "costAndProfitData": {
                  "itemCost": 40,
                  "formattedItemCost": "$40.00",
                  "profit": 25,
                  "formattedProfit": "$25.00",
                  "profitMargin": 0.3846
                },
                "weight": 1,
                "sku": "10004",
                "visible": true
              }
            }
          ],
          "lastUpdated": "2022-07-12T10:02:26.664Z",
          "createdDate": "2020-04-12T12:13:59.134Z",
          "seoData": {
            "tags": [
              {
                "type": "title",
                "children": "Colombian Arabica | Organic and Fair Trade",
                "custom": false,
                "disabled": false
              },
              {
                "type": "meta",
                "props": {
                  "name": "description",
                  "content": "The best organic fair trade coffee that Colombia has to offer."
                },
                "children": "",
                "custom": false,
                "disabled": false
              }
            ]
          },
          "ribbon": "Organic and Fair trade",
          "brand": "Coffee Company"
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Product ID (generated automatically by the catalog).

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Product name.

      Min: 1 character
      Max: 80 characters

    </ResponseField>

    <ResponseField name="slug" type="string">

      A friendly URL name (generated automatically by the catalog when a product is created), can be updated.

      Constraints: `maxLength 100`.

    </ResponseField>

    <ResponseField name="visible" type="boolean">

      Whether the product is visible to site visitors.

    </ResponseField>

    <ResponseField name="productType" type="string">

      Currently, only creating physical products ( `"productType": "physical"` ) is supported via the API.

      **Supported values:**

      - `unspecified_product_type`
      - `physical`
      - `digital`

    </ResponseField>

    <ResponseField name="description" type="string">

      Product description. Accepts [rich text](https://dev.wix.com/api/rest/wix-stores/rich-text).

    </ResponseField>

    <ResponseField name="sku" type="string">

      Stock keeping unit. If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, SKUs will be set per variant, and this field will be empty.

    </ResponseField>

    <ResponseField name="weight" type="number">

      Product weight. If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, weight will be set per variant, and this field will be empty.

    </ResponseField>

    <ResponseField name="weightRange" type="NumericPropertyRange">

      Product weight range. The minimum and maximum weights of all the variants.

      **Read-only.**

      <Expandable title="weightRange properties">

        <ResponseField name="minValue" type="number">

          Minimum value.

        </ResponseField>

        <ResponseField name="maxValue" type="number">

          Maximum value.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="productDimensions" type="Dimensions">

      Product dimensions (length, width, height of the physical product). If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, dimensions will be set per variant, and this field will be empty.

      <Expandable title="productDimensions properties">

        <ResponseField name="length" type="string">

          Length. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="width" type="string">

          Width. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="height" type="string">

          Height. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="unit" type="string">

          Unit of measurement for dimensions (length, width, height).

          **Supported values:**

          - `UNKNOWN_DIMENSIONS_UNIT`
          - `MM` — Millimeters.
          - `CM` — Centimeters.
          - `M` — Meters.
          - `IN` — Inches.
          - `FT` — Feet.
          - `YD` — Yards.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="packageDimensions" type="Dimensions">

      Package dimensions (length, width, height of the shipping package). If [variant management](https://support.wix.com/en/article/wix-stores-adding-and-customizing-product-options#setting-different-prices-for-variants) is enabled, dimensions will be set per variant, and this field will be empty.

      <Expandable title="packageDimensions properties">

        <ResponseField name="length" type="string">

          Length. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="width" type="string">

          Width. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="height" type="string">

          Height. Measurement unit is handled at the system level (metric/imperial).

        </ResponseField>

        <ResponseField name="unit" type="string">

          Unit of measurement for dimensions (length, width, height).

          **Supported values:**

          - `UNKNOWN_DIMENSIONS_UNIT`
          - `MM` — Millimeters.
          - `CM` — Centimeters.
          - `M` — Meters.
          - `IN` — Inches.
          - `FT` — Feet.
          - `YD` — Yards.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="stock" type="Stock">

      Product inventory status (in future this will be writable via Inventory API).

      **Read-only.**

      <Expandable title="stock properties">

        <ResponseField name="trackInventory" type="boolean">

          Whether inventory is being tracked

        </ResponseField>

        <ResponseField name="quantity" type="integer">

          Quantity currently left in inventory

        </ResponseField>

        <ResponseField name="inStock" type="boolean" deprecated>

          Whether the product is currently in stock (relevant only when tracking manually)
          Deprecated (use `inventoryStatus` instead)

          **Deprecated.**

        </ResponseField>

        <ResponseField name="inventoryStatus" type="string">

          The current status of the inventory
          + `IN_STOCK` - In stock
          + `OUT_OF_STOCK` - Not in stock
          + `PARTIALLY_OUT_OF_STOCK` - Some of the variants are not in stock

          **Supported values:**

          - `IN_STOCK` — In stock
          - `OUT_OF_STOCK` — Not in stock
          - `PARTIALLY_OUT_OF_STOCK` — Some of the variants are not in stock

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="price" type="PriceData" deprecated>

      Deprecated (use `priceData` instead).

      **Read-only.** **Deprecated.**

      <Expandable title="price properties">

        <ResponseField name="currency" type="string">

          Product price currency

          **Read-only.**

        </ResponseField>

        <ResponseField name="price" type="number">

          Product price

        </ResponseField>

        <ResponseField name="discountedPrice" type="number">

          Discounted product price (if no discounted price is set, the product price is returned)

          **Read-only.**

        </ResponseField>

        <ResponseField name="formatted" type="FormattedPrice">

          The product price and discounted price, formatted with the currency

          **Read-only.**

          <Expandable title="formatted properties">

            <ResponseField name="price" type="string">

              Product price formatted with the currency

            </ResponseField>

            <ResponseField name="discountedPrice" type="string">

              Discounted product price formatted with the currency (if no discounted price is set, the product formatted price is returned)

            </ResponseField>

            <ResponseField name="pricePerUnit" type="string">

              Price per unit

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pricePerUnit" type="number">

          Price per unit

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="priceData" type="PriceData">

      Price data.

      <Expandable title="priceData properties">

        <ResponseField name="currency" type="string">

          Product price currency

          **Read-only.**

        </ResponseField>

        <ResponseField name="price" type="number">

          Product price

        </ResponseField>

        <ResponseField name="discountedPrice" type="number">

          Discounted product price (if no discounted price is set, the product price is returned)

          **Read-only.**

        </ResponseField>

        <ResponseField name="formatted" type="FormattedPrice">

          The product price and discounted price, formatted with the currency

          **Read-only.**

          <Expandable title="formatted properties">

            <ResponseField name="price" type="string">

              Product price formatted with the currency

            </ResponseField>

            <ResponseField name="discountedPrice" type="string">

              Discounted product price formatted with the currency (if no discounted price is set, the product formatted price is returned)

            </ResponseField>

            <ResponseField name="pricePerUnit" type="string">

              Price per unit

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pricePerUnit" type="number">

          Price per unit

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="convertedPriceData" type="PriceData">

      Price data, converted to the currency specified in request header.

      **Read-only.**

      <Expandable title="convertedPriceData properties">

        <ResponseField name="currency" type="string">

          Product price currency

          **Read-only.**

        </ResponseField>

        <ResponseField name="price" type="number">

          Product price

        </ResponseField>

        <ResponseField name="discountedPrice" type="number">

          Discounted product price (if no discounted price is set, the product price is returned)

          **Read-only.**

        </ResponseField>

        <ResponseField name="formatted" type="FormattedPrice">

          The product price and discounted price, formatted with the currency

          **Read-only.**

          <Expandable title="formatted properties">

            <ResponseField name="price" type="string">

              Product price formatted with the currency

            </ResponseField>

            <ResponseField name="discountedPrice" type="string">

              Discounted product price formatted with the currency (if no discounted price is set, the product formatted price is returned)

            </ResponseField>

            <ResponseField name="pricePerUnit" type="string">

              Price per unit

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="pricePerUnit" type="number">

          Price per unit

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="priceRange" type="NumericPropertyRange">

      Product price range. The minimum and maximum prices of all the variants.

      **Read-only.**

      <Expandable title="priceRange properties">

        <ResponseField name="minValue" type="number">

          Minimum value.

        </ResponseField>

        <ResponseField name="maxValue" type="number">

          Maximum value.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="costAndProfitData" type="CostAndProfitData">

      Cost and profit data.

      <Expandable title="costAndProfitData properties">

        <ResponseField name="itemCost" type="number">

          Item cost.

        </ResponseField>

        <ResponseField name="formattedItemCost" type="string">

          Item cost formatted with currency symbol.

          **Read-only.**

        </ResponseField>

        <ResponseField name="profit" type="number">

          Profit. Calculated by reducing `cost` from `discounted_price`.

          **Read-only.** Constraints: `min 0`, `max 999999999.99`.

        </ResponseField>

        <ResponseField name="formattedProfit" type="string">

          Profit formatted with currency symbol.

          **Read-only.**

        </ResponseField>

        <ResponseField name="profitMargin" type="number">

          Profit Margin. Calculated by dividing `profit` by `discounted_price`.
          The result is rounded to 4 decimal places.

          **Read-only.** Constraints: `min 0`, `max 1`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="costRange" type="NumericPropertyRange">

      Product cost range. The minimum and maximum costs of all the variants.

      **Read-only.**

      <Expandable title="costRange properties">

        <ResponseField name="minValue" type="number">

          Minimum value.

        </ResponseField>

        <ResponseField name="maxValue" type="number">

          Maximum value.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="pricePerUnitData" type="PricePerUnitData">

      Price per unit data.

      <Expandable title="pricePerUnitData properties">

        <ResponseField name="totalQuantity" type="number">

          Total quantity

          Constraints: `min 0.01`, `max 999999999.99`.

        </ResponseField>

        <ResponseField name="totalMeasurementUnit" type="string">

          Total measurement unit

          **Supported values:**

          - `UNSPECIFIED`
          - `ML`
          - `CL`
          - `L`
          - `CBM`
          - `MG`
          - `G`
          - `KG`
          - `MM`
          - `CM`
          - `M`
          - `SQM`
          - `OZ`
          - `LB`
          - `FLOZ`
          - `PT`
          - `QT`
          - `GAL`
          - `IN`
          - `FT`
          - `YD`
          - `SQFT`

        </ResponseField>

        <ResponseField name="baseQuantity" type="number">

          Base quantity

          Constraints: `min 0.01`, `max 999999999.99`.

        </ResponseField>

        <ResponseField name="baseMeasurementUnit" type="string">

          Base measurement unit

          **Supported values:**

          - `UNSPECIFIED`
          - `ML`
          - `CL`
          - `L`
          - `CBM`
          - `MG`
          - `G`
          - `KG`
          - `MM`
          - `CM`
          - `M`
          - `SQM`
          - `OZ`
          - `LB`
          - `FLOZ`
          - `PT`
          - `QT`
          - `GAL`
          - `IN`
          - `FT`
          - `YD`
          - `SQFT`

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalInfoSections" type="AdditionalInfoSection[]">

      Additional text that the store owner can assign to the product (e.g. shipping details, refund policy, etc.).

      <Expandable title="additionalInfoSections properties">

        <ResponseField name="title" type="string">

          Product info section title

          Constraints: `maxLength 50`.

        </ResponseField>

        <ResponseField name="description" type="string">

          Product info section description

          Constraints: `maxLength 16000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="ribbons" type="Ribbon[]" deprecated>

      Deprecated (use `ribbon` instead).

      **Read-only.** **Deprecated.**

      <Expandable title="ribbons properties">

        <ResponseField name="text" type="string">

          Ribbon text

          Constraints: `maxLength 30`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="media" type="Media">

      Media items (images, videos etc) associated with this product (writable via [Add Product Media](https://dev.wix.com/api/rest/wix-stores/catalog/products/add-product-media) endpoint).

      **Read-only.**

      <Expandable title="media properties">

        <ResponseField name="mainMedia" type="MediaItem">

          Primary media (image, video etc) associated with this product.

          <Expandable title="mainMedia properties">

            <ResponseField name="thumbnail" type="MediaItemUrlAndSize">

              Media item thumbnail details.

              <Expandable title="thumbnail properties">

                <ResponseField name="url" type="string">

                  Media item URL.

                  Constraints: `format WEB_URL`.

                </ResponseField>

                <ResponseField name="width" type="integer">

                  Media item width.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="height" type="integer">

                  Media item height.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="format" type="string">

                  Media format (mp4, png, etc.).

                </ResponseField>

                <ResponseField name="altText" type="string">

                  Alt text. This text will be shown in case the image is not available.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="mediaType" type="string">

              Media item type (image, video, etc.).

              **Supported values:**

              - `unspecified_media_item_type`
              - `image` — Image media type.
              - `video` — Video media type.
              - `audio` — Audio media type.
              - `document` — Document media type.
              - `zip` — Zip media type.

            </ResponseField>

            <ResponseField name="title" type="string">

              Media item title.

            </ResponseField>

            <ResponseField name="_id" type="string">

              Media ID (for example, `"nsplsh_306d666a123a4a74306459~mv2_d_4517_2992_s_4_2.jpg"`).

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="item" type="item">

              item

              <Expandable title="item properties">

                <ResponseField name="image" type="MediaItemUrlAndSize">

                  Image data (URL, size).

                  _Nested structure omitted for brevity — see the `MediaItemUrlAndSize` object._

                </ResponseField>

                <ResponseField name="video" type="MediaItemVideo">

                  Video data (URL, size).

                  _Nested structure omitted for brevity — see the `MediaItemVideo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="items" type="MediaItem[]">

          Media (images, videos etc) associated with this product.

          <Expandable title="items properties">

            <ResponseField name="thumbnail" type="MediaItemUrlAndSize">

              Media item thumbnail details.

              <Expandable title="thumbnail properties">

                <ResponseField name="url" type="string">

                  Media item URL.

                  Constraints: `format WEB_URL`.

                </ResponseField>

                <ResponseField name="width" type="integer">

                  Media item width.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="height" type="integer">

                  Media item height.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="format" type="string">

                  Media format (mp4, png, etc.).

                </ResponseField>

                <ResponseField name="altText" type="string">

                  Alt text. This text will be shown in case the image is not available.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="mediaType" type="string">

              Media item type (image, video, etc.).

              **Supported values:**

              - `unspecified_media_item_type`
              - `image` — Image media type.
              - `video` — Video media type.
              - `audio` — Audio media type.
              - `document` — Document media type.
              - `zip` — Zip media type.

            </ResponseField>

            <ResponseField name="title" type="string">

              Media item title.

            </ResponseField>

            <ResponseField name="_id" type="string">

              Media ID (for example, `"nsplsh_306d666a123a4a74306459~mv2_d_4517_2992_s_4_2.jpg"`).

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="customTextFields" type="CustomTextField[]">

      Text box for the customer to add a message to their order (e.g., customization request). Currently writable only from the UI.

      **Read-only.**

      <Expandable title="customTextFields properties">

        <ResponseField name="title" type="string">

          Text box title

          Constraints: `maxLength 100`.

        </ResponseField>

        <ResponseField name="maxLength" type="integer">

          Text box input max length

          Constraints: `max 500`.

        </ResponseField>

        <ResponseField name="mandatory" type="boolean">

          Whether this text box is mandatory

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="manageVariants" type="boolean">

      Whether variants are being managed for this product - enables unique SKU, price and weight per variant. Also affects inventory data.

    </ResponseField>

    <ResponseField name="productOptions" type="ProductOption[]">

      Options for this product.

      Constraints: `maxItems 6`.

      <Expandable title="productOptions properties">

        <ResponseField name="optionType" type="string">

          Option type.

          **Read-only.**

          **Supported values:**

          - `unspecified_option_type` — Unspecified option type.
          - `drop_down` — Drop down.
          - `color` — Color.

        </ResponseField>

        <ResponseField name="name" type="string">

          Option name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ResponseField>

        <ResponseField name="choices" type="Choice[]">

          Choices available for this option.

          Constraints: `minItems 1`, `maxItems 100`.

          <Expandable title="choices properties">

            <ResponseField name="value" type="string">

              Choice value.

              Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="description" type="string">

              Choice description.

              Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="media" type="Media">

              Media items (images, videos) associated with this choice

              **Read-only.**

              <Expandable title="media properties">

                <ResponseField name="mainMedia" type="MediaItem">

                  Primary media (image, video etc) associated with this product.

                  _Nested structure omitted for brevity — see the `MediaItem` object._

                </ResponseField>

                <ResponseField name="items" type="MediaItem[]">

                  Media (images, videos etc) associated with this product.

                  _Nested structure omitted for brevity — see the `MediaItem[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="inStock" type="boolean">

              Based on the customer’s choices, which (if any) variants that include the selected choices are in stock

              **Read-only.**

            </ResponseField>

            <ResponseField name="visible" type="boolean">

              Based on the customer’s choices, which (if any) variants that include the selected choices are visible

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="productPageUrl" type="PageUrl">

      Product page URL for this product (generated automatically by the server).

      **Read-only.**

      <Expandable title="productPageUrl properties">

        <ResponseField name="base" type="string">

          Base URL. For premium sites, this is the domain. For free sites, this is the site URL (e.g mysite.wixsite.com/mysite).

        </ResponseField>

        <ResponseField name="path" type="string">

          Path to the product page - e.g /product-page/a-product.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="numericId" type="number">

      Product’s unique numeric ID (assigned in ascending order).
      Primarily used for sorting and filtering when crawling all products.

      **Read-only.**

    </ResponseField>

    <ResponseField name="inventoryItemId" type="string">

      Inventory item ID - ID referencing the inventory system.

      **Read-only.**

    </ResponseField>

    <ResponseField name="discount" type="Discount">

      Discount deducted from the product's original price.

      <Expandable title="discount properties">

        <ResponseField name="type" type="string">

          Discount type:
          + `"AMOUNT"`
          + `"PERCENT"`

          **Supported values:**

          - `UNDEFINED`
          - `NONE` — No discount
          - `AMOUNT` — Discount by a fixed amount
          - `PERCENT` — Discount by a percentage

        </ResponseField>

        <ResponseField name="value" type="number">

          Discount value

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="collectionIds" type="string[]">

      A list of all collection IDs that this product is included in (writable via the Catalog > Collection APIs).

      **Read-only.**

    </ResponseField>

    <ResponseField name="variants" type="Variant[]">

      Product variants, will be provided if the request was sent with the `includeVariants: true`.

      Max: 1,000 variants

      **Read-only.** Constraints: `maxItems 1000`.

      <Expandable title="variants properties">

        <ResponseField name="_id" type="string">

          Requested Variant ID

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="choices" type="object">

          Specific choices within a selection, as option-choice key-value pairs

          Constraints: `format map`.

          <Expandable title="choices properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="string" />

          </Expandable>

        </ResponseField>

        <ResponseField name="variant" type="VariantDataWithNoStock">

          variant

          <Expandable title="variant properties">

            <ResponseField name="priceData" type="PriceData">

              Variant price.

              <Expandable title="priceData properties">

                <ResponseField name="currency" type="string">

                  Product price currency

                  **Read-only.**

                </ResponseField>

                <ResponseField name="price" type="number">

                  Product price

                </ResponseField>

                <ResponseField name="discountedPrice" type="number">

                  Discounted product price (if no discounted price is set, the product price is returned)

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formatted" type="FormattedPrice">

                  The product price and discounted price, formatted with the currency

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `FormattedPrice` object._

                </ResponseField>

                <ResponseField name="pricePerUnit" type="number">

                  Price per unit

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="convertedPriceData" type="PriceData">

              Variant price data, converted to currency requested in header.

              **Read-only.**

              <Expandable title="convertedPriceData properties">

                <ResponseField name="currency" type="string">

                  Product price currency

                  **Read-only.**

                </ResponseField>

                <ResponseField name="price" type="number">

                  Product price

                </ResponseField>

                <ResponseField name="discountedPrice" type="number">

                  Discounted product price (if no discounted price is set, the product price is returned)

                  **Read-only.**

                </ResponseField>

                <ResponseField name="formatted" type="FormattedPrice">

                  The product price and discounted price, formatted with the currency

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `FormattedPrice` object._

                </ResponseField>

                <ResponseField name="pricePerUnit" type="number">

                  Price per unit

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="costAndProfitData" type="CostAndProfitData">

              Cost and profit data.

              <Expandable title="costAndProfitData properties">

                <ResponseField name="itemCost" type="number">

                  Item cost.

                </ResponseField>

                <ResponseField name="formattedItemCost" type="string">

                  Item cost formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="profit" type="number">

                  Profit. Calculated by reducing `cost` from `discounted_price`.

                  **Read-only.** Constraints: `min 0`, `max 999999999.99`.

                </ResponseField>

                <ResponseField name="formattedProfit" type="string">

                  Profit formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="profitMargin" type="number">

                  Profit Margin. Calculated by dividing `profit` by `discounted_price`.
                  The result is rounded to 4 decimal places.

                  **Read-only.** Constraints: `min 0`, `max 1`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="weight" type="number">

              Variant weight.

            </ResponseField>

            <ResponseField name="sku" type="string">

              Variant SKU (stock keeping unit).

            </ResponseField>

            <ResponseField name="visible" type="boolean">

              Whether the variant is visible to customers.

            </ResponseField>

            <ResponseField name="productDimensions" type="Dimensions">

              Product dimensions (length, width, height of the physical product).

              <Expandable title="productDimensions properties">

                <ResponseField name="length" type="string">

                  Length. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="width" type="string">

                  Width. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="height" type="string">

                  Height. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="unit" type="string">

                  Unit of measurement for dimensions (length, width, height).

                  **Supported values:**

                  - `UNKNOWN_DIMENSIONS_UNIT`
                  - `MM` — Millimeters.
                  - `CM` — Centimeters.
                  - `M` — Meters.
                  - `IN` — Inches.
                  - `FT` — Feet.
                  - `YD` — Yards.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="packageDimensions" type="Dimensions">

              Package dimensions (length, width, height of the shipping package).

              <Expandable title="packageDimensions properties">

                <ResponseField name="length" type="string">

                  Length. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="width" type="string">

                  Width. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="height" type="string">

                  Height. Measurement unit is handled at the system level (metric/imperial).

                </ResponseField>

                <ResponseField name="unit" type="string">

                  Unit of measurement for dimensions (length, width, height).

                  **Supported values:**

                  - `UNKNOWN_DIMENSIONS_UNIT`
                  - `MM` — Millimeters.
                  - `CM` — Centimeters.
                  - `M` — Meters.
                  - `IN` — Inches.
                  - `FT` — Feet.
                  - `YD` — Yards.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="stock" type="VariantStock">

          Variant inventory status.

          **Read-only.**

          <Expandable title="stock properties">

            <ResponseField name="trackQuantity" type="boolean">

              Whether inventory is being tracked.

            </ResponseField>

            <ResponseField name="quantity" type="integer">

              Quantity currently left in inventory.

            </ResponseField>

            <ResponseField name="inStock" type="boolean">

              Whether the product is currently in stock (relevant only when tracking manually).

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="lastUpdated" type="datetime">

      Date and time the product was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the product was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="seoData" type="SeoSchema">

      Custom SEO data for the product.

      <Expandable title="seoData properties">

        <ResponseField name="tags" type="Tag[]">

          SEO tag information.

          <Expandable title="tags properties">

            <ResponseField name="type" type="string">

              SEO tag type.

              Supported values: `title`, `meta`, `script`, `link`.

            </ResponseField>

            <ResponseField name="props" type="object">

              A `{"key": "value"}` pair object where each SEO tag property (`"name"`, `"content"`, `"rel"`, `"href"`) contains a value.
              For example: `{"name": "description", "content": "the description itself"}`.

            </ResponseField>

            <ResponseField name="meta" type="object">

              SEO tag metadata. For example, `{"height": 300, "width": 240}`.

            </ResponseField>

            <ResponseField name="children" type="string">

              SEO tag inner content. For example, `<title> inner content </title>`.

            </ResponseField>

            <ResponseField name="custom" type="boolean">

              Whether the tag is a [custom tag](https://support.wix.com/en/article/adding-additional-meta-tags-to-your-sites-pages) —
              i.e. an entry from the site's Advanced/Custom Tags list.

              This flag is scoped to that feature only. It is NOT an indicator of whether a standard tag
              (`title`, `description`, Open Graph, etc.) reflects a manually configured value versus a computed
              default/pattern — standard tags always resolve with `custom: false`, even when their content comes
              from a page's manually saved SEO title/description override. Don't use this field to verify whether
              a live page reflects a manual per-page SEO edit.

              Limitation: when `ResolveStaticPageSeoTags` is called without `seoData`, the per-page SEO data is read
              only from the Vibe/Wix-managed-headless override store. That store holds nothing for classic Wix Editor
              or Wix Studio pages, so even genuine Advanced/Custom Tags saved on those pages are missing from the
              response entirely — there, `false` means this API could not tell, not that no custom tag exists.
              Pass `seoData` explicitly to resolve against a known set of tags.

            </ResponseField>

            <ResponseField name="disabled" type="boolean">

              Whether the tag is disabled. If the tag is disabled, people can't find your page when searching for this phrase in search engines.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="settings" type="Settings">

          SEO general settings.

          <Expandable title="settings properties">

            <ResponseField name="preventAutoRedirect" type="boolean">

              Whether the [automatical redirect visits](https://support.wix.com/en/article/customizing-your-pages-seo-settings-in-the-seo-panel) from the old URL to the new one is enabled.

              Default: `false` (automatical redirect is enabled).

            </ResponseField>

            <ResponseField name="keywords" type="Keyword[]">

              User-selected keyword terms for a specific page.

              Constraints: `maxItems 5`.

              <Expandable title="keywords properties">

                <ResponseField name="term" type="string">

                  Keyword value.

                </ResponseField>

                <ResponseField name="isMain" type="boolean">

                  Whether the keyword is the main focus keyword.

                </ResponseField>

                <ResponseField name="origin" type="string">

                  The source that added the keyword terms to the SEO settings.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="ribbon" type="string">

      Product ribbon. Used to highlight relevant information about a product. For example, "Sale", "New Arrival", "Sold Out".

    </ResponseField>

    <ResponseField name="brand" type="string">

      Product brand. Including a brand name can help improve site and product [visibility on search engines](https://support.wix.com/en/article/adding-brand-names-to-boost-product-page-seo-in-wix-stores).

    </ResponseField>

  </Tab>

</Tabs>
