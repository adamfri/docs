---
title: "Query Programs"
description: "Retrieves a list of up to 50 programs, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 50 programs, given the provided paging, filtering, and sorting.

    Query Programs runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.limit` is `50`.
    - `paging.offset` is `0`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    Results are visibility-filtered according to the caller identity.

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$in` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$in` | Sort: `ASC`, `DESC` |
    | `categoryIds` | `$hasAll`, `$hasSome`, `$isEmpty` | None |
    | `seo.slug` | `$eq` | Sort: `ASC`, `DESC` |
    | `createdDate` |  | Sort: `ASC`, `DESC` |
    | `statusUpdatedDate` |  | Sort: `ASC`, `DESC` |
    | `socialGroupId` | `$eq`, `$in` | None |
    | `timeline.selfPaced` | `$eq` | None |
    | `price` | `$exists` | None |
    | `restrictions.accessType` | `$in` | None |
    | `description.title` |  | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/online-programs/v3/programs/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2">

      Query used to filter, sort, and page programs.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 1000`, `maxItems 100`.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 1000`, `maxItems 100`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="programs" type="Program[]">

      Programs that match the query and caller visibility.

      See the [Program Object](/api-reference/business-management/online-programs/programs/program-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata for the query response.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves published programs sorted by creation date.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/online-programs/v3/programs/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "status": {
              "$eq": "PUBLISHED"
            }
          },
          "sort": [
            {
              "fieldName": "createdDate",
              "order": "DESC"
            }
          ],
          "paging": {
            "limit": 20
          }
        }
      }'
      ```

      ```json Response
      {
        "programs": [
          {
            "id": "5ae8e5b5-f172-423e-81c8-0e4625f54dd9",
            "revision": "7",
            "createdDate": "2026-06-01T11:33:51.742Z",
            "updatedDate": "2026-06-01T12:42:08.909Z",
            "status": "PUBLISHED",
            "statusUpdatedDate": "2026-06-01T11:38:37.542Z",
            "categoryIds": [],
            "description": {
              "title": "Mastering English Pronunciation & Accent",
              "details": "A practical program for improving English pronunciation and accent clarity."
            },
            "timeline": {
              "selfPaced": false,
              "durationInDays": 6
            },
            "restrictions": {
              "hideFutureSteps": false,
              "resolveStepsInOrder": false,
              "shareProgress": true,
              "accessType": "SECRET"
            },
            "seo": {
              "slug": "mastering-english-pronunciation",
              "url": {
                "base": "https://example.wixsite.com/site",
                "path": "/challenge-page/mastering-english-pronunciation"
              },
              "courseWorkload": "P6D"
            },
            "rewards": [],
            "shouldSendInvoice": false,
            "videoStepsSettings": {
              "autoNavigate": false,
              "requiredCompletionPercentage": 0
            },
            "contentSummary": {
              "stepCount": 23,
              "sectionCount": 12
            },
            "ownerUserId": "2b2f5a4d-93f1-4d71-a8a6-10235c50d9f3"
          },
          {
            "id": "82814e00-dc1c-468b-ba80-6c0ed4bd3ee3",
            "revision": "6",
            "createdDate": "2026-06-01T10:31:01.144Z",
            "updatedDate": "2026-06-01T12:42:53.482Z",
            "status": "PUBLISHED",
            "statusUpdatedDate": "2026-06-01T10:31:02.882Z",
            "categoryIds": [],
            "description": {
              "title": "Effective English Learning for Professionals"
            },
            "timeline": {
              "selfPaced": true
            },
            "restrictions": {
              "hideFutureSteps": false,
              "resolveStepsInOrder": false,
              "shareProgress": true,
              "accessType": "PUBLIC"
            },
            "price": {
              "value": "25",
              "currency": "USD"
            },
            "seo": {
              "slug": "effective-english-learning-for-professionals",
              "courseWorkload": "P6D"
            },
            "rewards": [
              {
                "trigger": "ALL_STEPS_COMPLETED",
                "badgeIds": [
                  "ebcd57c1-0f9d-4090-a13b-d19deffecd16"
                ]
              }
            ],
            "shouldSendInvoice": false,
            "videoStepsSettings": {
              "autoNavigate": false,
              "requiredCompletionPercentage": 0
            },
            "contentSummary": {
              "stepCount": 6,
              "sectionCount": 2
            },
            "ownerUserId": "2b2f5a4d-93f1-4d71-a8a6-10235c50d9f3"
          }
        ],
        "pagingMetadata": {
          "count": 4,
          "total": 4
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 50 programs, given the provided paging, filtering, and sorting.

    Query Programs runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.limit` is `50`.
    - `paging.offset` is `0`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    Results are visibility-filtered according to the caller identity.

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$in` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$in` | Sort: `ASC`, `DESC` |
    | `categoryIds` | `$hasAll`, `$hasSome`, `$isEmpty` | None |
    | `seo.slug` | `$eq` | Sort: `ASC`, `DESC` |
    | `_createdDate` |  | Sort: `ASC`, `DESC` |
    | `statusUpdatedDate` |  | Sort: `ASC`, `DESC` |
    | `socialGroupId` | `$eq`, `$in` | None |
    | `timeline.selfPaced` | `$eq` | None |
    | `price` | `$exists` | None |
    | `restrictions.accessType` | `$in` | None |
    | `description.title` |  | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/online-programs';

    programs.queryPrograms(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="ProgramQuery" required>

      Query used to filter, sort, and page programs.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

          Constraints: `maxItems 100`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="programs" type="Program[]">

      Programs that match the query and caller visibility.

      See the [Program Object](/api-reference/business-management/online-programs/programs/program-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata for the query response.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-programs" id="ex-sdk-query-programs-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-programs" id="ex-sdk-query-programs-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-programs-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-programs-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryPrograms</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPrograms (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-programs-0"><span className="wix-picker-name">queryPrograms</span></label>
    <label htmlFor="ex-sdk-query-programs-1"><span className="wix-picker-name">queryPrograms (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryPrograms
    import { programs } from '@wix/online-programs';

    async function queryPrograms(query) {
      const response = await programs.queryPrograms(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPrograms (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function queryPrograms(query) {
      const response = await myWixClient.programs.queryPrograms(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
