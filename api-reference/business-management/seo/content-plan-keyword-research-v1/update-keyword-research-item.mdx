---
title: "Update Keyword Research Item"
description: "Updates a keyword research item."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates a keyword research item.

    `fieldMask` is required and must name at least one of `keyword` or
    `mainKeyword`. Those are the only writable fields, and an omitted or
    empty mask is rejected rather than treated as a request to write
    everything.

    `mainKeyword` only promotes. Set it to `true` to make the item its page's
    main keyword. Setting it to `false`, or leaving it out, changes nothing,
    so a main keyword can't be demoted with this method.

    This method updates an item that already exists. It can't add or delete a
    keyword.

    The first write to AI-generated research copies that research instead of
    changing it, so the response can carry a different `keywordResearchId`.
    Always send the ID from the most recent response on your next write.
    Sending the original ID again copies the research again.

    Changing `keyword` also refreshes that item's `searchVolume` and
    `competition`, even though the mask doesn't name them.

    Edits don't survive a later generation. A generation that completes after
    your edit produces fresh research that replaces the edited copy in every
    read.

    To update multiple keyword research items in a single API call, call
    [Bulk Update Keyword Research Items](https://dev.wix.com/docs/api-reference/business-management/seo/content-plan-keyword-research-v1/bulk-update-keyword-research-items).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/promote/seo/v1/keyword-research-items/{item.id}
    ```

    ## Path Params

    <ParamField path="item.id" type="string" required>

      ID of the keyword research item to update.

    </ParamField>

    ## Body Params

    <ParamField body="keywordResearchId" type="string" required>

      ID of the keyword research the item belongs to.

      Use the `keywordResearchId` from your most recent read or write.

    </ParamField>

    <ParamField body="item" type="UpdatedKeywordResearch" required>

      Keyword research item to update.

      <Expandable title="item properties">

        <ParamField body="id" type="string" required>

          ID of the keyword research item to update.

        </ParamField>

        <ParamField body="keyword" type="string">

          New keyword.

          Rejected if it's longer than 80 characters, or if it duplicates another
          keyword in the same research. Changing it also refreshes the item's
          `searchVolume` and `competition`.

        </ParamField>

        <ParamField body="mainKeyword" type="boolean">

          Whether to make this item its page's main keyword.

          Set to `true` to promote it. `false` and omitting the field both leave
          the current main keyword unchanged, so this field can't demote one.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fieldMask" type="FieldMask" required>

      Fields to update.

      Must name at least one of `keyword` or `mainKeyword`, the only writable
      fields. An omitted or empty mask is rejected.

      <Expandable title="fieldMask properties">

        <ParamField body="paths" type="string[]" />

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="keywordResearchItem" type="KeywordResearch">

      Updated keyword research item.

      See the [Content Plan Keyword Research Object](/api-reference/business-management/seo/content-plan-keyword-research-v1/content-plan-keyword-research-object) for this object's fields.

    </ResponseField>

    <ResponseField name="keywordResearchId" type="string">

      ID of the keyword research holding the updated item.

      This can differ from the ID you sent, because the first edit to
      generated research copies it. Use this value for your next write.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    The first edit to generated research copies it, so the returned keywordResearchId differs from the one sent. Use the returned ID for the next write.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/seo-content-plan-service/v1/keyword-research-items/b70d5a92-31ef-4c08-8a6d-45f19be7c203' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "keywordResearchId": "3f9c2b71-4d8e-4a15-9c63-b0e7d21a5f84",
        "item": {
          "id": "b70d5a92-31ef-4c08-8a6d-45f19be7c203",
          "keyword": "how to store coffee beans at home"
        },
        "fieldMask": "keyword"
      }'
      ```

      ```json Response
      {
        "keywordResearchItem": {
          "id": "b70d5a92-31ef-4c08-8a6d-45f19be7c203",
          "keyword": "how to store coffee beans at home",
          "pageId": "c1f83b57",
          "itemType": "STATIC_PAGE",
          "pageUrl": "https://www.example.com/shop/coffee-beans",
          "competition": 12,
          "searchVolume": 880,
          "pageDeleted": false,
          "primary": false,
          "clusterName": "coffee beans"
        },
        "keywordResearchId": "e5a71c38-9b40-4d26-83f7-6c02de914ab5"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates a keyword research item.

    `fieldMask` is required and must name at least one of `keyword` or
    `mainKeyword`. Those are the only writable fields, and an omitted or
    empty mask is rejected rather than treated as a request to write
    everything.

    `mainKeyword` only promotes. Set it to `true` to make the item its page's
    main keyword. Setting it to `false`, or leaving it out, changes nothing,
    so a main keyword can't be demoted with this method.

    This method updates an item that already exists. It can't add or delete a
    keyword.

    The first write to AI-generated research copies that research instead of
    changing it, so the response can carry a different `keywordResearchId`.
    Always send the ID from the most recent response on your next write.
    Sending the original ID again copies the research again.

    Changing `keyword` also refreshes that item's `searchVolume` and
    `competition`, even though the mask doesn't name them.

    Edits don't survive a later generation. A generation that completes after
    your edit produces fresh research that replaces the edited copy in every
    read.

    To update multiple keyword research items in a single API call, call
    [Bulk Update Keyword Research Items](https://dev.wix.com/docs/api-reference/business-management/seo/content-plan-keyword-research-v1/bulk-update-keyword-research-items).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { keywordResearch } from '@wix/seo';

    keywordResearch.updateKeywordResearchItem(_id, keywordResearchId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the keyword research item to update.

    </ParamField>

    <ParamField body="keywordResearchId" type="UpdateKeywordResearchItem" required>

      ID of the keyword research the item belongs to.

      Use the `keywordResearchId` from your most recent read or write.

      Constraints: `format GUID`.

      <Expandable title="keywordResearchId properties">

        <ParamField body="value" type="string" />

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateKeywordResearchItemOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="item" type="UpdatedKeywordResearch" required>

          Keyword research item to update.

          <Expandable title="item properties">

            <ParamField body="_id" type="string">

              ID of the keyword research item to update.

            </ParamField>

            <ParamField body="keyword" type="string">

              New keyword.

              Rejected if it's longer than 80 characters, or if it duplicates another
              keyword in the same research. Changing it also refreshes the item's
              `searchVolume` and `competition`.

            </ParamField>

            <ParamField body="mainKeyword" type="boolean">

              Whether to make this item its page's main keyword.

              Set to `true` to promote it. `false` and omitting the field both leave
              the current main keyword unchanged, so this field can't demote one.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fieldMask" type="FieldMask" required>

          Fields to update.

          Must name at least one of `keyword` or `mainKeyword`, the only writable
          fields. An omitted or empty mask is rejected.

          <Expandable title="fieldMask properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="keywordResearchItem" type="KeywordResearch">

      Updated keyword research item.

      See the [Content Plan Keyword Research Object](/api-reference/business-management/seo/content-plan-keyword-research-v1/content-plan-keyword-research-object) for this object's fields.

    </ResponseField>

    <ResponseField name="keywordResearchId" type="string">

      ID of the keyword research holding the updated item.

      This can differ from the ID you sent, because the first edit to
      generated research copies it. Use this value for your next write.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-keyword-research-item" id="ex-sdk-update-keyword-research-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-keyword-research-item" id="ex-sdk-update-keyword-research-item-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-keyword-research-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-keyword-research-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Change a keyword and make it its page's main keyword</span><span className="wix-picker-desc">fieldMask is required and accepts only the paths keyword and main_keyword. Field mask paths use the API's field names, so main_keyword is correct here even though the property you set is mainKeyword. The response can carry a different keywordResearchId, because the first write to generated research copies it. Send the ID from the response on your next write</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateKeywordResearchItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-keyword-research-item-0"><span className="wix-picker-name">Change a keyword and make it its page's main keyword</span><span className="wix-picker-desc">fieldMask is required and accepts only the paths keyword and main_keyword. Field mask paths use the API's field names, so main_keyword is correct here even though the property you set is mainKeyword. The response can carry a different keywordResearchId, because the first write to generated research copies it. Send the ID from the response on your next write</span></label>
    <label htmlFor="ex-sdk-update-keyword-research-item-1"><span className="wix-picker-name">updateKeywordResearchItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Change a keyword and make it its page's main keyword
    import { keywordResearch } from "@wix/seo";

    async function updateKeywordResearchItem() {
      const response = await keywordResearch.updateKeywordResearchItem(
        "c5f9e1a4-7b62-4d18-9a35-1e8f4c2d6b70",
        "8f3b6d2e-4c17-4a90-b5e3-9d70c1af2b48",
        {
          item: {
            keyword: "hand thrown ceramic mugs",
            mainKeyword: true,
          },
          fieldMask: ["keyword", "main_keyword"],
        },
      );
    }

    /* Promise resolves to:
     * {
     *   "keywordResearchItem": {
     *     "_id": "c5f9e1a4-7b62-4d18-9a35-1e8f4c2d6b70",
     *     "keyword": "hand thrown ceramic mugs",
     *     "pageId": "a1b2c3d4-e5f6-4890-abcd-ef1234567890",
     *     "itemType": "STATIC_PAGE",
     *     "pageUrl": "https://example.com/shop",
     *     "competition": 28,
     *     "searchVolume": 1300,
     *     "pageDeleted": false,
     *     "primary": true,
     *     "clusterName": "ceramic drinkware"
     *   },
     *   "keywordResearchId": "b2c4e8a0-5f31-4276-9c8d-3a1e7b90fd52"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateKeywordResearchItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { keywordResearch } from '@wix/seo';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { keywordResearch },
      // Include the auth strategy and host as relevant
    });

    async function updateKeywordResearchItem(_id,keywordResearchId,options) {
      const response = await myWixClient.keywordResearch.updateKeywordResearchItem(_id,keywordResearchId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
