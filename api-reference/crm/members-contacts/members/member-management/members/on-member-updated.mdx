---
title: "onMemberUpdated"
description: "The onMemberUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.members.v1.member_updated` |
| Package | `@wix/members` |
| Permissions | `Manage Members`, `Manage Bookings Services and Settings`, `Manage Events`, `Manage Portfolio`, `Access Verticals by Automations`, `Read Members and Contacts - all read permissions`, `Manage Restaurants - all permissions`, `Read Members`, `Set Up Automations`, `Manage Members and Contacts - all permissions` |

## Examples

<CodeGroup>

  ```javascript onMemberUpdated: Wix-hosted
  import { members } from '@wix/members';

  members.onMemberUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onMemberUpdated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { members } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        members,
      },
  });

  wixClient.members.onMemberUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
