---
title: "User Object"
description: "The User object in the Wix User API."
icon: "cube"
---

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      User ID.

    </ResponseField>

    <ResponseField name="roles" type="string[]" deprecated>

      Deprecated.

      **Deprecated.**

    </ResponseField>

    <ResponseField name="email" type="string">

      User's email address.

    </ResponseField>

    <ResponseField name="name" type="Name">

      User's name.

      <Expandable title="name properties">

        <ResponseField name="firstName" type="string">

          User's first name.

        </ResponseField>

        <ResponseField name="lastName" type="string">

          User's last name.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="profileImage" type="string">

      URL to user's profile image, when provided.

    </ResponseField>

    <ResponseField name="joinedTeamAt" type="datetime">

      Date the user joined the team.

    </ResponseField>

    <ResponseField name="policyIds" type="string[]" deprecated>

      Deprecated.

      **Deprecated.**

    </ResponseField>

    <ResponseField name="assignments" type="Assignment[]">

      Resources the user can access.

      <Expandable title="assignments properties">

        <ResponseField name="policy" type="AssignedPolicy">

          Role assigned to the user.

          <Expandable title="policy properties">

            <ResponseField name="policyId" type="string">

              Role ID.

            </ResponseField>

            <ResponseField name="title" type="string">

              Role title.

            </ResponseField>

            <ResponseField name="description" type="string">

              Role description.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="assignmentId" type="string">

          Unique ID for this specific assignment.

        </ResponseField>

        <ResponseField name="subject" type="Subject">

          Identity assigned to the asset in an assignment, referred to as subject. Supported subjects include user IDs, account IDs, and app IDs.

          <Expandable title="subject properties">

            <ResponseField name="id" type="string">

              ID of identity assigned to the asset.

            </ResponseField>

            <ResponseField name="subjectType" type="string">

              Type of identity assigned to the asset. Supported subject types include user IDs, account IDs, and app IDs.

              **Supported values:**

              - `ACCOUNT`
              - `USER`
              - `USER_GROUP`
              - `MEMBER_GROUP`
              - `VISITOR_GROUP`
              - `EXTERNAL_APP`
              - `ACCOUNT_GROUP`
              - `WIX_APP`

            </ResponseField>

            <ResponseField name="context" type="SubjectContext">

              Context of identity assigned to the asset. For example, a `subjectType` = `USER` will have `context` = `ACCOUNT`.

              <Expandable title="context properties">

                <ResponseField name="id" type="string" />

                <ResponseField name="contextType" type="string">

                  **Supported values:**

                  - `UNKNOWN_CTX`
                  - `ORG_CTX`
                  - `ACCOUNT_CTX`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      User ID.

    </ResponseField>

    <ResponseField name="roles" type="string[]" deprecated>

      Deprecated.

      **Deprecated.**

    </ResponseField>

    <ResponseField name="email" type="string">

      User's email address.

    </ResponseField>

    <ResponseField name="name" type="Name">

      User's name.

      <Expandable title="name properties">

        <ResponseField name="firstName" type="string">

          User's first name.

        </ResponseField>

        <ResponseField name="lastName" type="string">

          User's last name.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="profileImage" type="string">

      URL to user's profile image, when provided.

    </ResponseField>

    <ResponseField name="joinedTeamAt" type="datetime">

      Date the user joined the team.

    </ResponseField>

    <ResponseField name="policyIds" type="string[]" deprecated>

      Deprecated.

      **Deprecated.**

    </ResponseField>

    <ResponseField name="assignments" type="Assignment[]">

      Resources the user can access.

      <Expandable title="assignments properties">

        <ResponseField name="policy" type="AssignedPolicy">

          Role assigned to the user.

          <Expandable title="policy properties">

            <ResponseField name="policyId" type="string">

              Role ID.

            </ResponseField>

            <ResponseField name="title" type="string">

              Role title.

            </ResponseField>

            <ResponseField name="description" type="string">

              Role description.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="assignmentId" type="string">

          Unique ID for this specific assignment.

        </ResponseField>

        <ResponseField name="subject" type="Subject">

          Identity assigned to the asset in an assignment, referred to as subject. Supported subjects include user IDs, account IDs, and app IDs.

          <Expandable title="subject properties">

            <ResponseField name="_id" type="string">

              ID of identity assigned to the asset.

            </ResponseField>

            <ResponseField name="subjectType" type="string">

              Type of identity assigned to the asset. Supported subject types include user IDs, account IDs, and app IDs.

              **Supported values:**

              - `ACCOUNT`
              - `USER`
              - `USER_GROUP`
              - `MEMBER_GROUP`
              - `VISITOR_GROUP`
              - `EXTERNAL_APP`
              - `ACCOUNT_GROUP`
              - `WIX_APP`

            </ResponseField>

            <ResponseField name="context" type="SubjectContext">

              Context of identity assigned to the asset. For example, a `subjectType` = `USER` will have `context` = `ACCOUNT`.

              <Expandable title="context properties">

                <ResponseField name="_id" type="string" />

                <ResponseField name="contextType" type="string">

                  **Supported values:**

                  - `UNKNOWN_CTX`
                  - `ORG_CTX`
                  - `ACCOUNT_CTX`

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
