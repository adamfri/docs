---
title: "Restore From Trash Bin"
description: "Restores a form schema from the trash bin."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Restores a form schema from the trash bin. It will no longer be deleted after 90 days.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/form-schema-service/v4/forms/trash-bin/{formId}/restore
    ```

    ## Path Params

    <ParamField path="formId" type="string" required>

      ID of the form schema to restore from the trash bin.

    </ParamField>

    ## Response Object

    <ResponseField name="form" type="Form">

      Restored form schema.

      See the [Form Object](/api-reference/crm/forms/form-schemas/form-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Form Created | `wix.forms.v4.form_created` | Triggers when a form schema is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Restores a form from trash-bin

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/form-schema-service/v4/forms/trash-bin/2e9aa638-3b38-4c10-b625-0ff2323113a0/restore' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "form": {
          "id": "2e9aa638-3b38-4c10-b625-0ff2323113a0",
          "steps": [
            {
              "id": "01233a9e-3c3a-4f18-a0f7-03ede44dafb0",
              "name": "Page 1",
              "hidden": false,
              "layout": {
                "large": {
                  "items": [
                    {
                      "fieldId": "e4c81001-a757-461b-b751-9ca2da6c3c81",
                      "row": 1,
                      "column": 0,
                      "width": 12,
                      "height": 1
                    }
                  ],
                  "columns": null,
                  "rowHeight": null,
                  "margin": null,
                  "padding": null
                },
                "medium": null,
                "small": null
              }
            }
          ],
          "rules": [],
          "revision": "1",
          "createdDate": "2025-02-03T14:54:18.133Z",
          "updatedDate": "2025-02-03T14:54:18.679Z",
          "extendedFields": {
            "namespaces": {
              "@forms/form-app": {
                "automationId": "9480b208-ecc0-4d1f-8e7c-4bee4650a078"
              }
            }
          },
          "namespace": "wix.form_app.form",
          "mediaFolderId": "c2daf259-7403-41d9-8e0e-30a54dbe0275",
          "formFields": [
            {
              "id": "9d5daf72-646d-4942-b3d2-8a968f73244d",
              "hidden": false,
              "identifier": "SUBMIT_BUTTON",
              "fieldType": "DISPLAY",
              "displayOptions": {
                "displayFieldType": "PAGE_NAVIGATION",
                "pageNavigationOptions": {
                  "nextPageText": "Next",
                  "previousPageText": "Back",
                  "submitText": "Submit"
                }
              }
            }
          ],
          "deletedFormFields": [],
          "limitationRule": null,
          "spamFilterProtectionLevel": "ADVANCED",
          "requiredIndicatorProperties": null,
          "submitSettings": {
            "submitSuccessAction": "THANK_YOU_MESSAGE",
            "thankYouMessageOptions": {
              "durationInSeconds": 8,
              "richContent": {
                "nodes": [
                  {
                    "type": "PARAGRAPH",
                    "id": "2eee93",
                    "nodes": [
                      {
                        "type": "TEXT",
                        "id": "",
                        "nodes": [],
                        "style": null,
                        "textData": {
                          "text": "Thanks, we received your submission.",
                          "decorations": []
                        }
                      }
                    ],
                    "style": null,
                    "paragraphData": {
                      "textStyle": {
                        "textAlignment": "CENTER",
                        "lineHeight": null
                      },
                      "indentation": null,
                      "level": null
                    }
                  }
                ],
                "metadata": {
                  "version": 1,
                  "createdTimestamp": "2025-02-03T14:54:11.064Z",
                  "updatedTimestamp": "2025-02-03T14:54:11.064Z",
                  "id": "d85aaaa3-7281-4720-b52f-bc62ac851dec"
                },
                "documentStyle": null
              }
            }
          },
          "disabledFormMessage": null,
          "name": "My Form",
          "enabled": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Restores a form schema from the trash bin. It will no longer be deleted after 90 days.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { forms } from '@wix/forms';

    forms.restoreFromTrashBin(formId): Promise<RestoreFromTrashBinResponse>
    ```

    ## Method Parameters

    <ParamField body="formId" type="string" required>

      ID of the form schema to restore from the trash bin.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<RestoreFromTrashBinResponse>`.

    <ResponseField name="form" type="Form">

      Restored form schema.

      See the [Form Object](/api-reference/crm/forms/form-schemas/form-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Form Created | `wix.forms.v4.form_created` | Triggers when a form schema is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-restore-from-trash-bin" id="ex-sdk-restore-from-trash-bin-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-restore-from-trash-bin" id="ex-sdk-restore-from-trash-bin-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-restore-from-trash-bin" id="ex-sdk-restore-from-trash-bin-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-restore-from-trash-bin-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-restore-from-trash-bin-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">restoreFromTrashBin</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">restoreFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">restoreFromTrashBin (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-restore-from-trash-bin-0"><span className="wix-picker-name">restoreFromTrashBin</span></label>
    <label htmlFor="ex-sdk-restore-from-trash-bin-1"><span className="wix-picker-name">restoreFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-restore-from-trash-bin-2"><span className="wix-picker-name">restoreFromTrashBin (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript restoreFromTrashBin
    import { forms } from '@wix/forms';

    async function restoreFromTrashBin(formId) {
      const response = await forms.restoreFromTrashBin(formId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript restoreFromTrashBin (self-hosted)
    import { createClient } from '@wix/sdk';
    import { forms } from '@wix/forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { forms },
      // Include the auth strategy and host as relevant
    });

    async function restoreFromTrashBin(formId) {
      const response = await myWixClient.forms.restoreFromTrashBin(formId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript restoreFromTrashBin (with elevated permissions)
    import { forms } from '@wix/forms';
    import { auth } from '@wix/essentials';

    async function myRestoreFromTrashBinMethod(formId) {
      const elevatedRestoreFromTrashBin = auth.elevate(forms.restoreFromTrashBin);
      const response = await elevatedRestoreFromTrashBin(formId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
