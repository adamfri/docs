---
title: "onResourceTypeDeleted"
description: "Triggered when a resource type is deleted."
icon: "bolt"
---

Triggered when a resource type is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.resources.v2.resource_type_deleted` |
| Package | `@wix/bookings` |

## Examples

<CodeGroup>

  ```javascript onResourceTypeDeleted
  import { resourceTypes } from '@wix/bookings';

  resourceTypes.onResourceTypeDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onResourceTypeDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { resourceTypes } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        resourceTypes,
      },
  });

  wixClient.resourceTypes.onResourceTypeDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
