---
title: "List Membership Questions"
description: "Retrieves the membership questions for a group."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the membership questions for a group.

    <Note>

    This endpoint requires [visitor or member authentication](https://dev.wix.com/docs/rest/articles/getting-started/access-types-and-permissions).

    </Note>

    ## Endpoint

    ```http
    GET https://www.wixapis.com/social-groups-proxy/questions/v2/membership-questions/{groupId}
    ```

    ## Path Params

    <ParamField path="groupId" type="string" required>

      Group ID.

    </ParamField>

    ## Response Object

    <ResponseField name="questions" type="MembershipQuestion[]">

      Retrieved membership questions.

      See the [Membership Question Object](/api-reference/crm/community/groups/member-management/membership-questions/membership-question-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
          https://wixapis.com/social-groups/v2/membership-questions/707c1e28-c111-46e1-bafa-4e97c8fa490f \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "questions": [
              {
                  "id": "9a7b64c9-8b06-6602-b21f-869ae7cd673a",
                  "required": true,
                  "text": "Question 1"
              },
              {
                  "id": "38b0d2ff-1c03-df82-aea6-7222983d337e",
                  "required": false,
                  "text": "Question 2"
              }
          ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the membership questions for a group.

    <Note>

    This endpoint requires [visitor or member authentication](https://dev.wix.com/docs/rest/articles/getting-started/access-types-and-permissions).

    </Note>

    ## Method Declaration

    ```typescript
    import { membershipQuestions } from '@wix/groups';

    membershipQuestions.listMembershipQuestions(groupId): Promise<ListMembershipQuestionsResponse>
    ```

    ## Method Parameters

    <ParamField body="groupId" type="string" required>

      Group ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<ListMembershipQuestionsResponse>`.

    <ResponseField name="questions" type="MembershipQuestion[]">

      Retrieved membership questions.

      See the [Membership Question Object](/api-reference/crm/community/groups/member-management/membership-questions/membership-question-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-membership-questions" id="ex-sdk-list-membership-questions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-membership-questions" id="ex-sdk-list-membership-questions-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-membership-questions" id="ex-sdk-list-membership-questions-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-membership-questions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-membership-questions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listMembershipQuestions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listMembershipQuestions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listMembershipQuestions (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-membership-questions-0"><span className="wix-picker-name">listMembershipQuestions</span></label>
    <label htmlFor="ex-sdk-list-membership-questions-1"><span className="wix-picker-name">listMembershipQuestions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-membership-questions-2"><span className="wix-picker-name">listMembershipQuestions (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listMembershipQuestions
    import { membershipQuestions } from '@wix/groups';

    async function listMembershipQuestions(groupId) {
      const response = await membershipQuestions.listMembershipQuestions(groupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listMembershipQuestions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { membershipQuestions } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { membershipQuestions },
      // Include the auth strategy and host as relevant
    });

    async function listMembershipQuestions(groupId) {
      const response = await myWixClient.membershipQuestions.listMembershipQuestions(groupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listMembershipQuestions (with elevated permissions)
    import { membershipQuestions } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myListMembershipQuestionsMethod(groupId) {
      const elevatedListMembershipQuestions = auth.elevate(membershipQuestions.listMembershipQuestions);
      const response = await elevatedListMembershipQuestions(groupId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
