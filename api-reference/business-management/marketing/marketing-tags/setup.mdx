---
title: "Setup"
description: "To use the MarketingTags API, install the @wix/marketing-tags package using npm or Yarn: or Then import { marketingTags } from @wix/marketing-tags:"
icon: "file-lines"
---

<Note>This article applies to the JavaScript SDK only.</Note>

To use the MarketingTags API, install the `@wix/marketing-tags` package using npm or Yarn:
  ```bash
npm install @wix/marketing-tags
```

or

```bash
yarn add @wix/marketing-tags
```

Then import `{ marketingTags }` from `@wix/marketing-tags`:

```js
import { marketingTags } from '@wix/marketing-tags'
```
