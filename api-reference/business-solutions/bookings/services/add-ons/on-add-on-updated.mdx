---
title: "onAddOnUpdated"
description: "Triggered when an add-on is updated."
icon: "bolt"
---

Triggered when an add-on is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.addons.v1.add_on_updated` |
| Package | `@wix/bookings` |
| Permissions | `Manage Bookings`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onAddOnUpdated
  import { addOns } from '@wix/bookings';

  addOns.onAddOnUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onAddOnUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { addOns } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        addOns,
      },
  });

  wixClient.addOns.onAddOnUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
