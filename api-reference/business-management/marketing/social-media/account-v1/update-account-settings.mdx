---
title: "Update Account Settings"
description: "Updates the settings for a connected social channel account, such as the default page, board, or account to publish to."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates the settings for a connected social channel account, such as the default page, board, or account to publish to.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/social-publisher/v1/{channelName}/settings
    ```

    ## Path Params

    <ParamField path="channelName" type="string" required />

    ## Response Object

    Returns `UpdateAccountSettingsResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Update the settings for a connected social channel account, such as the default page, board, or account to publish to.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/social-publisher/v1/FACEBOOK/settings' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "facebook": {
          "defaultPageId": "112233445566778"
        }
      }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates the settings for a connected social channel account, such as the default page, board, or account to publish to.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { accounts } from '@wix/promote-growth-tools-publisher';

    accounts.updateAccountSettings(channelName, options): Promise
    ```

    ## Method Parameters

    <ParamField body="channelName" type="string" required>

      Supported social channel.

      **Supported values:**

      - `INSTAGRAM`
      - `FACEBOOK`
      - `YOUTUBE`
      - `LINKEDIN`
      - `TWITTER` — Deprecated: No longer functional as of July 31, 2026.
      - `PINTEREST`
      - `GBP` — Google Business Profile.
      - `TIKTOK`

    </ParamField>

    <ParamField body="options" type="UpdateAccountSettingsOptions" required>

      options

      <Expandable title="One of the following">

        <ParamField body="settings" type="settings">

          settings

          <Expandable title="settings properties">

            <ParamField body="facebook" type="FacebookSettings">

              facebook

              _Nested structure omitted for brevity — see the `FacebookSettings` object._

            </ParamField>

            <ParamField body="linkedin" type="LinkedinSettings">

              linkedin

              _Nested structure omitted for brevity — see the `LinkedinSettings` object._

            </ParamField>

            <ParamField body="instagram" type="InstagramSettings">

              instagram

              _Nested structure omitted for brevity — see the `InstagramSettings` object._

            </ParamField>

            <ParamField body="pinterest" type="PinterestSettings">

              pinterest

              _Nested structure omitted for brevity — see the `PinterestSettings` object._

            </ParamField>

            <ParamField body="gbp" type="GBPSettings">

              gbp

              _Nested structure omitted for brevity — see the `GBPSettings` object._

            </ParamField>

            <ParamField body="tiktok" type="TikTokSettings">

              tiktok

              _Nested structure omitted for brevity — see the `TikTokSettings` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `UpdateAccountSettingsResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-account-settings" id="ex-sdk-update-account-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-account-settings" id="ex-sdk-update-account-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-account-settings" id="ex-sdk-update-account-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-account-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-account-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateAccountSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateAccountSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateAccountSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-account-settings-0"><span className="wix-picker-name">updateAccountSettings</span></label>
    <label htmlFor="ex-sdk-update-account-settings-1"><span className="wix-picker-name">updateAccountSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-account-settings-2"><span className="wix-picker-name">updateAccountSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateAccountSettings
    import { accounts } from '@wix/promote-growth-tools-publisher';

    async function updateAccountSettings(channelName,options) {
      const response = await accounts.updateAccountSettings(channelName,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateAccountSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { accounts } from '@wix/promote-growth-tools-publisher';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { accounts },
      // Include the auth strategy and host as relevant
    });

    async function updateAccountSettings(channelName,options) {
      const response = await myWixClient.accounts.updateAccountSettings(channelName,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateAccountSettings (with elevated permissions)
    import { accounts } from '@wix/promote-growth-tools-publisher';
    import { auth } from '@wix/essentials';

    async function myUpdateAccountSettingsMethod(channelName,options) {
      const elevatedUpdateAccountSettings = auth.elevate(accounts.updateAccountSettings);
      const response = await elevatedUpdateAccountSettings(channelName,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
