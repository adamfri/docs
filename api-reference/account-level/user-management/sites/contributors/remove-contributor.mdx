---
title: "Remove Contributor"
description: "Removes a contributor from the site by clearing all their role assignments."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes a contributor from the site by clearing all their role assignments.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-IDENTITY.MANAGE-CONTRIBUTORS">Manage Contributors</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/roles-management/contributor/remove
    ```

    ## Body Params

    <ParamField body="accountId" type="string" required>

      Contributor's account ID to remove from the site.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    Returns `RemoveContributorResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/roles-management/contributor/remove' \
      -H 'Content-Type: application/json' \
      -H 'wix-site-id: 4339fba7-b533-4d10-a91b-822d825f29a5' \
      -H 'Authorization: <AUTH>' \
      -d '{
              "accountId": "fed9597b-00a1-4bd6-0000-aff2ec248e7a"
            }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes a contributor from the site by clearing all their role assignments.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-IDENTITY.MANAGE-CONTRIBUTORS">Manage Contributors</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contributors } from '@wix/user-management';

    contributors.removeContributor(accountId): Promise
    ```

    ## Method Parameters

    <ParamField body="accountId" type="string" required>

      Contributor's account ID to remove from the site.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `RemoveContributorResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-contributor" id="ex-sdk-remove-contributor-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-contributor" id="ex-sdk-remove-contributor-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-contributor-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-contributor-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Remove a contributor from a site with an API key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeContributor (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-contributor-0"><span className="wix-picker-name">Remove a contributor from a site with an API key</span></label>
    <label htmlFor="ex-sdk-remove-contributor-1"><span className="wix-picker-name">removeContributor (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Remove a contributor from a site with an API key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { contributors } from "@wix/user-management";
    const wixClient = createClient({
     modules: { contributors },
     auth: ApiKeyStrategy({
        siteId: "MY-SITE-ID",
        apiKey: "MY-API-KEY",
      }),
    });

    async function removeContributor(accountId) {
      const response = await contributors.removeContributor(accountId);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeContributor (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contributors } from '@wix/user-management';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contributors },
      // Include the auth strategy and host as relevant
    });

    async function removeContributor(accountId) {
      const response = await myWixClient.contributors.removeContributor(accountId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
