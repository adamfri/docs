---
title: "Query Ribbons"
description: "Retrieves a list of up to 100 ribbons, given the provided filtering, sorting, and cursor paging."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 ribbons, given the provided filtering, sorting, and cursor paging.
    Pass supported values to the `fields` array in the request to include those fields in the response.

    Query Brands runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `100`

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.RIBBON_READ">Read ribbons in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/ribbons/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC` — Ascending order.
              - `DESC` — Descending order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Supported values: `ASSIGNED_PRODUCTS_COUNT`

      Constraints: `maxItems 100`.

      **Supported values:**

      - `ASSIGNED_PRODUCT_COUNT`

    </ParamField>

    ## Response Object

    <ResponseField name="ribbons" type="Ribbon[]">

      List of ribbons.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Ribbon Object](/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/ribbon-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-query-ribbons" id="ex-rest-query-ribbons-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-query-ribbons" id="ex-rest-query-ribbons-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-query-ribbons-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-query-ribbons-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query ribbons with cursor paging</span><span className="wix-picker-desc">This is the followup query to the query ribbons example. It demonstrates how to use cursor paging to retrieve the next page of ribbons</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query ribbons</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-query-ribbons-0"><span className="wix-picker-name">Query ribbons with cursor paging</span><span className="wix-picker-desc">This is the followup query to the query ribbons example. It demonstrates how to use cursor paging to retrieve the next page of ribbons</span></label>
    <label htmlFor="ex-rest-query-ribbons-1"><span className="wix-picker-name">Query ribbons</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
       'https://www.wixapis.com/stores/v3/ribbons/query' \
       -H 'Content-type: application/json' \
       -H 'Authorization: <AUTH>' \
       -d '{
         "query": {
           "cursorPaging": {
              "cursor": "d8c06edfa2eef21fd087e1f5b2ed0daa259694e7.EiQqIgogCgRuYW1lEhgqFgoUCgskc3RhcnRzV2l0aBIFGgNOZXcaLQoMX2NyZWF0ZWREYXRlEAEaGyoZChcKCiR0aW1lc3RhbXASCREAYJzLOwV5QiIkZjQ4MGY3NmQtZjJkOS00NzliLTg5NzgtNjQ4NmYwNmY0NzJk"
           }
         }
       }'
      ```

      ```json Response
      {
        "ribbons": [
          {
            "id": "2254c3b3-6bbd-457d-b868-9122657c5d1c",
            "revision": "2",
            "createdDate": "2024-06-26T08:34:04.787Z",
            "updatedDate": "2024-06-26T08:39:24.371Z",
            "name": "New!"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {
            "prev": "f0c0028f5a13813719b4480f620a0afb2bb96954.CAESJCoiCiAKBG5hbWUSGCoWChQKCyRzdGFydHNXaXRoEgUaA05ldxotCgxfY3JlYXRlZERhdGUQARobKhkKFwoKJHRpbWVzdGFtcBIJEQAwd-M6BXlCIiQyMjU0YzNiMy02YmJkLTQ1N2QtYjg2OC05MTIyNjU3YzVkMWM"
          },
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
       'https://www.wixapis.com/stores/v3/ribbons/query' \
       -H 'Content-type: application/json' \
       -H 'Authorization: <AUTH>' \
       -d '{
         "query": {
           "filter": {"name": {"$startsWith":"New"}},
           "sort": [{"fieldName":"createdDate","order":"DESC"}],
           "cursorPaging": {"limit": 2}
          }
       }'
      ```

      ```json Response
      {
        "ribbons": [
          {
            "id": "438f0fc0-8913-4da3-bb1d-047bb92c3569",
            "revision": "1",
            "createdDate": "2024-06-26T09:00:04.066Z",
            "updatedDate": "2024-06-26T09:00:04.066Z",
            "name": "New Offer!"
          },
          {
            "id": "f480f76d-f2d9-479b-8978-6486f06f472d",
            "revision": "1",
            "createdDate": "2024-06-26T08:49:55.654Z",
            "updatedDate": "2024-06-26T08:49:55.654Z",
            "name": "New collection"
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {
            "next": "d8c06edfa2eef21fd087e1f5b2ed0daa259694e7.EiQqIgogCgRuYW1lEhgqFgoUCgskc3RhcnRzV2l0aBIFGgNOZXcaLQoMX2NyZWF0ZWREYXRlEAEaGyoZChcKCiR0aW1lc3RhbXASCREAYJzLOwV5QiIkZjQ4MGY3NmQtZjJkOS00NzliLTg5NzgtNjQ4NmYwNmY0NzJk"
          },
          "hasNext": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 ribbons, given the provided filtering, sorting, and cursor paging.
    Pass supported values to the `fields` array in the request to include those fields in the response.

    Query Brands runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `100`

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | None |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.RIBBON_READ">Read ribbons in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { ribbonsV3 } from '@wix/stores';

    ribbonsV3.queryRibbons(query, options): RibbonsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="RibbonQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC` — Ascending order.
              - `DESC` — Descending order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QueryRibbonsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Supported values: `ASSIGNED_PRODUCTS_COUNT`

          Constraints: `maxItems 100`.

          **Supported values:**

          - `ASSIGNED_PRODUCT_COUNT`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `RibbonsQueryBuilder`.

    <ResponseField name="ribbons" type="Ribbon[]">

      List of ribbons.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Ribbon Object](/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/ribbon-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-ribbons" id="ex-sdk-query-ribbons-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-ribbons" id="ex-sdk-query-ribbons-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-ribbons" id="ex-sdk-query-ribbons-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-ribbons-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-ribbons-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query ribbons by name prefix</span><span className="wix-picker-desc">Retrieve ribbons whose name starts with "New", sorted by newest first, two per page</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Page through ribbons with a cursor</span><span className="wix-picker-desc">Fetch the next page of ribbons using a cursor returned from a previous query</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryRibbons (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-ribbons-0"><span className="wix-picker-name">Query ribbons by name prefix</span><span className="wix-picker-desc">Retrieve ribbons whose name starts with "New", sorted by newest first, two per page</span></label>
    <label htmlFor="ex-sdk-query-ribbons-1"><span className="wix-picker-name">Page through ribbons with a cursor</span><span className="wix-picker-desc">Fetch the next page of ribbons using a cursor returned from a previous query</span></label>
    <label htmlFor="ex-sdk-query-ribbons-2"><span className="wix-picker-name">queryRibbons (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query ribbons by name prefix
    import { ribbonsV3 } from "@wix/stores";

    async function queryRibbons() {
      const response = await ribbonsV3.queryRibbons({
        filter: {
          name: {
            $startsWith: "New",
          },
        },
        sort: [
          {
            fieldName: "_createdDate",
            order: "DESC",
          },
        ],
        cursorPaging: {
          limit: 2,
        },
      });
      return response;
    }

    /* Promise resolves to:
     * {
     *   "ribbons": [
     *     {
     *       "id": "a1b2c3d4-e5f6-7a8b-9c0d-e1f2a3b4c5d6"
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 2,
     *     "hasNext": true
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Page through ribbons with a cursor
    import { ribbonsV3 } from "@wix/stores";

    async function queryRibbons() {
      const response = await ribbonsV3.queryRibbons({
        cursorPaging: {
          cursor:
            "1180ef2e2f07152dda5647.EiQqIgogCgRuYW1lEhgqFgoUCgskc3RhcnRzV2l0aBIFGgNOZXcaLQoMX2NyZWF0ZWREYXRlEAEaGyoZChcKCiR0aW1lc3RhbXASCREAYJzLOwV5QiIkZjQ4MGY3NmQtZjJkOS00NzliLTg5NzgtNjQ4NmYwNmY0NzJk",
        },
      });
      return response;
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryRibbons (self-hosted)
    import { createClient } from '@wix/sdk';
    import { ribbonsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { ribbonsV3 },
      // Include the auth strategy and host as relevant
    });

    async function queryRibbons(query,options) {
      const response = await myWixClient.ribbonsV3.queryRibbons(query,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
