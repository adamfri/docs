---
title: "Cancel Scheduled Item"
description: "Cancels the scheduling of a scheduled item."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Cancels the scheduling of a scheduled item.

    The item is no longer published at its scheduled time. Only an item with a `status` of `SCHEDULED` can be canceled.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/social-publisher/v1/items/{id}/cancel
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the scheduled item to cancel.

    </ParamField>

    ## Response Object

    <ResponseField name="item" type="Item">

      Canceled item.

      See the [Item Object](/api-reference/business-management/marketing/social-media/item-v1/item-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `SCHEDULED_ITEM_ALREADY_CANCELED` | The scheduled item was already canceled. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULED_ITEM_ALREADY_PUBLISHED` | The scheduled item was already published. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULED_ITEM_ALREADY_DELETED` | The scheduled item was already deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Cancel the scheduling of an item with a status of SCHEDULED. The item is no longer published at its scheduled time.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/social-publisher/v1/items/48d402d4-c1aa-4db6-bca4-cfd0b0892695/cancel' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{}'
      ```

      ```json Response
      {
        "item": {
          "id": "48d402d4-c1aa-4db6-bca4-cfd0b0892695",
          "channel": {
            "name": "YOUTUBE",
            "accountId": "UCa8fkLm3QxR7vTq2NpZ9wEd",
            "accountName": "Jane's Pottery Studio"
          },
          "type": "VIDEO",
          "status": "CANCELED",
          "schedulingInfo": {
            "scheduledDate": "2026-07-15T08:00:00.000Z",
            "schedulingTimestamp": "2026-06-11T07:44:34.787Z"
          },
          "youtubeVideo": {
            "channelId": "UCa8fkLm3QxR7vTq2NpZ9wEd",
            "title": "Behind the scenes: making our summer collection",
            "description": "A look inside our studio as we craft the new summer collection.",
            "notifySubscribers": true,
            "mediaWrapper": {
              "media": [
                {
                  "type": "VIDEO",
                  "url": "https://video.wixstatic.com/video/11062b_a191a276b1834d6fb1c5dee0e7d3df10/1080p/mp4/file.mp4"
                }
              ]
            }
          },
          "suggested": false,
          "external": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Cancels the scheduling of a scheduled item.

    The item is no longer published at its scheduled time. Only an item with a `status` of `SCHEDULED` can be canceled.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/promote-growth-tools-publisher';

    items.cancelScheduledItem(_id): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the scheduled item to cancel.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="item" type="Item">

      Canceled item.

      See the [Item Object](/api-reference/business-management/marketing/social-media/item-v1/item-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `SCHEDULED_ITEM_ALREADY_CANCELED` | The scheduled item was already canceled. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULED_ITEM_ALREADY_PUBLISHED` | The scheduled item was already published. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULED_ITEM_ALREADY_DELETED` | The scheduled item was already deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-cancel-scheduled-item" id="ex-sdk-cancel-scheduled-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-cancel-scheduled-item" id="ex-sdk-cancel-scheduled-item-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-cancel-scheduled-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-cancel-scheduled-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Cancel a scheduled item</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">cancelScheduledItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-cancel-scheduled-item-0"><span className="wix-picker-name">Cancel a scheduled item</span></label>
    <label htmlFor="ex-sdk-cancel-scheduled-item-1"><span className="wix-picker-name">cancelScheduledItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Cancel a scheduled item
    import { items } from "@wix/promote-growth-tools-publisher";

    const itemId = "48d402d4-c1aa-4db6-bca4-cfd0b0892695";

    async function cancelScheduledItem() {
      const response = await items.cancelScheduledItem(itemId);
    }

    /* Promise resolves to:
     * {
     *   "item": {
     *     "_id": "48d402d4-c1aa-4db6-bca4-cfd0b0892695",
     *     "channel": {
     *       "name": "YOUTUBE",
     *       "accountId": "UCa8fkLm3QxR7vTq2NpZ9wEd",
     *       "accountName": "Jane's Pottery Studio"
     *     },
     *     "type": "VIDEO",
     *     "status": "CANCELED",
     *     "schedulingInfo": {
     *       "scheduledDate": "2026-07-15T08:00:00.000Z",
     *       "schedulingTimestamp": "2026-06-11T07:47:51.377Z"
     *     },
     *     "youtubeVideo": {
     *       "channelId": "UCa8fkLm3QxR7vTq2NpZ9wEd",
     *       "title": "Behind the scenes: making our summer collection",
     *       "description": "A look inside our studio as we craft the new summer collection.",
     *       "notifySubscribers": true,
     *       "mediaWrapper": {
     *         "media": [
     *           {
     *             "type": "VIDEO",
     *             "url": "https://video.wixstatic.com/video/11062b_a191a276b1834d6fb1c5dee0e7d3df10/1080p/mp4/file.mp4"
     *           }
     *         ]
     *       }
     *     },
     *     "suggested": false,
     *     "external": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript cancelScheduledItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/promote-growth-tools-publisher';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function cancelScheduledItem(_id) {
      const response = await myWixClient.items.cancelScheduledItem(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
