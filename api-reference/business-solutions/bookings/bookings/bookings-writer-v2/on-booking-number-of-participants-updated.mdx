---
title: "onBookingNumberOfParticipantsUpdated"
description: "Triggered when the number of participants is updated."
icon: "bolt"
---

Triggered when the number of participants is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.v2.booking_number_of_participants_updated` |
| Package | `@wix/bookings` |
| Permissions | `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions`, `Read bookings calendar - including participants` |

## Examples

<CodeGroup>

  ```javascript onBookingNumberOfParticipantsUpdated
  import { bookings } from '@wix/bookings';

  bookings.onBookingNumberOfParticipantsUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBookingNumberOfParticipantsUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bookings } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bookings,
      },
  });

  wixClient.bookings.onBookingNumberOfParticipantsUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
