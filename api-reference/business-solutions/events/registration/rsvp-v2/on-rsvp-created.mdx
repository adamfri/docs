---
title: "onRsvpCreated"
description: "Triggered when RSVP is created."
icon: "bolt"
---

Triggered when RSVP is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v2.rsvp_created` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onRsvpCreated
  import { rsvpV2 } from '@wix/events';

  rsvpV2.onRsvpCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRsvpCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { rsvpV2 } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        rsvpV2,
      },
  });

  wixClient.rsvpV2.onRsvpCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
