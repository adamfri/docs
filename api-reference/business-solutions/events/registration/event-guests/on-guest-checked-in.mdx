---
title: "onGuestCheckedIn"
description: "Triggered when a guest has checked in."
icon: "bolt"
---

Triggered when a guest has checked in.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.guests.v1.guest_guest_checked_in` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onGuestCheckedIn
  import { guests } from '@wix/events';

  guests.onGuestCheckedIn((event) => {
      // handle your event here
  });
  ```

  ```javascript onGuestCheckedIn (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { guests } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        guests,
      },
  });

  wixClient.guests.onGuestCheckedIn((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
