---
title: "Events View Object"
description: "The eventsView object gives you details about how far into the future you can easily get a complete picture of a calendar."
icon: "cube"
---

The `eventsView` object gives you details about how far into the future you can
easily get a complete picture of a calendar. Subscribing to the event view
webhooks lets you keep track of a calendar's single-occurrence, `EXCEPTION`, and
`INSTANCE` events at least one year into the future.

<Tabs>

  <Tab title="REST">

    <ResponseField name="endDate" type="datetime">

      End date of the event view in `YYYY-MM-DDThh:mm:ss.sssZ` format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="futureDurationInDays" type="integer">

      Number of days from now until the event view ends.

      **Read-only.**

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "endDate": "2025-10-04T00:00:00Z",
        "futureDurationInDays": 380
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="endDate" type="datetime">

      End date of the event view in `YYYY-MM-DDThh:mm:ss.sssZ` format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="futureDurationInDays" type="integer">

      Number of days from now until the event view ends.

      **Read-only.**

    </ResponseField>

  </Tab>

</Tabs>
