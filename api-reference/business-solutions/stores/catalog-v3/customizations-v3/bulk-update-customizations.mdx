---
title: "Bulk Update Customizations"
description: "Updates multiple customizations."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Updates multiple customizations.

    Each time the customization is updated, `revision` increments by 1.
    The current `revision` must be passed when updating the customization.
    This ensures you're working with the latest customization and prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CUSTOMIZATION_WRITE">Customization write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/bulk/customizations/update
    ```

    ## Body Params

    <ParamField body="customizations" type="MaskedCustomization[]" required>

      List of customizations to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="customizations properties">

        <ParamField body="customization" type="Customization">

          Customization to update.

          See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the full customization entities in the response.

      Default: `false`

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Supported values: `ASSIGNED_PRODUCTS_COUNT`

      Constraints: `maxItems 100`.

      **Supported values:**

      - `ASSIGNED_PRODUCTS_COUNT` — Assigned products count.
      - `CHOICE_DISPLAY_IMAGE` — Returns display images for choices.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkCustomizationResult[]">

      Customizations updated by bulk action.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata for customization.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customization" type="Customization">

          Full customization entity.

          Returned only if `returnEntity: true` is passed in the request.

          See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Customization Updated | `wix.stores.catalog.v3.customization_updated` | Triggered when a customization is updated. |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates multiple customizations.

    Each time the customization is updated, `revision` increments by 1.
    The current `revision` must be passed when updating the customization.
    This ensures you're working with the latest customization and prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CUSTOMIZATION_WRITE">Customization write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customizationsV3 } from '@wix/stores';

    customizationsV3.bulkUpdateCustomizations(customizations, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="customizations" type="MaskedCustomization[]" required>

      List of customizations to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="customizations properties">

        <ParamField body="customization" type="Customization">

          Customization to update.

          See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="BulkUpdateCustomizationsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return the full customization entities in the response.

          Default: `false`

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Supported values: `ASSIGNED_PRODUCTS_COUNT`

          Constraints: `maxItems 100`.

          **Supported values:**

          - `ASSIGNED_PRODUCTS_COUNT` — Assigned products count.
          - `CHOICE_DISPLAY_IMAGE` — Returns display images for choices.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="results" type="BulkCustomizationResult[]">

      Customizations updated by bulk action.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata for customization.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="customization" type="Customization">

          Full customization entity.

          Returned only if `returnEntity: true` is passed in the request.

          See the [Customization Object](/api-reference/business-solutions/stores/catalog-v3/customizations-v3/customization-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Customization Updated | `wix.stores.catalog.v3.customization_updated` | Triggered when a customization is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-customizations" id="ex-sdk-bulk-update-customizations-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-customizations" id="ex-sdk-bulk-update-customizations-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-update-customizations" id="ex-sdk-bulk-update-customizations-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-customizations-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-customizations-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateCustomizations</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateCustomizations (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateCustomizations (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-customizations-0"><span className="wix-picker-name">bulkUpdateCustomizations</span></label>
    <label htmlFor="ex-sdk-bulk-update-customizations-1"><span className="wix-picker-name">bulkUpdateCustomizations (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-update-customizations-2"><span className="wix-picker-name">bulkUpdateCustomizations (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUpdateCustomizations
    import { customizationsV3 } from '@wix/stores';

    async function bulkUpdateCustomizations(customizations,options) {
      const response = await customizationsV3.bulkUpdateCustomizations(customizations,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateCustomizations (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customizationsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customizationsV3 },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdateCustomizations(customizations,options) {
      const response = await myWixClient.customizationsV3.bulkUpdateCustomizations(customizations,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateCustomizations (with elevated permissions)
    import { customizationsV3 } from '@wix/stores';
    import { auth } from '@wix/essentials';

    async function myBulkUpdateCustomizationsMethod(customizations,options) {
      const elevatedBulkUpdateCustomizations = auth.elevate(customizationsV3.bulkUpdateCustomizations);
      const response = await elevatedBulkUpdateCustomizations(customizations,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
