---
title: "Get Task"
description: "Retrieves the specified task."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the specified task.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/cms/v1/tasks/{taskId}
    ```

    ## Path Params

    <ParamField path="taskId" type="string" required>

      Task ID.

    </ParamField>

    ## Response Object

    <ResponseField name="task" type="Task">

      Retrieved task.

      See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://wixapis.com/autocms/v1/tasks/097a5564-0fec-4425-8d1e-3e753b48a422  \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "task": {
          "id": "097a5564-0fec-4425-8d1e-3e753b48a422",
          "type": "DELETE_BY_QUERY",
          "status": "COMPLETED",
          "startedAt": "2024-09-05T10:34:59.507Z",
          "finishedAt": "2024-09-05T10:35:03.654Z",
          "estimatedItemCount": 3,
          "itemsSucceeded": 3,
          "itemsFailed": 0,
          "failures": [],
          "deleteByQueryOptions": {
            "dataCollectionId": "Items",
            "environment": "LIVE",
            "filter": {
              "holidays": {
                "$in": ["Halloween"]
              }
            }
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the specified task.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.WRITE">Write Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tasks } from '@wix/data';

    tasks.getTask(taskId): Promise
    ```

    ## Method Parameters

    <ParamField body="taskId" type="string" required>

      Task ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Task`.

    See the [Background Task Object](/api-reference/business-solutions/cms/operations/background-tasks/background-task-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-task" id="ex-sdk-get-task-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-task" id="ex-sdk-get-task-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-task" id="ex-sdk-get-task-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-task-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-task-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getTask</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getTask (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-task-0"><span className="wix-picker-name">getTask</span></label>
    <label htmlFor="ex-sdk-get-task-1"><span className="wix-picker-name">getTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-task-2"><span className="wix-picker-name">getTask (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getTask
    import { tasks } from '@wix/data';

    async function getTask(taskId) {
      const response = await tasks.getTask(taskId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTask (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tasks } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tasks },
      // Include the auth strategy and host as relevant
    });

    async function getTask(taskId) {
      const response = await myWixClient.tasks.getTask(taskId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getTask (with elevated permissions)
    import { tasks } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myGetTaskMethod(taskId) {
      const elevatedGetTask = auth.elevate(tasks.getTask);
      const response = await elevatedGetTask(taskId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
