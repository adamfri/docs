---
title: "Get Plan"
description: "Retrieves a pricing plan by ID."
icon: "download"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Retrieves a pricing plan by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAIDPLANS.READ-PLANS">Read Pricing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/pricing-plans/v2/plans/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Plan ID.

    </ParamField>

    ## Response Object

    <ResponseField name="plan" type="Plan">

      Pricing plan.

      See the [Plan Object](/api-reference/business-solutions/pricing-plans/plans-deprecated/plan-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `plan_not_found` | Pricing plan was not found. |
    | `428` | `FAILED_PRECONDITION` | `PRICING_PLANS_NOT_INSTALLED` | The user does not have the Wix Pricing Plans app installed on the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
        https://www.wixapis.com/pricing-plans/v2/plans/ee187a1b-...-9f7a-936f2cd9d100
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          plan: {
            "id": "ee187a1b-...-9f7a-936f2cd9d100",
            "name": "VIP monthly",
            "description": "",
            "perks": {
              "values": [
                "Free consulting",
                "Multi-user"
              ]
            },
            "pricing": {
              "subscription": {
                "cycleDuration": {
                  "count": 1,
                  "unit": "MONTH"
                },
                "cycleCount": 3
              },
              "price": {
                "value": "23",
                "currency": "USD"
              }
            },
            "public": true,
            "archived": false,
            "primary": false,
            "hasOrders": true,
            "createdDate": "2021-01-10T09:05:20.063Z",
            "updatedDate": "2021-02-09T09:21:47.649Z",
            "slug": "vip-monthly",
            "maxPurchasesPerBuyer": 1,
            "allowFutureStartDate": true,
            "buyerCanCancel": true,
            "termsAndConditions": "No sharing please."
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Retrieves a pricing plan by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAIDPLANS.READ-PLANS">Read Pricing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { plans } from '@wix/pricing-plans';

    plans.getPlan(_id): Promise<Plan>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Plan ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Plan>`.

    See the [Plan Object](/api-reference/business-solutions/pricing-plans/plans-deprecated/plan-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `plan_not_found` | Pricing plan was not found. |
    | `428` | `FAILED_PRECONDITION` | `PRICING_PLANS_NOT_INSTALLED` | The user does not have the Wix Pricing Plans app installed on the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-plan" id="ex-sdk-get-plan-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-plan" id="ex-sdk-get-plan-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-plan" id="ex-sdk-get-plan-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-plan" id="ex-sdk-get-plan-3" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-plan-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-plan-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get a plan</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Get a plan (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Duplicate a plan (with $w)</span><span className="wix-picker-desc">The populatePlansDropdown function retrieves all plans and then populates them into the #plansDropdown. The #plansDropdown and #clonePlanBtn are set to disabled by default</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getPlan (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-plan-0"><span className="wix-picker-name">Get a plan</span></label>
    <label htmlFor="ex-sdk-get-plan-1"><span className="wix-picker-name">Get a plan (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-get-plan-2"><span className="wix-picker-name">Duplicate a plan (with $w)</span><span className="wix-picker-desc">The populatePlansDropdown function retrieves all plans and then populates them into the #plansDropdown. The #plansDropdown and #clonePlanBtn are set to disabled by default</span></label>
    <label htmlFor="ex-sdk-get-plan-3"><span className="wix-picker-name">getPlan (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get a plan
    import { plans} from '@wix/pricing-plans';

    /* Sample _id value: '838f2c9d-c8d0-4799-a10a-e2f23849db10' */

    export async function myGetPlanFunction(_id) {
      try {
        const myPlan = await plans.getPlan(_id);

        return myPlan;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "_createdDate": "2024-01-07T07:28:42.863Z",
     *   "_id": "838f2c9d-c8d0-4799-a10a-e2f23849db10",
     *   "_updatedDate": "2024-01-07T08:36:07.520Z",
     *   "allowFutureStartDate": false,
     *   "archived": false,
     *   "buyerCanCancel": true,
     *   "description": "Complete with all features. One month free trial.",
     *   "formId": "ee62cefa-bdc2-4b5d-baab-6faeef83cecb",
     *   "hasOrders": false,
     *   "maxPurchasesPerBuyer": 0,
     *   "name": "Premium Plan - annual - 30 day trial",
     *   "perks": {
     *     "values": [
     *       "Unlimited video library streaming access",
     *       "File sharing enabled for all channels"
     *     ]
     *   },
     *   "pricing": {
     *     "freeTrialDays": 30,
     *     "price": {
     *       "currency": "EUR",
     *       "value": "500"
     *     },
     *     "subscription": {
     *       "cycleCount": 2,
     *       "cycleDuration": {
     *         "count": 1,
     *         "unit": "YEAR"
     *       }
     *     }
     *   },
     *   "primary": false,
     *   "public": true,
     *   "slug": "premium-plan-annual-30-day-trial-1",
     *   "termsAndConditions": "Unlimited usage of services, subject to Fair Usage and Code of Conduct policies."
     * } 
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Get a plan (with elevated permissions)
    import { plans } from '@wix/pricing-plans';
    import { auth } from '@wix/essentials';

    /* Sample _id value: '838f2c9d-c8d0-4799-a10a-e2f23849db10' */

    const elevatedGetPlan = auth.elevate(plans.getPlan);

    export async function myGetPlanFunction(_id) {
      try {
        const myPlan = await elevatedGetPlan(_id);

        return myPlan;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "_createdDate": "2024-01-07T07:28:42.863Z",
     *   "_id": "838f2c9d-c8d0-4799-a10a-e2f23849db10",
     *   "_updatedDate": "2024-01-07T08:36:07.520Z",
     *   "allowFutureStartDate": false,
     *   "archived": false,
     *   "buyerCanCancel": true,
     *   "description": "Complete with all features. One month free trial.",
     *   "formId": "ee62cefa-bdc2-4b5d-baab-6faeef83cecb",
     *   "hasOrders": false,
     *   "maxPurchasesPerBuyer": 0,
     *   "name": "Premium Plan - annual - 30 day trial",
     *   "perks": {
     *     "values": [
     *       "Unlimited video library streaming access",
     *       "File sharing enabled for all channels"
     *     ]
     *   },
     *   "pricing": {
     *     "freeTrialDays": 30,
     *     "price": {
     *       "currency": "EUR",
     *       "value": "500"
     *     },
     *     "subscription": {
     *       "cycleCount": 2,
     *       "cycleDuration": {
     *         "count": 1,
     *         "unit": "YEAR"
     *       }
     *     }
     *   },
     *   "primary": false,
     *   "public": true,
     *   "slug": "premium-plan-annual-30-day-trial-1",
     *   "termsAndConditions": "Unlimited usage of services, subject to Fair Usage and Code of Conduct policies."
     * } 
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Duplicate a plan (with $w)
    /*******************************************
     * Backend code - plan-functions.web.js/ts *
     ******************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { plans } from '@wix/pricing-plans';
    import { auth } from '@wix/essentials';

    const elevatedListPlans = auth.elevate(plans.listPlans);
    const elevatedGetPlan = auth.elevate(plans.getPlan);
    const elevatedCreatePlan = auth.elevate(plans.createPlan);

    export const listPlans = webMethod(Permissions.Anyone, async () => {
      try {
        const response = await elevatedListPlans();
        const allPlans = response.plans;

        return allPlans;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    });

    export const getPlan = webMethod(Permissions.Anyone, async (planId) => {
      try {
        const selectedPlan = await elevatedGetPlan(planId);

        return selectedPlan;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    });

    export const createPlan = webMethod(Permissions.Anyone, async (plan) => {
      try {
        const newPlan = await elevatedCreatePlan(plan);

        return newPlan;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    });

    /*************
     * Page code *
     *************/

    import { listPlans, getPlan, createPlan } from 'backend/plan-functions.web';
    $w.onReady(async function () {
      $w('#plansDropdown').disable();
      $w('#clonePlanBtn').disable();
      populatePlansDropdown();

      let selectedPlan;

      $w('#plansDropdown').onChange(async () => {
        const planId = $w('#plansDropdown').value;
        selectedPlan = await getPlan(planId);

        // Set default value for the new plan name
        $w('#planName').value = `${selectedPlan.name}-duplicate`
        $w('#clonePlanBtn').enable();
      });

      $w('#clonePlanBtn').onClick(async () => {
        const newPlan = selectedPlan;
        // Use inputs to set the new plan name and price
        newPlan.name = $w('#planName').value;
        newPlan.pricing.price.value = $w('#price').value;

        await createPlan(newPlan);
      });
    });

    async function populatePlansDropdown() {
      const plans = await listPlans();
      $w('#plansDropdown').options = plans.map((plan) => {
        return {
          label: plan.name,
          value: plan._id
        }
      });
      $w('#plansDropdown').enable();
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getPlan (self-hosted)
    import { createClient } from '@wix/sdk';
    import { plans } from '@wix/pricing-plans';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { plans },
      // Include the auth strategy and host as relevant
    });

    async function getPlan(_id) {
      const response = await myWixClient.plans.getPlan(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
