---
title: "List Custom Field Suggestions"
description: "Retrieves a list of suggestions for both custom fields and existing fields available from the Wix Members Area."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of suggestions for both custom fields and existing fields available from the Wix Members Area.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-MEMBERS">Read Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/members/v1/custom-field-suggestions
    ```

    ## Query Params

    <ParamField query="paging" type="Paging">

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of items to load.

        </ParamField>

        <ParamField query="offset" type="integer">

          Number of items to skip in the current sort order.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="sorting" type="Sorting[]">

      <Expandable title="sorting properties">

        <ParamField query="fieldName" type="string">

          Name of the field to sort by.

          Constraints: `maxLength 512`.

        </ParamField>

        <ParamField query="order" type="string">

          Sort order.

          **Supported values:**

          - `ASC`
          - `DESC`

        </ParamField>

        <ParamField query="origin" type="AddressLocation">

          Origin point for geo-distance sorting on a GEO field
          results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

          <Expandable title="origin properties">

            <ParamField query="latitude" type="number">

              Address latitude.

            </ParamField>

            <ParamField query="longitude" type="number">

              Address longitude.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="suggestions" type="CustomFieldSuggestion[]">

      Custom field suggestions.

      See the [Custom Field Suggestion Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-field-suggestions/custom-field-suggestion-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        https://www.wixapis.com/members/v1/custom-field-suggestions \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>'
        -d '{
          {
            "sorting": []
          }
        },
      ```

      ```json Response
      {
        "suggestions": [
          {
            "name": "Company",
            "defaultPrivacy": "PRIVATE",
            "fieldType": "TEXT",
            "socialType": "UNKNOWN",
            "fieldOrigin": "CONTACT",
            "customFieldId": null
          },
          {
            "name": "Birthdate",
            "defaultPrivacy": "PRIVATE",
            "fieldType": "DATE",
            "socialType": "UNKNOWN",
            "fieldOrigin": "CONTACT",
            "customFieldId": null
          },
          {
            "name": "Favorite Animal",
            "defaultPrivacy": "PRIVATE",
            "fieldType": "TEXT",
            "socialType": "UNKNOWN",
            "fieldOrigin": "CUSTOM",
            "customFieldId": "e75aee4c-21c1-469a-ad3e-8f64ab8073cc"
          }
        ],
        "metadata": {
          "count": 3,
          "offset": 0,
          "total": 3,
          "tooManyToCount": false,
          "hasNext": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of suggestions for both custom fields and existing fields available from the Wix Members Area.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-MEMBERS">Read Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customFieldSuggestions } from '@wix/members';

    customFieldSuggestions.listCustomFieldSuggestions(options): Promise<ListCustomFieldSuggestionsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListCustomFieldSuggestionsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="Paging">

          paging

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="sorting" type="Sorting[]">

          <Expandable title="sorting properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListCustomFieldSuggestionsResponse>`.

    <ResponseField name="suggestions" type="CustomFieldSuggestion[]">

      Custom field suggestions.

      See the [Custom Field Suggestion Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-field-suggestions/custom-field-suggestion-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadata">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-custom-field-suggestions" id="ex-sdk-list-custom-field-suggestions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-custom-field-suggestions" id="ex-sdk-list-custom-field-suggestions-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-custom-field-suggestions" id="ex-sdk-list-custom-field-suggestions-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-custom-field-suggestions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-custom-field-suggestions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listCustomFieldSuggestions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listCustomFieldSuggestions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listCustomFieldSuggestions (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-custom-field-suggestions-0"><span className="wix-picker-name">listCustomFieldSuggestions</span></label>
    <label htmlFor="ex-sdk-list-custom-field-suggestions-1"><span className="wix-picker-name">listCustomFieldSuggestions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-custom-field-suggestions-2"><span className="wix-picker-name">listCustomFieldSuggestions (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listCustomFieldSuggestions
    import { customFieldSuggestions } from '@wix/members';

    async function listCustomFieldSuggestions(options) {
      const response = await customFieldSuggestions.listCustomFieldSuggestions(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listCustomFieldSuggestions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customFieldSuggestions } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customFieldSuggestions },
      // Include the auth strategy and host as relevant
    });

    async function listCustomFieldSuggestions(options) {
      const response = await myWixClient.customFieldSuggestions.listCustomFieldSuggestions(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listCustomFieldSuggestions (with elevated permissions)
    import { customFieldSuggestions } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myListCustomFieldSuggestionsMethod(options) {
      const elevatedListCustomFieldSuggestions = auth.elevate(customFieldSuggestions.listCustomFieldSuggestions);
      const response = await elevatedListCustomFieldSuggestions(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
