---
title: "Delete Like"
description: "Deletes a like created by the currently authenticated site visitor or member."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a like created by the currently authenticated site visitor or member.

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/go-headless/coding/rest-api/visitors-and-members/make-api-calls-with-oauth).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/blog/v1/likes/{likeId}
    ```

    ## Path Params

    <ParamField path="likeId" type="string" required>

      ID of the like to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteLikeResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Like Deleted | `wix.blog.v1.like_deleted` | Triggers when a like is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/blog/v1/likes/87654321-4321-4321-4321-210987654321' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a like created by the currently authenticated site visitor or member.

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/go-headless/coding/rest-api/visitors-and-members/make-api-calls-with-oauth).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BLOG.MANAGE-BLOG">Manage Blog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { likes } from '@wix/blog';

    likes.deleteLike(likeId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="likeId" type="string" required>

      ID of the like to delete.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Like Deleted | `wix.blog.v1.like_deleted` | Triggers when a like is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-like" id="ex-sdk-delete-like-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-like" id="ex-sdk-delete-like-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-like-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-like-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteLike</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteLike (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-like-0"><span className="wix-picker-name">deleteLike</span></label>
    <label htmlFor="ex-sdk-delete-like-1"><span className="wix-picker-name">deleteLike (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteLike
    import { likes } from '@wix/blog';

    async function deleteLike(likeId) {
      const response = await likes.deleteLike(likeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteLike (self-hosted)
    import { createClient } from '@wix/sdk';
    import { likes } from '@wix/blog';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { likes },
      // Include the auth strategy and host as relevant
    });

    async function deleteLike(likeId) {
      const response = await myWixClient.likes.deleteLike(likeId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
