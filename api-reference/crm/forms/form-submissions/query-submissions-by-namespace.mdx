---
title: "Query Submissions By Namespace"
description: "Note: The Form Submission API only works with the Wix Forms app."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    The Form Submission API only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.

    </Note>
    <br />

    Returns a list of up to 100 submissions, given the provided paging, filtering, and sorting.

    You can only query submissions from a specified namespace. Use the query filter on the `namespace` field, otherwise you will receive an error.

    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language), [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination), and [Field Projection](https://dev.wix.com/api/rest/getting-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `formId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `contactId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `namespace` | `$eq` | None |
    | `status` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `seen` | `$eq`, `$ne` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$gt`, `$gte`, `$in`, `$lt`, `$lte`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$gt`, `$gte`, `$in`, `$lt`, `$lte`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.memberId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.visitorId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.applicationId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.userId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `orderDetails.checkoutId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORMS.READ-SUBMISSIONS">Read Submissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/forms/v4/submissions/namespace/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 3`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="onlyYourOwn" type="boolean">

      Whether to return only your own submissions. If `false`, returns all submissions based on query filters.

    </ParamField>

    ## Response Object

    <ResponseField name="submissions" type="FormSubmission[]">

      The retrieved Submissions.

      See the [Submission Object](/api-reference/crm/forms/form-submissions/submission-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
        'https://www.wixapis.com/form-submission-service/v4/submissions/namespace/query' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "query": {
            "filter": {
              "formId": "e62e3011-55cf-4de3-a497-e097b52d86b8",
              "namespace": "wix.form_app.form"
            }
          },
          "onlyYourOwn": true
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "submissions": [
          {
            "id": "e62e3011-55cf-4de3-a497-e097b52d86b7",
            "formId": "e62e3011-55cf-4de3-a497-e097b52d86b8",
            "namespace": "wix.form_app.form",
            "status": "PENDING",
            "submissions": {
              "first_name": "Adam",
              "last_name": "Fisher"
            },
            "submitter": {
              "memberId": "4b42fb49-8c77-4128-859c-8247756f68ef"
            },
            "seen": false,
            "revision": "1",
            "createdDate": "2019-10-30T17:22:10.299Z",
            "updatedDate": "2019-10-30T17:22:10.299Z"
          }
        ],
        "metadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Note>

    The Form Submission API only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.

    </Note>
    <br />

    Returns a list of up to 100 submissions, given the provided paging, filtering, and sorting.

    You can only query submissions from a specified namespace. Use the query filter on the `namespace` field, otherwise you will receive an error.

    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language), [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination), and [Field Projection](https://dev.wix.com/api/rest/getting-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `formId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `contactId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `namespace` | `$eq` | None |
    | `status` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `seen` | `$eq`, `$ne` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$gt`, `$gte`, `$in`, `$lt`, `$lte`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$gt`, `$gte`, `$in`, `$lt`, `$lte`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.memberId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.visitorId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.applicationId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `submitter.userId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |
    | `orderDetails.checkoutId` | `$eq`, `$in`, `$ne`, `$nin` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FORMS.READ-SUBMISSIONS">Read Submissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { submissions } from '@wix/forms';

    submissions.querySubmissionsByNamespace(query, options): SubmissionsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="SubmissionQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 3`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QuerySubmissionsByNamespaceOptions">

      Query options.

      <Expandable title="options properties">

        <ParamField body="onlyYourOwn" type="boolean">

          Whether to return only your own submissions. If `false`, returns all submissions based on query filters.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `SubmissionsQueryBuilder`.

    <ResponseField name="submissions" type="FormSubmission[]">

      The retrieved Submissions.

      See the [Submission Object](/api-reference/crm/forms/form-submissions/submission-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-submissions-by-namespace" id="ex-sdk-query-submissions-by-namespace-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-submissions-by-namespace" id="ex-sdk-query-submissions-by-namespace-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-submissions-by-namespace" id="ex-sdk-query-submissions-by-namespace-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-submissions-by-namespace-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-submissions-by-namespace-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query submissions by namespace</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query submissions by namespace (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">querySubmissionsByNamespace (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-submissions-by-namespace-0"><span className="wix-picker-name">Query submissions by namespace</span></label>
    <label htmlFor="ex-sdk-query-submissions-by-namespace-1"><span className="wix-picker-name">Query submissions by namespace (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-query-submissions-by-namespace-2"><span className="wix-picker-name">querySubmissionsByNamespace (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query submissions by namespace
    import { submissions } from '@wix/forms';

    export async function myQuerySubmissionsByNamespaceFunction() {
      try {
        const items = await submissions.querySubmissionsByNamespace()
          .eq('namespace', 'wix.form_app.form')
          .find()
        console.log('Success! Submissions:', items);
          return items
      } catch (error) {
          console.error(error)
      }
    }

    /* Promise resolves to:
    [
        {
            "formId": "21bcb6c7-02b3-4ed1-b6db-7856094fac03",
            "namespace": "wix.form_app.form",
            "status": "CONFIRMED",
            "submissions": {
                "last_name": "Doe",
                "first_name": "John"
            },
            "revision": "3",
            "submitter": {
                "applicationId": "151e476a-715e-ec33-db9a-a7ff4d51f70a"
            },
            "seen": false,
            "_id": "abb9b5c9-a881-467c-9c34-b9bea43ca5f0",
            "_createdDate": "2023-12-28T12:54:04.652Z",
            "_updatedDate": "2023-12-28T12:54:05.483Z"
        },
        {
            "formId": "21bcb6c7-02b3-4ed1-b6db-7856094fac03",
            "namespace": "wix.form_app.form",
            "status": "CONFIRMED",
            "submissions": {
                "last_name": "McBride",
                "first_name": "Patsy"
            },
            "revision": "3",
            "submitter": {
                "applicationId": "151e476a-715e-ec33-db9a-a7ff4d51f70a"
            },
            "seen": false,
            "_id": "f8281b62-1b2f-45bf-ba7d-f041d7653d2d",
            "_createdDate": "2023-12-28T12:55:55.630Z",
            "_updatedDate": "2023-12-28T12:55:56.233Z"
        }
    ]
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Query submissions by namespace (with elevated permissions)
    import { submissions } from '@wix/forms';
    import { auth } from '@wix/essentials';

    const elevatedQuerySubmissionsByNamespace = auth.elevate(submissions.querySubmissionsByNamespace);

    export async function myQuerySubmissionsByNamespaceFunction() {
      try {
        const items = await elevatedQuerySubmissionsByNamespace()
          .eq('namespace', 'wix.form_app.form')
          .find()
        console.log('Success! Submissions:', items);
          return items
      } catch (error) {
          console.error(error)
      }
    }

    /* Promise resolves to:
    [
        {
            "formId": "21bcb6c7-02b3-4ed1-b6db-7856094fac03",
            "namespace": "wix.form_app.form",
            "status": "CONFIRMED",
            "submissions": {
                "last_name": "Doe",
                "first_name": "John"
            },
            "revision": "3",
            "submitter": {
                "applicationId": "151e476a-715e-ec33-db9a-a7ff4d51f70a"
            },
            "seen": false,
            "_id": "abb9b5c9-a881-467c-9c34-b9bea43ca5f0",
            "_createdDate": "2023-12-28T12:54:04.652Z",
            "_updatedDate": "2023-12-28T12:54:05.483Z"
        },
        {
            "formId": "21bcb6c7-02b3-4ed1-b6db-7856094fac03",
            "namespace": "wix.form_app.form",
            "status": "CONFIRMED",
            "submissions": {
                "last_name": "McBride",
                "first_name": "Patsy"
            },
            "revision": "3",
            "submitter": {
                "applicationId": "151e476a-715e-ec33-db9a-a7ff4d51f70a"
            },
            "seen": false,
            "_id": "f8281b62-1b2f-45bf-ba7d-f041d7653d2d",
            "_createdDate": "2023-12-28T12:55:55.630Z",
            "_updatedDate": "2023-12-28T12:55:56.233Z"
        }
    ]
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript querySubmissionsByNamespace (self-hosted)
    import { createClient } from '@wix/sdk';
    import { submissions } from '@wix/forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { submissions },
      // Include the auth strategy and host as relevant
    });

    async function querySubmissionsByNamespace(query,options) {
      const response = await myWixClient.submissions.querySubmissionsByNamespace(query,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
