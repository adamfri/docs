---
title: "Published Content Object"
description: "The Published Content object in the Wix Published Content API."
icon: "cube"
---

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Published content ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="schemaKey" type="SchemaKey">

      Schema key of the published content.

      <Expandable title="schemaKey properties">

        <ResponseField name="appId" type="string">

          ID of the app that created the schema.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="entityType" type="string">

          A unique name defined by the app developer to differentiate translation schemas for various entities within their app. For example, if an app developer is creating a translation schema for blog posts, the `entityType` can be `'blog-posts'`.

          Constraints: `minLength 1`, `maxLength 100`, `immutable`.

        </ResponseField>

        <ResponseField name="scope" type="string">

          Scope of the translation schema.

          Constraints: `immutable`.

          **Supported values:**

          - `GLOBAL` — Global schema for all sites.
          - `SITE` — Custom schema for a specific site.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="entityId" type="string">

      A unique identifier defined by the app developer to differentiate translation content for various entities within the site. For example, if an app developer is creating translation content for blog posts, the `entityId` can be `'blog-posts-1'`.

      **Read-only.** Constraints: `maxLength 100`.

    </ResponseField>

    <ResponseField name="locale" type="string">

      Published content locale.

      **Read-only.** Constraints: `format LANGUAGE_TAG`.

    </ResponseField>

    <ResponseField name="fields" type="PublishedContentField[]">

      List of fields that have been translated and published for the given locale.

      Constraints: `minItems 0`, `maxItems 2000`.

      <Expandable title="fields properties">

        <ResponseField name="schemaFieldKey" type="string">

          Unique string identifier that links a specific content field to its corresponding field in the schema.

          **Read-only.** Constraints: `maxLength 100`.

        </ResponseField>

        <ResponseField name="sequenceIds" type="string[]">

          For repeated fields. Contains the IDs of the messages linked to this field.

          Constraints: `maxLength 100`, `maxItems 100`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="parentEntityId" type="string">

      A reference to the parent content. For example, if the content is a menu item, this property would contain the `entityId` of the menu it belongs to.

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Custom field data for the published content object.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Published content ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="schemaKey" type="SchemaKey">

      Schema key of the published content.

      <Expandable title="schemaKey properties">

        <ResponseField name="appId" type="string">

          ID of the app that created the schema.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="entityType" type="string">

          A unique name defined by the app developer to differentiate translation schemas for various entities within their app. For example, if an app developer is creating a translation schema for blog posts, the `entityType` can be `'blog-posts'`.

          Constraints: `minLength 1`, `maxLength 100`, `immutable`.

        </ResponseField>

        <ResponseField name="scope" type="string">

          Scope of the translation schema.

          Constraints: `immutable`.

          **Supported values:**

          - `GLOBAL` — Global schema for all sites.
          - `SITE` — Custom schema for a specific site.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="entityId" type="string">

      A unique identifier defined by the app developer to differentiate translation content for various entities within the site. For example, if an app developer is creating translation content for blog posts, the `entityId` can be `'blog-posts-1'`.

      **Read-only.** Constraints: `maxLength 100`.

    </ResponseField>

    <ResponseField name="locale" type="string">

      Published content locale.

      **Read-only.** Constraints: `format LANGUAGE_TAG`.

    </ResponseField>

    <ResponseField name="fields" type="PublishedContentField[]">

      List of fields that have been translated and published for the given locale.

      Constraints: `minItems 0`, `maxItems 2000`.

      <Expandable title="fields properties">

        <ResponseField name="schemaFieldKey" type="string">

          Unique string identifier that links a specific content field to its corresponding field in the schema.

          **Read-only.** Constraints: `maxLength 100`.

        </ResponseField>

        <ResponseField name="sequenceIds" type="string[]">

          For repeated fields. Contains the IDs of the messages linked to this field.

          Constraints: `maxLength 100`, `maxItems 100`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="parentEntityId" type="string">

      A reference to the parent content. For example, if the content is a menu item, this property would contain the `entityId` of the menu it belongs to.

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Custom field data for the published content object.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
