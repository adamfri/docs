---
title: "Setup"
description: "To use the Currencies API, install the @wix/ecom package using npm or Yarn: or Then import { currencies } from @wix/ecom:"
icon: "file-lines"
---

<Note>This article applies to the JavaScript SDK only.</Note>

To use the Currencies API, install the `@wix/ecom` package using npm or Yarn:
  ```bash
npm install @wix/ecom
```

or

```bash
yarn add @wix/ecom
```

Then import `{ currencies }` from `@wix/ecom`:

```js
import { currencies } from '@wix/ecom'
```
