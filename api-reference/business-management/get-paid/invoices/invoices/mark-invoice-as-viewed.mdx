---
title: "Mark Invoice As Viewed"
description: "Marks an invoice as viewed."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Marks an invoice as viewed.

    Adds the `VIEWED` status qualifier to the invoice and records the view timestamp in `activityInfo`. For details, see [Status Qualifiers](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/status-qualifiers).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/invoices/v4/invoices/{invoiceId}/mark-as-viewed
    ```

    ## Path Params

    <ParamField path="invoiceId" type="string" required>

      Invoice ID.

    </ParamField>

    ## Response Object

    <ResponseField name="invoice" type="Invoice">

      Updated invoice.

      See the [Invoice Object](/api-reference/business-management/get-paid/invoices/invoices/invoice-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Updated | `wix.invoices.invoices.v4.invoice_updated` | Triggered when an invoice is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Marks invoice as viewed.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/invoices/v4/invoices/51be0e68-75c3-4b7a-97e7-3a6643ecf3e2/mark-as-viewed' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "invoiceId": "51be0e68-75c3-4b7a-97e7-3a6643ecf3e2"
        }'
      ```

      ```json Response
      {
        "invoice": {
          "id": "51be0e68-75c3-4b7a-97e7-3a6643ecf3e2",
          "revision": "4",
          "createdDate": "2025-01-15T10:05:23.299Z",
          "updatedDate": "2025-01-25T09:15:00.000Z",
          "status": "PUBLISHED",
          "statusQualifiers": ["VIEWED"],
          "availableActions": [
            "UPDATE",
            "PAY",
            "VOID",
            "SEND",
            "MARK_AS_SENT",
            "GENERATE_PDF_DOCUMENT",
            "ARCHIVE"
          ],
          "sourceReference": {
            "appId": "13ee94c1-b635-8505-3391-97919052c16f",
            "externalReferenceId": "e8b6776d-9efc-4452-b528-a93f651f99a7"
          },
          "reference": {
            "referenceType": "STANDALONE",
            "standaloneReference": {}
          },
          "regionalProperties": {
            "locale": {
              "languageCode": "en",
              "country": "US"
            },
            "language": "en",
            "timeZone": "America/New_York"
          },
          "numbering": {
            "number": 1,
            "prefix": "INV",
            "displayNumber": "INV-0001"
          },
          "issueDate": "2025-01-15T10:00:00.000Z",
          "dueDate": "2025-02-15T10:00:00.000Z",
          "title": "Invoice for Flowers Order",
          "currency": "USD",
          "customerInfo": {
            "contactId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "contactDetails": {
              "firstName": "John",
              "lastName": "Doe",
              "email": "john.doe@example.com",
              "phone": "+1-555-555-5555"
            },
            "billingAddress": {
              "country": "US",
              "subdivision": "NY",
              "city": "New York",
              "postalCode": "10001",
              "addressLine": "123 Billing Avenue"
            }
          },
          "lineItems": [
            {
              "lineItemType": "CUSTOM",
              "customItem": {
                "id": "36ec066d-7c29-4169-96e6-cbb4e3c25bcd",
                "name": "Roses",
                "description": "Beautiful red roses.",
                "quantity": "30",
                "price": "4.00",
                "totals": {
                  "subtotal": "120.00",
                  "priceBeforeTax": "120.00",
                  "tax": "0.00",
                  "priceAfterTax": "120.00"
                }
              }
            }
          ],
          "totals": {
            "subtotal": "120.00",
            "discount": "0.00",
            "shipping": "0.00",
            "tax": "0.00",
            "total": "120.00",
            "paidAmount": "0.00",
            "balance": "120.00"
          },
          "archived": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Marks an invoice as viewed.

    Adds the `VIEWED` status qualifier to the invoice and records the view timestamp in `activityInfo`. For details, see [Status Qualifiers](https://dev.wix.com/docs/api-reference/business-management/get-paid/invoices/status-qualifiers).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { invoices } from '@wix/get-paid';

    invoices.markInvoiceAsViewed(invoiceId): Promise
    ```

    ## Method Parameters

    <ParamField body="invoiceId" type="string" required>

      Invoice ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="invoice" type="Invoice">

      Updated invoice.

      See the [Invoice Object](/api-reference/business-management/get-paid/invoices/invoices/invoice-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Invoice Updated | `wix.invoices.invoices.v4.invoice_updated` | Triggered when an invoice is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-mark-invoice-as-viewed" id="ex-sdk-mark-invoice-as-viewed-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-mark-invoice-as-viewed" id="ex-sdk-mark-invoice-as-viewed-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-mark-invoice-as-viewed" id="ex-sdk-mark-invoice-as-viewed-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-mark-invoice-as-viewed-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-mark-invoice-as-viewed-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">markInvoiceAsViewed</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markInvoiceAsViewed (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markInvoiceAsViewed (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-mark-invoice-as-viewed-0"><span className="wix-picker-name">markInvoiceAsViewed</span></label>
    <label htmlFor="ex-sdk-mark-invoice-as-viewed-1"><span className="wix-picker-name">markInvoiceAsViewed (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-mark-invoice-as-viewed-2"><span className="wix-picker-name">markInvoiceAsViewed (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript markInvoiceAsViewed
    import { invoices } from '@wix/get-paid';

    async function markInvoiceAsViewed(invoiceId) {
      const response = await invoices.markInvoiceAsViewed(invoiceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markInvoiceAsViewed (self-hosted)
    import { createClient } from '@wix/sdk';
    import { invoices } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { invoices },
      // Include the auth strategy and host as relevant
    });

    async function markInvoiceAsViewed(invoiceId) {
      const response = await myWixClient.invoices.markInvoiceAsViewed(invoiceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markInvoiceAsViewed (with elevated permissions)
    import { invoices } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myMarkInvoiceAsViewedMethod(invoiceId) {
      const elevatedMarkInvoiceAsViewed = auth.elevate(invoices.markInvoiceAsViewed);
      const response = await elevatedMarkInvoiceAsViewed(invoiceId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
