---
title: "onReviewDeleted"
description: "Triggered when a review is deleted."
icon: "bolt"
---

Triggered when a review is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reviews.v1.review_deleted` |
| Package | `@wix/reviews` |
| Permissions | `Manage Stores`, `Manage Reviews` |

## Examples

<CodeGroup>

  ```javascript onReviewDeleted
  import { reviews } from '@wix/reviews';

  reviews.onReviewDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onReviewDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reviews } from '@wix/reviews';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reviews,
      },
  });

  wixClient.reviews.onReviewDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
