---
title: "Bulk Delete Ticket Definitions By Filter"
description: "Deletes multiple ticket definitions."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple ticket definitions.

    All ticket definitions that meet the specified `filter` criteria are deleted.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-TICKET-DEF">Manage Ticket Definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v3/bulk/ticket-definitions/delete
    ```

    ## Body Params

    <ParamField body="filter" type="object" required>

      Filter object. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details.

    </ParamField>

    ## Response Object

    Returns `BulkDeleteTicketDefinitionsByFilterResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ticket Definition Deleted | `wix.events.v3.ticket_definition_deleted` | Triggered when a ticket definition is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v3/bulk/ticket-definitions/delete' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "filter": {
          "eventId": "9d720f99-1b5a-4141-9877-d32985391e18"
        }
      }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple ticket definitions.

    All ticket definitions that meet the specified `filter` criteria are deleted.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-TICKET-DEF">Manage Ticket Definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { ticketDefinitionsV2 } from '@wix/events';

    ticketDefinitionsV2.bulkDeleteTicketDefinitionsByFilter(filter): Promise
    ```

    ## Method Parameters

    <ParamField body="filter" type="object" required>

      Filter object. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details.

    </ParamField>

    ## Returns

    Returns `BulkDeleteTicketDefinitionsByFilterResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ticket Definition Deleted | `wix.events.v3.ticket_definition_deleted` | Triggered when a ticket definition is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-ticket-definitions-by-filter" id="ex-sdk-bulk-delete-ticket-definitions-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-ticket-definitions-by-filter" id="ex-sdk-bulk-delete-ticket-definitions-by-filter-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-ticket-definitions-by-filter" id="ex-sdk-bulk-delete-ticket-definitions-by-filter-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-ticket-definitions-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-ticket-definitions-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteTicketDefinitionsByFilter</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteTicketDefinitionsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteTicketDefinitionsByFilter (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-ticket-definitions-by-filter-0"><span className="wix-picker-name">bulkDeleteTicketDefinitionsByFilter</span></label>
    <label htmlFor="ex-sdk-bulk-delete-ticket-definitions-by-filter-1"><span className="wix-picker-name">bulkDeleteTicketDefinitionsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-ticket-definitions-by-filter-2"><span className="wix-picker-name">bulkDeleteTicketDefinitionsByFilter (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteTicketDefinitionsByFilter
    import { ticketDefinitionsV2 } from '@wix/events';

    async function bulkDeleteTicketDefinitionsByFilter(filter) {
      const response = await ticketDefinitionsV2.bulkDeleteTicketDefinitionsByFilter(filter);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteTicketDefinitionsByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { ticketDefinitionsV2 } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { ticketDefinitionsV2 },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteTicketDefinitionsByFilter(filter) {
      const response = await myWixClient.ticketDefinitionsV2.bulkDeleteTicketDefinitionsByFilter(filter);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteTicketDefinitionsByFilter (with elevated permissions)
    import { ticketDefinitionsV2 } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteTicketDefinitionsByFilterMethod(filter) {
      const elevatedBulkDeleteTicketDefinitionsByFilter = auth.elevate(ticketDefinitionsV2.bulkDeleteTicketDefinitionsByFilter);
      const response = await elevatedBulkDeleteTicketDefinitionsByFilter(filter);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
