---
title: "Postpone End Date"
description: "Extends the duration of a pricing plan order by postponing the order's endDate."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Extends the duration of a pricing plan order by postponing the order's `endDate`. Postponing the end date of an order does not impact payments.

    New `endDate` must be later than the order's current `endDate`. Can't postpone orders that are unlimited.
    Can't postpone an order with `status`: `PAUSED`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAIDPLANS.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/pricing-plans/v2/orders/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Order ID.

    </ParamField>

    ## Body Params

    <ParamField body="endDate" type="datetime" required>

      New end date and time.

      Must be later than the current end date and time.

    </ParamField>

    ## Response Object

    Returns `PostponeEndDateResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Order End Date Postponed | `wix.pricing_plans.v2.order_end_date_postponed` | Triggered when an order's `endDate` is postponed. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X PATCH \
          'https://www.wixapis.com/pricing-plans/v2/orders/d5ce1234-c0a9-476c-abd3-c781bd589a78' \
          -H 'Authorization: <AUTH>' \
          --data-raw '{"endDate":"2022-10-26T13:45:53.129Z"}'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Extends the duration of a pricing plan order by postponing the order's `endDate`. Postponing the end date of an order does not impact payments.

    New `endDate` must be later than the order's current `endDate`. Can't postpone orders that are unlimited.
    Can't postpone an order with `status`: `PAUSED`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAIDPLANS.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { orders } from '@wix/pricing-plans';

    orders.postponeEndDate(_id, endDate): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Order ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="endDate" type="datetime" required>

      New end date and time.

      Must be later than the current end date and time.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Order End Date Postponed | `wix.pricing_plans.v2.order_end_date_postponed` | Triggered when an order's `endDate` is postponed. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-postpone-end-date" id="ex-sdk-postpone-end-date-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-postpone-end-date" id="ex-sdk-postpone-end-date-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-postpone-end-date" id="ex-sdk-postpone-end-date-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-postpone-end-date-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-postpone-end-date-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">postponeEndDate</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">postponeEndDate (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">postponeEndDate (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-postpone-end-date-0"><span className="wix-picker-name">postponeEndDate</span></label>
    <label htmlFor="ex-sdk-postpone-end-date-1"><span className="wix-picker-name">postponeEndDate (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-postpone-end-date-2"><span className="wix-picker-name">postponeEndDate (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript postponeEndDate
    import { orders } from '@wix/pricing-plans';

    async function postponeEndDate(_id,endDate) {
      const response = await orders.postponeEndDate(_id,endDate);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript postponeEndDate (self-hosted)
    import { createClient } from '@wix/sdk';
    import { orders } from '@wix/pricing-plans';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { orders },
      // Include the auth strategy and host as relevant
    });

    async function postponeEndDate(_id,endDate) {
      const response = await myWixClient.orders.postponeEndDate(_id,endDate);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript postponeEndDate (with elevated permissions)
    import { orders } from '@wix/pricing-plans';
    import { auth } from '@wix/essentials';

    async function myPostponeEndDateMethod(_id,endDate) {
      const elevatedPostponeEndDate = auth.elevate(orders.postponeEndDate);
      const response = await elevatedPostponeEndDate(_id,endDate);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
