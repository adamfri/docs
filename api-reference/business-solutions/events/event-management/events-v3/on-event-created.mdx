---
title: "onEventCreated"
description: "Triggered when event is created."
icon: "bolt"
---

Triggered when event is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v3.event_created` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Bookings Services and Settings`, `Manage Events - all permissions`, `Read Events`, `Manage Portfolio`, `Manage Restaurants - all permissions`, `Manage Guest List`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onEventCreated: Wix-hosted
  import { wixEventsV2 } from '@wix/events';

  wixEventsV2.onEventCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onEventCreated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { wixEventsV2 } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        wixEventsV2,
      },
  });

  wixClient.wixEventsV2.onEventCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
