---
title: "onFormTranslationDeleted"
description: "The onFormTranslationDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forms.v4.form_form_translation_deleted` |
| Package | `@wix/forms` |
| Permissions | `Manage Submissions`, `Manage Stores`, `Manage form submissions.`, `Read Intake Form (PII)`, `Manage Online Programs`, `Read Intake Form`, `Manage Intake Form`, `Manage Intake Form (PII)`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onFormTranslationDeleted
  import { forms } from '@wix/forms';

  forms.onFormTranslationDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onFormTranslationDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { forms } from '@wix/forms';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        forms,
      },
  });

  wixClient.forms.onFormTranslationDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
