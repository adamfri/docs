---
title: "Tax Group Object"
description: "A tax group is a category of specific line items grouped together based on their tax treatment."
icon: "cube"
---

A tax group is a category of specific line items grouped together based on their tax treatment.
You can create new tax groups to apply distinct tax rates and rules.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Tax group ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Tax group name.

      Constraints: `minLength 1`, `maxLength 200`.

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the tax group is updated. To prevent conflicting changes,
      the current revision must be passed when updating the tax group.

      Ignored when creating a tax group.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the tax group was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time the tax group was last updated.

      **Read-only.**

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Tax group ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Tax group name.

      Constraints: `minLength 1`, `maxLength 200`.

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the tax group is updated. To prevent conflicting changes,
      the current revision must be passed when updating the tax group.

      Ignored when creating a tax group.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the tax group was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time the tax group was last updated.

      **Read-only.**

    </ResponseField>

  </Tab>

</Tabs>
