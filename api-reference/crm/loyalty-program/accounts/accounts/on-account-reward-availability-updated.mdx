---
title: "onAccountRewardAvailabilityUpdated"
description: "Triggered when rewardAvailable field is changed on Account."
icon: "bolt"
---

Triggered when rewardAvailable field is changed on Account.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.v1.account_reward_availability_updated` |
| Package | `@wix/loyalty` |
| Permissions | `Read Loyalty`, `Manage Loyalty` |

## Examples

<CodeGroup>

  ```javascript onAccountRewardAvailabilityUpdated
  import { accounts } from '@wix/loyalty';

  accounts.onAccountRewardAvailabilityUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onAccountRewardAvailabilityUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { accounts } from '@wix/loyalty';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        accounts,
      },
  });

  wixClient.accounts.onAccountRewardAvailabilityUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
