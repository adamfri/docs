---
title: "onGroupRulesUpdated"
description: "Triggered when rules are updated."
icon: "bolt"
---

Triggered when rules are updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_rules_updated` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupRulesUpdated
  import { rules } from '@wix/groups';

  rules.onGroupRulesUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupRulesUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { rules } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        rules,
      },
  });

  wixClient.rules.onGroupRulesUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
