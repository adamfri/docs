---
title: "Setup"
description: "To use the TicketDefinitionsV2 API, install the @wix/events package using npm or Yarn: or Then import { ticketDefinitionsV2 } from @wix/events:"
icon: "file-lines"
---

<Note>This article applies to the JavaScript SDK only.</Note>

To use the TicketDefinitionsV2 API, install the `@wix/events` package using npm or Yarn:
  ```bash
npm install @wix/events
```

or

```bash
yarn add @wix/events
```

Then import `{ ticketDefinitionsV2 }` from `@wix/events`:

```js
import { ticketDefinitionsV2 } from '@wix/events'
```
