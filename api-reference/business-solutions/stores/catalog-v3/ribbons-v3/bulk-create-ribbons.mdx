---
title: "Bulk Create Ribbons"
description: "Creates multiple ribbons."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple ribbons.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.RIBBON_WRITE">Ribbon write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/bulk/ribbons/create
    ```

    ## Body Params

    <ParamField body="ribbons" type="Ribbon[]" required>

      Ribbons to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Ribbon Object](/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/ribbon-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the full created ribbon entities in the response.

      Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkRibbonResult[]">

      Ribbons created by bulk action.

      Constraints: `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata for ribbon.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Ribbon">

          Full ribbon entity.

          Returned only if `returnEntity: true` is passed in the request.

          See the [Ribbon Object](/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/ribbon-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ribbon Created | `wix.stores.catalog.v3.ribbon_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    Create 2 ribbons at once.

    <CodeGroup>

      ```bash Request
      curl -X POST \
       'https://www.wixapis.com/stores/v3/bulk/ribbons/create' \
       -H 'Content-type: application/json' \
       -H 'Authorization: <AUTH>' \
       -d '{
         "ribbons": [
           {
             "name": "New Offer!"
           },
           {
             "name": "New collection"
           }
         ],
         "returnEntity": true
       }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "9d80c64a-b39f-4b7c-b47a-2ba0e840b740",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "9d80c64a-b39f-4b7c-b47a-2ba0e840b740",
              "revision": "1",
              "createdDate": "2024-06-26T09:15:22.092Z",
              "updatedDate": "2024-06-26T09:15:22.092Z",
              "name": "New Offer!"
            }
          },
          {
            "itemMetadata": {
              "id": "d9f4daa3-d122-4e84-a6b4-432f3e48cfe4",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "id": "d9f4daa3-d122-4e84-a6b4-432f3e48cfe4",
              "revision": "1",
              "createdDate": "2024-06-26T09:15:22.092Z",
              "updatedDate": "2024-06-26T09:15:22.092Z",
              "name": "New collection"
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple ribbons.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.RIBBON_WRITE">Ribbon write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { ribbonsV3 } from '@wix/stores';

    ribbonsV3.bulkCreateRibbons(ribbons, options): Promise<BulkCreateRibbonsResponse>
    ```

    ## Method Parameters

    <ParamField body="ribbons" type="Ribbon[]" required>

      Ribbons to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Ribbon Object](/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/ribbon-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateRibbonsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return the full created ribbon entities in the response.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkCreateRibbonsResponse>`.

    <ResponseField name="results" type="BulkRibbonResult[]">

      Ribbons created by bulk action.

      Constraints: `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata for ribbon.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Ribbon">

          Full ribbon entity.

          Returned only if `returnEntity: true` is passed in the request.

          See the [Ribbon Object](/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/ribbon-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ribbon Created | `wix.stores.catalog.v3.ribbon_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-ribbons" id="ex-sdk-bulk-create-ribbons-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-ribbons" id="ex-sdk-bulk-create-ribbons-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-ribbons-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-ribbons-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk create ribbons</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateRibbons (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-ribbons-0"><span className="wix-picker-name">Bulk create ribbons</span></label>
    <label htmlFor="ex-sdk-bulk-create-ribbons-1"><span className="wix-picker-name">bulkCreateRibbons (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk create ribbons
    import { ribbonsV3 } from "@wix/stores";

    const ribbons = [
      {
        name: "New Offer!"
      },
      {
        name: "New collection"
      }
    ];

    const options = {
      returnEntity: true
    };

    async function bulkCreateRibbons() {
      const response = await ribbonsV3.bulkCreateRibbons(ribbons, options);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateRibbons (self-hosted)
    import { createClient } from '@wix/sdk';
    import { ribbonsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { ribbonsV3 },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateRibbons(ribbons,options) {
      const response = await myWixClient.ribbonsV3.bulkCreateRibbons(ribbons,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
