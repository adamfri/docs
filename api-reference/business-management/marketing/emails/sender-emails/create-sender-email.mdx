---
title: "Create Sender Email"
description: "Creates a sender email."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a sender email.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/sender-emails/v1/sender-emails
    ```

    ## Body Params

    <ParamField body="senderEmail" type="SenderEmail" required>

      Sender email info.

      See the [Sender Email Object](/api-reference/business-management/marketing/emails/sender-emails/sender-email-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="senderEmail" type="SenderEmail">

      The created sender email.

      See the [Sender Email Object](/api-reference/business-management/marketing/emails/sender-emails/sender-email-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `DUPLICATE` | There is already a sender email with this email address. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Email Created | `wix.promote.v1.sender_email_created` | Triggered when a sender email is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/sender-emails/v1/sender-emails' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
          "senderEmail": {
              "emailAddress": "john.doe@example.com"
          }
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "senderEmail": {
          "id": "30057014-8ecc-4a1b-9c53-bcc7dd033bb5",
          "createdDate": "2024-01-01T00:00:00.000Z",
          "updatedDate": "2024-01-01T00:00:00.000Z",
          "emailAddress": "john.doe@example.com",
          "verified": false
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a sender email.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderEmails } from '@wix/sender-emails';

    senderEmails.createSenderEmail(senderEmail): Promise<SenderEmail>
    ```

    ## Method Parameters

    <ParamField body="senderEmail" type="SenderEmail" required>

      Sender email info.

      See the [Sender Email Object](/api-reference/business-management/marketing/emails/sender-emails/sender-email-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<SenderEmail>`.

    See the [Sender Email Object](/api-reference/business-management/marketing/emails/sender-emails/sender-email-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `409` | `ALREADY_EXISTS` | `DUPLICATE` | There is already a sender email with this email address. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Email Created | `wix.promote.v1.sender_email_created` | Triggered when a sender email is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-sender-email" id="ex-sdk-create-sender-email-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-sender-email" id="ex-sdk-create-sender-email-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-sender-email" id="ex-sdk-create-sender-email-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-sender-email-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-sender-email-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createSenderEmail</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createSenderEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createSenderEmail (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-sender-email-0"><span className="wix-picker-name">createSenderEmail</span></label>
    <label htmlFor="ex-sdk-create-sender-email-1"><span className="wix-picker-name">createSenderEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-sender-email-2"><span className="wix-picker-name">createSenderEmail (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createSenderEmail
    import { senderEmails } from '@wix/sender-emails';

    async function createSenderEmail(senderEmail) {
      const response = await senderEmails.createSenderEmail(senderEmail);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createSenderEmail (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderEmails } from '@wix/sender-emails';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderEmails },
      // Include the auth strategy and host as relevant
    });

    async function createSenderEmail(senderEmail) {
      const response = await myWixClient.senderEmails.createSenderEmail(senderEmail);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createSenderEmail (with elevated permissions)
    import { senderEmails } from '@wix/sender-emails';
    import { auth } from '@wix/essentials';

    async function myCreateSenderEmailMethod(senderEmail) {
      const elevatedCreateSenderEmail = auth.elevate(senderEmails.createSenderEmail);
      const response = await elevatedCreateSenderEmail(senderEmail);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
