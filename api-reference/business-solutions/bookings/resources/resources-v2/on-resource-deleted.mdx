---
title: "onResourceDeleted"
description: "Triggered when a resource is deleted."
icon: "bolt"
---

Triggered when a resource is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.resources.v2.resource_deleted` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onResourceDeleted
  import { resources } from '@wix/bookings';

  resources.onResourceDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onResourceDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { resources } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        resources,
      },
  });

  wixClient.resources.onResourceDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
