---
title: "Get Marketing Settings"
description: "Retrieves the site's marketing settings."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the site's marketing settings.

    If no settings have been saved yet, the response contains only the derived `language`,
    `targetLocations`, and `businessLocation`. All preference fields (such as `marketingPlanGoal`,
    `socialChannels`, `frequency`, `topics`, and `marketingTools`) are empty. To see the default
    values applied when a plan is generated, call `GetMarketingSettingsDefaults`. Use `field_set`
    to limit which data sources are queried.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-settings
    ```

    ## Query Params

    <ParamField query="fieldSet" type="string[]">

      Data sources to include in the response. Omit to fetch all sources.

      Constraints: `minItems 0`, `maxItems 4`.

      **Supported values:**

      - `SITE_PROPERTIES` — Site language and locale derived from site properties.
      - `TARGET_LOCATIONS_LEGACY` — Deprecated SEO target locations (see `target_locations`).
      - `BUSINESS_LOCATION` — Business location from SEO settings (see `business_location`).
      - `SETTINGS` — Saved settings and tool-specific overrides from the database.

    </ParamField>

    ## Response Object

    <ResponseField name="marketingSettings" type="MarketingSettings">

      The site's marketing settings.

      See the [Marketing Settings Object](/api-reference/business-management/marketing/marketing-plan/marketing-settings-v1/marketing-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieve the marketing settings for the current site.

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-settings' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "marketingSettings": {
          "id": "f1c2e3a4-5b6c-47d8-9e0f-1a2b3c4d5e6f",
          "settings": {
            "toneOfVoice": "Friendly and approachable, with a focus on quality craftsmanship",
            "pointOfView": "FIRST_PERSON",
            "marketingPlanGoal": {
              "goalKey": "BRAND_AWARENESS"
            },
            "socialChannels": [
              "INSTAGRAM",
              "FACEBOOK",
              "PINTEREST"
            ],
            "frequency": {
              "interval": "WEEK",
              "count": 3
            },
            "marketingTools": [
              "SOCIAL_MARKETING",
              "EMAIL_MARKETING"
            ],
            "topics": {
              "included": [
                "sustainable fashion",
                "new arrivals"
              ],
              "excluded": [
                "politics"
              ],
              "coreTopic": "handmade apparel"
            },
            "calendar": {
              "ids": [
                "9b1d8f2a-7c3e-4a5b-8d6f-1e2c3b4a5d6e"
              ]
            },
            "businessProfile": {
              "businessGoal": "Grow online sales of our handmade apparel line",
              "valueProposition": "Ethically made, one-of-a-kind clothing",
              "targetAudience": "Eco-conscious shoppers aged 25-45"
            }
          },
          "createdAt": "2026-05-01T09:15:00.000Z",
          "updatedAt": "2026-06-10T14:32:11.000Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the site's marketing settings.

    If no settings have been saved yet, the response contains only the derived `language`,
    `targetLocations`, and `businessLocation`. All preference fields (such as `marketingPlanGoal`,
    `socialChannels`, `frequency`, `topics`, and `marketingTools`) are empty. To see the default
    values applied when a plan is generated, call `GetMarketingSettingsDefaults`. Use `field_set`
    to limit which data sources are queried.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingSettings } from '@wix/promote-growth-tools-marketing-plan';

    marketingSettings.getMarketingSettings(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="GetMarketingSettingsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fieldSet" type="string[]">

          Data sources to include in the response. Omit to fetch all sources.

          Constraints: `minItems 0`, `maxItems 4`.

          **Supported values:**

          - `SITE_PROPERTIES` — Site language and locale derived from site properties.
          - `TARGET_LOCATIONS_LEGACY` — Deprecated SEO target locations (see `target_locations`).
          - `BUSINESS_LOCATION` — Business location from SEO settings (see `business_location`).
          - `SETTINGS` — Saved settings and tool-specific overrides from the database.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="marketingSettings" type="MarketingSettings">

      The site's marketing settings.

      See the [Marketing Settings Object](/api-reference/business-management/marketing/marketing-plan/marketing-settings-v1/marketing-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-marketing-settings" id="ex-sdk-get-marketing-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-marketing-settings" id="ex-sdk-get-marketing-settings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-marketing-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-marketing-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get marketing settings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getMarketingSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-marketing-settings-0"><span className="wix-picker-name">Get marketing settings</span></label>
    <label htmlFor="ex-sdk-get-marketing-settings-1"><span className="wix-picker-name">getMarketingSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get marketing settings
    import { marketingSettings } from "@wix/promote-growth-tools-marketing-plan";

    async function getMarketingSettings() {
      const response = await marketingSettings.getMarketingSettings();
    }

    /* Promise resolves to:
     * {
     *   "_id": "f1c2e3a4-5b6c-47d8-9e0f-1a2b3c4d5e6f",
     *   "settings": {
     *     "toneOfVoice": "Friendly and approachable, with a focus on quality craftsmanship",
     *     "pointOfView": "FIRST_PERSON",
     *     "marketingPlanGoal": {
     *       "goalKey": "BRAND_AWARENESS"
     *     },
     *     "socialChannels": [
     *       "INSTAGRAM",
     *       "FACEBOOK"
     *     ],
     *     "frequency": {
     *       "interval": "WEEK",
     *       "count": 3
     *     },
     *     "marketingTools": [
     *       "SOCIAL_MARKETING",
     *       "EMAIL_MARKETING"
     *     ],
     *     "topics": {
     *       "included": [
     *         "sustainable fashion",
     *         "new arrivals"
     *       ],
     *       "excluded": [
     *         "politics"
     *       ],
     *       "coreTopic": "handmade apparel"
     *     },
     *     "calendar": {
     *       "ids": [
     *         "9b1d8f2a-7c3e-4a5b-8d6f-1e2c3b4a5d6e"
     *       ]
     *     },
     *     "businessProfile": {
     *       "businessGoal": "Grow online sales of our handmade apparel line",
     *       "valueProposition": "Ethically made, one-of-a-kind clothing",
     *       "targetAudience": "Eco-conscious shoppers aged 25-45"
     *     }
     *   },
     *   "createdAt": "2026-05-01T09:15:00.000Z",
     *   "updatedAt": "2026-06-10T14:32:11.000Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getMarketingSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingSettings } from '@wix/promote-growth-tools-marketing-plan';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingSettings },
      // Include the auth strategy and host as relevant
    });

    async function getMarketingSettings(options) {
      const response = await myWixClient.marketingSettings.getMarketingSettings(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
