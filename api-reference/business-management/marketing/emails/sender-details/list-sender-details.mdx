---
title: "List Sender Details"
description: "Retrieves a list of sender details."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of sender details.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/sender-details/v1/sender-details
    ```

    ## Query Params

    <ParamField query="paging" type="CursorPaging">

      Paging details.

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of items to load.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.
          You can get the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="senderDetails" type="SenderDetails[]">

      List of sender details.

      Constraints: `maxItems 100`.

      See the [Sender Details Object](/api-reference/business-management/marketing/emails/sender-details/sender-details-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl 'https://www.wixapis.com/sender-details/v1/sender-details' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "pagingMetadata": {
              "count": 2,
              "cursors": {},
              "hasNext": false
          },
          "senderDetails": [
              {
                  "id": "3dcaeca5-2b09-441c-86ab-808d85061511",
                  "createdDate": "2024-01-01T00:00:00.000Z",
                  "updatedDate": "2024-01-01T00:00:00.000Z",
                  "fromName": "John Doe",
                  "fromEmailAddress": "john.doe@example.com",
                  "default": true
              },
              {
                  "id": "a6351a69-f4cb-411c-9853-26634468b03d",
                  "createdDate": "2024-01-01T00:00:00.000Z",
                  "updatedDate": "2024-01-01T00:00:00.000Z",
                  "fromName": "Jane Doe",
                  "fromEmailAddress": "jane.doe@example.com",
                  "default": false
              },
          ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of sender details.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderDetails } from '@wix/sender-details';

    senderDetails.listSenderDetails(options): Promise<ListSenderDetailsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListSenderDetailsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="CursorPaging">

          Paging details.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to load.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.
              You can get the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListSenderDetailsResponse>`.

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="senderDetails" type="SenderDetails[]">

      List of sender details.

      Constraints: `maxItems 100`.

      See the [Sender Details Object](/api-reference/business-management/marketing/emails/sender-details/sender-details-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-sender-details" id="ex-sdk-list-sender-details-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-sender-details" id="ex-sdk-list-sender-details-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-sender-details" id="ex-sdk-list-sender-details-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-sender-details-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-sender-details-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listSenderDetails</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listSenderDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listSenderDetails (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-sender-details-0"><span className="wix-picker-name">listSenderDetails</span></label>
    <label htmlFor="ex-sdk-list-sender-details-1"><span className="wix-picker-name">listSenderDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-sender-details-2"><span className="wix-picker-name">listSenderDetails (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listSenderDetails
    import { senderDetails } from '@wix/sender-details';

    async function listSenderDetails(options) {
      const response = await senderDetails.listSenderDetails(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listSenderDetails (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderDetails } from '@wix/sender-details';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderDetails },
      // Include the auth strategy and host as relevant
    });

    async function listSenderDetails(options) {
      const response = await myWixClient.senderDetails.listSenderDetails(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listSenderDetails (with elevated permissions)
    import { senderDetails } from '@wix/sender-details';
    import { auth } from '@wix/essentials';

    async function myListSenderDetailsMethod(options) {
      const elevatedListSenderDetails = auth.elevate(senderDetails.listSenderDetails);
      const response = await elevatedListSenderDetails(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
