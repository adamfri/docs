---
title: "Query Intake Form Submissions"
description: "Retrieves a list of intake form submissions, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of intake form submissions, given the provided paging, filtering, and sorting.

    Submissions belonging to deleted intake forms aren't returned by Query Intake Form Submissions.

    Query Intake Form Submissions runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.limit` is `50`.
    - `paging.offset` is `0`.

    Learn more about [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `signer.submitter` | `$ne` | None |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INTAKE-FORM.READ">Read Intake Form</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_api/intake-forms/v1/submissions/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Filter object.
      See @API Query Language
      for more information.

      For a detailed list of supported filters, see
      @Supported Filters.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="intakeFormSubmissions" type="IntakeFormSubmission[]">

      Retrieved intake form submissions.

      See the [Intake Form Submission Object](/api-reference/crm/forms/other-services/intake-forms/intake-form-submissions/intake-form-submission-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/intake-forms/v1/intake-form-submissions/query' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "query": {
            "filter": {
              "createdDate": {"$gte": "2024-01-01T00:00:00.000Z"}
            },
            "sort": [
              {
                "fieldName": "createdDate",
                "order": "DESC"
              }
            ],
            "cursorPaging": {
              "limit": 50
            }
          }
        }'
      ```

      ```json Response
      {
        "intakeFormSubmissions": [
          {
            "id": "880e8400-e29b-41d4-a716-446655440003",
            "createdDate": "2024-01-20T14:30:00.000Z",
            "revision": "1",
            "signer": {
              "contactId": "770e8400-e29b-41d4-a716-446655440002",
              "fullName": "John Doe",
              "email": "john.doe@example.com",
              "submitter": "CUSTOMER"
            },
            "status": "VALID",
            "expirationDate": "2025-01-20T14:30:00.000Z",
            "updatedDate": "2024-01-20T14:30:00.000Z"
          },
          {
            "id": "990e8400-e29b-41d4-a716-446655440004",
            "createdDate": "2024-01-18T10:15:00.000Z",
            "revision": "1",
            "signer": {
              "contactId": "770e8400-e29b-41d4-a716-446655440005",
              "fullName": "Jane Smith",
              "email": "jane.smith@example.com",
              "submitter": "CUSTOMER"
            },
            "status": "VALID",
            "expirationDate": "2025-01-18T10:15:00.000Z",
            "updatedDate": "2024-01-18T10:15:00.000Z"
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of intake form submissions, given the provided paging, filtering, and sorting.

    Submissions belonging to deleted intake forms aren't returned by Query Intake Form Submissions.

    Query Intake Form Submissions runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.limit` is `50`.
    - `paging.offset` is `0`.

    Learn more about [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `signer.submitter` | `$ne` | None |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INTAKE-FORM.READ">Read Intake Form</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { intakeFormSubmissions } from '@wix/intake-forms';

    intakeFormSubmissions.queryIntakeFormSubmissions(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="IntakeFormSubmissionQuery" required>

      Filter object.
      See @API Query Language
      for more information.

      For a detailed list of supported filters, see
      @Supported Filters.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="intakeFormSubmissions" type="IntakeFormSubmission[]">

      Retrieved intake form submissions.

      See the [Intake Form Submission Object](/api-reference/crm/forms/other-services/intake-forms/intake-form-submissions/intake-form-submission-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-intake-form-submissions" id="ex-sdk-query-intake-form-submissions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-intake-form-submissions" id="ex-sdk-query-intake-form-submissions-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-intake-form-submissions" id="ex-sdk-query-intake-form-submissions-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-intake-form-submissions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-intake-form-submissions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryIntakeFormSubmissions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryIntakeFormSubmissions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryIntakeFormSubmissions (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-intake-form-submissions-0"><span className="wix-picker-name">queryIntakeFormSubmissions</span></label>
    <label htmlFor="ex-sdk-query-intake-form-submissions-1"><span className="wix-picker-name">queryIntakeFormSubmissions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-intake-form-submissions-2"><span className="wix-picker-name">queryIntakeFormSubmissions (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryIntakeFormSubmissions
    import { intakeFormSubmissions } from '@wix/intake-forms';

    async function queryIntakeFormSubmissions(query) {
      const response = await intakeFormSubmissions.queryIntakeFormSubmissions(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryIntakeFormSubmissions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { intakeFormSubmissions } from '@wix/intake-forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { intakeFormSubmissions },
      // Include the auth strategy and host as relevant
    });

    async function queryIntakeFormSubmissions(query) {
      const response = await myWixClient.intakeFormSubmissions.queryIntakeFormSubmissions(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryIntakeFormSubmissions (with elevated permissions)
    import { intakeFormSubmissions } from '@wix/intake-forms';
    import { auth } from '@wix/essentials';

    async function myQueryIntakeFormSubmissionsMethod(query) {
      const elevatedQueryIntakeFormSubmissions = auth.elevate(intakeFormSubmissions.queryIntakeFormSubmissions);
      const response = await elevatedQueryIntakeFormSubmissions(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
