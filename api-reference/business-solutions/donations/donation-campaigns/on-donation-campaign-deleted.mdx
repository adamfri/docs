---
title: "onDonationCampaignDeleted"
description: "Triggered when a donation campaign is deleted."
icon: "bolt"
---

Triggered when a donation campaign is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.donations.v2.donation_campaign_deleted` |
| Package | `@wix/donations` |
| Permissions | `Manage Donation Campaigns` |

## Examples

<CodeGroup>

  ```javascript onDonationCampaignDeleted
  import { donationCampaigns } from '@wix/donations';

  donationCampaigns.onDonationCampaignDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onDonationCampaignDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { donationCampaigns } from '@wix/donations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        donationCampaigns,
      },
  });

  wixClient.donationCampaigns.onDonationCampaignDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
