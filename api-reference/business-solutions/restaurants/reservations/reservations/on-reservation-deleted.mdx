---
title: "onReservationDeleted"
description: "Triggered when a reservation is deleted."
icon: "bolt"
---

Triggered when a reservation is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.table_reservations.v1.reservation_deleted` |
| Package | `@wix/table-reservations` |
| Permissions | `Manage Reservations (Medium)`, `Manage Reservations (Full)`, `Manage Restaurants - all permissions`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onReservationDeleted
  import { reservations } from '@wix/table-reservations';

  reservations.onReservationDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onReservationDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reservations } from '@wix/table-reservations';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reservations,
      },
  });

  wixClient.reservations.onReservationDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
