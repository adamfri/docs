---
title: "onReferringCustomerCreated"
description: "Triggered when a referring customer is created."
icon: "bolt"
---

Triggered when a referring customer is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.referral.v1.referring_customer_created` |
| Package | `@wix/referral` |
| Permissions | `Manage Referrals` |

## Examples

<CodeGroup>

  ```javascript onReferringCustomerCreated
  import { customers } from '@wix/referral';

  customers.onReferringCustomerCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onReferringCustomerCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { customers } from '@wix/referral';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        customers,
      },
  });

  wixClient.customers.onReferringCustomerCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
