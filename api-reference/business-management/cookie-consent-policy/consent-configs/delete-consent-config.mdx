---
title: "Delete Consent Config"
description: "Permanently deletes a consent config."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a consent config.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/bu/legal/v1/consent-configs/{consentConfigId}
    ```

    ## Path Params

    <ParamField path="consentConfigId" type="string" required>

      Consent config ID to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteConsentConfigResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Deleted | `wix.bu.legal.v1.consent_config_deleted` | Triggered when a consent config is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Delete a consent config

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/consent/consent-config/v1/consent-configs/334afbf6-5c8b-4b43-9e9e-dd28664ca28f' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a consent config.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { consentConfig } from '@wix/consent-policy';

    consentConfig.deleteConsentConfig(consentConfigId): Promise
    ```

    ## Method Parameters

    <ParamField body="consentConfigId" type="string" required>

      Consent config ID to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteConsentConfigResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Deleted | `wix.bu.legal.v1.consent_config_deleted` | Triggered when a consent config is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-consent-config" id="ex-sdk-delete-consent-config-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-consent-config" id="ex-sdk-delete-consent-config-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-consent-config" id="ex-sdk-delete-consent-config-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-consent-config-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-consent-config-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteConsentConfig</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteConsentConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteConsentConfig (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-consent-config-0"><span className="wix-picker-name">deleteConsentConfig</span></label>
    <label htmlFor="ex-sdk-delete-consent-config-1"><span className="wix-picker-name">deleteConsentConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-consent-config-2"><span className="wix-picker-name">deleteConsentConfig (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteConsentConfig
    import { consentConfig } from '@wix/consent-policy';

    async function deleteConsentConfig(consentConfigId) {
      const response = await consentConfig.deleteConsentConfig(consentConfigId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteConsentConfig (self-hosted)
    import { createClient } from '@wix/sdk';
    import { consentConfig } from '@wix/consent-policy';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { consentConfig },
      // Include the auth strategy and host as relevant
    });

    async function deleteConsentConfig(consentConfigId) {
      const response = await myWixClient.consentConfig.deleteConsentConfig(consentConfigId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteConsentConfig (with elevated permissions)
    import { consentConfig } from '@wix/consent-policy';
    import { auth } from '@wix/essentials';

    async function myDeleteConsentConfigMethod(consentConfigId) {
      const elevatedDeleteConsentConfig = auth.elevate(consentConfig.deleteConsentConfig);
      const response = await elevatedDeleteConsentConfig(consentConfigId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
