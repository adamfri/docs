---
title: "Add Bookings To Multi Service Booking"
description: "Expands an existing multi-service booking by adding existing single-service bookings to the package."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Expands an existing multi-service booking by adding existing single-service bookings to the package.

    ### When to call this method

    Call this method to add 1 or more existing single-service bookings to an existing multi-service booking.
    For creating a new multi-service booking with new single-service bookings, call [Create Multi Service Booking](https://dev.wix.com/docs/rest/business-solutions/bookings/bookings/bookings-writer-v2/create-multi-service-booking) instead.

    ### Package integration

    The timing of the single-service bookings to add must be compatible with the multi-service booking's sequential schedule to ensure no gaps or overlaps between services in the updated package.

    ### Requirements and limitations

    - __Maximum capacity__: The total number of single-service bookings can't exceed 8.
    - __Booking eligibility__: You can add only independent single-service bookings that aren't part of another multi-service booking.
    - __Status compatibility__: Added bookings must have compatible status with the target multi-service booking.
    - __Revision control__: You must provide current revision numbers for all single-service bookings to add.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/multiServiceBookings/v2/multi_service_bookings/add_bookings_to_multi_service_booking
    ```

    ## Body Params

    <ParamField body="multiServiceBookingId" type="string" required>

      ID of the multi-service booking.

    </ParamField>

    <ParamField body="bookings" type="BookingIdAndRevision[]" required>

      List of single-service booking IDs and their revision.

      Constraints: `maxItems 8`.

      <Expandable title="bookings properties">

        <ParamField body="bookingId" type="string" required>

          ID of the single-service booking.

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the booking is updated.
          To prevent conflicting changes, the current revision must be specified when managing the booking.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnFullEntity" type="boolean">

      Whether to return the single-service bookings that were added to the multi-service booking.

      Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="bookings" type="BookingResult[]">

      Single-service bookings that were added to the multi-service booking.

      <Expandable title="bookings properties">

        <ResponseField name="bookingId" type="string">

          Single-service booking ID. Always present.
          This value is identical to `booking.id`, which is only returned when `returnFullEntity` is `true`.

        </ResponseField>

        <ResponseField name="booking" type="Booking">

          Single-service booking entity. Only returned when `returnFullEntity` is `true`.

          See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `MULTI_SERVICE_BOOKING_NOT_FOUND` |  |
    | `404` | `NOT_FOUND` | `BOOKING_NOT_FOUND` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_REVISION_MISMATCH` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_ALREADY_PART_OF_ANOTHER_MULTI_SERVICE_BOOKING` |  |
    | `428` | `FAILED_PRECONDITION` | `MULTI_SERVICE_BOOKING_ALREADY_INCLUDES_BOOKING` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_TO_ADD_STATUS_NOT_VALID` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/_api/bookings-service/v2/multi_service_bookings/add_bookings_to_multi_service_booking' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "multiServiceBookingId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "bookings": [
            {
              "bookingId": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
              "revision": "1"
            },
            {
              "bookingId": "c3d4e5f6-a7b8-9012-cdef-123456789012",
              "revision": "1"
            }
          ],
          "returnFullEntity": true
        }'
      ```

      ```json Response
      {
        "bookings": [
          {
            "bookingId": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
            "booking": {
              "id": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
              "status": "CREATED",
              "paymentStatus": "NOT_PAID",
              "bookedEntity": {
                "slot": {
                  "scheduleId": "d4e5f6a7-b8c9-0123-defa-234567890123",
                  "startDate": "2024-02-15T15:45:00.000Z",
                  "endDate": "2024-02-15T16:30:00.000Z"
                }
              },
              "contactDetails": {
                "firstName": "Jane",
                "lastName": "Smith",
                "email": "jane.smith@example.com",
                "phone": "+1-555-123-4567"
              },
              "additionalFields": [
                {
                  "id": "e5f6a7b8-c9d0-1234-efab-345678901234",
                  "value": "Classic Red"
                }
              ]
            }
          },
          {
            "bookingId": "c3d4e5f6-a7b8-9012-cdef-123456789012",
            "booking": {
              "id": "c3d4e5f6-a7b8-9012-cdef-123456789012",
              "status": "CREATED",
              "paymentStatus": "NOT_PAID",
              "bookedEntity": {
                "slot": {
                  "scheduleId": "f6a7b8c9-d0e1-2345-fabc-456789012345",
                  "startDate": "2024-02-15T16:30:00.000Z",
                  "endDate": "2024-02-15T17:15:00.000Z"
                }
              },
              "contactDetails": {
                "firstName": "Jane",
                "lastName": "Smith",
                "email": "jane.smith@example.com",
                "phone": "+1-555-123-4567"
              },
              "additionalFields": [
                {
                  "id": "a7b8c9d0-e1f2-3456-abcd-567890123456",
                  "value": "Classic Red"
                }
              ]
            }
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Expands an existing multi-service booking by adding existing single-service bookings to the package.

    ### When to call this method

    Call this method to add 1 or more existing single-service bookings to an existing multi-service booking.
    For creating a new multi-service booking with new single-service bookings, call [Create Multi Service Booking](https://dev.wix.com/docs/rest/business-solutions/bookings/bookings/bookings-writer-v2/create-multi-service-booking) instead.

    ### Package integration

    The timing of the single-service bookings to add must be compatible with the multi-service booking's sequential schedule to ensure no gaps or overlaps between services in the updated package.

    ### Requirements and limitations

    - __Maximum capacity__: The total number of single-service bookings can't exceed 8.
    - __Booking eligibility__: You can add only independent single-service bookings that aren't part of another multi-service booking.
    - __Status compatibility__: Added bookings must have compatible status with the target multi-service booking.
    - __Revision control__: You must provide current revision numbers for all single-service bookings to add.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { bookings } from '@wix/bookings';

    bookings.addBookingsToMultiServiceBooking(multiServiceBookingId, options): Promise<AddBookingsToMultiServiceBookingResponse>
    ```

    ## Method Parameters

    <ParamField body="multiServiceBookingId" type="string" required>

      ID of the multi-service booking.

    </ParamField>

    <ParamField body="options" type="AddBookingsToMultiServiceBookingOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="bookings" type="BookingIdAndRevision[]" required>

          List of single-service booking IDs and their revision.

          Constraints: `maxItems 8`.

          <Expandable title="bookings properties">

            <ParamField body="bookingId" type="string" required>

              ID of the single-service booking.

            </ParamField>

            <ParamField body="revision" type="integer" required>

              Revision number, which increments by 1 each time the booking is updated.
              To prevent conflicting changes, the current revision must be specified when managing the booking.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="returnFullEntity" type="boolean">

          Whether to return the single-service bookings that were added to the multi-service booking.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<AddBookingsToMultiServiceBookingResponse>`.

    <ResponseField name="bookings" type="BookingResult[]">

      Single-service bookings that were added to the multi-service booking.

      <Expandable title="bookings properties">

        <ResponseField name="bookingId" type="string">

          Single-service booking ID. Always present.
          This value is identical to `booking.id`, which is only returned when `returnFullEntity` is `true`.

        </ResponseField>

        <ResponseField name="booking" type="Booking">

          Single-service booking entity. Only returned when `returnFullEntity` is `true`.

          See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `MULTI_SERVICE_BOOKING_NOT_FOUND` |  |
    | `404` | `NOT_FOUND` | `BOOKING_NOT_FOUND` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_REVISION_MISMATCH` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_ALREADY_PART_OF_ANOTHER_MULTI_SERVICE_BOOKING` |  |
    | `428` | `FAILED_PRECONDITION` | `MULTI_SERVICE_BOOKING_ALREADY_INCLUDES_BOOKING` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_TO_ADD_STATUS_NOT_VALID` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-add-bookings-to-multi-service-booking" id="ex-sdk-add-bookings-to-multi-service-booking-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-add-bookings-to-multi-service-booking" id="ex-sdk-add-bookings-to-multi-service-booking-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-add-bookings-to-multi-service-booking" id="ex-sdk-add-bookings-to-multi-service-booking-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-add-bookings-to-multi-service-booking-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-add-bookings-to-multi-service-booking-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">addBookingsToMultiServiceBooking</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">addBookingsToMultiServiceBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">addBookingsToMultiServiceBooking (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-add-bookings-to-multi-service-booking-0"><span className="wix-picker-name">addBookingsToMultiServiceBooking</span></label>
    <label htmlFor="ex-sdk-add-bookings-to-multi-service-booking-1"><span className="wix-picker-name">addBookingsToMultiServiceBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-add-bookings-to-multi-service-booking-2"><span className="wix-picker-name">addBookingsToMultiServiceBooking (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript addBookingsToMultiServiceBooking
    import { bookings } from '@wix/bookings';

    async function addBookingsToMultiServiceBooking(multiServiceBookingId,options) {
      const response = await bookings.addBookingsToMultiServiceBooking(multiServiceBookingId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript addBookingsToMultiServiceBooking (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bookings } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bookings },
      // Include the auth strategy and host as relevant
    });

    async function addBookingsToMultiServiceBooking(multiServiceBookingId,options) {
      const response = await myWixClient.bookings.addBookingsToMultiServiceBooking(multiServiceBookingId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript addBookingsToMultiServiceBooking (with elevated permissions)
    import { bookings } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myAddBookingsToMultiServiceBookingMethod(multiServiceBookingId,options) {
      const elevatedAddBookingsToMultiServiceBooking = auth.elevate(bookings.addBookingsToMultiServiceBooking);
      const response = await elevatedAddBookingsToMultiServiceBooking(multiServiceBookingId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
