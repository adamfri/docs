---
title: "Create Badge Assignment"
description: "Assigns a badge to a member."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Assigns a badge to a member.

    To assign multiple badges to multiple members, call [Bulk Create Badge Assignments](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/bulk-create-badge-assignments).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-BADGES">Manage Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/badges/v4/assignments
    ```

    ## Body Params

    <ParamField body="badgeAssignment" type="BadgeAssignment" required>

      Badge assignment to create.

      See the [Badge Assignment Object](/api-reference/crm/members-contacts/members/activity/badge-assignments/badge-assignment-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="badgeAssignment" type="BadgeAssignment">

      Created badge assignment.

      See the [Badge Assignment Object](/api-reference/crm/members-contacts/members/activity/badge-assignments/badge-assignment-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Badge Assignment Created | `wix.badges.v4.badge_assignment_created` | Triggered when a badge assignment is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        https://www.wixapis.com/badges/v4/assignments \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
                "badgeAssignment": {
                  "badgeId": "d3405ceb-5640-46e3-8163-5f110bf1db3a",
                  "memberId": "e7c0a2f8-4b9d-4b8a-9f3c-1e2d3f4a5b6c"
                }
            }'
      ```

      ```json Response
      {
         "badgeAssignment": {
             "id": "3ed982d6-2f45-41b8-a6f8-fdae05c3ea43",
             "badgeId": "d3405ceb-5640-46e3-8163-5f110bf1db3a",
             "memberId": "e7c0a2f8-4b9d-4b8a-9f3c-1e2d3f4a5b6c",
             "createdDate": "2024-11-26T09:59:09.937Z",
             "updatedDate": "2024-11-26T09:59:09.937Z"
         }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Assigns a badge to a member.

    To assign multiple badges to multiple members, call [Bulk Create Badge Assignments](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/bulk-create-badge-assignments).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-BADGES">Manage Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { badgeAssignments } from '@wix/members';

    badgeAssignments.createBadgeAssignment(badgeAssignment): Promise<BadgeAssignment>
    ```

    ## Method Parameters

    <ParamField body="badgeAssignment" type="BadgeAssignment" required>

      Badge assignment to create.

      See the [Badge Assignment Object](/api-reference/crm/members-contacts/members/activity/badge-assignments/badge-assignment-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<BadgeAssignment>`.

    See the [Badge Assignment Object](/api-reference/crm/members-contacts/members/activity/badge-assignments/badge-assignment-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Badge Assignment Created | `wix.badges.v4.badge_assignment_created` | Triggered when a badge assignment is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-badge-assignment" id="ex-sdk-create-badge-assignment-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-badge-assignment" id="ex-sdk-create-badge-assignment-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-badge-assignment" id="ex-sdk-create-badge-assignment-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-badge-assignment-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-badge-assignment-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createBadgeAssignment</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createBadgeAssignment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createBadgeAssignment (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-badge-assignment-0"><span className="wix-picker-name">createBadgeAssignment</span></label>
    <label htmlFor="ex-sdk-create-badge-assignment-1"><span className="wix-picker-name">createBadgeAssignment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-badge-assignment-2"><span className="wix-picker-name">createBadgeAssignment (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createBadgeAssignment
    import { badgeAssignments } from '@wix/members';

    async function createBadgeAssignment(badgeAssignment) {
      const response = await badgeAssignments.createBadgeAssignment(badgeAssignment);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createBadgeAssignment (self-hosted)
    import { createClient } from '@wix/sdk';
    import { badgeAssignments } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { badgeAssignments },
      // Include the auth strategy and host as relevant
    });

    async function createBadgeAssignment(badgeAssignment) {
      const response = await myWixClient.badgeAssignments.createBadgeAssignment(badgeAssignment);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createBadgeAssignment (with elevated permissions)
    import { badgeAssignments } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myCreateBadgeAssignmentMethod(badgeAssignment) {
      const elevatedCreateBadgeAssignment = auth.elevate(badgeAssignments.createBadgeAssignment);
      const response = await elevatedCreateBadgeAssignment(badgeAssignment);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
