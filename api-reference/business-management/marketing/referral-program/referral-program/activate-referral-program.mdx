---
title: "Activate Referral Program"
description: "Activates the referral program, changing its status to ACTIVE."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Activates the referral program, changing its status to `ACTIVE`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/_api/referral-programs/v1/program/activate
    ```

    ## Response Object

    <ResponseField name="referralProgram" type="ReferralProgram">

      Activated referral program.

      See the [Program Object](/api-reference/business-management/marketing/referral-program/referral-program/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Updated | `wix.loyalty.referral.v1.program_updated` | Triggered when a referral program is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/_api/referral-programs/v1/program/activate' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
          "referralProgram": {
              "name": "Share & Earn",
              "status": "ACTIVE",
              "revision": "3",
              "createdDate": "2024-01-01T10:49:25.803Z",
              "updatedDate": "2024-01-02T10:49:25.803Z",
              "referredFriendReward": {
                  "type": "LOYALTY_POINTS",
                  "loyaltyPointsOptions": {
                      "amount": 1
                  }
              },
              "referringCustomerReward": {
                  "type": "LOYALTY_POINTS",
                  "loyaltyPointsOptions": {
                      "amount": 1
                  }
              },
              "successfulReferralActions": [
                  "STORE_ORDER_PLACED"
              ],
              "emails": {
                  "encourageToReferFriends": [
                      "STORES"
                  ],
                  "notifyCustomersAboutReward": true
              },
              "premiumFeatures": {
                  "referralProgram": true
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Activates the referral program, changing its status to `ACTIVE`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/referral';

    programs.activateReferralProgram(): Promise<ActivateReferralProgramResponse>
    ```

    ## Returns

    Returns `Promise<ActivateReferralProgramResponse>`.

    <ResponseField name="referralProgram" type="ReferralProgram">

      Activated referral program.

      See the [Program Object](/api-reference/business-management/marketing/referral-program/referral-program/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Updated | `wix.loyalty.referral.v1.program_updated` | Triggered when a referral program is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-activate-referral-program" id="ex-sdk-activate-referral-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-activate-referral-program" id="ex-sdk-activate-referral-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-activate-referral-program" id="ex-sdk-activate-referral-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-activate-referral-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-activate-referral-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">activateReferralProgram</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">activateReferralProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">activateReferralProgram (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-activate-referral-program-0"><span className="wix-picker-name">activateReferralProgram</span></label>
    <label htmlFor="ex-sdk-activate-referral-program-1"><span className="wix-picker-name">activateReferralProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-activate-referral-program-2"><span className="wix-picker-name">activateReferralProgram (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript activateReferralProgram
    import { programs } from '@wix/referral';

    async function activateReferralProgram() {
      const response = await programs.activateReferralProgram();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript activateReferralProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/referral';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function activateReferralProgram() {
      const response = await myWixClient.programs.activateReferralProgram();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript activateReferralProgram (with elevated permissions)
    import { programs } from '@wix/referral';
    import { auth } from '@wix/essentials';

    async function myActivateReferralProgramMethod() {
      const elevatedActivateReferralProgram = auth.elevate(programs.activateReferralProgram);
      const response = await elevatedActivateReferralProgram();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
