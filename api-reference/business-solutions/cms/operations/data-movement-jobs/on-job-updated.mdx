---
title: "onJobUpdated"
description: "Triggered when a job's status updates."
icon: "bolt"
---

Triggered when a job's `status` updates.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.hub.v1.job_updated` |
| Package | `@wix/data` |
| Permissions | `Data Mover Manage Jobs` |

## Examples

<CodeGroup>

  ```javascript onJobUpdated
  import { movementJobs } from '@wix/data';

  movementJobs.onJobUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onJobUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { movementJobs } from '@wix/data';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        movementJobs,
      },
  });

  wixClient.movementJobs.onJobUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
