---
title: "Delete Item"
description: "Deletes an item."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes an item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/social-publisher/v1/items/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required />

    ## Response Object

    Returns `DeleteItemResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `ITEM_IS_DELETED` | The item was already deleted. |
    | `404` | `NOT_FOUND` | `ITEM_NOT_EXISTS` | Couldn't find the item. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Delete an item by its ID.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/social-publisher/v1/items/834dc801-3e5c-416a-bd14-b130a95d100e' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes an item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS">Manage Social Posts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/promote-growth-tools-publisher';

    items.deleteItem(_id): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteItemResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `ITEM_IS_DELETED` | The item was already deleted. |
    | `404` | `NOT_FOUND` | `ITEM_NOT_EXISTS` | Couldn't find the item. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Deleted | `wix.promote.publisher.v1.item_deleted` | Triggered when an item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-item" id="ex-sdk-delete-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-item" id="ex-sdk-delete-item-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete an item</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-item-0"><span className="wix-picker-name">Delete an item</span></label>
    <label htmlFor="ex-sdk-delete-item-1"><span className="wix-picker-name">deleteItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete an item
    import { items } from "@wix/promote-growth-tools-publisher";

    const itemId = "834dc801-3e5c-416a-bd14-b130a95d100e";

    async function deleteItem() {
      await items.deleteItem(itemId);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/promote-growth-tools-publisher';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function deleteItem(_id) {
      const response = await myWixClient.items.deleteItem(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
