---
title: "onBadgeUpdated"
description: "Triggered when a badge is updated."
icon: "bolt"
---

Triggered when a badge is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v4.badge_updated` |
| Package | `@wix/members` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Manage Stores`, `Manage Events`, `Manage Online Programs`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeUpdated
  import { badgesV2 } from '@wix/members';

  badgesV2.onBadgeUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badgesV2 } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        badgesV2,
      },
  });

  wixClient.badgesV2.onBadgeUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
