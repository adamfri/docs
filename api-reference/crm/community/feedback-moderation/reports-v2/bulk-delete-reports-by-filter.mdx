---
title: "Bulk Delete Reports By Filter"
description: "Deletes multiple reports by filter, and removes them from the report list in the dashboard."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple reports by filter, and removes them from the report list in the dashboard.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.MANAGE-REPORTS">Manage Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/reports/v2/reports/bulk/delete-by-filter
    ```

    ## Body Params

    <ParamField body="filter" type="object" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID. Pass this ID to [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/introduction) to retrieve job details and metadata.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Report Deleted | `wix.reports.v2.report_deleted` | Triggered when a report is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \ https://www.wixapis.com/reports/v2/reports/bulk/delete-by-filter \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "filter": {
                "entityId": {
                  "$eq": "50353fbc-b265-4f03-888f-a53aa272758d"
                }
            }
        }'
      ```

      ```json Response
      {
        "jobId": "4257085b-527c-4d0c-b011-fd5f86618599"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple reports by filter, and removes them from the report list in the dashboard.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.MANAGE-REPORTS">Manage Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { reports } from '@wix/reports';

    reports.bulkDeleteReportsByFilter(filter): Promise<BulkDeleteReportsByFilterResponse>
    ```

    ## Method Parameters

    <ParamField body="filter" type="object" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

    </ParamField>

    ## Returns

    Returns `Promise<BulkDeleteReportsByFilterResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID. Pass this ID to [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/introduction) to retrieve job details and metadata.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Report Deleted | `wix.reports.v2.report_deleted` | Triggered when a report is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-reports-by-filter" id="ex-sdk-bulk-delete-reports-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-reports-by-filter" id="ex-sdk-bulk-delete-reports-by-filter-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-reports-by-filter" id="ex-sdk-bulk-delete-reports-by-filter-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-reports-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-reports-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteReportsByFilter</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteReportsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteReportsByFilter (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-reports-by-filter-0"><span className="wix-picker-name">bulkDeleteReportsByFilter</span></label>
    <label htmlFor="ex-sdk-bulk-delete-reports-by-filter-1"><span className="wix-picker-name">bulkDeleteReportsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-reports-by-filter-2"><span className="wix-picker-name">bulkDeleteReportsByFilter (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteReportsByFilter
    import { reports } from '@wix/reports';

    async function bulkDeleteReportsByFilter(filter) {
      const response = await reports.bulkDeleteReportsByFilter(filter);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteReportsByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reports } from '@wix/reports';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reports },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteReportsByFilter(filter) {
      const response = await myWixClient.reports.bulkDeleteReportsByFilter(filter);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteReportsByFilter (with elevated permissions)
    import { reports } from '@wix/reports';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteReportsByFilterMethod(filter) {
      const elevatedBulkDeleteReportsByFilter = auth.elevate(reports.bulkDeleteReportsByFilter);
      const response = await elevatedBulkDeleteReportsByFilter(filter);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
