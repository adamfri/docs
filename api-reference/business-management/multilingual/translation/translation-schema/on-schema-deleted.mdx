---
title: "onSchemaDeleted"
description: "Triggered when translation schema is deleted."
icon: "bolt"
---

Triggered when translation schema is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.translation.v1.schema_deleted` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation Schema Read` |

## Examples

<CodeGroup>

  ```javascript onSchemaDeleted
  import { translationSchemas } from '@wix/multilingual';

  translationSchemas.onSchemaDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onSchemaDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { translationSchemas } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        translationSchemas,
      },
  });

  wixClient.translationSchemas.onSchemaDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
