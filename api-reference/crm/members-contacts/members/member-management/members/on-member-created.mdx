---
title: "onMemberCreated"
description: "Triggered when a member is created."
icon: "bolt"
---

Triggered when a member is created.

The site owner can configure the site to automatically approve members or require manual approval.

A member who has been approved either automatically or manually has a `status` of `APPROVED`. A created member waiting for approval has a `status` of `PENDING`. A `PENDING` member cannot log into the site.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.members.v1.member_created` |
| Package | `@wix/members` |
| Permissions | `Manage Members`, `Manage Bookings Services and Settings`, `Manage Events`, `Manage Portfolio`, `Access Verticals by Automations`, `Read Members and Contacts - all read permissions`, `Manage Restaurants - all permissions`, `Read Members`, `Set Up Automations`, `Manage Members and Contacts - all permissions` |

## Examples

<CodeGroup>

  ```javascript onMemberCreated: Wix-hosted
  import { members } from '@wix/members';

  members.onMemberCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onMemberCreated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { members } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        members,
      },
  });

  wixClient.members.onMemberCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
