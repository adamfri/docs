---
title: "onPostDeleted"
description: "An existing post was removed."
icon: "bolt"
---

An existing post was removed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_deleted` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostDeleted
  import { posts } from '@wix/forum';

  posts.onPostDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
