---
title: "Reschedule Multi Service Booking"
description: "Reschedules a multi-service booking by changing the timing for all or specific single-service bookings in the package."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Reschedules a multi-service booking by changing the timing for all or specific single-service bookings in the package.

    This method reschedules single-service bookings within the multi-service booking while maintaining sequential order. You must specify exact new timing for each service to ensure they remain back-to-back with no gaps or overlaps.

    This method fails if it can't reschedule at least 1 single-service booking. You must provide the current revision number for each single-service booking you're rescheduling to prevent conflicting changes.

    See [Reschedule Booking](https://dev.wix.com/docs/rest/business-solutions/bookings/bookings/bookings-writer-v2/reschedule-booking) for single-service booking rescheduling details.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_api/bookings-service/v2/multi_service_bookings/{multiServiceBookingId}/reschedule
    ```

    ## Path Params

    <ParamField path="multiServiceBookingId" type="string" required>

      ID of the multi-service booking to reschedule.

    </ParamField>

    ## Body Params

    <ParamField body="rescheduleBookingsInfo" type="RescheduleBookingInfo[]" required>

      Information about the single-service bookings to reschedule.

      <Expandable title="rescheduleBookingsInfo properties">

        <ParamField body="bookingId" type="string" required>

          ID of the booking to reschedule.

        </ParamField>

        <ParamField body="slot" type="Slot" required>

          Information about the new slot.

          <Expandable title="slot properties">

            <ParamField body="sessionId" type="string">

              Identifier for the underlying session when the session is a single session or generated from a recurring session.

            </ParamField>

            <ParamField body="serviceId" type="string">

              Service identifier. Required.

            </ParamField>

            <ParamField body="scheduleId" type="string">

              Schedule identifier. Required.

            </ParamField>

            <ParamField body="startDate" type="string">

              The start time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

            </ParamField>

            <ParamField body="endDate" type="string">

              The end time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

            </ParamField>

            <ParamField body="timezone" type="string">

              The timezone according to which the slot is calculated and presented.

            </ParamField>

            <ParamField body="resource" type="SlotResource">

              The resource required for this slot.
              When populated, the specified resource will be assigned to the slot upon confirmation according to its availability.
              When empty, if `skip_availability_validation` is `false`, a random available resource will be assigned to the slot upon confirmation.
              Otherwise, one of the service resources will be assigned to the slot randomly upon confirmation.

              _Nested structure omitted for brevity — see the `SlotResource` object._

            </ParamField>

            <ParamField body="location" type="Location">

              Geographic location of the slot.

              _Nested structure omitted for brevity — see the `Location` object._

            </ParamField>

            <ParamField body="eventId" type="string">

              Calendar event ID - not supported.
              If not empty, on all write flows (create/update), it takes priority over `sessionId`.
              So if both `sessionId` and `eventId` are provided, the `sessionId` will be based on the `eventId`.
              Otherwise, if `eventId` is empty on write flow,

            </ParamField>

            <ParamField body="resourceSelections" type="ResourceSelection[]">

              Information about how the customer has selected resources for the booking.
              Each resource type may have a different selection method.
              Check `resource` for resource details.

              Constraints: `maxItems 3`.

              _Nested structure omitted for brevity — see the `ResourceSelection[]` object._

            </ParamField>

            <ParamField body="allDay" type="boolean">

              Whether the slot spans one or more full days.
              For class bookings, this value is derived from the linked calendar event.
              For appointment bookings, this value is derived from the availability slot. Any caller-provided value is ignored.

              When `true`, `startDate` is set to `00:00:00` of the first day and `endDate` is set to `00:00:00` of the day after the last day (exclusive). Midnight boundaries are interpreted in the slot's `timezone`.

              Default: `false`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the booking is updated.
          To prevent conflicting changes, the current revision must be specified when managing the booking.

        </ParamField>

        <ParamField body="bookedAddOns" type="BookedAddOn[]">

          List of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) added to the booking.

          To replace the booking's the list of booked add-ons, all of these conditions must be met:
          - The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
          - You must have `BOOKINGS.MANAGE_ADDONS` permissions.
          - For bookings paid via an [eCommerce checkout](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/checkout/introduction): The related [eCommerce order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/introduction) must be in a draftable state to correctly adjust the price when modifying add-ons. You can call [Get Order Draftability Status](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/get-order-draftability-status), specifying the order ID to check if it's draftable. If the order isn't draftable, Reschedule Booking still succeeds, but the order's `priceSummary` isn't updated.

          Constraints: `maxItems 21`.

          <Expandable title="bookedAddOns properties">

            <ParamField body="id" type="string">

              The ID of the add-on.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="groupId" type="string">

              The ID of the add-on group.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="durationInMinutes" type="integer">

              The add-on duration in minutes at the time of booking.
              Populated for duration-based add-ons.

              **Read-only.**

            </ParamField>

            <ParamField body="quantity" type="integer">

              The quantity of booked add-ons.
              For duration-based add-ons, `quantity` is not applicable.
              If `quantity` is provided as `1` for a duration-based add-on, it will be accepted but the value will be cleared.
              If any other value is provided, an `INVALID_ARGUMENT` error will be returned with the message: "Invalid AddOn details: either duration or quantity must be set correctly".

            </ParamField>

            <ParamField body="name" type="string">

              Add-on `name` at the time of booking.

              **Read-only.**

            </ParamField>

            <ParamField body="nameTranslated" type="string">

              Add-on name translated to the language the customer used during booking.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="participantNotification" type="ParticipantNotification">

      Information about whether to notify the customer and the message to send.

      <Expandable title="participantNotification properties">

        <ParamField body="notifyParticipants" type="boolean">

          Whether to send a message about the changes to the customer.

          Default: `false`

        </ParamField>

        <ParamField body="message" type="string">

          Custom message to send to the participants about the changes to the booking.

        </ParamField>

        <ParamField body="metadata" type="object">

          Information about the delivery channels used to send the notification.
          For example, `{"channels": "SMS" }`, `{"channels": "EMAIL" }`, or `{"channels": "EMAIL, SMS" }`.

          Constraints: `format map`.

          <Expandable title="metadata properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="string" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="flowControlSettings" type="RescheduleBookingFlowControlSettings">

      Information about whether specific procedures of the standard Wix Bookings rescheduling flow are changed.
      For example, whether the availability of the new slot is checked before rescheduling the booking or if the customer can reschedule the booking even though the service's rescheduling policy doesn't allow it.

      <Expandable title="flowControlSettings properties">

        <ParamField body="ignoreReschedulePolicy" type="boolean">

          Whether the rescheduling policy applies when rescheduling the booking.

          When passing `false`, you can only reschedule a booking if the rescheduling
          policy allows it.
          When passing `true`, the rescheduling policy is ignored and external
          validation providers are also skipped.
          Default: `false`.

        </ParamField>

        <ParamField body="skipAvailabilityValidation" type="boolean">

          Whether the availability is checked before rescheduling the booking.

          When passing `false`, a booking is only created when the slot or
          schedule is available.
          Default: `false`.

        </ParamField>

        <ParamField body="skipBusinessConfirmation" type="boolean">

          Whether the rescheduled booking's status is automatically set to
          `CONFIRMED` for services that normally require the owner's manual
          confirmation.
          Default: `false`.

        </ParamField>

        <ParamField body="allowAddOnChanges" type="boolean">

          Whether you can update the list of add-ons associated with the booking when rescheduling.
          The calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) must have `BOOKINGS.MANAGE_ADDONS` permissions to specify `true`.

          Default: `false`.

          **Developer Preview.**

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnFullEntity" type="boolean">

      Whether to return the rescheduled bookings entities.

    </ParamField>

    ## Response Object

    <ResponseField name="multiServiceBooking" type="MultiServiceBooking">

      Rescheduled multi-service booking.

      <Expandable title="multiServiceBooking properties">

        <ResponseField name="id" type="string">

          Multi-service booking ID.

        </ResponseField>

        <ResponseField name="bookings" type="BookingResult[]">

          The single-service bookings that make up the multi-service booking package.

          <Expandable title="bookings properties">

            <ResponseField name="bookingId" type="string">

              Single-service booking ID. Always present.
              This value is identical to `booking.id`, which is only returned when `returnFullEntity` is `true`.

            </ResponseField>

            <ResponseField name="booking" type="Booking">

              Single-service booking entity. Only returned when `returnFullEntity` is `true`.

              See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `MULTI_SERVICE_BOOKING_NOT_FOUND` |  |
    | `404` | `NOT_FOUND` | `BOOKING_NOT_FOUND` |  |
    | `400` | `INVALID_ARGUMENT` | `REVISION_MISSING` |  |
    | `400` | `INVALID_ARGUMENT` | `NOT_ALL_BOOKINGS_WERE_SENT` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_BOOKING_INFO` |  |
    | `500` | `INTERNAL` | `SOME_BOOKINGS_UPDATES_FAILED` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_REVISION_MISMATCH` |  |
    | `428` | `FAILED_PRECONDITION` | `NO_OPEN_SPOTS` |  |
    | `403` | `PERMISSION_DENIED` | `MULTI_SERVICE_BOOKING_INCLUDES_MULTIPLE_STAFF_MEMBERS` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Booking Rescheduled | `wix.bookings.v2.booking_rescheduled` | Triggered when a booking is rescheduled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/_api/bookings-service/v2/multi_service_bookings/a1b2c3d4-e5f6-7890-abcd-ef1234567890/reschedule' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "rescheduleBookingsInfo": [
            {
              "bookingId": "b1c2d3e4-f5a6-7890-bcde-f12345678901",
              "revision": "1",
              "slot": {
                "scheduleId": "c1d2e3f4-a5b6-7890-cdef-123456789012",
                "startDate": "2024-02-16T10:00:00.000Z",
                "endDate": "2024-02-16T11:00:00.000Z",
                "location": {
                  "locationType": "OWNER_BUSINESS",
                  "id": "d1e2f3a4-b5c6-7890-defa-234567890123"
                }
              },
              "totalParticipants": 1
            },
            {
              "bookingId": "e1f2a3b4-c5d6-7890-efab-345678901234",
              "revision": "1",
              "slot": {
                "scheduleId": "f1a2b3c4-d5e6-7890-fabc-456789012345",
                "startDate": "2024-02-16T11:00:00.000Z",
                "endDate": "2024-02-16T11:45:00.000Z",
                "location": {
                  "locationType": "OWNER_BUSINESS",
                  "id": "d1e2f3a4-b5c6-7890-defa-234567890123"
                }
              },
              "totalParticipants": 1
            }
          ],
          "participantNotification": {
            "notifyParticipants": true,
            "message": "Your spa package has been rescheduled to tomorrow morning. See you then!"
          },
          "returnFullEntity": true
        }'
      ```

      ```json Response
      {
        "multiServiceBooking": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "bookings": [
            {
              "bookingId": "b1c2d3e4-f5a6-7890-bcde-f12345678901",
              "booking": {
                "id": "b1c2d3e4-f5a6-7890-bcde-f12345678901",
                "status": "CREATED",
                "paymentStatus": "NOT_PAID",
                "bookedEntity": {
                  "slot": {
                    "scheduleId": "c1d2e3f4-a5b6-7890-cdef-123456789012",
                    "startDate": "2024-02-16T10:00:00.000Z",
                    "endDate": "2024-02-16T11:00:00.000Z",
                    "location": {
                      "locationType": "OWNER_BUSINESS",
                      "id": "d1e2f3a4-b5c6-7890-defa-234567890123"
                    }
                  }
                },
                "contactDetails": {
                  "firstName": "Jane",
                  "lastName": "Smith",
                  "email": "jane.smith@example.com",
                  "phone": "+1-555-123-4567"
                },
                "totalParticipants": 1
              }
            },
            {
              "bookingId": "e1f2a3b4-c5d6-7890-efab-345678901234",
              "booking": {
                "id": "e1f2a3b4-c5d6-7890-efab-345678901234",
                "status": "CREATED",
                "paymentStatus": "NOT_PAID",
                "bookedEntity": {
                  "slot": {
                    "scheduleId": "f1a2b3c4-d5e6-7890-fabc-456789012345",
                    "startDate": "2024-02-16T11:00:00.000Z",
                    "endDate": "2024-02-16T11:45:00.000Z",
                    "location": {
                      "locationType": "OWNER_BUSINESS",
                      "id": "d1e2f3a4-b5c6-7890-defa-234567890123"
                    }
                  }
                },
                "contactDetails": {
                  "firstName": "Jane",
                  "lastName": "Smith",
                  "email": "jane.smith@example.com",
                  "phone": "+1-555-123-4567"
                },
                "totalParticipants": 1
              }
            }
          ]
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Reschedules a multi-service booking by changing the timing for all or specific single-service bookings in the package.

    This method reschedules single-service bookings within the multi-service booking while maintaining sequential order. You must specify exact new timing for each service to ensure they remain back-to-back with no gaps or overlaps.

    This method fails if it can't reschedule at least 1 single-service booking. You must provide the current revision number for each single-service booking you're rescheduling to prevent conflicting changes.

    See [Reschedule Booking](https://dev.wix.com/docs/rest/business-solutions/bookings/bookings/bookings-writer-v2/reschedule-booking) for single-service booking rescheduling details.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Method Declaration

    ```typescript
    import { bookings } from '@wix/bookings';

    bookings.rescheduleMultiServiceBooking(multiServiceBookingId, rescheduleBookingsInfo, options): Promise<RescheduleMultiServiceBookingResponse>
    ```

    ## Method Parameters

    <ParamField body="multiServiceBookingId" type="string" required>

      ID of the multi-service booking to reschedule.

    </ParamField>

    <ParamField body="rescheduleBookingsInfo" type="RescheduleMultiServiceBooking[]" required>

      Information about the single-service bookings to reschedule.

      <Expandable title="rescheduleBookingsInfo properties">

        <ParamField body="bookingId" type="string" required>

          ID of the booking to reschedule.

        </ParamField>

        <ParamField body="slot" type="Slot" required>

          Information about the new slot.

          <Expandable title="slot properties">

            <ParamField body="sessionId" type="string">

              Identifier for the underlying session when the session is a single session or generated from a recurring session.

            </ParamField>

            <ParamField body="serviceId" type="string">

              Service identifier. Required.

            </ParamField>

            <ParamField body="scheduleId" type="string">

              Schedule identifier. Required.

            </ParamField>

            <ParamField body="startDate" type="string">

              The start time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

            </ParamField>

            <ParamField body="endDate" type="string">

              The end time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

            </ParamField>

            <ParamField body="timezone" type="string">

              The timezone according to which the slot is calculated and presented.

            </ParamField>

            <ParamField body="resource" type="SlotResource">

              The resource required for this slot.
              When populated, the specified resource will be assigned to the slot upon confirmation according to its availability.
              When empty, if `skip_availability_validation` is `false`, a random available resource will be assigned to the slot upon confirmation.
              Otherwise, one of the service resources will be assigned to the slot randomly upon confirmation.

              _Nested structure omitted for brevity — see the `SlotResource` object._

            </ParamField>

            <ParamField body="location" type="Location">

              Geographic location of the slot.

              _Nested structure omitted for brevity — see the `Location` object._

            </ParamField>

            <ParamField body="eventId" type="string">

              Calendar event ID - not supported.
              If not empty, on all write flows (create/update), it takes priority over `sessionId`.
              So if both `sessionId` and `eventId` are provided, the `sessionId` will be based on the `eventId`.
              Otherwise, if `eventId` is empty on write flow,

            </ParamField>

            <ParamField body="resourceSelections" type="ResourceSelection[]">

              Information about how the customer has selected resources for the booking.
              Each resource type may have a different selection method.
              Check `resource` for resource details.

              Constraints: `maxItems 3`.

              _Nested structure omitted for brevity — see the `ResourceSelection[]` object._

            </ParamField>

            <ParamField body="allDay" type="boolean">

              Whether the slot spans one or more full days.
              For class bookings, this value is derived from the linked calendar event.
              For appointment bookings, this value is derived from the availability slot. Any caller-provided value is ignored.

              When `true`, `startDate` is set to `00:00:00` of the first day and `endDate` is set to `00:00:00` of the day after the last day (exclusive). Midnight boundaries are interpreted in the slot's `timezone`.

              Default: `false`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the booking is updated.
          To prevent conflicting changes, the current revision must be specified when managing the booking.

        </ParamField>

        <ParamField body="bookedAddOns" type="BookedAddOn[]">

          List of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) added to the booking.

          To replace the booking's the list of booked add-ons, all of these conditions must be met:
          - The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
          - You must have `BOOKINGS.MANAGE_ADDONS` permissions.
          - For bookings paid via an [eCommerce checkout](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/checkout/introduction): The related [eCommerce order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/introduction) must be in a draftable state to correctly adjust the price when modifying add-ons. You can call [Get Order Draftability Status](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/get-order-draftability-status), specifying the order ID to check if it's draftable. If the order isn't draftable, Reschedule Booking still succeeds, but the order's `priceSummary` isn't updated.

          Constraints: `maxItems 21`.

          <Expandable title="bookedAddOns properties">

            <ParamField body="_id" type="string">

              The ID of the add-on.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="groupId" type="string">

              The ID of the add-on group.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="durationInMinutes" type="integer">

              The add-on duration in minutes at the time of booking.
              Populated for duration-based add-ons.

              **Read-only.**

            </ParamField>

            <ParamField body="quantity" type="integer">

              The quantity of booked add-ons.
              For duration-based add-ons, `quantity` is not applicable.
              If `quantity` is provided as `1` for a duration-based add-on, it will be accepted but the value will be cleared.
              If any other value is provided, an `INVALID_ARGUMENT` error will be returned with the message: "Invalid AddOn details: either duration or quantity must be set correctly".

            </ParamField>

            <ParamField body="name" type="string">

              Add-on `name` at the time of booking.

              **Read-only.**

            </ParamField>

            <ParamField body="nameTranslated" type="string">

              Add-on name translated to the language the customer used during booking.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="RescheduleMultiServiceBookingOptions">

      options

      <Expandable title="options properties">

        <ParamField body="participantNotification" type="ParticipantNotification">

          Information about whether to notify the customer and the message to send.

          <Expandable title="participantNotification properties">

            <ParamField body="notifyParticipants" type="boolean">

              Whether to send a message about the changes to the customer.

              Default: `false`

            </ParamField>

            <ParamField body="message" type="string">

              Custom message to send to the participants about the changes to the booking.

            </ParamField>

            <ParamField body="metadata" type="object">

              Information about the delivery channels used to send the notification.
              For example, `{"channels": "SMS" }`, `{"channels": "EMAIL" }`, or `{"channels": "EMAIL, SMS" }`.

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="flowControlSettings" type="RescheduleBookingFlowControlSettings">

          Information about whether specific procedures of the standard Wix Bookings rescheduling flow are changed.
          For example, whether the availability of the new slot is checked before rescheduling the booking or if the customer can reschedule the booking even though the service's rescheduling policy doesn't allow it.

          <Expandable title="flowControlSettings properties">

            <ParamField body="ignoreReschedulePolicy" type="boolean">

              Whether the rescheduling policy applies when rescheduling the booking.

              When passing `false`, you can only reschedule a booking if the rescheduling
              policy allows it.
              When passing `true`, the rescheduling policy is ignored and external
              validation providers are also skipped.
              Default: `false`.

            </ParamField>

            <ParamField body="skipAvailabilityValidation" type="boolean">

              Whether the availability is checked before rescheduling the booking.

              When passing `false`, a booking is only created when the slot or
              schedule is available.
              Default: `false`.

            </ParamField>

            <ParamField body="skipBusinessConfirmation" type="boolean">

              Whether the rescheduled booking's status is automatically set to
              `CONFIRMED` for services that normally require the owner's manual
              confirmation.
              Default: `false`.

            </ParamField>

            <ParamField body="allowAddOnChanges" type="boolean">

              Whether you can update the list of add-ons associated with the booking when rescheduling.
              The calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) must have `BOOKINGS.MANAGE_ADDONS` permissions to specify `true`.

              Default: `false`.

              **Developer Preview.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="returnFullEntity" type="boolean">

          Whether to return the rescheduled bookings entities.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<RescheduleMultiServiceBookingResponse>`.

    <ResponseField name="multiServiceBooking" type="MultiServiceBooking">

      Rescheduled multi-service booking.

      <Expandable title="multiServiceBooking properties">

        <ResponseField name="_id" type="string">

          Multi-service booking ID.

        </ResponseField>

        <ResponseField name="bookings" type="BookingResult[]">

          The single-service bookings that make up the multi-service booking package.

          <Expandable title="bookings properties">

            <ResponseField name="bookingId" type="string">

              Single-service booking ID. Always present.
              This value is identical to `booking.id`, which is only returned when `returnFullEntity` is `true`.

            </ResponseField>

            <ResponseField name="booking" type="Booking">

              Single-service booking entity. Only returned when `returnFullEntity` is `true`.

              See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `MULTI_SERVICE_BOOKING_NOT_FOUND` |  |
    | `404` | `NOT_FOUND` | `BOOKING_NOT_FOUND` |  |
    | `400` | `INVALID_ARGUMENT` | `REVISION_MISSING` |  |
    | `400` | `INVALID_ARGUMENT` | `NOT_ALL_BOOKINGS_WERE_SENT` |  |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_BOOKING_INFO` |  |
    | `500` | `INTERNAL` | `SOME_BOOKINGS_UPDATES_FAILED` |  |
    | `428` | `FAILED_PRECONDITION` | `BOOKING_REVISION_MISMATCH` |  |
    | `428` | `FAILED_PRECONDITION` | `NO_OPEN_SPOTS` |  |
    | `403` | `PERMISSION_DENIED` | `MULTI_SERVICE_BOOKING_INCLUDES_MULTIPLE_STAFF_MEMBERS` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Booking Rescheduled | `wix.bookings.v2.booking_rescheduled` | Triggered when a booking is rescheduled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-reschedule-multi-service-booking" id="ex-sdk-reschedule-multi-service-booking-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-reschedule-multi-service-booking" id="ex-sdk-reschedule-multi-service-booking-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-reschedule-multi-service-booking-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-reschedule-multi-service-booking-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">rescheduleMultiServiceBooking</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">rescheduleMultiServiceBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-reschedule-multi-service-booking-0"><span className="wix-picker-name">rescheduleMultiServiceBooking</span></label>
    <label htmlFor="ex-sdk-reschedule-multi-service-booking-1"><span className="wix-picker-name">rescheduleMultiServiceBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript rescheduleMultiServiceBooking
    import { bookings } from '@wix/bookings';

    async function rescheduleMultiServiceBooking(multiServiceBookingId,rescheduleBookingsInfo,options) {
      const response = await bookings.rescheduleMultiServiceBooking(multiServiceBookingId,rescheduleBookingsInfo,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript rescheduleMultiServiceBooking (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bookings } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bookings },
      // Include the auth strategy and host as relevant
    });

    async function rescheduleMultiServiceBooking(multiServiceBookingId,rescheduleBookingsInfo,options) {
      const response = await myWixClient.bookings.rescheduleMultiServiceBooking(multiServiceBookingId,rescheduleBookingsInfo,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
