---
title: "Get Referral Event"
description: "Retrieves a referral event by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referral event by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/_api/referral-tracker/v1/referral-events/{referralEventId}
    ```

    ## Path Params

    <ParamField path="referralEventId" type="string" required>

      ID of the referral event to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="referralEvent" type="ReferralEvent">

      Retrieved referral event.

      See the [Referral Event Object](/api-reference/business-management/marketing/referral-program/referral-tracker/referral-event-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/_api/referral-tracker/v1/referral-events/017fad99-aa0f-47a9-8d4f-177fc6a07da3' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "referralEvent": {
          "id": "017fad99-aa0f-47a9-8d4f-177fc6a07da3",
          "revision": "1",
          "createdDate": "2024-07-08T11:22:00.826Z",
          "updatedDate": "2024-07-08T11:22:00.826Z",
          "rewardEvent": {
            "referralRewardId": "c8c732cf-2ca2-4025-9b1c-3be02f099cdb",
            "rewardType": "COUPON",
            "rewardedReferringCustomerId": "4fb41de4-da9c-4818-b54f-82c955ecc866"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referral event by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tracker } from '@wix/referral';

    tracker.getReferralEvent(referralEventId): Promise<ReferralEvent>
    ```

    ## Method Parameters

    <ParamField body="referralEventId" type="string" required>

      ID of the referral event to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<ReferralEvent>`.

    See the [Referral Event Object](/api-reference/business-management/marketing/referral-program/referral-tracker/referral-event-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-referral-event" id="ex-sdk-get-referral-event-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-referral-event" id="ex-sdk-get-referral-event-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-referral-event" id="ex-sdk-get-referral-event-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-referral-event-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-referral-event-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getReferralEvent</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferralEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferralEvent (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-referral-event-0"><span className="wix-picker-name">getReferralEvent</span></label>
    <label htmlFor="ex-sdk-get-referral-event-1"><span className="wix-picker-name">getReferralEvent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-referral-event-2"><span className="wix-picker-name">getReferralEvent (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getReferralEvent
    import { tracker } from '@wix/referral';

    async function getReferralEvent(referralEventId) {
      const response = await tracker.getReferralEvent(referralEventId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferralEvent (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tracker } from '@wix/referral';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tracker },
      // Include the auth strategy and host as relevant
    });

    async function getReferralEvent(referralEventId) {
      const response = await myWixClient.tracker.getReferralEvent(referralEventId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferralEvent (with elevated permissions)
    import { tracker } from '@wix/referral';
    import { auth } from '@wix/essentials';

    async function myGetReferralEventMethod(referralEventId) {
      const elevatedGetReferralEvent = auth.elevate(tracker.getReferralEvent);
      const response = await elevatedGetReferralEvent(referralEventId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
