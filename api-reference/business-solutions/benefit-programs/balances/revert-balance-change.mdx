---
title: "Revert Balance Change"
description: "Reverts a transaction created by calling Change Balance."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Reverts a transaction created by calling Change Balance.

    For example, if a transaction increased a balance's available credits by 5,
    calling Revert Balance Change with the transaction's ID will decrease the balance's available credit by 5.

    <Note>

    If the Change Balance call set the balance, Revert Transaction reverts the amount the balance changed by, ensuring subsequent balance changes are still reflected after the reversion.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/balances/changes/{transactionId}/revert
    ```

    ## Path Params

    <ParamField path="transactionId" type="string" required>

      ID of the transaction associated with the balance change to revert.

    </ParamField>

    ## Body Params

    <ParamField body="idempotencyKey" type="string" required deprecated>

      Ignored value, kept for backward compatibility. Idempotency is now handled by the system.

      **Deprecated.** Ignored value, kept for backward compatibility. Idempotency is now handled by the system. Removal: 2026-01-31. Constraints: `maxLength 200`.

    </ParamField>

    <ParamField body="instructingParty" type="IdentificationData">

      Identity reverting the balance change.

      <Expandable title="One of the following">

        <ParamField body="id" type="id">

          id

          <Expandable title="id properties">

            <ParamField body="anonymousVisitorId" type="string">

              ID of a site visitor that hasn't logged in to the site.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="memberId" type="string">

              ID of a site member.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="wixUserId" type="string">

              ID of a Wix user.

              Constraints: `format GUID`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="transactionId" type="string">

      ID of the transaction associated with the balance reversion.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CHANGE_ALREADY_REVERTED` | Transaction was already reverted. |
    | `428` | `FAILED_PRECONDITION` | `NOT_ENOUGH_BALANCE` | Insufficient credits to complete balance reversion. |
    | `404` | `NOT_FOUND` | `TRANSACTION_NOT_FOUND` | Transaction not found |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl https://www.wixapis.com/benefit-programs/v1/balances/changes/9fc6cade-d8b9-46c0-8255-4703afc01ce0/revert \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>'
        --data '{
          "idempotency_key": "31fc503c-4008-42a8-9699-362c69564aaa",
          "transaction_id": "9fc6cade-d8b9-46c0-8255-4703afc01ce0"
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "transaction_id": "5503ce7c-8b3d-41e9-90ab-3456a5fc7f1e"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Reverts a transaction created by calling Change Balance.

    For example, if a transaction increased a balance's available credits by 5,
    calling Revert Balance Change with the transaction's ID will decrease the balance's available credit by 5.

    <Note>

    If the Change Balance call set the balance, Revert Transaction reverts the amount the balance changed by, ensuring subsequent balance changes are still reflected after the reversion.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { balances } from '@wix/benefit-programs';

    balances.revertBalanceChange(transactionId, idempotencyKey, options): Promise<RevertBalanceChangeResponse>
    ```

    ## Method Parameters

    <ParamField body="transactionId" type="string" required>

      ID of the transaction associated with the balance change to revert.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="idempotencyKey" type="string" required deprecated>

      Ignored value, kept for backward compatibility. Idempotency is now handled by the system.

      **Deprecated.** Ignored value, kept for backward compatibility. Idempotency is now handled by the system. Removal: 2026-01-31. Constraints: `maxLength 200`.

    </ParamField>

    <ParamField body="options" type="RevertBalanceChangeOptions">

      options

      <Expandable title="options properties">

        <ParamField body="instructingParty" type="IdentificationData">

          Identity reverting the balance change.

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<RevertBalanceChangeResponse>`.

    <ResponseField name="transactionId" type="string">

      ID of the transaction associated with the balance reversion.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CHANGE_ALREADY_REVERTED` | Transaction was already reverted. |
    | `428` | `FAILED_PRECONDITION` | `NOT_ENOUGH_BALANCE` | Insufficient credits to complete balance reversion. |
    | `404` | `NOT_FOUND` | `TRANSACTION_NOT_FOUND` | Transaction not found |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Balance Updated | `wix.benefit_programs.v1.balance_updated` | Triggered when a balance is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-revert-balance-change" id="ex-sdk-revert-balance-change-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-revert-balance-change" id="ex-sdk-revert-balance-change-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-revert-balance-change" id="ex-sdk-revert-balance-change-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-revert-balance-change-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-revert-balance-change-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">revertBalanceChange</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">revertBalanceChange (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">revertBalanceChange (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-revert-balance-change-0"><span className="wix-picker-name">revertBalanceChange</span></label>
    <label htmlFor="ex-sdk-revert-balance-change-1"><span className="wix-picker-name">revertBalanceChange (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-revert-balance-change-2"><span className="wix-picker-name">revertBalanceChange (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript revertBalanceChange
    import { balances } from '@wix/benefit-programs';

    async function revertBalanceChange(transactionId,idempotencyKey,options) {
      const response = await balances.revertBalanceChange(transactionId,idempotencyKey,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript revertBalanceChange (self-hosted)
    import { createClient } from '@wix/sdk';
    import { balances } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { balances },
      // Include the auth strategy and host as relevant
    });

    async function revertBalanceChange(transactionId,idempotencyKey,options) {
      const response = await myWixClient.balances.revertBalanceChange(transactionId,idempotencyKey,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript revertBalanceChange (with elevated permissions)
    import { balances } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myRevertBalanceChangeMethod(transactionId,idempotencyKey,options) {
      const elevatedRevertBalanceChange = auth.elevate(balances.revertBalanceChange);
      const response = await elevatedRevertBalanceChange(transactionId,idempotencyKey,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
