---
title: "onPostUnliked"
description: "Triggered when a post is unliked."
icon: "bolt"
---

Triggered when a post is unliked.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v3.post_unliked` |
| Package | `@wix/blog` |
| Permissions | `Read Blog`, `Manage Blog`, `Read Draft Blog Posts` |

## Examples

<CodeGroup>

  ```javascript onPostUnliked
  import { posts } from '@wix/blog';

  posts.onPostUnliked((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostUnliked (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostUnliked((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
