---
title: "onCouponUpdated"
description: "Triggered when a coupon specification is updated."
icon: "bolt"
---

Triggered when a coupon specification is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecommerce.coupons.v2.coupon_updated` |
| Package | `@wix/marketing` |
| Permissions | `Manage Stores - all permissions`, `Manage Coupons` |

## Examples

<CodeGroup>

  ```javascript onCouponUpdated
  import { coupons } from '@wix/marketing';

  coupons.onCouponUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCouponUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { coupons } from '@wix/marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        coupons,
      },
  });

  wixClient.coupons.onCouponUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
