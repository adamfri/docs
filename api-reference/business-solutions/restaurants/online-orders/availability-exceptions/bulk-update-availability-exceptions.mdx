---
title: "Bulk Update Availability Exceptions"
description: "Updates multiple availability exceptions."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Updates multiple availability exceptions.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants-availability-exceptions/v1/bulk/availability-exceptions/update
    ```

    ## Body Params

    <ParamField body="availabilityExceptions" type="MaskedAvailabilityException[]" required>

      Availability exceptions entities to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="availabilityExceptions properties">

        <ParamField body="availabilityException" type="AvailabilityException">

          Availability exception entity to update.

          See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

        </ParamField>

        <ParamField body="mask" type="FieldMask">

          Explicit list of fields to update.

          <Expandable title="mask properties">

            <ParamField body="paths" type="string[]" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to receive the entity in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkAvailabilityExceptionResult[]">

      Information about the updated availability exceptions.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata of the availability exception update.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="AvailabilityException">

          Availability exception (present when return_entity is true).

          See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk Update Availability Exceptions metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Availability Exception Updated | `wix.restaurants.v1.availability_exception_updated` | Triggered when an availability exception is updated. |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates multiple availability exceptions.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { availabilityExceptions } from '@wix/restaurants';

    availabilityExceptions.bulkUpdateAvailabilityExceptions(availabilityExceptions, options): Promise
    ```

    ## Method Parameters

    <ParamField body="availabilityExceptions" type="MaskedAvailabilityException[]" required>

      Availability exceptions entities to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="availabilityExceptions properties">

        <ParamField body="availabilityException" type="AvailabilityException">

          Availability exception entity to update.

          See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

        </ParamField>

        <ParamField body="mask" type="string[]">

          Explicit list of fields to update.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="BulkUpdateAvailabilityExceptionsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to receive the entity in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkAvailabilityExceptionResult[]">

      Information about the updated availability exceptions.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata of the availability exception update.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="AvailabilityException">

          Availability exception (present when return_entity is true).

          See the [Availability Exception Object](/api-reference/business-solutions/restaurants/online-orders/availability-exceptions/availability-exception-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk Update Availability Exceptions metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Availability Exception Updated | `wix.restaurants.v1.availability_exception_updated` | Triggered when an availability exception is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-availability-exceptions" id="ex-sdk-bulk-update-availability-exceptions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-availability-exceptions" id="ex-sdk-bulk-update-availability-exceptions-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-update-availability-exceptions" id="ex-sdk-bulk-update-availability-exceptions-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-availability-exceptions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-availability-exceptions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateAvailabilityExceptions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateAvailabilityExceptions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateAvailabilityExceptions (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-availability-exceptions-0"><span className="wix-picker-name">bulkUpdateAvailabilityExceptions</span></label>
    <label htmlFor="ex-sdk-bulk-update-availability-exceptions-1"><span className="wix-picker-name">bulkUpdateAvailabilityExceptions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-update-availability-exceptions-2"><span className="wix-picker-name">bulkUpdateAvailabilityExceptions (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUpdateAvailabilityExceptions
    import { availabilityExceptions } from '@wix/restaurants';

    async function bulkUpdateAvailabilityExceptions(availabilityExceptions,options) {
      const response = await availabilityExceptions.bulkUpdateAvailabilityExceptions(availabilityExceptions,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateAvailabilityExceptions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { availabilityExceptions } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { availabilityExceptions },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdateAvailabilityExceptions(availabilityExceptions,options) {
      const response = await myWixClient.availabilityExceptions.bulkUpdateAvailabilityExceptions(availabilityExceptions,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateAvailabilityExceptions (with elevated permissions)
    import { availabilityExceptions } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myBulkUpdateAvailabilityExceptionsMethod(availabilityExceptions,options) {
      const elevatedBulkUpdateAvailabilityExceptions = auth.elevate(availabilityExceptions.bulkUpdateAvailabilityExceptions);
      const response = await elevatedBulkUpdateAvailabilityExceptions(availabilityExceptions,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
