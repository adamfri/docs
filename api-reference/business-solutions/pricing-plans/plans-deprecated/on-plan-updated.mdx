---
title: "onPlanUpdated"
description: "Triggered when a pricing plan is updated."
icon: "bolt"
---

Triggered when a pricing plan is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.plan_updated` |
| Package | `@wix/pricing-plans` |
| Permissions | `Read Pricing Plans`, `Manage Events`, `Manage Challenges`, `Manage Pricing Plans` |

## Examples

<CodeGroup>

  ```javascript onPlanUpdated
  import { plans } from '@wix/pricing-plans';

  plans.onPlanUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPlanUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { plans } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        plans,
      },
  });

  wixClient.plans.onPlanUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
