---
title: "Get Resource"
description: "Retrieves a resource."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a resource.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/bookings/v2/resources/{resourceId}
    ```

    ## Path Params

    <ParamField path="resourceId" type="string" required>

      ID of the resource to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="resource" type="Resource">

      Retrieved resource.

      See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/bookings/v2/resources/ffedf9dc-932d-439f-b1f4-e42b2a5a8bb5' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "resource": {
          "id": "ffedf9dc-932d-439f-b1f4-e42b2a5a8bb5",
          "revision": "1",
          "createdDate": "2024-11-22T13:36:47.104Z",
          "updatedDate": "2024-11-22T13:36:47.104Z",
          "name": "Meeting room with conference system",
          "typeId": "dd7790c7-f3ac-4821-accb-52ed385fd932",
          "locationOptions": {
            "availableInAllLocations": false,
            "specificLocationOptions": {
              "availableInBusinessLocations": true,
              "businessLocations": [
                {
                  "locationId": "1295e5ff-ecb3-41bc-8b6f-7345792e5ec0"
                }
              ]
            }
          },
          "eventsSchedule": {
            "scheduleId": "e263b1ca-a1e9-45bf-a39c-044e97c63500"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a resource.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { resources } from '@wix/bookings';

    resources.getResource(resourceId): Promise<Resource>
    ```

    ## Method Parameters

    <ParamField body="resourceId" type="string" required>

      ID of the resource to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Resource>`.

    See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-resource" id="ex-sdk-get-resource-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-resource" id="ex-sdk-get-resource-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-resource-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-resource-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getResource</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getResource (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-resource-0"><span className="wix-picker-name">getResource</span></label>
    <label htmlFor="ex-sdk-get-resource-1"><span className="wix-picker-name">getResource (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getResource
    import { resources } from '@wix/bookings';

    async function getResource(resourceId) {
      const response = await resources.getResource(resourceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getResource (self-hosted)
    import { createClient } from '@wix/sdk';
    import { resources } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { resources },
      // Include the auth strategy and host as relevant
    });

    async function getResource(resourceId) {
      const response = await myWixClient.resources.getResource(resourceId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
