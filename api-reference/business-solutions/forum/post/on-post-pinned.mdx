---
title: "onPostPinned"
description: "An existing post was pinned to the top of the post list."
icon: "bolt"
---

An existing post was pinned to the top of the post list.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_pinned` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostPinned
  import { posts } from '@wix/forum';

  posts.onPostPinned((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostPinned (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostPinned((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
