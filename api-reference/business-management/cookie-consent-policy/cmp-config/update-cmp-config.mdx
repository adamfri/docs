---
title: "Update Cmp Config"
description: "Updates a CMP config."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a CMP config.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CMP_CONFIG.MANAGE">Manage CMP Configurations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/consent/cmp/v2/cmp-configs
    ```

    ## Body Params

    <ParamField body="cmpConfig" type="CmpConfig" required>

      CMP config to be updated.

      See the [Cmp Config Object](/api-reference/business-management/cookie-consent-policy/cmp-config/cmp-config-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="cmpConfig" type="CmpConfig">

      Updated CMP config.

      See the [Cmp Config Object](/api-reference/business-management/cookie-consent-policy/cmp-config/cmp-config-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `NO_HTML_WEB_APPLICATION_FOUND` | Site not found. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/consent/cmp/v2/cmp-configs' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "cmp_config": {
             "revision": "2",
             "social_elements_blocked": true
          },
          "field_mask": {
             "paths": [
               "socialElementsBlocked"
             ]
          }
      }'
      ```

      ```json Response
      {
        "cmp_config": {
          "revision": "3",
          "created_date": "2025-02-16T11:10:47.613Z",
          "updated_date": "2025-02-16T14:13:37.818Z",
          "social_elements_blocked": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a CMP config.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CMP_CONFIG.MANAGE">Manage CMP Configurations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { cmp } from '@wix/consent-policy';

    cmp.updateCmpConfig(cmpConfig): Promise
    ```

    ## Method Parameters

    <ParamField body="cmpConfig" type="CmpConfig" required>

      CMP config to be updated.

      See the [Cmp Config Object](/api-reference/business-management/cookie-consent-policy/cmp-config/cmp-config-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `CmpConfig`.

    See the [Cmp Config Object](/api-reference/business-management/cookie-consent-policy/cmp-config/cmp-config-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `NO_HTML_WEB_APPLICATION_FOUND` | Site not found. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Cmp Config Updated | `wix.bu.legal.v2.cmp_config_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-cmp-config" id="ex-sdk-update-cmp-config-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-cmp-config" id="ex-sdk-update-cmp-config-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-cmp-config" id="ex-sdk-update-cmp-config-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-cmp-config-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-cmp-config-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateCmpConfig</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCmpConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCmpConfig (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-cmp-config-0"><span className="wix-picker-name">updateCmpConfig</span></label>
    <label htmlFor="ex-sdk-update-cmp-config-1"><span className="wix-picker-name">updateCmpConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-cmp-config-2"><span className="wix-picker-name">updateCmpConfig (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateCmpConfig
    import { cmp } from '@wix/consent-policy';

    async function updateCmpConfig(cmpConfig) {
      const response = await cmp.updateCmpConfig(cmpConfig);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCmpConfig (self-hosted)
    import { createClient } from '@wix/sdk';
    import { cmp } from '@wix/consent-policy';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { cmp },
      // Include the auth strategy and host as relevant
    });

    async function updateCmpConfig(cmpConfig) {
      const response = await myWixClient.cmp.updateCmpConfig(cmpConfig);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCmpConfig (with elevated permissions)
    import { cmp } from '@wix/consent-policy';
    import { auth } from '@wix/essentials';

    async function myUpdateCmpConfigMethod(cmpConfig) {
      const elevatedUpdateCmpConfig = auth.elevate(cmp.updateCmpConfig);
      const response = await elevatedUpdateCmpConfig(cmpConfig);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
