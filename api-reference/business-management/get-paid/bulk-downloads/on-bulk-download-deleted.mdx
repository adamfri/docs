---
title: "onBulkDownloadDeleted"
description: "Triggered when a bulk download is deleted."
icon: "bolt"
---

Triggered when a bulk download is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.get_paid.downloader.v1.bulk_download_deleted` |
| Package | `@wix/get-paid` |

## Examples

<CodeGroup>

  ```javascript onBulkDownloadDeleted
  import { bulkDownloads } from '@wix/get-paid';

  bulkDownloads.onBulkDownloadDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onBulkDownloadDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bulkDownloads } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bulkDownloads,
      },
  });

  wixClient.bulkDownloads.onBulkDownloadDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
