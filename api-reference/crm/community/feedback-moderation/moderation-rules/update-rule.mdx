---
title: "Update Rule"
description: "Updates a rule."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a rule.

    Each time a rule is updated, `revision` increments by 1. The existing `revision` must be included when updating the rule. This ensures you're working with the latest rule and prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MODERATION.MANAGE-MODERATION-RULES">Manage Moderation Rules</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/moderation/v1/rules/{rule.id}
    ```

    ## Path Params

    <ParamField path="rule.id" type="string" required>

      Rule ID.

    </ParamField>

    ## Body Params

    <ParamField body="rule" type="Rule" required>

      Rule to update.

      See the [Rule Object](/api-reference/crm/community/feedback-moderation/moderation-rules/rule-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="rule" type="Rule">

      Updated rule.

      See the [Rule Object](/api-reference/crm/community/feedback-moderation/moderation-rules/rule-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rule Updated | `wix.moderation.v1.rule_updated` | Triggered when a rule is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH https://www.wixapis.com/moderation/v1/rules/500ea62c-5f4c-4a51-b813-0bd7f239c6dd  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
                "rule": {
                  "id": "500ea62c-5f4c-4a51-b813-0bd7f239c6dd",
                  "revision": "1",
                  "namespace": "reviews/stores",
                  "audience": {
                    "type": "VISITORS"
                  }
                }
              }'
      ```

      ```json Response
      {
        "rule": {
          "id": "500ea62c-5f4c-4a51-b813-0bd7f239c6dd",
          "revision": "2",
          "createdDate": "2023-10-10T06:52:04.718Z",
          "updatedDate": "2023-10-10T06:58:01.595Z",
          "namespace": "reviews/stores",
          "name": "Check for Rating",
          "audience": {
            "type": "VISITORS"
          },
          "trigger": {
            "type": "ATTRIBUTE",
            "attributeOptions": {
              "name": "rating",
              "values": ["1", "2"]
            }
          },
          "exemptions": {
            "memberGroups": [],
            "memberIds": []
          },
          "action": {
            "type": "NEEDS_MANUAL_APPROVAL"
          },
          "enabled": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a rule.

    Each time a rule is updated, `revision` increments by 1. The existing `revision` must be included when updating the rule. This ensures you're working with the latest rule and prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MODERATION.MANAGE-MODERATION-RULES">Manage Moderation Rules</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { moderationRules } from '@wix/moderation-rules';

    moderationRules.updateRule(_id, rule): Promise<Rule>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Rule ID.

      **Read-only.**

    </ParamField>

    <ParamField body="rule" type="UpdateRule" required>

      Rule to update.

      <Expandable title="rule properties">

        <ParamField body="_id" type="string">

          Rule ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the rule is updated. To prevent conflicting changes, the existing `revision` must be used when updating a rule.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time when the rule was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time when the rule was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="namespace" type="string">

          The app name and entity which the rule belongs to.
          Currently, moderation rules support [Comments](https://dev.wix.com/docs/rest/crm/community/comments/comments/introduction)
          and [Reviews](https://dev.wix.com/docs/rest/crm/community/reviews/reviews/introduction):

          + When the `namespace` is `comments` the format is `comments/{APP_ID}`. For example, for moderating content on Wix Blog the `namespace` is `"comments/14bcded7-0066-7c35-14d7-466cb3f09103"`.
          + When the `namespace` is `reviews` the format is `reviews/{APP_NAME}`. For example, for moderating content on Wix Reviews the `namespace` is `"reviews/stores"`.

          To retrieve the ID for a Wix app, see
          [Apps Created By Wix](https://dev.wix.com/docs/rest/articles/get-started/apps-created-by-wix).

          Constraints: `minLength 1`, `maxLength 120`.

        </ParamField>

        <ParamField body="name" type="string">

          Rule name.

          Constraints: `maxLength 120`.

        </ParamField>

        <ParamField body="audience" type="Audience">

          Audience to which the rule applies.

          <Expandable title="audience properties">

            <ParamField body="type" type="string">

              Audience type.

              **Supported values:**

              - `VISITORS`
              - `MEMBERS`
              - `MEMBERS_AND_VISITORS`
              - `NEW_MEMBERS`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="options" type="options">

              options

              _Nested structure omitted for brevity — see the `options` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="trigger" type="Trigger">

          A condition that triggers the rule.

          **Note:** If you need to have several triggers for the same `namespace`, create separate rules.

          <Expandable title="trigger properties">

            <ParamField body="type" type="string">

              Trigger type.

              **Supported values:**

              - `PATTERNS` — The trigger is a specific word or phrase. You can specify additional options in the `patternsOptions` object.
              - `CONTENT_FEATURES` — The rule is triggered when non-textual features, such as videos, images, links, and attachments, exist in the content. You can specify additional options in the `contentFeaturesOptions` object.
              - `ATTRIBUTE` — A custom trigger based on an attribute and its value. For example, if you'd like to check all reviews with a low rating, then `name` would be `rating`, and `values` would be `[1, 2]`. You can specify additional options in the `patternsOptions` object.
              - `ALWAYS` — There is no specific trigger. The rule is triggered whenever new content is created or existing content is updated, with no other conditions required.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="options" type="options">

              options

              _Nested structure omitted for brevity — see the `options` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="exemptions" type="Exemptions">

          List of site members or groups to whom the rule doesn't apply.

          <Expandable title="exemptions properties">

            <ParamField body="memberGroups" type="string[]">

              List of member groups.

              Constraints: `format GUID`, `maxItems 50`.

            </ParamField>

            <ParamField body="memberIds" type="string[]">

              List of member IDs.

              Constraints: `format GUID`, `maxItems 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="action" type="Action">

          What action should be taken after the rule is triggered.

          <Expandable title="action properties">

            <ParamField body="type" type="string">

              Action type.

              **Supported values:**

              - `UNKNOWN_TYPE`
              - `REJECT` — Reject the content without any further review.
              - `NEEDS_MANUAL_APPROVAL` — Send the content for manual review.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="enabled" type="boolean">

          Whether the rule is enabled.

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom field data for the rule object.

          **Note:** You must configure extended fields using schema plugin extensions in your app's dashboard before you can access the extended fields with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Rule>`.

    See the [Rule Object](/api-reference/crm/community/feedback-moderation/moderation-rules/rule-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rule Updated | `wix.moderation.v1.rule_updated` | Triggered when a rule is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-rule" id="ex-sdk-update-rule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-rule" id="ex-sdk-update-rule-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-rule" id="ex-sdk-update-rule-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-rule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-rule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateRule</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateRule (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-rule-0"><span className="wix-picker-name">updateRule</span></label>
    <label htmlFor="ex-sdk-update-rule-1"><span className="wix-picker-name">updateRule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-rule-2"><span className="wix-picker-name">updateRule (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateRule
    import { moderationRules } from '@wix/moderation-rules';

    async function updateRule(_id,rule) {
      const response = await moderationRules.updateRule(_id,rule);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateRule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { moderationRules } from '@wix/moderation-rules';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { moderationRules },
      // Include the auth strategy and host as relevant
    });

    async function updateRule(_id,rule) {
      const response = await myWixClient.moderationRules.updateRule(_id,rule);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateRule (with elevated permissions)
    import { moderationRules } from '@wix/moderation-rules';
    import { auth } from '@wix/essentials';

    async function myUpdateRuleMethod(_id,rule) {
      const elevatedUpdateRule = auth.elevate(moderationRules.updateRule);
      const response = await elevatedUpdateRule(_id,rule);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
