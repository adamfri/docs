---
title: "onMenuCreated"
description: "The onMenuCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.menu_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onMenuCreated
  import { menus } from '@wix/restaurants';

  menus.onMenuCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onMenuCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { menus } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        menus,
      },
  });

  wixClient.menus.onMenuCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
