---
title: "Bulk Create Category"
description: "Creates multiple categories at once."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple categories at once.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/bulk/categories/create
    ```

    ## Body Params

    <ParamField body="categories" type="Category[]" required>

      Categories to create.

      Constraints: `minItems 1`, `maxItems 10`.

      See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkCategoryResult[]">

      Bulk create results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Category">

          Category.

          See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata of results.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/events/v1/bulk/categories/create' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "categories": [
         { 
           "name": "leather",
           "states": ["MANUAL"]
         },
         {
           "name": "shoes",
           "states": ["HIDDEN"]
         }
        ]
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "results": [
          {
            "itemMetadata": {
              "id": "6c313b96-1961-4f0f-b6d7-61788e1a25df",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "6c313b96-1961-4f0f-b6d7-61788e1a25df",
              "name": "leather",
              "createdDate": "2021-04-01T09:55:31.335Z"
            }
          },
          {
            "itemMetadata": {
              "id": "022cdce2-6d8d-4dfd-9210-4b6671539173",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "022cdce2-6d8d-4dfd-9210-4b6671539173",
              "name": "shoes",
              "createdDate": "2021-04-01T09:55:31.335Z"
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple categories at once.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/events';

    categories.bulkCreateCategory(categories): Promise<BulkCreateCategoryResponse>
    ```

    ## Method Parameters

    <ParamField body="categories" type="Category[]" required>

      Categories to create.

      Constraints: `minItems 1`, `maxItems 10`.

      See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<BulkCreateCategoryResponse>`.

    <ResponseField name="results" type="BulkCategoryResult[]">

      Bulk create results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Category">

          Category.

          See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata of results.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Created | `wix.events.v1.category_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-category" id="ex-sdk-bulk-create-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-category" id="ex-sdk-bulk-create-category-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-create-category" id="ex-sdk-bulk-create-category-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create multiple categories</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create multiple categories (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-category-0"><span className="wix-picker-name">Create multiple categories</span></label>
    <label htmlFor="ex-sdk-bulk-create-category-1"><span className="wix-picker-name">Create multiple categories (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-bulk-create-category-2"><span className="wix-picker-name">bulkCreateCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create multiple categories
    import { categories } from '@wix/events';

    /*
     * Sample categories value:
     * [
     *  {
     *    "name": "leather",
     *    "states": ["MANUAL"]
     *  },
     *  {
     *    "name": "shoes",
     *    "states": ["MANUAL"]
     *  }
     * ]
     */

    export async function myBulkCreateCategoryFunction(categories) {
      try {
        const createdCategories = await categories.bulkCreateCategory(categories);
        console.log('Created categories: ', createdCategories);
        return createdCategories;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 0,
     *         "success": true,
     *         "_id": "7d2560a8-b0ab-41fe-9c7c-6584157b2de1"
     *       },
     *       "item": {
     *         "name": "leather",
     *         "states": [
     *           "MANUAL"
     *         ],
     *         "_id": "7d2560a8-b0ab-41fe-9c7c-6584157b2de1",
     *         "_createdDate": "2024-04-23T07:27:41.048Z"
     *       }
     *     },
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 1,
     *         "success": true,
     *         "_id": "022cdce2-6d8d-4dfd-9210-4b6671539173"
     *       },
     *       "item": {
     *         "name": "shoes",
     *         "states": [
     *           "HIDDEN"
     *         ],
     *         "_id": "022cdce2-6d8d-4dfd-9210-4b6671539173",
     *         "_createdDate": "2024-04-23T07:27:41.048Z"
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 2,
     *     "totalFailures": 0,
     *     "undetailedFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Create multiple categories (with elevated permissions)
    import { categories } from '@wix/events';
    import { auth } from '@wix/essentials';

    /*
     * Sample categories value:
     * [
     *  {
     *    "name": "leather",
     *    "states": ["MANUAL"]
     *  },
     *  {
     *    "name": "shoes",
     *    "states": ["MANUAL"]
     *  }
     * ]
     */

    const elevatedBulkCreateCategory = auth.elevate(categories.bulkCreateCategory);

    export async function myBulkCreateCategoryFunction(categories) {
      try {
        const createdCategories = await elevatedBulkCreateCategory(categories);
        console.log('Created categories: ', createdCategories);
        return createdCategories;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 0,
     *         "success": true,
     *         "_id": "7d2560a8-b0ab-41fe-9c7c-6584157b2de1"
     *       },
     *       "item": {
     *         "name": "leather",
     *         "states": [
     *           "MANUAL"
     *         ],
     *         "_id": "7d2560a8-b0ab-41fe-9c7c-6584157b2de1",
     *         "_createdDate": "2024-04-23T07:27:41.048Z"
     *       }
     *     },
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 1,
     *         "success": true,
     *         "_id": "022cdce2-6d8d-4dfd-9210-4b6671539173"
     *       },
     *       "item": {
     *         "name": "shoes",
     *         "states": [
     *           "HIDDEN"
     *         ],
     *         "_id": "022cdce2-6d8d-4dfd-9210-4b6671539173",
     *         "_createdDate": "2024-04-23T07:27:41.048Z"
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 2,
     *     "totalFailures": 0,
     *     "undetailedFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateCategory(categories) {
      const response = await myWixClient.categories.bulkCreateCategory(categories);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
