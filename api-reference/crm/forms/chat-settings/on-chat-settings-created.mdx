---
title: "onChatSettingsCreated"
description: "The onChatSettingsCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forms.ai.v1.chat_settings_created` |
| Package | `@wix/forms` |
| Permissions | `Manage forms`, `Manage Online Programs` |

## Examples

<CodeGroup>

  ```javascript onChatSettingsCreated
  import { chatSettings } from '@wix/forms';

  chatSettings.onChatSettingsCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onChatSettingsCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { chatSettings } from '@wix/forms';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        chatSettings,
      },
  });

  wixClient.chatSettings.onChatSettingsCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
