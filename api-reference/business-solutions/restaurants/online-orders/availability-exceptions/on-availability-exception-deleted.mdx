---
title: "onAvailabilityExceptionDeleted"
description: "Triggered when an availability exception is deleted."
icon: "bolt"
---

Triggered when an availability exception is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.availability_exception_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onAvailabilityExceptionDeleted
  import { availabilityExceptions } from '@wix/restaurants';

  availabilityExceptions.onAvailabilityExceptionDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onAvailabilityExceptionDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { availabilityExceptions } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        availabilityExceptions,
      },
  });

  wixClient.availabilityExceptions.onAvailabilityExceptionDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
