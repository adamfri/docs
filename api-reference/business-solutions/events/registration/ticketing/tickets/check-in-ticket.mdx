---
title: "Check In Ticket"
description: "Checks in 1 or more tickets."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks in 1 or more tickets.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/tickets/check-in
    ```

    ## Body Params

    <ParamField body="eventId" type="string" required>

      Event ID to which the ticket belongs.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="ticketNumber" type="string[]">

      Tickets to check-in.

      Constraints: `minLength 1`, `maxLength 36`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="tickets" type="Ticket[]">

      Updated tickets.

      See the [Ticket Object](/api-reference/business-solutions/events/registration/ticketing/tickets/ticket-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Order Updated | `wix.events.ticketing.events.OrderUpdated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/events/v1/tickets/check-in' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "eventId": "ad18d12e-a6a9-4c17-abfa-6ff119479be2",
        "ticketNumber": [
          "FNVL-NIJT-WP021",
          "FNVL-O7MZ-0Q021"
        ]
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "tickets": [
          {
            "ticketNumber": "FNVL-NIJT-WP021",
            "orderNumber": "FNVL-NIJT-WP",
            "ticketDefinitionId": "d86ffbbd-aa9b-488d-a14b-11752b78e272",
            "name": "Early Bird",
            "price": {
              "amount": "12.34",
              "currency": "USD"
            },
            "free": false,
            "policy": "This is your event ticket. Ticket holders must present their tickets on entry.",
            "checkIn": {
              "created": "2020-05-29T14:53:43.854Z"
            },
            "orderStatus": "PAID",
            "orderArchived": false,
            "orderFullName": "John Doe",
            "guestDetails": {
              "guestAssigned": false,
              "firstName": "John",
              "lastName": "Doe",
              "email": "john.doe@example.com",
              "contactId": "3ef81e9f-106e-408d-a3ee-3df60838acc6"
            },
            "archived": false,
            "anonymized": false,
            "checkInUrl": "https://www.wixevents.com/check-in/FNVL-NIJT-WP021,ad18d12e-a6a9-4c17-abfa-6ff119479be2",
            "ticketPdfUrl": "https://apps.wix.com/events/doc/tickets/Ticket.pdf?request=<TOKEN>"
          },
          {
            "ticketNumber": "FNVL-O7MZ-0Q021",
            "orderNumber": "FNVL-O7MZ-0Q",
            "ticketDefinitionId": "d86ffbbd-aa9b-488d-a14b-11752b78e272",
            "name": "Early Bird",
            "price": {
              "amount": "11.11",
              "currency": "USD"
            },
            "free": false,
            "policy": "This is your event ticket. Ticket holders must present their tickets on entry.",
            "checkIn": {
              "created": "2020-05-29T14:53:43.859Z"
            },
            "orderStatus": "OFFLINE_PENDING",
            "orderArchived": false,
            "orderFullName": "Jane Doe",
            "guestDetails": {
              "guestAssigned": false,
              "firstName": "Jane",
              "lastName": "Doe",
              "email": "jane.doe@example.com",
              "contactId": "2b7494bc-550a-47d3-8bba-d22564ae8bdc"
            },
            "archived": false,
            "anonymized": false,
            "checkInUrl": "https://www.wixevents.com/check-in/FNVL-O7MZ-0Q021,ad18d12e-a6a9-4c17-abfa-6ff119479be2",
            "ticketPdfUrl": "https://apps.wix.com/events/doc/tickets/Ticket.pdf?request=<TOKEN>"
          }
        ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Checks in 1 or more tickets.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tickets } from '@wix/events';

    tickets.checkInTickets(eventId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="eventId" type="string" required>

      Event ID to which the ticket belongs.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="CheckInTicketOptions">

      Options for checking in tickets.

      <Expandable title="options properties">

        <ParamField body="ticketNumber" type="string[]">

          Tickets to check-in.

          Constraints: `minLength 1`, `maxLength 36`, `minItems 1`, `maxItems 100`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="tickets" type="Ticket[]">

      Updated tickets.

      See the [Ticket Object](/api-reference/business-solutions/events/registration/ticketing/tickets/ticket-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Ticket Order Updated | `wix.events.ticketing.events.OrderUpdated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-check-in-ticket" id="ex-sdk-check-in-ticket-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-check-in-ticket" id="ex-sdk-check-in-ticket-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-check-in-ticket" id="ex-sdk-check-in-ticket-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-check-in-ticket-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-check-in-ticket-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">checkInTickets</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkInTickets (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">checkInTickets (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-check-in-ticket-0"><span className="wix-picker-name">checkInTickets</span></label>
    <label htmlFor="ex-sdk-check-in-ticket-1"><span className="wix-picker-name">checkInTickets (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-check-in-ticket-2"><span className="wix-picker-name">checkInTickets (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript checkInTickets
    import { tickets } from '@wix/events';

    async function checkInTickets(eventId,options) {
      const response = await tickets.checkInTickets(eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkInTickets (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tickets } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tickets },
      // Include the auth strategy and host as relevant
    });

    async function checkInTickets(eventId,options) {
      const response = await myWixClient.tickets.checkInTickets(eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript checkInTickets (with elevated permissions)
    import { tickets } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myCheckInTicketsMethod(eventId,options) {
      const elevatedCheckInTickets = auth.elevate(tickets.checkInTickets);
      const response = await elevatedCheckInTickets(eventId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
