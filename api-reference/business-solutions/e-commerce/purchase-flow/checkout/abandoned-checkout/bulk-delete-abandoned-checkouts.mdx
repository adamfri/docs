---
title: "Bulk Delete Abandoned Checkouts"
description: "Deletes multiple abandoned checkouts."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple abandoned checkouts.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/ecom/v1/bulk/abandoned-checkout/delete
    ```

    ## Body Params

    <ParamField body="abandonedCheckoutIds" type="string[]" required>

      Abandoned checkout IDs to be deleted.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkAbandonedCheckoutResult[]">

      Results of the bulk delete operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          ID of the abandoned checkout that was deleted.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata about the bulk action.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Abandoned Checkout Deleted | `wix.ecom.v1.abandoned_checkout_deleted` | Triggerred when an abandoned checkout is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/ecom/v1/bulk/abandoned-checkouts/delete' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "abandonedCheckoutIds": [
            "b4ebb0b8-4482-4693-ae09-7b47e977484c",
            "f126d944-c0ae-4474-b416-3285e19ada7e",
            "e2bb2326-5cb0-47f8-a0c5-88cd8896a96b"
          ]
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "b4ebb0b8-4482-4693-ae09-7b47e977484c",
              "originalIndex": 0,
              "success": true,
              "error": null
            }
          },
          {
            "itemMetadata": {
              "id": "f126d944-c0ae-4474-b416-3285e19ada7e",
              "originalIndex": 1,
              "success": true,
              "error": null
            }
          },
          {
            "itemMetadata": {
              "id": "e2bb2326-5cb0-47f8-a0c5-88cd8896a96b",
              "originalIndex": 2,
              "success": true,
              "error": null
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 3,
          "totalFailures": 0,
          "unknownFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple abandoned checkouts.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { abandonedCheckouts } from '@wix/ecom';

    abandonedCheckouts.bulkDeleteAbandonedCheckouts(abandonedCheckoutIds): Promise
    ```

    ## Method Parameters

    <ParamField body="abandonedCheckoutIds" type="string[]" required>

      Abandoned checkout IDs to be deleted.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkAbandonedCheckoutResult[]">

      Results of the bulk delete operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          ID of the abandoned checkout that was deleted.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata about the bulk action.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Abandoned Checkout Deleted | `wix.ecom.v1.abandoned_checkout_deleted` | Triggerred when an abandoned checkout is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-abandoned-checkouts" id="ex-sdk-bulk-delete-abandoned-checkouts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-abandoned-checkouts" id="ex-sdk-bulk-delete-abandoned-checkouts-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-abandoned-checkouts" id="ex-sdk-bulk-delete-abandoned-checkouts-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-abandoned-checkouts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-abandoned-checkouts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteAbandonedCheckouts</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteAbandonedCheckouts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteAbandonedCheckouts (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-abandoned-checkouts-0"><span className="wix-picker-name">bulkDeleteAbandonedCheckouts</span></label>
    <label htmlFor="ex-sdk-bulk-delete-abandoned-checkouts-1"><span className="wix-picker-name">bulkDeleteAbandonedCheckouts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-abandoned-checkouts-2"><span className="wix-picker-name">bulkDeleteAbandonedCheckouts (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteAbandonedCheckouts
    import { abandonedCheckouts } from '@wix/ecom';

    async function bulkDeleteAbandonedCheckouts(abandonedCheckoutIds) {
      const response = await abandonedCheckouts.bulkDeleteAbandonedCheckouts(abandonedCheckoutIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteAbandonedCheckouts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { abandonedCheckouts } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { abandonedCheckouts },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteAbandonedCheckouts(abandonedCheckoutIds) {
      const response = await myWixClient.abandonedCheckouts.bulkDeleteAbandonedCheckouts(abandonedCheckoutIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteAbandonedCheckouts (with elevated permissions)
    import { abandonedCheckouts } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteAbandonedCheckoutsMethod(abandonedCheckoutIds) {
      const elevatedBulkDeleteAbandonedCheckouts = auth.elevate(abandonedCheckouts.bulkDeleteAbandonedCheckouts);
      const response = await elevatedBulkDeleteAbandonedCheckouts(abandonedCheckoutIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
