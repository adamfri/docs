---
title: "List Gift Cards By Email"
description: "Deprecated."
icon: "download"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Deprecated. Use List Gift Cards By Contact Details instead.

    Retrieves gift cards associated with a specific email address.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/gift-cards/v1/gift-cards
    ```

    ## Query Params

    <ParamField query="email" type="string" required>

      Email address to search for in sender or recipient information.

      Constraints: `format EMAIL`.

    </ParamField>

    ## Response Object

    <ResponseField name="giftCards" type="GiftCard[]">

      Gift cards associated with the specified email address.

      Returns up to 50 gift cards sorted by creation date in descending order.

      See the [Gift Card Object](/api-reference/business-solutions/gift-cards/gift-cards/gift-card-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    List existing gift cards associated with a given email address, either as the sender or the recipient.

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/gift-cards/v1/gift-cards' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
              "email": "johndoe@gmail.com"
            }'
      ```

      ```json Response
      {
        "giftCards": [
          {
            "id": "544d4606-8066-4836-af47-89bbd1be869f",
            "code": "************AREW",
            "initialValue": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "balance": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "currency": "USD",
            "expirationDate": "2026-05-25T00:00:00Z",
            "createdDate": "2024-09-25T08:00:05.715Z",
            "updatedDate": "2024-09-25T08:00:05.752Z",
            "disabledDate": null,
            "notificationInfo": {
              "recipient": {
                "email": "johndoe@gmail.com",
                "name": "John Doe"
              },
              "notificationDate": null,
              "personalizedMessage": null,
              "sender": {
                "email": "janedoe@gmail.com",
                "name": "Jane Doe"
              }
            },
            "source": "ORDER",
            "orderInfo": {
              "orderId": "84ab1c54-c7a5-4835-98ee-f3688819da24",
              "orderNumber": "10054"
            }
          },
          {
            "id": "4923b8be-fdcb-4f35-ad69-2e6508cba5b6",
            "code": "************5F8C",
            "initialValue": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "balance": {
              "amount": "0",
              "formattedAmount": "$0.00"
            },
            "currency": "USD",
            "expirationDate": "2026-05-24T00:00:00Z",
            "createdDate": "2024-09-24T12:34:34.603Z",
            "updatedDate": "2024-09-26T06:43:05.641Z",
            "disabledDate": null,
            "notificationInfo": {
              "recipient": {
                "email": "janedoe@gmail.com",
                "name": "Jane Doe"
              },
              "notificationDate": null,
              "personalizedMessage": null,
              "sender": {
                "email": "johndoe@gmail.com",
                "name": "John Doe"
              }
            },
            "source": "ORDER",
            "orderInfo": {
              "orderId": "43b199c1-4afb-4e10-89e7-7418e62cddc6",
              "orderNumber": "10051"
            }
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Deprecated. Use List Gift Cards By Contact Details instead.

    Retrieves gift cards associated with a specific email address.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { giftVouchers } from '@wix/gift-vouchers';

    giftVouchers.listGiftCardsByEmail(email): Promise
    ```

    ## Method Parameters

    <ParamField body="email" type="string" required>

      Email address to search for in sender or recipient information.

      Constraints: `format EMAIL`.

    </ParamField>

    ## Returns

    <ResponseField name="giftCards" type="GiftCard[]">

      Gift cards associated with the specified email address.

      Returns up to 50 gift cards sorted by creation date in descending order.

      See the [Gift Card Object](/api-reference/business-solutions/gift-cards/gift-cards/gift-card-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-gift-cards-by-email" id="ex-sdk-list-gift-cards-by-email-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-gift-cards-by-email" id="ex-sdk-list-gift-cards-by-email-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-gift-cards-by-email-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-gift-cards-by-email-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List gift cards by recipient email</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listGiftCardsByEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-gift-cards-by-email-0"><span className="wix-picker-name">List gift cards by recipient email</span></label>
    <label htmlFor="ex-sdk-list-gift-cards-by-email-1"><span className="wix-picker-name">listGiftCardsByEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List gift cards by recipient email
    import { giftVouchers } from "@wix/gift-vouchers";

    async function listGiftCardsByEmail() {
      const response = await giftVouchers.listGiftCardsByEmail(
        "johndoe@gmail.com"
      );
      return response;
    }

    /* Promise resolves to:
     * {
     *   giftCards: [
     *     {
     *       id: "ec3c4a90-cf46-4325-913b-c2ab2ae36549",
     *       code: "************4N6C",
     *       initialValue: { amount: "50", formattedAmount: "$50.00" },
     *       balance: { amount: "50", formattedAmount: "$50.00" },
     *       currency: "USD",
     *       source: "ORDER",
     *     },
     *   ],
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listGiftCardsByEmail (self-hosted)
    import { createClient } from '@wix/sdk';
    import { giftVouchers } from '@wix/gift-vouchers';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { giftVouchers },
      // Include the auth strategy and host as relevant
    });

    async function listGiftCardsByEmail(email) {
      const response = await myWixClient.giftVouchers.listGiftCardsByEmail(email);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
