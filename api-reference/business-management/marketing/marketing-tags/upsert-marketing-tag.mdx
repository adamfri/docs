---
title: "Upsert Marketing Tag"
description: "Creates or updates a marketing tag."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates or updates a marketing tag.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.MANAGE-TAGS">Manage Marketing Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/marketing/v1/tags
    ```

    ## Body Params

    <ParamField body="tag" type="MarketingTag" required>

      Marketing tag to create or update.

      See the [Tag Object](/api-reference/business-management/marketing/marketing-tags/tag-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="tag" type="MarketingTag">

      Created or updated marketing tag.

      See the [Tag Object](/api-reference/business-management/marketing/marketing-tags/tag-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Marketing Tag Created | `wix.marketing.v1.tag_created` | Triggered when a marketing tag is created. |
    | Marketing Tag Updated | `wix.marketing.v1.tag_updated` | Triggered when a marketing tag is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/marketing/v1/tags' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "tag": {
                "enabled": true,
                "googleAnalytics": {
                  "domain": "some-domain.com",
                  "trackingId": "UA-12345678-1",
                  "ipAnonymization": false
                }
              }
            }'
      ```

      ```json Response
      {
        "id": "994ba324-682c-4d81-abd6-84e4be44ba3c",
        "type": "GOOGLE_ANALYTICS",
        "enabled": true,
        "googleAnalytics": {
          "domain": "some-domain.com",
          "trackingId": "UA-12345678-1",
          "ipAnonymization": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates or updates a marketing tag.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.MANAGE-TAGS">Manage Marketing Tags</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingTags } from '@wix/marketing-tags';

    marketingTags.upsertMarketingTag(tag): Promise
    ```

    ## Method Parameters

    <ParamField body="tag" type="MarketingTag" required>

      Marketing tag to create or update.

      See the [Tag Object](/api-reference/business-management/marketing/marketing-tags/tag-object) for this object's fields.

    </ParamField>

    ## Returns

    <ResponseField name="tag" type="MarketingTag">

      Created or updated marketing tag.

      See the [Tag Object](/api-reference/business-management/marketing/marketing-tags/tag-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tag Created | `wix.marketing.v1.tag_created` | Triggered when a marketing tag is created. |
    | Tag Updated | `wix.marketing.v1.tag_updated` | Triggered when a marketing tag is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-upsert-marketing-tag" id="ex-sdk-upsert-marketing-tag-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-upsert-marketing-tag" id="ex-sdk-upsert-marketing-tag-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-upsert-marketing-tag" id="ex-sdk-upsert-marketing-tag-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-upsert-marketing-tag-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-upsert-marketing-tag-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">upsertMarketingTag</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">upsertMarketingTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">upsertMarketingTag (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-upsert-marketing-tag-0"><span className="wix-picker-name">upsertMarketingTag</span></label>
    <label htmlFor="ex-sdk-upsert-marketing-tag-1"><span className="wix-picker-name">upsertMarketingTag (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-upsert-marketing-tag-2"><span className="wix-picker-name">upsertMarketingTag (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript upsertMarketingTag
    import { marketingTags } from '@wix/marketing-tags';

    async function upsertMarketingTag(tag) {
      const response = await marketingTags.upsertMarketingTag(tag);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript upsertMarketingTag (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingTags } from '@wix/marketing-tags';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingTags },
      // Include the auth strategy and host as relevant
    });

    async function upsertMarketingTag(tag) {
      const response = await myWixClient.marketingTags.upsertMarketingTag(tag);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript upsertMarketingTag (with elevated permissions)
    import { marketingTags } from '@wix/marketing-tags';
    import { auth } from '@wix/essentials';

    async function myUpsertMarketingTagMethod(tag) {
      const elevatedUpsertMarketingTag = auth.elevate(marketingTags.upsertMarketingTag);
      const response = await elevatedUpsertMarketingTag(tag);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
