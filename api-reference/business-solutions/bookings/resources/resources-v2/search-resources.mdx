---
title: "Search Resources"
description: "Retrieves a list of resources matching the provided search criteria."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of resources matching the provided search criteria.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `singleResource.locationOptions.specificLocationOptions.businessLocations.locationId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `name` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `singleResource.locationOptions.availableInAllLocations` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `singleResource.locationOptions.specificLocationOptions.availableInBusinessLocations` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/resources/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search criteria including filter, sort, aggregations, and paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object for narrowing search results. For example, to return only resources available at specific business locations: `"filter": {"single_resource.location_options.specific_location_options.business_locations.location_id": {"$in": ["location-id-1", "location-id-2"]}}`.

          Learn more about the filter format in the [supported filters article](https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resources-v2/filtering-and-sorting).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Array of sort objects specifying result order. For example, to sort by resource name in ascending order: `"sort": [{"fieldName": "name", "order": "ASC"}]`.

          Learn more about the sort format in the [supported filters article](https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resources-v2/filtering-and-sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations for grouping data into categories (facets) and providing summaries for each category.
          For example, use aggregations to categorize search results by resource type, management type, or location availability.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation. Client must specify matching aggregation field below.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into 1 key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous 1.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example `name` or `locationOptions.businessLocations.locationId`.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="groupBy" type="GroupByAggregation" deprecated>

              Deprecated. Use `nested` instead.

              **Deprecated.** Deprecated. Use `nested` instead. Use `nested` instead. Removal: 2024-03-30.

              _Nested structure omitted for brevity — see the `GroupByAggregation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least 1 of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `locationOptions.availableInAllLocations`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database) or [ISO 8601 UTC offset format](https://en.wikipedia.org/wiki/ISO_8601#Time_offsets_from_UTC) for adjusting time fields in the specified filters and returned aggregation data. For example, `America/New_York`, `UTC`, or `+02:00`.

          Default: Time zone specified in the business [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/get-site-properties).

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor-based paging for result navigation. When requesting `cursor_paging.cursor`,
              `filter`, `sort`, or `search` can't be specified.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="resources" type="Resource[]">

      Retrieved resources that match the search criteria specified in the request.

      Each resource includes all available information such as name, type, management details,
      location options, working hour schedules, and availability configurations.

      See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor-based paging metadata for navigating through search results.

      Contains navigation information including the current page cursor, whether additional
      results are available, and result counts. Use the `next` cursor to fetch subsequent pages.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current response page.

          This count reflects the actual number of items in the current result set,
          which may be less than the requested limit if fewer items are available.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Navigation cursors for moving between result pages.

          Contains `next` and `prev` cursor tokens for pagination. Use the `next` cursor
          to retrieve subsequent pages and `prev` cursor to go back to previous pages.
          Learn more about cursor paging in the [API Query Language guide](https://dev.wix.com/api/rest/getting-started/api-query-language#getting-started_api-query-language_cursor-paging).

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor token for retrieving the next page of results.

              Use this token in subsequent requests to continue pagination forward.
              Value is `null` when on the last page of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor token for retrieving the previous page of results.

              Use this token to navigate backwards through result pages.
              Value is `null` when on the first page of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates whether additional results are available beyond the current page.

          - `true`: More results exist and can be retrieved using the `next` cursor.
          - `false`: This is the final page of results.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation results derived from the aggregations specified in the search request.

      Provides analytical summaries such as resource counts by type, location distribution,
      management type statistics, or custom groupings. Only populated when aggregations
      are included in the search criteria.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Array of aggregation results, each containing the aggregation metadata and its calculated values for the specified search criteria.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match specified kind as derived from search request.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into 1 key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous 1.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Path to the field to aggregate by in dot notation. For example `name` or `locationOptions.businessLocations.locationId`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-search-resources" id="ex-rest-search-resources-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-search-resources" id="ex-rest-search-resources-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-search-resources" id="ex-rest-search-resources-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-search-resources-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-search-resources-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search resources.</span><span className="wix-picker-desc">Search resources, filtering by resource type and then calculating the number of resources by business location</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Search resources.</span><span className="wix-picker-desc">Search resources, retrieving the ones that match the provided search term</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Search resources.</span><span className="wix-picker-desc">Search resources, filtering by resource type and sorting by name</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-search-resources-0"><span className="wix-picker-name">Search resources.</span><span className="wix-picker-desc">Search resources, filtering by resource type and then calculating the number of resources by business location</span></label>
    <label htmlFor="ex-rest-search-resources-1"><span className="wix-picker-name">Search resources.</span><span className="wix-picker-desc">Search resources, retrieving the ones that match the provided search term</span></label>
    <label htmlFor="ex-rest-search-resources-2"><span className="wix-picker-name">Search resources.</span><span className="wix-picker-desc">Search resources, filtering by resource type and sorting by name</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -v -X POST \
      'https://www.wixapis.com/bookings/v2/resources/search' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "search": {
          "aggregations": [
            {
              "name": "Resource count by business location",
              "type": "NESTED",
              "nested": {
                "nestedAggregations": [
                  {
                    "name": "resourceType",
                    "type": "VALUE",
                    "fieldPath": "type",
                    "value": {}
                  },
                  {
                    "name": "locationId",
                    "type": "VALUE",
                    "fieldPath": "locationOptions.specificLocationOptions.businessLocations.locationId",
                    "value": {}
                  }
                ]
              }
            }
          ],
          "filter": {
            "type": {
              "$eq": "dd7790c7-f3ac-4821-accb-52ed385fd932"
            }
          },
          "cursorPaging": {
            "limit": 0
          }
        }
      }'
      ```

      ```json Response
      {
        "resources": [],
        "pagingMetadata": {
          "count": 0,
          "cursors": {},
          "hasNext": false
        },
        "aggregationData": {
          "results": [
            {
              "name": "Resource count by business location",
              "type": "NESTED",
              "fieldPath": "",
              "nested": {
                "results": [
                  {
                    "results": {
                      "locationId": {
                        "value": {
                          "value": "808dfb00-0a74-4e70-bb3b-f96ff6ba1246",
                          "count": 1
                        }
                      },
                      "resourceType": {
                        "value": {
                          "value": "dd7790c7-f3ac-4821-accb-52ed385fd932",
                          "count": 3
                        }
                      }
                    }
                  },
                  {
                    "results": {
                      "locationId": {
                        "value": {
                          "value": "1295e5ff-ecb3-41bc-8b6f-7345792e5ec0",
                          "count": 1
                        }
                      },
                      "resourceType": {
                        "value": {
                          "value": "dd7790c7-f3ac-4821-accb-52ed385fd932",
                          "count": 3
                        }
                      }
                    }
                  }
                ]
              }
            }
          ]
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -v -X POST \
      'https://www.wixapis.com/bookings/v2/resources/search' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "search": {
          "search": {
            "fields": [
              "name"
            ],
            "expression": "conference"
          }
        }
      }'
      ```

      ```json Response
      {
        "resources": [
          {
            "id": "ffedf9dc-932d-439f-b1f4-e42b2a5a8bb5",
            "revision": "1",
            "createdDate": "2024-11-22T13:36:47.104Z",
            "updatedDate": "2024-11-22T13:36:47.104Z",
            "name": "Meeting room with conference system",
            "typeId": "dd7790c7-f3ac-4821-accb-52ed385fd932",
            "locationOptions": {
              "availableInAllLocations": false,
              "specificLocationOptions": {
                "availableInBusinessLocations": true,
                "businessLocations": [
                  {
                    "locationId": "1295e5ff-ecb3-41bc-8b6f-7345792e5ec0"
                  }
                ]
              }
            },
            "eventsSchedule": {
              "scheduleId": "e263b1ca-a1e9-45bf-a39c-044e97c63500"
            }
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -v -X POST \
      'https://www.wixapis.com/bookings/v2/resources/search' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "search": {
          "sort": [
            {
              "fieldName": "name"
            }
          ],
          "filter": {
            "type": {
              "$eq": "dd7790c7-f3ac-4821-accb-52ed385fd932"
            }
          }
        }
      }'
      ```

      ```json Response
      {
        "resources": [
          {
            "id": "ffedf9dc-932d-439f-b1f4-e42b2a5a8bb5",
            "revision": "1",
            "createdDate": "2024-11-22T13:36:47.104Z",
            "updatedDate": "2024-11-22T13:36:47.104Z",
            "name": "Meeting room with conference system",
            "typeId": "dd7790c7-f3ac-4821-accb-52ed385fd932",
            "locationOptions": {
              "availableInAllLocations": false,
              "specificLocationOptions": {
                "availableInBusinessLocations": true,
                "businessLocations": [
                  {
                    "locationId": "1295e5ff-ecb3-41bc-8b6f-7345792e5ec0"
                  }
                ]
              }
            },
            "eventsSchedule": {
              "scheduleId": "e263b1ca-a1e9-45bf-a39c-044e97c63500"
            }
          },
          {
            "id": "ef5f8d3d-9269-490c-a067-a3d226fcb5d8",
            "revision": "1",
            "createdDate": "2024-11-22T14:02:34.993Z",
            "updatedDate": "2024-11-22T14:02:34.993Z",
            "name": "Small meeting room",
            "typeId": "dd7790c7-f3ac-4821-accb-52ed385fd932",
            "locationOptions": {
              "availableInAllLocations": false,
              "specificLocationOptions": {
                "availableInBusinessLocations": true,
                "businessLocations": [
                  {
                    "locationId": "808dfb00-0a74-4e70-bb3b-f96ff6ba1246"
                  }
                ]
              }
            },
            "eventsSchedule": {
              "scheduleId": "ee500718-7ce3-478a-89fb-1d839a37e324"
            }
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of resources matching the provided search criteria.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `name` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { resources } from '@wix/bookings';

    resources.searchResources(search): Promise<SearchResourcesResponse>
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search criteria including filter, sort, aggregations, and paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object for narrowing search results. For example, to return only resources available at specific business locations: `"filter": {"single_resource.location_options.specific_location_options.business_locations.location_id": {"$in": ["location-id-1", "location-id-2"]}}`.

          Learn more about the filter format in the [supported filters article](https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resources-v2/filtering-and-sorting).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Array of sort objects specifying result order. For example, to sort by resource name in ascending order: `"sort": [{"fieldName": "name", "order": "ASC"}]`.

          Learn more about the sort format in the [supported filters article](https://dev.wix.com/docs/rest/business-solutions/bookings/resources/resources-v2/filtering-and-sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations for grouping data into categories (facets) and providing summaries for each category.
          For example, use aggregations to categorize search results by resource type, management type, or location availability.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation. Client must specify matching aggregation field below.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into 1 key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous 1.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example `name` or `locationOptions.businessLocations.locationId`.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="groupBy" type="GroupByAggregation" deprecated>

              Deprecated. Use `nested` instead.

              **Deprecated.** Deprecated. Use `nested` instead. Use `nested` instead. Removal: 2024-03-30.

              _Nested structure omitted for brevity — see the `GroupByAggregation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least 1 of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `locationOptions.availableInAllLocations`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone in [IANA tz database format](https://en.wikipedia.org/wiki/Tz_database) or [ISO 8601 UTC offset format](https://en.wikipedia.org/wiki/ISO_8601#Time_offsets_from_UTC) for adjusting time fields in the specified filters and returned aggregation data. For example, `America/New_York`, `UTC`, or `+02:00`.

          Default: Time zone specified in the business [site properties](https://dev.wix.com/docs/rest/business-management/site-properties/properties/get-site-properties).

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor-based paging for result navigation. When requesting `cursor_paging.cursor`,
              `filter`, `sort`, or `search` can't be specified.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<SearchResourcesResponse>`.

    <ResponseField name="resources" type="Resource[]">

      Retrieved resources that match the search criteria specified in the request.

      Each resource includes all available information such as name, type, management details,
      location options, working hour schedules, and availability configurations.

      See the [Resource Object](/api-reference/business-solutions/bookings/resources/resources-v2/resource-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor-based paging metadata for navigating through search results.

      Contains navigation information including the current page cursor, whether additional
      results are available, and result counts. Use the `next` cursor to fetch subsequent pages.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current response page.

          This count reflects the actual number of items in the current result set,
          which may be less than the requested limit if fewer items are available.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Navigation cursors for moving between result pages.

          Contains `next` and `prev` cursor tokens for pagination. Use the `next` cursor
          to retrieve subsequent pages and `prev` cursor to go back to previous pages.
          Learn more about cursor paging in the [API Query Language guide](https://dev.wix.com/api/rest/getting-started/api-query-language#getting-started_api-query-language_cursor-paging).

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor token for retrieving the next page of results.

              Use this token in subsequent requests to continue pagination forward.
              Value is `null` when on the last page of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor token for retrieving the previous page of results.

              Use this token to navigate backwards through result pages.
              Value is `null` when on the first page of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates whether additional results are available beyond the current page.

          - `true`: More results exist and can be retrieved using the `next` cursor.
          - `false`: This is the final page of results.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation results derived from the aggregations specified in the search request.

      Provides analytical summaries such as resource counts by type, location distribution,
      management type statistics, or custom groupings. Only populated when aggregations
      are included in the search criteria.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Array of aggregation results, each containing the aggregation metadata and its calculated values for the specified search criteria.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match specified kind as derived from search request.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into 1 key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous 1.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Path to the field to aggregate by in dot notation. For example `name` or `locationOptions.businessLocations.locationId`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-resources" id="ex-sdk-search-resources-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-resources" id="ex-sdk-search-resources-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-resources-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-resources-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">searchResources</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchResources (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-resources-0"><span className="wix-picker-name">searchResources</span></label>
    <label htmlFor="ex-sdk-search-resources-1"><span className="wix-picker-name">searchResources (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript searchResources
    import { resources } from '@wix/bookings';

    async function searchResources(search) {
      const response = await resources.searchResources(search);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchResources (self-hosted)
    import { createClient } from '@wix/sdk';
    import { resources } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { resources },
      // Include the auth strategy and host as relevant
    });

    async function searchResources(search) {
      const response = await myWixClient.resources.searchResources(search);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
