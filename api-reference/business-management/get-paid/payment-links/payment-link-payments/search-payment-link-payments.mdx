---
title: "Search Payment Link Payments"
description: "Retrieves a list of up to 100 payment links, given the specified filtering, search expression, sorting, and cursor paging (SDK | REST)."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 payment links, given the specified filtering, search expression, sorting, and cursor paging ([SDK](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/api-query-language) | [REST](https://dev.wix.com/api/rest/getting-started/api-query-language)).

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `amount` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `giftCardPaymentLinkPayment.contactDetails.vatId.type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.contactDetails.vatId.type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.method` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.order.type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.paymentMethod` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `regularPaymentLinkPayment.paymentProvider` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `transactionDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `giftCardPaymentLinkPayment.contactDetails.vatId.id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `giftCardPaymentLinkPayment.ecomOrderId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `paymentLinkId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `regularPaymentLinkPayment.contactDetails.vatId.id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `regularPaymentLinkPayment.order.ecomOrder.ecomOrderId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `giftCardPaymentLinkPayment.contactDetails.company` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `giftCardPaymentLinkPayment.contactDetails.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `giftCardPaymentLinkPayment.contactDetails.firstName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `giftCardPaymentLinkPayment.contactDetails.fullName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `giftCardPaymentLinkPayment.contactDetails.lastName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.company` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.firstName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.fullName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.lastName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/payment-links/v1/payment-link-payments/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch" required>

      Search options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation to perform. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
          Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="paymentLinkPayments" type="PaymentLinkPayment[]">

      Retrieved payment link payments.

      See the [Payment Link Payment Object](/api-reference/business-management/get-paid/payment-links/payment-link-payments/payment-link-payment-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Response aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          List of the aggregated data results.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Aggregation type. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves relevant payment link payments, given the provided paging, filtering, sorting and aggregations.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/payment-links/v1/payment-link-payments/search' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "search": {
            "sort": [
              {
                "fieldName": "createdDate",
                "order": "DESC"
              }
            ],
            "search": {
              "expression": "Joh",
              "fields": [
                "regularPaymentLinkPayment.contactDetails.firstName"
              ],
              "fuzzy": true
            },
            "cursorPaging": {
              "limit": 1
            }
          }
        }'
      ```

      ```json Response
      {
        "paymentLinkPayments": [
          {
            "id": "afb2f6c0-e497-4c69-9ddb-be8c82351456",
            "revision": "2",
            "createdDate": "2025-01-21T09:18:11.648Z",
            "amount": "10.99",
            "paymentLinkId": "6a206d71-52f5-4012-b3dd-8a283fea052e",
            "receiptId": "92161421-7897-40ed-9af5-5bc3b1f1fdeb",
            "type": "REGULAR",
            "regularPaymentLinkPayment": {
              "paymentMethod": "creditCard",
              "paymentProvider": "Credit/Debit Cards",
              "contactDetails": {
                "firstName": "John",
                "lastName": "Doe",
                "phone": "0123456789",
                "company": "Green Car Toys",
                "email": "business@example.com",
                "vatId": {
                  "id": "12345678",
                  "type": "CPF"
                },
                "fullName": "John Doe"
              },
              "billingAddress": {
                "country": "US",
                "subdivision": "NY",
                "city": "New York City",
                "postalCode": "10000",
                "streetAddress": {
                  "number": "1234",
                  "name": "Example",
                  "apt": "12A"
                },
                "formattedAddress": "NY, Example 12A"
              },
              "creditCardPaymentMethodData": {
                "network": "visa",
                "maskedCreditCard": "**** **** **** 5678",
                "holderName": "John Doe"
              },
              "order": {
                "type": "ECOM",
                "ecomOrder": {
                  "ecomOrderId": "095159f9-9f44-4ce0-a06f-312d59d56760"
                }
              },
              "method": "CREDIT_CARD"
            },
            "currencyCode": "USD",
            "ecomCheckout": {
              "ecomCheckoutId": "e19d84b5-dd8d-4621-92af-abc04b8903d9"
            }
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "total": 2,
          "cursors": {
            "next": "d276f86642fc0c1d70f45f611a860cc3c54be2d7.EhoKC2NyZWF0ZWREYXRlEAEaCREAkDZJiEh5QhIJGgUaAzEuMCABEioaJhokODM1NDYwMzEtYjExNC00MTk2LTk3NzItOTAyZTQzZTU3MGU1IAFQAVo7bmlsZS93aXgucGF5bWVudGxpbmtzLnBheW1lbnRzLnYxLnBheW1lbnRfbGlua19wYXltZW50LXBvYzJiJgokYzRhM2I0MWYtN2FhZC00MGI0LWE4YmItNGM1MzE3ZTQwNWMzagIIDw"
          },
          "hasNext": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 payment links, given the specified filtering, search expression, sorting, and cursor paging ([SDK](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/api-query-language) | [REST](https://dev.wix.com/api/rest/getting-started/api-query-language)).

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `amount` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `giftCardPaymentLinkPayment.contactDetails.vatId.type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.contactDetails.vatId.type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.method` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.order.type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `regularPaymentLinkPayment.paymentMethod` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `regularPaymentLinkPayment.paymentProvider` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `transactionDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `type` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `giftCardPaymentLinkPayment.ecomOrderId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `paymentLinkId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `regularPaymentLinkPayment.contactDetails.vatId._id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `regularPaymentLinkPayment.order.ecomOrder.ecomOrderId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `giftCardPaymentLinkPayment.contactDetails.company` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `giftCardPaymentLinkPayment.contactDetails.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `giftCardPaymentLinkPayment.contactDetails.firstName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `giftCardPaymentLinkPayment.contactDetails.lastName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.company` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.firstName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |
    | `regularPaymentLinkPayment.contactDetails.lastName` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { paymentLinkPayments } from '@wix/get-paid';

    paymentLinkPayments.searchPaymentLinkPayments(search): Promise<SearchPaymentLinkPaymentsResponse>
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Logical groupings of data into facets, with summaries for each facet. For example, use aggregations to allow site visitors to narrow down their search results by selecting specific categories.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation to perform. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          Time zone to adjust date-time-based filters and aggregations, in ISO 8601 (including offsets) or IANA time zone database (including time zone IDs) format.
          Applies to all relevant filters and aggregations, unless overridden by providing timestamps including time zone. For example, "2023-12-20T10:52:34.795Z".

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<SearchPaymentLinkPaymentsResponse>`.

    <ResponseField name="paymentLinkPayments" type="PaymentLinkPayment[]">

      Retrieved payment link payments.

      See the [Payment Link Payment Object](/api-reference/business-management/get-paid/payment-links/payment-link-payments/payment-link-payment-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Response aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          List of the aggregated data results.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              Aggregation name, returned in `aggregations.results.name`.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Aggregation type. Must align with the corresponding aggregation field.

              **Supported values:**

              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into one key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.
              - `DATE_HISTOGRAM` — Calculates the count of time values from the specified field in the dataset that fall within each time interval you define (hour, day, week, etc.).
              - `NESTED` — Flattened list of aggregations, where each aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-payment-link-payments" id="ex-sdk-search-payment-link-payments-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-payment-link-payments" id="ex-sdk-search-payment-link-payments-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-search-payment-link-payments" id="ex-sdk-search-payment-link-payments-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-payment-link-payments-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-payment-link-payments-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">searchPaymentLinkPayments</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchPaymentLinkPayments (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchPaymentLinkPayments (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-payment-link-payments-0"><span className="wix-picker-name">searchPaymentLinkPayments</span></label>
    <label htmlFor="ex-sdk-search-payment-link-payments-1"><span className="wix-picker-name">searchPaymentLinkPayments (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-search-payment-link-payments-2"><span className="wix-picker-name">searchPaymentLinkPayments (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript searchPaymentLinkPayments
    import { paymentLinkPayments } from '@wix/get-paid';

    async function searchPaymentLinkPayments(search) {
      const response = await paymentLinkPayments.searchPaymentLinkPayments(search);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchPaymentLinkPayments (self-hosted)
    import { createClient } from '@wix/sdk';
    import { paymentLinkPayments } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { paymentLinkPayments },
      // Include the auth strategy and host as relevant
    });

    async function searchPaymentLinkPayments(search) {
      const response = await myWixClient.paymentLinkPayments.searchPaymentLinkPayments(search);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchPaymentLinkPayments (with elevated permissions)
    import { paymentLinkPayments } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function mySearchPaymentLinkPaymentsMethod(search) {
      const elevatedSearchPaymentLinkPayments = auth.elevate(paymentLinkPayments.searchPaymentLinkPayments);
      const response = await elevatedSearchPaymentLinkPayments(search);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
