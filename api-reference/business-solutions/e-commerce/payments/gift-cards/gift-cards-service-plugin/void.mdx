---
title: "Void"
description: "This method requests that a gift card transaction be voided by your app."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    This method requests that a gift card transaction be voided by your app.
    Wix calls this method when a purchase fails after gift card redemption.

    ## Endpoint

    ```http
    POST /v1/void
    ```

    ## Body Params

    <ParamField body="appInstanceId" type="string" deprecated>

      App ID of the Gift Card provider. Deprecated.

      **Deprecated.** App ID of the Gift Card provider. Deprecated. Removal: 2025-07-01. Constraints: `format GUID`.

    </ParamField>

    <ParamField body="transactionId" type="string">

      Transaction ID to void.

      Constraints: `minLength 1`, `maxLength 100`.

    </ParamField>

    <ParamField body="locationId" type="string">

      Physical location ID. Can be based on the Locations API or an external provider.

    </ParamField>

    ## Response Object

    <ResponseField name="remainingBalance" type="number">

      Remaining balance on the Gift Card after voiding the transaction.

      Constraints: `min 0`, `max 999999999.99`.

    </ResponseField>

    <ResponseField name="currencyCode" type="string">

      Currency code.

      Constraints: `format CURRENCY`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `TRANSACTION_NOT_FOUND` | Couldn't find the transaction. |
    | `409` | `ALREADY_EXISTS` | `ALREADY_VOIDED` | Transaction was already voided. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_DISABLED` | Gift card is disabled. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_EXPIRED` | Gift card has expired. |
    | `428` | `FAILED_PRECONDITION` | `MISSING_CURRENCY` | Gift card currency is missing. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    The data payload will include the following object as an encoded JWT. For the purposes of this example, we show the request and response objects decoded.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'http://provider.example.com/v1/void' \
        -H 'user-agent: Wix' \
        -H 'accept-encoding: gzip, deflate' \
        -H 'content-type: text/plain; charset=utf-8' \
        -d '{
              "transactionId": "00000000-0000-0000-0000-000000000001"
            }'
      ```

      ```json Response
      {
        "remainingBalance": 100.00,
        "currencyCode": "USD"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    This method requests that a gift card transaction be voided by your app.
    Wix calls this method when a purchase fails after gift card redemption.

    ## Method Declaration

    ```typescript
    import { giftVouchersProvider } from '@wix/ecom';

    giftVouchersProvider.void(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="VoidRequest">

      request

      <Expandable title="request properties">

        <ParamField body="appInstanceId" type="string" deprecated>

          App ID of the Gift Card provider. Deprecated.

          **Deprecated.** App ID of the Gift Card provider. Deprecated. Removal: 2025-07-01. Constraints: `format GUID`.

        </ParamField>

        <ParamField body="transactionId" type="string">

          Transaction ID to void.

          Constraints: `minLength 1`, `maxLength 100`.

        </ParamField>

        <ParamField body="locationId" type="string">

          Physical location ID. Can be based on the Locations API or an external provider.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="remainingBalance" type="number">

      Remaining balance on the Gift Card after voiding the transaction.

      Constraints: `min 0`, `max 999999999.99`.

    </ResponseField>

    <ResponseField name="currencyCode" type="string">

      Currency code.

      Constraints: `format CURRENCY`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `TRANSACTION_NOT_FOUND` | Couldn't find the transaction. |
    | `409` | `ALREADY_EXISTS` | `ALREADY_VOIDED` | Transaction was already voided. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_DISABLED` | Gift card is disabled. |
    | `428` | `FAILED_PRECONDITION` | `GIFT_CARD_EXPIRED` | Gift card has expired. |
    | `428` | `FAILED_PRECONDITION` | `MISSING_CURRENCY` | Gift card currency is missing. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-void" id="ex-sdk-void-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-void" id="ex-sdk-void-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-void" id="ex-sdk-void-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-void-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-void-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">void</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">void (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">void (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-void-0"><span className="wix-picker-name">void</span></label>
    <label htmlFor="ex-sdk-void-1"><span className="wix-picker-name">void (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-void-2"><span className="wix-picker-name">void (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript void
    import { giftVouchersProvider } from '@wix/ecom/service-plugins';

    async function void(request,metadata) {
      const response = await giftVouchersProvider.void(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript void (self-hosted)
    import { createClient } from '@wix/sdk';
    import { giftVouchersProvider } from '@wix/ecom/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { giftVouchersProvider },
      // Include the auth strategy and host as relevant
    });

    async function void(request,metadata) {
      const response = await myWixClient.giftVouchersProvider.void(request,metadata);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript void (with elevated permissions)
    import { giftVouchersProvider } from '@wix/ecom/service-plugins';
    import { auth } from '@wix/essentials';

    async function myVoidMethod(request,metadata) {
      const elevatedVoid = auth.elevate(giftVouchersProvider.void);
      const response = await elevatedVoid(request,metadata);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
