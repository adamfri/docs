---
title: "Delete User Favorite List"
description: "Deletes the Wix user's list of favorite dashboard pages."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the Wix user's list of favorite dashboard pages.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-OS.MANAGE_DASHBOARD">Manage Dashboard</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/dashboard/v1/user-favorite-list/{favoriteListId}
    ```

    ## Path Params

    <ParamField path="favoriteListId" type="string" required>

      ID of the dashboard favorite list to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteUserFavoriteListResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Favorite List Deleted | `wix.dashboard.v1.favorite_list_deleted` | Triggered when the dashboard favorite list is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/dashboard/v1/user-favorite-list/c959d429-3136-41cb-9478-6acbd064e0fa' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the Wix user's list of favorite dashboard pages.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-OS.MANAGE_DASHBOARD">Manage Dashboard</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { favoriteList } from '@wix/dashboard-management';

    favoriteList.deleteUserFavoriteList(favoriteListId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="favoriteListId" type="string" required>

      ID of the dashboard favorite list to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Favorite List Deleted | `wix.dashboard.v1.favorite_list_deleted` | Triggered when the dashboard favorite list is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-user-favorite-list" id="ex-sdk-delete-user-favorite-list-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-user-favorite-list" id="ex-sdk-delete-user-favorite-list-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-user-favorite-list" id="ex-sdk-delete-user-favorite-list-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-user-favorite-list-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-user-favorite-list-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteUserFavoriteList</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteUserFavoriteList (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteUserFavoriteList (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-user-favorite-list-0"><span className="wix-picker-name">deleteUserFavoriteList</span></label>
    <label htmlFor="ex-sdk-delete-user-favorite-list-1"><span className="wix-picker-name">deleteUserFavoriteList (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-user-favorite-list-2"><span className="wix-picker-name">deleteUserFavoriteList (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteUserFavoriteList
    import { favoriteList } from '@wix/dashboard-management';

    async function deleteUserFavoriteList(favoriteListId) {
      const response = await favoriteList.deleteUserFavoriteList(favoriteListId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteUserFavoriteList (self-hosted)
    import { createClient } from '@wix/sdk';
    import { favoriteList } from '@wix/dashboard-management';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { favoriteList },
      // Include the auth strategy and host as relevant
    });

    async function deleteUserFavoriteList(favoriteListId) {
      const response = await myWixClient.favoriteList.deleteUserFavoriteList(favoriteListId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteUserFavoriteList (with elevated permissions)
    import { favoriteList } from '@wix/dashboard-management';
    import { auth } from '@wix/essentials';

    async function myDeleteUserFavoriteListMethod(favoriteListId) {
      const elevatedDeleteUserFavoriteList = auth.elevate(favoriteList.deleteUserFavoriteList);
      const response = await elevatedDeleteUserFavoriteList(favoriteListId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
