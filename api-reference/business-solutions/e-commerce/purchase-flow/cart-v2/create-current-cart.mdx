---
title: "Create Current Cart"
description: "Creates the customer's current cart."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates the customer's current cart.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ECOM.WRITE-CARTS-V2">Write Carts V2</span>
    <span className="wix-scope" title="SCOPE.ECOM.MANAGE-ADMIN">Manage eCommerce - Admin Permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/ecom/v2/carts/current
    ```

    ## Body Params

    <ParamField body="cart" type="Cart">

      Cart to create.

      See the [Cart Object](/api-reference/business-solutions/e-commerce/purchase-flow/cart-v2/cart-object) for this object's fields.

    </ParamField>

    <ParamField body="catalogItems" type="CatalogItemInput[]">

      A list of catalog items to add to the current cart.

      Constraints: `maxItems 300`.

      <Expandable title="catalogItems properties">

        <ParamField body="catalogReference" type="CatalogReference" required>

          Reference to the catalog item.

          <Expandable title="catalogReference properties">

            <ParamField body="catalogItemId" type="string">

              ID of the item within the catalog it belongs to.

              Constraints: `minLength 1`, `maxLength 36`.

            </ParamField>

            <ParamField body="appId" type="string">

              ID of the app providing the catalog.

              You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

              For items from Wix catalogs, the following values always apply:
              + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
              + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
              + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

              Constraints: `minLength 1`.

            </ParamField>

            <ParamField body="options" type="object">

              Additional item details in `key:value` pairs.

              Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

              For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
              or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="quantity" type="integer" required>

          Number of catalog items the customer requested.

          Constraints: `min 1`, `max 100000`.

        </ParamField>

        <ParamField body="selectedMembership" type="SelectedMembership" deprecated>

          Selected membership used as payment for this item.

          DEPRECATED: migrate to `membershipPayment`. Removed once all clients migrate.

          **Deprecated.** Use `membership_payment` instead. Removal: 2026-06-22.

          <Expandable title="selectedMembership properties">

            <ParamField body="id" type="string" required>

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ParamField>

            <ParamField body="appId" type="string" required>

              ID of the app providing this payment option.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="name" type="TranslatableString">

              Display name of the selected membership.

              **Read-only.**

              _Nested structure omitted for brevity — see the `TranslatableString` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="catalogOverrideFields" type="CatalogOverrideFields">

          Overriding values for catalog item properties.

          <Expandable title="catalogOverrideFields properties">

            <ParamField body="name" type="TranslatableString">

              Display name for the line item.

              _Nested structure omitted for brevity — see the `TranslatableString` object._

            </ParamField>

            <ParamField body="salePrice" type="string">

              Sale price for a single item after modifiers, before discounts.

            </ParamField>

            <ParamField body="fullPrice" type="string">

              Full price for a single item after modifiers, before discounts.

            </ParamField>

            <ParamField body="descriptionLines" type="DescriptionLine[]">

              Description lines for the item.
              Shown when displaying the line item to buyers.

              Constraints: `minItems 0`, `maxItems 20`.

              _Nested structure omitted for brevity — see the `DescriptionLine[]` object._

            </ParamField>

            <ParamField body="physicalProperties" type="PhysicalProperties">

              Physical properties of the item.

              _Nested structure omitted for brevity — see the `PhysicalProperties` object._

            </ParamField>

            <ParamField body="image" type="Image">

              Item image.

              _Nested structure omitted for brevity — see the `Image` object._

            </ParamField>

            <ParamField body="paymentOption" type="PaymentOption">

              Selected payment option for the item.

              _Nested structure omitted for brevity — see the `PaymentOption` object._

            </ParamField>

            <ParamField body="depositAmount" type="string">

              Partial payment due upfront during checkout.
              Only when `paymentOption.value` is `DEPOSIT_ONLINE`.

            </ParamField>

            <ParamField body="savePaymentMethod" type="boolean">

              Whether to save the payment method on the order.

              Default: `false`

            </ParamField>

            <ParamField body="inventoryAppId" type="string">

              ID of the app managing inventory.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom extended fields for the line item object.

          [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured using the Checkout & Orders - Line Items Schema Plugin in the app dashboard before they can be accessed with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="inventoryAppId" type="string">

          ID of the app managing inventory.

        </ParamField>

        <ParamField body="membershipPayment" type="MembershipInput">

          Membership used as payment for this item. Can be an existing membership, or a future one
          created from a plan in the same cart.

          Replaces `selectedMembership`.

          Use with `paymentOption` `MEMBERSHIP` or `MEMBERSHIP_OFFLINE`.

          **Developer Preview.**

          <Expandable title="One of the following">

            <ParamField body="membership" type="membership">

              membership

              _Nested structure omitted for brevity — see the `membership` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="customItems" type="CustomItemInput[]">

      A list of custom items to add to the current cart.
      Custom line items don't trigger the Catalog service plugin.

      Constraints: `maxItems 300`.

      <Expandable title="customItems properties">

        <ParamField body="name" type="TranslatableString" required>

          Display name for the line item.

          <Expandable title="name properties">

            <ParamField body="original" type="string">

              __Required.__ String in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

              Min: 1 character.
              Max: 200 characters.

              Constraints: `minLength 1`, `maxLength 200`.

            </ParamField>

            <ParamField body="translated" type="string">

              String translated into the buyer's language.

              Min: 1 character.
              Max: 400 characters.
              Default: Same as `original`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="quantityInfo" type="CustomItemQuantityInfo" required>

          Quantity details for the line item.

          <Expandable title="quantityInfo properties">

            <ParamField body="requestedQuantity" type="integer" required>

              Quantity the customer requested.

              Constraints: `min 1`, `max 100000`.

            </ParamField>

            <ParamField body="availableQuantity" type="integer">

              Maximum quantity available for purchase.

            </ParamField>

            <ParamField body="fixedQuantity" type="boolean">

              Whether the line item quantity is fixed and can't be changed.

              Default: `false`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="pricing" type="CustomItemPricingInfo" required>

          Pricing details for the line item.

          <Expandable title="pricing properties">

            <ParamField body="price" type="ConvertedMoney" required>

              Sale price for a single item, without modifiers.
              Includes tax when `taxInfo.pricesIncludeTax` is true.

              _Nested structure omitted for brevity — see the `ConvertedMoney` object._

            </ParamField>

            <ParamField body="fullPrice" type="ConvertedMoney">

              Full price for a single item before discounts, without modifiers.
              Defaults to `price` when not provided.
              Includes tax when `taxInfo.pricesIncludeTax` is true.

              _Nested structure omitted for brevity — see the `ConvertedMoney` object._

            </ParamField>

            <ParamField body="priceDescription" type="PriceDescription">

              Additional price description.
              For example, when the price is `0` but you need more detail, such as "Starts at $67".

              _Nested structure omitted for brevity — see the `PriceDescription` object._

            </ParamField>

            <ParamField body="priceUndetermined" type="boolean">

              Whether the price isn't yet defined and will be updated after the order is created.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="source" type="CustomItemSource">

          Information about the line item's origin.

          <Expandable title="source properties">

            <ParamField body="catalogReference" type="CatalogReference">

              Catalog and item reference: IDs for the item and catalog, plus optional details.
              Optional for custom line items, which don't trigger the Catalog service plugin.

              _Nested structure omitted for brevity — see the `CatalogReference` object._

            </ParamField>

            <ParamField body="rootCatalogItemId" type="string">

              When `catalogReference.catalogItemId` isn't the actual catalog item ID, this field returns the true catalog item ID.
              For example, for Wix Bookings, `catalogReference.catalogItemId` is the booking ID, so this value is the service ID.
              In most cases, this matches `catalogReference.catalogItemId`.
              Used in membership validation.

            </ParamField>

            <ParamField body="inventoryAppId" type="string">

              ID of the app managing inventory.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="attributes" type="CustomItemAttributes" required>

          Display details for the item.

          <Expandable title="attributes properties">

            <ParamField body="descriptionLines" type="DescriptionLine[]">

              Description lines for the item.

              Constraints: `maxItems 100`.

              _Nested structure omitted for brevity — see the `DescriptionLine[]` object._

            </ParamField>

            <ParamField body="image" type="Image">

              Item image.

              _Nested structure omitted for brevity — see the `Image` object._

            </ParamField>

            <ParamField body="url" type="PageUrlV2">

              URL of the item's page on the site.

              _Nested structure omitted for brevity — see the `PageUrlV2` object._

            </ParamField>

            <ParamField body="itemType" type="ItemType" required>

              Item type: preset or custom.

              _Nested structure omitted for brevity — see the `ItemType` object._

            </ParamField>

            <ParamField body="physicalProperties" type="PhysicalProperties">

              Physical properties of the item.
              When relevant, includes SKU, weight, and shippability.

              _Nested structure omitted for brevity — see the `PhysicalProperties` object._

            </ParamField>

            <ParamField body="serviceProperties" type="ServiceProperties">

              Service properties.
              When relevant, includes date and number of participants.

              _Nested structure omitted for brevity — see the `ServiceProperties` object._

            </ParamField>

            <ParamField body="membersOnly" type="boolean">

              Whether the item can only be purchased by site members.

              Default: `false`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="deliveryConfig" type="CustomItemDeliveryConfig">

          Delivery configuration for the line item.

          <Expandable title="deliveryConfig properties">

            <ParamField body="deliveryProfileId" type="string">

              Delivery profile ID.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="taxConfig" type="CustomItemTaxConfig">

          Tax configuration for the line item.

          <Expandable title="taxConfig properties">

            <ParamField body="taxableAddress" type="TaxableAddress">

              Address used for tax calculation.

              _Nested structure omitted for brevity — see the `TaxableAddress` object._

            </ParamField>

            <ParamField body="taxGroupId" type="string">

              Tax group ID for this line item.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paymentConfig" type="CustomItemPaymentConfig">

          Payment configuration for the line item.

          <Expandable title="paymentConfig properties">

            <ParamField body="depositAmount" type="ConvertedMoney">

              Partial payment due upfront during checkout.
              Only when `paymentOption` is `DEPOSIT_ONLINE`.

              _Nested structure omitted for brevity — see the `ConvertedMoney` object._

            </ParamField>

            <ParamField body="paymentOption" type="string">

              Selected payment option type for this item.

              Default: `FULL_PAYMENT_ONLINE`

              **Supported values:**

              - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
              - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
              - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
              - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
              - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
              - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="modifierGroups" type="ModifierGroup[]">

          Modifier groups added to the item.

          Constraints: `maxItems 25`.

          <Expandable title="modifierGroups properties">

            <ParamField body="id" type="string">

              Modifier group ID.

              **Read-only.** Constraints: `minLength 1`, `maxLength 36`.

            </ParamField>

            <ParamField body="name" type="TranslatableString">

              Modifier group name.

              _Nested structure omitted for brevity — see the `TranslatableString` object._

            </ParamField>

            <ParamField body="modifiers" type="ItemModifier[]">

              Modifiers in this group.

              Constraints: `minItems 1`, `maxItems 25`.

              _Nested structure omitted for brevity — see the `ItemModifier[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="coupons" type="CouponInput[]">

      Coupons to apply.
      Currently only one is supported.

      Constraints: `maxItems 1`.

      <Expandable title="coupons properties">

        <ParamField body="code" type="string">

          Coupon code.

          Constraints: `minLength 1`, `maxLength 50`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="deliveryMethod" type="DeliveryMethodInput">

      Delivery method to set.

      <Expandable title="deliveryMethod properties">

        <ParamField body="code" type="string">

          Delivery method code.

          Constraints: `maxLength 100`.

        </ParamField>

        <ParamField body="appId" type="string">

          Carrier app id.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="giftCards" type="GiftCardInput[]">

      Gift cards to add.

      Constraints: `maxItems 5`.

      <Expandable title="giftCards properties">

        <ParamField body="code" type="string">

          Gift card code.

          Constraints: `minLength 8`, `maxLength 20`.

        </ParamField>

        <ParamField body="redeemAmount" type="ConvertedMoney">

          The amount requested to redeem from this gift card.

          <Expandable title="redeemAmount properties">

            <ParamField body="amount" type="string">

              Monetary amount in the original currency, as specified in `businessInfo.currencyCode`.

              Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 999999999`.

            </ParamField>

            <ParamField body="convertedAmount" type="string">

              Monetary amount in the converted currency, as specified in `customerInfo.currencyCode`.

              Default: `amount`.

              **Read-only.** Constraints: `format DECIMAL_VALUE`, `>= 0`, `<= 999999999`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="cart" type="Cart">

      The customer's current cart.

      See the [Cart Object](/api-reference/business-solutions/e-commerce/purchase-flow/cart-v2/cart-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `ITEM_NOT_FOUND_IN_CATALOG` | One or more items weren't found in the catalog. |
    | `428` | `FAILED_PRECONDITION` | `INSUFFICIENT_INVENTORY` | Insufficient inventory for one or more items. |
    | `400` | `INVALID_ARGUMENT` | `MAX_LINE_ITEMS_EXCEEDED` | Number of line items exceeds the cart's limit. |
    | `400` | `INVALID_ARGUMENT` | `MAX_ITEM_MODIFIERS_EXCEEDED` | Number of modifier groups exceeds the item's limit. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_MODIFIER_GROUP_IDS` | Duplicate modifier group IDs in the request. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_ITEM_MODIFIER_IDS` | Duplicate item modifier IDs within a modifier group. |
    | `428` | `FAILED_PRECONDITION` | `LINE_ITEM_PRICE_CONFLICT` | The item's price conflicts with the catalog price. |
    | `428` | `FAILED_PRECONDITION` | `FIXED_QUANTITY_ITEM` | Can't change the quantity of a fixed-quantity item. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_MEMBERSHIP_PAYMENT_OPTION` | The selected membership payment option is invalid. |
    | `400` | `INVALID_ARGUMENT` | `PLAN_LINE_ITEM_NOT_FOUND` | A future membership references a plan line item that wasn't found in the cart. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_COUPON_CODE` | The coupon code is invalid. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_COUPON_STATUS` | The coupon isn't currently active. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_GIFT_CARD_CODE` | The gift card code is invalid. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_GIFT_CARD_STATUS` | The gift card isn't currently active. |
    | `400` | `INVALID_ARGUMENT` | `DELIVERY_METHOD_NOT_FOUND` | Couldn't find the delivery method. |
    | `428` | `FAILED_PRECONDITION` | `DELIVERY_METHOD_MISSING_ADDRESS` | A delivery address is required for the selected delivery method. |
    | `428` | `FAILED_PRECONDITION` | `DELIVERY_METHOD_MISSING_LINE_ITEMS` | The cart must have line items before setting a delivery method. |
    | `428` | `FAILED_PRECONDITION` | `DEPOSIT_AMOUNT_GREATER_THAN_UNIT_PRICE` | The deposit amount exceeds the item's unit price. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Cart Created | `wix.ecom.v2.cart_created` | Triggered when a cart is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Creates a current cart for the site visitor or member making the call.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/ecom/v2/carts/current' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{}'
      ```

      ```json Response
      {
        "cart": {
          "id": "ca727402-d5fb-4de4-afcd-e72738aad964",
          "createdDate": "2026-07-28T15:18:43.174Z",
          "updatedDate": "2026-07-28T15:18:43.174Z",
          "revision": "1",
          "lineItems": [],
          "subtotal": {
            "amount": "0",
            "convertedAmount": "0"
          },
          "coupons": [],
          "businessInfo": {
            "languageCode": "en",
            "currencyCode": "USD"
          },
          "customerInfo": {
            "visitorId": "74950359-4b2c-4fb4-bb36-a3ad6f812cc9",
            "languageCode": "en",
            "currencyCode": "USD"
          },
          "deliveryInfo": {
            "weightUnit": "KG"
          },
          "taxInfo": {
            "pricesIncludeTax": false
          },
          "paymentInfo": {
            "currencyCode": "USD"
          },
          "orderPlaced": false,
          "purchaseFlowId": "cb0526d7-56d4-4e97-bc00-2b492f8771a1",
          "currentCart": true,
          "demo": false,
          "source": {
            "channelType": "WEB",
            "createdBy": {
              "visitorId": "74950359-4b2c-4fb4-bb36-a3ad6f812cc9"
            }
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates the customer's current cart.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ECOM.WRITE-CARTS-V2">Write Carts V2</span>
    <span className="wix-scope" title="SCOPE.ECOM.MANAGE-ADMIN">Manage eCommerce - Admin Permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { currentCartV2 } from '@wix/ecom';

    currentCartV2.createCurrentCart(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="CreateCurrentCartOptions">

      options

      <Expandable title="options properties">

        <ParamField body="cart" type="Cart">

          Cart to create.

          See the [Cart Object](/api-reference/business-solutions/e-commerce/purchase-flow/cart-v2/cart-object) for this object's fields.

        </ParamField>

        <ParamField body="catalogItems" type="CatalogItemInput[]">

          A list of catalog items to add to the current cart.

          Constraints: `maxItems 300`.

          <Expandable title="catalogItems properties">

            <ParamField body="catalogReference" type="CatalogReference" required>

              Reference to the catalog item.

              _Nested structure omitted for brevity — see the `CatalogReference` object._

            </ParamField>

            <ParamField body="quantity" type="integer" required>

              Number of catalog items the customer requested.

              Constraints: `min 1`, `max 100000`.

            </ParamField>

            <ParamField body="selectedMembership" type="SelectedMembership" deprecated>

              Selected membership used as payment for this item.

              DEPRECATED: migrate to `membershipPayment`. Removed once all clients migrate.

              **Deprecated.** Use `membership_payment` instead. Removal: 2026-06-22.

              _Nested structure omitted for brevity — see the `SelectedMembership` object._

            </ParamField>

            <ParamField body="catalogOverrideFields" type="CatalogOverrideFields">

              Overriding values for catalog item properties.

              _Nested structure omitted for brevity — see the `CatalogOverrideFields` object._

            </ParamField>

            <ParamField body="extendedFields" type="ExtendedFields">

              Custom extended fields for the line item object.

              [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured using the Checkout & Orders - Line Items Schema Plugin in the app dashboard before they can be accessed with API calls.

              _Nested structure omitted for brevity — see the `ExtendedFields` object._

            </ParamField>

            <ParamField body="inventoryAppId" type="string">

              ID of the app managing inventory.

            </ParamField>

            <ParamField body="membershipPayment" type="MembershipInput">

              Membership used as payment for this item. Can be an existing membership, or a future one
              created from a plan in the same cart.

              Replaces `selectedMembership`.

              Use with `paymentOption` `MEMBERSHIP` or `MEMBERSHIP_OFFLINE`.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `MembershipInput` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="customItems" type="CustomItemInput[]">

          A list of custom items to add to the current cart.
          Custom line items don't trigger the Catalog service plugin.

          Constraints: `maxItems 300`.

          <Expandable title="customItems properties">

            <ParamField body="name" type="TranslatableString" required>

              Display name for the line item.

              _Nested structure omitted for brevity — see the `TranslatableString` object._

            </ParamField>

            <ParamField body="quantityInfo" type="CustomItemQuantityInfo" required>

              Quantity details for the line item.

              _Nested structure omitted for brevity — see the `CustomItemQuantityInfo` object._

            </ParamField>

            <ParamField body="pricing" type="CustomItemPricingInfo" required>

              Pricing details for the line item.

              _Nested structure omitted for brevity — see the `CustomItemPricingInfo` object._

            </ParamField>

            <ParamField body="source" type="CustomItemSource">

              Information about the line item's origin.

              _Nested structure omitted for brevity — see the `CustomItemSource` object._

            </ParamField>

            <ParamField body="attributes" type="CustomItemAttributes" required>

              Display details for the item.

              _Nested structure omitted for brevity — see the `CustomItemAttributes` object._

            </ParamField>

            <ParamField body="deliveryConfig" type="CustomItemDeliveryConfig">

              Delivery configuration for the line item.

              _Nested structure omitted for brevity — see the `CustomItemDeliveryConfig` object._

            </ParamField>

            <ParamField body="taxConfig" type="CustomItemTaxConfig">

              Tax configuration for the line item.

              _Nested structure omitted for brevity — see the `CustomItemTaxConfig` object._

            </ParamField>

            <ParamField body="paymentConfig" type="CustomItemPaymentConfig">

              Payment configuration for the line item.

              _Nested structure omitted for brevity — see the `CustomItemPaymentConfig` object._

            </ParamField>

            <ParamField body="modifierGroups" type="ModifierGroup[]">

              Modifier groups added to the item.

              Constraints: `maxItems 25`.

              _Nested structure omitted for brevity — see the `ModifierGroup[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="coupons" type="CouponInput[]">

          Coupons to apply.
          Currently only one is supported.

          Constraints: `maxItems 1`.

          <Expandable title="coupons properties">

            <ParamField body="code" type="string">

              Coupon code.

              Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="deliveryMethod" type="DeliveryMethodInput">

          Delivery method to set.

          <Expandable title="deliveryMethod properties">

            <ParamField body="code" type="string">

              Delivery method code.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="appId" type="string">

              Carrier app id.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="giftCards" type="GiftCardInput[]">

          Gift cards to add.

          Constraints: `maxItems 5`.

          <Expandable title="giftCards properties">

            <ParamField body="code" type="string">

              Gift card code.

              Constraints: `minLength 8`, `maxLength 20`.

            </ParamField>

            <ParamField body="redeemAmount" type="ConvertedMoney">

              The amount requested to redeem from this gift card.

              _Nested structure omitted for brevity — see the `ConvertedMoney` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="cart" type="Cart">

      The customer's current cart.

      See the [Cart Object](/api-reference/business-solutions/e-commerce/purchase-flow/cart-v2/cart-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `ITEM_NOT_FOUND_IN_CATALOG` | One or more items weren't found in the catalog. |
    | `428` | `FAILED_PRECONDITION` | `INSUFFICIENT_INVENTORY` | Insufficient inventory for one or more items. |
    | `400` | `INVALID_ARGUMENT` | `MAX_LINE_ITEMS_EXCEEDED` | Number of line items exceeds the cart's limit. |
    | `400` | `INVALID_ARGUMENT` | `MAX_ITEM_MODIFIERS_EXCEEDED` | Number of modifier groups exceeds the item's limit. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_MODIFIER_GROUP_IDS` | Duplicate modifier group IDs in the request. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_ITEM_MODIFIER_IDS` | Duplicate item modifier IDs within a modifier group. |
    | `428` | `FAILED_PRECONDITION` | `LINE_ITEM_PRICE_CONFLICT` | The item's price conflicts with the catalog price. |
    | `428` | `FAILED_PRECONDITION` | `FIXED_QUANTITY_ITEM` | Can't change the quantity of a fixed-quantity item. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_MEMBERSHIP_PAYMENT_OPTION` | The selected membership payment option is invalid. |
    | `400` | `INVALID_ARGUMENT` | `PLAN_LINE_ITEM_NOT_FOUND` | A future membership references a plan line item that wasn't found in the cart. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_COUPON_CODE` | The coupon code is invalid. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_COUPON_STATUS` | The coupon isn't currently active. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_GIFT_CARD_CODE` | The gift card code is invalid. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_GIFT_CARD_STATUS` | The gift card isn't currently active. |
    | `400` | `INVALID_ARGUMENT` | `DELIVERY_METHOD_NOT_FOUND` | Couldn't find the delivery method. |
    | `428` | `FAILED_PRECONDITION` | `DELIVERY_METHOD_MISSING_ADDRESS` | A delivery address is required for the selected delivery method. |
    | `428` | `FAILED_PRECONDITION` | `DELIVERY_METHOD_MISSING_LINE_ITEMS` | The cart must have line items before setting a delivery method. |
    | `428` | `FAILED_PRECONDITION` | `DEPOSIT_AMOUNT_GREATER_THAN_UNIT_PRICE` | The deposit amount exceeds the item's unit price. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Cart Created | `wix.ecom.v2.cart_created` | Triggered when a cart is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-current-cart" id="ex-sdk-create-current-cart-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-current-cart" id="ex-sdk-create-current-cart-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-current-cart-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-current-cart-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create the current cart</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCurrentCart (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-current-cart-0"><span className="wix-picker-name">Create the current cart</span></label>
    <label htmlFor="ex-sdk-create-current-cart-1"><span className="wix-picker-name">createCurrentCart (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create the current cart
    import { currentCartV2 } from "@wix/ecom";

    async function createCurrentCart() {
      const response = await currentCartV2.createCurrentCart({
        catalogItems: [
          {
            catalogReference: {
              catalogItemId: "d76664e8-6b62-49ec-9f1d-b03fb4a1b446",
              appId: "215238eb-22a5-4c36-9e7b-e7c08025e04e",
            },
            quantity: 1,
          },
        ],
      });
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCurrentCart (self-hosted)
    import { createClient } from '@wix/sdk';
    import { currentCartV2 } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { currentCartV2 },
      // Include the auth strategy and host as relevant
    });

    async function createCurrentCart(options) {
      const response = await myWixClient.currentCartV2.createCurrentCart(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
