---
title: "onPaymentLinkSent"
description: "Triggered when a payment link is sent."
icon: "bolt"
---

Triggered when a payment link is sent.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.paymentlinks.v1.payment_link_payment_link_sent` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Paylinks` |

## Examples

<CodeGroup>

  ```javascript onPaymentLinkSent
  import { paymentLinks } from '@wix/get-paid';

  paymentLinks.onPaymentLinkSent((event) => {
      // handle your event here
  });
  ```

  ```javascript onPaymentLinkSent (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { paymentLinks } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        paymentLinks,
      },
  });

  wixClient.paymentLinks.onPaymentLinkSent((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
