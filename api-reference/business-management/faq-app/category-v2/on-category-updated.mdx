---
title: "onCategoryUpdated"
description: "Triggered when a category is updated."
icon: "bolt"
---

Triggered when a category is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.faq.category.v2.category_updated` |
| Package | `@wix/faq` |
| Permissions | `Manage FAQ`, `Read FAQ` |

## Examples

<CodeGroup>

  ```javascript onCategoryUpdated
  import { category } from '@wix/faq';

  category.onCategoryUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { category } from '@wix/faq';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        category,
      },
  });

  wixClient.category.onCategoryUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
