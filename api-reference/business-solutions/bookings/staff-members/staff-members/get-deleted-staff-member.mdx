---
title: "Get Deleted Staff Member"
description: "Retrieves a deleted staff member from the trash bin."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a deleted staff member from the trash bin.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/bookings/v2/staff-members/trash-bin/{staffMemberId}
    ```

    ## Path Params

    <ParamField path="staffMemberId" type="string" required>

      ID of the deleted staff member to retrieve.

    </ParamField>

    ## Query Params

    <ParamField query="fields" type="string[]">

      Conditional fields to return.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `RESOURCE_DETAILS` — Includes `resource` in the response.
      - `ASSOCIATED_IDENTITY_STATUS` — Includes `associatedIdentityStatus` in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="staffMember" type="StaffMember">

      Retrieved staff member.

      See the [Staff Member Object](/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/bookings/v2/staff-members/trash-bin/0954bbb2-88cd-445c-9827-44253eb8b039' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "staffMember": {
          "id": "0954bbb2-88cd-445c-9827-44253eb8b039",
          "name": "Deleted Staff Member",
          "email": "deleted@example.com",
          "phone": "+1-555-0123",
          "resourceId": "136d9d9a-97ab-49fa-a0cf-ec24f500bd98",
          "revision": "2",
          "createdDate": "2024-08-28T13:22:32.784Z",
          "updatedDate": "2024-08-28T14:16:11.948Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a deleted staff member from the trash bin.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-PUBLIC">Read Bookings - Public Data</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { staffMembers } from '@wix/bookings';

    staffMembers.getDeletedStaffMember(staffMemberId, options): Promise
    ```

    ## Method Parameters

    <ParamField body="staffMemberId" type="string" required>

      ID of the deleted staff member to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="GetDeletedStaffMemberOptions">

      Options for getting the deleted staff member.

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Conditional fields to return.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `RESOURCE_DETAILS` — Includes `resource` in the response.
          - `ASSOCIATED_IDENTITY_STATUS` — Includes `associatedIdentityStatus` in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="staffMember" type="StaffMember">

      Retrieved staff member.

      See the [Staff Member Object](/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-deleted-staff-member" id="ex-sdk-get-deleted-staff-member-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-deleted-staff-member" id="ex-sdk-get-deleted-staff-member-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-deleted-staff-member-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-deleted-staff-member-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Retrieve a deleted staff member from the trash bin</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDeletedStaffMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-deleted-staff-member-0"><span className="wix-picker-name">Retrieve a deleted staff member from the trash bin</span></label>
    <label htmlFor="ex-sdk-get-deleted-staff-member-1"><span className="wix-picker-name">getDeletedStaffMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Retrieve a deleted staff member from the trash bin
    import { staffMembers } from "@wix/bookings";

    async function getDeletedStaffMember() {
      const response = await staffMembers.getDeletedStaffMember(
        "bbe378bb-7bad-4909-a9b8-6e62561e7af1",
      );
    }

    /* Promise resolves to:
     * {
     *   "staffMember": {
     *     "name": "Dana Cohen",
     *     "email": "dana.cohen@example.com",
     *     "description": "Senior stylist",
     *     "resourceId": "4fcbd727-3557-4828-9e91-e739c6baa338",
     *     "revision": "6",
     *     "serviceProvider": false,
     *     "_id": "bbe378bb-7bad-4909-a9b8-6e62561e7af1",
     *     "_createdDate": "2026-08-16T11:54:19.107Z",
     *     "_updatedDate": "2026-08-16T11:54:24.419Z"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDeletedStaffMember (self-hosted)
    import { createClient } from '@wix/sdk';
    import { staffMembers } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { staffMembers },
      // Include the auth strategy and host as relevant
    });

    async function getDeletedStaffMember(staffMemberId,options) {
      const response = await myWixClient.staffMembers.getDeletedStaffMember(staffMemberId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
