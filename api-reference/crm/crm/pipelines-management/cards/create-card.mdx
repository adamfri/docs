---
title: "Create Card"
description: "Creates a card in a specific pipeline stage."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a card in a specific pipeline stage.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-PIPELINES">Manage Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/crm/pipelines/v1/cards
    ```

    ## Body Params

    <ParamField body="card" type="Card">

      Card to create.

      See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="card" type="Card">

      Created card.

      See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `PIPELINE_NOT_FOUND` | The specified pipeline doesn't exist. |
    | `400` | `INVALID_ARGUMENT` | `STAGE_NOT_FOUND` | The specified stage doesn't exist in the pipeline. |
    | `404` | `NOT_FOUND` | `ASSIGNEE_NOT_FOUND` | The specified assignee doesn't exist. |
    | `400` | `INVALID_ARGUMENT` | `CONTACTS_NOT_FOUND` | One or more of the specified contact IDs don't exist. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_OUTCOME` | The specified stage is a done stage but the card doesn't have a valid outcome. |
    | `400` | `INVALID_ARGUMENT` | `ADDITIONAL_CONTACTS_WITHOUT_MAIN_CONTACT` | Additional contacts were specified without a main contact. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Card Created | `wix.crm.pipelines.v1.card_created` | Triggered when a card is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Creates a card with basic info.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/crm/pipelines/v1/cards' \
        -H 'Authorization:<AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "card": {
            "pipelineId": "af125a9e-d982-4ee5-ae77-2b14457ecdee",
            "stageId": "ac20a94c-0c0f-4584-966b-9ae82da82842",
            "name": "Important Deal",
            "description": "A high-value deal with a key client",
            "priority": "P1_URGENT",
            "dueDate": "2024-12-31T23:59:59.000Z",
            "value": {
              "value": "10000.00",
              "currency": "USD"
            },
            "mainContact": {
              "contactId": "718a5d90-459f-4b4e-9ccf-a8d4f5756834"
            }
          }
        }'
      ```

      ```json Response
      {
        "card": {
          "id": "968a36a4-8128-4100-a9b7-0ed94579208e",
          "revision": "1",
          "createdDate": "2025-07-23T15:26:25.652Z",
          "updatedDate": "2025-07-23T15:26:25.652Z",
          "pipelineId": "af125a9e-d982-4ee5-ae77-2b14457ecdee",
          "stageId": "ac20a94c-0c0f-4584-966b-9ae82da82842",
          "stageUpdatedDate": "2025-07-23T15:26:25.599Z",
          "stageUpdateSource": {
            "appId": "3fd1e173-605c-48b4-a4b4-8f7d65b110b6"
          },
          "name": "Important Deal",
          "description": "A high-value deal with a key client",
          "priority": "P1_URGENT",
          "dueDate": "2024-12-31T23:59:59Z",
          "staleDate": "2025-07-24T15:26:25.599Z",
          "value": {
            "value": "10000",
            "currency": "USD"
          },
          "outcome": "NONE",
          "mainContact": {
            "contactId": "718a5d90-459f-4b4e-9ccf-a8d4f5756834",
            "firstName": "John",
            "lastName": "Doe",
            "email": "john.joe@test.com",
            "phone": "+18005551234"
          },
          "additionalContacts": [],
          "linkedEntities": []
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a card in a specific pipeline stage.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-PIPELINES">Manage Pipelines</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { cards } from '@wix/crm';

    cards.createCard(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="CreateCardOptions">

      options

      <Expandable title="options properties">

        <ParamField body="card" type="Card">

          Card to create.

          See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Card`.

    See the [Card Object](/api-reference/crm/crm/pipelines-management/cards/card-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `PIPELINE_NOT_FOUND` | The specified pipeline doesn't exist. |
    | `400` | `INVALID_ARGUMENT` | `STAGE_NOT_FOUND` | The specified stage doesn't exist in the pipeline. |
    | `404` | `NOT_FOUND` | `ASSIGNEE_NOT_FOUND` | The specified assignee doesn't exist. |
    | `400` | `INVALID_ARGUMENT` | `CONTACTS_NOT_FOUND` | One or more of the specified contact IDs don't exist. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_OUTCOME` | The specified stage is a done stage but the card doesn't have a valid outcome. |
    | `400` | `INVALID_ARGUMENT` | `ADDITIONAL_CONTACTS_WITHOUT_MAIN_CONTACT` | Additional contacts were specified without a main contact. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Card Created | `wix.crm.pipelines.v1.card_created` | Triggered when a card is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-card" id="ex-sdk-create-card-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-card" id="ex-sdk-create-card-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-card" id="ex-sdk-create-card-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-card-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-card-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createCard</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCard (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-card-0"><span className="wix-picker-name">createCard</span></label>
    <label htmlFor="ex-sdk-create-card-1"><span className="wix-picker-name">createCard (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-card-2"><span className="wix-picker-name">createCard (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createCard
    import { cards } from '@wix/crm';

    async function createCard(options) {
      const response = await cards.createCard(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCard (self-hosted)
    import { createClient } from '@wix/sdk';
    import { cards } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { cards },
      // Include the auth strategy and host as relevant
    });

    async function createCard(options) {
      const response = await myWixClient.cards.createCard(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCard (with elevated permissions)
    import { cards } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myCreateCardMethod(options) {
      const elevatedCreateCard = auth.elevate(cards.createCard);
      const response = await elevatedCreateCard(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
