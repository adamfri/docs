---
title: "Delete Gallery"
description: "Deletes a gallery."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<div className="wix-api-columns">

<div className="wix-ref">

Deletes a gallery.

<Note>

* The deleted gallery appears as an empty widget on the live site.
* The deleted gallery is no longer retrieved when calling the [List Galleries](https://dev.wix.com/api/rest/site-content/pro-gallery/list-galleries) endpoint.

</Note>

<Note>

  **Authentication**

  You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

</Note>

## Permissions

This method requires one of the following permission scopes:

<div className="wix-scopes">

<span className="wix-scope" title="SCOPE.DC-PROGALLERY.MANAGE-GALLERIES">Manage Galleries</span>

</div>

Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

Callable by: `APP`.

## Endpoint

```http
DELETE https://www.wixapis.com/pro-gallery/v2/galleries/{galleryId}
```

## Path Params

<ParamField path="galleryId" type="string" required>

  ID of the gallery to delete.

</ParamField>

## Response Object

<ResponseField name="galleryId" type="string">

  ID of the deleted gallery.

  **Read-only.** Constraints: `format GUID`.

</ResponseField>

## Errors

<Note>

  This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

</Note>

</div>

<div className="wix-examples">

## Examples

<CodeGroup>

  ```bash Request
  curl -X DELETE \
  'https://www.wixapis.com/progallery/v2/galleries/c68c0dc5-088f-4205-b202-3eef797ea0ab' \
  -H 'Content-Type: application/json' \
  -H 'Authorization: <AUTH>'
  ```

  ```json Response
  {
    "galleryId": "c68c0dc5-088f-4205-b202-3eef797ea0ab"
  }
  ```

</CodeGroup>

</div>

</div>
