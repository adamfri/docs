---
title: "Get Marketing Activity Posts"
description: "Retrieves the social post drafts and video candidates for the given marketing activities."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the social post drafts and video candidates for the given marketing activities.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-activity-posts
    ```

    ## Body Params

    <ParamField body="marketingActivityIds" type="string[]" required>

      IDs of the marketing activities to retrieve posts for.

      **Read-only.** Constraints: `format GUID`, `minItems 1`, `maxItems 20`.

    </ParamField>

    ## Response Object

    <ResponseField name="marketingActivities" type="SocialPlanMarketingActivity[]">

      Marketing activities with their associated posts and video candidates.

      <Expandable title="marketingActivities properties">

        <ResponseField name="marketingActivityId" type="string">

          Marketing activity ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="date" type="datetime">

          Scheduled date of the marketing activity.

        </ResponseField>

        <ResponseField name="items" type="Item[]">

          Posts associated with this activity. Items can be of any status (`DRAFT`, `SCHEDULED`, or published).
          Only items whose `status` is `DRAFT` can be scheduled with `ScheduleDrafts`.

          Constraints: `minItems 0`, `maxItems 10`.

          <Expandable title="items properties">

            <ResponseField name="id" type="string">

              Item ID.

              **Read-only.**

            </ResponseField>

            <ResponseField name="channel" type="Channel">

              Social channel the item is published to, and the account within that channel.

              _Nested structure omitted for brevity — see the `Channel` object._

            </ResponseField>

            <ResponseField name="type" type="string">

              Item format. The `type` and the content set in `data` must match a combination supported by the item's `channel.name`:

              - Instagram: `POST` with `instagramPost`, or `STORY` with `instagramStory`.
              - Facebook: `POST` with `facebookPost`, `REEL` with `facebookReel`, or `STORY` with `facebookStory`.
              - YouTube: `VIDEO` with `youtubeVideo`, or `SHORT` with `youtubeShort`.
              - LinkedIn: `POST` with `linkedinPost`.
              - X (Twitter): `POST` with `twitterPost`. Deprecated: No longer functional as of July 31, 2026.
              - Pinterest: `POST` with `pinterestPost`.
              - Google Business Profile: `POST` with `gbpPost`.
              - TikTok: `POST` with `tiktokPhoto`, or `VIDEO` with `tiktokVideo`.

              **Supported values:**

              - `POST` — A standard post.
              - `REEL` — A short-form vertical video reel.
              - `VIDEO` — A video.
              - `SHORT` — A short-form vertical video.
              - `STORY` — A story that's available for a limited time.

            </ResponseField>

            <ResponseField name="status" type="string">

              Current status of the item in its lifecycle.

              **Read-only.**

              **Supported values:**

              - `PUBLISHED` — Published to the social channel.
              - `SCHEDULED` — Scheduled to be published at a future date.
              - `CANCELED` — Scheduling was canceled before the item was published.
              - `FAILED` — Publishing failed.
              - `DELETED` — Deleted.
              - `PROCESSING` — Publishing is in progress.
              - `DRAFT` — Saved as a draft, not yet published.
              - `IN_QUEUE` — Queued for publishing as part of a bulk operation.

            </ResponseField>

            <ResponseField name="publishedDate" type="datetime">

              Date and time the item was published. Returned only after the item is published.

              **Read-only.**

            </ResponseField>

            <ResponseField name="schedulingInfo" type="SchedulingInfo">

              Scheduling info. Set to schedule the item for a future date instead of publishing it immediately.

              _Nested structure omitted for brevity — see the `SchedulingInfo` object._

            </ResponseField>

            <ResponseField name="referenceId" type="string">

              Caller-defined reference that uniquely identifies the item. Can be used to prevent duplicate publishing: an attempt to publish a second item with the same `referenceId` fails. Can also be used as a UTM campaign ID.

            </ResponseField>

            <ResponseField name="siteAnalytics" type="SiteAnalytics">

              Aggregated analytics for the site, attributed to traffic from this post.

              _Nested structure omitted for brevity — see the `SiteAnalytics` object._

            </ResponseField>

            <ResponseField name="insightsSummary" type="InsightsSummary">

              Summary of the post's performance on the social channel, such as impressions and interactions.

              _Nested structure omitted for brevity — see the `InsightsSummary` object._

            </ResponseField>

            <ResponseField name="draftInfo" type="DraftInfo">

              Creation and update dates, returned for draft items.

              _Nested structure omitted for brevity — see the `DraftInfo` object._

            </ResponseField>

            <ResponseField name="suggested" type="boolean">

              Whether the item was generated as an AI suggestion.

            </ResponseField>

            <ResponseField name="external" type="boolean">

              Whether the item is a post created natively on the social channel rather than published through Wix.

            </ResponseField>

            <ResponseField name="publicationError" type="PublicationErrorDetails">

              Details of why the item failed to publish. Returned only when `status` is `FAILED`.

              **Read-only.**

              _Nested structure omitted for brevity — see the `PublicationErrorDetails` object._

            </ResponseField>

            <ResponseField name="siteAssetRef" type="AssetReference">

              Reference to the site content asset this post promotes (product, blog post, etc.).
              Mirrors marketing-plan-service's MarketingActivity.asset (named `asset` there;
              `site_asset_ref` here for clarity next to consumer_info). Written per-channel.

              _Nested structure omitted for brevity — see the `AssetReference` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="title" type="string">

          Title of the marketing activity.

        </ResponseField>

        <ResponseField name="description" type="string">

          Description of the marketing activity.

        </ResponseField>

        <ResponseField name="videoCandidates" type="VideoCandidate[]">

          Asynchronously generated video options for this activity. Poll `GetMarketingActivityPosts` to check their status.

          Constraints: `minItems 0`, `maxItems 10`.

          <Expandable title="videoCandidates properties">

            <ResponseField name="executionId" type="string">

              Unique identifier of the video generation process.

              **Read-only.**

            </ResponseField>

            <ResponseField name="status" type="string">

              Current generation status of the video candidate.

              **Supported values:**

              - `IN_PROGRESS` — Video is currently being generated.
              - `FAILED` — Generation failed. Add a new candidate to retry.
              - `READY` — Video is ready. `video_url` is populated.

            </ResponseField>

            <ResponseField name="videoUrl" type="string">

              URL of the generated video. Returned only when `status` is `READY`.

            </ResponseField>

            <ResponseField name="prompt" type="string">

              Prompt used to generate the video.

            </ResponseField>

            <ResponseField name="sourceMediaUrl" type="string">

              URL of the source media used as input for video generation.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="asset" type="AssetReference">

          Site asset this activity is grounded in.

          <Expandable title="asset properties">

            <ResponseField name="type" type="string">

              Type of the site asset (for example, product or blog post). See `SiteAsset.Type` for the closed set of values.

              **Supported values:**

              - `BLOG_POST` — A Wix Blog post.
              - `EVENT` — A Wix Events event.
              - `STORES_PRODUCT` — A Wix Stores product.
              - `BOOKINGS_SERVICE` — A Wix Bookings service.
              - `STORES_COUPON` — A Wix Stores coupon.
              - `STORES_CATEGORY` — A Wix Stores category.

            </ResponseField>

            <ResponseField name="id" type="string">

              ID of the asset within its provider's data store.

            </ResponseField>

            <ResponseField name="name" type="string">

              Display name of the asset (for example, the product title or blog post title), cached at activity-generation time.

              Constraints: `maxLength 3000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieve the marketing activities for the given IDs, including their publisher posts.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/promote/marketing-plan-service/v1/marketing-activity-posts' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "marketingActivityIds": [
          "b3f9d2e1-7a4c-4d68-9f02-1c5e8a6b4d30"
        ]
      }'
      ```

      ```json Response
      {
        "marketingActivities": [
          {
            "marketingActivityId": "b3f9d2e1-7a4c-4d68-9f02-1c5e8a6b4d30",
            "date": "2026-07-08T09:00:00.000Z",
            "title": "Summer collection launch",
            "description": "Announce the new handmade ceramic mugs summer collection with a 20% launch discount.",
            "items": [
              {
                "id": "5c7a1f0b-2d63-4e98-b1a4-9f02c6e83d71",
                "channel": {
                  "name": "INSTAGRAM",
                  "accountId": "17841405822304914"
                },
                "type": "POST",
                "status": "DRAFT",
                "instagramPost": {
                  "mediaWrapper": {
                    "media": [
                      {
                        "type": "IMAGE",
                        "url": "https://static.wixstatic.com/media/cf2434_4b3a2d8eb7f54bc89c0d4524430a2af3~mv2.jpg"
                      }
                    ]
                  },
                  "caption": "Our summer collection is here ☀️ Handmade ceramic mugs, 20% off this week only. #handmade #ceramics #summersale"
                },
                "draftInfo": {
                  "createdDate": "2026-06-25T08:05:12.330Z",
                  "updatedDate": "2026-06-25T08:05:12.330Z"
                },
                "suggested": true,
                "external": false
              }
            ]
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the social post drafts and video candidates for the given marketing activities.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingPlan } from '@wix/promote-growth-tools-marketing-plan';

    marketingPlan.getMarketingActivityPosts(marketingActivityIds): Promise
    ```

    ## Method Parameters

    <ParamField body="marketingActivityIds" type="string[]" required>

      IDs of the marketing activities to retrieve posts for.

      **Read-only.** Constraints: `format GUID`, `minItems 1`, `maxItems 20`.

    </ParamField>

    ## Returns

    <ResponseField name="marketingActivities" type="SocialPlanMarketingActivity[]">

      Marketing activities with their associated posts and video candidates.

      <Expandable title="marketingActivities properties">

        <ResponseField name="marketingActivityId" type="string">

          Marketing activity ID.

          **Read-only.**

        </ResponseField>

        <ResponseField name="date" type="datetime">

          Scheduled date of the marketing activity.

        </ResponseField>

        <ResponseField name="items" type="Item[]">

          Posts associated with this activity. Items can be of any status (`DRAFT`, `SCHEDULED`, or published).
          Only items whose `status` is `DRAFT` can be scheduled with `ScheduleDrafts`.

          Constraints: `minItems 0`, `maxItems 10`.

          <Expandable title="items properties">

            <ResponseField name="_id" type="string">

              Item ID.

              **Read-only.**

            </ResponseField>

            <ResponseField name="channel" type="Channel">

              Social channel the item is published to, and the account within that channel.

              _Nested structure omitted for brevity — see the `Channel` object._

            </ResponseField>

            <ResponseField name="type" type="string">

              Item format. The `type` and the content set in `data` must match a combination supported by the item's `channel.name`:

              - Instagram: `POST` with `instagramPost`, or `STORY` with `instagramStory`.
              - Facebook: `POST` with `facebookPost`, `REEL` with `facebookReel`, or `STORY` with `facebookStory`.
              - YouTube: `VIDEO` with `youtubeVideo`, or `SHORT` with `youtubeShort`.
              - LinkedIn: `POST` with `linkedinPost`.
              - X (Twitter): `POST` with `twitterPost`. Deprecated: No longer functional as of July 31, 2026.
              - Pinterest: `POST` with `pinterestPost`.
              - Google Business Profile: `POST` with `gbpPost`.
              - TikTok: `POST` with `tiktokPhoto`, or `VIDEO` with `tiktokVideo`.

              **Supported values:**

              - `POST` — A standard post.
              - `REEL` — A short-form vertical video reel.
              - `VIDEO` — A video.
              - `SHORT` — A short-form vertical video.
              - `STORY` — A story that's available for a limited time.

            </ResponseField>

            <ResponseField name="status" type="string">

              Current status of the item in its lifecycle.

              **Read-only.**

              **Supported values:**

              - `PUBLISHED` — Published to the social channel.
              - `SCHEDULED` — Scheduled to be published at a future date.
              - `CANCELED` — Scheduling was canceled before the item was published.
              - `FAILED` — Publishing failed.
              - `DELETED` — Deleted.
              - `PROCESSING` — Publishing is in progress.
              - `DRAFT` — Saved as a draft, not yet published.
              - `IN_QUEUE` — Queued for publishing as part of a bulk operation.

            </ResponseField>

            <ResponseField name="publishedDate" type="datetime">

              Date and time the item was published. Returned only after the item is published.

              **Read-only.**

            </ResponseField>

            <ResponseField name="schedulingInfo" type="SchedulingInfo">

              Scheduling info. Set to schedule the item for a future date instead of publishing it immediately.

              _Nested structure omitted for brevity — see the `SchedulingInfo` object._

            </ResponseField>

            <ResponseField name="referenceId" type="string">

              Caller-defined reference that uniquely identifies the item. Can be used to prevent duplicate publishing: an attempt to publish a second item with the same `referenceId` fails. Can also be used as a UTM campaign ID.

            </ResponseField>

            <ResponseField name="siteAnalytics" type="SiteAnalytics">

              Aggregated analytics for the site, attributed to traffic from this post.

              _Nested structure omitted for brevity — see the `SiteAnalytics` object._

            </ResponseField>

            <ResponseField name="insightsSummary" type="InsightsSummary">

              Summary of the post's performance on the social channel, such as impressions and interactions.

              _Nested structure omitted for brevity — see the `InsightsSummary` object._

            </ResponseField>

            <ResponseField name="draftInfo" type="DraftInfo">

              Creation and update dates, returned for draft items.

              _Nested structure omitted for brevity — see the `DraftInfo` object._

            </ResponseField>

            <ResponseField name="suggested" type="boolean">

              Whether the item was generated as an AI suggestion.

            </ResponseField>

            <ResponseField name="external" type="boolean">

              Whether the item is a post created natively on the social channel rather than published through Wix.

            </ResponseField>

            <ResponseField name="publicationError" type="PublicationErrorDetails">

              Details of why the item failed to publish. Returned only when `status` is `FAILED`.

              **Read-only.**

              _Nested structure omitted for brevity — see the `PublicationErrorDetails` object._

            </ResponseField>

            <ResponseField name="siteAssetRef" type="AssetReference">

              Reference to the site content asset this post promotes (product, blog post, etc.).
              Mirrors marketing-plan-service's MarketingActivity.asset (named `asset` there;
              `site_asset_ref` here for clarity next to consumer_info). Written per-channel.

              _Nested structure omitted for brevity — see the `AssetReference` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="title" type="string">

          Title of the marketing activity.

        </ResponseField>

        <ResponseField name="description" type="string">

          Description of the marketing activity.

        </ResponseField>

        <ResponseField name="videoCandidates" type="VideoCandidate[]">

          Asynchronously generated video options for this activity. Poll `GetMarketingActivityPosts` to check their status.

          Constraints: `minItems 0`, `maxItems 10`.

          <Expandable title="videoCandidates properties">

            <ResponseField name="executionId" type="string">

              Unique identifier of the video generation process.

              **Read-only.**

            </ResponseField>

            <ResponseField name="status" type="string">

              Current generation status of the video candidate.

              **Supported values:**

              - `IN_PROGRESS` — Video is currently being generated.
              - `FAILED` — Generation failed. Add a new candidate to retry.
              - `READY` — Video is ready. `video_url` is populated.

            </ResponseField>

            <ResponseField name="videoUrl" type="string">

              URL of the generated video. Returned only when `status` is `READY`.

            </ResponseField>

            <ResponseField name="prompt" type="string">

              Prompt used to generate the video.

            </ResponseField>

            <ResponseField name="sourceMediaUrl" type="string">

              URL of the source media used as input for video generation.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="asset" type="AssetReference">

          Site asset this activity is grounded in.

          <Expandable title="asset properties">

            <ResponseField name="type" type="string">

              Type of the site asset (for example, product or blog post). See `SiteAsset.Type` for the closed set of values.

              **Supported values:**

              - `BLOG_POST` — A Wix Blog post.
              - `EVENT` — A Wix Events event.
              - `STORES_PRODUCT` — A Wix Stores product.
              - `BOOKINGS_SERVICE` — A Wix Bookings service.
              - `STORES_COUPON` — A Wix Stores coupon.
              - `STORES_CATEGORY` — A Wix Stores category.

            </ResponseField>

            <ResponseField name="_id" type="string">

              ID of the asset within its provider's data store.

            </ResponseField>

            <ResponseField name="name" type="string">

              Display name of the asset (for example, the product title or blog post title), cached at activity-generation time.

              Constraints: `maxLength 3000`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-marketing-activity-posts" id="ex-sdk-get-marketing-activity-posts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-marketing-activity-posts" id="ex-sdk-get-marketing-activity-posts-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-marketing-activity-posts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-marketing-activity-posts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get the posts of specific marketing activities</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getMarketingActivityPosts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-marketing-activity-posts-0"><span className="wix-picker-name">Get the posts of specific marketing activities</span></label>
    <label htmlFor="ex-sdk-get-marketing-activity-posts-1"><span className="wix-picker-name">getMarketingActivityPosts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get the posts of specific marketing activities
    import { marketingPlan } from "@wix/promote-growth-tools-marketing-plan";

    const marketingActivityIds = ["b3f9d2e1-7a4c-4d68-9f02-1c5e8a6b4d30"];

    async function getMarketingActivityPosts() {
      const response =
        await marketingPlan.getMarketingActivityPosts(marketingActivityIds);
    }

    /* Promise resolves to:
     * {
     *   "marketingActivities": [
     *     {
     *       "marketingActivityId": "b3f9d2e1-7a4c-4d68-9f02-1c5e8a6b4d30",
     *       "date": "2026-07-08T09:00:00.000Z",
     *       "title": "Summer collection launch",
     *       "description": "Announce the new handmade ceramic mugs summer collection with a 20% launch discount.",
     *       "items": [
     *         {
     *           "_id": "5c7a1f0b-2d63-4e98-b1a4-9f02c6e83d71",
     *           "channel": {
     *             "name": "INSTAGRAM",
     *             "accountId": "17841405822304914"
     *           },
     *           "type": "POST",
     *           "status": "DRAFT",
     *           "instagramPost": {
     *             "mediaWrapper": {
     *               "media": [
     *                 {
     *                   "type": "IMAGE",
     *                   "url": "https://static.wixstatic.com/media/cf2434_4b3a2d8eb7f54bc89c0d4524430a2af3~mv2.jpg"
     *                 }
     *               ]
     *             },
     *             "caption": "Our summer collection is here ☀️ Handmade ceramic mugs, 20% off this week only. #handmade #ceramics #summersale"
     *           },
     *           "draftInfo": {
     *             "_createdDate": "2026-06-25T08:05:12.330Z",
     *             "_updatedDate": "2026-06-25T08:05:12.330Z"
     *           },
     *           "suggested": true,
     *           "external": false
     *         }
     *       ]
     *     }
     *   ]
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getMarketingActivityPosts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingPlan } from '@wix/promote-growth-tools-marketing-plan';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingPlan },
      // Include the auth strategy and host as relevant
    });

    async function getMarketingActivityPosts(marketingActivityIds) {
      const response = await myWixClient.marketingPlan.getMarketingActivityPosts(marketingActivityIds);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
