---
title: "onOrderReservationCreated"
description: "The onOrderReservationCreated event."
icon: "bolt"
tag: "Deprecated"
searchable: false
---

<Warning>

  **Deprecated.** Use `wix.events.v1.ticket_reservation_created` instead. Scheduled for removal on 2026-03-31.

</Warning>

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.ticketing.events.ReservationCreated` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Read Basic Events Order Info`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onOrderReservationCreated
  import { orders } from '@wix/events';

  orders.onOrderReservationCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderReservationCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderReservationCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
