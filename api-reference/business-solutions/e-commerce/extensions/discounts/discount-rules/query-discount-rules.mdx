---
title: "Query Discount Rules"
description: "Query discount rules using WQL (Wix Query Language)."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Query discount rules using [WQL (Wix Query Language)](https://dev.wix.com/api/rest/getting-started/api-query-language).
    Total entries (`pagingMetadata.total`) will be returned only for the first page.

    <Note>

    `discountRule.status` can't be used for querying.

    </Note>

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `active` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `revision` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `activeTimeInfo.start` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `activeTimeInfo.end` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `tags.privateTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.READ-ECOM">Read eCommerce - all read permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/ecom/v1/discount-rules/query
    ```

    ## Body Params

    <ParamField body="query" type="PlatformQuery">

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sorting options. For example, `[{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor pointing to page of results. Cannot be used together with `paging`. `cursorPaging.cursor` can not be used together with `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="discountRules" type="DiscountRule[]">

      List of discount rules.

      See the [Discount Rule Object](/api-reference/business-solutions/e-commerce/extensions/discounts/discount-rules/discount-rule-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PlatformPagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          The number of items returned in this response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          The offset which was requested. Returned if offset paging was used.

        </ResponseField>

        <ResponseField name="total" type="integer">

          The total number of items that match the query. Returned if offset paging was used.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through result pages. Returned if cursor paging was used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Query Discount rules by active and start date fields, sorted by name

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/ecom/v1/discount-rules/query' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{
          "query": {
            "sort": [
              {
                "fieldName": "name",
                "order": "ASC"
              }
            ],
            "filter": {
              "active": true,
              "activeTimeInfo.start": {
                "$gt": "2021-08-01T00:00:00Z"
              }
            },
            "cursorPaging": {
              "limit": 2
            }
          }
        }'
      ```

      ```json Response
      {
        "discountRules": [
          {
            "id": "15bac455-10d0-4478-8358-e06f697f6180",
            "revision": "8",
            "createdDate": "2022-08-07T08:12:22.347Z",
            "updatedDate": "2022-08-09T07:45:44.044Z",
            "active": true,
            "name": "10% discount when buying over 100$ and 5 items",
            "trigger": {
              "and": {
                "triggers": [
                  {
                    "subtotalRange": {
                      "scopes": [
                        {
                          "id": "specific_215238eb-22a5-4c36-9e7b-e7c08025e04e",
                          "type": "CATALOG_ITEM",
                          "catalogItemFilter": {
                            "catalogAppId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                            "catalogItemIds": [
                              "f150b16a-79ef-ef81-50f9-22e2df631822",
                              "187a07f6-b670-0650-c891-d60aeb797ee0"
                            ]
                          }
                        }
                      ],
                      "from": "100",
                      "to": null
                    },
                    "triggerType": "SUBTOTAL_RANGE"
                  },
                  {
                    "itemQuantityRange": {
                      "scopes": [
                        {
                          "id": "specific_215238eb-22a5-4c36-9e7b-e7c08025e04e",
                          "type": "CATALOG_ITEM",
                          "catalogItemFilter": {
                            "catalogAppId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                            "catalogItemIds": [
                              "f150b16a-79ef-ef81-50f9-22e2df631822",
                              "187a07f6-b670-0650-c891-d60aeb797ee0"
                            ]
                          }
                        }
                      ],
                      "from": 5,
                      "to": null
                    },
                    "triggerType": "ITEM_QUANTITY_RANGE"
                  }
                ]
              },
              "triggerType": "AND"
            },
            "activeTimeInfo": {
              "start": "2022-08-07T08:11:00Z",
              "end": "2022-08-11T07:43:00Z"
            },
            "discounts": {
              "values": [
                {
                  "targetType": "SPECIFIC_ITEMS",
                  "specificItemsInfo": {
                    "scopes": [
                      {
                        "id": "specific_215238eb-22a5-4c36-9e7b-e7c08025e04e",
                        "type": "CATALOG_ITEM",
                        "catalogItemFilter": {
                          "catalogAppId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                          "catalogItemIds": [
                            "f150b16a-79ef-ef81-50f9-22e2df631822",
                            "187a07f6-b670-0650-c891-d60aeb797ee0"
                          ]
                        }
                      }
                    ]
                  },
                  "percentage": 10,
                  "discountType": "PERCENTAGE"
                }
              ]
            },
            "status": "LIVE",
            "usageCount": 1
          },
          {
            "id": "35120105-1327-4624-8f7f-2720dcbab4d6",
            "revision": "40",
            "createdDate": "2023-02-28T11:56:05.996Z",
            "updatedDate": "2023-04-02T08:34:37.998Z",
            "active": false,
            "name": "10$ off Duffle Bags",
            "trigger": null,
            "activeTimeInfo": {
              "start": "2023-02-28T11:55:00Z",
              "end": null
            },
            "discounts": {
              "values": [
                {
                  "targetType": "SPECIFIC_ITEMS",
                  "specificItemsInfo": {
                    "scopes": [
                      {
                        "id": "collections_215238eb-22a5-4c36-9e7b-e7c08025e04e",
                        "type": "CUSTOM_FILTER",
                        "customFilter": {
                          "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                          "params": {
                            "collectionIds": [
                              "c31f8d152077bab24065f01c0e3152b746c54ce0"
                            ]
                          }
                        }
                      }
                    ]
                  },
                  "fixedAmount": "10",
                  "discountType": "FIXED_AMOUNT"
                }
              ]
            },
            "status": "LIVE",
            "usageCount": 6
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "offset": null,
          "total": 8,
          "cursors": {
            "next": "c21acd2f5eb388517dd9c781a9ba42b9e51ca540.ElAqTgo-ChRhY3RpdmVUaW1lSW5mby5zdGFydBImKiQKIgoDJGd0EhsqGQoXCgokdGltZXN0YW1wEgkRAAAAKvCvd0IKDAoGYWN0aXZlEgIgACIkMzUxMjAxMDUtMTMyNy00NjI0LThmN2YtMjcyMGRjYmFiNGQ2",
            "prev": null
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Query discount rules using [WQL (Wix Query Language)](https://dev.wix.com/api/rest/getting-started/api-query-language).
    Total entries (`pagingMetadata.total`) will be returned only for the first page.

    <Note>

    `discountRule.status` can't be used for querying.

    </Note>

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `active` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `revision` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `activeTimeInfo.start` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `activeTimeInfo.end` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `tags.privateTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.READ-ECOM">Read eCommerce - all read permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { discountRules } from '@wix/ecom';

    discountRules.queryDiscountRules(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="DiscountRuleQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sorting options. For example, `[{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor pointing to page of results. Cannot be used together with `paging`. `cursorPaging.cursor` can not be used together with `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="discountRules" type="DiscountRule[]">

      List of discount rules.

      See the [Discount Rule Object](/api-reference/business-solutions/e-commerce/extensions/discounts/discount-rules/discount-rule-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PlatformPagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          The number of items returned in this response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          The offset which was requested. Returned if offset paging was used.

        </ResponseField>

        <ResponseField name="total" type="integer">

          The total number of items that match the query. Returned if offset paging was used.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through result pages. Returned if cursor paging was used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-discount-rules" id="ex-sdk-query-discount-rules-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-discount-rules" id="ex-sdk-query-discount-rules-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-discount-rules" id="ex-sdk-query-discount-rules-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-discount-rules-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-discount-rules-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryDiscountRules</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryDiscountRules (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryDiscountRules (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-discount-rules-0"><span className="wix-picker-name">queryDiscountRules</span></label>
    <label htmlFor="ex-sdk-query-discount-rules-1"><span className="wix-picker-name">queryDiscountRules (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-discount-rules-2"><span className="wix-picker-name">queryDiscountRules (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryDiscountRules
    import { discountRules } from '@wix/ecom';

    async function queryDiscountRules(query) {
      const response = await discountRules.queryDiscountRules(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryDiscountRules (self-hosted)
    import { createClient } from '@wix/sdk';
    import { discountRules } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { discountRules },
      // Include the auth strategy and host as relevant
    });

    async function queryDiscountRules(query) {
      const response = await myWixClient.discountRules.queryDiscountRules(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryDiscountRules (with elevated permissions)
    import { discountRules } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myQueryDiscountRulesMethod(query) {
      const elevatedQueryDiscountRules = auth.elevate(discountRules.queryDiscountRules);
      const response = await elevatedQueryDiscountRules(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
