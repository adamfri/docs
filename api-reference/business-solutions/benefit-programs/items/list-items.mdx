---
title: "List Items"
description: "Retrieves a list of up to 1000 benefit items."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 1000 benefit items.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/benefit-programs/v1/items
    ```

    ## Query Params

    <ParamField query="filter" type="Filter">

      Filter.

      <Expandable title="filter properties">

        <ParamField query="type" type="string">

          Filter type.

          **Supported values:**

          - `BY_ITEM_SET_ID_AND_REFERENCE` — Use with `filter.byItemSetIdAndReferenceOptions`.
          - `BY_REFERENCE` — Use with `filter.byReferenceOptions`.

        </ParamField>

        <ParamField query="namespace" type="string">

          Filter by namespace.

          Constraints: `minLength 1`, `maxLength 20`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField query="filter" type="filter">

          filter

          <Expandable title="filter properties">

            <ParamField query="byItemSetIdAndReferenceOptions" type="ByItemSetIdAndReference">

              Filter the request by `itemSetId` and item reference.

              <Expandable title="byItemSetIdAndReferenceOptions properties">

                <ParamField query="filters" type="ByItemSetIdAndReferenceFilter[]">

                  Filter list.

                  Constraints: `minItems 1`, `maxItems 100`.

                  _Nested structure omitted for brevity — see the `ByItemSetIdAndReferenceFilter[]` object._

                </ParamField>

              </Expandable>

            </ParamField>

            <ParamField query="byReferenceOptions" type="ByReference">

              Filter the request item reference.

              <Expandable title="byReferenceOptions properties">

                <ParamField query="filters" type="ByReferenceFilter[]">

                  Filter list.

                  Constraints: `minItems 1`, `maxItems 100`.

                  _Nested structure omitted for brevity — see the `ByReferenceFilter[]` object._

                </ParamField>

              </Expandable>

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="cursorPaging" type="CursorPaging">

      Cursor paging

      <Expandable title="cursorPaging properties">

        <ParamField query="limit" type="integer">

          Maximum number of items to return.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="items" type="Item[]">

      Items.

      See the [Item Object](/api-reference/business-solutions/benefit-programs/items/item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-list-items" id="ex-rest-list-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-list-items" id="ex-rest-list-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-list-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-list-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">ListItems</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">ListItems</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-list-items-0"><span className="wix-picker-name">ListItems</span></label>
    <label htmlFor="ex-rest-list-items-1"><span className="wix-picker-name">ListItems</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
        "https://www.wixapis.com/benefit-programs/v1/items?filter.byReferenceOptions.filters%5B0%5D.externalId=00000000-0000-0000-0000-000000000001&filter.byReferenceOptions.filters%5B0%5D.providerAppId=00000000-0000-0000-0000-000000000000&filter.namespace=benefit_programs_app&filter.type=BY_REFERENCE"
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "items": [
          {
            "id": "124bc876-66d2-4603-8c20-395a09eb0e44",
            "revision": "1",
            "created_date": "2024-11-07T12:47:11.370Z",
            "updated_date": "2024-11-07T12:47:11.370Z",
            "external_id": "3f9a7c91-4e2b-4d5e-b6f8-0c2e5a9d4f34",
            "category": "",
            "item_set_id": "201db180-5043-45cf-a501-bd9c06df5ae5",
            "display_name": null,
            "provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
            "extended_fields": null,
            "namespace": "benefit_programs_app"
          }
        ],
        "metadata": {
          "count": 1,
          "cursors": {
            "next": null,
            "prev": null
          },
          "has_next": false
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X GET \
        "https://www.wixapis.com/benefit-programs/v1/items?filter.byItemSetIdAndReferenceOptions.filters%5B0%5D.externalId=00000000-0000-0000-0000-000000000001&filter.byItemSetIdAndReferenceOptions.filters%5B0%5D.itemSetId=201db180-5043-45cf-a501-bd9c06df5ae5&filter.byItemSetIdAndReferenceOptions.filters%5B0%5D.providerAppId=00000000-0000-0000-0000-000000000000&filter.namespace=benefit_programs_app&filter.type=BY_ITEM_SET_ID_AND_REFERENCE"
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "items": [
          {
            "id": "124bc876-66d2-4603-8c20-395a09eb0e44",
            "revision": "1",
            "created_date": "2024-11-07T12:47:11.370Z",
            "updated_date": "2024-11-07T12:47:11.370Z",
            "external_id": "3f9a7c91-4e2b-4d5e-b6f8-0c2e5a9d4f34",
            "category": "",
            "item_set_id": "201db180-5043-45cf-a501-bd9c06df5ae5",
            "display_name": null,
            "provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
            "extended_fields": null,
            "namespace": "benefit_programs_app",
          }
        ],
        "metadata": {
          "count": 1,
          "cursors": {
            "next": null,
            "prev": null
          },
          "has_next": false
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 1000 benefit items.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.READ_LIMITED">SCOPE.BENEFIT_PROGRAMS.READ (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/benefit-programs';

    items.listItems(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="ListItemsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="filter" type="Filter">

          Filter.

          <Expandable title="filter properties">

            <ParamField body="type" type="string">

              Filter type.

              **Supported values:**

              - `BY_ITEM_SET_ID_AND_REFERENCE` — Use with `filter.byItemSetIdAndReferenceOptions`.
              - `BY_REFERENCE` — Use with `filter.byReferenceOptions`.

            </ParamField>

            <ParamField body="namespace" type="string">

              Filter by namespace.

              Constraints: `minLength 1`, `maxLength 20`.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="filter" type="filter">

              filter

              _Nested structure omitted for brevity — see the `filter` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="cursorPaging" type="CursorPaging">

          Cursor paging

          <Expandable title="cursorPaging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="items" type="Item[]">

      Items.

      See the [Item Object](/api-reference/business-solutions/benefit-programs/items/item-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="CursorPagingMetadata">

      Metadata for paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-items" id="ex-sdk-list-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-items" id="ex-sdk-list-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-items" id="ex-sdk-list-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listItems</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listItems (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-items-0"><span className="wix-picker-name">listItems</span></label>
    <label htmlFor="ex-sdk-list-items-1"><span className="wix-picker-name">listItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-list-items-2"><span className="wix-picker-name">listItems (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listItems
    import { items } from '@wix/benefit-programs';

    async function listItems(options) {
      const response = await items.listItems(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function listItems(options) {
      const response = await myWixClient.items.listItems(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listItems (with elevated permissions)
    import { items } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myListItemsMethod(options) {
      const elevatedListItems = auth.elevate(items.listItems);
      const response = await elevatedListItems(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
