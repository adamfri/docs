---
title: "onRsvpDeleted"
description: "Triggered when RSVP is deleted."
icon: "bolt"
---

Triggered when RSVP is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v2.rsvp_deleted` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onRsvpDeleted
  import { rsvpV2 } from '@wix/events';

  rsvpV2.onRsvpDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onRsvpDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { rsvpV2 } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        rsvpV2,
      },
  });

  wixClient.rsvpV2.onRsvpDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
