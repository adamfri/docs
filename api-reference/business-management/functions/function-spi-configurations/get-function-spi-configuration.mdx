---
title: "Get Function Spi Configuration"
description: "Retrieves a service plugin configuration by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a service plugin configuration by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FUNCTIONS.MANAGE-FUNCTIONS">Manage Functions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/functions/v1/function-spi-configurations/{functionSpiConfigurationId}
    ```

    ## Path Params

    <ParamField path="functionSpiConfigurationId" type="string" required>

      ID of the service plugin configuration to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="functionSpiConfiguration" type="FunctionSpiConfiguration">

      The retrieved service plugin configuration.

      See the [Spi Configuration Object](/api-reference/business-management/functions/function-spi-configurations/spi-configuration-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl --request GET \
        https://www.wixapis.com/functions/v1/spi-configurations/00019845-6387-4006-9e9b-366caffed188 \
        --header "Content-Type: application/json" \
        --header "Authorization: <AUTH>"
      ```

      ```json Response
      {
        "functionSpiConfiguration": {
          "id": "00019845-6387-4006-9e9b-366caffed188",
          "revision": "1",
          "createdDate": "2024-11-28T11:55:15.984Z",
          "updatedDate": "2024-11-28T11:55:15.984Z",
          "functionId": "fb7d2934-4906-45b6-85cd-5d55d3cc9da0",
          "configuration": {
            "rewardDescription": "Loyalty Program Reward",
            "rewardName": "Frequent buyer perk"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a service plugin configuration by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-FUNCTIONS.MANAGE-FUNCTIONS">Manage Functions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { functionSpiConfigurations } from '@wix/functions';

    functionSpiConfigurations.getFunctionSpiConfiguration(functionSpiConfigurationId): Promise<FunctionSpiConfiguration>
    ```

    ## Method Parameters

    <ParamField body="functionSpiConfigurationId" type="string" required>

      ID of the service plugin configuration to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<FunctionSpiConfiguration>`.

    See the [Spi Configuration Object](/api-reference/business-management/functions/function-spi-configurations/spi-configuration-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-function-spi-configuration" id="ex-sdk-get-function-spi-configuration-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-function-spi-configuration" id="ex-sdk-get-function-spi-configuration-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-function-spi-configuration" id="ex-sdk-get-function-spi-configuration-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-function-spi-configuration-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-function-spi-configuration-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getFunctionSpiConfiguration</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getFunctionSpiConfiguration (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getFunctionSpiConfiguration (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-function-spi-configuration-0"><span className="wix-picker-name">getFunctionSpiConfiguration</span></label>
    <label htmlFor="ex-sdk-get-function-spi-configuration-1"><span className="wix-picker-name">getFunctionSpiConfiguration (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-function-spi-configuration-2"><span className="wix-picker-name">getFunctionSpiConfiguration (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getFunctionSpiConfiguration
    import { functionSpiConfigurations } from '@wix/functions';

    async function getFunctionSpiConfiguration(functionSpiConfigurationId) {
      const response = await functionSpiConfigurations.getFunctionSpiConfiguration(functionSpiConfigurationId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getFunctionSpiConfiguration (self-hosted)
    import { createClient } from '@wix/sdk';
    import { functionSpiConfigurations } from '@wix/functions';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { functionSpiConfigurations },
      // Include the auth strategy and host as relevant
    });

    async function getFunctionSpiConfiguration(functionSpiConfigurationId) {
      const response = await myWixClient.functionSpiConfigurations.getFunctionSpiConfiguration(functionSpiConfigurationId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getFunctionSpiConfiguration (with elevated permissions)
    import { functionSpiConfigurations } from '@wix/functions';
    import { auth } from '@wix/essentials';

    async function myGetFunctionSpiConfigurationMethod(functionSpiConfigurationId) {
      const elevatedGetFunctionSpiConfiguration = auth.elevate(functionSpiConfigurations.getFunctionSpiConfiguration);
      const response = await elevatedGetFunctionSpiConfiguration(functionSpiConfigurationId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
