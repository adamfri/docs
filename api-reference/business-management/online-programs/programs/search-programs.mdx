---
title: "Search Programs"
description: "Searches programs by title and structured filters."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Searches programs by title and structured filters.

    Use this method when you need full-text search over `description.title`.
    Results are visibility-filtered according to the caller identity.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `description.title` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$in` |
    | `createdDate` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` |  |
    | `timeline.selfPaced` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq` |
    | `restrictions.accessType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$in` |
    | `updatedDate` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` |  |
    | `categoryIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/online-programs/v3/programs/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search expression, filters, sorting, and cursor paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="groupBy" type="GroupByAggregation" deprecated>

              Deprecated. Use `nested` instead.

              **Deprecated.** Deprecated. Use `nested` instead. Use `nested` instead. Removal: 2024-03-30.

              _Nested structure omitted for brevity — see the `GroupByAggregation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use auto fuzzy search (allowing typos by a managed proximity algorithm).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="programs" type="Program[]">

      Programs that match the search and caller visibility.

      See the [Program Object](/api-reference/business-management/online-programs/programs/program-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data returned by the search backend.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request).

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches programs by title and filters to public or private published programs.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/online-programs/v3/programs/search' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "search": {
          "search": {
            "expression": "wellness"
          },
          "filter": {
            "status": {
              "$eq": "PUBLISHED"
            },
            "restrictions.accessType": {
              "$in": [
                "PUBLIC",
                "PRIVATE"
              ]
            }
          },
          "cursorPaging": {
            "limit": 20
          }
        }
      }'
      ```

      ```json Response
      {
        "programs": [
          {
            "id": "82814e00-dc1c-468b-ba80-6c0ed4bd3ee3",
            "revision": "6",
            "createdDate": "2026-06-01T10:31:01.144Z",
            "updatedDate": "2026-06-01T12:42:53.482Z",
            "status": "PUBLISHED",
            "statusUpdatedDate": "2026-06-01T10:31:02.882Z",
            "categoryIds": [],
            "description": {
              "title": "Effective English Learning for Professionals",
              "details": "A practical program for improving English communication in professional settings."
            },
            "timeline": {
              "selfPaced": true
            },
            "restrictions": {
              "hideFutureSteps": false,
              "resolveStepsInOrder": false,
              "shareProgress": true,
              "accessType": "PUBLIC"
            },
            "price": {
              "value": "25",
              "currency": "USD"
            },
            "seo": {
              "slug": "effective-english-learning-for-professionals",
              "courseWorkload": "P6D"
            },
            "rewards": [
              {
                "trigger": "ALL_STEPS_COMPLETED",
                "badgeIds": [
                  "ebcd57c1-0f9d-4090-a13b-d19deffecd16"
                ]
              }
            ],
            "shouldSendInvoice": false,
            "videoStepsSettings": {
              "autoNavigate": false,
              "requiredCompletionPercentage": 0
            },
            "contentSummary": {
              "stepCount": 6,
              "sectionCount": 2
            },
            "ownerUserId": "2b2f5a4d-93f1-4d71-a8a6-10235c50d9f3"
          },
          {
            "id": "103d2b17-4fa0-4042-beac-e15d7ba17e4f",
            "revision": "5",
            "createdDate": "2026-06-01T10:31:01.144Z",
            "updatedDate": "2026-06-01T11:53:44.252Z",
            "status": "PUBLISHED",
            "statusUpdatedDate": "2026-06-01T10:31:02.820Z",
            "categoryIds": [],
            "description": {
              "title": "Mastering English Fluency: An Intermediate Approach"
            },
            "timeline": {
              "selfPaced": true
            },
            "restrictions": {
              "hideFutureSteps": false,
              "resolveStepsInOrder": false,
              "shareProgress": true,
              "accessType": "PUBLIC"
            },
            "seo": {
              "slug": "103d2b17-4fa0-4042-beac-e15d7ba17e4f",
              "courseWorkload": "P6D"
            },
            "rewards": [
              {
                "trigger": "ALL_STEPS_COMPLETED",
                "badgeIds": [],
                "certificate": {
                  "id": "103d2b17-4fa0-4042-beac-e15d7ba17e4f",
                  "connectedDate": "2026-06-01T11:53:43.743Z"
                }
              }
            ],
            "shouldSendInvoice": false,
            "videoStepsSettings": {
              "autoNavigate": false,
              "requiredCompletionPercentage": 0
            },
            "contentSummary": {
              "stepCount": 6,
              "sectionCount": 2
            },
            "ownerUserId": "2b2f5a4d-93f1-4d71-a8a6-10235c50d9f3"
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Searches programs by title and structured filters.

    Use this method when you need full-text search over `description.title`.
    Results are visibility-filtered according to the caller identity.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `description.title` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$in` |
    | `_createdDate` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` |  |
    | `timeline.selfPaced` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq` |
    | `restrictions.accessType` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$in` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` |  |
    | `categoryIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$hasAll`, `$hasSome` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/online-programs';

    programs.searchPrograms(search): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search expression, filters, sorting, and cursor paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path.

              Constraints: `maxLength 200`.

            </ParamField>

            <ParamField body="groupBy" type="GroupByAggregation" deprecated>

              Deprecated. Use `nested` instead.

              **Deprecated.** Deprecated. Use `nested` instead. Use `nested` instead. Removal: 2024-03-30.

              _Nested structure omitted for brevity — see the `GroupByAggregation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use auto fuzzy search (allowing typos by a managed proximity algorithm).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="programs" type="Program[]">

      Programs that match the search and caller visibility.

      See the [Program Object](/api-reference/business-management/online-programs/programs/program-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data returned by the search backend.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request).

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-programs" id="ex-sdk-search-programs-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-programs" id="ex-sdk-search-programs-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-programs-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-programs-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">searchPrograms</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchPrograms (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-programs-0"><span className="wix-picker-name">searchPrograms</span></label>
    <label htmlFor="ex-sdk-search-programs-1"><span className="wix-picker-name">searchPrograms (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript searchPrograms
    import { programs } from '@wix/online-programs';

    async function searchPrograms(search) {
      const response = await programs.searchPrograms(search);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchPrograms (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function searchPrograms(search) {
      const response = await myWixClient.programs.searchPrograms(search);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
