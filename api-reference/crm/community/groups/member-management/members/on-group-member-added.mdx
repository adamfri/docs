---
title: "onGroupMemberAdded"
description: "Triggered when a new member is added to a group, either directly by an admin or when a Join Request is approved."
icon: "bolt"
---

Triggered when a new member is added to a group, either directly by an admin or when a Join Request is approved.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_member_added` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupMemberAdded
  import { members } from '@wix/groups';

  members.onGroupMemberAdded((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupMemberAdded (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { members } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        members,
      },
  });

  wixClient.members.onGroupMemberAdded((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
