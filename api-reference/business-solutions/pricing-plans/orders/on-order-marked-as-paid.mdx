---
title: "onOrderMarkedAsPaid"
description: "Triggered when an offline order is marked as paid."
icon: "bolt"
---

Triggered when an offline order is marked as paid.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.v2.order_marked_as_paid` |
| Package | `@wix/pricing-plans` |
| Permissions | `Manage Orders`, `Read Orders`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onOrderMarkedAsPaid
  import { orders } from '@wix/pricing-plans';

  orders.onOrderMarkedAsPaid((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderMarkedAsPaid (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderMarkedAsPaid((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
