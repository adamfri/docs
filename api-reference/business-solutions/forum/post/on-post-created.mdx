---
title: "onPostCreated"
description: "A new post was created."
icon: "bolt"
---

A new post was created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_created` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostCreated
  import { posts } from '@wix/forum';

  posts.onPostCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
