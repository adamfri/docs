---
title: "Delete Note"
description: "Deletes a note."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a note.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-NOTES">Manage Notes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/crm/notes/v2/notes/{noteId}
    ```

    ## Path Params

    <ParamField path="noteId" type="string" required>

      Note ID.

    </ParamField>

    ## Response Object

    Returns `DeleteNoteResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Note Deleted | `wix.crm.notes.v2.note_deleted` | Triggered when a note is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a Note

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/crm/notes/v2/notes/547bb310-8e15-45d2-8118-aa272dcb1d75' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a note.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-NOTES">Manage Notes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { notes } from '@wix/crm';

    notes.deleteNote(noteId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="noteId" type="string" required>

      Note ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Note Deleted | `wix.crm.notes.v2.note_deleted` | Triggered when a note is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-note" id="ex-sdk-delete-note-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-note" id="ex-sdk-delete-note-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-note" id="ex-sdk-delete-note-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-note-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-note-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteNote</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteNote (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteNote (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-note-0"><span className="wix-picker-name">deleteNote</span></label>
    <label htmlFor="ex-sdk-delete-note-1"><span className="wix-picker-name">deleteNote (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-note-2"><span className="wix-picker-name">deleteNote (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteNote
    import { notes } from '@wix/crm';

    async function deleteNote(noteId) {
      const response = await notes.deleteNote(noteId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteNote (self-hosted)
    import { createClient } from '@wix/sdk';
    import { notes } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { notes },
      // Include the auth strategy and host as relevant
    });

    async function deleteNote(noteId) {
      const response = await myWixClient.notes.deleteNote(noteId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteNote (with elevated permissions)
    import { notes } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myDeleteNoteMethod(noteId) {
      const elevatedDeleteNote = auth.elevate(notes.deleteNote);
      const response = await elevatedDeleteNote(noteId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
