---
title: "Create Dns Zone"
description: "Creates a DNS zone in Google's Cloud DNS."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a DNS zone in [Google's Cloud DNS](https://cloud.google.com/dns).

    If there is an existing DNS zone for the domain, it's deleted before
    the new zone is created.

    The call is synchronous, that means it fails in case of an error on
    Google's side.

    You can only set up a single `record` object per DNS record
    type. If you want to specify multiple values for the same record type, you
    must save them in the `values` for the relevant type.

    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.MANAGE_DNS_ZONES">DOMAINS.MANAGE_DNS_ZONES</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/domains/v1/dns-zones
    ```

    ## Body Params

    <ParamField body="dnsZone" type="DnsZone" required>

      DNS zone to create.

      See the [Dns Zone Object](/api-reference/account-level/domains/domain-dns/dns-zone-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="dnsZone" type="DnsZone">

      Created DNS zone.

      See the [Dns Zone Object](/api-reference/account-level/domains/domain-dns/dns-zone-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST
      'https://www.wixapis.com/domains/v1/dns-zones' \
      -H 'wix-account-id: <ACCOUNT_ID>' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "dnsZone": {
          "records": [
            {
              "values": [
                "10 aspmx.l.google.com"
              ],
              "ttl": 3600,
              "hostName": "mysite.com",
              "type": "MX"
            }
          ],
          "domainName": "mysite.com"
        }
      }'
      ```

      ```json Response
      {
        "dnsZone": {
          "records": [
            {
              "values": [
                "10 aspmx.l.google.com"
              ],
              "ttl": 3600,
              "hostName": "mysite.com",
              "type": "MX"
            }
          ],
          "domainName": "mysite.com"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a DNS zone in [Google's Cloud DNS](https://cloud.google.com/dns).

    If there is an existing DNS zone for the domain, it's deleted before
    the new zone is created.

    The call is synchronous, that means it fails in case of an error on
    Google's side.

    You can only set up a single `record` object per DNS record
    type. If you want to specify multiple values for the same record type, you
    must save them in the `values` for the relevant type.

    <Note>

    This call requires an account level API key and cannot be authenticated with the standard authorization header.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="DOMAINS.MANAGE_DNS_ZONES">DOMAINS.MANAGE_DNS_ZONES</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { domainDns } from '@wix/domains';

    domainDns.createDnsZone(dnsZone): Promise<DnsZone>
    ```

    ## Method Parameters

    <ParamField body="dnsZone" type="DnsZone" required>

      DNS zone to create.

      See the [Dns Zone Object](/api-reference/account-level/domains/domain-dns/dns-zone-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<DnsZone>`.

    See the [Dns Zone Object](/api-reference/account-level/domains/domain-dns/dns-zone-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Dns Zone Created | `wix.premium.domains.v1.dns_zone_created` | Triggered when a dns zone was created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-dns-zone" id="ex-sdk-create-dns-zone-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-dns-zone" id="ex-sdk-create-dns-zone-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-dns-zone-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-dns-zone-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a DNS zone with an API Key</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createDnsZone (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-dns-zone-0"><span className="wix-picker-name">Create a DNS zone with an API Key</span></label>
    <label htmlFor="ex-sdk-create-dns-zone-1"><span className="wix-picker-name">createDnsZone (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a DNS zone with an API Key
    import { createClient, ApiKeyStrategy } from "@wix/sdk";
    import { domainDns } from "@wix/domains";

    const wixClient = createClient({
      modules: { domainDns },
      auth: ApiKeyStrategy({
        siteId: "MY-SITE-ID",
        apiKey: "MY-API-KEY",
      }),
    });

    async function createDnsZone(dnsZone) {
      const response = await domainDns.createDnsZone(dnsZone);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createDnsZone (self-hosted)
    import { createClient } from '@wix/sdk';
    import { domainDns } from '@wix/domains';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { domainDns },
      // Include the auth strategy and host as relevant
    });

    async function createDnsZone(dnsZone) {
      const response = await myWixClient.domainDns.createDnsZone(dnsZone);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
