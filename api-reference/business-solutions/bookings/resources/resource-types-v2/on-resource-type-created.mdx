---
title: "onResourceTypeCreated"
description: "Triggered when a resource type is created."
icon: "bolt"
---

Triggered when a resource type is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.resources.v2.resource_type_created` |
| Package | `@wix/bookings` |

## Examples

<CodeGroup>

  ```javascript onResourceTypeCreated
  import { resourceTypes } from '@wix/bookings';

  resourceTypes.onResourceTypeCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onResourceTypeCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { resourceTypes } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        resourceTypes,
      },
  });

  wixClient.resourceTypes.onResourceTypeCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
