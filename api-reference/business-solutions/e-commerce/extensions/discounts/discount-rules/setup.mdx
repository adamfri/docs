---
title: "Setup"
description: "To use the DiscountRules API, install the @wix/ecom package using npm or Yarn: or Then import { discountRules } from @wix/ecom:"
icon: "file-lines"
---

<Note>This article applies to the JavaScript SDK only.</Note>

To use the DiscountRules API, install the `@wix/ecom` package using npm or Yarn:
  ```bash
npm install @wix/ecom
```

or

```bash
yarn add @wix/ecom
```

Then import `{ discountRules }` from `@wix/ecom`:

```js
import { discountRules } from '@wix/ecom'
```
