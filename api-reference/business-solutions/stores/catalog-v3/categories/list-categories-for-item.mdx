---
title: "List Categories For Item"
description: "Retrieves a list of categories that contain the specified item."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of categories that contain the specified item.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_READ">Read categories</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/categories/v1/categories/list-categories-for-item
    ```

    ## Body Params

    <ParamField body="item" type="ItemReference" required>

      Item reference info.

      <Expandable title="item properties">

        <ParamField body="catalogItemId" type="string">

          ID of the item within the catalog.

          For Wix Stores, this is the product ID from the [Catalog V3 Products API](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/products-v3/introduction).

          Constraints: `minLength 1`, `maxLength 36`.

        </ParamField>

        <ParamField body="appId" type="string">

          ID of the Wix app providing the catalog.

          Currently, only Wix Stores is supported. Use `"215238eb-22a5-4c36-9e7b-e7c08025e04e"` for all Wix Stores products.

          Constraints: `minLength 1`, `maxLength 36`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="treeReference" type="TreeReference" required>

      Category tree reference details.

      <Expandable title="treeReference properties">

        <ParamField body="appNamespace" type="string" required>

          Namespace of the app whose catalog this category tree belongs to.

          Currently, only `"@wix/stores"` is supported.

          Constraints: `minLength 4`, `maxLength 150`.

        </ParamField>

        <ParamField body="treeKey" type="string">

          Key to differentiate between multiple trees managed by the same app.

          For Wix Stores, pass `null` or omit this field, as each store has only one category tree.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="directCategoryIds" type="string[]">

      IDs of categories that directly contain this item.

      Constraints: `format GUID`, `maxItems 1000`.

    </ResponseField>

    <ResponseField name="allCategoryIds" type="string[]">

      IDs of all categories containing this item, including direct categories and all their ancestor categories.

      Constraints: `format GUID`, `maxItems 1000`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    List all categories that contain the item and their parent category.

    <CodeGroup>

      ```bash Request
      curl -X POST \
       'https://www.wixapis.com/categories/v1/categories/list-categories-for-item' \
       -H 'Content-Type: application/json' \
       -H 'Authorization: <AUTH>' \
       -d '{
             "item": {
               "catalogItemId": "0318a3d8-f31c-4ba3-98c7-387571a2eac4",
               "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e"
             },
             "treeReference": {
               "appNamespace": "@wix/stores"
             }
           }'
      ```

      ```json Response
      {
        "directCategoryIds": [
          "b13e1166-fdf2-425a-a35c-d698edcbafa7"
        ],
        "allCategoryIds": [
          "bbd52b32-21d2-4dbe-bfc5-d320f93f8911",
          "b13e1166-fdf2-425a-a35c-d698edcbafa7"
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of categories that contain the specified item.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_READ">Read categories</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/categories';

    categories.listCategoriesForItem(item, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="item" type="ItemReference" required>

      Item reference info.

      <Expandable title="item properties">

        <ParamField body="catalogItemId" type="string">

          ID of the item within the catalog.

          For Wix Stores, this is the product ID from the [Catalog V3 Products API](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/products-v3/introduction).

          Constraints: `minLength 1`, `maxLength 36`.

        </ParamField>

        <ParamField body="appId" type="string">

          ID of the Wix app providing the catalog.

          Currently, only Wix Stores is supported. Use `"215238eb-22a5-4c36-9e7b-e7c08025e04e"` for all Wix Stores products.

          Constraints: `minLength 1`, `maxLength 36`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="ListCategoriesForItemOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="treeReference" type="TreeReference" required>

          Category tree reference details.

          <Expandable title="treeReference properties">

            <ParamField body="appNamespace" type="string" required>

              Namespace of the app whose catalog this category tree belongs to.

              Currently, only `"@wix/stores"` is supported.

              Constraints: `minLength 4`, `maxLength 150`.

            </ParamField>

            <ParamField body="treeKey" type="string">

              Key to differentiate between multiple trees managed by the same app.

              For Wix Stores, pass `null` or omit this field, as each store has only one category tree.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="directCategoryIds" type="string[]">

      IDs of categories that directly contain this item.

      Constraints: `format GUID`, `maxItems 1000`.

    </ResponseField>

    <ResponseField name="allCategoryIds" type="string[]">

      IDs of all categories containing this item, including direct categories and all their ancestor categories.

      Constraints: `format GUID`, `maxItems 1000`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-categories-for-item" id="ex-sdk-list-categories-for-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-categories-for-item" id="ex-sdk-list-categories-for-item-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-categories-for-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-categories-for-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listCategoriesForItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listCategoriesForItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-categories-for-item-0"><span className="wix-picker-name">listCategoriesForItem</span></label>
    <label htmlFor="ex-sdk-list-categories-for-item-1"><span className="wix-picker-name">listCategoriesForItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listCategoriesForItem
    import { categories } from '@wix/categories';

    async function listCategoriesForItem(item,options) {
      const response = await categories.listCategoriesForItem(item,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listCategoriesForItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/categories';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function listCategoriesForItem(item,options) {
      const response = await myWixClient.categories.listCategoriesForItem(item,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
