---
title: "Update Add On Group"
description: "Updates an add-on group."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates an add-on group.

    To update only the display order for add-ons, call [Set Add Ons For Group](https://dev.wix.com/docs/rest/business-solutions/bookings/services/services-v2/set-add-ons-for-group) instead.

    You can't change which service an add-on group belongs to.
    Each add-on group is permanently associated with the service it was created for.
    To use the same add-ons with a different service, create a new add-on group for that service.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/services/add-on-groups/update
    ```

    ## Body Params

    <ParamField body="addOnGroup" type="AddOnGroup" required>

      Add-on group to update.

      <Expandable title="addOnGroup properties">

        <ParamField body="id" type="string" required>

          ID of the add-on group.
          Wix Bookings automatically populates this field when creating or updating an add-on group.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Name of the add-on group.

        </ParamField>

        <ParamField body="maxNumberOfAddOns" type="integer">

          Maximum number of different [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) from the group customers can add when booking the service.
          When empty, there's no upper limit.

        </ParamField>

        <ParamField body="addOnIds" type="string[]">

          List of IDs of all [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) included in the group.

          Constraints: `format GUID`, `maxItems 7`.

        </ParamField>

        <ParamField body="prompt" type="string">

          Description or instructional prompt of the add-on group that's displayed to customers when booking the service.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="serviceId" type="string" required>

      ID of the service that contains the add-on group.

    </ParamField>

    ## Response Object

    <ResponseField name="addOnGroup" type="AddOnGroup">

      Updated add-on group.

      <Expandable title="addOnGroup properties">

        <ResponseField name="id" type="string">

          ID of the add-on group.
          Wix Bookings automatically populates this field when creating or updating an add-on group.

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Name of the add-on group.

        </ResponseField>

        <ResponseField name="maxNumberOfAddOns" type="integer">

          Maximum number of different [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) from the group customers can add when booking the service.
          When empty, there's no upper limit.

        </ResponseField>

        <ResponseField name="addOnIds" type="string[]">

          List of IDs of all [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) included in the group.

          Constraints: `format GUID`, `maxItems 7`.

        </ResponseField>

        <ResponseField name="prompt" type="string">

          Description or instructional prompt of the add-on group that's displayed to customers when booking the service.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `GROUP_NOT_ON_SERVICE` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Service Updated | `wix.bookings.services.v2.service_updated` | Triggered when a service is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/bookings/v2/services/add-on-groups/update' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "addOnGroup": {
          "id": "f8e7d6c5-b4a3-9281-7605-948372615038",
          "name": "Premium Spa Treatments",
          "maxNumberOfAddOns": 3,
          "prompt": "Select up to 3 luxurious spa treatments for the ultimate relaxation"
        },
        "serviceId": "d779a301-398d-4552-aa8c-3bef0b65cedb"
      }'
      ```

      ```json Response
      {
        "addOnGroup": {
          "id": "f8e7d6c5-b4a3-9281-7605-948372615038",
          "name": "Premium Spa Treatments",
          "maxNumberOfAddOns": 3,
          "addOnIds": [
            "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "b2c3d4e5-f6g7-8901-bcde-f23456789012"
          ],
          "prompt": "Select up to 3 luxurious spa treatments for the ultimate relaxation"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates an add-on group.

    To update only the display order for add-ons, call [Set Add Ons For Group](https://dev.wix.com/docs/rest/business-solutions/bookings/services/services-v2/set-add-ons-for-group) instead.

    You can't change which service an add-on group belongs to.
    Each add-on group is permanently associated with the service it was created for.
    To use the same add-ons with a different service, create a new add-on group for that service.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { services } from '@wix/bookings';

    services.updateAddOnGroup(addOnGroup, options): Promise
    ```

    ## Method Parameters

    <ParamField body="addOnGroup" type="AddOnGroup" required>

      Add-on group to update.

      <Expandable title="addOnGroup properties">

        <ParamField body="_id" type="string" required>

          ID of the add-on group.
          Wix Bookings automatically populates this field when creating or updating an add-on group.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Name of the add-on group.

        </ParamField>

        <ParamField body="maxNumberOfAddOns" type="integer">

          Maximum number of different [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) from the group customers can add when booking the service.
          When empty, there's no upper limit.

        </ParamField>

        <ParamField body="addOnIds" type="string[]">

          List of IDs of all [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) included in the group.

          Constraints: `format GUID`, `maxItems 7`.

        </ParamField>

        <ParamField body="prompt" type="string">

          Description or instructional prompt of the add-on group that's displayed to customers when booking the service.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateAddOnGroupOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="serviceId" type="string" required>

          ID of the service that contains the add-on group.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="addOnGroup" type="AddOnGroup">

      Updated add-on group.

      <Expandable title="addOnGroup properties">

        <ResponseField name="_id" type="string">

          ID of the add-on group.
          Wix Bookings automatically populates this field when creating or updating an add-on group.

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Name of the add-on group.

        </ResponseField>

        <ResponseField name="maxNumberOfAddOns" type="integer">

          Maximum number of different [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) from the group customers can add when booking the service.
          When empty, there's no upper limit.

        </ResponseField>

        <ResponseField name="addOnIds" type="string[]">

          List of IDs of all [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) included in the group.

          Constraints: `format GUID`, `maxItems 7`.

        </ResponseField>

        <ResponseField name="prompt" type="string">

          Description or instructional prompt of the add-on group that's displayed to customers when booking the service.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `GROUP_NOT_ON_SERVICE` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Service Updated | `wix.bookings.services.v2.service_updated` | Triggered when a service is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-add-on-group" id="ex-sdk-update-add-on-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-add-on-group" id="ex-sdk-update-add-on-group-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-add-on-group" id="ex-sdk-update-add-on-group-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-add-on-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-add-on-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateAddOnGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateAddOnGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateAddOnGroup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-add-on-group-0"><span className="wix-picker-name">updateAddOnGroup</span></label>
    <label htmlFor="ex-sdk-update-add-on-group-1"><span className="wix-picker-name">updateAddOnGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-add-on-group-2"><span className="wix-picker-name">updateAddOnGroup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateAddOnGroup
    import { services } from '@wix/bookings';

    async function updateAddOnGroup(addOnGroup,options) {
      const response = await services.updateAddOnGroup(addOnGroup,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateAddOnGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { services } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { services },
      // Include the auth strategy and host as relevant
    });

    async function updateAddOnGroup(addOnGroup,options) {
      const response = await myWixClient.services.updateAddOnGroup(addOnGroup,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateAddOnGroup (with elevated permissions)
    import { services } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myUpdateAddOnGroupMethod(addOnGroup,options) {
      const elevatedUpdateAddOnGroup = auth.elevate(services.updateAddOnGroup);
      const response = await elevatedUpdateAddOnGroup(addOnGroup,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
