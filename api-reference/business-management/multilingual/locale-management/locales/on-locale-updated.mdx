---
title: "onLocaleUpdated"
description: "Triggered when a locale is updated."
icon: "bolt"
---

Triggered when a locale is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.locales.v2.locale_updated` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API` |

## Examples

<CodeGroup>

  ```javascript onLocaleUpdated
  import { locales } from '@wix/multilingual';

  locales.onLocaleUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocaleUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { locales } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        locales,
      },
  });

  wixClient.locales.onLocaleUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
