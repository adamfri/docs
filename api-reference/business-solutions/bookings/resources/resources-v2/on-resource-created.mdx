---
title: "onResourceCreated"
description: "Triggered when a resource is created."
icon: "bolt"
---

Triggered when a resource is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.resources.v2.resource_created` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onResourceCreated
  import { resources } from '@wix/bookings';

  resources.onResourceCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onResourceCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { resources } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        resources,
      },
  });

  wixClient.resources.onResourceCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
