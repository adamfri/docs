---
title: "Query Gift Card Transactions"
description: "Retrieves the transaction history for a specific gift card with filtering, sorting, and paging options."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the transaction history for a specific gift card with filtering, sorting, and paging options.

    Use this method to implement transaction history displays, generate audit reports,
    or investigate gift card balance changes. Transactions are automatically created
    for all gift card activities including redemptions, refunds, and manual adjustments.

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `type` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `operationType` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/gift-cards/v1/gift-cards/{giftCardId}/transactions/query
    ```

    ## Path Params

    <ParamField path="giftCardId" type="string" required>

      Gift card ID to retrieve transactions for.

    </ParamField>

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query expression to filter, sort, and page through transactions.
      Use to search by transaction type, creation date, or operation type.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="transactions" type="Transaction[]">

      The retrieved transactions matching the query criteria, ordered by creation date.

      See the [Transaction Object](/api-reference/business-solutions/gift-cards/transactions/transaction-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging information for navigating through large transaction histories.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.
          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Queries existing gift card transactions sorted by created date in descending order

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/gift-cards/v1/gift-cards/4923b8be-fdcb-4f35-ad69-2e6508cba5b6/transactions/query' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "query": {
            "sort": [
              {
                "fieldName": "createdDate",
                "order":"DESC"
              }
            ]
          }
        }'
      ```

      ```json Response
      {
        "transactions": [
          {
            "id": "b3672ef0-20dd-41ef-9075-7e9241a70e59",
            "createdDate": "2024-09-26T06:43:05.635Z",
            "type": "REDEEM",
            "giftCardId": "4923b8be-fdcb-4f35-ad69-2e6508cba5b6",
            "amount": {
              "amount": "52",
              "formattedAmount": "$52.00"
            },
            "updatedBalance": {
              "amount": "0",
              "formattedAmount": "$0.00"
            },
            "operationType": "SUBTRACT",
            "orderInfo": {
              "orderId": "b9504127-bbe6-46fa-84d0-ebabdfd55c2a",
              "orderNumber": null
            }
          },
          {
            "id": "8c5eba2d-ef5a-46ac-994f-7d625d3cccf9",
            "createdDate": "2024-09-24T12:44:36.190Z",
            "type": "REDEEM",
            "giftCardId": "4923b8be-fdcb-4f35-ad69-2e6508cba5b6",
            "amount": {
              "amount": "19",
              "formattedAmount": "$19.00"
            },
            "updatedBalance": {
              "amount": "52",
              "formattedAmount": "$52.00"
            },
            "operationType": "SUBTRACT",
            "orderInfo": {
              "orderId": "e644a728-1436-4cc3-a8a1-85b8abf78221",
              "orderNumber": null
            }
          },
          {
            "id": "d78d57bb-6516-4070-9340-817155e1cea6",
            "createdDate": "2024-09-24T12:43:53.679Z",
            "type": "REDEEM",
            "giftCardId": "4923b8be-fdcb-4f35-ad69-2e6508cba5b6",
            "amount": {
              "amount": "29",
              "formattedAmount": "$29.00"
            },
            "updatedBalance": {
              "amount": "71",
              "formattedAmount": "$71.00"
            },
            "operationType": "SUBTRACT",
            "orderInfo": {
              "orderId": "22714a3e-6ba2-43b9-b269-600cfa13e10c",
              "orderNumber": null
            }
          },
          {
            "id": "146008c5-c9af-44bb-9283-03ce6554cbb2",
            "createdDate": "2024-09-24T12:34:34.649Z",
            "type": "INITIAL",
            "giftCardId": "4923b8be-fdcb-4f35-ad69-2e6508cba5b6",
            "amount": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "updatedBalance": {
              "amount": "100",
              "formattedAmount": "$100.00"
            },
            "operationType": "ADD",
            "orderInfo": null
          }
        ],
        "pagingMetadata": {
          "count": 4,
          "cursors": {
            "next": null,
            "prev": null
          },
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves the transaction history for a specific gift card with filtering, sorting, and paging options.

    Use this method to implement transaction history displays, generate audit reports,
    or investigate gift card balance changes. Transactions are automatically created
    for all gift card activities including redemptions, refunds, and manual adjustments.

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `type` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `operationType` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { transactions } from '@wix/gift-vouchers';

    transactions.queryGiftCardTransactions(query, options): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="TransactionQuery" required>

      Query expression to filter, sort, and page through transactions.
      Use to search by transaction type, creation date, or operation type.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QueryGiftCardTransactionsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="giftCardId" type="string" required>

          Gift card ID to retrieve transactions for.

          Constraints: `format GUID`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="transactions" type="Transaction[]">

      The retrieved transactions matching the query criteria, ordered by creation date.

      See the [Transaction Object](/api-reference/business-solutions/gift-cards/transactions/transaction-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging information for navigating through large transaction histories.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.
          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-gift-card-transactions" id="ex-sdk-query-gift-card-transactions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-gift-card-transactions" id="ex-sdk-query-gift-card-transactions-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-gift-card-transactions" id="ex-sdk-query-gift-card-transactions-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-gift-card-transactions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-gift-card-transactions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryGiftCardTransactions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryGiftCardTransactions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryGiftCardTransactions (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-gift-card-transactions-0"><span className="wix-picker-name">queryGiftCardTransactions</span></label>
    <label htmlFor="ex-sdk-query-gift-card-transactions-1"><span className="wix-picker-name">queryGiftCardTransactions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-gift-card-transactions-2"><span className="wix-picker-name">queryGiftCardTransactions (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryGiftCardTransactions
    import { transactions } from '@wix/gift-vouchers';

    async function queryGiftCardTransactions(query,options) {
      const response = await transactions.queryGiftCardTransactions(query,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryGiftCardTransactions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { transactions } from '@wix/gift-vouchers';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { transactions },
      // Include the auth strategy and host as relevant
    });

    async function queryGiftCardTransactions(query,options) {
      const response = await myWixClient.transactions.queryGiftCardTransactions(query,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryGiftCardTransactions (with elevated permissions)
    import { transactions } from '@wix/gift-vouchers';
    import { auth } from '@wix/essentials';

    async function myQueryGiftCardTransactionsMethod(query,options) {
      const elevatedQueryGiftCardTransactions = auth.elevate(transactions.queryGiftCardTransactions);
      const response = await elevatedQueryGiftCardTransactions(query,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
