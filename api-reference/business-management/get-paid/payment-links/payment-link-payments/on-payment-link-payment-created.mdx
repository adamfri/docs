---
title: "onPaymentLinkPaymentCreated"
description: "Triggered when a payment link payment is created."
icon: "bolt"
---

Triggered when a payment link payment is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.paymentlinks.payments.v1.payment_link_payment_created` |
| Package | `@wix/get-paid` |

## Examples

<CodeGroup>

  ```javascript onPaymentLinkPaymentCreated
  import { paymentLinkPayments } from '@wix/get-paid';

  paymentLinkPayments.onPaymentLinkPaymentCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPaymentLinkPaymentCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { paymentLinkPayments } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        paymentLinkPayments,
      },
  });

  wixClient.paymentLinkPayments.onPaymentLinkPaymentCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
