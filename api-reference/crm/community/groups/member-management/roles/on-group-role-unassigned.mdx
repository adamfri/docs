---
title: "onGroupRoleUnassigned"
description: "Triggered when a role is unassigned from a group member."
icon: "bolt"
---

Triggered when a role is unassigned from a group member.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_role_unassigned` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupRoleUnassigned
  import { roles } from '@wix/groups';

  roles.onGroupRoleUnassigned((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupRoleUnassigned (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { roles } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        roles,
      },
  });

  wixClient.roles.onGroupRoleUnassigned((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
