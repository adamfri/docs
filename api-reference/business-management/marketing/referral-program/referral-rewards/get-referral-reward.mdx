---
title: "Get Referral Reward"
description: "Retrieves a referral reward."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referral reward.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/_api/referral-rewards/v1/referral-rewards/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Referral reward ID.

    </ParamField>

    ## Response Object

    <ResponseField name="referralReward" type="ReferralReward">

      Retrieved referral reward.

      See the [Referral Reward Object](/api-reference/business-management/marketing/referral-program/referral-rewards/referral-reward-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/_api/referral-rewards/v1/referral-rewards/c8c732cf-2ca2-4025-9b1c-3be02f099cdb' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
          "referralReward": {
              "id": "c8c732cf-2ca2-4025-9b1c-3be02f099cdb",
              "revision": "2",
              "createdDate": "2024-07-08T11:22:00.728Z",
              "updatedDate": "2024-07-08T11:27:57.197Z",
              "rewardType": "COUPON",
              "rewardedReferringCustomerId": "4fb41de4-da9c-4818-b54f-82c955ecc866",
              "coupon": {
                  "id": "43fa238a-b464-4a5b-9079-5682a5a1eab7",
                  "code": "6QW1ASGTZ0Q4",
                  "status": "APPLIED",
                  "couponSpecification": {
                      "name": "Your reward",
                      "discountType": "FIXED_AMOUNT",
                      "limitedToOneItem": true,
                      "appliesToSubscriptions": null,
                      "discountedCycleCount": null,
                      "fixedAmountOptions": {
                          "amount": 10
                      },
                      "scope": {
                          "namespace": "stores",
                          "group": null
                      }
                  }
              }
          }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a referral reward.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rewards } from '@wix/referral';

    rewards.getReferralReward(_id): Promise<ReferralReward>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Referral reward ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<ReferralReward>`.

    See the [Referral Reward Object](/api-reference/business-management/marketing/referral-program/referral-rewards/referral-reward-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-referral-reward" id="ex-sdk-get-referral-reward-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-referral-reward" id="ex-sdk-get-referral-reward-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-referral-reward" id="ex-sdk-get-referral-reward-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-referral-reward-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-referral-reward-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getReferralReward</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferralReward (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getReferralReward (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-referral-reward-0"><span className="wix-picker-name">getReferralReward</span></label>
    <label htmlFor="ex-sdk-get-referral-reward-1"><span className="wix-picker-name">getReferralReward (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-referral-reward-2"><span className="wix-picker-name">getReferralReward (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getReferralReward
    import { rewards } from '@wix/referral';

    async function getReferralReward(_id) {
      const response = await rewards.getReferralReward(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferralReward (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rewards } from '@wix/referral';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rewards },
      // Include the auth strategy and host as relevant
    });

    async function getReferralReward(_id) {
      const response = await myWixClient.rewards.getReferralReward(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getReferralReward (with elevated permissions)
    import { rewards } from '@wix/referral';
    import { auth } from '@wix/essentials';

    async function myGetReferralRewardMethod(_id) {
      const elevatedGetReferralReward = auth.elevate(rewards.getReferralReward);
      const response = await elevatedGetReferralReward(_id);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
