---
title: "Bulk Assign Events"
description: "Assigns events to multiple categories at once."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Assigns events to multiple categories at once.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/bulk/categories/events
    ```

    ## Body Params

    <ParamField body="categoryId" type="string[]" required>

      A list of category IDs to which events should be assigned.

      Constraints: `format GUID`, `minItems 1`, `maxItems 10`.

    </ParamField>

    <ParamField body="eventId" type="string[]" required>

      A list of events IDs.

      Constraints: `format GUID`, `minItems 1`, `maxItems 1`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkCategoryResult[]">

      Bulk assign results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Category">

          Category.

          See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata of results.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/events/v1/bulk/categories/events' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "categoryId": [
          "e9779de3-a085-4255-b3a6-9559990d4436",
          "027ac25d-cc64-4eb1-8666-4ea53cf7f134"
         ],
        "eventId": [
          "539a564e-48d2-4eac-8e14-d7a91be6b1c2"
        ]
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "results": [
          {
            "itemMetadata": {
              "id": "027ac25d-cc64-4eb1-8666-4ea53cf7f134",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "027ac25d-cc64-4eb1-8666-4ea53cf7f134",
              "name": "2",
              "createdDate": "2021-03-25T10:16:57.486Z"
            }
          },
          {
            "itemMetadata": {
              "id": "e9779de3-a085-4255-b3a6-9559990d4436",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "id": "e9779de3-a085-4255-b3a6-9559990d4436",
              "name": "technology",
              "createdDate": "2021-04-01T09:49:37.068Z"
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Assigns events to multiple categories at once.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/events';

    categories.bulkAssignEvents(categoryId, options): Promise<BulkAssignEventsResponse>
    ```

    ## Method Parameters

    <ParamField body="categoryId" type="string[]" required>

      A list of category IDs to which events should be assigned.

      Constraints: `format GUID`, `minItems 1`, `maxItems 10`.

    </ParamField>

    <ParamField body="options" type="BulkAssignEventsOptions" required>

      Options to use when assigning events to multiple categories.

      <Expandable title="options properties">

        <ParamField body="eventId" type="string[]" required>

          A list of events IDs.

          Constraints: `format GUID`, `minItems 1`, `maxItems 1`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkAssignEventsResponse>`.

    <ResponseField name="results" type="BulkCategoryResult[]">

      Bulk assign results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Category">

          Category.

          See the [Category Object](/api-reference/business-solutions/events/event-management/categories/category-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata of results.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-assign-events" id="ex-sdk-bulk-assign-events-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-assign-events" id="ex-sdk-bulk-assign-events-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-assign-events" id="ex-sdk-bulk-assign-events-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-assign-events" id="ex-sdk-bulk-assign-events-3" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-assign-events" id="ex-sdk-bulk-assign-events-4" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-assign-events-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-assign-events-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk assign events to multiple categories</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Bulk assign events to multiple categories (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Bulks assign events to multiple categories</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Bulks assign events to multiple categories (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkAssignEvents (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-assign-events-0"><span className="wix-picker-name">Bulk assign events to multiple categories</span></label>
    <label htmlFor="ex-sdk-bulk-assign-events-1"><span className="wix-picker-name">Bulk assign events to multiple categories (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-bulk-assign-events-2"><span className="wix-picker-name">Bulks assign events to multiple categories</span></label>
    <label htmlFor="ex-sdk-bulk-assign-events-3"><span className="wix-picker-name">Bulks assign events to multiple categories (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-bulk-assign-events-4"><span className="wix-picker-name">bulkAssignEvents (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk assign events to multiple categories
    import { categories } from '@wix/events';

    /* 
     * Sample categoryId value: ["89d3e8d4-45bc-4439-9e74-7e21b3418c88", "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"]
     * Sample options value: 
     * {
     *  "eventId": ["4e5e4adb-9778-4171-a9bb-44e27834ac89"]
     * }
     */

    export async function myBulkAssignEventsFunction(categoryId, options) {
      try {
        const assignedEvents = await categories.bulkAssignEvents(categoryId, options);
        console.log('Bulk assign results:', assignedEvents);
        return assignedEvents;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *  "results": [
     *   {
     *     "itemMetadata": {
     *       "originalIndex": 0,
     *       "success": true,
     *       "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"
     *     },
     *     "item": {
     *       "name": "workshop",
     *       "states": [
     *         "MANUAL"
     *       ],
     *       "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a",
     *       "_createdDate": "2022-12-13T11:03:19.174Z"
     *     }
     *   },
     *   {
     *     "itemMetadata": {
     *      "originalIndex": 1,
     *       "success": true,
     *       "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88"
     *     },
     *     "item": {
     *       "name": "technology",
     *       "states": [
     *         "MANUAL"
     *       ],
     *       "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88",
     *       "_createdDate": "2023-07-12T11:07:35.776Z"
     *     }
     *   }
     * ],
     * "bulkActionMetadata": {
     *   "totalSuccesses": 2,
     *   "totalFailures": 0,
     *   "undetailedFailures": 0
     * }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Bulk assign events to multiple categories (with elevated permissions)
    import { categories } from '@wix/events';
    import { auth } from '@wix/essentials';

    const elevatedBulkAssignEvents = auth.elevate(categories.bulkAssignEvents);

    /* 
     * Sample categoryId value: ["89d3e8d4-45bc-4439-9e74-7e21b3418c88", "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"]
     * Sample options value: 
     * {
     *  "eventId": ["4e5e4adb-9778-4171-a9bb-44e27834ac89"]
     * }
     */

    export async function myBulkAssignEventsFunction(categoryId, options) {
      try {
        const assignedEvents = await elevatedBulkAssignEvents(categoryId, options);
        console.log('Bulk assign results:', assignedEvents);
        return assignedEvents;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *  "results": [
     *   {
     *     "itemMetadata": {
     *       "originalIndex": 0,
     *       "success": true,
     *       "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"
     *     },
     *     "item": {
     *       "name": "workshop",
     *       "states": [
     *         "MANUAL"
     *       ],
     *       "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a",
     *       "_createdDate": "2022-12-13T11:03:19.174Z"
     *     }
     *   },
     *   {
     *     "itemMetadata": {
     *      "originalIndex": 1,
     *       "success": true,
     *       "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88"
     *     },
     *     "item": {
     *       "name": "technology",
     *       "states": [
     *         "MANUAL"
     *       ],
     *       "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88",
     *       "_createdDate": "2023-07-12T11:07:35.776Z"
     *     }
     *   }
     * ],
     * "bulkActionMetadata": {
     *   "totalSuccesses": 2,
     *   "totalFailures": 0,
     *   "undetailedFailures": 0
     * }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Bulks assign events to multiple categories
    import { categories } from '@wix/events';

    /* 
     * Sample categoryId value: ["89d3e8d4-45bc-4439-9e74-7e21b3418c88", "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"]
     * Sample options value: 
     * {
     *  "eventId": ["4e5e4adb-9778-4171-a9bb-44e27834ac89"]
     * }
     */

    export async function myBulkAssignEventsFunction(categoryId, options) {
      try {
        const assignedEvents = await categories.bulkAssignEvents(categoryId, options);
        console.log('Bulk assign results: ', assignedEvents);
        return assignedEvents;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 0,
     *         "success": true,
     *         "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"
     *       },
     *       "item": {
     *         "name": "workshop",
     *         "states": [
     *           "MANUAL"
     *         ],
     *         "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a",
     *         "_createdDate": "2022-12-13T11:03:19.174Z"
     *       }
     *     },
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 1,
     *         "success": true,
     *         "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88"
     *       },
     *       "item": {
     *         "name": "technology",
     *         "states": [
     *           "MANUAL"
     *         ],
     *         "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88",
     *         "_createdDate": "2023-07-12T11:07:35.776Z"
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 2,
     *     "totalFailures": 0,
     *     "undetailedFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Bulks assign events to multiple categories (with elevated permissions)
    import { categories } from '@wix/events';
    import { auth } from '@wix/essentials';

    const elevatedBulkAssignEvents = auth.elevate(categories.bulkAssignEvents);

    /* 
     * Sample categoryId value: ["89d3e8d4-45bc-4439-9e74-7e21b3418c88", "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"]
     * Sample options value: 
     * {
     *  "eventId": ["4e5e4adb-9778-4171-a9bb-44e27834ac89"]
     * }
     */

    export async function myBulkAssignEventsFunction(categoryId, options) {
      try {
        const assignedEvents = await elevatedBulkAssignEvents(categoryId, options);
        console.log('Bulk assign results: ', assignedEvents);
        return assignedEvents;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 0,
     *         "success": true,
     *         "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a"
     *       },
     *       "item": {
     *         "name": "workshop",
     *         "states": [
     *           "MANUAL"
     *         ],
     *         "_id": "6ec293a8-1b47-4337-9c4e-9a6aeb35e66a",
     *         "_createdDate": "2022-12-13T11:03:19.174Z"
     *       }
     *     },
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 1,
     *         "success": true,
     *         "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88"
     *       },
     *       "item": {
     *         "name": "technology",
     *         "states": [
     *           "MANUAL"
     *         ],
     *         "_id": "89d3e8d4-45bc-4439-9e74-7e21b3418c88",
     *         "_createdDate": "2023-07-12T11:07:35.776Z"
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 2,
     *     "totalFailures": 0,
     *     "undetailedFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkAssignEvents (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function bulkAssignEvents(categoryId,options) {
      const response = await myWixClient.categories.bulkAssignEvents(categoryId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
