---
title: "Delete Supplier"
description: "Permanently deletes a supplier from the marketplace."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Permanently deletes a supplier from the marketplace. This action cannot be undone. Use this endpoint when removing suppliers that are no longer active in your network.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.WRITE_MARKETPLACE">Write Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/suppliers-hub/v1/suppliers/{supplierId}
    ```

    ## Path Params

    <ParamField path="supplierId" type="string" required>

      Id of the Supplier to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteSupplierResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Supplier Deleted | `wix.suppliershub.marketplace_supplier.v1.supplier_deleted` | Triggered when a supplier is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Permanently removes a supplier from the marketplace

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/suppliers-hub/v1/suppliers/12345678-1234-1234-1234-123456789012' \
      -H 'Authorization: <AUTH>'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Permanently deletes a supplier from the marketplace. This action cannot be undone. Use this endpoint when removing suppliers that are no longer active in your network.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.WRITE_MARKETPLACE">Write Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { suppliers } from '@wix/suppliers-hub';

    suppliers.deleteSupplier(supplierId): Promise
    ```

    ## Method Parameters

    <ParamField body="supplierId" type="string" required>

      Id of the Supplier to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteSupplierResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Supplier Deleted | `wix.suppliershub.marketplace_supplier.v1.supplier_deleted` | Triggered when a supplier is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-supplier" id="ex-sdk-delete-supplier-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-supplier" id="ex-sdk-delete-supplier-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-supplier" id="ex-sdk-delete-supplier-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-supplier-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-supplier-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteSupplier</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSupplier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteSupplier (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-supplier-0"><span className="wix-picker-name">deleteSupplier</span></label>
    <label htmlFor="ex-sdk-delete-supplier-1"><span className="wix-picker-name">deleteSupplier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-supplier-2"><span className="wix-picker-name">deleteSupplier (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteSupplier
    import { suppliers } from '@wix/suppliers-hub';

    async function deleteSupplier(supplierId) {
      const response = await suppliers.deleteSupplier(supplierId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSupplier (self-hosted)
    import { createClient } from '@wix/sdk';
    import { suppliers } from '@wix/suppliers-hub';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { suppliers },
      // Include the auth strategy and host as relevant
    });

    async function deleteSupplier(supplierId) {
      const response = await myWixClient.suppliers.deleteSupplier(supplierId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteSupplier (with elevated permissions)
    import { suppliers } from '@wix/suppliers-hub';
    import { auth } from '@wix/essentials';

    async function myDeleteSupplierMethod(supplierId) {
      const elevatedDeleteSupplier = auth.elevate(suppliers.deleteSupplier);
      const response = await elevatedDeleteSupplier(supplierId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
