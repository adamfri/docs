---
title: "onCampaignPausedEvent"
description: "Triggered when a scheduled campaign is paused."
icon: "bolt"
---

Triggered when a scheduled campaign is paused.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.email_marketing.v1.campaign_campaign_paused_event` |
| Package | `@wix/email-marketing` |
| Permissions | `Manage Stores`, `Access Verticals by Automations`, `Set Up Automations`, `Read Email Marketing`, `Manage Email Marketing` |

## Examples

<CodeGroup>

  ```javascript onCampaignPausedEvent
  import { campaigns } from '@wix/email-marketing';

  campaigns.onCampaignPausedEvent((event) => {
      // handle your event here
  });
  ```

  ```javascript onCampaignPausedEvent (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { campaigns } from '@wix/email-marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        campaigns,
      },
  });

  wixClient.campaigns.onCampaignPausedEvent((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
