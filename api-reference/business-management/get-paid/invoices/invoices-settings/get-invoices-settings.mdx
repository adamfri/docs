---
title: "Get Invoices Settings"
description: "Retrieves a site's invoice settings."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a site's invoice settings.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/invoices/v4/invoices-settings
    ```

    ## Response Object

    <ResponseField name="invoicesSettings" type="InvoicesSettings">

      Retrieved invoice settings.

      See the [Invoices Settings Object](/api-reference/business-management/get-paid/invoices/invoices-settings/invoices-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `NO_META_SITE` | Couldn't find the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a site's invoice settings.

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/invoices-settings/v4/invoices-settings' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "invoicesSettings": {
          "revision": "1",
          "createdDate": "2024-01-15T10:30:00.000Z",
          "updatedDate": "2024-01-15T10:30:00.000Z",
          "numbering": {
            "prefix": "INV-",
            "nextNumber": 1001,
            "suffix": "-2024"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a site's invoice settings.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.INVOICES.MANAGE">Manage Invoices</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { invoicesSettings } from '@wix/get-paid';

    invoicesSettings.getInvoicesSettings(): Promise
    ```

    ## Returns

    <ResponseField name="invoicesSettings" type="InvoicesSettings">

      Retrieved invoice settings.

      See the [Invoices Settings Object](/api-reference/business-management/get-paid/invoices/invoices-settings/invoices-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `NO_META_SITE` | Couldn't find the site. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-invoices-settings" id="ex-sdk-get-invoices-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-invoices-settings" id="ex-sdk-get-invoices-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-invoices-settings" id="ex-sdk-get-invoices-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-invoices-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-invoices-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getInvoicesSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getInvoicesSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getInvoicesSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-invoices-settings-0"><span className="wix-picker-name">getInvoicesSettings</span></label>
    <label htmlFor="ex-sdk-get-invoices-settings-1"><span className="wix-picker-name">getInvoicesSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-invoices-settings-2"><span className="wix-picker-name">getInvoicesSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getInvoicesSettings
    import { invoicesSettings } from '@wix/get-paid';

    async function getInvoicesSettings() {
      const response = await invoicesSettings.getInvoicesSettings();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getInvoicesSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { invoicesSettings } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { invoicesSettings },
      // Include the auth strategy and host as relevant
    });

    async function getInvoicesSettings() {
      const response = await myWixClient.invoicesSettings.getInvoicesSettings();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getInvoicesSettings (with elevated permissions)
    import { invoicesSettings } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myGetInvoicesSettingsMethod() {
      const elevatedGetInvoicesSettings = auth.elevate(invoicesSettings.getInvoicesSettings);
      const response = await elevatedGetInvoicesSettings();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
