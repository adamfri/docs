---
title: "Delete A Coupon"
description: "Deletes a coupon."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a coupon.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-COUPONS.MANAGE-COUPONS">Manage Coupons</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/stores/v2/coupons/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the coupon to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteCouponResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Coupon Deleted | `wix.ecommerce.coupons.v2.coupon_deleted` | Triggered when a coupon is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Delete coupon by ID.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
         'https://www.wixapis.com/stores/v2/coupons/6df8d9ea-9e31-49af-b272-5e22861cce92' \
          -H 'Content-Type: application/json' \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a coupon.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-COUPONS.MANAGE-COUPONS">Manage Coupons</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { coupons } from '@wix/marketing';

    coupons.deleteCoupon(_id): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the coupon to delete.

    </ParamField>

    ## Returns

    Returns `DeleteCouponResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Coupon Deleted | `wix.ecommerce.coupons.v2.coupon_deleted` | Triggered when a coupon is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-a-coupon" id="ex-sdk-delete-a-coupon-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-a-coupon" id="ex-sdk-delete-a-coupon-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-a-coupon" id="ex-sdk-delete-a-coupon-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-a-coupon-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-a-coupon-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a coupon</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Delete a coupon (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCoupon (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-a-coupon-0"><span className="wix-picker-name">Delete a coupon</span></label>
    <label htmlFor="ex-sdk-delete-a-coupon-1"><span className="wix-picker-name">Delete a coupon (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-delete-a-coupon-2"><span className="wix-picker-name">deleteCoupon (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a coupon
    import { coupons } from '@wix/marketing';

    export function deleteCoupon(_id) {
      return coupons.deleteCoupon(_id);
    }

    // Returns a promise that is resolved when
    // the coupon is deleted.
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Delete a coupon (with elevated permissions)
    import { coupons } from '@wix/marketing';
    import { auth } from '@wix/essentials';

    const elevatedDeleteCoupon = auth.elevate(coupons.deleteCoupon);

    export function deleteCoupon(_id) {
      return elevatedDeleteCoupon(_id);
    }

    // Returns a promise that is resolved when
    // the coupon is deleted.
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCoupon (self-hosted)
    import { createClient } from '@wix/sdk';
    import { coupons } from '@wix/marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { coupons },
      // Include the auth strategy and host as relevant
    });

    async function deleteCoupon(_id) {
      const response = await myWixClient.coupons.deleteCoupon(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
