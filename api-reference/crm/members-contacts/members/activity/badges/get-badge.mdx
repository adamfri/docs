---
title: "Get Badge"
description: "Retrieves a badge."
icon: "download"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Retrieves a badge.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-BADGES">Read Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/members/v3/badges/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Badge ID.

    </ParamField>

    ## Response Object

    <ResponseField name="badge" type="Badge">

      Badge.

      See the [Badge Object](/api-reference/crm/members-contacts/members/activity/badges/badge-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Retrieves a badge.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-BADGES">Read Badges</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { badges } from '@wix/members';

    badges.getBadge(_id): Promise<Badge>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Badge ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Badge>`.

    See the [Badge Object](/api-reference/crm/members-contacts/members/activity/badges/badge-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-badge" id="ex-sdk-get-badge-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-badge" id="ex-sdk-get-badge-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-badge-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-badge-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get badge details</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getBadge (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-badge-0"><span className="wix-picker-name">Get badge details</span></label>
    <label htmlFor="ex-sdk-get-badge-1"><span className="wix-picker-name">getBadge (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get badge details
    import { badges } from '@wix/members';

    /* Sample _id value: '50dca3d3-f142-44ca-87e9-d9302e1d4dd5' */

    export async function myGetBadgeFunction(_id) {
      try {
        const badge = await badges.getBadge(_id);

        console.log('Retrieved badge: ', badge);
        return badge;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to: 
     * {
     *  "_createdDate": "2024-01-18T10:27:14.878Z",
     *  "_id": "215be5d9-4b32-4861-9eb5-2152930dd0b4",
     *  "_updatedDate": "2024-01-18T10:27:14.878Z",
     *  "backgroundColor": "#13785C",
     *  "description": "Most comments on site.",
     *  "icon": "https://static.wixstatic.com/shapes/11062b_73d6472a03884c758f9d39f83a1218c2.svg",
     *  "permissionsEnabled": false,
     *  "slug": "top-commenter",
     *  "textColor": "#FFFFFF",
     *  "title": "Top commenter"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getBadge (self-hosted)
    import { createClient } from '@wix/sdk';
    import { badges } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { badges },
      // Include the auth strategy and host as relevant
    });

    async function getBadge(_id) {
      const response = await myWixClient.badges.getBadge(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
