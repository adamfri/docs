---
title: "Query Payment Links"
description: "Retrieves a list of up to 100 payment links, given the specified filters, sorting, and paging (SDK | REST)."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 payment links, given the specified filters, sorting, and paging ([SDK](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/api-query-language) | [REST](https://dev.wix.com/api/rest/getting-started/api-query-language)).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `paymentsLimit` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `source` | `$exists` | Sort: `ASC`, `DESC` |
    | `totalReceived.paymentCount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `totals.paymentCount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `source.ecomReference.orderId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/paymentlinks/v1/payment-links/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="paymentLinks" type="PaymentLink[]">

      Retrieved payment links.

      See the [Payment Link Object](/api-reference/business-management/get-paid/payment-links/payment-links/payment-link-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a list of payment links, given the provided paging, filtering, and sorting.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/payment-links/v1/payment-links/query' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "query": {
            "filter": {
              "status": {
                "$eq": "ACTIVE",
              }
            },
            "sort": [
              {
                "fieldName": "createdDate",
                "order": "DESC"
              }
            ],
            "cursorPaging": {
              "limit": 1
            }
          }
        }'
      ```

      ```json Response
      {
        "paymentLinks": [
          {
            "id": "763736a8-b40b-4c1a-a8ea-f6dfa75e6197",
            "revision": "6",
            "createdDate": "2025-01-13T13:35:21.908Z",
            "updatedDate": "2025-01-13T13:48:19.131Z",
            "title": "My Fast Pay Link",
            "price": "110",
            "currency": "USD",
            "recipients": [
              {
                "contactId": "ef0a5cda-8b90-427d-8189-db40c1f33473",
                "sendMethods": [
                  "EMAIL_METHOD"
                ],
                "lastSendDate": "2025-01-13T13:48:19.129Z",
                "sendSuccessful": true
              }
            ],
            "totals": {
              "balance": "123.5",
              "paymentCount": 1
            },
            "status": "ACTIVE",
            "links": {
              "url": {
                "relativePath": "/_paylink/shortLinkId",
                "url": "https://example.com/mysite/_paylink/shortLinkId"
              }
            },
            "expirationDate": "2025-01-31T00:00:00Z",
            "paymentsLimit": 100,
            "availableActions": [
              "DEACTIVATE",
              "INITIATE_PAYMENT",
              "SEND"
            ],
            "source": {
              "appId": "324cf725-53d9-4bb2-b8f6-0c8ec9a77f45",
              "entityId": "b5710623-31c6-47c4-b8e3-044cdb5c229d"
            },
            "description": "This is a payment for bundle.",
            "lastPaymentDate": "2025-01-12T13:45:43.996Z",
            "note": {
              "updatedDate": "2025-01-12T13:35:21.492Z",
              "text": "This is for special site users."
            },
            "type": "ECOM",
            "ecomPaymentLink": {
              "lineItems": [
                {
                  "type": "CATALOG",
                  "catalogItem": {
                    "id": "53b8e5ac-da00-4103-8bd4-dce86429146a",
                    "quantity": 1,
                    "catalogReference": {
                      "catalogItemId": "315e6584-6895-8feb-3b09-5f00c4f89205",
                      "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                      "options": {
                        "variantId": "67a0948d-6c2e-4828-b6a5-7c7ae05c1e06"
                      }
                    },
                    "catalogOverrideFields": {
                      "price": "110",
                      "fullPrice": "110",
                      "descriptionLines": [
                        {
                          "name": {
                            "original": "Color",
                            "translated": "Color"
                          },
                          "lineType": "UNRECOGNISED",
                          "plainText": {
                            "original": "Red",
                            "translated": "Red"
                          }
                        }
                      ],
                      "physicalProperties": {
                        "weight": 3.4,
                        "sku": "SKU123456",
                        "shippable": true
                      },
                      "image": {
                        "id": "3c76e2_8891bbe3372a428aac976ac59aa0ac74",
                        "height": 1000,
                        "width": 1000
                      }
                    }
                  }
                }
              ]
            }
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {
            "next": "b7b8960913c865645d9ad644297c5cf2fe99c333.EiEqHwodCgVwcmljZRIUKhIKEAoDJGd0EgkRAAAAAAAAWUAaLQoMX2NyZWF0ZWREYXRlEAEaGyoZChcKCiR0aW1lc3RhbXASCREAIPM-q0V5QiIkMzE5MDdlMTUtMzBhNC00ZGI2LTgxY2UtNjEwZmZjMTliOGUx"
          },
          "hasNext": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of up to 100 payment links, given the specified filters, sorting, and paging ([SDK](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/api-query-language) | [REST](https://dev.wix.com/api/rest/getting-started/api-query-language)).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `paymentsLimit` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `status` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `source` | `$exists` | Sort: `ASC`, `DESC` |
    | `totalReceived.paymentCount` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { paymentLinks } from '@wix/get-paid';

    paymentLinks.queryPaymentLinks(query): PaymentLinksQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="PaymentLinkQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `PaymentLinksQueryBuilder`.

    <ResponseField name="paymentLinks" type="PaymentLink[]">

      Retrieved payment links.

      See the [Payment Link Object](/api-reference/business-management/get-paid/payment-links/payment-links/payment-link-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-payment-links" id="ex-sdk-query-payment-links-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-payment-links" id="ex-sdk-query-payment-links-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-payment-links" id="ex-sdk-query-payment-links-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-payment-links-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-payment-links-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryPaymentLinks</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPaymentLinks (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPaymentLinks (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-payment-links-0"><span className="wix-picker-name">queryPaymentLinks</span></label>
    <label htmlFor="ex-sdk-query-payment-links-1"><span className="wix-picker-name">queryPaymentLinks (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-payment-links-2"><span className="wix-picker-name">queryPaymentLinks (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryPaymentLinks
    import { paymentLinks } from '@wix/get-paid';

    async function queryPaymentLinks(query) {
      const response = await paymentLinks.queryPaymentLinks(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPaymentLinks (self-hosted)
    import { createClient } from '@wix/sdk';
    import { paymentLinks } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { paymentLinks },
      // Include the auth strategy and host as relevant
    });

    async function queryPaymentLinks(query) {
      const response = await myWixClient.paymentLinks.queryPaymentLinks(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPaymentLinks (with elevated permissions)
    import { paymentLinks } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myQueryPaymentLinksMethod(query) {
      const elevatedQueryPaymentLinks = auth.elevate(paymentLinks.queryPaymentLinks);
      const response = await elevatedQueryPaymentLinks(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
