---
title: "onPolicyUpdated"
description: "Triggered when a policy is updated."
icon: "bolt"
---

Triggered when a policy is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v2.policy_updated` |
| Package | `@wix/events` |
| Permissions | `Manage Events - all permissions`, `Manage Events`, `Read Policies`, `Manage Policies` |

## Examples

<CodeGroup>

  ```javascript onPolicyUpdated
  import { policies } from '@wix/events';

  policies.onPolicyUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPolicyUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { policies } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        policies,
      },
  });

  wixClient.policies.onPolicyUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
