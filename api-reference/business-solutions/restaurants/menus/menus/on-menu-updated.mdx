---
title: "onMenuUpdated"
description: "The onMenuUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.menu_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onMenuUpdated
  import { menus } from '@wix/restaurants';

  menus.onMenuUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onMenuUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { menus } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        menus,
      },
  });

  wixClient.menus.onMenuUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
