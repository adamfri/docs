---
title: "onCheckoutTemplateCreated"
description: "The onCheckoutTemplateCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.checkout_template_created` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions` |

## Examples

<CodeGroup>

  ```javascript onCheckoutTemplateCreated
  import { checkoutTemplates } from '@wix/ecom';

  checkoutTemplates.onCheckoutTemplateCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCheckoutTemplateCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { checkoutTemplates } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        checkoutTemplates,
      },
  });

  wixClient.checkoutTemplates.onCheckoutTemplateCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
