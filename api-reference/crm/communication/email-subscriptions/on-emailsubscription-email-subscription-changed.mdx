---
title: "onEmailsubscriptionEmailSubscriptionChanged"
description: "The onEmailsubscriptionEmailSubscriptionChanged event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `com.wixpress.emailsubscriptions.v1.EmailSubscriptionChanged` |
| Package | `@wix/email-subscriptions` |
| Permissions | `Manage Stores`, `Manage Email Subscriptions` |

## Examples

<CodeGroup>

  ```javascript onEmailsubscriptionEmailSubscriptionChanged
  import { emailSubscriptions } from '@wix/email-subscriptions';

  emailSubscriptions.onEmailsubscriptionEmailSubscriptionChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onEmailsubscriptionEmailSubscriptionChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { emailSubscriptions } from '@wix/email-subscriptions';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        emailSubscriptions,
      },
  });

  wixClient.emailSubscriptions.onEmailsubscriptionEmailSubscriptionChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
