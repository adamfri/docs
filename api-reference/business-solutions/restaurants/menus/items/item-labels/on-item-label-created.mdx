---
title: "onItemLabelCreated"
description: "Triggered when an item label is created."
icon: "bolt"
---

Triggered when an item label is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_label_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemLabelCreated
  import { itemLabels } from '@wix/restaurants';

  itemLabels.onItemLabelCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemLabelCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemLabels } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemLabels,
      },
  });

  wixClient.itemLabels.onItemLabelCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
