---
title: "onDiscountRuleDeleted"
description: "Triggered when a discount rule is deleted."
icon: "bolt"
---

Triggered when a discount rule is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.discounts.v1.discount_rule_deleted` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Read eCommerce - all read permissions`, `Read Stores - all read permissions`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions` |

## Examples

<CodeGroup>

  ```javascript onDiscountRuleDeleted: Wix-hosted
  import { discountRules } from '@wix/ecom';

  discountRules.onDiscountRuleDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onDiscountRuleDeleted: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { discountRules } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        discountRules,
      },
  });

  wixClient.discountRules.onDiscountRuleDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
