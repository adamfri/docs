---
title: "onRecipientDeleted"
description: "The onRecipientDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.recipient.v1.recipient_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onRecipientDeleted
  import { recipients } from '@wix/restaurants';

  recipients.onRecipientDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onRecipientDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { recipients } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        recipients,
      },
  });

  wixClient.recipients.onRecipientDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
