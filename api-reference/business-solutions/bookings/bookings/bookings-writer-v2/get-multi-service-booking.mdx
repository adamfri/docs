---
title: "Get Multi Service Booking"
description: "Retrieves a multi-service booking and all its associated single-service bookings."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a multi-service booking and all its associated single-service bookings.

    Returns the complete multi-service booking information including its ID, associated single-service bookings, and the total number of scheduled single-service bookings.

    If you call on behalf of an [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) with permissions to read only part of the multi-service booking, only the permitted single-service bookings are retrieved.
    The returned total number includes single-service bookings for which you don't have permissions.

    See [Query Extended Bookings](https://dev.wix.com/docs/rest/business-solutions/bookings/bookings/bookings-reader-v2/query-extended-bookings) for details about retrieving individual single-service bookings and additional metadata.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-CALENDAR">Read Bookings Calendar Availability</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/_api/bookings-service/v2/multi_service_bookings/{multiServiceBookingId}
    ```

    ## Path Params

    <ParamField path="multiServiceBookingId" type="string" required>

      ID of the multi-service booking.

    </ParamField>

    ## Response Object

    <ResponseField name="multiServiceBooking" type="MultiServiceBooking">

      Retrieved multi-service booking.

      <Expandable title="multiServiceBooking properties">

        <ResponseField name="id" type="string">

          Multi-service booking ID.

        </ResponseField>

        <ResponseField name="bookings" type="BookingResult[]">

          The single-service bookings that make up the multi-service booking package.

          <Expandable title="bookings properties">

            <ResponseField name="bookingId" type="string">

              Single-service booking ID. Always present.
              This value is identical to `booking.id`, which is only returned when `returnFullEntity` is `true`.

            </ResponseField>

            <ResponseField name="booking" type="Booking">

              Single-service booking entity. Only returned when `returnFullEntity` is `true`.

              See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="metadata" type="MultiServiceBookingMetadata">

      Details about how many single-service bookings belong to the multi-service booking.

      <Expandable title="metadata properties">

        <ResponseField name="totalNumberOfScheduledBookings" type="integer">

          Total number of `CONFIRMED` and `PENDING` single-service bookings belonging to the multi-service booking.
          The total includes the number of single-service bookings which couldn't be retrieved due to lack of permissions.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/_api/bookings-service/v2/multi_service_bookings/a1b2c3d4-e5f6-7890-abcd-ef1234567890' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "multiServiceBooking": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "bookings": [
            {
              "bookingId": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
              "booking": {
                "id": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
                "status": "CREATED",
                "paymentStatus": "NOT_PAID",
                "bookedEntity": {
                  "slot": {
                    "scheduleId": "massage-schedule-123",
                    "startDate": "2024-02-15T14:00:00.000Z",
                    "endDate": "2024-02-15T15:00:00.000Z"
                  }
                },
                "contactDetails": {
                  "firstName": "Jane",
                  "lastName": "Smith",
                  "email": "jane.smith@example.com",
                  "phone": "+1-555-123-4567"
                }
              }
            },
            {
              "bookingId": "c3d4e5f6-a7b8-9012-cdef-123456789012",
              "booking": {
                "id": "c3d4e5f6-a7b8-9012-cdef-123456789012",
                "status": "CREATED",
                "paymentStatus": "NOT_PAID",
                "bookedEntity": {
                  "slot": {
                    "scheduleId": "facial-schedule-789",
                    "startDate": "2024-02-15T15:00:00.000Z",
                    "endDate": "2024-02-15T15:45:00.000Z"
                  }
                },
                "contactDetails": {
                  "firstName": "Jane",
                  "lastName": "Smith",
                  "email": "jane.smith@example.com",
                  "phone": "+1-555-123-4567"
                }
              }
            }
          ]
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a multi-service booking and all its associated single-service bookings.

    Returns the complete multi-service booking information including its ID, associated single-service bookings, and the total number of scheduled single-service bookings.

    If you call on behalf of an [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) with permissions to read only part of the multi-service booking, only the permitted single-service bookings are retrieved.
    The returned total number includes single-service bookings for which you don't have permissions.

    See [Query Extended Bookings](https://dev.wix.com/docs/rest/business-solutions/bookings/bookings/bookings-reader-v2/query-extended-bookings) for details about retrieving individual single-service bookings and additional metadata.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-CALENDAR">Read Bookings Calendar Availability</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { bookings } from '@wix/bookings';

    bookings.getMultiServiceBooking(multiServiceBookingId): Promise<MultiServiceBooking>
    ```

    ## Method Parameters

    <ParamField body="multiServiceBookingId" type="string" required>

      ID of the multi-service booking.

    </ParamField>

    ## Returns

    Returns `Promise<MultiServiceBooking>`.

    <ResponseField name="_id" type="string">

      Multi-service booking ID.

    </ResponseField>

    <ResponseField name="bookings" type="BookingResult[]">

      The single-service bookings that make up the multi-service booking package.

      <Expandable title="bookings properties">

        <ResponseField name="bookingId" type="string">

          Single-service booking ID. Always present.
          This value is identical to `booking.id`, which is only returned when `returnFullEntity` is `true`.

        </ResponseField>

        <ResponseField name="booking" type="Booking">

          Single-service booking entity. Only returned when `returnFullEntity` is `true`.

          See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-multi-service-booking" id="ex-sdk-get-multi-service-booking-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-multi-service-booking" id="ex-sdk-get-multi-service-booking-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-multi-service-booking" id="ex-sdk-get-multi-service-booking-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-multi-service-booking-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-multi-service-booking-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getMultiServiceBooking</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getMultiServiceBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getMultiServiceBooking (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-multi-service-booking-0"><span className="wix-picker-name">getMultiServiceBooking</span></label>
    <label htmlFor="ex-sdk-get-multi-service-booking-1"><span className="wix-picker-name">getMultiServiceBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-multi-service-booking-2"><span className="wix-picker-name">getMultiServiceBooking (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getMultiServiceBooking
    import { bookings } from '@wix/bookings';

    async function getMultiServiceBooking(multiServiceBookingId) {
      const response = await bookings.getMultiServiceBooking(multiServiceBookingId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getMultiServiceBooking (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bookings } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bookings },
      // Include the auth strategy and host as relevant
    });

    async function getMultiServiceBooking(multiServiceBookingId) {
      const response = await myWixClient.bookings.getMultiServiceBooking(multiServiceBookingId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getMultiServiceBooking (with elevated permissions)
    import { bookings } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myGetMultiServiceBookingMethod(multiServiceBookingId) {
      const elevatedGetMultiServiceBooking = auth.elevate(bookings.getMultiServiceBooking);
      const response = await elevatedGetMultiServiceBooking(multiServiceBookingId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
