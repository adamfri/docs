---
title: "Update Staff Member Settings"
description: "Updates privacy settings for staff member contact information."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates privacy settings for staff member contact information.

    Each time the settings are updated, `revision` increments by 1.
    The current `revision` must be passed when updating the settings.
    This ensures you're working with the latest settings and prevents unintended overwrites.

    Changes to these settings take effect immediately and affect both API responses and live site display.
    When you enable `contactInformation.publiclyAccessible`, the `email` and `phone` fields in [staff member objects](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) include actual contact information, and this information is displayed on the live site.
    When you disable it, these fields are empty in API responses if the calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) doesn't have sufficient permissions, and contact information isn't displayed on the live site.

    Identities with appropriate permissions can access staff contact information in API responses regardless of this setting.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/bookings/v1/staff-member-settings
    ```

    ## Body Params

    <ParamField body="staffMemberSettings" type="StaffMemberSettings" required>

      Staff member settings to update, may be partial.

      See the [Staff Member Settings Object](/api-reference/business-solutions/bookings/staff-members/staff-member-settings/staff-member-settings-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="staffMemberSettings" type="StaffMemberSettings">

      Updated staff member settings.

      See the [Staff Member Settings Object](/api-reference/business-solutions/bookings/staff-members/staff-member-settings/staff-member-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Staff Member Settings Updated | `wix.bookings.staff_settings.v1.staff_member_settings_updated` | Triggered when a `staffMemberSettings` object is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-update-staff-member-settings" id="ex-rest-update-staff-member-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-update-staff-member-settings" id="ex-rest-update-staff-member-settings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-update-staff-member-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-update-staff-member-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Enable Contact Information Access</span><span className="wix-picker-desc">Updates privacy settings to make staff member email and phone numbers accessible via API</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Disable Contact Information Access</span><span className="wix-picker-desc">Updates privacy settings to make staff member email and phone numbers private</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-update-staff-member-settings-0"><span className="wix-picker-name">Enable Contact Information Access</span><span className="wix-picker-desc">Updates privacy settings to make staff member email and phone numbers accessible via API</span></label>
    <label htmlFor="ex-rest-update-staff-member-settings-1"><span className="wix-picker-name">Disable Contact Information Access</span><span className="wix-picker-desc">Updates privacy settings to make staff member email and phone numbers private</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/bookings/v1/staff-member-settings' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "staffMemberSettings": {
            "revision": "3",
            "contactInformation": {
              "publiclyAccessible": true
            }
          }
        }'
      ```

      ```json Response
      {
        "staffMemberSettings": {
          "id": "e7b3c9f2-4a5d-4e8c-9b2f-1a6c8d3e5f7b",
          "revision": "4",
          "createdDate": "2024-01-15T09:30:00.000Z",
          "updatedDate": "2024-03-21T16:45:00.000Z",
          "contactInformation": {
            "publiclyAccessible": true
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/bookings/v1/staff-member-settings' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "staffMemberSettings": {
            "revision": "4",
            "contactInformation": {
              "publiclyAccessible": false
            }
          }
        }'
      ```

      ```json Response
      {
        "staffMemberSettings": {
          "id": "e7b3c9f2-4a5d-4e8c-9b2f-1a6c8d3e5f7b",
          "revision": "5",
          "createdDate": "2024-01-15T09:30:00.000Z",
          "updatedDate": "2024-03-22T10:15:00.000Z",
          "contactInformation": {
            "publiclyAccessible": false
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates privacy settings for staff member contact information.

    Each time the settings are updated, `revision` increments by 1.
    The current `revision` must be passed when updating the settings.
    This ensures you're working with the latest settings and prevents unintended overwrites.

    Changes to these settings take effect immediately and affect both API responses and live site display.
    When you enable `contactInformation.publiclyAccessible`, the `email` and `phone` fields in [staff member objects](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) include actual contact information, and this information is displayed on the live site.
    When you disable it, these fields are empty in API responses if the calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) doesn't have sufficient permissions, and contact information isn't displayed on the live site.

    Identities with appropriate permissions can access staff contact information in API responses regardless of this setting.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { staffMemberSettings } from '@wix/bookings';

    staffMemberSettings.updateStaffMemberSettings(staffMemberSettings): Promise<StaffMemberSettings>
    ```

    ## Method Parameters

    <ParamField body="staffMemberSettings" type="StaffMemberSettings" required>

      Staff member settings to update, may be partial.

      See the [Staff Member Settings Object](/api-reference/business-solutions/bookings/staff-members/staff-member-settings/staff-member-settings-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<StaffMemberSettings>`.

    See the [Staff Member Settings Object](/api-reference/business-solutions/bookings/staff-members/staff-member-settings/staff-member-settings-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Staff Member Settings Updated | `wix.bookings.staff_settings.v1.staff_member_settings_updated` | Triggered when a `staffMemberSettings` object is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-staff-member-settings" id="ex-sdk-update-staff-member-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-staff-member-settings" id="ex-sdk-update-staff-member-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-staff-member-settings" id="ex-sdk-update-staff-member-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-staff-member-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-staff-member-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateStaffMemberSettings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateStaffMemberSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateStaffMemberSettings (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-staff-member-settings-0"><span className="wix-picker-name">updateStaffMemberSettings</span></label>
    <label htmlFor="ex-sdk-update-staff-member-settings-1"><span className="wix-picker-name">updateStaffMemberSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-staff-member-settings-2"><span className="wix-picker-name">updateStaffMemberSettings (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateStaffMemberSettings
    import { staffMemberSettings } from '@wix/bookings';

    async function updateStaffMemberSettings(staffMemberSettings) {
      const response = await staffMemberSettings.updateStaffMemberSettings(staffMemberSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateStaffMemberSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { staffMemberSettings } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { staffMemberSettings },
      // Include the auth strategy and host as relevant
    });

    async function updateStaffMemberSettings(staffMemberSettings) {
      const response = await myWixClient.staffMemberSettings.updateStaffMemberSettings(staffMemberSettings);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateStaffMemberSettings (with elevated permissions)
    import { staffMemberSettings } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myUpdateStaffMemberSettingsMethod(staffMemberSettings) {
      const elevatedUpdateStaffMemberSettings = auth.elevate(staffMemberSettings.updateStaffMemberSettings);
      const response = await elevatedUpdateStaffMemberSettings(staffMemberSettings);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
