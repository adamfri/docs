---
title: "Add Type To Location"
description: "Adds a location type to the specified location."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    Adds a location type to the specified location.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/locations/v1/locations/{id}/add-type
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the location where the type will be added.

    </ParamField>

    ## Body Params

    <ParamField body="locationType" type="string" required>

      Location type to add.

      **Supported values:**

      - `BRANCH`
      - `OFFICES`
      - `RECEPTION`
      - `HEADQUARTERS`
      - `INVENTORY`

    </ParamField>

    ## Response Object

    <ResponseField name="location" type="Location">

      Updated location.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_UPDATE_LOCATION_TYPES_FOR_LOCATION` | Can't update location types for this location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Location Updated | `wix.locations.v1.location_updated` |  |

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds a location type to the specified location.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/business-tools';

    locations.addTypeToLocation(_id, locationType): Promise<AddTypeToLocationResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the location where the type will be added.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="locationType" type="string" required>

      Location type to add.

      **Supported values:**

      - `BRANCH`
      - `OFFICES`
      - `RECEPTION`
      - `HEADQUARTERS`
      - `INVENTORY`

    </ParamField>

    ## Returns

    Returns `Promise<AddTypeToLocationResponse>`.

    <ResponseField name="location" type="Location">

      Updated location.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_UPDATE_LOCATION_TYPES_FOR_LOCATION` | Can't update location types for this location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Location Updated | `wix.locations.v1.location_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-add-type-to-location" id="ex-sdk-add-type-to-location-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-add-type-to-location" id="ex-sdk-add-type-to-location-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-add-type-to-location" id="ex-sdk-add-type-to-location-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-add-type-to-location-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-add-type-to-location-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">addTypeToLocation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">addTypeToLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">addTypeToLocation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-add-type-to-location-0"><span className="wix-picker-name">addTypeToLocation</span></label>
    <label htmlFor="ex-sdk-add-type-to-location-1"><span className="wix-picker-name">addTypeToLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-add-type-to-location-2"><span className="wix-picker-name">addTypeToLocation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript addTypeToLocation
    import { locations } from '@wix/business-tools';

    async function addTypeToLocation(_id,locationType) {
      const response = await locations.addTypeToLocation(_id,locationType);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript addTypeToLocation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/business-tools';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function addTypeToLocation(_id,locationType) {
      const response = await myWixClient.locations.addTypeToLocation(_id,locationType);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript addTypeToLocation (with elevated permissions)
    import { locations } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    async function myAddTypeToLocationMethod(_id,locationType) {
      const elevatedAddTypeToLocation = auth.elevate(locations.addTypeToLocation);
      const response = await elevatedAddTypeToLocation(_id,locationType);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
