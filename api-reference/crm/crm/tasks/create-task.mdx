---
title: "Create Task"
description: "Creates a new task."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new task.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-TASKS">Manage Tasks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/crm/tasks/v2/tasks
    ```

    ## Body Params

    <ParamField body="task" type="Task" required>

      Task to create.

      See the [Task Object](/api-reference/crm/crm/tasks/task-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="task" type="Task">

      The created task.

      See the [Task Object](/api-reference/crm/crm/tasks/task-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CONTACT_NOT_FOUND` | Couldn't find the contact. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Task Created | `wix.crm.tasks.v2.task_created` | Triggered when a task is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Creates a new Task

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/crm/tasks/v2/tasks' \
        -H 'Accept: application/json, text/plain */*' \
        -H 'Authorization: <AUTH>' \
        --data-raw '
          {
             "task": {
               "title": "Need to call someone",
                "description": "Explain about your site",
                "dueDate": "2022-08-17T09:00:00.000Z",
                "status": "ACTION_NEEDED",
                "assigneeId": "f6eb6c40-7379-481e-8d52-58cfb6dc4c9a",
                "contact": {
                  "id": "c3341c32-f40e-4de5-8e90-d181c9cddb04"
                }
             }
          }'
      ```

      ```json Response
      {
          "task": {
            "id": "4973f73a-40ef-11ee-be56-0242ac120002",
              "revision": "1",
              "createdDate": "2022-08-13T07:43:18.125Z",
              "updatedDate": "2022-08-13T08:49:37.054Z",
              "title": "Need to call someone",
              "description": "Explain about your site",
              "dueDate": "2022-08-17T09:00:00.000Z",
              "status": "ACTION_NEEDED",
              "source": {
                "sourceType": "USER",
                "userId": "fe213e9b-a560-4bcc-aae4-ef31c4152002"
              },
              "assigneeId": "f6eb6c40-7379-481e-8d52-58cfb6dc4c9a",
              "contact": {
                "id": "c3341c32-f40e-4de5-8e90-d181c9cddb04",
                "firstName": "Some",
                "lastName": "One",
                "email": "email@example.com",
                "phone": "+97297495550",
                "imageUrl": "https://www.wix.com/contacts-server/api/v1/metasite/avatar/c3341c32-f40e-4de5-8e90-d181c9cddb04/2528bd01086244efb191c1302d986566/"
              }
          }
        }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new task.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-TASKS">Manage Tasks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tasks } from '@wix/crm';

    tasks.createTask(task): Promise<Task>
    ```

    ## Method Parameters

    <ParamField body="task" type="Task" required>

      Task to create.

      See the [Task Object](/api-reference/crm/crm/tasks/task-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Task>`.

    See the [Task Object](/api-reference/crm/crm/tasks/task-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CONTACT_NOT_FOUND` | Couldn't find the contact. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Task Created | `wix.crm.tasks.v2.task_created` | Triggered when a task is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-task" id="ex-sdk-create-task-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-task" id="ex-sdk-create-task-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-task" id="ex-sdk-create-task-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-task-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-task-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a new task</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create a new task (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-task-0"><span className="wix-picker-name">Create a new task</span></label>
    <label htmlFor="ex-sdk-create-task-1"><span className="wix-picker-name">Create a new task (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-create-task-2"><span className="wix-picker-name">createTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a new task

    import { tasks } from '@wix/crm';

    /* Sample task value:
     * {
     *   contact: {
     *     _id: '5518ee7f-270e-40c4-b756-dad56e8f0ffc'
     *   },
     *   description: 'Send a follow up email',
     *   dueDate: new Date(2024, 1, 31),
     *   status: 'ACTION_NEEDED',
     *   title: 'Follow up'
     * }
    */

    export async function myCreateTaskFunction(task) {

      try {     
        const newTask = await tasks.createTask(task);
        console.log('Successfully created a new task:', newTask);

        return newTask;
      }
      catch(error){
        console.log(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "revision": "1",
     *   "title": "Follow up",
     *   "description": "Send a follow up email",
     *   "dueDate": "2024-01-31T00:00:00.000Z",
     *   "status": "ACTION_NEEDED",
     *   "source": {
     *     "sourceType": "APP",
     *     "appId": "151e476a-715e-ec33-db9a-a7ff4d51f70a"
     *   },
     *   "contact": {
     *     "firstName": "Sally",
     *     "lastName": "Smith",
     *     "email": "sally.smith@example.com",
     *     "phone": "+1 524-624-2486",
     *     "_id": "5518ee7f-270e-40c4-b756-dad56e8f0ffc"
     *   },
     *   "_id": "37c2c378-8085-4ef7-8a3e-6f341248e757",
     *   "_createdDate": "2024-01-18T12:16:37.452Z",
     *   "_updatedDate": "2024-01-18T12:16:37.452Z"
     * }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Create a new task (with elevated permissions)
    import { tasks } from '@wix/crm';
    import { auth } from '@wix/essentials';

    /* Sample task value:
     * {
     *   contact: {
     *     _id: '5518ee7f-270e-40c4-b756-dad56e8f0ffc'
     *   },
     *   description: 'Send a follow up email',
     *   dueDate: new Date(2024, 1, 31),
     *   status: 'ACTION_NEEDED',
     *   title: 'Follow up'
     * }
    */

    const elevatedCreateTask = auth.elevate(tasks.createTask);

    export async function myCreateTaskFunction(task) {

      try {     
        const newTask = await elevatedCreateTask(task);
        console.log('Successfully created a new task:', newTask);

        return newTask;
      }
      catch(error){
        console.log(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "revision": "1",
     *   "title": "Follow up",
     *   "description": "Send a follow up email",
     *   "dueDate": "2024-01-31T00:00:00.000Z",
     *   "status": "ACTION_NEEDED",
     *   "source": {
     *     "sourceType": "APP",
     *     "appId": "151e476a-715e-ec33-db9a-a7ff4d51f70a"
     *   },
     *   "contact": {
     *     "firstName": "Sally",
     *     "lastName": "Smith",
     *     "email": "sally.smith@example.com",
     *     "phone": "+1 524-624-2486",
     *     "_id": "5518ee7f-270e-40c4-b756-dad56e8f0ffc"
     *   },
     *   "_id": "37c2c378-8085-4ef7-8a3e-6f341248e757",
     *   "_createdDate": "2024-01-18T12:16:37.452Z",
     *   "_updatedDate": "2024-01-18T12:16:37.452Z"
     * }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createTask (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tasks } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tasks },
      // Include the auth strategy and host as relevant
    });

    async function createTask(task) {
      const response = await myWixClient.tasks.createTask(task);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
