---
title: "onPaymentLinkNoteSet"
description: "Triggered when a note is added or updated to a payment link."
icon: "bolt"
---

Triggered when a note is added or updated to a payment link.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.paymentlinks.v1.payment_link_payment_link_note_set` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Paylinks` |

## Examples

<CodeGroup>

  ```javascript onPaymentLinkNoteSet
  import { paymentLinks } from '@wix/get-paid';

  paymentLinks.onPaymentLinkNoteSet((event) => {
      // handle your event here
  });
  ```

  ```javascript onPaymentLinkNoteSet (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { paymentLinks } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        paymentLinks,
      },
  });

  wixClient.paymentLinks.onPaymentLinkNoteSet((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
