---
title: "Bulk Create Rewards"
description: "Creates multiple rewards."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple rewards.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/loyalty-rewards/v1/bulk/rewards/create
    ```

    ## Body Params

    <ParamField body="rewards" type="Reward[]" required>

      Rewards to create.

      Constraints: `minItems 1`, `maxItems 10`.

      See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkRewardResult[]">

      Created rewards.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Reward">

          Created reward.

          See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total successes and failures of the bulk create rewards action.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reward Created | `wix.loyalty.v1.reward_created` | Triggered when a reward is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/loyalty-rewards/bulk/rewards/create' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-raw '{
                       "rewards": [
                           {
                             "active": true,
                             "name": "Free shipping",
                             "type": "COUPON_REWARD",
                             "couponReward": {
                               "freeShipping": {
                                 "configsByTier": [
                                   {
                                     "tierId": null,
                                     "costInPoints": 20
                                   }
                                 ]
                               }
                             }
                           }
                       ]
                     }'
      ```

      ```json Response
      {
        "results": [
          {
            "item": {
              "id": "46d7bbce-6bb4-4174-ae5a-7f44c19f95ce",
              "name": "Free shipping",
              "requiredPoints": 20,
              "active": true,
              "type": "COUPON_REWARD",
              "couponReward": {
                "freeShipping": {
                  "configsByTier": [
                    {
                      "costInPoints": 20
                    }
                  ]
                }
              },
              "revision": "1",
              "createdDate": "2024-04-18T10:35:41.071Z",
              "updatedDate": "2024-04-18T10:35:41.071Z"
            },
            "itemMetadata": {
              "id": "46d7bbce-6bb4-4174-ae5a-7f44c19f95ce",
              "originalIndex": 0,
              "success": true,
              "error": null
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 1,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple rewards.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rewards } from '@wix/loyalty';

    rewards.bulkCreateRewards(rewards): Promise<BulkCreateRewardsResponse>
    ```

    ## Method Parameters

    <ParamField body="rewards" type="Reward[]" required>

      Rewards to create.

      Constraints: `minItems 1`, `maxItems 10`.

      See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<BulkCreateRewardsResponse>`.

    <ResponseField name="results" type="BulkRewardResult[]">

      Created rewards.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Reward">

          Created reward.

          See the [Reward Object](/api-reference/crm/loyalty-program/rewards/rewards/reward-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total successes and failures of the bulk create rewards action.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Reward Created | `wix.loyalty.v1.reward_created` | Triggered when a reward is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-rewards" id="ex-sdk-bulk-create-rewards-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-rewards" id="ex-sdk-bulk-create-rewards-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-create-rewards" id="ex-sdk-bulk-create-rewards-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-rewards-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-rewards-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateRewards</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateRewards (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateRewards (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-rewards-0"><span className="wix-picker-name">bulkCreateRewards</span></label>
    <label htmlFor="ex-sdk-bulk-create-rewards-1"><span className="wix-picker-name">bulkCreateRewards (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-create-rewards-2"><span className="wix-picker-name">bulkCreateRewards (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkCreateRewards
    import { rewards } from '@wix/loyalty';

    async function bulkCreateRewards(rewards) {
      const response = await rewards.bulkCreateRewards(rewards);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateRewards (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rewards } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rewards },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateRewards(rewards) {
      const response = await myWixClient.rewards.bulkCreateRewards(rewards);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateRewards (with elevated permissions)
    import { rewards } from '@wix/loyalty';
    import { auth } from '@wix/essentials';

    async function myBulkCreateRewardsMethod(rewards) {
      const elevatedBulkCreateRewards = auth.elevate(rewards.bulkCreateRewards);
      const response = await elevatedBulkCreateRewards(rewards);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
