---
title: "onInventoryItemStockStatusUpdated"
description: "Triggered when an inventory item's stock is updated."
icon: "bolt"
---

Triggered when an inventory item's stock is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.stores.catalog.v3.inventory_item_stock_status_updated` |
| Package | `@wix/stores` |
| Permissions | `Read inventory in v3 catalog`, `Read v3 catalog`, `Manage Products`, `Read Products`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onInventoryItemStockStatusUpdated: Wix-hosted
  import { inventoryItemsV3 } from '@wix/stores';

  inventoryItemsV3.onInventoryItemStockStatusUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onInventoryItemStockStatusUpdated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { inventoryItemsV3 } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        inventoryItemsV3,
      },
  });

  wixClient.inventoryItemsV3.onInventoryItemStockStatusUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
