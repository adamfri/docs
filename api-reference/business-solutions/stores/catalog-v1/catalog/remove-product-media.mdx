---
title: "Remove Product Media"
description: "Removes specified media items from a product."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes specified media items from a product.
    Pass an empty array to remove all media items.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v1/products/{id}/media/delete
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Product ID.

    </ParamField>

    ## Body Params

    <ParamField body="mediaIds" type="string[]">

      List of media IDs to remove. Pass an empty array to delete all media items for the product.

      Constraints: `maxItems 50`.

    </ParamField>

    ## Response Object

    Returns `RemoveProductMediaResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
         'https://www.wixapis.com/stores/v1/products/1044e7e4-37d1-0705-c5b3-623baae212fd/media/delete' \
          --data-binary '{
                           "mediaIds": [
                             "mediaId1",
                             "mediaId2"
                           ]
                         }' \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes specified media items from a product.
    Pass an empty array to remove all media items.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { products } from '@wix/stores';

    products.removeProductMedia(_id, mediaIds): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Product ID.

    </ParamField>

    <ParamField body="mediaIds" type="string[]" required>

      List of media IDs to remove. Pass an empty array to delete all media items for the product.

      Constraints: `maxItems 50`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-product-media" id="ex-sdk-remove-product-media-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-product-media" id="ex-sdk-remove-product-media-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-product-media-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-product-media-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Remove all media items from a product</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeProductMedia (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-product-media-0"><span className="wix-picker-name">Remove all media items from a product</span></label>
    <label htmlFor="ex-sdk-remove-product-media-1"><span className="wix-picker-name">removeProductMedia (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Remove all media items from a product
    /**************************************
     * Backend code - products.web.js/ts *
     **************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { products } from '@wix/stores';

    export const removeProductMedia = webMethod(Permissions.Anyone, (_id, mediaIds) => {
      return products.removeProductMedia(_id, mediaIds);
    });

    /*************
     * Page code *
     *************/
    import { items } from '@wix/data';

    import { removeProductMedia } from 'backend/products.web';

    // ... 

    const productName = ...;  // get name of product 

    items.query("Stores/Products")
      .eq("name", productName)
      .find()
      .then((results) => {
        if (results.items.length > 0) {
          const _id = results.items[0]._id;
          removeProductMedia(_id, []) // passing an empty array to remove all media items
            .then(() => {
              // all media items removed from the product 
            })
            .catch((error) => {
              // media items not removed from the product 
            });
        }
      });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeProductMedia (self-hosted)
    import { createClient } from '@wix/sdk';
    import { products } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { products },
      // Include the auth strategy and host as relevant
    });

    async function removeProductMedia(_id,mediaIds) {
      const response = await myWixClient.products.removeProductMedia(_id,mediaIds);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
