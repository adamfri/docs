---
title: "List Deleted Contacts"
description: "Retrieves a list of soft-deleted contacts from the trash bin, filtered by the provided identifiers and deletion date range."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of soft-deleted contacts from the trash bin, filtered by the provided identifiers and deletion date range.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/contacts/v5/contacts/trash-bin
    ```

    ## Query Params

    <ParamField query="externalId" type="string">

      External ID.

    </ParamField>

    <ParamField query="email" type="string">

      Email address.

    </ParamField>

    <ParamField query="phone" type="string">

      Phone number.

    </ParamField>

    <ParamField query="contactIds" type="string[]">

      Contact IDs.

      Constraints: `format GUID`, `maxItems 100`.

    </ParamField>

    <ParamField query="deletedFromDate" type="datetime">

      Filters to contacts deleted on or after this date and time.

    </ParamField>

    <ParamField query="deletedToDate" type="datetime">

      Filters to contacts deleted on or before this date and time.

    </ParamField>

    <ParamField query="paging" type="CursorPaging">

      Cursor paging.

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Maximum number of items to return in the results.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="contacts" type="Contact[]">

      Retrieved contacts.

      See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items matching the filter.
          Available only on the first page of Search results, not included in Query or List results.
          If the Search results span multiple pages, the value of `total` can exceed the number of items returned on the first page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Lists deleted contacts in the trash bin matching the provided email identifier.

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/contacts/v5/contacts/trash-bin?email=sofia.nguyen@example.com' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "contacts": [
          {
            "id": "4b4f4016-7bf1-4334-86eb-b0bebdc3f7df",
            "revision": "2",
            "createdDate": "2026-07-01T18:18:57.044Z",
            "updatedDate": "2026-07-01T18:20:08.191Z",
            "externalId": "crm-ext-ups-3d92",
            "name": {
              "first": "Sofia",
              "last": "Nguyen"
            },
            "email": {
              "email": "sofia.nguyen@example.com",
              "subscriptionStatus": {
                "status": "NOT_SET"
              },
              "deliverabilityStatus": "NOT_SET"
            },
            "additionalEmails": [],
            "phone": {
              "tag": "MOBILE",
              "phone": "+14155558823",
              "e164": "+14155558823",
              "subscriptionStatus": {
                "status": "NOT_SET"
              },
              "deliverabilityStatus": "NOT_SET"
            },
            "additionalPhones": [],
            "addresses": [],
            "company": {
              "name": "Nguyen Analytics",
              "jobTitle": "Data Lead"
            },
            "locationIds": [],
            "source": {
              "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438"
            },
            "lastActivity": {
              "date": "2026-07-01T18:18:57.044Z",
              "templateId": "202edbe8-e365-490d-aa18-4335b26ab8c1"
            },
            "segmentIds": []
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of soft-deleted contacts from the trash bin, filtered by the provided identifiers and deletion date range.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contactsV5 } from '@wix/crm';

    contactsV5.listDeletedContacts(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="ListDeletedContactsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="externalId" type="string">

          External ID.

        </ParamField>

        <ParamField body="email" type="string">

          Email address.

        </ParamField>

        <ParamField body="phone" type="string">

          Phone number.

        </ParamField>

        <ParamField body="contactIds" type="string[]">

          Contact IDs.

          Constraints: `format GUID`, `maxItems 100`.

        </ParamField>

        <ParamField body="deletedFromDate" type="datetime">

          Filters to contacts deleted on or after this date and time.

        </ParamField>

        <ParamField body="deletedToDate" type="datetime">

          Filters to contacts deleted on or before this date and time.

        </ParamField>

        <ParamField body="paging" type="CursorPaging">

          Cursor paging.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return in the results.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="contacts" type="Contact[]">

      Retrieved contacts.

      See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items matching the filter.
          Available only on the first page of Search results, not included in Query or List results.
          If the Search results span multiple pages, the value of `total` can exceed the number of items returned on the first page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-deleted-contacts" id="ex-sdk-list-deleted-contacts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-deleted-contacts" id="ex-sdk-list-deleted-contacts-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-deleted-contacts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-deleted-contacts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List deleted contacts in the trash bin</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listDeletedContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-deleted-contacts-0"><span className="wix-picker-name">List deleted contacts in the trash bin</span></label>
    <label htmlFor="ex-sdk-list-deleted-contacts-1"><span className="wix-picker-name">listDeletedContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List deleted contacts in the trash bin
    import { contactsV5 } from "@wix/crm";

    const options = {
      email: "sofia.nguyen@example.com",
    };

    async function listDeletedContacts() {
      const response = await contactsV5.listDeletedContacts(options);
    }

    /* Promise resolves to:
     * {
     *   "contacts": [
     *     {
     *       "revision": "2",
     *       "externalId": "crm-ext-ups-3d92",
     *       "name": { "first": "Sofia", "last": "Nguyen" },
     *       "email": {
     *         "email": "sofia.nguyen@example.com",
     *         "subscriptionStatus": { "status": "NOT_SET" },
     *         "deliverabilityStatus": "NOT_SET"
     *       },
     *       "phone": {
     *         "tag": "MOBILE",
     *         "phone": "+14155558823",
     *         "e164": "+14155558823",
     *         "subscriptionStatus": { "status": "NOT_SET" },
     *         "deliverabilityStatus": "NOT_SET"
     *       },
     *       "company": { "name": "Nguyen Analytics", "jobTitle": "Data Lead" },
     *       "source": { "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438" },
     *       "_id": "c97ff9b0-345e-44b9-a93f-2a1442a11f3f",
     *       "_createdDate": "2026-07-01T18:34:26.137Z",
     *       "_updatedDate": "2026-07-01T18:35:37.647Z"
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 1,
     *     "cursors": {},
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listDeletedContacts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contactsV5 } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contactsV5 },
      // Include the auth strategy and host as relevant
    });

    async function listDeletedContacts(options) {
      const response = await myWixClient.contactsV5.listDeletedContacts(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
