---
title: "onPropertiesSitePropertiesUpdated"
description: "Triggered when site properties are updated"
icon: "bolt"
---

Triggered when site properties are updated

## Event details

| Property | Value |
| --- | --- |
| Event type | `SitePropertiesUpdated` |
| Package | `@wix/business-tools` |
| Permissions | `Manage Your App` |

## Examples

<CodeGroup>

  ```javascript onPropertiesSitePropertiesUpdated: Wix-hosted
  import { siteProperties } from '@wix/business-tools';

  siteProperties.onPropertiesSitePropertiesUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPropertiesSitePropertiesUpdated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { siteProperties } from '@wix/business-tools';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        siteProperties,
      },
  });

  wixClient.siteProperties.onPropertiesSitePropertiesUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
