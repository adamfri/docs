---
title: "onCollectionCreated"
description: "Triggered when a collection is created."
icon: "bolt"
---

Triggered when a collection is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.collections.v1.collection_created` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onCollectionCreated
  import { collections } from '@wix/portfolio';

  collections.onCollectionCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCollectionCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { collections } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        collections,
      },
  });

  wixClient.collections.onCollectionCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
