---
title: "Bulk Assign Exempt Overrides"
description: "Assigns tax exempt overrides to multiple manual tax mappings in a single request."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Assigns tax exempt overrides to multiple manual tax mappings in a single request.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/billing/v1/manual-tax-mappings/bulk-assign-exempt-overrides
    ```

    ## Body Params

    <ParamField body="assignOverrides" type="AssignOverride[]">

      List of assign overrides.

      Constraints: `maxItems 50`.

      <Expandable title="assignOverrides properties">

        <ParamField body="manualTaxMappingId" type="string" required>

          ID of the manual tax mapping to which the exempt group override will be assigned.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="exemptGroupOverrides" type="ExemptGroupIdOverride[]" required>

          List of exempt group overrides to assign to the manual tax mapping.

          Constraints: `minItems 1`, `maxItems 50`.

          <Expandable title="exemptGroupOverrides properties">

            <ParamField body="exemptGroupId" type="string" required>

              ID of the tax exempt group.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="rate" type="string" required>

              Override tax rate for the exempt group in the context of the manual tax mapping.

              Constraints: `format DECIMAL_VALUE`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the updated manual tax mapping entities in the response.
      Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkExemptOverrideResult[]">

      List of updated manual tax mappings.

      Constraints: `maxItems 50`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the individual assign operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="ManualTaxMapping">

          Updated manual tax mapping. Only present if `returnEntity` was set to `true` in the request.

          See the [Manual Tax Mapping Object](/api-reference/business-solutions/e-commerce/extensions/tax/manual-tax-mappings/manual-tax-mapping-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the bulk assign operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Assigns tax exempt group rate overrides to multiple manual tax mappings in a single request

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/billing/v1/manual-tax-mappings/bulk-assign-exempt-overrides' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "assignOverrides": [
                {
                  "manualTaxMappingId": "5db67330-5991-40b0-983f-e7c8dafb5e69",
                  "exemptGroupOverrides": [
                    {
                      "exemptGroupId": "e5f6a7b8-c9d0-1e2f-3a4b-c5d6e7f8a9b0",
                      "rate": "0"
                    }
                  ]
                }
              ],
              "returnEntity": true
            }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "5db67330-5991-40b0-983f-e7c8dafb5e69",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "5db67330-5991-40b0-983f-e7c8dafb5e69",
              "revision": "3",
              "createdDate": "2026-07-20T12:36:19.665Z",
              "updatedDate": "2026-07-20T12:36:59.456Z",
              "taxGroupId": "1d14e4a0-8a8f-4d21-813c-b743eef41bfd",
              "taxRegionId": "c9a4e2db-201d-440e-af58-bebad1423716",
              "taxRate": "0.08",
              "taxType": "Sales Tax",
              "taxName": "STATE TAX",
              "jurisdiction": "New York",
              "jurisdictionType": "STATE",
              "exemptRates": [
                {
                  "taxExemptGroupId": "e5f6a7b8-c9d0-1e2f-3a4b-c5d6e7f8a9b0",
                  "taxRate": "0"
                }
              ]
            }
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Assigns tax exempt overrides to multiple manual tax mappings in a single request.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { manualTaxMappings } from '@wix/billing';

    manualTaxMappings.bulkAssignExemptOverrides(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkAssignExemptOverridesOptions">

      options

      <Expandable title="options properties">

        <ParamField body="assignOverrides" type="AssignOverride[]">

          List of assign overrides.

          Constraints: `maxItems 50`.

          <Expandable title="assignOverrides properties">

            <ParamField body="manualTaxMappingId" type="string" required>

              ID of the manual tax mapping to which the exempt group override will be assigned.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="exemptGroupOverrides" type="ExemptGroupIdOverride[]" required>

              List of exempt group overrides to assign to the manual tax mapping.

              Constraints: `minItems 1`, `maxItems 50`.

              _Nested structure omitted for brevity — see the `ExemptGroupIdOverride[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="returnEntity" type="boolean">

          Whether to return the updated manual tax mapping entities in the response.
          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkExemptOverrideResult[]">

      List of updated manual tax mappings.

      Constraints: `maxItems 50`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the individual assign operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="ManualTaxMapping">

          Updated manual tax mapping. Only present if `returnEntity` was set to `true` in the request.

          See the [Manual Tax Mapping Object](/api-reference/business-solutions/e-commerce/extensions/tax/manual-tax-mappings/manual-tax-mapping-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the bulk assign operation.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-assign-exempt-overrides" id="ex-sdk-bulk-assign-exempt-overrides-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-assign-exempt-overrides" id="ex-sdk-bulk-assign-exempt-overrides-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-assign-exempt-overrides-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-assign-exempt-overrides-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk Assign Exempt Overrides</span><span className="wix-picker-desc">Assign tax exempt group rate overrides to manual tax mappings in a single request</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkAssignExemptOverrides (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-assign-exempt-overrides-0"><span className="wix-picker-name">Bulk Assign Exempt Overrides</span><span className="wix-picker-desc">Assign tax exempt group rate overrides to manual tax mappings in a single request</span></label>
    <label htmlFor="ex-sdk-bulk-assign-exempt-overrides-1"><span className="wix-picker-name">bulkAssignExemptOverrides (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk Assign Exempt Overrides
    import { manualTaxMappings } from "@wix/billing";

    async function bulkAssignExemptOverrides() {
      const response = await manualTaxMappings.bulkAssignExemptOverrides({
        assignOverrides: [
          {
            manualTaxMappingId: "5db67330-5991-40b0-983f-e7c8dafb5e69",
            exemptGroupOverrides: [
              {
                exemptGroupId: "e5f6a7b8-c9d0-1e2f-3a4b-c5d6e7f8a9b0",
                rate: "0",
              },
            ],
          },
        ],
        returnEntity: true,
      });
      return response;
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "originalIndex": 0,
     *         "success": true,
     *         "_id": "5db67330-5991-40b0-983f-e7c8dafb5e69"
     *       },
     *       "item": {
     *         "taxGroupId": "1d14e4a0-8a8f-4d21-813c-b743eef41bfd",
     *         "taxRegionId": "c9a4e2db-201d-440e-af58-bebad1423716",
     *         "taxRate": "0.08",
     *         "taxType": "Sales Tax",
     *         "taxName": "STATE TAX",
     *         "jurisdiction": "New York",
     *         "jurisdictionType": "STATE",
     *         "exemptRates": [
     *           { "taxExemptGroupId": "e5f6a7b8-c9d0-1e2f-3a4b-c5d6e7f8a9b0", "taxRate": "0" }
     *         ],
     *         "_id": "5db67330-5991-40b0-983f-e7c8dafb5e69",
     *         "revision": "5",
     *         "_createdDate": "2026-07-20T12:36:19.665Z",
     *         "_updatedDate": "2026-07-20T12:39:07.772Z"
     *       }
     *     }
     *   ]
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkAssignExemptOverrides (self-hosted)
    import { createClient } from '@wix/sdk';
    import { manualTaxMappings } from '@wix/billing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { manualTaxMappings },
      // Include the auth strategy and host as relevant
    });

    async function bulkAssignExemptOverrides(options) {
      const response = await myWixClient.manualTaxMappings.bulkAssignExemptOverrides(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
