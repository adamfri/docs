---
title: "Create Operation Group"
description: "Creates a OperationGroup."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a OperationGroup.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants/v1/operation-groups
    ```

    ## Body Params

    <ParamField body="operationGroup" type="OperationGroup" required>

      OperationGroup to be created.

      See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="operationGroup" type="OperationGroup">

      The created OperationGroup.

      See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Group Created | `wix.restaurants.v1.operation_group_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/restaurants/v1/operation-groups' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "operation_group": {
            "name": "Catering"
        }'
      ```

      ```json Response
      {
        "operation_group": {
            "id": "63eb0bf7-96ea-4034-822b-7dd9a24197e9",
            "revision": "1",
            "created_date": "2024-07-09T10:17:08.865Z",
            "updated_date": "2024-07-09T10:17:08.865Z",
            "name": "Catering"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a OperationGroup.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { operationGroups } from '@wix/restaurants';

    operationGroups.createOperationGroup(operationGroup): Promise<OperationGroup>
    ```

    ## Method Parameters

    <ParamField body="operationGroup" type="OperationGroup" required>

      OperationGroup to be created.

      See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<OperationGroup>`.

    See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Group Created | `wix.restaurants.v1.operation_group_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-operation-group" id="ex-sdk-create-operation-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-operation-group" id="ex-sdk-create-operation-group-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-operation-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-operation-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createOperationGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createOperationGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-operation-group-0"><span className="wix-picker-name">createOperationGroup</span></label>
    <label htmlFor="ex-sdk-create-operation-group-1"><span className="wix-picker-name">createOperationGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createOperationGroup
    // @title Create Operation Group
    import { operationGroups } from "@wix/restaurants";

    async function createOperationGroup() {
      const response = await operationGroups.createOperationGroup({
        name: "Evening Service",
      });

      /* Promise resolves to:
       * {
       *   "operationGroup": {
       *     "id": "...",
       *     "revision": "1",
       *     "name": "Evening Service"
       *   }
       * }
       */
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createOperationGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { operationGroups } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { operationGroups },
      // Include the auth strategy and host as relevant
    });

    async function createOperationGroup(operationGroup) {
      const response = await myWixClient.operationGroups.createOperationGroup(operationGroup);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
