---
title: "Bulk Create Variants"
description: "Note: The Item Variants API only works with the Wix Restaurants Menus app."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item Variants API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Creates multiple item variants.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants/menus/v1/bulk/variants/create
    ```

    ## Body Params

    <ParamField body="variants" type="Variant[]" required>

      Item variants details.

      Constraints: `maxItems 100`.

      See the [Item Variant Object](/api-reference/business-solutions/restaurants/menus/items/item-variants/item-variant-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to receive the created item variants in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkVariantResult[]">

      Information about the created item variants.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for creation of item variants.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="variant" type="Variant">

          Created item variants. Only returned if the `returnEntity` value is `true`.

          See the [Item Variant Object](/api-reference/business-solutions/restaurants/menus/items/item-variants/item-variant-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the API call.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Variant Created | `wix.restaurants.menus.v1.item_variant_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/restaurants/item-variants/v1/bulk/variants/create \
          -H 'Authorization: <AUTH>'
              --data-binary '{
                  "variants": [
                      {
                        "name": "Small"
                      },
                      {
                        "name": "Medium"
                      },
                      {
                        "name": "Large"
                      }
                  ],
                  "returnEntity": false
          }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "02042f01-58ab-441c-33db-55221c855c11",
              "originalIndex": 0,
              "success": true
            }
          },
          {
            "itemMetadata": {
              "id": "02022fe1-58ab-ee1c-33db-45521c855e11",
              "originalIndex": 1,
              "success": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 3,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    <Note>

    The Item Variants API only works with the Wix Restaurants Menus app. Make sure you have installed this app from the [Wix App Market](https://www.wix.com/app-market/wix-restaurants-menus-new).

    </Note>

    Creates multiple item variants.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { itemVariants } from '@wix/restaurants';

    itemVariants.bulkCreateVariants(variants, options): Promise<BulkCreateVariantsResponse>
    ```

    ## Method Parameters

    <ParamField body="variants" type="Variant[]" required>

      Item variants details.

      Constraints: `maxItems 100`.

      See the [Item Variant Object](/api-reference/business-solutions/restaurants/menus/items/item-variants/item-variant-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateVariantsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to receive the created item variants in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkCreateVariantsResponse>`.

    <ResponseField name="results" type="BulkVariantResult[]">

      Information about the created item variants.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for creation of item variants.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="variant" type="Variant">

          Created item variants. Only returned if the `returnEntity` value is `true`.

          See the [Item Variant Object](/api-reference/business-solutions/restaurants/menus/items/item-variants/item-variant-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the API call.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Variant Created | `wix.restaurants.menus.v1.item_variant_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-variants" id="ex-sdk-bulk-create-variants-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-variants" id="ex-sdk-bulk-create-variants-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-variants-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-variants-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create multiple variants</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateVariants (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-variants-0"><span className="wix-picker-name">Create multiple variants</span></label>
    <label htmlFor="ex-sdk-bulk-create-variants-1"><span className="wix-picker-name">bulkCreateVariants (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create multiple variants
    import { itemVariants } from "@wix/restaurants";

    const variants = [
      {
        name: "Small"
      },
      {
        name: "Medium"
      },
      {
        name: "Large"
      }
    ];

    const options = {
      returnEntity: false
    };

    async function bulkCreateVariants() {
      const response = await itemVariants.bulkCreateVariants(variants, options);
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "itemMetadata": {
     *         "_id": "ea763e57-ae9a-4da8-97e5-1001a8cd185a",
     *         "originalIndex": 0,
     *         "success": true
     *       },
     *       "variant": {
     *         "_id": "ea763e57-ae9a-4da8-97e5-1001a8cd185a",
     *         "revision": "1",
     *         "name": "Small"
     *       }
     *     },
     *     {
     *       "itemMetadata": {
     *         "_id": "2a5a5ca9-1e96-4079-9d23-865a0c263eee",
     *         "originalIndex": 1,
     *         "success": true
     *       },
     *       "variant": {
     *         "_id": "2a5a5ca9-1e96-4079-9d23-865a0c263eee",
     *         "revision": "1",
     *         "name": "Medium"
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 3,
     *     "totalFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateVariants (self-hosted)
    import { createClient } from '@wix/sdk';
    import { itemVariants } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { itemVariants },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateVariants(variants,options) {
      const response = await myWixClient.itemVariants.bulkCreateVariants(variants,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
