---
title: "Reschedule Booking"
description: "Reschedules an appointment booking to a different slot or a class booking to a different session."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Reschedules an appointment booking to a different slot or a class booking to
    a different session.

    ### Updating add-ons

    You can also call this method to update the list of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) associated with the booking.
    To do so, both of these conditions must be met:
    + The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
    + You must have `BOOKINGS.MANAGE_ADDONS` permissions and specify `flowControlSettings.allowAddOnChanges` as `true`.

    ### Course booking limitation

    You can't reschedule course bookings.

    ### Appointment sessions

    For appointments, the old session is removed from the business calendar
    while a new session is added. We recommend calling [Query Availability](https://dev.wix.com/docs/rest/business-solutions/bookings/time-slots/availability-calendar/query-availability)
    first and specifying the entire retrieved `slot`.

    ### Class sessions

    For classes, the new session must be an existing session belonging to the
    same class. We recommend retrieving `availabilityEntries.slot.eventId`
    from [Query Availability](https://dev.wix.com/docs/rest/business-solutions/bookings/time-slots/availability-calendar/query-availability)
    to avoid failed Reschedule Booking calls due to unavailability. Specify
    only `slot.eventId` instead of the entire `slot` object.

    ### Add-ons

    You can call Reschedule Booking to replace the list of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) associated with the booking.

    To do so, all of these conditions must be met:
    - The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
    - You must have `BOOKINGS.MANAGE_ADDONS` permissions.
    - For bookings paid via an [eCommerce checkout](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/checkout/introduction): The related [eCommerce order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/introduction) must be in a draftable state to correctly adjust the price when modifying add-ons. You can call [Get Order Draftability Status](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/get-order-draftability-status), specifying the order ID to check if it's draftable. If the order isn't draftable, Reschedule Booking still succeeds, but the order's `priceSummary` isn't updated.

    ### Notify customers

    You can specify a `participantNotification.message` for the customer. To send
    the message, you must also specify `participantNotification.notifyParticipants`
    as `true`.

    ### Admin overwrites

    There are small but important differences when you specify special
    `flowControlSettings`:

    - `{"ignoreReschedulePolicy": true}`: The call succeeds even if the
    service's `reschedulePolicy` doesn't allow it. Also skips
    external validation providers.
    - `{"skipAvailabilityValidation": true}`: The call succeeds even if
    the specified session, slot, or resource isn't available. If you don't
    specify any resource, the call succeeds even if no resource of the relevant
    type is available.
    - `{"skipBusinessConfirmation": true}`: Any `PENDING` booking is
    automatically confirmed even if the services requires the merchants's
    manual confirmation.

    When using special `flowControlSettings`, ensure you have sufficient
    permissions. If you encounter failed calls due to insufficient permissions,
    consider the following options:

    - **App developers** can use a higher
    [permission](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions),
    such as `MANAGE BOOKINGS - ALL PERMISSIONS`.
    - **Site developers** can utilize
    [elevation](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/authorization/elevation).

    Granting additional permissions and using elevation permits method calls that
    would typically fail due to authorization checks. Therefore, you should use
    them intentionally and securely.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/bookings/{bookingId}/reschedule
    ```

    ## Path Params

    <ParamField path="bookingId" type="string" required>

      ID of the booking to reschedule.

    </ParamField>

    ## Body Params

    <ParamField body="slot" type="Slot" required>

      New slot of the booking.

      <Expandable title="slot properties">

        <ParamField body="sessionId" type="string">

          Identifier for the underlying session when the session is a single session or generated from a recurring session.

        </ParamField>

        <ParamField body="serviceId" type="string">

          Service identifier. Required.

        </ParamField>

        <ParamField body="scheduleId" type="string">

          Schedule identifier. Required.

        </ParamField>

        <ParamField body="startDate" type="string">

          The start time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

        </ParamField>

        <ParamField body="endDate" type="string">

          The end time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

        </ParamField>

        <ParamField body="timezone" type="string">

          The timezone according to which the slot is calculated and presented.

        </ParamField>

        <ParamField body="resource" type="SlotResource">

          The resource required for this slot.
          When populated, the specified resource will be assigned to the slot upon confirmation according to its availability.
          When empty, if `skip_availability_validation` is `false`, a random available resource will be assigned to the slot upon confirmation.
          Otherwise, one of the service resources will be assigned to the slot randomly upon confirmation.

          <Expandable title="resource properties">

            <ParamField body="id" type="string">

              Resource ID.

              **Read-only.**

            </ParamField>

            <ParamField body="name" type="string">

              Resource name.

            </ParamField>

            <ParamField body="transparency" type="string">

              The desired transparency for this resource in the calendar event.

              Supported values:
              + `OPAQUE`: The schedule is blocked during the booking, preventing other bookings involving the same resource from being scheduled at the same time.
              + `TRANSPARENT`: The schedule remains open during the booking, allowing other bookings to be scheduled concurrently.

              Default: `UNKNOWN_TRANSPARENCY`.

              **Supported values:**

              - `OPAQUE`
              - `TRANSPARENT`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="location" type="Location">

          Geographic location of the slot.

          <Expandable title="location properties">

            <ParamField body="id" type="string">

              Business Location ID. Present if the location is a business location.

            </ParamField>

            <ParamField body="name" type="string">

              Location name.

            </ParamField>

            <ParamField body="formattedAddress" type="string">

              A string containing the full address of this location.

            </ParamField>

            <ParamField body="locationType" type="string">

              Location type.

              **Supported values:**

              - `UNDEFINED` — Undefined location type.
              - `OWNER_BUSINESS` — The business address as set in the site’s general settings.
              - `OWNER_CUSTOM` — The address set when creating the service.
              - `CUSTOM` — The address set for the individual session.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="eventId" type="string">

          Calendar event ID - not supported.
          If not empty, on all write flows (create/update), it takes priority over `sessionId`.
          So if both `sessionId` and `eventId` are provided, the `sessionId` will be based on the `eventId`.
          Otherwise, if `eventId` is empty on write flow,

        </ParamField>

        <ParamField body="resourceSelections" type="ResourceSelection[]">

          Information about how the customer has selected resources for the booking.
          Each resource type may have a different selection method.
          Check `resource` for resource details.

          Constraints: `maxItems 3`.

          <Expandable title="resourceSelections properties">

            <ParamField body="resourceTypeId" type="string">

              ID of the [resource type](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/introduction).

            </ParamField>

            <ParamField body="selectionMethod" type="string">

              Information about how the customer has selected the resource for this resource type.

              **Supported values:**

              - `SPECIFIC_RESOURCE` — The customer explicitly chose a particular resource.
              - `ANY_RESOURCE` — The customer explicitly chose "any available resource" for this resource type.
              - `NO_SELECTION` — The customer wasn't offered a resource selection or agreement option for this resource type.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="allDay" type="boolean">

          Whether the slot spans one or more full days.
          For class bookings, this value is derived from the linked calendar event.
          For appointment bookings, this value is derived from the availability slot. Any caller-provided value is ignored.

          When `true`, `startDate` is set to `00:00:00` of the first day and `endDate` is set to `00:00:00` of the day after the last day (exclusive). Midnight boundaries are interpreted in the slot's `timezone`.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="revision" type="integer" required>

      Revision number, which increments by 1 each time the booking is rescheduled.
      To prevent conflicting changes, the current revision must be passed when
      rescheduling the booking.

    </ParamField>

    <ParamField body="participantNotification" type="ParticipantNotification">

      Information about whether to notify the customer about the rescheduling and
      the message to send.

      <Expandable title="participantNotification properties">

        <ParamField body="notifyParticipants" type="boolean">

          Whether to send a message about the changes to the customer.

          Default: `false`

        </ParamField>

        <ParamField body="message" type="string">

          Custom message to send to the participants about the changes to the booking.

        </ParamField>

        <ParamField body="metadata" type="object">

          Information about the delivery channels used to send the notification.
          For example, `{"channels": "SMS" }`, `{"channels": "EMAIL" }`, or `{"channels": "EMAIL, SMS" }`.

          Constraints: `format map`.

          <Expandable title="metadata properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="string" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="flowControlSettings" type="RescheduleBookingFlowControlSettings">

      Whether to ignore specific standard procedures of the Wix Bookings flow.
      For example, whether to check availability when rescheduling a booking.

      <Expandable title="flowControlSettings properties">

        <ParamField body="ignoreReschedulePolicy" type="boolean">

          Whether the rescheduling policy applies when rescheduling the booking.

          When passing `false`, you can only reschedule a booking if the rescheduling
          policy allows it.
          When passing `true`, the rescheduling policy is ignored and external
          validation providers are also skipped.
          Default: `false`.

        </ParamField>

        <ParamField body="skipAvailabilityValidation" type="boolean">

          Whether the availability is checked before rescheduling the booking.

          When passing `false`, a booking is only created when the slot or
          schedule is available.
          Default: `false`.

        </ParamField>

        <ParamField body="skipBusinessConfirmation" type="boolean">

          Whether the rescheduled booking's status is automatically set to
          `CONFIRMED` for services that normally require the owner's manual
          confirmation.
          Default: `false`.

        </ParamField>

        <ParamField body="allowAddOnChanges" type="boolean">

          Whether you can update the list of add-ons associated with the booking when rescheduling.
          The calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) must have `BOOKINGS.MANAGE_ADDONS` permissions to specify `true`.

          Default: `false`.

          **Developer Preview.**

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="bookedAddOns" type="BookedAddOn[]">

      List of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) added to the booking.

      To replace the booking's the list of booked add-ons, all of these conditions must be met:
      - The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
      - You must have `BOOKINGS.MANAGE_ADDONS` permissions.
      - For bookings paid via an [eCommerce checkout](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/checkout/introduction): The related [eCommerce order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/introduction) must be in a draftable state to correctly adjust the price when modifying add-ons. You can call [Get Order Draftability Status](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/get-order-draftability-status), specifying the order ID to check if it's draftable. If the order isn't draftable, Reschedule Booking still succeeds, but the order's `priceSummary` isn't updated.

      Constraints: `maxItems 21`.

      <Expandable title="bookedAddOns properties">

        <ParamField body="id" type="string">

          The ID of the add-on.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="groupId" type="string">

          The ID of the add-on group.

          Constraints: `format GUID`.

        </ParamField>

        <ParamField body="durationInMinutes" type="integer">

          The add-on duration in minutes at the time of booking.
          Populated for duration-based add-ons.

          **Read-only.**

        </ParamField>

        <ParamField body="quantity" type="integer">

          The quantity of booked add-ons.
          For duration-based add-ons, `quantity` is not applicable.
          If `quantity` is provided as `1` for a duration-based add-on, it will be accepted but the value will be cleared.
          If any other value is provided, an `INVALID_ARGUMENT` error will be returned with the message: "Invalid AddOn details: either duration or quantity must be set correctly".

        </ParamField>

        <ParamField body="name" type="string">

          Add-on `name` at the time of booking.

          **Read-only.**

        </ParamField>

        <ParamField body="nameTranslated" type="string">

          Add-on name translated to the language the customer used during booking.

          **Read-only.**

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="booking" type="Booking">

      Rescheduled booking.

      See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `BOOKING_NOT_FOUND` | Couldn't find the booking. |
    | `403` | `PERMISSION_DENIED` | `BOOKING_POLICY_VIOLATION` | The booking violates the service's booking policy. Check booking window restrictions, advance notice requirements, or scheduling rules. |
    | `403` | `PERMISSION_DENIED` | `UNAUTHORIZED_OPERATION` | The [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) used to call the method doesn't have the required permissions. |
    | `428` | `FAILED_PRECONDITION` | `SESSION_CAPACITY_EXCEEDED` | The session capacity was exceeded. The requested number of participants exceeds available capacity. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULE_CAPACITY_EXCEEDED` | The schedule capacity was exceeded. The requested number of participants exceeds available capacity. |
    | `428` | `FAILED_PRECONDITION` | `SLOT_NOT_AVAILABLE` | The slot is no longer available for booking. It may have been booked by another customer or is outside business hours. |
    | `400` | `INVALID_ARGUMENT` | `VALIDATION_FAILURE` | Validation failed. Verify all required booking fields are provided and valid. |
    | `428` | `FAILED_PRECONDITION` | `EMPTY_FORM_INFO` | Form submission information is missing. Provide the required form data. |
    | `428` | `FAILED_PRECONDITION` | `SESSION_NOT_FOUND` | The specified session doesn't exist. Verify the session ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULE_NOT_FOUND` | The specified schedule doesn't exist. Verify the schedule ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `RESOURCE_NOT_FOUND` | Couldn't find the resource. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_VALIDATING_AVAILABILITY` | Availability validation failed. The requested time slot or schedule may be fully booked or unavailable. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_RESOLVING_SESSION` | Couldn't retrieve session information. The session may not exist or is unavailable. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_RESOLVING_SCHEDULE` | Couldn't retrieve schedule information. The schedule may not exist or is unavailable. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_SERVICE_CHOICES` | The specified service choices are invalid. Verify the choices match the service's supported options and variants. |
    | `403` | `PERMISSION_DENIED` | `NOT_ALLOWED_TO_UPDATE_BOOKING_IN_MULTI_SERVICE_BOOKING` | This booking is part of a multi-service booking and can't be updated individually. Update the multi-service booking instead. |
    | `428` | `FAILED_PRECONDITION` | `ADD_ON_NOT_FOUND` | The specified add-on doesn't exist. Verify the add-on ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `ADD_ON_CHANGES_NOT_ALLOWED` | Add-ons can't be modified for this booking. Check the booking status and add-on configuration. |
    | `428` | `FAILED_PRECONDITION` | `ADD_ON_GROUP_NOT_FOUND` | The specified add-on group doesn't exist. Verify the add-on group ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `PRICE_CHANGE_NOT_ALLOWED` | The booking price can't be changed. Verify the booking status and pricing rules. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Booking Rescheduled | `wix.bookings.v2.booking_rescheduled` | Triggered when a booking is rescheduled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-reschedule-booking" id="ex-rest-reschedule-booking-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-reschedule-booking" id="ex-rest-reschedule-booking-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-reschedule-booking-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-reschedule-booking-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Reschedule an appointment booking</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Reschedule a class session booking</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-reschedule-booking-0"><span className="wix-picker-name">Reschedule an appointment booking</span></label>
    <label htmlFor="ex-rest-reschedule-booking-1"><span className="wix-picker-name">Reschedule a class session booking</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/bookings/a80daf61-41f2-4195-b592-978321e969b9/reschedule' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "slot": {
          "sessionId": "2mmoW0vwKcSFyxtOfCdKTATfNXY9sBJdc35TZCXaVhrOWCsHv0e8sE2dEIzNOeRWb2SMLNjSrwQk557YdfycK5OGQaOicTerujnR",
          "startDate":"2022-11-03T11:00:00.000Z",
          "endDate":"2022-11-04T12:00:00.000Z",
          "timezone":"Europe/Dublin",
          "resource": {
            "name":"Dodly",
            "id":"64bb860a-eda5-4992-9fc6-538d58e818ec"
          },
          "scheduleId":"14c80105-2dac-42fd-b901-e7109dee47f9",
          "serviceId":"032cd844-18fe-4ffc-86cc-e6e47f4243f0",
          "location": {
                "locationType":"OWNER_CUSTOM"
          }
        },
        "bookingId":"a80daf61-41f2-4195-b592-978321e969b9",
        "revision":"7",
        "participantNotification": {
            "notifyParticipants":false
        },
        "totalParticipants":1
      }'
      ```

      ```json Response
      {
        "booking": {
         "id":"a80daf61-41f2-4195-b592-978321e969b9",
          "bookedEntity": {
            "slot": {
              "sessionId":"2mmoW0vwKcSFyxtOfCdKTATfNXY9sBJdc35TZCXaVhrOWCsHv0e8sE2dEIzNOeRWb2SMLNjSrwQk557YdfycK5OGQaOicTerujnR",
              "eventId": "65a5776e-37f1-45f7-8e4c-f22d01b23254",
              "serviceId":"032cd844-18fe-4ffc-86cc-e6e47f4243f0",
              "scheduleId":"14c80105-2dac-42fd-b901-e7109dee47f9",
              "startDate":"2022-11-03T11:00:00.000Z",
              "endDate":"2022-11-04T12:00:00.000Z",
              "timezone":"Europe/Dublin",
              "resource": {
                "id":"64bb860a-eda5-4992-9fc6-538d58e818ec",
                "name":"dodly",
                "scheduleId":"2bc4bf60-cccb-42ce-a43d-05d1bbbfac4c"
              },
              "location": {
                "locationType":"OWNER_CUSTOM"}
              },
              "title":"Appointment Service Name",
              "tags":["INDIVIDUAL"]
              },
              "contactDetails": {
                "contactId": "a2ca1bc8-76e4-4295-8803-ab15aa506149",
                "firstName": "Yafa",
                "lastName": "Scallop",
                "email": "Yafa@example.com"
        },
        "additionalFields":[],
        "totalParticipants":1,
        "status":"CONFIRMED",
        "paymentStatus":"NOT_PAID",
        "selectedPaymentOption":"OFFLINE",
        "createdDate":"2022-10-30T12:28:16.643Z",
        "revision":"8",
        "createdBy": {
            "wixUserId":"086ca7ca-ee70-4ab3-a96e-78f8420e9428"
        },
        "startDate":"2022-11-03T11:00:00Z",
        "endDate":"2022-11-04T12:00:00Z",
        "updatedDate":"2022-10-30T12:54:25.961Z",
        "totalParticipants":1
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/bookings/70284a22-4511-4e86-bd3c-de17463882f0/reschedule' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "bookingId":"70284a22-4511-4e86-bd3c-de17463882f0",
        "slot": {
          "sessionId":"",
          "serviceId":"8bdfb0dc-9461-4f21-b576-f866f9f50dfb",
          "scheduleId":"1b5e8844-7d10-42a3-b326-87941e5efdcb",
          "startDate":"2022-11-02T14:00:00.000Z",
          "endDate":"2022-11-02T15:00:00.000Z",
          "timezone":"Europe/Dublin",
          "resource": {
            "id":"64bb860a-eda5-4992-9fc6-538d58e818ec",
            "name":"dodly"
          },
          "location": {
            "id":"9de094d5-8985-484e-9218-d020d9d17953",
            "name":"Location Name",
            "locationType":"OWNER_BUSINESS"
            }
        },
        "revision":"9"
      }'
      ```

      ```json Response
      {
        "booking": {
          "id": "70284a22-4511-4e86-bd3c-de17463882f0",
          "bookedEntity": {
            "slot": {
              "sessionId": "",
              "eventId": "65a5776e-37f1-45f7-8e4c-f22d01b23254",
              "serviceId": "8bdfb0dc-9461-4f21-b576-f866f9f50dfb",
              "scheduleId": "1b5e8844-7d10-42a3-b326-87941e5efdcb",
              "startDate": "2022-11-02T14:00:00.000Z",
              "endDate": "2022-11-02T15:00:00.000Z",
              "timezone": "Europe/Dublin",
              "resource": {
                "id": "64bb860a-eda5-4992-9fc6-538d58e818ec",
                "name": "dodly"
              },
              "location": {
                "id": "9de094d5-8985-484e-9218-d020d9d17953",
                "name": "Location Name",
                "locationType": "OWNER_BUSINESS"
              }
            },
            "title": "CLASS",
            "tags": [
              "GROUP"
            ]
          },
          "contactDetails": {
            "contactId": "a2ca1bc8-76e4-4295-8803-ab15aa506149",
            "firstName": "Nina",
            "lastName": "Margarenwaig",
            "email": "Nina@example.com"
          },
          "additionalFields": [],
          "totalParticipants": 1,
          "status": "CONFIRMED",
          "paymentStatus": "PAID",
          "selectedPaymentOption": "OFFLINE",
          "createdDate": "2022-10-19T12:45:24.572Z",
          "revision": "10",
          "createdBy": {
            "wixUserId": "086ca7ca-ee70-4ab3-a96e-78f8420e9428"
          },
          "startDate": "2022-11-02T14:00:00Z",
          "endDate": "2022-11-02T15:00:00Z",
          "updatedDate": "2022-10-30T13:53:59.013Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Reschedules an appointment booking to a different slot or a class booking to
    a different session.

    ### Updating add-ons

    You can also call this method to update the list of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) associated with the booking.
    To do so, both of these conditions must be met:
    + The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
    + You must have `BOOKINGS.MANAGE_ADDONS` permissions and specify `flowControlSettings.allowAddOnChanges` as `true`.

    ### Course booking limitation

    You can't reschedule course bookings.

    ### Appointment sessions

    For appointments, the old session is removed from the business calendar
    while a new session is added. We recommend calling [Query Availability](https://dev.wix.com/docs/rest/business-solutions/bookings/time-slots/availability-calendar/query-availability)
    first and specifying the entire retrieved `slot`.

    ### Class sessions

    For classes, the new session must be an existing session belonging to the
    same class. We recommend retrieving `availabilityEntries.slot.eventId`
    from [Query Availability](https://dev.wix.com/docs/rest/business-solutions/bookings/time-slots/availability-calendar/query-availability)
    to avoid failed Reschedule Booking calls due to unavailability. Specify
    only `slot.eventId` instead of the entire `slot` object.

    ### Add-ons

    You can call Reschedule Booking to replace the list of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) associated with the booking.

    To do so, all of these conditions must be met:
    - The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
    - You must have `BOOKINGS.MANAGE_ADDONS` permissions.
    - For bookings paid via an [eCommerce checkout](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/checkout/introduction): The related [eCommerce order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/introduction) must be in a draftable state to correctly adjust the price when modifying add-ons. You can call [Get Order Draftability Status](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/get-order-draftability-status), specifying the order ID to check if it's draftable. If the order isn't draftable, Reschedule Booking still succeeds, but the order's `priceSummary` isn't updated.

    ### Notify customers

    You can specify a `participantNotification.message` for the customer. To send
    the message, you must also specify `participantNotification.notifyParticipants`
    as `true`.

    ### Admin overwrites

    There are small but important differences when you specify special
    `flowControlSettings`:

    - `{"ignoreReschedulePolicy": true}`: The call succeeds even if the
    service's `reschedulePolicy` doesn't allow it. Also skips
    external validation providers.
    - `{"skipAvailabilityValidation": true}`: The call succeeds even if
    the specified session, slot, or resource isn't available. If you don't
    specify any resource, the call succeeds even if no resource of the relevant
    type is available.
    - `{"skipBusinessConfirmation": true}`: Any `PENDING` booking is
    automatically confirmed even if the services requires the merchants's
    manual confirmation.

    When using special `flowControlSettings`, ensure you have sufficient
    permissions. If you encounter failed calls due to insufficient permissions,
    consider the following options:

    - **App developers** can use a higher
    [permission](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions),
    such as `MANAGE BOOKINGS - ALL PERMISSIONS`.
    - **Site developers** can utilize
    [elevation](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/authorization/elevation).

    Granting additional permissions and using elevation permits method calls that
    would typically fail due to authorization checks. Therefore, you should use
    them intentionally and securely.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`.

    ## Method Declaration

    ```typescript
    import { bookings } from '@wix/bookings';

    bookings.rescheduleBooking(bookingId, slot, options): Promise<RescheduleBookingResponse>
    ```

    ## Method Parameters

    <ParamField body="bookingId" type="string" required>

      ID of the booking to reschedule.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="slot" type="RescheduleBooking" required>

      New slot of the booking.

      <Expandable title="slot properties">

        <ParamField body="sessionId" type="string">

          Identifier for the underlying session when the session is a single session or generated from a recurring session.

        </ParamField>

        <ParamField body="serviceId" type="string">

          Service identifier. Required.

        </ParamField>

        <ParamField body="scheduleId" type="string">

          Schedule identifier. Required.

        </ParamField>

        <ParamField body="startDate" type="string">

          The start time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

        </ParamField>

        <ParamField body="endDate" type="string">

          The end time of this slot in `YYYY-MM-DDThh:mm:ss`, `YYYY-MM-DDThh:mm:ss:SSS`, or `YYYY-MM-DDThh:mm:ss:SSSZZ` [ISO-8601 format](https://en.wikipedia.org/wiki/ISO_8601). For example, `2026-01-30T13:30:00`, `2026-01-30T13:30:00:000`, or `2026-01-30T13:30:00:000-05:00`.

        </ParamField>

        <ParamField body="timezone" type="string">

          The timezone according to which the slot is calculated and presented.

        </ParamField>

        <ParamField body="resource" type="SlotResource">

          The resource required for this slot.
          When populated, the specified resource will be assigned to the slot upon confirmation according to its availability.
          When empty, if `skip_availability_validation` is `false`, a random available resource will be assigned to the slot upon confirmation.
          Otherwise, one of the service resources will be assigned to the slot randomly upon confirmation.

          <Expandable title="resource properties">

            <ParamField body="_id" type="string">

              Resource ID.

              **Read-only.**

            </ParamField>

            <ParamField body="name" type="string">

              Resource name.

            </ParamField>

            <ParamField body="transparency" type="string">

              The desired transparency for this resource in the calendar event.

              Supported values:
              + `OPAQUE`: The schedule is blocked during the booking, preventing other bookings involving the same resource from being scheduled at the same time.
              + `TRANSPARENT`: The schedule remains open during the booking, allowing other bookings to be scheduled concurrently.

              Default: `UNKNOWN_TRANSPARENCY`.

              **Supported values:**

              - `OPAQUE`
              - `TRANSPARENT`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="location" type="Location">

          Geographic location of the slot.

          <Expandable title="location properties">

            <ParamField body="_id" type="string">

              Business Location ID. Present if the location is a business location.

            </ParamField>

            <ParamField body="name" type="string">

              Location name.

            </ParamField>

            <ParamField body="formattedAddress" type="string">

              A string containing the full address of this location.

            </ParamField>

            <ParamField body="locationType" type="string">

              Location type.

              **Supported values:**

              - `UNDEFINED` — Undefined location type.
              - `OWNER_BUSINESS` — The business address as set in the site’s general settings.
              - `OWNER_CUSTOM` — The address set when creating the service.
              - `CUSTOM` — The address set for the individual session.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="eventId" type="string">

          Calendar event ID - not supported.
          If not empty, on all write flows (create/update), it takes priority over `sessionId`.
          So if both `sessionId` and `eventId` are provided, the `sessionId` will be based on the `eventId`.
          Otherwise, if `eventId` is empty on write flow,

        </ParamField>

        <ParamField body="resourceSelections" type="ResourceSelection[]">

          Information about how the customer has selected resources for the booking.
          Each resource type may have a different selection method.
          Check `resource` for resource details.

          Constraints: `maxItems 3`.

          <Expandable title="resourceSelections properties">

            <ParamField body="resourceTypeId" type="string">

              ID of the [resource type](https://dev.wix.com/docs/api-reference/business-solutions/bookings/resources/resource-types-v2/introduction).

            </ParamField>

            <ParamField body="selectionMethod" type="string">

              Information about how the customer has selected the resource for this resource type.

              **Supported values:**

              - `SPECIFIC_RESOURCE` — The customer explicitly chose a particular resource.
              - `ANY_RESOURCE` — The customer explicitly chose "any available resource" for this resource type.
              - `NO_SELECTION` — The customer wasn't offered a resource selection or agreement option for this resource type.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="allDay" type="boolean">

          Whether the slot spans one or more full days.
          For class bookings, this value is derived from the linked calendar event.
          For appointment bookings, this value is derived from the availability slot. Any caller-provided value is ignored.

          When `true`, `startDate` is set to `00:00:00` of the first day and `endDate` is set to `00:00:00` of the day after the last day (exclusive). Midnight boundaries are interpreted in the slot's `timezone`.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="RescheduleBookingOptions" required>

      An object representing the available options for rescheduling a booking. 

      <Expandable title="options properties">

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the booking is rescheduled.
          To prevent conflicting changes, the current revision must be passed when
          rescheduling the booking.

        </ParamField>

        <ParamField body="participantNotification" type="ParticipantNotification">

          Information about whether to notify the customer about the rescheduling and
          the message to send.

          <Expandable title="participantNotification properties">

            <ParamField body="notifyParticipants" type="boolean">

              Whether to send a message about the changes to the customer.

              Default: `false`

            </ParamField>

            <ParamField body="message" type="string">

              Custom message to send to the participants about the changes to the booking.

            </ParamField>

            <ParamField body="metadata" type="object">

              Information about the delivery channels used to send the notification.
              For example, `{"channels": "SMS" }`, `{"channels": "EMAIL" }`, or `{"channels": "EMAIL, SMS" }`.

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="flowControlSettings" type="RescheduleBookingFlowControlSettings">

          Whether to ignore specific standard procedures of the Wix Bookings flow.
          For example, whether to check availability when rescheduling a booking.

          <Expandable title="flowControlSettings properties">

            <ParamField body="ignoreReschedulePolicy" type="boolean">

              Whether the rescheduling policy applies when rescheduling the booking.

              When passing `false`, you can only reschedule a booking if the rescheduling
              policy allows it.
              When passing `true`, the rescheduling policy is ignored and external
              validation providers are also skipped.
              Default: `false`.

            </ParamField>

            <ParamField body="skipAvailabilityValidation" type="boolean">

              Whether the availability is checked before rescheduling the booking.

              When passing `false`, a booking is only created when the slot or
              schedule is available.
              Default: `false`.

            </ParamField>

            <ParamField body="skipBusinessConfirmation" type="boolean">

              Whether the rescheduled booking's status is automatically set to
              `CONFIRMED` for services that normally require the owner's manual
              confirmation.
              Default: `false`.

            </ParamField>

            <ParamField body="allowAddOnChanges" type="boolean">

              Whether you can update the list of add-ons associated with the booking when rescheduling.
              The calling [identity](https://dev.wix.com/docs/build-apps/develop-your-app/access/about-identities) must have `BOOKINGS.MANAGE_ADDONS` permissions to specify `true`.

              Default: `false`.

              **Developer Preview.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="bookedAddOns" type="BookedAddOn[]">

          List of [add-ons](https://dev.wix.com/docs/api-reference/business-solutions/bookings/services/add-ons/introduction) added to the booking.

          To replace the booking's the list of booked add-ons, all of these conditions must be met:
          - The booking must have been created with `flowControlSettings.allowAddOnChanges` set to `true`.
          - You must have `BOOKINGS.MANAGE_ADDONS` permissions.
          - For bookings paid via an [eCommerce checkout](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/checkout/introduction): The related [eCommerce order](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/introduction) must be in a draftable state to correctly adjust the price when modifying add-ons. You can call [Get Order Draftability Status](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/draft-orders/get-order-draftability-status), specifying the order ID to check if it's draftable. If the order isn't draftable, Reschedule Booking still succeeds, but the order's `priceSummary` isn't updated.

          Constraints: `maxItems 21`.

          <Expandable title="bookedAddOns properties">

            <ParamField body="_id" type="string">

              The ID of the add-on.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="groupId" type="string">

              The ID of the add-on group.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="durationInMinutes" type="integer">

              The add-on duration in minutes at the time of booking.
              Populated for duration-based add-ons.

              **Read-only.**

            </ParamField>

            <ParamField body="quantity" type="integer">

              The quantity of booked add-ons.
              For duration-based add-ons, `quantity` is not applicable.
              If `quantity` is provided as `1` for a duration-based add-on, it will be accepted but the value will be cleared.
              If any other value is provided, an `INVALID_ARGUMENT` error will be returned with the message: "Invalid AddOn details: either duration or quantity must be set correctly".

            </ParamField>

            <ParamField body="name" type="string">

              Add-on `name` at the time of booking.

              **Read-only.**

            </ParamField>

            <ParamField body="nameTranslated" type="string">

              Add-on name translated to the language the customer used during booking.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="participantsInfo" type="participantsInfo">

          participantsInfo

          <Expandable title="participantsInfo properties">

            <ParamField body="totalParticipants" type="integer">

              Total number of participants. Specify when all participants have booked the
              same service variant.

            </ParamField>

            <ParamField body="participantsChoices" type="ParticipantChoices">

              Information about the service choices to book. Specify when not all
              participants have booked the same service variant.

              _Nested structure omitted for brevity — see the `ParticipantChoices` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<RescheduleBookingResponse>`.

    <ResponseField name="booking" type="Booking">

      Rescheduled booking.

      See the [Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-writer-v2/booking-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `BOOKING_NOT_FOUND` | Couldn't find the booking. |
    | `403` | `PERMISSION_DENIED` | `BOOKING_POLICY_VIOLATION` | The booking violates the service's booking policy. Check booking window restrictions, advance notice requirements, or scheduling rules. |
    | `403` | `PERMISSION_DENIED` | `UNAUTHORIZED_OPERATION` | The [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) used to call the method doesn't have the required permissions. |
    | `428` | `FAILED_PRECONDITION` | `SESSION_CAPACITY_EXCEEDED` | The session capacity was exceeded. The requested number of participants exceeds available capacity. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULE_CAPACITY_EXCEEDED` | The schedule capacity was exceeded. The requested number of participants exceeds available capacity. |
    | `428` | `FAILED_PRECONDITION` | `SLOT_NOT_AVAILABLE` | The slot is no longer available for booking. It may have been booked by another customer or is outside business hours. |
    | `400` | `INVALID_ARGUMENT` | `VALIDATION_FAILURE` | Validation failed. Verify all required booking fields are provided and valid. |
    | `428` | `FAILED_PRECONDITION` | `EMPTY_FORM_INFO` | Form submission information is missing. Provide the required form data. |
    | `428` | `FAILED_PRECONDITION` | `SESSION_NOT_FOUND` | The specified session doesn't exist. Verify the session ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULE_NOT_FOUND` | The specified schedule doesn't exist. Verify the schedule ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `RESOURCE_NOT_FOUND` | Couldn't find the resource. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_VALIDATING_AVAILABILITY` | Availability validation failed. The requested time slot or schedule may be fully booked or unavailable. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_RESOLVING_SESSION` | Couldn't retrieve session information. The session may not exist or is unavailable. |
    | `428` | `FAILED_PRECONDITION` | `FAILED_RESOLVING_SCHEDULE` | Couldn't retrieve schedule information. The schedule may not exist or is unavailable. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_SERVICE_CHOICES` | The specified service choices are invalid. Verify the choices match the service's supported options and variants. |
    | `403` | `PERMISSION_DENIED` | `NOT_ALLOWED_TO_UPDATE_BOOKING_IN_MULTI_SERVICE_BOOKING` | This booking is part of a multi-service booking and can't be updated individually. Update the multi-service booking instead. |
    | `428` | `FAILED_PRECONDITION` | `ADD_ON_NOT_FOUND` | The specified add-on doesn't exist. Verify the add-on ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `ADD_ON_CHANGES_NOT_ALLOWED` | Add-ons can't be modified for this booking. Check the booking status and add-on configuration. |
    | `428` | `FAILED_PRECONDITION` | `ADD_ON_GROUP_NOT_FOUND` | The specified add-on group doesn't exist. Verify the add-on group ID is correct. |
    | `428` | `FAILED_PRECONDITION` | `PRICE_CHANGE_NOT_ALLOWED` | The booking price can't be changed. Verify the booking status and pricing rules. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Booking Rescheduled | `wix.bookings.v2.booking_rescheduled` | Triggered when a booking is rescheduled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-reschedule-booking" id="ex-sdk-reschedule-booking-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-reschedule-booking" id="ex-sdk-reschedule-booking-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-reschedule-booking-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-reschedule-booking-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">rescheduleBooking</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">rescheduleBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-reschedule-booking-0"><span className="wix-picker-name">rescheduleBooking</span></label>
    <label htmlFor="ex-sdk-reschedule-booking-1"><span className="wix-picker-name">rescheduleBooking (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript rescheduleBooking
    import { bookings } from '@wix/bookings';

    async function rescheduleBooking(bookingId,slot,options) {
      const response = await bookings.rescheduleBooking(bookingId,slot,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript rescheduleBooking (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bookings } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bookings },
      // Include the auth strategy and host as relevant
    });

    async function rescheduleBooking(bookingId,slot,options) {
      const response = await myWixClient.bookings.rescheduleBooking(bookingId,slot,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
