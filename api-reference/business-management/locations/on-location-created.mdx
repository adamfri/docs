---
title: "onLocationCreated"
description: "Triggered when a location is created."
icon: "bolt"
---

Triggered when a location is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.locations.v1.location_created` |
| Package | `@wix/business-tools` |
| Permissions | `Manage Locations` |

## Examples

<CodeGroup>

  ```javascript onLocationCreated: Wix-hosted
  import { locations } from '@wix/business-tools';

  locations.onLocationCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocationCreated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { locations } from '@wix/business-tools';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        locations,
      },
  });

  wixClient.locations.onLocationCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
