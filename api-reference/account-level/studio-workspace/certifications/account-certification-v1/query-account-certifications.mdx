---
title: "Query Account Certifications"
description: "Retrieves a list of up to 1,000 account certifications, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1,000 account certifications, given the provided paging, filtering, and sorting.

    Query Account Certifications returns the full account certification records, including the underlying `userCertificationIds`, for the caller's own account only. To retrieve any account's certifications as a public, visitor-safe projection, call [List Account Certifications](https://dev.wix.com/docs/api-reference/account-level/studio-workspace/certifications/account-certification-v1/list-account-certifications) instead.

    Query Account Certifications runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `50`

    You can filter and sort by `id`, `certificationType`, and `createdDate`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `certificationType` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix user or by using an account level API key.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ACADEMY.ACCOUNT_CERTIFICATION_READ">SCOPE.ACADEMY.ACCOUNT_CERTIFICATION_READ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/academy/certifications/v1/account-certifications/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options. See [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language) for more information.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 50`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="accountCertifications" type="AccountCertification[]">

      List of account certifications.

      See the [Account Certification Object](/api-reference/account-level/studio-workspace/certifications/account-certification-v1/account-certification-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves the certifications held by your own account, filtered by certification type

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/academy/certifications/v1/account-certifications/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "certificationType": "VELO"
          },
          "cursorPaging": {
            "limit": 10
          }
        }
      }'
      ```

      ```json Response
      {
        "accountCertifications": [
          {
            "id": "c1d2e3f4-5a6b-4c7d-8e9f-0a1b2c3d4e5f",
            "revision": "3",
            "createdDate": "2024-01-15T10:30:00.000Z",
            "updatedDate": "2024-03-02T09:12:45.000Z",
            "userCertificationIds": [
              "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
              "9f8e7d6c-5b4a-4938-8271-6c5d4e3f2a1b"
            ],
            "certificationType": "VELO"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {
            "next": "eyJvZmZzZXQiOjEwfQ=="
          },
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1,000 account certifications, given the provided paging, filtering, and sorting.

    Query Account Certifications returns the full account certification records, including the underlying `userCertificationIds`, for the caller's own account only. To retrieve any account's certifications as a public, visitor-safe projection, call [List Account Certifications](https://dev.wix.com/docs/api-reference/account-level/studio-workspace/certifications/account-certification-v1/list-account-certifications) instead.

    Query Account Certifications runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `cursorPaging.limit` is `50`

    You can filter and sort by `id`, `certificationType`, and `createdDate`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `certificationType` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ACADEMY.ACCOUNT_CERTIFICATION_READ">SCOPE.ACADEMY.ACCOUNT_CERTIFICATION_READ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    <Note>This is an account-level method.</Note>

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { accountCertifications } from '@wix/account-certifications';

    accountCertifications.queryAccountCertifications(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="AccountCertificationQuery" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language) for more information.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 50`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="accountCertifications" type="AccountCertification[]">

      List of account certifications.

      See the [Account Certification Object](/api-reference/account-level/studio-workspace/certifications/account-certification-v1/account-certification-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Offset that was requested.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates if there are more results after the current page.
          If `true`, another page of results can be retrieved.
          If `false`, this is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-account-certifications" id="ex-sdk-query-account-certifications-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-account-certifications" id="ex-sdk-query-account-certifications-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-account-certifications-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-account-certifications-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query account certifications</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryAccountCertifications (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-account-certifications-0"><span className="wix-picker-name">Query account certifications</span></label>
    <label htmlFor="ex-sdk-query-account-certifications-1"><span className="wix-picker-name">queryAccountCertifications (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query account certifications
    import { accountCertifications } from "@wix/account-certifications";

    const query = {
      filter: { certificationType: accountCertifications.CertificationType.VELO },
      cursorPaging: { limit: 10 },
    };

    async function queryAccountCertifications() {
      const result = await accountCertifications.queryAccountCertifications(query);

      return result;
    }

    /* Promise resolves to:
     * {
     *   "accountCertifications": [
     *     {
     *       "_id": "c1d2e3f4-5a6b-4c7d-8e9f-0a1b2c3d4e5f",
     *       "revision": "3",
     *       "_createdDate": "2024-01-15T10:30:00.000Z",
     *       "_updatedDate": "2024-03-02T09:12:45.000Z",
     *       "userCertificationIds": [
     *         "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
     *         "9f8e7d6c-5b4a-4938-8271-6c5d4e3f2a1b"
     *       ],
     *       "certificationType": "VELO"
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 1,
     *     "cursors": { "next": "eyJvZmZzZXQiOjEwfQ==" },
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryAccountCertifications (self-hosted)
    import { createClient } from '@wix/sdk';
    import { accountCertifications } from '@wix/account-certifications';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { accountCertifications },
      // Include the auth strategy and host as relevant
    });

    async function queryAccountCertifications(query) {
      const response = await myWixClient.accountCertifications.queryAccountCertifications(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
