---
title: "Truncate Data Items"
description: "Removes all items from a collection."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Removes all items from a collection.

    ## Endpoint

    ```http
    POST /v3/items/truncate
    ```

    ## Body Params

    <ParamField body="collectionId" type="string" required>

      ID of the collection from which to remove all items.

    </ParamField>

    ## Response Object

    Returns `TruncateDataItemsResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST https://external-db.example.com/v3/items/truncate  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
        "collectionId": "cities"
      }'
      ```

      ```json Response
      {
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Removes all items from a collection.

    ## Method Declaration

    ```typescript
    import { externalDatabase } from '@wix/data';

    externalDatabase.truncateDataItems(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="TruncateDataItemsRequest">

      request

      <Expandable title="request properties">

        <ParamField body="collectionId" type="string">

          ID of the collection from which to remove all items.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `TruncateDataItemsResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-truncate-data-items" id="ex-sdk-truncate-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-truncate-data-items" id="ex-sdk-truncate-data-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-truncate-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-truncate-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Example of a truncate data items handler function</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">truncateDataItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-truncate-data-items-0"><span className="wix-picker-name">Example of a truncate data items handler function</span></label>
    <label htmlFor="ex-sdk-truncate-data-items-1"><span className="wix-picker-name">truncateDataItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Example of a truncate data items handler function
    import { externalDatabase } from '@wix/data/service-plugins';

    externalDatabase.provideHandlers({

      truncateDataItems: async ( payload ) => {
        const {request, metadata} = payload;
            // Use the `request` and `metadata` received from Wix and
            // apply custom logic.
          return {}
        }
    });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript truncateDataItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { externalDatabase } from '@wix/data/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { externalDatabase },
      // Include the auth strategy and host as relevant
    });

    async function truncateDataItems(request,metadata) {
      const response = await myWixClient.externalDatabase.truncateDataItems(request,metadata);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
