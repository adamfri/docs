---
title: "onProjectDeleted"
description: "Triggered when a project is deleted."
icon: "bolt"
---

Triggered when a project is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.projects.v1.project_deleted` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onProjectDeleted
  import { projects } from '@wix/portfolio';

  projects.onProjectDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onProjectDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { projects } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        projects,
      },
  });

  wixClient.projects.onProjectDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
