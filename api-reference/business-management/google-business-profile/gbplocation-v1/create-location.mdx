---
title: "Create Location"
description: "Imports an existing Google Business Profile location."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Imports an existing Google Business Profile location.

    Specify the Google account ID in `accountId` and the Google location ID in
    `location.id`. Both are available from List Unimported Locations.

    This doesn't create a listing in Google. It records a location the Google
    account already has, so that the site can manage it. To create a new listing,
    call Create Google Location.

    A Google account must be connected first, or the call fails with
    `CONNECTION_NOT_FOUND`. See the
    [Connection API](https://dev.wix.com/docs/api-reference/business-management/google-business-profile/connection-v1).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/gbp/v1/locations
    ```

    ## Body Params

    <ParamField body="location" type="GBPLocation" required>

      The location to record for this site.

      `id` is required and must be Google's location ID. `accountId` is required
      too, because it identifies which Google account holds the listing.

      See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="location" type="GBPLocation">

      The created location.

      See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CONNECTION_NOT_FOUND` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/gbp/v1/locations' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "location": {
          "id": "8034569321457853640",
          "accountId": "104312867792031877552"
        }
      }'
      ```

      ```json Response
      {
        "location": {
          "id": "8034569321457853640",
          "accountId": "104312867792031877552",
          "createdDate": "2026-06-02T11:38:54.207Z",
          "updatedDate": "2026-06-02T11:38:54.207Z",
          "revision": "1",
          "googleLocationId": "8034569321457853640"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Imports an existing Google Business Profile location.

    Specify the Google account ID in `accountId` and the Google location ID in
    `location.id`. Both are available from List Unimported Locations.

    This doesn't create a listing in Google. It records a location the Google
    account already has, so that the site can manage it. To create a new listing,
    call Create Google Location.

    A Google account must be connected first, or the call fails with
    `CONNECTION_NOT_FOUND`. See the
    [Connection API](https://dev.wix.com/docs/api-reference/business-management/google-business-profile/connection-v1).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/gbp';

    locations.createLocation(location): Promise
    ```

    ## Method Parameters

    <ParamField body="location" type="GBPLocation" required>

      The location to record for this site.

      `id` is required and must be Google's location ID. `accountId` is required
      too, because it identifies which Google account holds the listing.

      See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `GBPLocation`.

    See the [Gbplocation Object](/api-reference/business-management/google-business-profile/gbplocation-v1/gbplocation-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CONNECTION_NOT_FOUND` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Gbplocation Created | `wix.gbp.location.v1.gbplocation_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-location" id="ex-sdk-create-location-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-location" id="ex-sdk-create-location-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-location-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-location-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Import an existing Google location</span><span className="wix-picker-desc">Record a Google Business Profile location for the site by its Google identifiers</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-location-0"><span className="wix-picker-name">Import an existing Google location</span><span className="wix-picker-desc">Record a Google Business Profile location for the site by its Google identifiers</span></label>
    <label htmlFor="ex-sdk-create-location-1"><span className="wix-picker-name">createLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Import an existing Google location
    import { locations } from "@wix/gbp";

    async function createLocation() {
      const location = await locations.createLocation({
        _id: "8034569321457853640",
        accountId: "104312867792031877552",
      });

      console.log(location._id);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createLocation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/gbp';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function createLocation(location) {
      const response = await myWixClient.locations.createLocation(location);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
