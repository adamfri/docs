---
title: "Query Policies"
description: "Retrieves a list of policies according to the provided filters and paging."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of policies according to the provided filters and paging.

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `eventId` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `revision` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `body` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-POLICIES">Read Policies</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v2/policies/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2" required>

      Query options. See [API Query Langauge](https://dev.wix.com/api/rest/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format: &lt;br/> `"filter" : { "fieldName1": "value1", "fieldName2":{"$operator":"value2"} }`. &lt;br/> &lt;br/> **Example:** &lt;br/> `"filter" : { "id": "2224a9d1-79e6-4549-a5c5-bf7ce5aac1a5", "revision": {"$ne":"1"} }` &lt;br/> &lt;br/> See [supported fields and operators](https://dev.wix.com/api/rest/wix-events/policy-v2/filter-and-sort) for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format: &lt;br/> `[{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]` &lt;br/> &lt;br/> **Example:** &lt;br/> `[{"fieldName":"createdDate","direction":"DESC"}]` &lt;br/> &lt;br/> See [supported fields](https://dev.wix.com/api/rest/wix-events/policy-v2/filter-and-sort) for more information.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order (ASC/DESC). Defaults to ASC

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="policies" type="Policy[]">

      List of policies.

      See the [Policy Object](/api-reference/business-solutions/events/event-management/policies-v2/policy-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/policies/query' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      --data-binary '{
        "query": {
          "filter": {
            "eventId": {
              "$eq": "9d720f99-1b5a-4141-9877-d32985391e18"
            }
          },
          "sort": [
            {
              "fieldName": "createdDate",
              "order": "DESC"
            }
          ]
        }
      }'
      ```

      ```json Response
      {
        "policies": [
          {
            "id": "eef70fb3-8567-4861-9f77-ebd2ad3682d5",
            "revision": "1",
            "createdDate": "2023-02-14T12:31:52.850Z",
            "updatedDate": "2023-02-14T12:31:52.850Z",
            "name": "Terms and Conditions Event 1",
            "body": "<p>Tickets to this Event are issued on behalf of the Organiser and are subject to the following terms and conditions.</p>",
            "eventId": "9d720f99-1b5a-4141-9877-d32985391e18"
          },
          {
            "id": "7243931d-74e4-4d6a-91f0-4835fa79161e",
            "revision": "1",
            "createdDate": "2023-02-14T11:54:08.379Z",
            "updatedDate": "2023-02-14T11:54:08.379Z",
            "name": "Terms and Conditions Event 2",
            "body": "Nobody will be allowed admission to the Event without a valid ticket or pass.",
            "eventId": "9d720f99-1b5a-4141-9877-d32985391e18"
          }
        ],
        "metadata": {
          "count": 2,
          "cursors": {}
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of policies according to the provided filters and paging.

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `eventId` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `revision` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `body` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$in`, `$nin`, `$lt`, `$lte`, `$gt`, `$gte`, `$exists` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.READ-POLICIES">Read Policies</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { policies } from '@wix/events';

    policies.queryPolicies(query): PoliciesQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="PolicyQuery" required>

      Query options. See [API Query Langauge](https://dev.wix.com/api/rest/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format: &lt;br/> `"filter" : { "fieldName1": "value1", "fieldName2":{"$operator":"value2"} }`. &lt;br/> &lt;br/> **Example:** &lt;br/> `"filter" : { "id": "2224a9d1-79e6-4549-a5c5-bf7ce5aac1a5", "revision": {"$ne":"1"} }` &lt;br/> &lt;br/> See [supported fields and operators](https://dev.wix.com/api/rest/wix-events/policy-v2/filter-and-sort) for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format: &lt;br/> `[{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]` &lt;br/> &lt;br/> **Example:** &lt;br/> `[{"fieldName":"createdDate","direction":"DESC"}]` &lt;br/> &lt;br/> See [supported fields](https://dev.wix.com/api/rest/wix-events/policy-v2/filter-and-sort) for more information.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order (ASC/DESC). Defaults to ASC

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `PoliciesQueryBuilder`.

    <ResponseField name="policies" type="Policy[]">

      List of policies.

      See the [Policy Object](/api-reference/business-solutions/events/event-management/policies-v2/policy-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Metadata for the paginated results.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-policies" id="ex-sdk-query-policies-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-policies" id="ex-sdk-query-policies-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-policies-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-policies-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query Policies by name and sort by _createdDate in descending order</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryPolicies (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-policies-0"><span className="wix-picker-name">Query Policies by name and sort by _createdDate in descending order</span></label>
    <label htmlFor="ex-sdk-query-policies-1"><span className="wix-picker-name">queryPolicies (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query Policies by name and sort by _createdDate in descending order
    import { policies } from '@wix/events';

    //query policies
    async function queryPolicies() {
       try {
        const items = await policies.queryPolicies()
          .eq('eventId', '3d3d5c04-ece0-45a8-85f0-11a58edaa192')
          .ascending('_createdDate')
          .find();
        return items;
      }
      catch (error) {
        console.error(error);
        // Handle the error
       } 
     };

    /* Returns all found policies
    {
      "_items": [
        {
          "_id": "96b39980-e4e1-44e3-9ef0-9bec41d1cc10",
          "revision": "6",
          "_createdDate": "2023-03-08T13:55:18.057Z",
          "_updatedDate": "2023-03-15T13:43:11.794Z",
          "name": "Terms and Conditions Event 2",
          "body": "<p>Nobody will be allowed admission to the Event without a valid ticket or pass.</p>",
          "eventId": "3d3d5c04-ece0-45a8-85f0-11a58edaa192"
        },
        {
          "_id": "850beadf-f367-4197-a0c8-6624cb3bcef9",
          "revision": "1",
          "_createdDate": "2023-03-15T13:49:27.023Z",
          "_updatedDate": "2023-03-15T13:49:27.023Z",
          "name": "Terms and Conditions Event 1",
          "body": "<p>Tickets to this Event are issued on behalf of the Organiser and are subject to the following terms and conditions.</p>",
          "eventId": "3d3d5c04-ece0-45a8-85f0-11a58edaa192"
        }
      ],
      "_originQuery": {
        "filterTree": {
          "$and": [
            {
              "eventId": "3d3d5c04-ece0-45a8-85f0-11a58edaa192"
            }
          ]
        },
        "invalidArguments": [],
        "encoder": {},
        "transformationPaths": {},
        "sort": [
          {
            "fieldName": "createdDate",
            "order": "ASC"
          }
        ],
        "paging": {},
        "pagingMethod": "CURSOR",
        "builderOptions": {
          "cursorWithEmptyFilterAndSort": true
        }
      },
      "_limit": 50,
      "_nextCursor": "",
      "_prevCursor": "",
      "cursors": {
        "next": "",
        "prev": ""
      }
    }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryPolicies (self-hosted)
    import { createClient } from '@wix/sdk';
    import { policies } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { policies },
      // Include the auth strategy and host as relevant
    });

    async function queryPolicies(query) {
      const response = await myWixClient.policies.queryPolicies(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
