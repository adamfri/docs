---
title: "onBillableItemCreated"
description: "Triggered when a billable item is created."
icon: "bolt"
---

Triggered when a billable item is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.billable_items.v1.billable_item_created` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Billable Items`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onBillableItemCreated
  import { billableItems } from '@wix/get-paid';

  billableItems.onBillableItemCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBillableItemCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { billableItems } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        billableItems,
      },
  });

  wixClient.billableItems.onBillableItemCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
