---
title: "List Member Following"
description: "Retrieves a list of members who are followed by the given member."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of members who are followed by the given member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-FOLLOWERS">Read Member Connections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/members/v3/followers/{memberId}/following
    ```

    ## Path Params

    <ParamField path="memberId" type="string" required>

      ID of the member whose followers to list.

    </ParamField>

    ## Query Params

    <ParamField query="paging" type="CursorPaging">

      Pagination options. For more information, see
      [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging).

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Number of items to return. See [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging).

        </ParamField>

        <ParamField query="cursor" type="string">

          Cursor returned from last query response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="memberIds" type="string[]">

      Retrieved list of members who are followed by the given member.

      Constraints: `format GUID`.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items starting from given cursor.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/members/v3/followers/24d1fada-b1fb-44e3-ab31-d48b243b9d34/following' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "memberIds": [
          "550993c0-49de-444d-8301-8ac7e3ecdc51",
          "f7e6f0e5-1247-41e5-89d2-99deefbef9c4"
        ],
        "pagingMetadata": {
          "count": 2,
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of members who are followed by the given member.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.READ-FOLLOWERS">Read Member Connections</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { memberFollowers } from '@wix/members';

    memberFollowers.listMemberFollowing(memberId, options): Promise<ListMemberFollowingResult>
    ```

    ## Method Parameters

    <ParamField body="memberId" type="string" required>

      ID of the member whose followers to list.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="ListMemberFollowingOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="CursorPaging">

          Pagination options. For more information, see
          [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging).

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to return. See [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging).

            </ParamField>

            <ParamField body="cursor" type="string">

              Cursor returned from last query response.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListMemberFollowingResult>`.

    <ResponseField name="memberIds" type="string[]">

      Retrieved list of members who are followed by the given member.

      Constraints: `format GUID`.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Details on the paged set of results returned.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items starting from given cursor.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-member-following" id="ex-sdk-list-member-following-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-member-following" id="ex-sdk-list-member-following-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-member-following-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-member-following-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listMemberFollowing</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listMemberFollowing (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-member-following-0"><span className="wix-picker-name">listMemberFollowing</span></label>
    <label htmlFor="ex-sdk-list-member-following-1"><span className="wix-picker-name">listMemberFollowing (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listMemberFollowing
    import { memberFollowers } from '@wix/members';

    async function listMemberFollowing(memberId,options) {
      const response = await memberFollowers.listMemberFollowing(memberId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listMemberFollowing (self-hosted)
    import { createClient } from '@wix/sdk';
    import { memberFollowers } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { memberFollowers },
      // Include the auth strategy and host as relevant
    });

    async function listMemberFollowing(memberId,options) {
      const response = await myWixClient.memberFollowers.listMemberFollowing(memberId,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
