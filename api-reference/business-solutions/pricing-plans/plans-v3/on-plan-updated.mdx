---
title: "onPlanUpdated"
description: "Triggered when a plan is updated."
icon: "bolt"
---

Triggered when a plan is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.v3.plan_updated` |
| Package | `@wix/pricing-plans` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Read Pricing Plans`, `Manage plans and orders of Pricing Plans`, `Manage Events`, `Manage Pricing Plans` |

## Examples

<CodeGroup>

  ```javascript onPlanUpdated
  import { plansV3 } from '@wix/pricing-plans';

  plansV3.onPlanUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPlanUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { plansV3 } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        plansV3,
      },
  });

  wixClient.plansV3.onPlanUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
