---
title: "Update Extended Fields"
description: "Updates the extended fields of an order payment request."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates the extended fields of an order payment request.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/ecom/v1/order-payment-requests/{id}/update-extended-fields
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the entity to update.

    </ParamField>

    ## Body Params

    <ParamField body="namespace" type="string" required>

      Identifier for the app whose extended fields are being updated.

    </ParamField>

    <ParamField body="namespaceData" type="object" required>

      Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured.

    </ParamField>

    ## Response Object

    <ResponseField name="orderPaymentRequest" type="OrderPaymentRequest">

      Updated order payment request.

      See the [Order Payment Request Object](/api-reference/business-solutions/e-commerce/orders/order-payment-requests/order-payment-request-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Updates the extended fields of an order payment request with app-specific data.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/ecom/v1/order-payment-requests/3c8f1a2e-7b4d-4f9e-8c61-5d2e9a0b1c3f/update-extended-fields' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "namespace": "my-app-namespace",
          "namespaceData": {
            "invoiceId": "INV-2026-00123",
            "approvedBy": "manager@example.com"
          }
        }'
      ```

      ```json Response
      {
        "orderPaymentRequest": {
          "id": "3c8f1a2e-7b4d-4f9e-8c61-5d2e9a0b1c3f",
          "status": "UNPAID",
          "orderId": "a931e280-254a-49a7-9641-22ef64c444ae",
          "orderNumber": "10052",
          "amount": {
            "amount": "15.50",
            "formattedAmount": "$15.50"
          },
          "currency": "USD",
          "title": "Balance Due for Order #10052",
          "extendedFields": {
            "namespaces": {
              "my-app-namespace": {
                "invoiceId": "INV-2026-00123",
                "approvedBy": "manager@example.com"
              }
            }
          },
          "createdDate": "2026-06-01T14:22:18.456Z",
          "updatedDate": "2026-06-16T09:45:32.123Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates the extended fields of an order payment request.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { orderPaymentRequests } from '@wix/ecom';

    orderPaymentRequests.updateExtendedFields(_id, namespace, options): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the entity to update.

    </ParamField>

    <ParamField body="namespace" type="string" required>

      Identifier for the app whose extended fields are being updated.

    </ParamField>

    <ParamField body="options" type="UpdateExtendedFieldsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="namespaceData" type="object" required>

          Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="orderPaymentRequest" type="OrderPaymentRequest">

      Updated order payment request.

      See the [Order Payment Request Object](/api-reference/business-solutions/e-commerce/orders/order-payment-requests/order-payment-request-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-extended-fields" id="ex-sdk-update-extended-fields-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-extended-fields" id="ex-sdk-update-extended-fields-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-extended-fields-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-extended-fields-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update extended fields</span><span className="wix-picker-desc">Updates the extended fields of an order payment request with app-specific data</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateExtendedFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-extended-fields-0"><span className="wix-picker-name">Update extended fields</span><span className="wix-picker-desc">Updates the extended fields of an order payment request with app-specific data</span></label>
    <label htmlFor="ex-sdk-update-extended-fields-1"><span className="wix-picker-name">updateExtendedFields (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update extended fields

    import { orderPaymentRequests } from '@wix/ecom';

    const orderPaymentRequestId = '3c8f1a2e-7b4d-4f9e-8c61-5d2e9a0b1c3f';
    const namespace = 'my-app-namespace';
    const namespaceData = {
      invoiceId: 'INV-2026-00123',
      approvedBy: 'manager@example.com',
    };

    export async function myUpdateExtendedFieldsFunction() {
      try {
        const response = await orderPaymentRequests.updateExtendedFields(
          orderPaymentRequestId,
          namespace,
          { namespaceData }
        );
        console.log('Success! Updated order payment request:', response.orderPaymentRequest);
        return response;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "orderPaymentRequest": {
     *     "_id": "3c8f1a2e-7b4d-4f9e-8c61-5d2e9a0b1c3f",
     *     "status": "UNPAID",
     *     "orderId": "a931e280-254a-49a7-9641-22ef64c444ae",
     *     "orderNumber": "10052",
     *     "amount": {
     *       "amount": "15.50",
     *       "formattedAmount": "$15.50"
     *     },
     *     "currency": "USD",
     *     "title": "Balance Due for Order #10052",
     *     "extendedFields": {
     *       "namespaces": {
     *         "my-app-namespace": {
     *           "invoiceId": "INV-2026-00123",
     *           "approvedBy": "manager@example.com"
     *         }
     *       }
     *     },
     *     "_createdDate": "2026-06-01T14:22:18.456Z",
     *     "_updatedDate": "2026-06-16T09:45:32.123Z"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateExtendedFields (self-hosted)
    import { createClient } from '@wix/sdk';
    import { orderPaymentRequests } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { orderPaymentRequests },
      // Include the auth strategy and host as relevant
    });

    async function updateExtendedFields(_id,namespace,options) {
      const response = await myWixClient.orderPaymentRequests.updateExtendedFields(_id,namespace,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
