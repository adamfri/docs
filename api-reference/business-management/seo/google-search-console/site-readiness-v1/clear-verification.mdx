---
title: "Clear Verification"
description: "Removes the site's Google verification tag and Google connection."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Removes the site's Google verification tag and Google connection.

    Deletes the `google-site-verification` meta tag that Verify Site placed,
    discards the stored verification state, and also deletes the site's stored
    Google credentials. After this call the site's connection status is
    `NOT_CONNECTED`. To remove only the credentials and keep the verification
    tag, use the GSC Connection API's Disconnect instead. Neither call removes
    the site from Google Search Console.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/gsc/v1/clear-verification
    ```

    ## Response Object

    Returns `ClearVerificationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/gsc/v1/clear-verification' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{}'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Removes the site's Google verification tag and Google connection.

    Deletes the `google-site-verification` meta tag that Verify Site placed,
    discards the stored verification state, and also deletes the site's stored
    Google credentials. After this call the site's connection status is
    `NOT_CONNECTED`. To remove only the credentials and keep the verification
    tag, use the GSC Connection API's Disconnect instead. Neither call removes
    the site from Google Search Console.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { siteReadiness } from '@wix/gsc';

    siteReadiness.clearVerification(): Promise
    ```

    ## Returns

    Returns `ClearVerificationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-clear-verification" id="ex-sdk-clear-verification-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-clear-verification" id="ex-sdk-clear-verification-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-clear-verification-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-clear-verification-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Clear the site's Google verification</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">clearVerification (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-clear-verification-0"><span className="wix-picker-name">Clear the site's Google verification</span></label>
    <label htmlFor="ex-sdk-clear-verification-1"><span className="wix-picker-name">clearVerification (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Clear the site's Google verification
    import { siteReadiness } from "@wix/gsc";

    async function clearVerification() {
      await siteReadiness.clearVerification();
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript clearVerification (self-hosted)
    import { createClient } from '@wix/sdk';
    import { siteReadiness } from '@wix/gsc';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { siteReadiness },
      // Include the auth strategy and host as relevant
    });

    async function clearVerification() {
      const response = await myWixClient.siteReadiness.clearVerification();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
