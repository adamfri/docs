---
title: "Delete Category"
description: "Deletes a category."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a category.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/events/v1/categories/{categoryId}
    ```

    ## Path Params

    <ParamField path="categoryId" type="string" required>

      ID of category to be deleted.

    </ParamField>

    ## Response Object

    Returns `DeleteCategoryResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE 'https://www.wixapis.com/events/v1/categories/6c313b96-1961-4f0f-b6d7-61788e1a25df' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {}
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a category.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/events';

    categories.deleteCategory(categoryId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="categoryId" type="string" required>

      ID of category to be deleted.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Deleted | `wix.events.v1.category_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-category" id="ex-sdk-delete-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-category" id="ex-sdk-delete-category-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-category" id="ex-sdk-delete-category-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a category</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Delete a category (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-category-0"><span className="wix-picker-name">Delete a category</span></label>
    <label htmlFor="ex-sdk-delete-category-1"><span className="wix-picker-name">Delete a category (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-delete-category-2"><span className="wix-picker-name">deleteCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a category
    import { categories } from '@wix/events';

    // Sample categoryId value: "5fa9929d-6782-4927-9b8c-304ca9ca6e93" 

    export async function myDeleteCategoryFunction(categoryId) {
      try {
        const deletedCategory = await categories.deleteCategory(categoryId);
        console.log('Category is deleted');
        return deletedCategory;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Delete a category (with elevated permissions)
    import { categories } from '@wix/events';
    import { auth } from '@wix/essentials';

    const elevatedDeleteCategory = auth.elevate(categories.deleteCategory);

    // Sample categoryId value: "5fa9929d-6782-4927-9b8c-304ca9ca6e93" 

    export async function myDeleteCategoryFunction(categoryId) {
      try {
        const deletedCategory = await elevatedDeleteCategory(categoryId);
        console.log('Category is deleted');
        return deletedCategory;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function deleteCategory(categoryId) {
      const response = await myWixClient.categories.deleteCategory(categoryId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
