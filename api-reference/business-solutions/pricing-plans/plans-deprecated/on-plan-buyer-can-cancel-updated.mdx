---
title: "onPlanBuyerCanCancelUpdated"
description: "Triggered when a plan's buyerCanCancel field is updated."
icon: "bolt"
---

Triggered when a plan's `buyerCanCancel` field is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.pricing_plans.plan_buyer_can_cancel_updated` |
| Package | `@wix/pricing-plans` |
| Permissions | `Read Pricing Plans`, `Manage Events`, `Manage Challenges`, `Manage Pricing Plans` |

## Examples

<CodeGroup>

  ```javascript onPlanBuyerCanCancelUpdated
  import { plans } from '@wix/pricing-plans';

  plans.onPlanBuyerCanCancelUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPlanBuyerCanCancelUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { plans } from '@wix/pricing-plans';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        plans,
      },
  });

  wixClient.plans.onPlanBuyerCanCancelUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
