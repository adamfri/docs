---
title: "onGroupDescriptionChanged"
description: "Triggered when a group's description is changed."
icon: "bolt"
---

Triggered when a group's description is changed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_group_description_changed` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupDescriptionChanged
  import { groups } from '@wix/groups';

  groups.onGroupDescriptionChanged((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupDescriptionChanged (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { groups } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        groups,
      },
  });

  wixClient.groups.onGroupDescriptionChanged((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
