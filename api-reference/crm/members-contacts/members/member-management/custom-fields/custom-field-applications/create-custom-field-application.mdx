---
title: "Create Custom Field Application"
description: "Creates a custom field application."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a custom field application.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/members/v1/custom-fields-applications
    ```

    ## Body Params

    <ParamField body="application" type="CustomFieldApplication" required>

      Custom field application details.

      See the [Custom Field Application Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-field-applications/custom-field-application-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="application" type="CustomFieldApplication">

      The created custom field application.

      See the [Custom Field Application Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-field-applications/custom-field-application-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        https://www.wixapis.com/members/v1/members/custom-fields-applications \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>'
        -d '{
              "application": {
                  "customFieldId": "4e4b1840-3833-4dbd-93d1-b2c9a33caa7d",
                  "applications": {
                    "items": [
                      {
                        "applicationType": "MEMBER",
                        "entityId": "ca1f9c6a-78f3-4b37-b977-951793410042"
                      }
                    ]
                  },
                  "revision": 1
              }
          }'
      ```

      ```json Response
      {
        "application": {
          "customFieldId": "4e4b1840-3833-4dbd-93d1-b2c9a33caa7d",
          "customFieldKey": "custom.favorite-animal",
          "applications": {
                    "items": [
                      {
                        "applicationType": "MEMBER",
                        "entityId": "ca1f9c6a-78f3-4b37-b977-951793410042"
                      }
                    ]
                  },
          "revision": "1",
          "exclusions": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a custom field application.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customFieldApplications } from '@wix/members';

    customFieldApplications.createCustomFieldApplication(application): Promise
    ```

    ## Method Parameters

    <ParamField body="application" type="CustomFieldApplication" required>

      Custom field application details.

      See the [Custom Field Application Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-field-applications/custom-field-application-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `CustomFieldApplication`.

    See the [Custom Field Application Object](/api-reference/crm/members-contacts/members/member-management/custom-fields/custom-field-applications/custom-field-application-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Custom Field Application Created | `wix.members.v1.custom_field_application_created` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-custom-field-application" id="ex-sdk-create-custom-field-application-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-custom-field-application" id="ex-sdk-create-custom-field-application-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-custom-field-application" id="ex-sdk-create-custom-field-application-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-custom-field-application-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-custom-field-application-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createCustomFieldApplication</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCustomFieldApplication (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCustomFieldApplication (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-custom-field-application-0"><span className="wix-picker-name">createCustomFieldApplication</span></label>
    <label htmlFor="ex-sdk-create-custom-field-application-1"><span className="wix-picker-name">createCustomFieldApplication (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-custom-field-application-2"><span className="wix-picker-name">createCustomFieldApplication (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createCustomFieldApplication
    import { customFieldApplications } from '@wix/members';

    async function createCustomFieldApplication(application) {
      const response = await customFieldApplications.createCustomFieldApplication(application);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCustomFieldApplication (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customFieldApplications } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customFieldApplications },
      // Include the auth strategy and host as relevant
    });

    async function createCustomFieldApplication(application) {
      const response = await myWixClient.customFieldApplications.createCustomFieldApplication(application);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCustomFieldApplication (with elevated permissions)
    import { customFieldApplications } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myCreateCustomFieldApplicationMethod(application) {
      const elevatedCreateCustomFieldApplication = auth.elevate(customFieldApplications.createCustomFieldApplication);
      const response = await elevatedCreateCustomFieldApplication(application);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
