---
title: "onPaymentLinkDeleted"
description: "Triggered when a payment link is deleted."
icon: "bolt"
---

Triggered when a payment link is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.paymentlinks.v1.payment_link_deleted` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Paylinks` |

## Examples

<CodeGroup>

  ```javascript onPaymentLinkDeleted
  import { paymentLinks } from '@wix/get-paid';

  paymentLinks.onPaymentLinkDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onPaymentLinkDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { paymentLinks } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        paymentLinks,
      },
  });

  wixClient.paymentLinks.onPaymentLinkDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
