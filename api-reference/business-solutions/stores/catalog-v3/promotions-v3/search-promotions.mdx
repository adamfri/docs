---
title: "Search Promotions"
description: "Searches promotions using WQL - Wix Query Language with support for free-text search and aggregations."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Searches promotions using [WQL - Wix Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) with support for free-text search and aggregations.

    Use this method for complex queries that include free-text search, aggregations,
    or when you need faceted search results. For simple filtering, use Query Promotions instead.

    Learn more about the differences between [Query and Search](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-search-query-and-list-methods) methods.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `tags.privateTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `tags.publicTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `enabled` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CATALOG_READ">Read v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/promotions/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search query expression, including filter, sorting, paging, free-text search, and aggregations.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use auto fuzzy search (allowing typos by a managed proximity algorithm).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="promotions" type="Promotion[]">

      List of promotions matching the search.

      See the [Promotion Object](/api-reference/business-solutions/stores/catalog-v3/promotions-v3/promotion-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request).

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches promotions by free text matching the promotion name.

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/stores/v3/promotions/search' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        -d '{
          "search": {
            "search": {
              "expression": "sale"
            },
            "cursorPaging": {
              "limit": 10
            }
          }
        }'
      ```

      ```json Response
      {
        "promotions": [
          {
            "id": "27ba9dfc-71b2-4172-a2c5-32ce56f4a021",
            "revision": "2",
            "createdDate": "2026-07-08T19:31:53.039Z",
            "updatedDate": "2026-07-08T19:32:18.737Z",
            "name": "Winter Sale",
            "media": {
              "id": "b7a91c_3d5f8e2a41c6497f8b219d0e6c53a7f2~mv2.png",
              "altText": "Winter sale banner",
              "image": {
                "id": "b7a91c_3d5f8e2a41c6497f8b219d0e6c53a7f2~mv2.png",
                "url": "https://static.wixstatic.com/media/b7a91c_3d5f8e2a41c6497f8b219d0e6c53a7f2~mv2.png",
                "height": 184,
                "width": 544,
                "altText": "Winter sale banner",
                "filename": "winter-sale-banner.png",
                "sizeInBytes": "7108"
              },
              "displayName": "winter-sale-banner.png",
              "mediaType": "IMAGE"
            },
            "destinationUrl": "https://www.example.com/winter-sale-2026",
            "categories": [
              {
                "id": "f6c2e4a1-8b3d-4e5f-9a1b-2c3d4e5f6a7b",
                "position": 2,
                "appliedToSubcategories": false
              }
            ],
            "enabled": true
          },
          {
            "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
            "revision": "2",
            "createdDate": "2026-07-08T19:30:59.898Z",
            "updatedDate": "2026-07-08T19:31:34.338Z",
            "name": "Summer Sale - Final Days",
            "media": {
              "id": "a27d24_8a3c5e8a724f4b8a9065b0c6f4a2e7~mv2.png",
              "altText": "Summer sale banner with 50% off",
              "image": {
                "id": "a27d24_8a3c5e8a724f4b8a9065b0c6f4a2e7~mv2.png",
                "url": "https://static.wixstatic.com/media/a27d24_8a3c5e8a724f4b8a9065b0c6f4a2e7~mv2.png",
                "height": 184,
                "width": 544,
                "altText": "Summer sale banner with 50% off",
                "filename": "summer-sale-banner.png",
                "sizeInBytes": "7108"
              },
              "displayName": "summer-sale-banner.png",
              "mediaType": "IMAGE"
            },
            "destinationUrl": "https://www.example.com/summer-sale-final",
            "categories": [
              {
                "id": "f6c2e4a1-8b3d-4e5f-9a1b-2c3d4e5f6a7b",
                "position": 1,
                "appliedToSubcategories": true
              }
            ],
            "enabled": true
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Searches promotions using [WQL - Wix Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) with support for free-text search and aggregations.

    Use this method for complex queries that include free-text search, aggregations,
    or when you need faceted search results. For simple filtering, use Query Promotions instead.

    Learn more about the differences between [Query and Search](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-search-query-and-list-methods) methods.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `tags.privateTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `tags.publicTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$hasAll`, `$hasSome` |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` |
    | `enabled` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$ne`, `$in`, `$exists` |
    | `name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.CATALOG_READ">Read v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { promotionsV3 } from '@wix/stores';

    promotionsV3.searchPromotions(search): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search query expression, including filter, sorting, paging, free-text search, and aggregations.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations are a way to explore large amounts of data by displaying summaries about various partitions of the data and later allowing to narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name of aggregation, should be unique, will appear in aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation, client must provide matching aggregation field below.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by, use dot notation to specify json path.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty - will search in all searchable fields. Use dot notation to specify json path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use auto fuzzy search (allowing typos by a managed proximity algorithm).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects all filters and aggregations returned values.
          You may override this behavior in a specific filter by providing
          timestamps including time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="promotions" type="Promotion[]">

      List of promotions matching the search.

      See the [Promotion Object](/api-reference/business-solutions/stores/catalog-v3/promotions-v3/promotion-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          key = aggregation name (as derived from search request).

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match provided kind as derived from search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built - one per unique value.
              - `RANGE` — An aggregation, where user can define set of ranges - each representing a bucket.
              - `SCALAR` — A single-value metric aggregation. For example, min, max, sum, avg.
              - `DATE_HISTOGRAM` — An aggregation, where result buckets are dynamically built - one per time interval (hour, day, week, etc.)
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-promotions" id="ex-sdk-search-promotions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-promotions" id="ex-sdk-search-promotions-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-promotions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-promotions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search promotions with a filter</span><span className="wix-picker-desc">Searches promotions using WQL filtering and cursor paging. Also supports free-text search and aggregations</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchPromotions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-promotions-0"><span className="wix-picker-name">Search promotions with a filter</span><span className="wix-picker-desc">Searches promotions using WQL filtering and cursor paging. Also supports free-text search and aggregations</span></label>
    <label htmlFor="ex-sdk-search-promotions-1"><span className="wix-picker-name">searchPromotions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Search promotions with a filter
    import { promotionsV3 } from "@wix/stores";

    const search = {
      filter: { enabled: true },
      cursorPaging: { limit: 10 },
    };

    async function searchPromotions() {
      const response = await promotionsV3.searchPromotions(search);
      return response;
    }

    /* Promise resolves to (response.promotions):
     * [
     *   {
     *     "_id": "25fc9c97-1ecf-47a1-9350-5766867d307e",
     *     "revision": "3",
     *     "name": "Summer Sale Banner",
     *     "enabled": true,
     *     "tags": { "privateTags": { "tagIds": ["ssn24"] } }
     *   }
     * ]
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchPromotions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { promotionsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { promotionsV3 },
      // Include the auth strategy and host as relevant
    });

    async function searchPromotions(search) {
      const response = await myWixClient.promotionsV3.searchPromotions(search);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
