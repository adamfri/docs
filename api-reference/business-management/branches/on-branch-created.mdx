---
title: "onBranchCreated"
description: "Triggered when a branch is created."
icon: "bolt"
---

Triggered when a branch is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.editor.branches.v1.branch_created` |
| Package | `@wix/editor-branches` |
| Permissions | `Manage SEO Settings`, `Manage Site Branches` |

## Examples

<CodeGroup>

  ```javascript onBranchCreated
  import { branches } from '@wix/editor-branches';

  branches.onBranchCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBranchCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { branches } from '@wix/editor-branches';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        branches,
      },
  });

  wixClient.branches.onBranchCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
