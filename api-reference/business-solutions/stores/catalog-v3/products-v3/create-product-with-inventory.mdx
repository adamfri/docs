---
title: "Create Product With Inventory"
description: "Creates a new product with its inventory in a single call."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new product with its inventory in a single call.

    Pass `variantsInfo.variants.inventoryItem` to create inventory items for each variant in the default location. Each inventory item can track stock using either `quantity` (for exact stock counts) or `inStock` (for simple availability). If `inventoryItem` isn't provided, no inventory items are created.

    You can also add a ribbon, brand, info sections, options, and modifiers in the same request.

    Learn more about [inventory management](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-inventory-management) including tracking methods, preorder settings, and multi-location inventory.

    <Note>

    When you create a product with inline options or modifiers (without referencing existing customization IDs), new standalone [Customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities are automatically created. These can be reused across other products.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.PRODUCT_WRITE">Product write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/products-with-inventory
    ```

    ## Body Params

    <ParamField body="product" type="ProductWithInventory" required>

      Product to create with inventory.

      At least one variant must be provided and each variant must have relevant item in `choices` field for every item in `options`.
      If `options` is empty one default variant must be provided with empty `choices` list.

      <Expandable title="product properties">

        <ParamField body="id" type="string">

          Product ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer">

          Revision number, which increments by 1 each time the product is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the product.

          Ignored when creating a product with inventory.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string" required>

          Product name. Translatable.

        </ParamField>

        <ParamField body="slug" type="string">

          Product slug.

          If not provided, the slug is autogenerated based on the product name.

        </ParamField>

        <ParamField body="plainDescription" type="string">

          Product description in HTML.

          + When provided on create/update, this string must be valid HTML. It's then converted to rich content.
          + `plainDescription` is ignored when a value is also passed to the `description` field.
          > **Note:** Returned only when you pass `"PLAIN_DESCRIPTION"` to the `fields` array in Products API requests.

        </ParamField>

        <ParamField body="visible" type="boolean">

          Whether the product is visible to site visitors.

          Default: `true`

        </ParamField>

        <ParamField body="visibleInPos" type="boolean">

          Whether the product is visible in POS (point of sale).

          Default: `true`
          > **Note:** Always `false` for `productType: DIGITAL`.

        </ParamField>

        <ParamField body="media" type="Media">

          Product media items.

          <Expandable title="media properties">

            <ParamField body="main" type="ProductMedia">

              Main media (image, video, etc.) associated with this product.
              Automatically set to the first item in the media list.

              **Read-only.**

              _Nested structure omitted for brevity — see the `ProductMedia` object._

            </ParamField>

            <ParamField body="itemsInfo" type="MediaItemsInfo">

              All media items.
              > **Note:** Returned only when you pass `"MEDIA_ITEMS_INFO"` to the `fields` array in Products API requests.

              _Nested structure omitted for brevity — see the `MediaItemsInfo` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="seoData" type="SeoSchema">

          Product SEO data.

          <Expandable title="seoData properties">

            <ParamField body="tags" type="Tag[]">

              SEO tag information.

              _Nested structure omitted for brevity — see the `Tag[]` object._

            </ParamField>

            <ParamField body="settings" type="Settings">

              SEO general settings.

              _Nested structure omitted for brevity — see the `Settings` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="taxGroupId" type="string">

          [Tax group ID](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/tax-groups/introduction). Used to apply specific tax rates to products.

        </ParamField>

        <ParamField body="options" type="ConnectedOption[]">

          Product options, such as "Size" or "Color". Options define the ways a product can vary.

          When you provide options, you must also provide the corresponding variants. Each variant must have exactly one choice for each option. For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          Options are stored as reusable [customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities. Pass an existing customization's ID to reuse it, or define options inline and new customization entities are created automatically.

          Constraints: `maxItems 6`.

          <Expandable title="options properties">

            <ParamField body="id" type="string">

              ID of a customization with `customizationType: PRODUCT_OPTION`.

            </ParamField>

            <ParamField body="name" type="string">

              Option name.

            </ParamField>

            <ParamField body="optionRenderType" type="string">

              Option render type.

              **Supported values:**

              - `TEXT_CHOICES` — Text choices.
              - `SWATCH_CHOICES` — Swatch choices.

            </ParamField>

            <ParamField body="key" type="string">

              A read-only identifier generated from the option name.

              Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="modifiers" type="ConnectedModifier[]">

          Product modifiers. Collect additional information from customers without creating variants.

          Unlike options, modifiers don't affect inventory or create additional variants. Use them for things like gift messages or engraving text. For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          Modifiers are stored as reusable [customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities. Pass an existing customization's ID to reuse it, or define modifiers inline and new customization entities are created automatically.

          Constraints: `maxItems 10`.

          <Expandable title="modifiers properties">

            <ParamField body="id" type="string">

              ID of a customization with `customizationType: MODIFIER`.

            </ParamField>

            <ParamField body="name" type="string">

              Modifier title.

            </ParamField>

            <ParamField body="modifierRenderType" type="string">

              Modifier render type.

              **Supported values:**

              - `FREE_TEXT` — Free text.
              - `TEXT_CHOICES` — Text choices.
              - `SWATCH_CHOICES` — Swatch choices.

            </ParamField>

            <ParamField body="mandatory" type="boolean">

              Whether customer input is required for this modifier.

            </ParamField>

            <ParamField body="key" type="string">

              A read-only identifier generated from the modifier name.

              Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="brand" type="Brand">

          Product brand.

          + Pass `brand.name` to add a new brand while creating a product.
          + Pass an existing brand's `id` to assign that brand to the product.

          <Expandable title="brand properties">

            <ParamField body="id" type="string">

              Brand ID.

            </ParamField>

            <ParamField body="name" type="string">

              Brand name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="infoSections" type="InfoSection[]">

          Product info section.

          + Pass `infoSection.uniqueName`, `infoSection.title`, and `infoSection.description` to add a new info section while creating a product.
          + Pass an existing info section's `id` or `uniqueName` to assign that info section to the product.

          Constraints: `maxItems 20`.

          <Expandable title="infoSections properties">

            <ParamField body="id" type="string">

              Info section ID.

            </ParamField>

            <ParamField body="uniqueName" type="string">

              Info section unique name.
              > **Note:** Returned only when you pass `"INFO_SECTION"` to the `fields` array in Products API requests.

            </ParamField>

            <ParamField body="title" type="string">

              Info section title.
              > **Note:** Returned only when you pass `"INFO_SECTION"` to the `fields` array in Products API requests.

              **Read-only.**

            </ParamField>

            <ParamField body="description" type="RichContent">

              Info section description using rich content.
              > **Note:** Returned only when you pass `"INFO_SECTION_DESCRIPTION"` to the `fields` array in Products API requests.

              &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="indent.emoji.divider.codeBlock.file.gallery.giphy.image.table.link.textHighlight.textColor" exampleid="7dc9240e-d548-417a-abcf-0291b68b4303">
              &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
              &lt;/widget>

              **Read-only.**

              _Nested structure omitted for brevity — see the `RichContent` object._

            </ParamField>

            <ParamField body="plainDescription" type="string">

              Info section description in HTML.

              When provided on create/update, this string must be a valid HTML. It will then be converted to rich content.
              `plainDescription` is ignored when value is also passed to the `description` field.
              > **Note:** Returned only when you pass `"INFO_SECTION_PLAIN_DESCRIPTION"` to the `fields` array in Products API requests.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="ribbon" type="Ribbon">

          Primary product ribbon displayed on the product. Use `additionalRibbons` to assign further ribbons beyond the primary one.

          + Pass `ribbon.name` to add a new ribbon while creating a product.
          + Pass an existing ribbon's `id` or `name` to assign that ribbon to the product.

          <Expandable title="ribbon properties">

            <ParamField body="id" type="string">

              Ribbon ID.

            </ParamField>

            <ParamField body="name" type="string">

              Ribbon name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="additionalRibbons" type="Ribbon[]">

          Additional [ribbons](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/introduction) displayed on the product, beyond the primary `ribbon`.

          Use this field to highlight multiple product attributes at once, for example combining a "New" ribbon with a "Sale" ribbon. Both the primary and additional ribbons are rendered on product cards and on the product page. Ribbons appear in the same order as this list.

          Each entry follows the same assignment rules as `ribbon`:
          + Pass `additionalRibbons.name` to add a new ribbon while creating or updating the product.
          + Pass an existing ribbon's `id` or `name` to assign that ribbon to the product.

          A ribbon can be assigned to a product at most once: the same ribbon can't appear more than once in `additionalRibbons`, and a ribbon set as `ribbon` can't also appear in `additionalRibbons`.

          Supports up to 4 additional ribbons per product.

          **Developer Preview.** Constraints: `maxItems 4`.

          <Expandable title="additionalRibbons properties">

            <ParamField body="id" type="string">

              Ribbon ID.

            </ParamField>

            <ParamField body="name" type="string">

              Ribbon name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="mainCategoryId" type="string">

          The ID of the product's primary direct category, which defines the product's breadcrumbs path. For example, if the product's main category is "T-Shirts" (which is a subcategory of "Clothing"), the breadcrumbs path will be "Clothing > T-Shirts".

        </ParamField>

        <ParamField body="productType" type="string" required>

          Product type: `PHYSICAL` for tangible goods that require shipping, or `DIGITAL` for downloadable content.

          When passing `productType: PHYSICAL`, you must also pass `physicalProperties`. When passing `productType: DIGITAL`, you can optionally pass `digitalProperties` in each variant.

          Constraints: `immutable`.

          **Supported values:**

          - `PHYSICAL` — Physical product.
          - `DIGITAL` — Digital product.

        </ParamField>

        <ParamField body="handle" type="string">

          A unique human-friendly identifier for the product.

          Unlike the auto-generated `id`, the handle can be set when creating a product. This is useful when re-importing products from other systems, as it provides a stable identifier across platforms. If not provided during creation, one is automatically generated. Can't be changed after creation.

          **Read-only.**

        </ParamField>

        <ParamField body="variantsInfo" type="VariantsInfo" required>

          Product variants. Each variant represents a specific purchasable version of a product defined by option choices.

          Each variant must reference all product options via its `choices` array, using `optionChoiceNames` in requests.
          Variants aren't auto-generated. You must explicitly provide each variant when creating products with options.

          For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          <Expandable title="variantsInfo properties">

            <ParamField body="variants" type="VariantWithInventory[]">

              List of related variants.
              Partial update of variants is not supported so on update you must pass all data for all of them.
              If you want to update existing variant you must provide `id`, otherwise new variant with different id will be created which might break some integrations that rely on variant ids.

              Constraints: `minItems 1`, `maxItems 1000`.

              _Nested structure omitted for brevity — see the `VariantWithInventory[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="subscriptionDetails" type="SubscriptionDetails">

          Product subscriptions.

          Subscription discounts are defined at the product level but apply to each variant's `actualPrice`. This means the final subscription price varies per variant based on the variant's base price.

          <Expandable title="subscriptionDetails properties">

            <ParamField body="subscriptions" type="Subscription[]" required>

              Subscriptions.

              Constraints: `minItems 1`, `maxItems 6`.

              _Nested structure omitted for brevity — see the `Subscription[]` object._

            </ParamField>

            <ParamField body="allowOneTimePurchases" type="boolean">

              Whether to allow one-time purchases in addition to subscription-based purchases.

              Default: `false`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="purchaseEligibility" type="PurchaseEligibility">

          Purchase eligibility settings for the product.

          **Developer Preview.**

          <Expandable title="purchaseEligibility properties">

            <ParamField body="userCriteria" type="string">

              **Supported values:**

              - `MEMBERS_ONLY` — Product is available only to members.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom extended fields for the product object.

          [Extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions) must be configured in the [app dashboard](https://dev.wix.com/dc3/my-apps/) before they can be accessed with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tags

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="publicTags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="typedProperties" type="typedProperties">

          typedProperties

          <Expandable title="typedProperties properties">

            <ParamField body="physicalProperties" type="PhysicalProperties">

              Physical properties.

              Required when `productType: PHYSICAL`.

              _Nested structure omitted for brevity — see the `PhysicalProperties` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return inventory entities in the response.

      Default: `false`

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Constraints: `maxItems 100`.

      **Supported values:**

      - `URL` — The product's public storefront URL, including both relative path and full URL with base domain.
      - `CURRENCY` — Currency information including the currency code and formatted price amounts for storefront display.
      - `INFO_SECTION` — Returns basic info section metadata including IDs, unique names, and titles. Use with `INFO_SECTION_DESCRIPTION` or `INFO_SECTION_PLAIN_DESCRIPTION` to include content.
      - `MERCHANT_DATA` — Returns merchant-related financial data including cost ranges and variant revenue details like cost, profit, and profit margin.
      Requires the `SCOPE.STORES.PRODUCT_READ_ADMIN` permission scope.
      - `PLAIN_DESCRIPTION` — Provides product description as plain HTML text.
      - `INFO_SECTION_PLAIN_DESCRIPTION` — Returns info section descriptions as HTML content. Use with `INFO_SECTION` for complete metadata.
      - `SUBSCRIPTION_PRICES_INFO` — Details subscription pricing options, empty for non-subscription products.
      - `BREADCRUMBS_INFO` — Returns the category breadcrumb navigation path from root categories down to the product's main category, including category IDs, names, and slugs.
      - `WEIGHT_MEASUREMENT_UNIT_INFO` — Returns the weight measurement unit (KG, LB) for the product.
      - `VARIANT_OPTION_CHOICE_NAMES` — Enriches variant choices with human-readable option and choice names alongside render types.
      - `MEDIA_ITEMS_INFO` — Returns all media items associated with the product, including detailed metadata (URLs, alt-text, dimensions, filenames, file sizes).
      - `DESCRIPTION` — Returns the product description as rich content with complete node structure, metadata, and styling information.
      - `DIRECT_CATEGORIES_INFO` — Provides the IDs and indexes of categories the product is directly assigned to.
      - `ALL_CATEGORIES_INFO` — Provides IDs and indexes for all categories including inherited parent categories.
      - `INFO_SECTION_DESCRIPTION` — Returns info section descriptions as rich content with complete node structure, metadata, and styling information. Use with `INFO_SECTION` for complete metadata.
      - `THUMBNAIL` — Returns an optimized thumbnail image with standard dimensions, suitable for listings or previews.
      - `PRODUCT_CHOICES_MEDIA_REFERENCES` — Returns media references associated with product option and modifier choices instead of the deprecated `linked_media` field.

      When requested, choice objects will populate `media_references` with lightweight `MediaReference` objects containing only media IDs,
      while the `linked_media` field will be returned empty to avoid duplication and reduce response payload size.

      To get full media metadata (dimensions, alt text, filenames, etc.), request `MEDIA_ITEMS_INFO` on the product and correlate by media ID.

      > **Note:** The `media_references` field is available on `ConnectedOptionChoice` and `ConnectedModifierChoice` objects.
      - `PRODUCT_CHOICES_DISPLAY_IMAGE` — Returns display images for product option and modifier choices.
      When requested, choice objects will populate `display_image` with a lightweight image containing only ID, URL, and alt text.
      - `DISCOUNT_INFO` — Returns automatic discount info for product get/query/search responses and populates product-level `discountInfo.discountRuleNames`.

      To receive discounted variant prices in get/query/search responses, request this together with `MIN_PRICE_VARIANT` (returned in `variantSummary.minPriceVariant.price.priceAfterDiscount`).
      Formatted money amounts are returned only when `CURRENCY` is also requested.

    </ParamField>

    ## Response Object

    <ResponseField name="product" type="Product">

      Created product.

      See the [Product Object](/api-reference/business-solutions/stores/catalog-v3/products-v3/product-object) for this object's fields.

    </ResponseField>

    <ResponseField name="inventoryResults" type="BulkInventoryItemResults">

      Inventories created by bulk action.

      <Expandable title="inventoryResults properties">

        <ResponseField name="results" type="BulkInventoryItemResult[]">

          Inventories modified by bulk action.

          Constraints: `minItems 0`, `maxItems 1000`.

          <Expandable title="results properties">

            <ResponseField name="itemMetadata" type="ItemMetadata">

              Bulk action metadata for inventory item.

              _Nested structure omitted for brevity — see the `ItemMetadata` object._

            </ResponseField>

            <ResponseField name="item" type="InventoryItem">

              Full inventory item entity.

              Returned only if `returnEntity: true` is passed in the request.

              _Nested structure omitted for brevity — see the `InventoryItem` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

          Bulk action metadata.

          <Expandable title="bulkActionMetadata properties">

            <ResponseField name="totalSuccesses" type="integer">

              Number of items that were successfully processed.

            </ResponseField>

            <ResponseField name="totalFailures" type="integer">

              Number of items that couldn't be processed.

            </ResponseField>

            <ResponseField name="undetailedFailures" type="integer">

              Number of failures without details because detailed failure threshold was exceeded.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="error" type="ApplicationError">

          Error details in case of failed action.

          <Expandable title="error properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `RIBBON_NOT_FOUND` | Couldn't find the ribbon. |
    | `500` | `INTERNAL` | `RIBBON_CREATION_FAILED` | Ribbon creation failed. |
    | `400` | `INVALID_ARGUMENT` | `INFO_SECTION_CREATION_FAILED` | Info section creation failed. |
    | `404` | `NOT_FOUND` | `CUSTOMIZATION_NOT_EXIST` | Couldn't find the customization. |
    | `500` | `INTERNAL` | `CUSTOMIZATIONS_CREATION_FAILED` | Customization creation failed. |
    | `500` | `INTERNAL` | `ADD_CUSTOMIZATION_CHOICES_FAILED` | Failed to add customization choices. |
    | `404` | `NOT_FOUND` | `CHOICE_NOT_EXIST` | Couldn't find the choice. |
    | `400` | `INVALID_ARGUMENT` | `PRODUCT_OPTION_NAME_AND_TYPE_REQUIRED` | Option is missing `name` or `type`. Options without an `id` must include both. |
    | `400` | `INVALID_ARGUMENT` | `PRODUCT_OPTION_CHOICE_NAME_AND_TYPE_REQUIRED` | Option choice is missing `name` or `type`. Choices without an `id` must include both. |
    | `400` | `INVALID_ARGUMENT` | `MODIFIER_NAME_AND_TYPE_REQUIRED` | Modifier is missing `name` or `type`. Modifiers without an `id` must include both. |
    | `400` | `INVALID_ARGUMENT` | `MODIFIER_CHOICE_NAME_AND_TYPE_REQUIRED` | Modifier choice is missing `name` or `type`. Choices without an `id` must include both. |
    | `428` | `FAILED_PRECONDITION` | `COMPARE_AT_PRICE_LOWER_THAN_ACTUAL_PRICE` | The `compareAtPrice` is lower than the `actualPrice`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_ADDITIONAL` | Same ribbon appears more than once in `additionalRibbons`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_PRIMARY_AND_ADDITIONAL` | Same ribbon used as both primary ribbon and additional ribbon. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SLUG_FORMAT` | The `slug` provided is not a valid URL slug. |
    | `400` | `INVALID_ARGUMENT` | `REQUESTED_QUANTITY_MUST_BE_NON_NEGATIVE` | Inventory quantity is negative. The quantity must be non-negative. |
    | `400` | `INVALID_ARGUMENT` | `BRAND_MISSING_ID` | Brand is missing an `id`. |
    | `400` | `INVALID_ARGUMENT` | `PRICE_PER_UNIT_NOT_ALIGNED` | Price per unit isn't aligned between product and variants. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Product Created | `wix.stores.catalog.v3.product_created` | Triggered when a product is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-create-product-with-inventory" id="ex-rest-create-product-with-inventory-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-create-product-with-inventory" id="ex-rest-create-product-with-inventory-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-create-product-with-inventory-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-create-product-with-inventory-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a product where the variant is assigned a value for weight.</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create minimal possible product</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-create-product-with-inventory-0"><span className="wix-picker-name">Create a product where the variant is assigned a value for weight.</span></label>
    <label htmlFor="ex-rest-create-product-with-inventory-1"><span className="wix-picker-name">Create minimal possible product</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/stores/v3/products-with-inventory' \
      	-H 'Authorization: <AUTH>' \
      	-H 'Content-Type: application/json' \
      	-d '{
      		"product": {
      			"name": "Adjustable Reading Lamp",
      			"variantsInfo": {
      				"variants": [
      					{
      						"price": {
      							"actualPrice": {
      								"amount": "20"
      							}
      						},
      						"inventoryItem": {
      							"inStock": true
      						},
      						"physicalProperties": {
      							"weight": 60
      						}
      					}
      				]
      			},
      			"productType": "PHYSICAL",
      			"physicalProperties": {}
      		}
      	}'
      ```

      ```json Response
      {
        "product": {
          "id": "c7f39676-734b-4145-a307-af6577f4cfa2",
          "revision": "1",
          "createdDate": "2025-04-15T12:00:12.219Z",
          "updatedDate": "2025-04-15T12:00:12.219Z",
          "name": "Adjustable Reading Lamp",
          "slug": "adjustable-reading-lamp",
          "visible": true,
          "visibleInPos": true,
          "media": {
            "main": null,
            "itemsInfo": null
          },
          "options": [],
          "modifiers": [],
          "infoSections": [],
          "inventory": {
            "preorderAvailability": "NO_VARIANTS",
            "availabilityStatus": "IN_STOCK",
            "preorderStatus": "DISABLED"
          },
          "productType": "PHYSICAL",
          "physicalProperties": {
            "shippingWeightRange": {
              "minValue": 60,
              "maxValue": 60
            }
          },
          "handle": "Product_92467bc7-e4c4-4c16-b229-da3f5b00aea2",
          "actualPriceRange": {
            "minValue": {
              "amount": "20",
              "formattedAmount": null
            },
            "maxValue": {
              "amount": "20",
              "formattedAmount": null
            }
          },
          "compareAtPriceRange": {
            "minValue": {
              "amount": "0",
              "formattedAmount": null
            },
            "maxValue": {
              "amount": "0",
              "formattedAmount": null
            }
          },
          "variantsInfo": {
            "variants": [
              {
                "id": "6e1bc76c-959d-41f3-bb5d-522c4d0b6f11",
                "visible": true,
                "choices": [],
                "price": {
                  "actualPrice": {
                    "amount": "20",
                    "formattedAmount": null
                  }
                },
                "inventoryStatus": {
                  "inStock": false,
                  "preorderEnabled": false
                },
                "physicalProperties": {
                  "weight": 60
                }
              }
            ]
          },
          "variantSummary": {
            "variantCount": 1
          }
        },
        "inventoryResults": {
          "results": [
            {
              "itemMetadata": {
                "id": "ea023f89-9d2f-4812-b465-d91117ea1623",
                "originalIndex": 0,
                "success": true,
                "error": null
              },
              "item": {
                "id": "ea023f89-9d2f-4812-b465-d91117ea1623",
                "revision": "1",
                "createdDate": "2025-04-15T12:00:13.473Z",
                "updatedDate": "2025-04-15T12:00:13.473Z",
                "variantId": "6e1bc76c-959d-41f3-bb5d-522c4d0b6f11",
                "locationId": "68892ef3-3715-4ce3-8a6e-0561350f8860",
                "productId": "c7f39676-734b-4145-a307-af6577f4cfa2",
                "inStock": true,
                "trackQuantity": false,
                "availabilityStatus": "IN_STOCK",
                "preorderInfo": {
                  "enabled": false,
                  "counter": null,
                  "limit": null,
                  "message": null,
                  "quantity": null
                },
                "extendedFields": null
              }
            }
          ],
          "bulkActionMetadata": {
            "totalSuccesses": 1,
            "totalFailures": 0,
            "undetailedFailures": 0
          },
          "error": null
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/stores/v3/products-with-inventory' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
            "product": {
              "name": "Shopping bag",
              "productType": "PHYSICAL",
              "variantsInfo": {
                "variants": [
                  {
                    "price": {
                      "actualPrice": {
                        "amount": "0.5"
                      }
                    },
                    "inventoryItem": {
                      "inStock": false
                    }
                  }
                ]
              },
              "physicalProperties": {}
            }
          }'
      ```

      ```json Response
      {
        "product": {
          "id": "789e6969-6eb4-4799-aacb-e207037564c4",
          "revision": "1",
          "createdDate": "2025-01-02T07:36:01.066Z",
          "updatedDate": "2025-01-02T07:36:01.066Z",
          "name": "Shopping bag",
          "slug": "shopping-bag-1",
          "visible": true,
          "visibleInPos": true,
          "media": {
            "main": null,
            "itemsInfo": null
          },
          "options": [],
          "modifiers": [],
          "infoSections": [],
          "directCategoriesInfo": null,
          "allCategoriesInfo": null,
          "mainCategoryId": null,
          "inventory": {
            "preorderAvailability": "NO_VARIANTS",
            "availabilityStatus": "OUT_OF_STOCK",
            "preorderStatus": "DISABLED"
          },
          "productType": "PHYSICAL",
          "physicalProperties": {
            "pricePerUnit": null,
            "fulfillerId": null,
            "shippingWeightRange": null,
            "pricePerUnitRange": null,
            "weightMeasurementUnitInfo": null,
            "deliveryProfileId": null
          },
          "handle": "Product_f3e89a8b-5865-47de-86fa-cd923c2586e5",
          "actualPriceRange": {
            "minValue": {
              "amount": "0.5",
              "formattedAmount": null
            },
            "maxValue": {
              "amount": "0.5",
              "formattedAmount": null
            }
          },
          "compareAtPriceRange": {
            "minValue": {
              "amount": "0.5",
              "formattedAmount": null
            },
            "maxValue": {
              "amount": "0.5",
              "formattedAmount": null
            }
          },
          "variantsInfo": {
            "variants": [
              {
                "id": "a3c6d6b2-4265-4e9a-8406-cafd4a61fb06",
                "visible": true,
                "choices": [],
                "price": {
                  "actualPrice": {
                    "amount": "0.5",
                    "formattedAmount": null
                  },
                  "compareAtPrice": null
                },
                "inventoryStatus": {
                  "inStock": false,
                  "preorderEnabled": false
                }
              }
            ]
          },
          "subscriptionDetails": null,
          "extendedFields": null,
          "variantSummary": {
            "variantCount": 1
          }
        },
        "inventoryResults": {
          "results": [
            {
              "itemMetadata": {
                "id": "2937f8d3-3439-4e46-83ab-7a83e4e37da1",
                "originalIndex": 0,
                "success": true,
                "error": null
              },
              "item": null
            }
          ],
          "bulkActionMetadata": {
            "totalSuccesses": 1,
            "totalFailures": 0,
            "undetailedFailures": 0
          },
          "error": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a new product with its inventory in a single call.

    Pass `variantsInfo.variants.inventoryItem` to create inventory items for each variant in the default location. Each inventory item can track stock using either `quantity` (for exact stock counts) or `inStock` (for simple availability). If `inventoryItem` isn't provided, no inventory items are created.

    You can also add a ribbon, brand, info sections, options, and modifiers in the same request.

    Learn more about [inventory management](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-inventory-management) including tracking methods, preorder settings, and multi-location inventory.

    <Note>

    When you create a product with inline options or modifiers (without referencing existing customization IDs), new standalone [Customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities are automatically created. These can be reused across other products.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.PRODUCT_WRITE">Product write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { productsV3 } from '@wix/stores';

    productsV3.createProductWithInventory(product, options): Promise<CreateProductWithInventoryResponse>
    ```

    ## Method Parameters

    <ParamField body="product" type="ProductWithInventory" required>

      Product to create with inventory.

      At least one variant must be provided and each variant must have relevant item in `choices` field for every item in `options`.
      If `options` is empty one default variant must be provided with empty `choices` list.

      <Expandable title="product properties">

        <ParamField body="_id" type="string">

          Product ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer">

          Revision number, which increments by 1 each time the product is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the product.

          Ignored when creating a product with inventory.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string" required>

          Product name. Translatable.

        </ParamField>

        <ParamField body="slug" type="string">

          Product slug.

          If not provided, the slug is autogenerated based on the product name.

        </ParamField>

        <ParamField body="plainDescription" type="string">

          Product description in HTML.

          + When provided on create/update, this string must be valid HTML. It's then converted to rich content.
          + `plainDescription` is ignored when a value is also passed to the `description` field.
          > **Note:** Returned only when you pass `"PLAIN_DESCRIPTION"` to the `fields` array in Products API requests.

        </ParamField>

        <ParamField body="visible" type="boolean">

          Whether the product is visible to site visitors.

          Default: `true`

        </ParamField>

        <ParamField body="visibleInPos" type="boolean">

          Whether the product is visible in POS (point of sale).

          Default: `true`
          > **Note:** Always `false` for `productType: DIGITAL`.

        </ParamField>

        <ParamField body="media" type="Media">

          Product media items.

          <Expandable title="media properties">

            <ParamField body="main" type="ProductMedia">

              Main media (image, video, etc.) associated with this product.
              Automatically set to the first item in the media list.

              **Read-only.**

              _Nested structure omitted for brevity — see the `ProductMedia` object._

            </ParamField>

            <ParamField body="itemsInfo" type="MediaItemsInfo">

              All media items.
              > **Note:** Returned only when you pass `"MEDIA_ITEMS_INFO"` to the `fields` array in Products API requests.

              _Nested structure omitted for brevity — see the `MediaItemsInfo` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="seoData" type="SeoSchema">

          Product SEO data.

          <Expandable title="seoData properties">

            <ParamField body="tags" type="Tag[]">

              SEO tag information.

              _Nested structure omitted for brevity — see the `Tag[]` object._

            </ParamField>

            <ParamField body="settings" type="Settings">

              SEO general settings.

              _Nested structure omitted for brevity — see the `Settings` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="taxGroupId" type="string">

          [Tax group ID](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/tax-groups/introduction). Used to apply specific tax rates to products.

        </ParamField>

        <ParamField body="options" type="ConnectedOption[]">

          Product options, such as "Size" or "Color". Options define the ways a product can vary.

          When you provide options, you must also provide the corresponding variants. Each variant must have exactly one choice for each option. For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          Options are stored as reusable [customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities. Pass an existing customization's ID to reuse it, or define options inline and new customization entities are created automatically.

          Constraints: `maxItems 6`.

          <Expandable title="options properties">

            <ParamField body="_id" type="string">

              ID of a customization with `customizationType: PRODUCT_OPTION`.

            </ParamField>

            <ParamField body="name" type="string">

              Option name.

            </ParamField>

            <ParamField body="optionRenderType" type="string">

              Option render type.

              **Supported values:**

              - `TEXT_CHOICES` — Text choices.
              - `SWATCH_CHOICES` — Swatch choices.

            </ParamField>

            <ParamField body="key" type="string">

              A read-only identifier generated from the option name.

              Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="modifiers" type="ConnectedModifier[]">

          Product modifiers. Collect additional information from customers without creating variants.

          Unlike options, modifiers don't affect inventory or create additional variants. Use them for things like gift messages or engraving text. For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          Modifiers are stored as reusable [customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities. Pass an existing customization's ID to reuse it, or define modifiers inline and new customization entities are created automatically.

          Constraints: `maxItems 10`.

          <Expandable title="modifiers properties">

            <ParamField body="_id" type="string">

              ID of a customization with `customizationType: MODIFIER`.

            </ParamField>

            <ParamField body="name" type="string">

              Modifier title.

            </ParamField>

            <ParamField body="modifierRenderType" type="string">

              Modifier render type.

              **Supported values:**

              - `FREE_TEXT` — Free text.
              - `TEXT_CHOICES` — Text choices.
              - `SWATCH_CHOICES` — Swatch choices.

            </ParamField>

            <ParamField body="mandatory" type="boolean">

              Whether customer input is required for this modifier.

            </ParamField>

            <ParamField body="key" type="string">

              A read-only identifier generated from the modifier name.

              Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="brand" type="Brand">

          Product brand.

          + Pass `brand.name` to add a new brand while creating a product.
          + Pass an existing brand's `id` to assign that brand to the product.

          <Expandable title="brand properties">

            <ParamField body="_id" type="string">

              Brand ID.

            </ParamField>

            <ParamField body="name" type="string">

              Brand name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="infoSections" type="InfoSection[]">

          Product info section.

          + Pass `infoSection.uniqueName`, `infoSection.title`, and `infoSection.description` to add a new info section while creating a product.
          + Pass an existing info section's `id` or `uniqueName` to assign that info section to the product.

          Constraints: `maxItems 20`.

          <Expandable title="infoSections properties">

            <ParamField body="_id" type="string">

              Info section ID.

            </ParamField>

            <ParamField body="uniqueName" type="string">

              Info section unique name.
              > **Note:** Returned only when you pass `"INFO_SECTION"` to the `fields` array in Products API requests.

            </ParamField>

            <ParamField body="title" type="string">

              Info section title.
              > **Note:** Returned only when you pass `"INFO_SECTION"` to the `fields` array in Products API requests.

              **Read-only.**

            </ParamField>

            <ParamField body="description" type="RichContent">

              Info section description using rich content.
              > **Note:** Returned only when you pass `"INFO_SECTION_DESCRIPTION"` to the `fields` array in Products API requests.

              &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="indent.emoji.divider.codeBlock.file.gallery.giphy.image.table.link.textHighlight.textColor" exampleid="7dc9240e-d548-417a-abcf-0291b68b4303">
              &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
              &lt;/widget>

              **Read-only.**

              _Nested structure omitted for brevity — see the `RichContent` object._

            </ParamField>

            <ParamField body="plainDescription" type="string">

              Info section description in HTML.

              When provided on create/update, this string must be a valid HTML. It will then be converted to rich content.
              `plainDescription` is ignored when value is also passed to the `description` field.
              > **Note:** Returned only when you pass `"INFO_SECTION_PLAIN_DESCRIPTION"` to the `fields` array in Products API requests.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="ribbon" type="Ribbon">

          Primary product ribbon displayed on the product. Use `additionalRibbons` to assign further ribbons beyond the primary one.

          + Pass `ribbon.name` to add a new ribbon while creating a product.
          + Pass an existing ribbon's `id` or `name` to assign that ribbon to the product.

          <Expandable title="ribbon properties">

            <ParamField body="_id" type="string">

              Ribbon ID.

            </ParamField>

            <ParamField body="name" type="string">

              Ribbon name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="additionalRibbons" type="Ribbon[]">

          Additional [ribbons](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/introduction) displayed on the product, beyond the primary `ribbon`.

          Use this field to highlight multiple product attributes at once, for example combining a "New" ribbon with a "Sale" ribbon. Both the primary and additional ribbons are rendered on product cards and on the product page. Ribbons appear in the same order as this list.

          Each entry follows the same assignment rules as `ribbon`:
          + Pass `additionalRibbons.name` to add a new ribbon while creating or updating the product.
          + Pass an existing ribbon's `id` or `name` to assign that ribbon to the product.

          A ribbon can be assigned to a product at most once: the same ribbon can't appear more than once in `additionalRibbons`, and a ribbon set as `ribbon` can't also appear in `additionalRibbons`.

          Supports up to 4 additional ribbons per product.

          **Developer Preview.** Constraints: `maxItems 4`.

          <Expandable title="additionalRibbons properties">

            <ParamField body="_id" type="string">

              Ribbon ID.

            </ParamField>

            <ParamField body="name" type="string">

              Ribbon name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="mainCategoryId" type="string">

          The ID of the product's primary direct category, which defines the product's breadcrumbs path. For example, if the product's main category is "T-Shirts" (which is a subcategory of "Clothing"), the breadcrumbs path will be "Clothing > T-Shirts".

        </ParamField>

        <ParamField body="productType" type="string" required>

          Product type: `PHYSICAL` for tangible goods that require shipping, or `DIGITAL` for downloadable content.

          When passing `productType: PHYSICAL`, you must also pass `physicalProperties`. When passing `productType: DIGITAL`, you can optionally pass `digitalProperties` in each variant.

          Constraints: `immutable`.

          **Supported values:**

          - `PHYSICAL` — Physical product.
          - `DIGITAL` — Digital product.

        </ParamField>

        <ParamField body="handle" type="string">

          A unique human-friendly identifier for the product.

          Unlike the auto-generated `id`, the handle can be set when creating a product. This is useful when re-importing products from other systems, as it provides a stable identifier across platforms. If not provided during creation, one is automatically generated. Can't be changed after creation.

          **Read-only.**

        </ParamField>

        <ParamField body="variantsInfo" type="VariantsInfo" required>

          Product variants. Each variant represents a specific purchasable version of a product defined by option choices.

          Each variant must reference all product options via its `choices` array, using `optionChoiceNames` in requests.
          Variants aren't auto-generated. You must explicitly provide each variant when creating products with options.

          For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          <Expandable title="variantsInfo properties">

            <ParamField body="variants" type="VariantWithInventory[]">

              List of related variants.
              Partial update of variants is not supported so on update you must pass all data for all of them.
              If you want to update existing variant you must provide `id`, otherwise new variant with different id will be created which might break some integrations that rely on variant ids.

              Constraints: `minItems 1`, `maxItems 1000`.

              _Nested structure omitted for brevity — see the `VariantWithInventory[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="subscriptionDetails" type="SubscriptionDetails">

          Product subscriptions.

          Subscription discounts are defined at the product level but apply to each variant's `actualPrice`. This means the final subscription price varies per variant based on the variant's base price.

          <Expandable title="subscriptionDetails properties">

            <ParamField body="subscriptions" type="Subscription[]" required>

              Subscriptions.

              Constraints: `minItems 1`, `maxItems 6`.

              _Nested structure omitted for brevity — see the `Subscription[]` object._

            </ParamField>

            <ParamField body="allowOneTimePurchases" type="boolean">

              Whether to allow one-time purchases in addition to subscription-based purchases.

              Default: `false`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="purchaseEligibility" type="PurchaseEligibility">

          Purchase eligibility settings for the product.

          **Developer Preview.**

          <Expandable title="purchaseEligibility properties">

            <ParamField body="userCriteria" type="string">

              **Supported values:**

              - `MEMBERS_ONLY` — Product is available only to members.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom extended fields for the product object.

          [Extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions) must be configured in the [app dashboard](https://dev.wix.com/dc3/my-apps/) before they can be accessed with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tags

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="publicTags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="typedProperties" type="typedProperties">

          typedProperties

          <Expandable title="typedProperties properties">

            <ParamField body="physicalProperties" type="PhysicalProperties">

              Physical properties.

              Required when `productType: PHYSICAL`.

              _Nested structure omitted for brevity — see the `PhysicalProperties` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="CreateProductWithInventoryOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return inventory entities in the response.

          Default: `false`

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Constraints: `maxItems 100`.

          **Supported values:**

          - `URL` — The product's public storefront URL, including both relative path and full URL with base domain.
          - `CURRENCY` — Currency information including the currency code and formatted price amounts for storefront display.
          - `INFO_SECTION` — Returns basic info section metadata including IDs, unique names, and titles. Use with `INFO_SECTION_DESCRIPTION` or `INFO_SECTION_PLAIN_DESCRIPTION` to include content.
          - `MERCHANT_DATA` — Returns merchant-related financial data including cost ranges and variant revenue details like cost, profit, and profit margin.
          Requires the `SCOPE.STORES.PRODUCT_READ_ADMIN` permission scope.
          - `PLAIN_DESCRIPTION` — Provides product description as plain HTML text.
          - `INFO_SECTION_PLAIN_DESCRIPTION` — Returns info section descriptions as HTML content. Use with `INFO_SECTION` for complete metadata.
          - `SUBSCRIPTION_PRICES_INFO` — Details subscription pricing options, empty for non-subscription products.
          - `BREADCRUMBS_INFO` — Returns the category breadcrumb navigation path from root categories down to the product's main category, including category IDs, names, and slugs.
          - `WEIGHT_MEASUREMENT_UNIT_INFO` — Returns the weight measurement unit (KG, LB) for the product.
          - `VARIANT_OPTION_CHOICE_NAMES` — Enriches variant choices with human-readable option and choice names alongside render types.
          - `MEDIA_ITEMS_INFO` — Returns all media items associated with the product, including detailed metadata (URLs, alt-text, dimensions, filenames, file sizes).
          - `DESCRIPTION` — Returns the product description as rich content with complete node structure, metadata, and styling information.
          - `DIRECT_CATEGORIES_INFO` — Provides the IDs and indexes of categories the product is directly assigned to.
          - `ALL_CATEGORIES_INFO` — Provides IDs and indexes for all categories including inherited parent categories.
          - `INFO_SECTION_DESCRIPTION` — Returns info section descriptions as rich content with complete node structure, metadata, and styling information. Use with `INFO_SECTION` for complete metadata.
          - `THUMBNAIL` — Returns an optimized thumbnail image with standard dimensions, suitable for listings or previews.
          - `PRODUCT_CHOICES_MEDIA_REFERENCES` — Returns media references associated with product option and modifier choices instead of the deprecated `linked_media` field.

          When requested, choice objects will populate `media_references` with lightweight `MediaReference` objects containing only media IDs,
          while the `linked_media` field will be returned empty to avoid duplication and reduce response payload size.

          To get full media metadata (dimensions, alt text, filenames, etc.), request `MEDIA_ITEMS_INFO` on the product and correlate by media ID.

          > **Note:** The `media_references` field is available on `ConnectedOptionChoice` and `ConnectedModifierChoice` objects.
          - `PRODUCT_CHOICES_DISPLAY_IMAGE` — Returns display images for product option and modifier choices.
          When requested, choice objects will populate `display_image` with a lightweight image containing only ID, URL, and alt text.
          - `DISCOUNT_INFO` — Returns automatic discount info for product get/query/search responses and populates product-level `discountInfo.discountRuleNames`.

          To receive discounted variant prices in get/query/search responses, request this together with `MIN_PRICE_VARIANT` (returned in `variantSummary.minPriceVariant.price.priceAfterDiscount`).
          Formatted money amounts are returned only when `CURRENCY` is also requested.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CreateProductWithInventoryResponse>`.

    <ResponseField name="product" type="Product">

      Created product.

      See the [Product Object](/api-reference/business-solutions/stores/catalog-v3/products-v3/product-object) for this object's fields.

    </ResponseField>

    <ResponseField name="inventoryResults" type="BulkInventoryItemResults">

      Inventories created by bulk action.

      <Expandable title="inventoryResults properties">

        <ResponseField name="results" type="BulkInventoryItemResult[]">

          Inventories modified by bulk action.

          Constraints: `minItems 0`, `maxItems 1000`.

          <Expandable title="results properties">

            <ResponseField name="itemMetadata" type="ItemMetadata">

              Bulk action metadata for inventory item.

              _Nested structure omitted for brevity — see the `ItemMetadata` object._

            </ResponseField>

            <ResponseField name="item" type="InventoryItem">

              Full inventory item entity.

              Returned only if `returnEntity: true` is passed in the request.

              _Nested structure omitted for brevity — see the `InventoryItem` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

          Bulk action metadata.

          <Expandable title="bulkActionMetadata properties">

            <ResponseField name="totalSuccesses" type="integer">

              Number of items that were successfully processed.

            </ResponseField>

            <ResponseField name="totalFailures" type="integer">

              Number of items that couldn't be processed.

            </ResponseField>

            <ResponseField name="undetailedFailures" type="integer">

              Number of failures without details because detailed failure threshold was exceeded.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="error" type="ApplicationError">

          Error details in case of failed action.

          <Expandable title="error properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `RIBBON_NOT_FOUND` | Couldn't find the ribbon. |
    | `500` | `INTERNAL` | `RIBBON_CREATION_FAILED` | Ribbon creation failed. |
    | `400` | `INVALID_ARGUMENT` | `INFO_SECTION_CREATION_FAILED` | Info section creation failed. |
    | `404` | `NOT_FOUND` | `CUSTOMIZATION_NOT_EXIST` | Couldn't find the customization. |
    | `500` | `INTERNAL` | `CUSTOMIZATIONS_CREATION_FAILED` | Customization creation failed. |
    | `500` | `INTERNAL` | `ADD_CUSTOMIZATION_CHOICES_FAILED` | Failed to add customization choices. |
    | `404` | `NOT_FOUND` | `CHOICE_NOT_EXIST` | Couldn't find the choice. |
    | `400` | `INVALID_ARGUMENT` | `PRODUCT_OPTION_NAME_AND_TYPE_REQUIRED` | Option is missing `name` or `type`. Options without an `id` must include both. |
    | `400` | `INVALID_ARGUMENT` | `PRODUCT_OPTION_CHOICE_NAME_AND_TYPE_REQUIRED` | Option choice is missing `name` or `type`. Choices without an `id` must include both. |
    | `400` | `INVALID_ARGUMENT` | `MODIFIER_NAME_AND_TYPE_REQUIRED` | Modifier is missing `name` or `type`. Modifiers without an `id` must include both. |
    | `400` | `INVALID_ARGUMENT` | `MODIFIER_CHOICE_NAME_AND_TYPE_REQUIRED` | Modifier choice is missing `name` or `type`. Choices without an `id` must include both. |
    | `428` | `FAILED_PRECONDITION` | `COMPARE_AT_PRICE_LOWER_THAN_ACTUAL_PRICE` | The `compareAtPrice` is lower than the `actualPrice`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_ADDITIONAL` | Same ribbon appears more than once in `additionalRibbons`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_PRIMARY_AND_ADDITIONAL` | Same ribbon used as both primary ribbon and additional ribbon. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SLUG_FORMAT` | The `slug` provided is not a valid URL slug. |
    | `400` | `INVALID_ARGUMENT` | `REQUESTED_QUANTITY_MUST_BE_NON_NEGATIVE` | Inventory quantity is negative. The quantity must be non-negative. |
    | `400` | `INVALID_ARGUMENT` | `BRAND_MISSING_ID` | Brand is missing an `id`. |
    | `400` | `INVALID_ARGUMENT` | `PRICE_PER_UNIT_NOT_ALIGNED` | Price per unit isn't aligned between product and variants. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Product Created | `wix.stores.catalog.v3.product_created` | Triggered when a product is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-product-with-inventory" id="ex-sdk-create-product-with-inventory-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-product-with-inventory" id="ex-sdk-create-product-with-inventory-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-product-with-inventory" id="ex-sdk-create-product-with-inventory-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-product-with-inventory-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-product-with-inventory-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create a basic product with inventory</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create a product with weight and inventory</span><span className="wix-picker-desc">Create a product where the variant is assigned a value for weight</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createProductWithInventory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-product-with-inventory-0"><span className="wix-picker-name">Create a basic product with inventory</span></label>
    <label htmlFor="ex-sdk-create-product-with-inventory-1"><span className="wix-picker-name">Create a product with weight and inventory</span><span className="wix-picker-desc">Create a product where the variant is assigned a value for weight</span></label>
    <label htmlFor="ex-sdk-create-product-with-inventory-2"><span className="wix-picker-name">createProductWithInventory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create a basic product with inventory
    import { productsV3 } from "@wix/stores";

    const product = {
      name: "Shopping bag",
      productType: productsV3.ProductType.PHYSICAL,
      variantsInfo: {
        variants: [
          {
            price: {
              actualPrice: {
                amount: "0.5"
              }
            },
            inventoryItem: {
              inStock: false
            }
          }
        ]
      },
      physicalProperties: {}
    };

    async function createProductWithInventory() {
      const response = await productsV3.createProductWithInventory(product);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Create a product with weight and inventory
    import { productsV3 } from "@wix/stores";

    const product = {
      name: "Adjustable Reading Lamp",
      variantsInfo: {
        variants: [
          {
            price: {
              actualPrice: {
                amount: "20"
              }
            },
            inventoryItem: {
              inStock: true
            },
            physicalProperties: {
              weight: 60
            }
          }
        ]
      },
      productType: productsV3.ProductType.PHYSICAL,
      physicalProperties: {}
    };

    async function createProductWithInventory() {
      const response = await productsV3.createProductWithInventory(product);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createProductWithInventory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { productsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { productsV3 },
      // Include the auth strategy and host as relevant
    });

    async function createProductWithInventory(product,options) {
      const response = await myWixClient.productsV3.createProductWithInventory(product,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
