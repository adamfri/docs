---
title: "Delete Marketing Consent"
description: "Deletes a marketing consent."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a marketing consent.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.CONSENT-MANAGE">Manage Marketing Consents</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/marketing-consent/v1/marketing-consent/{marketingConsentId}
    ```

    ## Path Params

    <ParamField path="marketingConsentId" type="string" required>

      ID of the marketing consent to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteMarketingConsentResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Marketing Consent Deleted | `wix.marketing.marketing_consent.v1.marketing_consent_deleted` | Triggered when a marketing consent is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/marketing-consent/v1/marketing-consent/2178v046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a marketing consent.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.CONSENT-MANAGE">Manage Marketing Consents</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingConsent } from '@wix/marketing';

    marketingConsent.deleteMarketingConsent(marketingConsentId): Promise
    ```

    ## Method Parameters

    <ParamField body="marketingConsentId" type="string" required>

      ID of the marketing consent to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteMarketingConsentResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Marketing Consent Deleted | `wix.marketing.marketing_consent.v1.marketing_consent_deleted` | Triggered when a marketing consent is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-marketing-consent" id="ex-sdk-delete-marketing-consent-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-marketing-consent" id="ex-sdk-delete-marketing-consent-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-marketing-consent" id="ex-sdk-delete-marketing-consent-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-marketing-consent-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-marketing-consent-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingConsent</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingConsent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMarketingConsent (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-marketing-consent-0"><span className="wix-picker-name">deleteMarketingConsent</span></label>
    <label htmlFor="ex-sdk-delete-marketing-consent-1"><span className="wix-picker-name">deleteMarketingConsent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-marketing-consent-2"><span className="wix-picker-name">deleteMarketingConsent (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteMarketingConsent
    import { marketingConsent } from '@wix/marketing';

    async function deleteMarketingConsent(marketingConsentId) {
      const response = await marketingConsent.deleteMarketingConsent(marketingConsentId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMarketingConsent (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingConsent } from '@wix/marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingConsent },
      // Include the auth strategy and host as relevant
    });

    async function deleteMarketingConsent(marketingConsentId) {
      const response = await myWixClient.marketingConsent.deleteMarketingConsent(marketingConsentId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMarketingConsent (with elevated permissions)
    import { marketingConsent } from '@wix/marketing';
    import { auth } from '@wix/essentials';

    async function myDeleteMarketingConsentMethod(marketingConsentId) {
      const elevatedDeleteMarketingConsent = auth.elevate(marketingConsent.deleteMarketingConsent);
      const response = await elevatedDeleteMarketingConsent(marketingConsentId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
