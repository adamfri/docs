---
title: "Create Item"
description: "Creates a benefit item."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a benefit item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/items
    ```

    ## Body Params

    <ParamField body="item" type="Item" required>

      Item to create.

      See the [Item Object](/api-reference/business-solutions/benefit-programs/items/item-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="item" type="Item">

      Created item.

      See the [Item Object](/api-reference/business-solutions/benefit-programs/items/item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
        "https://www.wixapis.com/benefit-programs/v1/items"
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
      	--data '{
      		"item": {
      			"external_id": "3f9a7c91-4e2b-4d5e-b6f8-0c2e5a9d4f34",
      			"item_set_id": "201db180-5043-45cf-a501-bd9c06df5ae5",
      			"provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
      			"namespace": "benefit_programs_app"
      		}
      	}'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "item": {
          "id": "9d030ee9-f656-4a5c-8318-266a22aff552",
          "revision": "2",
          "created_date": "2025-04-24T06:38:51.177Z",
          "updated_date": "2025-04-24T06:40:47.420Z",
          "external_id": "3f9a7c91-4e2b-4d5e-b6f8-0c2e5a9d4f34",
          "category": "",
          "item_set_id": "201db180-5043-45cf-a501-bd9c06df5ae5",
          "display_name": "Ticket",
          "provider_app_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
          "extended_fields": null,
          "namespace": "benefit_programs_app"
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a benefit item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/benefit-programs';

    items.createItem(item): Promise
    ```

    ## Method Parameters

    <ParamField body="item" type="Item" required>

      Item to create.

      See the [Item Object](/api-reference/business-solutions/benefit-programs/items/item-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Item`.

    See the [Item Object](/api-reference/business-solutions/benefit-programs/items/item-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Item Created | `wix.benefit_programs.v1.item_created` | Triggered when an item is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-item" id="ex-sdk-create-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-item" id="ex-sdk-create-item-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-item" id="ex-sdk-create-item-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createItem (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-item-0"><span className="wix-picker-name">createItem</span></label>
    <label htmlFor="ex-sdk-create-item-1"><span className="wix-picker-name">createItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-item-2"><span className="wix-picker-name">createItem (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createItem
    import { items } from '@wix/benefit-programs';

    async function createItem(item) {
      const response = await items.createItem(item);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function createItem(item) {
      const response = await myWixClient.items.createItem(item);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createItem (with elevated permissions)
    import { items } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myCreateItemMethod(item) {
      const elevatedCreateItem = auth.elevate(items.createItem);
      const response = await elevatedCreateItem(item);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
