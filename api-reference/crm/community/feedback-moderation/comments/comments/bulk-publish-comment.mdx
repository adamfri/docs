---
title: "Bulk Publish Comment"
description: "Publishes multiple comments with the HIDDEN status."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Publishes multiple comments with the `HIDDEN` status. The comment status becomes `PUBLISHED`.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_COMMENTS.MANAGE.COMMENTS">Manage Comments</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/comments/v1/bulk/comments/publish-by-filter
    ```

    ## Body Params

    <ParamField body="appId" type="string" required>

      ID of the app where to publish the comment.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="filter" type="object" required>

      Filter to identify the comments that need to be published. Each key corresponds to an option name, and its values correspond to the choices for this option. For example, &#123;`"resourceId"`: `"64ad407e0bf87891bba7de3a"`, `"parentComment.id"`: `"726cb137-a9c3-408b-9cc2-2b64c6be8a95"`&#125;.

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID.

      Pass this ID to Get Async Job, in the Async Jobs API, to retrieve job details and metadata.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Comment Updated | `wix.comments.v2.comment_updated` | Triggered when a comment is updated. |
    | Comment Published | `wix.comments.v2.comment_comment_published` | Triggered when a comment is published. |
    | Comment Content Changed | `wix.comments.v2.comment_comment_content_changed` | Triggered when comment content is updated. |
    | Resource Comment Count Changed | `wix.comments.v2.comment_resource_comment_count_changed` | Triggered when the total number of comments has changed in a resource. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST https://www.wixapis.com/comments/v1/bulk/comments/publish-by-filter \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
                "appId": "14bcded7-0066-7c35-14d7-466cb3f09103",
                "filter": {
                  "author.memberId": "81b3e2a2-60f4-4016-ab20-c2fda4c4c4f2"
                }
              }'
      ```

      ```json Response
      {
        "jobId": "90616636-cfe2-4f3d-88ce-1c3d8e3eaa67"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Publishes multiple comments with the `HIDDEN` status. The comment status becomes `PUBLISHED`.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_COMMENTS.MANAGE.COMMENTS">Manage Comments</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { comments } from '@wix/comments';

    comments.bulkPublishComment(appId, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="appId" type="string" required>

      ID of the app where to publish the comment.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="BulkPublishCommentOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="filter" type="object" required>

          Filter to identify the comments that need to be published. Each key corresponds to an option name, and its values correspond to the choices for this option. For example, &#123;`"resourceId"`: `"64ad407e0bf87891bba7de3a"`, `"parentComment.id"`: `"726cb137-a9c3-408b-9cc2-2b64c6be8a95"`&#125;.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="jobId" type="string">

      Job ID.

      Pass this ID to Get Async Job, in the Async Jobs API, to retrieve job details and metadata.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Comment Updated | `wix.comments.v2.comment_updated` | Triggered when a comment is updated. |
    | Comment Comment Published | `wix.comments.v2.comment_comment_published` | Triggered when a comment is published. |
    | Comment Comment Content Changed | `wix.comments.v2.comment_comment_content_changed` | Triggered when comment content is updated. |
    | Comment Resource Comment Count Changed | `wix.comments.v2.comment_resource_comment_count_changed` | Triggered when the total number of comments has changed in a resource. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-publish-comment" id="ex-sdk-bulk-publish-comment-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-publish-comment" id="ex-sdk-bulk-publish-comment-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-publish-comment" id="ex-sdk-bulk-publish-comment-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-publish-comment-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-publish-comment-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkPublishComment</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkPublishComment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkPublishComment (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-publish-comment-0"><span className="wix-picker-name">bulkPublishComment</span></label>
    <label htmlFor="ex-sdk-bulk-publish-comment-1"><span className="wix-picker-name">bulkPublishComment (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-publish-comment-2"><span className="wix-picker-name">bulkPublishComment (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkPublishComment
    import { comments } from '@wix/comments';

    async function bulkPublishComment(appId,options) {
      const response = await comments.bulkPublishComment(appId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkPublishComment (self-hosted)
    import { createClient } from '@wix/sdk';
    import { comments } from '@wix/comments';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { comments },
      // Include the auth strategy and host as relevant
    });

    async function bulkPublishComment(appId,options) {
      const response = await myWixClient.comments.bulkPublishComment(appId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkPublishComment (with elevated permissions)
    import { comments } from '@wix/comments';
    import { auth } from '@wix/essentials';

    async function myBulkPublishCommentMethod(appId,options) {
      const elevatedBulkPublishComment = auth.elevate(comments.bulkPublishComment);
      const response = await elevatedBulkPublishComment(appId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
