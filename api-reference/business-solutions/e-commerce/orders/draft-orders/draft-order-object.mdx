---
title: "Draft Order Object"
description: "A temporary, editable version of an order."
icon: "cube"
---

A temporary, editable version of an order. Use draft orders to modify pricing, line items, or discounts on an existing order before committing the changes.

When committed, the draft order's changes are applied to the original order.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Draft order ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="orderId" type="string">

      ID of the order this draft commits to.

    </ResponseField>

    <ResponseField name="lineItems" type="ItemDetails[]">

      Line items.
      Includes details about changes when relevant.

      Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="lineItems properties">

        <ResponseField name="lineItem" type="OrderLineItem">

          Line item details after applied changes.

          <Expandable title="lineItem properties">

            <ResponseField name="id" type="string">

              Line item ID.

              Constraints: `immutable`.

            </ResponseField>

            <ResponseField name="productName" type="ProductName">

              Item name.

              + Stores - `product.name`
              + Bookings - `service.info.name`
              + Events - `ticket.name`

              <Expandable title="productName properties">

                <ResponseField name="original" type="string">

                  __Required.__ Item name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Item name translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="catalogReference" type="CatalogReference">

              References to the line item's origin catalog. Empty for custom line items.

              <Expandable title="catalogReference properties">

                <ResponseField name="catalogItemId" type="string">

                  ID of the item within the catalog it belongs to.

                  Constraints: `minLength 1`, `maxLength 36`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  ID of the app providing the catalog.

                  You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

                  For items from Wix catalogs, the following values always apply:
                  + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
                  + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
                  + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

                  Constraints: `minLength 1`.

                </ResponseField>

                <ResponseField name="options" type="object">

                  Additional item details in `key:value` pairs.

                  Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

                  For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
                  or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="quantity" type="integer">

              Line item quantity.

              Constraints: `min 1`, `max 100000`.

            </ResponseField>

            <ResponseField name="totalDiscount" type="Price">

              Total discount for this line item's entire quantity.

              **Read-only.**

              <Expandable title="totalDiscount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="descriptionLines" type="DescriptionLine[]">

              Line item description lines. Used for display purposes for the cart, checkout and order.

              Constraints: `minItems 0`, `maxItems 20`.

              <Expandable title="descriptionLines properties">

                <ResponseField name="name" type="DescriptionLineName">

                  Description line name.

                  _Nested structure omitted for brevity — see the `DescriptionLineName` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="image" type="Image">

              Line item image.

              <Expandable title="image properties">

                <ResponseField name="id" type="string">

                  WixMedia image ID.

                </ResponseField>

                <ResponseField name="url" type="string">

                  Image URL.

                </ResponseField>

                <ResponseField name="height" type="integer">

                  Original image height.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="width" type="integer">

                  Original image width.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="altText" type="string">

                  Image alt text.

                </ResponseField>

                <ResponseField name="filename" type="string">

                  Image filename.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="physicalProperties" type="PhysicalProperties">

              Physical properties of the item. When relevant, contains information such as SKU and item weight.

              <Expandable title="physicalProperties properties">

                <ResponseField name="weight" type="number">

                  Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request.

                </ResponseField>

                <ResponseField name="sku" type="string">

                  Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).

                </ResponseField>

                <ResponseField name="shippable" type="boolean">

                  Whether this line item is shippable.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="itemType" type="ItemType">

              Item type. Either a preset type or custom.

              <Expandable title="One of the following">

                <ResponseField name="itemTypeData" type="itemTypeData">

                  itemTypeData

                  _Nested structure omitted for brevity — see the `itemTypeData` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="fulfillerId" type="string">

              Fulfiller ID. Field is empty when the line item is self-fulfilled.
              To get fulfillment information, pass the order ID to [List Fulfillments For Single Order](https://dev.wix.com/docs/rest/api-reference/wix-e-commerce/order-fulfillments/list-fulfillments-for-single-order).

            </ResponseField>

            <ResponseField name="refundQuantity" type="integer">

              Number of items that were refunded.

            </ResponseField>

            <ResponseField name="restockQuantity" type="integer">

              Number of items restocked.

              For a per-location breakdown, see `restockLocations`.

            </ResponseField>

            <ResponseField name="price" type="Price">

              Line item price after line item discounts for display purposes.

              <Expandable title="price properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceBeforeDiscounts" type="Price">

              Line item price before line item discounts for display purposes. Defaults to `price` when not provided.

              **Read-only.**

              <Expandable title="priceBeforeDiscounts properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="totalPriceBeforeTax" type="Price">

              Total price after discounts, and before tax.

              **Read-only.**

              <Expandable title="totalPriceBeforeTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="totalPriceAfterTax" type="Price">

              Total price after all discounts and tax.

              **Read-only.**

              <Expandable title="totalPriceAfterTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="paymentOption" type="string">

              Type of selected payment option for current item.

              Default: `FULL_PAYMENT_ONLINE`

              **Supported values:**

              - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
              - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
              - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
              - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
              - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
              - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

            </ResponseField>

            <ResponseField name="taxDetails" type="ItemTaxFullDetails" deprecated>

              Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024.
              Tax details for this line item.

              **Deprecated.** Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024.
              Tax details for this line item. Use `tax_info` instead. Removal: 2024-09-30.

              <Expandable title="taxDetails properties">

                <ResponseField name="taxableAmount" type="Price">

                  Taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate percentage, as a decimal numeral between 0 and 1. For example, `"0.13"`.

                  Constraints: `>= 0`.

                </ResponseField>

                <ResponseField name="totalTax" type="Price">

                  The calculated tax, based on the `taxableAmount` and `taxRate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxInfo" type="LineItemTaxInfo">

              Represents all the relevant tax details for a specific line item.

              <Expandable title="taxInfo properties">

                <ResponseField name="taxAmount" type="Price">

                  Calculated tax, based on `taxable_amount` and `tax_rate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="Price">

                  Amount for which tax is calculated.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate %, as a decimal point.

                </ResponseField>

                <ResponseField name="taxGroupId" type="string">

                  Tax group ID.

                </ResponseField>

                <ResponseField name="taxIncludedInPrice" type="boolean">

                  Indicates whether the price already includes tax.

                </ResponseField>

                <ResponseField name="taxBreakdown" type="LineItemTaxBreakdown[]">

                  Tax information for a line item.

                  Constraints: `maxItems 7`.

                  _Nested structure omitted for brevity — see the `LineItemTaxBreakdown[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="digitalFile" type="DigitalFile">

              Digital file identifier, relevant only for items with type DIGITAL.

              <Expandable title="digitalFile properties">

                <ResponseField name="fileId" type="string">

                  ID of the secure file in media.

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

                <ResponseField name="link" type="string">

                  Link will exist after the digital links have been generated on the order.

                </ResponseField>

                <ResponseField name="expirationDate" type="datetime">

                  Link expiration time and date.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="subscriptionInfo" type="SubscriptionInfo">

              Subscription info.

              <Expandable title="subscriptionInfo properties">

                <ResponseField name="id" type="string">

                  Subscription ID.

                </ResponseField>

                <ResponseField name="cycleNumber" type="integer">

                  Subscription cycle. For example, if this order is for the 3rd cycle of a subscription, value will be `3`.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="subscriptionOptionTitle" type="string" deprecated>

                  Subscription option title. For example, `"Monthly coffee Subscription"`.

                  **Deprecated.** Subscription option title. For example, `"Monthly coffee Subscription"`. Use `title` instead. Removal: 2025-10-01. Constraints: `minLength 0`, `maxLength 150`.

                </ResponseField>

                <ResponseField name="subscriptionOptionDescription" type="string">

                  Subscription option description. For example, `"1kg of selected coffee, once a month"`.

                </ResponseField>

                <ResponseField name="subscriptionSettings" type="SubscriptionSettings">

                  Subscription detailed information.

                  Constraints: `immutable`.

                  _Nested structure omitted for brevity — see the `SubscriptionSettings` object._

                </ResponseField>

                <ResponseField name="chargesDescription" type="string">

                  Description of the charges that will be applied for subscription.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceDescription" type="PriceDescription">

              Additional description for the price. For example, when price is 0 but additional details about the actual price are needed - "Starts at $67".

              <Expandable title="priceDescription properties">

                <ResponseField name="original" type="string">

                  __Required.__ Price description in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Price description translated into the buyer's language.

                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="depositAmount" type="Price">

              Item's price amount to be charged during checkout. Relevant for items with a `paymentOption` value of `"DEPOSIT_ONLINE"`.

              **Read-only.**

              <Expandable title="depositAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="locations" type="LocationAndQuantity[]">

              Line item locations.

              The location's total quantity must not exceed the line item quantity.

              **Developer Preview.** Constraints: `minItems 0`, `maxItems 5`.

              <Expandable title="locations properties">

                <ResponseField name="id" type="string">

                  Location id in the associated owner app.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  Location owner app, if not provided then the site business info locations will be used.

                </ResponseField>

                <ResponseField name="quantity" type="integer">

                  Quantity for specific location.

                  Constraints: `min 1`, `max 100000`.

                </ResponseField>

                <ResponseField name="name" type="string">

                  Location name.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="lineItemPrice" type="Price">

              Total price **after** catalog discounts and line item discounts.

              <Expandable title="lineItemPrice properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxableAddress" type="TaxableAddress">

              Address used for tax calculation.

              <Expandable title="One of the following">

                <ResponseField name="TaxableAddressData" type="TaxableAddressData">

                  TaxableAddressData

                  _Nested structure omitted for brevity — see the `TaxableAddressData` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceUndetermined" type="boolean">

              Whether the price is not yet defined, and will be updated after the order is created.

              Default: `false`

            </ResponseField>

            <ResponseField name="fixedQuantity" type="boolean">

              Whether the line item quantity is fixed and cannot be changed.

              Default: `false`

              **Developer Preview.**

            </ResponseField>

            <ResponseField name="decimalQuantity" type="DecimalQuantity">

              Decimal-quantity metadata. Set when the line represents a
              non-integer purchase (e.g. 2.75 m² of fake grass).

              When set, the line item must have `fixedQuantity` = true and be set with `quantity` = 1.
              The decimal value lives in `decimalQuantity.value`.

              **Developer Preview.**

              <Expandable title="decimalQuantity properties">

                <ResponseField name="value" type="string">

                  The decimal quantity the buyer / merchant entered.

                  String-encoded decimal. The exact value is preserved — no
                  rounding, no conversion to an integer.

                  Examples: "2.75", "1.5", "0.75".

                  **Developer Preview.** Constraints: `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="description" type="TranslatableString">

                  Human-readable, pre-formatted display string for the quantity. For example, "2.75 square meters of fake grass".
                  Always set alongside `value`; use this directly for display rather than composing your own string from `value` and `unit`.

                  **Developer Preview.**

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="unit" type="unit">

                  unit

                  _Nested structure omitted for brevity — see the `unit` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="extendedFields" type="ExtendedFields">

              Custom extended fields for the line item object.

              [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.

              **Developer Preview.**

              <Expandable title="extendedFields properties">

                <ResponseField name="namespaces" type="object">

                  Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
                  The value of each key is structured according to the schema defined when the extended fields were configured.

                  You can only access fields for which you have the appropriate permissions.

                  Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

                  Constraints: `format map`.

                  _Nested structure omitted for brevity — see the `object` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="modifierGroups" type="ModifierGroup[]">

              Modifier groups that were added to the item.

              **Read-only.** **Developer Preview.** Constraints: `maxItems 10`.

              <Expandable title="modifierGroups properties">

                <ResponseField name="id" type="string">

                  Modifier group ID.

                  Constraints: `minLength 1`, `maxLength 36`.

                </ResponseField>

                <ResponseField name="name" type="TranslatableString">

                  Modifier group name.

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

                <ResponseField name="modifiers" type="ItemModifier[]">

                  List of modifiers in this group.

                  Constraints: `minItems 1`, `maxItems 10`.

                  _Nested structure omitted for brevity — see the `ItemModifier[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="shippingInfo" type="ShippingDetails">

      Shipping info.
      Includes details about changes when relevant.

      <Expandable title="shippingInfo properties">

        <ResponseField name="shippingInfo" type="ShippingInformation">

          Selected shipping details.

          <Expandable title="shippingInfo properties">

            <ResponseField name="carrierId" type="string">

              App Def Id of external provider which was a source of shipping info

            </ResponseField>

            <ResponseField name="code" type="string">

              Unique code (or ID) of selected shipping option. For example, `"usps_std_overnight"`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Shipping option title.
              For example, `"USPS Standard Overnight Delivery"`, `"Standard"` or `"First-Class Package International"`.

              Constraints: `maxLength 250`.

            </ResponseField>

            <ResponseField name="logistics" type="DeliveryLogistics">

              Shipping logistics.

              <Expandable title="logistics properties">

                <ResponseField name="deliveryTime" type="string">

                  Expected delivery time in free text. For example, `"3-5 business days"`.

                </ResponseField>

                <ResponseField name="instructions" type="string">

                  Instructions for the carrier. For example, `"Please knock on the door. If unanswered, please call contact number."`.

                </ResponseField>

                <ResponseField name="deliverByDate" type="datetime">

                  Deprecated - Latest expected delivery date and time in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

                </ResponseField>

                <ResponseField name="deliveryTimeSlot" type="DeliveryTimeSlot">

                  Expected delivery time slot with start and end times.

                  _Nested structure omitted for brevity — see the `DeliveryTimeSlot` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="Address" type="Address">

                  Address

                  _Nested structure omitted for brevity — see the `Address` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="cost" type="ShippingPrice">

              Shipping costs.

              <Expandable title="cost properties">

                <ResponseField name="price" type="Price">

                  Shipping price for display purposes.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="totalPriceBeforeTax" type="Price">

                  Total price of shipping after discounts (when relevant), and before tax.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="totalPriceAfterTax" type="Price">

                  Shipping price after all discounts (if any exist), and after tax.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxDetails" type="ItemTaxFullDetails" deprecated>

                  Tax details.

                  **Deprecated.** Tax details. Use `tax_info` instead. Removal: 2026-03-30.

                  _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

                </ResponseField>

                <ResponseField name="taxInfo" type="LineItemTaxInfo">

                  Represents all the relevant tax details for a shipping.

                  _Nested structure omitted for brevity — see the `LineItemTaxInfo` object._

                </ResponseField>

                <ResponseField name="discount" type="Price">

                  Shipping discount before tax.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="region" type="ShippingRegion">

              Shipping region.

              <Expandable title="region properties">

                <ResponseField name="name" type="string">

                  Name of shipping region. For example, `"Metropolitan London"`, or `"Outer Melbourne suburbs"`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Whether shipping info was added as part of the draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="ShippingChangedDetails">

              Shipping info before the change.

              <Expandable title="changedDetails properties">

                <ResponseField name="shippingInfoBeforeChange" type="ShippingInformation">

                  Shipping info before the change.

                  _Nested structure omitted for brevity — see the `ShippingInformation` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Whether shipping info was removed as part of the draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="buyerInfo" type="BuyerInfo" deprecated>

      Buyer information.

      **Deprecated.** Buyer information. Use `buyer_details` instead. Removal: 2024-08-30.

      <Expandable title="buyerInfo properties">

        <ResponseField name="contactId" type="string">

          Contact ID. Automatically created if one doesn't exist. For more information, see [Contacts API](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts/contact-v4/introduction).

        </ResponseField>

        <ResponseField name="email" type="string">

          Buyer's email address.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="visitorId" type="string">

              Visitor ID. Returned when the buyer isn't a logged-in site member.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="memberId" type="string">

              Member ID. Returned when the buyer is a logged-in site member.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="billingInfo" type="AddressWithContact" deprecated>

      Billing address and contact details.

      **Deprecated.** Billing address and contact details. Use `billing_details` instead. Removal: 2024-08-30.

      <Expandable title="billingInfo properties">

        <ResponseField name="address" type="Address">

          Address.

          <Expandable title="address properties">

            <ResponseField name="country" type="string">

              Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

            </ResponseField>

            <ResponseField name="subdivision" type="string">

              Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

            </ResponseField>

            <ResponseField name="city" type="string">

              City name.

            </ResponseField>

            <ResponseField name="postalCode" type="string">

              Postal or zip code.

            </ResponseField>

            <ResponseField name="streetAddress" type="StreetAddress">

              Street address.

              <Expandable title="streetAddress properties">

                <ResponseField name="number" type="string">

                  Street number.

                </ResponseField>

                <ResponseField name="name" type="string">

                  Street name.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="addressLine" type="string">

              Main address line (usually street name and number).

            </ResponseField>

            <ResponseField name="addressLine2" type="string">

              Free text providing more detailed address info. Usually contains apt, suite, floor.

            </ResponseField>

            <ResponseField name="geocode" type="AddressLocation">

              Geocode object containing latitude and longitude coordinates.

              <Expandable title="geocode properties">

                <ResponseField name="latitude" type="number">

                  Address latitude.

                </ResponseField>

                <ResponseField name="longitude" type="number">

                  Address longitude.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="countryFullname" type="string">

              Country's full name.

              **Read-only.**

            </ResponseField>

            <ResponseField name="subdivisionFullname" type="string">

              Subdivision full-name.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="contactDetails" type="FullAddressContactDetails">

          Contact details.

          <Expandable title="contactDetails properties">

            <ResponseField name="firstName" type="string">

              First name.

            </ResponseField>

            <ResponseField name="lastName" type="string">

              Last name.

            </ResponseField>

            <ResponseField name="phone" type="string">

              Phone number.

            </ResponseField>

            <ResponseField name="company" type="string">

              Company name.

            </ResponseField>

            <ResponseField name="vatId" type="VatId">

              Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

              <Expandable title="vatId properties">

                <ResponseField name="id" type="string">

                  Customer's tax ID.

                </ResponseField>

                <ResponseField name="type" type="string">

                  Tax type.

                  Supported values:
                  + `CPF`: for individual tax payers
                  + `CNPJ`: for corporations

                  **Supported values:**

                  - `UNSPECIFIED`
                  - `CPF` — CPF - for individual tax payers.
                  - `CNPJ` — CNPJ - for corporations

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalFees" type="AdditionalFeeDetails[]">

      Additional fees.

      Includes details about the source, and whether the additional fee is applied to the order.

      Constraints: `maxItems 100`.

      <Expandable title="additionalFees properties">

        <ResponseField name="additionalFee" type="AdditionalFee">

          Additional fee.

          <Expandable title="additionalFee properties">

            <ResponseField name="code" type="string">

              Additional fee's unique code for future processing.

            </ResponseField>

            <ResponseField name="name" type="string">

              Name of additional fee.

              Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="price" type="Price">

              Additional fee's price.

              <Expandable title="price properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxDetails" type="ItemTaxFullDetails" deprecated>

              Tax details.

              **Deprecated.** Tax details. Use `tax_info` instead. Removal: 2026-03-30.

              <Expandable title="taxDetails properties">

                <ResponseField name="taxableAmount" type="Price">

                  Taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate percentage, as a decimal numeral between 0 and 1. For example, `"0.13"`.

                  Constraints: `>= 0`.

                </ResponseField>

                <ResponseField name="totalTax" type="Price">

                  The calculated tax, based on the `taxableAmount` and `taxRate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxInfo" type="LineItemTaxInfo">

              Represents all the relevant tax details for additional fee.

              <Expandable title="taxInfo properties">

                <ResponseField name="taxAmount" type="Price">

                  Calculated tax, based on `taxable_amount` and `tax_rate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="Price">

                  Amount for which tax is calculated.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate %, as a decimal point.

                </ResponseField>

                <ResponseField name="taxGroupId" type="string">

                  Tax group ID.

                </ResponseField>

                <ResponseField name="taxIncludedInPrice" type="boolean">

                  Indicates whether the price already includes tax.

                </ResponseField>

                <ResponseField name="taxBreakdown" type="LineItemTaxBreakdown[]">

                  Tax information for a line item.

                  Constraints: `maxItems 7`.

                  _Nested structure omitted for brevity — see the `LineItemTaxBreakdown[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="providerAppId" type="string">

              SPI implementer's `appId`.

            </ResponseField>

            <ResponseField name="priceBeforeTax" type="Price">

              Additional fee's price before tax.

              <Expandable title="priceBeforeTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceAfterTax" type="Price">

              Additional fee's price after tax.

              <Expandable title="priceAfterTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="id" type="string">

              Additional fee's id.

              Constraints: `format GUID`, `immutable`.

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              Optional - Line items associated with this additional fee.
              If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="source" type="string">

              Specifies the entity that added the additional fee.

              **Developer Preview.**

              **Supported values:**

              - `SERVICE_PLUGIN` — The additional fee was added by an additional fee service plugin.
              - `ITEM` — The additional fee was added to the item by a catalog or custom line item.
              - `MANUAL` — The additional fee was added manually on request.
              - `SHIPPING` — The additional fee was added by the shipping provider.
              - `PLATFORM` — The additional fee was added by the Wix eCommerce platform.

            </ResponseField>

            <ResponseField name="taxableAddress" type="TaxableAddress">

              Address type tax was calculated against for this fee.

              **Developer Preview.**

              <Expandable title="One of the following">

                <ResponseField name="TaxableAddressData" type="TaxableAddressData">

                  TaxableAddressData

                  _Nested structure omitted for brevity — see the `TaxableAddressData` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="TaxDetails">

          Tax details related to the additional fee.

          <Expandable title="taxDetails properties">

            <ResponseField name="taxable" type="boolean">

              Whether the additional fee is taxable.

            </ResponseField>

            <ResponseField name="taxGroupId" type="string">

              Optional tax group ID to use when calculating tax for this additional fee.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="source" type="string">

          Source of the additional fee.

          Supported values:
          + `ORIGINAL_ORDER`: This additional fee is part of the original order.
          + `AUTOMATIC`: User added a catalog item which is configured to have an additional fee.
          + `CUSTOM`: This additional fee was created by the Create Custom Additional Fees method.

          **Supported values:**

          - `ORIGINAL_ORDER` — Part of the original order.
          - `AUTOMATIC` — Added automatically, as the result of recalculation caused by changes in draft order.
          - `CUSTOM` — Added manually to draft order.

        </ResponseField>

        <ResponseField name="applied" type="boolean">

          Whether this additional fee is applied to the order.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="discounts" type="DiscountDetails[]">

      Discounts.

      Includes details about the source, and whether the discount is applied to the order.

      Constraints: `maxItems 320`.

      <Expandable title="discounts properties">

        <ResponseField name="discount" type="AppliedDiscount">

          Discount details.

          <Expandable title="discount properties">

            <ResponseField name="discountType" type="string">

              Type of discount.

              **Supported values:**

              - `GLOBAL` — Discount applies to the entire order.
              - `SPECIFIC_ITEMS` — Discount applies to specific items.
              - `SHIPPING` — Discount applies to shipping. For example, free shipping.

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]" deprecated>

              __Deprecated.__ Use `lineItemDiscounts` instead.

              IDs of line items the discount applies to.

              **Deprecated.** __Deprecated.__ Use `lineItemDiscounts` instead.

              IDs of line items the discount applies to. Use `line_item_discounts` instead. Removal: 2024-10-30. Constraints: `format GUID`, `minItems 0`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="id" type="string">

              Discount ID.

            </ResponseField>

            <ResponseField name="lineItemDiscounts" type="LineItemDiscount[]">

              Line items the discount applies to, including the discount amount for each.

              **Developer Preview.** Constraints: `maxItems 300`.

              <Expandable title="lineItemDiscounts properties">

                <ResponseField name="id" type="string">

                  Line item ID.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="totalDiscount" type="Price">

                  Total discount amount for this line item.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="DiscountSource" type="DiscountSource">

              DiscountSource

              <Expandable title="DiscountSource properties">

                <ResponseField name="coupon" type="Coupon">

                  Coupon applied by the customer.

                  _Nested structure omitted for brevity — see the `Coupon` object._

                </ResponseField>

                <ResponseField name="merchantDiscount" type="MerchantDiscount">

                  Discount applied manually by the merchant.

                  _Nested structure omitted for brevity — see the `MerchantDiscount` object._

                </ResponseField>

                <ResponseField name="discountRule" type="DiscountRule">

                  Automatic discount applied by the system based on configured discount rules.

                  _Nested structure omitted for brevity — see the `DiscountRule` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="source" type="string">

          Source of the discount.

          Supported values:
          + `ORIGINAL_ORDER`: This discount is part of the original order.
          + `CUSTOM`: This discount was created by the Create Custom Discounts method.

          **Supported values:**

          - `ORIGINAL_ORDER` — Part of the original order.
          - `AUTOMATIC` — Added automatically, as the result of recalculation caused by changes in draft order.
          - `CUSTOM` — Added manually to draft order.

        </ResponseField>

        <ResponseField name="applied" type="boolean">

          Whether this discount is applied to the order.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the draft order was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time the draft order was updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdBy" type="CreatedBy">

      Identity of the draft order's initiator.

      **Read-only.**

      <Expandable title="One of the following">

        <ResponseField name="string" type="string">

          string

          <Expandable title="string properties">

            <ResponseField name="userId" type="string">

              User ID. When the draft order was created by a Wix user.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              App ID. When the draft order was created by an app.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="status" type="string">

      Draft order status.

      **Read-only.**

      **Supported values:**

      - `DRAFT` — Draft order is being edited.
      - `COMMITTED` — Draft order has been committed - No further modifications allowed.

      Draft orders with status 'COMMITTED' cannot be deleted.

    </ResponseField>

    <ResponseField name="weightUnit" type="string">

      Weight measurement unit. Defaults to the site's weight unit.

      **Supported values:**

      - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
      - `KG` — Kilograms.
      - `LB` — Pounds.

    </ResponseField>

    <ResponseField name="currency" type="string">

      Currency used for pricing.

    </ResponseField>

    <ResponseField name="priceSummary" type="PriceSummary">

      Price summary.

      **Read-only.**

      <Expandable title="priceSummary properties">

        <ResponseField name="subtotal" type="Price">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="Price">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="Price">

          Total tax applied to the order.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="Price">

          Total discount amount applied to the order.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="Price">

          Order's total price after discounts and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalAdditionalFees" type="Price">

          Total additional fees before tax.

          <Expandable title="totalAdditionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="taxSummary" type="TaxSummary" deprecated>

      Deprecated. Use `taxInfo` instead.
      This field will be removed on September 30, 2024.
      Tax summary.

      **Read-only.** **Deprecated.** Deprecated. Use `taxInfo` instead.
      This field will be removed on September 30, 2024.
      Tax summary. Use `tax_info` instead. Removal: 2024-09-30.

      <Expandable title="taxSummary properties">

        <ResponseField name="totalTax" type="Price">

          Total tax.

          **Read-only.**

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="balanceSummary" type="BalanceSummary">

      Balance summary.

      **Read-only.**

      <Expandable title="balanceSummary properties">

        <ResponseField name="balance" type="Balance">

          Amount left to pay for the order to be considered paid.

          **Read-only.**

          <Expandable title="balance properties">

            <ResponseField name="amount" type="string">

              Balance amount.

              A negative `amount` represents the amount to be refunded. This can happen due to overcharging or the order being modified after a payment has been made.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paid" type="Price">

          Sum of all approved and successful payments.

          The value includes payments that have subsequently been fully or partially refunded.

          **Read-only.**

          <Expandable title="paid properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="refunded" type="Price">

          Sum of all successfully refunded payments.

          **Read-only.**

          <Expandable title="refunded properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="authorized" type="Price">

          Sum of all authorized payments.

          **Read-only.**

          <Expandable title="authorized properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="platformFees" type="Price">

          Total platform fees charged across all payments.
          This reflects amounts collected, which may differ from the expected fees in `draftOrder.platformFeeSummary`.

          **Read-only.** **Developer Preview.**

          <Expandable title="platformFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalMinusPlatformFees" type="Price">

          Order total after deducting platform fees. Represents the net amount payable to the merchant.

          **Read-only.** **Developer Preview.**

          <Expandable title="totalMinusPlatformFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="cashRoundingAdjustment" type="CashRounding">

          The total adjustment applied to the cash payment due to rounding.

          Positive values indicate the total was rounded up; negative values indicate it was rounded down.

          **Read-only.** **Developer Preview.**

          <Expandable title="cashRoundingAdjustment properties">

            <ResponseField name="amount" type="string">

              Amount, can be negative or positive.

              Constraints: `>= -1000000000000000`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="commitSettings" type="DraftOrderCommitSettings">

      Side effects to perform on commit.

      <Expandable title="commitSettings properties">

        <ResponseField name="sendNotificationsToBuyer" type="boolean">

          Whether to send notifications to the buyer.

          Default: `true`

        </ResponseField>

        <ResponseField name="sendNotificationsToBusiness" type="boolean">

          Whether to send notifications to the business.

          Default: `true`

        </ResponseField>

        <ResponseField name="addActivitiesToOrder" type="boolean">

          Whether to add activities to the order.

          Default: `true`

        </ResponseField>

        <ResponseField name="sendNotificationsToCustomFulfillers" type="boolean">

          Whether to send email notifications to custom fulfillers when there is a change in the shippable items they fulfill.

          Default: `true`

        </ResponseField>

        <ResponseField name="updateInventory" type="InventoryUpdate[]">

          Line item IDs for which to perform an inventory change.
          When not provided, inventory will not change.

          Constraints: `maxItems 300`.

          <Expandable title="updateInventory properties">

            <ResponseField name="lineItemId" type="string">

              Line item ID.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="memberId" type="string">

      Member ID (if order created by a member).

    </ResponseField>

    <ResponseField name="purchaseFlowId" type="string">

      Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

    </ResponseField>

    <ResponseField name="recipientInfo" type="RecipientInfoDetails">

      Order recipient address and contact details.

      This field may differ from the address in `shippingInfo.logistics` when:
      + The chosen shipping option is pickup point or store pickup.
      + No shipping option is selected.

      <Expandable title="recipientInfo properties">

        <ResponseField name="recipientInfo" type="AddressWithContact">

          Order recipient address and contact details.

          <Expandable title="recipientInfo properties">

            <ResponseField name="address" type="Address">

              Address.

              <Expandable title="address properties">

                <ResponseField name="country" type="string">

                  Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

                </ResponseField>

                <ResponseField name="subdivision" type="string">

                  Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

                </ResponseField>

                <ResponseField name="city" type="string">

                  City name.

                </ResponseField>

                <ResponseField name="postalCode" type="string">

                  Postal or zip code.

                </ResponseField>

                <ResponseField name="streetAddress" type="StreetAddress">

                  Street address.

                  _Nested structure omitted for brevity — see the `StreetAddress` object._

                </ResponseField>

                <ResponseField name="addressLine" type="string">

                  Main address line (usually street name and number).

                </ResponseField>

                <ResponseField name="addressLine2" type="string">

                  Free text providing more detailed address info. Usually contains apt, suite, floor.

                </ResponseField>

                <ResponseField name="geocode" type="AddressLocation">

                  Geocode object containing latitude and longitude coordinates.

                  _Nested structure omitted for brevity — see the `AddressLocation` object._

                </ResponseField>

                <ResponseField name="countryFullname" type="string">

                  Country's full name.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="subdivisionFullname" type="string">

                  Subdivision full-name.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="contactDetails" type="FullAddressContactDetails">

              Contact details.

              <Expandable title="contactDetails properties">

                <ResponseField name="firstName" type="string">

                  First name.

                </ResponseField>

                <ResponseField name="lastName" type="string">

                  Last name.

                </ResponseField>

                <ResponseField name="phone" type="string">

                  Phone number.

                </ResponseField>

                <ResponseField name="company" type="string">

                  Company name.

                </ResponseField>

                <ResponseField name="vatId" type="VatId">

                  Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

                  _Nested structure omitted for brevity — see the `VatId` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that recipient information was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="RecipientInfoChangedDetails">

              Indicates that recipient information was changed. Contains recipient information before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="recipientInfoBeforeChange" type="AddressWithContact">

                  Order recipient address and contact details before draft.

                  _Nested structure omitted for brevity — see the `AddressWithContact` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that recipient information was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="taxInfo" type="OrderTaxInfo">

      Tax information.

      <Expandable title="taxInfo properties">

        <ResponseField name="totalTax" type="Price">

          Calculated tax, added from line items.

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxBreakdown" type="OrderTaxBreakdown[]">

          The summary of the tax breakdown for all the line items. It will hold for each tax name, the aggregated tax amount paid for it and the tax rate.

          Constraints: `minItems 0`, `maxItems 50`.

          <Expandable title="taxBreakdown properties">

            <ResponseField name="taxName" type="string">

              The name of the tax against which this tax amount was calculated.

              Constraints: `maxLength 200`.

            </ResponseField>

            <ResponseField name="taxType" type="string">

              The type of tax that was calculated. Depends on the company's nexus settings as well as the jurisdiction's tax laws.

              Constraints: `maxLength 200`.

            </ResponseField>

            <ResponseField name="jurisdiction" type="string">

              The name of the jurisdiction in which this tax detail applies.

              Constraints: `maxLength 200`.

            </ResponseField>

            <ResponseField name="jurisdictionType" type="string">

              The type of the jurisdiction in which this tax detail applies (e.g. Country,State,County,City,Special).

              **Supported values:**

              - `UNDEFINED`
              - `COUNTRY`
              - `STATE`
              - `COUNTY`
              - `CITY`
              - `SPECIAL`

            </ResponseField>

            <ResponseField name="rate" type="string">

              The rate at which this tax detail was calculated.

              Constraints: `format DECIMAL_VALUE`, `>= 0`.

            </ResponseField>

            <ResponseField name="aggregatedTaxAmount" type="Price">

              The sum of all the tax from line items that calculated by the tax identifiers.

              <Expandable title="aggregatedTaxAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxExempt" type="boolean">

          Whether the order is exempt from tax calculations.

          Default: `false`

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Custom field data for the draft order object.

      [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the [app dashboard](https://dev.wix.com/dc3/my-apps/) before they can be accessed with API calls.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="buyerDetails" type="BuyerDetails">

      Buyer information.

      Includes details about changes when relevant.

      <Expandable title="buyerDetails properties">

        <ResponseField name="buyerInfo" type="BuyerInfo">

          Buyer information.

          <Expandable title="buyerInfo properties">

            <ResponseField name="contactId" type="string">

              Contact ID. Automatically created if one doesn't exist. For more information, see [Contacts API](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts/contact-v4/introduction).

            </ResponseField>

            <ResponseField name="email" type="string">

              Buyer's email address.

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="id" type="id">

              id

              <Expandable title="id properties">

                <ResponseField name="visitorId" type="string">

                  Visitor ID. Returned when the buyer isn't a logged-in site member.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="memberId" type="string">

                  Member ID. Returned when the buyer is a logged-in site member.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that buyer information was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="BuyerChangedDetails">

              Indicates that buyer information was changed. Contains buyer information before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="buyerInfoBeforeChange" type="BuyerInfo">

                  Buyer information before draft.

                  _Nested structure omitted for brevity — see the `BuyerInfo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that buyer information was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="billingDetails" type="BillingDetails">

      Billing address and contact details.
      Includes details about changes when relevant.

      <Expandable title="billingDetails properties">

        <ResponseField name="billingInfo" type="AddressWithContact">

          Billing address and contact details.

          <Expandable title="billingInfo properties">

            <ResponseField name="address" type="Address">

              Address.

              <Expandable title="address properties">

                <ResponseField name="country" type="string">

                  Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

                </ResponseField>

                <ResponseField name="subdivision" type="string">

                  Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

                </ResponseField>

                <ResponseField name="city" type="string">

                  City name.

                </ResponseField>

                <ResponseField name="postalCode" type="string">

                  Postal or zip code.

                </ResponseField>

                <ResponseField name="streetAddress" type="StreetAddress">

                  Street address.

                  _Nested structure omitted for brevity — see the `StreetAddress` object._

                </ResponseField>

                <ResponseField name="addressLine" type="string">

                  Main address line (usually street name and number).

                </ResponseField>

                <ResponseField name="addressLine2" type="string">

                  Free text providing more detailed address info. Usually contains apt, suite, floor.

                </ResponseField>

                <ResponseField name="geocode" type="AddressLocation">

                  Geocode object containing latitude and longitude coordinates.

                  _Nested structure omitted for brevity — see the `AddressLocation` object._

                </ResponseField>

                <ResponseField name="countryFullname" type="string">

                  Country's full name.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="subdivisionFullname" type="string">

                  Subdivision full-name.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="contactDetails" type="FullAddressContactDetails">

              Contact details.

              <Expandable title="contactDetails properties">

                <ResponseField name="firstName" type="string">

                  First name.

                </ResponseField>

                <ResponseField name="lastName" type="string">

                  Last name.

                </ResponseField>

                <ResponseField name="phone" type="string">

                  Phone number.

                </ResponseField>

                <ResponseField name="company" type="string">

                  Company name.

                </ResponseField>

                <ResponseField name="vatId" type="VatId">

                  Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

                  _Nested structure omitted for brevity — see the `VatId` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that billing information was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="BillingChangedDetails">

              Indicates that billing information was changed. Contains billing information before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="billingInfoBeforeChange" type="AddressWithContact">

                  Billing address and contact details before draft.

                  _Nested structure omitted for brevity — see the `AddressWithContact` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that billing information was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="businessLocationDetails" type="BusinessLocationDetails">

      Order business location.

      Includes details about changes when relevant.

      <Expandable title="businessLocationDetails properties">

        <ResponseField name="businessLocation" type="Location">

          Business location.

          <Expandable title="businessLocation properties">

            <ResponseField name="id" type="string">

              Location ID.
              Learn more about the [Wix Locations API](https://dev.wix.com/docs/rest/business-management/locations/introduction).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="string">

              Location name.

              **Read-only.** Constraints: `minLength 1`, `maxLength 500`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that business location was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="BusinessLocationChangedDetails">

              Indicates that business location was changed. Contains business location before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="businessLocationBeforeChange" type="Location">

                  Business location before draft.

                  _Nested structure omitted for brevity — see the `Location` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that business location was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="platformFeeSummary" type="PlatformFeeSummary">

      Summary of platform fees for this draft order, including totals by charge type and a breakdown of individual fees.

      **Developer Preview.**

      <Expandable title="platformFeeSummary properties">

        <ResponseField name="total" type="Price">

          Total sum of all platform fees.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalPassOn" type="Price">

          Total amount of platform fees with `PASS_ON` charge type.

          <Expandable title="totalPassOn properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalAbsorbed" type="Price">

          Total amount of platform fees with `ABSORBED` charge type.

          <Expandable title="totalAbsorbed properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="fees" type="PlatformFee[]">

          Specific information about each platform fee.

          Constraints: `maxItems 300`.

          <Expandable title="fees properties">

            <ResponseField name="name" type="TranslatableString">

              Platform fee name.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  __Required.__ String in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  String translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="amount" type="Price">

              Platform fee amount.

              <Expandable title="amount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="lineItemId" type="string">

              ID of the line item the platform fee applies to.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="chargeType" type="string">

              Platform fee charge type.

              **Supported values:**

              - `PASS_ON` — Platform fee passed on to buyer.

              This type increases the order total, and is visible to the buyer and merchant as an additional fee.
              - `ABSORBED` — Platform fee absorbed by merchant.

              This type does not increase the order total, and is only visible to the merchant.

            </ResponseField>

            <ResponseField name="percentageRate" type="string">

              Percentage rate charged as platform fee.
              The fee rate percentage expressed as a decimal fraction between 0 and 1. For example, `0.05` for 5%.

              Constraints: `format DECIMAL_VALUE`, `<= 1`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "draftOrder": {
          "id": "08fa7662-e0e0-4b0f-9189-dcdca3d41545",
          "orderId": "ebad089a-c13a-4532-866d-2be7ce3ab7c8",
          "lineItems": [
            {
              "lineItem": {
                "id": "00000000-0000-0000-0000-000000000001",
                "productName": {
                  "original": "Classic T-Shirt",
                  "translated": "Classic T-Shirt"
                },
                "catalogReference": {
                  "catalogItemId": "b87b5f05-b35e-4292-9c5c-dae947a5130c",
                  "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                  "options": {
                    "variantId": "24d3f669-f590-4e59-92dd-769434be3a7b"
                  }
                },
                "quantity": 1,
                "totalDiscount": {
                  "amount": "0.00",
                  "formattedAmount": "$0.00"
                },
                "descriptionLines": [
                  {
                    "name": {
                      "original": "Size",
                      "translated": "Size"
                    },
                    "plainText": {
                      "original": "M",
                      "translated": "M"
                    }
                  }
                ],
                "physicalProperties": {
                  "weight": 300,
                  "sku": "TSHIRT-001-M",
                  "shippable": true
                },
                "itemType": {
                  "preset": "PHYSICAL"
                },
                "priceBeforeDiscounts": {
                  "amount": "29.99",
                  "formattedAmount": "$29.99"
                },
                "price": {
                  "amount": "29.99",
                  "formattedAmount": "$29.99"
                },
                "taxDetails": {
                  "taxableAmount": {
                    "amount": "29.99",
                    "formattedAmount": "$29.99"
                  },
                  "taxRate": "0",
                  "totalTax": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  },
                  "exemptAmount": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  }
                },
                "totalPriceBeforeTax": {
                  "amount": "29.99",
                  "formattedAmount": "$29.99"
                },
                "totalPriceAfterTax": {
                  "amount": "29.99",
                  "formattedAmount": "$29.99"
                },
                "paymentOption": "FULL_PAYMENT_ONLINE",
                "lineItemPrice": {
                  "amount": "29.99",
                  "formattedAmount": "$29.99"
                },
                "customLineItem": false,
                "taxInfo": {
                  "taxAmount": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  },
                  "taxableAmount": {
                    "amount": "29.99",
                    "formattedAmount": "$29.99"
                  },
                  "taxRate": "0",
                  "taxIncludedInPrice": false,
                  "exemptAmount": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  }
                },
                "rootCatalogItemId": "b87b5f05-b35e-4292-9c5c-dae947a5130c",
                "taxableAddress": {
                  "addressType": "SHIPPING"
                },
                "inventoryAppId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
                "priceUndetermined": false,
                "fixedQuantity": false,
                "priceBeforeDiscountsAndTax": {
                  "amount": "29.99",
                  "formattedAmount": "$29.99"
                }
              },
              "changedDetails": {
                "quantityBeforeChange": 2
              }
            },
            {
              "lineItem": {
                "id": "00000000-0000-0000-0000-000000000002",
                "productName": {
                  "original": "Gift wrapping"
                },
                "quantity": 1,
                "totalDiscount": {
                  "amount": "0.00",
                  "formattedAmount": "$0.00"
                },
                "priceBeforeDiscounts": {
                  "amount": "5.00",
                  "formattedAmount": "$5.00"
                },
                "price": {
                  "amount": "5.00",
                  "formattedAmount": "$5.00"
                },
                "taxDetails": {
                  "taxableAmount": {
                    "amount": "5.00",
                    "formattedAmount": "$5.00"
                  },
                  "taxRate": "0",
                  "totalTax": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  },
                  "exemptAmount": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  }
                },
                "totalPriceBeforeTax": {
                  "amount": "5.00",
                  "formattedAmount": "$5.00"
                },
                "totalPriceAfterTax": {
                  "amount": "5.00",
                  "formattedAmount": "$5.00"
                },
                "paymentOption": "FULL_PAYMENT_ONLINE",
                "lineItemPrice": {
                  "amount": "5.00",
                  "formattedAmount": "$5.00"
                },
                "customLineItem": true,
                "taxInfo": {
                  "taxAmount": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  },
                  "taxableAmount": {
                    "amount": "5.00",
                    "formattedAmount": "$5.00"
                  },
                  "taxRate": "0",
                  "taxIncludedInPrice": false,
                  "exemptAmount": {
                    "amount": "0",
                    "formattedAmount": "$0.00"
                  }
                },
                "taxableAddress": {
                  "addressType": "BILLING"
                },
                "priceUndetermined": false,
                "fixedQuantity": false,
                "priceBeforeDiscountsAndTax": {
                  "amount": "5.00",
                  "formattedAmount": "$5.00"
                }
              }
            }
          ],
          "createdDate": "2026-07-26T11:25:53.625Z",
          "updatedDate": "2026-07-26T11:26:16.573Z",
          "createdBy": {
            "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438"
          },
          "status": "COMMITTED",
          "weightUnit": "KG",
          "currency": "USD",
          "priceSummary": {
            "subtotal": {
              "amount": "34.99",
              "formattedAmount": "$34.99"
            },
            "shipping": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "tax": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "discount": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "totalPrice": {
              "amount": "34.99",
              "formattedAmount": "$34.99"
            },
            "totalWithGiftCard": {
              "amount": "34.99",
              "formattedAmount": "$34.99"
            },
            "totalWithoutGiftCard": {
              "amount": "34.99",
              "formattedAmount": "$34.99"
            },
            "totalAdditionalFees": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "total": {
              "amount": "34.99",
              "formattedAmount": "$34.99"
            }
          },
          "balanceSummary": {
            "balance": {
              "amount": "34.99",
              "formattedAmount": "$34.99"
            },
            "paid": {
              "amount": "0",
              "formattedAmount": "$0.00"
            },
            "refunded": {
              "amount": "0",
              "formattedAmount": "$0.00"
            },
            "authorized": {
              "amount": "0",
              "formattedAmount": "$0.00"
            },
            "platformFees": {
              "amount": "0",
              "formattedAmount": "$0.00"
            },
            "totalMinusPlatformFees": {
              "amount": "34.99",
              "formattedAmount": "$34.99"
            },
            "cashRoundingAdjustment": {
              "amount": "0",
              "formattedAmount": "$0.00"
            }
          },
          "commitSettings": {
            "sendNotificationsToBuyer": false,
            "sendNotificationsToBusiness": false
          },
          "recipientInfo": {
            "recipientInfo": {
              "address": {
                "country": "US",
                "subdivision": "US-NY",
                "city": "New York",
                "postalCode": "10173",
                "addressLine": "525 5th Ave"
              },
              "contactDetails": {
                "firstName": "John",
                "lastName": "Doe",
                "phone": "0555555555"
              }
            }
          },
          "taxInfo": {
            "totalTax": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "taxExempt": true,
            "totalExempt": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            }
          },
          "buyerDetails": {
            "buyerInfo": {
              "contactId": "e714c37e-5fc3-4fc7-a68a-6e469a41ee27",
              "email": "john.doe@example.com"
            }
          },
          "billingDetails": {
            "billingInfo": {
              "address": {
                "country": "US",
                "subdivision": "US-NY",
                "city": "New York",
                "postalCode": "10173",
                "addressLine": "525 5th Ave"
              },
              "contactDetails": {
                "firstName": "John",
                "lastName": "Doe"
              }
            }
          },
          "businessLocationDetails": {
            "businessLocation": {
              "id": "535aeee2-122a-4c01-9e48-a22ff330fd73",
              "name": "Main Store"
            }
          },
          "platformFeeSummary": {
            "total": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "totalPassOn": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "totalAbsorbed": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            }
          },
          "extendedFields": {
            "namespaces": {}
          }
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Draft order ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="orderId" type="string">

      ID of the order this draft commits to.

    </ResponseField>

    <ResponseField name="lineItems" type="ItemDetails[]">

      Line items.
      Includes details about changes when relevant.

      Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="lineItems properties">

        <ResponseField name="lineItem" type="OrderLineItem">

          Line item details after applied changes.

          <Expandable title="lineItem properties">

            <ResponseField name="_id" type="string">

              Line item ID.

              Constraints: `immutable`.

            </ResponseField>

            <ResponseField name="productName" type="ProductName">

              Item name.

              + Stores - `product.name`
              + Bookings - `service.info.name`
              + Events - `ticket.name`

              <Expandable title="productName properties">

                <ResponseField name="original" type="string">

                  __Required.__ Item name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Item name translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="catalogReference" type="CatalogReference">

              References to the line item's origin catalog. Empty for custom line items.

              <Expandable title="catalogReference properties">

                <ResponseField name="catalogItemId" type="string">

                  ID of the item within the catalog it belongs to.

                  Constraints: `minLength 1`, `maxLength 36`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  ID of the app providing the catalog.

                  You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

                  For items from Wix catalogs, the following values always apply:
                  + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
                  + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
                  + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

                  Constraints: `minLength 1`.

                </ResponseField>

                <ResponseField name="options" type="object">

                  Additional item details in `key:value` pairs.

                  Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

                  For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
                  or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="quantity" type="integer">

              Line item quantity.

              Constraints: `min 1`, `max 100000`.

            </ResponseField>

            <ResponseField name="totalDiscount" type="Price">

              Total discount for this line item's entire quantity.

              **Read-only.**

              <Expandable title="totalDiscount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="descriptionLines" type="DescriptionLine[]">

              Line item description lines. Used for display purposes for the cart, checkout and order.

              Constraints: `minItems 0`, `maxItems 20`.

              <Expandable title="descriptionLines properties">

                <ResponseField name="name" type="DescriptionLineName">

                  Description line name.

                  _Nested structure omitted for brevity — see the `DescriptionLineName` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="image" type="string">

              Line item image.

            </ResponseField>

            <ResponseField name="physicalProperties" type="PhysicalProperties">

              Physical properties of the item. When relevant, contains information such as SKU and item weight.

              <Expandable title="physicalProperties properties">

                <ResponseField name="weight" type="number">

                  Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request.

                </ResponseField>

                <ResponseField name="sku" type="string">

                  Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).

                </ResponseField>

                <ResponseField name="shippable" type="boolean">

                  Whether this line item is shippable.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="itemType" type="ItemType">

              Item type. Either a preset type or custom.

              <Expandable title="One of the following">

                <ResponseField name="itemTypeData" type="itemTypeData">

                  itemTypeData

                  _Nested structure omitted for brevity — see the `itemTypeData` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="fulfillerId" type="string">

              Fulfiller ID. Field is empty when the line item is self-fulfilled.
              To get fulfillment information, pass the order ID to [List Fulfillments For Single Order](https://dev.wix.com/docs/rest/api-reference/wix-e-commerce/order-fulfillments/list-fulfillments-for-single-order).

            </ResponseField>

            <ResponseField name="refundQuantity" type="integer">

              Number of items that were refunded.

            </ResponseField>

            <ResponseField name="restockQuantity" type="integer">

              Number of items restocked.

              For a per-location breakdown, see `restockLocations`.

            </ResponseField>

            <ResponseField name="price" type="Price">

              Line item price after line item discounts for display purposes.

              <Expandable title="price properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceBeforeDiscounts" type="Price">

              Line item price before line item discounts for display purposes. Defaults to `price` when not provided.

              **Read-only.**

              <Expandable title="priceBeforeDiscounts properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="totalPriceBeforeTax" type="Price">

              Total price after discounts, and before tax.

              **Read-only.**

              <Expandable title="totalPriceBeforeTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="totalPriceAfterTax" type="Price">

              Total price after all discounts and tax.

              **Read-only.**

              <Expandable title="totalPriceAfterTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="paymentOption" type="string">

              Type of selected payment option for current item.

              Default: `FULL_PAYMENT_ONLINE`

              **Supported values:**

              - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
              - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
              - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
              - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
              - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
              - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

            </ResponseField>

            <ResponseField name="taxDetails" type="ItemTaxFullDetails" deprecated>

              Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024.
              Tax details for this line item.

              **Deprecated.** Deprecated. Use `taxInfo` instead.
              This field will be removed on September 30, 2024.
              Tax details for this line item. Use `tax_info` instead. Removal: 2024-09-30.

              <Expandable title="taxDetails properties">

                <ResponseField name="taxableAmount" type="Price">

                  Taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate percentage, as a decimal numeral between 0 and 1. For example, `"0.13"`.

                  Constraints: `>= 0`.

                </ResponseField>

                <ResponseField name="totalTax" type="Price">

                  The calculated tax, based on the `taxableAmount` and `taxRate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxInfo" type="LineItemTaxInfo">

              Represents all the relevant tax details for a specific line item.

              <Expandable title="taxInfo properties">

                <ResponseField name="taxAmount" type="Price">

                  Calculated tax, based on `taxable_amount` and `tax_rate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="Price">

                  Amount for which tax is calculated.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate %, as a decimal point.

                </ResponseField>

                <ResponseField name="taxGroupId" type="string">

                  Tax group ID.

                </ResponseField>

                <ResponseField name="taxIncludedInPrice" type="boolean">

                  Indicates whether the price already includes tax.

                </ResponseField>

                <ResponseField name="taxBreakdown" type="LineItemTaxBreakdown[]">

                  Tax information for a line item.

                  Constraints: `maxItems 7`.

                  _Nested structure omitted for brevity — see the `LineItemTaxBreakdown[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="digitalFile" type="DigitalFile">

              Digital file identifier, relevant only for items with type DIGITAL.

              <Expandable title="digitalFile properties">

                <ResponseField name="fileId" type="string">

                  ID of the secure file in media.

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

                <ResponseField name="link" type="string">

                  Link will exist after the digital links have been generated on the order.

                </ResponseField>

                <ResponseField name="expirationDate" type="datetime">

                  Link expiration time and date.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="subscriptionInfo" type="SubscriptionInfo">

              Subscription info.

              <Expandable title="subscriptionInfo properties">

                <ResponseField name="_id" type="string">

                  Subscription ID.

                </ResponseField>

                <ResponseField name="cycleNumber" type="integer">

                  Subscription cycle. For example, if this order is for the 3rd cycle of a subscription, value will be `3`.

                  Constraints: `min 0`.

                </ResponseField>

                <ResponseField name="subscriptionOptionTitle" type="string" deprecated>

                  Subscription option title. For example, `"Monthly coffee Subscription"`.

                  **Deprecated.** Subscription option title. For example, `"Monthly coffee Subscription"`. Use `title` instead. Removal: 2025-10-01. Constraints: `minLength 0`, `maxLength 150`.

                </ResponseField>

                <ResponseField name="subscriptionOptionDescription" type="string">

                  Subscription option description. For example, `"1kg of selected coffee, once a month"`.

                </ResponseField>

                <ResponseField name="subscriptionSettings" type="SubscriptionSettings">

                  Subscription detailed information.

                  Constraints: `immutable`.

                  _Nested structure omitted for brevity — see the `SubscriptionSettings` object._

                </ResponseField>

                <ResponseField name="chargesDescription" type="string">

                  Description of the charges that will be applied for subscription.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceDescription" type="PriceDescription">

              Additional description for the price. For example, when price is 0 but additional details about the actual price are needed - "Starts at $67".

              <Expandable title="priceDescription properties">

                <ResponseField name="original" type="string">

                  __Required.__ Price description in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Constraints: `minLength 1`, `maxLength 100`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  Price description translated into the buyer's language.

                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="depositAmount" type="Price">

              Item's price amount to be charged during checkout. Relevant for items with a `paymentOption` value of `"DEPOSIT_ONLINE"`.

              **Read-only.**

              <Expandable title="depositAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="locations" type="LocationAndQuantity[]">

              Line item locations.

              The location's total quantity must not exceed the line item quantity.

              **Developer Preview.** Constraints: `minItems 0`, `maxItems 5`.

              <Expandable title="locations properties">

                <ResponseField name="_id" type="string">

                  Location id in the associated owner app.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="appId" type="string">

                  Location owner app, if not provided then the site business info locations will be used.

                </ResponseField>

                <ResponseField name="quantity" type="integer">

                  Quantity for specific location.

                  Constraints: `min 1`, `max 100000`.

                </ResponseField>

                <ResponseField name="name" type="string">

                  Location name.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="lineItemPrice" type="Price">

              Total price **after** catalog discounts and line item discounts.

              <Expandable title="lineItemPrice properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxableAddress" type="TaxableAddress">

              Address used for tax calculation.

              <Expandable title="One of the following">

                <ResponseField name="TaxableAddressData" type="TaxableAddressData">

                  TaxableAddressData

                  _Nested structure omitted for brevity — see the `TaxableAddressData` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceUndetermined" type="boolean">

              Whether the price is not yet defined, and will be updated after the order is created.

              Default: `false`

            </ResponseField>

            <ResponseField name="fixedQuantity" type="boolean">

              Whether the line item quantity is fixed and cannot be changed.

              Default: `false`

              **Developer Preview.**

            </ResponseField>

            <ResponseField name="decimalQuantity" type="DecimalQuantity">

              Decimal-quantity metadata. Set when the line represents a
              non-integer purchase (e.g. 2.75 m² of fake grass).

              When set, the line item must have `fixedQuantity` = true and be set with `quantity` = 1.
              The decimal value lives in `decimalQuantity.value`.

              **Developer Preview.**

              <Expandable title="decimalQuantity properties">

                <ResponseField name="value" type="string">

                  The decimal quantity the buyer / merchant entered.

                  String-encoded decimal. The exact value is preserved — no
                  rounding, no conversion to an integer.

                  Examples: "2.75", "1.5", "0.75".

                  **Developer Preview.** Constraints: `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="description" type="TranslatableString">

                  Human-readable, pre-formatted display string for the quantity. For example, "2.75 square meters of fake grass".
                  Always set alongside `value`; use this directly for display rather than composing your own string from `value` and `unit`.

                  **Developer Preview.**

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="unit" type="unit">

                  unit

                  _Nested structure omitted for brevity — see the `unit` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="extendedFields" type="ExtendedFields">

              Custom extended fields for the line item object.

              [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.

              **Developer Preview.**

              <Expandable title="extendedFields properties">

                <ResponseField name="namespaces" type="object">

                  Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
                  The value of each key is structured according to the schema defined when the extended fields were configured.

                  You can only access fields for which you have the appropriate permissions.

                  Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

                  Constraints: `format map`.

                  _Nested structure omitted for brevity — see the `object` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="modifierGroups" type="ModifierGroup[]">

              Modifier groups that were added to the item.

              **Read-only.** **Developer Preview.** Constraints: `maxItems 10`.

              <Expandable title="modifierGroups properties">

                <ResponseField name="_id" type="string">

                  Modifier group ID.

                  Constraints: `minLength 1`, `maxLength 36`.

                </ResponseField>

                <ResponseField name="name" type="TranslatableString">

                  Modifier group name.

                  _Nested structure omitted for brevity — see the `TranslatableString` object._

                </ResponseField>

                <ResponseField name="modifiers" type="ItemModifier[]">

                  List of modifiers in this group.

                  Constraints: `minItems 1`, `maxItems 10`.

                  _Nested structure omitted for brevity — see the `ItemModifier[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="shippingInfo" type="ShippingDetails">

      Shipping info.
      Includes details about changes when relevant.

      <Expandable title="shippingInfo properties">

        <ResponseField name="shippingInfo" type="ShippingInformation">

          Selected shipping details.

          <Expandable title="shippingInfo properties">

            <ResponseField name="carrierId" type="string">

              App Def Id of external provider which was a source of shipping info

            </ResponseField>

            <ResponseField name="code" type="string">

              Unique code (or ID) of selected shipping option. For example, `"usps_std_overnight"`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Shipping option title.
              For example, `"USPS Standard Overnight Delivery"`, `"Standard"` or `"First-Class Package International"`.

              Constraints: `maxLength 250`.

            </ResponseField>

            <ResponseField name="logistics" type="DeliveryLogistics">

              Shipping logistics.

              <Expandable title="logistics properties">

                <ResponseField name="deliveryTime" type="string">

                  Expected delivery time in free text. For example, `"3-5 business days"`.

                </ResponseField>

                <ResponseField name="instructions" type="string">

                  Instructions for the carrier. For example, `"Please knock on the door. If unanswered, please call contact number."`.

                </ResponseField>

                <ResponseField name="deliverByDate" type="datetime">

                  Deprecated - Latest expected delivery date and time in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.

                </ResponseField>

                <ResponseField name="deliveryTimeSlot" type="DeliveryTimeSlot">

                  Expected delivery time slot with start and end times.

                  _Nested structure omitted for brevity — see the `DeliveryTimeSlot` object._

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="Address" type="Address">

                  Address

                  _Nested structure omitted for brevity — see the `Address` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="cost" type="ShippingPrice">

              Shipping costs.

              <Expandable title="cost properties">

                <ResponseField name="price" type="Price">

                  Shipping price for display purposes.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="totalPriceBeforeTax" type="Price">

                  Total price of shipping after discounts (when relevant), and before tax.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="totalPriceAfterTax" type="Price">

                  Shipping price after all discounts (if any exist), and after tax.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxDetails" type="ItemTaxFullDetails" deprecated>

                  Tax details.

                  **Deprecated.** Tax details. Use `tax_info` instead. Removal: 2026-03-30.

                  _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

                </ResponseField>

                <ResponseField name="taxInfo" type="LineItemTaxInfo">

                  Represents all the relevant tax details for a shipping.

                  _Nested structure omitted for brevity — see the `LineItemTaxInfo` object._

                </ResponseField>

                <ResponseField name="discount" type="Price">

                  Shipping discount before tax.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="region" type="ShippingRegion">

              Shipping region.

              <Expandable title="region properties">

                <ResponseField name="name" type="string">

                  Name of shipping region. For example, `"Metropolitan London"`, or `"Outer Melbourne suburbs"`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Whether shipping info was added as part of the draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="ShippingChangedDetails">

              Shipping info before the change.

              <Expandable title="changedDetails properties">

                <ResponseField name="shippingInfoBeforeChange" type="ShippingInformation">

                  Shipping info before the change.

                  _Nested structure omitted for brevity — see the `ShippingInformation` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Whether shipping info was removed as part of the draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="buyerInfo" type="BuyerInfo" deprecated>

      Buyer information.

      **Deprecated.** Buyer information. Use `buyer_details` instead. Removal: 2024-08-30.

      <Expandable title="buyerInfo properties">

        <ResponseField name="contactId" type="string">

          Contact ID. Automatically created if one doesn't exist. For more information, see [Contacts API](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts/contact-v4/introduction).

        </ResponseField>

        <ResponseField name="email" type="string">

          Buyer's email address.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="visitorId" type="string">

              Visitor ID. Returned when the buyer isn't a logged-in site member.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="memberId" type="string">

              Member ID. Returned when the buyer is a logged-in site member.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="billingInfo" type="AddressWithContact" deprecated>

      Billing address and contact details.

      **Deprecated.** Billing address and contact details. Use `billing_details` instead. Removal: 2024-08-30.

      <Expandable title="billingInfo properties">

        <ResponseField name="address" type="Address">

          Address.

          <Expandable title="address properties">

            <ResponseField name="streetAddress" type="StreetAddress">

              streetAddress

              <Expandable title="streetAddress properties">

                <ResponseField name="name" type="string" />

                <ResponseField name="number" type="string" />

              </Expandable>

            </ResponseField>

            <ResponseField name="city" type="string" />

            <ResponseField name="subdivision" type="string" />

            <ResponseField name="country" type="string" />

            <ResponseField name="postalCode" type="string" />

            <ResponseField name="addressLine2" type="string" />

          </Expandable>

        </ResponseField>

        <ResponseField name="contactDetails" type="FullAddressContactDetails">

          Contact details.

          <Expandable title="contactDetails properties">

            <ResponseField name="firstName" type="string">

              First name.

            </ResponseField>

            <ResponseField name="lastName" type="string">

              Last name.

            </ResponseField>

            <ResponseField name="phone" type="string">

              Phone number.

            </ResponseField>

            <ResponseField name="company" type="string">

              Company name.

            </ResponseField>

            <ResponseField name="vatId" type="VatId">

              Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

              <Expandable title="vatId properties">

                <ResponseField name="_id" type="string">

                  Customer's tax ID.

                </ResponseField>

                <ResponseField name="type" type="string">

                  Tax type.

                  Supported values:
                  + `CPF`: for individual tax payers
                  + `CNPJ`: for corporations

                  **Supported values:**

                  - `UNSPECIFIED`
                  - `CPF` — CPF - for individual tax payers.
                  - `CNPJ` — CNPJ - for corporations

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalFees" type="AdditionalFeeDetails[]">

      Additional fees.

      Includes details about the source, and whether the additional fee is applied to the order.

      Constraints: `maxItems 100`.

      <Expandable title="additionalFees properties">

        <ResponseField name="additionalFee" type="AdditionalFee">

          Additional fee.

          <Expandable title="additionalFee properties">

            <ResponseField name="code" type="string">

              Additional fee's unique code for future processing.

            </ResponseField>

            <ResponseField name="name" type="string">

              Name of additional fee.

              Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="price" type="Price">

              Additional fee's price.

              <Expandable title="price properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxDetails" type="ItemTaxFullDetails" deprecated>

              Tax details.

              **Deprecated.** Tax details. Use `tax_info` instead. Removal: 2026-03-30.

              <Expandable title="taxDetails properties">

                <ResponseField name="taxableAmount" type="Price">

                  Taxable amount of this line item.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate percentage, as a decimal numeral between 0 and 1. For example, `"0.13"`.

                  Constraints: `>= 0`.

                </ResponseField>

                <ResponseField name="totalTax" type="Price">

                  The calculated tax, based on the `taxableAmount` and `taxRate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="taxInfo" type="LineItemTaxInfo">

              Represents all the relevant tax details for additional fee.

              <Expandable title="taxInfo properties">

                <ResponseField name="taxAmount" type="Price">

                  Calculated tax, based on `taxable_amount` and `tax_rate`.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxableAmount" type="Price">

                  Amount for which tax is calculated.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

                <ResponseField name="taxRate" type="string">

                  Tax rate %, as a decimal point.

                </ResponseField>

                <ResponseField name="taxGroupId" type="string">

                  Tax group ID.

                </ResponseField>

                <ResponseField name="taxIncludedInPrice" type="boolean">

                  Indicates whether the price already includes tax.

                </ResponseField>

                <ResponseField name="taxBreakdown" type="LineItemTaxBreakdown[]">

                  Tax information for a line item.

                  Constraints: `maxItems 7`.

                  _Nested structure omitted for brevity — see the `LineItemTaxBreakdown[]` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="providerAppId" type="string">

              SPI implementer's `appId`.

            </ResponseField>

            <ResponseField name="priceBeforeTax" type="Price">

              Additional fee's price before tax.

              <Expandable title="priceBeforeTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="priceAfterTax" type="Price">

              Additional fee's price after tax.

              <Expandable title="priceAfterTax properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="_id" type="string">

              Additional fee's id.

              Constraints: `format GUID`, `immutable`.

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              Optional - Line items associated with this additional fee.
              If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="source" type="string">

              Specifies the entity that added the additional fee.

              **Developer Preview.**

              **Supported values:**

              - `SERVICE_PLUGIN` — The additional fee was added by an additional fee service plugin.
              - `ITEM` — The additional fee was added to the item by a catalog or custom line item.
              - `MANUAL` — The additional fee was added manually on request.
              - `SHIPPING` — The additional fee was added by the shipping provider.
              - `PLATFORM` — The additional fee was added by the Wix eCommerce platform.

            </ResponseField>

            <ResponseField name="taxableAddress" type="TaxableAddress">

              Address type tax was calculated against for this fee.

              **Developer Preview.**

              <Expandable title="One of the following">

                <ResponseField name="TaxableAddressData" type="TaxableAddressData">

                  TaxableAddressData

                  _Nested structure omitted for brevity — see the `TaxableAddressData` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="TaxDetails">

          Tax details related to the additional fee.

          <Expandable title="taxDetails properties">

            <ResponseField name="taxable" type="boolean">

              Whether the additional fee is taxable.

            </ResponseField>

            <ResponseField name="taxGroupId" type="string">

              Optional tax group ID to use when calculating tax for this additional fee.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="source" type="string">

          Source of the additional fee.

          Supported values:
          + `ORIGINAL_ORDER`: This additional fee is part of the original order.
          + `AUTOMATIC`: User added a catalog item which is configured to have an additional fee.
          + `CUSTOM`: This additional fee was created by the Create Custom Additional Fees method.

          **Supported values:**

          - `ORIGINAL_ORDER` — Part of the original order.
          - `AUTOMATIC` — Added automatically, as the result of recalculation caused by changes in draft order.
          - `CUSTOM` — Added manually to draft order.

        </ResponseField>

        <ResponseField name="applied" type="boolean">

          Whether this additional fee is applied to the order.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="discounts" type="DiscountDetails[]">

      Discounts.

      Includes details about the source, and whether the discount is applied to the order.

      Constraints: `maxItems 320`.

      <Expandable title="discounts properties">

        <ResponseField name="discount" type="AppliedDiscount">

          Discount details.

          <Expandable title="discount properties">

            <ResponseField name="discountType" type="string">

              Type of discount.

              **Supported values:**

              - `GLOBAL` — Discount applies to the entire order.
              - `SPECIFIC_ITEMS` — Discount applies to specific items.
              - `SHIPPING` — Discount applies to shipping. For example, free shipping.

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]" deprecated>

              __Deprecated.__ Use `lineItemDiscounts` instead.

              IDs of line items the discount applies to.

              **Deprecated.** __Deprecated.__ Use `lineItemDiscounts` instead.

              IDs of line items the discount applies to. Use `line_item_discounts` instead. Removal: 2024-10-30. Constraints: `format GUID`, `minItems 0`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="_id" type="string">

              Discount ID.

            </ResponseField>

            <ResponseField name="lineItemDiscounts" type="LineItemDiscount[]">

              Line items the discount applies to, including the discount amount for each.

              **Developer Preview.** Constraints: `maxItems 300`.

              <Expandable title="lineItemDiscounts properties">

                <ResponseField name="_id" type="string">

                  Line item ID.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="totalDiscount" type="Price">

                  Total discount amount for this line item.

                  _Nested structure omitted for brevity — see the `Price` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="DiscountSource" type="DiscountSource">

              DiscountSource

              <Expandable title="DiscountSource properties">

                <ResponseField name="coupon" type="Coupon">

                  Coupon applied by the customer.

                  _Nested structure omitted for brevity — see the `Coupon` object._

                </ResponseField>

                <ResponseField name="merchantDiscount" type="MerchantDiscount">

                  Discount applied manually by the merchant.

                  _Nested structure omitted for brevity — see the `MerchantDiscount` object._

                </ResponseField>

                <ResponseField name="discountRule" type="DiscountRule">

                  Automatic discount applied by the system based on configured discount rules.

                  _Nested structure omitted for brevity — see the `DiscountRule` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="source" type="string">

          Source of the discount.

          Supported values:
          + `ORIGINAL_ORDER`: This discount is part of the original order.
          + `CUSTOM`: This discount was created by the Create Custom Discounts method.

          **Supported values:**

          - `ORIGINAL_ORDER` — Part of the original order.
          - `AUTOMATIC` — Added automatically, as the result of recalculation caused by changes in draft order.
          - `CUSTOM` — Added manually to draft order.

        </ResponseField>

        <ResponseField name="applied" type="boolean">

          Whether this discount is applied to the order.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the draft order was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time the draft order was updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdBy" type="CreatedBy">

      Identity of the draft order's initiator.

      **Read-only.**

      <Expandable title="One of the following">

        <ResponseField name="string" type="string">

          string

          <Expandable title="string properties">

            <ResponseField name="userId" type="string">

              User ID. When the draft order was created by a Wix user.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              App ID. When the draft order was created by an app.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="status" type="string">

      Draft order status.

      **Read-only.**

      **Supported values:**

      - `DRAFT` — Draft order is being edited.
      - `COMMITTED` — Draft order has been committed - No further modifications allowed.

      Draft orders with status 'COMMITTED' cannot be deleted.

    </ResponseField>

    <ResponseField name="weightUnit" type="string">

      Weight measurement unit. Defaults to the site's weight unit.

      **Supported values:**

      - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
      - `KG` — Kilograms.
      - `LB` — Pounds.

    </ResponseField>

    <ResponseField name="currency" type="string">

      Currency used for pricing.

    </ResponseField>

    <ResponseField name="priceSummary" type="PriceSummary">

      Price summary.

      **Read-only.**

      <Expandable title="priceSummary properties">

        <ResponseField name="subtotal" type="Price">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="Price">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="Price">

          Total tax applied to the order.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="Price">

          Total discount amount applied to the order.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="Price">

          Order's total price after discounts and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalAdditionalFees" type="Price">

          Total additional fees before tax.

          <Expandable title="totalAdditionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="taxSummary" type="TaxSummary" deprecated>

      Deprecated. Use `taxInfo` instead.
      This field will be removed on September 30, 2024.
      Tax summary.

      **Read-only.** **Deprecated.** Deprecated. Use `taxInfo` instead.
      This field will be removed on September 30, 2024.
      Tax summary. Use `tax_info` instead. Removal: 2024-09-30.

      <Expandable title="taxSummary properties">

        <ResponseField name="totalTax" type="Price">

          Total tax.

          **Read-only.**

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="balanceSummary" type="BalanceSummary">

      Balance summary.

      **Read-only.**

      <Expandable title="balanceSummary properties">

        <ResponseField name="balance" type="Balance">

          Amount left to pay for the order to be considered paid.

          **Read-only.**

          <Expandable title="balance properties">

            <ResponseField name="amount" type="string">

              Balance amount.

              A negative `amount` represents the amount to be refunded. This can happen due to overcharging or the order being modified after a payment has been made.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paid" type="Price">

          Sum of all approved and successful payments.

          The value includes payments that have subsequently been fully or partially refunded.

          **Read-only.**

          <Expandable title="paid properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="refunded" type="Price">

          Sum of all successfully refunded payments.

          **Read-only.**

          <Expandable title="refunded properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="authorized" type="Price">

          Sum of all authorized payments.

          **Read-only.**

          <Expandable title="authorized properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="platformFees" type="Price">

          Total platform fees charged across all payments.
          This reflects amounts collected, which may differ from the expected fees in `draftOrder.platformFeeSummary`.

          **Read-only.** **Developer Preview.**

          <Expandable title="platformFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalMinusPlatformFees" type="Price">

          Order total after deducting platform fees. Represents the net amount payable to the merchant.

          **Read-only.** **Developer Preview.**

          <Expandable title="totalMinusPlatformFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="cashRoundingAdjustment" type="CashRounding">

          The total adjustment applied to the cash payment due to rounding.

          Positive values indicate the total was rounded up; negative values indicate it was rounded down.

          **Read-only.** **Developer Preview.**

          <Expandable title="cashRoundingAdjustment properties">

            <ResponseField name="amount" type="string">

              Amount, can be negative or positive.

              Constraints: `>= -1000000000000000`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="commitSettings" type="DraftOrderCommitSettings">

      Side effects to perform on commit.

      <Expandable title="commitSettings properties">

        <ResponseField name="sendNotificationsToBuyer" type="boolean">

          Whether to send notifications to the buyer.

          Default: `true`

        </ResponseField>

        <ResponseField name="sendNotificationsToBusiness" type="boolean">

          Whether to send notifications to the business.

          Default: `true`

        </ResponseField>

        <ResponseField name="addActivitiesToOrder" type="boolean">

          Whether to add activities to the order.

          Default: `true`

        </ResponseField>

        <ResponseField name="sendNotificationsToCustomFulfillers" type="boolean">

          Whether to send email notifications to custom fulfillers when there is a change in the shippable items they fulfill.

          Default: `true`

        </ResponseField>

        <ResponseField name="updateInventory" type="InventoryUpdate[]">

          Line item IDs for which to perform an inventory change.
          When not provided, inventory will not change.

          Constraints: `maxItems 300`.

          <Expandable title="updateInventory properties">

            <ResponseField name="lineItemId" type="string">

              Line item ID.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="memberId" type="string">

      Member ID (if order created by a member).

    </ResponseField>

    <ResponseField name="purchaseFlowId" type="string">

      Persistent ID that correlates between the various eCommerce elements: cart, checkout, and order.

    </ResponseField>

    <ResponseField name="recipientInfo" type="RecipientInfoDetails">

      Order recipient address and contact details.

      This field may differ from the address in `shippingInfo.logistics` when:
      + The chosen shipping option is pickup point or store pickup.
      + No shipping option is selected.

      <Expandable title="recipientInfo properties">

        <ResponseField name="recipientInfo" type="AddressWithContact">

          Order recipient address and contact details.

          <Expandable title="recipientInfo properties">

            <ResponseField name="address" type="Address">

              Address.

              <Expandable title="address properties">

                <ResponseField name="streetAddress" type="StreetAddress">

                  streetAddress

                  _Nested structure omitted for brevity — see the `StreetAddress` object._

                </ResponseField>

                <ResponseField name="city" type="string" />

                <ResponseField name="subdivision" type="string" />

                <ResponseField name="country" type="string" />

                <ResponseField name="postalCode" type="string" />

                <ResponseField name="addressLine2" type="string" />

              </Expandable>

            </ResponseField>

            <ResponseField name="contactDetails" type="FullAddressContactDetails">

              Contact details.

              <Expandable title="contactDetails properties">

                <ResponseField name="firstName" type="string">

                  First name.

                </ResponseField>

                <ResponseField name="lastName" type="string">

                  Last name.

                </ResponseField>

                <ResponseField name="phone" type="string">

                  Phone number.

                </ResponseField>

                <ResponseField name="company" type="string">

                  Company name.

                </ResponseField>

                <ResponseField name="vatId" type="VatId">

                  Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

                  _Nested structure omitted for brevity — see the `VatId` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that recipient information was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="RecipientInfoChangedDetails">

              Indicates that recipient information was changed. Contains recipient information before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="recipientInfoBeforeChange" type="AddressWithContact">

                  Order recipient address and contact details before draft.

                  _Nested structure omitted for brevity — see the `AddressWithContact` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that recipient information was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="taxInfo" type="OrderTaxInfo">

      Tax information.

      <Expandable title="taxInfo properties">

        <ResponseField name="totalTax" type="Price">

          Calculated tax, added from line items.

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxBreakdown" type="OrderTaxBreakdown[]">

          The summary of the tax breakdown for all the line items. It will hold for each tax name, the aggregated tax amount paid for it and the tax rate.

          Constraints: `minItems 0`, `maxItems 50`.

          <Expandable title="taxBreakdown properties">

            <ResponseField name="taxName" type="string">

              The name of the tax against which this tax amount was calculated.

              Constraints: `maxLength 200`.

            </ResponseField>

            <ResponseField name="taxType" type="string">

              The type of tax that was calculated. Depends on the company's nexus settings as well as the jurisdiction's tax laws.

              Constraints: `maxLength 200`.

            </ResponseField>

            <ResponseField name="jurisdiction" type="string">

              The name of the jurisdiction in which this tax detail applies.

              Constraints: `maxLength 200`.

            </ResponseField>

            <ResponseField name="jurisdictionType" type="string">

              The type of the jurisdiction in which this tax detail applies (e.g. Country,State,County,City,Special).

              **Supported values:**

              - `UNDEFINED`
              - `COUNTRY`
              - `STATE`
              - `COUNTY`
              - `CITY`
              - `SPECIAL`

            </ResponseField>

            <ResponseField name="rate" type="string">

              The rate at which this tax detail was calculated.

              Constraints: `format DECIMAL_VALUE`, `>= 0`.

            </ResponseField>

            <ResponseField name="aggregatedTaxAmount" type="Price">

              The sum of all the tax from line items that calculated by the tax identifiers.

              <Expandable title="aggregatedTaxAmount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxExempt" type="boolean">

          Whether the order is exempt from tax calculations.

          Default: `false`

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Custom field data for the draft order object.

      [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the [app dashboard](https://dev.wix.com/dc3/my-apps/) before they can be accessed with API calls.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="buyerDetails" type="BuyerDetails">

      Buyer information.

      Includes details about changes when relevant.

      <Expandable title="buyerDetails properties">

        <ResponseField name="buyerInfo" type="BuyerInfo">

          Buyer information.

          <Expandable title="buyerInfo properties">

            <ResponseField name="contactId" type="string">

              Contact ID. Automatically created if one doesn't exist. For more information, see [Contacts API](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts/contact-v4/introduction).

            </ResponseField>

            <ResponseField name="email" type="string">

              Buyer's email address.

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="id" type="id">

              id

              <Expandable title="id properties">

                <ResponseField name="visitorId" type="string">

                  Visitor ID. Returned when the buyer isn't a logged-in site member.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="memberId" type="string">

                  Member ID. Returned when the buyer is a logged-in site member.

                  Constraints: `format GUID`.

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that buyer information was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="BuyerChangedDetails">

              Indicates that buyer information was changed. Contains buyer information before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="buyerInfoBeforeChange" type="BuyerInfo">

                  Buyer information before draft.

                  _Nested structure omitted for brevity — see the `BuyerInfo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that buyer information was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="billingDetails" type="BillingDetails">

      Billing address and contact details.
      Includes details about changes when relevant.

      <Expandable title="billingDetails properties">

        <ResponseField name="billingInfo" type="AddressWithContact">

          Billing address and contact details.

          <Expandable title="billingInfo properties">

            <ResponseField name="address" type="Address">

              Address.

              <Expandable title="address properties">

                <ResponseField name="streetAddress" type="StreetAddress">

                  streetAddress

                  _Nested structure omitted for brevity — see the `StreetAddress` object._

                </ResponseField>

                <ResponseField name="city" type="string" />

                <ResponseField name="subdivision" type="string" />

                <ResponseField name="country" type="string" />

                <ResponseField name="postalCode" type="string" />

                <ResponseField name="addressLine2" type="string" />

              </Expandable>

            </ResponseField>

            <ResponseField name="contactDetails" type="FullAddressContactDetails">

              Contact details.

              <Expandable title="contactDetails properties">

                <ResponseField name="firstName" type="string">

                  First name.

                </ResponseField>

                <ResponseField name="lastName" type="string">

                  Last name.

                </ResponseField>

                <ResponseField name="phone" type="string">

                  Phone number.

                </ResponseField>

                <ResponseField name="company" type="string">

                  Company name.

                </ResponseField>

                <ResponseField name="vatId" type="VatId">

                  Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed.

                  _Nested structure omitted for brevity — see the `VatId` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that billing information was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="BillingChangedDetails">

              Indicates that billing information was changed. Contains billing information before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="billingInfoBeforeChange" type="AddressWithContact">

                  Billing address and contact details before draft.

                  _Nested structure omitted for brevity — see the `AddressWithContact` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that billing information was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="businessLocationDetails" type="BusinessLocationDetails">

      Order business location.

      Includes details about changes when relevant.

      <Expandable title="businessLocationDetails properties">

        <ResponseField name="businessLocation" type="Location">

          Business location.

          <Expandable title="businessLocation properties">

            <ResponseField name="_id" type="string">

              Location ID.
              Learn more about the [Wix Locations API](https://dev.wix.com/docs/rest/business-management/locations/introduction).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="string">

              Location name.

              **Read-only.** Constraints: `minLength 1`, `maxLength 500`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="ChangeType" type="ChangeType">

          ChangeType

          <Expandable title="ChangeType properties">

            <ResponseField name="added" type="boolean">

              Indicates that business location was added during draft.

            </ResponseField>

            <ResponseField name="changedDetails" type="BusinessLocationChangedDetails">

              Indicates that business location was changed. Contains business location before draft.

              <Expandable title="changedDetails properties">

                <ResponseField name="businessLocationBeforeChange" type="Location">

                  Business location before draft.

                  _Nested structure omitted for brevity — see the `Location` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="removed" type="boolean">

              Indicates that business location was removed during draft.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="platformFeeSummary" type="PlatformFeeSummary">

      Summary of platform fees for this draft order, including totals by charge type and a breakdown of individual fees.

      **Developer Preview.**

      <Expandable title="platformFeeSummary properties">

        <ResponseField name="total" type="Price">

          Total sum of all platform fees.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalPassOn" type="Price">

          Total amount of platform fees with `PASS_ON` charge type.

          <Expandable title="totalPassOn properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalAbsorbed" type="Price">

          Total amount of platform fees with `ABSORBED` charge type.

          <Expandable title="totalAbsorbed properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="fees" type="PlatformFee[]">

          Specific information about each platform fee.

          Constraints: `maxItems 300`.

          <Expandable title="fees properties">

            <ResponseField name="name" type="TranslatableString">

              Platform fee name.

              <Expandable title="name properties">

                <ResponseField name="original" type="string">

                  __Required.__ String in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

                  Min: 1 character.
                  Max: 200 characters.

                  Constraints: `minLength 1`, `maxLength 200`.

                </ResponseField>

                <ResponseField name="translated" type="string">

                  String translated into the buyer's language.

                  Min: 1 character.
                  Max: 400 characters.
                  Default: Same as `original`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="amount" type="Price">

              Platform fee amount.

              <Expandable title="amount properties">

                <ResponseField name="amount" type="string">

                  Amount.

                  Constraints: `>= 0`, `<= 1000000000000000`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Amount formatted with currency symbol.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="lineItemId" type="string">

              ID of the line item the platform fee applies to.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="chargeType" type="string">

              Platform fee charge type.

              **Supported values:**

              - `PASS_ON` — Platform fee passed on to buyer.

              This type increases the order total, and is visible to the buyer and merchant as an additional fee.
              - `ABSORBED` — Platform fee absorbed by merchant.

              This type does not increase the order total, and is only visible to the merchant.

            </ResponseField>

            <ResponseField name="percentageRate" type="string">

              Percentage rate charged as platform fee.
              The fee rate percentage expressed as a decimal fraction between 0 and 1. For example, `0.05` for 5%.

              Constraints: `format DECIMAL_VALUE`, `<= 1`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
