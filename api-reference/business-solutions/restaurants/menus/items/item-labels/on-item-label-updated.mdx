---
title: "onItemLabelUpdated"
description: "Triggered when an item label is updated."
icon: "bolt"
---

Triggered when an item label is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.menus.v1.item_label_updated` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onItemLabelUpdated
  import { itemLabels } from '@wix/restaurants';

  itemLabels.onItemLabelUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onItemLabelUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { itemLabels } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        itemLabels,
      },
  });

  wixClient.itemLabels.onItemLabelUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
