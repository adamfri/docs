---
title: "Group Role"
description: "A group member may have multiple roles in a single group."
icon: "book-open"
---

A group member may have multiple roles in a single group.
Currently, only `MEMBER` and `ADMIN` roles are supported, but more roles may be available in the future.

## SDK package

```bash
npm install @wix/groups
```

Import the `roles` namespace from `@wix/groups`.

## Methods

<CardGroup cols={2}>

  <Card title="Assign Role" href="/api-reference/crm/community/groups/member-management/roles/assign-role">
    POST Assigns a specific role to group members.
  </Card>
  <Card title="Unassign Role" href="/api-reference/crm/community/groups/member-management/roles/unassign-role">
    POST Unassigns a role from group members.
  </Card>

</CardGroup>
