---
title: "onOperationCreated"
description: "Triggered when an operation is created."
icon: "bolt"
---

Triggered when an operation is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.operations.v1.operation_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onOperationCreated
  import { operations } from '@wix/restaurants';

  operations.onOperationCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOperationCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { operations } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        operations,
      },
  });

  wixClient.operations.onOperationCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
