---
title: "Get Project Item"
description: "Retrieves a project item."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a project item.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/portfolio/v1/items/{itemId}
    ```

    ## Path Params

    <ParamField path="itemId" type="string" required>

      Project item ID.

    </ParamField>

    ## Response Object

    <ResponseField name="item" type="Item">

      Project item.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/portfolio/v1/Items/456e7890-e89b-12d3-a456-426614174001' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "item": {
          "id": "456e7890-e89b-12d3-a456-426614174001",
          "projectId": "123e4567-e89b-12d3-a456-426614174000",
          "title": "Item Title 1",
          "description": "This is item description 1.",
          "sortOrder": 1.0,
          "type": "IMAGE",
          "image": {
            "imageInfo": {
              "url": "https://example.com/image1.jpg",
              "id": "3886bbcc7fb-66b77-888aaf-3c45227f"
            }
          },
          "createdDate": "2024-07-31T12:00:00Z",
          "updatedDate": "2024-07-31T12:00:00Z",
          "link": {
            "text": "Visit Site 1",
            "url": "https://example.com",
            "target": "_blank"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a project item.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { projectItems } from '@wix/portfolio';

    projectItems.getProjectItem(itemId): Promise<Item>
    ```

    ## Method Parameters

    <ParamField body="itemId" type="string" required>

      Project item ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Item>`.

    See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-project-item" id="ex-sdk-get-project-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-project-item" id="ex-sdk-get-project-item-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-project-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-project-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getProjectItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getProjectItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-project-item-0"><span className="wix-picker-name">getProjectItem</span></label>
    <label htmlFor="ex-sdk-get-project-item-1"><span className="wix-picker-name">getProjectItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getProjectItem
    import { projectItems } from '@wix/portfolio';

    async function getProjectItem(itemId) {
      const response = await projectItems.getProjectItem(itemId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getProjectItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { projectItems } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { projectItems },
      // Include the auth strategy and host as relevant
    });

    async function getProjectItem(itemId) {
      const response = await myWixClient.projectItems.getProjectItem(itemId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
