---
title: "End Program"
description: "Ends the specified program."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Ends the specified program.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/benefit-programs/v1/programs/end
    ```

    ## Body Params

    <ParamField body="programId" type="string" required>

      ID of the program to end.

      Constraints: `format GUID`.

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID of the ending of the program. Retrieve job details using the [Async Job API](https://dev.wix.com/docs/rest/business-management/async-job/introduction).

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PROGRAM_STATUS_TRANSITION_NOT_ALLOWED` | Program was already ended. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request POST https://www.wixapis.com/benefit-programs/v1/programs/end \
        -H "Authorization: <AUTH>" \
        -H "Content-Type: application/json" \
        --data '{
          "program_id": "e5c42e1e-cd10-4210-b483-bb59932b1fa7"
        }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "job_id": "683aa2bd-0219-45b8-aa80-fcac1c9a685d"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Ends the specified program.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BENEFIT_PROGRAMS.MANAGE">Manage benefit programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/benefit-programs';

    programs.endProgram(programId): Promise<EndProgramResponse>
    ```

    ## Method Parameters

    <ParamField body="programId" type="string" required>

      ID of the program to end.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<EndProgramResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID of the ending of the program. Retrieve job details using the [Async Job API](https://dev.wix.com/docs/rest/business-management/async-job/introduction).

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PROGRAM_STATUS_TRANSITION_NOT_ALLOWED` | Program was already ended. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Program Updated | `wix.benefit_programs.v1.program_updated` | Triggered when a program is updated. |
    | Program Ended | `wix.benefit_programs.v1.program_ended` | Triggered when a program ends. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-end-program" id="ex-sdk-end-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-end-program" id="ex-sdk-end-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-end-program" id="ex-sdk-end-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-end-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-end-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">endProgram</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">endProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">endProgram (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-end-program-0"><span className="wix-picker-name">endProgram</span></label>
    <label htmlFor="ex-sdk-end-program-1"><span className="wix-picker-name">endProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-end-program-2"><span className="wix-picker-name">endProgram (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript endProgram
    import { programs } from '@wix/benefit-programs';

    async function endProgram(programId) {
      const response = await programs.endProgram(programId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript endProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/benefit-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function endProgram(programId) {
      const response = await myWixClient.programs.endProgram(programId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript endProgram (with elevated permissions)
    import { programs } from '@wix/benefit-programs';
    import { auth } from '@wix/essentials';

    async function myEndProgramMethod(programId) {
      const elevatedEndProgram = auth.elevate(programs.endProgram);
      const response = await elevatedEndProgram(programId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
