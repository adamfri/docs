---
title: "onRsvpUpdated"
description: "The onRsvpUpdated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.rsvp.events.RsvpUpdated` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Manage Guest List`, `Read Event Tickets and Guest List` |

## Examples

<CodeGroup>

  ```javascript onRsvpUpdated
  import { rsvp } from '@wix/events';

  rsvp.onRsvpUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRsvpUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { rsvp } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        rsvp,
      },
  });

  wixClient.rsvp.onRsvpUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
