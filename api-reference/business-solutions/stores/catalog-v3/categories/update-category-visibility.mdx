---
title: "Update Category Visibility"
description: "Updates category visibility."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates category visibility.

    Setting visibility to `false` hides the category from site visitors. This also cascades to all subcategories. Hidden categories and their items remain accessible via API.

    Each time a category is updated, `revision` increments by 1.
    The current `revision` must be passed when updating a category.
    This ensures you're working with the latest category and prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_WRITE">Category write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/categories/v1/categories/visibility
    ```

    ## Body Params

    <ParamField body="categoryId" type="string" required>

      Category ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="visible" type="boolean" required>

      Whether the category is visible to site visitors.

      Visibility cascades to subcategories: setting a parent category to `false` also hides all its subcategories.
      Setting to `true` fails if any parent category is hidden.

    </ParamField>

    <ParamField body="treeReference" type="TreeReference" required>

      Category tree reference details.

      <Expandable title="treeReference properties">

        <ParamField body="appNamespace" type="string" required>

          Namespace of the app whose catalog this category tree belongs to.

          Currently, only `"@wix/stores"` is supported.

          Constraints: `minLength 4`, `maxLength 150`.

        </ParamField>

        <ParamField body="treeKey" type="string">

          Key to differentiate between multiple trees managed by the same app.

          For Wix Stores, pass `null` or omit this field, as each store has only one category tree.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="revision" type="integer" required>

      Latest revision of the category.
      To prevent conflicting changes, the current revision must be passed on update.

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Constraints: `maxItems 100`.

      **Supported values:**

      - `BREADCRUMBS_INFO`
      - `DESCRIPTION`
      - `RICH_CONTENT_DESCRIPTION`

    </ParamField>

    ## Response Object

    <ResponseField name="category" type="Category">

      Updated category.

      See the [Category Object](/api-reference/business-solutions/stores/catalog-v3/categories/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PARENT_CATEGORY_HIDDEN` | Category can't be visible when its parent category isn't visible. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Updated | `wix.categories.v1.category_updated` | Triggered when a category is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Updates a category's visible field to false.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/categories/v1/categories/visibility' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
            "categoryId": "bbd52b32-21d2-4dbe-bfc5-d320f93f8911",
            "visible": false,
            "revision": "2",
            "treeReference": {
              "appNamespace": "@wix/stores"
            }
        }'
      ```

      ```json Response
      {
        "category": {
          "id": "bbd52b32-21d2-4dbe-bfc5-d320f93f8911",
          "revision": "3",
          "createdDate": "2024-06-26T09:12:48.985Z",
          "updatedDate": "2024-06-26T11:10:33.390Z",
          "name": "Drink Containers",
          "itemCounter": 0,
          "description": "The Drinkware category includes a wide range of containers designed for holding beverages",
          "visible": false,
          "parentCategory": {
            "index": 8
          },
          "slug": "drinkware",
          "treeReference": {
            "appNamespace": "@wix/stores"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates category visibility.

    Setting visibility to `false` hides the category from site visitors. This also cascades to all subcategories. Hidden categories and their items remain accessible via API.

    Each time a category is updated, `revision` increments by 1.
    The current `revision` must be passed when updating a category.
    This ensures you're working with the latest category and prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_WRITE">Category write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/categories';

    categories.updateCategoryVisibility(categoryId, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="categoryId" type="string" required>

      Category ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="UpdateCategoryVisibilityOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="visible" type="boolean" required>

          Whether the category is visible to site visitors.

          Visibility cascades to subcategories: setting a parent category to `false` also hides all its subcategories.
          Setting to `true` fails if any parent category is hidden.

        </ParamField>

        <ParamField body="treeReference" type="TreeReference" required>

          Category tree reference details.

          <Expandable title="treeReference properties">

            <ParamField body="appNamespace" type="string" required>

              Namespace of the app whose catalog this category tree belongs to.

              Currently, only `"@wix/stores"` is supported.

              Constraints: `minLength 4`, `maxLength 150`.

            </ParamField>

            <ParamField body="treeKey" type="string">

              Key to differentiate between multiple trees managed by the same app.

              For Wix Stores, pass `null` or omit this field, as each store has only one category tree.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Latest revision of the category.
          To prevent conflicting changes, the current revision must be passed on update.

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Constraints: `maxItems 100`.

          **Supported values:**

          - `BREADCRUMBS_INFO`
          - `DESCRIPTION`
          - `RICH_CONTENT_DESCRIPTION`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="category" type="Category">

      Updated category.

      See the [Category Object](/api-reference/business-solutions/stores/catalog-v3/categories/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `PARENT_CATEGORY_HIDDEN` | Category can't be visible when its parent category isn't visible. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Updated | `wix.categories.v1.category_updated` | Triggered when a category is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-category-visibility" id="ex-sdk-update-category-visibility-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-category-visibility" id="ex-sdk-update-category-visibility-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-category-visibility" id="ex-sdk-update-category-visibility-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-category-visibility-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-category-visibility-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateCategoryVisibility</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCategoryVisibility (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCategoryVisibility (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-category-visibility-0"><span className="wix-picker-name">updateCategoryVisibility</span></label>
    <label htmlFor="ex-sdk-update-category-visibility-1"><span className="wix-picker-name">updateCategoryVisibility (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-category-visibility-2"><span className="wix-picker-name">updateCategoryVisibility (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateCategoryVisibility
    import { categories } from '@wix/categories';

    async function updateCategoryVisibility(categoryId,options) {
      const response = await categories.updateCategoryVisibility(categoryId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCategoryVisibility (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/categories';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function updateCategoryVisibility(categoryId,options) {
      const response = await myWixClient.categories.updateCategoryVisibility(categoryId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCategoryVisibility (with elevated permissions)
    import { categories } from '@wix/categories';
    import { auth } from '@wix/essentials';

    async function myUpdateCategoryVisibilityMethod(categoryId,options) {
      const elevatedUpdateCategoryVisibility = auth.elevate(categories.updateCategoryVisibility);
      const response = await elevatedUpdateCategoryVisibility(categoryId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
