---
title: "Remove Form From Trash Bin"
description: "Removes a form schema from the trash bin, permanently deleting it."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes a form schema from the trash bin, permanently deleting it.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/form-schema-service/v4/forms/trash-bin/{formId}
    ```

    ## Path Params

    <ParamField path="formId" type="string" required>

      ID of the form schema to permanently delete from the trash bin.

    </ParamField>

    ## Response Object

    Returns `RemoveFormFromTrashBinResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Permanently deletes form from trash bin

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/form-schema-service/v4/forms/trash-bin/c7dba23b-e0a6-4b01-8fdd-d06d34c0345d' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes a form schema from the trash bin, permanently deleting it.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { forms } from '@wix/forms';

    forms.removeFormFromTrashBin(formId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="formId" type="string" required>

      ID of the form schema to permanently delete from the trash bin.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-form-from-trash-bin" id="ex-sdk-remove-form-from-trash-bin-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-form-from-trash-bin" id="ex-sdk-remove-form-from-trash-bin-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-remove-form-from-trash-bin" id="ex-sdk-remove-form-from-trash-bin-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-form-from-trash-bin-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-form-from-trash-bin-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">removeFormFromTrashBin</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeFormFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeFormFromTrashBin (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-form-from-trash-bin-0"><span className="wix-picker-name">removeFormFromTrashBin</span></label>
    <label htmlFor="ex-sdk-remove-form-from-trash-bin-1"><span className="wix-picker-name">removeFormFromTrashBin (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-remove-form-from-trash-bin-2"><span className="wix-picker-name">removeFormFromTrashBin (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript removeFormFromTrashBin
    import { forms } from '@wix/forms';

    async function removeFormFromTrashBin(formId) {
      const response = await forms.removeFormFromTrashBin(formId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeFormFromTrashBin (self-hosted)
    import { createClient } from '@wix/sdk';
    import { forms } from '@wix/forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { forms },
      // Include the auth strategy and host as relevant
    });

    async function removeFormFromTrashBin(formId) {
      const response = await myWixClient.forms.removeFormFromTrashBin(formId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeFormFromTrashBin (with elevated permissions)
    import { forms } from '@wix/forms';
    import { auth } from '@wix/essentials';

    async function myRemoveFormFromTrashBinMethod(formId) {
      const elevatedRemoveFormFromTrashBin = auth.elevate(forms.removeFormFromTrashBin);
      const response = await elevatedRemoveFormFromTrashBin(formId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
