---
title: "onPostReacted"
description: "An existing post was reacted."
icon: "bolt"
---

An existing post was reacted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_reacted` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostReacted
  import { posts } from '@wix/forum';

  posts.onPostReacted((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostReacted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostReacted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
