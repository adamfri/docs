---
title: "Update Product Variants"
description: "Updates variants of a specified product."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates variants of a specified product.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/stores/v1/products/{id}/variants
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the product with managed variants.

    </ParamField>

    ## Body Params

    <ParamField body="variants" type="VariantOverride[]">

      Variant info to update.

      <Expandable title="variants properties">

        <ParamField body="choices" type="object">

          The specific choices available or chosen from within a selection (e.g., choosing the red Selection triggers the red Choice). You may specify all the relevant choices for a specific variant, or only some of the options, which will return all corresponding variants (Not relevant when passing variant IDs)

          Constraints: `format map`.

          <Expandable title="choices properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="string" />

          </Expandable>

        </ParamField>

        <ParamField body="variantIds" type="string[]">

          List of variant IDs
          (Not relevant when passing choices)

        </ParamField>

        <ParamField body="price" type="number">

          Variant price

        </ParamField>

        <ParamField body="cost" type="number">

          Variant cost of goods

        </ParamField>

        <ParamField body="weight" type="number">

          Variant weight

        </ParamField>

        <ParamField body="sku" type="string">

          Variant SKU (stock keeping unit)

        </ParamField>

        <ParamField body="visible" type="boolean">

          Whether the variant is visible to  customers

        </ParamField>

        <ParamField body="productDimensions" type="Dimensions">

          Product dimensions (length, width, height of the physical product)

          <Expandable title="productDimensions properties">

            <ParamField body="length" type="string">

              Length. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="width" type="string">

              Width. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="height" type="string">

              Height. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="unit" type="string">

              Unit of measurement for dimensions (length, width, height).

              **Supported values:**

              - `UNKNOWN_DIMENSIONS_UNIT`
              - `MM` — Millimeters.
              - `CM` — Centimeters.
              - `M` — Meters.
              - `IN` — Inches.
              - `FT` — Feet.
              - `YD` — Yards.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="packageDimensions" type="Dimensions">

          Package dimensions (length, width, height of the shipping package)

          <Expandable title="packageDimensions properties">

            <ParamField body="length" type="string">

              Length. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="width" type="string">

              Width. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="height" type="string">

              Height. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="unit" type="string">

              Unit of measurement for dimensions (length, width, height).

              **Supported values:**

              - `UNKNOWN_DIMENSIONS_UNIT`
              - `MM` — Millimeters.
              - `CM` — Centimeters.
              - `M` — Meters.
              - `IN` — Inches.
              - `FT` — Feet.
              - `YD` — Yards.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="variants" type="Variant[]">

      List of the product's variants.

      <Expandable title="variants properties">

        <ResponseField name="id" type="string">

          Requested Variant ID

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="choices" type="object">

          Specific choices within a selection, as option-choice key-value pairs

          Constraints: `format map`.

          <Expandable title="choices properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="string" />

          </Expandable>

        </ResponseField>

        <ResponseField name="variant" type="VariantDataWithNoStock">

          variant

          <Expandable title="variant properties">

            <ResponseField name="priceData" type="PriceData">

              Variant price.

              _Nested structure omitted for brevity — see the `PriceData` object._

            </ResponseField>

            <ResponseField name="convertedPriceData" type="PriceData">

              Variant price data, converted to currency requested in header.

              **Read-only.**

              _Nested structure omitted for brevity — see the `PriceData` object._

            </ResponseField>

            <ResponseField name="costAndProfitData" type="CostAndProfitData">

              Cost and profit data.

              _Nested structure omitted for brevity — see the `CostAndProfitData` object._

            </ResponseField>

            <ResponseField name="weight" type="number">

              Variant weight.

            </ResponseField>

            <ResponseField name="sku" type="string">

              Variant SKU (stock keeping unit).

            </ResponseField>

            <ResponseField name="visible" type="boolean">

              Whether the variant is visible to customers.

            </ResponseField>

            <ResponseField name="productDimensions" type="Dimensions">

              Product dimensions (length, width, height of the physical product).

              _Nested structure omitted for brevity — see the `Dimensions` object._

            </ResponseField>

            <ResponseField name="packageDimensions" type="Dimensions">

              Package dimensions (length, width, height of the shipping package).

              _Nested structure omitted for brevity — see the `Dimensions` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="stock" type="VariantStock">

          Variant inventory status.

          **Read-only.**

          <Expandable title="stock properties">

            <ResponseField name="trackQuantity" type="boolean">

              Whether inventory is being tracked.

            </ResponseField>

            <ResponseField name="quantity" type="integer">

              Quantity currently left in inventory.

            </ResponseField>

            <ResponseField name="inStock" type="boolean">

              Whether the product is currently in stock (relevant only when tracking manually).

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X PATCH \
         'https://www.wixapis.com/stores/v1/products/1044e7e4-37d1-0705-c5b3-623baae212fd/variants' \
          --data-binary '{
              "variants": [
                  {
                  "choices": {
                      "Size": 'S',
                      "Color": 'Blue'
                  },
                  "price": 100
                  }
                ]
              }' \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "variants": [
          {
            "id": "00000000-0000-0020-0005-92338aaf6c4a",
            "choices": {
              "Size": "S",
              "Color": "Blue"
            },
            "variant": {
              "priceData": {
                "currency": "USD",
                "price": 100,
                "discountedPrice": 100,
                "formatted": {
                  "price": "$100.00",
                  "discountedPrice": "$100.00"
                }
              },
              "weight": 0,
              "sku": "",
              "visible": true
            }
          }
        ]
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates variants of a specified product.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { products } from '@wix/stores';

    products.updateProductVariants(_id, variants): Promise<UpdateVariantsResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the product with managed variants.

    </ParamField>

    <ParamField body="variants" type="VariantOverride[]" required>

      Variant info to update.

      <Expandable title="variants properties">

        <ParamField body="choices" type="object">

          The specific choices available or chosen from within a selection (e.g., choosing the red Selection triggers the red Choice). You may specify all the relevant choices for a specific variant, or only some of the options, which will return all corresponding variants (Not relevant when passing variant IDs)

          Constraints: `format map`.

          <Expandable title="choices properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="string" />

          </Expandable>

        </ParamField>

        <ParamField body="variantIds" type="string[]">

          List of variant IDs
          (Not relevant when passing choices)

        </ParamField>

        <ParamField body="price" type="number">

          Variant price

        </ParamField>

        <ParamField body="cost" type="number">

          Variant cost of goods

        </ParamField>

        <ParamField body="weight" type="number">

          Variant weight

        </ParamField>

        <ParamField body="sku" type="string">

          Variant SKU (stock keeping unit)

        </ParamField>

        <ParamField body="visible" type="boolean">

          Whether the variant is visible to  customers

        </ParamField>

        <ParamField body="productDimensions" type="Dimensions">

          Product dimensions (length, width, height of the physical product)

          <Expandable title="productDimensions properties">

            <ParamField body="length" type="string">

              Length. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="width" type="string">

              Width. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="height" type="string">

              Height. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="unit" type="string">

              Unit of measurement for dimensions (length, width, height).

              **Supported values:**

              - `UNKNOWN_DIMENSIONS_UNIT`
              - `MM` — Millimeters.
              - `CM` — Centimeters.
              - `M` — Meters.
              - `IN` — Inches.
              - `FT` — Feet.
              - `YD` — Yards.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="packageDimensions" type="Dimensions">

          Package dimensions (length, width, height of the shipping package)

          <Expandable title="packageDimensions properties">

            <ParamField body="length" type="string">

              Length. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="width" type="string">

              Width. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="height" type="string">

              Height. Measurement unit is handled at the system level (metric/imperial).

            </ParamField>

            <ParamField body="unit" type="string">

              Unit of measurement for dimensions (length, width, height).

              **Supported values:**

              - `UNKNOWN_DIMENSIONS_UNIT`
              - `MM` — Millimeters.
              - `CM` — Centimeters.
              - `M` — Meters.
              - `IN` — Inches.
              - `FT` — Feet.
              - `YD` — Yards.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateVariantsResponse>`.

    <ResponseField name="variants" type="Variant[]">

      List of the product's variants.

      <Expandable title="variants properties">

        <ResponseField name="_id" type="string">

          Requested Variant ID

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="choices" type="object">

          Specific choices within a selection, as option-choice key-value pairs

          Constraints: `format map`.

          <Expandable title="choices properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="string" />

          </Expandable>

        </ResponseField>

        <ResponseField name="variant" type="VariantDataWithNoStock">

          variant

          <Expandable title="variant properties">

            <ResponseField name="priceData" type="PriceData">

              Variant price.

              _Nested structure omitted for brevity — see the `PriceData` object._

            </ResponseField>

            <ResponseField name="convertedPriceData" type="PriceData">

              Variant price data, converted to currency requested in header.

              **Read-only.**

              _Nested structure omitted for brevity — see the `PriceData` object._

            </ResponseField>

            <ResponseField name="costAndProfitData" type="CostAndProfitData">

              Cost and profit data.

              _Nested structure omitted for brevity — see the `CostAndProfitData` object._

            </ResponseField>

            <ResponseField name="weight" type="number">

              Variant weight.

            </ResponseField>

            <ResponseField name="sku" type="string">

              Variant SKU (stock keeping unit).

            </ResponseField>

            <ResponseField name="visible" type="boolean">

              Whether the variant is visible to customers.

            </ResponseField>

            <ResponseField name="productDimensions" type="Dimensions">

              Product dimensions (length, width, height of the physical product).

              _Nested structure omitted for brevity — see the `Dimensions` object._

            </ResponseField>

            <ResponseField name="packageDimensions" type="Dimensions">

              Package dimensions (length, width, height of the shipping package).

              _Nested structure omitted for brevity — see the `Dimensions` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="stock" type="VariantStock">

          Variant inventory status.

          **Read-only.**

          <Expandable title="stock properties">

            <ResponseField name="trackQuantity" type="boolean">

              Whether inventory is being tracked.

            </ResponseField>

            <ResponseField name="quantity" type="integer">

              Quantity currently left in inventory.

            </ResponseField>

            <ResponseField name="inStock" type="boolean">

              Whether the product is currently in stock (relevant only when tracking manually).

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-product-variants" id="ex-sdk-update-product-variants-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-product-variants" id="ex-sdk-update-product-variants-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-product-variants" id="ex-sdk-update-product-variants-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-product-variants-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-product-variants-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update the data of a variant (with $w)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update product variants</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateProductVariants (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-product-variants-0"><span className="wix-picker-name">Update the data of a variant (with $w)</span></label>
    <label htmlFor="ex-sdk-update-product-variants-1"><span className="wix-picker-name">Update product variants</span></label>
    <label htmlFor="ex-sdk-update-product-variants-2"><span className="wix-picker-name">updateProductVariants (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update the data of a variant (with $w)
    /**************************************
     * Backend code - products.web.js/ts *
     **************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { products } from '@wix/stores';

    export const updateVariantData = webMethod(Permissions.Anyone, (_id, variants) => {
      return products.updateProductVariants(_id, variants);
    });

    /*************
     * Page code *
     *************/

    import { updateVariantData } from 'backend/products.web';

    // ...

    $w('#myProductPage').getProduct()
      .then((product) => {
        let _id = product._id;
        let manageVariants = product.manageVariants;

        // check that variants can be added and updated
        // for this product
        if (manageVariants) {
          const weight = 10;
          const price = 15;
          const variants = [{
            weight,
            price,
            "choices": {
              "Color": "Blue",
              "Size": "Large"
            }
          }];

          updateVariantData(_id, variants)
            .then(() => {
              // product variant data have been updated
            })
            .catch((error) => {
              // there was an error updating the product variant
            });
        }
      });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update product variants
    import { products } from "@wix/stores";

    const productId = "1044e7e4-37d1-0705-c5b3-623baae212fd";

    const variants = [
      {
        choices: {
          Size: "S",
          Color: "Blue"
        },
        price: 100
      }
    ];

    async function updateProductVariants() {
      const response = await products.updateProductVariants(productId, variants);
    }

    /* Promise resolves to:
     * {
     *   "variants": [
     *     {
     *       "_id": "00000000-0000-0020-0005-92338aaf6c4a",
     *       "choices": {
     *         "Size": "S",
     *         "Color": "Blue"
     *       },
     *       "variant": {
     *         "priceData": {
     *           "currency": "USD",
     *           "price": 100,
     *           "formatted": {
     *             "price": "$100.00",
     *             "discountedPrice": "$100.00"
     *           }
     *         },
     *         "visible": true
     *       }
     *     }
     *   ]
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateProductVariants (self-hosted)
    import { createClient } from '@wix/sdk';
    import { products } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { products },
      // Include the auth strategy and host as relevant
    });

    async function updateProductVariants(_id,variants) {
      const response = await myWixClient.products.updateProductVariants(_id,variants);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
