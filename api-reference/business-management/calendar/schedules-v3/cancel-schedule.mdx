---
title: "Cancel Schedule"
description: "Sets a schedule's status to CANCELLED."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets a schedule's `status` to `CANCELLED`.

    Also cancels future events belonging to the schedule.

    Once a schedule is marked as `CANCELLED`, you can't reactivate it, update it,
    or assign new events.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CALENDAR.MANAGE">Manage Calendars</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/calendar/v3/schedules/{scheduleId}/cancel
    ```

    ## Path Params

    <ParamField path="scheduleId" type="string" required>

      ID of the schedule to cancel.

    </ParamField>

    ## Body Params

    <ParamField body="preserveFutureEventsWithParticipants" type="boolean">

      Whether to preserve future events with participants.
      Default: `false`.

    </ParamField>

    <ParamField body="participantNotification" type="ParticipantNotification">

      Information about whether participants of the schedule's updated events are
      notified and the message they receive.

      <Expandable title="participantNotification properties">

        <ParamField body="notifyParticipants" type="boolean">

          Whether to notify the participants about changes made to the schedule or event.

          Default: `false`

        </ParamField>

        <ParamField body="message" type="string">

          Message to send.

          Min: 1 character
          Max: 5000 characters

        </ParamField>

        <ParamField body="metadata" type="object">

          Information about the delivery channels used to send the notification.
          For example, `{"channels": "SMS" }`, `{"channels": "EMAIL" }`, or `{"channels": "EMAIL, SMS" }`.

          Max: 10 keys
          Max key length: 64 characters
          Max value length: 1000 characters

          Constraints: `format map`.

          <Expandable title="metadata properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="string" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="schedule" type="Schedule">

      Canceled schedule.

      See the [Schedule Object](/api-reference/business-management/calendar/schedules-v3/schedule-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULE_ALREADY_CANCELLED` | The schedule is already canceled. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Schedule Updated | `wix.calendar.v3.schedule_updated` | Triggered when a schedule is updated. |
    | Schedule Cancelled | `wix.calendar.v3.schedule_cancelled` | Triggered when a schedule is canceled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-cancel-schedule" id="ex-rest-cancel-schedule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-cancel-schedule" id="ex-rest-cancel-schedule-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-cancel-schedule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-cancel-schedule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Cancel Schedule With Notification</span><span className="wix-picker-desc">Cancels a schedule and sends a notification to its event participants</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Cancel Schedule</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-cancel-schedule-0"><span className="wix-picker-name">Cancel Schedule With Notification</span><span className="wix-picker-desc">Cancels a schedule and sends a notification to its event participants</span></label>
    <label htmlFor="ex-rest-cancel-schedule-1"><span className="wix-picker-name">Cancel Schedule</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/calendar/v3/schedules/af8fbdc9-0fd8-465c-9419-aca96126e8bb/cancel' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
            "participantNotification": {
              "notifyParticipants": true,
              "message": "Important Update: The Group Hip Hop Groove has been cancelled!"
            }
      }'
      ```

      ```json Response
      {
        "schedule": {
          "id": "af8fbdc9-0fd8-465c-9419-aca96126e8bb",
          "externalId": "4187d1f3-38a2-4cfd-b8f1-6333cd012e33",
          "name": "Group Hip Hop Groove",
          "status": "CANCELLED",
          "timeZone": "Europe/Dublin",
          "businessTimeZoneEnabled": true,
          "defaultTitle": "Group Hip Hop Groove",
          "defaultCapacity": 10,
          "appId": "13d21c63-b5ec-5912-8397-c3a5ddb27a97",
          "permissions": [],
          "revision": "2",
          "createdDate": "2024-10-07T06:53:02.534Z",
          "updatedDate": "2024-10-07T06:55:32.450Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/calendar/v3/schedules/3470efd4-bd31-4aab-b339-38cdce8e1bd2/cancel' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "schedule": {
          "id": "3470efd4-bd31-4aab-b339-38cdce8e1bd2",
          "externalId": "c84d2eb2-925e-49d8-910a-280473629415",
          "name": "Zumba",
          "status": "CANCELLED",
          "timeZone": "Europe/Dublin",
          "businessTimeZoneEnabled": true,
          "defaultTitle": "Zumba",
          "defaultCapacity": 10,
          "appId": "13d21c63-b5ec-5912-8397-c3a5ddb27a97",
          "permissions": [],
          "revision": "2",
          "createdDate": "2024-10-07T06:51:20.019Z",
          "updatedDate": "2024-10-07T06:53:43.510Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sets a schedule's `status` to `CANCELLED`.

    Also cancels future events belonging to the schedule.

    Once a schedule is marked as `CANCELLED`, you can't reactivate it, update it,
    or assign new events.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CALENDAR.MANAGE">Manage Calendars</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { schedules } from '@wix/calendar';

    schedules.cancelSchedule(scheduleId, options): Promise<CancelScheduleResponse>
    ```

    ## Method Parameters

    <ParamField body="scheduleId" type="string" required>

      ID of the schedule to cancel.

    </ParamField>

    <ParamField body="options" type="CancelScheduleOptions">

      options

      <Expandable title="options properties">

        <ParamField body="preserveFutureEventsWithParticipants" type="boolean">

          Whether to preserve future events with participants.
          Default: `false`.

        </ParamField>

        <ParamField body="participantNotification" type="ParticipantNotification">

          Information about whether participants of the schedule's updated events are
          notified and the message they receive.

          <Expandable title="participantNotification properties">

            <ParamField body="notifyParticipants" type="boolean">

              Whether to notify the participants about changes made to the schedule or event.

              Default: `false`

            </ParamField>

            <ParamField body="message" type="string">

              Message to send.

              Min: 1 character
              Max: 5000 characters

            </ParamField>

            <ParamField body="metadata" type="object">

              Information about the delivery channels used to send the notification.
              For example, `{"channels": "SMS" }`, `{"channels": "EMAIL" }`, or `{"channels": "EMAIL, SMS" }`.

              Max: 10 keys
              Max key length: 64 characters
              Max value length: 1000 characters

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<CancelScheduleResponse>`.

    <ResponseField name="schedule" type="Schedule">

      Canceled schedule.

      See the [Schedule Object](/api-reference/business-management/calendar/schedules-v3/schedule-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `SCHEDULE_ALREADY_CANCELLED` | The schedule is already canceled. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Schedule Updated | `wix.calendar.v3.schedule_updated` | Triggered when a schedule is updated. |
    | Schedule Updated With Metadata | `wix.calendar.v3.schedule_updated_with_metadata` | Triggered when a schedule is updated. |
    | Schedule Cancelled | `wix.calendar.v3.schedule_cancelled` | Triggered when a schedule is canceled. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-cancel-schedule" id="ex-sdk-cancel-schedule-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-cancel-schedule" id="ex-sdk-cancel-schedule-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-cancel-schedule" id="ex-sdk-cancel-schedule-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-cancel-schedule-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-cancel-schedule-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">cancelSchedule</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">cancelSchedule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">cancelSchedule (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-cancel-schedule-0"><span className="wix-picker-name">cancelSchedule</span></label>
    <label htmlFor="ex-sdk-cancel-schedule-1"><span className="wix-picker-name">cancelSchedule (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-cancel-schedule-2"><span className="wix-picker-name">cancelSchedule (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript cancelSchedule
    import { schedules } from '@wix/calendar';

    async function cancelSchedule(scheduleId,options) {
      const response = await schedules.cancelSchedule(scheduleId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript cancelSchedule (self-hosted)
    import { createClient } from '@wix/sdk';
    import { schedules } from '@wix/calendar';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { schedules },
      // Include the auth strategy and host as relevant
    });

    async function cancelSchedule(scheduleId,options) {
      const response = await myWixClient.schedules.cancelSchedule(scheduleId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript cancelSchedule (with elevated permissions)
    import { schedules } from '@wix/calendar';
    import { auth } from '@wix/essentials';

    async function myCancelScheduleMethod(scheduleId,options) {
      const elevatedCancelSchedule = auth.elevate(schedules.cancelSchedule);
      const response = await elevatedCancelSchedule(scheduleId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
