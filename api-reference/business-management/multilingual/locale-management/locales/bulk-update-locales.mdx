---
title: "Bulk Update Locales"
description: "Updates multiple locales."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates multiple locales.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MULTILINGUAL.MANAGE_TRANSLATIONS">Wix Multilingual</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/locales/v2/bulk/locale/update
    ```

    ## Body Params

    <ParamField body="locales" type="MaskedLocale[]" required>

      Locales to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="locales properties">

        <ParamField body="locale" type="Locale">

          Locales to update, may be partial.

          See the [Locale Object](/api-reference/business-management/multilingual/locale-management/locales/locale-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to receive the updated locales in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkLocaleResult[]">

      Information about the updated locales.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Locale">

          Updated locale. Returned when `returnEntity` is set to `true`.

          See the [Locale Object](/api-reference/business-management/multilingual/locale-management/locales/locale-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Locale Updated | `wix.multilingual.locales.v2.locale_updated` | Triggered when a locale is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/locales/v2/bulk/locale/update' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "locales": [
            {
              "locale": {
                "id": "en",
                "revision": "1",
                "languageCode": "en",
                "visibility": "HIDDEN",
                "flag": "USA"
              }
            }
          ],
          "returnEntity": true
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "item": {
              "id": "en",
              "revision": "1",
              "createdDate": "2024-11-20T17:32:28.442Z",
              "updatedDate": "2024-11-20T17:32:28.442Z",
              "languageCode": "en",
              "visibility": "HIDDEN",
              "primaryLocale": false,
              "visitorPrimaryLocale": false,
              "flag": "USA",
              "regionalFormat": "en-US",
              "effectiveUrlStructure": "SUBDOMAIN",
              "machineTranslationCode": "EN",
              "effectiveDisplayName": "English",
              "displayName": "English"
            },
            "itemMetadata": {
              "id": "en",
              "originalIndex": 0,
              "success": true
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 1,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates multiple locales.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.MULTILINGUAL.MANAGE_TRANSLATIONS">Wix Multilingual</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locales } from '@wix/multilingual';

    locales.bulkUpdateLocales(locales, options): Promise<BulkUpdateLocalesResponse>
    ```

    ## Method Parameters

    <ParamField body="locales" type="MaskedLocale[]" required>

      Locales to update.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="locales properties">

        <ParamField body="locale" type="Locale">

          Locales to update, may be partial.

          See the [Locale Object](/api-reference/business-management/multilingual/locale-management/locales/locale-object) for this object's fields.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="BulkUpdateLocalesOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to receive the updated locales in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkUpdateLocalesResponse>`.

    <ResponseField name="results" type="BulkLocaleResult[]">

      Information about the updated locales.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Item metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="Locale">

          Updated locale. Returned when `returnEntity` is set to `true`.

          See the [Locale Object](/api-reference/business-management/multilingual/locale-management/locales/locale-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Locale Updated | `wix.multilingual.locales.v2.locale_updated` | Triggered when a locale is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-locales" id="ex-sdk-bulk-update-locales-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-locales" id="ex-sdk-bulk-update-locales-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-update-locales" id="ex-sdk-bulk-update-locales-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-locales-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-locales-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateLocales</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateLocales (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdateLocales (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-locales-0"><span className="wix-picker-name">bulkUpdateLocales</span></label>
    <label htmlFor="ex-sdk-bulk-update-locales-1"><span className="wix-picker-name">bulkUpdateLocales (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-update-locales-2"><span className="wix-picker-name">bulkUpdateLocales (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUpdateLocales
    import { locales } from '@wix/multilingual';

    async function bulkUpdateLocales(locales,options) {
      const response = await locales.bulkUpdateLocales(locales,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateLocales (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locales } from '@wix/multilingual';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locales },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdateLocales(locales,options) {
      const response = await myWixClient.locales.bulkUpdateLocales(locales,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdateLocales (with elevated permissions)
    import { locales } from '@wix/multilingual';
    import { auth } from '@wix/essentials';

    async function myBulkUpdateLocalesMethod(locales,options) {
      const elevatedBulkUpdateLocales = auth.elevate(locales.bulkUpdateLocales);
      const response = await elevatedBulkUpdateLocales(locales,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
