---
title: "Estimate Totals"
description: "Estimates the subtotal and total for current site visitor’s cart."
icon: "upload"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Estimates the subtotal and total for current site visitor’s cart.

    Totals include tax and are based on the selected carrier service, shipping address, and billing information.

    <Note>

    Not passing any optional parameters will only estimate the cart items price totals.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.READ-ORDERS">Read Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/ecom/v1/carts/{id}/estimate-totals
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      Cart ID.

    </ParamField>

    ## Body Params

    <ParamField body="selectedShippingOption" type="SelectedShippingOption">

      Selected shipping option.

      Pass this field only when there is no existing shipping option in the cart.

      <Expandable title="selectedShippingOption properties">

        <ParamField body="carrierId" type="string">

          Carrier ID.

        </ParamField>

        <ParamField body="code" type="string">

          Selected shipping option code. For example, "usps_std_overnight".

          Constraints: `minLength 1`, `maxLength 100`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="shippingAddress" type="Address">

      Shipping address. Used for calculating tax and shipping (when applicable).

      <Expandable title="shippingAddress properties">

        <ParamField body="country" type="string">

          Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

        </ParamField>

        <ParamField body="subdivision" type="string">

          Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

        </ParamField>

        <ParamField body="city" type="string">

          City name.

        </ParamField>

        <ParamField body="postalCode" type="string">

          Postal or zip code.

        </ParamField>

        <ParamField body="streetAddress" type="StreetAddress">

          Street address.

          <Expandable title="streetAddress properties">

            <ParamField body="number" type="string">

              Street number.

            </ParamField>

            <ParamField body="name" type="string">

              Street name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="addressLine" type="string">

          Main address line (usually street name and number).

        </ParamField>

        <ParamField body="addressLine2" type="string">

          Free text providing more detailed address info. Usually contains apt, suite, floor.

        </ParamField>

        <ParamField body="geocode" type="AddressLocation">

          Geocode object containing latitude and longitude coordinates.

          <Expandable title="geocode properties">

            <ParamField body="latitude" type="number">

              Address latitude.

            </ParamField>

            <ParamField body="longitude" type="number">

              Address longitude.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="countryFullname" type="string">

          Country's full name.

          **Read-only.**

        </ParamField>

        <ParamField body="subdivisionFullname" type="string">

          Subdivision full-name.

          **Read-only.**

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="billingAddress" type="Address">

      Billing address. Used for calculating tax if all the items in the cart are not shippable.

      <Expandable title="billingAddress properties">

        <ParamField body="country" type="string">

          Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

        </ParamField>

        <ParamField body="subdivision" type="string">

          Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

        </ParamField>

        <ParamField body="city" type="string">

          City name.

        </ParamField>

        <ParamField body="postalCode" type="string">

          Postal or zip code.

        </ParamField>

        <ParamField body="streetAddress" type="StreetAddress">

          Street address.

          <Expandable title="streetAddress properties">

            <ParamField body="number" type="string">

              Street number.

            </ParamField>

            <ParamField body="name" type="string">

              Street name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="addressLine" type="string">

          Main address line (usually street name and number).

        </ParamField>

        <ParamField body="addressLine2" type="string">

          Free text providing more detailed address info. Usually contains apt, suite, floor.

        </ParamField>

        <ParamField body="geocode" type="AddressLocation">

          Geocode object containing latitude and longitude coordinates.

          <Expandable title="geocode properties">

            <ParamField body="latitude" type="number">

              Address latitude.

            </ParamField>

            <ParamField body="longitude" type="number">

              Address longitude.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="countryFullname" type="string">

          Country's full name.

          **Read-only.**

        </ParamField>

        <ParamField body="subdivisionFullname" type="string">

          Subdivision full-name.

          **Read-only.**

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="selectedMemberships" type="SelectedMemberships">

      The selected membership payment options and which line items they apply to.

      <Expandable title="selectedMemberships properties">

        <ParamField body="memberships" type="SelectedMembership[]">

          Selected memberships.

          Constraints: `maxItems 300`.

          <Expandable title="memberships properties">

            <ParamField body="id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ParamField>

            <ParamField body="appId" type="string">

              ID of the app providing this payment option.

              Constraints: `format GUID`.

            </ParamField>

            <ParamField body="lineItemIds" type="string[]">

              IDs of the line items this membership applies to.

              Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="calculateTax" type="boolean">

      Whether to calculate tax in the calculation request.

      Default: `true`

    </ParamField>

    <ParamField body="calculateShipping" type="boolean">

      Whether to calculate shipping in the calculation request.

      Default: `true`

    </ParamField>

    <ParamField body="calculateAdditionalFees" type="boolean">

      Whether to calculate additional fees in the calculation request.

      Default: `true`

    </ParamField>

    ## Response Object

    <ResponseField name="cart" type="Cart">

      Cart.

      See the [Cart Object](/api-reference/business-solutions/e-commerce/purchase-flow/cart/cart-object) for this object's fields.

    </ResponseField>

    <ResponseField name="calculatedLineItems" type="CalculatedLineItem[]">

      Calculated line items.

      <Expandable title="calculatedLineItems properties">

        <ResponseField name="lineItemId" type="string">

          Line item ID.

        </ResponseField>

        <ResponseField name="pricesBreakdown" type="LineItemPricesData">

          Price breakdown for this line item.

          <Expandable title="pricesBreakdown properties">

            <ResponseField name="totalPriceAfterTax" type="MultiCurrencyPrice">

              Total price after discounts, tax, and modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="totalPriceBeforeTax" type="MultiCurrencyPrice">

              Total price after discounts and modifiers, and before tax.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="taxDetails" type="ItemTaxFullDetails">

              Tax details.

              _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

            </ResponseField>

            <ResponseField name="totalDiscount" type="MultiCurrencyPrice">

              Total discount applied for the line item.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="price" type="MultiCurrencyPrice">

              Catalog price after catalog-defined discount, automatic discounts, and modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="priceBeforeDiscounts" type="MultiCurrencyPrice">

              Item price before automatic discounts, coupons, and global discounts; after catalog-defined discounts and modifiers.
              Defaults to `price` when not provided.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="lineItemPrice" type="MultiCurrencyPrice">

              Total line item price after catalog-defined discounts, automatic discounts, and modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="fullPrice" type="MultiCurrencyPrice">

              Catalog price before any discounts, with modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="modifiers" type="MultiCurrencyPrice">

              Sum of all modifiers for a single unit of the item.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="depositAmount" type="MultiCurrencyPrice">

              Partial payment out of `price` to be paid upfront during the checkout.
              Applies to catalog items with `lineItem.paymentOption` type `DEPOSIT_ONLINE` only.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paymentOption" type="string">

          Type of selected payment option for current item. Supported values:
          + `"FULL_PAYMENT_ONLINE"` - The entire payment for this item will happen as part of the checkout
          + `"FULL_PAYMENT_OFFLINE"` - The entire payment for this item will happen after the checkout
          + `"MEMBERSHIP"` - This item cannot be paid via monetary payment options, only via non monetary option such membership. When this option is used, price must be set to 0
          + `"DEPOSIT_ONLINE"` -  Partial payment of the given item will happen as part of the checkout. Amount to be paid is defined by deposit_amount field.

          **Supported values:**

          - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
          - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
          - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
          - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
          - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
          - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          tax calculation address.

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              _Nested structure omitted for brevity — see the `TaxableAddressData` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="priceSummary" type="PriceSummary">

      Price summary.

      <Expandable title="priceSummary properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="giftCard" type="GiftCard">

      Applied gift card.

      <Expandable title="giftCard properties">

        <ResponseField name="id" type="string" deprecated>

          Gift Card ID.

          **Deprecated.**

        </ResponseField>

        <ResponseField name="obfuscatedCode" type="string">

          Gift card obfuscated code.

        </ResponseField>

        <ResponseField name="amount" type="MultiCurrencyPrice">

          Actual amount to be redeemed from the gift card.

          <Expandable title="amount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="appId" type="string">

          App ID of the gift card provider.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="externalId" type="string">

          External ID in the gift card provider's system.
          Used for integration and tracking across different platforms.

        </ResponseField>

        <ResponseField name="requestedAmount" type="MultiCurrencyPrice">

          Requested amount to redeem from the gift card.

          **Developer Preview.**

          <Expandable title="requestedAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="taxSummary" type="TaxSummary">

      Tax summary.

      <Expandable title="taxSummary properties">

        <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

          Amount for which tax is calculated, added from line items.

          **Read-only.**

          <Expandable title="taxableAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalTax" type="MultiCurrencyPrice">

          Calculated tax, added from line items.

          **Read-only.**

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="calculationDetails" type="TaxCalculationDetails" deprecated>

          Tax calculator that was active when the order was created.

          **Deprecated.**

          <Expandable title="calculationDetails properties">

            <ResponseField name="rateType" type="string">

              Rate calculation type.

              **Supported values:**

              - `NO_TAX_COLLECTED` — no tax being collected for this request due to location of purchase
              - `MANUAL_RATE` — manual rate used for calculation
              - `AUTO_RATE` — autotax rate used for calculation
              - `FALLBACK_RATE` — fallback rate used for calculation

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="CalculationDetails" type="CalculationDetails">

              CalculationDetails

              _Nested structure omitted for brevity — see the `CalculationDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="shippingInfo" type="ShippingInformation">

      Shipping information.

      <Expandable title="shippingInfo properties">

        <ResponseField name="region" type="ShippingRegion">

          Shipping region.

          <Expandable title="region properties">

            <ResponseField name="id" type="string">

              Shipping region ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="string">

              Shipping region name.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedCarrierServiceOption" type="SelectedCarrierServiceOption">

          Selected shipping option.

          <Expandable title="selectedCarrierServiceOption properties">

            <ResponseField name="code" type="string">

              Unique identifier of selected option. For example, "usps_std_overnight".

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
              For example, "Standard" or "First-Class Package International".

              **Read-only.** Constraints: `maxLength 250`.

            </ResponseField>

            <ResponseField name="logistics" type="DeliveryLogistics">

              Delivery logistics.

              **Read-only.**

              _Nested structure omitted for brevity — see the `DeliveryLogistics` object._

            </ResponseField>

            <ResponseField name="cost" type="SelectedCarrierServiceOptionPrices">

              Shipping costs.

              **Read-only.**

              _Nested structure omitted for brevity — see the `SelectedCarrierServiceOptionPrices` object._

            </ResponseField>

            <ResponseField name="requestedShippingOption" type="boolean">

              Were we able to find the requested shipping option, or otherwise we fallback to the default one (the first)

              **Read-only.**

            </ResponseField>

            <ResponseField name="otherCharges" type="SelectedCarrierServiceOptionOtherCharge[]" deprecated>

              Other charges

              **Deprecated.** Other charges Use `additional_fees` instead. Removal: 2025-10-01.

              _Nested structure omitted for brevity — see the `SelectedCarrierServiceOptionOtherCharge[]` object._

            </ResponseField>

            <ResponseField name="carrierId" type="string">

              This carrier's unique ID

            </ResponseField>

            <ResponseField name="deliveryAllocations" type="DeliveryAllocation[]">

              Delivery solution allocations to different delivery carriers and delivery regions

              **Developer Preview.** Constraints: `maxItems 300`.

              _Nested structure omitted for brevity — see the `DeliveryAllocation[]` object._

            </ResponseField>

            <ResponseField name="partial" type="boolean">

              If the delivery solution is a partial and doesn't apply to all items.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="carrierServiceOptions" type="CarrierServiceOption[]">

          All shipping options.

          <Expandable title="carrierServiceOptions properties">

            <ResponseField name="carrierId" type="string">

              Carrier ID.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="shippingOptions" type="ShippingOption[]">

              Shipping options offered by this carrier for this request.

              _Nested structure omitted for brevity — see the `ShippingOption[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="appliedDiscounts" type="AppliedDiscount[]">

      Applied discounts.

      <Expandable title="appliedDiscounts properties">

        <ResponseField name="discountType" type="string">

          Discount type.

          **Supported values:**

          - `GLOBAL`
          - `SPECIFIC_ITEMS`
          - `SHIPPING`

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          IDs of line items the discount applies to.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="calculationErrors" type="CalculationErrors">

      Calculation errors.

      <Expandable title="calculationErrors properties">

        <ResponseField name="taxCalculationError" type="Details">

          Tax calculation error.

          <Expandable title="taxCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="couponCalculationError" type="Details">

          Coupon calculation error.

          <Expandable title="couponCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="giftCardCalculationError" type="Details" deprecated>

          DEPRECATED: Use gift_card_calculation_errors instead.

          **Deprecated.** DEPRECATED: Use gift_card_calculation_errors instead. Use `gift_card_calculation_errors` instead. Removal: 2026-12-01.

          <Expandable title="giftCardCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="orderValidationErrors" type="ApplicationError[]">

          Order validation errors.

          <Expandable title="orderValidationErrors properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="membershipError" type="Details">

          Membership payment methods calculation errors
          For example, will indicate that a line item that must be paid with membership payment doesn't have one or selected memberships are invalid

          <Expandable title="membershipError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discountsCalculationError" type="Details">

          Discount Rule calculation error.

          <Expandable title="discountsCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="shippingCalculationError" type="shippingCalculationError">

          shippingCalculationError

          <Expandable title="shippingCalculationError properties">

            <ResponseField name="generalShippingCalculationError" type="Details">

              General shipping calculation error.

              _Nested structure omitted for brevity — see the `Details` object._

            </ResponseField>

            <ResponseField name="carrierErrors" type="CarrierErrors">

              Errors related to the delivery carrier.

              _Nested structure omitted for brevity — see the `CarrierErrors` object._

            </ResponseField>

            <ResponseField name="selectedShippingOptionError" type="Details">

              Error related to the selected shipping option.

              _Nested structure omitted for brevity — see the `Details` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="weightUnit" type="string">

      Weight measurement unit. Defaults to site's weight unit.

      **Supported values:**

      - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
      - `KG` — Kilograms.
      - `LB` — Pounds.

    </ResponseField>

    <ResponseField name="currency" type="string">

      Currency used for pricing in this store.

      Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="payNow" type="PriceSummary">

      Minimal amount to pay in order to place the order.

      **Read-only.**

      <Expandable title="payNow properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payLater" type="PriceSummary">

      Remaining amount for the order to be fully paid.

      **Read-only.**

      <Expandable title="payLater properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="membershipOptions" type="MembershipOptions">

      Information about valid and invalid memberships, and which ones are selected for usage.

      <Expandable title="membershipOptions properties">

        <ResponseField name="eligibleMemberships" type="Membership[]" deprecated>

          List of payment options that can be used.
          Populated by the legacy ListEligibleMemberships flow.

          **Deprecated.** List of payment options that can be used.
          Populated by the legacy ListEligibleMemberships flow. Use `validated_existing_memberships` instead. Removal: 2026-10-01. Constraints: `maxItems 300`.

          <Expandable title="eligibleMemberships properties">

            <ResponseField name="id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the application providing this payment option.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="MembershipName">

              The name of this membership.

              _Nested structure omitted for brevity — see the `MembershipName` object._

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

              Only populated in the legacy `ListEligibleMemberships` flow, which still requires it. Empty on
              `ListMembershipsByIds`, a by-id lookup that receives no line items to report against.

              Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="credits" type="MembershipPaymentCredits">

              Membership's payment credits (balance information).

              _Nested structure omitted for brevity — see the `MembershipPaymentCredits` object._

            </ResponseField>

            <ResponseField name="expirationDate" type="datetime">

              Membership expiration date.

            </ResponseField>

            <ResponseField name="additionalData" type="object">

              Deprecated. Use `offering_description`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="invalidMemberships" type="InvalidMembership[]" deprecated>

          List of payment options that are owned by the member, but cannot be used due to reason provided.
          Populated by the legacy ListEligibleMemberships flow.

          **Deprecated.** List of payment options that are owned by the member, but cannot be used due to reason provided.
          Populated by the legacy ListEligibleMemberships flow. Use `validated_existing_memberships` instead. Removal: 2026-10-01. Constraints: `maxItems 300`.

          <Expandable title="invalidMemberships properties">

            <ResponseField name="membership" type="Membership">

              Membership details.

              _Nested structure omitted for brevity — see the `Membership` object._

            </ResponseField>

            <ResponseField name="reason" type="string">

              Reason why this membership is invalid and cannot be used.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedMemberships" type="SelectedMembership[]" deprecated>

          The selected membership payment options and which line items they apply to.
          Populated by the legacy ListEligibleMemberships flow.

          **Deprecated.** The selected membership payment options and which line items they apply to.
          Populated by the legacy ListEligibleMemberships flow. Use `validated_existing_memberships` instead. Removal: 2026-10-01. Constraints: `maxItems 300`.

          <Expandable title="selectedMemberships properties">

            <ResponseField name="id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the app providing this payment option.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              IDs of the line items this membership applies to.

              Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalFees" type="AdditionalFee[]">

      Additional fees.

      Constraints: `maxItems 100`.

      <Expandable title="additionalFees properties">

        <ResponseField name="code" type="string">

          Additional fee's unique code (or ID) for future processing.

        </ResponseField>

        <ResponseField name="name" type="string">

          Additional fee's name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ResponseField>

        <ResponseField name="price" type="MultiCurrencyPrice">

          Additional fee's price.

          <Expandable title="price properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="ItemTaxFullDetails">

          Tax details.

          <Expandable title="taxDetails properties">

            <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

              Amount for which tax is calculated.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="taxRate" type="string">

              Tax rate %, as a decimal point between 0 and 1.

              Constraints: `>= 0`, `<= 1`.

            </ResponseField>

            <ResponseField name="totalTax" type="MultiCurrencyPrice">

              Calculated tax, based on `taxable_amount` and `tax_rate`.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="rateBreakdown" type="TaxRateBreakdown[]" deprecated>

              If breakdown exists, the sum of rates in the breakdown must equal `tax_rate`. Deprecated - use 'tax_breakdown' instead.

              **Read-only.** **Deprecated.**

              _Nested structure omitted for brevity — see the `TaxRateBreakdown[]` object._

            </ResponseField>

            <ResponseField name="taxBreakdown" type="TaxBreakdown[]">

              tax information for a line item.

              **Read-only.** Constraints: `minItems 0`, `maxItems 1000`.

              _Nested structure omitted for brevity — see the `TaxBreakdown[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="providerAppId" type="string">

          Provider's app id.

        </ResponseField>

        <ResponseField name="priceBeforeTax" type="MultiCurrencyPrice">

          Additional fee's price before tax.

          <Expandable title="priceBeforeTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceAfterTax" type="MultiCurrencyPrice">

          Additional fee's price after tax.

          <Expandable title="priceAfterTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          Optional - Line items associated with this additional fee.
          If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="source" type="string">

          the source the additional fee was added from

          **Supported values:**

          - `UNKNOWN_ADDITIONAL_FEE_SOURCE`
          - `SERVICE_PLUGIN` — The additional fee was added by an additional fee service plugin
          - `ITEM` — The additional fee was added on the item either via the catalog or on custom line item
          - `MANUAL` — The additional fee was added manually on the request
          - `SHIPPING` — The additional fee was added by the shipping provider
          - `PLATFORM` — The additional fee was added by a Wix vertical and represents a Wix platform fee

        </ResponseField>

        <ResponseField name="translatedName" type="string">

          The translated name of the additional fee. The translation language is determined by the `languages` field in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          Address type tax was calculated against for this fee.

          **Developer Preview.**

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              _Nested structure omitted for brevity — see the `TaxableAddressData` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="violations" type="Violation[]">

      List of validation violations raised by the [Validations service plugin](https://dev.wix.com/docs/rest/business-solutions/e-commerce/service-plugins/validations-integration-service-plugin/introduction).

      **Read-only.**

      <Expandable title="violations properties">

        <ResponseField name="severity" type="string">

          Severity of the violation. The violations are shown on the cart and checkout pages. A warning is displayed as yellow, and allows a site visitor to proceed with caution. An error is displayed as red, and doesn't allow a site visitor to proceed with the eCommerce flow.

          **Supported values:**

          - `WARNING` — The user is allowed to move forward in the flow.
          - `ERROR` — The user is blocked from moving forward in the flow.
          For example, if callerContext is CART - moving to checkout is blocked. if callerContext is CHECKOUT, placing an order is blocked.

        </ResponseField>

        <ResponseField name="target" type="Target">

          Target location on a checkout or cart page where the violation will be displayed.

          <Expandable title="One of the following">

            <ResponseField name="targetType" type="targetType">

              targetType

              _Nested structure omitted for brevity — see the `targetType` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="description" type="string">

          Violation description. Can include rich text. Only HTTP or HTTPS links in the following format are allowed: `<a href="https://www.wix.com">Click me</a>`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    This example estimates price totals with a specific billing address.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/ecom/v1/carts/f97a2939-b1f3-41f2-9fc5-7ecea2060991/estimate-totals' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "billingAddress": {
          "city": "Aix-en-Provence",
          "subdivision": "FR-PAC",
          "postalCode": "13127",
          "streetAddress": {},
          "country": "FR",
          "addressLine": "43 Rue Bonnet",
          "addressLine2": "23rd floor"
        }
      }'
      ```

      ```json Response
      {
        "cart": {
          "id": "f97a2939-b1f3-41f2-9fc5-7ecea2060991",
          "lineItems": [
            {
              "id": "00000000-0000-0000-0000-000000000001",
              "quantity": 3,
              "catalogReference": {
                "catalogItemId": "c8539b66-7a44-fe18-affc-afec4be8562a",
                "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e"
              },
              "productName": {
                "original": "Shirt",
                "translated": "Shirt"
              },
              "url": {
                "relativePath": "/product-page/shirt",
                "url": "https://example.wixsite.com/my-site/product-page/shirt"
              },
              "price": {
                "amount": "10",
                "convertedAmount": "10",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "fullPrice": {
                "amount": "10",
                "convertedAmount": "10",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "priceBeforeDiscounts": {
                "amount": "10",
                "convertedAmount": "10",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "descriptionLines": [],
              "image": {
                "id": "3c76e2_c5331f937348492a97df87b0a3b34ea4~mv2.jpg",
                "url": "https://static.wixstatic.com/media/3c76e2_c5331f937348492a97df87b0a3b34ea4~mv2.jpg",
                "height": 1000,
                "width": 1000
              },
              "availability": {
                "status": "AVAILABLE"
              },
              "physicalProperties": {
                "sku": "364115376135191",
                "shippable": true
              },
              "itemType": {
                "preset": "PHYSICAL"
              },
              "paymentOption": "FULL_PAYMENT_ONLINE"
            }
          ],
          "buyerInfo": {
            "userId": "8b7eef41-7fbb-440b-928a-a442878112a3"
          },
          "currency": "USD",
          "conversionCurrency": "USD",
          "buyerLanguage": "en",
          "siteLanguage": "en",
          "taxIncludedInPrices": false,
          "weightUnit": "LB",
          "appliedDiscounts": [],
          "createdDate": "2023-02-13T10:34:55.727Z",
          "updatedDate": "2023-02-14T09:53:38.613Z",
          "contactInfo": {
            "address": {
              "country": "US",
              "subdivision": "US-NY",
              "city": "New York",
              "postalCode": "10011",
              "addressLine": "235 West 23rd Street",
              "addressLine2": "3rd floor",
              "countryFullname": "United States",
              "subdivisionFullname": "New York"
            },
            "contactDetails": {
              "firstName": "Jane",
              "lastName": "Doe",
              "phone": "+1234567890"
            }
          }
        },
        "calculatedLineItems": [
          {
            "lineItemId": "00000000-0000-0000-0000-000000000001",
            "pricesBreakdown": {
              "totalPriceAfterTax": {
                "amount": "20.00",
                "convertedAmount": "20.00",
                "formattedAmount": "$20.00",
                "formattedConvertedAmount": "$20.00"
              },
              "totalPriceBeforeTax": {
                "amount": "20.00",
                "convertedAmount": "20.00",
                "formattedAmount": "$20.00",
                "formattedConvertedAmount": "$20.00"
              },
              "taxDetails": {
                "taxableAmount": {
                  "amount": "20.00",
                  "convertedAmount": "20.00",
                  "formattedAmount": "$20.00",
                  "formattedConvertedAmount": "$20.00"
                },
                "taxRate": "0.0",
                "totalTax": {
                  "amount": "0.0",
                  "convertedAmount": "0.0",
                  "formattedAmount": "$0.00",
                  "formattedConvertedAmount": "$0.00"
                },
                "rateBreakdown": []
              },
              "totalDiscount": {
                "amount": "0",
                "convertedAmount": "0",
                "formattedAmount": "$0.00",
                "formattedConvertedAmount": "$0.00"
              },
              "price": {
                "amount": "10.00",
                "convertedAmount": "10.00",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "priceBeforeDiscounts": {
                "amount": "10.00",
                "convertedAmount": "10.00",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              },
              "lineItemPrice": {
                "amount": "20.00",
                "convertedAmount": "20.00",
                "formattedAmount": "$20.00",
                "formattedConvertedAmount": "$20.00"
              },
              "fullPrice": {
                "amount": "10.00",
                "convertedAmount": "10.00",
                "formattedAmount": "$10.00",
                "formattedConvertedAmount": "$10.00"
              }
            },
            "paymentOption": "FULL_PAYMENT_ONLINE"
          }
        ],
        "priceSummary": {
          "subtotal": {
            "amount": "20.00",
            "convertedAmount": "20.00",
            "formattedAmount": "$20.00",
            "formattedConvertedAmount": "$20.00"
          },
          "shipping": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "tax": {
            "amount": "0.0",
            "convertedAmount": "0.0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "discount": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "total": {
            "amount": "20.00",
            "convertedAmount": "20.00",
            "formattedAmount": "$20.00",
            "formattedConvertedAmount": "$20.00"
          },
          "additionalFees": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          }
        },
        "shippingInfo": {
          "carrierServiceOptions": []
        },
        "appliedDiscounts": [],
        "calculationErrors": {
          "orderValidationErrors": []
        },
        "weightUnit": "LB",
        "currency": "USD",
        "payNow": {
          "subtotal": {
            "amount": "20.00",
            "convertedAmount": "20.00",
            "formattedAmount": "$20.00",
            "formattedConvertedAmount": "$20.00"
          },
          "shipping": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "tax": {
            "amount": "0.0",
            "convertedAmount": "0.0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "discount": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "total": {
            "amount": "20.00",
            "convertedAmount": "20.00",
            "formattedAmount": "$20.00",
            "formattedConvertedAmount": "$20.00"
          },
          "additionalFees": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          }
        },
        "payLater": {
          "subtotal": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "shipping": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "tax": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "discount": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "total": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          },
          "additionalFees": {
            "amount": "0",
            "convertedAmount": "0",
            "formattedAmount": "$0.00",
            "formattedConvertedAmount": "$0.00"
          }
        },
        "additionalFees": []
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Estimates the subtotal and total for current site visitor’s cart.

    Totals include tax and are based on the selected carrier service, shipping address, and billing information.

    <Note>

    Not passing any optional parameters will only estimate the cart items price totals.

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.READ-ORDERS">Read Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { cart } from '@wix/ecom';

    cart.estimateTotals(_id, options): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Cart ID.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="EstimateTotalsOptions">

      Total estimation options.

      <Expandable title="options properties">

        <ParamField body="selectedShippingOption" type="SelectedShippingOption">

          Selected shipping option.

          Pass this field only when there is no existing shipping option in the cart.

          <Expandable title="selectedShippingOption properties">

            <ParamField body="carrierId" type="string">

              Carrier ID.

            </ParamField>

            <ParamField body="code" type="string">

              Selected shipping option code. For example, "usps_std_overnight".

              Constraints: `minLength 1`, `maxLength 100`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="shippingAddress" type="Address">

          Shipping address. Used for calculating tax and shipping (when applicable).

          <Expandable title="shippingAddress properties">

            <ParamField body="country" type="string">

              Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

            </ParamField>

            <ParamField body="subdivision" type="string">

              Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

            </ParamField>

            <ParamField body="city" type="string">

              City name.

            </ParamField>

            <ParamField body="postalCode" type="string">

              Postal or zip code.

            </ParamField>

            <ParamField body="streetAddress" type="StreetAddress">

              Street address.

              _Nested structure omitted for brevity — see the `StreetAddress` object._

            </ParamField>

            <ParamField body="addressLine" type="string">

              Main address line (usually street name and number).

            </ParamField>

            <ParamField body="addressLine2" type="string">

              Free text providing more detailed address info. Usually contains apt, suite, floor.

            </ParamField>

            <ParamField body="geocode" type="AddressLocation">

              Geocode object containing latitude and longitude coordinates.

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

            <ParamField body="countryFullname" type="string">

              Country's full name.

              **Read-only.**

            </ParamField>

            <ParamField body="subdivisionFullname" type="string">

              Subdivision full-name.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="billingAddress" type="Address">

          Billing address. Used for calculating tax if all the items in the cart are not shippable.

          <Expandable title="billingAddress properties">

            <ParamField body="country" type="string">

              Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.

            </ParamField>

            <ParamField body="subdivision" type="string">

              Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.

            </ParamField>

            <ParamField body="city" type="string">

              City name.

            </ParamField>

            <ParamField body="postalCode" type="string">

              Postal or zip code.

            </ParamField>

            <ParamField body="streetAddress" type="StreetAddress">

              Street address.

              _Nested structure omitted for brevity — see the `StreetAddress` object._

            </ParamField>

            <ParamField body="addressLine" type="string">

              Main address line (usually street name and number).

            </ParamField>

            <ParamField body="addressLine2" type="string">

              Free text providing more detailed address info. Usually contains apt, suite, floor.

            </ParamField>

            <ParamField body="geocode" type="AddressLocation">

              Geocode object containing latitude and longitude coordinates.

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

            <ParamField body="countryFullname" type="string">

              Country's full name.

              **Read-only.**

            </ParamField>

            <ParamField body="subdivisionFullname" type="string">

              Subdivision full-name.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="selectedMemberships" type="SelectedMemberships">

          The selected membership payment options and which line items they apply to.

          <Expandable title="selectedMemberships properties">

            <ParamField body="memberships" type="SelectedMembership[]">

              Selected memberships.

              Constraints: `maxItems 300`.

              _Nested structure omitted for brevity — see the `SelectedMembership[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="calculateTax" type="boolean">

          Whether to calculate tax in the calculation request.

          Default: `true`

        </ParamField>

        <ParamField body="calculateShipping" type="boolean">

          Whether to calculate shipping in the calculation request.

          Default: `true`

        </ParamField>

        <ParamField body="calculateAdditionalFees" type="boolean">

          Whether to calculate additional fees in the calculation request.

          Default: `true`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="cart" type="Cart">

      Cart.

      See the [Cart Object](/api-reference/business-solutions/e-commerce/purchase-flow/cart/cart-object) for this object's fields.

    </ResponseField>

    <ResponseField name="calculatedLineItems" type="CalculatedLineItem[]">

      Calculated line items.

      <Expandable title="calculatedLineItems properties">

        <ResponseField name="lineItemId" type="string">

          Line item ID.

        </ResponseField>

        <ResponseField name="pricesBreakdown" type="LineItemPricesData">

          Price breakdown for this line item.

          <Expandable title="pricesBreakdown properties">

            <ResponseField name="totalPriceAfterTax" type="MultiCurrencyPrice">

              Total price after discounts, tax, and modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="totalPriceBeforeTax" type="MultiCurrencyPrice">

              Total price after discounts and modifiers, and before tax.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="taxDetails" type="ItemTaxFullDetails">

              Tax details.

              _Nested structure omitted for brevity — see the `ItemTaxFullDetails` object._

            </ResponseField>

            <ResponseField name="totalDiscount" type="MultiCurrencyPrice">

              Total discount applied for the line item.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="price" type="MultiCurrencyPrice">

              Catalog price after catalog-defined discount, automatic discounts, and modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="priceBeforeDiscounts" type="MultiCurrencyPrice">

              Item price before automatic discounts, coupons, and global discounts; after catalog-defined discounts and modifiers.
              Defaults to `price` when not provided.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="lineItemPrice" type="MultiCurrencyPrice">

              Total line item price after catalog-defined discounts, automatic discounts, and modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="fullPrice" type="MultiCurrencyPrice">

              Catalog price before any discounts, with modifiers.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="modifiers" type="MultiCurrencyPrice">

              Sum of all modifiers for a single unit of the item.

              **Developer Preview.**

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="depositAmount" type="MultiCurrencyPrice">

              Partial payment out of `price` to be paid upfront during the checkout.
              Applies to catalog items with `lineItem.paymentOption` type `DEPOSIT_ONLINE` only.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="paymentOption" type="string">

          Type of selected payment option for current item. Supported values:
          + `"FULL_PAYMENT_ONLINE"` - The entire payment for this item will happen as part of the checkout
          + `"FULL_PAYMENT_OFFLINE"` - The entire payment for this item will happen after the checkout
          + `"MEMBERSHIP"` - This item cannot be paid via monetary payment options, only via non monetary option such membership. When this option is used, price must be set to 0
          + `"DEPOSIT_ONLINE"` -  Partial payment of the given item will happen as part of the checkout. Amount to be paid is defined by deposit_amount field.

          **Supported values:**

          - `FULL_PAYMENT_ONLINE` — The entire payment for this item happens as part of the checkout.
          - `FULL_PAYMENT_OFFLINE` — The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods.
          - `MEMBERSHIP` — Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online.
          - `DEPOSIT_ONLINE` — Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`.
          - `MEMBERSHIP_OFFLINE` — Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`.
          - `MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER` — Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          tax calculation address.

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              _Nested structure omitted for brevity — see the `TaxableAddressData` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="priceSummary" type="PriceSummary">

      Price summary.

      <Expandable title="priceSummary properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="giftCard" type="GiftCard">

      Applied gift card.

      <Expandable title="giftCard properties">

        <ResponseField name="_id" type="string" deprecated>

          Gift Card ID.

          **Deprecated.**

        </ResponseField>

        <ResponseField name="obfuscatedCode" type="string">

          Gift card obfuscated code.

        </ResponseField>

        <ResponseField name="amount" type="MultiCurrencyPrice">

          Actual amount to be redeemed from the gift card.

          <Expandable title="amount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="appId" type="string">

          App ID of the gift card provider.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="externalId" type="string">

          External ID in the gift card provider's system.
          Used for integration and tracking across different platforms.

        </ResponseField>

        <ResponseField name="requestedAmount" type="MultiCurrencyPrice">

          Requested amount to redeem from the gift card.

          **Developer Preview.**

          <Expandable title="requestedAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="taxSummary" type="TaxSummary">

      Tax summary.

      <Expandable title="taxSummary properties">

        <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

          Amount for which tax is calculated, added from line items.

          **Read-only.**

          <Expandable title="taxableAmount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="totalTax" type="MultiCurrencyPrice">

          Calculated tax, added from line items.

          **Read-only.**

          <Expandable title="totalTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="calculationDetails" type="TaxCalculationDetails" deprecated>

          Tax calculator that was active when the order was created.

          **Deprecated.**

          <Expandable title="calculationDetails properties">

            <ResponseField name="rateType" type="string">

              Rate calculation type.

              **Supported values:**

              - `NO_TAX_COLLECTED` — no tax being collected for this request due to location of purchase
              - `MANUAL_RATE` — manual rate used for calculation
              - `AUTO_RATE` — autotax rate used for calculation
              - `FALLBACK_RATE` — fallback rate used for calculation

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="CalculationDetails" type="CalculationDetails">

              CalculationDetails

              _Nested structure omitted for brevity — see the `CalculationDetails` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="shippingInfo" type="ShippingInformation">

      Shipping information.

      <Expandable title="shippingInfo properties">

        <ResponseField name="region" type="ShippingRegion">

          Shipping region.

          <Expandable title="region properties">

            <ResponseField name="_id" type="string">

              Shipping region ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="string">

              Shipping region name.

              Constraints: `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedCarrierServiceOption" type="SelectedCarrierServiceOption">

          Selected shipping option.

          <Expandable title="selectedCarrierServiceOption properties">

            <ResponseField name="code" type="string">

              Unique identifier of selected option. For example, "usps_std_overnight".

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="title" type="string">

              Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
              For example, "Standard" or "First-Class Package International".

              **Read-only.** Constraints: `maxLength 250`.

            </ResponseField>

            <ResponseField name="logistics" type="DeliveryLogistics">

              Delivery logistics.

              **Read-only.**

              _Nested structure omitted for brevity — see the `DeliveryLogistics` object._

            </ResponseField>

            <ResponseField name="cost" type="SelectedCarrierServiceOptionPrices">

              Shipping costs.

              **Read-only.**

              _Nested structure omitted for brevity — see the `SelectedCarrierServiceOptionPrices` object._

            </ResponseField>

            <ResponseField name="requestedShippingOption" type="boolean">

              Were we able to find the requested shipping option, or otherwise we fallback to the default one (the first)

              **Read-only.**

            </ResponseField>

            <ResponseField name="otherCharges" type="SelectedCarrierServiceOptionOtherCharge[]" deprecated>

              Other charges

              **Deprecated.** Other charges Use `additional_fees` instead. Removal: 2025-10-01.

              _Nested structure omitted for brevity — see the `SelectedCarrierServiceOptionOtherCharge[]` object._

            </ResponseField>

            <ResponseField name="carrierId" type="string">

              This carrier's unique ID

            </ResponseField>

            <ResponseField name="deliveryAllocations" type="DeliveryAllocation[]">

              Delivery solution allocations to different delivery carriers and delivery regions

              **Developer Preview.** Constraints: `maxItems 300`.

              _Nested structure omitted for brevity — see the `DeliveryAllocation[]` object._

            </ResponseField>

            <ResponseField name="partial" type="boolean">

              If the delivery solution is a partial and doesn't apply to all items.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="carrierServiceOptions" type="CarrierServiceOption[]">

          All shipping options.

          <Expandable title="carrierServiceOptions properties">

            <ResponseField name="carrierId" type="string">

              Carrier ID.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="shippingOptions" type="ShippingOption[]">

              Shipping options offered by this carrier for this request.

              _Nested structure omitted for brevity — see the `ShippingOption[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="appliedDiscounts" type="AppliedDiscount[]">

      Applied discounts.

      <Expandable title="appliedDiscounts properties">

        <ResponseField name="discountType" type="string">

          Discount type.

          **Supported values:**

          - `GLOBAL`
          - `SPECIFIC_ITEMS`
          - `SHIPPING`

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          IDs of line items the discount applies to.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="calculationErrors" type="CalculationErrors">

      Calculation errors.

      <Expandable title="calculationErrors properties">

        <ResponseField name="taxCalculationError" type="Details">

          Tax calculation error.

          <Expandable title="taxCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="couponCalculationError" type="Details">

          Coupon calculation error.

          <Expandable title="couponCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="giftCardCalculationError" type="Details" deprecated>

          DEPRECATED: Use gift_card_calculation_errors instead.

          **Deprecated.** DEPRECATED: Use gift_card_calculation_errors instead. Use `gift_card_calculation_errors` instead. Removal: 2026-12-01.

          <Expandable title="giftCardCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="orderValidationErrors" type="ApplicationError[]">

          Order validation errors.

          <Expandable title="orderValidationErrors properties">

            <ResponseField name="code" type="string">

              Error code.

            </ResponseField>

            <ResponseField name="description" type="string">

              Description of the error.

            </ResponseField>

            <ResponseField name="data" type="object">

              Data related to the error.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="membershipError" type="Details">

          Membership payment methods calculation errors
          For example, will indicate that a line item that must be paid with membership payment doesn't have one or selected memberships are invalid

          <Expandable title="membershipError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discountsCalculationError" type="Details">

          Discount Rule calculation error.

          <Expandable title="discountsCalculationError properties">

            <ResponseField name="tracing" type="object" deprecated>

              deprecated in API's - to enable migration from rendering arbitrary tracing to rest response

              **Deprecated.** Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ResponseField>

          </Expandable>

          <Expandable title="One of the following">

            <ResponseField name="kind" type="kind">

              kind

              _Nested structure omitted for brevity — see the `kind` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="shippingCalculationError" type="shippingCalculationError">

          shippingCalculationError

          <Expandable title="shippingCalculationError properties">

            <ResponseField name="generalShippingCalculationError" type="Details">

              General shipping calculation error.

              _Nested structure omitted for brevity — see the `Details` object._

            </ResponseField>

            <ResponseField name="carrierErrors" type="CarrierErrors">

              Errors related to the delivery carrier.

              _Nested structure omitted for brevity — see the `CarrierErrors` object._

            </ResponseField>

            <ResponseField name="selectedShippingOptionError" type="Details">

              Error related to the selected shipping option.

              _Nested structure omitted for brevity — see the `Details` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="weightUnit" type="string">

      Weight measurement unit. Defaults to site's weight unit.

      **Supported values:**

      - `UNSPECIFIED_WEIGHT_UNIT` — Weight unit can't be classified due to an error.
      - `KG` — Kilograms.
      - `LB` — Pounds.

    </ResponseField>

    <ResponseField name="currency" type="string">

      Currency used for pricing in this store.

      Constraints: `format CURRENCY`.

    </ResponseField>

    <ResponseField name="payNow" type="PriceSummary">

      Minimal amount to pay in order to place the order.

      **Read-only.**

      <Expandable title="payNow properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="payLater" type="PriceSummary">

      Remaining amount for the order to be fully paid.

      **Read-only.**

      <Expandable title="payLater properties">

        <ResponseField name="subtotal" type="MultiCurrencyPrice">

          Subtotal of all line items, before discounts and before tax.

          <Expandable title="subtotal properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="shipping" type="MultiCurrencyPrice">

          Total shipping price, before discounts and before tax.

          <Expandable title="shipping properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="tax" type="MultiCurrencyPrice">

          Total tax.

          <Expandable title="tax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="discount" type="MultiCurrencyPrice">

          Total calculated discount value.

          <Expandable title="discount properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="total" type="MultiCurrencyPrice">

          Total price after discounts, gift cards, and tax.

          <Expandable title="total properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="additionalFees" type="MultiCurrencyPrice">

          Total additional fees price before tax.

          <Expandable title="additionalFees properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="membershipOptions" type="MembershipOptions">

      Information about valid and invalid memberships, and which ones are selected for usage.

      <Expandable title="membershipOptions properties">

        <ResponseField name="eligibleMemberships" type="Membership[]" deprecated>

          List of payment options that can be used.
          Populated by the legacy ListEligibleMemberships flow.

          **Deprecated.** List of payment options that can be used.
          Populated by the legacy ListEligibleMemberships flow. Use `validated_existing_memberships` instead. Removal: 2026-10-01. Constraints: `maxItems 300`.

          <Expandable title="eligibleMemberships properties">

            <ResponseField name="_id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the application providing this payment option.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="name" type="MembershipName">

              The name of this membership.

              _Nested structure omitted for brevity — see the `MembershipName` object._

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.

              Only populated in the legacy `ListEligibleMemberships` flow, which still requires it. Empty on
              `ListMembershipsByIds`, a by-id lookup that receives no line items to report against.

              Constraints: `minLength 1`, `maxLength 100`, `maxItems 300`.

            </ResponseField>

            <ResponseField name="credits" type="MembershipPaymentCredits">

              Membership's payment credits (balance information).

              _Nested structure omitted for brevity — see the `MembershipPaymentCredits` object._

            </ResponseField>

            <ResponseField name="expirationDate" type="datetime">

              Membership expiration date.

            </ResponseField>

            <ResponseField name="additionalData" type="object">

              Deprecated. Use `offering_description`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="invalidMemberships" type="InvalidMembership[]" deprecated>

          List of payment options that are owned by the member, but cannot be used due to reason provided.
          Populated by the legacy ListEligibleMemberships flow.

          **Deprecated.** List of payment options that are owned by the member, but cannot be used due to reason provided.
          Populated by the legacy ListEligibleMemberships flow. Use `validated_existing_memberships` instead. Removal: 2026-10-01. Constraints: `maxItems 300`.

          <Expandable title="invalidMemberships properties">

            <ResponseField name="membership" type="Membership">

              Membership details.

              _Nested structure omitted for brevity — see the `Membership` object._

            </ResponseField>

            <ResponseField name="reason" type="string">

              Reason why this membership is invalid and cannot be used.

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="selectedMemberships" type="SelectedMembership[]" deprecated>

          The selected membership payment options and which line items they apply to.
          Populated by the legacy ListEligibleMemberships flow.

          **Deprecated.** The selected membership payment options and which line items they apply to.
          Populated by the legacy ListEligibleMemberships flow. Use `validated_existing_memberships` instead. Removal: 2026-10-01. Constraints: `maxItems 300`.

          <Expandable title="selectedMemberships properties">

            <ResponseField name="_id" type="string">

              Membership ID.

              Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              ID of the app providing this payment option.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="lineItemIds" type="string[]">

              IDs of the line items this membership applies to.

              Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 300`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalFees" type="AdditionalFee[]">

      Additional fees.

      Constraints: `maxItems 100`.

      <Expandable title="additionalFees properties">

        <ResponseField name="code" type="string">

          Additional fee's unique code (or ID) for future processing.

        </ResponseField>

        <ResponseField name="name" type="string">

          Additional fee's name.

          Constraints: `minLength 1`, `maxLength 50`.

        </ResponseField>

        <ResponseField name="price" type="MultiCurrencyPrice">

          Additional fee's price.

          <Expandable title="price properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxDetails" type="ItemTaxFullDetails">

          Tax details.

          <Expandable title="taxDetails properties">

            <ResponseField name="taxableAmount" type="MultiCurrencyPrice">

              Amount for which tax is calculated.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="taxRate" type="string">

              Tax rate %, as a decimal point between 0 and 1.

              Constraints: `>= 0`, `<= 1`.

            </ResponseField>

            <ResponseField name="totalTax" type="MultiCurrencyPrice">

              Calculated tax, based on `taxable_amount` and `tax_rate`.

              _Nested structure omitted for brevity — see the `MultiCurrencyPrice` object._

            </ResponseField>

            <ResponseField name="rateBreakdown" type="TaxRateBreakdown[]" deprecated>

              If breakdown exists, the sum of rates in the breakdown must equal `tax_rate`. Deprecated - use 'tax_breakdown' instead.

              **Read-only.** **Deprecated.**

              _Nested structure omitted for brevity — see the `TaxRateBreakdown[]` object._

            </ResponseField>

            <ResponseField name="taxBreakdown" type="TaxBreakdown[]">

              tax information for a line item.

              **Read-only.** Constraints: `minItems 0`, `maxItems 1000`.

              _Nested structure omitted for brevity — see the `TaxBreakdown[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="providerAppId" type="string">

          Provider's app id.

        </ResponseField>

        <ResponseField name="priceBeforeTax" type="MultiCurrencyPrice">

          Additional fee's price before tax.

          <Expandable title="priceBeforeTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="priceAfterTax" type="MultiCurrencyPrice">

          Additional fee's price after tax.

          <Expandable title="priceAfterTax properties">

            <ResponseField name="amount" type="string">

              Amount.

              Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="convertedAmount" type="string">

              Converted amount.

              **Read-only.** Constraints: `>= 0`, `<= 1000000000000000`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

            <ResponseField name="formattedConvertedAmount" type="string">

              Converted amount formatted with currency symbol.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="lineItemIds" type="string[]">

          Optional - Line items associated with this additional fee.
          If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="source" type="string">

          the source the additional fee was added from

          **Supported values:**

          - `UNKNOWN_ADDITIONAL_FEE_SOURCE`
          - `SERVICE_PLUGIN` — The additional fee was added by an additional fee service plugin
          - `ITEM` — The additional fee was added on the item either via the catalog or on custom line item
          - `MANUAL` — The additional fee was added manually on the request
          - `SHIPPING` — The additional fee was added by the shipping provider
          - `PLATFORM` — The additional fee was added by a Wix vertical and represents a Wix platform fee

        </ResponseField>

        <ResponseField name="translatedName" type="string">

          The translated name of the additional fee. The translation language is determined by the `languages` field in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).

        </ResponseField>

        <ResponseField name="taxableAddress" type="TaxableAddress">

          Address type tax was calculated against for this fee.

          **Developer Preview.**

          <Expandable title="One of the following">

            <ResponseField name="TaxableAddressData" type="TaxableAddressData">

              TaxableAddressData

              _Nested structure omitted for brevity — see the `TaxableAddressData` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="violations" type="Violation[]">

      List of validation violations raised by the [Validations service plugin](https://dev.wix.com/docs/rest/business-solutions/e-commerce/service-plugins/validations-integration-service-plugin/introduction).

      **Read-only.**

      <Expandable title="violations properties">

        <ResponseField name="severity" type="string">

          Severity of the violation. The violations are shown on the cart and checkout pages. A warning is displayed as yellow, and allows a site visitor to proceed with caution. An error is displayed as red, and doesn't allow a site visitor to proceed with the eCommerce flow.

          **Supported values:**

          - `WARNING` — The user is allowed to move forward in the flow.
          - `ERROR` — The user is blocked from moving forward in the flow.
          For example, if callerContext is CART - moving to checkout is blocked. if callerContext is CHECKOUT, placing an order is blocked.

        </ResponseField>

        <ResponseField name="target" type="Target">

          Target location on a checkout or cart page where the violation will be displayed.

          <Expandable title="One of the following">

            <ResponseField name="targetType" type="targetType">

              targetType

              _Nested structure omitted for brevity — see the `targetType` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="description" type="string">

          Violation description. Can include rich text. Only HTTP or HTTPS links in the following format are allowed: `<a href="https://www.wix.com">Click me</a>`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-estimate-totals" id="ex-sdk-estimate-totals-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-estimate-totals" id="ex-sdk-estimate-totals-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-estimate-totals-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-estimate-totals-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Estimate a cart's totals</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">estimateTotals (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-estimate-totals-0"><span className="wix-picker-name">Estimate a cart's totals</span></label>
    <label htmlFor="ex-sdk-estimate-totals-1"><span className="wix-picker-name">estimateTotals (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Estimate a cart's totals
    /*****************************************
     * Backend code - my-backend-file.web.js/ts *
     ****************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { cart } from '@wix/ecom';

    export const myEstimateTotalsFunction = webMethod(Permissions.Anyone, async (cartId, estimateOptions) => {
      try {
        const estimatedCartTotals = await cart.estimateTotals(cartId, estimateOptions);
        console.log('Success! Cart totals estimated:', estimatedCartTotals);
        return estimatedCartTotals;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    });

    /*************
     * Page code *
     ************/

    import { myEstimateTotalsFunction } from 'backend/my-backend-file.web';

    // Sample cartId:
    const cartId = '96a61a4b-6b61-47d1-a039-0213a8230ccd';

    // Sample options object:
    const estimateOptions = {
      "selectedShippingOption": {
        "code": "standard_us_shipping"
      },
      "shippingAddress": {
        "addressLine1": "235 West 23rd Street",
        "addressLine2": "3rd floor",
        "city": "New York",
        "country": "US",
        "postalCode": "10011",
        "streetAddress": {
          "name": "West 23rd Street",
          "number": "235"
        },
        "subdivision": "US-NY"
      },
      "billingAddress": {
        "addressLine1": "235 West 23rd Street",
        "addressLine2": "3rd floor",
        "city": "New York",
        "country": "US",
        "postalCode": "10011",
        "streetAddress": {
          "name": "West 23rd Street",
          "number": "235"
        },
        "subdivision": "US-NY"
      }
    };

    myEstimateTotalsFunction(cartId, estimateOptions)
      .then((estimatedCartTotals) => {
        const formattedShippingPrice = estimatedCartTotals.priceSummary.shipping.formattedAmount;
        const estimatedCartTotal = estimatedCartTotals.priceSummary.total.formattedAmount;

        console.log('Success! Cart totals estimated:', estimatedCartTotals);
        return estimatedCartTotals;
      })
      .catch((error) => {
        console.error(error);
        // Handle the error
      });

    /* Promise resolves to:
     *
     * {
     *   "cart": {
     *     "_id": "96a61a4b-6b61-47d1-a039-0213a8230ccd",
     *     "lineItems": [
     *       {
     *         "_id": "00000000-0000-0000-0000-000000000001",
     *         "quantity": 5,
     *         "catalogReference": {
     *           "catalogItemId": "c8539b66-7a44-fe18-affc-afec4be8562a",
     *           "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e"
     *         },
     *         "productName": {
     *           "original": "Shirt",
     *           "translated": "Shirt"
     *         },
     *         "url": "https://example.wixsite.com",
     *         "price": {
     *           "amount": "10",
     *           "convertedAmount": "10",
     *           "formattedAmount": "€10.00",
     *           "formattedConvertedAmount": "€10.00"
     *         },
     *         "fullPrice": {
     *           "amount": "10",
     *           "convertedAmount": "10",
     *           "formattedAmount": "€10.00",
     *           "formattedConvertedAmount": "€10.00"
     *         },
     *         "priceBeforeDiscounts": {
     *           "amount": "10",
     *           "convertedAmount": "10",
     *           "formattedAmount": "€10.00",
     *           "formattedConvertedAmount": "€10.00"
     *         },
     *         "descriptionLines": [],
     *         "image": "wix:image://v1/3c76e2_c5331f937348492a97df87b0a3b34ea4~mv2.jpg#originWidth=1000&originHeight=1000",
     *         "availability": {
     *           "status": "AVAILABLE"
     *         },
     *         "physicalProperties": {
     *           "sku": "364115376135191",
     *           "shippable": true
     *         },
     *         "itemType": {
     *           "preset": "PHYSICAL"
     *         },
     *         "paymentOption": "FULL_PAYMENT_ONLINE"
     *       }
     *     ],
     *     "buyerInfo": {
     *       "contactId": "f7dc17a6-825a-466e-a78e-c4abea0217db",
     *       "memberId": "c43190d2-eea3-493e-b6e8-f146850c6873"
     *     },
     *     "currency": "EUR",
     *     "conversionCurrency": "EUR",
     *     "buyerLanguage": "en",
     *     "siteLanguage": "en",
     *     "taxIncludedInPrices": false,
     *     "weightUnit": "KG",
     *     "subtotal": {
     *       "amount": "50",
     *       "convertedAmount": "50",
     *       "formattedAmount": "€50.00",
     *       "formattedConvertedAmount": "€50.00"
     *     },
     *     "appliedDiscounts": [],
     *     "_createdDate": "2022-05-16T12:04:01.244Z",
     *     "_updatedDate": "2022-05-23T11:55:30.023Z"
     *   },
     *   "calculatedLineItems": [
     *     {
     *       "lineItemId": "00000000-0000-0000-0000-000000000001",
     *       "pricesBreakdown": {
     *         "totalPriceAfterTax": {
     *           "amount": "50.00",
     *           "convertedAmount": "50.00",
     *           "formattedAmount": "€50.00",
     *           "formattedConvertedAmount": "€50.00"
     *         },
     *         "totalPriceBeforeTax": {
     *           "amount": "50.00",
     *           "convertedAmount": "50.00",
     *           "formattedAmount": "€50.00",
     *           "formattedConvertedAmount": "€50.00"
     *         },
     *         "taxDetails": {
     *           "taxableAmount": {
     *             "amount": "50.00",
     *             "convertedAmount": "50.00",
     *             "formattedAmount": "€50.00",
     *             "formattedConvertedAmount": "€50.00"
     *           },
     *           "taxRate": "0.0",
     *           "totalTax": {
     *             "amount": "0.0",
     *             "convertedAmount": "0.0",
     *             "formattedAmount": "€0.00",
     *             "formattedConvertedAmount": "€0.00"
     *           },
     *           "rateBreakdown": []
     *         },
     *         "totalDiscount": {
     *           "amount": "0",
     *           "convertedAmount": "0",
     *           "formattedAmount": "€0.00",
     *           "formattedConvertedAmount": "€0.00"
     *         },
     *         "price": {
     *           "amount": "10.00",
     *           "convertedAmount": "10.00",
     *           "formattedAmount": "€10.00",
     *           "formattedConvertedAmount": "€10.00"
     *         },
     *         "priceBeforeDiscounts": {
     *           "amount": "10.00",
     *           "convertedAmount": "",
     *           "formattedAmount": "",
     *           "formattedConvertedAmount": ""
     *         }
     *       },
     *       "paymentOption": "FULL_PAYMENT_ONLINE"
     *     }
     *   ],
     *   "priceSummary": {
     *     "subtotal": {
     *       "amount": "50.00",
     *       "convertedAmount": "50.00",
     *       "formattedAmount": "€50.00",
     *       "formattedConvertedAmount": "€50.00"
     *     },
     *     "shipping": {
     *       "amount": "10.0",
     *       "convertedAmount": "10.0",
     *       "formattedAmount": "€10.00",
     *       "formattedConvertedAmount": "€10.00"
     *     },
     *     "tax": {
     *       "amount": "0.0",
     *       "convertedAmount": "0.0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "discount": {
     *       "amount": "0",
     *       "convertedAmount": "0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "total": {
     *       "amount": "60.00",
     *       "convertedAmount": "60.00",
     *       "formattedAmount": "€60.00",
     *       "formattedConvertedAmount": "€60.00"
     *     }
     *   },
     *   "shippingInfo": {
     *     "region": {
     *       "_id": "009fbe5d-89d3-7825-cbbf-1aab4d908b73",
     *       "name": "USA shipping"
     *     },
     *     "selectedCarrierServiceOption": {
     *       "code": "ed5bbce2-9533-dff4-7db0-13702fd139c5",
     *       "title": "Standard US Shipping",
     *       "logistics": {
     *         "deliveryTime": ""
     *       },
     *       "cost": {
     *         "totalPriceAfterTax": {
     *           "amount": "10.0",
     *           "convertedAmount": "10.0",
     *           "formattedAmount": "€10.00",
     *           "formattedConvertedAmount": "€10.00"
     *         },
     *         "totalPriceBeforeTax": {
     *           "amount": "10",
     *           "convertedAmount": "10",
     *           "formattedAmount": "€10.00",
     *           "formattedConvertedAmount": "€10.00"
     *         },
     *         "taxDetails": {
     *           "taxRate": "0.0",
     *           "totalTax": {
     *             "amount": "0.0",
     *             "convertedAmount": "0.0",
     *             "formattedAmount": "€0.00",
     *             "formattedConvertedAmount": "€0.00"
     *           },
     *           "rateBreakdown": []
     *         },
     *         "price": {
     *           "amount": "10",
     *           "convertedAmount": "10",
     *           "formattedAmount": "€10.00",
     *           "formattedConvertedAmount": "€10.00"
     *         }
     *       },
     *       "requestedShippingOption": false,
     *       "otherCharges": [],
     *       "carrierId": "c8a08776-c095-4dec-8553-8f9698d86adc"
     *     },
     *     "carrierServiceOptions": [
     *       {
     *         "carrierId": "c8a08776-c095-4dec-8553-8f9698d86adc",
     *         "shippingOptions": [
     *           {
     *             "code": "ed5bbce2-9533-dff4-7db0-13702fd139c5",
     *             "title": "Standard US Shipping",
     *             "logistics": {
     *               "deliveryTime": ""
     *             },
     *             "cost": {
     *               "price": {
     *                 "amount": "10",
     *                 "convertedAmount": "10",
     *                 "formattedAmount": "€10.00",
     *                 "formattedConvertedAmount": "€10.00"
     *               },
     *               "otherCharges": []
     *             }
     *           }
     *         ]
     *       }
     *     ]
     *   },
     *   "appliedDiscounts": [],
     *   "calculationErrors": {
     *     "orderValidationErrors": []
     *   },
     *   "weightUnit": "KG",
     *   "currency": "EUR",
     *   "payNow": {
     *     "subtotal": {
     *       "amount": "50.00",
     *       "convertedAmount": "50.00",
     *       "formattedAmount": "€50.00",
     *       "formattedConvertedAmount": "€50.00"
     *     },
     *     "shipping": {
     *       "amount": "10.0",
     *       "convertedAmount": "10.0",
     *       "formattedAmount": "€10.00",
     *       "formattedConvertedAmount": "€10.00"
     *     },
     *     "tax": {
     *       "amount": "0.0",
     *       "convertedAmount": "0.0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "discount": {
     *       "amount": "0",
     *       "convertedAmount": "0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "total": {
     *       "amount": "60.00",
     *       "convertedAmount": "60.00",
     *       "formattedAmount": "€60.00",
     *       "formattedConvertedAmount": "€60.00"
     *     }
     *   },
     *   "payLater": {
     *     "subtotal": {
     *       "amount": "0",
     *       "convertedAmount": "0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "shipping": {
     *       "amount": "0",
     *       "convertedAmount": "0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "tax": {
     *       "amount": "0",
     *       "convertedAmount": "0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "discount": {
     *       "amount": "0",
     *       "convertedAmount": "0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     },
     *     "total": {
     *       "amount": "0",
     *       "convertedAmount": "0",
     *       "formattedAmount": "€0.00",
     *       "formattedConvertedAmount": "€0.00"
     *     }
     *   },
     *   "membershipOptions": {
     *     "eligibleMemberships": [],
     *     "invalidMemberships": [],
     *     "selectedMemberships": []
     *   }
     * }
     *
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript estimateTotals (self-hosted)
    import { createClient } from '@wix/sdk';
    import { cart } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { cart },
      // Include the auth strategy and host as relevant
    });

    async function estimateTotals(_id,options) {
      const response = await myWixClient.cart.estimateTotals(_id,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
