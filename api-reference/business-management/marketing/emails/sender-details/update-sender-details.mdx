---
title: "Update Sender Details"
description: "Updates a sender."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a sender.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/sender-details/v1/sender-details/{senderDetails.id}
    ```

    ## Path Params

    <ParamField path="senderDetails.id" type="string" required>

      Sender details ID.

    </ParamField>

    ## Body Params

    <ParamField body="senderDetails" type="SenderDetails" required>

      Sender details to update.

      See the [Sender Details Object](/api-reference/business-management/marketing/emails/sender-details/sender-details-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="senderDetails" type="SenderDetails">

      The updated sender details.

      See the [Sender Details Object](/api-reference/business-management/marketing/emails/sender-details/sender-details-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Details Updated | `wix.promote.v1.sender_details_updated` | Triggered when sender details are updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X PATCH 'https://www.wixapis.com/sender-details/v1/sender-details/3dcaeca5-2b09-441c-86ab-808d85061511' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
          "senderDetails": {
              "fromName": "Johnny Doe",
              "fromEmailAddress": "john.doe@example.com"
          }
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "senderDetails": {
              "id": "3dcaeca5-2b09-441c-86ab-808d85061511",
              "createdDate": "2024-01-01T00:00:00.000Z",
              "updatedDate": "2024-01-02T00:00:00.000Z",
              "fromName": "Johnny Doe",
              "fromEmailAddress": "john.doe@example.com",
              "default": true
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a sender.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderDetails } from '@wix/sender-details';

    senderDetails.updateSenderDetails(_id, senderDetails): Promise<SenderDetails>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Sender details ID.

      **Read-only.**

    </ParamField>

    <ParamField body="senderDetails" type="UpdateSenderDetails" required>

      Sender details to update.

      <Expandable title="senderDetails properties">

        <ParamField body="_id" type="string">

          Sender details ID.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the sender details were created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the sender details were last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="fromName" type="string">

          Sender name. This name appears in the email as "from:" name.

        </ParamField>

        <ParamField body="fromEmailAddress" type="string">

          Sender email address. Recipients can also reply to this email address.

          > **Note:** An email address must be verified using https://dev.wix.com/docs/api-reference/business-management/marketing/emails/sender-emails/introduction#sender-email-verification-flow before it can be used to create a sender.

        </ParamField>

        <ParamField body="default" type="boolean">

          Whether the sender is default.

          **Read-only.**

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Extensions allowing users to save custom data related to the sender details.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<SenderDetails>`.

    See the [Sender Details Object](/api-reference/business-management/marketing/emails/sender-details/sender-details-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Sender Details Updated | `wix.promote.v1.sender_details_updated` | Triggered when sender details are updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-sender-details" id="ex-sdk-update-sender-details-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-sender-details" id="ex-sdk-update-sender-details-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-sender-details" id="ex-sdk-update-sender-details-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-sender-details-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-sender-details-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateSenderDetails</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateSenderDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateSenderDetails (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-sender-details-0"><span className="wix-picker-name">updateSenderDetails</span></label>
    <label htmlFor="ex-sdk-update-sender-details-1"><span className="wix-picker-name">updateSenderDetails (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-sender-details-2"><span className="wix-picker-name">updateSenderDetails (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateSenderDetails
    import { senderDetails } from '@wix/sender-details';

    async function updateSenderDetails(_id,senderDetails) {
      const response = await senderDetails.updateSenderDetails(_id,senderDetails);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateSenderDetails (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderDetails } from '@wix/sender-details';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderDetails },
      // Include the auth strategy and host as relevant
    });

    async function updateSenderDetails(_id,senderDetails) {
      const response = await myWixClient.senderDetails.updateSenderDetails(_id,senderDetails);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateSenderDetails (with elevated permissions)
    import { senderDetails } from '@wix/sender-details';
    import { auth } from '@wix/essentials';

    async function myUpdateSenderDetailsMethod(_id,senderDetails) {
      const elevatedUpdateSenderDetails = auth.elevate(senderDetails.updateSenderDetails);
      const response = await elevatedUpdateSenderDetails(_id,senderDetails);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
