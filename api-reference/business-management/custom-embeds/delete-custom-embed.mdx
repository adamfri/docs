---
title: "Delete Custom Embed"
description: "Permanently deletes a custom embed."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a custom embed.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS">SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/embeds/v1/custom-embeds/{customEmbedId}
    ```

    ## Path Params

    <ParamField path="customEmbedId" type="string" required>

      Custom embed ID to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteCustomEmbedResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CUSTOM_EMBED_NOT_FOUND` | Couldn't find the custom embed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Custom Embed Deleted | `wix.embeds.v1.custom_embed_deleted` | Triggered when a custom embed is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/embeds/v1/custom-embeds/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a custom embed.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS">SCOPE.EDITOR.MANAGE_CUSTOM_EMBEDS</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customEmbeds } from '@wix/embeds';

    customEmbeds.deleteCustomEmbed(customEmbedId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="customEmbedId" type="string" required>

      Custom embed ID to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `404` | `NOT_FOUND` | `CUSTOM_EMBED_NOT_FOUND` | Couldn't find the custom embed. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Custom Embed Deleted | `wix.embeds.v1.custom_embed_deleted` | Triggered when a custom embed is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-custom-embed" id="ex-sdk-delete-custom-embed-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-custom-embed" id="ex-sdk-delete-custom-embed-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-custom-embed" id="ex-sdk-delete-custom-embed-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-custom-embed-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-custom-embed-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteCustomEmbed</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCustomEmbed (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCustomEmbed (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-custom-embed-0"><span className="wix-picker-name">deleteCustomEmbed</span></label>
    <label htmlFor="ex-sdk-delete-custom-embed-1"><span className="wix-picker-name">deleteCustomEmbed (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-custom-embed-2"><span className="wix-picker-name">deleteCustomEmbed (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteCustomEmbed
    import { customEmbeds } from '@wix/embeds';

    async function deleteCustomEmbed(customEmbedId) {
      const response = await customEmbeds.deleteCustomEmbed(customEmbedId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCustomEmbed (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customEmbeds } from '@wix/embeds';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customEmbeds },
      // Include the auth strategy and host as relevant
    });

    async function deleteCustomEmbed(customEmbedId) {
      const response = await myWixClient.customEmbeds.deleteCustomEmbed(customEmbedId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCustomEmbed (with elevated permissions)
    import { customEmbeds } from '@wix/embeds';
    import { auth } from '@wix/essentials';

    async function myDeleteCustomEmbedMethod(customEmbedId) {
      const elevatedDeleteCustomEmbed = auth.elevate(customEmbeds.deleteCustomEmbed);
      const response = await elevatedDeleteCustomEmbed(customEmbedId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
