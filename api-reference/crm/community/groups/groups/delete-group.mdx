---
title: "Delete Group"
description: "Delete Group in the Wix Group API."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/social-groups-proxy/groups/v2/groups/{groupId}
    ```

    ## Path Params

    <ParamField path="groupId" type="string" required>

      ID of the group to delete.

    </ParamField>

    ## Response Object

    <ResponseField name="group" type="Group">

      Deleted group.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Group Deleted | `wix.social_groups.v2.group_deleted` | Triggered when a group is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X DELETE \
          https://www.wixapis.com/social-groups-proxy/groups/v2/groups/8a258cf5-49f3-41dc-b320-65ec2598315b \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "group": {
              "id": "8a258cf5-49f3-41dc-b320-65ec2598315b",
              "slug": "group-1",
              "privacyStatus": "PUBLIC",
              "name": "Group 1",
              "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
              "memberTitle": "Friends",
              "coverImage": {
                  "image": {
                      "mediaId": "4d6eab_666134ee68464d76aa838619344c4369~mv2.jpg",
                      "width": 1024,
                      "height": 150
                  }
              },
              "settings": {
                  "membersCanInvite": false,
                  "membersCanApprove": false,
                  "welcomeMemberPostEnabled": true,
                  "groupDetailsChangedPostEnabled": true,
                  "showMemberList": true
              },
              "membersCount": 12,
              "ownerId": "f98e4407-15bf-43a3-87fb-a954c49bb20f",
              "createdDate": "2020-11-06T14:03:50Z",
              "updatedDate": "2020-12-19T16:58:07Z",
              "lastActivityDate": "2020-12-10T11:25:08Z"
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    ## Method Declaration

    ```typescript
    import { groups } from '@wix/groups';

    groups.deleteGroup(groupId): Promise<DeleteGroupResponse>
    ```

    ## Method Parameters

    <ParamField body="groupId" type="string" required>

      ID of the group to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<DeleteGroupResponse>`.

    <ResponseField name="group" type="Group">

      Deleted group.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Group Deleted | `wix.social_groups.v2.group_deleted` | Triggered when a group is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-group" id="ex-sdk-delete-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-group" id="ex-sdk-delete-group-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-group" id="ex-sdk-delete-group-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteGroup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-group-0"><span className="wix-picker-name">deleteGroup</span></label>
    <label htmlFor="ex-sdk-delete-group-1"><span className="wix-picker-name">deleteGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-group-2"><span className="wix-picker-name">deleteGroup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteGroup
    import { groups } from '@wix/groups';

    async function deleteGroup(groupId) {
      const response = await groups.deleteGroup(groupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { groups } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { groups },
      // Include the auth strategy and host as relevant
    });

    async function deleteGroup(groupId) {
      const response = await myWixClient.groups.deleteGroup(groupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteGroup (with elevated permissions)
    import { groups } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myDeleteGroupMethod(groupId) {
      const elevatedDeleteGroup = auth.elevate(groups.deleteGroup);
      const response = await elevatedDeleteGroup(groupId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
