---
title: "onCategoryDeleted"
description: "Triggered when a category is removed."
icon: "bolt"
tag: "Deprecated"
searchable: false
---

<Warning>

  **Deprecated.** Scheduled for removal on 2025-10-15.

</Warning>

Triggered when a category is removed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.category_deleted` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onCategoryDeleted
  import { categories } from '@wix/forum';

  categories.onCategoryDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categories } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        categories,
      },
  });

  wixClient.categories.onCategoryDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
