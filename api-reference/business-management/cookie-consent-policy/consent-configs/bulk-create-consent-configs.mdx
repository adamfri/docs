---
title: "Bulk Create Consent Configs"
description: "Creates multiple consent configs."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple consent configs.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bu/legal/v1/bulk/consent-configs/create
    ```

    ## Body Params

    <ParamField body="consentConfigs" type="ConsentConfig[]" required>

      List of consent configs to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the created consent configs in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkConsentConfigResult[]">

      Consent configs created by bulk action.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="ConsentConfig">

          Consent config item, returned when `return_entity` = `true`.

          See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Created | `wix.bu.legal.v1.consent_config_created` | Triggered when a consent config is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Create multiple consent configs in a single operation

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/consent/consent-config/v1/bulk/consent-configs/create' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "consentConfigs": [
            {
              "configType": "TAG_OVERRIDE",
              "consentCategory": "essential",
              "tagDetails": {
                "appId": "73bc078f-b2d7-4ee7-a6e6-41b28ec661d9",
                "componentId": "c7afb240-4cbb-4bbb-adeb-936818698cc9",
                "version": "^1.0.0"
              }
            },
            {
              "configType": "TAG_OVERRIDE",
              "consentCategory": "analytics",
              "tagDetails": {
                "appId": "84cd189g-6d3e-5ff8-b7f7-52c39fd772e0",
                "componentId": "d8bfc351-5dcc-5ccc-bfec-047929709dd0",
                "version": "^2.0.0"
              }
            }
          ],
          "returnEntity": true
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "334afbf6-5c8b-4b43-9e9e-dd28664ca28f",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "334afbf6-5c8b-4b43-9e9e-dd28664ca28f",
              "revision": "1",
              "createdDate": "2025-03-04T07:58:24.131Z",
              "updatedDate": "2025-03-04T07:58:24.131Z",
              "configType": "TAG_OVERRIDE",
              "consentCategory": "essential",
              "tagDetails": {
                "appId": "73bc078f-b2d7-4ee7-a6e6-41b28ec661d9",
                "componentId": "c7afb240-4cbb-4bbb-adeb-936818698cc9",
                "version": "^1.0.0"
              }
            }
          },
          {
            "itemMetadata": {
              "id": "445bfcg7-6d9c-5c54-0f0f-ee39775db39g",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "id": "445bfcg7-6d9c-5c54-0f0f-ee39775db39g",
              "revision": "1",
              "createdDate": "2025-03-04T07:58:25.142Z",
              "updatedDate": "2025-03-04T07:58:25.142Z",
              "configType": "TAG_OVERRIDE",
              "consentCategory": "analytics",
              "tagDetails": {
                "appId": "84cd189g-6d3e-5ff8-b7f7-52c39fd772e0",
                "componentId": "d8bfc351-5dcc-5ccc-bfec-047929709dd0",
                "version": "^2.0.0"
              }
            }
          }
        ],
        "bulkActionMetadata": {
          "totalCount": 2,
          "successCount": 2,
          "failureCount": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates multiple consent configs.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { consentConfig } from '@wix/consent-policy';

    consentConfig.bulkCreateConsentConfigs(consentConfigs, options): Promise
    ```

    ## Method Parameters

    <ParamField body="consentConfigs" type="ConsentConfig[]" required>

      List of consent configs to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateConsentConfigsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return the created consent configs in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkConsentConfigResult[]">

      Consent configs created by bulk action.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Bulk action metadata.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="ConsentConfig">

          Consent config item, returned when `return_entity` = `true`.

          See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Created | `wix.bu.legal.v1.consent_config_created` | Triggered when a consent config is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-consent-configs" id="ex-sdk-bulk-create-consent-configs-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-consent-configs" id="ex-sdk-bulk-create-consent-configs-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-create-consent-configs" id="ex-sdk-bulk-create-consent-configs-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-consent-configs-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-consent-configs-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateConsentConfigs</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateConsentConfigs (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateConsentConfigs (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-consent-configs-0"><span className="wix-picker-name">bulkCreateConsentConfigs</span></label>
    <label htmlFor="ex-sdk-bulk-create-consent-configs-1"><span className="wix-picker-name">bulkCreateConsentConfigs (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-create-consent-configs-2"><span className="wix-picker-name">bulkCreateConsentConfigs (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkCreateConsentConfigs
    import { consentConfig } from '@wix/consent-policy';

    async function bulkCreateConsentConfigs(consentConfigs,options) {
      const response = await consentConfig.bulkCreateConsentConfigs(consentConfigs,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateConsentConfigs (self-hosted)
    import { createClient } from '@wix/sdk';
    import { consentConfig } from '@wix/consent-policy';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { consentConfig },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateConsentConfigs(consentConfigs,options) {
      const response = await myWixClient.consentConfig.bulkCreateConsentConfigs(consentConfigs,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateConsentConfigs (with elevated permissions)
    import { consentConfig } from '@wix/consent-policy';
    import { auth } from '@wix/essentials';

    async function myBulkCreateConsentConfigsMethod(consentConfigs,options) {
      const elevatedBulkCreateConsentConfigs = auth.elevate(consentConfig.bulkCreateConsentConfigs);
      const response = await elevatedBulkCreateConsentConfigs(consentConfigs,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
