---
title: "Get Conversation"
description: "Retrieves a conversation by its ID."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a conversation by its ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="containers:chat:v1:conversation:get_conversation">containers:chat:v1:conversation:get_conversation</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    GET https://www.wixapis.com/containers/chat/v1/conversations/{conversationId}
    ```

    ## Path Params

    <ParamField path="conversationId" type="string" required>

      Conversation ID.

    </ParamField>

    ## Response Object

    <ResponseField name="conversation" type="Conversation">

      Retrieved conversation.

      See the [Conversation Object](/api-reference/mobile/containers-app/containers-chat/conversation-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a conversation by its ID.

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/containers/chat/v1/conversations/1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "conversation": {
          "id": "1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10",
          "projectId": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
          "title": "new chat",
          "status": "ACTIVE",
          "revision": "1",
          "createdDate": "2026-07-29T09:00:00.000Z",
          "updatedDate": "2026-07-29T09:00:00.000Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a conversation by its ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="containers:chat:v1:conversation:get_conversation">containers:chat:v1:conversation:get_conversation</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { chat } from '@wix/containers';

    chat.getConversation(conversationId): Promise
    ```

    ## Method Parameters

    <ParamField body="conversationId" type="string" required>

      Conversation ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Conversation`.

    See the [Conversation Object](/api-reference/mobile/containers-app/containers-chat/conversation-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-conversation" id="ex-sdk-get-conversation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-conversation" id="ex-sdk-get-conversation-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-conversation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-conversation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Get a conversation by ID</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getConversation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-conversation-0"><span className="wix-picker-name">Get a conversation by ID</span></label>
    <label htmlFor="ex-sdk-get-conversation-1"><span className="wix-picker-name">getConversation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Get a conversation by ID
    import { chat } from "@wix/containers";

    async function getConversation() {
      const response = await chat.getConversation("1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10");
    }

    /* Promise resolves to:
     * {
     *   "_id": "1f5b2c8e-9a3d-4c21-8b77-2d2f6e0a9c10",
     *   "projectId": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
     *   "status": "ACTIVE",
     *   "title": "new chat",
     *   "revision": 1,
     *   "_createdDate": "2026-07-29T09:00:00.000Z",
     *   "_updatedDate": "2026-07-29T09:00:00.000Z"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getConversation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { chat } from '@wix/containers';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { chat },
      // Include the auth strategy and host as relevant
    });

    async function getConversation(conversationId) {
      const response = await myWixClient.chat.getConversation(conversationId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
