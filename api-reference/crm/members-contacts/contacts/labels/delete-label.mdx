---
title: "Delete Label"
description: "Deletes the specified label from a site and removes it from the contacts it applies to."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the specified label from a site and removes it from the contacts it applies to.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-LABELS">Manage Contact Labels</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/contacts/v4/labels/{key}
    ```

    ## Path Params

    <ParamField path="key" type="string" required>

      Label key to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteLabelResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Contact Label Deleted | `wix.contacts.v4.label_deleted` | Triggered when a label is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE 'https://www.wixapis.com/contacts/v4/labels/custom.my-label' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the specified label from a site and removes it from the contacts it applies to.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.MANAGE-LABELS">Manage Contact Labels</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { labels } from '@wix/crm';

    labels.deleteLabel(key): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="key" type="string" required>

      Label key to delete.

      Constraints: `minLength 1`, `maxLength 80`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Label Deleted | `wix.contacts.v4.label_deleted` | Triggered when a label is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-label" id="ex-sdk-delete-label-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-label" id="ex-sdk-delete-label-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-label" id="ex-sdk-delete-label-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-label-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-label-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a label</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Delete a label (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteLabel (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-label-0"><span className="wix-picker-name">Delete a label</span></label>
    <label htmlFor="ex-sdk-delete-label-1"><span className="wix-picker-name">Delete a label (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-delete-label-2"><span className="wix-picker-name">deleteLabel (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a label
    import { labels } from '@wix/crm';

    /* Sample key value: 'custom.at-risk' */

    export async function myDeleteLabelFunction(key) {

      try {
        const deletedLabel = await labels.deleteLabel(key);    
        console.log('Label deleted.');

      } catch (error) {
        console.log(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Delete a label (with elevated permissions)
    import { labels } from '@wix/crm';
    import { auth } from '@wix/essentials';

    /* Sample key value: 'custom.at-risk' */

    const elevatedDeleteLabel = auth.elevate(labels.deleteLabel);

    export async function myDeleteLabelFunction(key) {

      try {
        const deletedLabel = await elevatedDeleteLabel(key);    
        console.log('Label deleted.');

      } catch (error) {
        console.log(error);
        // Handle the error
      }
    }

    /* Promise resolves to void */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteLabel (self-hosted)
    import { createClient } from '@wix/sdk';
    import { labels } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { labels },
      // Include the auth strategy and host as relevant
    });

    async function deleteLabel(key) {
      const response = await myWixClient.labels.deleteLabel(key);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
