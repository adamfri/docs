---
title: "onBrandDeleted"
description: "Triggered when a brand is deleted."
icon: "bolt"
---

Triggered when a brand is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.stores.catalog.v3.brand_deleted` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Read v3 catalog`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Product write in v3 catalog`, `Manage v3 catalog`, `Read brands in catalog v3`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onBrandDeleted
  import { brandsV3 } from '@wix/stores';

  brandsV3.onBrandDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onBrandDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { brandsV3 } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        brandsV3,
      },
  });

  wixClient.brandsV3.onBrandDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
