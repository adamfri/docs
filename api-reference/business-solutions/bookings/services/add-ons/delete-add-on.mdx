---
title: "Delete Add On"
description: "Deletes an add-on and removes it from all add-on groups."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes an add-on and removes it from all add-on groups.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/addons/v1/add-ons/{addOnId}
    ```

    ## Path Params

    <ParamField path="addOnId" type="string" required>

      ID of the add-on to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteAddOnResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Add On Deleted | `wix.addons.v1.add_on_deleted` | Triggered when an add-on is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a AddOn

    <CodeGroup>

      ```bash Request
      curl -X 'DELETE' \
        'https://www.wixapis.com/addons/v1/add-ons/3b6225b5-b52e-4bf6-a16b-2669d9aa4b47' \
        -H 'authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes an add-on and removes it from all add-on groups.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { addOns } from '@wix/bookings';

    addOns.deleteAddOn(addOnId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="addOnId" type="string" required>

      ID of the add-on to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Add On Deleted | `wix.addons.v1.add_on_deleted` | Triggered when an add-on is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-add-on" id="ex-sdk-delete-add-on-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-add-on" id="ex-sdk-delete-add-on-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-add-on" id="ex-sdk-delete-add-on-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-add-on-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-add-on-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteAddOn</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAddOn (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteAddOn (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-add-on-0"><span className="wix-picker-name">deleteAddOn</span></label>
    <label htmlFor="ex-sdk-delete-add-on-1"><span className="wix-picker-name">deleteAddOn (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-add-on-2"><span className="wix-picker-name">deleteAddOn (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteAddOn
    import { addOns } from '@wix/bookings';

    async function deleteAddOn(addOnId) {
      const response = await addOns.deleteAddOn(addOnId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAddOn (self-hosted)
    import { createClient } from '@wix/sdk';
    import { addOns } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { addOns },
      // Include the auth strategy and host as relevant
    });

    async function deleteAddOn(addOnId) {
      const response = await myWixClient.addOns.deleteAddOn(addOnId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteAddOn (with elevated permissions)
    import { addOns } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myDeleteAddOnMethod(addOnId) {
      const elevatedDeleteAddOn = auth.elevate(addOns.deleteAddOn);
      const response = await elevatedDeleteAddOn(addOnId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
