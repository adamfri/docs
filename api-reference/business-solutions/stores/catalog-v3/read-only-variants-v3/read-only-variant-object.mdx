---
title: "Read Only Variant Object"
description: "A read-only representation of a product variant with enhanced query capabilities."
icon: "cube"
---

A read-only representation of a product variant with enhanced query capabilities.

Product variants represent different options of a product, such as size, color, or material combinations.
This read-only variant provides direct access to variant data without requiring nested product queries,
making it ideal for inventory management, variant comparison, and e-commerce catalog displays.

The variant includes both variant-specific information (like SKU, price, and option choices), as well as
contextual product information (like product name, visibility, categories) for convenience.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string" deprecated>

      Deprecated. Use `variantId` together with `productData.productId` as the unique variant key.

      **Read-only.** **Deprecated.** Deprecated. Use `variantId` together with `productData.productId` as the unique variant key. Use `product_id, variant_id` instead. Removal: 2026-12-31. Constraints: `format GUID`.

    </ResponseField>

    <ResponseField name="visible" type="boolean">

      Whether the variant is visible to site visitors.

      Default: `true`

      **Read-only.**

    </ResponseField>

    <ResponseField name="sku" type="string">

      Stock keeping unit (SKU) for inventory tracking and identification.

      The SKU is typically used for inventory management, order fulfillment, and product identification
      across different systems.

      **Read-only.**

    </ResponseField>

    <ResponseField name="barcode" type="string">

      Barcode for the variant, typically used for point-of-sale systems and inventory management.

      Common barcode formats include UPC, EAN, and Code 128. The barcode helps with
      quick product identification and checkout processes.

      **Read-only.**

    </ResponseField>

    <ResponseField name="optionChoices" type="OptionChoice[]">

      Product option choices that define this variant.

      Each option choice represents a specific selection for a product option (like "Size: Large" or "Color: Red").
      An empty list indicates this is the default variant of a product with no managed options.

      **Read-only.** Constraints: `minItems 0`, `maxItems 6`.

      <Expandable title="optionChoices properties">

        <ResponseField name="optionChoiceIds" type="OptionChoiceIds">

          Option and choice identifiers.

          **Read-only.**

          <Expandable title="optionChoiceIds properties">

            <ResponseField name="optionId" type="string">

              Product option ID.

              **Read-only.** Constraints: `minLength 1`, `maxLength 36`.

            </ResponseField>

            <ResponseField name="choiceId" type="string">

              Specific choice ID within the option.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="optionChoiceNames" type="OptionChoiceNames">

          Human-readable option and choice names.

          **Read-only.**

          <Expandable title="optionChoiceNames properties">

            <ResponseField name="optionName" type="string">

              Display name of the product option.

              For example, "Size", "Color", or "Material".

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="choiceName" type="string">

              Display name of the chosen option value.

              For example, "Large", "Red", or "Cotton".

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="renderType" type="string">

              Visual representation style for the option choice.

              Determines how the option is displayed in the product selector UI.
              Common values include "TEXT_CHOICES", "COLOR_CHOICES", or "IMAGE_CHOICES".

              **Read-only.** Constraints: `maxLength 50`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="price" type="PriceInfo">

      Pricing information for the variant.

      **Read-only.**

      <Expandable title="price properties">

        <ResponseField name="actualPrice" type="FixedMonetaryAmount">

          Current selling price of the variant.

          **Read-only.**

          <Expandable title="actualPrice properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="compareAtPrice" type="FixedMonetaryAmount">

          Original price before any discounts.

          Used to show customers the savings when the variant is on sale.
          If not set, no original price is displayed.

          **Read-only.**

          <Expandable title="compareAtPrice properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="revenueDetails" type="RevenueDetails">

      Revenue and cost details for business analytics.

      Includes cost, profit, and profit margin calculations.
      Returned only when you pass `"MERCHANT_DATA"` to the `fields` parameter.

      **Read-only.**

      <Expandable title="revenueDetails properties">

        <ResponseField name="cost" type="FixedMonetaryAmount">

          Cost of the variant for profit calculations.

          The amount you pay to produce or acquire this variant.

          **Read-only.**

          <Expandable title="cost properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="profit" type="FixedMonetaryAmount">

          Profit amount calculated by subtracting cost from the discounted price.

          **Read-only.**

          <Expandable title="profit properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="profitMargin" type="number">

          Profit margin as a percentage of the selling price.

          Calculated by dividing profit by the discounted price. The result is rounded to 4 decimal places.
          A value of 0.25 represents a 25% profit margin.

          **Read-only.** Constraints: `min 0`, `max 1`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="media" type="ProductMedia">

      Media associated with the variant.

      Typically includes images, videos, or other media files specific to this variant.

      **Read-only.**

      <Expandable title="media properties">

        <ResponseField name="altText" type="string">

          Image alt text.

          **Read-only.**

        </ResponseField>

        <ResponseField name="displayName" type="string">

          Media display name.
          Allows to override the default media name. Can be passed only when `setBy: url`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="mediaType" type="string">

          Media type.

          **Read-only.**

          **Supported values:**

          - `IMAGE` — Image media type.
          - `VIDEO` — Video media type.

        </ResponseField>

        <ResponseField name="thumbnail" type="Thumbnail">

          Media thumbnail.

          **Read-only.**

          <Expandable title="thumbnail properties">

            <ResponseField name="url" type="string">

              Thumbnail URL.

              **Read-only.** Constraints: `format WEB_URL`.

            </ResponseField>

            <ResponseField name="height" type="integer">

              Thumbnail height.

              **Read-only.**

            </ResponseField>

            <ResponseField name="width" type="integer">

              Thumbnail width.

              **Read-only.**

            </ResponseField>

            <ResponseField name="altText" type="string">

              Thumbnail alt text.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="uploadId" type="string">

          ID used to upload media to Wix Media Manager.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="setBy" type="setBy">

          setBy

          <Expandable title="setBy properties">

            <ResponseField name="id" type="string">

              ID of existing media from Wix Media Manager.

              **Read-only.** Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="url" type="string">

              Media URL.

              **Read-only.** Constraints: `format WEB_URL`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="media" type="media">

          media

          <Expandable title="media properties">

            <ResponseField name="image" type="Image">

              Product image.

              **Read-only.**

              <Expandable title="image properties">

                <ResponseField name="id" type="string">

                  WixMedia image ID.

                  Constraints: `maxLength 400`.

                </ResponseField>

                <ResponseField name="url" type="string">

                  Image URL.

                  Constraints: `format WEB_URL`.

                </ResponseField>

                <ResponseField name="height" type="integer">

                  Original image height.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="width" type="integer">

                  Original image width.

                  **Read-only.**

                </ResponseField>

                <ResponseField name="altText" type="string">

                  Image alt text.

                </ResponseField>

                <ResponseField name="filename" type="string">

                  Image filename.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="video" type="VideoV2">

              Product video.

              **Read-only.**

              <Expandable title="video properties">

                <ResponseField name="id" type="string">

                  WixMedia ID.

                  Constraints: `maxLength 400`.

                </ResponseField>

                <ResponseField name="resolutions" type="VideoResolution[]">

                  Available resolutions for the video, starting with the optimal resolution.

                  **Read-only.** Constraints: `maxItems 100`.

                  _Nested structure omitted for brevity — see the `VideoResolution[]` object._

                </ResponseField>

                <ResponseField name="filename" type="string">

                  Video filename.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="subscriptionPricesInfo" type="SubscriptionPricesInfo">

      Subscription pricing information when the variant is part of subscription plans.

      Shows calculated prices after applying subscription discounts to the variant's price.
      Returned only when you pass `"SUBSCRIPTION_PRICES_INFO"` to the `fields` parameter.

      **Read-only.**

      <Expandable title="subscriptionPricesInfo properties">

        <ResponseField name="subscriptionPrices" type="SubscriptionPrice[]">

          Pricing for different subscription plans.

          Shows how the variant's price changes with different subscription intervals or discounts.

          **Read-only.** Constraints: `maxItems 6`.

          <Expandable title="subscriptionPrices properties">

            <ResponseField name="subscriptionId" type="string">

              Subscription plan ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="price" type="FixedMonetaryAmount">

              Variant price with subscription discount applied.

              **Read-only.**

              <Expandable title="price properties">

                <ResponseField name="amount" type="string">

                  Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

                  **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Formatted monetary amount. For example, `"$3.99"`.
                  > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="pricePerUnit" type="SubscriptionPricePerUnit">

              Price per unit for subscription pricing.

              **Read-only.**

              <Expandable title="pricePerUnit properties">

                <ResponseField name="value" type="string">

                  Calculated price per unit for the subscription.

                  Takes into account the subscription discount and the variant's price per unit settings.

                  **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

                </ResponseField>

                <ResponseField name="description" type="string">

                  Description of the subscription price per unit.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="inventoryStatus" type="InventoryStatus">

      Current inventory and availability status.

      **Read-only.**

      <Expandable title="inventoryStatus properties">

        <ResponseField name="inStock" type="boolean">

          Whether the variant is currently in stock and available for purchase.

          **Read-only.**

        </ResponseField>

        <ResponseField name="preorderEnabled" type="boolean">

          Whether customers can place preorders for this variant when it's out of stock.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="productData" type="ProductFields">

      Product information for context and convenience.

      Includes essential product details like name, visibility, and categories to avoid
      requiring separate product API calls when working with variants.

      **Read-only.**

      <Expandable title="productData properties">

        <ResponseField name="productId" type="string">

          Product ID.

          **Read-only.** Constraints: `minLength 1`, `maxLength 36`.

        </ResponseField>

        <ResponseField name="name" type="string">

          Product name displayed to customers.

          **Read-only.**

        </ResponseField>

        <ResponseField name="productType" type="string">

          Type of product determining available properties and behavior.

          **Read-only.**

          **Supported values:**

          - `PHYSICAL` — Physical product.
          - `DIGITAL` — Digital product.

        </ResponseField>

        <ResponseField name="slug" type="string">

          URL-friendly identifier for the product.

          Used in product page URLs and for SEO optimization.

          **Read-only.**

        </ResponseField>

        <ResponseField name="visible" type="boolean">

          Whether the product is visible to site visitors.

          Default: `true`

          **Read-only.**

        </ResponseField>

        <ResponseField name="visibleInPos" type="boolean">

          Whether the product appears in point-of-sale systems.

          Default: `true`
          Always `false` for digital products.

          **Read-only.**

        </ResponseField>

        <ResponseField name="mainCategoryId" type="string">

          Primary category assignment for the product.

          Determines the main category used for navigation and organization.

          **Read-only.**

        </ResponseField>

        <ResponseField name="currency" type="string">

          Currency used for pricing this product.

          Three-letter currency code in ISO-4217 alphabetic format.
          Defaults to the site's currency setting unless specified in request headers.
          Returned only when you pass `"CURRENCY"` to the `fields` parameter.

          **Read-only.**

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number, which increments by 1 each time the product is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the product.

          **Read-only.**

        </ResponseField>

        <ResponseField name="handle" type="string">

          A unique human-friendly identifier for the product.

          The handle can be set by the user to ensure consistency across multiple platforms.

          **Read-only.**

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="typedProperties" type="typedProperties">

          typedProperties

          <Expandable title="typedProperties properties">

            <ResponseField name="physicalProperties" type="PhysicalProperties">

              Physical product properties such as weight and dimensions.

              Required when `productType` is `PHYSICAL`.

              <Expandable title="physicalProperties properties">

                <ResponseField name="weightMeasurementUnitInfo" type="WeightMeasurementUnitInfo">

                  Weight measurement unit information for the product.

                  Defines the unit of measurement (kg, lb, etc.) used for product weight calculations.
                  Returned only when you pass `"WEIGHT_MEASUREMENT_UNIT_INFO"` to the `fields` parameter.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `WeightMeasurementUnitInfo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="variantId" type="string">

      Variant ID. Not guaranteed to be unique across products, so it should be
      used together with `productData.productId` as the unique variant key.

      **Read-only.** Constraints: `format GUID`.

    </ResponseField>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string" deprecated>

      Deprecated. Use `variantId` together with `productData.productId` as the unique variant key.

      **Read-only.** **Deprecated.** Deprecated. Use `variantId` together with `productData.productId` as the unique variant key. Use `product_id, variant_id` instead. Removal: 2026-12-31. Constraints: `format GUID`.

    </ResponseField>

    <ResponseField name="visible" type="boolean">

      Whether the variant is visible to site visitors.

      Default: `true`

      **Read-only.**

    </ResponseField>

    <ResponseField name="sku" type="string">

      Stock keeping unit (SKU) for inventory tracking and identification.

      The SKU is typically used for inventory management, order fulfillment, and product identification
      across different systems.

      **Read-only.**

    </ResponseField>

    <ResponseField name="barcode" type="string">

      Barcode for the variant, typically used for point-of-sale systems and inventory management.

      Common barcode formats include UPC, EAN, and Code 128. The barcode helps with
      quick product identification and checkout processes.

      **Read-only.**

    </ResponseField>

    <ResponseField name="optionChoices" type="OptionChoice[]">

      Product option choices that define this variant.

      Each option choice represents a specific selection for a product option (like "Size: Large" or "Color: Red").
      An empty list indicates this is the default variant of a product with no managed options.

      **Read-only.** Constraints: `minItems 0`, `maxItems 6`.

      <Expandable title="optionChoices properties">

        <ResponseField name="optionChoiceIds" type="OptionChoiceIds">

          Option and choice identifiers.

          **Read-only.**

          <Expandable title="optionChoiceIds properties">

            <ResponseField name="optionId" type="string">

              Product option ID.

              **Read-only.** Constraints: `minLength 1`, `maxLength 36`.

            </ResponseField>

            <ResponseField name="choiceId" type="string">

              Specific choice ID within the option.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="optionChoiceNames" type="OptionChoiceNames">

          Human-readable option and choice names.

          **Read-only.**

          <Expandable title="optionChoiceNames properties">

            <ResponseField name="optionName" type="string">

              Display name of the product option.

              For example, "Size", "Color", or "Material".

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="choiceName" type="string">

              Display name of the chosen option value.

              For example, "Large", "Red", or "Cotton".

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ResponseField>

            <ResponseField name="renderType" type="string">

              Visual representation style for the option choice.

              Determines how the option is displayed in the product selector UI.
              Common values include "TEXT_CHOICES", "COLOR_CHOICES", or "IMAGE_CHOICES".

              **Read-only.** Constraints: `maxLength 50`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="price" type="PriceInfo">

      Pricing information for the variant.

      **Read-only.**

      <Expandable title="price properties">

        <ResponseField name="actualPrice" type="FixedMonetaryAmount">

          Current selling price of the variant.

          **Read-only.**

          <Expandable title="actualPrice properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="compareAtPrice" type="FixedMonetaryAmount">

          Original price before any discounts.

          Used to show customers the savings when the variant is on sale.
          If not set, no original price is displayed.

          **Read-only.**

          <Expandable title="compareAtPrice properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="revenueDetails" type="RevenueDetails">

      Revenue and cost details for business analytics.

      Includes cost, profit, and profit margin calculations.
      Returned only when you pass `"MERCHANT_DATA"` to the `fields` parameter.

      **Read-only.**

      <Expandable title="revenueDetails properties">

        <ResponseField name="cost" type="FixedMonetaryAmount">

          Cost of the variant for profit calculations.

          The amount you pay to produce or acquire this variant.

          **Read-only.**

          <Expandable title="cost properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="profit" type="FixedMonetaryAmount">

          Profit amount calculated by subtracting cost from the discounted price.

          **Read-only.**

          <Expandable title="profit properties">

            <ResponseField name="amount" type="string">

              Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

              **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

            </ResponseField>

            <ResponseField name="formattedAmount" type="string">

              Formatted monetary amount. For example, `"$3.99"`.
              > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="profitMargin" type="number">

          Profit margin as a percentage of the selling price.

          Calculated by dividing profit by the discounted price. The result is rounded to 4 decimal places.
          A value of 0.25 represents a 25% profit margin.

          **Read-only.** Constraints: `min 0`, `max 1`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="media" type="ProductMedia">

      Media associated with the variant.

      Typically includes images, videos, or other media files specific to this variant.

      **Read-only.**

      <Expandable title="media properties">

        <ResponseField name="altText" type="string">

          Image alt text.

          **Read-only.**

        </ResponseField>

        <ResponseField name="displayName" type="string">

          Media display name.
          Allows to override the default media name. Can be passed only when `setBy: url`.

          **Read-only.**

        </ResponseField>

        <ResponseField name="mediaType" type="string">

          Media type.

          **Read-only.**

          **Supported values:**

          - `IMAGE` — Image media type.
          - `VIDEO` — Video media type.

        </ResponseField>

        <ResponseField name="thumbnail" type="Thumbnail">

          Media thumbnail.

          **Read-only.**

          <Expandable title="thumbnail properties">

            <ResponseField name="url" type="string">

              Thumbnail URL.

              **Read-only.** Constraints: `format WEB_URL`.

            </ResponseField>

            <ResponseField name="height" type="integer">

              Thumbnail height.

              **Read-only.**

            </ResponseField>

            <ResponseField name="width" type="integer">

              Thumbnail width.

              **Read-only.**

            </ResponseField>

            <ResponseField name="altText" type="string">

              Thumbnail alt text.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="uploadId" type="string">

          ID used to upload media to Wix Media Manager.

          **Read-only.** Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="setBy" type="setBy">

          setBy

          <Expandable title="setBy properties">

            <ResponseField name="_id" type="string">

              ID of existing media from Wix Media Manager.

              **Read-only.** Constraints: `minLength 1`, `maxLength 200`.

            </ResponseField>

            <ResponseField name="url" type="string">

              Media URL.

              **Read-only.** Constraints: `format WEB_URL`.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="media" type="media">

          media

          <Expandable title="media properties">

            <ResponseField name="image" type="string">

              Product image.

              **Read-only.**

            </ResponseField>

            <ResponseField name="video" type="string">

              Product video.

              **Read-only.**

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="subscriptionPricesInfo" type="SubscriptionPricesInfo">

      Subscription pricing information when the variant is part of subscription plans.

      Shows calculated prices after applying subscription discounts to the variant's price.
      Returned only when you pass `"SUBSCRIPTION_PRICES_INFO"` to the `fields` parameter.

      **Read-only.**

      <Expandable title="subscriptionPricesInfo properties">

        <ResponseField name="subscriptionPrices" type="SubscriptionPrice[]">

          Pricing for different subscription plans.

          Shows how the variant's price changes with different subscription intervals or discounts.

          **Read-only.** Constraints: `maxItems 6`.

          <Expandable title="subscriptionPrices properties">

            <ResponseField name="subscriptionId" type="string">

              Subscription plan ID.

              **Read-only.** Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="price" type="FixedMonetaryAmount">

              Variant price with subscription discount applied.

              **Read-only.**

              <Expandable title="price properties">

                <ResponseField name="amount" type="string">

                  Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

                  **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

                </ResponseField>

                <ResponseField name="formattedAmount" type="string">

                  Formatted monetary amount. For example, `"$3.99"`.
                  > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Read-Only Variants API requests.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="pricePerUnit" type="SubscriptionPricePerUnit">

              Price per unit for subscription pricing.

              **Read-only.**

              <Expandable title="pricePerUnit properties">

                <ResponseField name="value" type="string">

                  Calculated price per unit for the subscription.

                  Takes into account the subscription discount and the variant's price per unit settings.

                  **Read-only.** Constraints: `>= 0`, `<= 999999999.99`.

                </ResponseField>

                <ResponseField name="description" type="string">

                  Description of the subscription price per unit.

                  **Read-only.**

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="inventoryStatus" type="InventoryStatus">

      Current inventory and availability status.

      **Read-only.**

      <Expandable title="inventoryStatus properties">

        <ResponseField name="inStock" type="boolean">

          Whether the variant is currently in stock and available for purchase.

          **Read-only.**

        </ResponseField>

        <ResponseField name="preorderEnabled" type="boolean">

          Whether customers can place preorders for this variant when it's out of stock.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="productData" type="ProductFields">

      Product information for context and convenience.

      Includes essential product details like name, visibility, and categories to avoid
      requiring separate product API calls when working with variants.

      **Read-only.**

      <Expandable title="productData properties">

        <ResponseField name="productId" type="string">

          Product ID.

          **Read-only.** Constraints: `minLength 1`, `maxLength 36`.

        </ResponseField>

        <ResponseField name="name" type="string">

          Product name displayed to customers.

          **Read-only.**

        </ResponseField>

        <ResponseField name="productType" type="string">

          Type of product determining available properties and behavior.

          **Read-only.**

          **Supported values:**

          - `PHYSICAL` — Physical product.
          - `DIGITAL` — Digital product.

        </ResponseField>

        <ResponseField name="slug" type="string">

          URL-friendly identifier for the product.

          Used in product page URLs and for SEO optimization.

          **Read-only.**

        </ResponseField>

        <ResponseField name="visible" type="boolean">

          Whether the product is visible to site visitors.

          Default: `true`

          **Read-only.**

        </ResponseField>

        <ResponseField name="visibleInPos" type="boolean">

          Whether the product appears in point-of-sale systems.

          Default: `true`
          Always `false` for digital products.

          **Read-only.**

        </ResponseField>

        <ResponseField name="mainCategoryId" type="string">

          Primary category assignment for the product.

          Determines the main category used for navigation and organization.

          **Read-only.**

        </ResponseField>

        <ResponseField name="currency" type="string">

          Currency used for pricing this product.

          Three-letter currency code in ISO-4217 alphabetic format.
          Defaults to the site's currency setting unless specified in request headers.
          Returned only when you pass `"CURRENCY"` to the `fields` parameter.

          **Read-only.**

        </ResponseField>

        <ResponseField name="revision" type="integer">

          Revision number, which increments by 1 each time the product is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the product.

          **Read-only.**

        </ResponseField>

        <ResponseField name="handle" type="string">

          A unique human-friendly identifier for the product.

          The handle can be set by the user to ensure consistency across multiple platforms.

          **Read-only.**

        </ResponseField>

      </Expandable>

      <Expandable title="One of the following">

        <ResponseField name="typedProperties" type="typedProperties">

          typedProperties

          <Expandable title="typedProperties properties">

            <ResponseField name="physicalProperties" type="PhysicalProperties">

              Physical product properties such as weight and dimensions.

              Required when `productType` is `PHYSICAL`.

              <Expandable title="physicalProperties properties">

                <ResponseField name="weightMeasurementUnitInfo" type="WeightMeasurementUnitInfo">

                  Weight measurement unit information for the product.

                  Defines the unit of measurement (kg, lb, etc.) used for product weight calculations.
                  Returned only when you pass `"WEIGHT_MEASUREMENT_UNIT_INFO"` to the `fields` parameter.

                  **Read-only.**

                  _Nested structure omitted for brevity — see the `WeightMeasurementUnitInfo` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="variantId" type="string">

      Variant ID. Not guaranteed to be unique across products, so it should be
      used together with `productData.productId` as the unique variant key.

      **Read-only.** Constraints: `format GUID`.

    </ResponseField>

  </Tab>

</Tabs>
