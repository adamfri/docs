---
title: "Delete Info Section"
description: "Deletes an info section."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an info section.

    <Note>

    Deleting an info section will also remove it from all products it is assigned to.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INFO_SECTION_WRITE">Info section write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/stores/v3/info-sections/{infoSectionId}
    ```

    ## Path Params

    <ParamField path="infoSectionId" type="string" required>

      Info section ID

    </ParamField>

    ## Response Object

    Returns `DeleteInfoSectionResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Info Section Deleted | `wix.stores.catalog.v3.info_section_deleted` | Triggered when an info section is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/stores/v3/info-sections/5db34212-4870-4a28-b210-187be25b24cd' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes an info section.

    <Note>

    Deleting an info section will also remove it from all products it is assigned to.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.INFO_SECTION_WRITE">Info section write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { infoSectionsV3 } from '@wix/stores';

    infoSectionsV3.deleteInfoSection(infoSectionId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="infoSectionId" type="string" required>

      Info section ID

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Info Section Deleted | `wix.stores.catalog.v3.info_section_deleted` | Triggered when an info section is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-info-section" id="ex-sdk-delete-info-section-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-info-section" id="ex-sdk-delete-info-section-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-info-section" id="ex-sdk-delete-info-section-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-info-section-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-info-section-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteInfoSection</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteInfoSection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteInfoSection (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-info-section-0"><span className="wix-picker-name">deleteInfoSection</span></label>
    <label htmlFor="ex-sdk-delete-info-section-1"><span className="wix-picker-name">deleteInfoSection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-info-section-2"><span className="wix-picker-name">deleteInfoSection (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteInfoSection
    import { infoSectionsV3 } from '@wix/stores';

    async function deleteInfoSection(infoSectionId) {
      const response = await infoSectionsV3.deleteInfoSection(infoSectionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteInfoSection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { infoSectionsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { infoSectionsV3 },
      // Include the auth strategy and host as relevant
    });

    async function deleteInfoSection(infoSectionId) {
      const response = await myWixClient.infoSectionsV3.deleteInfoSection(infoSectionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteInfoSection (with elevated permissions)
    import { infoSectionsV3 } from '@wix/stores';
    import { auth } from '@wix/essentials';

    async function myDeleteInfoSectionMethod(infoSectionId) {
      const elevatedDeleteInfoSection = auth.elevate(infoSectionsV3.deleteInfoSection);
      const response = await elevatedDeleteInfoSection(infoSectionId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
