---
title: "Delete Back In Stock Notification Request"
description: "Deletes a back in stock notification request."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a back in stock notification request.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES-MEGA.MANAGE-STORES">Manage Stores - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/back-in-stock-service/v1/back-in-stock-notification-requests/{id}
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the notification request to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteBackInStockNotificationRequestResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Back In Stock Notification Request Deleted | `wix.ecom.v1.back_in_stock_notification_request_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/back-in-stock-service/v1/back-in-stock-notification-requests/0e68fbd2-0e49-47a6-9678-58911a58339c' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a back in stock notification request.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES-MEGA.MANAGE-STORES">Manage Stores - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { backInStockNotifications } from '@wix/ecom';

    backInStockNotifications.deleteBackInStockNotificationRequest(_id): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the notification request to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Back In Stock Notification Request Deleted | `wix.ecom.v1.back_in_stock_notification_request_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-back-in-stock-notification-request" id="ex-sdk-delete-back-in-stock-notification-request-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-back-in-stock-notification-request" id="ex-sdk-delete-back-in-stock-notification-request-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-back-in-stock-notification-request" id="ex-sdk-delete-back-in-stock-notification-request-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-back-in-stock-notification-request-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-back-in-stock-notification-request-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteBackInStockNotificationRequest</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBackInStockNotificationRequest (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBackInStockNotificationRequest (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-back-in-stock-notification-request-0"><span className="wix-picker-name">deleteBackInStockNotificationRequest</span></label>
    <label htmlFor="ex-sdk-delete-back-in-stock-notification-request-1"><span className="wix-picker-name">deleteBackInStockNotificationRequest (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-back-in-stock-notification-request-2"><span className="wix-picker-name">deleteBackInStockNotificationRequest (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteBackInStockNotificationRequest
    import { backInStockNotifications } from '@wix/ecom';

    async function deleteBackInStockNotificationRequest(_id) {
      const response = await backInStockNotifications.deleteBackInStockNotificationRequest(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBackInStockNotificationRequest (self-hosted)
    import { createClient } from '@wix/sdk';
    import { backInStockNotifications } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { backInStockNotifications },
      // Include the auth strategy and host as relevant
    });

    async function deleteBackInStockNotificationRequest(_id) {
      const response = await myWixClient.backInStockNotifications.deleteBackInStockNotificationRequest(_id);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBackInStockNotificationRequest (with elevated permissions)
    import { backInStockNotifications } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myDeleteBackInStockNotificationRequestMethod(_id) {
      const elevatedDeleteBackInStockNotificationRequest = auth.elevate(backInStockNotifications.deleteBackInStockNotificationRequest);
      const response = await elevatedDeleteBackInStockNotificationRequest(_id);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
