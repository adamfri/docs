---
title: "Add Schedule Item"
description: "Adds a schedule item to a draft schedule."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds a schedule item to a draft schedule.  For the same date and time you can have multiple items.

    Note that draft items are not publicly available unless published.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v1/schedule/draft
    ```

    ## Body Params

    <ParamField body="eventId" type="string" required>

      Event ID to add the schedule item to.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="item" type="ScheduleItemData">

      Schedule item.

      <Expandable title="item properties">

        <ParamField body="hidden" type="boolean">

          Whether the schedule item is hidden from guests.

        </ParamField>

        <ParamField body="name" type="string" required>

          Schedule item name.

          Constraints: `maxLength 120`.

        </ParamField>

        <ParamField body="timeSlot" type="TimeInterval" required>

          Time slot of a schedule item.

          <Expandable title="timeSlot properties">

            <ParamField body="start" type="datetime" required>

              Start of the time slot interval, inclusive.

            </ParamField>

            <ParamField body="end" type="datetime" required>

              End of the time slot interval, exclusive.

            </ParamField>

            <ParamField body="timeZoneId" type="string">

              Time zone ID in TZ database format, e.g., `"EST"`, `"America/Los_Angeles"`.
              Defaults to `"Etc/UTC"` when not set.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="description" type="string">

          Rich text schedule item description.

          Constraints: `maxLength 10000`.

        </ParamField>

        <ParamField body="stageName" type="string">

          Stage or room name in which a session takes place.

          Constraints: `maxLength 30`.

        </ParamField>

        <ParamField body="tags" type="string[]">

          Schedule item tags. Used to organize the items by a theme.

          Constraints: `minLength 1`, `maxLength 30`, `maxItems 5`.

        </ParamField>

        <ParamField body="status" type="string">

          Schedule item status.

          **Supported values:**

          - `SCHEDULED` — Item is scheduled for a future date.
          - `CANCELED` — Item is canceled.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="item" type="ScheduleItem">

      Schedule item.

      See the [Schedule Item Object](/api-reference/business-solutions/events/event-management/schedule-items/schedule-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/events/v1/agenda/draft' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Adds a schedule item to a draft schedule.  For the same date and time you can have multiple items.

    Note that draft items are not publicly available unless published.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS-MEGA.MANAGE-EVENTS">Manage Events - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { schedule } from '@wix/events';

    schedule.addScheduleItem(eventId, options): Promise<NonNullablePaths>
    ```

    ## Method Parameters

    <ParamField body="eventId" type="string" required>

      Event ID to add the schedule item to.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="AddScheduleItemOptions">

      Optional fields.

      <Expandable title="options properties">

        <ParamField body="item" type="ScheduleItemData">

          Schedule item.

          <Expandable title="item properties">

            <ParamField body="hidden" type="boolean">

              Whether the schedule item is hidden from guests.

            </ParamField>

            <ParamField body="name" type="string" required>

              Schedule item name.

              Constraints: `maxLength 120`.

            </ParamField>

            <ParamField body="timeSlot" type="TimeInterval" required>

              Time slot of a schedule item.

              _Nested structure omitted for brevity — see the `TimeInterval` object._

            </ParamField>

            <ParamField body="description" type="string">

              Rich text schedule item description.

              Constraints: `maxLength 10000`.

            </ParamField>

            <ParamField body="stageName" type="string">

              Stage or room name in which a session takes place.

              Constraints: `maxLength 30`.

            </ParamField>

            <ParamField body="tags" type="string[]">

              Schedule item tags. Used to organize the items by a theme.

              Constraints: `minLength 1`, `maxLength 30`, `maxItems 5`.

            </ParamField>

            <ParamField body="status" type="string">

              Schedule item status.

              **Supported values:**

              - `SCHEDULED` — Item is scheduled for a future date.
              - `CANCELED` — Item is canceled.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<NonNullablePaths>`.

    <ResponseField name="item" type="ScheduleItem">

      Schedule item.

      See the [Schedule Item Object](/api-reference/business-solutions/events/event-management/schedule-items/schedule-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-add-schedule-item" id="ex-sdk-add-schedule-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-add-schedule-item" id="ex-sdk-add-schedule-item-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-add-schedule-item" id="ex-sdk-add-schedule-item-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-add-schedule-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-add-schedule-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">addScheduleItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">addScheduleItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">addScheduleItem (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-add-schedule-item-0"><span className="wix-picker-name">addScheduleItem</span></label>
    <label htmlFor="ex-sdk-add-schedule-item-1"><span className="wix-picker-name">addScheduleItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-add-schedule-item-2"><span className="wix-picker-name">addScheduleItem (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript addScheduleItem
    import { schedule } from '@wix/events';

    async function addScheduleItem(eventId,options) {
      const response = await schedule.addScheduleItem(eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript addScheduleItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { schedule } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { schedule },
      // Include the auth strategy and host as relevant
    });

    async function addScheduleItem(eventId,options) {
      const response = await myWixClient.schedule.addScheduleItem(eventId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript addScheduleItem (with elevated permissions)
    import { schedule } from '@wix/events';
    import { auth } from '@wix/essentials';

    async function myAddScheduleItemMethod(eventId,options) {
      const elevatedAddScheduleItem = auth.elevate(schedule.addScheduleItem);
      const response = await elevatedAddScheduleItem(eventId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
