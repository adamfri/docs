---
title: "Query Reports"
description: "Retrieves a list of reports, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of reports, given the provided paging, filtering, and sorting.
    Query Reports runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `entityName` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `entityId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `extendedFields` | `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.READ-REPORTS">Read Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/reports/v2/reports/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sorting options

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="reports" type="Report[]">

      List of reports.

      Constraints: `maxItems 100`.

      See the [Report Object](/api-reference/crm/community/feedback-moderation/reports-v2/report-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \ https://www.wixapis.com/reports/v2/reports/query \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "query": {
                  "filter": {
                    "entityId": {
                      "$eq": "50353fbc-b265-4f03-888f-a53aa272758d"
                    }
                  }
              }
        }'
      ```

      ```json Response
      { 
        "reports": [
          {
            "id": "dd7cfd02-1837-4fb8-bc6f-86316f0984a9",
            "entityName": "comment",
            "entityId": "50353fbc-b265-4f03-888f-a53aa272758d",
            "identity": {
              "identityType": "MEMBER",
              "memberId": "141a3e01-da55-4b3a-a44a-2f194bfc8897"
            },
            "reason": {
              "reasonType": "DRUGS",
              "description": "This person promotes drug usage."
            },
            "revision": "1",
            "createdDate": "2021-10-26T17:22:10.299Z",
            "updatedDate": "2021-10-26T17:22:10.299Z"
          },
          {
            "id": "2e776fae-80b6-4a5e-b6b9-891d8bc4d481",
            "entityName": "comment",
            "entityId": "50353fbc-b265-4f03-888f-a53aa272758d",
            "identity": {
              "identityType": "MEMBER",
              "memberId": "141a3e01-da55-4b3a-a44a-2f194bfc8897"
            },
            "reason": {
              "reasonType": "UNAUTHORIZED_SALES",
              "description": "This person sells illegal stuff."
            },
            "revision": "1",
            "createdDate": "2021-10-26T17:22:10.299Z",
            "updatedDate": "2021-10-26T17:22:10.299Z"
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of reports, given the provided paging, filtering, and sorting.
    Query Reports runs with these defaults, which you can override:
    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `100`
    - `paging.offset` is `0`

    To learn about working with _Query_ endpoints, see [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `_createdDate` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte` | Sort: `ASC`, `DESC` |
    | `extendedFields` | `$exists` | Sort: `ASC`, `DESC` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC_REPORTS.READ-REPORTS">Read Reports</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { reports } from '@wix/reports';

    reports.queryReports(query): ReportsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="ReportQuery" required>

      Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more information.

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sorting options

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `ReportsQueryBuilder`.

    <ResponseField name="reports" type="Report[]">

      List of reports.

      Constraints: `maxItems 100`.

      See the [Report Object](/api-reference/crm/community/feedback-moderation/reports-v2/report-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-reports" id="ex-sdk-query-reports-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-reports" id="ex-sdk-query-reports-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-reports" id="ex-sdk-query-reports-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-reports-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-reports-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryReports</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryReports (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryReports (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-reports-0"><span className="wix-picker-name">queryReports</span></label>
    <label htmlFor="ex-sdk-query-reports-1"><span className="wix-picker-name">queryReports (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-reports-2"><span className="wix-picker-name">queryReports (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryReports
    import { reports } from '@wix/reports';

    async function queryReports(query) {
      const response = await reports.queryReports(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryReports (self-hosted)
    import { createClient } from '@wix/sdk';
    import { reports } from '@wix/reports';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { reports },
      // Include the auth strategy and host as relevant
    });

    async function queryReports(query) {
      const response = await myWixClient.reports.queryReports(query);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryReports (with elevated permissions)
    import { reports } from '@wix/reports';
    import { auth } from '@wix/essentials';

    async function myQueryReportsMethod(query) {
      const elevatedQueryReports = auth.elevate(reports.queryReports);
      const response = await elevatedQueryReports(query);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
