---
title: "onTicketDefinitionSalePeriodUpdated"
description: "Triggered when a sale period is updated."
icon: "bolt"
---

Triggered when a sale period is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v3.ticket_definition_sale_period_updated` |
| Package | `@wix/events` |
| Permissions | `Manage Events - all permissions`, `Manage Orders`, `Manage Ticket Definitions` |

## Examples

<CodeGroup>

  ```javascript onTicketDefinitionSalePeriodUpdated: Wix-hosted
  import { ticketDefinitionsV2 } from '@wix/events';

  ticketDefinitionsV2.onTicketDefinitionSalePeriodUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTicketDefinitionSalePeriodUpdated: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { ticketDefinitionsV2 } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        ticketDefinitionsV2,
      },
  });

  wixClient.ticketDefinitionsV2.onTicketDefinitionSalePeriodUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
