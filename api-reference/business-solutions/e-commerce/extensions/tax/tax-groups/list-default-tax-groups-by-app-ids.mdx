---
title: "List Default Tax Groups By App Ids"
description: "Retrieves default tax groups for specific apps."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves default tax groups for specific apps.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/billing/v1/tax-groups/list-default-tax-groups-by-app-ids
    ```

    ## Body Params

    <ParamField body="appIds" type="string[]" required>

      App IDs to retrieve default tax groups for.

      Constraints: `minLength 1`, `minItems 1`, `maxItems 300`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="ListDefaultTaxGroupsByAppIdsResult[]">

      Retrieved default tax groups.

      Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="results properties">

        <ResponseField name="taxGroupMetadata" type="ItemMetadata">

          Information about success or failure to retrieve default tax groups.

          <Expandable title="taxGroupMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxGroup" type="TaxGroup">

          Retrieved default tax groups.

          See the [Tax Group Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-groups/tax-group-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Lists all default tax groups for a Wix site that has the Wix Stores app installed.

    <CodeGroup>

      ```bash Request
      curl -X POST \
          'https://www.wixapis.com/billing/v1/tax-groups/list-default-tax-groups-by-app-ids' \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
            "appIds": [
              "215238eb-22a5-4c36-9e7b-e7c08025e04e"
            ]
          }'
      ```

      ```json Response
      {
        "results": [
          {
            "taxGroupMetadata": {
              "id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
              "originalIndex": 0,
              "success": true
            },
            "taxGroup": {
              "id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
              "name": "Products"
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 1,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves default tax groups for specific apps.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { taxGroups } from '@wix/ecom';

    taxGroups.listDefaultTaxGroupsByAppIds(appIds): Promise
    ```

    ## Method Parameters

    <ParamField body="appIds" type="string[]" required>

      App IDs to retrieve default tax groups for.

      Constraints: `minLength 1`, `minItems 1`, `maxItems 300`.

    </ParamField>

    ## Returns

    <ResponseField name="results" type="ListDefaultTaxGroupsByAppIdsResult[]">

      Retrieved default tax groups.

      Constraints: `minItems 1`, `maxItems 300`.

      <Expandable title="results properties">

        <ResponseField name="taxGroupMetadata" type="ItemMetadata">

          Information about success or failure to retrieve default tax groups.

          <Expandable title="taxGroupMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="taxGroup" type="TaxGroup">

          Retrieved default tax groups.

          See the [Tax Group Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-groups/tax-group-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk action metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-default-tax-groups-by-app-ids" id="ex-sdk-list-default-tax-groups-by-app-ids-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-default-tax-groups-by-app-ids" id="ex-sdk-list-default-tax-groups-by-app-ids-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-default-tax-groups-by-app-ids-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-default-tax-groups-by-app-ids-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List Default Tax Groups by App IDs</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listDefaultTaxGroupsByAppIds (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-default-tax-groups-by-app-ids-0"><span className="wix-picker-name">List Default Tax Groups by App IDs</span></label>
    <label htmlFor="ex-sdk-list-default-tax-groups-by-app-ids-1"><span className="wix-picker-name">listDefaultTaxGroupsByAppIds (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List Default Tax Groups by App IDs
    import { taxGroups } from "@wix/ecom";

    async function listDefaultTaxGroupsByAppIds() {
      const response = await taxGroups.listDefaultTaxGroupsByAppIds([
        "215238eb-22a5-4c36-9e7b-e7c08025e04e", // Wix Stores app ID
      ]);
      return response;
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "taxGroupMetadata": {
     *         "originalIndex": 0,
     *         "success": true,
     *         "_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e"
     *       },
     *       "taxGroup": {
     *         "_id": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
     *         "name": "Products"
     *       }
     *     }
     *   ],
     *   "bulkActionMetadata": {
     *     "totalSuccesses": 1,
     *     "totalFailures": 0,
     *     "undetailedFailures": 0
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listDefaultTaxGroupsByAppIds (self-hosted)
    import { createClient } from '@wix/sdk';
    import { taxGroups } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { taxGroups },
      // Include the auth strategy and host as relevant
    });

    async function listDefaultTaxGroupsByAppIds(appIds) {
      const response = await myWixClient.taxGroups.listDefaultTaxGroupsByAppIds(appIds);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
