---
title: "Automation Object"
description: "The Automation object in the Wix Automation API."
icon: "cube"
---

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Automation ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number. This increments by 1 whenever the automation is updated. Specify the current revision number whenever updating an existing automation.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdBy" type="AuditInfo">

      Who created the automation.

      **Read-only.** Constraints: `immutable`.

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="userId" type="string">

              [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

              You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      When the automation was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedBy" type="AuditInfo">

      Who last updated the automation.

      **Read-only.**

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="userId" type="string">

              [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

              You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      When the automation was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Automation name as displayed on the user's site.

      Constraints: `minLength 1`, `maxLength 500`.

    </ResponseField>

    <ResponseField name="description" type="string">

      Automation description.

    </ResponseField>

    <ResponseField name="configuration" type="AutomationConfiguration">

      Automation configuration.

      <Expandable title="configuration properties">

        <ResponseField name="status" type="string">

          Status of the automation on the site.

          **Supported values:**

          - `ACTIVE` — Active. Active automations can be triggered.
          - `INACTIVE` — Inactive. Inactive automations cannot be triggered.

        </ResponseField>

        <ResponseField name="trigger" type="Trigger">

          Trigger configuration.

          <Expandable title="trigger properties">

            <ResponseField name="appId" type="string">

              ID of the app that defines the trigger.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="triggerKey" type="string">

              Trigger key.

              Learn about [setting up a trigger](https://dev.wix.com/docs/rest/business-management/automations/triggers/add-a-trigger-to-your-app#step-1--set-up-the-trigger).

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

            <ResponseField name="filters" type="Filter[]">

              Schema field filters. All filter conditions must be met for the automation to run.

              Learn more about setting up [automation filters](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#filters).

              Constraints: `maxItems 5`.

              <Expandable title="filters properties">

                <ResponseField name="id" type="string">

                  Filter ID.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="fieldKey" type="string">

                  Field key.

                  You can find the field key in [the trigger payload schema](https://dev.wix.com/docs/rest/business-management/automations/triggers/the-trigger-payload-schema).

                  Constraints: `minLength 1`, `maxLength 110`.

                </ResponseField>

                <ResponseField name="filterExpression" type="string">

                  Filter expression in bracket notation.

                  For the automation to run, the expression must evaluate to `true`.

                  Constraints: `maxLength 5000`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="scheduledEventOffset" type="FutureDateActivationOffset">

              Automation offset. You can schedule automations to run before the trigger occurs.

              Learn more about [scheduled events](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#scheduled-events).

              <Expandable title="scheduledEventOffset properties">

                <ResponseField name="preScheduledEventOffsetExpression" type="string">

                  Amount of time before the trigger to run the automation.

                  > **Note**: To delay an automation after the trigger event occurs, use a [delay action](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#delay-action).

                  Constraints: `maxLength 1000`.

                </ResponseField>

                <ResponseField name="scheduledEventOffsetTimeUnit" type="string">

                  Unit in which to set the action offset.

                  **Supported values:**

                  - `MINUTES` — Minutes.
                  - `HOURS` — Hours.
                  - `DAYS` — Days.
                  - `WEEKS` — Weeks.
                  - `MONTHS` — Months.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="overrideSchema" type="object">

              Trigger schema override. When specified, this replaces the trigger schema.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="rootActionIds" type="string[]">

          Root action IDs. A root action is the first action that runs after the trigger occurs. Root actions run in parallel.

          > **Note**: You can currently only specify 1 root action.

          Constraints: `format GUID`, `maxItems 20`.

        </ResponseField>

        <ResponseField name="actions" type="object">

          Actions the automation can execute, as `key:value` pairs. For the key, specify the action ID. The value must be an object structured as described below.

          Constraints: `format map`.

          <Expandable title="actions properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="Action">

              <Expandable title="value properties">

                <ResponseField name="id" type="string">

                  Action ID.

                  Unique identifier for the action instance in the current automation. Use this to refer to this action in the automation flow.

                  If not specified, automatically generated by Wix.

                </ResponseField>

                <ResponseField name="type" type="string">

                  [Action type](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#action-type).

                  **Supported values:**

                  - `APP_DEFINED` — Defined by a [Wix app](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#app-defined-action). This action type is available in the site dashboard.
                  - `CONDITION` — Condition action. Evaluates a condition to determine which action runs next.

                  Learn more about [adding and setting up conditions](https://support.wix.com/en/article/the-new-automation-builder-understanding-conditions-operators) in Wix Automations.

                  > **Note**: When `skipActionExpression` evaluates to `true`, the actions in `conditionInfo.truePostActionIds` run and those in `conditionInfo.falsePostActionIds` are skipped.
                  - `CODE_CONDITION` — Code condition action. Evaluates custom code to determine which action runs next.

                  Learn more about [adding and setting up conditions](https://support.wix.com/en/article/the-new-automation-builder-understanding-conditions-operators) in Wix Automations.

                  > **Note**: When `skipActionExpression` evaluates to `true`, the actions in `codeConditionInfo.truePostActionIds` run and those in `codeConditionInfo.falsePostActionIds` are skipped.
                  - `DELAY` — Delay. The automation must wait before executing the next step.
                  - `RATE_LIMIT` — Rate-limiter. This stops the automation flow if the subsequent action has reached its maximum allowed executions.

                </ResponseField>

                <ResponseField name="displayName" type="string">

                  Editable display name for the action.

                </ResponseField>

                <ResponseField name="namespace" type="string">

                  [Action namespace](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#namespace). This differentiates it from other actions of the same type.

                  If the action outputs any data, the data appears under the namespace in the payload sent to the subsequent steps in the automation. If the user has multiple actions with the same `appId` and `actionKey`, the output of the previous action is overwritten.

                </ResponseField>

                <ResponseField name="skipActionExpression" type="string">

                  Evaluated to determine whether the action is skipped. When evaluated to `true`, the action is skipped. Otherwise, the action runs.

                  When empty, the action runs.

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="info" type="info">

                  info

                  _Nested structure omitted for brevity — see the `info` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="origin" type="string">

      How the automation was added to the user's site.

      Constraints: `immutable`.

      **Supported values:**

      - `USER` — Created by a [Wix user](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-user).
      - `APPLICATION` — Created by a [Wix app](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-app) for a particular site.
      - `PREINSTALLED` — [Preinstalled automation](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/automations/pre-installed-automations/about-pre-installed-automations).

    </ResponseField>

    <ResponseField name="settings" type="AutomationSettings">

      Automation settings. Applied only for pre-installed and application automations. Not applied to User origin automations

      <Expandable title="settings properties">

        <ResponseField name="hidden" type="boolean">

          Whether the automation is hidden from users.

          Default: `false`

        </ResponseField>

        <ResponseField name="readonly" type="boolean">

          Whether the automation is read-only.

          When `true`, site owners can't modify the automation or any of its actions. When `false`, users can configure specific actions as skippable or read-only using `settings.actionSettings`.

          Default: `false`.

          > **Note**: Setting `readOnly` to `true` overrides `settings.actionSettings`.

        </ResponseField>

        <ResponseField name="disableDelete" type="boolean">

          Whether to disable the option to delete the automation from the site.

          Default: `false`.

        </ResponseField>

        <ResponseField name="disableStatusChange" type="boolean">

          Whether to disable the option to change the automation's `configuration.status`.

          Default: `false`.

        </ResponseField>

        <ResponseField name="actionSettings" type="ActionSettings">

          Automation action settings.

          <Expandable title="actionSettings properties">

            <ResponseField name="permanentActionIds" type="string[]">

              IDs of actions whose `skipActionExpression` can't be modified.

              An action's `skipActionExpression` determines whether the action is skipped. Including an action's ID in this array makes its `skipActionExpression` permanent, so it can't be changed.

              When empty, the `skipActionExpression` of all actions can be edited.

              Constraints: `format GUID`, `maxItems 30`.

            </ResponseField>

            <ResponseField name="readonlyActionIds" type="string[]">

              IDs of read-only actions. Read-only actions cannot be modified.

              When empty and the automation's `settings.readonly` is `false`, all actions are modifiable.

              Constraints: `format GUID`, `maxItems 30`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="draftInfo" type="DraftInfo">

      When the automation is a draft, the draft details.

      **Read-only.**

      <Expandable title="draftInfo properties">

        <ResponseField name="originalAutomationId" type="string">

          ID of the original automation.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="archived" type="boolean">

      Whether the automation is archived.

      To archive an automation, set this to `true`. To restore an archived automation, set this to `false`.

    </ResponseField>

    <ResponseField name="autoArchivePolicy" type="AutoArchivePolicy">

      Auto archive policy

      <Expandable title="autoArchivePolicy properties">

        <ResponseField name="archiveDate" type="datetime">

          Date when to archive the automation
          If this date in the past, nothing will happen (automation will not go into archived state)
          If this date in the future, on this date the automation will be updated with archived = true and configuration.status = INACTIVE
          After this date the automation may be unarchived and archived again, this date will have no influence

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "id": "b0f12a71-d6f3-403e-9ebf-f47d4c700787",
        "revision": "6",
        "createdBy": {
          "userId": "ac829f87-a4a4-465c-b1c2-c19c63abc388"
        },
        "createdDate": "2025-05-08T09:15:48.121Z",
        "updatedBy": {
          "userId": "ac829f87-a4a4-465c-b1c2-c19c63abc388"
        },
        "updatedDate": "2025-05-08T09:16:38.628Z",
        "name": "Process form submission and send notification email",
        "configuration": {
          "status": "ACTIVE",
          "trigger": {
            "appId": "225dd912-7dea-4738-8688-4b8c6955ffc2",
            "triggerKey": "wix_form_app-form_submitted",
            "filters": []
          },
          "rootActionIds": [
            "6f1d7f5f-a559-472c-b526-f5cd2be7306f"
          ],
          "actions": {
            "2f4656ae-2edb-47dd-89bb-e36ca63cb434": {
              "id": "2f4656ae-2edb-47dd-89bb-e36ca63cb434",
              "type": "APP_DEFINED",
              "namespace": "triggered-emails-3",
              "appDefinedInfo": {
                "appId": "135c3d92-0fea-1f9d-2ba5-2a1dfb04297e",
                "actionKey": "triggered-emails",
                "inputMapping": {
                  "sendToUnsubscribed": false,
                  "transactional": false,
                  "messageUpdatedDate": "2025-05-08T09:16:00.687Z",
                  "dynamicParams": [],
                  "contactId": "{{var(\"contactId\")}}",
                  "messageId": "92fcd637-d6b4-4886-97cf-622d4f01063b",
                  "excludeFromQuota": false,
                  "notificationTopicId": "ba9ae5ad-1856-4d66-b66c-fd644bf1a840",
                  "actionConfigVersion": "V3"
                },
                "skipConditionOrExpressionGroups": [],
                "postActionIds": []
              }
            },
            "5145db50-1320-4ffd-a09f-28438f73951b": {
              "id": "5145db50-1320-4ffd-a09f-28438f73951b",
              "type": "DELAY",
              "namespace": "DELAY-2",
              "delayInfo": {
                "offsetExpression": "{{1}}",
                "offsetTimeUnit": "DAYS",
                "postActionIds": [
                  "2f4656ae-2edb-47dd-89bb-e36ca63cb434"
                ]
              }
            },
            "6f1d7f5f-a559-472c-b526-f5cd2be7306f": {
              "id": "6f1d7f5f-a559-472c-b526-f5cd2be7306f",
              "type": "RATE_LIMIT",
              "namespace": "RATE_LIMIT-1",
              "rateLimitInfo": {
                "maxActivationsExpression": "{{1}}",
                "rateLimitDurationTimeUnit": "UNKNOWN_TIME_UNIT",
                "uniqueIdentifierExpression": "{{var(\"contactId\")}}",
                "postActionIds": [
                  "5145db50-1320-4ffd-a09f-28438f73951b"
                ]
              }
            }
          }
        },
        "origin": "USER",
        "settings": {
          "hidden": false,
          "readonly": false,
          "disableDelete": false,
          "disableStatusChange": false,
          "actionSettings": {
            "permanentActionIds": [],
            "readonlyActionIds": [],
            "disableDelayAddition": false,
            "disableConditionAddition": false
          },
          "domain": "USER"
        },
        "archived": false
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Automation ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number. This increments by 1 whenever the automation is updated. Specify the current revision number whenever updating an existing automation.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdBy" type="AuditInfo">

      Who created the automation.

      **Read-only.** Constraints: `immutable`.

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="userId" type="string">

              [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

              You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      When the automation was created.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedBy" type="AuditInfo">

      Who last updated the automation.

      **Read-only.**

      <Expandable title="One of the following">

        <ResponseField name="id" type="id">

          id

          <Expandable title="id properties">

            <ResponseField name="userId" type="string">

              [User ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#user-id).

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="appId" type="string">

              [App ID](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#app-id).

              You can find the app ID on the [Home page](https://manage.wix.com/app-selector?title=Select+an+App&primaryButtonText=Select+Site&actionUrl=https%3A%2F%2Fdev.wix.com%2Fapps%2F%7BappId%7D%home) in the app's dashboard.

              Constraints: `format GUID`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      When the automation was last updated.

      **Read-only.**

    </ResponseField>

    <ResponseField name="name" type="string">

      Automation name as displayed on the user's site.

      Constraints: `minLength 1`, `maxLength 500`.

    </ResponseField>

    <ResponseField name="description" type="string">

      Automation description.

    </ResponseField>

    <ResponseField name="configuration" type="AutomationConfiguration">

      Automation configuration.

      <Expandable title="configuration properties">

        <ResponseField name="status" type="string">

          Status of the automation on the site.

          **Supported values:**

          - `ACTIVE` — Active. Active automations can be triggered.
          - `INACTIVE` — Inactive. Inactive automations cannot be triggered.

        </ResponseField>

        <ResponseField name="trigger" type="Trigger">

          Trigger configuration.

          <Expandable title="trigger properties">

            <ResponseField name="appId" type="string">

              ID of the app that defines the trigger.

              Constraints: `format GUID`.

            </ResponseField>

            <ResponseField name="triggerKey" type="string">

              Trigger key.

              Learn about [setting up a trigger](https://dev.wix.com/docs/rest/business-management/automations/triggers/add-a-trigger-to-your-app#step-1--set-up-the-trigger).

              Constraints: `minLength 1`, `maxLength 100`.

            </ResponseField>

            <ResponseField name="filters" type="Filter[]">

              Schema field filters. All filter conditions must be met for the automation to run.

              Learn more about setting up [automation filters](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#filters).

              Constraints: `maxItems 5`.

              <Expandable title="filters properties">

                <ResponseField name="_id" type="string">

                  Filter ID.

                  Constraints: `format GUID`.

                </ResponseField>

                <ResponseField name="fieldKey" type="string">

                  Field key.

                  You can find the field key in [the trigger payload schema](https://dev.wix.com/docs/rest/business-management/automations/triggers/the-trigger-payload-schema).

                  Constraints: `minLength 1`, `maxLength 110`.

                </ResponseField>

                <ResponseField name="filterExpression" type="string">

                  Filter expression in bracket notation.

                  For the automation to run, the expression must evaluate to `true`.

                  Constraints: `maxLength 5000`.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="scheduledEventOffset" type="FutureDateActivationOffset">

              Automation offset. You can schedule automations to run before the trigger occurs.

              Learn more about [scheduled events](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#scheduled-events).

              <Expandable title="scheduledEventOffset properties">

                <ResponseField name="preScheduledEventOffsetExpression" type="string">

                  Amount of time before the trigger to run the automation.

                  > **Note**: To delay an automation after the trigger event occurs, use a [delay action](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#delay-action).

                  Constraints: `maxLength 1000`.

                </ResponseField>

                <ResponseField name="scheduledEventOffsetTimeUnit" type="string">

                  Unit in which to set the action offset.

                  **Supported values:**

                  - `MINUTES` — Minutes.
                  - `HOURS` — Hours.
                  - `DAYS` — Days.
                  - `WEEKS` — Weeks.
                  - `MONTHS` — Months.

                </ResponseField>

              </Expandable>

            </ResponseField>

            <ResponseField name="overrideSchema" type="object">

              Trigger schema override. When specified, this replaces the trigger schema.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="rootActionIds" type="string[]">

          Root action IDs. A root action is the first action that runs after the trigger occurs. Root actions run in parallel.

          > **Note**: You can currently only specify 1 root action.

          Constraints: `format GUID`, `maxItems 20`.

        </ResponseField>

        <ResponseField name="actions" type="object">

          Actions the automation can execute, as `key:value` pairs. For the key, specify the action ID. The value must be an object structured as described below.

          Constraints: `format map`.

          <Expandable title="actions properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="Action">

              <Expandable title="value properties">

                <ResponseField name="_id" type="string">

                  Action ID.

                  Unique identifier for the action instance in the current automation. Use this to refer to this action in the automation flow.

                  If not specified, automatically generated by Wix.

                </ResponseField>

                <ResponseField name="type" type="string">

                  [Action type](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#action-type).

                  **Supported values:**

                  - `APP_DEFINED` — Defined by a [Wix app](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#app-defined-action). This action type is available in the site dashboard.
                  - `CONDITION` — Condition action. Evaluates a condition to determine which action runs next.

                  Learn more about [adding and setting up conditions](https://support.wix.com/en/article/the-new-automation-builder-understanding-conditions-operators) in Wix Automations.

                  > **Note**: When `skipActionExpression` evaluates to `true`, the actions in `conditionInfo.truePostActionIds` run and those in `conditionInfo.falsePostActionIds` are skipped.
                  - `CODE_CONDITION` — Code condition action. Evaluates custom code to determine which action runs next.

                  Learn more about [adding and setting up conditions](https://support.wix.com/en/article/the-new-automation-builder-understanding-conditions-operators) in Wix Automations.

                  > **Note**: When `skipActionExpression` evaluates to `true`, the actions in `codeConditionInfo.truePostActionIds` run and those in `codeConditionInfo.falsePostActionIds` are skipped.
                  - `DELAY` — Delay. The automation must wait before executing the next step.
                  - `RATE_LIMIT` — Rate-limiter. This stops the automation flow if the subsequent action has reached its maximum allowed executions.

                </ResponseField>

                <ResponseField name="displayName" type="string">

                  Editable display name for the action.

                </ResponseField>

                <ResponseField name="namespace" type="string">

                  [Action namespace](https://dev.wix.com/docs/rest/business-management/automations/automations/automations-v2/configure-your-automation#namespace). This differentiates it from other actions of the same type.

                  If the action outputs any data, the data appears under the namespace in the payload sent to the subsequent steps in the automation. If the user has multiple actions with the same `appId` and `actionKey`, the output of the previous action is overwritten.

                </ResponseField>

                <ResponseField name="skipActionExpression" type="string">

                  Evaluated to determine whether the action is skipped. When evaluated to `true`, the action is skipped. Otherwise, the action runs.

                  When empty, the action runs.

                </ResponseField>

              </Expandable>

              <Expandable title="One of the following">

                <ResponseField name="info" type="info">

                  info

                  _Nested structure omitted for brevity — see the `info` object._

                </ResponseField>

              </Expandable>

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="origin" type="string">

      How the automation was added to the user's site.

      Constraints: `immutable`.

      **Supported values:**

      - `USER` — Created by a [Wix user](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-user).
      - `APPLICATION` — Created by a [Wix app](https://dev.wix.com/docs/build-apps/get-started/overview/glossary#wix-app) for a particular site.
      - `PREINSTALLED` — [Preinstalled automation](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/automations/pre-installed-automations/about-pre-installed-automations).

    </ResponseField>

    <ResponseField name="settings" type="AutomationSettings">

      Automation settings. Applied only for pre-installed and application automations. Not applied to User origin automations

      <Expandable title="settings properties">

        <ResponseField name="hidden" type="boolean">

          Whether the automation is hidden from users.

          Default: `false`

        </ResponseField>

        <ResponseField name="readonly" type="boolean">

          Whether the automation is read-only.

          When `true`, site owners can't modify the automation or any of its actions. When `false`, users can configure specific actions as skippable or read-only using `settings.actionSettings`.

          Default: `false`.

          > **Note**: Setting `readOnly` to `true` overrides `settings.actionSettings`.

        </ResponseField>

        <ResponseField name="disableDelete" type="boolean">

          Whether to disable the option to delete the automation from the site.

          Default: `false`.

        </ResponseField>

        <ResponseField name="disableStatusChange" type="boolean">

          Whether to disable the option to change the automation's `configuration.status`.

          Default: `false`.

        </ResponseField>

        <ResponseField name="actionSettings" type="ActionSettings">

          Automation action settings.

          <Expandable title="actionSettings properties">

            <ResponseField name="permanentActionIds" type="string[]">

              IDs of actions whose `skipActionExpression` can't be modified.

              An action's `skipActionExpression` determines whether the action is skipped. Including an action's ID in this array makes its `skipActionExpression` permanent, so it can't be changed.

              When empty, the `skipActionExpression` of all actions can be edited.

              Constraints: `format GUID`, `maxItems 30`.

            </ResponseField>

            <ResponseField name="readonlyActionIds" type="string[]">

              IDs of read-only actions. Read-only actions cannot be modified.

              When empty and the automation's `settings.readonly` is `false`, all actions are modifiable.

              Constraints: `format GUID`, `maxItems 30`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="draftInfo" type="DraftInfo">

      When the automation is a draft, the draft details.

      **Read-only.**

      <Expandable title="draftInfo properties">

        <ResponseField name="originalAutomationId" type="string">

          ID of the original automation.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="archived" type="boolean">

      Whether the automation is archived.

      To archive an automation, set this to `true`. To restore an archived automation, set this to `false`.

    </ResponseField>

    <ResponseField name="autoArchivePolicy" type="AutoArchivePolicy">

      Auto archive policy

      <Expandable title="autoArchivePolicy properties">

        <ResponseField name="archiveDate" type="datetime">

          Date when to archive the automation
          If this date in the past, nothing will happen (automation will not go into archived state)
          If this date in the future, on this date the automation will be updated with archived = true and configuration.status = INACTIVE
          After this date the automation may be unarchived and archived again, this date will have no influence

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
