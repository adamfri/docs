---
title: "Sort and Filter"
description: "The following table shows field support for filters and sorting for the Categories object: | Fields | Supported Filters | Sortable | |-------------------------…"
icon: "file-lines"
---

The following table shows field support for filters and sorting for the Categories object:

| Fields                                | Supported Filters                                                | Sortable |
|---------------------------------------|------------------------------------------------------------------|----------|
| `id`                                  | `$eq`, `$ne`, `$in`, `$startsWith`, `$lt`, `$lte`, `$gt`, `$gte` | Sortable |
| `createdDate`                         | `$eq`, `$ne`, `$in`, `$lt`, `$lte`, `$gt`, `$gte`                | Sortable |
| `name`                                | `$eq`, `$ne`, `$in`, `$startsWith`, `$lt`, `$lte`, `$gt`, `$gte` | Sortable |
| `ratingsSettings.ratingsEnabled`      | `$eq`, `$ne`, `$in`                                              | Sortable |
| `ratingsSettings.ratingRequired`      | `$eq`, `$ne`, `$in`                                              | Sortable |
| `permissionsSettings.createComment`   | `$eq`, `$ne`, `$in`                                              | Sortable |
| `permissionsSettings.createReply`     | `$eq`, `$ne`, `$in`                                              | Sortable |
| `permissionsSettings.createReact`     | `$eq`, `$ne`, `$in`                                              | Sortable |
| `appId`                               | `$eq`, `$ne`, `$in`, `$startsWith`, `$lt`, `$lte`, `$gt`, `$gte` | Sortable |

**Related content:**
[API Query Language](fallback::https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
[Query Category](fallback::https://dev.wix.com/docs/api-reference/crm/community/comments/categories/query-category)
