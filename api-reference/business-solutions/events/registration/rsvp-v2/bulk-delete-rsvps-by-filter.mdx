---
title: "Bulk Delete Rsvps By Filter"
description: "Deletes multiple RSVPs."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple RSVPs.

    This method is asynchronous. It returns a `jobId` immediately and deletes the matching RSVPs in the background.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v2/bulk/rsvps/delete-by-filter
    ```

    ## Body Params

    <ParamField body="filter" type="object" required>

      Filter object.

    </ParamField>

    <ParamField body="search" type="SearchDetails">

      Search details.

      <Expandable title="search properties">

        <ParamField body="mode" type="string">

          Search mode. Defines the search logic for combining multiple terms in the `expression`.

          **Supported values:**

          - `OR` — At least one of the search terms must be present.
          - `AND` — Searches by all provided words.

        </ParamField>

        <ParamField body="expression" type="string">

          Search term or expression.

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to search in. If the array is empty, all searchable fields are searched.
          Use dot notation to specify a JSON path. For example, For example, `order.address.streetName`.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

        <ParamField body="fuzzy" type="boolean">

          Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID. Call [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/get-async-job) with this ID to retrieve the job details and metadata.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `EMPTY_FILTER` | Filter can't be empty. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Deleted | `wix.events.v2.rsvp_deleted` | Triggered when an RSVP is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/bulk/rsvps/delete-by-filter' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "filter": {
          "status": "NO"
        }
      }'
      ```

      ```json Response
      {
        "jobId": "b551071c-0688-4c43-84fd-3f7f106c7a39"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple RSVPs.

    This method is asynchronous. It returns a `jobId` immediately and deletes the matching RSVPs in the background.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { rsvpV2 } from '@wix/events';

    rsvpV2.bulkDeleteRsvpsByFilter(filter, options): Promise<BulkDeleteRsvpsByFilterResponse>
    ```

    ## Method Parameters

    <ParamField body="filter" type="object" required>

      Filter object.

    </ParamField>

    <ParamField body="options" type="BulkDeleteRsvpsByFilterOptions">

      options

      <Expandable title="options properties">

        <ParamField body="search" type="SearchDetails">

          Search details.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least one of the search terms must be present.
              - `AND` — Searches by all provided words.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched.
              Use dot notation to specify a JSON path. For example, For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkDeleteRsvpsByFilterResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID. Call [Get Async Job](https://dev.wix.com/docs/rest/business-management/async-job/get-async-job) with this ID to retrieve the job details and metadata.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `EMPTY_FILTER` | Filter can't be empty. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Deleted | `wix.events.v2.rsvp_deleted` | Triggered when an RSVP is deleted. |
    | Rsvp Rsvps Balance Updated | `wix.events.v2.rsvp_rsvps_balance_updated` | Triggered when an RSVP balance is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-rsvps-by-filter" id="ex-sdk-bulk-delete-rsvps-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-rsvps-by-filter" id="ex-sdk-bulk-delete-rsvps-by-filter-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-rsvps-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-rsvps-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk delete RSVPs by status</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteRsvpsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-rsvps-by-filter-0"><span className="wix-picker-name">Bulk delete RSVPs by status</span></label>
    <label htmlFor="ex-sdk-bulk-delete-rsvps-by-filter-1"><span className="wix-picker-name">bulkDeleteRsvpsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk delete RSVPs by status
    import { rsvpV2 } from "@wix/events";

    async function bulkDeleteRsvpsByFilter() {
      const response = await rsvpV2.bulkDeleteRsvpsByFilter({
        filter: { status: "NO" },
      });
      return response;
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteRsvpsByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rsvpV2 } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rsvpV2 },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteRsvpsByFilter(filter,options) {
      const response = await myWixClient.rsvpV2.bulkDeleteRsvpsByFilter(filter,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
