---
title: "Update Operation Group"
description: "Updates a OperationGroup."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a OperationGroup.

    Each time the OperationGroup is updated,
    `revision` increments by 1.
    The current `revision` must be passed when updating the OperationGroup.
    This ensures you're working with the latest OperationGroup
    and prevents unintended overwrites.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/restaurants/v1/operation-groups/{operationGroup.id}
    ```

    ## Path Params

    <ParamField path="operationGroup.id" type="string" required>

      OperationsGroup ID.

    </ParamField>

    ## Body Params

    <ParamField body="operationGroup" type="OperationGroup" required>

      OperationGroup to be updated, may be partial.

      See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="operationGroup" type="OperationGroup">

      Updated OperationGroup.

      See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Group Updated | `wix.restaurants.v1.operation_group_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH https://www.wixapis.com/restaurants/v1/operation-groups/48605ac5-53fc-49b4-96b2-87f7636f5ce0 \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>' \
        --data-raw '{
            "operationGroup": {
               "revision": "1",
               "name": "Ordering page 2",
            }
          }'
      ```

      ```json Response
      "operationGroup": {
          "id": "48605ac5-53fc-49b4-96b2-87f7636f5ce0",
          "revision": "1",
          "createdDate": "2024-07-09T10:17:08.865Z",
          "updatedDate": "2024-07-09T10:17:08.865Z",
          "name": "Ordering page 22"
         }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a OperationGroup.

    Each time the OperationGroup is updated,
    `revision` increments by 1.
    The current `revision` must be passed when updating the OperationGroup.
    This ensures you're working with the latest OperationGroup
    and prevents unintended overwrites.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { operationGroups } from '@wix/restaurants';

    operationGroups.updateOperationGroup(_id, operationGroup): Promise<OperationGroup>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      OperationsGroup ID.

      **Read-only.**

    </ParamField>

    <ParamField body="operationGroup" type="UpdateOperationGroup" required>

      OperationGroup to be updated, may be partial.

      <Expandable title="operationGroup properties">

        <ParamField body="_id" type="string">

          OperationsGroup ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the OperationsGroup is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the OperationsGroup.

          Ignored when creating a OperationsGroup.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the OperationsGroup was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the OperationsGroup was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          The name of the operations group

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Data Extensions

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tags ([SDK](https://dev.wix.com/docs/sdk/backend-modules/tags/tags/introduction) | [REST](https://dev.wix.com/docs/rest/business-management/tags/introduction)) used to classify and sort different types of operation groups.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<OperationGroup>`.

    See the [Operation Group Object](/api-reference/business-solutions/restaurants/online-orders/operation-groups/operation-group-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Group Updated | `wix.restaurants.v1.operation_group_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-operation-group" id="ex-sdk-update-operation-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-operation-group" id="ex-sdk-update-operation-group-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-operation-group" id="ex-sdk-update-operation-group-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-operation-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-operation-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateOperationGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateOperationGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateOperationGroup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-operation-group-0"><span className="wix-picker-name">updateOperationGroup</span></label>
    <label htmlFor="ex-sdk-update-operation-group-1"><span className="wix-picker-name">updateOperationGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-operation-group-2"><span className="wix-picker-name">updateOperationGroup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateOperationGroup
    import { operationGroups } from '@wix/restaurants';

    async function updateOperationGroup(_id,operationGroup) {
      const response = await operationGroups.updateOperationGroup(_id,operationGroup);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateOperationGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { operationGroups } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { operationGroups },
      // Include the auth strategy and host as relevant
    });

    async function updateOperationGroup(_id,operationGroup) {
      const response = await myWixClient.operationGroups.updateOperationGroup(_id,operationGroup);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateOperationGroup (with elevated permissions)
    import { operationGroups } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myUpdateOperationGroupMethod(_id,operationGroup) {
      const elevatedUpdateOperationGroup = auth.elevate(operationGroups.updateOperationGroup);
      const response = await elevatedUpdateOperationGroup(_id,operationGroup);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
