---
title: "Get Category"
description: "Retrieves a category by ID."
icon: "download"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a category by ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="WIX_COMMENTS.CATEGORY_READ">WIX_COMMENTS.CATEGORY_READ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    GET https://www.wixapis.com/comments/v1/categories/{categoryId}
    ```

    ## Path Params

    <ParamField path="categoryId" type="string" required>

      Category ID.

    </ParamField>

    ## Response Object

    <ResponseField name="category" type="Category">

      Retrieved category.

      See the [Category Object](/api-reference/crm/community/feedback-moderation/comments/categories/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET https://www.wixapis.com/comments/v1/categories/1b11f859-eb54-4eed-97fb-b5498793e8c1  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
                 "categoryId":"1b11f859-eb54-4eed-97fb-b5498793e8c1"
              }'
      ```

      ```json Response
      {
        "category": {
          "id": "1b11f859-eb54-4eed-97fb-b5498793e8c1",
          "name": "Test updated category",
          "guestCommenting": true,
          "guestReactions": true,
          "reactionType": "REACTIONS",
          "repliesReactionType": "REACTIONS",
          "markedCommentLabelType": "FEATURED",
          "customMarkedCommentLabel": null,
          "mainReaction": {
            "iconType": "THUMBS_UP"
          },
          "ratingsSettings": {
            "ratingsEnabled": false,
            "ratingRequired": false
          },
          "revision": "2",
          "createdDate": "2024-10-02T13:04:39.621Z",
          "updatedDate": "2024-10-02T14:29:42.397Z",
          "permissionsSettings": {
            "createComment": {
              "role": "ALL"
            },
            "createReply": {
              "role": "ALL"
            },
            "createReact": {
              "role": "ALL"
            }
          },
          "active": true,
          "appId": "91c9d6a7-6667-41fb-b0b4-7d3b3ff0b02e"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a category by ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="WIX_COMMENTS.CATEGORY_READ">WIX_COMMENTS.CATEGORY_READ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { categories } from '@wix/comments';

    categories.getCategory(categoryId): Promise<Category>
    ```

    ## Method Parameters

    <ParamField body="categoryId" type="string" required>

      Category ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<Category>`.

    See the [Category Object](/api-reference/crm/community/feedback-moderation/comments/categories/category-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-category" id="ex-sdk-get-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-category" id="ex-sdk-get-category-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-category" id="ex-sdk-get-category-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getCategory</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getCategory (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-category-0"><span className="wix-picker-name">getCategory</span></label>
    <label htmlFor="ex-sdk-get-category-1"><span className="wix-picker-name">getCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-category-2"><span className="wix-picker-name">getCategory (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getCategory
    import { categories } from '@wix/comments';

    async function getCategory(categoryId) {
      const response = await categories.getCategory(categoryId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categories } from '@wix/comments';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categories },
      // Include the auth strategy and host as relevant
    });

    async function getCategory(categoryId) {
      const response = await myWixClient.categories.getCategory(categoryId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getCategory (with elevated permissions)
    import { categories } from '@wix/comments';
    import { auth } from '@wix/essentials';

    async function myGetCategoryMethod(categoryId) {
      const elevatedGetCategory = auth.elevate(categories.getCategory);
      const response = await elevatedGetCategory(categoryId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
