---
title: "Get Default Branch"
description: "Retrieves the default branch."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the default branch.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DOCUMENT-MANAGEMENT.MANAGE-BRANCHES">Manage Site Branches</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/branches/v1/branches/default
    ```

    ## Response Object

    <ResponseField name="branch" type="Branch">

      Default branch.

      See the [Branch Object](/api-reference/business-management/branches/branch-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves the default branch for the site

    <CodeGroup>

      ```bash Request
      curl -X GET \
          'https://www.wixapis.com/branches/v1/branches/default' \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "branch": {
          "id":"00000000-0000-0000-0000-000000000000",
          "revision":"1",
          "type":"ORIGINAL_BRANCH",
          "sourceType":"SOURCE_BRANCH",
          "lastPublishDate":"1736941884789",
          "editorType": {
            "editorTypeOptions": "STUDIO"
          },
          "default": true,
          "sourceBranchProperties": {
            "branchId":"00000000-0000-0000-0000-000000000000"
          },
          "createdDate":"2025-01-15T09:30:02.472Z",
          "updatedDate":"2025-01-15T09:30:02.472Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves the default branch.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DOCUMENT-MANAGEMENT.MANAGE-BRANCHES">Manage Site Branches</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { branches } from '@wix/editor-branches';

    branches.getDefaultBranch(): Promise<GetDefaultBranchResponse>
    ```

    ## Returns

    Returns `Promise<GetDefaultBranchResponse>`.

    <ResponseField name="branch" type="Branch">

      Default branch.

      See the [Branch Object](/api-reference/business-management/branches/branch-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-default-branch" id="ex-sdk-get-default-branch-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-default-branch" id="ex-sdk-get-default-branch-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-default-branch" id="ex-sdk-get-default-branch-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-default-branch-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-default-branch-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getDefaultBranch</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDefaultBranch (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDefaultBranch (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-default-branch-0"><span className="wix-picker-name">getDefaultBranch</span></label>
    <label htmlFor="ex-sdk-get-default-branch-1"><span className="wix-picker-name">getDefaultBranch (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-default-branch-2"><span className="wix-picker-name">getDefaultBranch (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getDefaultBranch
    import { branches } from '@wix/editor-branches';

    async function getDefaultBranch() {
      const response = await branches.getDefaultBranch();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDefaultBranch (self-hosted)
    import { createClient } from '@wix/sdk';
    import { branches } from '@wix/editor-branches';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { branches },
      // Include the auth strategy and host as relevant
    });

    async function getDefaultBranch() {
      const response = await myWixClient.branches.getDefaultBranch();
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDefaultBranch (with elevated permissions)
    import { branches } from '@wix/editor-branches';
    import { auth } from '@wix/essentials';

    async function myGetDefaultBranchMethod() {
      const elevatedGetDefaultBranch = auth.elevate(branches.getDefaultBranch);
      const response = await elevatedGetDefaultBranch();
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
