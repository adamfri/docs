---
title: "Remove Data Items"
description: "Removes one or more items from a collection."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Removes one or more items from a collection. The response object must contain the removed item.

    ## Endpoint

    ```http
    POST /v3/items/remove
    ```

    ## Body Params

    <ParamField body="collectionId" type="string" required>

      ID of the collection from which to remove items.

    </ParamField>

    <ParamField body="itemIds" type="string[]" required>

      IDs of items to remove.

      Constraints: `minItems 1`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="DataItemModificationResult[]">

      Response must include either the removed item or an error.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-remove-data-items" id="ex-rest-remove-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-remove-data-items" id="ex-rest-remove-data-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-remove-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-remove-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Remove several items</span><span className="wix-picker-desc">Removes two data items from a collection. One removal fails while the other succeeds</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Remove a single item from a collection</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-remove-data-items-0"><span className="wix-picker-name">Remove several items</span><span className="wix-picker-desc">Removes two data items from a collection. One removal fails while the other succeeds</span></label>
    <label htmlFor="ex-rest-remove-data-items-1"><span className="wix-picker-name">Remove a single item from a collection</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://external-db.example.com/v3/items/remove  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
        "collectionId": "cities",
        "itemIds": ["c285e77c-a86b-4361-a55f-c6b934d70187", "e831385d-d067-4905-a98a-456f5a526ca2"]
      }'
      ```

      ```json Response
      {
        "results": [{
          "item": {
            "name": "New York",
            "_id": "c285e77c-a86b-4361-a55f-c6b934d70187",
            "_owner": "facda016-24ef-4909-a90e-70687f17f658",
            "_createdDate": {
              "$date": "2021-01-02T03:04:05Z"
            },
            "_updatedDate": {
              "$date": "2021-04-05T06:07:08Z"
            }
          }
        }, {
          "error": {
            "errorCode": "ITEM_NOT_FOUND",
            "errorMessage": "Item does not exist",
            "data": {
              "itemId": "e831385d-d067-4905-a98a-456f5a526ca2"
            }
          }
        }]
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST https://external-db.example.com/v3/items/remove  \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>' \
          -d '{
        "collectionId": "cities",
        "itemIds": ["c285e77c-a86b-4361-a55f-c6b934d70187"]
      }'
      ```

      ```json Response
      {
        "results": [{
          "item": {
            "name": "New York",
            "_id": "c285e77c-a86b-4361-a55f-c6b934d70187",
            "_owner": "facda016-24ef-4909-a90e-70687f17f658",
            "_createdDate": {
              "$date": "2021-01-02T03:04:05Z"
            },
            "_updatedDate": {
              "$date": "2021-04-05T06:07:08Z"
            }
          }
        }]
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Removes one or more items from a collection. The response object must contain the removed item.

    ## Method Declaration

    ```typescript
    import { externalDatabase } from '@wix/data';

    externalDatabase.removeDataItems(request, metadata): Promise
    ```

    ## Method Parameters

    <ParamField body="request" type="RemoveDataItemsRequest">

      request

      <Expandable title="request properties">

        <ParamField body="collectionId" type="string">

          ID of the collection from which to remove items.

        </ParamField>

        <ParamField body="itemIds" type="string[]">

          IDs of items to remove.

          Constraints: `minItems 1`.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="metadata" type="Context">

      this message is not directly used by any service,
      it exists to describe the expected parameters that SHOULD be provided to invoked Velo methods as part of open-platform.
      e.g. SPIs, event-handlers, etc..
      NOTE: this context object MUST be provided as the last argument in each Velo method signature.

      Example:
      ```typescript
      export function wixStores_onOrderCanceled({ event, metadata }: OrderCanceledEvent) {
      ...
      }
      ```

      <Expandable title="metadata properties">

        <ParamField body="requestId" type="string">

          A unique identifier of the request. You may print this ID to your logs to help with future debugging and easier correlation with Wix's logs.

        </ParamField>

        <ParamField body="currency" type="string">

          [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) 3-letter currency code.

        </ParamField>

        <ParamField body="identity" type="IdentificationData">

          An object that describes the identity that triggered this request.

          <Expandable title="identity properties">

            <ParamField body="identityType" type="string">

              **Read-only.**

              **Supported values:**

              - `ANONYMOUS_VISITOR`
              - `MEMBER`
              - `WIX_USER`
              - `APP`

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="id" type="id">

              id

              _Nested structure omitted for brevity — see the `id` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="languages" type="string[]">

          A string representing a language and region in the format of `"xx-XX"`. First 2 letters represent the language code according to ISO 639-1. This is followed by a dash "-", and then a by 2 capital letters representing the region according to ISO 3166-2. For example, `"en-US"`.

        </ParamField>

        <ParamField body="instanceId" type="string">

          The service provider app's instance ID.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="DataItemModificationResult[]">

      Response must include either the removed item or an error.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-remove-data-items" id="ex-sdk-remove-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-remove-data-items" id="ex-sdk-remove-data-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-remove-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-remove-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Example of a results return value</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">removeDataItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-remove-data-items-0"><span className="wix-picker-name">Example of a results return value</span></label>
    <label htmlFor="ex-sdk-remove-data-items-1"><span className="wix-picker-name">removeDataItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Example of a results return value
    import { externalDatabase } from '@wix/data/service-plugins';

    externalDatabase.provideHandlers({

      removeDataItems: async ( payload ) => {
        const {request, metadata} = payload;
            // Use the `request` and `metadata` received from Wix and
            // apply custom logic.
          return {
            // Return your response exactly as documented to integrate with Wix.
            // Return value example:
            results: [
              {
                item: {
                  name: "New York",
                  _id: "c285e77c-a86b-4361-a55f-c6b934d70187",
                  _owner: "facda016-24ef-4909-a90e-70687f17f658",
                  _createdDate: {
                    $date: "2021-01-02T03:04:05Z"
                  },
                  _updatedDate: {
                    $date: "2021-04-05T06:07:08Z"
                  }
                }
              }
            ]
          }
        }
    });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript removeDataItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { externalDatabase } from '@wix/data/service-plugins';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { externalDatabase },
      // Include the auth strategy and host as relevant
    });

    async function removeDataItems(request,metadata) {
      const response = await myWixClient.externalDatabase.removeDataItems(request,metadata);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
