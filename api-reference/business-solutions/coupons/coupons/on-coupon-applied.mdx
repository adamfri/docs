---
title: "onCouponApplied"
description: "Triggered when a coupon is applied."
icon: "bolt"
---

Triggered when a coupon is applied.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecommerce.coupons.v2.coupon_applied` |
| Package | `@wix/marketing` |
| Permissions | `Manage Stores - all permissions`, `Manage Coupons` |

## Examples

<CodeGroup>

  ```javascript onCouponApplied
  import { coupons } from '@wix/marketing';

  coupons.onCouponApplied((event) => {
      // handle your event here
  });
  ```

  ```javascript onCouponApplied (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { coupons } from '@wix/marketing';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        coupons,
      },
  });

  wixClient.coupons.onCouponApplied((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
