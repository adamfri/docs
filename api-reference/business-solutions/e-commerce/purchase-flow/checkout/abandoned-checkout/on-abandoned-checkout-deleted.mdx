---
title: "onAbandonedCheckoutDeleted"
description: "Triggerred when an abandoned checkout is deleted."
icon: "bolt"
---

Triggerred when an abandoned checkout is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.abandoned_checkout_deleted` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Read eCommerce - all read permissions`, `Read Orders`, `Read Stores - all read permissions`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onAbandonedCheckoutDeleted
  import { abandonedCheckouts } from '@wix/ecom';

  abandonedCheckouts.onAbandonedCheckoutDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onAbandonedCheckoutDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { abandonedCheckouts } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        abandonedCheckouts,
      },
  });

  wixClient.abandonedCheckouts.onAbandonedCheckoutDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
