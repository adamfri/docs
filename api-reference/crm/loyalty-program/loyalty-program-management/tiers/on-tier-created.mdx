---
title: "onTierCreated"
description: "Triggered when a tier has been created."
icon: "bolt"
---

Triggered when a tier has been created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.v1.tier_created` |
| Package | `@wix/loyalty` |

## Examples

<CodeGroup>

  ```javascript onTierCreated
  import { tiers } from '@wix/loyalty';

  tiers.onTierCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onTierCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { tiers } from '@wix/loyalty';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        tiers,
      },
  });

  wixClient.tiers.onTierCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
