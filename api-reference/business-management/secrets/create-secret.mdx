---
title: "Create Secret"
description: "Creates a secret."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a secret.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.VELO.MANAGE_SECRETS">Manage Secrets</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/secrets-service/api/v1/secrets
    ```

    ## Body Params

    <ParamField body="secret" type="Secret" required>

      Details of a secret.

      See the [Secret Object](/api-reference/business-management/secrets/secret-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="id" type="string">

      Unique secret ID.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST 'https://www.wixapis.com/_api/cloud-secrets-vault-server/api/v1/secrets' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
           "secret": {
              "name": "secret_key",
              "description": "AWS secret access key",
              "value": "Fm8OfflH6bJOwWjenqAtLurLbkiMNvmhQHZV+118"
          }
      }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "id": "a560ff70-3e9b-41b6-b66d-cf3434085f7d"
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a secret.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.VELO.MANAGE_SECRETS">Manage Secrets</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { secrets } from '@wix/secrets';

    secrets.createSecret(secret): Promise<string>
    ```

    ## Method Parameters

    <ParamField body="secret" type="Secret" required>

      Fields of a new secret. 

      See the [Secret Object](/api-reference/business-management/secrets/secret-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<string>`.

    <ResponseField name="_id" type="string">

      Unique secret ID.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Secret Created | `wix.velo.secrets_vault.v1.secret_created` | Triggered when a secret is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-secret" id="ex-sdk-create-secret-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-secret" id="ex-sdk-create-secret-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-secret" id="ex-sdk-create-secret-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-secret-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-secret-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createSecret</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createSecret (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createSecret (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-secret-0"><span className="wix-picker-name">createSecret</span></label>
    <label htmlFor="ex-sdk-create-secret-1"><span className="wix-picker-name">createSecret (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-secret-2"><span className="wix-picker-name">createSecret (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createSecret
    import { secrets } from '@wix/secrets';

    async function createSecret(secret) {
      const response = await secrets.createSecret(secret);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createSecret (self-hosted)
    import { createClient } from '@wix/sdk';
    import { secrets } from '@wix/secrets';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { secrets },
      // Include the auth strategy and host as relevant
    });

    async function createSecret(secret) {
      const response = await myWixClient.secrets.createSecret(secret);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createSecret (with elevated permissions)
    import { secrets } from '@wix/secrets';
    import { auth } from '@wix/essentials';

    async function myCreateSecretMethod(secret) {
      const elevatedCreateSecret = auth.elevate(secrets.createSecret);
      const response = await elevatedCreateSecret(secret);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
