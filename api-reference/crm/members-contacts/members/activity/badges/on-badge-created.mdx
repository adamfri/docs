---
title: "onBadgeCreated"
description: "Triggered when a badge is created."
icon: "bolt"
---

Triggered when a badge is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v3.badge_created` |
| Package | `@wix/members` |
| Permissions | `Manage Stores`, `Manage Challenges`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeCreated
  import { badges } from '@wix/members';

  badges.onBadgeCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badges } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        badges,
      },
  });

  wixClient.badges.onBadgeCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
