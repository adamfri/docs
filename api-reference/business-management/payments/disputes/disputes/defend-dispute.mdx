---
title: "Defend Dispute"
description: "Defends a dispute by submitting evidence to the payment service provider or bank handling the dispute."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Defends a dispute by submitting evidence to the payment service provider or bank handling the dispute.

    To upload evidence documents, use the [Dispute Evidence Documents API](https://dev.wix.com/docs/api-reference/business-management/payments/disputes/dispute-evidence-documents/create-dispute-evidence-document). This method submits all the evidence documents uploaded for the specified dispute.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.MANAGE-DISPUTES">Manage Disputes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/payments/disputes/v1/disputes/{disputeId}/defend
    ```

    ## Path Params

    <ParamField path="disputeId" type="string" required>

      ID of the dispute to defend.

    </ParamField>

    ## Response Object

    <ResponseField name="dispute" type="Dispute">

      Updated dispute after submitting evidence.

      See the [Dispute Object](/api-reference/business-management/payments/disputes/disputes/dispute-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Dispute Updated | `wix.payments.disputes.v1.dispute_updated` | Triggered when a dispute is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Initiates the dispute defense process - evidence must be submitted separately via Dispute Evidence Document API

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/payments/disputes/v1/disputes/12345678-1234-1234-1234-123456789012/defend' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json'
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Defends a dispute by submitting evidence to the payment service provider or bank handling the dispute.

    To upload evidence documents, use the [Dispute Evidence Documents API](https://dev.wix.com/docs/api-reference/business-management/payments/disputes/dispute-evidence-documents/create-dispute-evidence-document). This method submits all the evidence documents uploaded for the specified dispute.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-PAYMENTS.MANAGE-DISPUTES">Manage Disputes</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { disputes } from '@wix/payments';

    disputes.defendDispute(disputeId): Promise
    ```

    ## Method Parameters

    <ParamField body="disputeId" type="string" required>

      ID of the dispute to defend.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    <ResponseField name="dispute" type="Dispute">

      Updated dispute after submitting evidence.

      See the [Dispute Object](/api-reference/business-management/payments/disputes/disputes/dispute-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Dispute Updated | `wix.payments.disputes.v1.dispute_updated` | Triggered when a dispute is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-defend-dispute" id="ex-sdk-defend-dispute-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-defend-dispute" id="ex-sdk-defend-dispute-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-defend-dispute" id="ex-sdk-defend-dispute-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-defend-dispute-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-defend-dispute-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">defendDispute</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">defendDispute (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">defendDispute (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-defend-dispute-0"><span className="wix-picker-name">defendDispute</span></label>
    <label htmlFor="ex-sdk-defend-dispute-1"><span className="wix-picker-name">defendDispute (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-defend-dispute-2"><span className="wix-picker-name">defendDispute (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript defendDispute
    import { disputes } from '@wix/payments';

    async function defendDispute(disputeId) {
      const response = await disputes.defendDispute(disputeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript defendDispute (self-hosted)
    import { createClient } from '@wix/sdk';
    import { disputes } from '@wix/payments';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { disputes },
      // Include the auth strategy and host as relevant
    });

    async function defendDispute(disputeId) {
      const response = await myWixClient.disputes.defendDispute(disputeId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript defendDispute (with elevated permissions)
    import { disputes } from '@wix/payments';
    import { auth } from '@wix/essentials';

    async function myDefendDisputeMethod(disputeId) {
      const elevatedDefendDispute = auth.elevate(disputes.defendDispute);
      const response = await elevatedDefendDispute(disputeId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
