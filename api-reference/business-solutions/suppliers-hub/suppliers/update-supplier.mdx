---
title: "Update Supplier"
description: "Updates a supplier."
icon: "pen"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates a supplier.

    Each time the supplier is updated, `revision` increments by 1. The current `revision` must be passed when updating the supplier. This ensures you're working with the latest supplier information and prevents conflicting changes. If the revision is outdated, the request fails and you must fetch the current supplier state before retrying.

    When updating rating information, update both `rating` and `reviewCount` together to maintain consistency.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.WRITE_MARKETPLACE">Write Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/suppliers-hub/v1/suppliers/{supplier.id}
    ```

    ## Path Params

    <ParamField path="supplier.id" type="string" required>

      Supplier ID.

    </ParamField>

    ## Body Params

    <ParamField body="supplier" type="Supplier" required>

      Supplier to update.

      See the [Supplier Object](/api-reference/business-solutions/suppliers-hub/suppliers/supplier-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="supplier" type="Supplier">

      Updated supplier with incremented revision number.

      See the [Supplier Object](/api-reference/business-solutions/suppliers-hub/suppliers/supplier-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Supplier Updated | `wix.suppliershub.marketplace_supplier.v1.supplier_updated` | Triggered when a supplier is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Updates supplier rating and trust status with revision control

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/suppliers-hub/v1/suppliers/12345678-1234-1234-1234-123456789012' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "supplier": {
          "id": "0d24a5c5-45e6-4ace-ae47-3ce9bed851d8",
          "name": "Not top rated anymore",
          "rating": "4.95",
          "revision": "1",
          "reviewCount": 13223
        }
      }'
      ```

      ```json Response
      {
        "supplier": {
          "id": "0d24a5c5-45e6-4ace-ae47-3ce9bed851d8",
          "revision": "2",
          "createdDate": "2025-09-25T12:52:03.538Z",
          "updatedDate": "2025-09-25T13:16:19.637Z",
          "name": "Not top rated anymore",
          "appId": "22bef345-3c5b-4c18-b782-74d4085112ff",
          "location": {
            "country": "CH"
          },
          "rating": "4.95",
          "verified": true,
          "types": [
            "WHOLESALE",
            "DROPSHIPPING"
          ],
          "reviewCount": 12544,
          "tags": null
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Updates a supplier.

    Each time the supplier is updated, `revision` increments by 1. The current `revision` must be passed when updating the supplier. This ensures you're working with the latest supplier information and prevents conflicting changes. If the revision is outdated, the request fails and you must fetch the current supplier state before retrying.

    When updating rating information, update both `rating` and `reviewCount` together to maintain consistency.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.SUPPLIERS_HUB.WRITE_MARKETPLACE">Write Marketplace</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { suppliers } from '@wix/suppliers-hub';

    suppliers.updateSupplier(_id, supplier): Promise
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Supplier ID.

      **Read-only.**

    </ParamField>

    <ParamField body="supplier" type="UpdateSupplier" required>

      Supplier to update.

      <Expandable title="supplier properties">

        <ParamField body="_id" type="string">

          Supplier ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the Supplier is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the Supplier.

          Ignored when creating a Supplier.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the Supplier was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the Supplier was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Supplier name. Displayed to providers when browsing and selecting suppliers for their product catalogs.

        </ParamField>

        <ParamField body="appId" type="string">

          App ID of the provider application responsible for managing the supplier and its products. Automatically set to the ID of the application making the create request.

          **Read-only.**

        </ParamField>

        <ParamField body="location" type="Location">

          Supplier location.

          <Expandable title="location properties">

            <ParamField body="country" type="string">

              2-letter country code in [ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1) format. For example, `US` for United States or `GB` for United Kingdom.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="rating" type="string">

          Supplier rating from 1.00 to 5.00. For example, `4.25`. Provider applications manage ratings and should update both `rating` and `reviewCount` together to maintain consistency.

        </ParamField>

        <ParamField body="verified" type="boolean">

          Whether the supplier has passed verification processes.

          Default: `false`

        </ParamField>

        <ParamField body="types" type="string[]">

          Supplier business model types. `DROPSHIPPING` suppliers ship directly to end customers, while `WHOLESALE` suppliers ship in bulk to the provider who fulfills orders. A supplier can support multiple fulfillment models.

          Constraints: `minItems 1`, `maxItems 2`.

          **Supported values:**

          - `DROPSHIPPING` — Supplier ships products directly to end customers. The provider does not handle physical inventory or fulfillment.
          - `WHOLESALE` — Supplier ships products in bulk to the provider, who then fulfills orders to end customers.

        </ParamField>

        <ParamField body="reviewCount" type="integer">

          Number of reviews for the supplier. Provider applications should update both `rating` and `reviewCount` together to maintain consistency.

        </ParamField>

        <ParamField body="tags" type="Tags">

          Supplier tags for organization and categorization.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="publicTags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Supplier`.

    See the [Supplier Object](/api-reference/business-solutions/suppliers-hub/suppliers/supplier-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Supplier Updated | `wix.suppliershub.marketplace_supplier.v1.supplier_updated` | Triggered when a supplier is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-supplier" id="ex-sdk-update-supplier-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-supplier" id="ex-sdk-update-supplier-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-supplier" id="ex-sdk-update-supplier-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-supplier-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-supplier-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateSupplier</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateSupplier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateSupplier (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-supplier-0"><span className="wix-picker-name">updateSupplier</span></label>
    <label htmlFor="ex-sdk-update-supplier-1"><span className="wix-picker-name">updateSupplier (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-supplier-2"><span className="wix-picker-name">updateSupplier (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateSupplier
    import { suppliers } from '@wix/suppliers-hub';

    async function updateSupplier(_id,supplier) {
      const response = await suppliers.updateSupplier(_id,supplier);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateSupplier (self-hosted)
    import { createClient } from '@wix/sdk';
    import { suppliers } from '@wix/suppliers-hub';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { suppliers },
      // Include the auth strategy and host as relevant
    });

    async function updateSupplier(_id,supplier) {
      const response = await myWixClient.suppliers.updateSupplier(_id,supplier);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateSupplier (with elevated permissions)
    import { suppliers } from '@wix/suppliers-hub';
    import { auth } from '@wix/essentials';

    async function myUpdateSupplierMethod(_id,supplier) {
      const elevatedUpdateSupplier = auth.elevate(suppliers.updateSupplier);
      const response = await elevatedUpdateSupplier(_id,supplier);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
