---
title: "onCategoryDeleted"
description: "The onCategoryDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.categories.v1.category_deleted` |
| Package | `@wix/categories` |
| Permissions | `Manage Stores - all permissions`, `Read categories`, `Read v3 catalog`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Manage v3 catalog`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onCategoryDeleted
  import { categories } from '@wix/categories';

  categories.onCategoryDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categories } from '@wix/categories';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        categories,
      },
  });

  wixClient.categories.onCategoryDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
