---
title: "Query Extended Bookings"
description: "Retrieves up to 100 bookings, given the specified paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 bookings, given the specified paging, filtering, and sorting.

    Query Extended Bookings runs with these defaults, which you can override:

    - `id` sorted in `ASC` order
    - `cursorPaging.limit` is `50`

    To retrieve course bookings, filter by `scheduleId` of the relevant
    service.

    You can retrieve information about which actions the customer can perform
    for the bookings. To do so, pass `withBookingAllowedActions` as `true`.

    When using filters for dates, you must use [UTC time](https://en.wikipedia.org/wiki/Coordinated_Universal_Time).

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-CALENDAR-WITH-PARTICIPANTS">Read bookings calendar - including participants</span>
    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>
    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-SENSITIVE">Read Bookings - Including Participants</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_api/bookings-reader/v2/extended-bookings/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2" required>

      Information about filters, paging, and sorting.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. In the first request,
              specify `cursorPaging.limit`. For following requests, specify the
              retrieved `cursorPaging.cursor` token and not `query.filter` or
              `query.sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="withBookingAllowedActions" type="boolean">

      Whether to return information about the actions the customer can perform for the bookings.

    </ParamField>

    <ParamField body="withBookingAttendanceInfo" type="boolean">

      Whether to return information about the attendance for the bookings.

    </ParamField>

    <ParamField body="withBookingConferencingDetails" type="boolean">

      Whether to return information about the online conferencing details for the bookings.

    </ParamField>

    <ParamField body="withBookingPolicySettings" type="boolean">

      Whether to retrieve information about booking policy settings.

      You must have the `BOOKINGS.BOOKING_POLICY_SNAPSHOT_READ` permission scope
      when passing `true`.

      Default: `false`.

    </ParamField>

    <ParamField body="withBookingFeeDetails" type="boolean">

      Whether to retrieve information about [booking fee](https://dev.wix.com/docs/api-reference/business-solutions/bookings/pricing/booking-fees/introduction)
      statuses.

      Information about booking fees with a status of `PREVIEW` isn't returned,
      and only fees that have already been applied are included in the response.
      If there are hypothetical fees for a booking that may be canceled in the
      future, these fees aren't returned. To retrieve such fees, call
      [List Booking Fees] By Booking Ids](https://dev.wix.com/docs/api-reference/business-solutions/bookings/pricing/booking-fees/list-booking-fees-by-booking-ids)
      instead.

      You must have the `BOOKINGS.BOOKING_FEES_READ` permission scope when passing
      `true`.

      Default: `false`

    </ParamField>

    <ParamField body="withEcomOrder" type="boolean">

      Whether to return information about the order from ecom.

      Learn more about [eCommerce orders](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/introduction).

      You must have the `ECOM.READ_ORDERS` permission scope
      when passing `true`.

      Default: `false`.

      **Developer Preview.**

    </ParamField>

    ## Response Object

    <ResponseField name="extendedBookings" type="ExtendedBooking[]">

      Retrieved bookings.

      May include information about the attendance or actions the customer can
      perform, depending on your request.

      See the [Extended Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-reader-v2/extended-booking-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-query-extended-bookings" id="ex-rest-query-extended-bookings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-query-extended-bookings" id="ex-rest-query-extended-bookings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-query-extended-bookings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-query-extended-bookings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Retrieve a specific extended booking including its policy settings and booking fee details</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query extended bookings by session ID</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-query-extended-bookings-0"><span className="wix-picker-name">Retrieve a specific extended booking including its policy settings and booking fee details</span></label>
    <label htmlFor="ex-rest-query-extended-bookings-1"><span className="wix-picker-name">Query extended bookings by session ID</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/bookings-reader/v2/extended-bookings/query' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "query": {
          "filter": {
            "id": "f1e4390a-0c1b-46b5-b98a-780a04bbfc7a"
          }
        },
        "withBookingPolicySettings": true,
        "withBookingFeeDetails": true
      }'
      ```

      ```json Response
      {
        "extendedBookings": [
          {
            "booking": {
              "id": "f1e4390a-0c1b-46b5-b98a-780a04bbfc7a",
              "bookedEntity": {
                "slot": {
                  "sessionId": "2mmoW0vwKcSFyxtOfCdMqSSTmKDgpUj7GScjbiUmluC70c9ZlfokCWY6u946Ta6fM49mOfVRF47YzyQXzPS85NrotUsAzcFdcjgz",
                  "serviceId": "a512b177-d1cf-4043-a7bd-7f7951e1ba60",
                  "scheduleId": "a38d1093-d608-4476-85bc-2fc28d741673",
                  "eventId": "e0d90b0c-49cd-47b1-bd9f-e0e0c0e1ecdd",
                  "startDate": "2024-07-22T11:00:00.000-05:00",
                  "endDate": "2024-07-22T12:00:00.000-05:00",
                  "timezone": "America/Chicago",
                  "resource": {
                    "id": "64a512e8-4821-41d4-b4f5-d7e8f0f63182",
                    "name": "Dominik Nowakowski",
                    "scheduleId": "54e4a23b-a6ca-49e9-b509-8f09096e229e"
                  },
                  "location": {
                    "id": "bbcc1dfb-beea-44dd-bd30-660edc1d7abc",
                    "name": "Location 1",
                    "locationType": "OWNER_BUSINESS"
                  }
                },
                "title": "101 test",
                "tags": [
                  "INDIVIDUAL"
                ]
              },
              "contactDetails": {
                "contactId": "989ff3e9-5ad7-4291-a165-e4690b2ffc32",
                "firstName": "Kate",
                "lastName": "Abrego Matias",
                "email": "Kate-AbregoMatias12@example.com",
                "phone": "0693119567"
              },
              "additionalFields": [],
              "status": "CANCELED",
              "paymentStatus": "NOT_PAID",
              "selectedPaymentOption": "OFFLINE",
              "createdDate": "2024-07-21T13:55:42.460Z",
              "revision": "6",
              "createdBy": {
                "wixUserId": "11111111-1111-1111-1111-111111111111"
              },
              "startDate": "2024-07-22T16:00:00Z",
              "endDate": "2024-07-22T17:00:00Z",
              "updatedDate": "2024-07-21T13:56:28.789Z",
              "totalParticipants": 1
            },
            "bookingPolicySettings": {
              "cancellationFeeEnabled": true
            },
            "bookingFeeDetails": {
              "cancellationFeeStatus": "NOT_YET_APPLIED_TO_ORDER"
            }
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {}
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/bookings-reader/v2/extended-bookings/query' \
      -H 'Authorization: <AUTH>' \
      -d '{
         "query": {
           "filter": {
             "bookedEntity.item.slot.sessionId" : ["2mmoW0vwKcSFyxtOfCdKZoL2PDsyWG3fw2loVjs4xCdGebAdETRJyFCbPMBvCs5hidrevMmkVChbcMGNF9RsMDpPSUy0Ew4Kaohh"]
           },
           "sort": []
         },
         "withBookingAllowedActions": false,
         "withBookingAttendanceInfo": false,
         "withBookingConferencingDetails": false,
         "withBookingPolicySettings": false,
         "withBookingFeeDetails": false
      }'
      ```

      ```json Response
      {
      	"extendedBookings": [ {
      	  "booking": {
      			"id": "42914cf7-4041-48a0-b0e7-d00f2811f968",
      			"bookedEntity": {
      				"slot": {
      					"sessionId": "2mmoW0vwKcSFyxtOfCdKZoL2PDsyWG3fw2loVjs4xCdGebAdETRJyFCbPMBvCs5hidrevMmkVChbcMGNF9RsMDpPSUy0Ew4Kaohh",
                          "eventId": "65a5776e-37f1-45f7-8e4c-f22d01b23254",
      					"serviceId": "6e7f01de-a6d5-4a1c-8b25-87cf7c3cbe81",
      					"scheduleId": "a34331f7-474e-4433-b3f9-13637d114dc4",
      					"startDate": "2021-07-05T13:15:00.000+01:00",
      					"endDate": "2021-07-05T15:15:00.000+01:00",
      					"timezone": "Europe/Dublin",
      					"resource": {
      						"id": "e13a90ac-c4b1-4fbd-969f-7c8fc0310363",
      						"name": "Jane Brown",
      						"email": "Jane@example.com",
      						"scheduleId": "163507aa-6b75-4725-bac3-a61d6a8d2df2"
      						},
      						"location": {
      							"id": "efaf91d1-72b1-4036-a715-8d848ec1857e",
      							"name": "HOME",
      							"locationType": "OWNER_BUSINESS"
      						}
      					},
      					"title": "Example Service",
      					"tags": [
      						"INDIVIDUAL"
      					]
      				},
      				"contactDetails": {
      					"contactId": "5160e721-cfe9-4ad4-986e-76e52f4ab682",
      					"firstName": "Lucien Lamalo",
      					"email": "LucienL@example.com",
      					"timeZone": "Europe/Dublin",
      					"countryCode": "US"
      				},
      				"additionalFields": [
      					{
      						"id": "06d592d3-05ef-436f-8dcb-ba806c8984fd",
      						"label": "Example Message",
      						"valueType": "LONG_TEXT"
      					},
      					{
      						"id": "3c79b439-5300-4a08-add8-8bb810de7b69",
      						"value": "true",
      						"label": "Example Checkbox",
      						"valueType": "CHECK_BOX"
      					}
      				],
      				"totalParticipants": 1,
      				"status": "CONFIRMED",
      				"paymentStatus": "PAID",
      				"selectedPaymentOption": "ONLINE",
      				"createdDate": "2021-06-27T14:18:59.108Z",
      				"revision": "3",
      				"startDate": "2021-07-05T12:15:00Z",
      				"endDate": "2021-07-05T14:15:00Z",
      				"updatedDate": "2021-06-27T14:19:01.023Z"
      			}
      		}
      	],
      	"pagingMetadata": {
      		"count": 1,
      		"total": 1,
      		"cursors": {}
      	}
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves up to 100 bookings, given the specified paging, filtering, and sorting.

    Query Extended Bookings runs with these defaults, which you can override:

    - `id` sorted in `ASC` order
    - `cursorPaging.limit` is `50`

    To retrieve course bookings, filter by `scheduleId` of the relevant
    service.

    You can retrieve information about which actions the customer can perform
    for the bookings. To do so, pass `withBookingAllowedActions` as `true`.

    When using filters for dates, you must use [UTC time](https://en.wikipedia.org/wiki/Coordinated_Universal_Time).

    To learn about working with _Query_ endpoints, see
    [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-CALENDAR-WITH-PARTICIPANTS">Read bookings calendar - including participants</span>
    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>
    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-SENSITIVE">Read Bookings - Including Participants</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { extendedBookings } from '@wix/bookings';

    extendedBookings.queryExtendedBookings(query, options): Promise<QueryExtendedBookingsResponse>
    ```

    ## Method Parameters

    <ParamField body="query" type="ExtendedBookingQuery" required>

      Information about filters, paging, and sorting.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. In the first request,
              specify `cursorPaging.limit`. For following requests, specify the
              retrieved `cursorPaging.cursor` token and not `query.filter` or
              `query.sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QueryExtendedBookingsOptions">

      Additional options for performing the query.

      <Expandable title="options properties">

        <ParamField body="withBookingAllowedActions" type="boolean">

          Whether to return information about the actions the customer can perform for the bookings.

        </ParamField>

        <ParamField body="withBookingAttendanceInfo" type="boolean">

          Whether to return information about the attendance for the bookings.

        </ParamField>

        <ParamField body="withBookingConferencingDetails" type="boolean">

          Whether to return information about the online conferencing details for the bookings.

        </ParamField>

        <ParamField body="withBookingPolicySettings" type="boolean">

          Whether to retrieve information about booking policy settings.

          You must have the `BOOKINGS.BOOKING_POLICY_SNAPSHOT_READ` permission scope
          when passing `true`.

          Default: `false`.

        </ParamField>

        <ParamField body="withBookingFeeDetails" type="boolean">

          Whether to retrieve information about [booking fee](https://dev.wix.com/docs/api-reference/business-solutions/bookings/pricing/booking-fees/introduction)
          statuses.

          Information about booking fees with a status of `PREVIEW` isn't returned,
          and only fees that have already been applied are included in the response.
          If there are hypothetical fees for a booking that may be canceled in the
          future, these fees aren't returned. To retrieve such fees, call
          [List Booking Fees] By Booking Ids](https://dev.wix.com/docs/api-reference/business-solutions/bookings/pricing/booking-fees/list-booking-fees-by-booking-ids)
          instead.

          You must have the `BOOKINGS.BOOKING_FEES_READ` permission scope when passing
          `true`.

          Default: `false`

        </ParamField>

        <ParamField body="withEcomOrder" type="boolean">

          Whether to return information about the order from ecom.

          Learn more about [eCommerce orders](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/orders/introduction).

          You must have the `ECOM.READ_ORDERS` permission scope
          when passing `true`.

          Default: `false`.

          **Developer Preview.**

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<QueryExtendedBookingsResponse>`.

    <ResponseField name="extendedBookings" type="ExtendedBooking[]">

      Retrieved bookings.

      May include information about the attendance or actions the customer can
      perform, depending on your request.

      See the [Extended Booking Object](/api-reference/business-solutions/bookings/bookings/bookings-reader-v2/extended-booking-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-extended-bookings" id="ex-sdk-query-extended-bookings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-extended-bookings" id="ex-sdk-query-extended-bookings-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-extended-bookings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-extended-bookings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query canceled bookings of a schedule</span><span className="wix-picker-desc">Retrieves canceled bookings of the specified schedule starting on or after June 1, sorted by start date in descending order, including the actions the customer can perform for each booking</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryExtendedBookings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-extended-bookings-0"><span className="wix-picker-name">Query canceled bookings of a schedule</span><span className="wix-picker-desc">Retrieves canceled bookings of the specified schedule starting on or after June 1, sorted by start date in descending order, including the actions the customer can perform for each booking</span></label>
    <label htmlFor="ex-sdk-query-extended-bookings-1"><span className="wix-picker-name">queryExtendedBookings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query canceled bookings of a schedule
    import { extendedBookings } from "@wix/bookings";

    async function queryExtendedBookings() {
      const response = await extendedBookings.queryExtendedBookings(
        {
          filter: {
            "bookedEntity.item.slot.scheduleId": "3752ffe8-2d58-4541-91ec-cd981c4ccb28",
            status: "CANCELED",
            startDate: { $gte: "2026-06-01T00:00:00.000Z" },
          },
          sort: [{ fieldName: "startDate", order: "DESC" }],
          cursorPaging: { limit: 10 },
        },
        { withBookingAllowedActions: true },
      );

      const bookings = response.extendedBookings ?? [];
      const nextCursor = response.pagingMetadata?.cursors?.next;
      console.log(`Retrieved ${bookings.length} canceled bookings`);
      console.log("First booking status:", bookings[0]?.booking?.status);
      console.log("Cursor for the next page:", nextCursor);
    }

    /* Promise resolves to:
     * {
     *   "extendedBookings": [
     *     {
     *       "booking": {
     *         "bookedEntity": {
     *           "slot": {
     *             "serviceId": "0ebaeea4-a710-420c-9a88-9135e1a3c071",
     *             "scheduleId": "3752ffe8-2d58-4541-91ec-cd981c4ccb28",
     *             "startDate": "2026-06-16T14:30:00.000-04:00",
     *             "endDate": "2026-06-16T15:00:00.000-04:00",
     *             "timezone": "America/New_York",
     *             "resource": {
     *               "name": "Business Owner",
     *               "email": "owner@example.com",
     *               "scheduleId": "8a69a784-9b19-4633-9661-97e8b4e92c38",
     *               "type": "1cd44cf8-756f-41c3-bd90-3e2ffcaf1155",
     *               "_id": "804d8f27-c4ed-4fdb-acc9-a9fc3a8deda3"
     *             },
     *             "location": {
     *               "locationType": "OWNER_BUSINESS"
     *             },
     *             "resourceSelections": [
     *               {
     *                 "resourceTypeId": "1cd44cf8-756f-41c3-bd90-3e2ffcaf1155",
     *                 "selectionMethod": "SPECIFIC_RESOURCE"
     *               }
     *             ],
     *             "allDay": false
     *           },
     *           "title": "30-Min Follow-Up",
     *           "tags": [
     *             "INDIVIDUAL"
     *           ]
     *         },
     *         "contactDetails": {
     *           "firstName": "Alex",
     *           "lastName": "Smith",
     *           "email": "alex.smith@example.com"
     *         },
     *         "status": "CANCELED",
     *         "revision": "2",
     *         "createdBy": {
     *           "identityType": "APP",
     *           "appId": "a0a70193-1a7e-4d7a-98e1-a73b1a8ed633"
     *         },
     *         "startDate": "2026-06-16T18:30:00.000Z",
     *         "endDate": "2026-06-16T19:00:00.000Z",
     *         "totalParticipants": 1,
     *         "appId": "13d21c63-b5ec-5912-8397-c3a5ddb27a97",
     *         "_id": "6181d50b-bd87-4177-8ecc-4e83537c16f2",
     *         "_createdDate": "2026-06-15T08:21:21.151Z",
     *         "_updatedDate": "2026-06-15T08:21:28.314Z"
     *       },
     *       "allowedActions": {
     *         "cancel": false,
     *         "reschedule": false,
     *         "bookAnother": true
     *       }
     *     },
     *     {
     *       "booking": {
     *         "bookedEntity": {
     *           "slot": {
     *             "serviceId": "0ebaeea4-a710-420c-9a88-9135e1a3c071",
     *             "scheduleId": "3752ffe8-2d58-4541-91ec-cd981c4ccb28",
     *             "startDate": "2026-06-16T14:30:00.000-04:00",
     *             "endDate": "2026-06-16T15:00:00.000-04:00",
     *             "timezone": "America/New_York",
     *             "resource": {
     *               "name": "Business Owner",
     *               "email": "owner@example.com",
     *               "scheduleId": "8a69a784-9b19-4633-9661-97e8b4e92c38",
     *               "type": "1cd44cf8-756f-41c3-bd90-3e2ffcaf1155",
     *               "_id": "804d8f27-c4ed-4fdb-acc9-a9fc3a8deda3"
     *             },
     *             "location": {
     *               "locationType": "OWNER_BUSINESS"
     *             },
     *             "resourceSelections": [
     *               {
     *                 "resourceTypeId": "1cd44cf8-756f-41c3-bd90-3e2ffcaf1155",
     *                 "selectionMethod": "SPECIFIC_RESOURCE"
     *               }
     *             ],
     *             "allDay": false
     *           },
     *           "title": "30-Min Follow-Up",
     *           "tags": [
     *             "INDIVIDUAL"
     *           ]
     *         },
     *         "contactDetails": {
     *           "firstName": "Isabella",
     *           "lastName": "Garcia",
     *           "email": "isabella.garcia@example.com"
     *         },
     *         "status": "CANCELED",
     *         "revision": "3",
     *         "createdBy": {
     *           "identityType": "APP",
     *           "appId": "a0a70193-1a7e-4d7a-98e1-a73b1a8ed633"
     *         },
     *         "startDate": "2026-06-16T18:30:00.000Z",
     *         "endDate": "2026-06-16T19:00:00.000Z",
     *         "totalParticipants": 1,
     *         "appId": "13d21c63-b5ec-5912-8397-c3a5ddb27a97",
     *         "_id": "2ecdbc80-4058-4c75-b784-de414ee9d954",
     *         "_createdDate": "2026-06-15T08:21:07.797Z",
     *         "_updatedDate": "2026-06-15T08:21:25.075Z"
     *       },
     *       "allowedActions": {
     *         "cancel": false,
     *         "reschedule": false,
     *         "bookAnother": true
     *       }
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 10,
     *     "cursors": {
     *       "next": "bb6c9b02c1b6e74e546bd8e2c318d713..."
     *     },
     *     "hasNext": true
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryExtendedBookings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { extendedBookings } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { extendedBookings },
      // Include the auth strategy and host as relevant
    });

    async function queryExtendedBookings(query,options) {
      const response = await myWixClient.extendedBookings.queryExtendedBookings(query,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
