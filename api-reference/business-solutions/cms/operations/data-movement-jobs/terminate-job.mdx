---
title: "Terminate Job"
description: "Terminates the specified job."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Terminates the specified job.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE_MOVEMENT_JOBS">Data Mover Manage Jobs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data-mover/v1/jobs/{jobId}/terminate
    ```

    ## Path Params

    <ParamField path="jobId" type="string" required>

      ID of the job to terminate.

    </ParamField>

    ## Response Object

    <ResponseField name="job" type="DataMovementJob">

      Details of the terminated job.

      See the [Job Object](/api-reference/business-solutions/cms/operations/data-movement-jobs/job-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/data-mover/v1/jobs/8646fc6e-58f3-4d78-b3ab-6a63a5a46400/terminate' \
          -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "job": {
          "id": "8646fc6e-58f3-4d78-b3ab-6a63a5a46400",
          "name": "Wix Data Collection Export",
          "startedAt": "2024-03-15T14:30:29.162Z",
          "status": "FAILED",
          "source": {
            "wixDataCollection": {
              "collectionId": "TodoList"
            }
          },
          "destination": {
            "file": {
              "format": "CSV",
              "fileName": "TodoList.csv",
              "url": "http://example.com/files/TodoList.csv"
            }
          },
          "finishedAt": "2024-03-15T14:30:33.273Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Terminates the specified job.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.MANAGE_MOVEMENT_JOBS">Data Mover Manage Jobs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { movementJobs } from '@wix/data';

    movementJobs.terminateJob(jobId): Promise<TerminateJobResponse>
    ```

    ## Method Parameters

    <ParamField body="jobId" type="string" required>

      ID of the job to terminate.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<TerminateJobResponse>`.

    <ResponseField name="job" type="DataMovementJob">

      Details of the terminated job.

      See the [Job Object](/api-reference/business-solutions/cms/operations/data-movement-jobs/job-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-terminate-job" id="ex-sdk-terminate-job-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-terminate-job" id="ex-sdk-terminate-job-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-terminate-job" id="ex-sdk-terminate-job-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-terminate-job-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-terminate-job-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">terminateJob</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">terminateJob (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">terminateJob (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-terminate-job-0"><span className="wix-picker-name">terminateJob</span></label>
    <label htmlFor="ex-sdk-terminate-job-1"><span className="wix-picker-name">terminateJob (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-terminate-job-2"><span className="wix-picker-name">terminateJob (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript terminateJob
    import { movementJobs } from '@wix/data';

    async function terminateJob(jobId) {
      const response = await movementJobs.terminateJob(jobId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript terminateJob (self-hosted)
    import { createClient } from '@wix/sdk';
    import { movementJobs } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { movementJobs },
      // Include the auth strategy and host as relevant
    });

    async function terminateJob(jobId) {
      const response = await myWixClient.movementJobs.terminateJob(jobId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript terminateJob (with elevated permissions)
    import { movementJobs } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myTerminateJobMethod(jobId) {
      const elevatedTerminateJob = auth.elevate(movementJobs.terminateJob);
      const response = await elevatedTerminateJob(jobId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
