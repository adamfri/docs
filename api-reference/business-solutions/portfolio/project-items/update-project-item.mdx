---
title: "Update Project Item"
description: "Updates a project item."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a project item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/portfolio/v1/items/{item.id}
    ```

    ## Path Params

    <ParamField path="item.id" type="string" required>

      Project item ID.

    </ParamField>

    ## Body Params

    <ParamField body="item" type="Item" required>

      The project item to update.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="item" type="Item">

      The updated project item.

      See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Updated | `wix.portfolio.project_items.v1.project_item_updated` | Triggered when a project item is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/portfolio/v1/Items/456e7890-e89b-12d3-a456-426614174001' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "item": {
          "title": "Updated Item Title",
          "description": "This is an updated item description.",
          "sortOrder": 1.0,
          "image": {
            "imageInfo": {
              "url": "https://example.com/new-image1.jpg",
              "id": "9a7eef_afc3d989928f45198a16175e3c7aba3d"
            }
          },
          "link": {
            "text": "Visit Updated Site",
            "url": "https://example.com/update",
            "target": "_blank"
          }
        }
      }'
      ```

      ```json Response
      {
        "item": {
          "id": "456e7890-e89b-12d3-a456-426614174001",
          "projectId": "123e4567-e89b-12d3-a456-426614174000",
          "title": "Updated Item Title",
          "description": "This is an updated item description.",
          "sortOrder": 1.0,
          "type": "IMAGE",
          "image": {
            "imageInfo": {
              "url": "https://example.com/new-image1.jpg",
              "id": "9a7eef_afc3d989928f45198a16175e3c7aba3d"
            }
          },
          "createdDate": "2024-07-31T12:00:00Z",
          "updatedDate": "2024-09-31T12:30:00Z",
          "link": {
            "text": "Visit Updated Site",
            "url": "https://example.com/update",
            "target": "_blank"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a project item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { projectItems } from '@wix/portfolio';

    projectItems.updateProjectItem(_id, item): Promise<Item>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Project item ID.

      **Read-only.**

    </ParamField>

    <ParamField body="item" type="UpdateProjectItem" required>

      The project item to update.

      <Expandable title="item properties">

        <ParamField body="projectId" type="string">

          Project ID.

        </ParamField>

        <ParamField body="_id" type="string">

          Project item ID.

          **Read-only.**

        </ParamField>

        <ParamField body="sortOrder" type="number">

          Index that determines which position a project is displayed in the project.  &lt;br />

          Default: [Epoch](https://www.epoch101.com/) timestamp. &lt;br />

        </ParamField>

        <ParamField body="title" type="string">

          Project item title.

        </ParamField>

        <ParamField body="description" type="string">

          Project item description.

        </ParamField>

        <ParamField body="type" type="string">

          Project item data type.

          **Read-only.**

          **Supported values:**

          - `UNDEFINED` — Undefined item type.
          - `IMAGE` — Image item type.
          - `VIDEO` — Video item type.

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the project item was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the project item was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="link" type="Link">

          Project item link.

          <Expandable title="link properties">

            <ParamField body="text" type="string">

              Display text of the link.

            </ParamField>

            <ParamField body="url" type="string">

              Target URL of the link.

            </ParamField>

            <ParamField body="target" type="string">

              Whether the link opens in a new tab or window. One of:
              * `'_blank'`: The link opens in a new tab or window.
              * `'_self'`: The link opens in the same tab or window.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="metadata" type="metadata">

          metadata

          <Expandable title="metadata properties">

            <ParamField body="image" type="Image">

              Information about the Wix Media image.

              _Nested structure omitted for brevity — see the `Image` object._

            </ParamField>

            <ParamField body="video" type="Video">

              Information about the Wix Media video.

              _Nested structure omitted for brevity — see the `Video` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Item>`.

    See the [Project Item Object](/api-reference/business-solutions/portfolio/project-items/project-item-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Updated | `wix.portfolio.project_items.v1.project_item_updated` | Triggered when a project item is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-project-item" id="ex-sdk-update-project-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-project-item" id="ex-sdk-update-project-item-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-project-item" id="ex-sdk-update-project-item-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-project-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-project-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateProjectItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateProjectItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateProjectItem (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-project-item-0"><span className="wix-picker-name">updateProjectItem</span></label>
    <label htmlFor="ex-sdk-update-project-item-1"><span className="wix-picker-name">updateProjectItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-project-item-2"><span className="wix-picker-name">updateProjectItem (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateProjectItem
    import { projectItems } from '@wix/portfolio';

    async function updateProjectItem(_id,item) {
      const response = await projectItems.updateProjectItem(_id,item);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateProjectItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { projectItems } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { projectItems },
      // Include the auth strategy and host as relevant
    });

    async function updateProjectItem(_id,item) {
      const response = await myWixClient.projectItems.updateProjectItem(_id,item);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateProjectItem (with elevated permissions)
    import { projectItems } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myUpdateProjectItemMethod(_id,item) {
      const elevatedUpdateProjectItem = auth.elevate(projectItems.updateProjectItem);
      const response = await elevatedUpdateProjectItem(_id,item);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
