---
title: "Create Consent Config"
description: "Creates a consent config."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a consent config.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bu/legal/v1/consent-configs
    ```

    ## Body Params

    <ParamField body="consentConfig" type="ConsentConfig" required>

      Consent config to create.

      See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="consentConfig" type="ConsentConfig">

      Created consent config.

      See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Created | `wix.bu.legal.v1.consent_config_created` | Triggered when a consent config is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Creates a consent config with basic info

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/consent/consent-config/v1/consent-configs' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-binary '{
          "consentConfig": {
            "configType": "TAG_OVERRIDE",
            "consentCategory": "essential",
            "tagDetails": {
                "appId": "73bc078f-b2d7-4ee7-a6e6-41b28ec661d9",
                "componentId": "c7afb240-4cbb-4bbb-adeb-936818698cc9",
                "version": "^1.0.0"
            }
          }
        }'
      ```

      ```json Response
      {
        "consentConfig": {
          "id": "334afbf6-5c8b-4b43-9e9e-dd28664ca28f",
          "revision": "1",
          "createdDate": "2025-03-04T07:58:24.131Z",
          "updatedDate": "2025-03-04T07:58:24.131Z",
          "configType": "TAG_OVERRIDE",
          "consentCategory": "essential",
          "tagDetails": {
              "appId": "73bc078f-b2d7-4ee7-a6e6-41b28ec661d9",
              "componentId": "c7afb240-4cbb-4bbb-adeb-936818698cc9",
              "version": "^1.0.0"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a consent config.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CONSENT_CONFIG.MANAGE">Consent config</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { consentConfig } from '@wix/consent-policy';

    consentConfig.createConsentConfig(consentConfig): Promise
    ```

    ## Method Parameters

    <ParamField body="consentConfig" type="ConsentConfig" required>

      Consent config to create.

      See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `ConsentConfig`.

    See the [Consent Config Object](/api-reference/business-management/cookie-consent-policy/consent-configs/consent-config-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Consent Config Created | `wix.bu.legal.v1.consent_config_created` | Triggered when a consent config is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-consent-config" id="ex-sdk-create-consent-config-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-consent-config" id="ex-sdk-create-consent-config-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-consent-config" id="ex-sdk-create-consent-config-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-consent-config-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-consent-config-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createConsentConfig</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createConsentConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createConsentConfig (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-consent-config-0"><span className="wix-picker-name">createConsentConfig</span></label>
    <label htmlFor="ex-sdk-create-consent-config-1"><span className="wix-picker-name">createConsentConfig (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-consent-config-2"><span className="wix-picker-name">createConsentConfig (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createConsentConfig
    import { consentConfig } from '@wix/consent-policy';

    async function createConsentConfig(consentConfig) {
      const response = await consentConfig.createConsentConfig(consentConfig);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createConsentConfig (self-hosted)
    import { createClient } from '@wix/sdk';
    import { consentConfig } from '@wix/consent-policy';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { consentConfig },
      // Include the auth strategy and host as relevant
    });

    async function createConsentConfig(consentConfig) {
      const response = await myWixClient.consentConfig.createConsentConfig(consentConfig);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createConsentConfig (with elevated permissions)
    import { consentConfig } from '@wix/consent-policy';
    import { auth } from '@wix/essentials';

    async function myCreateConsentConfigMethod(consentConfig) {
      const elevatedCreateConsentConfig = auth.elevate(consentConfig.createConsentConfig);
      const response = await elevatedCreateConsentConfig(consentConfig);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
