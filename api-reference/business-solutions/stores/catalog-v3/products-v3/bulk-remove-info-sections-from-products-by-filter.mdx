---
title: "Bulk Remove Info Sections From Products By Filter"
description: "Removes info sections from multiple products, given the provided filter and search expression."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes [info sections](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/info-sections-v3/introduction) from multiple products, given the provided filter and search expression.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.PRODUCT_WRITE">Product write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/bulk/products/remove-info-sections-by-filter
    ```

    ## Body Params

    <ParamField body="filter" type="object" required>

      Filter object.

    </ParamField>

    <ParamField body="infoSectionIds" type="string[]" required>

      IDs of info sections to remove.

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    <ParamField body="search" type="SearchDetails">

      Free text to match in searchable fields.

      <Expandable title="search properties">

        <ParamField body="mode" type="string">

          Defines how separate search terms in `expression` are combined

          **Supported values:**

          - `OR` — Any of the search terms must be present
          - `AND` — All search terms must be present

        </ParamField>

        <ParamField body="expression" type="string">

          Search term or expression

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

        <ParamField body="fuzzy" type="boolean">

          Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID.

      Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/introduction) to retrieve job details and metadata..

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Product Updated | `wix.stores.catalog.v3.product_updated` | Triggered when a product is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/stores/v3/bulk/products/remove-info-sections-by-filter' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
              "filter": {
                  "createdDate": {
                      "$gt": "2024-07-22T13:22:45"
                  }
              },
              "infoSectionIds": [
                  "b9b39003-0edd-41c0-9403-a0fe19cc5bb4",
                  "1dac2b73-3592-4d80-9a6e-4892caf4e058"
              ]
          }'
      ```

      ```json Response
      {
          "jobId": "3ab904a4-d0d5-4e9f-8f48-d9f25bd7178d"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes [info sections](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/info-sections-v3/introduction) from multiple products, given the provided filter and search expression.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.PRODUCT_WRITE">Product write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { productsV3 } from '@wix/stores';

    productsV3.bulkRemoveInfoSectionsFromProductsByFilter(filter, options): Promise<V3BulkRemoveInfoSectionsFromProductsByFilterResponse>
    ```

    ## Method Parameters

    <ParamField body="filter" type="object" required>

      Filter object.

    </ParamField>

    <ParamField body="options" type="BulkRemoveInfoSectionsFromProductsByFilterOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="infoSectionIds" type="string[]" required>

          IDs of info sections to remove.

          Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined

              **Supported values:**

              - `OR` — Any of the search terms must be present
              - `AND` — All search terms must be present

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<V3BulkRemoveInfoSectionsFromProductsByFilterResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID.

      Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/introduction) to retrieve job details and metadata..

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Product Updated | `wix.stores.catalog.v3.product_updated` | Triggered when a product is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-remove-info-sections-from-products-by-filter" id="ex-sdk-bulk-remove-info-sections-from-products-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-remove-info-sections-from-products-by-filter" id="ex-sdk-bulk-remove-info-sections-from-products-by-filter-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-remove-info-sections-from-products-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-remove-info-sections-from-products-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk remove info sections from products by filter</span><span className="wix-picker-desc">Remove info sections from all products created after a specific date</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkRemoveInfoSectionsFromProductsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-remove-info-sections-from-products-by-filter-0"><span className="wix-picker-name">Bulk remove info sections from products by filter</span><span className="wix-picker-desc">Remove info sections from all products created after a specific date</span></label>
    <label htmlFor="ex-sdk-bulk-remove-info-sections-from-products-by-filter-1"><span className="wix-picker-name">bulkRemoveInfoSectionsFromProductsByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk remove info sections from products by filter
    import { productsV3 } from "@wix/stores";

    const filter = {
      createdDate: {
        $gt: "2024-07-22T13:22:45"
      }
    };

    const options = {
      infoSectionIds: [
        "b9b39003-0edd-41c0-9403-a0fe19cc5bb4",
        "1dac2b73-3592-4d80-9a6e-4892caf4e058"
      ]
    };

    async function bulkRemoveInfoSectionsFromProductsByFilter() {
      const response = await productsV3.bulkRemoveInfoSectionsFromProductsByFilter(filter, options);
    }

    /* Promise resolves to:
     * {
     *   "jobId": "3ab904a4-d0d5-4e9f-8f48-d9f25bd7178d"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkRemoveInfoSectionsFromProductsByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { productsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { productsV3 },
      // Include the auth strategy and host as relevant
    });

    async function bulkRemoveInfoSectionsFromProductsByFilter(filter,options) {
      const response = await myWixClient.productsV3.bulkRemoveInfoSectionsFromProductsByFilter(filter,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
