---
title: "Get Sender Email"
description: "Retrieves a sender email by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a sender email by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/sender-emails/v1/sender-emails/{senderEmailId}
    ```

    ## Path Params

    <ParamField path="senderEmailId" type="string" required>

      ID of the sender email to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="senderEmail" type="SenderEmail">

      The requested sender email info.

      See the [Sender Email Object](/api-reference/business-management/marketing/emails/sender-emails/sender-email-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl 'https://www.wixapis.com/sender-emails/v1/sender-emails/30057014-8ecc-4a1b-9c53-bcc7dd033bb5' \
      -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "senderEmail": {
          "id": "30057014-8ecc-4a1b-9c53-bcc7dd033bb5",
          "createdDate": "2024-01-01T00:00:00.000Z",
          "updatedDate": "2024-01-01T00:00:00.000Z",
          "emailAddress": "john.doe@example.com",
          "verified": true
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a sender email by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS">Access Verticals by Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { senderEmails } from '@wix/sender-emails';

    senderEmails.getSenderEmail(senderEmailId): Promise<SenderEmail>
    ```

    ## Method Parameters

    <ParamField body="senderEmailId" type="string" required>

      ID of the sender email to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<SenderEmail>`.

    See the [Sender Email Object](/api-reference/business-management/marketing/emails/sender-emails/sender-email-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-sender-email" id="ex-sdk-get-sender-email-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-sender-email" id="ex-sdk-get-sender-email-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-sender-email" id="ex-sdk-get-sender-email-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-sender-email-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-sender-email-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getSenderEmail</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSenderEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getSenderEmail (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-sender-email-0"><span className="wix-picker-name">getSenderEmail</span></label>
    <label htmlFor="ex-sdk-get-sender-email-1"><span className="wix-picker-name">getSenderEmail (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-sender-email-2"><span className="wix-picker-name">getSenderEmail (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getSenderEmail
    import { senderEmails } from '@wix/sender-emails';

    async function getSenderEmail(senderEmailId) {
      const response = await senderEmails.getSenderEmail(senderEmailId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSenderEmail (self-hosted)
    import { createClient } from '@wix/sdk';
    import { senderEmails } from '@wix/sender-emails';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { senderEmails },
      // Include the auth strategy and host as relevant
    });

    async function getSenderEmail(senderEmailId) {
      const response = await myWixClient.senderEmails.getSenderEmail(senderEmailId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getSenderEmail (with elevated permissions)
    import { senderEmails } from '@wix/sender-emails';
    import { auth } from '@wix/essentials';

    async function myGetSenderEmailMethod(senderEmailId) {
      const elevatedGetSenderEmail = auth.elevate(senderEmails.getSenderEmail);
      const response = await elevatedGetSenderEmail(senderEmailId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
