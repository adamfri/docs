---
title: "onDraftDeleted"
description: "Triggered when a draft post is deleted."
icon: "bolt"
---

Triggered when a draft post is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.blog.v3.draft_deleted` |
| Package | `@wix/blog` |
| Permissions | `Manage Blog`, `Read Draft Blog Posts`, `Read Blog` |

## Examples

<CodeGroup>

  ```javascript onDraftDeleted
  import { draftPosts } from '@wix/blog';

  draftPosts.onDraftDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onDraftDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { draftPosts } from '@wix/blog';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        draftPosts,
      },
  });

  wixClient.draftPosts.onDraftDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
