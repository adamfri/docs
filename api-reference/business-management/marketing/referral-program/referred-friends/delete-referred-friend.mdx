---
title: "Delete Referred Friend"
description: "Deletes a referred friend."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a referred friend.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/referral_friends/v1/referred-friends/{referredFriendId}
    ```

    ## Path Params

    <ParamField path="referredFriendId" type="string" required>

      ID of the referred friend to delete.

    </ParamField>

    ## Query Params

    <ParamField query="revision" type="string">

      Revision number, which increments by 1 each time the referred friend is updated.
      To prevent conflicting changes, the current revision must be passed when deleting the referred friend.

    </ParamField>

    ## Response Object

    Returns `DeleteReferredFriendResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Referred Friend Deleted | `wix.loyalty.referral.v1.referred_friend_deleted` | Triggered when a referred friend is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/referral_friends/v1/referred-friends/3857d039-b129-4393-b983-947078b6a007' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a referred friend.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-REFERRALS.MANAGE-REFERRALS">Manage Referrals</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { friends } from '@wix/referral';

    friends.deleteReferredFriend(referredFriendId, options): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="referredFriendId" type="string" required>

      ID of the referred friend to delete.

      Constraints: `format GUID`.

    </ParamField>

    <ParamField body="options" type="DeleteReferredFriendOptions">

      options

      <Expandable title="options properties">

        <ParamField body="revision" type="string">

          Revision number, which increments by 1 each time the referred friend is updated.
          To prevent conflicting changes, the current revision must be passed when deleting the referred friend.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Referred Friend Deleted | `wix.loyalty.referral.v1.referred_friend_deleted` | Triggered when a referred friend is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-referred-friend" id="ex-sdk-delete-referred-friend-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-referred-friend" id="ex-sdk-delete-referred-friend-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-referred-friend" id="ex-sdk-delete-referred-friend-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-referred-friend-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-referred-friend-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteReferredFriend</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReferredFriend (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteReferredFriend (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-referred-friend-0"><span className="wix-picker-name">deleteReferredFriend</span></label>
    <label htmlFor="ex-sdk-delete-referred-friend-1"><span className="wix-picker-name">deleteReferredFriend (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-referred-friend-2"><span className="wix-picker-name">deleteReferredFriend (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteReferredFriend
    import { friends } from '@wix/referral';

    async function deleteReferredFriend(referredFriendId,options) {
      const response = await friends.deleteReferredFriend(referredFriendId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReferredFriend (self-hosted)
    import { createClient } from '@wix/sdk';
    import { friends } from '@wix/referral';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { friends },
      // Include the auth strategy and host as relevant
    });

    async function deleteReferredFriend(referredFriendId,options) {
      const response = await myWixClient.friends.deleteReferredFriend(referredFriendId,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteReferredFriend (with elevated permissions)
    import { friends } from '@wix/referral';
    import { auth } from '@wix/essentials';

    async function myDeleteReferredFriendMethod(referredFriendId,options) {
      const elevatedDeleteReferredFriend = auth.elevate(friends.deleteReferredFriend);
      const response = await elevatedDeleteReferredFriend(referredFriendId,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
