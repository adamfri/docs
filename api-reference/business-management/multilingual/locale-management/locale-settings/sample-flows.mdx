---
title: "Sample Flows"
description: "This article present a possible use case you can support using the Locale Settings API."
icon: "file-lines"
---

<Note>This article applies to the REST API only.</Note>

This article present a possible use case you can support using the Locale Settings API. This can be a helpful jumping off point as you plan your implementation.

## Configure locale settings to support a multilingual site

You can configure a site's locale settings to enable content translation and improve SEO performance. 

1. Call [Get Locale Settings](fallback::https://dev.wix.com/docs/api-reference/business-management/multilingual/locale-management/locale-settings/get-locale-settings) to retrieve and check the site's primary locale and its current configuration.

1. Enable multilingual mode so that a site can support multiple locales by calling [Set Multilingual Mode](fallback::https://dev.wix.com/docs/api-reference/business-management/multilingual/locale-management/locale-settings/set-multilingual-mode) and setting `multilingualModeEnabled` to `true`. 

1. Improve the site's SEO performance by calling [Update Locale Settings](fallback::https://dev.wix.com/docs/api-reference/business-management/multilingual/locale-management/locale-settings/update-locale-settings) and setting `urlStructure` to `SUBDIRECTORY`.
