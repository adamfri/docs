---
title: "Delete Tax Exempt Group"
description: "Deletes a tax exempt group."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a tax exempt group.

    Deleting a tax exempt group also removes its exempt override rates from any manual tax mappings. Contacts previously associated with the group through their contact tax details are no longer exempt through it.

    To delete multiple tax exempt groups in a single API call, call [Bulk Delete Tax Exempt Groups](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/bulk-delete-tax-exempt-groups).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/billing/v1/tax-exempt-groups/{taxExemptGroupId}
    ```

    ## Path Params

    <ParamField path="taxExemptGroupId" type="string" required>

      ID of the tax exempt group to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteTaxExemptGroupResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tax Exempt Group Deleted | `wix.billing.v1.tax_exempt_group_deleted` | Triggered when a tax exempt group is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a tax exempt group by ID.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
          'https://www.wixapis.com/billing/v1/tax-exempt-groups/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a tax exempt group.

    Deleting a tax exempt group also removes its exempt override rates from any manual tax mappings. Contacts previously associated with the group through their contact tax details are no longer exempt through it.

    To delete multiple tax exempt groups in a single API call, call [Bulk Delete Tax Exempt Groups](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/bulk-delete-tax-exempt-groups).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { taxExemptGroups } from '@wix/ecom';

    taxExemptGroups.deleteTaxExemptGroup(taxExemptGroupId): Promise
    ```

    ## Method Parameters

    <ParamField body="taxExemptGroupId" type="string" required>

      ID of the tax exempt group to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteTaxExemptGroupResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tax Exempt Group Deleted | `wix.billing.v1.tax_exempt_group_deleted` | Triggered when a tax exempt group is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-tax-exempt-group" id="ex-sdk-delete-tax-exempt-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-tax-exempt-group" id="ex-sdk-delete-tax-exempt-group-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-tax-exempt-group" id="ex-sdk-delete-tax-exempt-group-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-tax-exempt-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-tax-exempt-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteTaxExemptGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteTaxExemptGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteTaxExemptGroup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-tax-exempt-group-0"><span className="wix-picker-name">deleteTaxExemptGroup</span></label>
    <label htmlFor="ex-sdk-delete-tax-exempt-group-1"><span className="wix-picker-name">deleteTaxExemptGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-tax-exempt-group-2"><span className="wix-picker-name">deleteTaxExemptGroup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteTaxExemptGroup
    import { taxExemptGroups } from '@wix/ecom';

    async function deleteTaxExemptGroup(taxExemptGroupId) {
      const response = await taxExemptGroups.deleteTaxExemptGroup(taxExemptGroupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteTaxExemptGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { taxExemptGroups } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { taxExemptGroups },
      // Include the auth strategy and host as relevant
    });

    async function deleteTaxExemptGroup(taxExemptGroupId) {
      const response = await myWixClient.taxExemptGroups.deleteTaxExemptGroup(taxExemptGroupId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteTaxExemptGroup (with elevated permissions)
    import { taxExemptGroups } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myDeleteTaxExemptGroupMethod(taxExemptGroupId) {
      const elevatedDeleteTaxExemptGroup = auth.elevate(taxExemptGroups.deleteTaxExemptGroup);
      const response = await elevatedDeleteTaxExemptGroup(taxExemptGroupId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
