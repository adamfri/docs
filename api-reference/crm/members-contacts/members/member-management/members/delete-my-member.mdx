---
title: "Delete My Member"
description: "Deletes the current member."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the current member.

    After calling this method, the member is logged out of the site.

    All content created by this member is transferred to another account. For example, if a Wix user had blog posts, those posts are transferred to the account specified in `contentAssignedId`, which then becomes the owner of those posts.

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    To delete a member by `ID`, call [Delete Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/delete-member).

    To delete multiple members by `memberId`, call [Bulk Delete Members](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/bulk-delete-members).

    To delete multiple members by a specified filter, call [Bulk Delete Members By Filter](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/bulk-delete-members-by-filter).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/members/v1/members/my
    ```

    ## Query Params

    <ParamField query="contentAssigneeId" type="string">

      ID of a member receiving the deleted member's content.

    </ParamField>

    ## Response Object

    Returns `DeleteMyMemberResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `OWNER_OR_CONTRIBUTOR_MEMBER_DELETE_FORBIDDEN` | Site owner or contributor can't be deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Member Deleted | `wix.members.v1.member_deleted` | Triggered when a member is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        https://www.wixapis.com/members/v1/members/my \
        -H 'Content-Type: application/json;charset=UTF-8' \
        -H 'Authorization: <AUTH>'
        -d '{
           "contentAssigneeId": "ee616475-4b4a-495e-bf75-896f867dc569"
        }
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes the current member.

    After calling this method, the member is logged out of the site.

    All content created by this member is transferred to another account. For example, if a Wix user had blog posts, those posts are transferred to the account specified in `contentAssignedId`, which then becomes the owner of those posts.

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    To delete a member by `ID`, call [Delete Member](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/delete-member).

    To delete multiple members by `memberId`, call [Bulk Delete Members](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/bulk-delete-members).

    To delete multiple members by a specified filter, call [Bulk Delete Members By Filter](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/bulk-delete-members-by-filter).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { members } from '@wix/members';

    members.deleteMyMember(options): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="options" type="DeleteMyMemberOptions">

      options

      <Expandable title="options properties">

        <ParamField body="contentAssigneeId" type="string">

          ID of a member receiving the deleted member's content.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `OWNER_OR_CONTRIBUTOR_MEMBER_DELETE_FORBIDDEN` | Site owner or contributor can't be deleted. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Member Deleted | `wix.members.v1.member_deleted` | Triggered when a member is deleted. |
    | Member Content Reassignment Requested | `wix.members.v1.member_content_reassignment_requested` | Triggered when a member is deleted with selection to reassign his/her content to another member. |
    | Member Content Deletion Requested | `wix.members.v1.member_content_deletion_requested` | Triggered when a member is deleted with selection to delete his/her content. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-my-member" id="ex-sdk-delete-my-member-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-my-member" id="ex-sdk-delete-my-member-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-my-member" id="ex-sdk-delete-my-member-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-my-member-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-my-member-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteMyMember</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMyMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteMyMember (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-my-member-0"><span className="wix-picker-name">deleteMyMember</span></label>
    <label htmlFor="ex-sdk-delete-my-member-1"><span className="wix-picker-name">deleteMyMember (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-my-member-2"><span className="wix-picker-name">deleteMyMember (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteMyMember
    import { members } from '@wix/members';

    async function deleteMyMember(options) {
      const response = await members.deleteMyMember(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMyMember (self-hosted)
    import { createClient } from '@wix/sdk';
    import { members } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { members },
      // Include the auth strategy and host as relevant
    });

    async function deleteMyMember(options) {
      const response = await myWixClient.members.deleteMyMember(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteMyMember (with elevated permissions)
    import { members } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myDeleteMyMemberMethod(options) {
      const elevatedDeleteMyMember = auth.elevate(members.deleteMyMember);
      const response = await elevatedDeleteMyMember(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
