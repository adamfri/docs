---
title: "onFulfillmentMethodDeleted"
description: "Triggered when a fulfillment method is deleted."
icon: "bolt"
---

Triggered when a fulfillment method is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.v1.fulfillment_method_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onFulfillmentMethodDeleted
  import { fulfillmentMethods } from '@wix/restaurants';

  fulfillmentMethods.onFulfillmentMethodDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onFulfillmentMethodDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { fulfillmentMethods } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        fulfillmentMethods,
      },
  });

  wixClient.fulfillmentMethods.onFulfillmentMethodDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
