---
title: "Card Object"
description: "A business entity representing a deal, lead, opportunity, or project that moves through pipeline stages."
icon: "cube"
---

A business entity representing a deal, lead, opportunity, or project that moves through pipeline stages.

Cards contain comprehensive business information including contact details, monetary values,
priorities, due dates, and outcomes. They progress through pipeline stages as work advances
and can be assigned to team members for ownership and responsibility tracking.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      Card ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the card is updated.
      To prevent conflicting changes, the current revision must be passed when updating the card.

      **Read-only.**

    </ResponseField>

    <ResponseField name="createdDate" type="datetime">

      Date and time the card was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="updatedDate" type="datetime">

      Date and time the card was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="pipelineId" type="string">

      ID of the pipeline containing this card.

      **Read-only.**

    </ResponseField>

    <ResponseField name="stageId" type="string">

      ID of the cards' current.

    </ResponseField>

    <ResponseField name="stageUpdatedDate" type="datetime">

      Date and time when the card last moved between stagesin [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="stageUpdateSource" type="CardSource">

      How the card was moved to its current stage.

      **Read-only.**

      <Expandable title="stageUpdateSource properties">

        <ResponseField name="userId" type="string">

          ID of the Wix user who last moved the card.

          **Read-only.**

        </ResponseField>

        <ResponseField name="appId" type="string">

          ID of the app that last moved the card.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="name" type="string">

      The Card name.

    </ResponseField>

    <ResponseField name="description" type="string">

      Card description.

    </ResponseField>

    <ResponseField name="priority" type="string">

      Priority level of the card for task management.

      Default: `P3_MEDIUM`

      **Supported values:**

      - `P1_URGENT` — Urgent priority. Card requires immediate attention.
      - `P2_HIGH` — High priority. Card is important and should be addressed promptly.
      - `P3_MEDIUM` — Medium priority. Card should be completed within regular workflow timelines.
      - `P4_LOW` — Low priority. Card can be addressed when time and resources allow.

    </ResponseField>

    <ResponseField name="assigneeId" type="string">

      ID of the Wix user assigned to manage the card.

    </ResponseField>

    <ResponseField name="dueDate" type="datetime">

      Date and time when the card is expected to moved to the done stage in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
      If the card remains active past this date, it becomes overdue.

    </ResponseField>

    <ResponseField name="staleDate" type="datetime">

      Date and time when the card becomes stale based on the stage's stale duration setting in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
      This date is automatically calculated and updated when the card moves to a new stage.

      **Read-only.**

    </ResponseField>

    <ResponseField name="value" type="Money">

      Monetary value assigned to the card.

      <Expandable title="value properties">

        <ResponseField name="value" type="string">

          Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

          Constraints: `format DECIMAL_VALUE`.

        </ResponseField>

        <ResponseField name="currency" type="string">

          Three-letter currency code in ISO-4217 format.
          Currency is the same as the pipeline currency and is immutable.

          **Read-only.** Constraints: `format CURRENCY`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="outcome" type="string">

      Final outcome of the card when moved to the done stage.

      Default: `NONE`

      **Supported values:**

      - `NONE` — No specific outcome. The card reached the final stage but hasn't been specified as successful or unsuccessful.
      - `WON` — Successful outcome. The card achieved its intended goal.
      - `LOST` — Unsuccessful outcome. The card didn't achieve its intended goal.

    </ResponseField>

    <ResponseField name="mainContact" type="ContactInfo">

      Primary contact associated with the card.

      <Expandable title="mainContact properties">

        <ResponseField name="contactId" type="string">

          ID of the contact associated with the card.

        </ResponseField>

        <ResponseField name="firstName" type="string">

          Contact's first name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="lastName" type="string">

          Contact's last name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="email" type="string">

          Contact's primary email.

          **Read-only.**

        </ResponseField>

        <ResponseField name="phone" type="string">

          Contact's primary phone.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalContacts" type="ContactInfo[]">

      Secondary contacts associated with the card.

      Constraints: `maxItems 10`.

      <Expandable title="additionalContacts properties">

        <ResponseField name="contactId" type="string">

          ID of the contact associated with the card.

        </ResponseField>

        <ResponseField name="firstName" type="string">

          Contact's first name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="lastName" type="string">

          Contact's last name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="email" type="string">

          Contact's primary email.

          **Read-only.**

        </ResponseField>

        <ResponseField name="phone" type="string">

          Contact's primary phone.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="linkedEntities" type="LinkedEntity[]">

      Other entities linked to the card.

      For example, orders, invoices, activities, or other relevant entities.

      Constraints: `maxItems 20`.

      <Expandable title="linkedEntities properties">

        <ResponseField name="entityId" type="string">

          ID of the linked entity.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="referencedEntity" type="string">

          Linked entity type.

          Identifies the type of entity associated with this card such as a contact submitted by a form, or an invoice sent to close a deal.
          You can store any value that helps identify the entity type.
          Use the item selection provider key to make the item selectable by the client.
          For example, an invoice uses "13ee94c1-b635-8505-3391-97919052c16f_InvoiceSelectionProvider".

          Constraints: `minLength 1`, `maxLength 100`.

        </ResponseField>

        <ResponseField name="linkedDate" type="datetime">

          Date and time the entity was linked to the card in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Extended fields data.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="tags" type="PrivateTags">

      Tags for organizing and categorizing the card.

      <Expandable title="tags properties">

        <ResponseField name="privateTags" type="TagList">

          Tags that are exposed to anyone with access to the entity, including site members and visitors.

          <Expandable title="privateTags properties">

            <ResponseField name="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 5`, `maxItems 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "id": "d152a427-7254-4eda-ae38-e476041cc0be",
        "revision": "2",
        "createdDate": "2025-07-23T11:14:43.461Z",
        "updatedDate": "2025-07-23T11:25:17.892Z",
        "stageUpdatedDate": "2025-07-23T11:14:43.461Z",
        "pipelineId": "a1b2c3d4-e5f6-7890-abcd-1234567890ef",
        "stageId": "9700847b-d27a-42dd-82ab-991de143ec68",
        "assigneeId": "u2u2u2u2-3333-4444-5555-666666666666",
        "name": "Enterprise Software Deal 2025 - Qualified Lead",
        "description": "TechCorp Solutions has completed technical demo and security review. Moving to contract negotiation phase. Decision timeline accelerated due to Q4 budget availability. Key stakeholders: Sarah (CTO), Michael (VP Engineering), Jennifer (Procurement).",
        "priority": "P1_URGENT",
        "dueDate": "2025-07-30T17:00:00.000Z",
        "value": {
          "value": "225000.00",
          "currency": "EUR"
        },
        "stageUpdateSource": {
          "userId": "u2u2u2u2-3333-4444-5555-666666666666",
          "appId": "3fd1e173-605c-48b4-a4b4-8f7d65b110b6"
        },
        "outcome": "NONE",
        "mainContact": {
          "contactId": "c1d2e3f4-5678-90ab-cdef-1234567890ab",
          "firstName": "Sarah",
          "lastName": "Johnson",
          "email": "sarah.johnson@techcorp.com",
          "phone": "+18005551234"
        },
        "additionalContacts": [
          {
            "contactId": "c2e3f4g5-6789-01bc-def0-1234567890cd",
            "firstName": "Michael",
            "lastName": "Chen",
            "email": "michael.chen@techcorp.com",
            "phone": "+18005551234"
          },
          {
            "contactId": "c3f4g5h6-7890-12cd-ef01-234567890def",
            "firstName": "Jennifer",
            "lastName": "Davis",
            "email": "jennifer.davis@techcorp.com",
            "phone": "+18005551234"
          },
          {
            "contactId": "c4g5h6i7-8901-23de-f012-34567890ef01",
            "firstName": "Robert",
            "lastName": "Wilson",
            "email": "robert.wilson@techcorp.com",
            "phone": "+18005551234"
          }
        ],
        "linkedEntities": [
          {
            "entityId": "e1a2b3c4-5678-90ab-cdef-1234567890ab",
            "referencedEntity": "55cd9036-36bb-480b-8ddc-afda3cb2eb8d_PriceQuoteSelectionProvider2",
            "linkedDate": "2025-07-23T11:14:43.461Z"
          },
          {
            "entityId": "e2b3c4d5-6789-01bc-def0-1234567890bc",
            "referencedEntity": "cc552162-24a4-45e0-9695-230c4931ef40_inquiryServicesPipeline",
            "linkedDate": "2025-07-23T11:14:43.461Z"
          },
          {
            "entityId": "e3c4d5e6-7890-12cd-ef01-234567890cd",
            "referencedEntity": "13ee94c1-b635-8505-3391-97919052c16f_InvoiceSelectionProvider2",
            "linkedDate": "2025-07-23T11:25:17.892Z"
          }
        ],
        "extendedFields": {
          "leadSource": "Website Demo",
          "companySize": "500-1000 employees",
          "industry": "Technology",
          "decisionMaker": "CTO",
          "lastActivity": "Security Review Completed",
          "nextStep": "Contract Negotiation"
        },
        "tags": {
          "privateTags": {
            "tagIds": ["enterprise", "software", "qualified", "urgent", "q4-budget"]
          }
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      Card ID.

      **Read-only.**

    </ResponseField>

    <ResponseField name="revision" type="integer">

      Revision number, which increments by 1 each time the card is updated.
      To prevent conflicting changes, the current revision must be passed when updating the card.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_createdDate" type="datetime">

      Date and time the card was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="_updatedDate" type="datetime">

      Date and time the card was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="pipelineId" type="string">

      ID of the pipeline containing this card.

      **Read-only.**

    </ResponseField>

    <ResponseField name="stageId" type="string">

      ID of the cards' current.

    </ResponseField>

    <ResponseField name="stageUpdatedDate" type="datetime">

      Date and time when the card last moved between stagesin [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

      **Read-only.**

    </ResponseField>

    <ResponseField name="stageUpdateSource" type="CardSource">

      How the card was moved to its current stage.

      **Read-only.**

      <Expandable title="stageUpdateSource properties">

        <ResponseField name="userId" type="string">

          ID of the Wix user who last moved the card.

          **Read-only.**

        </ResponseField>

        <ResponseField name="appId" type="string">

          ID of the app that last moved the card.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="name" type="string">

      The Card name.

    </ResponseField>

    <ResponseField name="description" type="string">

      Card description.

    </ResponseField>

    <ResponseField name="priority" type="string">

      Priority level of the card for task management.

      Default: `P3_MEDIUM`

      **Supported values:**

      - `P1_URGENT` — Urgent priority. Card requires immediate attention.
      - `P2_HIGH` — High priority. Card is important and should be addressed promptly.
      - `P3_MEDIUM` — Medium priority. Card should be completed within regular workflow timelines.
      - `P4_LOW` — Low priority. Card can be addressed when time and resources allow.

    </ResponseField>

    <ResponseField name="assigneeId" type="string">

      ID of the Wix user assigned to manage the card.

    </ResponseField>

    <ResponseField name="dueDate" type="datetime">

      Date and time when the card is expected to moved to the done stage in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
      If the card remains active past this date, it becomes overdue.

    </ResponseField>

    <ResponseField name="staleDate" type="datetime">

      Date and time when the card becomes stale based on the stage's stale duration setting in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
      This date is automatically calculated and updated when the card moves to a new stage.

      **Read-only.**

    </ResponseField>

    <ResponseField name="value" type="Money">

      Monetary value assigned to the card.

      <Expandable title="value properties">

        <ResponseField name="value" type="string">

          Monetary amount. For example, `"3.99"`, or `"-4.99"` for a negative amount.

          Constraints: `format DECIMAL_VALUE`.

        </ResponseField>

        <ResponseField name="currency" type="string">

          Three-letter currency code in ISO-4217 format.
          Currency is the same as the pipeline currency and is immutable.

          **Read-only.** Constraints: `format CURRENCY`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="outcome" type="string">

      Final outcome of the card when moved to the done stage.

      Default: `NONE`

      **Supported values:**

      - `NONE` — No specific outcome. The card reached the final stage but hasn't been specified as successful or unsuccessful.
      - `WON` — Successful outcome. The card achieved its intended goal.
      - `LOST` — Unsuccessful outcome. The card didn't achieve its intended goal.

    </ResponseField>

    <ResponseField name="mainContact" type="ContactInfo">

      Primary contact associated with the card.

      <Expandable title="mainContact properties">

        <ResponseField name="contactId" type="string">

          ID of the contact associated with the card.

        </ResponseField>

        <ResponseField name="firstName" type="string">

          Contact's first name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="lastName" type="string">

          Contact's last name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="email" type="string">

          Contact's primary email.

          **Read-only.**

        </ResponseField>

        <ResponseField name="phone" type="string">

          Contact's primary phone.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="additionalContacts" type="ContactInfo[]">

      Secondary contacts associated with the card.

      Constraints: `maxItems 10`.

      <Expandable title="additionalContacts properties">

        <ResponseField name="contactId" type="string">

          ID of the contact associated with the card.

        </ResponseField>

        <ResponseField name="firstName" type="string">

          Contact's first name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="lastName" type="string">

          Contact's last name.

          **Read-only.**

        </ResponseField>

        <ResponseField name="email" type="string">

          Contact's primary email.

          **Read-only.**

        </ResponseField>

        <ResponseField name="phone" type="string">

          Contact's primary phone.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="linkedEntities" type="LinkedEntity[]">

      Other entities linked to the card.

      For example, orders, invoices, activities, or other relevant entities.

      Constraints: `maxItems 20`.

      <Expandable title="linkedEntities properties">

        <ResponseField name="entityId" type="string">

          ID of the linked entity.

          Constraints: `format GUID`.

        </ResponseField>

        <ResponseField name="referencedEntity" type="string">

          Linked entity type.

          Identifies the type of entity associated with this card such as a contact submitted by a form, or an invoice sent to close a deal.
          You can store any value that helps identify the entity type.
          Use the item selection provider key to make the item selectable by the client.
          For example, an invoice uses "13ee94c1-b635-8505-3391-97919052c16f_InvoiceSelectionProvider".

          Constraints: `minLength 1`, `maxLength 100`.

        </ResponseField>

        <ResponseField name="linkedDate" type="datetime">

          Date and time the entity was linked to the card in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.

          **Read-only.**

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="extendedFields" type="ExtendedFields">

      Extended fields data.

      <Expandable title="extendedFields properties">

        <ResponseField name="namespaces" type="object">

          Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
          The value of each key is structured according to the schema defined when the extended fields were configured.

          You can only access fields for which you have the appropriate permissions.

          Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

          Constraints: `format map`.

          <Expandable title="namespaces properties">

            <ResponseField name="key" type="string" />

            <ResponseField name="value" type="object" />

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="tags" type="PrivateTags">

      Tags for organizing and categorizing the card.

      <Expandable title="tags properties">

        <ResponseField name="privateTags" type="TagList">

          Tags that are exposed to anyone with access to the entity, including site members and visitors.

          <Expandable title="privateTags properties">

            <ResponseField name="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 5`, `maxItems 100`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
