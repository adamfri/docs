---
title: "onGroupRequestRejected"
description: "Triggered when a group request is rejected."
icon: "bolt"
---

Triggered when a group request is rejected.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_request_rejected` |
| Package | `@wix/groups` |
| Permissions | `Manage Social Groups` |

## Examples

<CodeGroup>

  ```javascript onGroupRequestRejected
  import { groupRequests } from '@wix/groups';

  groupRequests.onGroupRequestRejected((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupRequestRejected (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { groupRequests } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        groupRequests,
      },
  });

  wixClient.groupRequests.onGroupRequestRejected((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
