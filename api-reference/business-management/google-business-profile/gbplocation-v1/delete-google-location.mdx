---
title: "Delete Google Location"
description: "Deletes a Business Profile listing from Google and removes it from the site."
icon: "trash"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a Business Profile listing from Google and removes it from the site.

    This is the usual way to delete a location. To remove a location from the site
    while leaving the Google listing alone, call Delete Location.

    Google refuses to delete some listings through its API, including verified or
    claimed listings, chain-managed locations, and suspended ones. Those have to be
    removed by the site owner at business.google.com.

    Errors:
    - `NOT_FOUND`: the location isn't recorded for this site. Google isn't called.
    - `LOCATION_NOT_DELETABLE_VIA_API`: Google won't allow the delete, for one of
    the reasons above. The location is left in place. Note this is checked before
    the delete, and Google's answer can change in between, so a delete can still
    fail after the check passed.
    - `GOOGLE_API_CALL_FAILED`: the call to Google failed. The location is left in
    place so the call can be retried. If Google keeps refusing, remove the
    listing at business.google.com and then call Delete Location.
    - `LOCATION_DB_PERSIST_FAILED`: Google deleted the listing but the site's copy
    couldn't be removed. Call Delete Location to finish the job.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/gbp/v1/locations/google/{locationId}
    ```

    ## Path Params

    <ParamField path="locationId" type="string" required>

      ID of the location to delete. This is Google's location identifier.

    </ParamField>

    ## Response Object

    Returns `DeleteGoogleLocationResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `GOOGLE_API_CALL_FAILED` |  |
    | `500` | `INTERNAL` | `LOCATION_DB_PERSIST_FAILED` |  |
    | `428` | `FAILED_PRECONDITION` | `LOCATION_NOT_DELETABLE_VIA_API` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/gbp/v1/locations/google/8034569321457853640' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Deletes a Business Profile listing from Google and removes it from the site.

    This is the usual way to delete a location. To remove a location from the site
    while leaving the Google listing alone, call Delete Location.

    Google refuses to delete some listings through its API, including verified or
    claimed listings, chain-managed locations, and suspended ones. Those have to be
    removed by the site owner at business.google.com.

    Errors:
    - `NOT_FOUND`: the location isn't recorded for this site. Google isn't called.
    - `LOCATION_NOT_DELETABLE_VIA_API`: Google won't allow the delete, for one of
    the reasons above. The location is left in place. Note this is checked before
    the delete, and Google's answer can change in between, so a delete can still
    fail after the check passed.
    - `GOOGLE_API_CALL_FAILED`: the call to Google failed. The location is left in
    place so the call can be retried. If Google keeps refusing, remove the
    listing at business.google.com and then call Delete Location.
    - `LOCATION_DB_PERSIST_FAILED`: Google deleted the listing but the site's copy
    couldn't be removed. Call Delete Location to finish the job.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-SEO">Manage SEO Settings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/gbp';

    locations.deleteGoogleLocation(locationId): Promise
    ```

    ## Method Parameters

    <ParamField body="locationId" type="string" required>

      ID of the location to delete. This is Google's location identifier.

      Constraints: `minLength 1`, `maxLength 255`.

    </ParamField>

    ## Returns

    Returns `DeleteGoogleLocationResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `GOOGLE_API_CALL_FAILED` |  |
    | `500` | `INTERNAL` | `LOCATION_DB_PERSIST_FAILED` |  |
    | `428` | `FAILED_PRECONDITION` | `LOCATION_NOT_DELETABLE_VIA_API` |  |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Gbplocation Deleted | `wix.gbp.location.v1.gbplocation_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-google-location" id="ex-sdk-delete-google-location-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-google-location" id="ex-sdk-delete-google-location-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-google-location-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-google-location-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a location on Google and from the site</span><span className="wix-picker-desc">Remove the Google Business Profile listing and the stored location record</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteGoogleLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-google-location-0"><span className="wix-picker-name">Delete a location on Google and from the site</span><span className="wix-picker-desc">Remove the Google Business Profile listing and the stored location record</span></label>
    <label htmlFor="ex-sdk-delete-google-location-1"><span className="wix-picker-name">deleteGoogleLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a location on Google and from the site
    import { locations } from "@wix/gbp";

    async function deleteGoogleLocation() {
      await locations.deleteGoogleLocation("8034569321457853640");

      console.log("Location deleted");
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteGoogleLocation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/gbp';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function deleteGoogleLocation(locationId) {
      const response = await myWixClient.locations.deleteGoogleLocation(locationId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
