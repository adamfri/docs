---
title: "Bulk Remove Products From Categories By Filter"
description: "Removes multiple products, given the provided filter and search expression, from up to 5 categories."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes multiple products, given the provided filter and search expression, from up to 5 categories.

    Learn more about the [Categories API](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/categories/introduction).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_ITEM_WRITE">Category item write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/stores/v3/bulk/products/remove-from-categories-by-filter
    ```

    ## Body Params

    <ParamField body="filter" type="object">

      Filter object.

    </ParamField>

    <ParamField body="categoryIds" type="string[]" required>

      IDs of the categories from which products will be removed.

      Constraints: `format GUID`, `minItems 1`, `maxItems 5`.

    </ParamField>

    <ParamField body="search" type="SearchDetails">

      Free text to match in searchable fields.

      <Expandable title="search properties">

        <ParamField body="mode" type="string">

          Defines how separate search terms in `expression` are combined

          **Supported values:**

          - `OR` — Any of the search terms must be present
          - `AND` — All search terms must be present

        </ParamField>

        <ParamField body="expression" type="string">

          Search term or expression

        </ParamField>

        <ParamField body="fields" type="string[]">

          Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

        <ParamField body="fuzzy" type="boolean">

          Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="jobId" type="string">

      Job ID.

      Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/introduction) to retrieve job details and metadata..

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/stores/v3/bulk/products/remove-from-categories-by-filter' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
              "categoryIds": [
                  "7a083f69-3d3e-4cfd-a3ef-43fe68e967e5"
              ],
              "filter": {
                  "productType": "PHYSICAL"
              },
              "search": {
                  "expression": "Summer"
              }
          }'
      ```

      ```json Response
      {
          "jobId": "98b3f70d-f632-45e6-89a8-b860e0431f0d"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Removes multiple products, given the provided filter and search expression, from up to 5 categories.

    Learn more about the [Categories API](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/categories/introduction).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CATEGORIES.CATEGORY_ITEM_WRITE">Category item write</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { productsV3 } from '@wix/stores';

    productsV3.bulkRemoveProductsFromCategoriesByFilter(options): Promise<BulkRemoveProductsFromCategoriesByFilterResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkRemoveProductsFromCategoriesByFilterOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="filter" type="object">

          Filter object.

        </ParamField>

        <ParamField body="categoryIds" type="string[]" required>

          IDs of the categories from which products will be removed.

          Constraints: `format GUID`, `minItems 1`, `maxItems 5`.

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Defines how separate search terms in `expression` are combined

              **Supported values:**

              - `OR` — Any of the search terms must be present
              - `AND` — All search terms must be present

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `order.address.streetName`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkRemoveProductsFromCategoriesByFilterResponse>`.

    <ResponseField name="jobId" type="string">

      Job ID.

      Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/introduction) to retrieve job details and metadata..

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-remove-products-from-categories-by-filter" id="ex-sdk-bulk-remove-products-from-categories-by-filter-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-remove-products-from-categories-by-filter" id="ex-sdk-bulk-remove-products-from-categories-by-filter-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-remove-products-from-categories-by-filter-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-remove-products-from-categories-by-filter-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Bulk remove products from categories by filter</span><span className="wix-picker-desc">Remove all physical products containing "Summer" in their name from a category</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkRemoveProductsFromCategoriesByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-remove-products-from-categories-by-filter-0"><span className="wix-picker-name">Bulk remove products from categories by filter</span><span className="wix-picker-desc">Remove all physical products containing "Summer" in their name from a category</span></label>
    <label htmlFor="ex-sdk-bulk-remove-products-from-categories-by-filter-1"><span className="wix-picker-name">bulkRemoveProductsFromCategoriesByFilter (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Bulk remove products from categories by filter
    import { productsV3 } from "@wix/stores";

    const options = {
      categoryIds: [
        "7a083f69-3d3e-4cfd-a3ef-43fe68e967e5"
      ],
      filter: {
        productType: "PHYSICAL"
      },
      search: {
        expression: "Summer"
      }
    };

    async function bulkRemoveProductsFromCategoriesByFilter() {
      const response = await productsV3.bulkRemoveProductsFromCategoriesByFilter(options);
    }

    /* Promise resolves to:
     * {
     *   "jobId": "98b3f70d-f632-45e6-89a8-b860e0431f0d"
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkRemoveProductsFromCategoriesByFilter (self-hosted)
    import { createClient } from '@wix/sdk';
    import { productsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { productsV3 },
      // Include the auth strategy and host as relevant
    });

    async function bulkRemoveProductsFromCategoriesByFilter(options) {
      const response = await myWixClient.productsV3.bulkRemoveProductsFromCategoriesByFilter(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
