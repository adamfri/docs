---
title: "onStaffMemberDisconnectedFromUser"
description: "Triggered when a Wix user is disconnected from a staff member."
icon: "bolt"
---

Triggered when a Wix user is disconnected from a staff member.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.staff.v1.staff_member_disconnected_from_user` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onStaffMemberDisconnectedFromUser
  import { staffMembers } from '@wix/bookings';

  staffMembers.onStaffMemberDisconnectedFromUser((event) => {
      // handle your event here
  });
  ```

  ```javascript onStaffMemberDisconnectedFromUser (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { staffMembers } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        staffMembers,
      },
  });

  wixClient.staffMembers.onStaffMemberDisconnectedFromUser((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
