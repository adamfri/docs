---
title: "onStaffMemberSettingsUpdated"
description: "Triggered when a staffMemberSettings object is updated."
icon: "bolt"
---

Triggered when a `staffMemberSettings` object is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.staff_settings.v1.staff_member_settings_updated` |
| Package | `@wix/bookings` |
| Permissions | `Manage Bookings`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onStaffMemberSettingsUpdated
  import { staffMemberSettings } from '@wix/bookings';

  staffMemberSettings.onStaffMemberSettingsUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onStaffMemberSettingsUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { staffMemberSettings } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        staffMemberSettings,
      },
  });

  wixClient.staffMemberSettings.onStaffMemberSettingsUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
