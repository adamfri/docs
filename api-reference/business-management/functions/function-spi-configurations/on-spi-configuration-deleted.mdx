---
title: "onSpiConfigurationDeleted"
description: "Triggered when a service plugin configuration is deleted."
icon: "bolt"
---

Triggered when a service plugin configuration is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.functions.v1.spi_configuration_deleted` |
| Package | `@wix/functions` |
| Permissions | `Manage Stores`, `Manage Functions`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onSpiConfigurationDeleted
  import { functionSpiConfigurations } from '@wix/functions';

  functionSpiConfigurations.onSpiConfigurationDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onSpiConfigurationDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { functionSpiConfigurations } from '@wix/functions';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        functionSpiConfigurations,
      },
  });

  wixClient.functionSpiConfigurations.onSpiConfigurationDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
