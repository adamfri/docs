---
title: "Bulk Create Tax Exempt Groups"
description: "Creates multiple tax exempt groups in a single request."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates multiple tax exempt groups in a single request.

    To create a single tax exempt group, call [Create Tax Exempt Group](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/create-tax-exempt-group).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/billing/v1/bulk/tax-exempt-groups/create
    ```

    ## Body Params

    <ParamField body="taxExemptGroups" type="TaxExemptGroup[]" required>

      Tax exempt groups to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Tax Exempt Group Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/tax-exempt-group-object) for this object's fields.

    </ParamField>

    <ParamField body="returnEntity" type="boolean">

      Whether to return the created tax exempt groups in the response.

      Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkTaxExemptGroupResult[]">

      Results of the bulk create operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the single create operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="TaxExemptGroup">

          Created tax exempt group. Returned only if `returnEntity` is `true` in the request.

          See the [Tax Exempt Group Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/tax-exempt-group-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk create operation metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tax Exempt Group Created | `wix.billing.v1.tax_exempt_group_created` | Triggered when a tax exempt group is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    Creates multiple tax exempt groups in a single request.

    <CodeGroup>

      ```bash Request
      curl -X POST \
          'https://www.wixapis.com/billing/v1/bulk/tax-exempt-groups/create' \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
          "taxExemptGroups": [
            { "name": "Non-profits" },
            { "name": "Resellers" }
          ],
          "returnEntity": true
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "itemMetadata": {
              "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
              "originalIndex": 0,
              "success": true
            },
            "item": {
              "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
              "revision": "1",
              "createdDate": "2025-08-01T17:22:10.299Z",
              "updatedDate": "2025-08-01T17:22:10.299Z",
              "name": "Non-profits"
            }
          },
          {
            "itemMetadata": {
              "id": "9157e04d-8686-4109-b6bc-d02be944d158",
              "originalIndex": 1,
              "success": true
            },
            "item": {
              "id": "9157e04d-8686-4109-b6bc-d02be944d158",
              "revision": "1",
              "createdDate": "2025-08-01T17:22:10.299Z",
              "updatedDate": "2025-08-01T17:22:10.299Z",
              "name": "Resellers"
            }
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Creates multiple tax exempt groups in a single request.

    To create a single tax exempt group, call [Create Tax Exempt Group](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/create-tax-exempt-group).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { taxExemptGroups } from '@wix/ecom';

    taxExemptGroups.bulkCreateTaxExemptGroups(taxExemptGroups, options): Promise
    ```

    ## Method Parameters

    <ParamField body="taxExemptGroups" type="TaxExemptGroup[]" required>

      Tax exempt groups to create.

      Constraints: `minItems 1`, `maxItems 100`.

      See the [Tax Exempt Group Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/tax-exempt-group-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="BulkCreateTaxExemptGroupsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="returnEntity" type="boolean">

          Whether to return the created tax exempt groups in the response.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="BulkTaxExemptGroupResult[]">

      Results of the bulk create operation.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the single create operation.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

              Constraints: `min 0`.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action for this item was successful. When `false`, the `error` field is returned.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="item" type="TaxExemptGroup">

          Created tax exempt group. Returned only if `returnEntity` is `true` in the request.

          See the [Tax Exempt Group Object](/api-reference/business-solutions/e-commerce/extensions/tax/tax-exempt-groups/tax-exempt-group-object) for this object's fields.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Bulk create operation metadata.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

          Constraints: `min 0`.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

          Constraints: `min 0`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Tax Exempt Group Created | `wix.billing.v1.tax_exempt_group_created` | Triggered when a tax exempt group is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-create-tax-exempt-groups" id="ex-sdk-bulk-create-tax-exempt-groups-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-create-tax-exempt-groups" id="ex-sdk-bulk-create-tax-exempt-groups-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-create-tax-exempt-groups" id="ex-sdk-bulk-create-tax-exempt-groups-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-create-tax-exempt-groups-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-create-tax-exempt-groups-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateTaxExemptGroups</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateTaxExemptGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkCreateTaxExemptGroups (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-create-tax-exempt-groups-0"><span className="wix-picker-name">bulkCreateTaxExemptGroups</span></label>
    <label htmlFor="ex-sdk-bulk-create-tax-exempt-groups-1"><span className="wix-picker-name">bulkCreateTaxExemptGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-create-tax-exempt-groups-2"><span className="wix-picker-name">bulkCreateTaxExemptGroups (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkCreateTaxExemptGroups
    import { taxExemptGroups } from '@wix/ecom';

    async function bulkCreateTaxExemptGroups(taxExemptGroups,options) {
      const response = await taxExemptGroups.bulkCreateTaxExemptGroups(taxExemptGroups,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateTaxExemptGroups (self-hosted)
    import { createClient } from '@wix/sdk';
    import { taxExemptGroups } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { taxExemptGroups },
      // Include the auth strategy and host as relevant
    });

    async function bulkCreateTaxExemptGroups(taxExemptGroups,options) {
      const response = await myWixClient.taxExemptGroups.bulkCreateTaxExemptGroups(taxExemptGroups,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkCreateTaxExemptGroups (with elevated permissions)
    import { taxExemptGroups } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myBulkCreateTaxExemptGroupsMethod(taxExemptGroups,options) {
      const elevatedBulkCreateTaxExemptGroups = auth.elevate(taxExemptGroups.bulkCreateTaxExemptGroups);
      const response = await elevatedBulkCreateTaxExemptGroups(taxExemptGroups,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
