---
title: "onCategoryCreated"
description: "Triggered when a category is created."
icon: "bolt"
---

Triggered when a category is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v1.category_created` |
| Package | `@wix/comments` |

## Examples

<CodeGroup>

  ```javascript onCategoryCreated
  import { categories } from '@wix/comments';

  categories.onCategoryCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onCategoryCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { categories } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        categories,
      },
  });

  wixClient.categories.onCategoryCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
