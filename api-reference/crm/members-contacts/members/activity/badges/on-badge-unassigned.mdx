---
title: "onBadgeUnassigned"
description: "Triggered when a member's badge is removed."
icon: "bolt"
---

Triggered when a member's badge is removed.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v3.badge_badge_unassigned` |
| Package | `@wix/members` |
| Permissions | `Manage Stores`, `Manage Challenges`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeUnassigned
  import { badges } from '@wix/members';

  badges.onBadgeUnassigned((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeUnassigned (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badges } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        badges,
      },
  });

  wixClient.badges.onBadgeUnassigned((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
