---
title: "onProjectItemCreated"
description: "Triggered when a project item is created."
icon: "bolt"
---

Triggered when a project item is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.project_items.v1.project_item_created` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onProjectItemCreated
  import { projectItems } from '@wix/portfolio';

  projectItems.onProjectItemCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onProjectItemCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { projectItems } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        projectItems,
      },
  });

  wixClient.projectItems.onProjectItemCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
