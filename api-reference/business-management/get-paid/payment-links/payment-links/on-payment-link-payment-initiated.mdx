---
title: "onPaymentLinkPaymentInitiated"
description: "Triggered when a payment link payment is initiated."
icon: "bolt"
---

Triggered when a payment link payment is initiated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.paymentlinks.v1.payment_link_payment_link_payment_initiated` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Paylinks` |

## Examples

<CodeGroup>

  ```javascript onPaymentLinkPaymentInitiated
  import { paymentLinks } from '@wix/get-paid';

  paymentLinks.onPaymentLinkPaymentInitiated((event) => {
      // handle your event here
  });
  ```

  ```javascript onPaymentLinkPaymentInitiated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { paymentLinks } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        paymentLinks,
      },
  });

  wixClient.paymentLinks.onPaymentLinkPaymentInitiated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
