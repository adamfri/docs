---
title: "onContentUpdated"
description: "Triggered when translation content is updated."
icon: "bolt"
---

Triggered when translation content is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.translation.v1.content_updated` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API`, `Wix Multilingual - Translation Content + Published Content Read` |

## Examples

<CodeGroup>

  ```javascript onContentUpdated
  import { translationContents } from '@wix/multilingual';

  translationContents.onContentUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onContentUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { translationContents } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        translationContents,
      },
  });

  wixClient.translationContents.onContentUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
