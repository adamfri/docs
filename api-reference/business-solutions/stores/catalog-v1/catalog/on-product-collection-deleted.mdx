---
title: "onProductCollectionDeleted"
description: "Triggered when a collection is deleted."
icon: "bolt"
---

Triggered when a collection is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `com.wix.ecommerce.catalog.api.v1.CollectionDeleted` |
| Package | `@wix/stores` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Products`, `Read Stores - all read permissions`, `Read Products`, `Manage Restaurants - all permissions`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onProductCollectionDeleted
  import { products } from '@wix/stores';

  products.onProductCollectionDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onProductCollectionDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { products } from '@wix/stores';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        products,
      },
  });

  wixClient.products.onProductCollectionDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
