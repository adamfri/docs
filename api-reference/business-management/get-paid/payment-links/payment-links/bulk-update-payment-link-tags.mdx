---
title: "Bulk Update Payment Link Tags"
description: "Updates tags on multiple payment links."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    Updates tags on multiple payment links.
    A tag that is specified both in the list of assign and unassign tags, will be assigned.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/paymentlinks/v1/payment-links/bulk-update-tags
    ```

    ## Body Params

    <ParamField body="ids" type="string[]" required>

      Payment Link IDs to update.

      Constraints: `format GUID`, `maxItems 100`.

    </ParamField>

    <ParamField body="assignTags" type="Tags" required>

      Tags to assign to the payment links.

      <Expandable title="assignTags properties">

        <ParamField body="privateTags" type="TagList">

          Tags that require an additional permission in order to access them, normally not given to site members or visitors.

          <Expandable title="privateTags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 25`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="TagList">

          Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

          <Expandable title="tags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 25`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="unassignTags" type="Tags">

      Tags to unassign from the payment links.

      <Expandable title="unassignTags properties">

        <ParamField body="privateTags" type="TagList">

          Tags that require an additional permission in order to access them, normally not given to site members or visitors.

          <Expandable title="privateTags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 25`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="TagList">

          Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

          <Expandable title="tags properties">

            <ParamField body="tagIds" type="string[]">

              List of tag IDs.

              Constraints: `maxLength 25`, `maxItems 100`.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkUpdatePaymentLinkTagsResult[]">

      Bulk update operation results.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the item that was updated.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the bulk action.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates tags on multiple payment links.
    A tag that is specified both in the list of assign and unassign tags, will be assigned.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PAYLINKS.MANAGE">Manage Paylinks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { paymentLinks } from '@wix/get-paid';

    paymentLinks.bulkUpdatePaymentLinkTags(ids, options): Promise<BulkUpdatePaymentLinkTagsResponse>
    ```

    ## Method Parameters

    <ParamField body="ids" type="string[]" required>

      Payment Link IDs to update.

      Constraints: `format GUID`, `maxItems 100`.

    </ParamField>

    <ParamField body="options" type="BulkUpdatePaymentLinkTagsOptions" required>

      Field options. The `assignTags` object **must** be passed.

      <Expandable title="options properties">

        <ParamField body="assignTags" type="Tags" required>

          Tags to assign to the payment links.

          <Expandable title="assignTags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="unassignTags" type="Tags">

          Tags to unassign from the payment links.

          <Expandable title="unassignTags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkUpdatePaymentLinkTagsResponse>`.

    <ResponseField name="results" type="BulkUpdatePaymentLinkTagsResult[]">

      Bulk update operation results.

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata for the item that was updated.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata for the bulk action.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-update-payment-link-tags" id="ex-sdk-bulk-update-payment-link-tags-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-update-payment-link-tags" id="ex-sdk-bulk-update-payment-link-tags-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-update-payment-link-tags" id="ex-sdk-bulk-update-payment-link-tags-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-update-payment-link-tags-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-update-payment-link-tags-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdatePaymentLinkTags</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdatePaymentLinkTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkUpdatePaymentLinkTags (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-update-payment-link-tags-0"><span className="wix-picker-name">bulkUpdatePaymentLinkTags</span></label>
    <label htmlFor="ex-sdk-bulk-update-payment-link-tags-1"><span className="wix-picker-name">bulkUpdatePaymentLinkTags (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-update-payment-link-tags-2"><span className="wix-picker-name">bulkUpdatePaymentLinkTags (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkUpdatePaymentLinkTags
    import { paymentLinks } from '@wix/get-paid';

    async function bulkUpdatePaymentLinkTags(ids,options) {
      const response = await paymentLinks.bulkUpdatePaymentLinkTags(ids,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdatePaymentLinkTags (self-hosted)
    import { createClient } from '@wix/sdk';
    import { paymentLinks } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { paymentLinks },
      // Include the auth strategy and host as relevant
    });

    async function bulkUpdatePaymentLinkTags(ids,options) {
      const response = await myWixClient.paymentLinks.bulkUpdatePaymentLinkTags(ids,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkUpdatePaymentLinkTags (with elevated permissions)
    import { paymentLinks } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myBulkUpdatePaymentLinkTagsMethod(ids,options) {
      const elevatedBulkUpdatePaymentLinkTags = auth.elevate(paymentLinks.bulkUpdatePaymentLinkTags);
      const response = await elevatedBulkUpdatePaymentLinkTags(ids,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
