---
title: "List Collections"
description: "Retrieves a list all collections in a portfolio."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list all collections in a portfolio.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/portfolio/v1/collections
    ```

    ## Query Params

    <ParamField query="paging" type="CursorPaging">

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Maximum number of items to return in the results.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="includePageUrl" type="boolean">

      Whether to include the collection's page URL and relative path in the response. Default: `false`

    </ParamField>

    ## Response Object

    <ResponseField name="collections" type="Collection[]">

      List of collections.

      See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/portfolio/v1/collections' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      ```

      ```json Response
      {
        "collections": [
          {
            "id": "123e4567-e89b-12d3-a456-426614174000",
            "revision": "1",
            "title": "Collection Title 1",
            "description": "Description of collection 1.",
            "slug": "collection-title-1",
            "coverImage": {
              "imageInfo": {
                "id": "8c074c67-2926-4518-92ef-c79d7810b3a4"
              }
            },
            "hidden": false,
            "sortOrder": 1.0,
            "createdDate": "2024-07-31T12:00:00Z",
            "updatedDate": "2024-07-31T12:00:00Z",
            "url": {
              "relativePath": "/collections/collection-title-1",
              "url": "https://example.com/collections/collection-title-1"
            }
          },
          {
            "id": "223e4567-e89b-12d3-a456-426614174000",
            "revision": "1",
            "title": "Collection Title 2",
            "description": "Description of collection 2.",
            "slug": "collection-title-2",
            "coverImage": {
              "imageInfo": {
                "id": "42460836-02b9-4640-8d47-2d9fe9aba7e4"
              }
            },
            "hidden": true,
            "sortOrder": 2.0,
            "createdDate": "2024-07-31T12:10:00Z",
            "updatedDate": "2024-07-31T12:10:00Z",
            "url": {
              "relativePath": "/collections/collection-title-2",
              "url": "https://example.com/collections/collection-title-2"
            }
          }
        ],
        "metadata": {
          "count": 2,
          "offset": 0,
          "total": 2,
          "tooManyToCount": false,
          "cursors": {
            "next": "next_cursor",
            "prev": "prev_cursor"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list all collections in a portfolio.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { collections } from '@wix/portfolio';

    collections.listCollections(options): Promise<ListCollectionsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListCollectionsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="paging" type="CursorPaging">

          paging

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return in the results.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="includePageUrl" type="boolean">

          Whether to include the collection's page URL and relative path in the response. Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListCollectionsResponse>`.

    <ResponseField name="collections" type="Collection[]">

      List of collections.

      See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    </ResponseField>

    <ResponseField name="metadata" type="PagingMetadataV2">

      Paging metadata.

      <Expandable title="metadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-collections" id="ex-sdk-list-collections-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-collections" id="ex-sdk-list-collections-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-collections-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-collections-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">listCollections</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listCollections (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-collections-0"><span className="wix-picker-name">listCollections</span></label>
    <label htmlFor="ex-sdk-list-collections-1"><span className="wix-picker-name">listCollections (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript listCollections
    import { collections } from '@wix/portfolio';

    async function listCollections(options) {
      const response = await collections.listCollections(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listCollections (self-hosted)
    import { createClient } from '@wix/sdk';
    import { collections } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { collections },
      // Include the auth strategy and host as relevant
    });

    async function listCollections(options) {
      const response = await myWixClient.collections.listCollections(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
