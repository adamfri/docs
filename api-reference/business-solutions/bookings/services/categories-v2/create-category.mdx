---
title: "Create Category"
description: "Creates a category."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a category.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/categories
    ```

    ## Body Params

    <ParamField body="category" type="Category" required>

      Category to create.

      See the [Category Object](/api-reference/business-solutions/bookings/services/categories-v2/category-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="category" type="Category">

      Created category.

      See the [Category Object](/api-reference/business-solutions/bookings/services/categories-v2/category-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CATEGORY_LIMIT_REACHED` | The site has reached the maximum of 100 categories. Delete an existing category before creating a new 1. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Created | `wix.bookings.categories.v2.category_created` | Triggered when a category is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/categories' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "category": {
          "name": "Our services"
        }
      }'
      ```

      ```json Response
      {
        "category": {
          "id": "f3f96d98-f106-415c-a4d5-f9bbffd56797",
          "revision": "1",
          "createdDate": "2024-11-07T23:15:35.629Z",
          "updatedDate": "2024-11-07T23:15:35.629Z",
          "name": "Our services",
          "sortOrder": 23214105
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a category.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { categoriesV2 } from '@wix/bookings';

    categoriesV2.createCategory(category): Promise<Category>
    ```

    ## Method Parameters

    <ParamField body="category" type="Category" required>

      Category to create.

      See the [Category Object](/api-reference/business-solutions/bookings/services/categories-v2/category-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<Category>`.

    See the [Category Object](/api-reference/business-solutions/bookings/services/categories-v2/category-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `CATEGORY_LIMIT_REACHED` | The site has reached the maximum of 100 categories. Delete an existing category before creating a new 1. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Category Created | `wix.bookings.categories.v2.category_created` | Triggered when a category is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-category" id="ex-sdk-create-category-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-category" id="ex-sdk-create-category-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-category" id="ex-sdk-create-category-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-category-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-category-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createCategory</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createCategory (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-category-0"><span className="wix-picker-name">createCategory</span></label>
    <label htmlFor="ex-sdk-create-category-1"><span className="wix-picker-name">createCategory (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-category-2"><span className="wix-picker-name">createCategory (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createCategory
    import { categoriesV2 } from '@wix/bookings';

    async function createCategory(category) {
      const response = await categoriesV2.createCategory(category);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCategory (self-hosted)
    import { createClient } from '@wix/sdk';
    import { categoriesV2 } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { categoriesV2 },
      // Include the auth strategy and host as relevant
    });

    async function createCategory(category) {
      const response = await myWixClient.categoriesV2.createCategory(category);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createCategory (with elevated permissions)
    import { categoriesV2 } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myCreateCategoryMethod(category) {
      const elevatedCreateCategory = auth.elevate(categoriesV2.createCategory);
      const response = await elevatedCreateCategory(category);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
