---
title: "Update Collection"
description: "Updates a collection."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a collection.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/portfolio/v1/collections/{collection.id}
    ```

    ## Path Params

    <ParamField path="collection.id" type="string" required>

      Collection ID.

    </ParamField>

    ## Body Params

    <ParamField body="collection" type="Collection" required>

      Collection to update.

      See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="collection" type="Collection">

      Updated collection.

      See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Collection Updated | `wix.portfolio.collections.v1.collection_updated` | Triggered when a collection is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/portfolio/v1/collections/123e4567-e89b-12d3-a456-426614174000' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "collection": {
          "id": "123e4567-e89b-12d3-a456-426614174000",
          "revision": "1",
          "title": "Updated Collection Title",
          "description": "This is an updated collection description.",
          "slug": "updated-collection-title",
          "coverImage": {
            "imageInfo": {
              "id": "8c074c67-2926-4518-92ef-c79d7810b3a4"
            }
          },
          "hidden": false,
          "sortOrder": 1.0
        }
      }'
      ```

      ```json Response
      {
        "collection": {
          "id": "123e4567-e89b-12d3-a456-426614174000",
          "revision": "2",
          "title": "Updated Collection Title",
          "description": "This is an updated collection description.",
          "slug": "updated-collection-title",
          "coverImage": {
            "imageInfo": {
              "id": "8c074c67-2926-4518-92ef-c79d7810b3a4"
            }
          },
          "hidden": false,
          "sortOrder": 1.0,
          "createdDate": "2024-07-31T12:00:00Z",
          "updatedDate": "2024-07-31T12:30:00Z",
          "url": {
            "relativePath": "/collections/updated-collection-title",
            "url": "https://example.com/collections/updated-collection-title"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a collection.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { collections } from '@wix/portfolio';

    collections.updateCollection(_id, collection): Promise<Collection>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Collection ID.

      **Read-only.**

    </ParamField>

    <ParamField body="collection" type="UpdateCollection" required>

      Collection to update.

      <Expandable title="collection properties">

        <ParamField body="_id" type="string">

          Collection ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the collection is updated. To prevent conflicting changes, the existing revision must be passed when updating the collection object.

          **Read-only.**

        </ParamField>

        <ParamField body="title" type="string">

          Collection title.

        </ParamField>

        <ParamField body="description" type="string">

          Collection description.

        </ParamField>

        <ParamField body="slug" type="string">

          Collection slug.

        </ParamField>

        <ParamField body="coverImage" type="Image">

          Collection cover image.

          <Expandable title="coverImage properties">

            <ParamField body="imageInfo" type="string">

              Information about the Wix Media image.

            </ParamField>

            <ParamField body="focalPoint" type="Point">

              Focal point of the image.

              _Nested structure omitted for brevity — see the `Point` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="hidden" type="boolean">

          Whether the collection is hidden from the portfolio. Default: `false`

        </ParamField>

        <ParamField body="sortOrder" type="number">

          Index that determines which position a collection is displayed in the portfolio. &lt;br />

          Default: [Epoch](https://www.epoch101.com/) timestamp. &lt;br />

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the collection was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the collection was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="url" type="string">

          Collection page URL and relative path. Returned when `includePageUrl` is `true` in the request.

          **Read-only.**

        </ParamField>

        <ParamField body="seoData" type="SeoSchema">

          Collection SEO data.

          <Expandable title="seoData properties">

            <ParamField body="tags" type="Tag[]">

              SEO tag information.

              _Nested structure omitted for brevity — see the `Tag[]` object._

            </ParamField>

            <ParamField body="settings" type="Settings">

              SEO general settings.

              _Nested structure omitted for brevity — see the `Settings` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Collection>`.

    See the [Collection Object](/api-reference/business-solutions/portfolio/collections/collection-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Collection Updated | `wix.portfolio.collections.v1.collection_updated` | Triggered when a collection is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-collection" id="ex-sdk-update-collection-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-collection" id="ex-sdk-update-collection-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-collection" id="ex-sdk-update-collection-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-collection-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-collection-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateCollection</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCollection (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-collection-0"><span className="wix-picker-name">updateCollection</span></label>
    <label htmlFor="ex-sdk-update-collection-1"><span className="wix-picker-name">updateCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-collection-2"><span className="wix-picker-name">updateCollection (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateCollection
    import { collections } from '@wix/portfolio';

    async function updateCollection(_id,collection) {
      const response = await collections.updateCollection(_id,collection);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCollection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { collections } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { collections },
      // Include the auth strategy and host as relevant
    });

    async function updateCollection(_id,collection) {
      const response = await myWixClient.collections.updateCollection(_id,collection);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCollection (with elevated permissions)
    import { collections } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myUpdateCollectionMethod(_id,collection) {
      const elevatedUpdateCollection = auth.elevate(collections.updateCollection);
      const response = await elevatedUpdateCollection(_id,collection);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
