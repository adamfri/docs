---
title: "Search Contacts"
description: "Retrieves a list of contacts using a free-text search combined with filtering and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of contacts using a free-text search combined with filtering and sorting.

    Search combines free-text matching with filtering, sorting, and cursor paging. To learn about the query syntax, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

    Searchable properties: `name.full`, `email.email`, `phone.phone`, `company.name`, `company.jobTitle`, `memberInfo.email`, and `memberInfo.profileInfo.nickname`.

    Name format: Set `nameFormat` to control how names are matched and ordered:
    - `BY_FIRST_NAME`: Orders by first name, then last name.
    - `BY_LAST_NAME`: Orders by last name, then first name.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `assigneeId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.memberId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `source.appId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `source.userId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `timeZone` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.profileInfo.privacyStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.userInfo.role` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.emailVerificationRequired` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `memberInfo.emailVerified` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `memberInfo.profileInfo.muted` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `memberInfo.profileInfo.reported` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `lastActivity.date` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `memberInfo.sessionInfo.lastMobileLogin` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `memberInfo.sessionInfo.lastWebLogin` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `memberInfo.signupDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `company.jobTitle` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `company.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `email.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `memberInfo.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `memberInfo.profileInfo.nickname` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `name.first` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `name.last` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `phone.phone` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `email.deliverabilityStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `email.subscriptionStatus.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `phone.deliverabilityStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `phone.subscriptionStatus.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `locale` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `phone.e164` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `source.additionalInfo` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `locationIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `memberInfo.groupInfo.groupIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `memberInfo.sessionInfo.mobileAppNames` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `segmentIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `tags.privateTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `addresses.address.country` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome` |
    | `addresses.address.subdivision` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome` |
    | `addresses.recipient.phone` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome` |
    | `additionalEmails.email` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `additionalPhones.e164` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `additionalPhones.phone` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.address.addressLine` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.address.city` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.address.postalCode` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.address.streetAddress.name` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.address.subdivisionFullname` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.recipient.company` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.recipient.firstName` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.recipient.lastName` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `birthdate` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `name.full` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: ✗ |  |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/contacts/v5/contacts/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      WQL search expression.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations, also known as faceted search. Lets you explore large amounts of data by displaying summaries about different partitions of the data, and narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name for the aggregation. Must be unique. Appears in the aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation. Must match the aggregation type provided in the `kind` field.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built, one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges, each representing a bucket.
              - `SCALAR` — A single-value metric aggregation, such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built, one per time interval (hour, day, week, and so on).
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify the JSON path.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              How separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty, searches all searchable fields. Use dot notation to specify the JSON path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use fuzzy search, which allows typos using a managed proximity algorithm.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects the values returned by all filters and aggregations.
          You can override this behavior in a specific filter by providing
          timestamps that include a time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="nameFormat" type="string">

      Name Format for sorting and searching.
      When sorting by `displayName`, the returned contacts are ordered by first and last names, by default.
      Alternatively, when sorting by `displayName` with `BY_LAST_NAME`, the returned contacts are ordered by last and first names.
      For contacts that do not have names, sorting is by email or phone.
      When searching by `name.full`, the contacts returned are matched and ordered by first and last names.
      Alternatively, when searching by `name.full` with `BY_LAST_NAME`, the contacts returned
      are matched and ordered by last and first names.

      **Supported values:**

      - `BY_FIRST_NAME` — Names will be formatted starting with the first name followed by the last name.
      - `BY_LAST_NAME` — Names will be formatted starting with the last name followed by the first name.

    </ParamField>

    <ParamField body="semanticQuery" type="string">

      Natural language query for semantic search. Mutually exclusive with cursor paging with a cursor.

    </ParamField>

    ## Response Object

    <ResponseField name="contacts" type="Contact[]">

      Retrieved contacts.

      See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items matching the filter.
          Available only on the first page of Search results, not included in Query or List results.
          If the Search results span multiple pages, the value of `total` can exceed the number of items returned on the first page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Results of the requested aggregations.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of the aggregation, as provided in the search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation. Matches the `kind` provided in the search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built, one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges, each representing a bucket.
              - `SCALAR` — A single-value metric aggregation, such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built, one per time interval (hour, day, week, and so on).
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by, as provided in the search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Searches contacts using free-text in search.search.expression with sort and cursor paging.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/contacts/v5/contacts/search' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "search": {
          "search": {
            "expression": "John",
            "mode": "OR",
            "fuzzy": true
          },
          "sort": [
            {
              "fieldName": "createdDate",
              "order": "ASC"
            }
          ],
          "cursorPaging": {
            "limit": 10
          }
        },
        "nameFormat": "BY_FIRST_NAME"
      }'
      ```

      ```json Response
      {
        "contacts": [
          {
            "id": "a64d222b-d16e-4f68-9926-064aee9f47a2",
            "revision": "1",
            "createdDate": "2026-07-01T18:14:34.941Z",
            "updatedDate": "2026-07-01T18:14:34.941Z",
            "name": {
              "first": "John",
              "last": "Doe"
            },
            "email": {
              "email": "john.doe@example.com",
              "subscriptionStatus": {
                "status": "NOT_SET"
              },
              "deliverabilityStatus": "NOT_SET"
            },
            "additionalEmails": [],
            "phone": {
              "tag": "OTHER",
              "phone": "+1-555-0101",
              "subscriptionStatus": {
                "status": "NO_SUBSCRIPTION_STATUS"
              },
              "deliverabilityStatus": "NO_PHONE_DELIVERABILITY_STATUS"
            },
            "additionalPhones": [],
            "addresses": [
              {
                "tag": "OTHER",
                "address": {
                  "country": "US",
                  "city": "New York",
                  "postalCode": "10001",
                  "streetAddress": {
                    "number": "",
                    "name": "",
                    "apt": ""
                  },
                  "subdivisions": []
                },
                "defaultAddress": true,
                "id": "897f164c-631c-47db-85f7-29621e19418e"
              }
            ],
            "company": {
              "name": "Acme Corp",
              "jobTitle": "Software Engineer"
            },
            "locationIds": [],
            "source": {
              "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438"
            },
            "lastActivity": {
              "date": "2026-07-01T18:14:34.941Z",
              "templateId": "202edbe8-e365-490d-aa18-4335b26ab8c1"
            },
            "segmentIds": []
          },
          {
            "id": "51481384-482d-4d71-8993-b4c70a964026",
            "revision": "2",
            "createdDate": "2026-07-01T18:14:36.361Z",
            "updatedDate": "2026-07-01T18:18:33.465Z",
            "name": {
              "first": "Robert",
              "last": "Johnson"
            },
            "email": {
              "email": "bob.johnson@example.com",
              "subscriptionStatus": {
                "status": "NOT_SET"
              },
              "deliverabilityStatus": "NOT_SET"
            },
            "additionalEmails": [],
            "phone": {
              "tag": "OTHER",
              "phone": "+1-555-0103",
              "subscriptionStatus": {
                "status": "NO_SUBSCRIPTION_STATUS"
              },
              "deliverabilityStatus": "NO_PHONE_DELIVERABILITY_STATUS"
            },
            "additionalPhones": [],
            "addresses": [],
            "company": {
              "name": "Johnson Consulting",
              "jobTitle": "Principal Consultant"
            },
            "locationIds": [],
            "source": {
              "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438"
            },
            "lastActivity": {
              "date": "2026-07-01T18:14:36.361Z",
              "templateId": "202edbe8-e365-490d-aa18-4335b26ab8c1"
            },
            "segmentIds": []
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false,
          "total": 2
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of contacts using a free-text search combined with filtering and sorting.

    Search combines free-text matching with filtering, sorting, and cursor paging. To learn about the query syntax, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).

    Searchable properties: `name.full`, `email.email`, `phone.phone`, `company.name`, `company.jobTitle`, `memberInfo.email`, and `memberInfo.profileInfo.nickname`.

    Name format: Set `nameFormat` to control how names are matched and ordered:
    - `BY_FIRST_NAME`: Orders by first name, then last name.
    - `BY_LAST_NAME`: Orders by last name, then first name.

    | Property | Capabilities | Filters |
    | --- | --- | --- |
    | `assigneeId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.memberId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `source.appId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `source.userId` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `timeZone` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.profileInfo.privacyStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.userInfo.role` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin` |
    | `memberInfo.emailVerificationRequired` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `memberInfo.emailVerified` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `memberInfo.profileInfo.muted` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `memberInfo.profileInfo.reported` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$ne` |
    | `_createdDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `_updatedDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `lastActivity.date` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `memberInfo.sessionInfo.lastMobileLogin` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `memberInfo.sessionInfo.lastWebLogin` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `memberInfo.signupDate` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |
    | `company.jobTitle` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `company.name` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `email.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `memberInfo.email` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `memberInfo.profileInfo.nickname` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `name.first` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `name.last` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `phone.phone` | Search: <span style={{ color: "#2e7d32" }}>✓</span><br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `_id` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `email.deliverabilityStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `email.subscriptionStatus.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `phone.deliverabilityStatus` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `phone.subscriptionStatus.status` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$in`, `$ne`, `$nin` |
    | `locale` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `phone.e164` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `source.additionalInfo` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$eq`, `$exists`, `$in`, `$ne`, `$nin`, `$startsWith` |
    | `locationIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `memberInfo.groupInfo.groupIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `memberInfo.sessionInfo.mobileAppNames` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `segmentIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `tags.privateTags.tagIds` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$isEmpty` |
    | `addresses.address.country` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome` |
    | `addresses.address.subdivision` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome` |
    | `addresses.recipient.phone` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome` |
    | `additionalEmails.email` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `additionalPhones.e164` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `additionalPhones.phone` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.address.city` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.address.postalCode` | Search: ✗<br /> Aggregate: <span style={{ color: "#2e7d32" }}>✓</span><br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.recipient.company` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.recipient.firstName` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `addresses.recipient.lastName` | Search: ✗<br /> Aggregate: ✗<br /> Sort: ✗ | `$exists`, `$hasAll`, `$hasSome`, `$startsWith` |
    | `birthdate` | Search: ✗<br /> Aggregate: ✗<br /> Sort: `ASC`, `DESC` | `$eq`, `$gt`, `$gte`, `$lt`, `$lte`, `$ne` |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contactsV5 } from '@wix/crm';

    contactsV5.searchContacts(search, options): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      WQL search expression.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          List of sort objects.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="aggregations" type="Aggregation[]">

          Aggregations, also known as faceted search. Lets you explore large amounts of data by displaying summaries about different partitions of the data, and narrow the navigation to a specific partition.

          Constraints: `maxItems 10`.

          <Expandable title="aggregations properties">

            <ParamField body="name" type="string">

              User-defined name for the aggregation. Must be unique. Appears in the aggregation results.

            </ParamField>

            <ParamField body="type" type="string">

              Type of aggregation. Must match the aggregation type provided in the `kind` field.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built, one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges, each representing a bucket.
              - `SCALAR` — A single-value metric aggregation, such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built, one per time interval (hour, day, week, and so on).
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ParamField>

            <ParamField body="fieldPath" type="string">

              Field to aggregate by. Use dot notation to specify the JSON path.

              Constraints: `maxLength 200`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              How separate search terms in `expression` are combined.

              **Supported values:**

              - `OR` — Any of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If empty, searches all searchable fields. Use dot notation to specify the JSON path.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to use fuzzy search, which allows typos using a managed proximity algorithm.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="timeZone" type="string">

          UTC offset or IANA time zone. Valid values are
          ISO 8601 UTC offsets, such as +02:00 or -06:00,
          and IANA time zone IDs, such as Europe/Rome.

          Affects the values returned by all filters and aggregations.
          You can override this behavior in a specific filter by providing
          timestamps that include a time zone. For example, `"2023-12-20T10:52:34.795Z"`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="SearchContactsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="nameFormat" type="string">

          Name Format for sorting and searching.
          When sorting by `displayName`, the returned contacts are ordered by first and last names, by default.
          Alternatively, when sorting by `displayName` with `BY_LAST_NAME`, the returned contacts are ordered by last and first names.
          For contacts that do not have names, sorting is by email or phone.
          When searching by `name.full`, the contacts returned are matched and ordered by first and last names.
          Alternatively, when searching by `name.full` with `BY_LAST_NAME`, the contacts returned
          are matched and ordered by last and first names.

          **Supported values:**

          - `BY_FIRST_NAME` — Names will be formatted starting with the first name followed by the last name.
          - `BY_LAST_NAME` — Names will be formatted starting with the last name followed by the first name.

        </ParamField>

        <ParamField body="semanticQuery" type="string">

          Natural language query for semantic search. Mutually exclusive with cursor paging with a cursor.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="contacts" type="Contact[]">

      Retrieved contacts.

      See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts-v5/contact-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items matching the filter.
          Available only on the first page of Search results, not included in Query or List results.
          If the Search results span multiple pages, the value of `total` can exceed the number of items returned on the first page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Results of the requested aggregations.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of the aggregation, as provided in the search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation. Matches the `kind` provided in the search request.

              **Supported values:**

              - `VALUE` — An aggregation where result buckets are dynamically built, one per unique value.
              - `RANGE` — An aggregation where you can define a set of ranges, each representing a bucket.
              - `SCALAR` — A single-value metric aggregation, such as min, max, sum, or avg.
              - `DATE_HISTOGRAM` — An aggregation where result buckets are dynamically built, one per time interval (hour, day, week, and so on).
              - `NESTED` — Multi-level aggregation, where each next aggregation is nested within the previous one.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by, as provided in the search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-contacts" id="ex-sdk-search-contacts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-contacts" id="ex-sdk-search-contacts-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-contacts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-contacts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search contacts with a text query</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-contacts-0"><span className="wix-picker-name">Search contacts with a text query</span></label>
    <label htmlFor="ex-sdk-search-contacts-1"><span className="wix-picker-name">searchContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Search contacts with a text query
    import { contactsV5 } from "@wix/crm";

    async function searchContacts() {
      const response = await contactsV5.searchContacts(
        {
          search: {
            expression: "John",
            mode: contactsV5.Mode.OR,
            fuzzy: true,
          },
          sort: [{ fieldName: "_createdDate", order: contactsV5.SortOrder.ASC }],
          cursorPaging: { limit: 10 },
        },
        {
          nameFormat: "BY_FIRST_NAME",
        },
      );
    }

    /* Promise resolves to:
     * {
     *   "contacts": [
     *     {
     *       "revision": "2",
     *       "name": { "first": "John", "last": "Doe" },
     *       "email": {
     *         "email": "john.doe@example.com",
     *         "subscriptionStatus": { "status": "NOT_SET" },
     *         "deliverabilityStatus": "NOT_SET"
     *       },
     *       "company": { "name": "Acme Corp", "jobTitle": "Software Engineer" },
     *       "source": { "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438" },
     *       "_id": "085ccffa-6686-427f-a310-9b5e3058ebbc"
     *     },
     *     {
     *       "revision": "3",
     *       "name": { "first": "Robert", "last": "Johnson" },
     *       "email": {
     *         "email": "bob.johnson@example.com",
     *         "subscriptionStatus": { "status": "NOT_SET" },
     *         "deliverabilityStatus": "NOT_SET"
     *       },
     *       "company": { "name": "Johnson Consulting", "jobTitle": "Principal Consultant" },
     *       "source": { "appId": "e9022116-5b20-46d1-a29d-fd1b611ca438" },
     *       "_id": "e3f2e525-571b-4274-add8-06034b27c13c"
     *     }
     *   ],
     *   "pagingMetadata": {
     *     "count": 2,
     *     "cursors": {},
     *     "hasNext": false,
     *     "total": 2
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchContacts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contactsV5 } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contactsV5 },
      // Include the auth strategy and host as relevant
    });

    async function searchContacts(search,options) {
      const response = await myWixClient.contactsV5.searchContacts(search,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
