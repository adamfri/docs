---
title: "Query Latest Actions"
description: "Retrieves a list of the latest version of each action, based on the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of the latest version of each action, based on the provided paging, filtering, and sorting.

    To retrieve all stored versions of each action, call [Query Actions](https://dev.wix.com/docs/api-reference/business-management/automations/actions/action-catalog/query-actions).

    Query Latest Actions runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.limit` is `50`.
    - `paging.offset` is `0`.

    To learn about working with query methods, see:
    - [API Query Language](https://dev.wix.com/docs/api-reference/articles/getting-started/api-query-language)
    - [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging)
    - [Field projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection)

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/v1/actions/query-latest
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2" required>

      Query options.

      See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more information.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="Action[]">

      Retrieved actions. Contains only the latest version of each action.

      See the [Action Object](/api-reference/business-management/automations/actions/action-catalog/action-object) for this object's fields.

    </ResponseField>

    <ResponseField name="paging" type="PagingMetadataV2">

      Paging metadata of the response.

      <Expandable title="paging properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/v1/actions/query-latest' \
          -H 'Authorization: <AUTH>' \
          -H 'Content-Type: application/json' \
          -d '{
            "query": {
              "filter": {
                "appId": "e4c3e640-0b63-4bd9-8574-53f8c14e0236"
              },
              "paging": {
                "limit": 10,
                "offset": 0
              }
            }
          }'
      ```

      ```json Response
      {
        "results": [
          {
            "id": "a3d2f8e1-7c4b-4a9e-b6d5-1e8f3c2a9b7d",
            "appId": "e4c3e640-0b63-4bd9-8574-53f8c14e0236",
            "actionKey": "send-gift-card",
            "inputSchema": {
              "$schema": "http://json-schema.org/draft-07/schema",
              "type": "object",
              "required": ["contactId", "amount"],
              "properties": {
                "contactId": {
                  "type": "string",
                  "format": "uuid",
                  "title": "Contact ID"
                },
                "amount": {
                  "type": "number",
                  "title": "Gift card amount"
                }
              }
            },
            "displayName": "Send a gift card",
            "description": "Send a gift card to a contact via email.",
            "implementedMethods": {
              "validateConfiguration": false,
              "getQuotaInfo": false
            },
            "interfaceConfiguration": {
              "type": "GENERIC"
            },
            "createdDate": "2024-11-15T10:30:00.000Z",
            "updatedDate": "2024-11-20T14:45:00.000Z"
          }
        ],
        "paging": {
          "total": 1,
          "limit": 10,
          "offset": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of the latest version of each action, based on the provided paging, filtering, and sorting.

    To retrieve all stored versions of each action, call [Query Actions](https://dev.wix.com/docs/api-reference/business-management/automations/actions/action-catalog/query-actions).

    Query Latest Actions runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.limit` is `50`.
    - `paging.offset` is `0`.

    To learn about working with query methods, see:
    - [API Query Language](https://dev.wix.com/docs/api-reference/articles/getting-started/api-query-language)
    - [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging)
    - [Field projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection)

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CRM.SETUP-AUTOMATIONS">Set Up Automations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { actionCatalog } from '@wix/automations';

    actionCatalog.queryLatestActions(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="QueryV2" required>

      Query options.

      See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more information.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          Constraints: `maxItems 4`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 200`, `maxItems 20`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="Action[]">

      Retrieved actions. Contains only the latest version of each action.

      See the [Action Object](/api-reference/business-management/automations/actions/action-catalog/action-object) for this object's fields.

    </ResponseField>

    <ResponseField name="paging" type="PagingMetadataV2">

      Paging metadata of the response.

      <Expandable title="paging properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Flag that indicates the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-latest-actions" id="ex-sdk-query-latest-actions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-latest-actions" id="ex-sdk-query-latest-actions-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-latest-actions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-latest-actions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query latest actions</span><span className="wix-picker-desc">Queries the latest version of each action, filtered by app ID</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryLatestActions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-latest-actions-0"><span className="wix-picker-name">Query latest actions</span><span className="wix-picker-desc">Queries the latest version of each action, filtered by app ID</span></label>
    <label htmlFor="ex-sdk-query-latest-actions-1"><span className="wix-picker-name">queryLatestActions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query latest actions
    import { actionCatalog } from "@wix/automations";

    async function queryLatestActions() {
      const response = await actionCatalog.queryLatestActions({
        filter: {
          appId: "e4c3e640-0b63-4bd9-8574-53f8c14e0236",
        },
        paging: { limit: 10, offset: 0 },
      });
      return response;
    }

    /* Promise resolves to:
     * {
     *   "results": [
     *     {
     *       "_id": "a3d2f8e1-7c4b-4a9e-b6d5-1e8f3c2a9b7d",
     *       "appId": "e4c3e640-0b63-4bd9-8574-53f8c14e0236",
     *       "actionKey": "send-gift-card",
     *       "inputSchema": { ... },
     *       "displayName": "Send a gift card",
     *       "description": "Send a gift card to a contact via email.",
     *       "implementedMethods": { "validateConfiguration": false, "getQuotaInfo": false },
     *       "interfaceConfiguration": { "type": "GENERIC" },
     *       "_createdDate": "2024-11-15T10:30:00.000Z",
     *       "_updatedDate": "2024-11-20T14:45:00.000Z"
     *     }
     *   ],
     *   "paging": { "total": 1, "limit": 10, "offset": 0 }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryLatestActions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { actionCatalog } from '@wix/automations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { actionCatalog },
      // Include the auth strategy and host as relevant
    });

    async function queryLatestActions(query) {
      const response = await myWixClient.actionCatalog.queryLatestActions(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
