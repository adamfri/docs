---
title: "onServiceDeleted"
description: "Triggered when a service is deleted."
icon: "bolt"
---

Triggered when a service is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.services.v2.service_deleted` |
| Package | `@wix/bookings` |
| Permissions | `Manage Stores`, `Read Bookings - Public Data`, `Manage Bookings Services and Settings`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onServiceDeleted
  import { services } from '@wix/bookings';

  services.onServiceDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onServiceDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { services } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        services,
      },
  });

  wixClient.services.onServiceDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
