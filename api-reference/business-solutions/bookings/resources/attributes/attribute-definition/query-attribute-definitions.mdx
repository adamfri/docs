---
title: "Query Attribute Definitions"
description: "Retrieves a list of up to 1000 attribute definitions, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1000 attribute definitions, given the provided paging, filtering, and sorting.

    Query Attribute Definitions runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `50`
    - `paging.offset` is `0`

    Supported filter and sort fields: `id`, `name`, `valueType`, `visibility`, `tags.publicTags.tagIds`, `tags.privateTags.tagIds`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `valueType` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `visibility` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `tags.publicTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |
    | `tags.privateTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="bookings:attributes:v1:attribute_definition:query_attribute_definitions">bookings:attributes:v1:attribute_definition:query_attribute_definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Endpoint

    ```http
    POST https://www.wixapis.com/_api/attributes/v1/attribute-definitions/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      WQL expression.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="attributeDefinitions" type="AttributeDefinition[]">

      List of attribute definitions.

      See the [Attribute Definition Object](/api-reference/business-solutions/bookings/resources/attributes/attribute-definition/attribute-definition-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/_api/attributes/v1/attribute-definitions/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "valueType": "NUMBER"
          },
          "cursorPaging": {
            "limit": 50
          }
        }
      }'
      ```

      ```json Response
      {
        "attributeDefinitions": [
          {
            "id": "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
            "revision": "1",
            "createdDate": "2024-01-15T10:30:00.000Z",
            "updatedDate": "2024-01-15T10:30:00.000Z",
            "name": "Capacity",
            "valueType": "NUMBER",
            "numberConfig": {
              "min": 1,
              "max": 20
            },
            "defaultValue": {
              "numberDefault": 2
            },
            "visibility": true
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {}
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of up to 1000 attribute definitions, given the provided paging, filtering, and sorting.

    Query Attribute Definitions runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order
    - `paging.limit` is `50`
    - `paging.offset` is `0`

    Supported filter and sort fields: `id`, `name`, `valueType`, `visibility`, `tags.publicTags.tagIds`, `tags.privateTags.tagIds`.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `name` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `valueType` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `visibility` | `$eq`, `$ne`, `$in`, `$exists` | Sort: `ASC`, `DESC` |
    | `tags.publicTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |
    | `tags.privateTags.tagIds` | `$hasSome`, `$hasAll`, `$exists`, `$isEmpty` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="bookings:attributes:v1:attribute_definition:query_attribute_definitions">bookings:attributes:v1:attribute_definition:query_attribute_definitions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    ## Method Declaration

    ```typescript
    import { attributeDefinition } from '@wix/bookings';

    attributeDefinition.queryAttributeDefinitions(query): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="AttributeDefinitionQuery" required>

      WQL expression.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor paging options.

              Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="attributeDefinitions" type="AttributeDefinition[]">

      List of attribute definitions.

      See the [Attribute Definition Object](/api-reference/business-solutions/bookings/resources/attributes/attribute-definition/attribute-definition-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-attribute-definitions" id="ex-sdk-query-attribute-definitions-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-attribute-definitions" id="ex-sdk-query-attribute-definitions-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-attribute-definitions-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-attribute-definitions-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query number attribute definitions</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryAttributeDefinitions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-attribute-definitions-0"><span className="wix-picker-name">Query number attribute definitions</span></label>
    <label htmlFor="ex-sdk-query-attribute-definitions-1"><span className="wix-picker-name">queryAttributeDefinitions (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query number attribute definitions
    import { attributeDefinition } from "@wix/bookings";

    const query = {
      filter: { valueType: attributeDefinition.ValueType.NUMBER },
      cursorPaging: { limit: 50 },
    };

    async function queryAttributeDefinitions() {
      const response = await attributeDefinition.queryAttributeDefinitions(query);
    }

    /* Promise resolves to:
     * {
     *   "attributeDefinitions": [
     *     {
     *       "_id": "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
     *       "revision": "1",
     *       "name": "Capacity",
     *       "valueType": "NUMBER",
     *       "numberConfig": { "min": 1, "max": 20 },
     *       "defaultValue": { "numberDefault": 2 },
     *       "visibility": true
     *     }
     *   ],
     *   "pagingMetadata": { "count": 1, "cursors": {} }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryAttributeDefinitions (self-hosted)
    import { createClient } from '@wix/sdk';
    import { attributeDefinition } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { attributeDefinition },
      // Include the auth strategy and host as relevant
    });

    async function queryAttributeDefinitions(query) {
      const response = await myWixClient.attributeDefinition.queryAttributeDefinitions(query);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
