---
title: "onProjectItemUpdated"
description: "Triggered when a project item is updated."
icon: "bolt"
---

Triggered when a project item is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.project_items.v1.project_item_updated` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onProjectItemUpdated
  import { projectItems } from '@wix/portfolio';

  projectItems.onProjectItemUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onProjectItemUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { projectItems } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        projectItems,
      },
  });

  wixClient.projectItems.onProjectItemUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
