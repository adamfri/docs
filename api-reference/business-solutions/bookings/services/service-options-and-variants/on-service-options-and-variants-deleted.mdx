---
title: "onServiceOptionsAndVariantsDeleted"
description: "The onServiceOptionsAndVariantsDeleted event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.catalog.v1.service_options_and_variants_deleted` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings Services and Settings`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions` |

## Examples

<CodeGroup>

  ```javascript onServiceOptionsAndVariantsDeleted
  import { serviceOptionsAndVariants } from '@wix/bookings';

  serviceOptionsAndVariants.onServiceOptionsAndVariantsDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onServiceOptionsAndVariantsDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { serviceOptionsAndVariants } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        serviceOptionsAndVariants,
      },
  });

  wixClient.serviceOptionsAndVariants.onServiceOptionsAndVariantsDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
