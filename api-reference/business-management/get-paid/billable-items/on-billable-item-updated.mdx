---
title: "onBillableItemUpdated"
description: "Triggered when a billable item is updated."
icon: "bolt"
---

Triggered when a billable item is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.billable_items.v1.billable_item_updated` |
| Package | `@wix/get-paid` |
| Permissions | `Manage Stores`, `Manage Billable Items`, `Manage Events` |

## Examples

<CodeGroup>

  ```javascript onBillableItemUpdated
  import { billableItems } from '@wix/get-paid';

  billableItems.onBillableItemUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBillableItemUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { billableItems } from '@wix/get-paid';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        billableItems,
      },
  });

  wixClient.billableItems.onBillableItemUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
