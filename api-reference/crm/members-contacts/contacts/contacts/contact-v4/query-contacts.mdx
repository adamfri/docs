---
title: "Query Contacts"
description: "Retrieves a list of contacts, given the provided paging, filtering, and sorting."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of contacts, given the provided [paging](https://dev.wix.com/api/rest/getting-started/sorting-and-paging#getting-started_sorting-and-paging_paging), [filtering, and sorting](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/contact-v4/sort-filter-and-search).

    Up to 1,000 contacts can be returned per request.
    Query Contacts runs with these defaults, which you can override:

    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `50`
    - `paging.offset` is `0`

    For field support for filters and sorting, see
    [Contacts: Supported Filters and Sorting](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/contact-v4/sort-filter-and-search).

    To learn about working with _Query_ methods, see
    [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/rest/articles/getting-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `id` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `info.locale` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `createdDate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sort: `ASC`, `DESC` |
    | `lastActivity.activityDate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sort: `ASC`, `DESC` |
    | `info.birthdate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sort: `ASC`, `DESC` |
    | `updatedDate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | None |
    | `primaryInfo.email` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.name.first` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.name.last` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.company` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.jobTitle` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `primaryInfo.phone` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | None |
    | `info.labelKeys` | `$hasSome`, `$hasAll` | None |

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/contacts/v4/contacts/query
    ```

    ## Body Params

    <ParamField body="search" type="string">

      Plain text search for an exact match.

      Supported properties:
      `info.name.first`, `info.name.last`, `info.emails.email`, `info.phones.phone`

      Max: 100 characters

    </ParamField>

    <ParamField body="query" type="Query">

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.
          See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language)
          for more information.

          For a detailed list of supported filters, see
          [Supported Filters](https://dev.wix.com/api/rest/contacts/contacts/supported-filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sorting options.
          See [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination)
          for more information.

          Max: 1 sort object

          Constraints: `maxItems 1`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Field to sort by.

              Supported properties:
              `createdDate`, `lastActivity.activityDate`, `primaryInfo.email`, `info.name.first`, `info.name.last`, `info.company`,
              `info.jobTitle`, `info.birthdate`

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.
              Use `ASC` for ascending order or `DESC` for descending order.

              Defaults to `ASC`.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paging" type="Paging">

          Pagination options.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to return. Learn more about pagination in [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

              Default: `50`. &lt;br>
              Maximum: `1000`.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          List of projected fields to return.
          If both `fields` and `fieldsets` are sent in the request,
          the union of both lists is returned.
          `id` and `revision` are always returned.

          Supported properties:
          `source`, `createdDate`, `updatedDate`, `lastActivity`, `primaryInfo`,
          `info.name`, `info.emails`, `info.phones`, `info.addresses`, `info.company`,
          `info.jobTitle`, `info.picture`, `info.birthdate`, `info.locale`,
          `info.labelKeys`, `info.locations`, `info.extendedFields`

          Constraints: `maxLength 100`, `maxItems 100`.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Predefined sets of fields to return.
          If both `fields` and `fieldsets` are sent in the request,
          the union of both lists is returned.

          - `BASIC`: Returns `id`, `revision`, `primaryInfo`, `info.name`.
          - `COMMUNICATION_DETAILS`: Returns `id`, `revision`, `primaryInfo`, `info.name`, `info.emails`, `info.phones`, `info.addresses`.
          - `EXTENDED`: Returns `id`, `revision`, `primaryInfo`, `info.name`, `info.extendedFields`.
          - `FULL`: Returns all fields.

          Default: If `fields` is omitted from the request, `FULL`.

          Constraints: `maxItems 10`.

          **Supported values:**

          - `BASIC` — name, primaryEmail, primaryPhone
          - `COMMUNICATION_DETAILS` — name, phones, emails, addresses
          - `EXTENDED` — name, primaryInfo(email, phone), extendedFields
          - `FULL` — Full contact object

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="contacts" type="Contact[]">

      List of contacts.

      See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts/contact-v4/contact-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          The number of items returned in this response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          The offset which was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          The total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          A flag that indicates the server failed to calculate 'total' field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_SORT_FIELD` | Unsupported sort field. |
    | `400` | `INVALID_ARGUMENT` | `ILLEGAL_LIMIT` | Illegal paging limit. The limit must be between 1 and 1,000. |
    | `400` | `INVALID_ARGUMENT` | `MALFORMED_FILTER` | Filter couldn't be parsed. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER` | At least 1 of the provided filters isn't supported. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_FIELD_UNKNOWN` | Field doesn't exist and can't be filtered. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_OPERATOR` | Unsupported filter operator for the provided field. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_VALUE` | Unsupported filter value. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_VALUE_TYPE` | Unsupported filter value type. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-query-contacts" id="ex-rest-query-contacts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-query-contacts" id="ex-rest-query-contacts-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-query-contacts" id="ex-rest-query-contacts-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-query-contacts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-query-contacts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query Contacts by Email</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query Contacts by Amount Spent, Sorted by First Name</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Query Contacts by Last Name</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-query-contacts-0"><span className="wix-picker-name">Query Contacts by Email</span></label>
    <label htmlFor="ex-rest-query-contacts-1"><span className="wix-picker-name">Query Contacts by Amount Spent, Sorted by First Name</span></label>
    <label htmlFor="ex-rest-query-contacts-2"><span className="wix-picker-name">Query Contacts by Last Name</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/contacts/v4/contacts/query' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{
          "query": {
              "filter": {"info.emails.email": "email2@example.com"},
              "fieldsets": ["BASIC","COMMUNICATION_DETAILS"]
            }
          }'
      ```

      ```json Response
      {
        "contacts": [
          {
            "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
            "revision": 1,
            "primaryInfo": {
              "email": "email@example.com",
              "phone": "097495550"
            },
            "info": {
              "name": {
                "first": "Anabelle",
                "last": "Smith"
              },
              "emails": {
                "items": [
                  {
                    "id": "d8f0d88c-af92-4237-833f-56aaaa2b5168",
                    "tag": "MAIN",
                    "email": "email@example.com",
                    "primary": true
                  },
                  {
                    "id": "5b430771-e6f1-4bea-9eb5-ad7472270605",
                    "tag": "MAIN",
                    "email": "email2@example.com"
                  }
                ]
              },
              "phones": {
                "items": [
                  {
                    "id": "e5214c79-af80-4eff-87f2-408b0be4e710",
                    "tag": "HOME",
                    "countryCode": "IL",
                    "phone": "097495550",
                    "e164Phone": "+97297495550",
                    "primary": true
                  },
                  {
                    "id": "59323148-ecf2-4bbe-a7eb-c1b01f43845e",
                    "tag": "MOBILE",
                    "countryCode": "UA",
                    "phone": "0443955571",
                    "e164Phone": "+380443955571"
                  }
                ]
              },
              "addresses": {
                "items": [
                  {
                    "id": "3f8bba59-150f-4ee7-9c58-632cbdae222c",
                    "tag": "HOME",
                    "address": {
                      "country": "US",
                      "subdivision": "US-ME",
                      "city": "Richmond",
                      "postalCode": "04357",
                      "addressLine": "1772  Fantages Way",
                      "addressLine2": "Apt 17"
                    }
                  },
                  {
                    "id": "be0b1932-e081-46b0-8dfa-a9f2409fa3a8",
                    "tag": "WORK",
                    "address": {
                      "country": "US",
                      "subdivision": "US-ME",
                      "city": "Richmond",
                      "postalCode": "04357",
                      "streetAddress": {
                        "number": "5",
                        "name": "Wingate"
                      },
                      "addressLine2": "the code to the building is: *1234"
                    }
                  }
                ]
              }
            }
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "offset": 0,
          "total": 1
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/contacts/v4/contacts/query' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{
          "query": {
              "filter":   {
                  "info.extendedFields.ecom.totalSpentAmount": {"$lte": 50.0 }
              },
              "sort": [
                  {
                      "fieldName": "info.name.last",
                      "order" : "ASC"
                  }
              ],
              "fields": ["info.name", "info.extendedFields"],
              "paging": {
                  "limit": 30,
                  "offset": 30
                }
            }
        }'
      ```

      ```json Response
      {
        "contacts": [
          {
            "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
            "revision": 1,
            "info": {
              "name": {
                "first": "Anabelle",
                "last": "Smith"
              },
              "extendedFields": {
                "items": {
                  "custom.color": "Blue",
                  "custom.number": 12,
                  "custom.date": "2020-01-01",
                  "custom.url": "http://domain.com",
                  "members.membershipStatus": "APPROVED",
                  "emailSubscriptions.subscriptionStatus": "SUBSCRIBED",
                  "emailSubscriptions.deliverabilityStatus": "ACTIVE",
                  "emailSubscriptions.effectiveEmail": "email@test.rocks"
                }
              }
            }
          },
          {
            "id": "e50c4534-d0a5-11ea-87d0-0242ac130003",
            "revision": 1,
            "info": {
              "name": {
                "first": "Jane",
                "last": "Smith"
              },
              "extendedFields": {
                "items": {
                  "custom.color": "Green",
                  "custom.number": 1984,
                  "custom.date": "2020-01-01",
                  "custom.url": "http://domain.com",
                  "members.membershipStatus": "APPROVED",
                  "subscriptions.subscriptionStatus": "NOT SET",
                  "subscriptions.effectiveEmail": "email@test.rocks"
                }
              }
            }
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "offset": 30,
          "total": 32
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/contacts/v4/contacts/query' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{
          "query": {
            "filter": {
              "info.name.last": "Smith"
            },
            "fieldsets": ["BASIC"]
          }
        }'
      ```

      ```json Response
      {
        "contacts": [
          {
            "id": "8046df3c-7575-4098-a5ab-c91ad8f33c47",
            "revision": 1,
            "primaryInfo": {
              "email": "email@example.com",
              "phone": "097495550"
            },
            "info": {
              "name": {
                "first": "John",
                "last": "Smith"
              }
            }
          },
          {
            "id": "e50c4534-d0a5-11ea-87d0-0242ac130003",
            "revision": 1,
            "primaryInfo": {
              "email": "email211@example.com",
              "phone": "097499991"
            },
            "info": {
              "name": {
                "first": "Jane",
                "last": "Smith"
              }
            }
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "offset": 0,
          "total": 2
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of contacts, given the provided [paging](https://dev.wix.com/api/rest/getting-started/sorting-and-paging#getting-started_sorting-and-paging_paging), [filtering, and sorting](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/contact-v4/sort-filter-and-search).

    Up to 1,000 contacts can be returned per request.
    Query Contacts runs with these defaults, which you can override:

    - `createdDate` is sorted in `ASC` order
    - `paging.limit` is `50`
    - `paging.offset` is `0`

    For field support for filters and sorting, see
    [Contacts: Supported Filters and Sorting](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/contact-v4/sort-filter-and-search).

    To learn about working with _Query_ methods, see
    [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language),
    [Sorting and Paging](https://dev.wix.com/docs/rest/articles/getting-started/sorting-and-paging),
    and [Field Projection](https://dev.wix.com/docs/rest/articles/getting-started/field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `info.locale` | `$eq`, `$ne`, `$in`, `$exists` | None |
    | `_createdDate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sort: `ASC`, `DESC` |
    | `lastActivity.activityDate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sort: `ASC`, `DESC` |
    | `info.birthdate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | Sort: `ASC`, `DESC` |
    | `_updatedDate` | `$eq`, `$ne`, `$gt`, `$lt`, `$gte`, `$lte` | None |
    | `primaryInfo.email` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.name.first` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.name.last` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.company` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `info.jobTitle` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `primaryInfo.phone` | `$eq`, `$ne`, `$in`, `$exists`, `$startsWith` | None |
    | `info.labelKeys` | `$hasSome`, `$hasAll` | None |

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CONTACTS.READ-CONTACTS">Read Contacts</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { contacts } from '@wix/crm';

    contacts.queryContacts(query, options): ContactsQueryBuilder
    ```

    ## Method Parameters

    <ParamField body="query" type="ContactQuery" required>

      Query options.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.
          See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language)
          for more information.

          For a detailed list of supported filters, see
          [Supported Filters](https://dev.wix.com/api/rest/contacts/contacts/supported-filters).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sorting options.
          See [Sorting and Paging](https://dev.wix.com/api/rest/getting-started/pagination)
          for more information.

          Max: 1 sort object

          Constraints: `maxItems 1`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Field to sort by.

              Supported properties:
              `createdDate`, `lastActivity.activityDate`, `primaryInfo.email`, `info.name.first`, `info.name.last`, `info.company`,
              `info.jobTitle`, `info.birthdate`

              Constraints: `maxLength 100`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.
              Use `ASC` for ascending order or `DESC` for descending order.

              Defaults to `ASC`.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paging" type="Paging">

          Pagination options.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Number of items to return. Learn more about pagination in [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#paging).

              Default: `50`. &lt;br>
              Maximum: `1000`.

            </ParamField>

            <ParamField body="offset" type="integer">

              Number of items to skip in the current sort order.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QueryContactsOptions">

      Query contact options.

      <Expandable title="options properties">

        <ParamField body="search" type="string">

          Plain text search for an exact match.

          Supported properties:
          `info.name.first`, `info.name.last`, `info.emails.email`, `info.phones.phone`

          Max: 100 characters

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `ContactsQueryBuilder`.

    <ResponseField name="contacts" type="Contact[]">

      List of contacts.

      See the [Contact Object](/api-reference/crm/members-contacts/contacts/contacts/contact-v4/contact-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadata">

      Details on the paged set of results returned.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          The number of items returned in this response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          The offset which was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          The total number of items that match the query.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          A flag that indicates the server failed to calculate 'total' field.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_SORT_FIELD` | Unsupported sort field. |
    | `400` | `INVALID_ARGUMENT` | `ILLEGAL_LIMIT` | Illegal paging limit. The limit must be between 1 and 1,000. |
    | `400` | `INVALID_ARGUMENT` | `MALFORMED_FILTER` | Filter couldn't be parsed. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER` | At least 1 of the provided filters isn't supported. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_FIELD_UNKNOWN` | Field doesn't exist and can't be filtered. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_OPERATOR` | Unsupported filter operator for the provided field. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_VALUE` | Unsupported filter value. |
    | `400` | `INVALID_ARGUMENT` | `UNSUPPORTED_FILTER_VALUE_TYPE` | Unsupported filter value type. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-contacts" id="ex-sdk-query-contacts-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-contacts" id="ex-sdk-query-contacts-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-query-contacts" id="ex-sdk-query-contacts-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-contacts-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-contacts-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">queryContacts</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryContacts (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-contacts-0"><span className="wix-picker-name">queryContacts</span></label>
    <label htmlFor="ex-sdk-query-contacts-1"><span className="wix-picker-name">queryContacts (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-query-contacts-2"><span className="wix-picker-name">queryContacts (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript queryContacts
    import { contacts } from '@wix/crm';

    async function queryContacts(query,options) {
      const response = await contacts.queryContacts(query,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryContacts (self-hosted)
    import { createClient } from '@wix/sdk';
    import { contacts } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { contacts },
      // Include the auth strategy and host as relevant
    });

    async function queryContacts(query,options) {
      const response = await myWixClient.contacts.queryContacts(query,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryContacts (with elevated permissions)
    import { contacts } from '@wix/crm';
    import { auth } from '@wix/essentials';

    async function myQueryContactsMethod(query,options) {
      const elevatedQueryContacts = auth.elevate(contacts.queryContacts);
      const response = await elevatedQueryContacts(query,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
