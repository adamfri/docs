---
title: "onBadgeAssignmentDeleted"
description: "Triggered when a badge assignment is deleted."
icon: "bolt"
---

Triggered when a badge assignment is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v4.badge_assignment_deleted` |
| Package | `@wix/members` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Manage Stores`, `Manage Events`, `Manage Online Programs`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeAssignmentDeleted
  import { badgeAssignments } from '@wix/members';

  badgeAssignments.onBadgeAssignmentDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeAssignmentDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badgeAssignments } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        badgeAssignments,
      },
  });

  wixClient.badgeAssignments.onBadgeAssignmentDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
