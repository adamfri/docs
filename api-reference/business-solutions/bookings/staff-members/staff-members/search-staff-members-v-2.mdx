---
title: "Search Staff Members V 2"
description: "Retrieves a list of staff members that match the provided search terms, given the provided sorting and paging."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of staff members that match the provided search terms, given the provided
    sorting and paging.

    Returns all matching staff members, including both service providers and staff members who
    don't provide services. To retrieve only service providers, filter by `serviceProvider`.

    Matches the search expression against the staff member's `name`.

    ### Defaults

    Search Staff Members V2 has the following default settings, which you can override:
    + Sorted by `id` in ascending order.
    + `cursorPaging.limit` set to `100`.

    ### See also

    For the fields you can filter and sort by, see [Filtering and Sorting](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/staff-members/filtering-and-sorting).

    To learn about working with Search methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language) and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RENTALS.MANAGE">Manage_Wix_Rentals_App_Forms_submissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/bookings/v2/staff-members/search
    ```

    ## Body Params

    <ParamField body="search" type="CursorSearch">

      Search criteria including filter, sort, and paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object for narrowing search results. For example, to return only staff members with specific email domains: `"filter": {"email": {"$contains": "@company.com"}}`.

          Learn more about the filter format in the [supported filters article](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/filtering-and-sorting).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Array of sort objects specifying result order. For example, to sort by creation date in descending order: `"sort": [{"fieldName": "createdDate", "order": "DESC"}]`.

          Learn more about the sort format in the [supported filters article](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/filtering-and-sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least 1 of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `resource.workingHoursSchedules.shared`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor-based paging for result navigation. Can't be used together with 'paging'.
              `cursor_paging.cursor` can't be used together with `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Conditional fields to return in the response.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `RESOURCE_DETAILS` — Includes `resource` in the response.
      - `ASSOCIATED_IDENTITY_STATUS` — Includes `associatedIdentityStatus` in the response.

    </ParamField>

    ## Response Object

    <ResponseField name="staffMembers" type="StaffMember[]">

      Retrieved staff members that match the search criteria specified in the request.

      Each staff member includes their profile information such as name, email, phone number,
      working hours schedule, connected user details, and any conditional fields requested
      (resource details, conferencing providers, or identity status).

      See the [Staff Member Object](/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor-based paging metadata for navigating through search results.

      Contains pagination details including current cursor position, availability of additional
      results, and item counts. Use the `next` cursor to retrieve subsequent result pages.
      Note: Staff members don't support aggregations, so only cursor and count information is provided.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current response page.

          This count reflects the actual number of items in the current result set,
          which may be less than the requested limit if fewer items are available.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Navigation cursors for moving between result pages.

          Contains `next` and `prev` cursor tokens for pagination. Use the `next` cursor
          to retrieve subsequent pages and `prev` cursor to go back to previous pages.
          Learn more about cursor paging in the [API Query Language guide](https://dev.wix.com/api/rest/getting-started/api-query-language#getting-started_api-query-language_cursor-paging).

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor token for retrieving the next page of results.

              Use this token in subsequent requests to continue pagination forward.
              Value is `null` when on the last page of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor token for retrieving the previous page of results.

              Use this token to navigate backwards through result pages.
              Value is `null` when on the first page of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates whether additional results are available beyond the current page.

          - `true`: More results exist and can be retrieved using the `next` cursor.
          - `false`: This is the final page of results.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data results (currently reserved for future use).

      Staff member searches don't currently support aggregations. This field is reserved
      for potential future aggregation capabilities such as grouping by working hours,
      location assignments, or user connection status.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Array of aggregation results, each containing the aggregation metadata and its calculated values for the specified search criteria.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match specified kind as derived from search request.

              **Supported values:**

              - `UNKNOWN_AGGREGATION_TYPE`
              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into 1 key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Search all staff members, including staff members who are not service providers, matching a search term against the name.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/bookings/v2/staff-members/search' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "search": {
          "search": {
            "fields": [
              "name"
            ],
            "expression": "Front-Desk"
          },
          "cursorPaging": {
            "limit": 100
          }
        }
      }'
      ```

      ```json Response
      {
        "staffMembers": [
          {
            "id": "7c147d9e-89a8-4e2e-a723-b095db0610c1",
            "name": "Sam Front-Desk",
            "resourceId": "e0936563-7fd8-4e08-9db0-a327b2cb653e",
            "serviceProvider": false,
            "revision": "1",
            "createdDate": "2024-09-02T09:14:05.120Z",
            "updatedDate": "2024-09-02T09:14:05.120Z"
          }
        ],
        "pagingMetadata": {
          "count": 1,
          "cursors": {
            "next": null,
            "prev": null
          },
          "hasNext": false
        },
        "aggregationData": null
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of staff members that match the provided search terms, given the provided
    sorting and paging.

    Returns all matching staff members, including both service providers and staff members who
    don't provide services. To retrieve only service providers, filter by `serviceProvider`.

    Matches the search expression against the staff member's `name`.

    ### Defaults

    Search Staff Members V2 has the following default settings, which you can override:
    + Sorted by `id` in ascending order.
    + `cursorPaging.limit` set to `100`.

    ### See also

    For the fields you can filter and sort by, see [Filtering and Sorting](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/staff-members/filtering-and-sorting).

    To learn about working with Search methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language) and [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging).

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RENTALS.MANAGE">Manage_Wix_Rentals_App_Forms_submissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `MEMBER`, `APP`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { staffMembers } from '@wix/bookings';

    staffMembers.searchStaffMembersV2(search, options): Promise
    ```

    ## Method Parameters

    <ParamField body="search" type="CursorSearch" required>

      Search criteria including filter, sort, and paging options.

      <Expandable title="search properties">

        <ParamField body="filter" type="object">

          Filter object for narrowing search results. For example, to return only staff members with specific email domains: `"filter": {"email": {"$contains": "@company.com"}}`.

          Learn more about the filter format in the [supported filters article](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/filtering-and-sorting).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Array of sort objects specifying result order. For example, to sort by creation date in descending order: `"sort": [{"fieldName": "createdDate", "order": "DESC"}]`.

          Learn more about the sort format in the [supported filters article](https://dev.wix.com/docs/api-reference/business-solutions/bookings/staff-members/filtering-and-sorting).

          Constraints: `maxItems 10`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="search" type="SearchDetails">

          Free text to match in searchable fields.

          <Expandable title="search properties">

            <ParamField body="mode" type="string">

              Search mode. Defines the search logic for combining multiple terms in the `expression`.

              **Supported values:**

              - `OR` — At least 1 of the search terms must be present.
              - `AND` — All search terms must be present.

            </ParamField>

            <ParamField body="expression" type="string">

              Search term or expression.

            </ParamField>

            <ParamField body="fields" type="string[]">

              Fields to search in. If the array is empty, all searchable fields are searched. Use dot notation to specify a JSON path. For example, `resource.workingHoursSchedules.shared`.

              Constraints: `maxLength 200`, `maxItems 20`.

            </ParamField>

            <ParamField body="fuzzy" type="boolean">

              Whether to enable the search function to use an algorithm to automatically find results that are close to the search expression, such as typos and declensions.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor-based paging for result navigation. Can't be used together with 'paging'.
              `cursor_paging.cursor` can't be used together with `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="SearchStaffMembersV2Options">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Conditional fields to return in the response.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `RESOURCE_DETAILS` — Includes `resource` in the response.
          - `ASSOCIATED_IDENTITY_STATUS` — Includes `associatedIdentityStatus` in the response.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="staffMembers" type="StaffMember[]">

      Retrieved staff members that match the search criteria specified in the request.

      Each staff member includes their profile information such as name, email, phone number,
      working hours schedule, connected user details, and any conditional fields requested
      (resource details, conferencing providers, or identity status).

      See the [Staff Member Object](/api-reference/business-solutions/bookings/staff-members/staff-members/staff-member-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Cursor-based paging metadata for navigating through search results.

      Contains pagination details including current cursor position, availability of additional
      results, and item counts. Use the `next` cursor to retrieve subsequent result pages.
      Note: Staff members don't support aggregations, so only cursor and count information is provided.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the current response page.

          This count reflects the actual number of items in the current result set,
          which may be less than the requested limit if fewer items are available.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Navigation cursors for moving between result pages.

          Contains `next` and `prev` cursor tokens for pagination. Use the `next` cursor
          to retrieve subsequent pages and `prev` cursor to go back to previous pages.
          Learn more about cursor paging in the [API Query Language guide](https://dev.wix.com/api/rest/getting-started/api-query-language#getting-started_api-query-language_cursor-paging).

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor token for retrieving the next page of results.

              Use this token in subsequent requests to continue pagination forward.
              Value is `null` when on the last page of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor token for retrieving the previous page of results.

              Use this token to navigate backwards through result pages.
              Value is `null` when on the first page of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Indicates whether additional results are available beyond the current page.

          - `true`: More results exist and can be retrieved using the `next` cursor.
          - `false`: This is the final page of results.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="aggregationData" type="AggregationData">

      Aggregation data results (currently reserved for future use).

      Staff member searches don't currently support aggregations. This field is reserved
      for potential future aggregation capabilities such as grouping by working hours,
      location assignments, or user connection status.

      <Expandable title="aggregationData properties">

        <ResponseField name="results" type="AggregationResults[]">

          Array of aggregation results, each containing the aggregation metadata and its calculated values for the specified search criteria.

          Constraints: `maxItems 10000`.

          <Expandable title="results properties">

            <ResponseField name="name" type="string">

              User-defined name of aggregation as derived from search request.

              Constraints: `maxLength 100`.

            </ResponseField>

            <ResponseField name="type" type="string">

              Type of aggregation that must match specified kind as derived from search request.

              **Supported values:**

              - `UNKNOWN_AGGREGATION_TYPE`
              - `VALUE` — Calculates the distribution of a specific field's values within a dataset, providing insights into the overall distribution and key statistics of those values.
              - `RANGE` — Calculates the count of the values from the specified field in the dataset that fall within the range of each bucket you define.
              - `SCALAR` — Calculates a single numerical value from a dataset, summarizing the dataset into 1 key metric: `COUNT_DISTINCT`, `SUM`, `AVG`, `MIN`, or `MAX`.

            </ResponseField>

            <ResponseField name="fieldPath" type="string">

              Field to aggregate by as derived from search request.

              Constraints: `maxLength 200`.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-search-staff-members-v-2" id="ex-sdk-search-staff-members-v-2-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-search-staff-members-v-2" id="ex-sdk-search-staff-members-v-2-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-search-staff-members-v-2-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-search-staff-members-v-2-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Search all staff members</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">searchStaffMembersV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-search-staff-members-v-2-0"><span className="wix-picker-name">Search all staff members</span></label>
    <label htmlFor="ex-sdk-search-staff-members-v-2-1"><span className="wix-picker-name">searchStaffMembersV2 (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Search all staff members
    import { staffMembers } from "@wix/bookings";

    async function searchStaffMembersV2() {
      const response = await staffMembers.searchStaffMembersV2({
        search: {
          fields: ["name"],
          expression: "Front-Desk",
        },
        cursorPaging: {
          limit: 100,
        },
      });
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript searchStaffMembersV2 (self-hosted)
    import { createClient } from '@wix/sdk';
    import { staffMembers } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { staffMembers },
      // Include the auth strategy and host as relevant
    });

    async function searchStaffMembersV2(search,options) {
      const response = await myWixClient.staffMembers.searchStaffMembersV2(search,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
