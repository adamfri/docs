---
title: "List Booking Fees By Booking Ids"
description: "Retrieves booking fees by booking IDs."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves booking fees by booking IDs.

    Instead of returning `bookingFee` objects with the `cancellationFee.price.value`
    set to `0.00`, the method doesn't return a `bookingFee` object for the relevant
    booking IDs. For example, no `bookingFee` object is returned if the canceled
    booking was free or if the booking was canceled before the start of the earliest
    cancellation window with an associated fee.

    If the service's booking policy has been updated since the booking was created,
    booking fees are calculated according to the [booking policy snapshot](https://dev.wix.com/docs/rest/business-solutions/bookings/policies/booking-policy-snapshots/introduction)

    rather than the current version of the [policy](https://dev.wix.com/docs/rest/business-solutions/bookings/policies/booking-policies/introduction).

    This method calculates the cancellation fee amount based on the time of the
    call, you can't specify a time. Similarly, it calculates the cancellation fee
    based on the number of participants who canceled, not a provided number.

    A `cancellationFee.status` of `PREVIEW` indicates that the booking fee is
    informational only; the customer isn't required to pay it. When the `status` is
    set to `UNKNOWN_STATUS` there is no eCommerce order associated with the booking. For
    example, if a custom checkout was used for the booking instead of the
    [eCommerce checkout](https://dev.wix.com/docs/rest/business-solutions/e-commerce/checkout/introduction).
    If multiple events would trigger the calculation of a booking fee, for example
    when a booking is first canceled and then marked as not attended, Wix calculates
    the booking fee based on the first trigger. In this example, the booking
    cancellation.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-SENSITIVE">Read Bookings - Including Participants</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/_api/booking-fees/v1/booking-fees
    ```

    ## Query Params

    <ParamField query="bookingIds" type="string[]">

      IDs of the bookings to retrieve booking fees for.

      Constraints: `format GUID`, `maxItems 5`.

    </ParamField>

    <ParamField query="multiServiceBookingIds" type="string[]">

      IDs of the multi service bookings to retrieve booking fees for.

      Constraints: `format GUID`, `maxItems 1`.

    </ParamField>

    ## Response Object

    <ResponseField name="bookingFees" type="BookingFee[]">

      List of retrieved booking fees.

      See the [Booking Fee Object](/api-reference/business-solutions/bookings/pricing/booking-fees/booking-fee-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `MISSING_BOOKING_IDS` | No booking IDs were specified. Provide at least 1 booking ID. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-list-booking-fees-by-booking-ids" id="ex-rest-list-booking-fees-by-booking-ids-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-list-booking-fees-by-booking-ids" id="ex-rest-list-booking-fees-by-booking-ids-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-list-booking-fees-by-booking-ids-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-list-booking-fees-by-booking-ids-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List booking fees by multi service booking ID</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">List booking fees by booking ID</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-list-booking-fees-by-booking-ids-0"><span className="wix-picker-name">List booking fees by multi service booking ID</span></label>
    <label htmlFor="ex-rest-list-booking-fees-by-booking-ids-1"><span className="wix-picker-name">List booking fees by booking ID</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/_api/booking-fees/v1/booking-fees?multiServiceBookingIds=9ebf44e5-9000-4b27-8b42-ffa77fa42ad5' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "bookingFees": [
          {
            "id": "a717b687-aaa8-4e35-85ba-adbd88bbadd8",
            "bookingId": "a717b687-aaa8-4e35-85ba-adbd88bbadd8",
            "cancellationFee": {
              "price": {
                "value": "5.0",
                "currency": "USD"
              },
              "status": "NOT_YET_APPLIED_TO_ORDER",
              "trigger": "NOT_ATTENDED"
            },
            "policyDetails": {
              "id": "295e2750-852f-4c52-9693-fc6c0d0c587b",
              "description": "This is our policy"
            }
          },
          {
            "id": "ff04b4b3-b386-4db1-8990-60c5ec6f4229",
            "bookingId": "ff04b4b3-b386-4db1-8990-60c5ec6f4229",
            "cancellationFee": {
              "price": {
                "value": "5.0",
                "currency": "USD"
              },
              "status": "NOT_YET_APPLIED_TO_ORDER",
              "trigger": "NOT_ATTENDED"
            },
            "policyDetails": {
              "id": "295e2750-852f-4c52-9693-fc6c0d0c587b",
              "description": "This is our policy"
            }
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X GET \
      'https://www.wixapis.com/_api/booking-fees/v1/booking-fees?bookingIds=b5d05e4b-4e75-4fa8-a8de-1827682ff03c' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "bookingFees": [
          {
            "id": "b5d05e4b-4e75-4fa8-a8de-1827682ff03c",
            "bookingId": "b5d05e4b-4e75-4fa8-a8de-1827682ff03c",
            "cancellationFee": {
              "price": {
                "value": "5.0",
                "currency": "USD"
              },
              "status": "NOT_YET_APPLIED_TO_ORDER",
              "trigger": "NOT_ATTENDED"
            },
            "policyDetails": {
              "id": "295e2750-852f-4c52-9693-fc6c0d0c587b",
              "description": "This is our policy"
            }
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves booking fees by booking IDs.

    Instead of returning `bookingFee` objects with the `cancellationFee.price.value`
    set to `0.00`, the method doesn't return a `bookingFee` object for the relevant
    booking IDs. For example, no `bookingFee` object is returned if the canceled
    booking was free or if the booking was canceled before the start of the earliest
    cancellation window with an associated fee.

    If the service's booking policy has been updated since the booking was created,
    booking fees are calculated according to the [booking policy snapshot](https://dev.wix.com/docs/rest/business-solutions/bookings/policies/booking-policy-snapshots/introduction)

    rather than the current version of the [policy](https://dev.wix.com/docs/rest/business-solutions/bookings/policies/booking-policies/introduction).

    This method calculates the cancellation fee amount based on the time of the
    call, you can't specify a time. Similarly, it calculates the cancellation fee
    based on the number of participants who canceled, not a provided number.

    A `cancellationFee.status` of `PREVIEW` indicates that the booking fee is
    informational only; the customer isn't required to pay it. When the `status` is
    set to `UNKNOWN_STATUS` there is no eCommerce order associated with the booking. For
    example, if a custom checkout was used for the booking instead of the
    [eCommerce checkout](https://dev.wix.com/docs/rest/business-solutions/e-commerce/checkout/introduction).
    If multiple events would trigger the calculation of a booking fee, for example
    when a booking is first canceled and then marked as not attended, Wix calculates
    the booking fee based on the first trigger. In this example, the booking
    cancellation.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.READ-BOOKINGS-SENSITIVE">Read Bookings - Including Participants</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { bookingFees } from '@wix/bookings';

    bookingFees.listBookingFeesByBookingIds(options): Promise<ListBookingFeesByBookingIdsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListBookingFeesByBookingIdsOptions">

      Options to use when listing booking fees.

      <Expandable title="options properties">

        <ParamField body="bookingIds" type="string[]">

          IDs of the bookings to retrieve booking fees for.

          Constraints: `format GUID`, `maxItems 5`.

        </ParamField>

        <ParamField body="multiServiceBookingIds" type="string[]">

          IDs of the multi service bookings to retrieve booking fees for.

          Constraints: `format GUID`, `maxItems 1`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListBookingFeesByBookingIdsResponse>`.

    <ResponseField name="bookingFees" type="BookingFee[]">

      List of retrieved booking fees.

      See the [Booking Fee Object](/api-reference/business-solutions/bookings/pricing/booking-fees/booking-fee-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `MISSING_BOOKING_IDS` | No booking IDs were specified. Provide at least 1 booking ID. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-booking-fees-by-booking-ids" id="ex-sdk-list-booking-fees-by-booking-ids-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-booking-fees-by-booking-ids" id="ex-sdk-list-booking-fees-by-booking-ids-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-booking-fees-by-booking-ids-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-booking-fees-by-booking-ids-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List booking fees by booking IDs</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listBookingFeesByBookingIds (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-booking-fees-by-booking-ids-0"><span className="wix-picker-name">List booking fees by booking IDs</span></label>
    <label htmlFor="ex-sdk-list-booking-fees-by-booking-ids-1"><span className="wix-picker-name">listBookingFeesByBookingIds (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List booking fees by booking IDs
    import { bookingFees } from "@wix/bookings";

    async function listBookingFees() {
      const response = await bookingFees.listBookingFeesByBookingIds({
        bookingIds: ["b4ca5322-13ff-4bb5-9af7-7f70a9d3f1bc"],
      });
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listBookingFeesByBookingIds (self-hosted)
    import { createClient } from '@wix/sdk';
    import { bookingFees } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { bookingFees },
      // Include the auth strategy and host as relevant
    });

    async function listBookingFeesByBookingIds(options) {
      const response = await myWixClient.bookingFees.listBookingFeesByBookingIds(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
