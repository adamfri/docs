---
title: "onJoinGroupRequestApproved"
description: "Triggered when a join group request is approved."
icon: "bolt"
---

Triggered when a join group request is approved.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.join_group_request_approved` |
| Package | `@wix/groups` |
| Permissions | `Manage Social Groups` |

## Examples

<CodeGroup>

  ```javascript onJoinGroupRequestApproved
  import { joinGroupRequests } from '@wix/groups';

  joinGroupRequests.onJoinGroupRequestApproved((event) => {
      // handle your event here
  });
  ```

  ```javascript onJoinGroupRequestApproved (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { joinGroupRequests } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        joinGroupRequests,
      },
  });

  wixClient.joinGroupRequests.onJoinGroupRequestApproved((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
