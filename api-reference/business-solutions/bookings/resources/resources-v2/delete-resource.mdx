---
title: "Delete Resource"
description: "Deletes a resource."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a resource.

    Deleting a resource cancels its event schedule and all its working hour
    schedules that aren't shared with another resource. Learn more about
    [how Bookings uses the Calendar APIs](https://dev.wix.com/docs/api-reference/business-management/calendar/wix-bookings-integration).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/bookings/v2/resources/{resourceId}
    ```

    ## Path Params

    <ParamField path="resourceId" type="string" required>

      ID of the resource to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteResourceResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Resource Deleted | `wix.bookings.resources.v2.resource_deleted` | Triggered when a resource is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/bookings/v2/resources/ffedf9dc-932d-439f-b1f4-e42b2a5a8bb5' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a resource.

    Deleting a resource cancels its event schedule and all its working hour
    schedules that aren't shared with another resource. Learn more about
    [how Bookings uses the Calendar APIs](https://dev.wix.com/docs/api-reference/business-management/calendar/wix-bookings-integration).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-BOOKINGS.MANAGE-BOOKINGS">Manage Bookings</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { resources } from '@wix/bookings';

    resources.deleteResource(resourceId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="resourceId" type="string" required>

      ID of the resource to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Resource Deleted | `wix.bookings.resources.v2.resource_deleted` | Triggered when a resource is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-resource" id="ex-sdk-delete-resource-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-resource" id="ex-sdk-delete-resource-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-resource" id="ex-sdk-delete-resource-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-resource-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-resource-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteResource</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteResource (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteResource (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-resource-0"><span className="wix-picker-name">deleteResource</span></label>
    <label htmlFor="ex-sdk-delete-resource-1"><span className="wix-picker-name">deleteResource (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-resource-2"><span className="wix-picker-name">deleteResource (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteResource
    import { resources } from '@wix/bookings';

    async function deleteResource(resourceId) {
      const response = await resources.deleteResource(resourceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteResource (self-hosted)
    import { createClient } from '@wix/sdk';
    import { resources } from '@wix/bookings';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { resources },
      // Include the auth strategy and host as relevant
    });

    async function deleteResource(resourceId) {
      const response = await myWixClient.resources.deleteResource(resourceId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteResource (with elevated permissions)
    import { resources } from '@wix/bookings';
    import { auth } from '@wix/essentials';

    async function myDeleteResourceMethod(resourceId) {
      const elevatedDeleteResource = auth.elevate(resources.deleteResource);
      const response = await elevatedDeleteResource(resourceId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
