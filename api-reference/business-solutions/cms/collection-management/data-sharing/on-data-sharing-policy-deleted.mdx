---
title: "onDataSharingPolicyDeleted"
description: "Triggered when a data sharing policy is deleted."
icon: "bolt"
---

Triggered when a data sharing policy is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.data.v1.data_sharing_policy_deleted` |
| Package | `@wix/data` |
| Permissions | `Manage Data Collection Sharing` |

## Examples

<CodeGroup>

  ```javascript onDataSharingPolicyDeleted
  import { sharing } from '@wix/data';

  sharing.onDataSharingPolicyDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onDataSharingPolicyDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { sharing } from '@wix/data';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        sharing,
      },
  });

  wixClient.sharing.onDataSharingPolicyDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
