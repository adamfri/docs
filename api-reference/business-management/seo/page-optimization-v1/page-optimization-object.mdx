---
title: "Page Optimization Object"
description: "A whole-page set of SEO optimization suggestions, generated for one page and focus keyword."
icon: "cube"
---

A whole-page set of SEO optimization suggestions, generated for one page
and focus keyword.

Each suggestion is a before/after pair: `before` is the page's current
text (empty when the page has none, such as a missing meta description),
and `after` is the suggested replacement. Locate the element to change by
its `before` text. Suggestions are stored per page, so a later generation
for the same page replaces this set.

<Tabs>

  <Tab title="REST">

    <ResponseField name="id" type="string">

      ID of this suggestion set.

      **Read-only.**

    </ResponseField>

    <ResponseField name="metaTitle" type="TextSuggestion">

      Suggested title tag.

      **Read-only.**

      <Expandable title="metaTitle properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="metaDescription" type="TextSuggestion">

      Suggested description tag.

      **Read-only.**

      <Expandable title="metaDescription properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="h1" type="TextSuggestion">

      Suggested H1 heading.

      **Read-only.**

      <Expandable title="h1 properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="h2OrH3" type="TextSuggestion">

      Suggested subheading, for an H2 or H3 element of the page.

      **Read-only.**

      <Expandable title="h2OrH3 properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="content" type="TextSuggestion[]">

      Suggested rewrites of the page's body text, one per text element.

      **Read-only.** Constraints: `maxItems 100`.

      <Expandable title="content properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Example

    <CodeGroup>

      ```json Object
      {
        "pageOptimization": {
          "id": "3f9c1b2e-8a4d-4c6e-9b7a-2d5e8f1c4a6b",
          "metaTitle": {
            "before": "Services | Portland Pottery Studio",
            "after": "Pottery Classes in Portland | Portland Pottery Studio"
          },
          "metaDescription": {
            "before": "",
            "after": "Join hands-on pottery classes in Portland for beginners and experienced makers. Wheel-throwing, hand-building, and glazing workshops every week."
          },
          "h1": {
            "before": "Our services",
            "after": "Pottery classes in Portland for every skill level"
          },
          "h2OrH3": {
            "before": "Wheel-throwing classes",
            "after": "Beginner and advanced wheel-throwing classes"
          },
          "content": [
            {
              "before": "We offer classes for all levels.",
              "after": "Our Portland pottery classes welcome complete beginners and experienced potters alike, with small groups and all materials included."
            }
          ]
        }
      }
      ```

    </CodeGroup>

  </Tab>

  <Tab title="SDK">

    <ResponseField name="_id" type="string">

      ID of this suggestion set.

      **Read-only.**

    </ResponseField>

    <ResponseField name="metaTitle" type="TextSuggestion">

      Suggested title tag.

      **Read-only.**

      <Expandable title="metaTitle properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="metaDescription" type="TextSuggestion">

      Suggested description tag.

      **Read-only.**

      <Expandable title="metaDescription properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="h1" type="TextSuggestion">

      Suggested H1 heading.

      **Read-only.**

      <Expandable title="h1 properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="h2OrH3" type="TextSuggestion">

      Suggested subheading, for an H2 or H3 element of the page.

      **Read-only.**

      <Expandable title="h2OrH3 properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="content" type="TextSuggestion[]">

      Suggested rewrites of the page's body text, one per text element.

      **Read-only.** Constraints: `maxItems 100`.

      <Expandable title="content properties">

        <ResponseField name="before" type="string">

          The page's current text. Empty when the page has no text in this slot,
          for example, a missing meta description.

          **Read-only.**

        </ResponseField>

        <ResponseField name="after" type="string">

          The suggested text.

          **Read-only.** Constraints: `maxLength 1000`.

        </ResponseField>

      </Expandable>

    </ResponseField>

  </Tab>

</Tabs>
