---
title: "onPostUnreacted"
description: "An existing post was unreacted."
icon: "bolt"
---

An existing post was unreacted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.forum.post_unreacted` |
| Package | `@wix/forum` |
| Permissions | `Read Forums`, `Manage Forums` |

## Examples

<CodeGroup>

  ```javascript onPostUnreacted
  import { posts } from '@wix/forum';

  posts.onPostUnreacted((event) => {
      // handle your event here
  });
  ```

  ```javascript onPostUnreacted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { posts } from '@wix/forum';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        posts,
      },
  });

  wixClient.posts.onPostUnreacted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
