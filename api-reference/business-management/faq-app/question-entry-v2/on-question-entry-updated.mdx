---
title: "onQuestionEntryUpdated"
description: "Triggered when a question entry is updated."
icon: "bolt"
---

Triggered when a question entry is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.faq.question_entry.v2.question_entry_updated` |
| Package | `@wix/faq` |
| Permissions | `Manage FAQ`, `Read FAQ` |

## Examples

<CodeGroup>

  ```javascript onQuestionEntryUpdated
  import { questionEntry } from '@wix/faq';

  questionEntry.onQuestionEntryUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onQuestionEntryUpdated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { questionEntry } from '@wix/faq';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        questionEntry,
      },
  });

  wixClient.questionEntry.onQuestionEntryUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
