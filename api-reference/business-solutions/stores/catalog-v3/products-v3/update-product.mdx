---
title: "Update Product"
description: "Updates a product."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a product.

    Each time the product is updated, `revision` increments by 1.
    The current `revision` must be passed when updating the product.
    This ensures you're working with the latest product and prevents unintended overwrites.

    <Note>

    + To update array fields like `options`, `modifiers`, `variantsInfo.variants`, and any others, you must pass the entire existing array. Passing only the changed item overwrites the entire array, not just merge the single change.
    + To update `variantsInfo.variants` you must also pass `options` and vice versa. This is because variants and options are mutually dependent and must be aligned.
    + To update existing `variantsInfo.variants`, make sure to provide `variantsInfo.variants.id`. If no ID is passed, a variant is created with a new ID.
    + Updating media per variant isn't supported. Instead, assign media items to product option choices using the `linkedMedia` field. You must use preexisting product media.
    + Partial-update semantics: Update Product supports partial updates with the following rules:
      - At the request level, only `product`, `product.id`, and `product.revision` are required.
      - Other fields shown as required in the schema apply only when their parent field is provided.
      - Top-level fields you omit (for example, `name`, `ribbon`, `brand`, `taxGroupId`) are left unchanged.
      - The full-array overwrite rule applies only to repeated fields (`options`, `modifiers`, `variantsInfo.variants`); top-level scalar and object fields are partially updatable on their own.
      - To assign or change a brand without touching any other field, specify only `id`, `revision`, and `brand.id`. Supplying `brand.name` without `brand.id` is rejected with a `BRAND_MISSING_ID` validation error.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.PRODUCT_WRITE">Product write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/stores/v3/products/{product.id}
    ```

    ## Path Params

    <ParamField path="product.id" type="string" required>

      Product ID.

    </ParamField>

    ## Body Params

    <ParamField body="product" type="Product" required>

      Product to update.

      See the [Product Object](/api-reference/business-solutions/stores/catalog-v3/products-v3/product-object) for this object's fields.

    </ParamField>

    <ParamField body="fields" type="string[]">

      Fields to include in the response.

      Constraints: `maxItems 100`.

      **Supported values:**

      - `URL` — The product's public storefront URL, including both relative path and full URL with base domain.
      - `CURRENCY` — Currency information including the currency code and formatted price amounts for storefront display.
      - `INFO_SECTION` — Returns basic info section metadata including IDs, unique names, and titles. Use with `INFO_SECTION_DESCRIPTION` or `INFO_SECTION_PLAIN_DESCRIPTION` to include content.
      - `MERCHANT_DATA` — Returns merchant-related financial data including cost ranges and variant revenue details like cost, profit, and profit margin.
      Requires the `SCOPE.STORES.PRODUCT_READ_ADMIN` permission scope.
      - `PLAIN_DESCRIPTION` — Provides product description as plain HTML text.
      - `INFO_SECTION_PLAIN_DESCRIPTION` — Returns info section descriptions as HTML content. Use with `INFO_SECTION` for complete metadata.
      - `SUBSCRIPTION_PRICES_INFO` — Details subscription pricing options, empty for non-subscription products.
      - `BREADCRUMBS_INFO` — Returns the category breadcrumb navigation path from root categories down to the product's main category, including category IDs, names, and slugs.
      - `WEIGHT_MEASUREMENT_UNIT_INFO` — Returns the weight measurement unit (KG, LB) for the product.
      - `VARIANT_OPTION_CHOICE_NAMES` — Enriches variant choices with human-readable option and choice names alongside render types.
      - `MEDIA_ITEMS_INFO` — Returns all media items associated with the product, including detailed metadata (URLs, alt-text, dimensions, filenames, file sizes).
      - `DESCRIPTION` — Returns the product description as rich content with complete node structure, metadata, and styling information.
      - `DIRECT_CATEGORIES_INFO` — Provides the IDs and indexes of categories the product is directly assigned to.
      - `ALL_CATEGORIES_INFO` — Provides IDs and indexes for all categories including inherited parent categories.
      - `INFO_SECTION_DESCRIPTION` — Returns info section descriptions as rich content with complete node structure, metadata, and styling information. Use with `INFO_SECTION` for complete metadata.
      - `THUMBNAIL` — Returns an optimized thumbnail image with standard dimensions, suitable for listings or previews.
      - `PRODUCT_CHOICES_MEDIA_REFERENCES` — Returns media references associated with product option and modifier choices instead of the deprecated `linked_media` field.

      When requested, choice objects will populate `media_references` with lightweight `MediaReference` objects containing only media IDs,
      while the `linked_media` field will be returned empty to avoid duplication and reduce response payload size.

      To get full media metadata (dimensions, alt text, filenames, etc.), request `MEDIA_ITEMS_INFO` on the product and correlate by media ID.

      > **Note:** The `media_references` field is available on `ConnectedOptionChoice` and `ConnectedModifierChoice` objects.
      - `PRODUCT_CHOICES_DISPLAY_IMAGE` — Returns display images for product option and modifier choices.
      When requested, choice objects will populate `display_image` with a lightweight image containing only ID, URL, and alt text.
      - `DISCOUNT_INFO` — Returns automatic discount info for product get/query/search responses and populates product-level `discountInfo.discountRuleNames`.

      To receive discounted variant prices in get/query/search responses, request this together with `MIN_PRICE_VARIANT` (returned in `variantSummary.minPriceVariant.price.priceAfterDiscount`).
      Formatted money amounts are returned only when `CURRENCY` is also requested.

    </ParamField>

    ## Response Object

    <ResponseField name="product" type="Product">

      Updated product.

      See the [Product Object](/api-reference/business-solutions/stores/catalog-v3/products-v3/product-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `MISSING_VARIANT_OPTION_CHOICE` | Every variant's choice must reference an existing option in `options.id`. |
    | `428` | `FAILED_PRECONDITION` | `DUPLICATE_VARIANT_OPTION_CHOICE` | Multiple variants have identical choices in `variantsInfo.variants.choices`. |
    | `428` | `FAILED_PRECONDITION` | `DUPLICATE_PRODUCT_OPTIONS` | Multiple options have identical `name` and `optionRenderType`. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_DEFAULT_VARIANT` | Product with multiple variants has a variant with empty `choices`. Only single-variant products can have empty choices. |
    | `400` | `INVALID_ARGUMENT` | `FIELD_MASK_VALUE_NOT_SUPPORTED` | Update field mask includes read-only fields. |
    | `428` | `FAILED_PRECONDITION` | `VARIANTS_ARE_NOT_ALIGNED_WITH_PRODUCT` | Variants aren't aligned with the product. The variant `typedProperties` must match the `productType`, measurement units must align, and each variant must have exactly 1 choice for every option. |
    | `409` | `ALREADY_EXISTS` | `DUPLICATE_SLUG_ERROR` | There is already a product with this `slug`. |
    | `404` | `NOT_FOUND` | `DIGITAL_FILE_NOT_FOUND_ERROR` | Couldn't find the digital file referenced in `variantsInfo.variants.digitalProperties`. |
    | `409` | `ALREADY_EXISTS` | `BARCODE_ALREADY_EXIST_ERROR` | There is already a variant with this `barcode`. |
    | `428` | `FAILED_PRECONDITION` | `DUPLICATE_BARCODE_ERROR` | Multiple variants have identical `barcode` in the request. |
    | `409` | `ALREADY_EXISTS` | `SKU_ALREADY_EXIST_ERROR` | There is already a variant with this `sku`. |
    | `409` | `ALREADY_EXISTS` | `VARIANT_ID_ALREADY_EXISTS` | Multiple variants have identical `id`. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_PRODUCT_TYPE_FOR_SUBSCRIPTION` | Subscription added to a digital product. Digital products can't have subscriptions. |
    | `428` | `FAILED_PRECONDITION` | `ERROR_ALL_SUBSCRIPTIONS_HIDDEN_FOR_SUBSCRIPTION_ONLY_PRODUCT` | All subscriptions are hidden for a subscription-only product. At least 1 subscription must be visible when `allowOneTimePurchases` is false. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_SUBSCRIPTION_DISCOUNT_AMOUNT` | Subscription discount `amountOff` exceeds the minimum variant price. |
    | `409` | `ALREADY_EXISTS` | `SUBSCRIPTION_ID_ALREADY_EXISTS` | Multiple subscriptions have identical IDs. |
    | `428` | `FAILED_PRECONDITION` | `PREORDER_VALIDATION_ERROR` | Variant enabled for preorder on a subscription product. Products with subscriptions can't have preorder variants. |
    | `500` | `INTERNAL` | `BRAND_CREATION_FAILED` | Brand creation failed. |
    | `404` | `NOT_FOUND` | `RIBBON_NOT_FOUND` | Couldn't find the ribbon. |
    | `400` | `INVALID_ARGUMENT` | `INFO_SECTION_ERROR_OCCURRED` | Info section update failed. |
    | `404` | `NOT_FOUND` | `CUSTOMIZATION_NOT_EXIST` | Couldn't find the customization. |
    | `500` | `INTERNAL` | `ADD_CUSTOMIZATION_CHOICES_FAILED` | Failed to add customization choices. |
    | `404` | `NOT_FOUND` | `CHOICE_NOT_EXIST` | Couldn't find the choice. |
    | `400` | `INVALID_ARGUMENT` | `FIELD_MASK_WITH_FIELD_NOT_PASSED_ERROR` | Field mask includes fields not provided in the request. |
    | `428` | `FAILED_PRECONDITION` | `COMPARE_AT_PRICE_LOWER_THAN_ACTUAL_PRICE` | The `compareAtPrice` is lower than the `actualPrice`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_ADDITIONAL` | Same ribbon appears more than once in `additionalRibbons`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_PRIMARY_AND_ADDITIONAL` | Same ribbon used as both primary ribbon and additional ribbon. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SLUG_FORMAT` | The `slug` provided is not a valid URL slug. |
    | `400` | `INVALID_ARGUMENT` | `VARIANTS_TYPED_PROPERTIES_ARE_NOT_ALIGNED` | Variants have different `typedProperties` types. All variants must have the same type. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_OPTIONS_ON_UPDATE_VARIANTS` | Update request includes `variants` but is missing `options`. Both must be provided together. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_VARIANT_OPTION_CHOICE` | Variant choice doesn't exist in the product's option choices. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_VARIANT_OPTION` | Variant option doesn't exist in the product's options. |
    | `400` | `INVALID_ARGUMENT` | `SUBSCRIPTION_INVALID_AUTO_RENEWAL` | The `autoRenewal` field is set to false. Set the number of billing cycles in `billingCycles` to prevent automatic renewal instead. |
    | `400` | `INVALID_ARGUMENT` | `DIGITAL_PRODUCT_CANNOT_BE_VISIBLE_IN_POS` | Digital product is marked as visible in POS. Digital products can't be visible in POS. |
    | `400` | `INVALID_ARGUMENT` | `DESCRIPTION_SIZE_TOO_BIG` | Product `description` field exceeds the maximum size of 32000 bytes. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_MEDIA_ERROR` | Multiple media items have identical IDs. Each media ID must be unique. |
    | `400` | `INVALID_ARGUMENT` | `BRAND_MISSING_ID` | Brand is missing an `id`. |
    | `400` | `INVALID_ARGUMENT` | `PRICE_PER_UNIT_NOT_ALIGNED` | Price per unit isn't aligned between product and variants. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Product Updated | `wix.stores.catalog.v3.product_updated` | Triggered when a product is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-update-product" id="ex-rest-update-product-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-update-product" id="ex-rest-update-product-1" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-update-product" id="ex-rest-update-product-2" className="wix-picker-radio" />
    <input type="radio" name="ex-rest-update-product" id="ex-rest-update-product-3" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-update-product-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-update-product-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update product</span><span className="wix-picker-desc">Update different product fields. Pass only fields that you want to update</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update a single field on a product (brand)</span><span className="wix-picker-desc">Partial update. Specify only id, revision, and the field you want to change. This example changes the brand</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update product's main and choice images</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update product name</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-update-product-0"><span className="wix-picker-name">Update product</span><span className="wix-picker-desc">Update different product fields. Pass only fields that you want to update</span></label>
    <label htmlFor="ex-rest-update-product-1"><span className="wix-picker-name">Update a single field on a product (brand)</span><span className="wix-picker-desc">Partial update. Specify only id, revision, and the field you want to change. This example changes the brand</span></label>
    <label htmlFor="ex-rest-update-product-2"><span className="wix-picker-name">Update product's main and choice images</span></label>
    <label htmlFor="ex-rest-update-product-3"><span className="wix-picker-name">Update product name</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X PATCH 'https://www.wixapis.com/stores/v3/products/93f2e5ab-60a5-4173-babb-9198fc52bdd3' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
                  "fields": [
                      "VARIANT_OPTION_CHOICE_NAMES",
                      "MERCHANT_DATA",
                      "INFO_SECTION",
                      "DESCRIPTION",
                      "URL",
                      "CURRENCY",
                      "WEIGHT_MEASUREMENT_UNIT_INFO"
                  ],
                  "product": {
                      "id": "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
                      "revision": "3",
                      "name": "Premium Coffee",
                      "description": {
                          "nodes": [
                              {
                                  "type": "PARAGRAPH",
                                  "id": "foo",
                                  "nodes": [
                                      {
                                          "type": "TEXT",
                                          "textData": {
                                              "text": "Tasty, high quality and eco friendly, in a beautiful gift box."
                                          }
                                      }
                                  ],
                                  "paragraphData": {
                                      "textStyle": {
                                          "textAlignment": "AUTO"
                                      }
                                  }
                              }
                          ],
                          "metadata": {
                              "version": 1,
                              "id": "fb97b2c1-a3ff-4319-be5f-bbb7fdf0776c"
                          }
                      },
                      "visible": false,
                      "visibleInPos": false,
                      "infoSections": [
                          {
                              "id": "59954f23-fbcd-42e5-ba61-7c8cf9d8b409"
                          },
                          {
                              "uniqueName": "Origin",
                              "title": "Made in",
                              "description": {
                                  "nodes": [
                                      {
                                          "type": "PARAGRAPH",
                                          "id": "xjkk51429",
                                          "nodes": [
                                              {
                                                  "type": "TEXT",
                                                  "id": "",
                                                  "nodes": [],
                                                  "textData": {
                                                      "text": "Brazil",
                                                      "decorations": []
                                                  }
                                              }
                                          ]
                                      }
                                  ]
                              }
                          }
                      ],
                      "ribbon": {
                          "name": "New arrival"
                      }
                  }
              }'
      ```

      ```json Response
      {
        "product": {
          "id": "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
          "revision": "4",
          "createdDate": "2025-01-01T09:50:31.438Z",
          "updatedDate": "2025-01-02T07:14:02.734Z",
          "name": "Premium Coffee",
          "slug": "coffee",
          "url": {
            "relativePath": "/product-page/coffee",
            "url": "https://mysite.wixstudio.com/my-site-79/product-page/coffee"
          },
          "description": {
            "nodes": [
              {
                "type": "PARAGRAPH",
                "id": "foo",
                "nodes": [
                  {
                    "type": "TEXT",
                    "id": "",
                    "nodes": [],
                    "style": null
                  }
                ],
                "style": null
              }
            ],
            "metadata": {
              "version": 1,
              "createdTimestamp": null,
              "updatedTimestamp": null,
              "id": "fb97b2c1-a3ff-4319-be5f-bbb7fdf0776c"
            },
            "documentStyle": null
          },
          "plainDescription": null,
          "visible": false,
          "visibleInPos": true,
          "media": {
            "main": null,
            "itemsInfo": null
          },
          "seoData": null,
          "taxGroupId": null,
          "options": [],
          "modifiers": [],
          "brand": null,
          "infoSections": [],
          "ribbon": {
            "id": "ad3eb492-9d35-4f47-8a1f-cb7bd8a4e66d",
            "name": "New arrival"
          },
          "directCategoriesInfo": null,
          "allCategoriesInfo": null,
          "mainCategoryId": "1aecdc65-176f-4f07-a846-77a8d5079eb5",
          "costRange": {
            "minValue": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            },
            "maxValue": {
              "amount": "0.00",
              "formattedAmount": "$0.00"
            }
          },
          "inventory": {
            "preorderAvailability": "NO_VARIANTS",
            "availabilityStatus": "OUT_OF_STOCK",
            "preorderStatus": "DISABLED"
          },
          "productType": "PHYSICAL",
          "physicalProperties": {
            "pricePerUnit": null,
            "fulfillerId": null,
            "shippingWeightRange": null,
            "pricePerUnitRange": null,
            "weightMeasurementUnitInfo": {
              "weightMeasurementUnit": "KG"
            },
            "deliveryProfileId": null
          },
          "handle": "Product_be600651-94cf-42ad-881f-3bd69fb4a320",
          "currency": "USD",
          "breadcrumbsInfo": null,
          "actualPriceRange": {
            "minValue": {
              "amount": "10.00",
              "formattedAmount": "$10.00"
            },
            "maxValue": {
              "amount": "10.00",
              "formattedAmount": "$10.00"
            }
          },
          "compareAtPriceRange": {
            "minValue": {
              "amount": "10.00",
              "formattedAmount": "$10.00"
            },
            "maxValue": {
              "amount": "10.00",
              "formattedAmount": "$10.00"
            }
          },
          "variantsInfo": {
            "variants": [
              {
                "id": "d44f6c3f-e641-4e54-9cb3-5796e58fa1cd",
                "visible": true,
                "sku": "12345",
                "barcode": null,
                "choices": [],
                "price": {
                  "actualPrice": {
                    "amount": "10.00",
                    "formattedAmount": "$10.00"
                  },
                  "compareAtPrice": null
                },
                "revenueDetails": null,
                "media": null,
                "subscriptionPricesInfo": null,
                "inventoryStatus": {
                  "inStock": false,
                  "preorderEnabled": false
                }
              }
            ]
          },
          "subscriptionDetails": null,
          "extendedFields": null,
          "variantSummary": {
            "variantCount": 1
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X PATCH 'https://www.wixapis.com/stores/v3/products/93f2e5ab-60a5-4173-babb-9198fc52bdd3' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "product": {
          "id": "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
          "revision": "3",
          "brand": {
            "id": "c0d3bf32-45bb-47ea-a3fe-668242bbba6c"
          }
        }
      }'
      ```

      ```json Response
      {
        "product": {
          "id": "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
          "revision": "4",
          "name": "Premium Coffee",
          "slug": "premium-coffee",
          "visible": true,
          "productType": "PHYSICAL",
          "brand": {
            "id": "c0d3bf32-45bb-47ea-a3fe-668242bbba6c",
            "name": "Adidas"
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/stores/v3/products/def25830-7627-4c6c-bde6-4df21d0c6605' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
            "product": {
              "id": "def25830-7627-4c6c-bde6-4df21d0c6605",
              "revision": "3",
              "media": {
                "main": {
                  "url": "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png",
                  "altText": "Sample Product main image"
                },
                "itemsInfo": {
                  "items": [
                    {
                      "url": "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png",
                      "altText": "Sample Product main image"
                    },
                    {
                      "url": "https://static.wixstatic.com/media/abc123_choiceimage789~mv2.jpg",
                      "altText": "Medium size image"
                    }
                  ]
                }
              },
              "options": [
                {
                  "id": "d7579f10-4bd7-450c-9a13-91b513b1328c",
                  "name": "size",
                  "key": "size",
                  "optionRenderType": "TEXT_CHOICES",
                  "choicesSettings": {
                    "choices": [
                      {
                        "choiceId": "41c9d242-65fe-4330-a8c1-caaded437018",
                        "choiceType": "CHOICE_TEXT",
                        "key": "Medium",
                        "name": "Medium",
                        "visible": true,
                        "inStock": false,
                        "linkedMedia": [
                          {
                            "url": "https://static.wixstatic.com/media/abc123_choiceimage789~mv2.jpg",
                            "altText": "Medium size image"
                          }
                        ]
                      }
                    ]
                  }
                }
              ],
              "variantsInfo": {
                "variants": [
                  {
                    "id": "3f3db5ba-c8da-461f-982a-5f83316db8a1",
                    "visible": true,
                    "choices": [
                      {
                        "optionChoiceIds": {
                          "optionId": "d7579f10-4bd7-450c-9a13-91b513b1328c",
                          "choiceId": "41c9d242-65fe-4330-a8c1-caaded437018"
                        },
                        "optionChoiceNames": {
                          "choiceName": "Medium",
                          "optionName": "Size",
                          "renderType": "TEXT_CHOICES"
                        }
                      }
                    ],
                    "price": {
                      "actualPrice": {
                        "amount": "10"
                      }
                    },
                    "physicalProperties": {},
                    "inventoryStatus": {
                      "inStock": false,
                      "preorderEnabled": false
                    }
                  }
                ]
              }
            }
        }'
      ```

      ```json Response
      {
        "product": {
          "id": "def25830-7627-4c6c-bde6-4df21d0c6605",
          "revision": "4",
          "createdDate": "2025-07-13T10:55:26.934Z",
          "updatedDate": "2025-07-13T11:08:45.429Z",
          "name": "Sample Product",
          "slug": "sample-product",
          "visible": true,
          "visibleInPos": true,
          "media": {
            "main": {
              "url": "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png",
              "altText": "Sample Product main image",
              "mediaType": "IMAGE",
              "uploadId": "b0aece2a-26d8-427f-bcc0-7e943bfd0909"
            }
          },
          "options": [
            {
              "id": "d7579f10-4bd7-450c-9a13-91b513b1328c",
              "name": "size",
              "optionRenderType": "TEXT_CHOICES",
              "choicesSettings": {
                "choices": [
                  {
                    "choiceId": "41c9d242-65fe-4330-a8c1-caaded437018",
                    "linkedMedia": [
                      {
                        "url": "https://static.wixstatic.com/media/abc123_choiceimage789~mv2.jpg",
                        "altText": "Medium size image",
                        "mediaType": "IMAGE",
                        "uploadId": "7cca4c27-3667-41af-b17c-256ab6c325fc"
                      }
                    ],
                    "choiceType": "CHOICE_TEXT",
                    "key": "Medium",
                    "name": "Medium",
                    "inStock": false,
                    "visible": true
                  }
                ]
              },
              "key": "size"
            }
          ],
          "modifiers": [],
          "infoSections": [],
          "mainCategoryId": "9107571a-a4ab-467d-b98f-77851bd5116c",
          "inventory": {
            "availabilityStatus": "OUT_OF_STOCK",
            "preorderStatus": "DISABLED",
            "preorderAvailability": "NO_VARIANTS"
          },
          "productType": "PHYSICAL",
          "physicalProperties": {},
          "handle": "Product_7d434376-78c8-449f-883c-2f31e0a4fa74",
          "actualPriceRange": {
            "minValue": {
              "amount": "10"
            },
            "maxValue": {
              "amount": "10"
            }
          },
          "compareAtPriceRange": {
            "minValue": {
              "amount": "0"
            },
            "maxValue": {
              "amount": "0"
            }
          },
          "variantsInfo": {
            "variants": [
              {
                "id": "3f3db5ba-c8da-461f-982a-5f83316db8a1",
                "visible": true,
                "choices": [
                  {
                    "optionChoiceIds": {
                      "optionId": "d7579f10-4bd7-450c-9a13-91b513b1328c",
                      "choiceId": "41c9d242-65fe-4330-a8c1-caaded437018"
                    }
                  }
                ],
                "price": {
                  "actualPrice": {
                    "amount": "10"
                  }
                },
                "media": {
                  "url": "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png",
                  "altText": "Sample Product main image",
                  "mediaType": "IMAGE",
                  "uploadId": "b0aece2a-26d8-427f-bcc0-7e943bfd0909"
                },
                "physicalProperties": {},
                "inventoryStatus": {
                  "inStock": false,
                  "preorderEnabled": false
                }
              }
            ]
          },
          "variantSummary": {
            "variantCount": 1
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X PATCH 'https://www.wixapis.com/stores/v3/products/9e8924aa-c3f2-4fe6-bc98-ace47295c52d' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
            "product": {
              "id": "9e8924aa-c3f2-4fe6-bc98-ace47295c52d",
              "revision": "2",
              "name": "Shopping bag"
            }
          }'
      ```

      ```json Response
      {
        "product": {
          "id": "9e8924aa-c3f2-4fe6-bc98-ace47295c52d",
          "revision": "3",
          "createdDate": "2025-01-01T10:13:14.540Z",
          "updatedDate": "2025-01-01T12:59:03.102Z",
          "name": "Shopping bag",
          "slug": "shopping-bag",
          "url": null,
          "description": null,
          "plainDescription": null,
          "visible": true,
          "visibleInPos": true,
          "media": {
            "main": null,
            "itemsInfo": null
          },
          "seoData": null,
          "taxGroupId": null,
          "options": [],
          "modifiers": [],
          "brand": null,
          "infoSections": [],
          "ribbon": null,
          "directCategoriesInfo": null,
          "allCategoriesInfo": null,
          "mainCategoryId": "1aecdc65-176f-4f07-a846-77a8d5079eb5",
          "costRange": null,
          "inventory": {
            "preorderAvailability": "NO_VARIANTS",
            "availabilityStatus": "OUT_OF_STOCK",
            "preorderStatus": "DISABLED"
          },
          "productType": "PHYSICAL",
          "physicalProperties": {
            "pricePerUnit": null,
            "fulfillerId": null,
            "shippingWeightRange": null,
            "pricePerUnitRange": null,
            "weightMeasurementUnitInfo": null,
            "deliveryProfileId": null
          },
          "handle": "Product_a7daf3a2-2a31-49b3-8a64-8264cd1006c7",
          "currency": null,
          "breadcrumbsInfo": null,
          "actualPriceRange": {
            "minValue": {
              "amount": "0.5",
              "formattedAmount": null
            },
            "maxValue": {
              "amount": "0.5",
              "formattedAmount": null
            }
          },
          "compareAtPriceRange": {
            "minValue": {
              "amount": "0.5",
              "formattedAmount": null
            },
            "maxValue": {
              "amount": "0.5",
              "formattedAmount": null
            }
          },
          "variantsInfo": {
            "variants": [
              {
                "id": "75ba03bc-e56f-4b6d-be52-691eb2ed5fd7",
                "visible": true,
                "sku": null,
                "barcode": null,
                "choices": [],
                "price": {
                  "actualPrice": {
                    "amount": "0.5",
                    "formattedAmount": null
                  },
                  "compareAtPrice": null
                },
                "revenueDetails": null,
                "media": null,
                "subscriptionPricesInfo": null,
                "inventoryStatus": {
                  "inStock": false,
                  "preorderEnabled": false
                },
                "physicalProperties": {
                  "weight": null,
                  "pricePerUnit": null
                }
              }
            ]
          },
          "subscriptionDetails": null,
          "extendedFields": null,
          "variantSummary": {
            "variantCount": 1
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a product.

    Each time the product is updated, `revision` increments by 1.
    The current `revision` must be passed when updating the product.
    This ensures you're working with the latest product and prevents unintended overwrites.

    <Note>

    + To update array fields like `options`, `modifiers`, `variantsInfo.variants`, and any others, you must pass the entire existing array. Passing only the changed item overwrites the entire array, not just merge the single change.
    + To update `variantsInfo.variants` you must also pass `options` and vice versa. This is because variants and options are mutually dependent and must be aligned.
    + To update existing `variantsInfo.variants`, make sure to provide `variantsInfo.variants.id`. If no ID is passed, a variant is created with a new ID.
    + Updating media per variant isn't supported. Instead, assign media items to product option choices using the `linkedMedia` field. You must use preexisting product media.
    + Partial-update semantics: Update Product supports partial updates with the following rules:
      - At the request level, only `product`, `product.id`, and `product.revision` are required.
      - Other fields shown as required in the schema apply only when their parent field is provided.
      - Top-level fields you omit (for example, `name`, `ribbon`, `brand`, `taxGroupId`) are left unchanged.
      - The full-array overwrite rule applies only to repeated fields (`options`, `modifiers`, `variantsInfo.variants`); top-level scalar and object fields are partially updatable on their own.
      - To assign or change a brand without touching any other field, specify only `id`, `revision`, and `brand.id`. Supplying `brand.name` without `brand.id` is rejected with a `BRAND_MISSING_ID` validation error.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.STORES.PRODUCT_WRITE">Product write in v3 catalog</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { productsV3 } from '@wix/stores';

    productsV3.updateProduct(_id, product, options): Promise<V3Product>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Product ID.

      **Read-only.**

    </ParamField>

    <ParamField body="product" type="UpdateProduct" required>

      Product to update.

      <Expandable title="product properties">

        <ParamField body="_id" type="string">

          Product ID.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the product is updated.
          To prevent conflicting changes,
          the current revision must be passed when updating the product.

          Ignored when creating a product.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time the product was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time the product was updated.

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Product name. Translatable.

        </ParamField>

        <ParamField body="slug" type="string">

          Product slug. A URL-friendly identifier used in the product page address.

          If not provided, the slug is autogenerated based on the product name.

        </ParamField>

        <ParamField body="url" type="string">

          URL to the site's product page.

          > **Note:** Returned only when you pass `"URL"` to the `fields` array in Products API requests.

          **Read-only.**

        </ParamField>

        <ParamField body="description" type="RichContent">

          Product description using rich content.
          > **Note:** Returned only when you pass `"DESCRIPTION"` to the `fields` array in Products API requests.

          &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="indent.emoji.divider.codeBlock.file.gallery.giphy.image.table.link.textHighlight.textColor" exampleid="7dc9240e-d548-417a-abcf-0291b68b4303">
          &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
          &lt;/widget>

          <Expandable title="description properties">

            <ParamField body="nodes" type="Node[]">

              Node objects representing a rich content document.

              _Nested structure omitted for brevity — see the `Node[]` object._

            </ParamField>

            <ParamField body="metadata" type="Metadata">

              Object metadata.

              _Nested structure omitted for brevity — see the `Metadata` object._

            </ParamField>

            <ParamField body="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              _Nested structure omitted for brevity — see the `DocumentStyle` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="plainDescription" type="string">

          Product description in HTML.

          + When provided on create/update, this string must be valid HTML. It's then converted to rich content.
          + `plainDescription` is ignored when a value is also passed to the `description` field.
          > **Note:** Returned only when you pass `"PLAIN_DESCRIPTION"` to the `fields` array in Products API requests.

        </ParamField>

        <ParamField body="visible" type="boolean">

          Whether the product is visible to site visitors.

          Default: `true`

          > **Note:** For products without options, updating this field automatically
          > updates the default variant's visibility to match.
          > For products with options, the product and variant visibility values are independent.

        </ParamField>

        <ParamField body="visibleInPos" type="boolean">

          Whether the product is visible in POS (point of sale).

          Default: `true`
          > **Note:** Always `false` for `productType: DIGITAL`.

        </ParamField>

        <ParamField body="media" type="Media">

          Product media items. For a detailed explanation of product media, see [About Product Media](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/products-v3/about-product-media).

          <Expandable title="media properties">

            <ParamField body="main" type="ProductMedia">

              Main media (image, video, etc.) associated with this product.
              Automatically set to the first item in the media list.

              **Read-only.**

              _Nested structure omitted for brevity — see the `ProductMedia` object._

            </ParamField>

            <ParamField body="itemsInfo" type="MediaItemsInfo">

              All media items.
              > **Note:** Returned only when you pass `"MEDIA_ITEMS_INFO"` to the `fields` array in Products API requests.

              _Nested structure omitted for brevity — see the `MediaItemsInfo` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="seoData" type="SeoSchema">

          Product SEO data.

          <Expandable title="seoData properties">

            <ParamField body="tags" type="Tag[]">

              SEO tag information.

              _Nested structure omitted for brevity — see the `Tag[]` object._

            </ParamField>

            <ParamField body="settings" type="Settings">

              SEO general settings.

              _Nested structure omitted for brevity — see the `Settings` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="taxGroupId" type="string">

          [Tax group ID](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/tax-groups/introduction). Used to apply specific tax rates to products.

        </ParamField>

        <ParamField body="options" type="ConnectedOption[]">

          Product options, such as "Size" or "Color". Options define the ways a product can vary.

          When you provide options, you must also provide the corresponding variants. Each variant must have exactly one choice for each option. For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          Options are stored as reusable [customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities. Pass an existing customization's ID to reuse it, or define options inline and new customization entities are created automatically.

          Constraints: `maxItems 6`.

          <Expandable title="options properties">

            <ParamField body="_id" type="string">

              ID of a customization with `customizationType: PRODUCT_OPTION`.

            </ParamField>

            <ParamField body="name" type="string">

              Option name.

            </ParamField>

            <ParamField body="optionRenderType" type="string">

              Option render type.

              **Supported values:**

              - `TEXT_CHOICES` — Text choices.
              - `SWATCH_CHOICES` — Swatch choices.

            </ParamField>

            <ParamField body="key" type="string">

              A read-only identifier generated from the option name.

              Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="modifiers" type="ConnectedModifier[]">

          Product modifiers. Collect additional information from customers without creating variants.

          Unlike options, modifiers don't affect inventory or create additional variants. Use them for things like gift messages or engraving text. For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          Modifiers are stored as reusable [customization](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/customizations-v3/introduction) entities. Pass an existing customization's ID to reuse it, or define modifiers inline and new customization entities are created automatically.

          Constraints: `maxItems 10`.

          <Expandable title="modifiers properties">

            <ParamField body="_id" type="string">

              ID of a customization with `customizationType: MODIFIER`.

            </ParamField>

            <ParamField body="name" type="string">

              Modifier title.

            </ParamField>

            <ParamField body="modifierRenderType" type="string">

              Modifier render type.

              **Supported values:**

              - `FREE_TEXT` — Free text.
              - `TEXT_CHOICES` — Text choices.
              - `SWATCH_CHOICES` — Swatch choices.

            </ParamField>

            <ParamField body="mandatory" type="boolean">

              Whether customer input is required for this modifier.

            </ParamField>

            <ParamField body="key" type="string">

              A read-only identifier generated from the modifier name.

              Use `key` in the `catalogReference.options` object when [integrating Catalog V3 with eCommerce APIs](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration).

              **Read-only.** Constraints: `minLength 1`, `maxLength 50`.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="brand" type="Brand">

          Product [brand](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/brands-v3/introduction).

          + Pass `brand.name` to add a new brand while creating a product.
          + Pass an existing brand's `id` to assign that brand to the product.

          <Expandable title="brand properties">

            <ParamField body="_id" type="string">

              Brand ID.

            </ParamField>

            <ParamField body="name" type="string">

              Brand name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="infoSections" type="InfoSection[]">

          Product [info section](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/info-sections-v3/introduction).

          + Pass `infoSection.uniqueName`, `infoSection.title`, and `infoSection.description` to add a new info section while creating a product.
          + Pass an existing info section's `id` or `uniqueName` to assign that info section to the product.

          Constraints: `maxItems 20`.

          <Expandable title="infoSections properties">

            <ParamField body="_id" type="string">

              Info section ID.

            </ParamField>

            <ParamField body="uniqueName" type="string">

              Info section unique name.
              > **Note:** Returned only when you pass `"INFO_SECTION"` to the `fields` array in Products API requests.

            </ParamField>

            <ParamField body="title" type="string">

              Info section title.
              > **Note:** Returned only when you pass `"INFO_SECTION"` to the `fields` array in Products API requests.

              **Read-only.**

            </ParamField>

            <ParamField body="description" type="RichContent">

              Info section description using rich content.
              > **Note:** Returned only when you pass `"INFO_SECTION_DESCRIPTION"` to the `fields` array in Products API requests.

              &lt;widget src="https://apps.wix.com/_serverless/ricos-playground-services/goto/api-component" plugins="indent.emoji.divider.codeBlock.file.gallery.giphy.image.table.link.textHighlight.textColor" exampleid="7dc9240e-d548-417a-abcf-0291b68b4303">
              &lt;a href="https://dev.wix.com/docs/ricos/api-reference/ricos-document">See Ricos document reference&lt;/a>
              &lt;/widget>

              **Read-only.**

              _Nested structure omitted for brevity — see the `RichContent` object._

            </ParamField>

            <ParamField body="plainDescription" type="string">

              Info section description in HTML.

              When provided on create/update, this string must be a valid HTML. It will then be converted to rich content.
              `plainDescription` is ignored when value is also passed to the `description` field.
              > **Note:** Returned only when you pass `"INFO_SECTION_PLAIN_DESCRIPTION"` to the `fields` array in Products API requests.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="ribbon" type="Ribbon">

          Primary [ribbon](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/introduction) displayed on the product. Use `additionalRibbons` to assign further ribbons beyond the primary one.

          + Pass `ribbon.name` to add a new ribbon while creating a product.
          + Pass an existing ribbon's `id` or `name` to assign that ribbon to the product.

          <Expandable title="ribbon properties">

            <ParamField body="_id" type="string">

              Ribbon ID.

            </ParamField>

            <ParamField body="name" type="string">

              Ribbon name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="directCategoriesInfo" type="ProductCategoriesInfo">

          List of [categories](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/categories/introduction) that directly contain this product.

          Updated automatically when a product is added/removed from a category, when an item is moved within a category, or when a category is deleted.
          > **Note:** Returned only when you pass `"DIRECT_CATEGORIES_INFO"` to the `fields` array in Products API requests.

          **Read-only.**

          <Expandable title="directCategoriesInfo properties">

            <ParamField body="categories" type="ProductCategory[]">

              A list of categories related to the product.

              **Read-only.** Constraints: `maxItems 2000`.

              _Nested structure omitted for brevity — see the `ProductCategory[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="allCategoriesInfo" type="ProductCategoriesInfo">

          List of [categories](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/categories/introduction) that directly contain this product, as well as their parent categories.
          > **Note:** Returned only when you pass `"ALL_CATEGORIES_INFO"` to the `fields` array in Products API requests.

          **Read-only.**

          <Expandable title="allCategoriesInfo properties">

            <ParamField body="categories" type="ProductCategory[]">

              A list of categories related to the product.

              **Read-only.** Constraints: `maxItems 2000`.

              _Nested structure omitted for brevity — see the `ProductCategory[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="mainCategoryId" type="string">

          The ID of the product's primary direct [category](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/categories/introduction), which defines the product's breadcrumbs path. For example, if the product's main category is "T-Shirts" (which is a subcategory of "Clothing"), the breadcrumbs path will be "Clothing > T-Shirts".

        </ParamField>

        <ParamField body="costRange" type="PriceRange">

          Product cost range - minimum and maximum costs of all product variants.

          > **Note:** Returned only when the following conditions are met:
          > + You pass `"MERCHANT_DATA"` to the `fields` array in Products API requests.
          > + Your app has the required `SCOPE.STORES.PRODUCT_READ_ADMIN` permission scope.

          **Read-only.**

          <Expandable title="costRange properties">

            <ParamField body="minValue" type="FixedMonetaryAmount">

              Minimum value.

              _Nested structure omitted for brevity — see the `FixedMonetaryAmount` object._

            </ParamField>

            <ParamField body="maxValue" type="FixedMonetaryAmount">

              Maximum value.

              _Nested structure omitted for brevity — see the `FixedMonetaryAmount` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="inventory" type="Inventory">

          Product inventory info.

          > **Note:** This field reflects the aggregated inventory status from the default location only. Use the [Inventory Items API](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/inventory-items-v3/introduction) to manage inventory for specific locations. Learn more about [inventory management](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-inventory-management).

          **Read-only.**

          <Expandable title="inventory properties">

            <ParamField body="availabilityStatus" type="string">

              Current availability status.

              **Read-only.**

              **Supported values:**

              - `IN_STOCK` — All variants are in stock and available for purchase.
              - `OUT_OF_STOCK` — All variants are out of stock.
              - `PARTIALLY_OUT_OF_STOCK` — Some variants are out of stock and some are in stock and available for purchase.

            </ParamField>

            <ParamField body="preorderStatus" type="string">

              Current preorder status.

              **Read-only.**

              **Supported values:**

              - `ENABLED` — All variants are enabled for preorder.
              - `DISABLED` — All variants are disabled for preorder.
              - `PARTIALLY_ENABLED` — Some variants are disabled and some are enabled for preorder.

            </ParamField>

            <ParamField body="preorderAvailability" type="string">

              Preorder availability status.

              **Read-only.**

              **Supported values:**

              - `ALL_VARIANTS` — All the product variants are available for preorder.
              - `NO_VARIANTS` — None of the product variants are available for preorder.
              - `SOME_VARIANTS` — Some of the product variants are available for preorder.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="productType" type="string">

          Product type: `PHYSICAL` for tangible goods that require shipping, or `DIGITAL` for downloadable content.

          When passing `productType: PHYSICAL`, you must also pass `physicalProperties`. When passing `productType: DIGITAL`, you can optionally pass `digitalProperties` in each variant.

          Constraints: `immutable`.

          **Supported values:**

          - `PHYSICAL` — Physical product.
          - `DIGITAL` — Digital product.

        </ParamField>

        <ParamField body="handle" type="string">

          A unique human-friendly identifier for the product.

          Unlike the auto-generated `id`, the handle can be set when creating a product. This is useful when re-importing products from other systems, as it provides a stable identifier across platforms. If not provided during creation, one is automatically generated. Can't be changed after creation.

          **Read-only.**

        </ParamField>

        <ParamField body="currency" type="string">

          Currency used for the pricing of this product, in [ISO-4217](https://en.wikipedia.org/wiki/ISO_4217#List_of_ISO_4217_currency_codes) format.

          Defaults to the currency defined in the site settings, unless specified in the request's `x-wix-currency` header.
          > **Note:** Returned only when you pass `"CURRENCY"` to the `fields` array in Products API requests.

          **Read-only.**

        </ParamField>

        <ParamField body="breadcrumbsInfo" type="BreadcrumbsInfo">

          Breadcrumbs of the `mainCategoryId`. Used to navigate to parent [categories](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/categories/introduction). Useful for building navigation UI and for SEO purposes.
          > **Note:** Returned only when you pass `"BREADCRUMBS_INFO"` to the `fields` array in Products API requests.

          **Read-only.**

          <Expandable title="breadcrumbsInfo properties">

            <ParamField body="breadcrumbs" type="BreadCrumb[]">

              Breadcrumbs.

              **Read-only.** Constraints: `maxItems 5`.

              _Nested structure omitted for brevity — see the `BreadCrumb[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="actualPriceRange" type="PriceRange">

          Minimum and maximum current selling prices across all product variants.

          **Read-only.**

          <Expandable title="actualPriceRange properties">

            <ParamField body="minValue" type="FixedMonetaryAmount">

              Minimum value.

              _Nested structure omitted for brevity — see the `FixedMonetaryAmount` object._

            </ParamField>

            <ParamField body="maxValue" type="FixedMonetaryAmount">

              Maximum value.

              _Nested structure omitted for brevity — see the `FixedMonetaryAmount` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="compareAtPriceRange" type="PriceRange">

          Minimum and maximum compare-at prices (original prices before discounts) across all product variants. Used to show savings to customers.

          **Read-only.**

          <Expandable title="compareAtPriceRange properties">

            <ParamField body="minValue" type="FixedMonetaryAmount">

              Minimum value.

              _Nested structure omitted for brevity — see the `FixedMonetaryAmount` object._

            </ParamField>

            <ParamField body="maxValue" type="FixedMonetaryAmount">

              Maximum value.

              _Nested structure omitted for brevity — see the `FixedMonetaryAmount` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="variantsInfo" type="VariantsInfo">

          Product variants. Each variant represents a specific purchasable version of a product defined by option choices.

          Each variant must reference all product options via its `choices` array, using `optionChoiceNames` in requests.
          You must explicitly provide each variant when creating products with options.

          For more information, see [About Product Options and Variants](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/about-product-options-and-variants).

          <Expandable title="variantsInfo properties">

            <ParamField body="variants" type="Variant[]">

              List of related variants.

              Constraints: `minItems 1`, `maxItems 1000`.

              _Nested structure omitted for brevity — see the `Variant[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="purchaseEligibility" type="PurchaseEligibility">

          Purchase eligibility settings for the product.

          **Developer Preview.**

          <Expandable title="purchaseEligibility properties">

            <ParamField body="userCriteria" type="string">

              **Supported values:**

              - `MEMBERS_ONLY` — Product is available only to members.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="additionalRibbons" type="Ribbon[]">

          Additional [ribbons](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/ribbons-v3/introduction) displayed on the product, beyond the primary `ribbon`.

          Use this field to highlight multiple product attributes at once, for example combining a "New" ribbon with a "Sale" ribbon. Both the primary and additional ribbons are rendered on product cards and on the product page.

          Each entry follows the same assignment rules as `ribbon`:
          + Pass `additionalRibbons.name` to add a new ribbon while creating or updating the product.
          + Pass an existing ribbon's `id` or `name` to assign that ribbon to the product.

          A ribbon can be assigned to a product at most once: the same ribbon can't appear more than once in `additionalRibbons`, and a ribbon set as `ribbon` can't also appear in `additionalRibbons`.

          Supports up to 4 additional ribbons per product.

          **Developer Preview.** Constraints: `maxItems 4`.

          <Expandable title="additionalRibbons properties">

            <ParamField body="_id" type="string">

              Ribbon ID.

            </ParamField>

            <ParamField body="name" type="string">

              Ribbon name.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Custom extended fields for the product object.

          [Extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions) must be configured in the app dashboard before they can be accessed with API calls.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tags assigned to the product, organized into public and private tags.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="publicTags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="subscriptionDetails" type="SubscriptionDetails">

          Product subscriptions.

          Subscription discounts are defined at the product level but apply to each variant's `actualPrice`. This means the final subscription price varies per variant based on the variant's base price.

          <Expandable title="subscriptionDetails properties">

            <ParamField body="subscriptions" type="Subscription[]">

              Subscriptions.

              Constraints: `minItems 1`, `maxItems 6`.

              _Nested structure omitted for brevity — see the `Subscription[]` object._

            </ParamField>

            <ParamField body="allowOneTimePurchases" type="boolean">

              Whether to allow one-time purchases in addition to subscription-based purchases.

              Default: `false`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="variantSummary" type="VariantSummary">

          The total number of variants for the product.

          **Read-only.**

          <Expandable title="variantSummary properties">

            <ParamField body="variantCount" type="integer">

              The total number of variants for the product.

              **Read-only.**

            </ParamField>

            <ParamField body="minPriceVariant" type="Variant">

              The variant with the lowest `actualPrice` among visible variants for this product.

              Hidden variants are excluded; ties are broken by variant order. Empty if the product has no visible variants.
              > **Note:** Returned only when you pass `"MIN_PRICE_VARIANT"` to the `fields` array in Products API requests.

              **Read-only.** **Developer Preview.**

              _Nested structure omitted for brevity — see the `Variant` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="typedProperties" type="typedProperties">

          typedProperties

          <Expandable title="typedProperties properties">

            <ParamField body="physicalProperties" type="PhysicalProperties">

              Physical properties.

              Required when `productType: PHYSICAL`.

              _Nested structure omitted for brevity — see the `PhysicalProperties` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="UpdateProductOptions">

      options

      <Expandable title="options properties">

        <ParamField body="fields" type="string[]">

          Fields to include in the response.

          Constraints: `maxItems 100`.

          **Supported values:**

          - `URL` — The product's public storefront URL, including both relative path and full URL with base domain.
          - `CURRENCY` — Currency information including the currency code and formatted price amounts for storefront display.
          - `INFO_SECTION` — Returns basic info section metadata including IDs, unique names, and titles. Use with `INFO_SECTION_DESCRIPTION` or `INFO_SECTION_PLAIN_DESCRIPTION` to include content.
          - `MERCHANT_DATA` — Returns merchant-related financial data including cost ranges and variant revenue details like cost, profit, and profit margin.
          Requires the `SCOPE.STORES.PRODUCT_READ_ADMIN` permission scope.
          - `PLAIN_DESCRIPTION` — Provides product description as plain HTML text.
          - `INFO_SECTION_PLAIN_DESCRIPTION` — Returns info section descriptions as HTML content. Use with `INFO_SECTION` for complete metadata.
          - `SUBSCRIPTION_PRICES_INFO` — Details subscription pricing options, empty for non-subscription products.
          - `BREADCRUMBS_INFO` — Returns the category breadcrumb navigation path from root categories down to the product's main category, including category IDs, names, and slugs.
          - `WEIGHT_MEASUREMENT_UNIT_INFO` — Returns the weight measurement unit (KG, LB) for the product.
          - `VARIANT_OPTION_CHOICE_NAMES` — Enriches variant choices with human-readable option and choice names alongside render types.
          - `MEDIA_ITEMS_INFO` — Returns all media items associated with the product, including detailed metadata (URLs, alt-text, dimensions, filenames, file sizes).
          - `DESCRIPTION` — Returns the product description as rich content with complete node structure, metadata, and styling information.
          - `DIRECT_CATEGORIES_INFO` — Provides the IDs and indexes of categories the product is directly assigned to.
          - `ALL_CATEGORIES_INFO` — Provides IDs and indexes for all categories including inherited parent categories.
          - `INFO_SECTION_DESCRIPTION` — Returns info section descriptions as rich content with complete node structure, metadata, and styling information. Use with `INFO_SECTION` for complete metadata.
          - `THUMBNAIL` — Returns an optimized thumbnail image with standard dimensions, suitable for listings or previews.
          - `PRODUCT_CHOICES_MEDIA_REFERENCES` — Returns media references associated with product option and modifier choices instead of the deprecated `linked_media` field.

          When requested, choice objects will populate `media_references` with lightweight `MediaReference` objects containing only media IDs,
          while the `linked_media` field will be returned empty to avoid duplication and reduce response payload size.

          To get full media metadata (dimensions, alt text, filenames, etc.), request `MEDIA_ITEMS_INFO` on the product and correlate by media ID.

          > **Note:** The `media_references` field is available on `ConnectedOptionChoice` and `ConnectedModifierChoice` objects.
          - `PRODUCT_CHOICES_DISPLAY_IMAGE` — Returns display images for product option and modifier choices.
          When requested, choice objects will populate `display_image` with a lightweight image containing only ID, URL, and alt text.
          - `DISCOUNT_INFO` — Returns automatic discount info for product get/query/search responses and populates product-level `discountInfo.discountRuleNames`.

          To receive discounted variant prices in get/query/search responses, request this together with `MIN_PRICE_VARIANT` (returned in `variantSummary.minPriceVariant.price.priceAfterDiscount`).
          Formatted money amounts are returned only when `CURRENCY` is also requested.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<V3Product>`.

    See the [Product Object](/api-reference/business-solutions/stores/catalog-v3/products-v3/product-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `MISSING_VARIANT_OPTION_CHOICE` | Every variant's choice must reference an existing option in `options.id`. |
    | `428` | `FAILED_PRECONDITION` | `DUPLICATE_VARIANT_OPTION_CHOICE` | Multiple variants have identical choices in `variantsInfo.variants.choices`. |
    | `428` | `FAILED_PRECONDITION` | `DUPLICATE_PRODUCT_OPTIONS` | Multiple options have identical `name` and `optionRenderType`. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_DEFAULT_VARIANT` | Product with multiple variants has a variant with empty `choices`. Only single-variant products can have empty choices. |
    | `400` | `INVALID_ARGUMENT` | `FIELD_MASK_VALUE_NOT_SUPPORTED` | Update field mask includes read-only fields. |
    | `428` | `FAILED_PRECONDITION` | `VARIANTS_ARE_NOT_ALIGNED_WITH_PRODUCT` | Variants aren't aligned with the product. The variant `typedProperties` must match the `productType`, measurement units must align, and each variant must have exactly 1 choice for every option. |
    | `409` | `ALREADY_EXISTS` | `DUPLICATE_SLUG_ERROR` | There is already a product with this `slug`. |
    | `404` | `NOT_FOUND` | `DIGITAL_FILE_NOT_FOUND_ERROR` | Couldn't find the digital file referenced in `variantsInfo.variants.digitalProperties`. |
    | `409` | `ALREADY_EXISTS` | `BARCODE_ALREADY_EXIST_ERROR` | There is already a variant with this `barcode`. |
    | `428` | `FAILED_PRECONDITION` | `DUPLICATE_BARCODE_ERROR` | Multiple variants have identical `barcode` in the request. |
    | `409` | `ALREADY_EXISTS` | `SKU_ALREADY_EXIST_ERROR` | There is already a variant with this `sku`. |
    | `409` | `ALREADY_EXISTS` | `VARIANT_ID_ALREADY_EXISTS` | Multiple variants have identical `id`. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_PRODUCT_TYPE_FOR_SUBSCRIPTION` | Subscription added to a digital product. Digital products can't have subscriptions. |
    | `428` | `FAILED_PRECONDITION` | `ERROR_ALL_SUBSCRIPTIONS_HIDDEN_FOR_SUBSCRIPTION_ONLY_PRODUCT` | All subscriptions are hidden for a subscription-only product. At least 1 subscription must be visible when `allowOneTimePurchases` is false. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_SUBSCRIPTION_DISCOUNT_AMOUNT` | Subscription discount `amountOff` exceeds the minimum variant price. |
    | `409` | `ALREADY_EXISTS` | `SUBSCRIPTION_ID_ALREADY_EXISTS` | Multiple subscriptions have identical IDs. |
    | `428` | `FAILED_PRECONDITION` | `PREORDER_VALIDATION_ERROR` | Variant enabled for preorder on a subscription product. Products with subscriptions can't have preorder variants. |
    | `500` | `INTERNAL` | `BRAND_CREATION_FAILED` | Brand creation failed. |
    | `404` | `NOT_FOUND` | `RIBBON_NOT_FOUND` | Couldn't find the ribbon. |
    | `400` | `INVALID_ARGUMENT` | `INFO_SECTION_ERROR_OCCURRED` | Info section update failed. |
    | `404` | `NOT_FOUND` | `CUSTOMIZATION_NOT_EXIST` | Couldn't find the customization. |
    | `500` | `INTERNAL` | `ADD_CUSTOMIZATION_CHOICES_FAILED` | Failed to add customization choices. |
    | `404` | `NOT_FOUND` | `CHOICE_NOT_EXIST` | Couldn't find the choice. |
    | `400` | `INVALID_ARGUMENT` | `FIELD_MASK_WITH_FIELD_NOT_PASSED_ERROR` | Field mask includes fields not provided in the request. |
    | `428` | `FAILED_PRECONDITION` | `COMPARE_AT_PRICE_LOWER_THAN_ACTUAL_PRICE` | The `compareAtPrice` is lower than the `actualPrice`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_ADDITIONAL` | Same ribbon appears more than once in `additionalRibbons`. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_RIBBON_IN_PRIMARY_AND_ADDITIONAL` | Same ribbon used as both primary ribbon and additional ribbon. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_SLUG_FORMAT` | The `slug` provided is not a valid URL slug. |
    | `400` | `INVALID_ARGUMENT` | `VARIANTS_TYPED_PROPERTIES_ARE_NOT_ALIGNED` | Variants have different `typedProperties` types. All variants must have the same type. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_OPTIONS_ON_UPDATE_VARIANTS` | Update request includes `variants` but is missing `options`. Both must be provided together. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_VARIANT_OPTION_CHOICE` | Variant choice doesn't exist in the product's option choices. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_VARIANT_OPTION` | Variant option doesn't exist in the product's options. |
    | `400` | `INVALID_ARGUMENT` | `SUBSCRIPTION_INVALID_AUTO_RENEWAL` | The `autoRenewal` field is set to false. Set the number of billing cycles in `billingCycles` to prevent automatic renewal instead. |
    | `400` | `INVALID_ARGUMENT` | `DIGITAL_PRODUCT_CANNOT_BE_VISIBLE_IN_POS` | Digital product is marked as visible in POS. Digital products can't be visible in POS. |
    | `400` | `INVALID_ARGUMENT` | `DESCRIPTION_SIZE_TOO_BIG` | Product `description` field exceeds the maximum size of 32000 bytes. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATE_MEDIA_ERROR` | Multiple media items have identical IDs. Each media ID must be unique. |
    | `400` | `INVALID_ARGUMENT` | `BRAND_MISSING_ID` | Brand is missing an `id`. |
    | `400` | `INVALID_ARGUMENT` | `PRICE_PER_UNIT_NOT_ALIGNED` | Price per unit isn't aligned between product and variants. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Product Updated | `wix.stores.catalog.v3.product_updated` | Triggered when a product is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-product" id="ex-sdk-update-product-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-product" id="ex-sdk-update-product-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-product" id="ex-sdk-update-product-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-product" id="ex-sdk-update-product-3" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-product" id="ex-sdk-update-product-4" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-product-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-product-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a product's name</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update a product's main and choice images</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update a product's details</span><span className="wix-picker-desc">Update product name, description, visibility, info sections, and ribbon</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Partial update to change a single top-level field (brand)</span><span className="wix-picker-desc">Specify only _id, revision, and the field you want to change. Top-level fields you omit are left unchanged</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateProduct (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-product-0"><span className="wix-picker-name">Update a product's name</span></label>
    <label htmlFor="ex-sdk-update-product-1"><span className="wix-picker-name">Update a product's main and choice images</span></label>
    <label htmlFor="ex-sdk-update-product-2"><span className="wix-picker-name">Update a product's details</span><span className="wix-picker-desc">Update product name, description, visibility, info sections, and ribbon</span></label>
    <label htmlFor="ex-sdk-update-product-3"><span className="wix-picker-name">Partial update to change a single top-level field (brand)</span><span className="wix-picker-desc">Specify only _id, revision, and the field you want to change. Top-level fields you omit are left unchanged</span></label>
    <label htmlFor="ex-sdk-update-product-4"><span className="wix-picker-name">updateProduct (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a product's name
    import { productsV3 } from "@wix/stores";

    const productId = "9e8924aa-c3f2-4fe6-bc98-ace47295c52d";

    const product = {
      id: "9e8924aa-c3f2-4fe6-bc98-ace47295c52d",
      revision: "2",
      name: "Shopping bag"
    };

    async function updateProduct() {
      const response = await productsV3.updateProduct(productId, product);
    }

    /* Promise resolves to:
     * {
     *   "_id": "9e8924aa-c3f2-4fe6-bc98-ace47295c52d",
     *   "revision": "3",
     *   "name": "Shopping bag",
     *   "slug": "shopping-bag",
     *   "visible": true,
     *   "productType": "PHYSICAL",
     *   "actualPriceRange": {
     *     "minValue": { "amount": "0.5" },
     *     "maxValue": { "amount": "0.5" }
     *   },
     *   "variantsInfo": {
     *     "variants": [
     *       {
     *         "_id": "6f52f714-bd4b-4c48-97cd-207cbf8233f9",
     *         "visible": true,
     *         "price": {
     *           "actualPrice": { "amount": "0.5" }
     *         }
     *       }
     *     ]
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update a product's main and choice images
    import { productsV3 } from "@wix/stores";

    const productId = "def25830-7627-4c6c-bde6-4df21d0c6605";

    const product = {
      _id: "def25830-7627-4c6c-bde6-4df21d0c6605",
      revision: "3",
      media: {
        main: {
          url: "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png",
          altText: "Sample Product main image"
        },
        itemsInfo: {
          items: [
            {
              url: "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png",
              altText: "Sample Product main image"
            },
            {
              url: "https://static.wixstatic.com/media/abc123_choiceimage789~mv2.jpg",
              altText: "Medium size image"
            }
          ]
        }
      },
      options: [
        {
          _id: "d7579f10-4bd7-450c-9a13-91b513b1328c",
          name: "size",
          key: "size",
          optionRenderType: productsV3.ProductOptionRenderType.TEXT_CHOICES,
          choicesSettings: {
            choices: [
              {
                choiceId: "41c9d242-65fe-4330-a8c1-caaded437018",
                choiceType: productsV3.ChoiceType.CHOICE_TEXT,
                key: "Medium",
                name: "Medium",
                visible: true,
                inStock: false,
                linkedMedia: [
                  {
                    url: "https://static.wixstatic.com/media/abc123_choiceimage789~mv2.jpg",
                    altText: "Medium size image"
                  }
                ]
              }
            ]
          }
        }
      ],
      variantsInfo: {
        variants: [
          {
            _id: "3f3db5ba-c8da-461f-982a-5f83316db8a1",
            visible: true,
            choices: [
              {
                optionChoiceIds: {
                  optionId: "d7579f10-4bd7-450c-9a13-91b513b1328c",
                  choiceId: "41c9d242-65fe-4330-a8c1-caaded437018"
                },
                optionChoiceNames: {
                  choiceName: "Medium",
                  optionName: "Size",
                  renderType: productsV3.ProductOptionRenderType.TEXT_CHOICES
                }
              }
            ],
            price: {
              actualPrice: {
                amount: "10"
              }
            },
            physicalProperties: {},
            inventoryStatus: {
              inStock: false,
              preorderEnabled: false
            }
          }
        ]
      }
    };

    async function updateProduct() {
      const response = await productsV3.updateProduct(productId, product);
    }

    /* Promise resolves to:
     * {
     *   "_id": "def25830-7627-4c6c-bde6-4df21d0c6605",
     *   "revision": "4",
     *   "name": "Sample Product",
     *   "slug": "sample-product",
     *   "visible": true,
     *   "media": {
     *     "main": {
     *       "url": "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png",
     *       "altText": "Sample Product main image",
     *       "mediaType": "IMAGE"
     *     },
     *     "itemsInfo": {
     *       "items": [
     *         { "url": "https://static.wixstatic.com/media/abc123_mainimage456~mv2.png", "altText": "Sample Product main image", "mediaType": "IMAGE" },
     *         { "url": "https://static.wixstatic.com/media/abc123_choiceimage789~mv2.jpg", "altText": "Medium size image", "mediaType": "IMAGE" }
     *       ]
     *     }
     *   },
     *   "options": [
     *     {
     *       "_id": "d7579f10-4bd7-450c-9a13-91b513b1328c",
     *       "name": "size",
     *       "optionRenderType": "TEXT_CHOICES",
     *       "choicesSettings": {
     *         "choices": [
     *           {
     *             "choiceId": "41c9d242-65fe-4330-a8c1-caaded437018",
     *             "choiceType": "CHOICE_TEXT",
     *             "name": "Medium",
     *             "visible": true,
     *             "linkedMedia": [{ "url": "https://static.wixstatic.com/media/abc123_choiceimage789~mv2.jpg", "altText": "Medium size image" }]
     *           }
     *         ]
     *       }
     *     }
     *   ],
     *   "productType": "PHYSICAL",
     *   "actualPriceRange": {
     *     "minValue": { "amount": "10" },
     *     "maxValue": { "amount": "10" }
     *   },
     *   "variantsInfo": {
     *     "variants": [
     *       {
     *         "_id": "3f3db5ba-c8da-461f-982a-5f83316db8a1",
     *         "visible": true,
     *         "price": { "actualPrice": { "amount": "10" } },
     *         "inventoryStatus": { "inStock": false, "preorderEnabled": false }
     *       }
     *     ]
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update a product's details
    import { productsV3 } from "@wix/stores";

    const productId = "93f2e5ab-60a5-4173-babb-9198fc52bdd3";

    const product = {
      _id: "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
      revision: "3",
      name: "Premium Coffee",
      description: {
        nodes: [
          {
            type: productsV3.NodeType.PARAGRAPH,
            id: "foo",
            nodes: [
              {
                type: productsV3.NodeType.TEXT,
                textData: {
                  text: "Tasty, high quality and eco friendly, in a beautiful gift box."
                }
              }
            ],
            paragraphData: {
              textStyle: {
                textAlignment: productsV3.TextAlignment.AUTO
              }
            }
          }
        ],
        metadata: {
          version: 1,
          _id: "fb97b2c1-a3ff-4319-be5f-bbb7fdf0776c"
        }
      },
      visible: false,
      visibleInPos: false,
      infoSections: [
        {
          _id: "59954f23-fbcd-42e5-ba61-7c8cf9d8b409"
        },
        {
          uniqueName: "Origin",
          title: "Made in",
          description: {
            nodes: [
              {
                type: productsV3.NodeType.PARAGRAPH,
                id: "xjkk51429",
                nodes: [
                  {
                    type: productsV3.NodeType.TEXT,
                    id: "",
                    nodes: [],
                    textData: {
                      text: "Brazil",
                      decorations: []
                    }
                  }
                ]
              }
            ]
          }
        }
      ],
      ribbon: {
        name: "New arrival"
      }
    };

    const options = {
      fields: [
        productsV3.SingleEntityOpsRequestedFields.VARIANT_OPTION_CHOICE_NAMES,
        productsV3.SingleEntityOpsRequestedFields.MERCHANT_DATA,
        productsV3.SingleEntityOpsRequestedFields.INFO_SECTION,
        productsV3.SingleEntityOpsRequestedFields.DESCRIPTION,
        productsV3.SingleEntityOpsRequestedFields.URL,
        productsV3.SingleEntityOpsRequestedFields.CURRENCY,
        productsV3.SingleEntityOpsRequestedFields.WEIGHT_MEASUREMENT_UNIT_INFO
      ]
    };

    async function updateProduct() {
      const response = await productsV3.updateProduct(productId, product, options);
    }

    /* Promise resolves to:
     * {
     *   "_id": "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
     *   "revision": "4",
     *   "name": "Premium Coffee",
     *   "slug": "premium-coffee",
     *   "url": "https://example.com/product-page/premium-coffee",
     *   "description": {
     *     "nodes": [{ "type": "PARAGRAPH", "id": "foo", "nodes": [{ "type": "TEXT", "textData": { "text": "Tasty, high quality and eco friendly, in a beautiful gift box." } }] }],
     *     "metadata": { "version": 1, "id": "fb97b2c1-a3ff-4319-be5f-bbb7fdf0776c" }
     *   },
     *   "visible": false,
     *   "visibleInPos": false,
     *   "productType": "PHYSICAL",
     *   "currency": "EUR",
     *   "infoSections": [
     *     { "_id": "59954f23-fbcd-42e5-ba61-7c8cf9d8b409", "uniqueName": "Refund policy", "title": "Refund policy" },
     *     { "_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890", "uniqueName": "Origin", "title": "Made in" }
     *   ],
     *   "ribbon": { "_id": "new-arrival-ribbon-id", "name": "New Arrival" },
     *   "actualPriceRange": {
     *     "minValue": { "amount": "9.00", "formattedAmount": "€9.00" },
     *     "maxValue": { "amount": "15.00", "formattedAmount": "€15.00" }
     *   },
     *   "physicalProperties": {
     *     "weightMeasurementUnitInfo": { "weightMeasurementUnit": "KG" }
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Partial update to change a single top-level field (brand)
    import { productsV3 } from "@wix/stores";

    const productId = "93f2e5ab-60a5-4173-babb-9198fc52bdd3";

    const product = {
      _id: "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
      revision: "3",
      brand: {
        _id: "c0d3bf32-45bb-47ea-a3fe-668242bbba6c"
      }
    };

    async function updateProductBrand() {
      const response = await productsV3.updateProduct(productId, product);
    }

    /* Promise resolves to:
     * {
     *   "_id": "93f2e5ab-60a5-4173-babb-9198fc52bdd3",
     *   "revision": "4",
     *   "name": "Premium Coffee",
     *   "slug": "premium-coffee",
     *   "visible": true,
     *   "productType": "PHYSICAL",
     *   "brand": {
     *     "_id": "c0d3bf32-45bb-47ea-a3fe-668242bbba6c",
     *     "name": "Adidas"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateProduct (self-hosted)
    import { createClient } from '@wix/sdk';
    import { productsV3 } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { productsV3 },
      // Include the auth strategy and host as relevant
    });

    async function updateProduct(_id,product,options) {
      const response = await myWixClient.productsV3.updateProduct(_id,product,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
