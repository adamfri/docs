---
title: "Update Form"
description: "Updates a form schema."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a form schema.

    Updating `formFields` replaces the whole array. Any field that's missing from the array you send is moved to `deletedFormFields`, so resend every field you want to keep.

    For how to compose each kind of form field, including which `identifier`, `inputType`, and `componentType` values to combine, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/form-schema-service/v4/forms/{form.id}
    ```

    ## Path Params

    <ParamField path="form.id" type="string" required>

      Form schema ID.

    </ParamField>

    ## Body Params

    <ParamField body="form" type="Form" required>

      Form schema to update.

      See the [Form Object](/api-reference/crm/forms/form-schemas/form-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="form" type="Form">

      Updated form schema.

      See the [Form Object](/api-reference/crm/forms/form-schemas/form-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `DELETED_FIELD_COUNT_OVER_MAX` | Number of deleted fields exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_FIELD_TARGETS` | Multiple fields have the same target name. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_FIELD_IDS` | Multiple fields have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_STEP_IDS` | Multiple steps have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_RULE_IDS` | Multiple rules have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_NESTED_FORM_IDS` | Nested form fields are missing required form IDs. |
    | `400` | `INVALID_ARGUMENT` | `NON_EXISTING_NESTED_FORMS` | Referenced nested forms don't exist. |
    | `400` | `INVALID_ARGUMENT` | `NESTED_FORM_FIELDS_MISSING` | Nested form is missing required fields. |
    | `400` | `INVALID_ARGUMENT` | `FIELD_SCHEMA_NOT_SUPPORTED` | Field schema isn't supported by the platform. |
    | `400` | `INVALID_ARGUMENT` | `FORM_FIELDS_COUNT_EXCEEDED` | Number of form fields exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `NAMESPACE_FORMS_COUNT_EXCEEDED` | Number of forms in the namespace exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `FORM_SIZE_EXCEEDED` | Form size exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `TEMPLATE_NOT_EXTENDABLE` | Template can't be extended or modified. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_FORM_STEPS` | Form steps configuration is invalid. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_FORM_FIELDS` | Form is missing required fields. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_FORM_RULES` | Form is missing required rules. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_FORM_CONTACT_MAPPINGS` | Form is missing required contact field mappings. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_PRODUCT_IDS` | Multiple products have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_PRODUCT_NAME` | Product is missing a required name. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Form Updated | `wix.forms.v4.form_updated` | Triggers when a form schema is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Updates a form

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/form-schema-service/v4/forms/76d01133-44df-490e-810f-c10e30f330a1' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
               "form":{
                  "id":"76d01133-44df-490e-810f-c10e30f330a1",
                  "formFields":[
                     {
                        "id":"783ee9a6-60d6-4dfc-5693-59f7a5f1107e",
                        "hidden":false,
                        "identifier":"SUBMIT_BUTTON",
                        "fieldType":"DISPLAY",
                        "displayOptions":{
                           "displayFieldType":"PAGE_NAVIGATION",
                           "pageNavigationOptions":{
                              "nextPageText":"Next",
                              "previousPageText":"Back",
                              "submitText":"Submit"
                           }
                        }
                     },
                     {
                        "id":"810933d9-dd9e-4ed2-0b85-e557501b0f98",
                        "hidden":false,
                        "identifier":"TEXT_INPUT",
                        "fieldType":"INPUT",
                        "inputOptions":{
                           "target":"short_answer_6b77",
                           "pii":false,
                           "required":false,
                           "inputType":"STRING",
                           "stringOptions":{
                              "validation":{
                                 "minLength":null,
                                 "maxLength":null,
                                 "pattern":null,
                                 "format":"UNKNOWN_FORMAT",
                                 "enum":[

                                 ]
                              },
                              "componentType":"TEXT_INPUT",
                              "textInputOptions":{
                                 "label":"Updated short answer field",
                                 "showLabel":true,
                                 "mediaSettings":{
                                    "imagePosition":"ABOVE",
                                    "imageAlignment":"CENTER",
                                    "imageFit":"COVER"
                                 }
                              }
                           },
                           "readOnly":false
                        }
                     }
                  ],
                  "name":"Updated form",
                  "revision":"9"
               }
            }'
      ```

      ```json Response
      {
        "form": {
          "id": "76d01133-44df-490e-810f-c10e30f330a1",
          "formFields": [
            {
              "id": "783ee9a6-60d6-4dfc-5693-59f7a5f1107e",
              "hidden": false,
              "identifier": "SUBMIT_BUTTON",
              "fieldType": "DISPLAY",
              "displayOptions": {
                "displayFieldType": "PAGE_NAVIGATION",
                "pageNavigationOptions": {
                  "nextPageText": "Next",
                  "previousPageText": "Back",
                  "submitText": "Submit"
                }
              }
            },
            {
              "id": "810933d9-dd9e-4ed2-0b85-e557501b0f98",
              "hidden": false,
              "identifier": "TEXT_INPUT",
              "fieldType": "INPUT",
              "inputOptions": {
                "target": "short_answer_6b77",
                "pii": false,
                "required": false,
                "inputType": "STRING",
                "readOnly": false,
                "stringOptions": {
                  "validation": {
                    "format": "UNKNOWN_FORMAT",
                    "enum": []
                  },
                  "componentType": "TEXT_INPUT",
                  "textInputOptions": {
                    "label": "Updated short answer field",
                    "showLabel": true,
                    "mediaSettings": {
                      "imagePosition": "ABOVE",
                      "imageAlignment": "CENTER",
                      "imageFit": "COVER"
                    }
                  }
                }
              }
            }
          ],
          "steps": [
            {
              "id": "76978216-2017-4b76-0df2-86cc449aa06c",
              "name": "Page 1",
              "hidden": false,
              "layout": {
                "large": {
                  "items": [
                    {
                      "fieldId": "783ee9a6-60d6-4dfc-5693-59f7a5f1107e",
                      "row": 2,
                      "column": 0,
                      "width": 12,
                      "height": 1
                    },
                    {
                      "fieldId": "810933d9-dd9e-4ed2-0b85-e557501b0f98",
                      "row": 0,
                      "column": 0,
                      "width": 12,
                      "height": 1
                    },
                    {
                      "fieldId": "d17c677c-6174-4012-ae96-4e905f622bb3",
                      "row": 1,
                      "column": 0,
                      "width": 12,
                      "height": 1
                    }
                  ]
                }
              }
            }
          ],
          "revision": "10",
          "createdDate": "2025-08-11T12:20:26.617Z",
          "updatedDate": "2025-08-11T13:00:14.817Z",
          "deletedFormFields": [
            {
              "id": "d17c677c-6174-4012-ae96-4e905f622bb3",
              "hidden": false,
              "identifier": "RATING_INPUT",
              "fieldType": "INPUT",
              "inputOptions": {
                "target": "star_rating_23a0",
                "pii": false,
                "required": false,
                "inputType": "NUMBER",
                "readOnly": false,
                "numberOptions": {
                  "validation": {
                    "maximum": 5,
                    "minimum": 1,
                    "multipleOf": 1,
                    "enum": []
                  },
                  "componentType": "RATING_INPUT",
                  "ratingInputOptions": {
                    "label": "Star rating",
                    "showLabel": true
                  }
                }
              }
            }
          ],
          "extendedFields": {
            "namespaces": {
              "@forms/form-app": {
                "automationId": "3a5f8706-a6c6-4980-8751-5bdc2dd106d1"
              }
            }
          },
          "namespace": "wix.form_app.form",
          "spamFilterProtectionLevel": "ADVANCED",
          "submitSettings": {
            "submitSuccessAction": "THANK_YOU_MESSAGE",
            "thankYouMessageOptions": {
              "durationInSeconds": 8,
              "richContent": {
                "nodes": [
                  {
                    "type": "PARAGRAPH",
                    "id": "81bct30",
                    "nodes": [
                      {
                        "type": "TEXT",
                        "id": "",
                        "nodes": [],
                        "textData": {
                          "text": "Thanks, we received your submission.",
                          "decorations": []
                        }
                      }
                    ],
                    "paragraphData": {
                      "textStyle": {
                        "textAlignment": "CENTER"
                      }
                    }
                  }
                ],
                "metadata": {
                  "version": 1,
                  "createdTimestamp": "2025-08-11T12:16:47.898Z",
                  "updatedTimestamp": "2025-08-11T12:16:47.898Z",
                  "id": "8f09392a-064f-484d-bf53-5af7945a797e"
                }
              }
            }
          },
          "enabled": true,
          "name": "Updated form",
          "formRules": []
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a form schema.

    Updating `formFields` replaces the whole array. Any field that's missing from the array you send is moved to `deletedFormFields`, so resend every field you want to keep.

    For how to compose each kind of form field, including which `identifier`, `inputType`, and `componentType` values to combine, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.FORMS.EDIT-FORM">Manage forms</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { forms } from '@wix/forms';

    forms.updateForm(_id, form): Promise<Form>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Form schema ID.

      **Read-only.**

    </ParamField>

    <ParamField body="form" type="UpdateForm" required>

      Form schema to update.

      <Expandable title="form properties">

        <ParamField body="_id" type="string">

          Form schema ID.

          **Read-only.**

        </ParamField>

        <ParamField body="formFields" type="Field[]">

          Form fields that define the input elements and their validation rules.

          The order of this array isn't the order the fields are displayed in. Display order comes from `steps`, where each layout item positions a field by `row` and `column`. Don't render fields in array order.

          For a detailed explanation of how to compose each kind of field, including which `identifier`, `inputType`, and `componentType` values to combine, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

          Constraints: `maxItems 500`.

          <Expandable title="formFields properties">

            <ParamField body="_id" type="string" required>

              Field ID.

              The ID is stored in lowercase. Because the form's layout references each field by this ID, pass a lowercase GUID so that `id` and the layout's `fieldId` still match after the form schema is stored. IDs must be unique in the form.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="hidden" type="boolean">

              Whether the field is hidden from submitters.

              Default: `false`

            </ParamField>

            <ParamField body="identifier" type="string">

              Identification for the field, describing its purpose. For example, `TEXT_INPUT` or `CONTACTS_EMAIL`.

              Set this to the known value for the kind of field you're creating.
              An unrecognized value, such as a custom string or a GUID, is dropped rather than rejected: the request still succeeds, but the field doesn't appear in the response's `formFields`, and neither the field nor its submitted values appear in the Wix dashboard.

              Several fields share a general-purpose `componentType`, so `identifier` is often the only value that distinguishes them.
              For example, both a short answer field and a long answer field use the `TEXT_INPUT` component, and are told apart by their `TEXT_INPUT` and `TEXT_AREA` identifiers.

              For the full list of known values, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

            </ParamField>

            <ParamField body="fieldType" type="string">

              Field type.

              `INPUT` fields collect data from submitters and are configured in `inputOptions`.
              `DISPLAY` fields show information without collecting data and are configured in `displayOptions`.

              **Supported values:**

              - `UNKNOWN_FIELD_TYPE` — Unknown field type.
              - `INPUT` — Input field that collects data from users.
              - `DISPLAY` — Display field that shows information without collecting input.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="steps" type="Step[]">

          Layout configuration that defines how form fields are organized across multiple steps.
          Each step represents a page of the form.

          Every field in `formFields`, including the submit button, must be placed by a layout item whose `fieldId` matches the field's `id`. A field that isn't placed still stores submitted values, but the Wix dashboard renders it, and the submissions made against it, as empty.

          Use a single step unless the form is split across pages.

          Constraints: `maxItems 100`.

          <Expandable title="steps properties">

            <ParamField body="_id" type="string" required>

              Step ID.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="name" type="string">

              Name of the step.

            </ParamField>

            <ParamField body="hidden" type="boolean">

              Whether the step is hidden.

            </ParamField>

            <ParamField body="layout" type="FormLayout">

              Layout configuration that defines how form fields are arranged within this step.

              _Nested structure omitted for brevity — see the `FormLayout` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="rules" type="FormRule[]" deprecated>

          Form rules, can be applied to layout and items properties.

          **Deprecated.** Form rules, can be applied to layout and items properties. Use `form_rules` instead. Removal: 2026-04-01. Constraints: `maxItems 100`.

          <Expandable title="rules properties">

            <ParamField body="_id" type="string" required>

              Rule ID.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="condition" type="object">

              Condition that determines when this rule's overrides should be applied.

            </ParamField>

            <ParamField body="overrides" type="FormOverride[]">

              Changes to apply to form elements when the condition evaluates to true.

              Constraints: `maxItems 500`.

              _Nested structure omitted for brevity — see the `FormOverride[]` object._

            </ParamField>

            <ParamField body="name" type="string">

              Name for the rule.

            </ParamField>

            <ParamField body="namespaceRule" type="boolean">

              Namespace rules are part of business logic of app owning namespace, have limited editing possibilities

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the form schema is updated.
          To prevent conflicting changes, the current revision must be passed when updating the form schema.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date and time when the form schema was created.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date and time when the form schema was last updated.

          **Read-only.**

        </ParamField>

        <ParamField body="deletedFields" type="FormField[]" deprecated>

          Deprecated. Use `deletedFormFields` instead.

          **Read-only.** **Deprecated.** Deprecated. Use `deletedFormFields` instead. Use `deleted_form_fields` instead. Removal: 2027-02-01. Constraints: `maxItems 150`.

          <Expandable title="deletedFields properties">

            <ParamField body="_id" type="string">

              Item ID.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="target" type="string">

              Definition of a target where the value of field belongs.

            </ParamField>

            <ParamField body="validation" type="Validation">

              Validation of field output value.

              _Nested structure omitted for brevity — see the `Validation` object._

            </ParamField>

            <ParamField body="pii" type="boolean">

              Mark the field as containing personal information. This will encrypt user data when storing it.

            </ParamField>

            <ParamField body="hidden" type="boolean">

              Whether the field is hidden.

            </ParamField>

            <ParamField body="view" type="object">

              Field view properties.

            </ParamField>

            <ParamField body="dataExtensionsDetails" type="DataExtensionsDetails">

              Details identifying field, which is extension of other entity

              _Nested structure omitted for brevity — see the `DataExtensionsDetails` object._

            </ParamField>

            <ParamField body="readOnly" type="boolean">

              Whether the field is read only.
              Default: false

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="deletedFormFields" type="Field[]">

          Form fields that were soft deleted.

          **Read-only.** Constraints: `maxItems 150`.

          <Expandable title="deletedFormFields properties">

            <ParamField body="_id" type="string">

              Field ID.

              The ID is stored in lowercase. Because the form's layout references each field by this ID, pass a lowercase GUID so that `id` and the layout's `fieldId` still match after the form schema is stored. IDs must be unique in the form.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="hidden" type="boolean">

              Whether the field is hidden from submitters.

              Default: `false`

            </ParamField>

            <ParamField body="identifier" type="string">

              Identification for the field, describing its purpose. For example, `TEXT_INPUT` or `CONTACTS_EMAIL`.

              Set this to the known value for the kind of field you're creating.
              An unrecognized value, such as a custom string or a GUID, is dropped rather than rejected: the request still succeeds, but the field doesn't appear in the response's `formFields`, and neither the field nor its submitted values appear in the Wix dashboard.

              Several fields share a general-purpose `componentType`, so `identifier` is often the only value that distinguishes them.
              For example, both a short answer field and a long answer field use the `TEXT_INPUT` component, and are told apart by their `TEXT_INPUT` and `TEXT_AREA` identifiers.

              For the full list of known values, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).

            </ParamField>

            <ParamField body="fieldType" type="string">

              Field type.

              `INPUT` fields collect data from submitters and are configured in `inputOptions`.
              `DISPLAY` fields show information without collecting data and are configured in `displayOptions`.

              **Supported values:**

              - `UNKNOWN_FIELD_TYPE` — Unknown field type.
              - `INPUT` — Input field that collects data from users.
              - `DISPLAY` — Display field that shows information without collecting input.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Data extensions for storing additional custom properties.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="namespace" type="string">

          Unique identifier for the app that owns this form schema.
          For example, `wix.form_app.form` for Wix Forms.

          See a list of namespaces for [apps made by Wix](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/introduction#namespaces-of-apps-made-by-wix) section.

          Constraints: `minLength 10`, `maxLength 50`, `immutable`.

        </ParamField>

        <ParamField body="mediaFolderId" type="string">

          Media folder ID for storing uploaded files from form submissions.

          **Read-only.**

        </ParamField>

        <ParamField body="limitationRule" type="LimitationRule">

          Submission limits that can automatically disable the form when reached.

          <Expandable title="limitationRule properties">

            <ParamField body="maxAllowedSubmissions" type="integer">

              Limitation by submission count, disables form when a set amount of submissions is reached.

            </ParamField>

            <ParamField body="dateTimeDeadline" type="datetime">

              Limitation by submission date, disables form when a set date and time is reached.

            </ParamField>

            <ParamField body="submissionLimitPerUser" type="integer">

              Limitation per user submission count, disables form when a set amount of submissions per user is reached.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="spamFilterProtectionLevel" type="string">

          Level of spam protection applied to form submissions.

          Default: `ADVANCED`

          **Supported values:**

          - `NONE` — No spam filtering. All submissions are accepted without screening.
          - `BASIC` — Basic spam filtering. Provides minimal protection against obvious spam and the form is open to a high risk of spam submissions.
          - `ADVANCED` — Advanced spam filtering. Provides comprehensive protection with intelligent screening and the form is open to a low risk of spam submissions.

        </ParamField>

        <ParamField body="requiredIndicatorProperties" type="RequiredIndicatorProperties">

          Configuration for how required field indicators are displayed to the submitter.

          <Expandable title="requiredIndicatorProperties properties">

            <ParamField body="requiredIndicator" type="string">

              Type of visual indicator used to mark required fields.

              **Supported values:**

              - `ASTERISK` — Display an asterisk (*) symbol to indicate required fields.
              - `TEXT` — Display the text "Required" to indicate required fields.
              - `NONE` — No visual indicator is shown for required fields.

            </ParamField>

            <ParamField body="requiredIndicatorPlacement" type="string">

              Position where the required indicator appears relative to the field title.

              **Supported values:**

              - `AFTER_FIELD_TITLE` — Place the required indicator after the field title.
              - `BEFORE_FIELD_TITLE` — Place the required indicator before the field title.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="submitSettings" type="SubmitSettings">

          Configuration for actions taken after a form is successfully submitted.

          <Expandable title="submitSettings properties">

            <ParamField body="submitSuccessAction" type="string">

              Action that's triggered after a form is successfully submitted.

              **Supported values:**

              - `NO_ACTION` — No action is taken after form submission.
              - `THANK_YOU_MESSAGE` — Display a thank you message after form submission.
              - `REDIRECT` — Redirect the user to a specified URL after form submission.
              - `POPUP` — Display a popup after form submission.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="submitSuccessActionOptions" type="submitSuccessActionOptions">

              submitSuccessActionOptions

              _Nested structure omitted for brevity — see the `submitSuccessActionOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="disabledFormMessage" type="RichContent">

          Message displayed to visitors when the form is disabled.
          This allows you to inform submitters why the form isn't available.

          <Expandable title="disabledFormMessage properties">

            <ParamField body="nodes" type="Node[]">

              Node objects representing a rich content document.

              _Nested structure omitted for brevity — see the `Node[]` object._

            </ParamField>

            <ParamField body="metadata" type="Metadata">

              Object metadata.

              _Nested structure omitted for brevity — see the `Metadata` object._

            </ParamField>

            <ParamField body="documentStyle" type="DocumentStyle">

              Global styling for header, paragraph, block quote, and code block nodes in the object.

              _Nested structure omitted for brevity — see the `DocumentStyle` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="enabled" type="boolean">

          Whether the form is effectively active — `true` only when the user has not disabled the form
          **and** no system service has placed an `activationBlock` on it.

          Writing `enabled: true` records the user's intent to activate the form, but the value read
          back may still be `false` if `activationBlock` is present (for example, a plan restriction is in
          effect). Once the block is lifted the form becomes active automatically without any additional
          write.

          Default: `true`

        </ParamField>

        <ParamField body="name" type="string">

          Display name of the form schema.

          Names are unique in a namespace. If the name you pass is already taken, the request still succeeds, and the form schema is stored under a numbered variation of that name instead of returning an error. Read `name` from the response to learn the name the schema was actually saved under.

        </ParamField>

        <ParamField body="formRules" type="Rule[]">

          Business logic rules that can modify form behavior based on user input or other conditions.
          These rules can show/hide fields, change validation, or trigger actions.

          Constraints: `maxItems 100`.

          <Expandable title="formRules properties">

            <ParamField body="_id" type="string" required>

              Rule ID.

              Constraints: `format GUID`, `immutable`.

            </ParamField>

            <ParamField body="expression" type="ConditionNode">

              Conditional expression that determines when this rule should be applied.
              The overrides defined in this rule are applied when this expression evaluates to true.

              _Nested structure omitted for brevity — see the `ConditionNode` object._

            </ParamField>

            <ParamField body="overrides" type="FormOverride[]">

              Form modifications that are applied when the rule expression evaluates to true.
              Each override specifies how a form element should be changed.

              Constraints: `maxItems 500`.

              _Nested structure omitted for brevity — see the `FormOverride[]` object._

            </ParamField>

            <ParamField body="name" type="string">

              Human-readable name for the rule.

            </ParamField>

            <ParamField body="namespaceRule" type="boolean">

              Namespace rules are part of business logic of app owning namespace, have limited editing possibilities

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="tags" type="Tags">

          Tag IDs collections associated with the current entity.

          <Expandable title="tags properties">

            <ParamField body="privateTags" type="TagList">

              Tags that require an additional permission in order to access them, normally not given to site members or visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

            <ParamField body="tags" type="TagList">

              Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors.

              _Nested structure omitted for brevity — see the `TagList` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="quizSettings" type="QuizSettings">

          Quiz configuration for a scored form. Set this when the form is a quiz whose
          answers are scored: it holds the passing criteria (score threshold in points or
          percentage), how the result is shown on the confirmation page (score, pass/fail
          status, or both), and the pass/fail messages. Absent on non-quiz forms.

          <Expandable title="quizSettings properties">

            <ParamField body="message" type="RichContent">

              Rich-content message shown on the quiz result (confirmation) page, alongside any
              score or pass/fail status configured in `results_display`.

              _Nested structure omitted for brevity — see the `RichContent` object._

            </ParamField>

            <ParamField body="resultsDisplay" type="string">

              How to display quiz results in the confirmation page.
              Default: `DO_NOT_DISPLAY`

              **Supported values:**

              - `DO_NOT_DISPLAY` — Do not show quiz score and passing status.
              - `SCORE` — Display only the user's score.
              - `PASSING_STATUS` — Display the pass or fail message based on the user's score.
              - `SCORE_AND_PASSING_STATUS` — Display both the score and pass/fail message.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="passingCriteria" type="passingCriteria">

              passingCriteria

              _Nested structure omitted for brevity — see the `passingCriteria` object._

            </ParamField>

            <ParamField body="resultsDisplayOptions" type="resultsDisplayOptions">

              resultsDisplayOptions

              _Nested structure omitted for brevity — see the `resultsDisplayOptions` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="autoFillContact" type="string">

          Source used for contact autofill when filling form fields. Default: FORM_INPUT.

          **Supported values:**

          - `MEMBER_DATA` — Use site member data to autofill contact fields.
          - `FORM_INPUT` — Use form input data to autofill contact fields.

        </ParamField>

        <ParamField body="submissionAccess" type="string">

          Controls who can read submissions of this form beyond the standard permission.

          Default: `OWNER_AND_COLLABORATORS`

          **Supported values:**

          - `UNKNOWN_SUBMISSION_ACCESS` — Unknown submission access.
          - `OWNER_AND_COLLABORATORS` — Only users with `WIX_FORM.SUBMISSIONS_READ_ANY` permission. Default behavior.
          - `MEMBERS` — Logged-in site members can read submissions.
          - `PUBLIC` — Any site visitor with a valid Wix session can read submissions.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Form>`.

    See the [Form Object](/api-reference/crm/forms/form-schemas/form-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `428` | `FAILED_PRECONDITION` | `DELETED_FIELD_COUNT_OVER_MAX` | Number of deleted fields exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_FIELD_TARGETS` | Multiple fields have the same target name. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_FIELD_IDS` | Multiple fields have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_STEP_IDS` | Multiple steps have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_RULE_IDS` | Multiple rules have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_NESTED_FORM_IDS` | Nested form fields are missing required form IDs. |
    | `400` | `INVALID_ARGUMENT` | `NON_EXISTING_NESTED_FORMS` | Referenced nested forms don't exist. |
    | `400` | `INVALID_ARGUMENT` | `NESTED_FORM_FIELDS_MISSING` | Nested form is missing required fields. |
    | `400` | `INVALID_ARGUMENT` | `FIELD_SCHEMA_NOT_SUPPORTED` | Field schema isn't supported by the platform. |
    | `400` | `INVALID_ARGUMENT` | `FORM_FIELDS_COUNT_EXCEEDED` | Number of form fields exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `NAMESPACE_FORMS_COUNT_EXCEEDED` | Number of forms in the namespace exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `FORM_SIZE_EXCEEDED` | Form size exceeds the server's limit. |
    | `400` | `INVALID_ARGUMENT` | `TEMPLATE_NOT_EXTENDABLE` | Template can't be extended or modified. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_FORM_STEPS` | Form steps configuration is invalid. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_FORM_FIELDS` | Form is missing required fields. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_FORM_RULES` | Form is missing required rules. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_FORM_CONTACT_MAPPINGS` | Form is missing required contact field mappings. |
    | `400` | `INVALID_ARGUMENT` | `DUPLICATED_PRODUCT_IDS` | Multiple products have the same ID. |
    | `400` | `INVALID_ARGUMENT` | `MISSING_PRODUCT_NAME` | Product is missing a required name. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Form Updated | `wix.forms.v4.form_updated` | Triggers when a form schema is updated. |
    | Form Pii Fields Updated | `wix.forms.v4.form_pii_fields_updated` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-form" id="ex-sdk-update-form-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-form" id="ex-sdk-update-form-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-form" id="ex-sdk-update-form-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-form-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-form-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateForm</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateForm (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateForm (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-form-0"><span className="wix-picker-name">updateForm</span></label>
    <label htmlFor="ex-sdk-update-form-1"><span className="wix-picker-name">updateForm (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-form-2"><span className="wix-picker-name">updateForm (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateForm
    import { forms } from '@wix/forms';

    async function updateForm(_id,form) {
      const response = await forms.updateForm(_id,form);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateForm (self-hosted)
    import { createClient } from '@wix/sdk';
    import { forms } from '@wix/forms';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { forms },
      // Include the auth strategy and host as relevant
    });

    async function updateForm(_id,form) {
      const response = await myWixClient.forms.updateForm(_id,form);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateForm (with elevated permissions)
    import { forms } from '@wix/forms';
    import { auth } from '@wix/essentials';

    async function myUpdateFormMethod(_id,form) {
      const elevatedUpdateForm = auth.elevate(forms.updateForm);
      const response = await elevatedUpdateForm(_id,form);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
