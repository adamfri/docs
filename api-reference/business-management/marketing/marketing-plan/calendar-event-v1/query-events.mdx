---
title: "Query Events"
description: "Retrieves events that occur within a given date range."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves events that occur within a given date range.

    The request must include a date range filter with both `filter.date.$gte` (start, inclusive)
    and `filter.date.$lte` (end, inclusive). Omitting either field returns an `INVALID_ARGUMENT`
    error. Only this date filter is honored: any `sort`, `paging`, `cursorPaging`, `fields`, or
    `fieldsets` in the request are ignored. Results are always sorted by date in ascending order,
    and the response is capped at 100 events with no cursor or paging (narrow the date range to
    stay within the cap).

    Results include one-time events, the individual occurrences of recurring events that fall
    within the range, and read-only events from the site's connected calendars (such as Google
    Calendar).

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/promote/marketing-plan-service/v1/events/query
    ```

    ## Body Params

    <ParamField body="query" type="QueryV2">

      Query options. Only the date range filter (`filter.date.$gte` and `filter.date.$lte`) is
      honored. Any `sort`, `paging`, `cursorPaging`, `fields`, or `fieldsets` values are ignored.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object.

          Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object.

          Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

          Constraints: `maxItems 5`.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 200`, `maxItems 5`.

        </ParamField>

        <ParamField body="fieldsets" type="string[]">

          Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

          Constraints: `maxLength 200`, `maxItems 5`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of items.

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="events" type="CalendarEvent[]">

      List of events (and recurring occurrences) that fall within the requested date range.

      Constraints: `minItems 0`, `maxItems 100`.

      See the [Calendar Event Object](/api-reference/business-management/marketing/marketing-plan/calendar-event-v1/calendar-event-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieve calendar events that fall within a date range. The only honored filter is a date range using $gte and $lte.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/promote/marketing-plan-service/v1/events/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "date": {
              "$gte": "2026-07-01T00:00:00.000Z",
              "$lte": "2026-07-31T23:59:59.999Z"
            }
          }
        }
      }'
      ```

      ```json Response
      {
        "events": [
          {
            "id": "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
            "name": "Summer Sale Launch",
            "description": "Kick off our summer promotion across all social channels.",
            "date": "2026-07-01T09:00:00.000Z"
          }
        ]
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves events that occur within a given date range.

    The request must include a date range filter with both `filter.date.$gte` (start, inclusive)
    and `filter.date.$lte` (end, inclusive). Omitting either field returns an `INVALID_ARGUMENT`
    error. Only this date filter is honored: any `sort`, `paging`, `cursorPaging`, `fields`, or
    `fieldsets` in the request are ignored. Results are always sorted by date in ascending order,
    and the response is capped at 100 events with no cursor or paging (narrow the date range to
    stay within the cap).

    Results include one-time events, the individual occurrences of recurring events that fall
    within the range, and read-only events from the site's connected calendars (such as Google
    Calendar).

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PROMOTE.MANAGE-MARKETING-PLAN">Manage Marketing Plans</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { calendarEvents } from '@wix/promote-growth-tools-marketing-plan';

    calendarEvents.queryEvents(options): Promise
    ```

    ## Method Parameters

    <ParamField body="options" type="QueryEventsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="query" type="QueryV2">

          Query options. Only the date range filter (`filter.date.$gte` and `filter.date.$lte`) is
          honored. Any `sort`, `paging`, `cursorPaging`, `fields`, or `fieldsets` values are ignored.

          <Expandable title="query properties">

            <ParamField body="filter" type="object">

              Filter object.

              Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).

            </ParamField>

            <ParamField body="sort" type="Sorting[]">

              Sort object.

              Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).

              Constraints: `maxItems 5`.

              _Nested structure omitted for brevity — see the `Sorting[]` object._

            </ParamField>

            <ParamField body="fields" type="string[]">

              Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.

              Constraints: `maxLength 200`, `maxItems 5`.

            </ParamField>

            <ParamField body="fieldsets" type="string[]">

              Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.

              Constraints: `maxLength 200`, `maxItems 5`.

            </ParamField>

          </Expandable>

          <Expandable title="One of the following">

            <ParamField body="pagingMethod" type="pagingMethod">

              pagingMethod

              _Nested structure omitted for brevity — see the `pagingMethod` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="events" type="CalendarEvent[]">

      List of events (and recurring occurrences) that fall within the requested date range.

      Constraints: `minItems 0`, `maxItems 100`.

      See the [Calendar Event Object](/api-reference/business-management/marketing/marketing-plan/calendar-event-v1/calendar-event-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-events" id="ex-sdk-query-events-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-events" id="ex-sdk-query-events-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-events-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-events-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query events in a date range</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">queryEvents (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-events-0"><span className="wix-picker-name">Query events in a date range</span></label>
    <label htmlFor="ex-sdk-query-events-1"><span className="wix-picker-name">queryEvents (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query events in a date range
    import { calendarEvents } from "@wix/promote-growth-tools-marketing-plan";

    const query = {
      filter: {
        date: {
          $gte: "2026-07-01T00:00:00.000Z",
          $lte: "2026-07-31T23:59:59.999Z",
        },
      },
    };

    async function queryEvents() {
      const response = await calendarEvents.queryEvents({ query });
    }

    /* Promise resolves to:
     * {
     *   "events": [
     *     {
     *       "_id": "d7a341c4-196b-46e7-813c-2b0fc1847ec6",
     *       "name": "Summer Sale Launch",
     *       "description": "Kick off our summer promotion across all social channels.",
     *       "date": "2026-07-01T09:00:00.000Z"
     *     }
     *   ]
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript queryEvents (self-hosted)
    import { createClient } from '@wix/sdk';
    import { calendarEvents } from '@wix/promote-growth-tools-marketing-plan';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { calendarEvents },
      // Include the auth strategy and host as relevant
    });

    async function queryEvents(options) {
      const response = await myWixClient.calendarEvents.queryEvents(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
