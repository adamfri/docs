---
title: "Filter and Sort"
description: "The following table shows field support for filters and sorting for the Category object: | Field | Query Filter Operators | Sortable | |-----------------|-----…"
icon: "file-lines"
---

<Note>This article applies to the REST API only.</Note>

The following table shows field support for filters and sorting for the Category object:

| Field           | Query Filter Operators                                                    | Sortable |
|-----------------|---------------------------------------------------------------------------|----------|
| `createdDate`   | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$nin`, `$exists`      | Sortable |
| `name`          | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$nin`, `$exists`      | Sortable |
| `states`        | `$hasSome`                                                                |          |
| `id`            | `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$nin`, `$exists`      |          |

__Related content:__
[API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language),
[Query Categories](fallback::https://dev.wix.com/docs/rest/business-solutions/events/categories/query-categories)
