---
title: "Bulk Delete Project Items"
description: "Deletes multiple project items."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple project items.

    To delete a single project item, call Delete Project Item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/portfolio/project-items/api/v1/bulk/portfolio/items/delete
    ```

    ## Query Params

    <ParamField query="itemIds" type="string[]" required>

      IDs of project items to delete.

      Constraints: `format GUID`, `maxLength 100`, `minItems 1`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkDeleteProjectItemResult[]">

      List of individual Bulk Delete Project Items results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Information about the deleted project item.
          Including its ID, index in the bulk request and whether it was successfully deleted.

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="itemId" type="string">

          ID of the deleted project item.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total number of successes and failures for Bulk Delete Project Items.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Deleted | `wix.portfolio.project_items.v1.project_item_deleted` | Triggered when a project item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/api/v1/bulk/portfolio/items/delete' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "projectId": "f23e4567-e89b-12d3-a456-426614174000",
        "itemIds": [
          "456e7890-e89b-12d3-a456-426614174001",
          "456e7890-e89b-12d3-a456-426614174002"
        ]
      }'
      ```

      ```json Response
      {
        "projectId": "f23e4567-e89b-12d3-a456-426614174000",
        "results": [
          {
            "itemMetadata": {
              "id": "456e7890-e89b-12d3-a456-426614174001",
              "originalIndex": 0,
              "success": true
            },
            "itemId": "456e7890-e89b-12d3-a456-426614174001"
          },
          {
            "itemMetadata": {
              "id": "456e7890-e89b-12d3-a456-426614174002",
              "originalIndex": 1,
              "success": true
            },
            "itemId": "456e7890-e89b-12d3-a456-426614174002"
          }
        ],
        "bulkActionMetadata": {
          "totalSuccesses": 2,
          "totalFailures": 0,
          "undetailedFailures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes multiple project items.

    To delete a single project item, call Delete Project Item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { projectItems } from '@wix/portfolio';

    projectItems.bulkDeleteProjectItems(options): Promise<BulkDeleteProjectItemsResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="BulkDeleteProjectItemsOptions" required>

      options

      <Expandable title="options properties">

        <ParamField body="itemIds" type="string[]" required>

          IDs of project items to delete.

          Constraints: `format GUID`, `maxLength 100`, `minItems 1`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<BulkDeleteProjectItemsResponse>`.

    <ResponseField name="results" type="BulkDeleteProjectItemResult[]">

      List of individual Bulk Delete Project Items results.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Information about the deleted project item.
          Including its ID, index in the bulk request and whether it was successfully deleted.

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="itemId" type="string">

          ID of the deleted project item.

          Constraints: `format GUID`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Total number of successes and failures for Bulk Delete Project Items.

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Project Item Deleted | `wix.portfolio.project_items.v1.project_item_deleted` | Triggered when a project item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-project-items" id="ex-sdk-bulk-delete-project-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-project-items" id="ex-sdk-bulk-delete-project-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-project-items" id="ex-sdk-bulk-delete-project-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-project-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-project-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteProjectItems</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteProjectItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteProjectItems (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-project-items-0"><span className="wix-picker-name">bulkDeleteProjectItems</span></label>
    <label htmlFor="ex-sdk-bulk-delete-project-items-1"><span className="wix-picker-name">bulkDeleteProjectItems (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-project-items-2"><span className="wix-picker-name">bulkDeleteProjectItems (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteProjectItems
    import { projectItems } from '@wix/portfolio';

    async function bulkDeleteProjectItems(options) {
      const response = await projectItems.bulkDeleteProjectItems(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteProjectItems (self-hosted)
    import { createClient } from '@wix/sdk';
    import { projectItems } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { projectItems },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteProjectItems(options) {
      const response = await myWixClient.projectItems.bulkDeleteProjectItems(options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteProjectItems (with elevated permissions)
    import { projectItems } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteProjectItemsMethod(options) {
      const elevatedBulkDeleteProjectItems = auth.elevate(projectItems.bulkDeleteProjectItems);
      const response = await elevatedBulkDeleteProjectItems(options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
