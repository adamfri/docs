---
title: "onReportDeleted"
description: "Triggered when a report is deleted."
icon: "bolt"
---

Triggered when a report is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.reports.v2.report_deleted` |
| Package | `@wix/reports` |
| Permissions | `Manage Reports`, `Read Reports` |

## Examples

<CodeGroup>

  ```javascript onReportDeleted
  import { reports } from '@wix/reports';

  reports.onReportDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onReportDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { reports } from '@wix/reports';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        reports,
      },
  });

  wixClient.reports.onReportDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
