---
title: "onLoyaltyImportCreated"
description: "Triggered when a loyalty import is created."
icon: "bolt"
---

Triggered when a loyalty import is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.loyalty.imports.v1.loyalty_import_created` |
| Package | `@wix/loyalty` |

## Examples

<CodeGroup>

  ```javascript onLoyaltyImportCreated
  import { imports } from '@wix/loyalty';

  imports.onLoyaltyImportCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onLoyaltyImportCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { imports } from '@wix/loyalty';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        imports,
      },
  });

  wixClient.imports.onLoyaltyImportCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
