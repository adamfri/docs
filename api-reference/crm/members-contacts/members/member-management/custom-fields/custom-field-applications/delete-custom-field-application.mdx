---
title: "Delete Custom Field Application"
description: "Deletes a custom field application."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a custom field application.

    When you delete the application, the field is automatically applied to all members.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/members/v1/custom-fields-applications/{customFieldId}
    ```

    ## Path Params

    <ParamField path="customFieldId" type="string" required>

      ID of the custom field with an application to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteCustomFieldApplicationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        https://www.wixapis.com/members/v1/members/custom-fields-applications/4e4b1840-3833-4dbd-93d1-b2c9a33caa7d \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a custom field application.

    When you delete the application, the field is automatically applied to all members.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEMBERS.MANAGE-MEMBERS">Manage Members</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { customFieldApplications } from '@wix/members';

    customFieldApplications.deleteCustomFieldApplication(customFieldId): Promise
    ```

    ## Method Parameters

    <ParamField body="customFieldId" type="string" required>

      ID of the custom field with an application to delete.

    </ParamField>

    ## Returns

    Returns `DeleteCustomFieldApplicationResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Custom Field Application Deleted | `wix.members.v1.custom_field_application_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-custom-field-application" id="ex-sdk-delete-custom-field-application-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-custom-field-application" id="ex-sdk-delete-custom-field-application-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-custom-field-application" id="ex-sdk-delete-custom-field-application-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-custom-field-application-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-custom-field-application-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteCustomFieldApplication</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCustomFieldApplication (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCustomFieldApplication (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-custom-field-application-0"><span className="wix-picker-name">deleteCustomFieldApplication</span></label>
    <label htmlFor="ex-sdk-delete-custom-field-application-1"><span className="wix-picker-name">deleteCustomFieldApplication (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-custom-field-application-2"><span className="wix-picker-name">deleteCustomFieldApplication (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteCustomFieldApplication
    import { customFieldApplications } from '@wix/members';

    async function deleteCustomFieldApplication(customFieldId) {
      const response = await customFieldApplications.deleteCustomFieldApplication(customFieldId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCustomFieldApplication (self-hosted)
    import { createClient } from '@wix/sdk';
    import { customFieldApplications } from '@wix/members';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { customFieldApplications },
      // Include the auth strategy and host as relevant
    });

    async function deleteCustomFieldApplication(customFieldId) {
      const response = await myWixClient.customFieldApplications.deleteCustomFieldApplication(customFieldId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCustomFieldApplication (with elevated permissions)
    import { customFieldApplications } from '@wix/members';
    import { auth } from '@wix/essentials';

    async function myDeleteCustomFieldApplicationMethod(customFieldId) {
      const elevatedDeleteCustomFieldApplication = auth.elevate(customFieldApplications.deleteCustomFieldApplication);
      const response = await elevatedDeleteCustomFieldApplication(customFieldId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
