---
title: "Delete Collection"
description: "Deletes a collection."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a collection.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/portfolio/v1/collections/{collectionId}
    ```

    ## Path Params

    <ParamField path="collectionId" type="string" required>

      ID of the collection to delete.

    </ParamField>

    ## Response Object

    <ResponseField name="collectionId" type="string">

      ID of the deleted collection.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Collection Deleted | `wix.portfolio.collections.v1.collection_deleted` | Triggered when a collection is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/portfolio/v1/collections/123e4567-e89b-12d3-a456-426614174000' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "collectionId": "123e4567-e89b-12d3-a456-426614174000"
      }'
      ```

      ```json Response
      {
        "collectionId": "123e4567-e89b-12d3-a456-426614174000"
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a collection.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.PORTFOLIO.MANAGE-PORTFOLIO">Manage Portfolio</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { collections } from '@wix/portfolio';

    collections.deleteCollection(collectionId): Promise<DeleteCollectionResponse>
    ```

    ## Method Parameters

    <ParamField body="collectionId" type="string" required>

      ID of the collection to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<DeleteCollectionResponse>`.

    <ResponseField name="collectionId" type="string">

      ID of the deleted collection.

      Constraints: `format GUID`.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Collection Deleted | `wix.portfolio.collections.v1.collection_deleted` | Triggered when a collection is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-collection" id="ex-sdk-delete-collection-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-collection" id="ex-sdk-delete-collection-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-collection" id="ex-sdk-delete-collection-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-collection-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-collection-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteCollection</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCollection (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-collection-0"><span className="wix-picker-name">deleteCollection</span></label>
    <label htmlFor="ex-sdk-delete-collection-1"><span className="wix-picker-name">deleteCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-collection-2"><span className="wix-picker-name">deleteCollection (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteCollection
    import { collections } from '@wix/portfolio';

    async function deleteCollection(collectionId) {
      const response = await collections.deleteCollection(collectionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCollection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { collections } from '@wix/portfolio';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { collections },
      // Include the auth strategy and host as relevant
    });

    async function deleteCollection(collectionId) {
      const response = await myWixClient.collections.deleteCollection(collectionId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCollection (with elevated permissions)
    import { collections } from '@wix/portfolio';
    import { auth } from '@wix/essentials';

    async function myDeleteCollectionMethod(collectionId) {
      const elevatedDeleteCollection = auth.elevate(collections.deleteCollection);
      const response = await elevatedDeleteCollection(collectionId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
