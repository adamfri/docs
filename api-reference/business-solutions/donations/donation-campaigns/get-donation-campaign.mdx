---
title: "Get Donation Campaign"
description: "Retrieves a donation campaign by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a donation campaign by ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DONATIONS.MANAGE-CAMPAIGNS">Manage Donation Campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/donation-campaigns/v2/donation-campaigns/{donationCampaignId}
    ```

    ## Path Params

    <ParamField path="donationCampaignId" type="string" required>

      ID of the donation campaign to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="donationCampaign" type="DonationCampaign">

      Retrieved donation campaign.

      See the [Donation Campaign Object](/api-reference/business-solutions/donations/donation-campaigns/donation-campaign-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `SITE_CURRENCY_UNAVAILABLE` | Site currency settings are temporarily unavailable. Try again later. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves a donation campaign by its ID

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/donation-campaigns/v2/donation-campaigns/87f8066e-f0e9-41da-b4ca-4bceb2ecdf31' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json'
      ```

      ```json Response
      {
        "donationCampaign": {
          "id": "87f8066e-f0e9-41da-b4ca-4bceb2ecdf31",
          "revision": "2",
          "createdDate": "2025-07-21T10:44:05.043Z",
          "updatedDate": "2025-07-21T10:53:06.206Z",
          "name": "Wildlife Protection Fund",
          "archived": true,
          "status": "COLLECTING",
          "predefinedDonationAmounts": [
            {
              "id": "549d4e0e-5311-4008-8e4c-8354e6f052b0",
              "price": {
                "amount": "50",
                "convertedAmount": "50",
                "formattedAmount": "€50.00",
                "formattedConvertedAmount": "€50.00"
              },
              "description": "Protect one animal for a month"
            }
          ],
          "campaignGoal": {
            "targetAmount": {
              "amount": "25000",
              "convertedAmount": "25000",
              "formattedAmount": "€25,000.00",
              "formattedConvertedAmount": "€25,000.00"
            }
          },
          "commentsEnabled": false,
          "donationFrequencies": [
            "ONE_TIME",
            "MONTH"
          ],
          "askDonorCoverFee": true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a donation campaign by ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DONATIONS.MANAGE-CAMPAIGNS">Manage Donation Campaigns</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { donationCampaigns } from '@wix/donations';

    donationCampaigns.getDonationCampaign(donationCampaignId): Promise<DonationCampaign>
    ```

    ## Method Parameters

    <ParamField body="donationCampaignId" type="string" required>

      ID of the donation campaign to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<DonationCampaign>`.

    See the [Donation Campaign Object](/api-reference/business-solutions/donations/donation-campaigns/donation-campaign-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `500` | `INTERNAL` | `SITE_CURRENCY_UNAVAILABLE` | Site currency settings are temporarily unavailable. Try again later. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-donation-campaign" id="ex-sdk-get-donation-campaign-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-donation-campaign" id="ex-sdk-get-donation-campaign-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-donation-campaign-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-donation-campaign-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getDonationCampaign</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getDonationCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-donation-campaign-0"><span className="wix-picker-name">getDonationCampaign</span></label>
    <label htmlFor="ex-sdk-get-donation-campaign-1"><span className="wix-picker-name">getDonationCampaign (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getDonationCampaign
    import { donationCampaigns } from '@wix/donations';

    async function getDonationCampaign(donationCampaignId) {
      const response = await donationCampaigns.getDonationCampaign(donationCampaignId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getDonationCampaign (self-hosted)
    import { createClient } from '@wix/sdk';
    import { donationCampaigns } from '@wix/donations';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { donationCampaigns },
      // Include the auth strategy and host as relevant
    });

    async function getDonationCampaign(donationCampaignId) {
      const response = await myWixClient.donationCampaigns.getDonationCampaign(donationCampaignId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
