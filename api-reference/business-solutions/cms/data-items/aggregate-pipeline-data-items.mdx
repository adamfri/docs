---
title: "Aggregate Pipeline Data Items"
description: "Runs an aggregation pipeline on a data collection and returns the results."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Runs an aggregation pipeline on a data collection and returns the results.

    Aggregations enable you to perform advanced data calculations, transformations, and groupings. While [Aggregate Data Items](https://dev.wix.com/docs/rest/business-solutions/cms/data-items/aggregate-data-items) allows you to perform basic aggregations, Aggregate Pipeline Data Items allows you to perform advanced aggregations sequentially in stages, with each stage transforming the results of the previous stage.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.READ">Read Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/data/v2/items/aggregate-pipeline
    ```

    ## Body Params

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection to run the aggregation pipeline on.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="pipeline" type="AggregationPipeline">

      Aggregation pipeline configuration containing stages and paging options.

      <Expandable title="pipeline properties">

        <ParamField body="stages" type="Stage[]">

          Sequence of aggregation operations to apply to the data.

          Stages are processed in order, with each stage operating on the output of the previous stage.

          Constraints: `maxItems 100`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of results.

              Learn more about [sorting and paging](https://dev.wix.com/docs/rest/articles/get-started/sorting-and-paging).

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token referring to a page of results.

              Not used in the first request. Subsequent requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="returnTotalCount" type="boolean">

      Whether to return the total result count when running a query with offset paging. When `true`, the `pagingMetadata` property in the response contains a `total` field.

      Learn more about [offset paging](https://dev.wix.com/docs/rest/articles/get-started/sorting-and-paging#paging).

      Default: `false`.

    </ParamField>

    <ParamField body="consistentRead" type="boolean">

      Whether to read from the primary database for the most current data.

      This decreases performance but ensures data retrieved is up to date even immediately after an update.

      Learn more about [Wix Data and eventual consistency](https://dev.wix.com/api/rest/wix-data/wix-data/eventual-consistency).

      Default: `false`.

    </ParamField>

    <ParamField body="language" type="string">

      Language to run the aggregation in, in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.

      When provided, the aggregation runs on items in the specified language. When not provided, the aggregation runs on items in the default language.

      > **Note:** The Wix user must enable translation for the specified language in [Wix Multilingual](https://www.wix.com/app-market/wix-multilingual).

      Learn more about [using Wix Multilingual to translate CMS content](https://support.wix.com/en/article/wix-multilingual-translating-cms-collection-content).

    </ParamField>

    <ParamField body="appOptions" type="object">

      Additional parameters specific to the [Wix app collection](https://support.wix.com/en/article/cms-formerly-content-manager-working-with-wix-app-collections) you are querying.

      Learn more about [querying Wix app collections](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-app-collections/querying-wix-app-collections).

    </ParamField>

    <ParamField body="publishPluginOptions" type="PublishPluginOptions">

      Options for the Publish plugin.

      This plugin allows items in a [data collection](https://dev.wix.com/docs/rest/business-solutions/cms/data-collections/data-collection-object) to be marked as draft or published. Published items are visible to site visitors, while draft items aren't.

      <Expandable title="publishPluginOptions properties">

        <ParamField body="includeDraftItems" type="boolean">

          Whether to include draft items.

          When `true`, both published and draft items are affected. Default: `false`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="object[]">

      Aggregation results.

      Constraints: `maxItems 10000`.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging information.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used, `returnTotalCount` is `true` in the request, and `tooManyToCount` is false.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Whether the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-aggregate-pipeline-data-items" id="ex-rest-aggregate-pipeline-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-aggregate-pipeline-data-items" id="ex-rest-aggregate-pipeline-data-items-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-aggregate-pipeline-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-aggregate-pipeline-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Find the 5 most densely populated cities with 500K+ residents</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Group cities by state and calculate average population per state</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-aggregate-pipeline-data-items-0"><span className="wix-picker-name">Find the 5 most densely populated cities with 500K+ residents</span></label>
    <label htmlFor="ex-rest-aggregate-pipeline-data-items-1"><span className="wix-picker-name">Group cities by state and calculate average population per state</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items/aggregate-pipeline' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "dataCollectionId": "cities",
        "pipeline": {
          "stages": [
            {"filter": {"population": {"$gt": 500000}}},
            {"projection": {
              "fields": [
                {"include": {"fieldName": "city"}},
                {"include": {"fieldName": "state"}},
                {"include": {"fieldName": "population"}},
                {"include": {"fieldName": "area"}},
                {"reshape": {
                  "resultFieldName": "populationDensity",
                  "expression": {"divide": {
                    "firstExpression": {"fieldPath": "population"},
                    "secondExpression": {"fieldPath": "area"}
                  }}
                }},
                {"reshape": {
                  "resultFieldName": "cityInfo",
                  "expression": {"concat": {
                    "expressions": [{"fieldPath": "city"}, {"text": ", "}, {"fieldPath": "state"}]
                  }}
                }}
              ]
            }},
            {"sort": {"values": [{"fieldName": "populationDensity", "order": "DESC"}]}},
            {"limit": 5}
          ]
        }
      }'
      ```

      ```json Response
      {
        "results": [
          {"city": "New York", "area": 783.8, "populationDensity": 10717, "state": "New York", "_id": "56eadaa7-2fe1-4127-9c35-13f147c35fec", "population": 8400000, "cityInfo": "New York, New York"},
          {"city": "San Francisco", "area": 121.4, "populationDensity": 6919, "state": "California", "_id": "f27ee2f2-4bc1-47f3-bee4-1bc88b762294", "population": 840000, "cityInfo": "San Francisco, California"}
        ],
        "pagingMetadata": {"count": 2, "tooManyToCount": false}
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/wix-data/v2/items/aggregate-pipeline' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "dataCollectionId": "cities",
        "pipeline": {
          "stages": [
            {"filter": {"year": 2022}},
            {"group": {
              "groupIds": [{"key": "state", "expression": {"fieldPath": "state"}}],
              "accumulators": [
                {"resultFieldName": "avgPopulation", "avg": {"expression": {"fieldPath": "population"}}},
                {"resultFieldName": "cityCount", "sum": {"expression": {"numeric": 1}}}
              ]
            }},
            {"sort": {"values": [{"fieldName": "avgPopulation", "order": "DESC"}]}}
          ]
        }
      }'
      ```

      ```json Response
      {
        "results": [
          {"_id": {"state": "Illinois"}, "avgPopulation": 2700000, "cityCount": 1, "state": "Illinois"},
          {"_id": {"state": "Florida"}, "avgPopulation": 390000, "cityCount": 2, "state": "Florida"}
        ],
        "pagingMetadata": {"count": 2, "tooManyToCount": false}
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Runs an aggregation pipeline on a data collection and returns the results.

    Aggregations enable you to perform advanced data calculations, transformations, and groupings. While [Aggregate Data Items](https://dev.wix.com/docs/rest/business-solutions/cms/data-items/aggregate-data-items) allows you to perform basic aggregations, Aggregate Pipeline Data Items allows you to perform advanced aggregations sequentially in stages, with each stage transforming the results of the previous stage.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-DATA.READ">Read Data Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { items } from '@wix/data';

    items.aggregatePipeline(dataCollectionId, pipeline, options): Promise
    ```

    ## Method Parameters

    <ParamField body="dataCollectionId" type="string" required>

      ID of the collection to run the aggregation pipeline on.

      Constraints: `maxLength 256`.

    </ParamField>

    <ParamField body="pipeline" type="AggregationPipeline" required>

      Aggregation pipeline configuration containing stages and paging options.

      <Expandable title="pipeline properties">

        <ParamField body="stages" type="Stage[]">

          Sequence of aggregation operations to apply to the data.

          Stages are processed in order, with each stage operating on the output of the previous stage.

          Constraints: `maxItems 100`.

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="paging" type="Paging">

              Paging options to limit and skip the number of results.

              Learn more about [sorting and paging](https://dev.wix.com/docs/rest/articles/get-started/sorting-and-paging).

              _Nested structure omitted for brevity — see the `Paging` object._

            </ParamField>

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token referring to a page of results.

              Not used in the first request. Subsequent requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="WixDataAggregateOptions">

      Options to use when processing this operation.

      <Expandable title="options properties">

        <ParamField body="suppressHooks" type="boolean">

          Prevents hooks from running for the operation. Can only be used in the [backend code of a Wix site](https://dev.wix.com/docs/develop-websites/articles/coding-with-velo/backend-code/about-the-site-backend).

        </ParamField>

        <ParamField body="showDrafts" type="boolean">

          When `true`, operations include draft items. Read operations include draft items in their response, and write operations modify draft items.
          Default: `false`.

        </ParamField>

        <ParamField body="appOptions" type="object">

          Options for [querying Wix app collections](https://dev.wix.com/docs/develop-websites/articles/wix-apps/wix-app-collections/querying-wix-app-collections).

        </ParamField>

        <ParamField body="language" type="string">

          Language to translate result text into, in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
          If provided, the result text is returned in the specified language. If not provided, the result text is not translated.

          > **Note:** Translation for the specified language must be enabled for the collection in [Wix Multilingual](https://www.wix.com/app-market/wix-multilingual).

        </ParamField>

        <ParamField body="consistentRead" type="boolean">

          When `true`, reads data from the primary database instance. This decreases performance but ensures data retrieved is up-to-date even immediately after an update. Learn more about [Wix Data and eventual consistency](https://dev.wix.com/docs/sdk/backend-modules/data/eventual-consistency).

        </ParamField>

        <ParamField body="returnTotalCount" type="boolean">

          When `true`, the query results include a `totalCount` and `totalPages` properties containing the totals of items matching query. Requesting total count slows down the query.

          Default: `false`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="results" type="object[]">

      Aggregation results.

      Constraints: `maxItems 10000`.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="PagingMetadataV2">

      Paging information.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in the response.

        </ResponseField>

        <ResponseField name="offset" type="integer">

          Offset that was requested.

        </ResponseField>

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used, `returnTotalCount` is `true` in the request, and `tooManyToCount` is false.

        </ResponseField>

        <ResponseField name="tooManyToCount" type="boolean">

          Whether the server failed to calculate the `total` field.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor pointing to next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-aggregate-pipeline-data-items" id="ex-sdk-aggregate-pipeline-data-items-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-aggregate-pipeline-data-items" id="ex-sdk-aggregate-pipeline-data-items-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-aggregate-pipeline-data-items" id="ex-sdk-aggregate-pipeline-data-items-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-aggregate-pipeline-data-items-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-aggregate-pipeline-data-items-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">aggregatePipeline</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">aggregatePipeline (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">aggregatePipeline (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-aggregate-pipeline-data-items-0"><span className="wix-picker-name">aggregatePipeline</span></label>
    <label htmlFor="ex-sdk-aggregate-pipeline-data-items-1"><span className="wix-picker-name">aggregatePipeline (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-aggregate-pipeline-data-items-2"><span className="wix-picker-name">aggregatePipeline (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript aggregatePipeline
    import { items } from '@wix/data';

    async function aggregatePipeline(dataCollectionId,pipeline,options) {
      const response = await items.aggregatePipeline(dataCollectionId,pipeline,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript aggregatePipeline (self-hosted)
    import { createClient } from '@wix/sdk';
    import { items } from '@wix/data';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { items },
      // Include the auth strategy and host as relevant
    });

    async function aggregatePipeline(dataCollectionId,pipeline,options) {
      const response = await myWixClient.items.aggregatePipeline(dataCollectionId,pipeline,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript aggregatePipeline (with elevated permissions)
    import { items } from '@wix/data';
    import { auth } from '@wix/essentials';

    async function myAggregatePipelineMethod(dataCollectionId,pipeline,options) {
      const elevatedAggregatePipeline = auth.elevate(items.aggregatePipeline);
      const response = await elevatedAggregatePipeline(dataCollectionId,pipeline,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
