---
title: "Delete Billable Item"
description: "Permanently deletes a billable item."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a billable item.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BILLABLE_ITEMS.MANAGE-BILLABLE-ITEMS">Manage Billable Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/billable-items/v1/billable-items/{billableItemId}
    ```

    ## Path Params

    <ParamField path="billableItemId" type="string" required>

      Billable item ID to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteBillableItemResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Billable Item Deleted | `wix.billable_items.v1.billable_item_deleted` | Triggered when a billable item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a Billable Item

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/billable-items/v1/billable-items/8046df3c-7575-4098-a5ab-c91ad8f33c47' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Permanently deletes a billable item.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.BILLABLE_ITEMS.MANAGE-BILLABLE-ITEMS">Manage Billable Items</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { billableItems } from '@wix/get-paid';

    billableItems.deleteBillableItem(billableItemId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="billableItemId" type="string" required>

      Billable item ID to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Billable Item Deleted | `wix.billable_items.v1.billable_item_deleted` | Triggered when a billable item is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-billable-item" id="ex-sdk-delete-billable-item-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-billable-item" id="ex-sdk-delete-billable-item-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-billable-item" id="ex-sdk-delete-billable-item-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-billable-item-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-billable-item-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">deleteBillableItem</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBillableItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteBillableItem (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-billable-item-0"><span className="wix-picker-name">deleteBillableItem</span></label>
    <label htmlFor="ex-sdk-delete-billable-item-1"><span className="wix-picker-name">deleteBillableItem (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-delete-billable-item-2"><span className="wix-picker-name">deleteBillableItem (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript deleteBillableItem
    import { billableItems } from '@wix/get-paid';

    async function deleteBillableItem(billableItemId) {
      const response = await billableItems.deleteBillableItem(billableItemId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBillableItem (self-hosted)
    import { createClient } from '@wix/sdk';
    import { billableItems } from '@wix/get-paid';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { billableItems },
      // Include the auth strategy and host as relevant
    });

    async function deleteBillableItem(billableItemId) {
      const response = await myWixClient.billableItems.deleteBillableItem(billableItemId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteBillableItem (with elevated permissions)
    import { billableItems } from '@wix/get-paid';
    import { auth } from '@wix/essentials';

    async function myDeleteBillableItemMethod(billableItemId) {
      const elevatedDeleteBillableItem = auth.elevate(billableItems.deleteBillableItem);
      const response = await elevatedDeleteBillableItem(billableItemId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
