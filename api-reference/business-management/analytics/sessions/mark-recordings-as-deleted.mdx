---
title: "Mark Recordings As Deleted"
description: "Marks browser session recordings as deleted."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Marks browser session recordings as deleted.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ANALYTICS-AND-REPORTS.MANAGE-SESSIONS">Manage Session Recording Analytics - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/analytics/v1/sessions/recordings-deleted
    ```

    ## Body Params

    <ParamField body="sessionIds" type="string[]" required>

      Browser session IDs.

      Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    Returns `MarkRecordingsAsDeletedResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/analytics/v1/sessions/recordings-deleted' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "sessionIds": ["7f08f28b-f4ca-47be-879e-c871966ee0c0", "29c8ae61-88b3-4589-b14a-9faaaa7fb8be"]
        }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Marks browser session recordings as deleted.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ANALYTICS-AND-REPORTS.MANAGE-SESSIONS">Manage Session Recording Analytics - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { analyticsSession } from '@wix/analytics-session';

    analyticsSession.markRecordingsAsDeleted(sessionIds): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="sessionIds" type="string[]" required>

      Browser session IDs.

      Constraints: `minLength 1`, `maxLength 100`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-mark-recordings-as-deleted" id="ex-sdk-mark-recordings-as-deleted-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-mark-recordings-as-deleted" id="ex-sdk-mark-recordings-as-deleted-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-mark-recordings-as-deleted" id="ex-sdk-mark-recordings-as-deleted-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-mark-recordings-as-deleted-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-mark-recordings-as-deleted-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">markRecordingsAsDeleted</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markRecordingsAsDeleted (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">markRecordingsAsDeleted (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-mark-recordings-as-deleted-0"><span className="wix-picker-name">markRecordingsAsDeleted</span></label>
    <label htmlFor="ex-sdk-mark-recordings-as-deleted-1"><span className="wix-picker-name">markRecordingsAsDeleted (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-mark-recordings-as-deleted-2"><span className="wix-picker-name">markRecordingsAsDeleted (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript markRecordingsAsDeleted
    import { analyticsSession } from '@wix/analytics-session';

    async function markRecordingsAsDeleted(sessionIds) {
      const response = await analyticsSession.markRecordingsAsDeleted(sessionIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markRecordingsAsDeleted (self-hosted)
    import { createClient } from '@wix/sdk';
    import { analyticsSession } from '@wix/analytics-session';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { analyticsSession },
      // Include the auth strategy and host as relevant
    });

    async function markRecordingsAsDeleted(sessionIds) {
      const response = await myWixClient.analyticsSession.markRecordingsAsDeleted(sessionIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript markRecordingsAsDeleted (with elevated permissions)
    import { analyticsSession } from '@wix/analytics-session';
    import { auth } from '@wix/essentials';

    async function myMarkRecordingsAsDeletedMethod(sessionIds) {
      const elevatedMarkRecordingsAsDeleted = auth.elevate(analyticsSession.markRecordingsAsDeleted);
      const response = await elevatedMarkRecordingsAsDeleted(sessionIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
