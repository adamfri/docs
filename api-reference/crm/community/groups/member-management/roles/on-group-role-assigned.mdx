---
title: "onGroupRoleAssigned"
description: "Triggered when a role is assigned to an existing group member."
icon: "bolt"
---

Triggered when a role is assigned to an existing group member.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.social_groups.v2.group_role_assigned` |
| Package | `@wix/groups` |

## Examples

<CodeGroup>

  ```javascript onGroupRoleAssigned
  import { roles } from '@wix/groups';

  roles.onGroupRoleAssigned((event) => {
      // handle your event here
  });
  ```

  ```javascript onGroupRoleAssigned (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { roles } from '@wix/groups';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        roles,
      },
  });

  wixClient.roles.onGroupRoleAssigned((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
