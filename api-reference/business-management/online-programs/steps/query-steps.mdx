---
title: "Query Steps"
description: "Retrieves a list of steps, given the provided paging, filtering, and sorting."
icon: "upload"
tag: "Preview"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of steps, given the provided paging, filtering, and sorting.

    Query Steps runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.offset` is `0`.

    For predictable response sizes, we recommend setting `paging.limit` up to `100`.

    If your app is already scoped to a specific program, pass `programId` in addition to the query filters. `programId` adds program context to the request and doesn't replace section-based filtering in `query`.
    Supported sorting fields are `createdDate`, `id`, `programId`, and `ordering`. When sorting by `ordering`, also filter by `sectionId`.
    Use `descriptionFieldSet` to control how much description content to return.
    Some returned fields depend on the caller's permissions.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `sectionId` | `$eq`, `$in` | None |
    | `createdDate` |  | Sort: `ASC`, `DESC` |
    | `ordering` |  | Sort: `ASC`, `DESC` |
    | `id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `programId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/online-programs/v3/steps/query
    ```

    ## Body Params

    <ParamField body="query" type="CursorQuery">

      Query object.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="descriptionFieldSet" type="string">

      Predefined set of description fields to return.

      **Supported values:**

      - `STANDARD` — Returns the standard step description fields.
      - `EXTENDED` — Returns additional step description fields when they're available to the caller.

    </ParamField>

    <ParamField body="programId" type="string">

      Program ID.

      Constraints: `minLength 0`, `maxLength 36`.

    </ParamField>

    ## Response Object

    <ResponseField name="steps" type="Step[]">

      Retrieved steps.

      See the [Step Object](/api-reference/business-management/online-programs/steps/step-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    Retrieves steps in a section using cursor-based paging.

    <CodeGroup>

      ```bash Request
      curl -X POST \
      'https://www.wixapis.com/online-programs/v3/steps/query' \
      -H 'Authorization: <AUTH>' \
      -H 'Content-Type: application/json' \
      -d '{
        "query": {
          "filter": {
            "sectionId": "b142d26b-2c56-4b85-9f8d-6bea0920eb4d"
          },
          "sort": [
            {
              "fieldName": "ordering",
              "order": "ASC"
            }
          ],
          "cursorPaging": {
            "limit": 10
          }
        },
        "descriptionFieldSet": "EXTENDED"
      }'
      ```

      ```json Response
      {
        "steps": [
          {
            "id": "3d6af890-6411-49b2-a4fb-8a63ae520f78",
            "revision": "2",
            "createdDate": "2026-08-18T09:54:08.770Z",
            "updatedDate": "2026-08-18T09:55:31.214Z",
            "sectionId": "b142d26b-2c56-4b85-9f8d-6bea0920eb4d",
            "description": {
              "title": "Prepare your workspace and materials",
              "details": "Set up the tools and materials you need before starting."
            },
            "ordering": 20,
            "stepType": "ARTICLE",
            "programId": "9a76e7d5-235a-4821-8f36-2d8bd0e7e9c1"
          },
          {
            "id": "e87125b7-9ad1-4d9d-85c7-24765fab16c8",
            "revision": "2",
            "createdDate": "2026-08-18T09:54:08.770Z",
            "updatedDate": "2026-08-18T09:55:31.214Z",
            "sectionId": "b142d26b-2c56-4b85-9f8d-6bea0920eb4d",
            "description": {
              "title": "Share your learning goals",
              "details": "Write down the outcome you want to achieve this week."
            },
            "ordering": 30,
            "stepType": "ARTICLE",
            "programId": "9a76e7d5-235a-4821-8f36-2d8bd0e7e9c1"
          }
        ],
        "pagingMetadata": {
          "count": 2,
          "cursors": {},
          "hasNext": false
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Info>

      **Developer Preview.** This method is in Developer Preview and may change without notice.

    </Info>

    Retrieves a list of steps, given the provided paging, filtering, and sorting.

    Query Steps runs with these defaults, which you can override:

    - `createdDate` is sorted in `DESC` order.
    - `paging.offset` is `0`.

    For predictable response sizes, we recommend setting `paging.limit` up to `100`.

    If your app is already scoped to a specific program, pass `programId` in addition to the query filters. `programId` adds program context to the request and doesn't replace section-based filtering in `query`.
    Supported sorting fields are `createdDate`, `id`, `programId`, and `ordering`. When sorting by `ordering`, also filter by `sectionId`.
    Use `descriptionFieldSet` to control how much description content to return.
    Some returned fields depend on the caller's permissions.

    To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).

    | Property | Filters | Sort |
    | --- | --- | --- |
    | `sectionId` | `$eq`, `$in` | None |
    | `_createdDate` |  | Sort: `ASC`, `DESC` |
    | `ordering` |  | Sort: `ASC`, `DESC` |
    | `_id` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |
    | `programId` | `$eq`, `$ne`, `$in`, `$exists`, `$gt`, `$gte`, `$lt`, `$lte`, `$startsWith` | Sort: `ASC`, `DESC` |

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.CHALLENGES.MANAGE">Manage Online Programs</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { steps } from '@wix/online-programs';

    steps.querySteps(query, options): Promise
    ```

    ## Method Parameters

    <ParamField body="query" type="StepQuery" required>

      Query object.

      <Expandable title="query properties">

        <ParamField body="filter" type="object">

          Filter object in the following format:
          `"filter" : {`
          "fieldName1": "value1",
          "fieldName2":&#123;"$operator":"value2"&#125;
          &#125;``
          Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`

        </ParamField>

        <ParamField body="sort" type="Sorting[]">

          Sort object in the following format:
          `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="pagingMethod" type="pagingMethod">

          pagingMethod

          <Expandable title="pagingMethod properties">

            <ParamField body="cursorPaging" type="CursorPaging">

              Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.

              _Nested structure omitted for brevity — see the `CursorPaging` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="QueryStepsOptions">

      options

      <Expandable title="options properties">

        <ParamField body="descriptionFieldSet" type="string">

          Predefined set of description fields to return.

          **Supported values:**

          - `STANDARD` — Returns the standard step description fields.
          - `EXTENDED` — Returns additional step description fields when they're available to the caller.

        </ParamField>

        <ParamField body="programId" type="string">

          Program ID.

          Constraints: `minLength 0`, `maxLength 36`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    <ResponseField name="steps" type="Step[]">

      Retrieved steps.

      See the [Step Object](/api-reference/business-management/online-programs/steps/step-object) for this object's fields.

    </ResponseField>

    <ResponseField name="pagingMetadata" type="CursorPagingMetadata">

      Paging metadata.

      <Expandable title="pagingMetadata properties">

        <ResponseField name="count" type="integer">

          Number of items returned in current page.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursor strings that point to the next page, previous page, or both.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

            <ResponseField name="prev" type="string">

              Cursor pointing to the previous page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="hasNext" type="boolean">

          Whether there are more pages to retrieve following the current page.

          + `true`: Another page of results can be retrieved.
          + `false`: This is the last page.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-query-steps" id="ex-sdk-query-steps-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-query-steps" id="ex-sdk-query-steps-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-query-steps-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-query-steps-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Query steps in a section</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">querySteps (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-query-steps-0"><span className="wix-picker-name">Query steps in a section</span></label>
    <label htmlFor="ex-sdk-query-steps-1"><span className="wix-picker-name">querySteps (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Query steps in a section
    import { steps } from "@wix/online-programs";

    const stepsPage = await steps.querySteps(
      {
        filter: {
          sectionId: "b142d26b-2c56-4b85-9f8d-6bea0920eb4d",
        },
        sort: [
          {
            fieldName: "ordering",
            order: "ASC",
          },
        ],
        cursorPaging: {
          limit: 10,
        },
      },
      { descriptionFieldSet: "EXTENDED" },
    );

    /* Promise resolves to:
    {
      "steps": [
        {
          "_id": "3d6af890-6411-49b2-a4fb-8a63ae520f78",
          "revision": "2",
          "_createdDate": "2026-08-18T09:54:08.770Z",
          "_updatedDate": "2026-08-18T09:55:31.214Z",
          "sectionId": "b142d26b-2c56-4b85-9f8d-6bea0920eb4d",
          "description": {
            "title": "Prepare your workspace and materials",
            "details": "Set up the tools and materials you need before starting."
          },
          "ordering": 20,
          "stepType": "ARTICLE",
          "programId": "9a76e7d5-235a-4821-8f36-2d8bd0e7e9c1"
        },
        {
          "_id": "e87125b7-9ad1-4d9d-85c7-24765fab16c8",
          "revision": "2",
          "_createdDate": "2026-08-18T09:54:08.770Z",
          "_updatedDate": "2026-08-18T09:55:31.214Z",
          "sectionId": "b142d26b-2c56-4b85-9f8d-6bea0920eb4d",
          "description": {
            "title": "Share your learning goals",
            "details": "Write down the outcome you want to achieve this week."
          },
          "ordering": 30,
          "stepType": "ARTICLE",
          "programId": "9a76e7d5-235a-4821-8f36-2d8bd0e7e9c1"
        }
      ],
      "pagingMetadata": {
        "count": 2,
        "cursors": {},
        "hasNext": false
      }
    }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript querySteps (self-hosted)
    import { createClient } from '@wix/sdk';
    import { steps } from '@wix/online-programs';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { steps },
      // Include the auth strategy and host as relevant
    });

    async function querySteps(query,options) {
      const response = await myWixClient.steps.querySteps(query,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
