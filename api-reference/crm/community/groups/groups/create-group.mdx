---
title: "Create Group"
description: "Creates a group."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a group.

    When a group is created, the newly created group is added to the [Groups List](https://support.wix.com/en/article/wix-groups-about-your-groups-pages#groups-group-list) page
    of a site.

    Specify a `creatorId` to set the group's creator. The group's creator will automatically become a group admin. See [Terminology](https://dev.wix.com/docs/rest/crm/community/groups/terminology) for more information on roles.

    Wix users determine who can create a group.
    This setting can be found in [a site's Dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Select%20Site&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Fwix-groups/settings?) under **Groups > General Settings > Group Creation**.
    If set to **members with approval**, site members can create a group with the Create Group method, and the group becomes a `createRequest` with a status of `PENDING`.
    A Wix user either approves or rejects the request to create a group.
    If set to **all site members**, site members can create a group with the Create Group method and no approval is required.
    If set to **only admins**, only Wix users can create a group with the Create Group method.
    Default is set to **members with approval**.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/social-groups-proxy/groups/v2/groups
    ```

    ## Body Params

    <ParamField body="group" type="Group" required>

      Group to create.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ParamField>

    <ParamField body="creatorId" type="string">

      ID of the member who created the group, from the Members API. This member will automatically become an admin.

    </ParamField>

    <ParamField body="contentType" type="string">

      Content type.

      **Supported values:**

      - `PLAIN_TEXT`
      - `DRAFTJS`
      - `RICH_CONTENT`

    </ParamField>

    ## Response Object

    <ResponseField name="group" type="Group">

      Created group.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Group Created | `wix.social_groups.v2.group_created` | Triggered when a group is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X POST \
          https://www.wixapis.com/social-groups-proxy/groups/v2/groups \
          -H 'Content-Type: application/json;charset=UTF-8' \
          -H 'Authorization: <AUTH>'
          -d '{
              "group": {
                  "privacyStatus": "PUBLIC",
                  "name": "Group 1",
                  "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
                  "memberTitle": "Friends",
                  "coverImage": {
                      "image": {
                          "mediaId": "4d6eab_666134ee68464d76aa838619344c4369~mv2.jpg",
                          "width": 1024,
                          "height": 150
                      }
                  },
                  "settings": {
                      "membersCanInvite": false,
                      "membersCanApprove": false,
                      "welcomeMemberPostEnabled": true,
                      "groupDetailsChangedPostEnabled": true,
                      "showMemberList": true
                  }
              },
              "creatorId": "f98e4407-15bf-43a3-87fb-a954c49bb20f"
          }'
      ~~~
      ```

      ```json Response
      ~~~json
      {
          "group": {
              "id": "8a258cf5-49f3-41dc-b320-65ec2598315b",
              "slug": "group-1",
              "privacyStatus": "PUBLIC",
              "name": "Group 1",
              "description": "Welcome to the group! You can connect with other members, get updates and share videos.",
              "memberTitle": "Friends",
              "coverImage": {
                  "image": {
                      "mediaId": "4d6eab_666134ee68464d76aa838619344c4369~mv2.jpg",
                      "width": 1024,
                      "height": 150
                  }
              },
              "settings": {
                  "membersCanInvite": false,
                  "membersCanApprove": false,
                  "welcomeMemberPostEnabled": true,
                  "groupDetailsChangedPostEnabled": true,
                  "showMemberList": true
              },
              "membersCount": 1,
              "ownerId": "f98e4407-15bf-43a3-87fb-a954c49bb20f",
              "createdDate": "2020-11-06T14:03:50Z"
          }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates a group.

    When a group is created, the newly created group is added to the [Groups List](https://support.wix.com/en/article/wix-groups-about-your-groups-pages#groups-group-list) page
    of a site.

    Specify a `creatorId` to set the group's creator. The group's creator will automatically become a group admin. See [Terminology](https://dev.wix.com/docs/rest/crm/community/groups/terminology) for more information on roles.

    Wix users determine who can create a group.
    This setting can be found in [a site's Dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Select%20Site&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Fwix-groups/settings?) under **Groups > General Settings > Group Creation**.
    If set to **members with approval**, site members can create a group with the Create Group method, and the group becomes a `createRequest` with a status of `PENDING`.
    A Wix user either approves or rejects the request to create a group.
    If set to **all site members**, site members can create a group with the Create Group method and no approval is required.
    If set to **only admins**, only Wix users can create a group with the Create Group method.
    Default is set to **members with approval**.

    ## Method Declaration

    ```typescript
    import { groups } from '@wix/groups';

    groups.createGroup(group, options): Promise<Group>
    ```

    ## Method Parameters

    <ParamField body="group" type="Group" required>

      Group to create.

      See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="CreateGroupOptions">

      Optional fields for group creation. 

      <Expandable title="options properties">

        <ParamField body="creatorId" type="string">

          ID of the member who created the group, from the Members API. This member will automatically become an admin.

        </ParamField>

        <ParamField body="contentType" type="string">

          Content type.

          **Supported values:**

          - `PLAIN_TEXT`
          - `DRAFTJS`
          - `RICH_CONTENT`

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Group>`.

    See the [Group Object](/api-reference/crm/community/groups/groups/group-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Group Created | `wix.social_groups.v2.group_created` | Triggered when a group is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-group" id="ex-sdk-create-group-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-group" id="ex-sdk-create-group-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-create-group" id="ex-sdk-create-group-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-group-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-group-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">createGroup</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createGroup (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-group-0"><span className="wix-picker-name">createGroup</span></label>
    <label htmlFor="ex-sdk-create-group-1"><span className="wix-picker-name">createGroup (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-create-group-2"><span className="wix-picker-name">createGroup (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript createGroup
    import { groups } from '@wix/groups';

    async function createGroup(group,options) {
      const response = await groups.createGroup(group,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createGroup (self-hosted)
    import { createClient } from '@wix/sdk';
    import { groups } from '@wix/groups';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { groups },
      // Include the auth strategy and host as relevant
    });

    async function createGroup(group,options) {
      const response = await myWixClient.groups.createGroup(group,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createGroup (with elevated permissions)
    import { groups } from '@wix/groups';
    import { auth } from '@wix/essentials';

    async function myCreateGroupMethod(group,options) {
      const elevatedCreateGroup = auth.elevate(groups.createGroup);
      const response = await elevatedCreateGroup(group,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
