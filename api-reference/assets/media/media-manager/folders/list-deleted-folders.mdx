---
title: "List Deleted Folders"
description: "Retrieves a list of folders in the Media Manager's trash bin."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of folders in the Media Manager's trash bin.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEDIA.READ-MEDIAMANAGER">Read Media Manager</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/site-media/v1/trash-bin/folders
    ```

    ## Query Params

    <ParamField query="parentFolderId" type="string">

      ID of the folder's parent folder.

    </ParamField>

    <ParamField query="sort" type="Sorting">

      Field name and order to sort by. One of:
      * `displayName`
      * `updatedDate`
      Default: `updatedDate` in `desc` order.

      <Expandable title="sort properties">

        <ParamField query="fieldName" type="string">

          Name of the field to sort by.

          Constraints: `maxLength 512`.

        </ParamField>

        <ParamField query="order" type="string">

          Sort order.

          **Supported values:**

          - `ASC`
          - `DESC`

        </ParamField>

        <ParamField query="origin" type="AddressLocation">

          Origin point for geo-distance sorting on a GEO field
          results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

          <Expandable title="origin properties">

            <ParamField query="latitude" type="number">

              Address latitude.

            </ParamField>

            <ParamField query="longitude" type="number">

              Address longitude.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField query="paging" type="CursorPaging">

      Cursor and paging information.

      <Expandable title="paging properties">

        <ParamField query="limit" type="integer">

          Maximum number of items to return in the results.

        </ParamField>

        <ParamField query="cursor" type="string">

          Pointer to the next or previous page in the list of results.

          Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
          Not relevant for the first request.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="folders" type="Folder[]">

      List of folders in the Media Manager's trash bin.

      Constraints: `maxItems 200`.

      See the [Folder Object](/api-reference/assets/media/media-manager/folders/folder-object) for this object's fields.

    </ResponseField>

    <ResponseField name="nextCursor" type="PagingMetadataV2">

      The next cursor if it exists.

      <Expandable title="nextCursor properties">

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    This example sorts by `updatedDate` in `DESC` order with a paging limit of 2.

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/site-media/v1/trash-bin/folders?sort.fieldName=updateDate&sort.order=DESC&paging.limit=2' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "folders": [
          {
            "id": "d147e1ab3e8b4114ad93d13bb9e23540",
            "displayName": "New Folder",
            "parentFolderId": "trash-root",
            "createdDate": "2022-08-30T16:56:29Z",
            "updatedDate": "2022-08-30T16:56:29Z",
            "state": "OK"
          },
          {
            "id": "5bfba6861eed4f009c6abba0dc8b47db",
            "displayName": "New Folder 2",
            "parentFolderId": "trash-root",
            "createdDate": "2022-04-17T15:37:02Z",
            "updatedDate": "2022-04-26T20:09:32Z",
            "state": "OK"
          }
        ],
        "nextCursor": {
          "cursors": {
            "next": ""
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a list of folders in the Media Manager's trash bin.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MEDIA.READ-MEDIAMANAGER">Read Media Manager</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { folders } from '@wix/media';

    folders.listDeletedFolders(options): Promise<ListDeletedFoldersResponse>
    ```

    ## Method Parameters

    <ParamField body="options" type="ListDeletedFoldersOptions">

      Options to use when listing deleted folders from the trash bin.

      <Expandable title="options properties">

        <ParamField body="parentFolderId" type="string">

          ID of the folder's parent folder.

        </ParamField>

        <ParamField body="sort" type="Sorting">

          Field name and order to sort by. One of:
          * `displayName`
          * `updatedDate`
          Default: `updatedDate` in `desc` order.

          <Expandable title="sort properties">

            <ParamField body="fieldName" type="string">

              Name of the field to sort by.

              Constraints: `maxLength 512`.

            </ParamField>

            <ParamField body="order" type="string">

              Sort order.

              **Supported values:**

              - `ASC`
              - `DESC`

            </ParamField>

            <ParamField body="origin" type="AddressLocation">

              Origin point for geo-distance sorting on a GEO field
              results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).

              _Nested structure omitted for brevity — see the `AddressLocation` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="paging" type="CursorPaging">

          Cursor and paging information.

          <Expandable title="paging properties">

            <ParamField body="limit" type="integer">

              Maximum number of items to return in the results.

            </ParamField>

            <ParamField body="cursor" type="string">

              Pointer to the next or previous page in the list of results.

              Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
              Not relevant for the first request.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<ListDeletedFoldersResponse>`.

    <ResponseField name="folders" type="Folder[]">

      List of folders in the Media Manager's trash bin.

      Constraints: `maxItems 200`.

      See the [Folder Object](/api-reference/assets/media/media-manager/folders/folder-object) for this object's fields.

    </ResponseField>

    <ResponseField name="nextCursor" type="PagingMetadataV2">

      The next cursor if it exists.

      <Expandable title="nextCursor properties">

        <ResponseField name="total" type="integer">

          Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set.

        </ResponseField>

        <ResponseField name="cursors" type="Cursors">

          Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used.

          <Expandable title="cursors properties">

            <ResponseField name="next" type="string">

              Cursor string pointing to the next page in the list of results.

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-list-deleted-folders" id="ex-sdk-list-deleted-folders-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-list-deleted-folders" id="ex-sdk-list-deleted-folders-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-deleted-folders" id="ex-sdk-list-deleted-folders-2" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-deleted-folders" id="ex-sdk-list-deleted-folders-3" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-deleted-folders" id="ex-sdk-list-deleted-folders-4" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-list-deleted-folders" id="ex-sdk-list-deleted-folders-5" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-list-deleted-folders-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-list-deleted-folders-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">List deleted folders</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">List deleted folders (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">List deleted folders with sort</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">List deleted folders with sort (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Bulk restore all folders from the trash bin (with $w)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">listDeletedFolders (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-list-deleted-folders-0"><span className="wix-picker-name">List deleted folders</span></label>
    <label htmlFor="ex-sdk-list-deleted-folders-1"><span className="wix-picker-name">List deleted folders (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-list-deleted-folders-2"><span className="wix-picker-name">List deleted folders with sort</span></label>
    <label htmlFor="ex-sdk-list-deleted-folders-3"><span className="wix-picker-name">List deleted folders with sort (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-list-deleted-folders-4"><span className="wix-picker-name">Bulk restore all folders from the trash bin (with $w)</span></label>
    <label htmlFor="ex-sdk-list-deleted-folders-5"><span className="wix-picker-name">listDeletedFolders (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript List deleted folders
    import { folders } from '@wix/media';

    async function listDeletedFolders() {
      try {
        const deletedFolders = await folders.listDeletedFolders();

        console.log("Deleted Folders:", deletedFolders);
        return deletedFolders;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "folders": [
     *     {
     *       "_createdDate": "2023-08-21T09:32:47.000Z",
     *       "_id": "d4051a6890724365a78678a6208ac37e",
     *       "_updatedDate": "2023-08-21T09:32:47.000Z",
     *       "displayName": "family",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     },
     *     {
     *       "_createdDate": "2023-08-21T05:34:03.000Z",
     *       "_id": "302fc049d70c41dea33fa4a27ab481ba",
     *       "_updatedDate": "2023-08-21T05:34:03.000Z",
     *       "displayName": "Videos",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     }
     *   ],
     *   "nextCursor": {
     *     "cursors": {
     *       "next": ""
     *     },
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript List deleted folders (with elevated permissions)
    import { folders } from '@wix/media';
    import { auth } from '@wix/essentials';

    const elevatedListDeletedFolders = auth.elevate(folders.listDeletedFolders);

    async function listDeletedFolders() {
      try {
        const deletedFolders = await elevatedListDeletedFolders();

        console.log("Deleted Folders:", deletedFolders);
        return deletedFolders;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "folders": [
     *     {
     *       "_createdDate": "2023-08-21T09:32:47.000Z",
     *       "_id": "d4051a6890724365a78678a6208ac37e",
     *       "_updatedDate": "2023-08-21T09:32:47.000Z",
     *       "displayName": "family",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     },
     *     {
     *       "_createdDate": "2023-08-21T05:34:03.000Z",
     *       "_id": "302fc049d70c41dea33fa4a27ab481ba",
     *       "_updatedDate": "2023-08-21T05:34:03.000Z",
     *       "displayName": "Videos",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     }
     *   ],
     *   "nextCursor": {
     *     "cursors": {
     *       "next": ""
     *     },
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript List deleted folders with sort
    import { folders } from '@wix/media';

    /* Sample options value:
     * {
     *   sort: {
     *     fieldName: 'displayName',
     *     order: 'ASC'
     *   }
     * }
     */

    export async function listDeletedFolders(options) {
      try {
        const deletedFolders = await folders.listDeletedFolders(options);

        console.log("Deleted folders, sorted alphabetically by name:", deletedFolders);
        return deletedFolders;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "folders": [
     *     {
     *       "_createdDate": "2023-08-21T05:34:03.000Z",
     *       "_id": "302fc049d70c41dea33fa4a27ab481ba",
     *       "_updatedDate": "2023-08-21T05:34:03.000Z",
     *       "displayName": "Videos",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     },
     *     {
     *       "_createdDate": "2023-08-21T09:32:47.000Z",
     *       "_id": "d4051a6890724365a78678a6208ac37e",
     *       "_updatedDate": "2023-08-21T09:32:47.000Z",
     *       "displayName": "family",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     }
     *   ],
     *   "nextCursor": {
     *     "cursors": {
     *       "next": ""
     *     },
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript List deleted folders with sort (with elevated permissions)
    import { folders } from '@wix/media';
    import { auth } from '@wix/essentials';

    /* Sample options value:
     * {
     *   sort: {
     *     fieldName: 'displayName',
     *     order: 'ASC'
     *   }
     * }
     */

    export async function listDeletedFolders(options) {
      try {
        const elevatedListDeletedFolders = auth.elevate(folders.listDeletedFolders);
        const deletedFolders = await elevatedListDeletedFolders(options);

        console.log("Deleted folders, sorted alphabetically by name:", deletedFolders);
        return deletedFolders;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "folders": [
     *     {
     *       "_createdDate": "2023-08-21T05:34:03.000Z",
     *       "_id": "302fc049d70c41dea33fa4a27ab481ba",
     *       "_updatedDate": "2023-08-21T05:34:03.000Z",
     *       "displayName": "Videos",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     },
     *     {
     *       "_createdDate": "2023-08-21T09:32:47.000Z",
     *       "_id": "d4051a6890724365a78678a6208ac37e",
     *       "_updatedDate": "2023-08-21T09:32:47.000Z",
     *       "displayName": "family",
     *       "parentFolderId": "trash-root",
     *       "state": "DELETED"
     *     }
     *   ],
     *   "nextCursor": {
     *     "cursors": {
     *       "next": ""
     *     },
     *     "hasNext": false
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Bulk restore all folders from the trash bin (with $w)
    /********************************************
     * Backend code - restore-folders.web.js/ts *
     *******************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { folders } from '@wix/media';
    import { auth } from '@wix/essentials';

    export const restoreAllFolders = webMethod(Permissions.Anyone, async () => {
      try {
        const elevatedListDeletedFolders = auth.elevate(folders.listDeletedFolders);
        const deletedFolders = await elevatedListDeletedFolders();

        const deletedFolderIds = deletedFolders.folders.map((folder) => {
            return folder._id;
        });

        const elevatedBulkRestoreFoldersFromTrashBin = auth.elevate(folders.bulkRestoreFoldersFromTrashBin);
        const restoredFolders = await elevatedBulkRestoreFoldersFromTrashBin(deletedFolderIds);

        return restoredFolders;
      } catch (error) {
        console.error(error);
      }
    });

    /*************
     * Page code *
     ************/

    import { restoreAllFolders } from 'backend/restore-folders.web';

    $w.onReady(async () => {
      $w('#restoreAll').onClick(async () => {
        const restoredFolders = await restoreAllFolders();

        console.log('Restored all folders:', restoredFolders);
        $w('#bulkRestoreSuccessMsg').show();
      });
    });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript listDeletedFolders (self-hosted)
    import { createClient } from '@wix/sdk';
    import { folders } from '@wix/media';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { folders },
      // Include the auth strategy and host as relevant
    });

    async function listDeletedFolders(options) {
      const response = await myWixClient.folders.listDeletedFolders(options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
