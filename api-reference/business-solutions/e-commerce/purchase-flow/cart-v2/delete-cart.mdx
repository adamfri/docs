---
title: "Delete Cart"
description: "Deletes a cart."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a cart.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ECOM.WRITE-CARTS-V2_LIMITED">Write Carts V2 (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/ecom/v2/carts/{cartId}
    ```

    ## Path Params

    <ParamField path="cartId" type="string" required>

      Cart ID.

    </ParamField>

    ## Response Object

    Returns `DeleteCartResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Cart Deleted | `wix.ecom.v2.cart_deleted` | Triggered when a cart is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a cart.

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
      'https://www.wixapis.com/ecom/v2/carts/ca727402-f531-4bcf-ab09-db70ea0bd008' \
      -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a cart.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.ECOM.WRITE-CARTS-V2_LIMITED">Write Carts V2 (PII)</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { cartV2 } from '@wix/ecom';

    cartV2.deleteCart(cartId): Promise
    ```

    ## Method Parameters

    <ParamField body="cartId" type="string" required>

      Cart ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `DeleteCartResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Cart Deleted | `wix.ecom.v2.cart_deleted` | Triggered when a cart is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-cart" id="ex-sdk-delete-cart-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-cart" id="ex-sdk-delete-cart-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-cart-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-cart-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a cart</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCart (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-cart-0"><span className="wix-picker-name">Delete a cart</span></label>
    <label htmlFor="ex-sdk-delete-cart-1"><span className="wix-picker-name">deleteCart (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a cart
    import { cartV2 } from "@wix/ecom";

    async function deleteCart() {
      await cartV2.deleteCart("ca727402-f531-4bcf-ab09-db70ea0bd008");
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCart (self-hosted)
    import { createClient } from '@wix/sdk';
    import { cartV2 } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { cartV2 },
      // Include the auth strategy and host as relevant
    });

    async function deleteCart(cartId) {
      const response = await myWixClient.cartV2.deleteCart(cartId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
