---
title: "Update Participation"
description: "Updates a participation object."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a `participation` object.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CALENDAR.MANAGE">Manage Calendars</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/calendar/v3/participations/{participation.id}
    ```

    ## Path Params

    <ParamField path="participation.id" type="string" required>

      Participation ID.

    </ParamField>

    ## Body Params

    <ParamField body="participation" type="Participation" required>

      `participation` object to update.

      See the [Participation Object](/api-reference/business-management/calendar/participations-v3/participation-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="participation" type="Participation">

      Updated `participation` object.

      See the [Participation Object](/api-reference/business-management/calendar/participations-v3/participation-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Participation Updated | `wix.calendar.v3.participation_updated` | Triggered when a `participation` object is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    Updates a participation's party size.

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
      'https://www.wixapis.com/calendar/v3/participations/b123e549-eb05-4c6e-8dab-0409c7ebc006' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH>' \
      -d '{
        "participation": {
          "partySize": 5,
          "revision": "1"
        }
      }'
      ```

      ```json Response
      {
        "participation": {
          "id": "b123e549-eb05-4c6e-8dab-0409c7ebc006",
          "externalId": "c7e4cd82-1d17-49b0-b1df-6e1eaed60a20",
          "participant": {
            "name": "Jane Doe",
            "phone": "55-555-5555",
            "email": "jane.doe@example.com",
            "contactId": "5f4a86c5-fadf-427e-96e7-d57c14a4f49d"
          },
          "eventId": "130o4ncdcmm8csGKUihodsF8Lnx5vQuUmP9uqeurIYireDpysq5nOxSIMvXKf80QrOXzc09i1GXRepTUj2L01bzmifAqWHwp2pTmmtR3TmHJx7Tv6ODrmgS",
          "partySize": 5,
          "status": "CONFIRMED",
          "appId": "13d21c63-b5ec-5912-8397-c3a5ddb27a97",
          "revision": "2",
          "createdDate": "2024-10-07T09:14:54.796Z",
          "updatedDate": "2024-10-07T09:17:24.935Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates a `participation` object.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CALENDAR.MANAGE">Manage Calendars</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { participations } from '@wix/calendar';

    participations.updateParticipation(_id, participation): Promise<Participation>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Participation ID.

      **Read-only.**

    </ParamField>

    <ParamField body="participation" type="UpdateParticipation" required>

      `participation` object to update.

      <Expandable title="participation properties">

        <ParamField body="_id" type="string">

          Participation ID.

          **Read-only.**

        </ParamField>

        <ParamField body="externalId" type="string">

          External ID.

        </ParamField>

        <ParamField body="participant" type="Participant">

          Information about the participant.

          <Expandable title="participant properties">

            <ParamField body="name" type="string">

              Participant name.

              Min: 1 character
              Max: 200 characters

            </ParamField>

            <ParamField body="phone" type="string">

              Participant's phone number.

            </ParamField>

            <ParamField body="email" type="string">

              Participant's email address.

            </ParamField>

            <ParamField body="contactId" type="string">

              [Contact](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/contact-v4/contact-object)
              ID of the parcipant.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="partySize" type="integer">

          Party size of the group or `1` for individuals.

          Min: `1`
          Max: `1000`

        </ParamField>

        <ParamField body="status" type="string">

          Status of the party's participation in the event. Depending on your
          implementation, confirmation may be handled by the business owner or the customer.

          Supported values:
          + `CONFIRMED`: The party's participation is confirmed. For example, the business owner has confirmed a [booking](https://dev.wix.com/docs/rest/business-solutions/bookings/about-wix-bookings).
          + `PENDING_CONFIRMATION`: The party's participation is awaiting confirmation. For example, the business owner still needs to approve the [booking](https://dev.wix.com/docs/rest/business-solutions/bookings/about-wix-bookings).

          Default: `CONFIRMED`

          **Supported values:**

          - `CONFIRMED` — The party's participation is confirmed. For example, the business owner has confirmed a [booking](https://dev.wix.com/docs/rest/business-solutions/bookings/about-wix-bookings).
          - `PENDING_CONFIRMATION` — The party's participation is awaiting confirmation. For example, the business owner still needs to approve the [booking](https://dev.wix.com/docs/rest/business-solutions/bookings/about-wix-bookings).

        </ParamField>

        <ParamField body="appId" type="string">

          ID of the app that owns the participation `object`. Identical to `appId` of the
          [schedule](https://dev.wix.com/docs/rest/business-management/calendar/schedules-v3/schedule-object)
          that the related
          [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
          belongs to.

          **Read-only.**

        </ParamField>

        <ParamField body="revision" type="integer" required>

          Revision number, which increments by 1 each time the participation is updated.
          To prevent conflicting changes, the current revision must be passed when updating the participation.
          Ignored when creating a participation.

          **Read-only.**

        </ParamField>

        <ParamField body="_createdDate" type="datetime">

          Date the participation was created in `YYYY-MM-DDThh:mm:ss.sssZ` format.

          **Read-only.**

        </ParamField>

        <ParamField body="_updatedDate" type="datetime">

          Date the participation was updated in `YYYY-MM-DDThh:mm:ss.sssZ` format.

          **Read-only.**

        </ParamField>

        <ParamField body="extendedFields" type="ExtendedFields">

          Extensions enabling applications or users to save custom data related to the participation.

          <Expandable title="extendedFields properties">

            <ParamField body="namespaces" type="object">

              Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
              The value of each key is structured according to the schema defined when the extended fields were configured.

              You can only access fields for which you have the appropriate permissions.

              Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).

              Constraints: `format map`.

              _Nested structure omitted for brevity — see the `object` object._

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

      <Expandable title="One of the following">

        <ParamField body="participatedItem" type="participatedItem">

          participatedItem

          <Expandable title="participatedItem properties">

            <ParamField body="scheduleId" type="string">

              ID of the [schedule](https://dev.wix.com/docs/rest/business-management/calendar/schedules-v3/schedule-object)
              associated with the participation.

            </ParamField>

            <ParamField body="eventId" type="string">

              ID of the [event](https://dev.wix.com/docs/rest/business-management/calendar/events-v3/event-object)
              associated with the participation.

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Participation>`.

    See the [Participation Object](/api-reference/business-management/calendar/participations-v3/participation-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Participation Updated | `wix.calendar.v3.participation_updated` | Triggered when a `participation` object is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-participation" id="ex-sdk-update-participation-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-participation" id="ex-sdk-update-participation-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-participation" id="ex-sdk-update-participation-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-participation-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-participation-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">updateParticipation</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateParticipation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateParticipation (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-participation-0"><span className="wix-picker-name">updateParticipation</span></label>
    <label htmlFor="ex-sdk-update-participation-1"><span className="wix-picker-name">updateParticipation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-update-participation-2"><span className="wix-picker-name">updateParticipation (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript updateParticipation
    import { participations } from '@wix/calendar';

    async function updateParticipation(_id,participation) {
      const response = await participations.updateParticipation(_id,participation);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateParticipation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { participations } from '@wix/calendar';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { participations },
      // Include the auth strategy and host as relevant
    });

    async function updateParticipation(_id,participation) {
      const response = await myWixClient.participations.updateParticipation(_id,participation);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateParticipation (with elevated permissions)
    import { participations } from '@wix/calendar';
    import { auth } from '@wix/essentials';

    async function myUpdateParticipationMethod(_id,participation) {
      const elevatedUpdateParticipation = auth.elevate(participations.updateParticipation);
      const response = await elevatedUpdateParticipation(_id,participation);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
