---
title: "onBookingPolicyDeleted"
description: "Triggered when a booking policy is deleted."
icon: "bolt"
---

Triggered when a booking policy is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.bookings.v1.booking_policy_deleted` |
| Package | `@wix/bookings` |
| Permissions | `Read Bookings - Public Data`, `Manage Bookings Services and Settings`, `Manage Bookings`, `Read Bookings - Including Participants`, `Read Bookings - all read permissions`, `Manage Bookings - all permissions`, `Picasso private app mega scope` |

## Examples

<CodeGroup>

  ```javascript onBookingPolicyDeleted
  import { bookingPolicies } from '@wix/bookings';

  bookingPolicies.onBookingPolicyDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onBookingPolicyDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { bookingPolicies } from '@wix/bookings';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        bookingPolicies,
      },
  });

  wixClient.bookingPolicies.onBookingPolicyDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
