---
title: "onBadgeCreated"
description: "Triggered when a badge is created."
icon: "bolt"
---

Triggered when a badge is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.badges.v4.badge_created` |
| Package | `@wix/members` |
| Permissions | `Manage_Wix_Rentals_App_Forms_submissions`, `Manage Stores`, `Manage Events`, `Manage Online Programs`, `Manage Badges`, `Read Badges` |

## Examples

<CodeGroup>

  ```javascript onBadgeCreated
  import { badgesV2 } from '@wix/members';

  badgesV2.onBadgeCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onBadgeCreated (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { badgesV2 } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        badgesV2,
      },
  });

  wixClient.badgesV2.onBadgeCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
