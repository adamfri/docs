---
title: "Pause Loyalty Program"
description: "Changes the program status to \"PAUSED\"."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Changes the program status to `"PAUSED"`.

    See [Pausing Your Loyalty Program](https://support.wix.com/en/article/wix-loyalty-program-pausing-your-loyalty-program) for more information.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/loyalty-programs/v1/program/pause
    ```

    ## Response Object

    <ResponseField name="loyaltyProgram" type="LoyaltyProgram">

      Paused loyalty program.

      See the [Program Object](/api-reference/crm/loyalty-program/loyalty-program-management/program/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/loyalty-programs/v1/program/pause' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "loyaltyProgram": {
          "name": "Star Loyalty Program",
          "pointDefinition": {
            "customName": "Stars",
            "icon": {
              "id": "",
              "url": "shapes/39dce0a5d1ce498f95526b1390eaf585.svg",
              "height": 0,
              "width": 0
            }
          },
          "status": "PAUSED",
          "pointsExpiration": {
            "status": "DISABLED",
            "monthsOfInactivity": 3,
            "expiringPointsPercentage": 100
          },
          "premiumFeatures": {
            "loyaltyProgram": true,
            "tiers": true,
            "pointsExpiration": true
          },
          "createdDate": "2022-05-22T08:18:32.186Z",
          "updatedDate": "2022-05-25T10:36:32.430Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Changes the program status to `"PAUSED"`.

    See [Pausing Your Loyalty Program](https://support.wix.com/en/article/wix-loyalty-program-pausing-your-loyalty-program) for more information.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LOYALTY.MANAGE-LOYALTY">Manage Loyalty</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { programs } from '@wix/loyalty';

    programs.pauseLoyaltyProgram(): Promise<PauseLoyaltyProgramResponse>
    ```

    ## Returns

    Returns `Promise<PauseLoyaltyProgramResponse>`.

    <ResponseField name="loyaltyProgram" type="LoyaltyProgram">

      Paused loyalty program.

      See the [Program Object](/api-reference/crm/loyalty-program/loyalty-program-management/program/program-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-pause-loyalty-program" id="ex-sdk-pause-loyalty-program-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-pause-loyalty-program" id="ex-sdk-pause-loyalty-program-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-pause-loyalty-program" id="ex-sdk-pause-loyalty-program-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-pause-loyalty-program-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-pause-loyalty-program-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Pause loyalty program</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Pause loyalty program (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">pauseLoyaltyProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-pause-loyalty-program-0"><span className="wix-picker-name">Pause loyalty program</span></label>
    <label htmlFor="ex-sdk-pause-loyalty-program-1"><span className="wix-picker-name">Pause loyalty program (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-pause-loyalty-program-2"><span className="wix-picker-name">pauseLoyaltyProgram (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Pause loyalty program

    import { programs } from "@wix/loyalty";

    async function pauseLoyaltyProgram() {
      try {
        const result = await programs.pauseLoyaltyProgram();
        return result;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     *
     * {
     *   "loyaltyProgram": {
     *     "_createdDate": "2024-06-05T20:31:49.155Z",
     *     "_updatedDate": "2024-06-10T07:04:27.130Z",
     *     "name": "Loyalty",
     *     "pointDefinition": {
     *       "icon": "shapes/8de7ee72dcd944caa60f9e226d900f1d.svg"
     *     },
     *     "premiumFeatures": {
     *       "loyaltyProgram": true,
     *       "tiers": true,
     *       "pointsExpiration": true
     *     },
     *     "pointsExpiration": {
     *       "status": "ENABLED",
     *       "monthsOfInactivity": 3,
     *       "expiringPointsPercentage": 100
     *     },
     *     "status": "PAUSED"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Pause loyalty program (with elevated permissions)
    import { programs } from "@wix/loyalty";
    import { auth } from "@wix/essentials";

    const elevatedPauseLoyaltyProgram = auth.elevate(programs.pauseLoyaltyProgram);

    async function pauseLoyaltyProgram() {
      try {
        const result = await elevatedPauseLoyaltyProgram();
        return result;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     *
     * {
     *   "loyaltyProgram": {
     *     "_createdDate": "2024-06-05T20:31:49.155Z",
     *     "_updatedDate": "2024-06-10T07:04:27.130Z",
     *     "name": "Loyalty",
     *     "pointDefinition": {
     *       "icon": "shapes/8de7ee72dcd944caa60f9e226d900f1d.svg"
     *     },
     *     "premiumFeatures": {
     *       "loyaltyProgram": true,
     *       "tiers": true,
     *       "pointsExpiration": true
     *     },
     *     "pointsExpiration": {
     *       "status": "ENABLED",
     *       "monthsOfInactivity": 3,
     *       "expiringPointsPercentage": 100
     *     },
     *     "status": "PAUSED"
     *   }
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript pauseLoyaltyProgram (self-hosted)
    import { createClient } from '@wix/sdk';
    import { programs } from '@wix/loyalty';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { programs },
      // Include the auth strategy and host as relevant
    });

    async function pauseLoyaltyProgram() {
      const response = await myWixClient.programs.pauseLoyaltyProgram();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
