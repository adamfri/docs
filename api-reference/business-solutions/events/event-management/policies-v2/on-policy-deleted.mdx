---
title: "onPolicyDeleted"
description: "Triggered when a policy is deleted."
icon: "bolt"
---

Triggered when a policy is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.v2.policy_deleted` |
| Package | `@wix/events` |
| Permissions | `Manage Events - all permissions`, `Manage Events`, `Read Policies`, `Manage Policies` |

## Examples

<CodeGroup>

  ```javascript onPolicyDeleted
  import { policies } from '@wix/events';

  policies.onPolicyDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onPolicyDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { policies } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        policies,
      },
  });

  wixClient.policies.onPolicyDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
