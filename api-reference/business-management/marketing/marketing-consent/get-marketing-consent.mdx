---
title: "Get Marketing Consent"
description: "Retrieves a marketing consent."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a marketing consent.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.CONSENT-READ">Read Marketing Consents</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/marketing-consent/v1/marketing-consent/{marketingConsentId}
    ```

    ## Path Params

    <ParamField path="marketingConsentId" type="string" required>

      Marketing consent ID.

    </ParamField>

    ## Response Object

    <ResponseField name="marketingConsent" type="MarketingConsent">

      The requested marketing consent.

      See the [Marketing Consent Object](/api-reference/business-management/marketing/marketing-consent/marketing-consent-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X GET \
        'https://www.wixapis.com/marketing-consent/v1/marketing-consent/22916e0c-72af-e441-a29b-8875888559a9' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {
        "marketingConsent": {
          "id": "22916e0c-72af-e441-a29b-8875888559a9",
          "revision": "1",
          "details": {
            "type": "EMAIL",
            "email": "mytest@test.com"
          },
          "state": "CONFIRMED",
          "lastConfirmationActivity": {
            "source": "IN_PERSON",
            "description": "Confirmed by Samantha.",
            "updatedDate": "2022-10-03T00:00:00Z",
            "optInLevel": "UNKNOWN_OPT_IN_LEVEL"
          },
          "extendedFields": {},
          "createdDate": "2022-11-16T14:07:50.150Z",
          "updatedDate": "2022-11-16T14:07:50.150Z"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a marketing consent.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MARKETING.CONSENT-READ">Read Marketing Consents</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { marketingConsent } from '@wix/marketing';

    marketingConsent.getMarketingConsent(marketingConsentId): Promise
    ```

    ## Method Parameters

    <ParamField body="marketingConsentId" type="string" required>

      Marketing consent ID.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `MarketingConsent`.

    See the [Marketing Consent Object](/api-reference/business-management/marketing/marketing-consent/marketing-consent-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-marketing-consent" id="ex-sdk-get-marketing-consent-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-marketing-consent" id="ex-sdk-get-marketing-consent-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-get-marketing-consent" id="ex-sdk-get-marketing-consent-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-marketing-consent-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-marketing-consent-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getMarketingConsent</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getMarketingConsent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getMarketingConsent (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-marketing-consent-0"><span className="wix-picker-name">getMarketingConsent</span></label>
    <label htmlFor="ex-sdk-get-marketing-consent-1"><span className="wix-picker-name">getMarketingConsent (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-get-marketing-consent-2"><span className="wix-picker-name">getMarketingConsent (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getMarketingConsent
    import { marketingConsent } from '@wix/marketing';

    async function getMarketingConsent(marketingConsentId) {
      const response = await marketingConsent.getMarketingConsent(marketingConsentId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getMarketingConsent (self-hosted)
    import { createClient } from '@wix/sdk';
    import { marketingConsent } from '@wix/marketing';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { marketingConsent },
      // Include the auth strategy and host as relevant
    });

    async function getMarketingConsent(marketingConsentId) {
      const response = await myWixClient.marketingConsent.getMarketingConsent(marketingConsentId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getMarketingConsent (with elevated permissions)
    import { marketingConsent } from '@wix/marketing';
    import { auth } from '@wix/essentials';

    async function myGetMarketingConsentMethod(marketingConsentId) {
      const elevatedGetMarketingConsent = auth.elevate(marketingConsent.getMarketingConsent);
      const response = await elevatedGetMarketingConsent(marketingConsentId);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
