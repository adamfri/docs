---
title: "onMemberDeleted"
description: "Triggered when a member is deleted."
icon: "bolt"
---

Triggered when a member is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.members.v1.member_deleted` |
| Package | `@wix/members` |
| Permissions | `Manage Members`, `Manage Bookings Services and Settings`, `Manage Events`, `Manage Portfolio`, `Access Verticals by Automations`, `Read Members and Contacts - all read permissions`, `Manage Restaurants - all permissions`, `Read Members`, `Set Up Automations`, `Manage Members and Contacts - all permissions` |

## Examples

<CodeGroup>

  ```javascript onMemberDeleted: Wix-hosted
  import { members } from '@wix/members';

  members.onMemberDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onMemberDeleted: Self-hosted
  import { createClient, AppStrategy } from '@wix/sdk';
  import { members } from '@wix/members';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        members,
      },
  });

  wixClient.members.onMemberDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
