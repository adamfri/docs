---
title: "onBranchDeleted"
description: "Triggered when a branch is deleted."
icon: "bolt"
---

Triggered when a branch is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.editor.branches.v1.branch_deleted` |
| Package | `@wix/editor-branches` |
| Permissions | `Manage SEO Settings`, `Manage Site Branches` |

## Examples

<CodeGroup>

  ```javascript onBranchDeleted
  import { branches } from '@wix/editor-branches';

  branches.onBranchDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onBranchDeleted (self-managed)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { branches } from '@wix/editor-branches';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        branches,
      },
  });

  wixClient.branches.onBranchDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
