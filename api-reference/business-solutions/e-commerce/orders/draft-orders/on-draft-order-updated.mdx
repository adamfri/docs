---
title: "onDraftOrderUpdated"
description: "Triggered when a draft order is updated."
icon: "bolt"
---

Triggered when a draft order is updated.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.ecom.v1.draft_order_updated` |
| Package | `@wix/ecom` |
| Permissions | `Manage Stores - all permissions`, `Manage Stores`, `Manage Restaurants - all permissions`, `Manage eCommerce - all permissions` |

## Examples

<CodeGroup>

  ```javascript onDraftOrderUpdated
  import { draftOrders } from '@wix/ecom';

  draftOrders.onDraftOrderUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onDraftOrderUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { draftOrders } from '@wix/ecom';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        draftOrders,
      },
  });

  wixClient.draftOrders.onDraftOrderUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
