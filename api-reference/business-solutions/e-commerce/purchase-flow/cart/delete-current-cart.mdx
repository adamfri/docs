---
title: "Delete Current Cart"
description: "Deletes the current site visitor's cart."
icon: "trash"
tag: "Deprecated"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Deletes the current site visitor's cart.

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/rest/articles/getting-started/access-types-and-permissions).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/ecom/v1/carts/current
    ```

    ## Response Object

    Returns `DeleteCartResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Cart Deleted | `wix.ecom.v1.cart_deleted` | Triggered when a cart is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/ecom/v1/carts/current' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    <Warning>

      **Deprecated.** This method is deprecated.

    </Warning>

    Deletes the current site visitor's cart.

    <Note>

    This method requires [visitor or member authentication](https://dev.wix.com/docs/rest/articles/getting-started/access-types-and-permissions).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-ECOM-MEGA.MANAGE-ECOM">Manage eCommerce - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { currentCart } from '@wix/ecom';

    currentCart.deleteCurrentCart(): Promise<void>
    ```

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Cart Deleted | `wix.ecom.v1.cart_deleted` | Triggered when a cart is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-current-cart" id="ex-sdk-delete-current-cart-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-current-cart" id="ex-sdk-delete-current-cart-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-current-cart-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-current-cart-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete the current site visitor's cart</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteCurrentCart (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-current-cart-0"><span className="wix-picker-name">Delete the current site visitor's cart</span></label>
    <label htmlFor="ex-sdk-delete-current-cart-1"><span className="wix-picker-name">deleteCurrentCart (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete the current site visitor's cart
    /**************************************
     * Backend code - my-backend-file.web.js/ts *
     *************************************/

    import { Permissions, webMethod } from '@wix/web-methods';
    import { currentCart } from '@wix/ecom';

    export const myDeleteCurrentCartFunction = webMethod(Permissions.Anyone, async () => {
      try {
        await currentCart.deleteCurrentCart();
        console.log('Success! Deleted cart');
        return;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    });

    /*************
     * Page code *
     ************/

    import { myDeleteCurrentCartFunction } from 'backend/my-backend-file.web';

    myDeleteCurrentCartFunction()
      .then(() => {
        console.log('Success! Deleted current cart');
        return;
      })
      .catch((error) => {
        console.error(error);
        // Handle the error
      });
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteCurrentCart (self-hosted)
    import { createClient } from '@wix/sdk';
    import { currentCart } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { currentCart },
      // Include the auth strategy and host as relevant
    });

    async function deleteCurrentCart() {
      const response = await myWixClient.currentCart.deleteCurrentCart();
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
