---
title: "onSpiConfigurationCreated"
description: "Triggered when a service plugin configuration is created."
icon: "bolt"
---

Triggered when a service plugin configuration is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.functions.v1.spi_configuration_created` |
| Package | `@wix/functions` |
| Permissions | `Manage Stores`, `Manage Functions`, `Manage Restaurants - all permissions`, `Set Up Automations` |

## Examples

<CodeGroup>

  ```javascript onSpiConfigurationCreated
  import { functionSpiConfigurations } from '@wix/functions';

  functionSpiConfigurations.onSpiConfigurationCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onSpiConfigurationCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { functionSpiConfigurations } from '@wix/functions';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        functionSpiConfigurations,
      },
  });

  wixClient.functionSpiConfigurations.onSpiConfigurationCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
