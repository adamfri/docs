---
title: "Bulk Delete Operation Groups"
description: "Delete multiple OperationGroups in a single request."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Delete multiple OperationGroups in a single request. Works synchronously.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/restaurants/v1/bulk/operation-groups/delete
    ```

    ## Body Params

    <ParamField body="operationGroupIds" type="string[]" required>

      OperationGroup ids to be deleted

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Response Object

    <ResponseField name="results" type="BulkOperationGroupResult[]">

      Results

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata regarding the specific single delete operation

          <Expandable title="itemMetadata properties">

            <ResponseField name="id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata regarding the bulk delete operation

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Group Deleted | `wix.restaurants.v1.operation_group_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/restaurants/v1/bulk/operation-groups/delete' \
        -H 'Authorization: <AUTH>' \
        -H 'Content-Type: application/json' \
        --data-raw '{
          "operation_group_ids": [
            "4820c639-4a9b-4583-8617-897e1102f369",
            "68dc7603-3bc3-4641-b744-be84904190f9"
          ]
        }'
      ```

      ```json Response
      {
        "results": [
          {
            "item_metadata": {
              "id": "4820c639-4a9b-4583-8617-897e1102f369",
              "original_index": 0,
              "success": true,
              "error": null
            }
          },
          {
            "item_metadata": {
              "id": "68dc7603-3bc3-4641-b744-be84904190f9",
              "original_index": 1,
              "success": true,
              "error": null
            }
          }
        ],
        "bulk_action_metadata": {
          "total_successes": 2,
          "total_failures": 0,
          "undetailed_failures": 0
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Delete multiple OperationGroups in a single request. Works synchronously.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.RESTAURANTS.MEGA-SCOPES">Manage Restaurants - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { operationGroups } from '@wix/restaurants';

    operationGroups.bulkDeleteOperationGroups(operationGroupIds): Promise<BulkDeleteOperationGroupsResponse>
    ```

    ## Method Parameters

    <ParamField body="operationGroupIds" type="string[]" required>

      OperationGroup ids to be deleted

      Constraints: `format GUID`, `minItems 1`, `maxItems 100`.

    </ParamField>

    ## Returns

    Returns `Promise<BulkDeleteOperationGroupsResponse>`.

    <ResponseField name="results" type="BulkOperationGroupResult[]">

      Results

      Constraints: `minItems 1`, `maxItems 100`.

      <Expandable title="results properties">

        <ResponseField name="itemMetadata" type="ItemMetadata">

          Metadata regarding the specific single delete operation

          <Expandable title="itemMetadata properties">

            <ResponseField name="_id" type="string">

              Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).

            </ResponseField>

            <ResponseField name="originalIndex" type="integer">

              Index of the item within the request array. Allows for correlation between request and response items.

            </ResponseField>

            <ResponseField name="success" type="boolean">

              Whether the requested action was successful for this item. When `false`, the `error` field is populated.

            </ResponseField>

            <ResponseField name="error" type="ApplicationError">

              Details about the error in case of failure.

              _Nested structure omitted for brevity — see the `ApplicationError` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

      </Expandable>

    </ResponseField>

    <ResponseField name="bulkActionMetadata" type="BulkActionMetadata">

      Metadata regarding the bulk delete operation

      <Expandable title="bulkActionMetadata properties">

        <ResponseField name="totalSuccesses" type="integer">

          Number of items that were successfully processed.

        </ResponseField>

        <ResponseField name="totalFailures" type="integer">

          Number of items that couldn't be processed.

        </ResponseField>

        <ResponseField name="undetailedFailures" type="integer">

          Number of failures without details because detailed failure threshold was exceeded.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Operation Group Deleted | `wix.restaurants.v1.operation_group_deleted` |  |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-bulk-delete-operation-groups" id="ex-sdk-bulk-delete-operation-groups-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-bulk-delete-operation-groups" id="ex-sdk-bulk-delete-operation-groups-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-bulk-delete-operation-groups" id="ex-sdk-bulk-delete-operation-groups-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-bulk-delete-operation-groups-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-bulk-delete-operation-groups-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteOperationGroups</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteOperationGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">bulkDeleteOperationGroups (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-bulk-delete-operation-groups-0"><span className="wix-picker-name">bulkDeleteOperationGroups</span></label>
    <label htmlFor="ex-sdk-bulk-delete-operation-groups-1"><span className="wix-picker-name">bulkDeleteOperationGroups (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-bulk-delete-operation-groups-2"><span className="wix-picker-name">bulkDeleteOperationGroups (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript bulkDeleteOperationGroups
    import { operationGroups } from '@wix/restaurants';

    async function bulkDeleteOperationGroups(operationGroupIds) {
      const response = await operationGroups.bulkDeleteOperationGroups(operationGroupIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteOperationGroups (self-hosted)
    import { createClient } from '@wix/sdk';
    import { operationGroups } from '@wix/restaurants';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { operationGroups },
      // Include the auth strategy and host as relevant
    });

    async function bulkDeleteOperationGroups(operationGroupIds) {
      const response = await myWixClient.operationGroups.bulkDeleteOperationGroups(operationGroupIds);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript bulkDeleteOperationGroups (with elevated permissions)
    import { operationGroups } from '@wix/restaurants';
    import { auth } from '@wix/essentials';

    async function myBulkDeleteOperationGroupsMethod(operationGroupIds) {
      const elevatedBulkDeleteOperationGroups = auth.elevate(operationGroups.bulkDeleteOperationGroups);
      const response = await elevatedBulkDeleteOperationGroups(operationGroupIds);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
