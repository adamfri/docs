---
title: "Archive Location"
description: "Archives a location."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Archives a location.

    <Note>

    + Changes the `archived` boolean of a location to `true`.
    + You can't change a location's `status` using this endpoint.
    + Archived locations can't be updated.
    + The `default` location can't be archived.

    </Note>

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/locations/v1/locations/{id}/archive
    ```

    ## Path Params

    <ParamField path="id" type="string" required>

      ID of the location to archive.

    </ParamField>

    ## Response Object

    <ResponseField name="location" type="Location">

      Archived location.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_ARCHIVE_DEFAULT` | Can't archive the default location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Location Archive Status | `wix.locations.v1.location_archive_status` | Triggered when a location is archived. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/locations/v1/locations/97dc43b5-521b-48e9-8710-a3543f00f0ca/archive' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        --data-binary '{}'
      ```

      ```json Response
      {
        "location": {
          "id":"97dc43b5-521b-48e9-8710-a3543f00f0ca",
          "name":"3rd Avenue",
          "default":false,
          "status":"ACTIVE",
          "locationType":"UNKNOWN",
          "fax":"1800-555-XXX",
          "timeZone":"America/New_York",
          "email":"employee-email@domain.com",
          "phone":"1800-555-XXX",
          "address":{
          "country":"US",
          "subdivision":"NY",
          "city":"New York",
          "postalCode":"10003",
          "streetAddress":{
             "number":"33",
             "name":"3rd Avenue",
             "apt":""
          },
          "formattedAddress":"Alumni Hall, 33 3rd Ave, New York, NY 10003, USA",
          "geocode":{
             "latitude":40.7302968,
             "longitude":-73.98899449999999
          }
          },
          "revision":"3",
          "archived":true
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Archives a location.

    <Note>

    + Changes the `archived` boolean of a location to `true`.
    + You can't change a location's `status` using this endpoint.
    + Archived locations can't be updated.
    + The `default` location can't be archived.

    </Note>

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-MULTILOCATION.MANAGE-LOCATIONS">Manage Locations</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { locations } from '@wix/business-tools';

    locations.archiveLocation(_id): Promise<ArchiveLocationResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      ID of the location to archive.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<ArchiveLocationResponse>`.

    <ResponseField name="location" type="Location">

      Archived location.

      See the [Location Object](/api-reference/business-management/locations/location-object) for this object's fields.

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `CANNOT_ARCHIVE_DEFAULT` | Can't archive the default location. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Location Archive Status | `wix.locations.v1.location_archive_status` | Triggered when a location is archived. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-archive-location" id="ex-sdk-archive-location-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-archive-location" id="ex-sdk-archive-location-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-archive-location" id="ex-sdk-archive-location-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-archive-location-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-archive-location-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Archive a location</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Archive a location (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">archiveLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-archive-location-0"><span className="wix-picker-name">Archive a location</span></label>
    <label htmlFor="ex-sdk-archive-location-1"><span className="wix-picker-name">Archive a location (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-archive-location-2"><span className="wix-picker-name">archiveLocation (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Archive a location
    import { locations } from '@wix/business-tools';

    /* Sample _id value: '0a965e36-4071-4df0-905b-75458817430a' */

    export async function myArchiveLocationFunction(_id) {
      try {
        const archivedLocation = await locations.archiveLocation(_id);
        console.log('Location has been archived:', archivedLocation);

        return archivedLocation;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "_id": "0a965e36-4071-4df0-905b-75458817430a"
     *   "address": {
     *     "streetAddress": {
     *       "apt": "",
     *       "name": "New Road",
     *       "number": "12"
     *     },
     *     "city": "Kolkata",
     *     "postalCode": "70027"
     *   },
     *   "archived": true,
     *   "default": false,
     *   "description": "New store in India!",
     *   "email": "store@example.com",
     *   "name": "India Store",
     *   "phone": "0208 209 9087",
     *   "revision": "2",
     *   "status": "ACTIVE",
     *   "timeZone": "Asia/Calcutta",
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Archive a location (with elevated permissions)
    import { locations } from '@wix/business-tools';
    import { auth } from '@wix/essentials';

    /* Sample _id value: '0a965e36-4071-4df0-905b-75458817430a' */

    export async function myArchiveLocationFunction(_id) {
      try {
        const elevatedArchiveLocation = auth.elevate(locations.archiveLocation);
        const archivedLocation = await elevatedArchiveLocation(_id);
        console.log('Location has been archived:', archivedLocation);

        return archivedLocation;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
     * {
     *   "_id": "0a965e36-4071-4df0-905b-75458817430a"
     *   "address": {
     *     "streetAddress": {
     *       "apt": "",
     *       "name": "New Road",
     *       "number": "12"
     *     },
     *     "city": "Kolkata",
     *     "postalCode": "70027"
     *   },
     *   "archived": true,
     *   "default": false,
     *   "description": "New store in India!",
     *   "email": "store@example.com",
     *   "name": "India Store",
     *   "phone": "0208 209 9087",
     *   "revision": "2",
     *   "status": "ACTIVE",
     *   "timeZone": "Asia/Calcutta",
     * }
     */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript archiveLocation (self-hosted)
    import { createClient } from '@wix/sdk';
    import { locations } from '@wix/business-tools';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { locations },
      // Include the auth strategy and host as relevant
    });

    async function archiveLocation(_id) {
      const response = await myWixClient.locations.archiveLocation(_id);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
