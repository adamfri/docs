---
title: "Delete Task"
description: "Deletes a task by ID."
icon: "trash"
mode: "wide"
---

<span className="wix-method wix-method-delete">DELETE</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a task by ID.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-TASKS">Manage Tasks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    DELETE https://www.wixapis.com/crm/tasks/v2/tasks/{taskId}
    ```

    ## Path Params

    <ParamField path="taskId" type="string" required>

      ID of the task to delete.

    </ParamField>

    ## Response Object

    Returns `DeleteTaskResponse`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Task Deleted | `wix.crm.tasks.v2.task_deleted` | Triggered when a task is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    Deletes a new Task

    <CodeGroup>

      ```bash Request
      curl -X DELETE \
        'https://www.wixapis.com/crm/tasks/v2/tasks/4973f73a-40ef-11ee-be56-0242ac120002' \
        -H 'Authorization: <AUTH>'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Deletes a task by ID.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-CRM.MANAGE-TASKS">Manage Tasks</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { tasks } from '@wix/crm';

    tasks.deleteTask(taskId): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="taskId" type="string" required>

      ID of the task to delete.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Task Deleted | `wix.crm.tasks.v2.task_deleted` | Triggered when a task is deleted. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-delete-task" id="ex-sdk-delete-task-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-delete-task" id="ex-sdk-delete-task-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-delete-task" id="ex-sdk-delete-task-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-delete-task-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-delete-task-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Delete a task</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Delete a task (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">deleteTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-delete-task-0"><span className="wix-picker-name">Delete a task</span></label>
    <label htmlFor="ex-sdk-delete-task-1"><span className="wix-picker-name">Delete a task (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-delete-task-2"><span className="wix-picker-name">deleteTask (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Delete a task
    import { tasks } from '@wix/crm';

    /* Sample taskId value:
     * '3194f3f2-945f-4810-8905-d02b24f9e790'
     */

    export async function myDeleteTaskFunction(taskId) {

      try {
        await tasks.deleteTask(taskId);

        console.log('Task deleted.')
      } catch (error) {
        console.log(error);
        //Handle the error
      }
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Delete a task (with elevated permissions)
    import { tasks } from '@wix/crm';
    import { auth } from '@wix/essentials';

    /* Sample taskId value:
     * '3194f3f2-945f-4810-8905-d02b24f9e790'
     */

    const elevatedDeleteTask = auth.elevate(tasks.deleteTask);

    export async function myDeleteTaskFunction(taskId) {

      try {
        await elevatedDeleteTask(taskId);

        console.log('Task deleted.')
      } catch (error) {
        console.log(error);
        //Handle the error
      }
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript deleteTask (self-hosted)
    import { createClient } from '@wix/sdk';
    import { tasks } from '@wix/crm';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { tasks },
      // Include the auth strategy and host as relevant
    });

    async function deleteTask(taskId) {
      const response = await myWixClient.tasks.deleteTask(taskId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
