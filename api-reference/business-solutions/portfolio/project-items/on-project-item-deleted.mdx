---
title: "onProjectItemDeleted"
description: "Triggered when a project item is deleted."
icon: "bolt"
---

Triggered when a project item is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.project_items.v1.project_item_deleted` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onProjectItemDeleted
  import { projectItems } from '@wix/portfolio';

  projectItems.onProjectItemDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onProjectItemDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { projectItems } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        projectItems,
      },
  });

  wixClient.projectItems.onProjectItemDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
