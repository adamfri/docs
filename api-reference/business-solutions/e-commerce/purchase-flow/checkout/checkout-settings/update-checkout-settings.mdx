---
title: "Update Checkout Settings"
description: "Updates the site's checkout settings."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the site's checkout settings.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/ecom/v1/checkout-settings
    ```

    ## Body Params

    <ParamField body="checkoutSettings" type="CheckoutSettings" required>

      Checkout settings to update.

      See the [Checkout Settings Object](/api-reference/business-solutions/e-commerce/purchase-flow/checkout/checkout-settings/checkout-settings-object) for this object's fields.

    </ParamField>

    ## Response Object

    <ResponseField name="checkoutSettings" type="CheckoutSettings">

      Updated checkout settings.

      See the [Checkout Settings Object](/api-reference/business-solutions/e-commerce/purchase-flow/checkout/checkout-settings/checkout-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Checkout Settings Updated | `wix.ecom.v1.checkout_settings_updated` | Triggered when checkout settings are updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      curl -X PATCH \
        'https://www.wixapis.com/ecom/v1/checkout-settings' \
        -H 'Content-Type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
              "checkoutSettings": {
                  "checkoutFields": {
                    "giftCardRedeemEnabled": true,
                    "mitEnabled": false
                  }
              }
          }'
      ```

      ```json Response
      {
        "checkoutSettings": {
          "checkoutPolicies": {
            "termsAndConditions": {
              "visible": true,
              "content": "All product listings, prices, and specifications are subject to change without notice. ACCS reserves the right to modify or discontinue products at any time."
            },
            "privacyPolicy": {
              "visible": true,
              "content": "Your privacy is important to us. Please review our Privacy Policy to understand how we collect, use, and disclose information."
            },
            "returnPolicy": {
              "visible": true,
              "content": "Please refer to our Return Policy page for information on returns and refunds. ACCS reserves the right to refuse returns that do not meet our policy criteria."
            },
            "digitalItemPolicy": {
              "visible": false,
              "content": ""
            },
            "contactUs": {
              "visible": true,
              "content": "Email: accs@mail.com"
            },
            "customPolicy": {
              "visible": true,
              "content": "Placing an order on our website constitutes an offer to purchase the products. We reserve the right to refuse or cancel any order for any reason. Payment must be received before order processing.",
              "title": "Orders and Payments"
            }
          },
          "checkoutFields": {
            "subscriptionCheckbox": {
              "visible": true,
              "checkedByDefault": true
            },
            "policyAgreementCheckbox": {
              "visible": true,
              "checkedByDefault": false
            },
            "giftCardRedeemEnabled": true,
            "mitEnabled": false
          }
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates the site's checkout settings.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-ORDERS">Manage Orders</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { checkoutSettings } from '@wix/ecom';

    checkoutSettings.updateCheckoutSettings(checkoutSettings): Promise<UpdateCheckoutSettingsResponse>
    ```

    ## Method Parameters

    <ParamField body="checkoutSettings" type="CheckoutSettings" required>

      Checkout settings to update.

      See the [Checkout Settings Object](/api-reference/business-solutions/e-commerce/purchase-flow/checkout/checkout-settings/checkout-settings-object) for this object's fields.

    </ParamField>

    ## Returns

    Returns `Promise<UpdateCheckoutSettingsResponse>`.

    <ResponseField name="checkoutSettings" type="CheckoutSettings">

      Updated checkout settings.

      See the [Checkout Settings Object](/api-reference/business-solutions/e-commerce/purchase-flow/checkout/checkout-settings/checkout-settings-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Checkout Settings Updated | `wix.ecom.v1.checkout_settings_updated` | Triggered when checkout settings are updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-checkout-settings" id="ex-sdk-update-checkout-settings-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-checkout-settings" id="ex-sdk-update-checkout-settings-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-update-checkout-settings" id="ex-sdk-update-checkout-settings-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-checkout-settings-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-checkout-settings-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update checkout settings</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Update checkout settings (with elevated permissions)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCheckoutSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-checkout-settings-0"><span className="wix-picker-name">Update checkout settings</span></label>
    <label htmlFor="ex-sdk-update-checkout-settings-1"><span className="wix-picker-name">Update checkout settings (with elevated permissions)</span></label>
    <label htmlFor="ex-sdk-update-checkout-settings-2"><span className="wix-picker-name">updateCheckoutSettings (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update checkout settings
    import { checkoutSettings } from '@wix/ecom';

    /* Sample update value:
    {
      "checkoutSettingsInfo": {
            "checkoutFields": {
                "mitEnabled": false
            }
        }
    }
    */

    export async function myUpdateCheckoutSettingsFunction(checkoutSettingsInfo) {
      try {
        const settings = await checkoutSettings.updateCheckoutSettings(checkoutSettingsInfo);
        console.log('Success! Updated checkout settings:', settings);
        return settings;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
    {
      "checkoutSettings": {
        "checkoutPolicies": {
          "termsAndConditions": {
            "visible": true,
            "content": "All product listings, prices, and specifications are subject to change without notice. ACCS reserves the right to modify or discontinue products at any time."
          },
          "privacyPolicy": {
            "visible": true,
            "content": "Your privacy is important to us. Please review our Privacy Policy to understand how we collect, use, and disclose information."
          },
          "returnPolicy": {
            "visible": true,
            "content": "Please refer to our Return Policy page for information on returns and refunds. ACCS reserves the right to refuse returns that do not meet our policy criteria."
          },
          "digitalItemPolicy": {
            "visible": false,
            "content": ""
          },
          "contactUs": {
            "visible": true,
            "content": "Email: accs@mail.com"
          },
          "customPolicy": {
            "visible": true,
            "content": "Placing an order on our website constitutes an offer to purchase the products. We reserve the right to refuse or cancel any order for any reason. Payment must be received before order processing.",
            "title": "Orders and Payments"
          }
        },
        "checkoutFields": {
          "subscriptionCheckbox": {
            "visible": true,
            "checkedByDefault": false
          },
          "policyAgreementCheckbox": {
            "visible": true,
            "checkedByDefault": false
          },
          "giftCardRedeemEnabled": true,
          "mitEnabled": false
        }
      }
    }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript Update checkout settings (with elevated permissions)
    import { checkoutSettings } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    /* Sample update value:
    {
      "checkoutSettingsInfo": {
            "checkoutFields": {
                "mitEnabled": false
            }
        }
    }
    */

    const elevatedUpdateCheckoutSettings = auth.elevate(checkoutSettings.updateCheckoutSettings);

    export async function myUpdateCheckoutSettingsFunction(checkoutSettingsInfo) {
      try {
        const settings = await elevatedUpdateCheckoutSettings(checkoutSettingsInfo);
        console.log('Success! Updated checkout settings:', settings);
        return settings;
      } catch (error) {
        console.error(error);
        // Handle the error
      }
    }

    /* Promise resolves to:
    {
      "checkoutSettings": {
        "checkoutPolicies": {
          "termsAndConditions": {
            "visible": true,
            "content": "All product listings, prices, and specifications are subject to change without notice. ACCS reserves the right to modify or discontinue products at any time."
          },
          "privacyPolicy": {
            "visible": true,
            "content": "Your privacy is important to us. Please review our Privacy Policy to understand how we collect, use, and disclose information."
          },
          "returnPolicy": {
            "visible": true,
            "content": "Please refer to our Return Policy page for information on returns and refunds. ACCS reserves the right to refuse returns that do not meet our policy criteria."
          },
          "digitalItemPolicy": {
            "visible": false,
            "content": ""
          },
          "contactUs": {
            "visible": true,
            "content": "Email: accs@mail.com"
          },
          "customPolicy": {
            "visible": true,
            "content": "Placing an order on our website constitutes an offer to purchase the products. We reserve the right to refuse or cancel any order for any reason. Payment must be received before order processing.",
            "title": "Orders and Payments"
          }
        },
        "checkoutFields": {
          "subscriptionCheckbox": {
            "visible": true,
            "checkedByDefault": false
          },
          "policyAgreementCheckbox": {
            "visible": true,
            "checkedByDefault": false
          },
          "giftCardRedeemEnabled": true,
          "mitEnabled": false
        }
      }
    }
    */
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCheckoutSettings (self-hosted)
    import { createClient } from '@wix/sdk';
    import { checkoutSettings } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { checkoutSettings },
      // Include the auth strategy and host as relevant
    });

    async function updateCheckoutSettings(checkoutSettings) {
      const response = await myWixClient.checkoutSettings.updateCheckoutSettings(checkoutSettings);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
