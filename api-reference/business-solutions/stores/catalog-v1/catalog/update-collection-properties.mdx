---
title: "Update Collection Properties"
description: "Updates specified properties of a collection."
icon: "pen"
mode: "wide"
---

<span className="wix-method wix-method-patch">PATCH</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates specified properties of a collection.
    To add products to a collection, call the [Add Products to Collection](https://dev.wix.com/api/rest/wix-stores/catalog/collections/add-products-to-collection) endpoint.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    PATCH https://www.wixapis.com/stores/v1/collections/{collection.id}
    ```

    ## Path Params

    <ParamField path="collection.id" type="string" required>

      Collection ID (generated automatically by the catalog).

    </ParamField>

    ## Body Params

    <ParamField body="collection" type="Collection" required>

      Collection info.

      <Expandable title="collection properties">

        <ParamField body="id" type="string" required>

          Collection ID (generated automatically by the catalog).

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Collection name.

        </ParamField>

        <ParamField body="media" type="Media">

          Media items (images, videos etc) associated with this collection. Read only.

          **Read-only.**

          <Expandable title="media properties">

            <ParamField body="mainMedia" type="MediaItem">

              Primary media (image, video etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem` object._

            </ParamField>

            <ParamField body="items" type="MediaItem[]">

              Media (images, videos etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="numberOfProducts" type="integer">

          Number of products in the collection. Read only.

          **Read-only.**

        </ParamField>

        <ParamField body="description" type="string">

          Collection description.

        </ParamField>

        <ParamField body="slug" type="string">

          Collection slug.

        </ParamField>

        <ParamField body="visible" type="boolean">

          Collection visibility. Only impacts dynamic pages, no impact on static pages. Default: `true`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Response Object

    <ResponseField name="collection" type="Collection">

      Updated collection.

      <Expandable title="collection properties">

        <ResponseField name="id" type="string">

          Collection ID (generated automatically by the catalog).

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Collection name.

        </ResponseField>

        <ResponseField name="media" type="Media">

          Media items (images, videos etc) associated with this collection. Read only.

          **Read-only.**

          <Expandable title="media properties">

            <ResponseField name="mainMedia" type="MediaItem">

              Primary media (image, video etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem` object._

            </ResponseField>

            <ResponseField name="items" type="MediaItem[]">

              Media (images, videos etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="numberOfProducts" type="integer">

          Number of products in the collection. Read only.

          **Read-only.**

        </ResponseField>

        <ResponseField name="description" type="string">

          Collection description.

        </ResponseField>

        <ResponseField name="slug" type="string">

          Collection slug.

        </ResponseField>

        <ResponseField name="visible" type="boolean">

          Collection visibility. Only impacts dynamic pages, no impact on static pages. Default: `true`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl -X PATCH \
         'https://www.wixapis.com/stores/v1/collections/81093e7d-a251-4a22-a238-df3aa816f3dc' \
          --data-binary '{
                         	"collection": {
                         		"name": "Updated name"
                         	}
                         }' \
         -H 'Content-Type: application/json' \
         -H 'Authorization: <AUTH>'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "collection": {
          "id": "81093e7d-a251-4a22-a238-df3aa816f3dc",
          "name": "Updated name",
          "media": {
              "items": []
          }
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Updates specified properties of a collection.
    To add products to a collection, call the [Add Products to Collection](https://dev.wix.com/api/rest/wix-stores/catalog/collections/add-products-to-collection) endpoint.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES.MANAGE-PRODUCTS">Manage Products</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { products } from '@wix/stores';

    products.updateCollection(_id, collection): Promise<UpdateCollectionResponse>
    ```

    ## Method Parameters

    <ParamField body="_id" type="string" required>

      Collection ID (generated automatically by the catalog).

      **Read-only.**

    </ParamField>

    <ParamField body="collection" type="Collection" required>

      Collection info to update.

      <Expandable title="collection properties">

        <ParamField body="_id" type="string">

          Collection ID (generated automatically by the catalog).

          **Read-only.**

        </ParamField>

        <ParamField body="name" type="string">

          Collection name.

        </ParamField>

        <ParamField body="media" type="Media">

          Media items (images, videos etc) associated with this collection. Read only.

          **Read-only.**

          <Expandable title="media properties">

            <ParamField body="mainMedia" type="MediaItem">

              Primary media (image, video etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem` object._

            </ParamField>

            <ParamField body="items" type="MediaItem[]">

              Media (images, videos etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem[]` object._

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="numberOfProducts" type="integer">

          Number of products in the collection. Read only.

          **Read-only.**

        </ParamField>

        <ParamField body="description" type="string">

          Collection description.

        </ParamField>

        <ParamField body="slug" type="string">

          Collection slug.

        </ParamField>

        <ParamField body="visible" type="boolean">

          Collection visibility. Only impacts dynamic pages, no impact on static pages. Default: `true`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<UpdateCollectionResponse>`.

    <ResponseField name="collection" type="Collection">

      Updated collection.

      <Expandable title="collection properties">

        <ResponseField name="_id" type="string">

          Collection ID (generated automatically by the catalog).

          **Read-only.**

        </ResponseField>

        <ResponseField name="name" type="string">

          Collection name.

        </ResponseField>

        <ResponseField name="media" type="Media">

          Media items (images, videos etc) associated with this collection. Read only.

          **Read-only.**

          <Expandable title="media properties">

            <ResponseField name="mainMedia" type="MediaItem">

              Primary media (image, video etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem` object._

            </ResponseField>

            <ResponseField name="items" type="MediaItem[]">

              Media (images, videos etc) associated with this product.

              _Nested structure omitted for brevity — see the `MediaItem[]` object._

            </ResponseField>

          </Expandable>

        </ResponseField>

        <ResponseField name="numberOfProducts" type="integer">

          Number of products in the collection. Read only.

          **Read-only.**

        </ResponseField>

        <ResponseField name="description" type="string">

          Collection description.

        </ResponseField>

        <ResponseField name="slug" type="string">

          Collection slug.

        </ResponseField>

        <ResponseField name="visible" type="boolean">

          Collection visibility. Only impacts dynamic pages, no impact on static pages. Default: `true`.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-update-collection-properties" id="ex-sdk-update-collection-properties-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-update-collection-properties" id="ex-sdk-update-collection-properties-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-update-collection-properties-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-update-collection-properties-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Update a collection</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">updateCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-update-collection-properties-0"><span className="wix-picker-name">Update a collection</span></label>
    <label htmlFor="ex-sdk-update-collection-properties-1"><span className="wix-picker-name">updateCollection (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Update a collection
    import { products } from "@wix/stores";

    const collection = {
      _id: "81093e7d-a251-4a22-a238-df3aa816f3dc",
      name: "Updated name"
    };

    async function updateCollection() {
      const response = await products.updateCollection(collection);
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript updateCollection (self-hosted)
    import { createClient } from '@wix/sdk';
    import { products } from '@wix/stores';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { products },
      // Include the auth strategy and host as relevant
    });

    async function updateCollection(_id,collection) {
      const response = await myWixClient.products.updateCollection(_id,collection);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
