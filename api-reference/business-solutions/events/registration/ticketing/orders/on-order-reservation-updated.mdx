---
title: "onOrderReservationUpdated"
description: "The onOrderReservationUpdated event."
icon: "bolt"
tag: "Deprecated"
searchable: false
---

<Warning>

  **Deprecated.** Use `wix.events.v1.ticket_reservation_updated` instead. Scheduled for removal on 2026-03-31.

</Warning>

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.events.ticketing.events.ReservationUpdated` |
| Package | `@wix/events` |
| Permissions | `Read Events - all read permissions`, `Manage Events - all permissions`, `Manage Events`, `Read Basic Events Order Info`, `Manage Orders` |

## Examples

<CodeGroup>

  ```javascript onOrderReservationUpdated
  import { orders } from '@wix/events';

  orders.onOrderReservationUpdated((event) => {
      // handle your event here
  });
  ```

  ```javascript onOrderReservationUpdated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { orders } from '@wix/events';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        orders,
      },
  });

  wixClient.orders.onOrderReservationUpdated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
