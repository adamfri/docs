---
title: "Report Items Back In Stock"
description: "Sends notifications for back in stock requests."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sends notifications for back in stock requests.

    <Note>

    Automations must be turned on in a [Wix user's dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Go%20to%20Back-in-Stock&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https://www.wix.com/dashboard/&#123;&#123;metaSiteId&#125;&#125;/store/back-in-stock) for notifications to send.

    </Note>

    This endpoint triggers notifications for requests in 1 of 2 ways:
    1. For a specific item, with the `catalogReference` information.
    2. For specific requests, with `requestIds`.

    `itemDetails` are required and may populate dynamic values in the notification template, as follows:
    + `itemDetails.name` passes to the template as `item.name`
    + `itemDetails.price` passes to the template as `item.price`
    + `itemDetails.image.url` passes to the template as `item.image.url`

    If the notification template doesn't include `item.price`, `item.name`, or `item.image.url`, values should
    be passed in `extraAutomationTemplateParameters`.

    After this endpoint is called, the `status` for the request will update to `NOTIFICATION_SENT` if it sends
    successfully, or to `FAILED` if it fails to send.

    <Note>

      **Authentication**

      You can only call this method when authenticated as a Wix app or Wix user [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES-MEGA.MANAGE-STORES">Manage Stores - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/back-in-stock-service/v1/back-in-stock-notification-requests/report-items-back-in-stock
    ```

    ## Body Params

    <ParamField body="catalogReference" type="CatalogReference">

      `catalogReference` item to send notifications for.

      Cannot be used with `requestIds`.

      <Expandable title="catalogReference properties">

        <ParamField body="catalogItemId" type="string">

          ID of the item within the catalog it belongs to.

          Constraints: `minLength 1`, `maxLength 36`.

        </ParamField>

        <ParamField body="appId" type="string">

          ID of the app providing the catalog.

          You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

          For items from Wix catalogs, the following values always apply:
          + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
          + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
          + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

          Constraints: `minLength 1`.

        </ParamField>

        <ParamField body="options" type="object">

          Additional item details in `key:value` pairs.

          Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

          For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
          or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="requestIds" type="string[]">

      IDs of requests to send notifications for.

      Cannot be used with `catalogReference`.

    </ParamField>

    <ParamField body="itemDetails" type="BackInStockItemDetails" required>

      Item details to use in notifications.

      `itemDetails` may populate dynamic values in the notification template, as follows:
      + `itemDetails.name` passes to the template as `item.name`
      + `itemDetails.price` passes to the template as `item.price`
      + `itemDetails.image.url` passes to the template as `item.image.url`

      Use `extraAutomationTemplateParameters` to pass additional dynamic values.

      <Expandable title="itemDetails properties">

        <ParamField body="name" type="string" required>

          Item name.

        </ParamField>

        <ParamField body="price" type="string" required>

          Item price.

          Constraints: `>= 0`.

        </ParamField>

        <ParamField body="image" type="Image">

          Item image.

          <Expandable title="image properties">

            <ParamField body="id" type="string">

              WixMedia image ID.

            </ParamField>

            <ParamField body="url" type="string">

              Image URL.

            </ParamField>

            <ParamField body="height" type="integer">

              Original image height.

              **Read-only.**

            </ParamField>

            <ParamField body="width" type="integer">

              Original image width.

              **Read-only.**

            </ParamField>

            <ParamField body="altText" type="string">

              Image alt text.

            </ParamField>

            <ParamField body="filename" type="string">

              Image filename.

              **Read-only.**

            </ParamField>

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="extraAutomationTemplateParameters" type="object">

      Additional key-value pairs to pass to the back in stock notification template.

      Constraints: `format map`.

      <Expandable title="extraAutomationTemplateParameters properties">

        <ParamField body="key" type="string" />

        <ParamField body="value" type="string" />

      </Expandable>

    </ParamField>

    ## Response Object

    Returns `ReportItemsBackInStockResponse`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `REQUEST_CONTAINS_BOTH_CATALOG_REFERENCE_AND_REQUEST_IDS` | Request can't contain both `catalogReference` and `requestIds`. Use one or the other. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_ITEM_IMAGE_URL` | The `itemDetails.image.url` field contains an invalid URL. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-report-items-back-in-stock" id="ex-rest-report-items-back-in-stock-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-report-items-back-in-stock" id="ex-rest-report-items-back-in-stock-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-report-items-back-in-stock-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-report-items-back-in-stock-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Report items back in stock by request IDs</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Report items back in stock by catalog reference</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-report-items-back-in-stock-0"><span className="wix-picker-name">Report items back in stock by request IDs</span></label>
    <label htmlFor="ex-rest-report-items-back-in-stock-1"><span className="wix-picker-name">Report items back in stock by catalog reference</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/back-in-stock-service/v1/back-in-stock-notification-requests/report-items-back-in-stock' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "requestIds": [
            "40d0ffe7-b8fa-40c8-adf4-f3b3760b4b8b"
          ],
          "itemDetails": {
            "name": "No. 06 Sumatra Gayo",
            "price": "19.00",
            "image": {
              "url": "https://static.wixstatic.com/media/8ed784_9ce8256e2d27495bb4c59813ef63604c~mv2.png",
              "id": "8ed784_9ce8256e2d27495bb4c59813ef63604c~mv2.png"
            }
          },
          "extraAutomationTemplateParameters": {
            "checkoutUrl": "https://www.wixsite.com/ecom-s1/product-page/no-06-sumatra-gayo"
          }
        }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST \
        'https://www.wixapis.com/back-in-stock-service/v1/back-in-stock-notification-requests/report-items-back-in-stock' \
        -H 'Content-type: application/json' \
        -H 'Authorization: <AUTH>' \
        -d '{
          "catalogReference": {
            "catalogItemId": "11ce8c82-1936-4219-8678-4eb009d006dc",
            "appId": "215238eb-22a5-4c36-9e7b-e7c08025e04e",
            "options": {
              "variantId": "786b8f4e-b0d1-4851-a9cf-ef38ece5d72e"
            }
          },
          "itemDetails": {
            "name": "No. 07 Sidamo Natural",
            "price": "22.00",
            "image": {
              "url": "https://static.wixstatic.com/media/8ed784_63d2210db7d24cad85cec2f592dc0ee2~mv2.png",
              "id": "8ed784_63d2210db7d24cad85cec2f592dc0ee2~mv2.png"
            }
          },
          "extraAutomationTemplateParameters": {
            "checkoutUrl": "https://www.wixsite.com/ecom-s1/product-page/no-07-sidamo-natural"
          }
        }'
      ```

      ```json Response
      {}
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Sends notifications for back in stock requests.

    <Note>

    Automations must be turned on in a [Wix user's dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Go%20to%20Back-in-Stock&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https://www.wix.com/dashboard/&#123;&#123;metaSiteId&#125;&#125;/store/back-in-stock) for notifications to send.

    </Note>

    This endpoint triggers notifications for requests in 1 of 2 ways:
    1. For a specific item, with the `catalogReference` information.
    2. For specific requests, with `requestIds`.

    `itemDetails` are required and may populate dynamic values in the notification template, as follows:
    + `itemDetails.name` passes to the template as `item.name`
    + `itemDetails.price` passes to the template as `item.price`
    + `itemDetails.image.url` passes to the template as `item.image.url`

    If the notification template doesn't include `item.price`, `item.name`, or `item.image.url`, values should
    be passed in `extraAutomationTemplateParameters`.

    After this endpoint is called, the `status` for the request will update to `NOTIFICATION_SENT` if it sends
    successfully, or to `FAILED` if it fails to send.

    <Note>

      **Authentication**

      This method may require higher-level permissions than the caller has, based on their [identity](https://dev.wix.com/docs/api-reference/articles/authentication/about-identities) and [roles and permissions](https://support.wix.com/en/article/roles-permissions-overview). In these cases, you can ensure the method is called with the required level of authorization by using one of the following:

      - In Wix sites and Wix apps, use [elevated permissions](https://dev.wix.com/docs/api-reference/articles/authentication/about-elevated-permissions).
      - In Wix Headless projects, use [API key authentication](https://dev.wix.com/docs/go-headless/develop-your-project/admin-operations/about-admin-operations).

    </Note>

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-STORES-MEGA.MANAGE-STORES">Manage Stores - all permissions</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`.

    ## Method Declaration

    ```typescript
    import { backInStockNotifications } from '@wix/ecom';

    backInStockNotifications.reportItemsBackInStock(itemDetails, options): Promise<void>
    ```

    ## Method Parameters

    <ParamField body="itemDetails" type="BackInStockItemDetails" required>

      Item details to use in notifications.

      `itemDetails` may populate dynamic values in the notification template, as follows:
      + `itemDetails.name` passes to the template as `item.name`
      + `itemDetails.price` passes to the template as `item.price`
      + `itemDetails.image.url` passes to the template as `item.image.url`

      Use `extraAutomationTemplateParameters` to pass additional dynamic values.

      <Expandable title="itemDetails properties">

        <ParamField body="name" type="string" required>

          Item name.

        </ParamField>

        <ParamField body="price" type="string" required>

          Item price.

          Constraints: `>= 0`.

        </ParamField>

        <ParamField body="image" type="string">

          Item image.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="options" type="ReportItemsBackInStockOptions">

      Report options.

      <Expandable title="options properties">

        <ParamField body="catalogReference" type="CatalogReference">

          `catalogReference` item to send notifications for.

          Cannot be used with `requestIds`.

          <Expandable title="catalogReference properties">

            <ParamField body="catalogItemId" type="string">

              ID of the item within the catalog it belongs to.

              Constraints: `minLength 1`, `maxLength 36`.

            </ParamField>

            <ParamField body="appId" type="string">

              ID of the app providing the catalog.

              You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).

              For items from Wix catalogs, the following values always apply:
              + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
              + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
              + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`

              Constraints: `minLength 1`.

            </ParamField>

            <ParamField body="options" type="object">

              Additional item details in `key:value` pairs.

              Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.

              For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
              or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="requestIds" type="string[]">

          IDs of requests to send notifications for.

          Cannot be used with `catalogReference`.

        </ParamField>

        <ParamField body="extraAutomationTemplateParameters" type="object">

          Additional key-value pairs to pass to the back in stock notification template.

          Constraints: `format map`.

          <Expandable title="extraAutomationTemplateParameters properties">

            <ParamField body="key" type="string" />

            <ParamField body="value" type="string" />

          </Expandable>

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<void>`.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `REQUEST_CONTAINS_BOTH_CATALOG_REFERENCE_AND_REQUEST_IDS` | Request can't contain both `catalogReference` and `requestIds`. Use one or the other. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_ITEM_IMAGE_URL` | The `itemDetails.image.url` field contains an invalid URL. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-report-items-back-in-stock" id="ex-sdk-report-items-back-in-stock-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-report-items-back-in-stock" id="ex-sdk-report-items-back-in-stock-1" className="wix-picker-radio" />
    <input type="radio" name="ex-sdk-report-items-back-in-stock" id="ex-sdk-report-items-back-in-stock-2" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-report-items-back-in-stock-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-report-items-back-in-stock-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">reportItemsBackInStock</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">reportItemsBackInStock (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">reportItemsBackInStock (with elevated permissions)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-report-items-back-in-stock-0"><span className="wix-picker-name">reportItemsBackInStock</span></label>
    <label htmlFor="ex-sdk-report-items-back-in-stock-1"><span className="wix-picker-name">reportItemsBackInStock (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>
    <label htmlFor="ex-sdk-report-items-back-in-stock-2"><span className="wix-picker-name">reportItemsBackInStock (with elevated permissions)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript reportItemsBackInStock
    import { backInStockNotifications } from '@wix/ecom';

    async function reportItemsBackInStock(itemDetails,options) {
      const response = await backInStockNotifications.reportItemsBackInStock(itemDetails,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript reportItemsBackInStock (self-hosted)
    import { createClient } from '@wix/sdk';
    import { backInStockNotifications } from '@wix/ecom';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { backInStockNotifications },
      // Include the auth strategy and host as relevant
    });

    async function reportItemsBackInStock(itemDetails,options) {
      const response = await myWixClient.backInStockNotifications.reportItemsBackInStock(itemDetails,options);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript reportItemsBackInStock (with elevated permissions)
    import { backInStockNotifications } from '@wix/ecom';
    import { auth } from '@wix/essentials';

    async function myReportItemsBackInStockMethod(itemDetails,options) {
      const elevatedReportItemsBackInStock = auth.elevate(backInStockNotifications.reportItemsBackInStock);
      const response = await elevatedReportItemsBackInStock(itemDetails,options);
    }
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
