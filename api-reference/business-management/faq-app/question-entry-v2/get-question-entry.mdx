---
title: "Get Question Entry"
description: "Retrieves a single question entry by ID."
icon: "download"
mode: "wide"
---

<span className="wix-method wix-method-get">GET</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a single question entry by ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LABS.READ-FAQ">Read FAQ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    GET https://www.wixapis.com/faq/v2/question-entries/{questionEntryId}
    ```

    ## Path Params

    <ParamField path="questionEntryId" type="string" required>

      ID of the question entry to retrieve.

    </ParamField>

    ## Response Object

    <ResponseField name="questionEntry" type="QuestionEntry">

      Retrieved question entry.

      See the [Question Entry Object](/api-reference/business-management/faq-app/question-entry-v2/question-entry-object) for this object's fields.

    </ResponseField>

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <CodeGroup>

      ```bash Request
      ~~~cURL
      curl --request GET \
        --url https://www.wixapis.com/faq/v2/question-entries/a1b2c3d4-e5f6-7890-abcd-ef1234567890 \
        --header 'Authorization: <AUTH>' \
        --header 'Content-Type: application/json'
      ~~~
      ```

      ```json Response
      ~~~json
      {
        "questionEntry": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "revision": "2",
          "createdDate": "2024-12-06T14:30:00.123Z",
          "updatedDate": "2024-12-06T15:15:30.456Z",
          "question": "How long does shipping take?",
          "draftjs": "{\"blocks\":[{\"key\":\"abc123\",\"text\":\"Standard shipping takes 3-5 business days. Express shipping is available and takes 1-2 business days. International shipping may take 7-14 business days depending on the destination.\",\"type\":\"unstyled\",\"depth\":0,\"inlineStyleRanges\":[],\"entityRanges\":[],\"data\":{}}],\"entityMap\":{}}",
          "labels": [
            {
              "title": "customer-service",
              "sortOrder": 1.0
            },
            {
              "title": "shipping",
              "sortOrder": 2.0
            }
          ],
          "sortOrder": 1.0,
          "categoryId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "slug": "how-long-does-shipping-take",
          "shareLink": "https://example.wixsite.com/faq/how-long-does-shipping-take"
        }
      }
      ~~~
      ```

    </CodeGroup>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Retrieves a single question entry by ID.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-LABS.READ-FAQ">Read FAQ</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { questionEntry } from '@wix/faq';

    questionEntry.getQuestionEntry(questionEntryId): Promise<QuestionEntry>
    ```

    ## Method Parameters

    <ParamField body="questionEntryId" type="string" required>

      ID of the question entry to retrieve.

      Constraints: `format GUID`.

    </ParamField>

    ## Returns

    Returns `Promise<QuestionEntry>`.

    See the [Question Entry Object](/api-reference/business-management/faq-app/question-entry-v2/question-entry-object) for this object's fields.

    ## Errors

    <Note>

      This method doesn't return any custom errors, but may return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-get-question-entry" id="ex-sdk-get-question-entry-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-get-question-entry" id="ex-sdk-get-question-entry-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-get-question-entry-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-get-question-entry-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">getQuestionEntry</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">getQuestionEntry (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-get-question-entry-0"><span className="wix-picker-name">getQuestionEntry</span></label>
    <label htmlFor="ex-sdk-get-question-entry-1"><span className="wix-picker-name">getQuestionEntry (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript getQuestionEntry
    import { questionEntry } from '@wix/faq';

    async function getQuestionEntry(questionEntryId) {
      const response = await questionEntry.getQuestionEntry(questionEntryId);
    };
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript getQuestionEntry (self-hosted)
    import { createClient } from '@wix/sdk';
    import { questionEntry } from '@wix/faq';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { questionEntry },
      // Include the auth strategy and host as relevant
    });

    async function getQuestionEntry(questionEntryId) {
      const response = await myWixClient.questionEntry.getQuestionEntry(questionEntryId);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
