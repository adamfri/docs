---
title: "onProjectCreated"
description: "Triggered when a project is created."
icon: "bolt"
---

Triggered when a project is created.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.portfolio.projects.v1.project_created` |
| Package | `@wix/portfolio` |
| Permissions | `Manage Portfolio` |

## Examples

<CodeGroup>

  ```javascript onProjectCreated
  import { projects } from '@wix/portfolio';

  projects.onProjectCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onProjectCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { projects } from '@wix/portfolio';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        projects,
      },
  });

  wixClient.projects.onProjectCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
