---
title: "onOperationDeleted"
description: "Triggered when an operation is deleted."
icon: "bolt"
---

Triggered when an operation is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.operations.v1.operation_deleted` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onOperationDeleted
  import { operations } from '@wix/restaurants';

  operations.onOperationDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onOperationDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { operations } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        operations,
      },
  });

  wixClient.operations.onOperationDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
