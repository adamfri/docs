---
title: "Create Rsvp"
description: "Creates an RSVP."
icon: "upload"
mode: "wide"
---

<span className="wix-method wix-method-post">POST</span>

<Tabs>

  <Tab title="REST">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates an RSVP.

    The event's `registration.initialType` must be `RSVP`. Events created with `TICKETING` registration can't accept RSVPs, and because `initialType` is immutable, changing `registration.type` later doesn't make them eligible.

    To create an RSVP with multiple guests, specify the `additionalGuestDetails.guestCount` and `additionalGuestDetails.guestNames` fields.

    Before creating an RSVP, retrieve the event's registration form by calling
    [Get Event](https://dev.wix.com/docs/rest/business-solutions/events/events-v3/get-event) with `fields: ["FORM"]`.
    Build `form.inputValues` from `event.form.controls[].inputs`, using each input's name, mandatory flag, and predefined options.
    Unrecognized input names, missing mandatory values for `YES` responses, and values outside predefined options cause an `INVALID_FORM_RESPONSE` error.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Endpoint

    ```http
    POST https://www.wixapis.com/events/v2/rsvps
    ```

    ## Body Params

    <ParamField body="rsvp" type="Rsvp" required>

      RSVP info.

      See the [Rsvp Object](/api-reference/business-solutions/events/registration/rsvp-v2/rsvp-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="ModificationOptions">

      RSVP options. &lt;br> The **WIX_EVENTS.MANAGE_RSVP** permission is required.

      <Expandable title="options properties">

        <ParamField body="ignoreLimits" type="boolean">

          Whether to create or update an RSVP regardless of the event's guest limit.

        </ParamField>

        <ParamField body="ignoreFormValidation" type="boolean">

          Whether to ignore the form validation.

        </ParamField>

        <ParamField body="ignoreResponseStatus" type="boolean">

          Whether to accept the RSVP `status` even when the event's registration doesn't allow it, for example when registration is closed or hasn't started, the waitlist is unavailable, or the event only accepts `YES` responses.

        </ParamField>

        <ParamField body="ignoreAdditionalGuestNames" type="boolean">

          Whether to ignore the additional guest names validation.

        </ParamField>

      </Expandable>

    </ParamField>

    <ParamField body="fields" type="string[]">

      Predefined sets of fields to return.

      Constraints: `maxItems 3`.

      **Supported values:**

      - `DETAILS` — Returns `createdDate`, `updatedDate`, `firstName`, `lastName`, `totalGuests`, `guests`.
      - `FORM` — Returns `form`.
      - `CONTACT_DETAILS` — Returns `email`.

    </ParamField>

    ## Response Object

    <ResponseField name="rsvp" type="Rsvp">

      Created RSVP.

      See the [Rsvp Object](/api-reference/business-solutions/events/registration/rsvp-v2/rsvp-object) for this object's fields.

    </ResponseField>

    <ResponseField name="calendarLinks" type="CalendarLinks">

      "Add to calendar" links.

      <Expandable title="calendarLinks properties">

        <ResponseField name="google" type="string">

          "Add to Google calendar" URL.

        </ResponseField>

        <ResponseField name="ics" type="string">

          "Download ICS calendar file" URL.

        </ResponseField>

      </Expandable>

    </ResponseField>

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `EVENT_NOT_FOUND` | Couldn't find the event. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_FORM_RESPONSE` | The submitted form response doesn't match the event's registration form requirements. Call [Get Event](https://dev.wix.com/docs/rest/business-solutions/events/events-v3/get-event) with `fields: ["FORM"]` to inspect the expected input names, mandatory fields, and predefined options. |
    | `400` | `INVALID_ARGUMENT` | `UNEXPECTED_RSVP_STATUS` | RSVP status isn't valid for this operation. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_EVENT_TYPE` | Event type doesn't support RSVPs. The event's `registration.initialType` isn't `RSVP`. |
    | `428` | `FAILED_PRECONDITION` | `MEMBER_ALREADY_REGISTERED` | Member already has an RSVP for this event. |
    | `428` | `FAILED_PRECONDITION` | `RSVP_LIMIT_EXCEEDED` | Event has reached the maximum number of RSVPs allowed per event. |
    | `428` | `FAILED_PRECONDITION` | `GUEST_LIMIT_EXCEEDED` | Number of guests exceeds the event's guest limit. |
    | `428` | `FAILED_PRECONDITION` | `RSVPS_CLOSED` | RSVPs for this event are closed. |
    | `428` | `FAILED_PRECONDITION` | `RSVPS_NOT_STARTED` | RSVPs for this event haven't started yet. |
    | `428` | `FAILED_PRECONDITION` | `WAITING_LIST_UNAVAILABLE` | Waitlist isn't available for this event. |
    | `428` | `FAILED_PRECONDITION` | `MEMBER_EMAIL_ALREADY_REGISTERED` | There is already an RSVP with this email address for this event. |
    | `400` | `INVALID_ARGUMENT` | `ADDITIONAL_GUESTS_MUST_HAVE_NAMES` | Additional guests must have names when `guestCount` is greater than 0. |
    | `428` | `FAILED_PRECONDITION` | `FORM_SUBMISSIONS_NOT_SUPPORTED` | The event has no registration form, so the submitted `formSubmission` can't be validated. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/rest/articles/getting-started/errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Created | `wix.events.v2.rsvp_created` | Triggered when an RSVP is created. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-rest-create-rsvp" id="ex-rest-create-rsvp-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-rest-create-rsvp" id="ex-rest-create-rsvp-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-rest-create-rsvp-open" className="wix-picker-toggle" />

    <label htmlFor="ex-rest-create-rsvp-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create RSVP with multiple guests</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">Create RSVP with 1 guest</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-rest-create-rsvp-0"><span className="wix-picker-name">Create RSVP with multiple guests</span></label>
    <label htmlFor="ex-rest-create-rsvp-1"><span className="wix-picker-name">Create RSVP with 1 guest</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/rsvps' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "rsvp": {
          "eventId": "32c0eab1-b7a0-4ec2-9fb6-db76f76ee488",
          "firstName": "John",
          "lastName": "Doe",
          "email": "johndoe@mail.com",
          "form": {
            "inputValues": [
              {
                "inputName": "firstName",
                "value": "John"
              },
              {
                "inputName": "lastName",
                "value": "Doe"
              },
              {
                "inputName": "email",
                "value": "johndoe@mail.com"
              },
              {
                "inputName": "additionalGuests",
                "value": "2"
              },
              {
                "inputName": "guestNames",
                "values": [
                  "Patsy McBride",
                  "Randy Ramirez"
                ]
              }
            ]
          },
          "status": "YES"
        }
      }'
      ```

      ```json Response
      {
        "rsvp": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "revision": "1",
          "eventId": "32c0eab1-b7a0-4ec2-9fb6-db76f76ee488",
          "createdDate": "2024-03-15T10:00:00.000Z",
          "updatedDate": "2024-03-15T10:00:00.000Z",
          "contactId": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
          "firstName": "John",
          "lastName": "Doe",
          "email": "johndoe@mail.com",
          "form": {
            "inputValues": [
              {
                "inputName": "firstName",
                "value": "John"
              },
              {
                "inputName": "lastName",
                "value": "Doe"
              },
              {
                "inputName": "email",
                "value": "johndoe@mail.com"
              },
              {
                "inputName": "additionalGuests",
                "value": "2"
              },
              {
                "inputName": "guestNames",
                "values": [
                  "Patsy McBride",
                  "Randy Ramirez"
                ]
              }
            ]
          },
          "status": "YES",
          "totalGuests": 3,
          "language": "en",
          "fullyCheckedIn": false,
          "checkedInGuestCount": 0
        },
        "calendarLinks": {
          "google": "https://calendar.google.com/calendar/render?action=TEMPLATE&text=Event&dates=20240401T180000Z/20240401T200000Z",
          "ics": "https://www.wixapis.com/events/v2/calendar/ics/32c0eab1-b7a0-4ec2-9fb6-db76f76ee488"
        }
      }
      ```

    </CodeGroup>

    </div>

    <div className="wix-picker-panel">

    <CodeGroup>

      ```bash Request
      curl -X POST 'https://www.wixapis.com/events/v2/rsvps' \
      -H 'Content-Type: application/json' \
      -H 'Authorization: <AUTH TOKEN>' \
      -d '{
        "rsvp": {
          "eventId": "32c0eab1-b7a0-4ec2-9fb6-db76f76ee488",
          "firstName": "John",
          "lastName": "Doe",
          "email": "johndoe@mail.com",
          "form": {
            "inputValues": [
              {
                "inputName": "firstName",
                "value": "John"
              },
              {
                "inputName": "lastName",
                "value": "Doe"
              },
              {
                "inputName": "email",
                "value": "johndoe@mail.com"
              }
            ]
          },
          "status": "YES"
        }
      }'
      ```

      ```json Response
      {
        "rsvp": {
          "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
          "revision": "1",
          "eventId": "32c0eab1-b7a0-4ec2-9fb6-db76f76ee488",
          "createdDate": "2024-03-15T10:00:00.000Z",
          "updatedDate": "2024-03-15T10:00:00.000Z",
          "contactId": "c9d8e7f6-a5b4-3210-fedc-ba9876543210",
          "firstName": "John",
          "lastName": "Doe",
          "email": "johndoe@mail.com",
          "form": {
            "inputValues": [
              {
                "inputName": "firstName",
                "value": "John"
              },
              {
                "inputName": "lastName",
                "value": "Doe"
              },
              {
                "inputName": "email",
                "value": "johndoe@mail.com"
              }
            ]
          },
          "status": "YES",
          "totalGuests": 1
        },
        "calendarLinks": {
          "google": "https://calendar.google.com/calendar/render?action=TEMPLATE&text=My+Event&dates=20240415T180000Z/20240415T200000Z",
          "ics": "https://www.wixapis.com/events/v1/calendar/ics/32c0eab1-b7a0-4ec2-9fb6-db76f76ee488"
        }
      }
      ```

    </CodeGroup>

    </div>

    </div>

    </div>

    </div>

    </div>

  </Tab>

  <Tab title="SDK">

    <div className="wix-api-columns">

    <div className="wix-ref">

    Creates an RSVP.

    The event's `registration.initialType` must be `RSVP`. Events created with `TICKETING` registration can't accept RSVPs, and because `initialType` is immutable, changing `registration.type` later doesn't make them eligible.

    To create an RSVP with multiple guests, specify the `additionalGuestDetails.guestCount` and `additionalGuestDetails.guestNames` fields.

    Before creating an RSVP, retrieve the event's registration form by calling
    [Get Event](https://dev.wix.com/docs/rest/business-solutions/events/events-v3/get-event) with `fields: ["FORM"]`.
    Build `form.inputValues` from `event.form.controls[].inputs`, using each input's name, mandatory flag, and predefined options.
    Unrecognized input names, missing mandatory values for `YES` responses, and values outside predefined options cause an `INVALID_FORM_RESPONSE` error.

    ## Permissions

    This method requires one of the following permission scopes:

    <div className="wix-scopes">

    <span className="wix-scope" title="SCOPE.DC-EVENTS.MANAGE-GUEST-LIST">Manage Guest List</span>

    </div>

    Learn more about [app permissions](https://dev.wix.com/docs/build-apps/develop-your-app/access/authorization/about-permissions).

    Callable by: `APP`, `MEMBER`, `VISITOR`.

    ## Method Declaration

    ```typescript
    import { rsvpV2 } from '@wix/events';

    rsvpV2.createRsvp(rsvp, options): Promise<Rsvp>
    ```

    ## Method Parameters

    <ParamField body="rsvp" type="Rsvp" required>

      RSVP info.

      See the [Rsvp Object](/api-reference/business-solutions/events/registration/rsvp-v2/rsvp-object) for this object's fields.

    </ParamField>

    <ParamField body="options" type="CreateRsvpOptions">

      options

      <Expandable title="options properties">

        <ParamField body="options" type="ModificationOptions">

          RSVP options. &lt;br> The **WIX_EVENTS.MANAGE_RSVP** permission is required.

          <Expandable title="options properties">

            <ParamField body="ignoreLimits" type="boolean">

              Whether to create or update an RSVP regardless of the event's guest limit.

            </ParamField>

            <ParamField body="ignoreFormValidation" type="boolean">

              Whether to ignore the form validation.

            </ParamField>

            <ParamField body="ignoreResponseStatus" type="boolean">

              Whether to accept the RSVP `status` even when the event's registration doesn't allow it, for example when registration is closed or hasn't started, the waitlist is unavailable, or the event only accepts `YES` responses.

            </ParamField>

            <ParamField body="ignoreAdditionalGuestNames" type="boolean">

              Whether to ignore the additional guest names validation.

            </ParamField>

          </Expandable>

        </ParamField>

        <ParamField body="fields" type="string[]">

          Predefined sets of fields to return.

          Constraints: `maxItems 3`.

          **Supported values:**

          - `DETAILS` — Returns `createdDate`, `updatedDate`, `firstName`, `lastName`, `totalGuests`, `guests`.
          - `FORM` — Returns `form`.
          - `CONTACT_DETAILS` — Returns `email`.

        </ParamField>

      </Expandable>

    </ParamField>

    ## Returns

    Returns `Promise<Rsvp>`.

    See the [Rsvp Object](/api-reference/business-solutions/events/registration/rsvp-v2/rsvp-object) for this object's fields.

    ## Errors

    | Status | Code | Application code | Description |
    | --- | --- | --- | --- |
    | `400` | `INVALID_ARGUMENT` | `EVENT_NOT_FOUND` | Couldn't find the event. |
    | `400` | `INVALID_ARGUMENT` | `INVALID_FORM_RESPONSE` | The submitted form response doesn't match the event's registration form requirements. Call [Get Event](https://dev.wix.com/docs/rest/business-solutions/events/events-v3/get-event) with `fields: ["FORM"]` to inspect the expected input names, mandatory fields, and predefined options. |
    | `400` | `INVALID_ARGUMENT` | `UNEXPECTED_RSVP_STATUS` | RSVP status isn't valid for this operation. |
    | `428` | `FAILED_PRECONDITION` | `INVALID_EVENT_TYPE` | Event type doesn't support RSVPs. The event's `registration.initialType` isn't `RSVP`. |
    | `428` | `FAILED_PRECONDITION` | `MEMBER_ALREADY_REGISTERED` | Member already has an RSVP for this event. |
    | `428` | `FAILED_PRECONDITION` | `RSVP_LIMIT_EXCEEDED` | Event has reached the maximum number of RSVPs allowed per event. |
    | `428` | `FAILED_PRECONDITION` | `GUEST_LIMIT_EXCEEDED` | Number of guests exceeds the event's guest limit. |
    | `428` | `FAILED_PRECONDITION` | `RSVPS_CLOSED` | RSVPs for this event are closed. |
    | `428` | `FAILED_PRECONDITION` | `RSVPS_NOT_STARTED` | RSVPs for this event haven't started yet. |
    | `428` | `FAILED_PRECONDITION` | `WAITING_LIST_UNAVAILABLE` | Waitlist isn't available for this event. |
    | `428` | `FAILED_PRECONDITION` | `MEMBER_EMAIL_ALREADY_REGISTERED` | There is already an RSVP with this email address for this event. |
    | `400` | `INVALID_ARGUMENT` | `ADDITIONAL_GUESTS_MUST_HAVE_NAMES` | Additional guests must have names when `guestCount` is greater than 0. |
    | `428` | `FAILED_PRECONDITION` | `FORM_SUBMISSIONS_NOT_SUPPORTED` | The event has no registration form, so the submitted `formSubmission` can't be validated. |

    <Note>

      This method may also return standard errors. [Learn more about standard Wix errors](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-errors).

    </Note>

    ## Event Triggers

    This method triggers the following events:

    | Event | Type | Description |
    | --- | --- | --- |
    | Rsvp Created | `wix.events.v2.rsvp_created` | Triggered when an RSVP is created. |
    | Rsvp Rsvps Balance Updated | `wix.events.v2.rsvp_rsvps_balance_updated` | Triggered when an RSVP balance is updated. |

    </div>

    <div className="wix-examples">

    ## Examples

    <div className="wix-picker">

    <input type="radio" name="ex-sdk-create-rsvp" id="ex-sdk-create-rsvp-0" className="wix-picker-radio" defaultChecked />
    <input type="radio" name="ex-sdk-create-rsvp" id="ex-sdk-create-rsvp-1" className="wix-picker-radio" />

    <input type="checkbox" id="ex-sdk-create-rsvp-open" className="wix-picker-toggle" />

    <label htmlFor="ex-sdk-create-rsvp-open" className="wix-picker-button">

    <span className="wix-picker-current"><span className="wix-picker-name">Create an RSVP for an event</span></span>
    <span className="wix-picker-current"><span className="wix-picker-name">createRsvp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></span>

    </label>

    <div className="wix-picker-options">

    <label htmlFor="ex-sdk-create-rsvp-0"><span className="wix-picker-name">Create an RSVP for an event</span></label>
    <label htmlFor="ex-sdk-create-rsvp-1"><span className="wix-picker-name">createRsvp (self-hosted)</span><span className="wix-picker-desc">Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client)</span></label>

    </div>

    <div className="wix-picker-panels">

    <div className="wix-picker-panel">

    ```javascript Create an RSVP for an event
    import { rsvpV2 } from "@wix/events";

    const rsvp = {
      eventId: "201037a0-3ecc-4ee4-976b-a1c931c42a6b",
      firstName: "Jane",
      lastName: "Doe",
      email: "jane.doe@example.com",
      form: {
        inputValues: [
          { inputName: "firstName", value: "Jane" },
          { inputName: "lastName", value: "Doe" },
          { inputName: "email", value: "jane.doe@example.com" },
        ],
      },
      status: "YES" as const,
    };

    async function createRsvp() {
      const response = await rsvpV2.createRsvp(rsvp);
      return response;
    }
    ```

    </div>

    <div className="wix-picker-panel">

    ```javascript createRsvp (self-hosted)
    import { createClient } from '@wix/sdk';
    import { rsvpV2 } from '@wix/events';
    // Import the auth strategy for the relevant access type
    // Import the relevant host module if needed

    const myWixClient = createClient ({
      modules: { rsvpV2 },
      // Include the auth strategy and host as relevant
    });

    async function createRsvp(rsvp,options) {
      const response = await myWixClient.rsvpV2.createRsvp(rsvp,options);
    };
    ```

    </div>

    </div>

    </div>

    <Note>

      Self-hosted SDK calls require you to [create a client](https://dev.wix.com/docs/sdk/articles/work-with-the-sdk/about-the-wix-client).

    </Note>

    </div>

    </div>

  </Tab>

</Tabs>
