---
title: "onLocaleSettingsDeleted"
description: "Triggered when the site is deleted."
icon: "bolt"
---

Triggered when the site is deleted.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.multilingual.settings.v2.locale_settings_deleted` |
| Package | `@wix/multilingual` |
| Permissions | `Wix Multilingual - Translation API` |

## Examples

<CodeGroup>

  ```javascript onLocaleSettingsDeleted
  import { localeSettings } from '@wix/multilingual';

  localeSettings.onLocaleSettingsDeleted((event) => {
      // handle your event here
  });
  ```

  ```javascript onLocaleSettingsDeleted (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { localeSettings } from '@wix/multilingual';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        localeSettings,
      },
  });

  wixClient.localeSettings.onLocaleSettingsDeleted((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
