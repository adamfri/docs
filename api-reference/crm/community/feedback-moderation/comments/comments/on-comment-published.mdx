---
title: "onCommentPublished"
description: "Triggered when a comment is published."
icon: "bolt"
---

Triggered when a comment is published.

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.comments.v2.comment_comment_published` |
| Package | `@wix/comments` |
| Permissions | `Read Comments`, `Manage Comments` |

## Examples

<CodeGroup>

  ```javascript onCommentPublished
  import { comments } from '@wix/comments';

  comments.onCommentPublished((event) => {
      // handle your event here
  });
  ```

  ```javascript onCommentPublished (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { comments } from '@wix/comments';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
     }),
     modules: {
        comments,
      },
  });

  wixClient.comments.onCommentPublished((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
