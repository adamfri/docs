---
title: "onRecipientCreated"
description: "The onRecipientCreated event."
icon: "bolt"
---

## Event details

| Property | Value |
| --- | --- |
| Event type | `wix.restaurants.recipient.v1.recipient_created` |
| Package | `@wix/restaurants` |
| Permissions | `Manage Restaurants - all permissions` |

## Examples

<CodeGroup>

  ```javascript onRecipientCreated
  import { recipients } from '@wix/restaurants';

  recipients.onRecipientCreated((event) => {
      // handle your event here
  });
  ```

  ```javascript onRecipientCreated (self-hosted)
  import { createClient, AppStrategy } from '@wix/sdk';
  import { recipients } from '@wix/restaurants';

  const wixClient = createClient({
    auth: AppStrategy({
      appId: 'MY-APP-ID',
      publicKey: 'YOUR_APP_PUBLIC_KEY',
    }),
    modules: {
        recipients,
      },
  });

  wixClient.recipients.onRecipientCreated((event) => {
      // handle your event here
  });
  ```

</CodeGroup>
